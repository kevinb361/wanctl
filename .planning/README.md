# Local planning records

This project uses Saga records locally. Operational planning, evidence, audit artifacts, and deployment-specific context are intentionally excluded from the public repository.

Only this boundary notice is public. Public-safe project behavior and usage belong in `README.md`, `CLAUDE.md`, and `docs/`; deployment configuration belongs in ignored local files derived from `configs/examples/`.
