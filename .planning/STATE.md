---
saga_state_version: 1.0
milestone: v1.61
milestone_name: qos_classification_contract
status: complete
stopped_at: v1.61 closed and pushed; no active milestone
last_updated: "2026-07-19T12:21:00-05:00"
last_activity: "Saga semantic spine reconciliation complete: latest v1.61 canonical, v1.58/v1.60 history archived, saga-lint clean."
---

## v1.60 Shipped 2026-07-05

Three work items completed, 5/5 PROVEN in TRACEABILITY.md:

1. **SEED-007 (Storage hygiene):** wanctl_state fire-on-change — 95% row reduction verified live (`31e82d8d`).
2. **SEED-006 (Silicom bypass):** Phase B completed, 7 scenarios + orchestrator deployed (`acaaeadd`).
3. **steering-degraded-on-clean-restart:** Phase 224 fix verified via live restart — SPECTRUM_GOOD immediately (`dccca17b`).

Decision record: `decisions/2702-saga-mode-for-ops-work.md`

### Active Work

- **Saga semantic spine reconciliation — COMPLETE (repo-only).** Root REQUIREMENTS/TRACEABILITY now cover latest v1.61 only; SAFE-24 is canonical; retained v1.58/v1.60 records are archived under `.planning/milestones/`. `saga-lint` and `git diff --check` pass. No application, production, or network changes.

- **ROLE-01 read-only readiness assessment — NOT READY / TIME-GATED.** Both cake-autorate services and state bridges are active/successful with zero restarts and no warning-or-higher journal entries since the mechanically proven continuity anchor on 2026-07-10. Both WAN rollback preflights and dry-run plans pass; historical ATT rollback drill remains PASS. The strict 14-day gate matures no earlier than `2026-07-24T08:55:27-05:00`. UDP/3480 and NNTP selectors remain applied but at `0 packets / 0 bytes`; no synthetic traffic was generated. Evidence: `.planning/evidence/role01-readiness-20260718.md`. No production mutation or new milestone was initiated.

- **REQ-003 slice 10 — documented RouterOS insertion repair (repo-only): COMPLETE.** The generic-RTP helper now uses direct SSH exec with exit/stderr plus RouterOS stdout error-marker detection, disabled add-time `place-before=[find where ...]`, `find`-identified enable/remove, exact field/order readback, and reconnecting exact cleanup. TDD: stdout-error test RED then GREEN; focused suite `35 passed`; Ruff, compilation, and Ansible syntax pass; full infra `make ci` `93 passed`; diff checks clean. Independent Claude review found the stdout/rc=0 gap, the repair was added, and re-review returned `NO FINDINGS`. Final live read-only status reports `baseline`, hash `bd0958a0b751ae352cc9c1b093d77c7cf0a76da27d0e90c315224d8b5871f98a`, `changed: false`; mutation task skipped. No live mutation was authorized or performed.

- **REQ-003 slice 11 — fresh generic-RTP diagnostic approval packet: COMPLETE / STOPPED AT MUTATION GATE.** Two authoritative SSH-CLI status reads proved `baseline`, `changed=false`, and stable mangle hash `bd0958a0b751ae352cc9c1b093d77c7cf0a76da27d0e90c315224d8b5871f98a`. The contract audit passed FastTrack, DSCP map, wash/trust ordering, and steering eligibility while reporting only the expected five application-equivalence gaps. wanctl read-only preflight passed `25/25`; both DNS resolvers answered; steering was healthy at `SPECTRUM_GOOD`/GREEN with route guard and reconciliation `ok`; CAKE/state-bridge/watchdog services passed. Packet: `infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-generic-rtp-20260718T121541Z.md`. No mutation, conntrack clear, restart, route/queue change, or bridge retirement occurred. One generic-RTP diagnostic apply requires separate explicit production approval.

- **REQ-003 slice 12 — direct-SSH `place-before` generic-RTP canary: INSERTION SUCCEEDED / ROLLED BACK FAIL-CLOSED / DIAGNOSIS CORRECTED.** Approval covered one generic-RTP diagnostic apply at baseline hash `bd0958a0...1f98a`. The helper, fresh status, and raw numbered capture all placed enabled `CONTRACT: HIGH generic RTP` at #36 immediately before the actual catch-all at #37. The independent audit falsely treated narrow terminal `NORMAL: QUIC UDP 443` rule #21 as the global default boundary and rejected the canary. Exact rollback correctly restored the original baseline and healthy postflight. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-generic-rtp-20260718T123334Z.md`; correction: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/root-cause-audit-false-positive-20260718T131244Z.md`.

- **REQ-003 slice 13 — catch-all audit repair: COMPLETE / REVIEWED.** Infra commit `21187c3` defines the default boundary as an enabled prerouting terminal QOS_NORMAL/no-mark rule without traffic-narrowing selectors, fails closed when no catch-all exists, preserves explicit per-selector PASS evidence while unrelated gaps keep overall status FAIL, and adds two RED/GREEN regressions. Offline replay proves generic RTP PASS via #36 on the canary capture and absent on the restored baseline. Focused guards `3 passed`; audit module `19 passed`; full infra suite `95 passed`; Ruff, compile, Ansible syntax, ansible-lint, yamllint, and diff checks pass. Fresh Claude Opus/high exact-commit second-pass review: `PASS`, zero blockers. Commit pushed to `origin/main`. Production remains at exact baseline; no live access or mutation occurred. Next gate is a fresh approval packet for one generic-RTP reapply using explicit selector evidence.

- **REQ-003 slice 14 — corrected-verifier generic-RTP reapply packet: COMPLETE / PAUSED AT EXPLICIT MUTATION GATE.** Fresh authoritative status reports `baseline`, `changed=false`, exact hash `bd0958a0...1f98a`; mutation task skipped. Fresh audit capture `20260718_133406-routeros-qos-contract` has only the expected five baseline gaps and all other checks PASS. Fresh wanctl proof `wanctl-live-preflight-20260718T133428Z.json` passed `25/25`, route owner/guard/conflicts are `wanctl`/`ok`/`0`, steering is healthy `SPECTRUM_GOOD`, both CAKE bridges are GREEN, expected services are active/enabled, and both DNS resolvers answered. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-generic-rtp-reapply-20260718T133503Z.md`. Acceptance requires explicit generic-RTP PASS evidence before the true catch-all; overall audit remains expected FAIL for four unrelated selectors. No mutation, conntrack clear, restart, route/queue change, bridge retirement, or load generation occurred.

- **REQ-003 slice 15 — corrected-verifier generic-RTP reapply: CANARY ACTIVE / VERIFIED.** Explicit approval authorized exactly one `generic-rtp` apply against fresh baseline hash `bd0958a0...1f98a`. Apply returned `applied`, hash `39f11729...0d83`; two independent status reads agreed and remained stable. Corrected audit capture `20260718_134242-routeros-qos-contract` explicitly passed `generic-rtp: prerouting udp dst-port=16384-32767 -> QOS_HIGH via #36 CONTRACT: HIGH generic RTP`; raw numbered order proves enabled #36 immediately before true catch-all #37. Overall audit remains expected FAIL for WireGuard, SSH, UDP 3480, and NNTP only. Post-apply wanctl proof `wanctl-live-preflight-20260718T134308Z.json` passed `25/25`; both DNS resolvers answered; expected services and health endpoints passed. No rollback condition fired, so the corrected canary remains active. No conntrack clear, restart, route/queue change, bridge retirement, other selector, or load generation occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-generic-rtp-reapply-20260718T134346Z.md`.

- **REQ-003 slice 16 — generic-RTP counter proof: COMPLETE / INSPECT-ONLY.** The repo-owned read-only wrapper captured the exact enabled selector at `110` naturally occurring packets / `10,428` bytes with the intended new/unmarked UDP `16384-32767 -> QOS_HIGH`, terminal fields. No synthetic traffic was generated. Fresh status remained `applied`, `changed=false`, hash `39f11729...0d83`; wanctl preflight passed `25/25`; both DNS resolvers answered. No mutation or bridge retirement occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/generic-rtp-natural-counter-proof-20260718T135025Z.md`. Next bounded slice is a fresh read-only approval packet for WireGuard; it does not authorize apply.

- **REQ-003 slice 17 — WireGuard approval packet: COMPLETE / PAUSED AT EXPLICIT MUTATION GATE.** Registry contract is exact new/unmarked prerouting UDP `51820 -> QOS_HIGH`, terminal, selector-specific confirmation tokens. Fresh WireGuard status is `baseline`, `changed=false`, hash `39f11729...0d83`; RouterOS mutation task skipped. Fresh audit capture `20260718_135351-routeros-qos-contract` passes generic RTP and reports only the expected WireGuard, SSH, UDP 3480, and NNTP gaps. Fresh wanctl proof `wanctl-live-preflight-20260718T135357Z.json` passed `25/25`; both DNS resolvers answered. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-wireguard-20260718T135421Z.md`. Acceptance requires explicit WireGuard PASS before the true catch-all while preserving generic RTP; overall audit remains expected FAIL for three unrelated selectors. No mutation, probe traffic, restart, conntrack clear, route/queue change, or bridge retirement occurred.

- **REQ-003 slice 18 — WireGuard classifier apply: CANARY ACTIVE / VERIFIED.** Explicit approval authorized exactly one WireGuard UDP/51820 apply against fresh hash `39f11729...0d83`. Apply returned `applied`, hash `862700a2...7293`; two independent status reads agreed and remained stable. Corrected audit capture `20260718_135736-routeros-qos-contract` explicitly passed generic RTP #36 and WireGuard #37 immediately before true catch-all #38. Overall audit remains expected FAIL for SSH, UDP 3480, and NNTP only. Immediate WireGuard counters were zero because this tunnel is used only while the operator is away from home; natural-hit observation is deferred until actual offsite use and is non-blocking. No synthetic probe was authorized or generated. Generic RTP continued at `216` packets / `21,517` bytes. Post-apply wanctl proof `wanctl-live-preflight-20260718T135805Z.json` passed `25/25`; both DNS resolvers answered. No rollback condition fired. No conntrack clear, restart, route/queue change, bridge retirement, other selector, or controlled load occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-wireguard-20260718T135914Z.md`. Next bounded slice is a fresh SSH approval packet; it does not authorize apply.

- **REQ-003 slice 19 — WireGuard natural counter proof: COMPLETE / INSPECT-ONLY.** After the operator reported actual offsite-tunnel use, authoritative read-only stats showed the exact enabled WireGuard selector at `1` packet / `60` bytes. The rule began at zero, so this is natural post-deployment match proof; no probe was generated. Parallel status remained `applied`, `changed=false`, hash `862700a2...7293`. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/wireguard-natural-counter-proof-20260718T151235Z.md`. WireGuard field/order/audit/traffic evidence is complete. Next bounded slice remains a fresh SSH approval packet; it does not authorize apply.

- **REQ-003 slice 20 — SSH dead-rule hazard, reviewed repair, and approval packet: COMPLETE / PAUSED AT EXPLICIT MUTATION GATE.** Inspection proved the prior helper design was unsafe for SSH: live #15 `MEDIUM: Interactive traffic (TCP)` covers TCP/22, marks new traffic `QOS_MEDIUM`, and precedes the tail selector lane, so a later SSH rule requiring `connection-mark=no-mark` would be unreachable. No packet was issued against that design. RED tests reproduced both helper misplacement and the audit false negative. The repaired helper gives SSH a dedicated placement lane immediately before the guarded broad TCP producer while preserving that producer byte-for-byte and retaining one-rule rollback; other selectors remain in their canonical pre-default lane. The audit rejects desired terminal rules shadowed by earlier conflicting producers. Review-requested action scoping and fail-closed/benign-boundary tests were added. Focused suites pass `61`; full infra `make ci` passes `102`; Ruff lint and diff checks pass. Independent Claude Opus second pass: `PASS`, no blocking correctness or safety findings. Fresh live SSH status remains `baseline`, `changed=false`, hash `862700a2...7293`; fresh audit reports only SSH/UDP3480/NNTP gaps; wanctl preflight passed `25/25`; configured resolvers `.53/.54` answered. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-ssh-20260718T152547Z.md`. Production was not mutated. One exact SSH preemption apply requires separate explicit approval.

- **REQ-003 slice 21 — SSH preemption live canary: COMPLETE / ACTIVE / NATURAL-TRAFFIC-PROVEN.** Ambiguous `continue` did not cross the mutation gate; the operator then explicitly approved exactly one SSH TCP/22 -> `QOS_HIGH` preemption canary. Fresh baseline hash `862700a2...7293`, audit, wanctl `25/25`, services, and both resolvers passed. The helper added exact enabled `CONTRACT: HIGH SSH` at authoritative #15 immediately before #16 `MEDIUM: Interactive traffic (TCP)` without modifying the broad producer. Apply and independent status converged on hash `72d16f5d...c2cb8`; audit explicitly passed SSH, generic RTP, and WireGuard and exited expected `2` only for UDP/3480 and NNTP. Post-apply wanctl `25/25` and both resolvers passed. Read-only counters showed `4` packets / `224` bytes with no generated load or conntrack clearing, proving natural post-deployment traffic. Rollback was not triggered; SSH remains active. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-ssh-20260718T155557Z.md`.

- **REQ-003 slice 22 — UDP/3480 approval packet: COMPLETE / REVIEWED / PAUSED AT EXPLICIT MUTATION GATE.** Two authoritative status reads agree on `baseline`, `changed=false`, hash `32c853ff...e8e`; no mutation task ran. Fresh audit passes SSH #15, generic RTP #35, WireGuard #36, DSCP/FastTrack/order/steering checks and exits expected `2` only for UDP/3480 and NNTP. Fresh wanctl preflight passed `25/25`; both resolvers answered. Normalized read-only comparison disclosed unrelated pre-packet game-rule drift (two Enshrouded rules absent, one Valheim UDP/5520 rule added); cause is unattributed, current state was stable across two reads, and the exact fresh hash is the fail-closed anchor. Independent Claude Opus read-only review cross-checked the packet against the helper, policy, playbook, and live numbered capture and returned `PASS` with no blocking correctness or safety findings. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-realtime-udp-3480-20260718T161635Z.md`. No RouterOS mutation, generated traffic, conntrack clear, restart, route/queue change, or bridge retirement occurred. One exact UDP/3480 apply requires separate explicit approval.

- **REQ-003 slice 23 — approved UDP/3480 live canary: BLOCKED BEFORE MUTATION / HASH GATE WORKED.** Explicit approval covered one attempt against reviewed hash `32c853ff...e8e`. Mandatory immediate preflight returned `baseline`, `changed=false`, but fresh hash `ed2fd55a...5beb`, so no apply command ran and no rollback was needed. Fresh audit remained healthy with only UDP/3480 and NNTP open; wanctl preflight passed `25/25`; both resolvers answered. Normalized authoritative captures show unrelated concurrent game-policy drift: Valheim UDP/5520 was removed and Hytale UDP/5520 plus UDP/55200 were added. The external writer is unattributed. The old packet anchor is stale and the approval cannot be reused. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/blocked-canary-result-realtime-udp-3480-20260718T170631Z.md`. No RouterOS mutation, generated traffic, conntrack clear, restart, routing/queue change, NNTP apply, or bridge retirement occurred. Wait for the external writer to settle, then require two stable reads, a new packet, and new explicit approval.

- **REQ-003 slice 24 — refreshed UDP/3480 stability packet: COMPLETE / REVIEWED / PAUSED AT NEW EXPLICIT MUTATION GATE.** The full policy hash held at `ed2fd55a...5beb` across a fresh numbered contract audit and two authoritative status reads separated by 59 seconds; both status reads were `baseline`, `changed=false`, and skipped mutation. Audit remained expected exit `2` with only UDP/3480 and NNTP open while SSH #15, generic RTP #35, WireGuard #36, DSCP map, order, and steering checks passed. wanctl preflight passed `25/25`; both resolvers answered. The refreshed packet preserves the current Hytale/game baseline, binds apply and rollback to exact hashes, and fails closed on any new drift. Independent Claude Opus read-only review returned `PASS`, no blockers. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-realtime-udp-3480-refresh-20260718T171943Z.md`. No RouterOS mutation, NNTP apply, bridge retirement, conntrack clear, restart, routing/queue change, or generated traffic occurred. A new explicit one-attempt approval is required.

- **REQ-003 slice 25 — approved UDP/3480 live canary: CANARY ACTIVE / VERIFIED / COUNTER OBSERVATION DEFERRED.** Immediate pre-apply status exactly matched approved hash `ed2fd55a...5beb`. The helper added only enabled new/unmarked UDP/3480 -> `QOS_HIGH`; apply and two independent post-status reads converged on `18a29b0a...90f6`. Fresh numbered audit explicitly passes UDP/3480 at authoritative #37 immediately before true default #38, while SSH #15, generic RTP #35, WireGuard #36, DSCP/order/steering checks remain PASS; overall exit `2` is expected solely for NNTP. wanctl passed `25/25`; both resolvers answered. Exact read-only stats show enabled terminal fields and `0` packets / `0` bytes; no synthetic traffic was generated, so natural proof is deferred. No rollback condition fired and the selector remains active. No NNTP, bridge retirement, conntrack clear, restart, route/queue/qdisc/rate change, or game-policy mutation occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-realtime-udp-3480-20260718T174527Z.md`.

- **REQ-003 slice 26 — NNTP approval packet: COMPLETE / REVIEWED / PAUSED AT EXPLICIT MUTATION GATE.** Registry and policy agree on exact enabled new/unmarked TCP/119 -> `QOS_LOW`, terminal, with selector-specific apply/rollback tokens. Fresh audit capture `20260718_174851-routeros-qos-contract` passes SSH #15, generic RTP #35, WireGuard #36, UDP/3480 #37, DSCP/order/steering checks, and reports NNTP as the sole gap. Two authoritative NNTP status reads remained `baseline`, `changed=false`, at stable hash `18a29b0a...90f6`; mutation skipped. wanctl passed `25/25`; both resolvers answered. Independent Claude Opus read-only review cross-checked packet/helper/playbook/policy/live capture and returned `PASS`, no blockers. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-nntp-20260718T174945Z.md`. No RouterOS mutation, bridge retirement, conntrack clear, restart, route/queue/rate change, or traffic generation occurred. One exact NNTP apply requires separate explicit approval and must make the audit overall PASS.

- **REQ-003 slice 27 — approved NNTP live canary: CANARY ACTIVE / VERIFIED / CONTRACT AUDIT OVERALL PASS.** Immediate pre-apply status exactly matched approved hash `18a29b0a...90f6`. The helper added only enabled new/unmarked TCP/119 -> `QOS_LOW`; apply and two independent post-status reads converged on `4ad83ba7...921e`. Fresh numbered audit returned overall PASS and explicitly proves SSH #15, generic RTP #35, WireGuard #36, UDP/3480 #37, and NNTP #38 immediately before true default #39, plus DSCP/order/steering/FastTrack checks. wanctl passed `25/25`; both resolvers answered. Exact stats show terminal NNTP fields and `0` packets / `0` bytes; no synthetic traffic was generated, so natural proof is deferred. No rollback condition fired; NNTP remains active. No bridge retirement, conntrack clear, restart, route/queue/qdisc/rate change, or game-policy mutation occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-nntp-20260718T180241Z.md`.

- **REQ-003 slice 28 — Spectrum bridge duplicate-retirement implementation and packet: COMPLETE / REVIEWED / PAUSED AT EXPLICIT PRODUCTION GATE.** TDD RED proved the existing Spectrum fallback; targeted GREEN passed, bridge suite passed `6`, isolated nft loads passed, and full `make ci` passed `5,767` with 90.14% coverage. The durable repo target removes only RouterOS-equivalent Spectrum generic RTP, SSH, WireGuard, UDP/3480, and NNTP fallbacks; UDP/3478-3479 is retained and ATT remains staged for later. Live inspection exposed deployment drift: live hash `649bd585...b569` lacks the repo's pending AF31 import on both upload chains, so deploying the worktree would silently cross the one-WAN scope. A temporary target was therefore derived byte-for-byte from live, changes only `spectrum_dl`, preserves upload chains and ATT exactly, and hashes to `12300940...068c`. The guarded helper is read-only by default, requires both exact apply gates, creates a root-owned mode-0444 content-addressed backup, verifies service/rules/hash, runs RouterOS audit + wanctl/DNS/HTTPS checks, and performs independently verified exact rollback on disagreement. Claude Code initial and hardened-path reviews returned PASS. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-spectrum-bridge-retirement-20260718T182500Z.md`. Production remains unchanged; one exact Spectrum reload requires separate explicit approval. No ATT retirement, AF31 deployment, conntrack clear, route/queue/qdisc/rate change, or generated traffic is included.

- **REQ-003 slice 29 — approved Spectrum retirement and natural observation: CANARY ACTIVE / VERIFIED.** Exact hash-gated reload moved live from `649bd585...b569` to `12300940...068c` and created a root-owned mode-0444 backup with the exact baseline hash. Independent reads prove the five intended Spectrum duplicates absent, UDP/3478-3479 retained, ATT unchanged, upload AF31 count still zero, and service active/successful. RouterOS audit is overall PASS; wanctl passed `25/25`; health, both DNS resolvers, and HTTPS passed. A 15-second natural-only delta moved Bulk/BestEffort/Video/Voice on both WANs with zero new drops and zero backlog; no traffic was generated. No rollback condition fired. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-spectrum-bridge-retirement-20260718T183654Z.md`.

- **REQ-003 slice 30 — ATT bridge duplicate-retirement implementation and packet: COMPLETE / REVIEWED / PAUSED AT EXPLICIT PRODUCTION GATE.** TDD RED failed on the live-equivalent ATT duplicates; targeted GREEN passed after removing only generic RTP, SSH, and NNTP and narrowing UDP/3478-3480 to retained 3478-3479. Bridge suite passed `6`, isolated nft load passed, and full `make ci` passed `5,767` with 90.14% coverage. The durable repo target is now symmetric. Because live still lacks the repo's AF31 imports, a temporary candidate was derived from exact post-Spectrum live hash `12300940...068c`; structural proof confirms only `att_dl` changes, Spectrum/upload/forward are byte-identical, AF31 remains absent, and target hash is `e1063434...03d8`. The ATT helper is read-only by default, requires exact apply gates, creates an immutable exact backup, verifies both retired WAN chains and AF31 absence, and independently verifies rollback. Bash syntax, ShellCheck, status, fail-closed gate, target syntax/scope, and Claude Opus/high review all PASS with zero blockers. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-att-bridge-retirement-20260718T184500Z.md`. Production remains at the verified post-Spectrum baseline; one exact ATT reload requires separate explicit approval. No AF31 deployment, conntrack clear, route/queue/qdisc/rate change, or generated traffic is included.

- **REQ-003 slice 31 — approved ATT retirement and natural observation: CANARY ACTIVE / VERIFIED / BOTH WAN RETIREMENT COMPLETE.** Immediate status exactly matched approved baseline `12300940...068c`. The helper reloaded only the reviewed ATT-derived target and converged on `e1063434...03d8`; immutable rollback backup is exact baseline, mode `0444 root:root`. Independent nft reads prove generic RTP, SSH, UDP/3480, and NNTP fallbacks absent on ATT, UDP/3478-3479 retained, Spectrum still retired, and upload AF31 count still zero. RouterOS audit is overall PASS; wanctl passed `25/25`; service, health, both DNS resolvers, and HTTPS passed. A 15-second natural-only delta moved all four CAKE tins on both WANs with zero new drops and zero backlog. No traffic was generated and no rollback condition fired. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-att-bridge-retirement-20260718T190353Z.md`. Duplicate application classifier retirement is complete on both WANs. REQ-003 remains open only because the repo's AF31 upload-import contract is not yet live; that convergence is a separate production change requiring a fresh exact packet and approval.

- **REQ-003 slice 32 — AF31 live-convergence package: COMPLETE / REVIEWED / PAUSED AT EXPLICIT PRODUCTION GATE.** Exact live capture is `e1063434...03d8` with zero AF31 imports. The durable repo differs by explanatory comments plus two executable AF31 rules, so the temporary candidate was derived from exact live bytes and adds only one AF31→conntrack-mark-`0x2` import to each WAN upload chain: exactly two added lines, zero removals, no other byte changes. Candidate target `a6b85d55...04884` has executable-rule parity with the durable repo; both retired download chains and UDP/3478-3479 remain unchanged. Isolated nft load passed; bridge suite passed `6`; full `make ci` passed `5,767` with 90.14% coverage; RouterOS audit is overall PASS; wanctl preflight passed `25/25`; helper Bash syntax, ShellCheck, read-only status, and missing-gate fail-closed checks passed. The helper requires exact token+baseline gates, creates a root-owned mode-0444 exact backup, verifies one AF31 rule per WAN and both retired download chains, and independently verifies automatic or manual rollback. Claude Opus/high independent read-only review returned PASS with zero blockers; its sole LOW doc-only note confirms intentionally retained live comments have no executable impact. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-af31-convergence-20260718T191753Z.md`. Production remains unchanged at `e1063434...03d8`; one exact AF31 bridge reload requires separate explicit approval. No RouterOS, conntrack, qdisc/rate, route/NAT/steering, autorate, or generated-traffic mutation is included.

- **REQ-003 slice 33 — approved AF31 convergence attempt: STRUCTURALLY VERIFIED / ACCEPTANCE DISAGREEMENT / EXACTLY ROLLED BACK.** Immediate preflight matched approved source `e1063434...03d8`; the helper reached exact target `a6b85d55...04884`, created an immutable exact backup, and independent reads proved two AF31 imports total, one per WAN, active/successful service, overall-PASS RouterOS audit, wanctl `25/25`, healthy endpoint, both DNS resolvers, and HTTPS. The first CAKE delta parser exposed a real telemetry-shape bug (`tins` is top-level, not `options.tins`) and produced no evidence. A corrected shape-tolerant natural 15-second observation then found concurrent Spectrum Bulk saturation: 376,006,250 bytes, 252,578 packets, 11,979 drops, and 337,644 ending backlog; ATT and other tins had zero new drops/backlog. This is not evidence the AF31 rules caused the bulk load, but it disagreed with the approved zero-drop/backlog acceptance and could not be waived. The authorized exact rollback restored live hash `e1063434...03d8`; independent status proves service active/successful, AF31 count zero, healthy endpoint, both resolvers, and HTTPS. No synthetic traffic or RouterOS/conntrack/qdisc/rate/route/NAT/steering/autorate mutation occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-attempt-result-af31-convergence-rollback-20260718T201849Z.md`. The one-attempt approval is consumed. REQ-003 is blocked pending a fresh quiet-window or explicitly revised acceptance packet and separate approval.

- **REQ-003 slice 34 — load-aware AF31 acceptance package: COMPLETE / REVIEWED / PAUSED AT FRESH PRODUCTION GATE.** Read-only characterization proved the earlier Spectrum Bulk saturation had cleared: repeated three-window samples showed zero new drops and zero ending backlog on all tins, with exact `8002:`/`8001:` handles and `diffserv4`. New `cake-qos-natural-window.py` accepts both observed tc JSON shapes, makes three uncongested five-second windows a strict pre-copy/pre-backup/pre-mutation gate, and makes post-apply acceptance load-aware: exact qdisc handles, `diffserv4`, four tins, and monotonic counters are mandatory while natural drops/backlog are reported rather than misclassified as nft failure. The v2 helper uses fresh non-replayable tokens and unchanged exact `e1063434...03d8 -> a6b85d55...04884` hashes. Positive live quiet/continuity checks, modeled congestion exit `3`, modeled counter-reset failure, Python compile/Ruff, Bash syntax/ShellCheck, target nft load/hash, unauthorized exit `2`, bridge tests `6`, wanctl diff check, and infra CI `102` all pass. First Claude Opus/high review found a real inherited Bash `errexit` hole: `if ! post_apply_checks` suppresses `set -e` inside the function, so bare audit/preflight/health/HTTPS commands were not guaranteed to gate rollback. v2 now explicitly returns failure for every audit, preflight, health, HTTPS, DNS, and continuity check; static and dynamic negative harnesses pass. Independent second-pass review returned PASS with no blockers and confirmed exact rollback wiring plus unchanged pre-mutation/remote/manual controls. Packet: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/approval-packet-af31-convergence-v2-20260718T215014Z.md`. Production remains exact healthy baseline `e1063434...03d8`; the first approval is consumed and only a fresh explicit v2 approval may invoke one reload. No live mutation or generated traffic occurred in this slice.

- **REQ-003 slice 35 — approved load-aware v2 AF31 convergence: ACTIVE / VERIFIED / REQUIREMENT CLOSED.** Immediate helper status and a separate SSH read both matched complete approved source hash `e1063434...03d8` with active/successful service. The mandatory pre-copy/pre-backup/pre-mutation gate completed three consecutive five-second windows with zero new drops and zero ending backlog across every tin while preserving `spec-router=8002:`, `att-router=8001:`, `diffserv4`, and four tins. The guarded reload created exact immutable mode-0444 root-owned baseline backup `/etc/wanctl/bridge-qos.nft.pre-af31-convergence-20260718T220305Z-e10634346b9da8490332316583abd117b1140590945717804e133bc82e8a03d8.nft` and reached exact target `a6b85d55...04884`. Fresh audit returned overall PASS for all five RouterOS selectors; wanctl preflight passed `25/25`; service, health, both resolvers, and HTTPS passed. The 15-second load-aware continuity check preserved both exact handles, `diffserv4`, four tins, monotonic counters, and zero ending backlog while natural traffic moved; no traffic was generated. Separate fresh-session reads re-proved exact live/backup hashes, metadata, one normalized AF31 import per WAN, active/successful service, and CAKE state. Two ad-hoc verifier failures were parser defects only—nft removed redundant `ether type ip`, and concatenated independent `tc` arrays were invalid JSON—and corrected authoritative reads passed without any live disagreement. No rollback condition fired. No RouterOS, conntrack, qdisc/rate, route/NAT/steering, watchdog, autorate, generated-traffic, or unrelated policy mutation occurred. Evidence: `../infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/live-canary-result-af31-convergence-v2-20260718T220448Z.md`. REQ-003 is now `[x]`/PROVEN; all v1.61 requirements are drained pending repository finalization and mandatory independent frontier close-out.

- **v1.61 close-out — FRONTIER VERIFIED / ROADMAP SHIPPED / AUDIT CONDITIONAL.** The configured one-shot Claude Opus/high auditor exited `0` and freshly wrote both `TRACEABILITY.md` and `AUDIT.md`. It independently reproduced the bridge suite (`6 passed`), steering/bridge suite (`283 passed`), and infra audit suite (`24 passed`), verified the continuous live hash lineage from `649bd585` through final `a6b85d55`, and classified REQ-001 through REQ-006 plus SAFE-24 as PROVEN: `6 PROVEN / 0 ASSERTED / 0 OPEN`. Audit verdict is CONDITIONAL only on durable commit, deferred natural UDP/3480 and NNTP hit confirmation, and the stated no-live-access evidence boundary; no requirement rework is requested. `.planning/phases/` contains only older v1.48/v1.58/v1.59 material and no v1.61 phase directories, so no unrelated phase archive move was performed. The actual ROADMAP v1.61 line was mechanically changed from `🚧` to `✅` and re-read successfully. Active milestone pointer is now none; repository finalization/commits are the only remaining close-out work.

- **v1.61 / REQ-001–REQ-002 (repo-only): COMPLETE.** Contract recorded and AF31 import added on both WAN upload chains. TDD evidence: expected RED failure, targeted GREEN (`1 passed`), bridge-QoS suite (`4 passed`), namespace nft syntax check (`NFT_SYNTAX_OK`), and full `make ci` (`5,758 passed`, 90.17% coverage). Production unchanged.
- **REQ-003 contract-proof slice (repo-only): COMPLETE.** Exact EF/AF31/CS1 import parity, Voice/Video/Bulk restore parity, carrier wash, and unmatched-CS0 Best Effort fallback are mechanically asserted on both WANs. Evidence: bridge suite `5 passed`, namespace `NFT_SYNTAX_OK`, full `make ci` (`5,759 passed`, 90.17% coverage). No fallback classifier was removed; REQ-003 remains open for safe retirement work.
- **REQ-005 read-only audit: COMPLETE.** `infra-ansible/scripts/routeros-qos-contract-audit.py` checks FastTrack, the EF/AF31/CS1/CS0 map, wash-before-trust ordering, per-application class equivalence, multiword RouterOS fields, and same-chain composite producer/consumer safety. Audit tests: `15 passed`; policy tests: `3 passed`; canary tests: `6 passed`; full infra-ansible suite: `50 passed`; `make ci`, Ruff, compilation, JSON validation, and Ansible syntax all pass. Post-rollback live audit still reports the five expected application-equivalence gaps and retained legacy-route warning.
- **REQ-003 retirement checkpoint: BLOCKED and now mechanically gated.** The audit reports generic RTP `16384-32767`, WireGuard `51820`, SSH `22`, UDP `3480`, and NNTP `119` as exact live coverage gaps. A producer counts only when it is enabled, same-class, new-connection-only, terminal, and reachable before the default classifier. No bridge classifier was removed or deployed.
- **REQ-004 package prerequisite: COMPLETE.** `infra-ansible/artifacts/network-changes/20260717_routeros-qos-composite-policy/` defines `QOS_HIGH_ATT` as an inspectable composite of EF queue intent and sticky ATT affinity, restricts assignment to explicit new-connection producers, keeps DNS on plain `QOS_HIGH`, removes the broad legacy route in the proposed full-policy state, and includes an approval-gated bounded Work-VPN canary with exact rollback.
- **Work-VPN composite-mark canary package: LIVE ATTEMPT FAILED SAFELY.** Exact preflight passed and apply reached `state=canary`, but a new TCP connection to the Work-VPN endpoint timed out over `to_ATT`. The producer was restored immediately, active `QOS_HIGH_ATT` connections drained to zero, compatibility rules were removed, and status returned to `baseline`. Both internal resolvers and endpoint TCP/443 passed after rollback; cake-shaper QoS/autorate services remained active.
- **Live checkpoint — REQ-006:** corrected reapply remains active. A download-only Spectrum speedtest transferred 163,662,768 bytes at 130.67 Mbit/s while the actual FortiVPN session remained assured/reply-seen as `QOS_HIGH_ATT` through ATT. Both resolvers passed 40/40 uncached probes, Spectrum connections demoted to `QOS_LOW`, Spectrum Bulk and ATT Voice counters increased, and all QoS/autorate services stayed active.
- **REQ-004/REQ-006 DNS-safe adaptive convergence: COMPLETE.** The broad `QOS_HIGH` route is retired and replaced by exact `ADAPTIVE: Work VPN eligible for ATT` selection for eligible new Work-VPN connections. Live verification exposed and fixed startup logical/rule-state drift; full wanctl CI passed (`5,766 passed`, 90.14%), independent Claude review passed, and the reviewed daemon module was deployed with matching SHA-256. Controller restart disabled and verified the exact producer under `SPECTRUM_GOOD`. Approval-gated demigration and remigration passed without clearing conntrack; both resolvers passed 50/50 final probes; all QoS/autorate services and steering health remained active.
- **Adaptive status gate repair (repo-only): COMPLETE.** Reproduced the stale `canary-active-corrected-verified` assertion, updated it to the live `adaptive-active-corrected-verified` state, and added a direct assertion for `bounded_adaptive_migration_status=live-verified`. Focused test passed; full infra `make ci` passed with 58 tests. No live mutation or policy behavior change.
- **REQ-003 slice 1 — generic RTP migration package (repo-only): COMPLETE.** Added the exact new/unmarked UDP `16384-32767 -> QOS_HIGH` producer, immediate pre-default ordering, read-only status, a counter-insensitive live mangle SHA-256 mutation anchor, distinct confirmations, duplicate/drift checks, failed-move cleanup, idempotent rollback, Ansible wrapper, and operator procedure. Focused suite: `12 passed`; full infra `make ci`: `70 passed`. Independent Claude review passed after one repair cycle. Infra commit `359305a`. No live command, classifier retirement, or conntrack clear occurred.
- **REQ-003 slice 2 — finite classifier registry (repo-only): COMPLETE.** Extended the proven canary machinery behind an explicit five-selector registry covering generic RTP, WireGuard `51820`, SSH `22`, UDP `3480`, and NNTP `119`. Every selector is mechanically bound to `policy.json`, uses new/unmarked exact rules and selector-specific confirmations, converges into one canonical block before the default regardless of apply order, stages disabled until placement succeeds, cleans up on placement/enable failure, and retains fresh-hash gating plus read-only default status. Focused suite: `20 passed`; full infra `make ci`: `78 passed`. Independent Claude review passed after one repair cycle. No live command, classifier retirement, or conntrack clear occurred.
- **REQ-003 slice 3 — live read-only approval packet: COMPLETE / STOPPED AT MUTATION GATE.** Fresh status for all five finite selectors returned `state=baseline`, `changed=false`, and the same current mangle anchor `f59a7c9a2352bc965a5d31c36072d6a34d2f78d96e2269c237d8262216bf7123`. Fresh contract audit capture `20260718_012433-routeros-qos-contract` passed FastTrack, DSCP map, wash/trust ordering, and steering eligibility while reporting the exact five expected application-equivalence gaps. No RouterOS mutation, conntrack clear, service restart, or bridge retirement occurred. Next action requires explicit production approval for the bounded per-selector applies.
- **REQ-003 slice 4 — generic-RTP live canary: FAILED SAFELY / CORRECTION REVIEWED / RENEWED APPROVAL REQUIRED.** The approved generic-RTP apply returned `applied`, but a fresh independent audit found the enabled rule after the terminal default and therefore unreachable. Remaining applies froze immediately. Exact rollback with the fresh post-change anchor restored `baseline` and the original full mangle hash; both resolvers, six shaping/bridge/steering units, steering health, router reachability, route guard, and the original five-gap audit state passed. No conntrack clear occurred. Root cause: the mutating API session's order readback did not match a subsequent fresh connection. Infra commit `f0d71e3` now closes the mutation session, requires fresh-session target/order proof, and exactly removes the new row on fresh-session failure; focused suite `22 passed`, full infra CI `80 passed`, independent Claude second-pass review `PASS`. Corrected live reapply is stopped at renewed explicit approval.
- **REQ-003 slice 5 — corrected generic-RTP canary: FAILED SAFELY / ESCALATED.** Renewed approval covered generic RTP only. Fresh baseline, five-gap audit, DNS, authoritative 25/25 wanctl preflight, services, and health endpoints passed. Corrected apply passed its immediate new-session gate, but a later independent status had a different anchor and the audit again found the enabled selector after the terminal default at rule #36. Exact rollback with that latest anchor restored the original full hash and healthy five-gap baseline; no other selector was attempted and no conntrack was cleared. This is the second verification failure, so local repair/retry is exhausted. Escalation must determine whether RouterOS needs add-time placement, active-rule move semantics, or a bounded settle/poll proof before any new live approval.
- **REQ-003 slice 6 — frontier add-time-placement package: COMPLETE / STOPPED AT NEW APPROVAL GATE.** Codex replaced the failed add-disabled/move/enable choreography with candidate add-time `place-before` placement in the selector's final enabled state, three fresh sessions over a bounded settle window, reconnecting exact-ID-plus-field cleanup for every changed-apply verification failure, deterministic connection closing, and a policy hash that excludes only counters and reboot-volatile RouterOS `.id` while retaining order and all policy fields. Helper success now declares bounded—not durable—proof and requires the separate independent contract audit. Focused xdist suite: `28 passed`; full infra `make ci`: `86 passed`; Claude second-pass review: `PASS`, zero blocking findings. Infra commit `c34d814`. Add-time firewall-mangle placement remains unproven live; production is still the restored five-gap baseline and any third generic-RTP canary requires fresh explicit approval.
- **REQ-003 slice 7 — add-time generic-RTP canary: FAILED SAFELY / ROOT CAUSE NARROWED.** Explicit approval covered generic RTP only. The helper used add-time `place-before`, reported `applied`, and observed the same canonical state and `.id`-independent anchor across three fresh sessions plus a later independent API status. The authoritative CLI/export-backed contract audit simultaneously saw the enabled selector at rule #36 after the terminal default and rejected it as unreachable. Exact rollback restored `baseline` and the original `.id`-independent anchor; both resolvers and the authoritative 25/25 post-rollback preflight passed. No other selector, conntrack clear, restart, routing change, queue change, bridge retirement, or load generation occurred. API `print` ordering is therefore not an acceptable source of truth for mangle evaluation order on this device/path. REQ-003 is blocked until mutation and verification use an authoritative CLI/export-equivalent mechanism or ordered insertion is redesigned.
- **REQ-003 slice 8 — authoritative SSH-CLI canary package: COMPLETE / STOPPED AT FRESH APPROVAL GATE.** `infra-ansible` commit `5e3d05d` removes librouteros/API ordering from status and mutation, uses host-key-pinned RouterOS SSH CLI `print detail` as the ordering authority, consumes display numbers only in the same persistent session and print generation, performs disabled add → exact readback → numeric move → placement proof → numeric enable → canonical proof, and reconnects for exact-field cleanup or rollback verification without blind retries. Status uses the read-only identity; apply/rollback use the operator identity. Exact terminal-default fields, wrapped comments, empty values, flags, policy hash state/order, unknown host keys, no-op mutations, transport failures, ambiguous/missing/drifted cleanup candidates, and rollback uncertainty are tested. Focused suite: `36 passed`; full infra `make ci`: `94 passed`; artifact Ansible syntax, Ruff, Black, compile, and diff checks pass. Final independent Claude review: `PASS`, zero blockers. Production remains at the restored healthy five-gap baseline; no live mutation, conntrack clear, restart, routing/queue change, bridge retirement, or load generation occurred. Any new generic-RTP canary requires a fresh status anchor, independent preflight, rollback packet, and explicit production approval.
- **REQ-003 slice 9 — authoritative SSH-CLI generic-RTP canary: FAILED CLOSED / ROOT CAUSE UNOBSERVED.** Fresh status and 25/25 preflight passed at anchor `bd0958a0...1f98a`; Spectrum was `GOOD`, DNS answered, and route/RouterOS guards were healthy. The explicitly approved apply exited nonzero, and immediate authoritative status proved exact cleanup back to the same anchor and expected healthy five-gap audit state. The pre-repair Ansible `no_log` task discarded the helper's sanitized stderr, so the failure stage cannot be recovered and no retry occurred. Infra commit `ea008d4` now preserves credential secrecy while surfacing only validated helper JSON errors or a static redacted fallback; focused `36`, full CI `94`, offline malformed-stderr harness, and independent Claude review all pass. Production remains at baseline. A diagnostic canary requires a fresh packet and explicit approval.

- **t_bfe1e19b (C901 refactor):** Done — `_run_logging_metrics` extracted into 6 private helpers. Complexity 17→below threshold. Commit `cd777d91`.

### Deferred (post-ROLE-01)

- **FLIP-02 (fping/native keep canary):** Closed as moot. Native Python controller retiring; cake-autorate already uses fping. Code remains available if needed.
- **TIN-SPARSE-01 (CAKE tin skip-on-unchanged):** Deferred. Consumer audit found raw-history semantics in `wanctl-history --tins`. Revisit after ROLE-01 — only state-bridge remains as consumer.
- **IRTT-MIG-01 (IRTT first-class backend):** Deferred. Dead stub (`NotImplementedError`). No value while cake-autorate handles RTT measurement natively. Remove or implement post-ROLE-01.
- **RECLAIM-04 (Spectrum upload reclaim):** Closed — no action needed. Actual demand ~22 Mbps < 30 Mbps floor (73% utilization). Lowering floor tightens the queue, hurts latency. Raising ceiling to 40 Mbps is cosmetic (never approached). Current config optimal for observed workload.
- **ROLE-01 (native-controller retirement):** Rollback drill completed 2026-07-09 (PASS — native wanctl@att started, ran healthy, stopped, cake-autorate resumed cleanly). Remaining gate: >=14 consecutive stable cake-autorate days (~Jul 19 target).

### Deferred

None

### Completed

- **SEED-010 (Silicom test harness validation):** All 7 scenarios tested live 2026-07-09. Fixed 3 bugs: `set_dis_disc off` before disconnect (`silicom-bypass`), `shift` in `cmd_chaos` (`silicom-test`), `--both-wan-confirm` for dual-WAN loss. Commit `e2b0e6f3`.

### Key State

- 6 routes managed, guard ok, 0 conflicts
- Both failover bridges armed
- Steering daemon: healthy, SPECTRUM_GOOD
- Spectrum UL: floor 30 Mbps, ceiling 36 Mbps (soak complete — no changes)
- cake-autorate active rate controller (since 2026-07-05)
- State-bridge downsampling: raw→1m every 15 min (commit `5c410373`)
