# v1.43 UL Suppression Metrics & Gate Calibration (ARCHIVED)

**Shipped:** v1.43.0 to production 2026-05-13 (audit `passed` 15/15)
**Tagged:** v1.43 (commit at v1.43 archive close — see `git tag v1.43`)
**Status:** Archived 2026-05-13. Phase 204 closed `passed` after gap-closure cycle Plans 204-07..10 produced valid post-`d44e2fd` CALIB-01/CALIB-04 evidence under the corrected boundary-marker aggregator. SAFE-07 closeout invariant held: zero control-path source diff between Phase 201 close (`b72b463`) and v1.43 close, verified at every phase boundary.

---

# Roadmap: wanctl v1.43 UL Suppression Metrics & Gate Calibration

**Milestone Goal:** Repair the metric contract behind the failed D-14 secondary watchdog from Phase 201, capture target-edge evidence in the same baseline soak, and recalibrate a soak-grounded D-14 successor gate — without changing controller behavior.

**Phases:** 3 (202 METRIC, 203 OBSV, 204 CALIB) | **Requirements mapped:** 14/14 v1.43 REQ-IDs ✓ + SAFE-07 cross-cutting

**Granularity:** fine
**Phase-order rationale:** SEED-002 → SEED-004 → SEED-003 (NOT seed-priority order). The 24h Spectrum baseline soak in Phase 204 is the milestone's most expensive evidence primitive (operator approval + 24h wall clock + production deploy). Phase 203's per-sample `load_rtt_delta_us` must be live before that soak fires so a single 24h run produces both the recalibration baseline (CALIB-01) and the target-edge distribution (OBSV-06). Joint Claude+Codex (gpt-5.5 xhigh) scope decision recorded 2026-05-06.

**Closeout invariant (SAFE-07, cross-cutting):** No controller tuning permitted within v1.43. SEED-005 conservative UL tuning sweep is structurally barred from this milestone (named for v1.44, not soft-deferred). SAFE-05 occurrence pins for v1.42 control-path values remained unchanged at v1.43 close; no control-path source diff between Phase 201 close and v1.43 close.

**Production deploy cadence:** Phases 202 and 203 are additive `/health` schema and soak-harness work — no production canary required. Phase 204 required two production deploys, both gated on operator approval: (1) METRIC-01 + OBSV-05 binary for the CALIB-01 baseline soak, (2) the recalibrated threshold for the CALIB-04 verification soak.

**Cross-milestone references preserved:**
- Phase 201 RETRO Lesson #1 (metric-semantics framing) — primary basis for SEED-002 / Phase 202.
- v1.42 Plan 201-15 / 201-16 two-snapshot rollback pattern — reused for both Phase 204 production deploys.
- v1.42 D-19 vs D-14 dual-gate framing — reused as Phase 204 verification soak gate (D-19 stays at 0 floor hits, D-14-successor passes the new threshold).

## Phase Summary

| #   | Phase                                                                | Plans | Status                | Completed   |
|-----|----------------------------------------------------------------------|-------|-----------------------|-------------|
| 202 | UL Suppression Metric Semantics (METRIC)                             | 4/4   | Complete              | 2026-05-06  |
| 203 | Target-Edge Churn Instrumentation (OBSV)                             | 3/3   | Complete              | 2026-05-06  |
| 204 | D-14 Successor Recalibration (CALIB)                                 | 10/10 | Complete (re-verified)| 2026-05-13  |

---

## Phase 202: UL Suppression Metric Semantics (METRIC)

**Goal:** Operators can read completed-window UL suppression counts decomposed by cause from `/health.wans[].upload`, and the new metric matches codex re-aggregation values from the v1.42 reference soak — without changing controller behavior.

**Depends on:** Nothing (first v1.43 phase). Built on top of v1.42.1 production binary.
**Requirements:** METRIC-01, METRIC-02, METRIC-03, METRIC-04, METRIC-05, SAFE-07 (cross-cutting)
**Production canary:** Not required — additive `/health` schema and counter accounting only, no control-path change (per SEED-002 frontmatter).

**Success Criteria (all met):**
1. Operator can read `suppressions_completed_window_count` from `/health.wans[].upload` with values that emit only at 60s window boundaries; live `suppressions_per_min` is preserved untouched at the same path. ✓
2. Each suppression increment is classified at source (dwell-hold from `queue_controller.py:348`, backlog-recovery, or other) and surfaced as additive per-cause `/health.wans[].upload` count fields. ✓
3. Replay test against `.planning/milestones/v1.42-phases/201-docsis-aware-ul-congestion-control/soak/20260505T132736Z/soak-capture.ndjson` confirms completed-window counts match the codex re-aggregation values (peak mean ~13.9/min, p95=41, max=124). ✓
4. SAFE-05 v1.43 occurrence pins re-established for the new metric keys; existing v1.42 control-path pins remain byte-identical (SAFE-07 verification). ✓
5. `CHANGELOG.md` and `docs/CONFIGURATION.md` document the additive `/health` field set and the metric-semantics framing (live counter vs completed-window). ✓

**Plans:**
- [x] 202-01: Counter accounting + additive `/health` schema (per-cause suppression counters in `QueueController`; completed-window snapshots through hysteresis payloads; lifetime suppression counters by cause).
- [x] 202-02: Replay-fixture completed-window oracle (METRIC-03 replay against v1.42 reference soak; offline `aggregate_completed_windows` helper pinning reset-boundary semantics).
- [x] 202-03: SAFE-05 occurrence pins (METRIC-04 pins for new v1.43 suppression metric tokens; SAFE-07 closeout guidance in canonical pin test).
- [x] 202-04: Documentation (v1.43-dev CHANGELOG; operator-facing suppression metric semantics docs; corrected active v1.43 fixture-path references).

**Note:** VALIDATION.md `status: approved` with `nyquist_compliant: false` and `reconstructed_from: SUMMARY+VERIFICATION` — validation artifact was reconstructed retroactively after phase already executed and passed (11/11 truths verified, full suite 4940 passed). Test coverage is in place; only the formal Nyquist contract artifact is partial. Operator accepted as-is at v1.43 close.

---

## Phase 203: Target-Edge Churn Instrumentation (OBSV)

**Goal:** Operators can read per-sample `load_rtt_delta_us` directly from soak NDJSON and a zone × cause-tag histogram from `soak-summary.json`, so future plans can read the target-edge distribution directly instead of inferring from RTT-integral and zone-trace surfaces.

**Depends on:** Phase 202 (cause-tag classification from METRIC-02 is the histogram breakdown axis; additive `/health` and soak-schema precedent established by SEED-002 is mirrored here).
**Requirements:** OBSV-05, OBSV-06, OBSV-07, OBSV-08, SAFE-07 (cross-cutting)
**Production canary:** Not required — soak-harness additive change; reads existing exposed `/health` fields and computes delta locally (per SEED-004 frontmatter).

**Success Criteria (all met):**
1. Soak NDJSON captures per-sample `load_rtt_delta_us` (= `effective_ul_load_rtt - baseline_rtt_ms` in microseconds) on every sample. ✓
2. `soak-summary.json` aggregates `load_rtt_delta_us` as histogram + p50/p95/p99/max over the soak window, broken down by zone (GREEN/YELLOW/SOFT_RED/RED) and by cause-tag (from Phase 202's METRIC-02). ✓
3. Golden-fixture replay test confirms the new field is populated and aggregated correctly against a known-good capture; SAFE-05 control-path pins remain unchanged (SAFE-07 verification). ✓
4. Soak harness README and `CHANGELOG.md` document the new field, the zone × cause-tag breakdown contract, and the no-control-path-change invariant. ✓

**Plans:**
- [x] 203-01: Soak capture script + projection test (canonical versioned soak-capture harness with v1.43 target-edge churn projection keys; capture-side projection contract test for synthesized `/health` payloads).
- [x] 203-02: Soak-summary aggregator + replay fixtures (stdlib soak-summary aggregator for `diagnostic_distribution.load_rtt_delta_us`; upload zone × cause attribution matrix; deterministic synthetic NDJSON + golden summary fixtures).
- [x] 203-03: Docs + SAFE-07 verification (operator-facing soak harness docs; CHANGELOG v1.43-dev entries; re-runnable SAFE-07 source-diff gate for `src/wanctl` invariance).

**Tech debt routed to v1.44 (non-blocking):**
- WR-01: `scripts/check-safe07-source-diff.sh` does not fail on uncommitted/staged `src/wanctl/` edits; verifiers compensated manually.
- WR-02: `scripts/soak-capture.sh` runs under `set -euo pipefail` with no per-row error bypass — a single transient `curl`/`jq` failure aborts a 24h soak.

---

## Phase 204: D-14 Successor Recalibration (CALIB)

**Goal:** A clean 24h Spectrum baseline soak under post-Plan-201-14 production yields a soak-calibrated D-14 successor threshold with explicit operator rationale, and a verification 24h soak passes the dual gate cleanly — closing the metric watchdog without any control-path change.

**Depends on:** Phase 202 (METRIC-01 must ship to production) AND Phase 203 (OBSV-05 must be live in the soak harness). Single 24h baseline soak fires only after both binary + harness changes are deployed.
**Requirements:** CALIB-01, CALIB-02, CALIB-03, CALIB-04, CALIB-05, SAFE-07 (cross-cutting)
**Production canary:** Required (two deploys, two-snapshot rollback per v1.42 Plan 201-15 / 201-16 pattern).

**Success Criteria (all met):**
1. 24h baseline soak on production Spectrum (cake-shaper) under post-Plan-201-14 binary + new metric live produces a representative completed-window suppression-count distribution (mean, p50, p95, p99, max) — basis for threshold derivation. ✓ (corrected-boundary rerun `20260509T183037Z` after Plan 204-07 boundary-marker remediation)
2. Operator-approved D-14 successor threshold recorded with explicit rationale in `204-CALIB-02-OPERATOR-APPROVAL.md`, referencing CALIB-01's distribution and explicitly tying the number to the post-fix control surface. ✓ (final threshold 175 in `scripts/calib_02_threshold.json`)
3. Soak harness watchdog computation now uses the completed-window count statistic; legacy live-counter-snapshot mean emitted alongside for one transition cycle, then dropped in a follow-up (v1.44). ✓
4. Verification 24h soak under the recalibrated threshold passes the dual gate cleanly: D-19 primary at 0 floor hits AND D-14-successor passes at the new threshold. ✓ (soak `20260512T004208Z`: `primary_gate.delta=0`, completed-window p99 dwell_hold `135.62 ≤ 175`, verdict `pass`)
5. RETRO captures threshold-basis hygiene as durable lesson: thresholds inherited from qualitative framing must be soak-calibrated against the actual post-fix control surface before they become gates. SAFE-05 control-path pins remain byte-identical at v1.43 close (SAFE-07 verification). ✓

**Plans:**
- [x] 204-01: Predeploy gate + Deploy 1 (v1.43.0 binary deployed on cake-shaper via Plan 201-15 two-snapshot rollback; postdeploy health evidence with METRIC-01 + OBSV-05 fields live).
- [x] 204-02: CALIB-01 baseline soak + distribution math (`aggregate_completed_window_distribution()`; 24h Spectrum capture and soak-summary.json distribution).
- [x] 204-03: CALIB-02 threshold + operator approval (operator-approved D-14 successor threshold triple; `scripts/calib_02_threshold.json` for Plan 204-04 watchdog loading; recorded open-Q2 decision to gate against `by_cause.dwell_hold`).
- [x] 204-04: CALIB-03 watchdog aggregator + Deploy 2 (dual-emission watchdog; v1.42 legacy oracle regression at `6.466842364880155 ± 1e-6`; harness-only Deploy 2 evidence; `scripts/soak-capture.sh` unchanged).
- [x] 204-05: CALIB-04 verification soak (pre-remediation; superseded by 204-09 after boundary-marker remediation).
- [x] 204-06: RETRO + SAFE-07 closeout (threshold-basis hygiene lesson; SAFE-07 closeout verification artifact; Nyquist validation closeout; v1.43 milestone shipped metadata; v1.44 follow-up TODO for `secondary_gate_legacy` removal).
- [x] 204-07: CALIB-01 rerun baseline (corrected-boundary CALIB-01 rerun `20260509T183037Z` under current aggregator; valid completed-window distribution with explicit `ul_hysteresis_window_start_epoch` boundary source).
- [x] 204-08: CALIB-02 reevaluation Branch B (material-change reevaluation; re-approved threshold `150` against `by_cause.dwell_hold` p99; updated `scripts/calib_02_threshold.json` reference to corrected-boundary CALIB-01 evidence).
- [x] 204-09: CALIB-04 rerun verification (corrected-boundary CALIB-04 rerun `20260510T203642Z` `FAIL-A` at `secondary_value=151.0` vs threshold 150; Branch A continuation `20260512T004208Z` `PASS` at threshold 175).
- [x] 204-10: Closeout refresh (Phase 204 verification refreshed to satisfied; gap-closure RETRO addendum for d44e2fd remediation cycle; REQUIREMENTS/ROADMAP/STATE/CHANGELOG closeout metadata; boundary-marker projection and watchdog fixture assertions aligned to threshold 175).

---

## Inherited Deferrals (carried into v1.43, untouched at close)

- **VALN-05b** — ATT cake-primary canary. Administratively deferred since v1.40; v1.39 closure flipped gating from technical to historical. Disposition unchanged at v1.43 boundary; resolution requires its own ADR. Tracked in `.planning/todos/pending/2026-04-24-resolve-att-cake-primary-canary-after-phase-196.md`.
- **SEED-001** — Spectrum topology-correct CAKE mode (920Mbit besteffort wash). Dormant; triggers on `cake_signal.py` / `EXCLUDED_PARAMS` / CAKE-mode work. Pulling SEED-001 into v1.43 would have confounded the D-14 evidence chain.
- **SEED-005** — Conservative UL tuning sweep (gated). Named for v1.44; structurally barred from v1.43 by SAFE-07 invariant. Prereqs (now met): METRIC-01 + OBSV-05 + CALIB-01 all live in production with clean baseline soak under CALIB-02's recalibrated threshold.

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions:**

- Phase order 002 → 004 → 003 (joint Claude + Codex `gpt-5.5 xhigh` scope decision 2026-05-06) — baseline-soak primitive sequencing over seed-priority order.
- D-14 successor threshold gated on `by_cause.dwell_hold.p99` (operator open-Q2 decision in Plan 204-03) rather than top-level p99 — isolates the YELLOW-edge dwell-hold path that drove the original Phase 201 FAIL.
- Final threshold 175 (Branch A) accepted over Branch B's 150 after `FAIL-A` verification soak `20260510T203642Z` `secondary_value=151.0` demonstrated insufficient headroom under the corrected-boundary distribution.
- Boundary-marker remediation cycle (Plans 204-07..10) classified as evidence-pipeline revalidation rather than rework — `d44e2fd` aggregator fix retroactively invalidated pre-fix soak summaries; corrected-boundary rerun produced valid distribution under current aggregator.
- SAFE-07 closeout invariant treated as cross-cutting hard requirement, verified at every phase boundary via `scripts/check-safe07-source-diff.sh` against `b72b463`.

**Issues Resolved:**

- D-14 secondary watchdog (Phase 201 inherited FAIL at `6.466842.../60s mean` vs `<5.0`) closed via soak-calibrated completed-window p99 dwell-hold gate at 175.
- Metric-semantics confusion (`suppressions_per_min` 60s reset counter sampled as rate) resolved by additive completed-window counter contract; `suppressions_per_min` preserved untouched for backward compat.
- Target-edge churn evidence opacity closed via per-sample `load_rtt_delta_us` in NDJSON + zone × cause-tag histogram aggregation.
- Threshold-basis hygiene gap surfaced and documented as durable RETRO lesson: thresholds inherited from qualitative framing must be soak-calibrated against the actual post-fix control surface before they become gates.

**Issues Deferred (routed to v1.44):**

- WR-01: `scripts/check-safe07-source-diff.sh` does not fail on uncommitted/staged `src/wanctl/` edits; verifiers compensated manually.
- WR-02: `scripts/soak-capture.sh` aborts a 24h soak on a single transient curl/jq failure (`set -euo pipefail` with no per-row bypass).
- SEED-005: Conservative UL tuning sweep — prereqs now met; structurally named for v1.44.
- `secondary_gate_legacy` block removal (Plan 204-06 TODO) — emit-both transition cycle; drop legacy block in v1.44.
- CALIB-02 YAML-promotion evaluation (Plan 204-06 TODO) — promote threshold from soak-harness constant to YAML config key after v1.44 validation.

**Technical Debt Incurred:**

- Phase 202 VALIDATION.md `nyquist_compliant: false`, reconstructed retroactively from SUMMARY+VERIFICATION; test coverage in place, only contract artifact partial. Operator accepted at v1.43 close. Optional backfill via `/gsd-validate-phase 202`.

---

_For current project status, see `.planning/ROADMAP.md`._
