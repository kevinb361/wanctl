# v1.42 Requirements (ARCHIVED)

**Archived:** 2026-05-06
**Audit:** Skipped — Route B closure decision (D-19 PASS, D-14 deferred to v1.43+) is on the record in `201-RETRO.md` and `201-VERIFICATION.md` `closure_route` block. Re-auditing would re-discover the documented operator-accepted gap.
**Closure mode:** `gaps_found` via operator Route B 2026-05-06 — D-19 primary VALN-06 floor-hit gate PASS shipped on v1.42.1; D-14 secondary suppression watchdog deferred to v1.43+ as `metric_semantics_and_recalibration`.

---

## v1.42 Requirements

v1.42 inherited a single blocking requirement from v1.41 Phase 200 — there is no separate v1.42 requirements block. The traceability row below is preserved verbatim from `REQUIREMENTS.md` v1.41 traceability (which carried the partial-closure trail through v1.41 archive).

### Verification (VALN)

- [x] **VALN-06** (inherited blocking from v1.41 Phase 200): Spectrum UL saturation gate — a 10–15 min `iperf3 -P4` saturated upload loop at the deployed UL ceiling does not collapse to the UL floor in any cycle. Verified pre/post idle baselines bookend the run. This is the deploy-gate acceptance test; it must pass before the binary is promoted to `/opt/wanctl`. A 24-hour Spectrum regression soak runs after as a watchdog, not as the verdict.
  - **Outcome:** CLOSED via Route B 2026-05-06.
  - **D-19 primary gate (PASS):** recanary `20260505T122513Z` `verdict=pass`, `primary_gate_value=0`, `ul_floor_hits_during_load=0`. 24h soak `20260505T132736Z` floor-hit delta `0` on production v1.42.1. Phase-goal control behavior achieved.
  - **D-14 secondary watchdog (FAIL → deferred):** `ul_hysteresis_suppression_rate_per_60s_mean=6.466842364880155` vs `<5.0`. FAIL on the YELLOW-edge dwell-hold path (`queue_controller.py:348`), not the bounded RED decay path Plan 201-14 fixed (`queue_controller.py:361-376`). Original threshold never soak-calibrated against the post-fix control surface. Classified `metric_semantics_and_recalibration`; successor work deferred to v1.43 as `SEED-002`..`SEED-005`.

## Out of Scope (locked from PLAN-time, preserved at archive)

| Feature | Reason |
|---------|--------|
| Modem SNMP / DOCSIS HCS counters | New transport + firmware dependency; RTT-integral + CAKE corroborator sufficient on available signals (D-05) |
| Live-tunable DOCSIS-mode keys via SIGUSR1 | Restart-required for control-path stability (D-08) |
| Global default for `setpoint_mbps` | Setpoint is link-specific; principled defaults not knowable without per-link evidence (D-07) |
| `setpoint_pct` companion key | Rejected to keep validator + ordering surface minimal (D-07) |
| ATT, fiber, DSL, and other non-DOCSIS YAMLs | Absence of `docsis_mode` preserves legacy 3-state UL behavior verbatim; no edits (D-17) |
| Tightening soak watchdog below `<5/60s` | Premature before zero-floor-hit canary first proves the mode (D-14) |
| Per-direction DL state-machine changes | DL is healthy; v1.42 is UL-only (same scope discipline as v1.41) |

## Traceability (final, archive-time)

| Requirement | Phase | Final Status | Evidence |
|-------------|-------|--------------|----------|
| VALN-06 | Phase 201 | Closed (gaps_found, Route B) — D-19 PASS / D-14 deferred to v1.43 | recanary `20260505T122513Z` PASS; soak `20260505T132736Z` D-19 PASS / D-14 FAIL; `201-RETRO.md`; `201-VERIFICATION.md` `closure_route`; `SEED-002..SEED-005` |

## Cross-Milestone Carryover (forward to v1.43)

- **D-14 suppression watchdog recalibration** — four ordered backlog seeds (`SEED-002` through `SEED-005`) carry the metric-semantics + recalibration work forward to v1.43.
- **VALN-05b** ATT cake-primary canary — administratively deferred since v1.40; v1.39 closure converted gating phrase from technical to historical.

---
*v1.42 phase scope opened: 2026-05-04*
*v1.42 archived: 2026-05-06*
