---
phase: 237-hil-failure-injection-harness-closeout
plan: 04
type: execute
wave: 4
depends_on: ["237-01", "237-02", "237-03"]
files_modified:
  - .gitignore
  - .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json
  - .planning/phases/237-hil-failure-injection-harness-closeout/237-04-SUMMARY.md
autonomous: false
requirements: [SAFE-16, HARN-05]
must_haves:
  truths:
    - "tests/silicom/ runtime result artifacts are gitignored (ephemeral HIL output, redaction-safe by default)"
    - "SAFE-16 protected-path zero-diff is proven at the 237 phase boundary against v1.51 (protected controller paths + configs/att.yaml only — NOT a global worktree-clean claim)"
    - "A separate git status --short report records overall worktree state at milestone close, distinct from the SAFE protected-path fields"
    - "Operator confirms the close-time SAFE-16 evidence before milestone close"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rule for ephemeral tests/silicom/ HIL result dirs"
      contains: "tests/silicom/"
    - path: ".planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json"
      provides: "SAFE-16 phase-boundary + milestone-close evidence (controller_path_diff_count: 0; dirty_tree_clean scopes to protected paths + configs/att.yaml only)"
      contains: "controller_path_diff_count"
    - path: ".planning/phases/237-hil-failure-injection-harness-closeout/237-04-SUMMARY.md"
      provides: "Milestone closeout summary + operator SAFE-16 sign-off record"
      contains: "approved"
  key_links:
    - from: "scripts/phase237-safe16-boundary-check.sh"
      to: ".planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json"
      via: "tool run at phase boundary + close, anchor v1.51"
      pattern: "safe16-boundary-237"
---

<objective>
Close the phase: (1) resolve RESEARCH Open Question 1 by `.gitignore`-ing `tests/silicom/` (ephemeral HIL result dirs — the recommended default; redaction is already enforced by `phase213-steering-snapshot.sh`, but gitignore is the belt-and-suspenders against IP/secret leak per CLAUDE.md public-safe), and (2) run the SAFE-16 boundary-check tool at the 237 phase boundary, proving PROTECTED controller-path zero-diff against `v1.51`, record a SEPARATE overall `git status --short` report, then have the operator confirm the close-time evidence — the 10th consecutive milestone holding the SAFE-07..15 discipline.

Purpose: SAFE-16 is the milestone-close invariant. The harness must not have nudged a single PROTECTED controller-path byte. Note the precise scope: the SAFE tool's `dirty_tree_clean` field covers ONLY the protected controller paths + `configs/att.yaml` — it does NOT assert the whole worktree is clean. The separate `git status --short` report captures overall worktree state so protected-path cleanliness is never mistaken for global cleanliness. The gitignore prevents runtime HIL artifacts from leaking sensitive state into the tracked tree.
Output: `.gitignore` rule, `evidence/safe16-boundary-237.json` (passing), a `git status --short` snapshot recorded in the SUMMARY, operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md

@scripts/phase237-safe16-boundary-check.sh
@.gitignore

<interfaces>
<!-- SAFE-16 evidence contract (schema proven in safe16-boundary-236.json). -->

scripts/phase237-safe16-boundary-check.sh --anchor v1.51 emits:
  .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json
Required asserts: passed == true, controller_path_diff_count == 0, att_config_diff_count == 0,
  dirty_tree_clean == true, all per_file_sha256_equal true,
  baseline_commit == 531f36ac36ceccb2e4dd2d47edd84fba9081c053 (git rev-parse v1.51).
SCOPE: dirty_tree_clean covers ONLY the protected controller paths + configs/att.yaml. It is NOT a
  global worktree-clean signal. Overall worktree state is reported SEPARATELY via `git status --short`.
Posture: read-only git inspection that EMITS an evidence JSON (no worktree/index/ref mutation).

Open Question 1 resolution: .gitignore tests/silicom/ (ephemeral). Operator-significant
runs may opt-in to committing a REDACTED result.json under the phase evidence dir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gitignore ephemeral HIL result dirs + run SAFE-16 boundary proof + separate worktree report</name>
  <read_first>
    - .gitignore (append target — confirm no existing tests/silicom rule)
    - scripts/phase237-safe16-boundary-check.sh (the SAFE-16 tool from Plan 01 — note dirty_tree_clean scopes to protected paths + configs/att.yaml only)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md (Open Question 1; Pitfall 5 anchor-drift)
    - .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json (schema/asserts to match)
  </read_first>
  <files>.gitignore, .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json</files>
  <action>Append a `tests/silicom/` ignore rule to .gitignore (ephemeral HIL result dirs; redaction-safe default per RESEARCH Open Question 1) with a one-line comment noting redacted result.json summaries may be opt-in committed under the phase evidence dir. Confirm no existing rule already covers it (avoid a duplicate). Then run `bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51` to regenerate evidence/safe16-boundary-237.json at the current phase-boundary HEAD (after Plans 01-03 landed). This is read-only git inspection that EMITS the evidence JSON — it must not mutate the worktree/index/refs. SEPARATELY capture `git status --short` output and `git diff --stat v1.51..HEAD -- src/wanctl configs/att.yaml` and record both in the 237-04-SUMMARY.md (the SAFE JSON dirty_tree_clean field covers ONLY protected paths + configs/att.yaml; the git status --short snapshot is the distinct overall-worktree record — do NOT conflate them). Do NOT touch any src/wanctl file. If the boundary check reports a non-zero controller_path_diff_count OR the v1.51..HEAD protected-path diff is non-empty, STOP — a controller-path file was touched and must be reverted before proceeding (this is the SAFE-16 violation guard).</action>
  <verify>
    <automated>grep -q "tests/silicom/" .gitignore && bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51 && python3 -c "import json; d=json.load(open('.planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json')); assert d['passed'] is True; assert d['controller_path_diff_count']==0; assert d['att_config_diff_count']==0; assert d['dirty_tree_clean'] is True; assert d['baseline_commit']=='531f36ac36ceccb2e4dd2d47edd84fba9081c053'; print('SAFE-16 boundary OK')" && git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains a tests/silicom/ rule with an explanatory comment
    - evidence/safe16-boundary-237.json asserts passed==true, controller_path_diff_count==0, att_config_diff_count==0, dirty_tree_clean==true, baseline_commit==531f36ac...
    - all per_file_sha256_equal values are true
    - SUMMARY records a SEPARATE `git status --short` snapshot AND the `git diff --stat v1.51..HEAD -- src/wanctl configs/att.yaml` result, explicitly distinct from the SAFE JSON dirty_tree_clean field (protected-path cleanliness is NOT presented as global worktree cleanliness)
    - git status shows tests/silicom/ runtime artifacts are ignored (a stray run dir does not appear as untracked)
    - `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check; also the milestone-boundary proof)
  </acceptance_criteria>
  <done>HIL artifacts gitignored; SAFE-16 phase-boundary evidence passes at v1.51 with zero PROTECTED controller-path diff; a separate git status --short worktree snapshot is recorded distinct from the SAFE protected-path fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SAFE-16 milestone-close operator sign-off</name>
  <what-built>
    The full Phase 237 HIL harness (silicom-test failover/ab-cake/chaos with separate EXIT and INT/TERM restore handlers), the DEPLOY-03 single documented standalone deploy path (harness + scenarios + phase213 capture helpers), and the SAFE-16 phase-boundary zero-diff evidence at v1.51 (controller_path_diff_count: 0). This is the v1.52 milestone-close gate — the 10th consecutive milestone holding the SAFE-07..15 controller-path zero-diff discipline.
  </what-built>
  <how-to-verify>
    1. Review the SAFE-16 evidence: `cat .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json` — confirm passed=true, controller_path_diff_count=0, baseline_commit matches `git rev-parse v1.51`. Note `dirty_tree_clean` here means protected-paths + configs/att.yaml clean, NOT global worktree clean.
    2. Confirm zero PROTECTED controller-path drift across the milestone: `git diff --stat v1.51..HEAD -- src/wanctl/ configs/att.yaml` should be empty.
    3. Review the SEPARATE overall worktree snapshot recorded in the SUMMARY (`git status --short`) — distinct from the SAFE JSON fields.
    4. (Optional, on cake-shaper) Run a live spec-modem failover behind the gate: `SILICOM_TEST_LIVE_CONFIRM=1 silicom-test failover spec-modem`, then `silicom-bypass status all` to confirm all pairs returned to NIC mode. Inspect the result dir under tests/silicom/.
    5. Confirm HARN-04 safety: review that test_restore_on_midrun_failure and test_restore_on_signal (signal-derived EXIT) are green in the suite.
  </how-to-verify>
  <resume-signal>Type "approved" to record SAFE-16 milestone-close sign-off, or describe issues to address.</resume-signal>
  <action>Verification-only checkpoint — no mutation. Present the SAFE-16 close-time evidence, the empty protected-path milestone diff, AND the separate git status --short worktree snapshot to the operator and wait for explicit approval before milestone close. Record the sign-off (operator name + UTC timestamp) and the git status --short snapshot in the plan SUMMARY.</action>
  <verify><human-check>Operator types "approved" after confirming controller_path_diff_count=0, empty v1.51..HEAD src/wanctl + configs/att.yaml diff, and reviewing the separate git status --short snapshot.</human-check></verify>
  <done>Operator sign-off recorded in 237-04-SUMMARY.md, alongside the separate git status --short worktree snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git worktree -> SAFE-16 evidence | read-only inspection that emits an evidence JSON; must not mutate tree/index/refs |
| runtime HIL output -> tracked repo tree | result dirs may contain state JSON; must not leak into git |
| protected-path cleanliness -> global cleanliness claim | dirty_tree_clean scopes to protected paths + configs/att.yaml only; separate git status --short reports overall state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-14 | Spoofing | wrong anchor producing a false SAFE-16 pass | mitigate | Pin v1.51; assert baseline_commit==531f36ac... in verify; operator confirms at close |
| T-237-15 | Information Disclosure | runtime state JSON leaking into tracked tree | mitigate | .gitignore tests/silicom/; redaction already enforced by phase213-steering-snapshot.sh; opt-in commit only redacted summaries |
| T-237-16 | Tampering | a controller-path file silently modified during the phase | mitigate | SAFE-16 tool fails closed on any controller_path_diff_count>0; per-wave + close `git diff --exit-code v1.51..HEAD` guard; task STOPs on non-zero; operator gate |
| T-237-21 | Repudiation / overclaim | reading protected-path cleanliness as global worktree cleanliness | mitigate | dirty_tree_clean scope documented; separate git status --short snapshot recorded distinct from SAFE JSON fields |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- .gitignore covers tests/silicom/
- SAFE-16 evidence passes at v1.51 (controller_path_diff_count: 0, dirty_tree_clean: true — protected-path scope)
- Separate git status --short worktree snapshot recorded in SUMMARY, distinct from SAFE JSON fields
- `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave + milestone-boundary SAFE-16 check)
- Operator records milestone-close sign-off
</verification>

<success_criteria>
- SAFE-16 PROTECTED-path zero-diff proven at phase boundary AND confirmed at milestone close (10th consecutive)
- protected-path cleanliness NOT overstated as global worktree cleanliness; separate git status --short recorded
- Ephemeral HIL artifacts gitignored; no state/secret leak into tracked tree
- Operator sign-off recorded
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-04-SUMMARY.md` when done
</output>
</content>
</invoke>
