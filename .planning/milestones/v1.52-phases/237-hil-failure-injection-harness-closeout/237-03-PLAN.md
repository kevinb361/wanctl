---
phase: 237-hil-failure-injection-harness-closeout
plan: 03
type: execute
wave: 3
depends_on: ["237-02"]
files_modified:
  - scripts/deploy.sh
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
autonomous: true
requirements: [DEPLOY-03]
must_haves:
  truths:
    - "deploy.sh --silicom-bypass-only installs silicom-test + scenario files + BOTH phase213 capture helpers (to /usr/local/libexec/wanctl/) via the existing standalone path"
    - "test_artifacts_repo_owned asserts silicom-test, scenarios, AND the phase213 capture helpers are repo-owned and referenced by deploy.sh"
    - "deploy_silicom_bypass wraps its remote_tmp cleanup in an EXIT trap so a mid-deploy failure still cleans the remote staging dir"
    - "docs/SILICOM-BYPASS.md documents the harness + its runtime deps as part of the single standalone deploy path + operator live gates"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "deploy_silicom_bypass extended to stage+install silicom-test + scenarios + phase213 capture helpers (atomic, off-by-default, trap-cleaned remote_tmp)"
      contains: "silicom-test"
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "DEPLOY-03 artifact-ownership assertions for harness + capture-helper artifacts"
      contains: "silicom-test"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "Documented deploy + operator-gated harness procedure"
      contains: "silicom-test"
  key_links:
    - from: "scripts/deploy.sh"
      to: "scripts/silicom-test"
      via: "scp + sudo install in deploy_silicom_bypass"
      pattern: "scripts/silicom-test"
    - from: "scripts/deploy.sh"
      to: "/usr/local/libexec/wanctl/phase213-steering-snapshot.sh"
      via: "scp + sudo install of the capture helpers the harness defaults to"
      pattern: "phase213-steering-snapshot"
    - from: "tests/test_silicom_bypass_cli.py"
      to: "scripts/deploy.sh"
      via: "test_artifacts_repo_owned parses $PROJECT_ROOT refs from deploy body"
      pattern: "silicom-test"
---

<objective>
Finalize DEPLOY-03: make ALL bypass tooling the deployed harness depends on — the new `silicom-test` harness, its scenario files, AND the two `phase213-*` capture helpers it defaults to at runtime — repo-owned and deployable via the single documented standalone path. Per the RESEARCH DEPLOY-03 decision (and SEED-006 open question), REUSE the existing `deploy.sh --silicom-bypass-only` standalone mode (true-standalone, atomic, off-by-default) rather than a separate installer. Extend the deploy function, extend the artifact-ownership tests, and document the harness in the existing standalone-install + operator-gated sections of `docs/SILICOM-BYPASS.md`.

Purpose: A single, tested, documented deploy path is the DEPLOY-03 contract. Critically, the deployed harness defaults its snapshot/poller seams to `/usr/local/libexec/wanctl/phase213-*.sh`; if deploy.sh does not install those helpers, the harness running outside the repo CWD cannot capture state (HARN-05 fragile/unusable). This plan closes that gap.
Output: extended `deploy_silicom_bypass` (harness + scenarios + capture helpers, trap-cleaned remote_tmp), new ownership assertions, updated docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md

@scripts/deploy.sh
@scripts/phase213-steering-snapshot.sh
@scripts/phase213-health-poller.sh
@tests/test_silicom_bypass_cli.py
@docs/SILICOM-BYPASS.md

<interfaces>
<!-- Exact analog identifiers to extend (from 237-PATTERNS.md + read of deploy.sh/tests). -->

deploy.sh: deploy_silicom_bypass() (the standalone install fn) uses per-deploy private staging:
  remote_tmp=$(ssh ... mktemp -d); chmod 700; scp "$PROJECT_ROOT/scripts/<x>" ...;
  ssh ... sudo install -o root -g root -m 0755 ... /usr/local/sbin/<x>; ... rm -rf "$remote_tmp"
  It ALREADY calls deploy_watchdog_artifacts() internally, which has its OWN mktemp -d staging dir
  (pre-existing and fine — do NOT try to collapse it). Ends with `systemctl daemon-reload` only;
  enables/starts NOTHING. print_silicom_bypass_plan (dry-run) must list ALL new artifacts.

tests/test_silicom_bypass_cli.py:
  SILICOM_BYPASS_ARTIFACTS set (~lines 26-37) — add scripts/silicom-test, scenario file(s),
    AND scripts/phase213-steering-snapshot.sh + scripts/phase213-health-poller.sh.
  test_artifacts_repo_owned() (~lines 1323-1351) parses $PROJECT_ROOT/<path> refs out of the deploy
  function body and asserts each exists in repo; new entries MUST appear in the deploy body to be picked up.
  Standalone guards that must stay green: test_deploy_has_silicom_standalone_mode,
  test_silicom_standalone_short_circuits, test_silicom_deploy_uses_private_atomic_staging,
  test_deploy_watchdog_off_by_default.

docs/SILICOM-BYPASS.md: "Standalone install" section (~lines 102-124) bullet list of what
  --silicom-bypass-only installs; "Operator-gated live procedures" section (~line 97).
  Public-safe: no IPs/hostnames beyond existing cake-shaper and 127.0.0.1.

Install targets:
  /usr/local/sbin/silicom-test (0755)
  /usr/local/share/silicom-test-scenarios/*.sh (0644) — matches SILICOM_TEST_SCENARIO_DIR default
  /usr/local/libexec/wanctl/phase213-steering-snapshot.sh (0755) — matches SILICOM_TEST_SNAPSHOT default
  /usr/local/libexec/wanctl/phase213-health-poller.sh (0755) — matches SILICOM_TEST_HEALTH_POLLER default
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deploy_silicom_bypass to install silicom-test + scenarios + phase213 capture helpers, with trap-cleaned remote_tmp</name>
  <read_first>
    - scripts/deploy.sh (deploy_silicom_bypass ~574-617, including the nested deploy_watchdog_artifacts call with its own mktemp; the standalone --silicom-bypass-only handler ~865-898; print_silicom_bypass_plan dry-run path)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "scripts/deploy.sh" — atomic-install idiom to copy per artifact)
    - scripts/silicom-test, scripts/silicom-test-scenarios/ (the harness artifacts to install — from Plan 02)
    - scripts/phase213-steering-snapshot.sh, scripts/phase213-health-poller.sh (the capture helpers the harness defaults to — install to /usr/local/libexec/wanctl/)
  </read_first>
  <files>scripts/deploy.sh</files>
  <action>In deploy_silicom_bypass(), add staging+install for FOUR artifact groups using the SAME per-deploy private idiom already in the function (reuse the existing top-level remote_tmp; do NOT add a NEW dedicated mktemp for the harness — the nested deploy_watchdog_artifacts mktemp is pre-existing and stays):
  1. scp "$PROJECT_ROOT/scripts/silicom-test" into remote_tmp, then sudo install -o root -g root -m 0755 to /usr/local/sbin/silicom-test.
  2. install each scripts/silicom-test-scenarios/*.sh into /usr/local/share/silicom-test-scenarios/ at mode 0644 (sudo install -d the dir first), matching SILICOM_TEST_SCENARIO_DIR.
  3. sudo install -d /usr/local/libexec/wanctl, then scp+install scripts/phase213-steering-snapshot.sh to /usr/local/libexec/wanctl/phase213-steering-snapshot.sh at 0755 (matches SILICOM_TEST_SNAPSHOT default).
  4. scp+install scripts/phase213-health-poller.sh to /usr/local/libexec/wanctl/phase213-health-poller.sh at 0755 (matches SILICOM_TEST_HEALTH_POLLER default).
Echo each destination. Wrap the remote_tmp cleanup in an EXIT trap scoped to deploy_silicom_bypass: register `trap 'ssh "$TARGET_HOST" "rm -rf '"'"'$remote_tmp'"'"'" 2>/dev/null || true' RETURN` (or an equivalent function-scoped cleanup) right after remote_tmp is created so a mid-deploy failure (failed scp/ssh/install) still cleans the remote staging dir; keep the existing trailing rm -rf as the success-path cleanup (idempotent). Keep this change SCOPED — do not refactor the rest of the function or the nested watchdog deploy. Keep the function ending at systemctl daemon-reload only — enable/start NOTHING (preserve off-by-default posture). Add ALL FOUR artifact groups (harness, scenarios, both phase213 helpers) to print_silicom_bypass_plan dry-run output so --dry-run lists every runtime dependency. Do NOT touch any src/wanctl file. Do NOT change the standalone short-circuit logic or the WAN-flag rejection.</action>
  <verify>
    <automated>shellcheck scripts/deploy.sh && bash -n scripts/deploy.sh && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k "standalone or watchdog_off or atomic_staging or short_circuit" -q && git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml</automated>
  </verify>
  <acceptance_criteria>
    - shellcheck + bash -n clean on scripts/deploy.sh
    - deploy_silicom_bypass body contains scripts/silicom-test, the scenario dir install, AND both phase213 capture helpers installed to /usr/local/libexec/wanctl/
    - remote_tmp cleanup is wrapped in a function-scoped EXIT/RETURN trap (mid-deploy failure still cleans remote staging); the success-path rm -rf remains
    - No NEW dedicated mktemp added for the harness — the harness + scenarios + capture helpers reuse the existing silicom-bypass staging dir (the pre-existing nested deploy_watchdog_artifacts mktemp is unchanged and acceptable)
    - Existing standalone guards stay green: test_deploy_has_silicom_standalone_mode, test_silicom_standalone_short_circuits, test_silicom_deploy_uses_private_atomic_staging, test_deploy_watchdog_off_by_default
    - Function still ends with daemon-reload only; no systemctl enable/start added
    - print_silicom_bypass_plan dry-run lists ALL four runtime deps (harness, scenarios, phase213-steering-snapshot, phase213-health-poller)
    - `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check)
  </acceptance_criteria>
  <done>Standalone deploy installs the harness + scenarios + both capture helpers atomically and off-by-default, with trap-cleaned remote staging and full dry-run coverage; existing standalone guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEPLOY-03 artifact-ownership assertions (incl. capture helpers + dir-membership) + docs</name>
  <read_first>
    - tests/test_silicom_bypass_cli.py (SILICOM_BYPASS_ARTIFACTS set ~26-37; test_artifacts_repo_owned ~1323-1351)
    - docs/SILICOM-BYPASS.md (Standalone install ~102-124; Operator-gated live procedures ~97)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (sections "tests/test_silicom_bypass_cli.py" + "docs/SILICOM-BYPASS.md")
    - scripts/deploy.sh (the deploy body Task 1 just extended — the test parses $PROJECT_ROOT refs from it)
  </read_first>
  <files>tests/test_silicom_bypass_cli.py, docs/SILICOM-BYPASS.md</files>
  <behavior>
    - test_artifacts_repo_owned: parsed $PROJECT_ROOT refs from deploy body now include scripts/silicom-test AND scripts/phase213-steering-snapshot.sh AND scripts/phase213-health-poller.sh; every SILICOM_BYPASS_ARTIFACTS entry exists in repo
    - scenario dir-membership: if scenarios install via a dir/glob loop rather than per-file scp, a dir-membership assertion proves the scenario dir is repo-owned and wired into deploy.sh (the per-file parser must not silently miss glob-installed scenarios)
    - DEPLOY-03 coverage: harness + at least one scenario file + both capture helpers asserted repo-owned and referenced by deploy.sh
  </behavior>
  <action>Add scripts/silicom-test, the scenario file(s) (scripts/silicom-test-scenarios/cake-ab-spectrum.sh, scripts/silicom-test-scenarios/failover-spectrum.sh), AND both capture helpers (scripts/phase213-steering-snapshot.sh, scripts/phase213-health-poller.sh) to the SILICOM_BYPASS_ARTIFACTS set in tests/test_silicom_bypass_cli.py so test_artifacts_repo_owned asserts they exist AND are referenced from the deploy body. If scenarios are installed via a glob/dir loop rather than per-file scp (so the per-file `$PROJECT_ROOT/...` parser would miss them), add an explicit dir-membership assertion: prove scripts/silicom-test-scenarios/ is repo-owned AND that deploy.sh references the scenario dir (e.g. the install target /usr/local/share/silicom-test-scenarios and the source dir), and assert each repo scenario file exists. In docs/SILICOM-BYPASS.md: extend the "Standalone install" bullet list to add /usr/local/sbin/silicom-test, /usr/local/share/silicom-test-scenarios/, and /usr/local/libexec/wanctl/phase213-steering-snapshot.sh + phase213-health-poller.sh (note these are the harness's runtime capture deps); add a short "Failure-injection harness" subsection under "Operator-gated live procedures" documenting the explicit live-WAN gate (SILICOM_TEST_LIVE_CONFIRM), default-safe spec-modem pair, the louder att-modem gate (SILICOM_TEST_ATT_CONFIRM), and the tests/silicom/<ts>-<scenario>/ result layout. Keep it public-safe (only cake-shaper + 127.0.0.1 literals). Do NOT touch src/wanctl.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -k "deploy or repo_owned or artifacts" -q && python3 -c "t=open('docs/SILICOM-BYPASS.md').read(); assert 'silicom-test' in t and 'spec-modem' in t and 'SILICOM_TEST_ATT_CONFIRM' in t and 'phase213-steering-snapshot' in t; print('docs OK')" && git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml</automated>
  </verify>
  <acceptance_criteria>
    - test_artifacts_repo_owned green with scripts/silicom-test, scenario file(s), AND both phase213 capture helpers in SILICOM_BYPASS_ARTIFACTS
    - If scenarios install via glob/dir loop, a dir-membership assertion proves the scenario dir is repo-owned and wired into deploy.sh (no silent miss)
    - Full tests/test_silicom_bypass_cli.py suite stays green (W-INV -k invariant included)
    - docs/SILICOM-BYPASS.md mentions silicom-test, spec-modem default-safe pair, SILICOM_TEST_LIVE_CONFIRM and SILICOM_TEST_ATT_CONFIRM gates, the phase213 capture helpers as runtime deps, and tests/silicom/ result layout
    - docs contain no IPs/hostnames beyond cake-shaper and 127.0.0.1
    - `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check)
  </acceptance_criteria>
  <done>Harness + scenarios + capture helpers proven repo-owned + deploy-referenced (with dir-membership coverage for glob-installed scenarios); the single documented standalone path covers them; docs describe both operator gates and the result layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev repo -> cake-shaper root install | scp + sudo install crosses host + privilege boundary |
| deploy mode selection -> wanctl release path | standalone must NOT trigger the wanctl restart path |
| mid-deploy failure -> remote staging dir | a failed scp/ssh/install must not leak the remote tmp dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-10 | Tampering | non-atomic install leaving a partial harness | mitigate | Reuse the proven per-deploy mktemp + chmod 700 + atomic `sudo install` idiom; staged then installed |
| T-237-11 | Elevation of Privilege | deploy enabling/starting a unit unexpectedly | mitigate | Preserve off-by-default posture: function ends at daemon-reload only; test_deploy_watchdog_off_by_default stays green |
| T-237-12 | Tampering | standalone mode bleeding into wanctl release cadence | mitigate | Do not change the short-circuit; test_silicom_standalone_short_circuits stays green |
| T-237-13 | Information Disclosure | docs leaking private host/IP detail | mitigate | Public-safe doc rule (CLAUDE.md): only cake-shaper + 127.0.0.1 literals |
| T-237-19 | DoS | deployed harness unusable outside repo CWD (missing capture helpers) | mitigate | Install phase213-steering-snapshot.sh + phase213-health-poller.sh to /usr/local/libexec/wanctl/ matching the harness absolute defaults; ownership-tested |
| T-237-20 | Information Disclosure / Tampering | leaked remote staging dir on mid-deploy failure | mitigate | Function-scoped EXIT/RETURN trap cleans remote_tmp even on failure |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- shellcheck + bash -n clean on scripts/deploy.sh
- Existing standalone/off-by-default guards green
- test_artifacts_repo_owned green with harness + scenarios + both capture helpers (dir-membership for glob installs)
- print_silicom_bypass_plan dry-run lists every runtime dependency
- remote_tmp cleanup is trap-guarded (mid-deploy failure cleans staging)
- Full tests/test_silicom_bypass_cli.py suite green (incl. -k invariant)
- docs public-safe, mention harness + both operator gates + capture deps + result layout
- `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check)
</verification>

<success_criteria>
- All bypass tooling (CLI, watchdog units, boot service, harness, scenarios, phase213 capture helpers) repo-owned + deployable via one documented standalone path (DEPLOY-03)
- Deployed harness can capture state outside repo CWD (capture helpers installed at the absolute default paths)
- Off-by-default posture preserved; standalone decoupling intact; remote staging trap-cleaned
- Zero controller-path mutation (per-wave SAFE-16 diff clean)
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-03-SUMMARY.md` when done
</output>
</content>
</invoke>
