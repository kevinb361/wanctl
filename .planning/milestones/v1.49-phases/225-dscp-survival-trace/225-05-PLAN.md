---
phase: 225
plan: "05"
title: "Gap closure: refresh SAFE-13 boundary record at the final phase HEAD"
wave: 2
gap_closure: true
depends_on:
  - "225-04"
requirements:
  - SAFE-13
files_modified:
  - .planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json
autonomous: true
must_haves:
  truths:
    - "Closes GAP-4 (truth #9, partial): the committed `evidence/safe13-boundary-check.json` records `head_commit` equal to the ACTUAL final phase-boundary HEAD — i.e. after the 225-04 capture-script fixes are committed — not the stale `baa9b4b`. This plan runs LAST so the stamped HEAD is the true final boundary; if it ran before 225-04 landed it would go stale again."
    - "The refreshed record still PASSES the SAFE-13 invariant at full SAFE-12 standard: `passed=true`, `controller_path_diff_count=0`, `att_config_diff_count=0`, `dirty_tree_clean=true`, and every `per_file_sha256_equal` entry true vs the `v1.48` anchor. The gap fixes touched only `scripts/phase225-dscp-ingress-capture.sh` (a diagnostic script, NOT a controller-path file), so the controller-path/ATT zero-diff invariant must remain intact — if it does NOT, that is a real SAFE-13 violation and the plan must STOP and surface it rather than overwrite."
    - "No controller-path source is modified by this plan: it only re-runs the existing read-only `scripts/phase225-safe13-boundary-check.sh` and commits its regenerated JSON output. The boundary script itself is unchanged."
  artifacts:
    - ".planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json — regenerated with head_commit == final phase HEAD, passed=true."
  key_links:
    - from: "scripts/phase225-safe13-boundary-check.sh"
      to: "evidence/safe13-boundary-check.json"
      via: "default --out path, re-run after 225-04 commits"
      pattern: "head_commit"
---

<objective>
Refresh the SAFE-13 boundary record so the committed `safe13-boundary-check.json` stamps the TRUE
final phase-boundary HEAD. Verification found the committed record stale (`head_commit=baa9b4b` while
HEAD advanced); the invariant itself passes at current HEAD. This plan re-runs the existing read-only
boundary check AFTER the 225-04 capture-script fixes are committed, so the stamped HEAD is final and
will not go stale again.

Purpose: a committed boundary artifact that actually represents the phase's final HEAD with the
controller-path/ATT zero-diff invariant proven.
Output: regenerated `evidence/safe13-boundary-check.json`.

SEQUENCING: this is the LAST plan of the gap closure. It depends on 225-04 (the capture-script fixes)
being committed first, so it stamps the real final boundary HEAD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/225-dscp-survival-trace/225-VERIFICATION.md
@.planning/phases/225-dscp-survival-trace/225-03-PLAN.md

# The reusable read-only boundary check (unchanged by this plan) and the stale artifact it regenerates:
@scripts/phase225-safe13-boundary-check.sh
@.planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json
</context>

<tasks>

<task id="225-05-1" type="auto">
<name>Task 1: Re-run the SAFE-13 boundary check and commit the refreshed JSON at the final HEAD</name>
<files>.planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json</files>
<read_first>
- scripts/phase225-safe13-boundary-check.sh   (the read-only check being re-run; default --anchor v1.48, default --out is the committed JSON)
- .planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json   (current stale artifact — head_commit baa9b4b)
- .planning/phases/225-dscp-survival-trace/225-VERIFICATION.md   (gap 4 — truth #9 partial; final-boundary HEAD requirement)
- .planning/phases/225-dscp-survival-trace/225-03-PLAN.md   (225-03-2 — original SAFE-13 run + the pass criteria to re-confirm)
</read_first>
<action>
PRECONDITION: confirm the 225-04 capture-script fixes are already committed (this plan is wave 2,
depends_on 225-04). Run the existing read-only boundary check with its defaults:
`scripts/phase225-safe13-boundary-check.sh` (anchor `v1.48`, out
`.planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json`). Do NOT modify the
boundary script and do NOT modify any controller-path source. The script regenerates the JSON in place.

Confirm the regenerated record has `passed=true`, `controller_path_diff_count=0`,
`att_config_diff_count=0`, `dirty_tree_clean=true`, every `per_file_sha256_equal` value true, and
`head_commit` equal to current `git rev-parse HEAD` (the post-225-04 HEAD). If `passed` is false for
ANY reason — a controller-path or ATT diff, a sha mismatch, or a dirty/untracked tree over a protected
path — STOP: that is a real SAFE-13 violation (the gap fixes were supposed to touch only the diagnostic
script). Surface the offending files/state rather than committing a failing or hand-edited record; do
not overwrite the invariant to make it pass.

If the only difference vs the prior committed record is the expected `head_commit`/`checked_at`
refresh (and `passed=true`), commit the regenerated JSON. This commit becomes the final phase-boundary
HEAD, so the stamped `head_commit` is authoritative.
</action>
<verify>
  <automated>scripts/phase225-safe13-boundary-check.sh && python3 -c "import json,subprocess; d=json.load(open('.planning/phases/225-dscp-survival-trace/evidence/safe13-boundary-check.json')); h=subprocess.run(['git','rev-parse','HEAD'],capture_output=True,text=True).stdout.strip(); assert d['passed'] is True and d['controller_path_diff_count']==0 and d['att_config_diff_count']==0 and d['dirty_tree_clean'] is True and all(d['per_file_sha256_equal'].values()) and d['head_commit']==h, (d['passed'], d['head_commit'], h)"</automated>
</verify>
<acceptance_criteria>
- `scripts/phase225-safe13-boundary-check.sh` re-run exits 0 (passed=true) and regenerates `evidence/safe13-boundary-check.json`.
- The committed JSON has `passed=true`, `controller_path_diff_count=0`, `att_config_diff_count=0`, `dirty_tree_clean=true`, and every `per_file_sha256_equal` value true.
- `head_commit` in the committed JSON equals `git rev-parse HEAD` at the time of this plan's commit (no longer the stale `baa9b4b`), and `baseline_commit` equals `git rev-parse v1.48`.
- The boundary script `scripts/phase225-safe13-boundary-check.sh` is unchanged by this plan; only `evidence/safe13-boundary-check.json` is modified.
- No controller-path source file (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, `alert_engine.py`, `fusion_healer.py`, `backends/`) and no `configs/att.yaml` is modified by this plan.
- If the check fails for any reason, the executor STOPS and surfaces the offending files/state instead of committing a failing or hand-edited record.
</acceptance_criteria>
<done>
The committed SAFE-13 boundary record stamps the true final phase HEAD with the controller-path/ATT
zero-diff invariant proven (passed=true). GAP-4 closed; truth #9 moves from partial to verified.
</done>
</task>

</tasks>

<verification>
- `evidence/safe13-boundary-check.json` `head_commit` == final `git rev-parse HEAD` (post-225-04), no longer `baa9b4b`.
- `passed=true`, `controller_path_diff_count=0`, `att_config_diff_count=0`, `dirty_tree_clean=true`, all `per_file_sha256_equal` true, anchor `v1.48`.
- Boundary script unchanged; only the JSON refreshed; no controller-path source touched.
</verification>

<success_criteria>
- GAP-4 closed: committed SAFE-13 record represents the final phase-boundary HEAD and passes the full SAFE-12-standard invariant.
- SAFE-13 controller-path/ATT byte-identical-vs-v1.48 invariant held across the entire gap closure.
</success_criteria>

<output>
Create `.planning/phases/225-dscp-survival-trace/225-05-SUMMARY.md` when done.
</output>
