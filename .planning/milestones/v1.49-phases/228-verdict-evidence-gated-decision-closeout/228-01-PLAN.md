---
phase: 228-verdict-evidence-gated-decision-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase226-baseline-summary.py
  - tests/test_phase228_tin_separation.py
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "parse_tc_qdisc() parses BOTH CAKE tc-output formats: the besteffort single-tin block form (`Tin 0` header rows) AND the diffserv4 COLUMNAR form (`Bulk / Best Effort / Video / Voice` header line followed by metric rows like `pk_delay`, `av_delay`, `backlog`, `pkts`, `drops`) — VERIFIED FACT: the authoritative candidate `tc-qdisc-*.during.txt` artifacts are columnar, so the current `TIN_RE = re.compile(r\"^\\s*Tin\\s+(?P<name>\\S+)\")` parses ZERO candidate tins and the candidate summary `interfaces` is `{}`; fixing this is the prerequisite for ANY evidence-based tin separation (HIGH review finding)"
    - "Tins are normalized with BOTH a stable id AND the CAKE name so downstream code never assumes `BE == id 0`: each parsed tin carries `{id, cake_name}` where for columnar diffserv4 the names are Bulk/Best Effort/Video/Voice (BE is the `Best Effort` column, NOT positionally id 0) and for besteffort the single tin is `{id: 0, cake_name: Best Effort}`; the BE tin is identified by cake_name == 'Best Effort' (or the lone tin in single-tin output), never by hardcoded index 0 (HIGH review finding)"
    - "The summary generator emits an additive top-level `tin_separation` block per interface (spec-router, spec-modem) computing the D-05 occupancy signal (non-BE packets + non-BE sojourn/delay presence) and the D-06 delay-gap signal (max non-BE-vs-BE per-tin queue-delay gap) so the Phase 228 gate-eval reads tin separation from the diffable summary, not by re-parsing tc inside the evaluator (D-02)"
    - "Every pre-existing summary field is preserved by-key: rrul_p99_latency_under_load_ms_mean, baseline_window (restart_rate/transition_rate/floor_hit_cycles/soft_red_dwell_s), marked_ef, ref_udp_unmarked, ref_tcp_unmarked, provenance, schema_version remain unchanged in value; the candidate `interfaces` block is EXPECTED to change from `{}` to four real diffserv4 tins (this is the desired parser CORRECTION, not suspect drift) — the baseline `interfaces` (already populated with tin 0) keeps its existing values"
    - "The tin_separation block reports, per interface: non_be_tins present (by cake_name/id), non_be_packets_delta total, the per-tin queue-delay values, the non-BE-vs-BE delay gap in ms, and whether that gap exceeds NOISE_BAND_MS read from scripts/phase226-thresholds.json (not a duplicated literal) — the D-06 MAGNITUDE_RULE comparison the gate evaluates"
    - "A besteffort capture (single tin) yields a tin_separation block that correctly reports no non-BE tins / no useful separation rather than erroring — besteffort wash collapses all traffic into Best Effort, so 'no separation' is the expected, valid besteffort result"
    - "Both authoritative summaries are re-emitted in place from their already-captured tc-qdisc artifacts with NO recapture and NO live-host read: baseline-20260604T154929Z and candidate-20260604T163152Z only; the aborted first attempt (baseline-20260604T153654Z / candidate-20260604T154125Z) is NOT touched (D-08)"
    - "Re-emit provenance is honest: re-emitting changes baseline-summary.json (and BASELINE-SUMMARY.md), whose hashes are pinned in each dir's artifact-sha256.txt; the re-emit either refreshes artifact-sha256.txt for those files OR writes a separate phase228-reemit-sha256.txt — and the candidate summary carries a `parser_correction` provenance note recording that `interfaces` went from empty to four diffserv4 tins because columnar parsing was added (so the populated-from-empty change is auditable, not silent)"
  artifacts:
    - path: scripts/phase226-baseline-summary.py
      provides: "columnar diffserv4 + besteffort tin parsing (id+cake_name normalized) and an additive tin_separation block (D-05 occupancy + D-06 delay-gap vs NOISE_BAND_MS); reads NOISE_BAND_MS from thresholds JSON"
      contains: "tin_separation"
    - path: .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/baseline-20260604T154929Z/baseline-summary.json
      provides: "re-emitted authoritative besteffort summary with tin_separation block"
      contains: "tin_separation"
    - path: .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/baseline-summary.json
      provides: "re-emitted authoritative diffserv4 candidate summary: interfaces now populated with four tins + tin_separation block + parser_correction note"
      contains: "tin_separation"
    - path: tests/test_phase228_tin_separation.py
      provides: "regression proof: columnar diffserv4 parsing against the REAL committed candidate tc artifact (all four tins parse), single-tin besteffort parsing, tin_separation correctness multi/single-tin, BE-by-name not index, pre-existing-field preservation"
      contains: "tin_separation"
  key_links:
    - from: scripts/phase226-baseline-summary.py
      to: scripts/phase226-thresholds.json
      via: "reads NOISE_BAND_MS for the D-06 delay-gap magnitude comparison"
      pattern: "NOISE_BAND_MS|noise_band"
    - from: scripts/phase228-gate-eval.py
      to: scripts/phase226-baseline-summary.py
      via: "gate-eval reads the tin_separation block this plan adds (summary-in -> verdict-out)"
      pattern: "tin_separation"
---

<objective>
Fix `scripts/phase226-baseline-summary.py` to parse diffserv4 COLUMNAR CAKE tc output (the
prerequisite the cross-AI review flagged HIGH), THEN extend it with an additive `tin_separation`
block so the GATE-01 tin-separation gate (D-05 occupancy + D-06 delay-gap) can be read from the
diffable summary rather than re-parsed from tc inside the verdict evaluator (D-02). Then re-emit
BOTH authoritative summaries in place from their already-captured tc artifacts — read-only re-run,
no recapture, no production touch.

Purpose: the gate-eval (plan 228-02) consumes summary JSON, not raw tc. VERIFIED ROOT CAUSE: the
candidate `interfaces` block is currently `{}` because `parse_tc_qdisc()`'s
`TIN_RE = re.compile(r"^\s*Tin\s+(?P<name>\S+)")` only matches the besteffort single-tin BLOCK form
(`Tin 0` header). The authoritative diffserv4 candidate `tc-qdisc-*.during.txt` artifacts are
COLUMNAR — a `Bulk  Best Effort  Video  Voice` header line followed by metric rows
(`thresh / target / pk_delay / av_delay / backlog / pkts / drops / marks / ...`) with one value
column per tin. The parser must learn this columnar form (and normalize tins as `{id, cake_name}`,
NOT assuming BE == id 0). Without that fix the candidate carries zero tins, so the tin-separation
gate (228-02) would fail-closed on MISSING DATA rather than on a real "no useful separation"
finding — breaking D-01's "all five families evaluated against real data" guarantee. (Scope note:
the overall reject verdict is NOT at risk — it stands on the populated headline RRUL p99 gate; this
fix is about making the tin-separation family evidence-based, per the review.)

Only after parsing works does the additive `tin_separation` block compute the derived
non-BE-vs-BE metric the threshold set evaluates (D-05 `non_be_packets`/`non_be_sojourn` occupancy,
D-06 `per_tin_queue_delay`/`per_tin_backlog` delay gap vs `NOISE_BAND_MS`). The besteffort baseline
collapses to a single Best Effort tin — "no separation" is the correct besteffort result, not an
error.

Output: a summary generator that parses both tc formats and emits `tin_separation`, both
authoritative summaries re-emitted (candidate `interfaces` now populated with four real tins + a
`parser_correction` provenance note + refreshed/companion sha256 manifest), and a regression test
that includes a REAL-FIXTURE assertion against the committed candidate tc artifact plus the
multi-tin / single-tin behavior and pre-existing-field preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md

# The summary generator to extend additively (read in full)
@scripts/phase226-baseline-summary.py
# Locked thresholds — the tin-separation rule + NOISE_BAND_MS the new block compares against
@scripts/phase226-thresholds.json

<interfaces>
<!-- Facts verified against the repo at planning time; do NOT reflow existing summary logic. -->

The summary generator today (`scripts/phase226-baseline-summary.py`):
- `TIN_RE = re.compile(r"^\s*Tin\s+(?P<name>\S+)")` — VERIFIED: matches ONLY the besteffort
  single-tin block header (`Tin 0`). It does NOT match the diffserv4 columnar header.
- `parse_tc_qdisc(text)` -> `dict[str, TinCounters]` keyed by tin name; walks lines, sets
  `current` on a `TIN_RE` match, then fills packets/drops/backlog/delay from subsequent
  `pk_delay` / `av_delay` / `backlog` / `pkts` / `drops` / `Sent ...` rows for `current`.
- `compute_tin_deltas(before, during, after)` -> per-tin before/during/after deltas + a
  `tin_queue_delay_spread_ms` per tin (computed for the MD table).
- The summary JSON `interfaces` block is `{iface: {tin_key: {mean_*_delta, runs[], tin_queue_delay_spread_ms, stddev_ms}}}`.
- It ALSO emits `baseline_window` (restart_rate, transition_rate, floor_hit_cycles, soft_red_dwell_s),
  `rrul_p99_latency_under_load_ms_mean`, `marked_ef`, `ref_udp_unmarked`, `ref_tcp_unmarked`,
  `provenance`, `schema_version`, `run_count`.

SOURCE FACT (VERIFIED against the committed artifacts):
- BESTEFFORT baseline (`baseline-20260604T154929Z`): tc output is the single-tin BLOCK form
  (`Tin 0` then `pk_delay/av_delay/backlog/...` rows). Current parser HANDLES this — its summary
  `interfaces` is populated with tin "0" under both spec-router and spec-modem.
- DIFFSERV4 candidate (`candidate-20260604T163152Z`): tc output is COLUMNAR. Example
  (`run-01/tc-qdisc-spec-modem.during.txt`):
  ```
                     Bulk  Best Effort        Video        Voice
    thresh        750Kbit       12Mbit        6Mbit        3Mbit
    target         24.2ms          5ms          5ms       6.05ms
    pk_delay       51.3ms       2.49ms       1.97ms       3.47ms
    av_delay       12.1ms        772us        555us        1.9ms
    backlog            0b           0b           0b           0b
    pkts        132975085    219734243     36184306     10148909
    drops          842557      5060104        26317        33968
    marks           73094       394147        72320           23
  ```
  Current parser parses ZERO tins from this -> candidate summary `interfaces` is `{}` (VERIFIED).
  THIS is the bug to fix first.

Columnar parsing contract to add:
- Detect the tin-name header line: the line whose tokens are exactly the CAKE class names in order
  (`Bulk`, `Best Effort`, `Video`, `Voice` for diffserv4; note `Best Effort` is TWO words — split
  on >=2 spaces / fixed columns, not single whitespace). Establish N ordered tins with
  `{id: <0-based column index>, cake_name: <name>}`.
- For each subsequent metric row (`pk_delay`, `av_delay`, `backlog`, `pkts`, `drops`, `marks`, ...),
  map the i-th value to the i-th tin. Reuse the existing `_to_ms` / byte parsing helpers.
- The BE tin is the one whose `cake_name == "Best Effort"` (NOT positional index 0 — in diffserv4
  Best Effort is column index 1). For single-tin besteffort output, the lone tin is BE.
- Keep the existing `Tin 0` block path working (besteffort baseline still parses unchanged).

`scripts/phase226-thresholds.json` -> `TIN_SEPARATION`:
- OCCUPANCY_REQUIRED + OCCUPANCY_METRICS [non_be_packets, non_be_sojourn]
- DELAY_GAP_REQUIRED + DELAY_GAP_METRICS [per_tin_queue_delay, per_tin_backlog]
- MAGNITUDE_RULE: "non-BE vs BE per-tin delay gap must exceed the Snapshot A baseline 3-run spread"
- NOISE_BAND_MS.value = 24.206 (read this from the JSON; do NOT hardcode the literal in the .py)

Non-BE / separation semantics: "Non-BE" = any tin whose `cake_name != "Best Effort"`. "Non-BE
occupancy" = any non-zero packet/sojourn presence in a non-BE tin. The D-06 delay gap = (max non-BE
mean per-tin delay) - (BE tin mean per-tin delay); useful separation requires that gap to exceed
NOISE_BAND_MS AND non-BE occupancy to be present.

Re-emit provenance (review MEDIUM): both authoritative dirs have an `artifact-sha256.txt` that
pins the current baseline-summary.json / BASELINE-SUMMARY.md hashes. Re-emitting changes those
files, staling the manifest. Handle deliberately: after re-emit, EITHER refresh the affected
entries in each dir's `artifact-sha256.txt`, OR write a sibling `phase228-reemit-sha256.txt` with
the new hashes (and have 228-02 pin both the original capture manifest and the re-emit hashes).
Pick one and state it in the plan output. Also: the candidate `interfaces` changing from `{}` to
four tins is a DESIRED correction, not drift — record a `parser_correction` provenance note in the
re-emitted candidate summary so the populated-from-empty change is auditable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add columnar diffserv4 tc parsing (HIGH fix) + normalize tins as {id, cake_name}</name>
  <files>scripts/phase226-baseline-summary.py</files>
  <read_first>
    - scripts/phase226-baseline-summary.py (CONFIRM the bug: TIN_RE only matches `Tin N`; parse_tc_qdisc keys tins by that name; columnar diffserv4 output parses to ZERO tins. Read the _to_ms / byte helpers you will reuse for the columnar value cells.)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/run-01/tc-qdisc-spec-modem.during.txt (the REAL columnar form to parse: `Bulk  Best Effort  Video  Voice` header + metric rows)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/baseline-20260604T154929Z/run-01/tc-qdisc-spec-router.during.txt (the besteffort `Tin 0` block form that must KEEP parsing)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-02 extend-summary-not-parse-tc-in-eval)
  </read_first>
  <action>
    Make `parse_tc_qdisc()` parse BOTH tc-output formats and normalize each tin to carry a stable
    `{id, cake_name}` identity (TinCounters gains a `cake_name` field; `id` is the 0-based column /
    block index):
    (1) Keep the existing `Tin N` BLOCK path (besteffort baseline) working unchanged — its lone tin
        becomes `{id: 0, cake_name: "Best Effort"}`.
    (2) Add a COLUMNAR path: detect the tin-name header line (tokens are the CAKE class names, e.g.
        `Bulk`, `Best Effort`, `Video`, `Voice`; `Best Effort` is two words — split on >=2-space
        column boundaries / fixed columns, NOT single whitespace). Establish N ordered tins. For each
        following metric row (`pk_delay`, `av_delay`, `sp_delay`, `backlog`, `pkts`, `bytes`, `drops`,
        `marks`, ...), map the i-th value cell to the i-th tin, reusing `_to_ms` and the byte/int
        parsing already in the module.
    The BE tin is identified by `cake_name == "Best Effort"` (or the lone tin in single-tin output) —
    NEVER by hardcoded index 0 (in diffserv4 Best Effort is column index 1). Keep
    `compute_tin_deltas()` and the `interfaces` block shape working; tin keys may remain the
    name/id but every tin row must expose its `cake_name` so downstream non-BE selection is by name.
    Do NOT change any non-tin field (rrul p99, baseline_window, marked_ef, ref_*).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/phase226-baseline-summary.py').read())" && .venv/bin/ruff check scripts/phase226-baseline-summary.py && python3 -c "import sys; from pathlib import Path; import importlib.util as u; s=u.spec_from_file_location('p226sum','scripts/phase226-baseline-summary.py'); m=u.module_from_spec(s); sys.modules['p226sum']=m; s.loader.exec_module(m); t=m.parse_tc_qdisc(Path('.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/run-01/tc-qdisc-spec-modem.during.txt').read_text()); names={getattr(v,'cake_name',None) for v in t.values()}; assert len(t)==4, ('expected 4 diffserv4 tins, got', len(t)); assert 'Best Effort' in names, names; print('OK columnar diffserv4 parsed 4 tins:', sorted(names))"</automated>
  </verify>
  <done>parse_tc_qdisc parses the REAL committed columnar candidate artifact into all four diffserv4 tins (Bulk/Best Effort/Video/Voice), each carrying {id, cake_name}; the besteffort `Tin 0` block still parses; BE is identified by cake_name, not index 0; ruff + ast clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the additive tin_separation block in the summary generator</name>
  <files>scripts/phase226-baseline-summary.py</files>
  <read_first>
    - scripts/phase226-baseline-summary.py (the now-columnar-aware parser from Task 1; the interfaces block carries per-tin mean_delay_delta_ms / mean_backlog_bytes_delta / mean_packets_delta + cake_name. You ADD a derived block over this in-memory per-tin data, not by re-parsing tc.)
    - scripts/phase226-thresholds.json (TIN_SEPARATION rule + NOISE_BAND_MS.value — read NOISE_BAND_MS at runtime; never duplicate the 24.206 literal in the .py)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-02 extend-summary-not-parse-tc-in-eval; D-06 magnitude rule already frozen)
  </read_first>
  <action>
    Add an additive top-level `tin_separation` block to the summary JSON, computed from the
    per-interface per-tin `interfaces` data assembled in the generator (do NOT re-parse tc; derive
    from the in-memory per-tin rows). For each interface (spec-router, spec-modem) emit:
    `be_tin` ({id, cake_name} of the Best Effort tin, selected by cake_name == "Best Effort" / the
    lone tin), `non_be_tins` (sorted list of {id, cake_name} for tins with cake_name != "Best Effort"
    that have non-zero `mean_packets_delta`), `non_be_packets_delta` (sum of non-BE mean packet
    deltas — the D-05 `non_be_packets` occupancy signal), `non_be_present` (bool: any non-BE
    occupancy), the per-tin `mean_delay_delta_ms` map keyed by cake_name (the D-06
    `per_tin_queue_delay` signal) and per-tin `mean_backlog_bytes_delta` map (`per_tin_backlog`),
    `delay_gap_ms` (max non-BE `mean_delay_delta_ms` minus the BE-tin `mean_delay_delta_ms`; null when
    there are no non-BE tins), `noise_band_ms` (read from thresholds JSON via a `--thresholds` arg,
    default `scripts/phase226-thresholds.json`, reading `TIN_SEPARATION.NOISE_BAND_MS.value`), and
    `useful_separation` (bool: `non_be_present` AND `delay_gap_ms` is not null AND
    `delay_gap_ms > noise_band_ms` — the D-06 MAGNITUDE_RULE).
    Add a `--thresholds PATH` CLI arg (default scripts/phase226-thresholds.json) so the NOISE_BAND_MS
    source is explicit and the literal lives only in the JSON. A besteffort tree (single Best Effort
    tin) must yield `non_be_tins: []`, `non_be_present: false`, `delay_gap_ms: null`,
    `useful_separation: false` WITHOUT error. Keep EVERY existing field and the existing top-level
    shape intact (additive only). Add a short `tin_separation` row group to the BASELINE-SUMMARY.md
    table section consistent with the existing per-tin table (do not remove the existing table).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/phase226-baseline-summary.py').read())" && .venv/bin/ruff check scripts/phase226-baseline-summary.py && python3 scripts/phase226-baseline-summary.py --help 2>&1 | grep -qi "thresholds"</automated>
  </verify>
  <done>Module parses and passes ruff; `--help` shows a `--thresholds` option; the generator computes a `tin_separation` block over the in-memory per-tin data, selecting BE by cake_name, reading NOISE_BAND_MS from the thresholds JSON (no 24.206 literal in the .py); a single-tin tree produces non_be_present=false / useful_separation=false without error.</done>
</task>

<task type="auto">
  <name>Task 3: Re-emit both authoritative summaries in place (read-only) + handle sha256 manifest + parser_correction</name>
  <files>
    .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/baseline-20260604T154929Z/baseline-summary.json
    .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/baseline-summary.json
  </files>
  <read_first>
    - scripts/phase226-baseline-summary.py (the now-columnar-aware, tin_separation-emitting generator from Tasks 1-2 — re-run it over the existing run trees; confirm the invocation it expects: the run-dir tree + tc artifacts already present)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/artifact-sha256.txt (the manifest that pins baseline-summary.json/BASELINE-SUMMARY.md hashes — re-emit stales it; decide refresh-in-place vs sibling phase228-reemit-sha256.txt)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-08 hard-pin authoritative pair; EXCLUDE the aborted attempt 153654Z/154125Z)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-VERIFICATION.md (names baseline-20260604T154929Z + candidate-20260604T163152Z as the authoritative verdict-ready pair)
  </read_first>
  <action>
    Re-run the extended generator over ONLY the two authoritative evidence dirs:
    `baseline-20260604T154929Z` and `candidate-20260604T163152Z`. Pass
    `--thresholds scripts/phase226-thresholds.json`. READ-ONLY re-emit over already-captured artifacts
    — no live-host read, no recapture, no production touch. Do NOT touch the aborted first attempt
    (`baseline-20260604T153654Z`, `candidate-20260604T154125Z`) per D-08.
    Expected diffs (NOT all "additive only" — the candidate parse is a CORRECTION):
    - BASELINE besteffort: additive only — new `tin_separation` block (+ MD row group); pre-existing
      values (rrul p99, baseline_window rates, marked_ef, ref_*, the already-populated tin-0
      `interfaces`) unchanged.
    - CANDIDATE diffserv4: `interfaces` changes from `{}` to FOUR real tins (Bulk/Best Effort/Video/
      Voice) — the desired parser correction — PLUS the new `tin_separation` block; rrul p99 /
      baseline_window / marked_ef / ref_* unchanged. Add a `provenance.parser_correction` note in the
      candidate summary recording: "columnar diffserv4 tc parsing added in Phase 228-01; interfaces
      populated from empty (4 tins) — see 228-REVIEWS HIGH finding". This makes the populated-from-
      empty change auditable, not silent.
    Provenance manifest: after re-emit, EITHER refresh the baseline-summary.json / BASELINE-SUMMARY.md
    entries in each dir's `artifact-sha256.txt`, OR write a sibling `phase228-reemit-sha256.txt` in
    each dir with the new hashes. Pick one approach and state it in the 228-01 SUMMARY so 228-02 knows
    which manifest to pin. (Whichever you pick, the verdict provenance in 228-02 pins the CURRENT
    re-emitted summary hashes, not only the stale capture-time manifest.)
  </action>
  <verify>
    <automated>python3 -c "import json; b=json.load(open('.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/baseline-20260604T154929Z/baseline-summary.json')); c=json.load(open('.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/baseline-summary.json')); assert 'tin_separation' in b and 'tin_separation' in c, 'tin_separation missing'; assert abs(b['rrul_p99_latency_under_load_ms_mean']-345.14793561629267)<1e-6, 'baseline rrul p99 drifted on re-emit'; assert len(c['interfaces'])>=1 and all(len(v)==4 for v in c['interfaces'].values()), ('candidate interfaces not populated with 4 tins', {k:len(v) for k,v in c['interfaces'].items()}); print('OK both re-emitted; baseline p99 stable; candidate interfaces populated with 4 diffserv4 tins')"</automated>
  </verify>
  <done>Both authoritative summaries contain a `tin_separation` block; baseline RRUL p99 mean byte-stable; candidate `interfaces` now populated with four diffserv4 tins (parser correction) + a parser_correction provenance note; the sha256 manifest is refreshed or a phase228-reemit-sha256.txt sibling written (stated in SUMMARY); aborted-attempt dirs untouched; no live-host read or recapture.</done>
</task>

<task type="auto">
  <name>Task 4: Regression test — REAL-FIXTURE columnar parsing + tin_separation correctness + additive preservation</name>
  <files>tests/test_phase228_tin_separation.py</files>
  <read_first>
    - scripts/phase226-baseline-summary.py (parse_tc_qdisc + the tin_separation computation under test)
    - scripts/phase226-thresholds.json (NOISE_BAND_MS source the test feeds / asserts is read from the JSON, not hardcoded)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/candidate-20260604T163152Z/run-01/tc-qdisc-spec-router.during.txt (the REAL committed columnar fixture the test parses)
    - tests/ (pick the closest existing summary/harness test as the style analog before writing)
  </read_first>
  <action>
    Add pytest checks. REQUIRED real-fixture test (the HIGH-finding guard):
    (real) parse_tc_qdisc on the committed candidate `tc-qdisc-spec-router.during.txt` (and the
    spec-modem one) returns exactly four tins whose cake_names are {Bulk, Best Effort, Video, Voice},
    each with non-zero pkts and a parsed delay; and parse_tc_qdisc on the committed BESTEFFORT
    `tc-qdisc-spec-router.during.txt` returns a single Best Effort tin — proving both formats parse
    from real artifacts. Then synthetic checks:
    (a) a multi-tin interface (BE low delay, a non-BE tin with delay gap ABOVE NOISE_BAND_MS + non-zero
        packets) yields non_be_present=true, delay_gap_ms > noise_band, useful_separation=true;
    (b) a multi-tin interface whose non-BE delay gap is BELOW NOISE_BAND_MS yields
        useful_separation=false even though non-BE occupancy is present;
    (c) a single Best Effort tin yields non_be_tins=[], non_be_present=false, delay_gap_ms=None,
        useful_separation=false with NO exception;
    (d) BE is selected by cake_name == "Best Effort", NOT index 0 — feed a diffserv4-shaped fixture
        where Best Effort is column index 1 and assert be_tin.id == 1 and the gap is computed against
        the Best Effort tin (this locks the no-hardcoded-index-0 fix);
    (e) NOISE_BAND_MS is read from the thresholds JSON path passed in, not hardcoded — feed a temp
        thresholds file with a distinct NOISE_BAND_MS and assert the threshold used changes;
    (f) the generator still emits the pre-existing top-level keys (rrul_p99_latency_under_load_ms_mean,
        baseline_window, marked_ef, ref_udp_unmarked, ref_tcp_unmarked, interfaces, provenance,
        schema_version) — additive contract holds.
    Drive via the generator's functions / a temp run tree; the real-fixture test reads the committed
    artifacts directly. Do not require a live target.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase228_tin_separation.py -q</automated>
  </verify>
  <done>All tests pass, including the REAL-FIXTURE assertion that the committed columnar candidate artifact parses to four named tins and the committed besteffort artifact parses to one; the suite fails if a future edit breaks columnar parsing, reintroduces a hardcoded BE index 0, drops the single-tin guard, hardcodes the noise band, breaks the delay-gap-vs-noise_band logic, or removes a pre-existing summary field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> committed evidence (read-only) | Re-emit reads already-captured tc/health/iperf artifacts; no live-host read, no recapture, no production mutation. |
| summary generator -> thresholds JSON | Reads NOISE_BAND_MS as the single source of truth; the literal is never duplicated into the .py. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-01 | Tampering | Re-emit silently mutates a pre-existing numeric field | mitigate | Task 3 verify asserts baseline RRUL p99 byte-stable; Task 4 (f) asserts all pre-existing keys survive. Candidate `interfaces` going empty->4-tins is a DECLARED correction (parser_correction note), not silent drift. |
| T-228-02 | Repudiation | Wrong dataset re-emitted (aborted attempt) | mitigate | Task 3 hard-pins only 154929Z / 163152Z and explicitly excludes 153654Z / 154125Z per D-08. |
| T-228-03 | Tampering | NOISE_BAND_MS duplicated/diverged from the locked JSON | mitigate | Generator reads NOISE_BAND_MS from `--thresholds` JSON; Task 4 (e) proves it is not hardcoded. |
| T-228-16 | Spoofing | Tin-separation gate evaluated on MISSING data (empty candidate interfaces) instead of real tins | mitigate | Task 1 adds columnar parsing; Task 4 (real) asserts the committed candidate artifact parses to 4 named tins — the gate reads real data, not `{}`. (HIGH review finding.) |
| T-228-17 | Tampering | BE tin misidentified by hardcoded index 0 (wrong in diffserv4 where BE is column 1) | mitigate | BE selected by cake_name == "Best Effort"; Task 4 (d) locks be_tin.id == 1 for diffserv4. |
| T-228-18 | Repudiation | Re-emit stales artifact-sha256.txt, breaking provenance | mitigate | Task 3 refreshes the manifest or writes phase228-reemit-sha256.txt; 228-02 pins the current re-emitted hashes. |
| T-228-SC | Tampering | npm/pip installs | accept | No new package installs — reuses installed python3 + the existing generator deps. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(...)"` + `ruff check` clean on the generator.
- parse_tc_qdisc parses the REAL committed columnar candidate artifact into 4 named tins; the besteffort `Tin 0` block still parses.
- `--help` shows `--thresholds`.
- Both authoritative summaries gain a `tin_separation` block; baseline RRUL p99 mean byte-stable; candidate `interfaces` populated with 4 tins + parser_correction note.
- artifact-sha256.txt refreshed or phase228-reemit-sha256.txt written (stated in SUMMARY).
- Aborted-attempt dirs untouched (D-08).
- `.venv/bin/pytest tests/test_phase228_tin_separation.py -q` green (incl. the real-fixture test).
- No production mutation, no live-host read, no recapture — read-only re-emit only.
</verification>

<success_criteria>
- parse_tc_qdisc parses BOTH besteffort block AND diffserv4 columnar tc output; tins normalized as {id, cake_name}; BE selected by name, not index 0 (HIGH review fix).
- Summary generator emits an additive `tin_separation` block (D-05 occupancy + D-06 delay-gap vs NOISE_BAND_MS) over the now-real per-tin data (D-02).
- NOISE_BAND_MS read from the locked thresholds JSON, never duplicated as a literal.
- Both authoritative summaries re-emitted; candidate `interfaces` corrected empty->4-tins with a parser_correction note; sha256 staling handled; aborted attempt excluded (D-08).
- Besteffort single Best Effort tin -> "no separation" as a valid result, not an error.
- Regression test locks columnar real-fixture parsing, multi/single-tin behavior, BE-by-name, and additive preservation.
</success_criteria>

<output>
Create `.planning/phases/228-verdict-evidence-gated-decision-closeout/228-01-SUMMARY.md` when done
</output>
