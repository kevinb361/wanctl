---
phase: 226-baseline-capture-threshold-lock-snapshot-a
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [AB-02, GATE-01, SAFE-13]
files_modified:
  - scripts/phase226-baseline-summary.py
  - tests/phase226/test_tc_qdisc_parser.py
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/baseline-summary.json
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/BASELINE-SUMMARY.md
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/artifact-sha256.txt
  - scripts/phase226-thresholds.json

must_haves:
  truths:
    - "parse_tc_qdisc reads real CAKE per-tin rows (pkts/drops/backlog/av_delay/pk_delay) and returns non-zero counters for live evidence."
    - "Regenerated baseline-summary.json reports non-zero tin_queue_delay_spread_ms for at least one interface, matching the retained 3-run evidence (spec-modem in a ~20-30 ms band, spec-router < 1 ms)."
    - "GATE-01 NOISE_BAND_MS.value is > 0 and re-provenanced (sha256 + derived_at) against the SAME regenerated summary the manifest records (no skew)."
    - "artifact-sha256.txt is refreshed so its baseline-summary.json and BASELINE-SUMMARY.md entries equal the freshly-computed hashes; sha256sum -c passes."
    - "SAFE-13 holds: zero diff to controller-path/ATT-protected files vs v1.48, and the full change set stays inside the Phase-226 evidence/tooling allowlist."
  artifacts:
    - path: "scripts/phase226-baseline-summary.py"
      provides: "Real-CAKE per-tin row parsing in parse_tc_qdisc"
      contains: "av_delay"
    - path: "tests/phase226/test_tc_qdisc_parser.py"
      provides: "Real-format regression test that fails on the old parser, proving backlog + peak delay"
      contains: "av_delay"
    - path: ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/baseline-summary.json"
      provides: "Regenerated baseline summary with non-zero deltas/spread"
      contains: "tin_queue_delay_spread_ms"
    - path: ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/artifact-sha256.txt"
      provides: "Refreshed evidence hash manifest matching the regenerated derived artifacts"
      contains: "baseline-summary.json"
    - path: "scripts/phase226-thresholds.json"
      provides: "NOISE_BAND_MS recomputed and re-provenanced from valid baseline"
      contains: "NOISE_BAND_MS"
  key_links:
    - from: "scripts/phase226-baseline-summary.py"
      to: "real CAKE per-tin block"
      via: "parse_tc_qdisc regexes for pkts/drops/backlog/av_delay/pk_delay"
      pattern: "av_delay|pk_delay|pkts|drops|backlog"
    - from: "scripts/phase226-thresholds.json"
      to: "regenerated baseline-summary.json"
      via: "NOISE_BAND_MS.derived_from.sha256"
      pattern: "sha256"
    - from: ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/artifact-sha256.txt"
      to: "regenerated baseline-summary.json + BASELINE-SUMMARY.md"
      via: "sha256sum manifest refresh excluding artifact-sha256.txt itself"
      pattern: "baseline-summary\\.json"
---

<objective>
Close the two BLOCKED Phase 226 must-haves (AB-02 baseline evidence, GATE-01 threshold lock) without re-capturing any live data. Both failures trace to ONE bug: `parse_tc_qdisc()` only recognizes synthetic summary lines (`Sent N bytes N pkt`, `Dropped|Backlog`, `Avge|Peak delay`) and returns all-zero `TinCounters` against the real CAKE per-tin block present in the retained evidence. That zero summary poisoned `NOISE_BAND_MS.value` (0.0).

Fix the parser to read the real per-tin block, add the regression test that would have caught it (proving pkts/drops/backlog/av_delay/pk_delay), regenerate the baseline artifacts from the intact raw evidence, refresh the evidence hash manifest, then recompute and re-provenance the locked noise-band constant against the SAME final summary the manifest records.

Purpose: The baseline summary and the GATE-01 tin-separation noise band must be evidentially trustworthy before Phase 227's candidate `diffserv4 wash` deploy. A zero-valued noise band would let any candidate "pass" tin separation trivially.
Output: Corrected parser, real-format regression test, regenerated `baseline-summary.json` + `BASELINE-SUMMARY.md`, refreshed `artifact-sha256.txt`, and re-provenanced `scripts/phase226-thresholds.json`.

Scope guard: Everything downstream of `parse_tc_qdisc` (`_spread`, `compute_tin_deltas`, delta math, markdown render) already works and MUST NOT be restructured — only row recognition is broken. The cumulative→delta model (`during − before`) is correct because CAKE counters are cumulative since qdisc creation. Deployment is besteffort (single `Tin 0`); `tin_queue_delay_spread_ms` is the spread across the 3 per-run mean `av_delay` values (run-to-run noise band), which the existing `_spread(delays)` call already computes once the parser populates `avg_delay_ms`. Do NOT touch the controller path, CAKE mode, or threshold semantics beyond filling the already-frozen `NOISE_BAND_MS` constant (SAFE-13). This is a corrected derivation from UNCHANGED raw evidence, not retroactive evidence mutation — only derived summaries + the hash manifest change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-VERIFICATION.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md
@scripts/phase226-baseline-summary.py
@scripts/phase226-baseline-capture.sh
@scripts/phase226-thresholds.json
@tests/phase226/test_tc_qdisc_parser.py

<interfaces>
<!-- Real CAKE `tc -s qdisc` per-tin block (retained evidence: run-01 spec-router/spec-modem during). -->
<!-- Executor parses THIS shape — bare indented labels, not synthetic Sent/Dropped/Avge lines. -->

Real per-tin block (one block per `Tin N`):
  Tin 0
  thresh        920Mbit
  target            5ms
  interval        100ms
  pk_delay        131us
  av_delay         54us
  sp_delay          5us
  backlog            0b
  pkts       1551553525
  bytes    1774567043006
  drops          657899
  marks            6585

Units on delay rows: us | ms | s (reuse existing `_to_ms`). backlog has a trailing `b` (bytes).
`pkts` and `drops` are bare integers. Values are cumulative since qdisc creation.

Evidence facts the executor MUST rely on (verified against retained files):
  - run-01 spec-router during: backlog 0b, pk_delay 131us, av_delay 54us
    -> proves delay parsing but CANNOT prove non-zero backlog (it is 0b).
  - run-01 spec-modem during:  backlog 1844b, pk_delay 3.78ms, av_delay 694us
    -> proves non-zero backlog_bytes AND non-zero peak_delay_ms.
  Use spec-modem during for backlog/peak proof; use spec-router during for the delay proof.

Existing dataclass (DO NOT change shape):
  TinCounters(name, packets=0, drops=0, backlog_bytes=0, avg_delay_ms=0.0, peak_delay_ms=0.0)

Existing helper to reuse for delay unit conversion:
  _to_ms(value: str, unit: str) -> float   # us/1000, s*1000, ms passthrough

Existing test bootstrap (Task 1 verify command MUST mirror this — module inserted into sys.modules BEFORE exec_module so @dataclass resolves):
  spec = importlib.util.spec_from_file_location("phase226_baseline_summary", MODULE_PATH)
  summary = importlib.util.module_from_spec(spec)
  sys.modules["phase226_baseline_summary"] = summary
  spec.loader.exec_module(summary)

Evidence hash manifest regeneration (reuse the capture script's exact form — phase226-baseline-capture.sh lines 324-326):
  find "$CAPTURE_DIR" -type f ! -name 'artifact-sha256.txt' -print0 | sort -z | xargs -0 sha256sum > "$CAPTURE_DIR/artifact-sha256.txt"
  Run from repo root with CAPTURE_DIR as the repo-relative evidence path so manifest entries stay repo-relative (matches existing manifest). Do NOT invent a new hashing scheme.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach parse_tc_qdisc to read real CAKE per-tin rows</name>
  <read_first>scripts/phase226-baseline-summary.py</read_first>
  <files>scripts/phase226-baseline-summary.py</files>
  <behavior>
    - Real-format input with a `Tin 0` block populates TinCounters.packets from the bare `pkts` row (e.g. 1551553525), not 0.
    - `drops` bare-integer row populates TinCounters.drops (e.g. 657899).
    - `backlog <N>b` row populates TinCounters.backlog_bytes from the integer before the `b` suffix (spec-modem during: 1844).
    - `av_delay <N>{us|ms|s}` populates avg_delay_ms via _to_ms (54us -> 0.054).
    - `pk_delay <N>{us|ms|s}` populates peak_delay_ms via _to_ms (131us -> 0.131; 3.78ms -> 3.78).
    - `Tin N` header still starts a new tin block (existing TIN_RE behavior preserved).
    - Real per-tin fields take precedence: when both a real per-tin label and a synthetic helper line are present for the same field, the parsed real value must NOT be overwritten by a synthetic fallback. The existing synthetic-fixture tests must keep passing unchanged.
  </behavior>
  <action>In `scripts/phase226-baseline-summary.py`, add real-CAKE per-tin row recognition inside `parse_tc_qdisc()`'s per-line loop, after the existing `TIN_RE` block guard. Add module-level compiled regexes near the existing TIN_RE/SENT_RE/SIMPLE_KV_RE/DELAY_RE (around lines 24-27) for: bare `pkts` integer, bare `drops` integer, `backlog <int>b` (capture integer before the `b`), and `av_delay`/`pk_delay <float><us|ms|s>`. Anchor each regex to the indented per-tin label at line start after leading whitespace (so a bare `drops`/`pkts` line is unambiguous and does not collide with the root header's `dropped`/`pkt` words). Map: pkts->packets (int), drops->drops (int), backlog->backlog_bytes (int), av_delay->avg_delay_ms via `_to_ms`, pk_delay->peak_delay_ms via `_to_ms`. Reuse the existing `_to_ms` helper for unit conversion — do not duplicate it. Implement real-field precedence: the real per-tin branch writes the authoritative value; any retained synthetic branch must not clobber a field already set by the real branch for the current tin (e.g. only assign the synthetic value when the key is absent). Do NOT alter `compute_tin_deltas`, `_delta`, `_spread`, `build_summary`, or `write_markdown`. Preserve the existing `TinCounters` dataclass shape exactly. Keep the synthetic-line branches in place for backward compat (the existing fixtures depend on them) but make the real-format branch authoritative.</action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,sys,pathlib; p=pathlib.Path('scripts/phase226-baseline-summary.py'); s=importlib.util.spec_from_file_location('phase226_baseline_summary',p); m=importlib.util.module_from_spec(s); sys.modules['phase226_baseline_summary']=m; s.loader.exec_module(m); base='.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/run-01'; r=m.parse_tc_qdisc(pathlib.Path(base+'/tc-qdisc-spec-router.during.txt').read_text())['0']; assert r.packets>0 and r.drops>0 and r.avg_delay_ms>0 and r.peak_delay_ms>0, ('router',r); md=m.parse_tc_qdisc(pathlib.Path(base+'/tc-qdisc-spec-modem.during.txt').read_text())['0']; assert md.backlog_bytes>0 and md.peak_delay_ms>0, ('modem',md); print('OK router',r,'modem backlog',md.backlog_bytes,'modem pk',md.peak_delay_ms)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints: a spec-router TinCounters with packets>0, drops>0, avg_delay_ms>0, peak_delay_ms>0; AND spec-modem backlog_bytes>0 (1844) and peak_delay_ms>0 (3.78). The command inserts the module into sys.modules BEFORE exec_module (mirroring the test bootstrap) so @dataclass resolves.
    - `git diff --stat` shows changes ONLY to `scripts/phase226-baseline-summary.py` (no other source files touched).
    - `TinCounters` field names/defaults are unchanged from the pre-edit definition.
  </acceptance_criteria>
  <done>parse_tc_qdisc returns non-zero packets/drops/avg_delay_ms/peak_delay_ms for spec-router and non-zero backlog_bytes/peak_delay_ms for spec-modem real CAKE per-tin evidence; downstream functions unchanged; real fields take precedence over synthetic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real-format regression tests that would have caught the bug</name>
  <read_first>tests/phase226/test_tc_qdisc_parser.py</read_first>
  <files>tests/phase226/test_tc_qdisc_parser.py</files>
  <behavior>
    - An inline test parses a real-CAKE `Tin 0` block using ONLY real row labels (pkts/drops/backlog/av_delay/pk_delay), no synthetic Sent/Dropped/Avge helper lines, carrying NON-ZERO backlog and pk_delay, and asserts packets>0, drops>0, backlog_bytes>0, avg_delay_ms equal to the expected `_to_ms` conversion, and peak_delay_ms>0.
    - A retained-evidence test parses `run-01/tc-qdisc-spec-router.during.txt` and asserts `parse_tc_qdisc(...)['0'].packets > 0` and `avg_delay_ms > 0`.
    - A retained-evidence test parses `run-01/tc-qdisc-spec-modem.during.txt` and asserts `backlog_bytes > 0` and `peak_delay_ms > 0` (this file carries backlog 1844b / pk_delay 3.78ms; spec-router during has backlog 0b and cannot prove backlog).
    - The existing four synthetic-fixture tests continue to pass (do not modify them).
  </behavior>
  <action>In `tests/phase226/test_tc_qdisc_parser.py`, add test functions using the module already loaded via `summary` (the importlib bootstrap at top — it already inserts into `sys.modules`; reuse it, do not re-bootstrap). Inline test: define a multiline string containing a real-format `Tin 0` block (bare `pkts`, `drops`, `backlog 1844b`, `av_delay 54us`, `pk_delay 3.78ms`, NO synthetic helper lines), call `summary.parse_tc_qdisc(...)`, assert tin `"0"` has packets>0, drops>0, backlog_bytes==1844, avg_delay_ms==pytest.approx(0.054), and peak_delay_ms==pytest.approx(3.78). Retained-evidence tests: resolve paths relative to `ROOT` (`.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/run-01/tc-qdisc-spec-router.during.txt` and `.../tc-qdisc-spec-modem.during.txt`); `pytest.skip` if a file is absent; for spec-router assert `["0"].packets > 0` and `["0"].avg_delay_ms > 0`; for spec-modem assert `["0"].backlog_bytes > 0` and `["0"].peak_delay_ms > 0`. Do NOT add synthetic Sent/Dropped/Avge lines to any new test. Do NOT modify the existing four tests or the fixtures.</action>
  <verify>
    <automated>.venv/bin/pytest tests/phase226/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/phase226/ -q` passes with at least 7 tests (4 existing + 3 new).
    - At least one new test references real CAKE labels (`av_delay`, `pkts`, `drops`, `backlog`) and contains NO `Sent`/`Dropped`/`Avge`/`Peak delay` strings, and asserts non-zero `backlog_bytes` and `peak_delay_ms`.
    - The new real-format assertions target non-zero values the old (synthetic-only) parser cannot produce — they would FAIL against the pre-Task-1 parser.
  </acceptance_criteria>
  <done>Real-format regression coverage exists and passes; it exercises an inline real block with non-zero backlog/pk_delay plus retained spec-router (delay) and spec-modem (backlog/peak) evidence files.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate baseline artifacts, refresh hash manifest, recompute and re-provenance NOISE_BAND_MS</name>
  <read_first>scripts/phase226-thresholds.json</read_first>
  <files>.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/baseline-summary.json, .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/BASELINE-SUMMARY.md, .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/artifact-sha256.txt, scripts/phase226-thresholds.json</files>
  <action>Execute these sub-steps strictly in order — the ordering is load-bearing so the thresholds sha256 and the manifest entry both bind to the SAME final summary with no skew.
1. Regenerate derived summaries: re-run the summary helper against the RETAINED raw evidence dir (no live re-capture): `.venv/bin/python scripts/phase226-baseline-summary.py --capture-dir .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z`. This overwrites `baseline-summary.json` and `BASELINE-SUMMARY.md` in place with real non-zero deltas/spread. These are now FINAL — do not regenerate them again after this step.
2. Refresh the evidence hash manifest LAST among evidence files, reusing the capture script's exact form (phase226-baseline-capture.sh lines 324-326), excluding the manifest itself, run from repo root with the repo-relative capture dir so entries stay repo-relative: `find .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z -type f ! -name 'artifact-sha256.txt' -print0 | sort -z | xargs -0 sha256sum > .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/artifact-sha256.txt`. Do NOT invent a new hashing scheme. The manifest's `baseline-summary.json` and `BASELINE-SUMMARY.md` entries must now equal the freshly-computed hashes.
3. Compute the new noise-band value: read `tin_queue_delay_spread_ms` from the regenerated `baseline-summary.json` for each interface/tin and take the max across interfaces per the existing `NOISE_BAND_MS.derivation` text (besteffort single-tin). Expected for this retained set: spec-modem spread in a sane band (~20-30 ms), spec-router spread < 1 ms; the max becomes the noise band.
4. Re-provenance `scripts/phase226-thresholds.json` `TIN_SEPARATION.NOISE_BAND_MS`: set `value` to the recomputed max spread (a positive float), set `derived_from.sha256` to `sha256sum` of the FINAL regenerated `baseline-summary.json` (the same bytes the manifest hashed in step 2 — assert they match), and set `derived_at` to the current UTC timestamp (ISO-8601 Z). Do NOT change any other key in the JSON, do NOT alter the `source`/`derivation`/`path` strings, and do NOT add numeric values to `GATE-01-THRESHOLDS.md` (single-source-of-truth rule — its prose already points at the JSON and stays correct).
Note for the SUMMARY: the OLD invalid summary hash is `186f4a72fa346d202e8c2fb20dea769a4950220e9cc373ffa4f3c307bab5a5f6`; record it alongside the NEW hash and an explicit "raw run-NN tc/health artifacts unchanged — only derived summaries + manifest were corrected" statement. Leave the parser and tests from Tasks 1-2 untouched.</action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && D=.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z && S=$D/baseline-summary.json && (cd "$D" && sha256sum -c artifact-sha256.txt) && python3 -c "import json,hashlib; s=json.load(open('$S')); spreads={(i,t):v['tin_queue_delay_spread_ms'] for i,tins in s['interfaces'].items() for t,v in tins.items()}; assert any(x>0 for x in spreads.values()), spreads; modem=[v for (i,t),v in spreads.items() if i=='spec-modem']; router=[v for (i,t),v in spreads.items() if i=='spec-router']; assert any(20.0<=x<=30.0 for x in modem), ('spec-modem spread out of 20-30ms band',modem); assert all(x<1.0 for x in router), ('spec-router spread not <1ms',router); th=json.load(open('scripts/phase226-thresholds.json'))['TIN_SEPARATION']['NOISE_BAND_MS']; assert th['value']>0, th['value']; h=hashlib.sha256(open('$S','rb').read()).hexdigest(); assert th['derived_from']['sha256']==h, (th['derived_from']['sha256'],h); print('OK value=',th['value'],'sha=',h[:12])"</automated>
  </verify>
  <acceptance_criteria>
    - `sha256sum -c artifact-sha256.txt` run from the evidence dir exits 0 (manifest matches all evidence files including the regenerated summary + markdown).
    - Regenerated `baseline-summary.json` has non-zero `tin_queue_delay_spread_ms`; spec-modem spread is in the ~20-30 ms band and spec-router spread is < 1 ms (evidence-specific bands, asserted by the verify command). NOTE: `>0` is an artifact of THIS specific 3-run set, not a parser invariant — a future baseline with three identical `av_delay` samples could validly yield 0.0. The band check is what locks this evidence set.
    - `scripts/phase226-thresholds.json` `TIN_SEPARATION.NOISE_BAND_MS.value` > 0 (GATE-01 acceptance: must be > 0 for THIS evidence set since the max spread is non-zero).
    - `NOISE_BAND_MS.derived_from.sha256` equals `sha256sum` of the FINAL regenerated `baseline-summary.json` AND equals the hash recorded for that file in `artifact-sha256.txt` (no skew between thresholds provenance and the manifest).
    - `derived_at` is updated to a current UTC ISO-8601 Z timestamp.
    - `GATE-01-THRESHOLDS.md` contains NO numeric threshold literals (single-source rule preserved); `git diff` shows it unchanged.
  </acceptance_criteria>
  <done>Baseline artifacts regenerated from unchanged raw evidence; hash manifest refreshed and self-consistent; NOISE_BAND_MS recomputed > 0 and re-provenanced to the same final summary hash the manifest records.</done>
</task>

<task type="auto">
  <name>Task 4: SAFE-13 boundary re-check + change-set allowlist enforcement</name>
  <read_first>scripts/phase225-safe13-boundary-check.sh</read_first>
  <files>.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json</files>
  <action>After Tasks 1-3 are committed/staged, re-run the existing comprehensive SAFE-13 boundary proof against the v1.48 anchor to confirm the closure work touched only evidence/instrumentation tooling, not the protected controller path or ATT config. Run `scripts/phase225-safe13-boundary-check.sh --anchor v1.48 --out .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json` (the same invocation Phase 226 used). This script is the AUTHORITATIVE protected-set check — it already covers the FULL SAFE-13 protected set (controllers, `cake_signal.py`, `alert_engine.py`, `fusion_healer.py`, `src/wanctl/backends/`, `configs/att.yaml`, etc.); do NOT hand-roll a narrower per-file diff that omits members of the protected set. Then enforce a POSITIVE change-set allowlist: compute `git diff --name-only v1.48` and assert that EVERY changed path falls inside the allowed Phase-226 set — `scripts/phase226-baseline-summary.py`, `scripts/phase226-thresholds.json`, `tests/phase226/` (any file under it), and `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/` (evidence + this plan/summary) — and that the COMPLEMENT (any path outside the allowlist) is EMPTY. This catches stray drift that is neither controller-path nor in the allowlist, which the protected-list check alone would miss. Do NOT modify any controller-path file to make this pass; if the SAFE-13 check fails or the allowlist complement is non-empty, STOP and report — it means an earlier task strayed out of scope.</action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json')); assert d.get('passed') is True, d; assert d.get('controller_path_diff_count',1)==0 and d.get('att_config_diff_count',1)==0, d; print('SAFE-13 OK')" && python3 -c "import subprocess; allowed=('scripts/phase226-baseline-summary.py','scripts/phase226-thresholds.json'); out=subprocess.run(['git','diff','--name-only','v1.48'],capture_output=True,text=True).stdout.split(); stray=[p for p in out if not (p in allowed or p.startswith('tests/phase226/') or p.startswith('.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/'))]; assert not stray, ('paths outside allowlist',stray); print('allowlist clean; changed=',out)"</automated>
  </verify>
  <acceptance_criteria>
    - `safe13-boundary-check.json` has `passed: true`, `controller_path_diff_count: 0`, `att_config_diff_count: 0`. The comprehensive script (authoritative) covers the FULL protected set including `fusion_healer.py`, `src/wanctl/backends/`, and `configs/att.yaml`.
    - The positive allowlist check passes: `git diff --name-only v1.48` produces ONLY paths inside `scripts/phase226-baseline-summary.py`, `scripts/phase226-thresholds.json`, `tests/phase226/*`, and `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/*`; the complement (any path outside the allowlist) is empty.
    - No controller-path or ATT file appears in the change set; if it does, execution STOPS and reports out-of-scope drift.
  </acceptance_criteria>
  <done>SAFE-13 boundary re-verified via the comprehensive protected-set script (zero controller-path/ATT diff vs v1.48), AND the full change set is positively confirmed inside the Phase-226 evidence/tooling allowlist with an empty complement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained tc evidence files -> parser | `tc -s qdisc` text is operator-captured router output, parsed offline by `parse_tc_qdisc`. Not attacker-controlled in this deployment, but malformed/partial captures could mis-parse. |
| regenerated summary -> thresholds.json | Derived numeric (`NOISE_BAND_MS.value`) and provenance hash flow from parser output into the locked GATE-01 artifact. A silent parse error would re-poison the constant. |
| regenerated summary -> artifact-sha256.txt | The evidence hash manifest binds the committed derived artifacts to their bytes. A stale manifest (not refreshed after regeneration) would break the retained-evidence provenance story even if thresholds.json points at the new summary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering | `parse_tc_qdisc` on malformed/truncated tc text | mitigate | Regexes are anchored per-line with explicit units; non-matching lines are ignored rather than mis-assigned. Task 1 verify + Task 2 real-evidence tests assert non-zero packets/drops/backlog/delay from actual retained captures (spec-router for delay, spec-modem for backlog/peak), catching a regression to all-zero parsing. |
| T-226-02 | Information disclosure | regenerated `baseline-summary.json` / `BASELINE-SUMMARY.md` | accept | Inputs are tc counter rows and health NDJSON already committed in retained evidence; no secrets/credentials are read or emitted by the summary helper. No new disclosure surface introduced by the fix. |
| T-226-03 | Tampering / reverse-fitting | `NOISE_BAND_MS` constant in `thresholds.json` | mitigate | Value is derived only from already-captured baseline evidence (no live re-capture, no candidate data exists yet); `derived_from.sha256` is bound to the FINAL regenerated summary and asserted equal to BOTH the freshly-computed hash AND the manifest entry in Task 3 verify, preventing a stale, hand-edited, or skewed value from passing. |
| T-226-04 | Tampering / provenance staleness | `artifact-sha256.txt` evidence manifest | mitigate | Manifest is regenerated AFTER the summary/markdown are final, reusing the capture script's exact `find ... ! -name 'artifact-sha256.txt' ... \| sort -z \| xargs -0 sha256sum` form (no bespoke hashing). Task 3 verify runs `sha256sum -c artifact-sha256.txt` from the evidence dir (exit 0), proving every committed evidence file including the regenerated derived artifacts matches its recorded hash — corrected derivation, not retroactive raw-evidence mutation. |
| T-226-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan; uses existing venv (`.venv/bin/python`, `.venv/bin/pytest`) and stdlib only. No legitimacy gate required. |
</threat_model>

<verification>
Full closure check (run after all tasks):

```
.venv/bin/pytest tests/phase226/ -q
```
- Passes with the new real-format regression tests (>= 7 tests; backlog + peak delay proven).

```
cd .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z && sha256sum -c artifact-sha256.txt
```
- Exits 0: the refreshed manifest matches every evidence file including the regenerated summary + markdown.

```
python3 -c "import json,hashlib; S='.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/baseline/baseline-20260604T113435Z/baseline-summary.json'; s=json.load(open(S)); spreads={(i,t):v['tin_queue_delay_spread_ms'] for i,tins in s['interfaces'].items() for t,v in tins.items()}; assert any(x>0 for x in spreads.values()); assert any(20.0<=v<=30.0 for (i,t),v in spreads.items() if i=='spec-modem'); assert all(v<1.0 for (i,t),v in spreads.items() if i=='spec-router'); th=json.load(open('scripts/phase226-thresholds.json'))['TIN_SEPARATION']['NOISE_BAND_MS']; assert th['value']>0; assert th['derived_from']['sha256']==hashlib.sha256(open(S,'rb').read()).hexdigest(); print('AB-02 + GATE-01 closed')"
```

```
python3 -c "import json; d=json.load(open('.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json')); assert d['passed'] and d['controller_path_diff_count']==0 and d['att_config_diff_count']==0; print('SAFE-13 held')"
```
</verification>

<success_criteria>
- AB-02: `parse_tc_qdisc` reads real CAKE per-tin rows; regenerated `baseline-summary.json` reports non-zero packets/drops/backlog/delay deltas and non-zero `tin_queue_delay_spread_ms` (spec-modem ~20-30 ms band, spec-router < 1 ms). Real-format tests prove backlog + peak-delay parsing against retained evidence.
- GATE-01: `TIN_SEPARATION.NOISE_BAND_MS.value > 0`, re-provenanced (`derived_from.sha256` matches the FINAL regenerated summary AND the manifest entry, `derived_at` refreshed); `GATE-01-THRESHOLDS.md` still free of numeric literals. (The `>0` value is correct for THIS 3-run evidence; it is not asserted as a universal parser invariant.)
- Provenance integrity: `artifact-sha256.txt` refreshed (excluding itself); `sha256sum -c` exits 0; no skew between the thresholds summary hash and the manifest's summary entry. SUMMARY records old hash `186f4a72...`, new hash, and "raw run artifacts unchanged".
- Regression guard: real-format tests (no synthetic helper lines) present and passing; they would fail against the old parser.
- SAFE-13: zero controller-path/ATT diff vs v1.48 via the comprehensive protected-set script; full change set positively confirmed inside the Phase-226 allowlist (`scripts/phase226-baseline-summary.py`, `scripts/phase226-thresholds.json`, `tests/phase226/*`, `.planning/phases/226-.../*`) with empty complement.
- No live re-capture, no CAKE-mode change, no threshold-semantics change beyond filling the frozen constant.
</success_criteria>

<output>
Create `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-05-SUMMARY.md` when done. Record: the OLD invalid summary hash `186f4a72fa346d202e8c2fb20dea769a4950220e9cc373ffa4f3c307bab5a5f6`, the NEW summary hash, the recomputed `NOISE_BAND_MS.value`, and an explicit statement that raw run-NN tc/health artifacts are unchanged — only derived summaries + the hash manifest were corrected.
</output>
