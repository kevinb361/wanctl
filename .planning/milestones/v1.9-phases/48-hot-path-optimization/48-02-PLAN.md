---
phase: 48-hot-path-optimization
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified: []
autonomous: false
requirements: [OPTM-02, OPTM-03, OPTM-04]

must_haves:
  truths:
    - "OPTM-02 disposition documented: router communication already 0.0-0.2ms, no code change needed"
    - "OPTM-03 disposition documented: CAKE stats at 2s interval, not applicable to 50ms optimization"
    - "OPTM-04 production measurement requested: router CPU target <=40% after icmplib change"
  artifacts: []
  key_links: []
---

<objective>
Document profiling-evidence dispositions for OPTM-02 and OPTM-03 (no code changes per D5), and provide a checkpoint for OPTM-04 production router CPU measurement after icmplib deployment.

Purpose: Close out remaining Phase 48 requirements. OPTM-02/03 are satisfied by Phase 47 profiling evidence. OPTM-04 requires production measurement after the icmplib change from Plan 01 is deployed.
Output: Phase 48 requirements fully addressed -- OPTM-01 via code (Plan 01), OPTM-02/03 via evidence, OPTM-04 via production measurement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-hot-path-optimization/48-CONTEXT.md
@.planning/phases/47-cycle-profiling-infrastructure/PROFILING-ANALYSIS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify icmplib deployment and measure production impact</name>
  <what-built>
    Plan 01 replaced subprocess.run(["ping"]) with icmplib raw ICMP sockets in the RTT measurement hot path. This eliminates ~2-5ms subprocess fork overhead per cycle.

    **OPTM-02 (Router communication):** Phase 47 profiling shows 0.0-0.2ms average. Flash wear protection already prevents unnecessary API calls. No code change needed -- already optimized.

    **OPTM-03 (CAKE stats):** CAKE stats only read by steering daemon at 2s intervals, not the 50ms autorate hot path. Not applicable to this optimization phase.

    **OPTM-04 (Router CPU):** Needs production measurement before/after icmplib change.
  </what-built>
  <how-to-verify>
    **Step 1: Deploy icmplib change to production**
    - Build and deploy the updated container with icmplib dependency
    - Verify wanctl starts successfully (no SocketPermissionError in logs):
      `ssh cake-spectrum 'journalctl -u wanctl@spectrum --since "5 min ago" | grep -i "permission\|error\|icmp"'`

    **Step 2: Collect post-optimization profiling data (1 hour)**
    - Enable profiling: restart with `--profile` flag
    - Wait 1 hour for data collection
    - Collect profiling logs: `ssh cake-spectrum 'journalctl -u wanctl@spectrum --since "1 hour ago" | grep "Profiling report"'`

    **Step 3: Compare before/after metrics**
    - Phase 47 baseline (before): Spectrum avg 41.0ms, ATT avg 31.1ms
    - Target: avg cycle reduced by >=3ms (Spectrum <=38ms, ATT <=28ms)
    - Target: P99 Spectrum <=55ms, ATT <=33ms
    - Target: Zero "subprocess" references in profiling output

    **Step 4: Measure router CPU under load (OPTM-04)**
    - Run RRUL stress test or equivalent load
    - Monitor router CPU: `ssh 10.10.99.1 '/system resource monitor interval=1'` (or via Winbox)
    - Phase 47 baseline: 45% peak under RRUL
    - Target: <=40% peak
    - Note: If still >40% after icmplib change, capture as future work -- not a Phase 48 blocker per D5

    **Step 5: Verify no behavioral regression**
    - Confirm congestion detection still sub-second
    - Confirm rate adjustments working normally
    - Check health endpoints: `ssh cake-spectrum 'curl -s http://127.0.0.1:9101/health | python3 -m json.tool'`
  </how-to-verify>
  <resume-signal>
    Share the before/after profiling comparison numbers and router CPU measurement. Type "approved" if targets met, or describe any issues found. If router CPU is still >40%, note the actual value -- this will be captured as future work per D5 decision.
  </resume-signal>
</task>

</tasks>

<verification>
- OPTM-02: Documented as satisfied by profiling evidence (0.0-0.2ms avg router communication)
- OPTM-03: Documented as not applicable (CAKE stats at 2s interval)
- OPTM-04: Production router CPU measured before/after icmplib change
- No behavioral regressions in production
</verification>

<success_criteria>
- Production deployment of icmplib change is successful (no SocketPermissionError)
- Before/after profiling comparison shows >=3ms avg cycle reduction
- Router CPU measurement collected (target <=40%, actual value documented)
- Congestion detection responsiveness unchanged (sub-second)
- All four OPTM requirements have documented disposition
</success_criteria>

<output>
After completion, create `.planning/phases/48-hot-path-optimization/48-02-SUMMARY.md`
</output>
