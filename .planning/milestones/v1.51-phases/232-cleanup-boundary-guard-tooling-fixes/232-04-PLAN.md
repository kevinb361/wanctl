---
phase: 232-cleanup-boundary-guard-tooling-fixes
plan: 04
type: execute
wave: 3
depends_on: [232-01]
files_modified:
  - scripts/check-cleanup-boundary.sh
  - tests/test_cleanup_boundary_guard.py
autonomous: true
gap_closure: true
requirements: [BOUND-01]

must_haves:
  truths:
    - "The cleanup boundary guard fails closed when an anchor-present protected file is removed from the git index, even if identical file content remains in the worktree."
    - "The cleanup boundary guard treats protected manifest rows as files, not generic paths, and fails closed when a protected file is replaced by a directory."
    - "Anchor-present protected rows must remain tracked, while the intentionally anchor-absent future-doc row remains allowed to be untracked but must still be a regular file."
  artifacts:
    - path: "scripts/check-cleanup-boundary.sh"
      provides: "BOUND-01 fail-closed status classification for untracked and non-file protected rows"
      contains: "UNTRACKED"
    - path: "tests/test_cleanup_boundary_guard.py"
      provides: "Regression tests for git rm --cached and directory replacement bypasses"
      contains: "git rm --cached"
  key_links:
    - from: "tests/test_cleanup_boundary_guard.py"
      to: "scripts/check-cleanup-boundary.sh"
      via: "scratch-repo subprocess runs that mutate only temp git state"
      pattern: "rm.*--cached|mkdir"
    - from: "scripts/check-cleanup-boundary.sh"
      to: "git index state"
      via: "git ls-files --error-unmatch tracked check"
      pattern: "tracked"
---

<objective>
Close the Phase 232 verification gap in BOUND-01 by hardening the cleanup boundary guard against the two bypass states found during verification: git-index removal of an anchor-present protected file with content left in the worktree, and replacement of a protected file with a directory.

Purpose: BOUND-01 must be a fail-closed sweep gate before Phase 233. A guard that passes untracked protected files or protected directories does not protect the denylist.
Output: Targeted updates to `scripts/check-cleanup-boundary.sh` and `tests/test_cleanup_boundary_guard.py`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-VERIFICATION.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-01-SUMMARY.md
@scripts/check-cleanup-boundary.sh
@tests/test_cleanup_boundary_guard.py

<interfaces>
Existing guard contracts the executor must preserve:

From `scripts/check-cleanup-boundary.sh`:
```bash
ANCHOR="v1.50"
OUT=".planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/cleanup-boundary-check.json"
# Exit contract: 0 clean / 1 BOUND-01 VIOLATION / 2 usage or git/anchor error
--print-manifest prints: class<TAB>path<TAB>policy
```

Existing Python status fields in each JSON row:
```python
{
    "class": item_class,
    "path": path,
    "policy": policy,
    "anchor_present": anchor_present,
    "exists": exists,
    "tracked": tracked,
    "status": status,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for BOUND-01 bypass states</name>
  <files>tests/test_cleanup_boundary_guard.py</files>
  <read_first>
    - tests/test_cleanup_boundary_guard.py (existing scratch-repo helpers: `run_guard`, `_make_scratch_repo`, `row_for`)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-VERIFICATION.md lines describing the two failed spot-checks
  </read_first>
  <behavior>
    - `git rm --cached src/wanctl/autorate_continuous.py` in the scratch repo must make the guard return 1; JSON row status must be `UNTRACKED`; `tracked` must be `False`; stderr must contain `BOUND-01 VIOLATION`.
    - Replacing `scripts/phase231-rollback.sh` with a directory in the scratch repo must make the guard return 1; JSON row status must be `NON_FILE`; `exists` must be `True`; `is_file` must be `False`.
    - Existing future-doc anchor-absent behavior remains valid: the future doc may be untracked only when `anchor_present` is false, but deletion still returns 1.
  </behavior>
  <action>
    Extend `tests/test_cleanup_boundary_guard.py` with two failing regression tests before changing the guard:

    1. Add `test_guard_fails_closed_on_untracked_anchor_present_file`: create a scratch repo, run `git rm --cached src/wanctl/autorate_continuous.py` with `subprocess.run(["git", "rm", "--cached", "src/wanctl/autorate_continuous.py"], cwd=repo, check=True, ...)`, leave the worktree file in place, run the guard, and assert return code 1 plus the row details above.
    2. Add `test_guard_fails_closed_on_must_exist_directory_replacement`: create a scratch repo, `unlink()` `scripts/phase231-rollback.sh`, then `mkdir()` the same path, run the guard, and assert return code 1 plus the row details above.

    Do not mutate the real repository. Keep all bypass setup inside `_make_scratch_repo(tmp_path)` output. These tests should fail against the current guard before Task 2.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cleanup_boundary_guard.py` contains `test_guard_fails_closed_on_untracked_anchor_present_file`.
    - `tests/test_cleanup_boundary_guard.py` contains `git", "rm", "--cached"`.
    - `tests/test_cleanup_boundary_guard.py` contains `test_guard_fails_closed_on_must_exist_directory_replacement`.
    - The new tests assert statuses `UNTRACKED` and `NON_FILE` exactly.
  </acceptance_criteria>
  <done>Regression tests encode both verifier-found bypasses and are ready to pass once the guard is hardened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden cleanup boundary status classification</name>
  <files>scripts/check-cleanup-boundary.sh, tests/test_cleanup_boundary_guard.py</files>
  <read_first>
    - scripts/check-cleanup-boundary.sh (current Python heredoc status logic around `exists`, `tracked`, `worktree_blob`, and violations)
    - tests/test_cleanup_boundary_guard.py (new regression tests from Task 1)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-VERIFICATION.md (required missing items)
  </read_first>
  <behavior>
    - For every manifest row, `Path(path).is_file()` is the file-presence predicate; directories are violations.
    - If `anchor_present is True` and `tracked is False`, status is `UNTRACKED` and the guard exits 1, even when the worktree file content hashes equal the anchor blob.
    - If the path exists but is not a regular file, status is `NON_FILE` and the guard exits 1.
    - The future-doc anchor-absent row may remain untracked but must still be a regular file; if replaced by a directory it is `NON_FILE`.
  </behavior>
  <action>
    Update only the Python heredoc classification in `scripts/check-cleanup-boundary.sh`:

    - Add `is_file = path_obj.is_file()` and include `"is_file": is_file` in each JSON row.
    - Keep `exists = path_obj.exists()` for diagnostics, but stop using it as proof that a protected file is present.
    - Classification order must be:
      1. `not exists` → `MISSING`
      2. `not is_file` → `NON_FILE`
      3. `anchor_present and not tracked` → `UNTRACKED`
      4. `policy == "must-match-anchor" and anchor_present and worktree_oid != anchor_oid` → `MODIFIED`
      5. `policy == "must-exist" and anchor_present and worktree_oid != anchor_oid` → `allowlisted-modified`
      6. else → `ok`
    - Update the violation set from `{"MISSING", "MODIFIED"}` to `{"MISSING", "NON_FILE", "UNTRACKED", "MODIFIED"}`.
    - Preserve the existing exception by rule, not by special casing: the future-doc row has `anchor_present: false`, so `anchor_present and not tracked` is false; it still fails if missing or non-file.
    - Preserve JSON keys already consumed by verification (`proof_type`, `captured_utc`, `anchor`, `overall_pass`, `checks`, `tracked`, `status`).
    - Do not modify `src/wanctl/**`, deploy units, rollback scripts, or docs in this plan.
  </action>
  <verify>
    <automated>bash -n scripts/check-cleanup-boundary.sh && shellcheck scripts/check-cleanup-boundary.sh && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-cleanup-boundary.sh` contains `is_file = path_obj.is_file()`.
    - `scripts/check-cleanup-boundary.sh` contains status strings `NON_FILE` and `UNTRACKED`.
    - `scripts/check-cleanup-boundary.sh` has `anchor_present and not tracked` before hash-difference checks.
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` exits 0.
  </acceptance_criteria>
  <done>The guard exits non-zero for untracked anchor-present protected files and protected directory replacements, while preserving existing clean and anchor-absent future-doc behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run Phase 232 BOUND-01 closure verification</name>
  <files>scripts/check-cleanup-boundary.sh, tests/test_cleanup_boundary_guard.py</files>
  <read_first>
    - scripts/check-cleanup-boundary.sh
    - tests/test_cleanup_boundary_guard.py
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-VERIFICATION.md
  </read_first>
  <action>
    Run the closure commands that prove the verification gap is closed. Use `/tmp/bound01-gap-closure.json` for the evidence smoke output. Confirm the focused phase test slice still passes after the hardened guard. Confirm SAFE-15 surface discipline by checking there is no diff under `src/wanctl/` from this plan.
  </action>
  <verify>
    <automated>bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-gap-closure.json && python3 -c "import json; d=json.load(open('/tmp/bound01-gap-closure.json')); assert d['overall_pass'] is True and any(r['status']=='ok' for r in d['checks'])" && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py tests/test_phase231_rollback.py tests/test_phase231_migration_held.py tests/test_operator_digest.py -q && git diff --quiet -- src/wanctl/</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/bound01-gap-closure.json` has `overall_pass: true` on the clean repo.
    - Focused phase tests exit 0.
    - `git diff --quiet -- src/wanctl/` exits 0.
  </acceptance_criteria>
  <done>BOUND-01 gap-closure evidence passes, existing Phase 232 tooling tests remain green, and SAFE-15 controller-path discipline is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index/worktree state → guard verdict | The guard converts untrusted mutable repo state into an allow/block decision for cleanup sweep work. |
| scratch test repo → pytest assertion | Tests simulate malicious or accidental bypass states without touching the real worktree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-04-01 | Tampering | `scripts/check-cleanup-boundary.sh` status logic | mitigate | Treat non-files and untracked anchor-present rows as violations before hash comparison; add exact regression tests for both verifier-found bypasses. |
| T-232-04-02 | Repudiation | JSON evidence | mitigate | Preserve per-row `tracked`, add `is_file`, and emit exact `UNTRACKED`/`NON_FILE` status strings for auditability. |
| T-232-04-03 | Denial of Service | future-doc anchor-absent row | mitigate | Require only regular-file existence when `anchor_present` is false, preserving intentional untracked future-doc behavior while still rejecting missing/directories. |
| T-232-04-SC | Tampering | production/controller path | accept | Plan touches only guard script and tests; verification includes `git diff --quiet -- src/wanctl/`. |
</threat_model>

<verification>
- `bash -n scripts/check-cleanup-boundary.sh` passes.
- `shellcheck scripts/check-cleanup-boundary.sh` passes.
- `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` passes with tests covering `UNTRACKED` and `NON_FILE` bypass states.
- Focused Phase 232 regression slice passes: `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py tests/test_phase231_rollback.py tests/test_phase231_migration_held.py tests/test_operator_digest.py -q`.
- `git diff --quiet -- src/wanctl/` confirms no controller-path mutation.
</verification>

<success_criteria>
- Phase 232 verification truth 1 is now achievable: BOUND-01 guard fails closed if an anchor-present protected file is removed from git tracking or if a protected file path becomes a directory.
- Existing BOUND-01 semantics remain intact: clean repo passes; modified `must-match-anchor` rows fail; modified `must-exist` rows pass only when still regular files; missing protected rows fail.
- SAFE-15 discipline remains intact: no `src/wanctl/**` source diff.
</success_criteria>

<output>
After completion, create `.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-04-SUMMARY.md`
</output>
