---
phase: 233-gated-repo-hygiene-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/cake-autorate-trials/
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt
autonomous: false
requirements: [SWEEP-01]
user_setup: []

must_haves:
  truths:
    - "The 22 superseded one-off run_* trial scripts (plus parse_flent_summary.py and their result dirs) no longer exist on disk under .planning/cake-autorate-trials/"
    - "The future-doc denylist source WANCTL_CAKE_AUTORATE_FUTURE.md still exists and the BOUND-01 guard exits 0 after removal"
    - "The findings docs (SPECTRUM_CAKE_FINDINGS.md, *-review-*.md, *-findings-*.md) are preserved unless the operator approves their removal"
    - "An explicit, enumerable removal manifest was generated and approved before any rm; every disk entry under the trials dir is classified remove/keep"
  artifacts:
    - path: ".planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md"
      provides: "BOUND-01 denylist source; must-exist protected (deletion fails guard)"
      contains: "safe to remove soon"
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt"
      provides: "Explicit operator-approved remove/keep classification of every top-level entry in the trials dir"
      contains: "REMOVE"
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json"
      provides: "BOUND-01 guard evidence emitted post-removal (plan-specific filename, no collision with sibling Wave 1 plans or Phase 232 default)"
      contains: "passed"
  key_links:
    - from: "trial-script removal"
      to: "scripts/check-cleanup-boundary.sh"
      via: "post-removal guard run exits 0 with --out evidence/cleanup-boundary-233-01.json"
      pattern: "check-cleanup-boundary"
---

<objective>
Remove the superseded one-off `run_*` trial scripts (and their result dirs) from the
git-ignored `.planning/cake-autorate-trials/` subtree per the future-doc "safe to remove
soon" policy, while preserving the denylist-source doc and the findings/knowledge docs.
Removal is manifest-driven: every top-level entry in the trials dir is explicitly classified
remove/keep before any `rm`, so non-`run_*` dirs (`att`, `att-tuning`, `periodic`,
`__pycache__`, named experiment dirs, loose `.csv`/`.md` artifacts) have an explicit disposition.

Purpose: SWEEP-01 — consolidate the repo to the two-mode reality by removing experiment
tooling that is fully superseded by repo-owned deploy artifacts. The trial subtree is ~68M
of untracked one-offs with zero tracked references.
Output: The `run_*` trial scripts + their result dirs removed per an operator-approved manifest;
BOUND-01 guard evidence emitted at `evidence/cleanup-boundary-233-01.json` (plan-specific path)
showing the protected surface was not touched; the removal manifest committed as durable evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the explicit removal manifest and prove deletion safety</name>
  <read_first>
    - .planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md (SWEEP-01 Inventory section: the 22 run_* names, the keep-list, Pitfall 1, A3/A4 "delete not archive, keep findings .md")
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-01 deletion-safety pattern + critical constraints)
    - .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md (cleanup policy lines around "safe to remove soon"; the no-delete list — confirm what must be kept)
  </read_first>
  <action>
    Enumerate EVERY top-level entry in `.planning/cake-autorate-trials/` with
    `ls -1A .planning/cake-autorate-trials/` and write an explicit classification manifest to
    `.planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt`. Each line is
    `REMOVE <path>` or `KEEP <path> (<reason>)` — there must be NO unclassified entry. Classify per these rules:
    - REMOVE: every `run_*` one-off script (the 22 names in 233-RESEARCH.md SWEEP-01 Inventory:
      run_one_trial.sh, run_netperf_ping_trial.sh, run_att_netperf_trial.py, run_att_dev_bound_trial.py,
      run_att_dev_bound_full_trial.py, run_att_http_multidown.py, run_att_manual_tcp12down.py,
      run_spectrum_dev_bound_full_trial.py, run_spectrum_cap_sweep.py, run_spectrum_4mode_ladder.py,
      run_spectrum_cake_flags_sweep.py, run_spectrum_cake_placement.py, run_spectrum_dallas_same_window_ab.py,
      run_spectrum_dual_iperf_lower_cap_ladder.py, run_spectrum_first_hop_localization.py,
      run_spectrum_htb_fq_codel_alternating.py, run_spectrum_htb_fq_codel_ceiling_sweep.py,
      run_spectrum_path_localization.py, run_spectrum_qdisc_followup.py, run_spectrum_qdisc_offload_mechanics.py,
      run_spectrum_raw_vs_inline_ab.py, run_spectrum_same_window_path_variance.py), plus parse_flent_summary.py,
      plus `__pycache__/`, plus every result/output directory — both the timestamped dirs (matching
      `*-202[0-9]*Z`) AND the non-timestamped result-like dirs that are experiment OUTPUT, not knowledge:
      `att`, `att-tuning`, `att-multiflow-http`, `att-multiflow-manual`, `periodic`,
      `iperf3-controlled-smoke`, and every `spectrum-*` / `netperf-*` / `reflector-*` / `upload-check-*` /
      `measurement-*` result dir. Also REMOVE the loose `cloudflare-speedtest-*.csv`/`.md` measurement-output
      pair (raw output, not curated findings).
    - KEEP (never touch): WANCTL_CAKE_AUTORATE_FUTURE.md (BOUND-01 must-exist denylist source — deleting it
      fails the guard); SPECTRUM_CAKE_FINDINGS.md; spectrum-att-drain-isolated-production-test-*.md;
      spectrum-dallas-endpoint-review-*.md; and any other `*-review-*.md` / `*-findings-*.md` curated knowledge doc.
    For EACH path marked REMOVE, run the deletion-safety proof
    `git grep -F -l -- "<basename>" -- ':!.planning/cake-autorate-trials'` (fixed-string `-F` to avoid regex
    surprises from dots) and confirm it returns no output (zero tracked references); record the result inline
    in the manifest. Do NOT delete anything in this task — only produce the verified manifest. Do not rely on
    the boundary guard for deletion safety; the trial files are untracked and outside the guard manifest (Pitfall 1).
  </action>
  <verify>
    <automated>test -f .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt && ! grep -qiE '^(remove|keep) ' .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt && echo "manifest uses uppercase REMOVE/KEEP" ; for n in $(grep -i '^REMOVE ' .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt | awk '{print $2}' | xargs -n1 basename); do git grep -F -l -- "$n" -- ':!.planning/cake-autorate-trials' && echo "TRACKED REF FOUND: $n" && exit 1; done; echo "zero tracked refs for entire removal set"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/removal-manifest-233-01.txt` exists and classifies EVERY top-level entry from `ls -1A .planning/cake-autorate-trials/` as exactly one of `REMOVE <path>` or `KEEP <path> (<reason>)` — zero unclassified entries (count of manifest data lines equals count of `ls -1A` entries)
    - For EVERY basename on a `REMOVE` line, `git grep -F -l -- "<basename>" -- ':!.planning/cake-autorate-trials'` returns no output (zero tracked references) — the verify loop covers the full removal set, not a sample
    - WANCTL_CAKE_AUTORATE_FUTURE.md, SPECTRUM_CAKE_FINDINGS.md, every `*-review-*.md` and `*-findings-*.md` appear on `KEEP` lines (never REMOVE)
    - `test -f .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` exits 0 (nothing deleted yet)
  </acceptance_criteria>
  <done>Explicit remove/keep manifest covering every trials-dir entry produced; zero-tracked-reference proof recorded for every REMOVE entry; findings docs + FUTURE doc on the keep-list; nothing deleted yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator confirms destructive removal per the manifest</name>
  <what-built>
    An explicit removal manifest (`evidence/removal-manifest-233-01.txt`) classifying every top-level entry
    under `.planning/cake-autorate-trials/` as REMOVE or KEEP, with a per-name `git grep -F` zero-tracked-reference
    proof for every REMOVE entry. Non-`run_*` result dirs (`att`, `att-tuning`, `periodic`, named experiment dirs,
    `__pycache__`, loose `.csv`/`.md` raw output) are explicitly classified — no ambiguous scope. The denylist-source
    doc and curated findings docs are on the keep-list.
  </what-built>
  <how-to-verify>
    1. Read `evidence/removal-manifest-233-01.txt` end-to-end. Confirm every REMOVE line is genuinely superseded
       experiment tooling/output and every KEEP line is genuinely curated knowledge or the protected FUTURE doc.
    2. Pay attention to the non-timestamped dirs (`att`, `att-tuning`, `periodic`, `att-multiflow-*`,
       `iperf3-controlled-smoke`) — confirm these are experiment output to remove, not something to retain.
    3. Confirm the disposition: outright `rm` of the REMOVE set (recommended — "archiving" within an already-ignored
       subtree is a git no-op per RESEARCH A3). If you instead want a tracked archive, say so — that is a different
       (additive) task.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `rm` of exactly the REMOVE set in the manifest, or edit the manifest classification (name specific entries to move between REMOVE/KEEP) before approving.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove approved entries, emit BOUND-01 guard evidence, force-add durable artifacts</name>
  <read_first>
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (Evidence JSONs section: emit via --out, do not hand-author; Gate-before-and-after pattern)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-03-PLAN.md (the established `git add -f` + SKIP_DOC_CHECK=1 commit pattern for gitignored planning artifacts)
    - scripts/check-cleanup-boundary.sh (header / exit contract: 0 = clean, 1 = denylist violation, 2 = usage; default --out is the Phase 232 path — always pass --out here)
  </read_first>
  <action>
    Run `bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-pre-233-01.json` (throwaway /tmp path, NOT the
    Phase 232 default) and confirm exit 0 (pre-state clean). Remove exactly the operator-approved REMOVE set from
    `evidence/removal-manifest-233-01.txt` via `rm`/`rm -r` (iterate the REMOVE lines; do not delete any KEEP entry).
    Do NOT remove WANCTL_CAKE_AUTORATE_FUTURE.md, SPECTRUM_CAKE_FINDINGS.md, or any `*-review-*.md`/`*-findings-*.md`.
    Ensure the evidence dir `.planning/phases/233-gated-repo-hygiene-sweep/evidence/` exists, then run
    `bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json`
    (plan-specific filename — must NOT write the Phase 232 default path and must NOT collide with sibling Wave 1 plans)
    and confirm exit 0 (no protected surface touched). Run the guard regression test. Note in the SUMMARY that the
    trial subtree is git-ignored, so removal produces no `git` diff — this is expected. Because `.planning/` is
    gitignored, the new evidence JSON, the removal manifest, and `233-01-SUMMARY.md` will NOT be committed by a plain
    `git add` — force-add them: `git add -f .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json
    .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt
    .planning/phases/233-gated-repo-hygiene-sweep/233-01-SUMMARY.md` (use SKIP_DOC_CHECK=1 if the doc pre-commit hook
    fires) after running project-finalizer. Before committing, run `git status --short` and confirm only the three
    expected force-added planning artifacts are staged.
  </action>
  <verify>
    <automated>bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-01.json` exits 0 and the JSON does not report a denylist violation
    - The Phase 232 default evidence path (.planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/cleanup-boundary-check.json) is NOT modified by this plan (guard always invoked with an explicit --out)
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` exits 0 (9 passed)
    - For EVERY basename on a manifest REMOVE line, `ls .planning/cake-autorate-trials/<name> 2>/dev/null` returns nothing (entire removal set gone, not just a sample) AND no `run_*` entry remains: `ls .planning/cake-autorate-trials/run_* 2>/dev/null` returns nothing
    - `test -f .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` exits 0 and every KEEP-listed findings doc still exists
    - After commit, `git ls-files .planning/phases/233-gated-repo-hygiene-sweep/ | grep -E '(cleanup-boundary-233-01.json|removal-manifest-233-01.txt|233-01-SUMMARY.md)'` lists all three (force-add succeeded; artifacts durable, not silently uncommitted)
  </acceptance_criteria>
  <done>Approved REMOVE set deleted; no run_* entry remains; FUTURE doc and findings docs preserved; BOUND-01 guard exits 0 with plan-specific evidence; guard test green; evidence JSON + manifest + SUMMARY force-added and confirmed tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → git-tracked retention set | Untracked trial files are removed; the protected retention set (denylist) must remain intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-01 | Denial of Service | Accidental deletion of a still-referenced or protected file | mitigate | Explicit remove/keep manifest classifying every entry (Task 1) + per-name `git grep -F -l` zero-reference proof + BOUND-01 guard exit 0 post-removal (Task 3); FUTURE doc + findings docs on explicit keep-list |
| T-233-02 | Tampering | Collateral touch of a denylisted surface during `rm` | mitigate | `scripts/check-cleanup-boundary.sh --out <plan-specific>` run before (to /tmp) and after removal; exit 1 = revert |
| T-233-03 | Denial of Service | Destructive `rm` without operator intent | mitigate | `checkpoint:human-verify` (blocking-human) gates the actual removal against the reviewed manifest; `always_confirm_destructive` honored |
| T-233-13 | Repudiation | Evidence/manifest/summary silently uncommitted because `.planning/` is gitignored | mitigate | `git add -f` the evidence JSON, removal manifest, and SUMMARY; post-commit `git ls-files` acceptance check confirms they are tracked |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; RESEARCH omits the legitimacy audit by design (zero external packages) |
</threat_model>

<verification>
- Explicit removal manifest covers every trials-dir entry; zero unclassified
- BOUND-01 guard exits 0 after removal (`evidence/cleanup-boundary-233-01.json`, plan-specific path)
- Phase 232 default evidence path untouched (guard always invoked with --out)
- `tests/test_cleanup_boundary_guard.py` green (9 passed)
- No `run_*` entry remains; FUTURE doc + findings docs present
- Evidence JSON + manifest + SUMMARY force-added and confirmed tracked via `git ls-files`
</verification>

<success_criteria>
Superseded `run_*` one-off trial scripts (and their result dirs) are removed per an explicit
operator-approved manifest (or operator-chosen disposition applied); the BOUND-01 boundary guard
passes (exit 0) writing plan-specific evidence, confirming no denylisted/"not safe to remove yet"
surface was touched; the denylist-source doc and findings docs are preserved; the evidence,
manifest, and SUMMARY are force-added so they are durably tracked (SWEEP-01).
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-01-SUMMARY.md` when done
</output>
