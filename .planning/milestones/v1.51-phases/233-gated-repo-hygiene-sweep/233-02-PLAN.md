---
phase: 233-gated-repo-hygiene-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PROFILING.md
  - docs/PERFORMANCE.md
  - docs/RUNBOOK.md
  - docs/CABLE_TUNING.md
  - docs/STEERING.md
  - docs/SILICOM-BYPASS.md
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md
autonomous: false
requirements: [SWEEP-02]
user_setup: []

must_haves:
  truths:
    - "docs/PROFILING.md, docs/PERFORMANCE.md, and docs/RUNBOOK.md each carry a mode-disambiguation note that mentions external cake-autorate mode"
    - "Native wanctl@<wan> operational command examples are retained (annotation only, no deletion): the per-doc wanctl@ hit count does not drop from the captured pre-edit baseline"
    - "A per-hit disposition table classifies every remaining wanctl@ reference, proving no stale native-ownership claim remains uncovered"
    - "The operator-judgment docs (CABLE_TUNING, STEERING, SILICOM-BYPASS) are edited only per explicit operator decision"
  artifacts:
    - path: "docs/PROFILING.md"
      provides: "Profiling runbook with native/external mode disambiguation note"
      contains: "cake-autorate"
    - path: "docs/PERFORMANCE.md"
      provides: "Performance doc with mode disambiguation note"
      contains: "cake-autorate"
    - path: "docs/RUNBOOK.md"
      provides: "Operator runbook with mode disambiguation note"
      contains: "cake-autorate"
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md"
      provides: "Per-hit disposition table for every remaining wanctl@ reference + pre/post wanctl@ counts proving annotation-only"
      contains: "disposition"
  key_links:
    - from: "docs/PROFILING.md note"
      to: "README.md:265-268 mode-note template"
      via: "copied phrasing (cake-autorate-<wan>.service / state-bridge)"
      pattern: "cake-autorate-<wan>"
---

<objective>
Add a one-line mode-disambiguation note (native `wanctl@` vs external cake-autorate mode) to
the active operational docs that show `wanctl@<wan>` commands without any external-mode mention,
mirroring the canonical README phrasing. Do NOT delete native operational examples — native mode
is a real rollback/profiling procedure and is denylist-protected. SWEEP-02 closure is proven not
by "a note exists" but by a per-hit disposition table that classifies every remaining `wanctl@`
reference and pre/post `wanctl@` counts proving the edit was annotation-only.

Purpose: SWEEP-02 — close the residual native-ownership doc gap left after v1.50 Phase 231
DOCS-04. The DOCS-04 "no wanctl@ hits" claim is stale: PROFILING/PERFORMANCE/RUNBOOK carry
native-unit references with zero external-mode notes.
Output: Mode-disambiguation notes added to the 3 clear-residual docs; the 3 operator-judgment
docs handled per operator decision; a per-hit disposition table + pre/post count diff proves the
residual is clear and the edit deleted no native examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture wanctl@ baseline, then add mode-disambiguation notes to PROFILING, PERFORMANCE, RUNBOOK</name>
  <read_first>
    - README.md (lines ~263-268 — the canonical mode-note phrasing template to copy verbatim in intent)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-02 section: exact phrasing template, application rule, Pitfall 2 "do not delete native examples")
    - .planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md (SWEEP-02 Inventory table: which line ranges carry the native-unit hits per doc)
    - docs/PROFILING.md (current top + first wanctl@ command block)
    - docs/PERFORMANCE.md (current top + first wanctl@ command block)
    - docs/RUNBOOK.md (current top + the journalctl wanctl@ block near line 398)
  </read_first>
  <action>
    FIRST capture a reproducible pre-edit baseline of `wanctl@` hit counts across all 6 candidate docs and write it
    to `.planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md` under a "Pre-edit
    wanctl@ counts" heading: run `grep -c 'wanctl@' docs/PROFILING.md docs/PERFORMANCE.md docs/RUNBOOK.md
    docs/CABLE_TUNING.md docs/STEERING.md docs/SILICOM-BYPASS.md` and record each count. This baseline makes the
    "examples not deleted" criterion reproducible (it is not derivable post-edit otherwise).
    THEN, for each of docs/PROFILING.md, docs/PERFORMANCE.md, docs/RUNBOOK.md: insert ONE mode-disambiguation note
    near the top of the doc or immediately above the first `wanctl@<wan>` command block. The note must state that the
    examples assume native `wanctl@` mode and that in external cake-autorate mode the relevant units are
    `cake-autorate-<wan>.service` and `cake-autorate-<wan>-state-bridge.service` (and steering.service), with the
    state JSON / health contract remaining wanctl-compatible — mirror the README phrasing at README.md:265-268.
    Do NOT delete or alter any `systemctl`/`journalctl`/`wanctl@` command examples — native mode is a
    denylist-protected rollback/profiling procedure (Pitfall 2). Do NOT touch docs/DEPLOYMENT.md or docs/UPGRADING.md
    (must-exist guard rows; already compliant). The literal token "cake-autorate" must appear in each edited doc
    after the change, and the post-edit `wanctl@` count for each edited doc must be >= its captured baseline (a note
    adds no `wanctl@` tokens, so equality is expected — a DROP means an example was deleted).
  </action>
  <verify>
    <automated>for f in docs/PROFILING.md docs/PERFORMANCE.md docs/RUNBOOK.md; do c=$(grep -ciE 'cake-autorate|external mode' "$f"); [ "$c" -ge 1 ] || { echo "MISSING note: $f"; exit 1; }; done; echo "all 3 annotated"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/sweep02-disposition-233-02.md` records a pre-edit `wanctl@` count for all 6 candidate docs (reproducible baseline)
    - `grep -ciE 'cake-autorate|external mode' docs/PROFILING.md` returns >= 1 (and same for PERFORMANCE.md, RUNBOOK.md)
    - For each edited doc, post-edit `grep -c 'wanctl@' <doc>` >= the recorded baseline (no native command example deleted) — assert via comparison to the baseline numbers in the disposition file, not a bare count
    - `bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-233-02-task1.json` exits 0 (no must-exist/must-match-anchor doc collaterally damaged; throwaway /tmp path, never the Phase 232 default)
  </acceptance_criteria>
  <done>Pre-edit wanctl@ baseline captured; the 3 clear-residual docs each carry a mode-disambiguation note mentioning external cake-autorate mode; native command examples retained (post-edit count >= baseline); boundary guard exits 0 via explicit --out.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator decides disposition of the 3 judgment-call docs</name>
  <decision>Whether to add a mode-disambiguation note to docs/CABLE_TUNING.md, docs/STEERING.md, and docs/SILICOM-BYPASS.md.</decision>
  <context>
    These three docs carry native `wanctl@<wan>` references but RESEARCH classifies them as
    judgment calls: CABLE_TUNING.md is mostly historical past-tense tuning narrative; SILICOM-BYPASS.md
    references the native unit by design (the bypass watchdog targets `wanctl@<wan>.service` as the
    thing being stopped/started). STEERING.md is the standout: inspection (docs/STEERING.md:321,331)
    shows LIVE OPERATIONAL commands `ssh cake-spectrum 'sudo systemctl stop wanctl@spectrum'` and
    `... start wanctl@spectrum` — these read as a current procedure, not historical narrative, so the
    review flagged `annotate-none` as too weak for STEERING.md. SWEEP-02's bar is "describes Spectrum/ATT
    as native-wanctl-owned rate control WITHOUT noting external mode" — live operational stop/start examples
    meet that bar; pure historical narrative and by-design watchdog references may not. None are guard-protected;
    all are freely editable. Recommended default: annotate STEERING.md (its commands are operational, not
    historical) and leave CABLE_TUNING.md / SILICOM-BYPASS.md as-is (historical / by-design).
  </context>
  <options>
    <option id="annotate-steering-only">
      <name>Annotate STEERING.md only (recommended default)</name>
      <pros>Covers the doc with verified live operational stop/start wanctl@ examples; leaves clearly-historical (CABLE_TUNING) and by-design (SILICOM-BYPASS) docs untouched; matches the review's "annotate STEERING unless proven historical-only" guidance</pros>
      <cons>Leaves a residual native-unit mention in CABLE_TUNING/SILICOM-BYPASS without an external-mode note (acceptable if classified historical/by-design in the disposition table)</cons>
    </option>
    <option id="annotate-none">
      <name>Leave all 3 as-is</name>
      <pros>No edits to historical narrative or by-design references; lowest edit footprint</pros>
      <cons>STEERING.md's live operational wanctl@ commands keep a stale current-ownership implication with no external-mode note — review flagged this as too weak</cons>
    </option>
    <option id="annotate-all-three">
      <name>Annotate all 3 with the standard note</name>
      <pros>Uniform disambiguation everywhere a native unit is named</pros>
      <cons>Adds notes to historical/by-design content where the note is arguably noise</cons>
    </option>
  </options>
  <resume-signal>Select: annotate-steering-only, annotate-none, annotate-all-three (or name specific docs to annotate).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply operator decision and prove SWEEP-02 residual is clear via per-hit disposition table</name>
  <read_first>
    - README.md (lines ~265-268 — note phrasing template)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-02 phrasing template + application rule)
    - .planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md (the pre-edit baseline captured in Task 1)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-03-PLAN.md (the `git add -f` + SKIP_DOC_CHECK=1 commit pattern for gitignored planning artifacts)
    - docs/CABLE_TUNING.md, docs/STEERING.md, docs/SILICOM-BYPASS.md (only the doc(s) the operator chose to annotate)
  </read_first>
  <action>
    Apply exactly the operator's Task 2 decision: if annotate-none, make no judgment-doc edits in this task;
    otherwise add the same one-line mode-disambiguation note (mirroring README.md:265-268) to each operator-selected
    doc, near the top or above the first `wanctl@<wan>` block, without deleting any existing examples.
    THEN complete the disposition table in `evidence/sweep02-disposition-233-02.md`: enumerate EVERY remaining
    `wanctl@` hit across all 6 docs with `grep -n 'wanctl@' docs/PROFILING.md docs/PERFORMANCE.md docs/RUNBOOK.md
    docs/CABLE_TUNING.md docs/STEERING.md docs/SILICOM-BYPASS.md`, and for each hit assign exactly one disposition:
    `native-mode example`, `historical note`, `by-design bypass reference`, or `covered by nearby external-mode note`.
    Record post-edit `wanctl@` counts and assert each is >= the Task 1 baseline (no example deleted). The table is the
    SWEEP-02 closure proof — every hit must be classified, and no hit may be left as an uncovered stale current-ownership
    claim. Run the boundary guard with an explicit --out and the guard regression test to confirm no protected surface
    was touched. Because `.planning/` is gitignored, force-add the disposition evidence and SUMMARY:
    `git add -f .planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md
    .planning/phases/233-gated-repo-hygiene-sweep/233-02-SUMMARY.md` (plus the doc edits via normal `git add`).
    Run project-finalizer; commit (use SKIP_DOC_CHECK=1 only if the doc pre-commit hook blocks a hygiene-only commit).
    Before committing run `git status --short` and confirm only the expected docs + force-added planning artifacts are staged.
  </action>
  <verify>
    <automated>bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-233-02-task3.json && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q && grep -qi 'disposition' .planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-233-02-task3.json` exits 0 (explicit --out; Phase 232 default path never written by this plan)
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` exits 0
    - `evidence/sweep02-disposition-233-02.md` contains a per-hit disposition row for EVERY `wanctl@` hit returned by `grep -n 'wanctl@' <all 6 docs>` — each hit classified native-mode/historical/by-design/covered, with zero hits left as an uncovered current-ownership claim
    - The disposition file records post-edit `wanctl@` counts >= the Task 1 baseline for all 6 docs (annotation-only proven, not assumed)
    - For every doc the operator chose to annotate, `grep -ciE 'cake-autorate|external mode' <doc>` returns >= 1
    - After commit, `git ls-files | grep -E 'sweep02-disposition-233-02.md|233-02-SUMMARY.md'` lists both (force-add succeeded; evidence durable)
  </acceptance_criteria>
  <done>Operator decision applied; per-hit disposition table classifies every remaining wanctl@ hit; post-edit counts >= baseline (no examples deleted); boundary guard exits 0 via explicit --out; disposition evidence + SUMMARY force-added and confirmed tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc edit → guard-protected doc set | Hygiene annotations must not delete or corrupt must-exist / must-match-anchor docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-04 | Tampering | A doc edit misstates the production mode (e.g., claims native owns rates) | mitigate | Note copies the verified README:265-268 phrasing (external mode owns rates); operator reviews judgment docs in Task 2 |
| T-233-05 | Denial of Service | Deleting a native operational example that is the documented rollback/profiling procedure | mitigate | Annotation-only rule (Pitfall 2); pre-edit `wanctl@` baseline captured + post-edit count >= baseline assertion proves no example deleted; boundary guard exit 0 |
| T-233-06 | Tampering | Collateral edit to a must-exist (DEPLOYMENT/UPGRADING) or must-match-anchor doc | mitigate | Those docs explicitly out of edit scope; `scripts/check-cleanup-boundary.sh --out <explicit>` exit 0 confirms untouched |
| T-233-14 | Repudiation | SWEEP-02 falsely closed by a weak "a note exists" grep while a stale native-ownership claim remains | mitigate | Per-hit disposition table classifies every remaining `wanctl@` hit; closure requires zero uncovered current-ownership claims, not just `grep >= 1` |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- PROFILING/PERFORMANCE/RUNBOOK each grep >= 1 for `cake-autorate|external mode`
- Pre/post `wanctl@` counts captured; post >= baseline for all 6 docs (annotation-only)
- Per-hit disposition table classifies every remaining `wanctl@` hit (no uncovered ownership claim)
- Boundary guard exits 0 via explicit --out; guard test green
- Operator-decision docs handled per Task 2; per-doc disposition recorded
- Disposition evidence + SUMMARY force-added and confirmed tracked
</verification>

<success_criteria>
No active doc describes Spectrum/ATT as native-wanctl-owned rate control without noting the
current external cake-autorate mode — proven by a per-hit disposition table where every remaining
`wanctl@` reference is classified and none is an uncovered current-ownership claim; native
operational examples retained (post-edit count >= captured baseline); boundary guard (explicit --out)
confirms no protected doc surface damaged (SWEEP-02).
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-02-SUMMARY.md` when done
</output>
