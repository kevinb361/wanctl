---
phase: 234-planning-metadata-reconciliation-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.md
  - .planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.json
  - .planning/todos/closed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md
  - .planning/todos/closed/2026-04-28-add-silicom-bypass-test-harness.md
  - .planning/todos/pending/2026-04-28-add-silicom-bypass-nic-operational-tooling.md
  - .planning/todos/pending/2026-04-28-add-silicom-bypass-test-harness.md
autonomous: true
requirements: [META-01, META-02]
must_haves:
  truths:
    - "All 12 quick-archive slugs are indexed with a disposition; none are deleted"
    - "The PLAN-only slug (005) is distinguished from the 11 shipped-with-SUMMARY slugs in the index"
    - "Neither silicom todo simultaneously claims pending and completed state"
    - "Both stale pending silicom todos are closed-with-pointer to SEED-006 (moved to closed/, not deleted, not false-closed)"
    - "SEED-006 remains the single canonical dormant carrier, content-hash-unchanged (proven by git hash-object before/after, since SEED-006 is untracked and git diff is blind to it)"
  artifacts:
    - path: ".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.md"
      provides: "Human-readable per-slug disposition table for the 12 orphan quick-task slugs"
      contains: "archived-in-place"
    - path: ".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.json"
      provides: "Machine-assertable index of all 12 slugs with classification + tracked status + the exact expected slug set"
      contains: "005-fix-watchdog-safe-startup-maintenance"
    - path: ".planning/todos/closed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md"
      provides: "Close-with-pointer record for silicom operational-tooling todo"
      contains: "closed_by_phase: 234"
    - path: ".planning/todos/closed/2026-04-28-add-silicom-bypass-test-harness.md"
      provides: "Close-with-pointer record for silicom test-harness todo"
      contains: "closed_by_phase: 234"
  key_links:
    - from: ".planning/todos/closed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md"
      to: ".planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md"
      via: "verdict + Resolution body pointer"
      pattern: "consolidated_into_SEED-006|SEED-006"
    - from: ".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.md"
      to: ".planning/milestones/quick-archive/"
      via: "per-slug enumeration"
      pattern: "260503-cfs"
---

<objective>
Reconcile the two purely-bookkeeping carry-forward items: index the 12 orphan
quick-task slugs (META-01) and close the 2 stale `pending/` silicom todo
duplicates with SEED-006 pointers (META-02). Both are disjoint `.planning/`
metadata surfaces; no controller code, no `src/wanctl/`, no deletions.

Purpose: Make the `.planning/` tree internally consistent so the deferred-items
ledger reflects the real on-disk state and no artifact claims two contradictory
states. This is assembly from existing in-repo precedents, not construction.

Output: A dual-format quick-archive index under the phase `evidence/` dir, and
two close-with-pointer todos moved from `pending/` to `closed/`. STATE.md ledger
and SAFE-15 boundary proof are handled in Plan 234-02 (so SAFE-15 captures the
commits from this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/234-planning-metadata-reconciliation-closeout/234-RESEARCH.md
@.planning/phases/234-planning-metadata-reconciliation-closeout/234-PATTERNS.md

# META-01 source: the 12 already-archived slugs
@.planning/milestones/quick-archive/

# META-02 sources: canonical carrier (LEAVE UNCHANGED) + close-with-pointer precedent
@.planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md
@.planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md

<project_convention>
`.planning/` is gitignored in this repo. Committing planning artifacts requires
`git add -f`. The `gsd-sdk query commit` helper fails on these paths — use git
directly with the project's commit-message convention (`docs(234): ...`), and
route the commit through the mandatory project-finalizer workflow per CLAUDE.md.
This plan touches ZERO `src/wanctl/` files — that IS the SAFE-15 invariant.

CRITICAL — git visibility (drives this plan's verification design): SEED-006 and
both `completed/` silicom copies are git-UNTRACKED (force-add-only paths). Most
quick-archive slugs are untracked too (11/12). Therefore `git diff --quiet` and
`git status --porcelain` are BLIND to changes/deletions of these files and can
false-pass. Where a file must be proven unchanged, this plan uses
`git hash-object`/`sha256sum` before/after comparison. Where deletions must be
caught, this plan asserts the exact expected on-disk name set, not git status.
</project_convention>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the META-01 quick-archive index (12 slugs, none deleted)</name>
  <files>.planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.md, .planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.json</files>
  <read_first>
    - .planning/phases/234-planning-metadata-reconciliation-closeout/234-PATTERNS.md (the "META-01 Slug Classification" table is the verified, pre-computed disposition source; the "Classification manifest" pattern section gives the manifest shape)
    - .planning/phases/233-gated-repo-hygiene-sweep/evidence/removal-manifest-233-01.txt (header-comment + one-row-per-entry classification-manifest precedent)
    - .planning/phases/233-gated-repo-hygiene-sweep/evidence/sweep02-disposition-233-02.md (markdown disposition-table + pre/post-count precedent)
    - .planning/milestones/quick-archive/ (the 12 slug dirs being indexed)
  </read_first>
  <action>
    Enumerate the 12 directories under `.planning/milestones/quick-archive/` and write a dual-format index to the phase `evidence/` dir. Do NOT run literal `/gsd-cleanup` — these slugs are already archived-in-place and are not phase dirs, so the workflow would do nothing; this is a `/gsd-cleanup`-STYLE index only.

    Classify each slug exactly per the verified 234-PATTERNS.md "META-01 Slug Classification" table: 11 of 12 are `shipped` (have a `*-SUMMARY.md`), and exactly one — `005-fix-watchdog-safe-startup-maintenance` — is `PLAN-only` (no SUMMARY). Record git-tracked status: 11 untracked, only `260503-cfs-fix-spectrum-alerting-severity` is tracked. Confirm classification at runtime with `ls <slug>/ | grep -i summary` per slug and `git ls-files .planning/milestones/quick-archive/<slug>/` for tracked status; document that verification method in the manifest header comments (mirroring removal-manifest-233-01.txt).

    The 12 expected slug names (the binding "none deleted" set — the JSON `slugs[].slug` values MUST equal this set exactly) are: `001-rename-phase2b-to-confidence-based-steer`, `002-fix-health-version`, `003-remove-deprecated-sample-params`, `004-fix-socket-warnings`, `005-fix-watchdog-safe-startup-maintenance`, `6-lan-accessible-health-endpoints-and-dual`, `7-fix-flapping-alert-bugs-rule-name-mismat`, `8-fix-flapping-alert-detection-cooldown-ke`, `260319-lk3-fix-state-file-persistence-and-tuning-pa`, `260320-9wi-update-readme-and-config-schema-docs-for`, `260327-uy3-add-spike-detector-confirmation-counter-`, `260503-cfs-fix-spectrum-alerting-severity`.

    `quick-archive-index.md`: a markdown disposition table with one row per slug carrying columns Slug, Classification (`shipped`/`PLAN-only`), Git-tracked (`yes`/`no`), Disposition (`archived-in-place (none deleted)` for all 12). Include a pre/post no-deletion proof line asserting `ls -d .planning/milestones/quick-archive/*/ | wc -l == 12` before and after. Add a header note that META-01 resolves these as archived-in-place with a pointer index, not via deletion, and that 11/12 being untracked is exactly why "none deleted" is the binding rule (git status cannot see an untracked-slug deletion).

    `quick-archive-index.json`: a machine-assertable object with `requirement: "META-01"`, `total: 12`, `disposition: "archived-in-place"`, `none_deleted: true`, and a `slugs` array of 12 entries each `{slug, classification, git_tracked}`. The `005-fix-watchdog-safe-startup-maintenance` entry MUST carry `classification: "PLAN-only"`; the other 11 `classification: "shipped"`; only `260503-cfs-fix-spectrum-alerting-severity` `git_tracked: true`. The set of `slugs[].slug` values MUST equal the 12 expected names above.

    Do not modify, move, or delete anything under `.planning/milestones/quick-archive/`. This task only reads that tree and writes the two evidence files.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import json, os, glob, subprocess, sys
EXPECTED = {
    "001-rename-phase2b-to-confidence-based-steer",
    "002-fix-health-version",
    "003-remove-deprecated-sample-params",
    "004-fix-socket-warnings",
    "005-fix-watchdog-safe-startup-maintenance",
    "6-lan-accessible-health-endpoints-and-dual",
    "7-fix-flapping-alert-bugs-rule-name-mismat",
    "8-fix-flapping-alert-detection-cooldown-ke",
    "260319-lk3-fix-state-file-persistence-and-tuning-pa",
    "260320-9wi-update-readme-and-config-schema-docs-for",
    "260327-uy3-add-spike-detector-confirmation-counter-",
    "260503-cfs-fix-spectrum-alerting-severity",
}
# On-disk slug set must EXACTLY equal the expected 12 (catches any deletion,
# even of untracked dirs that git status cannot see).
on_disk = {os.path.basename(p.rstrip("/")) for p in glob.glob(".planning/milestones/quick-archive/*/")}
assert on_disk == EXPECTED, f"on-disk slug set drift: missing={EXPECTED-on_disk} unexpected={on_disk-EXPECTED}"
idx = ".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.json"
d = json.load(open(idx))
assert d["total"] == 12 and len(d["slugs"]) == 12, "index total/len mismatch"
assert d["none_deleted"] is True and d["disposition"] == "archived-in-place"
assert {s["slug"] for s in d["slugs"]} == EXPECTED, "index slug set != expected 12"
assert sum(1 for s in d["slugs"] if s["classification"] == "PLAN-only") == 1
assert sum(1 for s in d["slugs"] if s.get("git_tracked")) == 1
md = open(".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/quick-archive-index.md").read()
assert "archived-in-place" in md and "005-fix-watchdog-safe-startup-maintenance" in md
print("META-01 index OK: 12/12 slugs present, none deleted")
PY</automated>
  </verify>
  <acceptance_criteria>
    - The on-disk slug set under `.planning/milestones/quick-archive/*/` equals the exact 12-name expected set (the Python assertion exits non-zero on ANY missing or unexpected slug — including deletion of an untracked slug git status cannot see)
    - `quick-archive-index.json` parses; `total==12`, `len(slugs)==12`, `none_deleted==true`, `disposition=="archived-in-place"`, the `slugs[].slug` set equals the 12 expected names, exactly 1 slug `classification=="PLAN-only"` (`005`), exactly 1 slug `git_tracked==true` (`260503-cfs`)
    - `quick-archive-index.md` exists, contains the string `archived-in-place`, and lists `005-fix-watchdog-safe-startup-maintenance` as `PLAN-only`
    - The verify command is a single Python heredoc that exits non-zero (fails the task) if any assertion fails — there is no `|| echo`-style branch that swallows the failure
  </acceptance_criteria>
  <done>The 12 orphan quick-task slugs are indexed in dual format with per-slug classification and tracked status, all dispositioned archived-in-place, and a fail-closed set-equality assertion proves none were deleted (even untracked ones invisible to git).</done>
</task>

<task type="auto">
  <name>Task 2: Close the 2 stale pending silicom todos with SEED-006 pointers (META-02)</name>
  <files>.planning/todos/closed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md, .planning/todos/closed/2026-04-28-add-silicom-bypass-test-harness.md, .planning/todos/pending/2026-04-28-add-silicom-bypass-nic-operational-tooling.md, .planning/todos/pending/2026-04-28-add-silicom-bypass-test-harness.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-28-add-silicom-bypass-nic-operational-tooling.md (stale duplicate to move; preserve its created/title/area/files frontmatter)
    - .planning/todos/pending/2026-04-28-add-silicom-bypass-test-harness.md (second stale duplicate to move)
    - .planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md (EXACT close-with-pointer precedent: frontmatter keys + `## Resolution` body shape)
    - .planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md (canonical dormant carrier — read to cite it correctly; do NOT edit it)
    - .planning/todos/completed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md (the already-consolidated completed copy that MUST stay byte-unchanged — read to cite, do NOT edit)
    - .planning/todos/completed/2026-04-28-add-silicom-bypass-test-harness.md (second completed copy that MUST stay byte-unchanged)
    - .planning/phases/234-planning-metadata-reconciliation-closeout/234-PATTERNS.md (the META-02 "close-with-pointer" application section with exact frontmatter keys and Resolution wording)
  </read_first>
  <action>
    BEFORE touching anything, capture content hashes of the three files that MUST remain byte-unchanged (SEED-006 and the two `completed/` copies). These are all git-UNTRACKED, so `git diff --quiet` is blind to them and would false-pass; a content hash is the only sound proof. Run and record the output:

    `git hash-object .planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md .planning/todos/completed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md .planning/todos/completed/2026-04-28-add-silicom-bypass-test-harness.md`

    Save those three before-hashes (paste them into the SUMMARY). For reference, SEED-006's current hash at planning time is `95f8ed3c86687ca03412b4ddec59aa487309c46a` — the after-hash MUST equal whatever the before-hash run produced (do not hard-code the planning-time value as the gate; capture fresh at execution time, since SEED-006 is dormant and should not have changed).

    Move (do not copy, do not `rm`) both `pending/` silicom todos into `.planning/todos/closed/`, preserving their existing frontmatter (`created`, `title`, `area`, and the `files:` planned list) and adding two keys mirroring the digest-permission precedent: `closed_by_phase: 234` and `verdict: consolidated_into_SEED-006_canonical_dormant_carrier`. The `pending/` copies are git-TRACKED, so use `git mv` (NOT plain mv) so the move is visible to git; verify the `pending/` paths no longer exist afterward.

    Append a `## Resolution` body to each closed todo stating: this is a stale duplicate of the corresponding `.planning/todos/completed/2026-04-28-add-silicom-bypass-*.md` copy, which was already consolidated into `.planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md` (Phase A) on 2026-05-26; SEED-006 is the single canonical dormant carrier; the underlying bypass-watchdog work is operationally real (the ATT migration hit a live bypass-watchdog failure mode per STATE.md) and is explicitly NOT false-closed — it remains deferred to v1.52 scoping under SEED-006; this entry only removes the duplicate pending claim. Reference META-02 and Phase 234 Plan 01 by name.

    Leave SEED-006 and both `completed/` copies byte-unchanged — they already carry the canonical state and (for the completed copies) the `## Closure — 2026-05-26` footer. Do not edit them. After the moves and Resolution edits, re-run `git hash-object` on the same three files and assert each after-hash equals its before-hash.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import os, subprocess, sys
def h(p):
    return subprocess.check_output(["git", "hash-object", p], text=True).strip()
UNCHANGED = [
    ".planning/seeds/SEED-006-v145-silicom-bypass-tooling-and-harness.md",
    ".planning/todos/completed/2026-04-28-add-silicom-bypass-nic-operational-tooling.md",
    ".planning/todos/completed/2026-04-28-add-silicom-bypass-test-harness.md",
]
# The SUMMARY must contain the before-hashes; for an automated gate we re-derive
# the invariant differently: these dormant files must match their committed-in-
# SUMMARY before-hash. Here we assert they parse and that the move did not touch
# them by comparing against the recorded before-hashes file the executor writes.
rec = ".planning/phases/234-planning-metadata-reconciliation-closeout/evidence/seed006-unchanged-hashes.txt"
assert os.path.exists(rec), f"missing before/after hash record {rec} (executor must write it)"
lines = [l.strip() for l in open(rec) if l.strip() and not l.startswith("#")]
recorded = {}
for l in lines:
    sha, path = l.split(None, 1)
    recorded[path] = sha
for p in UNCHANGED:
    assert p in recorded, f"{p} not in hash record"
    assert h(p) == recorded[p], f"CONTENT CHANGED: {p} now {h(p)} != recorded {recorded[p]}"
# pending gone, closed present with pointer frontmatter + SEED-006 pointer
for stem in ["nic-operational-tooling", "test-harness"]:
    pend = f".planning/todos/pending/2026-04-28-add-silicom-bypass-{stem}.md"
    clo = f".planning/todos/closed/2026-04-28-add-silicom-bypass-{stem}.md"
    assert not os.path.exists(pend), f"pending still exists: {pend}"
    assert os.path.exists(clo), f"closed missing: {clo}"
    body = open(clo).read()
    assert "closed_by_phase: 234" in body, f"missing closed_by_phase in {clo}"
    assert "consolidated_into_SEED-006" in body, f"missing verdict in {clo}"
    assert "SEED-006" in body, f"missing SEED-006 pointer in {clo}"
    assert "v1.52" in body, f"missing v1.52 deferral note in {clo}"
print("META-02 OK: pending moved to closed with SEED-006 pointer; SEED-006 + completed copies hash-unchanged")
PY</automated>
  </verify>
  <acceptance_criteria>
    - A before/after hash record file `evidence/seed006-unchanged-hashes.txt` exists listing `git hash-object` SHAs for SEED-006 and both `completed/` copies; the live `git hash-object` of each equals its recorded SHA (proves byte-unchanged via content hash, NOT `git diff --quiet`, because these files are untracked)
    - Neither `pending/2026-04-28-add-silicom-bypass-nic-operational-tooling.md` nor `pending/2026-04-28-add-silicom-bypass-test-harness.md` exists
    - Both files now exist under `.planning/todos/closed/` with frontmatter key `closed_by_phase: 234` and `verdict: consolidated_into_SEED-006_canonical_dormant_carrier`
    - Each closed todo's `## Resolution` body cites `SEED-006`, states the work is NOT false-closed, and references it remaining deferred to `v1.52`
    - The verify command is a single Python heredoc that exits non-zero on any failed assertion — no `|| echo`-style branch swallows a failure
    - The move used `git mv` (pending copies are tracked); no `rm` was used: each pending file's content survives in its closed counterpart (created/title/area frontmatter preserved)
  </acceptance_criteria>
  <done>Both stale pending silicom todos are closed-with-pointer to SEED-006 via git mv, no duplicate pending/completed claim remains, the operationally real work is not false-closed, and SEED-006 plus both completed copies stay canonical and content-hash-unchanged (proven by hash compare, not git diff).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor intent → `.planning/` filesystem | A metadata edit could silently destroy or misrepresent planning history; this is the only meaningful boundary (no network, no user input, no auth). Compounded by git-untracked paths where git status/diff are blind. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-01 | Tampering / data-loss | `.planning/milestones/quick-archive/` (11/12 untracked → deletions invisible to git) | mitigate | META-01 indexes archived-in-place; verify asserts the on-disk slug set EXACTLY equals the expected 12-name set in a fail-closed Python check (catches untracked-slug deletion git status cannot see); no `rm`/`git rm` permitted |
| T-234-02 | Repudiation / data-loss | SEED-006 + completed silicom copies (all git-untracked) | mitigate | Close-with-pointer (git mv, not delete); `git hash-object` before/after content-hash equality proves SEED-006 and both completed copies byte-unchanged (`git diff --quiet` is BLIND to these untracked files and is NOT used); Resolution body forbids false-closing operationally real bypass-watchdog work |
| T-234-03 | Tampering | `src/wanctl/` controller path (SAFE-15) | mitigate | This plan declares zero `src/wanctl/` files; SAFE-15 boundary proof in Plan 234-02 captures all 234 commits and asserts `controller_path_diff_count: 0` |
| T-234-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A); slopcheck gate moot |
</threat_model>

<verification>
- On-disk slug set under `.planning/milestones/quick-archive/*/` equals the exact 12-name set (fail-closed Python assertion; no slug deleted, including untracked ones)
- `quick-archive-index.{md,json}` exist and classify all 12 slugs (1 PLAN-only `005`, 1 tracked `260503-cfs`)
- Both silicom pending todos absent; both present under `closed/` with `closed_by_phase: 234` + SEED-006 pointer + v1.52 deferral note
- `git hash-object` after-hash == recorded before-hash for SEED-006 and both `completed/` copies (content-hash proof; `git diff --quiet` deliberately NOT used on these untracked files)
- `git status --porcelain -- src/wanctl/` is empty (SAFE-15 not perturbed by this plan)
</verification>

<success_criteria>
- META-01: 12 orphan quick-task slugs indexed/archived-in-place with pointer, none deleted (proven by exact-set assertion), ledger-ready disposition recorded
- META-02: silicom todos and SEED-006 reconciled to a single canonical state (SEED-006 canonical dormant carrier; pending dupes closed-with-pointer), no false-closing of real bypass work, SEED-006 + completed copies content-hash-unchanged
- Zero `src/wanctl/` changes
</success_criteria>

<output>
Create `.planning/phases/234-planning-metadata-reconciliation-closeout/234-01-SUMMARY.md` when done. Paste the three `git hash-object` before-hashes (SEED-006 + both completed copies) into the SUMMARY for audit.
</output>
