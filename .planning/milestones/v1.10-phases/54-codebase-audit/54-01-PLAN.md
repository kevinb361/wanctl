---
phase: 54-codebase-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/54-codebase-audit/AUDIT-REPORT.md
  - src/wanctl/steering/__init__.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "Audit report documents all remaining code duplication between autorate and steering daemons with consolidation recommendations"
    - "Audit report catalogs all functions with CC>10, categorized as address/skip/leave with rationale"
    - "Audit report reviews all __init__.py exports and import layering"
    - "steering/__init__.py uses direct imports with no try/except conditional block"
    - "CONFIDENCE_AVAILABLE is always True (no false optionality)"
  artifacts:
    - path: ".planning/phases/54-codebase-audit/AUDIT-REPORT.md"
      provides: "Complete codebase audit covering duplication, module boundaries, and complexity"
      min_lines: 80
    - path: "src/wanctl/steering/__init__.py"
      provides: "Simplified module init with direct imports"
      contains: "CONFIDENCE_AVAILABLE = True"
  key_links:
    - from: "src/wanctl/steering/__init__.py"
      to: "src/wanctl/steering/steering_confidence.py"
      via: "direct import (no try/except)"
      pattern: "from \\.steering_confidence import"
---

<objective>
Write the codebase audit report documenting duplication, module boundaries, and complexity hotspots. Then fix the one module boundary issue: simplify steering/__init__.py conditional imports.

Purpose: AUDIT-01/02/03 require a documented audit. The audit report captures research findings in a permanent artifact. The steering __init__.py fix resolves the only module boundary issue found.
Output: AUDIT-REPORT.md + cleaned steering/__init__.py
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-codebase-audit/54-RESEARCH.md

@src/wanctl/steering/__init__.py
</context>

<interfaces>
<!-- Current steering/__init__.py structure (lines 34-80) that needs simplification -->
```python
# Current (problematic):
CONFIDENCE_AVAILABLE = False
try:
    from . import steering_confidence as _sc
    CONFIDENCE_AVAILABLE = bool(_sc)
except ImportError:
    pass

# ... conditional __all__.extend and attribute assignment
if CONFIDENCE_AVAILABLE:
    ConfidenceController = _sc.ConfidenceController
    # etc.
```

<!-- steering_confidence.py exports (always present in package) -->
```python
# From src/wanctl/steering/steering_confidence.py:
class ConfidenceController: ...
class ConfidenceSignals: ...
class ConfidenceWeights: ...
class TimerState: ...
def compute_confidence(...): ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write codebase audit report</name>
  <files>.planning/phases/54-codebase-audit/AUDIT-REPORT.md</files>
  <action>
Create AUDIT-REPORT.md with three sections derived from 54-RESEARCH.md findings:

**Section 1: Code Duplication (AUDIT-01)**
Document the 6 duplicated patterns from research (table format):
- _record_profiling(): 43 lines x 2, near-identical -> EXTRACT (Plan 02)
- _check_deadline(): 8 lines x 2, identical logic -> EXTRACT (Plan 02)
- emergency_lock_cleanup(): 6 lines x 2 -> LEAVE (too small)
- Storage init + maintenance: 16-26 lines -> EXTRACT (Plan 02)
- Daemon loop boilerplate: 45-98 lines -> LEAVE (too divergent)
- Shutdown cleanup sequence: 60-89 lines -> PARTIAL (_check_deadline only)

For each: cite file:line, similarity assessment, consolidation decision with rationale.

**Section 2: Module Boundaries (AUDIT-02)**
- Table of all 4 __init__.py files with status (clean/needs-fix)
- Import layering diagram (4 layers, no circular deps)
- Cross-package import documentation (steering/health.py -> health_check.py is intentional)
- Note: router_client.py __all__ includes private constants (minor, documented only)
- steering/__init__.py fix: describe the simplification (Task 2 of this plan)

**Section 3: Complexity Hotspots (AUDIT-03)**
- Table of all 15 functions with CC>10 from research
- Categorization: Address (main() x2), Skip/Protected (run_cycle, adjust_4state), Leave (all others with rationale)
- Action items: main() extractions deferred to Plan 02
- Note: CC in error_handling.py, config validation, retry decorators is inherent to domain

Include a summary section at top with key findings count and a "Changes Applied" section listing what this phase actually modifies vs documents-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-codebase-audit/AUDIT-REPORT.md && wc -l .planning/phases/54-codebase-audit/AUDIT-REPORT.md | awk '{if ($1 >= 80) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>AUDIT-REPORT.md exists with 80+ lines covering all three audit areas (duplication, boundaries, complexity) with specific file:line references and categorized recommendations</done>
</task>

<task type="auto">
  <name>Task 2: Simplify steering/__init__.py conditional imports</name>
  <files>src/wanctl/steering/__init__.py</files>
  <action>
Replace the try/except conditional import block (lines 34-80) with direct imports. steering_confidence.py always exists in the package, so the ImportError guard is dead code creating false optionality.

Changes:
1. Remove `CONFIDENCE_AVAILABLE = False` and the try/except block (lines 34-41)
2. Replace with direct imports: `from .steering_confidence import ConfidenceController, ConfidenceSignals, ConfidenceWeights, TimerState, compute_confidence`
3. Set `CONFIDENCE_AVAILABLE = True` as a simple constant (preserves API for any code checking it)
4. Replace the conditional `if CONFIDENCE_AVAILABLE:` block (lines 65-80) with nothing -- the direct imports already make symbols available at module level
5. Make `__all__` a single static list including all confidence symbols (no dynamic `.extend()`)

The final file should have: module docstring, direct imports from all submodules, `CONFIDENCE_AVAILABLE = True`, static `__all__` list. No try/except, no conditional logic, no `_sc` alias.

Preserve the existing comment style (section headers for Core classes, CAKE statistics, etc.) in `__all__`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>steering/__init__.py uses direct imports, CONFIDENCE_AVAILABLE=True as constant, static __all__, no conditional import logic. All existing tests pass.</done>
</task>

</tasks>

<verification>
- AUDIT-REPORT.md covers AUDIT-01 (duplication), AUDIT-02 (boundaries), AUDIT-03 (complexity)
- steering/__init__.py has no try/except, no conditional imports
- `from wanctl.steering import CONFIDENCE_AVAILABLE; assert CONFIDENCE_AVAILABLE` succeeds
- Full test suite passes with no regressions
</verification>

<success_criteria>
1. AUDIT-REPORT.md documents all 6 duplication patterns, all 4 __init__.py files, and all 15 CC>10 functions with categorized action items
2. steering/__init__.py simplified to direct imports with static __all__
3. All 2037+ tests pass
4. ruff check clean
</success_criteria>

<output>
After completion, create `.planning/phases/54-codebase-audit/54-01-SUMMARY.md`
</output>
