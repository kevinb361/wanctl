---
phase: 56-integration-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/autorate_continuous.py
  - src/wanctl/steering/daemon.py
  - docs/CONFIG_SCHEMA.md
  - tests/test_autorate_config.py
  - tests/test_steering_daemon.py
autonomous: true
requirements: [OPS-01, CLEAN-04]

must_haves:
  truths:
    - "Both daemon config loaders default verify_ssl to True when YAML omits the key"
    - "CONFIG_SCHEMA.md documents transport default as rest, matching all code paths"
    - "Explicit verify_ssl: false in YAML still works (no regression)"
  artifacts:
    - path: "src/wanctl/autorate_continuous.py"
      provides: "Secure verify_ssl default in autorate config loader"
      contains: 'router.get("verify_ssl", True)'
    - path: "src/wanctl/steering/daemon.py"
      provides: "Secure verify_ssl default in steering config loader"
      contains: 'router.get("verify_ssl", True)'
    - path: "docs/CONFIG_SCHEMA.md"
      provides: "Accurate transport default documentation"
      contains: "rest"
    - path: "tests/test_autorate_config.py"
      provides: "Test proving autorate verify_ssl defaults to True"
      contains: "test_verify_ssl_defaults_to_true_when_omitted"
    - path: "tests/test_steering_daemon.py"
      provides: "Test proving steering verify_ssl defaults to True"
      contains: "test_verify_ssl_defaults_to_true_when_omitted"
  key_links:
    - from: "src/wanctl/autorate_continuous.py"
      to: "src/wanctl/routeros_rest.py"
      via: "config.router_verify_ssl flows to RouterOSREST.from_config"
      pattern: 'router_verify_ssl.*True'
    - from: "src/wanctl/steering/daemon.py"
      to: "src/wanctl/routeros_rest.py"
      via: "config.router_verify_ssl flows to RouterOSREST.from_config"
      pattern: 'router_verify_ssl.*True'
---

<objective>
Close two residual integration gaps from the v1.10 milestone audit: fix verify_ssl semantic contradiction (OPS-01) and update stale CONFIG_SCHEMA.md transport default (CLEAN-04).

Purpose: Align runtime defaults with documentation -- verify_ssl should be True by default (secure), transport default should be "rest" in docs (matching code since Phase 50).
Output: 2 code fixes, 1 doc fix, 2 new tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-integration-gap-fixes/56-RESEARCH.md
@.planning/phases/56-integration-gap-fixes/56-VALIDATION.md
@.planning/v1.10-MILESTONE-AUDIT.md

@src/wanctl/autorate_continuous.py
@src/wanctl/steering/daemon.py
@docs/CONFIG_SCHEMA.md
@tests/test_autorate_config.py
@tests/test_steering_daemon.py

<interfaces>
<!-- Key code locations the executor needs. -->

From src/wanctl/autorate_continuous.py (line 462):
```python
self.router_verify_ssl = router.get("verify_ssl", False)  # BUG: should be True
```

From src/wanctl/steering/daemon.py (line 174):
```python
self.router_verify_ssl = router.get("verify_ssl", False)  # BUG: should be True
```

From src/wanctl/routeros_rest.py (line 151, for reference -- already correct):
```python
verify_ssl = getattr(config, "router_verify_ssl", True)  # Fallback never triggers
```

From tests/test_autorate_config.py (line 675):
```python
class TestConfigRouterTransportDefault:
    # Pattern: write YAML to tmp_path without the key, load Config, assert default
    def test_router_transport_defaults_to_rest_when_omitted(self, tmp_path):
```

From tests/test_steering_daemon.py (line 2685):
```python
class TestSteeringConfig:
    # Pattern: modify valid_config_dict fixture, write YAML, load SteeringConfig, assert
    def test_router_transport_defaults_to_rest(self, tmp_path, valid_config_dict):
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix verify_ssl defaults and add tests (OPS-01)</name>
  <files>src/wanctl/autorate_continuous.py, src/wanctl/steering/daemon.py, tests/test_autorate_config.py, tests/test_steering_daemon.py</files>
  <behavior>
    - Test: autorate Config defaults router_verify_ssl to True when verify_ssl key omitted from YAML
    - Test: steering SteeringConfig defaults router_verify_ssl to True when verify_ssl key omitted from YAML
    - Test: explicit verify_ssl: false in YAML still sets router_verify_ssl to False (no regression)
  </behavior>
  <action>
RED: Write failing tests first.

In tests/test_autorate_config.py, add class TestConfigVerifySslDefault after TestConfigRouterTransportDefault (after line ~730). Follow the same pattern as TestConfigRouterTransportDefault -- write full YAML config to tmp_path without the verify_ssl key, load Config, assert config.router_verify_ssl is True. Add a second test with explicit verify_ssl: false to confirm it still works.

In tests/test_steering_daemon.py, add method test_verify_ssl_defaults_to_true_when_omitted to class TestSteeringConfig (after test_router_transport_defaults_to_rest at line ~2697). Follow existing pattern: delete "verify_ssl" from valid_config_dict["router"] if present, write YAML, load SteeringConfig, assert config.router_verify_ssl is True.

Run tests -- they MUST fail (Red).

GREEN: Fix the two config loaders.

In src/wanctl/autorate_continuous.py line 462: change `router.get("verify_ssl", False)` to `router.get("verify_ssl", True)`.

In src/wanctl/steering/daemon.py line 174: change `router.get("verify_ssl", False)` to `router.get("verify_ssl", True)`.

Run tests -- they MUST pass (Green).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_autorate_config.py tests/test_steering_daemon.py -v -x -k "verify_ssl"</automated>
  </verify>
  <done>Both daemon config loaders default verify_ssl to True. Tests prove the default and confirm explicit False still works. Full test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONFIG_SCHEMA.md transport default (CLEAN-04)</name>
  <files>docs/CONFIG_SCHEMA.md</files>
  <action>
Update three locations in docs/CONFIG_SCHEMA.md where transport default is documented as "ssh" but should be "rest":

1. Line 49 -- table row: change default column from `"ssh"` to `"rest"`, change description from `"ssh" or "rest"` to `"rest" or "ssh"` (list default first).

2. Line 55 -- description: change `ssh (default): Uses SSH/Paramiko for RouterOS communication` to `rest (default): Uses RouterOS REST API (faster, requires password instead of ssh_key)`. Move the ssh line below it and remove "(default)" from the rest line description. Result:
   - `rest` (default): Uses RouterOS REST API (faster, requires password instead of ssh_key)
   - `ssh`: Uses SSH/Paramiko for RouterOS communication

3. Lines 72, 77 -- YAML example: change comment `# SSH transport (default)` to `# REST transport (default)`, and change `transport: "ssh"  # Optional, this is the default` to `transport: "rest"  # Optional, this is the default`.

Do NOT change anything else in the file. The verify_ssl documentation (line 51, lines 58-69) is already correct.
  </action>
  <verify>
    <automated>grep -n 'ssh.*default\|"ssh"' docs/CONFIG_SCHEMA.md | grep -v verify_ssl | grep -v "ssh_key" | grep -v "SSH:" | grep -v Paramiko</automated>
  </verify>
  <done>CONFIG_SCHEMA.md documents transport default as "rest" in all three locations (table, description, YAML example). No remaining references to SSH as default. verify_ssl documentation unchanged.</done>
</task>

</tasks>

<verification>
1. `.venv/bin/pytest tests/ -v` -- full suite passes (no regressions)
2. `.venv/bin/ruff check src/ tests/` -- no lint violations
3. `grep -n 'verify_ssl.*False' src/wanctl/autorate_continuous.py src/wanctl/steering/daemon.py` -- returns empty (no False defaults remain)
4. `grep -n '"ssh"' docs/CONFIG_SCHEMA.md` -- no remaining references to ssh as default (only legitimate ssh references like ssh_key, SSH transport option)
</verification>

<success_criteria>
- OPS-01 closed: verify_ssl defaults to True in both daemon config loaders, matching REST client and documentation
- CLEAN-04 closed: CONFIG_SCHEMA.md transport default says "rest" everywhere, matching code since Phase 50
- Full test suite green with 2+ new verify_ssl tests
- No regressions in existing transport or SSL tests
</success_criteria>

<output>
After completion, create `.planning/phases/56-integration-gap-fixes/56-01-SUMMARY.md`
</output>
