# Plan 45-01: Steering Daemon Shutdown Parity

## Goal
Bring steering/daemon.py shutdown cleanup to parity with autorate_continuous.py.

## Truths (must be verifiable after implementation)

1. steering/daemon.py finally block calls router.close() before exit
2. steering/daemon.py finally block calls save_state() on the SteeringDaemon before exit
3. steering/daemon.py finally block closes MetricsWriter connection before exit
4. autorate_continuous.py finally block closes MetricsWriter connection before exit
5. All close/cleanup calls are wrapped in try/except to prevent cleanup failures from masking the original exit
6. Tests verify cleanup methods are called during shutdown sequence

## Tasks

### Task 1: Add router.close() to steering daemon finally block
- **File:** `src/wanctl/steering/daemon.py` (finally block ~line 1580)
- **Change:** After health server shutdown, close the router connection
- Pattern: match autorate_continuous.py lines 2037-2047
- Must handle case where daemon.router may not exist (early failure)
- Wrap in try/except with logging

### Task 2: Add state save to steering daemon finally block
- **File:** `src/wanctl/steering/daemon.py` (finally block)
- **Change:** Call daemon.save_state() if daemon exists
- Must handle case where daemon was never created
- Wrap in try/except — state save failure should not prevent other cleanup

### Task 3: Add MetricsWriter.close() to both daemons
- **Files:** `src/wanctl/steering/daemon.py`, `src/wanctl/autorate_continuous.py`
- **Change:** After router close, call MetricsWriter singleton close
- Pattern: `MetricsWriter._instance` check, then `.close()` if exists
- Import MetricsWriter only if storage was configured

### Task 4: Add tests for steering daemon shutdown cleanup
- **File:** `tests/test_steering_daemon.py` (TestMainEntryPoint class)
- Tests to add:
  - test_main_shutdown_closes_router_connection
  - test_main_shutdown_saves_state
  - test_main_shutdown_closes_metrics_writer
  - test_main_cleanup_continues_on_router_close_error
- Use existing _mock_storage fixture and mock patterns

## Artifacts
- Modified: `src/wanctl/steering/daemon.py` (finally block expansion)
- Modified: `src/wanctl/autorate_continuous.py` (MetricsWriter close addition)
- Modified: `tests/test_steering_daemon.py` (4+ new tests)

## Dependencies
- None (uses existing close() methods on all components)
