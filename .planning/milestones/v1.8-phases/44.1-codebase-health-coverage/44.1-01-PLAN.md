# Plan 44.1-01: Codebase Health & Coverage Recovery

## Goal
Fix test pollution, recover 90%+ coverage, and sync stale documentation.

## Tasks

### Task 1: Delete MagicMock/ artifact and add to .gitignore
- Delete `MagicMock/` directory from project root
- Add `MagicMock/` to `.gitignore` as safety net
- **Verify**: Directory gone, entry in .gitignore

### Task 2: Fix MagicMock guard in steering daemon
- `src/wanctl/steering/daemon.py` lines ~652 and ~1460: add `isinstance(db_path, str)` guard
- Match existing pattern from `src/wanctl/autorate_continuous.py:1797-1798`
- **Verify**: Run test suite, confirm MagicMock/ not recreated

### Task 3: Add steering_confidence.py tests
- Create `tests/test_steering_confidence.py`
- Cover: `compute_confidence()` — all state paths (RED, SOFT_RED sustained/not, YELLOW, GREEN), RTT delta tiers, drops trend, queue sustained
- Cover: `FlapDetector` — record_toggle, check_flapping, penalty activation/expiry, disabled mode
- Cover: `DryRunLogger` — enabled/disabled, ENABLE/DISABLE decision logging
- Cover: `ConfidenceController.evaluate()` — good state with high confidence, degraded state recovery, dry-run returns None
- Target: raise steering_confidence.py from 42% to 80%+, overall from 89.45% to 90%+
- **Verify**: `pytest tests/test_steering_confidence.py -v` passes, `make ci` passes

### Task 4: Update stale documentation
- `PROJECT.md`: Update "Current State" section to v1.8, update test count
- Roadmap already fixed during phase insertion
- **Verify**: Docs match reality

## Success Criteria
1. `make ci` passes (including coverage >= 90%)
2. `MagicMock/` directory not created by test runs
3. `steering_confidence.py` coverage >= 80%
4. Documentation accurate

## Estimated Tests
~30-40 new tests for steering_confidence.py coverage
