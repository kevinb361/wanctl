# Phase 44.1: Codebase Health & Coverage Recovery

## Why This Phase Exists

After a month gap between sessions, a codebase health audit revealed:

1. **Coverage regression**: 89.45% (below 90% CI threshold) — primarily from `steering_confidence.py` at 42.1%
2. **Test pollution bug**: `MagicMock/` directory created at project root during test runs (SQLite DBs from unguarded mock paths)
3. **Stale documentation**: v1.8-ROADMAP.md showed Phase 44 as "Planned" when it was complete; PROJECT.md references v1.7 as current

## Root Cause Analysis

### Coverage Gap
- `steering_confidence.py` has 243 statements, 119 missed (42.1%)
- Only timer mechanics tested (`test_steering_timers.py`, 11 tests)
- `compute_confidence()`, `FlapDetector`, `DryRunLogger`, and `ConfidenceController.evaluate()` have no dedicated tests
- This file alone accounts for 27% of all uncovered lines (119/444)

### Test Pollution
- `steering/daemon.py:652` and `:1460` pass MagicMock to `Path()` and `MetricsWriter()`
- `autorate_continuous.py:1797-1798` already has the fix pattern: `if db_path and isinstance(db_path, str)`
- The MagicMock `__str__` creates real directories: `MagicMock/mock.data.get().get()/`

### Doc Drift
- Normal drift from completing work without updating progress tracking
- Phase 44 verified 2026-01-29 but roadmap not updated at that time

## Scope

Maintenance-only. No new features, no algorithm changes, no production behavior changes.
