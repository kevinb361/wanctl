---
phase: 183-dashboard-history-contract-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md
autonomous: true
requirements: []
enables_downstream:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - OPER-05
must_haves:
  truths:
    - "An operator reading 183-dashboard-ambiguity-audit.md can name exactly what /metrics/history returns today and where in health_check.py it is emitted"
    - "An operator reading the audit can name exactly what HistoryBrowserWidget renders today and what it ignores"
    - "The audit enumerates every ambiguity point where a user could mistake the history tab for merged cross-WAN history"
    - "The audit captures current degraded/failure behavior for fetch errors and missing metadata.source"
    - "The audit records whether current dashboard wording matches RUNBOOK.md and DEPLOYMENT.md operator guidance"
  artifacts:
    - path: ".planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md"
      provides: "Read-only audit of dashboard history-tab behavior, payload usage, and operator ambiguity points"
      contains:
        - "## Current /metrics/history Envelope"
        - "## Current HistoryBrowserWidget Behavior"
        - "## Ambiguity Points"
        - "## Degraded And Failure Behavior Today"
        - "## Operator Doc Wording Alignment"
  key_links:
    - from: "183-dashboard-ambiguity-audit.md"
      to: "src/wanctl/health_check.py"
      via: "line-number citations for the /metrics/history handler and metadata.source.mode emission"
      pattern: "health_check\\.py:[0-9]+"
    - from: "183-dashboard-ambiguity-audit.md"
      to: "src/wanctl/dashboard/widgets/history_browser.py"
      via: "line-number citations for render path and metadata handling"
      pattern: "history_browser\\.py:[0-9]+"
---

<objective>
Audit the current dashboard history tab behavior end-to-end so that Phase 183-02 can write a
dashboard-facing source contract against concrete evidence, not assumptions.

Purpose: Without a grounded audit, the contract in 183-02 risks inventing field names,
misreading the /metrics/history envelope, or missing operator ambiguity points that the
contract must fix.

Output: `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md`
— a read-only markdown artifact enumerating current behavior, payload usage, and every
point where an operator could misread the history tab as merged cross-WAN history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/183-dashboard-history-contract-audit/183-CONTEXT.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md
@.planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
@docs/RUNBOOK.md
@docs/DEPLOYMENT.md
@src/wanctl/health_check.py
@src/wanctl/dashboard/widgets/history_browser.py
@src/wanctl/dashboard/app.py
@tests/dashboard/test_history_browser.py
@tests/test_health_check.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the current /metrics/history envelope and source metadata emission</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md</files>
  <read_first>
    - src/wanctl/health_check.py (full file; focus on the /metrics/history handler starting near line 180, the _handle_history method near line 792, and the DB resolution helper near line 863 that emits `local_configured_db`)
    - tests/test_health_check.py (search for `/metrics/history`, `metadata`, and `source` coverage)
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md
  </read_first>
  <action>
    Create `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md` with a short
    one-paragraph header ("Phase 183 read-only audit. No code changes. Drives 183-02 contract.") and then write
    the section `## Current /metrics/history Envelope`.

    In that section, document the following using direct line-number citations against `src/wanctl/health_check.py`
    (format: `health_check.py:NNN`, one citation per claim):

    1. The top-level envelope shape (data + metadata) and newest-first ordering.
    2. The exact field path(s) the endpoint uses to expose source context. The audit MUST name
       `metadata.source.mode` and `metadata.source.db_paths` verbatim, quoting the handler lines that build them.
    3. The known `metadata.source.mode` values that the handler can emit today (at minimum `local_configured_db`
       from the endpoint-local resolution path). Quote the literal string values, do not paraphrase.
    4. When the endpoint falls back, when it short-circuits, and what "endpoint-local" actually means in code terms.

    Then add a short subsection `### Test Coverage Today` that lists which behaviors of the /metrics/history
    envelope `tests/test_health_check.py` already exercises (source metadata, local DB resolution, error paths),
    with line citations. Do NOT propose new test coverage here — this is inventory only.

    CRITICAL: Do NOT propose changes to `health_check.py`. Do NOT invent new field names. If a field does not
    exist in the current code, say so explicitly. Every claim must cite a line in `health_check.py` or
    `test_health_check.py`.
  </action>
  <verify>
    <automated>grep -c "health_check.py:" .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md | awk '{ exit ($1 >= 5 ? 0 : 1) }'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md` exists
    - Contains heading `## Current /metrics/history Envelope`
    - Contains the literal string `metadata.source.mode`
    - Contains the literal string `metadata.source.db_paths`
    - Contains the literal string `local_configured_db`
    - Contains at least 5 distinct `health_check.py:` line-number citations
    - Contains subsection `### Test Coverage Today` with at least 2 `test_health_check.py:` citations
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/` reports no modifications
  </acceptance_criteria>
  <done>
    Section fully grounded in current code; no invented fields; no proposals; no src/test/docs edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inventory the current HistoryBrowserWidget render path and operator-visible surface</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md</files>
  <read_first>
    - src/wanctl/dashboard/widgets/history_browser.py (full 163 lines)
    - src/wanctl/dashboard/app.py (focus on history-tab wiring)
    - tests/dashboard/test_history_browser.py (full 212 lines)
  </read_first>
  <action>
    Append the section `## Current HistoryBrowserWidget Behavior` to the same audit file.

    Document with line-number citations against `history_browser.py` and `app.py`:

    1. How the widget fetches data (which HTTP path, which client, any caching).
    2. What fields of the payload it actually reads and renders (rows, columns, summary stats).
    3. What fields of the payload it IGNORES today — specifically, whether it touches `metadata`,
       `metadata.source`, `metadata.source.mode`, or `metadata.source.db_paths` at all. Cite the exact lines
       proving "ignored" or "not referenced."
    4. What labeling, title, tab name, and tooltips the history tab currently shows to an operator. Quote every
       operator-visible literal string (tab title, headings, empty-state text, error text).
    5. How the widget is wired into the dashboard tab pane in `app.py` (line cite).

    Then add subsection `### Widget Test Coverage Today` listing what `tests/dashboard/test_history_browser.py`
    currently asserts, with line citations. Flag whether any existing assertion exercises source semantics or
    degraded-state messaging.

    CRITICAL: Do NOT propose changes. If `metadata.source` is not referenced anywhere in the widget, state that
    plainly with a citation showing the absence (e.g., "grep `metadata` in history_browser.py returns 0 matches"
    with a `grep -n` command shown). Every operator-visible literal string must be quoted exactly.
  </action>
  <verify>
    <automated>grep -c "history_browser.py:" .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md | awk '{ exit ($1 >= 5 ? 0 : 1) }'</automated>
  </verify>
  <acceptance_criteria>
    - Audit file contains heading `## Current HistoryBrowserWidget Behavior`
    - Contains subsection `### Widget Test Coverage Today`
    - Contains at least 5 distinct `history_browser.py:` citations
    - Contains at least 1 `app.py:` citation for tab-pane wiring
    - Contains at least 2 `test_history_browser.py:` citations
    - Contains at least one explicit statement about whether `metadata.source` is referenced in the widget
    - Contains at least 3 quoted operator-visible literal strings from the widget (tab title / headings / empty-state / error text)
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/` reports no modifications
  </acceptance_criteria>
  <done>
    Widget behavior inventoried with direct citations; operator-visible strings quoted verbatim; no proposals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enumerate ambiguity points, degraded-case behavior, and doc alignment gaps</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md</files>
  <read_first>
    - The in-progress `183-dashboard-ambiguity-audit.md` from Tasks 1 and 2
    - docs/RUNBOOK.md (search for `/metrics/history`, `endpoint-local`, `merged`, `wanctl.history`)
    - docs/DEPLOYMENT.md (search for `/metrics/history`, `endpoint-local`, merged CLI wording)
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md (CLI merged vs HTTP endpoint-local lock)
    - .planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
    - src/wanctl/dashboard/widgets/history_browser.py (re-read error and empty-state branches)
  </read_first>
  <action>
    Append three final sections to `183-dashboard-ambiguity-audit.md`:

    **Section `## Ambiguity Points`** — A numbered list of concrete ways an operator, looking at the current
    history tab, could wrongly conclude it shows merged cross-WAN history. Each item MUST:
      - Cite what the widget currently shows (tab title / heading / column / absence of labeling) with a
        `history_browser.py:NNN` citation.
      - State the wrong conclusion an operator could draw.
      - Name which CONTEXT.md decision ID (D-01..D-14) the ambiguity violates.
    Produce at least 5 numbered ambiguity points. Include at minimum: (a) the tab title itself implies nothing
    about scope, (b) rendered rows carry no per-row source context, (c) `metadata.source.mode` is not surfaced,
    (d) there is no handoff to `wanctl.history` CLI, (e) degraded/missing-metadata state does not warn the operator.

    **Section `## Degraded And Failure Behavior Today`** — Document the current behavior of the widget when:
      - The HTTP fetch fails (timeout / non-200).
      - The payload is present but `metadata` is missing.
      - The payload is present but `metadata.source` is missing or malformed.
      - The payload has zero rows.
    Cite the exact `history_browser.py` lines that handle (or fail to handle) each case. For any case that is
    NOT explicitly handled today, say so and cite the absence. Quote any user-visible error/empty text verbatim.

    **Section `## Operator Doc Wording Alignment`** — Compare:
      - What `docs/RUNBOOK.md` tells operators about endpoint-local HTTP vs merged CLI history (quote key phrases).
      - What `docs/DEPLOYMENT.md` tells operators about endpoint-local `/metrics/history` vs merged CLI reads
        (quote key phrases).
      - What the dashboard history tab actually shows today.
    Produce a short bullet list of docs-vs-dashboard mismatches. This is the seed list that Phase 185 (OPER-05)
    will reconcile. Do NOT propose doc edits here — just record the gap.

    End the file with a one-paragraph section `## Handoff To 183-02` stating that this audit is read-only and
    that the dashboard-facing source contract is defined in `183-dashboard-source-contract.md`.

    CRITICAL: No proposals for code, test, or doc changes. No new field names. No recommendations. This is
    strictly a gap inventory. Every ambiguity item must reference a CONTEXT.md decision ID.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md; grep -q "## Ambiguity Points" "$F" && grep -q "## Degraded And Failure Behavior Today" "$F" && grep -q "## Operator Doc Wording Alignment" "$F" && grep -q "## Handoff To 183-02" "$F" && [ $(grep -cE "D-0[1-9]|D-1[0-4]" "$F") -ge 5 ]'</automated>
  </verify>
  <acceptance_criteria>
    - Audit file contains headings `## Ambiguity Points`, `## Degraded And Failure Behavior Today`, `## Operator Doc Wording Alignment`, `## Handoff To 183-02`
    - `## Ambiguity Points` section contains at least 5 numbered items
    - At least 5 references to decision IDs in the `D-01`..`D-14` range appear in the file
    - `## Degraded And Failure Behavior Today` section cites at least 3 distinct failure/edge cases (fetch error, missing metadata, missing metadata.source, empty rows)
    - `## Operator Doc Wording Alignment` contains at least one quoted phrase from `docs/RUNBOOK.md` and one from `docs/DEPLOYMENT.md`
    - File contains no lines proposing edits to `src/`, `tests/`, or `docs/` (no `TODO:`, no `should change`, no `we recommend editing`)
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/` reports no modifications
  </acceptance_criteria>
  <done>
    Audit artifact is complete, grounded, read-only, and ready for 183-02 to consume.
  </done>
</task>

</tasks>

<verification_criteria>
The audit is complete when:
1. `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md` exists
2. It contains all five required top-level sections (Envelope, Widget Behavior, Ambiguity Points, Degraded/Failure, Doc Alignment) plus the handoff note
3. Every behavioral claim cites a specific line number in `health_check.py`, `history_browser.py`, `app.py`, or a test file
4. Every ambiguity point references at least one CONTEXT.md decision ID (D-01..D-14)
5. No files outside `.planning/phases/183-dashboard-history-contract-audit/` are modified
6. No section proposes changes — this is a read-only gap inventory
</verification_criteria>

<success_criteria>
- Artifact exists and matches the section contract above
- Grep checks in each task's `<verify>` block pass
- `git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md` is empty
- Phase 183-02 can open the audit and reference concrete line numbers when drafting the contract
</success_criteria>

<output>
After completion, create `.planning/phases/183-dashboard-history-contract-audit/183-01-SUMMARY.md`
summarizing: sections written, files cited, total ambiguity points enumerated, and the handoff
to 183-02.
</output>
