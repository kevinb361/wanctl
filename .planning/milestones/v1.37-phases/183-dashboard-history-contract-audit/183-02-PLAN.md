---
phase: 183-dashboard-history-contract-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 183-01
files_modified:
  - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md
autonomous: true
requirements: []
enables_downstream:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - OPER-05
must_haves:
  truths:
    - "An operator reading 183-dashboard-source-contract.md can hand it to a Phase 184 implementer and a Phase 185 test author without reopening backend semantics"
    - "Every CONTEXT.md decision D-01 through D-14 is named and mapped to a contract section"
    - "Every downstream requirement (DASH-01, DASH-02, DASH-03, DASH-04, OPER-05) is traced to contract acceptance criteria"
    - "The contract preserves the HTTP-local vs CLI-merged distinction at every point where wording is prescribed"
    - "The contract defines degraded and failure messaging that forbids silent fallback to merged-history implications"
    - "The contract names the exact CLI/module invocation operators must reach from the history tab"
  artifacts:
    - path: ".planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md"
      provides: "Locked dashboard-facing source contract with acceptance criteria and D-01..D-14 traceability"
      contains:
        - "## Scope And Non-Goals"
        - "## Labeling Requirements"
        - "## Source Metadata Requirements"
        - "## Operator Handoff Requirements"
        - "## Degraded And Failure Requirements"
        - "## Acceptance Criteria"
        - "## Traceability"
        - "## Out Of Scope"
  key_links:
    - from: "183-dashboard-source-contract.md"
      to: ".planning/phases/183-dashboard-history-contract-audit/183-CONTEXT.md"
      via: "every decision D-01..D-14 is cited at least once"
      pattern: "D-(0[1-9]|1[0-4])"
    - from: "183-dashboard-source-contract.md"
      to: ".planning/REQUIREMENTS.md"
      via: "traceability table mapping DASH-01..04 and OPER-05"
      pattern: "(DASH-0[1-4]|OPER-05)"
    - from: "183-dashboard-source-contract.md"
      to: "183-dashboard-ambiguity-audit.md"
      via: "contract references the audit file for each ambiguity being closed"
---

<objective>
Define the dashboard-facing source contract and acceptance criteria that Phase 184 (UI
implementation) and Phase 185 (regression tests + doc alignment) can execute directly.

Purpose: The audit from 183-01 enumerates what's wrong today; this plan locks what
"correct" looks like in operator-facing terms, without reopening backend history semantics.
The contract must be specific enough to drive UI copy, test assertions, and doc alignment.

Output: `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md`
— a locked markdown contract with at least 10 grep-verifiable acceptance criteria and a
traceability table covering every CONTEXT.md decision (D-01..D-14) and every downstream
requirement (DASH-01..04, OPER-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/183-dashboard-history-contract-audit/183-CONTEXT.md
@.planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md
@docs/RUNBOOK.md
@docs/DEPLOYMENT.md
@src/wanctl/health_check.py
@src/wanctl/history.py
@src/wanctl/dashboard/widgets/history_browser.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the contract skeleton with scope, labeling, and source metadata requirements</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md</files>
  <read_first>
    - .planning/phases/183-dashboard-history-contract-audit/183-CONTEXT.md (re-read every decision D-01..D-14 verbatim — the contract cites these)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md (the 183-01 output — anchor every requirement to audit citations where applicable)
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md (CLI merged, HTTP endpoint-local, additive metadata.source lock)
    - src/wanctl/health_check.py (confirm that `metadata.source.mode` and `metadata.source.db_paths` are the authoritative field names — do NOT invent alternatives)
    - docs/RUNBOOK.md (borrow wording tone for operator-facing phrases)
    - docs/DEPLOYMENT.md
  </read_first>
  <action>
    Create `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md` with this
    structure, in this exact order. Each listed section is REQUIRED.

    **Header paragraph:** One paragraph stating "Phase 183-02 locked contract. Markdown-only. No backend
    changes. Consumed by Phase 184 (UI) and Phase 185 (tests + docs)." Reference `183-dashboard-ambiguity-audit.md`
    as the evidence base and `183-CONTEXT.md` as the decision source.

    **Section `## Scope And Non-Goals`:**
      - Scope: clarify dashboard history-tab source semantics, labeling, source metadata surfacing, operator
        handoff, and degraded-state messaging.
      - Non-goals: NO backend change to `/metrics/history`, NO storage/retention/compaction changes, NO control
        semantics, NO widget placement/styling details (those are Phase 184 discretion), NO test implementation
        (Phase 185).
      - Explicitly cite D-14 ("specific enough to drive UI copy, test assertions, and doc alignment without
        reopening backend semantics") as the governing constraint.

    **Section `## Labeling Requirements`** with three subsections, one per locked decision:
      - `### L1. Endpoint-Local Framing (D-01)` — The history tab MUST make it explicit that `/metrics/history`
        is an endpoint-local HTTP history view for the connected autorate daemon, not a merged cross-WAN
        history reader. Include at least one placeholder literal string the UI MUST display, phrased as:
        `REQUIRED PHRASE (placeholder, final wording is Phase 184 discretion): "This view shows history from
        the connected autorate endpoint only."` Also add a PROHIBITED-wording note: the UI MUST NOT use bare
        phrases that imply merged cross-WAN scope (e.g. "all WANs", "merged history", "cross-WAN history")
        unless qualified.
      - `### L2. No Implied Parity With wanctl.history (D-02)` — The contract forbids the UI from implying the
        history tab is equivalent to the `wanctl.history` CLI/module. Any wording that names `wanctl.history`
        must frame it as the separate authoritative merged path, not a parallel option.
      - `### L3. Plainly Visible Explanation (D-03)` — The acceptance criteria must require at least one plainly
        visible (not tooltip-only, not modal-only) explanatory sentence inside the history tab itself. Short
        labels are allowed elsewhere as long as this one visible explanation exists.

    **Section `## Source Metadata Requirements`** with three subsections:
      - `### S1. metadata.source Is Required Context (D-04)` — The dashboard contract MUST treat `metadata.source`
        as required operator context, not optional debug detail. Name the authoritative backend fields
        (`metadata.source.mode`, `metadata.source.db_paths`) as the truth underneath UI wording. Do NOT invent new field names.
      - `### S2. Mode Surfacing In Operator Wording (D-05)` — The UI MUST surface `metadata.source.mode` in
        operator-comprehensible wording, and MUST preserve enough context to explain which backing DB path(s)
        the endpoint used. The contract must require a mapping from known mode values to operator phrases.
        At minimum include `local_configured_db` → operator-facing phrase such as "endpoint-local DB configured
        for this autorate daemon" (placeholder wording).
      - `### S3. Raw Field Values Allowed In Diagnostics Only (D-06)` — Raw payload field values such as
        `local_configured_db` may appear in diagnostic/detail views, but the primary UI contract MUST translate
        them into operator-facing wording rather than exposing raw internal values without explanation.

    CRITICAL rules for this task:
      - Every requirement subsection MUST cite its decision ID (`D-01`..`D-06`) in the subsection body.
      - Do NOT invent new `metadata.source` fields.
      - Do NOT propose changes to `health_check.py`.
      - Do NOT specify exact widget placement or colors — that is Phase 184.
      - Placeholder UI strings must be marked as placeholders so Phase 184 can finalize wording within the
        constraint envelope.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md; grep -q "## Scope And Non-Goals" "$F" && grep -q "## Labeling Requirements" "$F" && grep -q "## Source Metadata Requirements" "$F" && grep -q "metadata.source.mode" "$F" && grep -q "metadata.source.db_paths" "$F" && grep -q "local_configured_db" "$F" && grep -q "D-01" "$F" && grep -q "D-02" "$F" && grep -q "D-03" "$F" && grep -q "D-04" "$F" && grep -q "D-05" "$F" && grep -q "D-06" "$F" && grep -q "D-14" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md` exists
    - Contains headings `## Scope And Non-Goals`, `## Labeling Requirements`, `## Source Metadata Requirements`
    - Contains the literal strings `metadata.source.mode`, `metadata.source.db_paths`, `local_configured_db`
    - Contains `D-01`, `D-02`, `D-03`, `D-04`, `D-05`, `D-06`, and `D-14` at least once each
    - Contains at least one labeled placeholder UI string for endpoint-local framing
    - Contains at least one explicit PROHIBITED-wording note for merged-history implications
    - Contains a mode → operator-phrase mapping entry for `local_configured_db`
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md` reports no modifications
  </acceptance_criteria>
  <done>
    Scope, labeling, and source-metadata requirements locked and traceable to D-01..D-06 and D-14.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add operator handoff and degraded/failure requirements with CLI path verified</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md</files>
  <read_first>
    - src/wanctl/history.py (verify the exact module entrypoint — confirm `python -m wanctl.history` is a valid invocation, and capture the real CLI name and any subcommands actually exposed today; the contract must name the CLI/module path that exists, not invent one)
    - docs/RUNBOOK.md (how operators are currently told to invoke the merged CLI path)
    - docs/DEPLOYMENT.md
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-reader-parity-decision.md
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md (degraded-state section)
    - src/wanctl/dashboard/widgets/history_browser.py (degraded/error branches — contract must be consistent with what the widget is capable of expressing without backend changes)
  </read_first>
  <action>
    Append to `183-dashboard-source-contract.md`.

    **Section `## Operator Handoff Requirements`** with three subsections:
      - `### H1. Canonical Merged-Proof Path (D-07)` — The history tab MUST expose a clear operator path to the
        authoritative merged CLI workflow. Name the canonical invocation explicitly, e.g.,
        `python -m wanctl.history` (verify the exact module/CLI name from `src/wanctl/history.py` while reading
        for this task; if the actual entrypoint differs, use the real one and note why). The contract MUST
        record both the invocation and a short description of what it proves (merged cross-WAN history).
      - `### H2. Framed As "Use When You Need Merged Proof" (D-08)` — The UI MUST frame the handoff as the
        answer to the operator question "what do I run when I need merged cross-WAN proof?" It must NOT be
        framed as a generic "help" link or buried in a tooltip-only surface.
      - `### H3. Wording Intent Locked, Placement Deferred (D-09)` — Contract locks the need for the handoff
        and the wording intent. Exact widget placement, visual treatment, and interaction details are Phase 184
        discretion; the contract must not prescribe them.

    **Section `## Degraded And Failure Requirements`** with three subsections:
      - `### F1. Explicit Failure Messaging, No Silent Fallback (D-10)` — On fetch error (timeout / non-200 /
        malformed JSON), the history tab MUST show an explicit operator-visible error and MUST NOT render
        previously-fetched rows in a way that implies current merged history. Include a PLACEHOLDER required
        error string such as `"History unavailable — see docs/RUNBOOK.md for merged CLI path"` (placeholder).
      - `### F2. Missing/Incomplete metadata.source Is Ambiguous, Not Silent (D-11)` — If `metadata.source` is
        absent or `metadata.source.mode` is missing, the UI MUST surface that the history tab is in a degraded
        "source unknown" state, NOT render as if it were trusted endpoint-local history. Include a placeholder
        degraded-state string such as `"Source context unavailable — treat as ambiguous; use wanctl.history CLI
        for authoritative merged view"` (placeholder).
      - `### F3. Regression Surface Areas For Phase 185 (D-12)` — Enumerate the regression surface areas Phase 185
        MUST cover: (a) success with `local_configured_db` rendering, (b) endpoint-local label visible, (c) CLI
        handoff visible, (d) fetch-error state, (e) missing-metadata state, (f) missing-`metadata.source.mode`
        state. Do NOT specify test framework details — just the surface areas.

    CRITICAL rules:
      - When verifying the CLI invocation, use the actual entrypoint from `src/wanctl/history.py`. If it is
        `python -m wanctl.history`, state that. If it is a console_scripts binary (e.g. `wanctl-history`), state
        that. Do NOT guess.
      - Every subsection MUST cite its decision ID (`D-07`..`D-12`) in body text.
      - Placeholder UI strings must be marked as placeholders.
      - Do NOT propose backend changes.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md; grep -q "## Operator Handoff Requirements" "$F" && grep -q "## Degraded And Failure Requirements" "$F" && grep -q "D-07" "$F" && grep -q "D-08" "$F" && grep -q "D-09" "$F" && grep -q "D-10" "$F" && grep -q "D-11" "$F" && grep -q "D-12" "$F" && grep -qE "wanctl\\.history|wanctl-history" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Contract file contains headings `## Operator Handoff Requirements` and `## Degraded And Failure Requirements`
    - Contains `D-07`, `D-08`, `D-09`, `D-10`, `D-11`, `D-12` at least once each
    - Contains the actual CLI/module path verified against `src/wanctl/history.py` (either `python -m wanctl.history` or a real console-script name)
    - Contains at least one placeholder required error-state string
    - Contains at least one placeholder required degraded-metadata state string
    - Contains a Phase-185 regression-surface enumeration with at least 6 bullet points
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md` reports no modifications
  </acceptance_criteria>
  <done>
    Handoff and degraded/failure sections locked; CLI path verified against real code; decisions D-07..D-12 cited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add acceptance criteria, traceability table, and out-of-scope closure</name>
  <files>.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md</files>
  <read_first>
    - The in-progress `183-dashboard-source-contract.md` (Tasks 1 and 2 of this plan)
    - .planning/phases/183-dashboard-history-contract-audit/183-CONTEXT.md (decisions D-01..D-14)
    - .planning/REQUIREMENTS.md (DASH-01..DASH-04, OPER-05)
    - .planning/ROADMAP.md (Phase 184 and Phase 185 plan IDs)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-ambiguity-audit.md
  </read_first>
  <action>
    Append the final three sections to `183-dashboard-source-contract.md`.

    **Section `## Acceptance Criteria`** — A numbered list with AT LEAST 10 items. Each item MUST be
    grep-verifiable, meaning: Phase 184 UI or Phase 185 tests could write a check that pass/fails on either
    (a) a literal UI string appearing, (b) a metadata field being rendered, or (c) a degraded-state message
    being shown. Each item MUST name either a required string, a required metadata field, or a required
    degraded/error message. Each item MUST also tag the decision ID(s) it enforces and the downstream
    requirement ID(s) it satisfies.

    Required items (at minimum — more are welcome):
      1. History tab header contains a plainly visible explanation phrase equivalent to "endpoint-local" or
         "from the connected autorate endpoint" (exact wording Phase 184 discretion). [D-01, D-03, DASH-01]
      2. History tab UI does NOT contain any bare phrase implying merged cross-WAN scope without qualification.
         [D-01, DASH-01]
      3. When `metadata.source.mode == "local_configured_db"`, the UI renders an operator-facing label distinct
         from any merged-history wording. [D-04, D-05, D-06, DASH-02]
      4. The UI renders, in some operator-comprehensible form, information derived from
         `metadata.source.db_paths`. [D-05, DASH-02]
      5. Raw string `local_configured_db` is NOT the primary operator label; it may appear in a diagnostic
         detail surface only. [D-06, DASH-02]
      6. The history tab exposes a visible CLI handoff naming the real `wanctl.history` (or equivalent verified)
         invocation, framed as "use this when you need merged cross-WAN proof". [D-07, D-08, DASH-03]
      7. When the HTTP fetch fails, the UI shows an explicit "history unavailable" style message (placeholder
         wording) and does NOT render stale rows implying merged history. [D-10, DASH-04]
      8. When `metadata.source` is missing or malformed, the UI shows an explicit "source context unavailable /
         ambiguous" message and does NOT render as trusted endpoint-local history. [D-11, DASH-04]
      9. When `metadata.source.mode` is present but `metadata.source.db_paths` is missing or empty, the UI
         still shows the degraded/ambiguous framing. [D-11, DASH-04]
     10. Operator documentation (`docs/RUNBOOK.md`, `docs/DEPLOYMENT.md`) matches the dashboard's source
         wording after Phase 185 alignment. [OPER-05]

    Add a short intro sentence stating these criteria are the Phase 184/185 pass/fail envelope and that
    Phase 184 may refine wording within these constraints but may not relax them.

    **Section `## Traceability`** — A markdown table with columns:
    `| Decision | Contract Section | Downstream Phase | Downstream Requirement | Notes |`
    The table MUST contain one row for every decision `D-01` through `D-14`. For each row:
      - Contract Section: the `##` or `###` heading(s) in this file where the decision is enforced.
      - Downstream Phase: `184` or `185` (or both) based on ROADMAP.md Phase 184/185 plans.
      - Downstream Requirement: one or more of `DASH-01`, `DASH-02`, `DASH-03`, `DASH-04`, `OPER-05`. Use the
        existing REQUIREMENTS.md traceability as the source of truth (DASH-01..03 → Phase 184, DASH-04 and
        OPER-05 → Phase 185). D-13 and D-14 are meta-decisions about this phase itself; map them to their
        governing sections (Scope And Non-Goals and the contract as a whole) and mark downstream as `—` if
        they have no direct downstream requirement, but still list them.
      - Notes: short free text or `—`.

    **Section `## Out Of Scope`** — A bulleted list restating what this contract does NOT cover:
      - Backend changes to `/metrics/history`
      - Storage topology / retention / compaction
      - Control-loop semantics
      - Exact widget placement, styling, visual treatment, interaction details (Phase 184 discretion)
      - Regression test implementation details (Phase 185 discretion)
      - Broader dashboard redesign beyond the history tab

    **Final line:** `*Contract locked by Phase 183-02. Implemented by Phase 184. Verified by Phase 185.*`

    CRITICAL:
      - The acceptance-criteria list MUST be at least 10 numbered items.
      - Every decision D-01..D-14 MUST appear in the traceability table.
      - Every requirement DASH-01, DASH-02, DASH-03, DASH-04, OPER-05 MUST appear in the traceability table at
        least once.
      - Do NOT add or remove CONTEXT.md decisions.
      - Do NOT propose backend changes.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md; set -e; grep -q "## Acceptance Criteria" "$F"; grep -q "## Traceability" "$F"; grep -q "## Out Of Scope" "$F"; for id in D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-13 D-14; do grep -q "$id" "$F" || { echo "missing $id"; exit 1; }; done; for id in DASH-01 DASH-02 DASH-03 DASH-04 OPER-05; do grep -q "$id" "$F" || { echo "missing $id"; exit 1; }; done; N=$(grep -cE "^[[:space:]]*[0-9]+\\." "$F"); [ "$N" -ge 10 ] || { echo "need 10 numbered items, have $N"; exit 1; }; git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md | grep -q . && exit 1 || exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - Contract file contains headings `## Acceptance Criteria`, `## Traceability`, `## Out Of Scope`
    - Contract file contains `D-01` through `D-14` at least once each (all 14 decision IDs)
    - Contract file contains `DASH-01`, `DASH-02`, `DASH-03`, `DASH-04`, `OPER-05` at least once each
    - `## Acceptance Criteria` section contains at least 10 numbered items
    - `## Traceability` table contains one row per decision D-01..D-14 (14 data rows minimum)
    - Contract ends with the locked-by/implemented-by/verified-by line
    - `git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md` reports no modifications
  </acceptance_criteria>
  <done>
    Contract is locked, traceable, complete, and ready for Phase 184 and Phase 185 to execute against.
  </done>
</task>

</tasks>

<verification_criteria>
The contract is complete when:
1. `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md` exists with all 8 required top-level sections: Scope And Non-Goals, Labeling Requirements, Source Metadata Requirements, Operator Handoff Requirements, Degraded And Failure Requirements, Acceptance Criteria, Traceability, Out Of Scope.
2. Every CONTEXT.md decision D-01..D-14 appears at least once and is mapped in the traceability table.
3. Every downstream requirement (DASH-01, DASH-02, DASH-03, DASH-04, OPER-05) appears at least once.
4. `## Acceptance Criteria` contains at least 10 numbered, grep-verifiable items, each naming a literal string, metadata field, or degraded-state message.
5. The CLI handoff names the verified real invocation from `src/wanctl/history.py`.
6. Backend field names `metadata.source.mode` and `metadata.source.db_paths` are used verbatim; no new field names are invented.
7. No files outside `.planning/phases/183-dashboard-history-contract-audit/` are modified.
</verification_criteria>

<success_criteria>
- Contract artifact exists, is locked, and self-verifies via the grep checks above
- Traceability covers D-01..D-14 and DASH-01..04 + OPER-05 without gaps
- `git status --porcelain src/ tests/ docs/ deploy/ scripts/ CLAUDE.md` is empty
- Phase 184 can pick up this file and produce UI changes without re-asking "what does the contract mean?"
- Phase 185 can pick up this file and write regression assertions directly from the acceptance criteria
</success_criteria>

<output>
After completion, create `.planning/phases/183-dashboard-history-contract-audit/183-02-SUMMARY.md`
summarizing: contract sections written, decision coverage (D-01..D-14), requirement coverage
(DASH-01..04, OPER-05), total acceptance criteria count, and confirmation that the verified
CLI invocation matches the real `wanctl.history` entrypoint.
</output>
