---
phase: 185-verification-and-operator-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOYMENT.md
  - docs/RUNBOOK.md
  - docs/GETTING-STARTED.md
autonomous: true
requirements:
  - OPER-05
user_setup: []

must_haves:
  truths:
    - "docs/DEPLOYMENT.md contains the canonical D-05 distinction naming python3 -m wanctl.history as the authoritative merged cross-WAN proof path and describing /metrics/history as endpoint-local"
    - "docs/RUNBOOK.md Storage Topology And History Checks section contains the canonical D-05 distinction with no bare merged-history phrasing that implies the HTTP endpoint is cross-WAN"
    - "docs/GETTING-STARTED.md contains a brief history/monitoring pointer subsection naming python3 -m wanctl.history for merged cross-WAN proof and /metrics/history as endpoint-local"
    - "None of the three docs contain parity language describing /metrics/history as authoritative, merged, or cross-WAN"
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "Deployment-time storage/history section aligned with dashboard wording"
      contains: "endpoint-local"
    - path: "docs/RUNBOOK.md"
      provides: "Troubleshooting history-check section aligned with dashboard wording"
      contains: "endpoint-local"
    - path: "docs/GETTING-STARTED.md"
      provides: "First-pass operator pointer to the merged-CLI proof path"
      contains: "python3 -m wanctl.history"
  key_links:
    - from: "docs/DEPLOYMENT.md"
      to: "python3 -m wanctl.history"
      via: "explicit canonical-proof-path paragraph"
      pattern: "python3 -m wanctl.history"
    - from: "docs/RUNBOOK.md"
      to: "python3 -m wanctl.history"
      via: "Storage Topology And History Checks section"
      pattern: "python3 -m wanctl.history"
    - from: "docs/GETTING-STARTED.md"
      to: "python3 -m wanctl.history"
      via: "Monitoring And History pointer"
      pattern: "python3 -m wanctl.history"
---

<objective>
Align operator-facing documentation with the Phase 183/184 dashboard history
contract. Every doc edit enforces the single canonical rule from CONTEXT.md
D-05: /metrics/history is the endpoint-local HTTP history view for the
connected autorate daemon, and python3 -m wanctl.history is the
authoritative merged cross-WAN proof path.

DEPLOYMENT.md and RUNBOOK.md already have this wording from earlier phases;
this plan tightens it and removes residual split-sentence phrasing that can
be read as implying the HTTP endpoint is cross-WAN. GETTING-STARTED.md
currently says nothing about monitoring or history, so this plan adds a
brief pointer subsection that establishes the canonical split for first-pass
operators.

Purpose: Prove OPER-05 by making every operator workflow surface teach the
same history-source semantics the dashboard now enforces.

Output:
  - docs/DEPLOYMENT.md (surgical tightening of step 8 storage/history paragraph)
  - docs/RUNBOOK.md (surgical tightening of Storage Topology And History Checks)
  - docs/GETTING-STARTED.md (new "Monitoring And History" pointer subsection)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md
@.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md
@docs/DEPLOYMENT.md
@docs/RUNBOOK.md
@docs/GETTING-STARTED.md

<notes>
Scope constraint (CONTEXT.md D-04, D-06): this plan touches ONLY
docs/DEPLOYMENT.md, docs/RUNBOOK.md, and docs/GETTING-STARTED.md. Scripts,
helper commands, and other docs are out of scope unless they contain
conflicting operator-facing wording, and current source inspection shows
they do not.

Scope constraint (CONTEXT.md D-08): dashboard wording must avoid parity
claims with wanctl.history — this is enforced at the code-constants layer in
Plan 01 and must not be weakened by doc edits here. The docs must
consistently describe /metrics/history as endpoint-local and
python3 -m wanctl.history as the authoritative merged proof path.

Canonical D-05 sentence shape (reuse verbatim across all three docs so
operators see the same language):
  "/metrics/history is the endpoint-local HTTP history view for the
  connected autorate daemon; python3 -m wanctl.history is the
  authoritative merged cross-WAN proof path."

Existing wording as of 2026-04-14:
  - docs/DEPLOYMENT.md lines 87-95 already distinguishes the two paths but
    splits the distinction across three sentences and omits the canonical
    compound form.
  - docs/RUNBOOK.md lines 275-279 already distinguishes the two paths but
    also uses a three-sentence form.
  - docs/GETTING-STARTED.md contains no history guidance at all.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten docs/DEPLOYMENT.md step 8 storage/history paragraph</name>
  <files>docs/DEPLOYMENT.md</files>
  <read_first>
    - docs/DEPLOYMENT.md (entire file, with special attention to lines 72-96)
    - .planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md (D-04, D-05, D-08)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (L1, L2, H1, Acceptance Criteria 12)
  </read_first>
  <action>
    Read docs/DEPLOYMENT.md lines 87-95 which currently contain this block:

      For autorate history validation, do not target `/var/lib/wanctl/metrics.db` directly.
      On the current production hosts, the reliable cross-WAN proof path is:

      - `sudo -n env PYTHONPATH=/opt python3 -m wanctl.history ...`
      - direct DB inventory and spot-checks when needed

      `/metrics/history` is the endpoint-local history surface for the daemon serving that IP.
      Use it for endpoint availability, response shape, and that WAN's local history view. Use the
      CLI path for authoritative merged cross-WAN reads.

    Replace that block verbatim with:

      For autorate history validation, do not target `/var/lib/wanctl/metrics.db` directly.
      On the current production hosts:

      - `/metrics/history` is the endpoint-local HTTP history view for the connected autorate daemon.
      - `python3 -m wanctl.history` is the authoritative merged cross-WAN proof path.

      Use `/metrics/history` (for example via the `curl` command above) to confirm endpoint availability,
      response shape, and that WAN's local history view. Use
      `sudo -n env PYTHONPATH=/opt python3 -m wanctl.history ...` when you need merged cross-WAN proof;
      fall back to direct DB inventory only if the CLI is unavailable. The dashboard history tab
      surfaces this same distinction via `metadata.source` so operators see it in both places.

    Preserve line 87 ("For autorate history validation, do not target `/var/lib/wanctl/metrics.db` directly.") verbatim as the opening sentence of the replacement block.

    Do NOT touch any other section of DEPLOYMENT.md. Do NOT reorder the numbered deploy steps. Do NOT modify the compaction paragraph that follows (starting around line 97).
  </action>
  <verify>
    <automated>grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/DEPLOYMENT.md &amp;&amp; grep -q "authoritative merged cross-WAN proof path" docs/DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/DEPLOYMENT.md` == 1
    - `grep -c "authoritative merged cross-WAN proof path" docs/DEPLOYMENT.md` == 1
    - `grep -c "python3 -m wanctl.history" docs/DEPLOYMENT.md` >= 2 (existing ssh example + new canonical sentence)
    - `grep -c "reliable cross-WAN proof path" docs/DEPLOYMENT.md` == 0 (old wording removed)
    - `grep -c "the endpoint-local history surface for the daemon serving that IP" docs/DEPLOYMENT.md` == 0 (old split-sentence form removed)
    - `grep -c "metadata.source" docs/DEPLOYMENT.md` >= 1 (dashboard cross-reference present)
    - Numbered deploy step header still reachable: `grep -c "^8\. Re-check the active storage topology" docs/DEPLOYMENT.md` == 1
    - `grep -c "compact-metrics-dbs.sh" docs/DEPLOYMENT.md` >= 1 (compaction workflow preserved)
  </acceptance_criteria>
  <done>
    docs/DEPLOYMENT.md step 8 carries the canonical D-05 distinction in a
    two-bullet plus prose form, the old three-sentence wording is gone, and
    the rest of the file is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten docs/RUNBOOK.md Storage Topology And History Checks section</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - docs/RUNBOOK.md (entire file, with special attention to lines 247-290)
    - .planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md (D-04, D-05, D-08)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (L1, H1, Acceptance Criteria 12)
  </read_first>
  <action>
    Read docs/RUNBOOK.md lines 275-279 which currently contain:

      On the current production hosts, the module-based CLI invocation above is the authoritative
      cross-WAN proof path. `/metrics/history` is the endpoint-local HTTP history surface for the
      daemon serving that IP. Use the HTTP path to confirm endpoint availability, response shape, and
      that WAN's local history view. Use the CLI plus direct DB inventory if you need authoritative
      cross-WAN verification.

    Replace that paragraph verbatim with:

      On the current production hosts:

      - `/metrics/history` is the endpoint-local HTTP history view for the connected autorate daemon.
      - `python3 -m wanctl.history` is the authoritative merged cross-WAN proof path.

      Use the `curl` command above to confirm endpoint availability, response shape, and that WAN's
      local history view. Use the `python3 -m wanctl.history` command above — falling back to direct
      DB inventory only if the CLI is unavailable — when you need merged cross-WAN verification. The
      dashboard history tab surfaces this same distinction through `metadata.source`, so the rule is
      identical in the TUI and in this runbook.

    Do NOT touch the preceding "For retained history, use the supported readers..." sentence or the two example commands at lines 271-272 — those are the commands the new paragraph references via "above".

    Do NOT touch the compaction paragraph that starts at line 281.

    Do NOT modify the `wanctl-history --alerts` operator-tools reference at line 244 — that references the alerts CLI, not the merged history reader.
  </action>
  <verify>
    <automated>grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/RUNBOOK.md &amp;&amp; grep -q "authoritative merged cross-WAN proof path" docs/RUNBOOK.md &amp;&amp; grep -q "metadata.source" docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/RUNBOOK.md` == 1
    - `grep -c "authoritative merged cross-WAN proof path" docs/RUNBOOK.md` == 1
    - `grep -c "the endpoint-local HTTP history surface for the" docs/RUNBOOK.md` == 0 (old wording removed)
    - `grep -c "the module-based CLI invocation above is the authoritative" docs/RUNBOOK.md` == 0 (old wording removed)
    - `grep -c "python3 -m wanctl.history" docs/RUNBOOK.md` >= 2 (existing ssh example + new canonical sentence)
    - `grep -c "metadata.source" docs/RUNBOOK.md` >= 1
    - `grep -c "wanctl-history --alerts" docs/RUNBOOK.md` == 1 (alerts reference preserved)
    - `grep -c "^## Storage Topology And History Checks" docs/RUNBOOK.md` == 1 (section header preserved)
  </acceptance_criteria>
  <done>
    docs/RUNBOOK.md Storage Topology And History Checks section carries the
    canonical D-05 distinction in the same two-bullet plus prose form as
    DEPLOYMENT.md, the old split-sentence wording is gone, the alerts CLI
    reference is preserved, and the rest of the file is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add "Monitoring And History" pointer subsection to docs/GETTING-STARTED.md</name>
  <files>docs/GETTING-STARTED.md</files>
  <read_first>
    - docs/GETTING-STARTED.md (entire file, 218 lines, with special attention to lines 173-219)
    - .planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md (D-04, D-05)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (L1, H1)
  </read_first>
  <action>
    docs/GETTING-STARTED.md currently jumps from "Confirm the service is
    running" (lines 173-180) directly to "## Common Issues" (line 181) with
    no mention of history, monitoring, or the dashboard. A first-pass
    operator reading only this guide could form the incorrect assumption
    that there is only one history surface.

    Insert a new `## Monitoring And History` section between the existing
    "After the service is up, confirm:" block (ends at line 179) and the
    "## Common Issues" section (line 181). The new section MUST use the same
    canonical D-05 wording the other two docs use, so all three operator
    entry points teach the same rule.

    Insert the following subsection verbatim AFTER the "After the service is
    up, confirm:" bullet block and BEFORE "## Common Issues":

      ## Monitoring And History

      Once `wanctl` is running, two complementary history surfaces are available for any deployed WAN:

      - `/metrics/history` (HTTP) is the endpoint-local HTTP history view for the connected autorate daemon. Query it with `curl` against `http://<health-ip>:9101/metrics/history?range=1h&limit=5` to confirm endpoint availability, response shape, and that WAN's local history view.
      - `python3 -m wanctl.history` (CLI) is the authoritative merged cross-WAN proof path. Run it as `sudo -n env PYTHONPATH=/opt python3 -m wanctl.history --last 1h --metrics wanctl_rtt_ms --json` when you need merged cross-WAN proof across all deployed autorate instances on the host.

      The dashboard history tab surfaces the same distinction via `metadata.source`, so the rule is identical in the TUI, in the runbook, and in the deployment workflow. For deeper operator detail see [`RUNBOOK.md`](RUNBOOK.md) and [`DEPLOYMENT.md`](DEPLOYMENT.md).

    Do NOT change "## Common Issues" or any subsequent content. Do NOT reorder the "## Related Docs" list at the bottom of the file.
  </action>
  <verify>
    <automated>grep -q "^## Monitoring And History" docs/GETTING-STARTED.md &amp;&amp; grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/GETTING-STARTED.md &amp;&amp; grep -q "authoritative merged cross-WAN proof path" docs/GETTING-STARTED.md &amp;&amp; grep -q "python3 -m wanctl.history" docs/GETTING-STARTED.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Monitoring And History" docs/GETTING-STARTED.md` == 1
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/GETTING-STARTED.md` == 1
    - `grep -c "authoritative merged cross-WAN proof path" docs/GETTING-STARTED.md` == 1
    - `grep -c "python3 -m wanctl.history" docs/GETTING-STARTED.md` >= 1
    - `grep -c "metadata.source" docs/GETTING-STARTED.md` >= 1
    - `grep -c "^## Common Issues" docs/GETTING-STARTED.md` == 1 (untouched section still present)
    - `grep -c "^## Related Docs" docs/GETTING-STARTED.md` == 1 (untouched section still present)
    - The Monitoring And History section appears BEFORE Common Issues: `awk '/^## Monitoring And History/{m=NR} /^## Common Issues/{c=NR} END{exit !(m>0 && c>0 && m<c)}' docs/GETTING-STARTED.md` exits 0
  </acceptance_criteria>
  <done>
    docs/GETTING-STARTED.md contains a new "## Monitoring And History"
    section inserted before "## Common Issues" that carries the canonical
    D-05 distinction and cross-references RUNBOOK.md and DEPLOYMENT.md. The
    rest of the file is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cross-doc parity sweep</name>
  <files></files>
  <read_first>
    - docs/DEPLOYMENT.md (Task 1 output)
    - docs/RUNBOOK.md (Task 2 output)
    - docs/GETTING-STARTED.md (Task 3 output)
    - .planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md (D-05, D-08)
  </read_first>
  <action>
    After Tasks 1-3 complete, run a sweep across the three doc files to
    confirm there is NO residual wording that describes `/metrics/history`
    as authoritative or cross-WAN, and that the canonical D-05 sentence
    appears in all three docs:

      # The canonical fragment MUST appear at least once in each doc
      for f in docs/DEPLOYMENT.md docs/RUNBOOK.md docs/GETTING-STARTED.md; do
        grep -q "endpoint-local HTTP history view for the connected autorate daemon" "$f" || { echo "MISSING canonical phrase in $f"; exit 1; }
        grep -q "authoritative merged cross-WAN proof path" "$f" || { echo "MISSING merged-proof phrase in $f"; exit 1; }
      done

      # /metrics/history MUST NOT be described as authoritative or merged
      # Allow-list check: find any line that mentions /metrics/history AND one of the
      # forbidden parity words, and fail if the line is not the endpoint-local sentence.
      for f in docs/DEPLOYMENT.md docs/RUNBOOK.md docs/GETTING-STARTED.md; do
        if grep -n "/metrics/history" "$f" | grep -E "authoritative|merged history|cross-WAN history" | grep -v "endpoint-local" | grep -v "authoritative merged cross-WAN proof path"; then
          echo "PARITY VIOLATION in $f"; exit 1
        fi
      done

    If any check fails, STOP and return the offending file and line to the
    orchestrator. Do NOT edit files to silence the check — any violation is
    a real operator-wording regression and must be fixed in Tasks 1-3.
  </action>
  <verify>
    <automated>for f in docs/DEPLOYMENT.md docs/RUNBOOK.md docs/GETTING-STARTED.md; do grep -q "endpoint-local HTTP history view for the connected autorate daemon" "$f" &amp;&amp; grep -q "authoritative merged cross-WAN proof path" "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All three docs contain "endpoint-local HTTP history view for the connected autorate daemon" (exactly once each)
    - All three docs contain "authoritative merged cross-WAN proof path" (exactly once each)
    - No doc contains a line mentioning "/metrics/history" together with parity tokens ("authoritative", "merged history", "cross-WAN history") other than the canonical sentence
    - `git diff --stat docs/DEPLOYMENT.md docs/RUNBOOK.md docs/GETTING-STARTED.md` shows all three files changed
    - No source code file under src/ was modified by this plan (verify with `git diff --stat src/` empty for this plan's scope)
  </acceptance_criteria>
  <done>
    All three operator-facing docs carry the canonical D-05 distinction in
    identical language, no doc contains parity claims equating
    `/metrics/history` with the merged CLI reader, and no source code was
    touched.
  </done>
</task>

</tasks>

<verification>
Phase verification for this plan:
  - docs/DEPLOYMENT.md, docs/RUNBOOK.md, docs/GETTING-STARTED.md all contain the canonical D-05 sentence fragments
  - No doc equates /metrics/history with merged or authoritative CLI reading
  - docs/GETTING-STARTED.md has a new "## Monitoring And History" section before "## Common Issues"
  - No source code changes
  - No changes to scripts, config examples, or helper tool output (out of scope per D-06)
</verification>

<success_criteria>
OPER-05 is proven by three operator-facing docs carrying identical canonical
D-05 wording that matches the Phase 184 dashboard HISTORY_COPY and HANDOFF
constants. First-pass, deployment, and troubleshooting operators all learn
the same rule: /metrics/history is endpoint-local; python3 -m wanctl.history
is the authoritative merged cross-WAN proof path.
</success_criteria>

<output>
After completion, create `.planning/phases/185-verification-and-operator-alignment/185-02-SUMMARY.md`
</output>
