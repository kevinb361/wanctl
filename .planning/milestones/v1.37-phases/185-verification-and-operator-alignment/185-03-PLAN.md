---
phase: 185-verification-and-operator-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - 185-01
  - 185-02
files_modified:
  - .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md
autonomous: true
requirements:
  - DASH-04
  - OPER-05
user_setup: []

must_haves:
  truths:
    - "185-VERIFICATION.md exists and maps every Phase 183 contract acceptance criterion (items 1-12) to concrete evidence: a test function (from Plan 01) or a doc section (from Plan 02)"
    - "185-VERIFICATION.md explicitly confirms DASH-04 is met by enumerating the test functions covering each F3 regression surface area from the Phase 183 contract"
    - "185-VERIFICATION.md explicitly confirms OPER-05 is met by pointing to the canonical D-05 sentence presence in all three operator docs"
    - "185-VERIFICATION.md explicitly records that (a) dashboard wording avoids parity claims with wanctl.history, (b) HANDOFF_TEXT is verbatim and covered by a widget-state-matrix test, (c) fetch_error / source_missing / mode_missing / db_paths_missing states stay operator-visible rather than silently fallen-back"
    - "185-VERIFICATION.md records the `.venv/bin/pytest tests/dashboard/ -q` command as the reproducible regression proof and captures its exit code"
  artifacts:
    - path: ".planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md"
      provides: "Phase 185 closeout traceability artifact for DASH-04 and OPER-05"
      contains: "DASH-04"
      min_lines: 80
  key_links:
    - from: ".planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md"
      to: "tests/dashboard/test_history_state.py"
      via: "named test functions listed in the traceability table"
      pattern: "test_classify_history_state"
    - from: ".planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md"
      to: "tests/dashboard/test_history_browser.py"
      via: "named widget test functions listed in the traceability table"
      pattern: "TestHistoryBrowserSourceContract"
    - from: ".planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md"
      to: ".planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md"
      via: "explicit citation of acceptance criteria 1-12 mapped to evidence"
      pattern: "183-dashboard-source-contract"
---

<objective>
Produce the Phase 185 closeout verification artifact. Trace every Phase 183
locked contract item to concrete evidence created in Plan 01 (tests) or
Plan 02 (operator docs), and explicitly confirm DASH-04 and OPER-05 are
met. This plan is repo-side and traceability-driven per CONTEXT.md D-07
and D-09 — no live-production or control-loop evidence is required.

Purpose: Lock v1.37 milestone closeout. After this plan, both v1.37
requirements (DASH-04, OPER-05) have a single, auditable artifact that
maps them back to the Phase 183 contract and to concrete test / doc
evidence.

Output:
  - .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md
@.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md
@.planning/phases/184-dashboard-history-source-surfacing/184-CONTEXT.md

# Plan 01 + Plan 02 outputs (must exist before this plan runs)
@tests/dashboard/test_history_state.py
@tests/dashboard/test_history_browser.py
@docs/DEPLOYMENT.md
@docs/RUNBOOK.md
@docs/GETTING-STARTED.md

# Dashboard implementation under test (read-only reference for closeout)
@src/wanctl/dashboard/widgets/history_state.py
@src/wanctl/dashboard/widgets/history_browser.py

<notes>
This plan runs in wave 2 and depends on both 185-01 and 185-02. The
executor MUST read the files produced by those plans — do NOT cite test
functions or doc sections speculatively. Every row in the traceability
table MUST be grep-verifiable against the actual Plan 01 / Plan 02 output.

Scope constraint (CONTEXT.md D-09): no live-production evidence required.
This plan does not SSH to cake-shaper, does not run canary-check.sh
against production, and does not assert anything about storage footprints.

Scope constraint (CONTEXT.md D-07, D-08): the closeout MUST explicitly
confirm three invariants:
  1. Dashboard wording avoids parity claims with wanctl.history
     (proven by the module-scope _assert_no_parity_language guard in
     history_browser.py plus test_history_copy_no_parity_language_in_banners)
  2. HANDOFF_TEXT stays verbatim across all history states
     (proven by test_handoff_text_is_verbatim_across_all_states)
  3. Degraded / failure states stay operator-visible
     (proven by the five widget-state-matrix tests + the classifier branch tests)
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dashboard regression slice and capture exit status</name>
  <files></files>
  <read_first>
    - tests/dashboard/test_history_state.py (verify it exists — Plan 01 output)
    - tests/dashboard/test_history_browser.py (verify TestHistoryBrowserSourceContract class exists — Plan 01 output)
  </read_first>
  <action>
    Run the Phase 185 dashboard regression slice command exactly as the
    closeout artifact will record it:

      .venv/bin/pytest tests/dashboard/ -q

    Capture stdout, stderr, and the exit code. If exit code is non-zero,
    STOP and return failure — the closeout artifact must not be written
    against a red regression suite. Record the collected test count (e.g.
    "N passed in X.XXs") for use in Task 3.

    Also confirm, via grep, that Plan 01 and Plan 02 outputs actually
    exist before proceeding:

      test -f tests/dashboard/test_history_state.py
      grep -q "class TestHistoryBrowserSourceContract" tests/dashboard/test_history_browser.py
      grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/DEPLOYMENT.md
      grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/RUNBOOK.md
      grep -q "endpoint-local HTTP history view for the connected autorate daemon" docs/GETTING-STARTED.md

    Any failed check = return failure to the orchestrator; do not fabricate
    missing upstream artifacts.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/dashboard/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/dashboard/ -q` exits 0
    - `test -f tests/dashboard/test_history_state.py` (file exists)
    - `grep -c "class TestHistoryBrowserSourceContract" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/DEPLOYMENT.md` == 1
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/RUNBOOK.md` == 1
    - `grep -c "endpoint-local HTTP history view for the connected autorate daemon" docs/GETTING-STARTED.md` == 1
    - The pytest stdout total (N passed) is recorded for Task 3
  </acceptance_criteria>
  <done>
    Dashboard regression slice is green, all Plan 01 / Plan 02 upstream
    artifacts are present, and the pytest output summary is captured for
    the closeout artifact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the DASH-04 / OPER-05 traceability table</name>
  <files></files>
  <read_first>
    - tests/dashboard/test_history_state.py (full file — to copy exact test function names)
    - tests/dashboard/test_history_browser.py (TestHistoryBrowserSourceContract class — to copy exact method names)
    - docs/DEPLOYMENT.md (to cite exact section / line)
    - docs/RUNBOOK.md (to cite exact section / line)
    - docs/GETTING-STARTED.md (to cite exact section / line)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (Acceptance Criteria 1-12 + F3 regression surface areas)
  </read_first>
  <action>
    No files written in this task. Assemble (in memory) the traceability
    content that Task 3 will write into 185-VERIFICATION.md. The content
    MUST be derived by actually reading Plan 01 and Plan 02 outputs — not
    by restating this plan's expectations.

    For each Phase 183 Acceptance Criterion (1 through 12), identify the
    concrete evidence:

      Criterion 1 (endpoint-local visible)
        -> HISTORY_COPY.BANNER_SUCCESS copy test +
           widget success-state test (TestHistoryBrowserSourceContract::test_success_state_renders_banner_detail_and_handoff)
      Criterion 2 (no bare merged phrases)
        -> test_history_copy_no_parity_language_in_banners
           AND module-scope _assert_no_parity_language guard in history_browser.py
      Criterion 3 (mode-specific label, not raw)
        -> test_success_state_renders_banner_detail_and_handoff (asserts MODE_PHRASE_LOCAL in source-detail)
           AND test_success_state_merged_discovery_lists_db_basenames (asserts MODE_PHRASE_MERGED)
      Criterion 4 (db_paths-derived context)
        -> test_success_state_renders_banner_detail_and_handoff (1-path form)
           + test_success_state_merged_discovery_lists_db_basenames (N-path form)
      Criterion 5 (raw mode not primary label)
        -> confirmed via source_detail format assertion in the success tests + the _format_source_detail contract
      Criterion 6 (CLI handoff present)
        -> test_handoff_text_is_verbatim_across_all_states
           + test_history_copy_handoff_mentions_python3_m_wanctl_history_verbatim
      Criterion 7 (no parity claim with wanctl.history)
        -> test_history_copy_no_parity_language_in_banners
           + module-scope _assert_no_parity_language guard
      Criterion 8 (fetch-failure explicit, no stale rows)
        -> test_fetch_error_state_clears_table_and_shows_unavailable_banner
      Criterion 9 (metadata.source missing -> ambiguous)
        -> test_source_missing_state_shows_ambiguous_banner_and_preserves_handoff
           + test_classify_history_state_source_missing_*
      Criterion 10 (db_paths missing/empty -> ambiguous)
        -> test_db_paths_missing_state_shows_db_paths_ambiguous_banner
           + test_classify_history_state_db_paths_missing_*
      Criterion 11 (success uses mode + db_paths)
        -> test_success_state_renders_banner_detail_and_handoff
      Criterion 12 (operator docs match dashboard wording)
        -> docs/DEPLOYMENT.md §8 canonical D-05 sentence
           + docs/RUNBOOK.md Storage Topology And History Checks canonical D-05 sentence
           + docs/GETTING-STARTED.md Monitoring And History canonical D-05 sentence

    Additionally, identify the mode_missing evidence (F3 regression surface
    area not explicitly numbered in Acceptance Criteria but required by §F3):
      - test_mode_missing_state_shows_mode_ambiguous_banner
      - test_classify_history_state_mode_missing_when_mode_unknown
      - test_classify_history_state_mode_missing_when_mode_not_string
      - test_classify_history_state_mode_missing_when_mode_absent

    For each test function cited, verify it actually exists by grepping the
    file BEFORE Task 3 writes it down. Any name that doesn't exist in the
    actual Plan 01 output MUST be replaced with the real name from that file,
    or Task 3 halts and reports the mismatch.
  </action>
  <verify>
    <automated>grep -q "test_success_state_renders_banner_detail_and_handoff" tests/dashboard/test_history_browser.py &amp;&amp; grep -q "test_classify_history_state_fetch_error_for_exception" tests/dashboard/test_history_state.py &amp;&amp; grep -q "test_history_copy_no_parity_language_in_banners" tests/dashboard/test_history_state.py</automated>
  </verify>
  <acceptance_criteria>
    - Every test function name to be cited in Task 3 is verified present in its file via `grep -q`
    - Every doc section to be cited in Task 3 is verified present via `grep -q "endpoint-local HTTP history view for the connected autorate daemon"`
    - If any name or section is missing, this task halts and reports the mismatch — no fabrication
  </acceptance_criteria>
  <done>
    The traceability mapping between Phase 183 contract items and Plan 01 /
    Plan 02 evidence is fully grep-verified against the actual upstream
    outputs and ready to be written by Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md</name>
  <files>.planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md</files>
  <read_first>
    - (all files listed in Task 2's read_first — already read in Task 2, no re-read needed)
    - .planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md (to cite D-07, D-08, D-09 in the closeout)
  </read_first>
  <action>
    Create the closeout artifact at
    `.planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md`.

    The file MUST contain these sections in this exact order:

    1. `# Phase 185 Verification — Dashboard History Source Clarity Closeout`
    2. `## Purpose` — one paragraph explaining that this file closes
       DASH-04 and OPER-05 for v1.37 against the Phase 183 locked contract,
       and that closeout is intentionally repo-side per CONTEXT.md D-09.
    3. `## Regression Proof Command` — exact reproducible command and the
       captured exit code from Task 1:
         ```
         .venv/bin/pytest tests/dashboard/ -q
         # exit 0
         # <N passed> from Task 1 output
         ```
    4. `## Contract Traceability Table` — a markdown table with columns:
         | Phase 183 Contract Item | Requirement | Evidence (test or doc) | File |
       Rows MUST cover all 12 acceptance criteria from the contract plus
       the four F3 regression surface areas for mode_missing. Each
       "Evidence" cell names a specific test function or a specific doc
       section heading. Each "File" cell is the actual path (for example
       `tests/dashboard/test_history_browser.py` or `docs/DEPLOYMENT.md`).
       Use ONLY names verified in Task 2.
    5. `## DASH-04 Confirmation` — prose confirmation listing:
         - the pure classifier test file and the branches it covers
         - the widget state-matrix test class and the states it covers
         - the HANDOFF_TEXT verbatim invariant across all states
         - the classifier module's documented D-15 precedence rule
       End with: `DASH-04 is satisfied by the evidence above.`
    6. `## OPER-05 Confirmation` — prose confirmation listing:
         - the canonical D-05 sentence present in docs/DEPLOYMENT.md
         - the canonical D-05 sentence present in docs/RUNBOOK.md
         - the canonical D-05 sentence present in docs/GETTING-STARTED.md
         - that all three docs describe `/metrics/history` as endpoint-local
           and `python3 -m wanctl.history` as the authoritative merged
           cross-WAN proof path
       End with: `OPER-05 is satisfied by the evidence above.`
    7. `## Invariant Confirmations` — three explicit statements required by
       CONTEXT.md D-08:
         1. "Dashboard wording avoids parity claims with wanctl.history"
            — cite the module-scope `_assert_no_parity_language` import-time
            guard in `src/wanctl/dashboard/widgets/history_browser.py` and
            the `test_history_copy_no_parity_language_in_banners` test.
         2. "Merged-CLI handoff stays verbatim across every history state"
            — cite `HistoryBrowserWidget.HANDOFF_TEXT` and
            `test_handoff_text_is_verbatim_across_all_states`.
         3. "Degraded and failure states stay operator-visible, never
            silently fallen back" — cite the five widget state-matrix tests
            plus the classifier branch tests.
    8. `## Out Of Scope For Closeout` — one paragraph restating CONTEXT.md
       D-09: no live-production storage evidence, no control-loop evidence,
       no backend `/metrics/history` semantic changes. Any such work is
       explicitly deferred beyond v1.37.
    9. `## References` — bullet list citing:
         - `.planning/REQUIREMENTS.md` (DASH-04, OPER-05 rows)
         - `.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md` (locked contract)
         - `.planning/phases/184-dashboard-history-source-surfacing/184-CONTEXT.md` (Phase 184 UI decisions, not reopened)
         - `.planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md` (D-01..D-09, authoritative for Phase 185 scope)
         - `.planning/phases/185-verification-and-operator-alignment/185-01-SUMMARY.md` (Plan 01 evidence summary, may not exist yet until Plan 01 completes — cite the plan file path instead if missing)
         - `.planning/phases/185-verification-and-operator-alignment/185-02-SUMMARY.md` (Plan 02 evidence summary, same caveat)
         - `.planning/ROADMAP.md` §Phase 185

    Close the file with a footer line of the form:
      `*Verification locked: <YYYY-MM-DD>. Milestone v1.37.*`

    Use today's date from the active environment rather than hardcoding.
  </action>
  <verify>
    <automated>test -f .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md &amp;&amp; grep -q "DASH-04 is satisfied" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md &amp;&amp; grep -q "OPER-05 is satisfied" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` exists
    - `grep -c "^# Phase 185 Verification" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "^## Regression Proof Command" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c ".venv/bin/pytest tests/dashboard/ -q" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "^## Contract Traceability Table" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "^## DASH-04 Confirmation" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "^## OPER-05 Confirmation" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "^## Invariant Confirmations" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "DASH-04 is satisfied" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "OPER-05 is satisfied" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` == 1
    - `grep -c "HANDOFF_TEXT" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "_assert_no_parity_language" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "183-dashboard-source-contract" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "test_handoff_text_is_verbatim_across_all_states" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "test_history_copy_no_parity_language_in_banners" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "docs/DEPLOYMENT.md" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "docs/RUNBOOK.md" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `grep -c "docs/GETTING-STARTED.md" .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 1
    - `wc -l < .planning/phases/185-verification-and-operator-alignment/185-VERIFICATION.md` >= 80 (substantive, not a stub)
  </acceptance_criteria>
  <done>
    185-VERIFICATION.md exists, maps every Phase 183 contract acceptance
    criterion to concrete evidence from Plan 01 (tests) or Plan 02 (docs),
    explicitly confirms DASH-04 and OPER-05, restates the three invariants
    from CONTEXT.md D-08, and records the reproducible regression command.
  </done>
</task>

</tasks>

<verification>
Phase verification for this plan:
  - 185-VERIFICATION.md exists at the phase directory with all nine required sections
  - Every test function cited in the traceability table grep-verifies against the actual Plan 01 output files
  - Every doc section cited grep-verifies against the actual Plan 02 output files
  - DASH-04 and OPER-05 are each explicitly marked as satisfied with evidence
  - The three CONTEXT.md D-08 invariants are explicitly recorded
  - `.venv/bin/pytest tests/dashboard/ -q` still exits 0 (non-regression)
  - No source code or non-doc files were modified by this plan
</verification>

<success_criteria>
Phase 185 is closed when 185-VERIFICATION.md exists, dashboard tests are
green, operator docs carry canonical D-05 wording, and DASH-04 + OPER-05
are both confirmed satisfied. The v1.37 milestone can then be archived
with a single auditable traceability artifact pointing at all evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/185-verification-and-operator-alignment/185-03-SUMMARY.md`
</output>
