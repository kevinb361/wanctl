---
phase: 185-verification-and-operator-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/dashboard/test_history_state.py
  - tests/dashboard/test_history_browser.py
autonomous: true
requirements:
  - DASH-04
user_setup: []

must_haves:
  truths:
    - "A pure classifier test module asserts all five HistoryState branches against HISTORY_COPY constants without mounting Textual"
    - "HistoryBrowserWidget widget tests prove HANDOFF_TEXT renders verbatim in every fetch state (success, fetch_error, source_missing, mode_missing, db_paths_missing)"
    - "Widget tests prove fetch_error clears the DataTable and shows BANNER_FETCH_ERROR + DETAIL_FETCH_ERROR (no silent fallback)"
    - "Widget tests prove each F2 degraded state surfaces its specific ambiguous banner copy and DETAIL_AMBIGUOUS"
    - "`.venv/bin/pytest tests/dashboard/ -q` exits 0"
  artifacts:
    - path: "tests/dashboard/test_history_state.py"
      provides: "Pure classify_history_state() coverage for all 5 HistoryState branches"
      contains: "def test_classify_history_state_"
    - path: "tests/dashboard/test_history_browser.py"
      provides: "Widget-level regressions for HANDOFF_TEXT verbatim render and banner/detail copy per state"
      contains: "HANDOFF_TEXT"
  key_links:
    - from: "tests/dashboard/test_history_state.py"
      to: "src/wanctl/dashboard/widgets/history_state.py"
      via: "import HISTORY_COPY, HistoryState, classify_history_state"
      pattern: "from wanctl.dashboard.widgets.history_state import"
    - from: "tests/dashboard/test_history_browser.py"
      to: "src/wanctl/dashboard/widgets/history_browser.py"
      via: "HistoryBrowserWidget.HANDOFF_TEXT assertion"
      pattern: "HANDOFF_TEXT"
---

<objective>
Add focused regression coverage that locks the Phase 183 dashboard history
contract in code. Cover the pure history_state classifier (5 branches) plus
HistoryBrowserWidget compose/fetch behavior for success, fetch_error, and
all three F2 ambiguous-source states, asserting directly against the
HISTORY_COPY constants and HistoryBrowserWidget.HANDOFF_TEXT surface exposed
in Phase 184.

Purpose: Prove DASH-04 by proving each of the eight regression surface areas
named in 183-dashboard-source-contract.md §F3 (D-12) maps to a concrete test
without reopening Phase 184 UI decisions and without changing any backend.

Output:
  - tests/dashboard/test_history_state.py (new) — pure classifier/copy tests
  - tests/dashboard/test_history_browser.py (expanded) — state-matrix widget tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/185-verification-and-operator-alignment/185-CONTEXT.md
@.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md
@.planning/phases/184-dashboard-history-source-surfacing/184-CONTEXT.md
@src/wanctl/dashboard/widgets/history_state.py
@src/wanctl/dashboard/widgets/history_browser.py
@tests/dashboard/test_history_browser.py

<interfaces>
<!-- Public contract under test. Import these directly in new tests. -->

From src/wanctl/dashboard/widgets/history_state.py:
```python
class HistoryState(str, Enum):
    SUCCESS = "success"
    FETCH_ERROR = "fetch_error"
    SOURCE_MISSING = "source_missing"
    MODE_MISSING = "mode_missing"
    DB_PATHS_MISSING = "db_paths_missing"

KNOWN_SOURCE_MODES: frozenset[str] = frozenset({"local_configured_db", "merged_discovery"})

@dataclass(frozen=True)
class HistoryCopy:
    BANNER_SUCCESS: str          # "Endpoint-local history from the connected autorate daemon."
    BANNER_FETCH_ERROR: str      # "History unavailable."
    BANNER_SOURCE_MISSING: str   # "Source context unavailable — treat this history view as ambiguous."
    BANNER_MODE_MISSING: str     # "Source mode unavailable — treat this history view as ambiguous."
    BANNER_DB_PATHS_MISSING: str # "Source database paths unavailable — treat this history view as ambiguous."
    DETAIL_FETCH_ERROR: str      # "Use python3 -m wanctl.history when you need merged cross-WAN proof."
    DETAIL_AMBIGUOUS: str        # "Use python3 -m wanctl.history for authoritative merged proof."
    HANDOFF: str                 # "For merged cross-WAN proof, run: python3 -m wanctl.history"
    MODE_PHRASE_LOCAL: str       # "Connected endpoint local database"
    MODE_PHRASE_MERGED: str      # "Discovered database set on this endpoint"
    SUMMARY_NO_DATA: str         # "No data"

HISTORY_COPY = HistoryCopy()

def classify_history_state(resp_or_exc: Any) -> HistoryState: ...
# Precedence (D-15, first match wins):
#   1. Exception            -> FETCH_ERROR
#   2. not-dict / metadata not dict / source not dict -> SOURCE_MISSING
#   3. mode not str or mode not in KNOWN_SOURCE_MODES  -> MODE_MISSING
#   4. db_paths not list or empty                       -> DB_PATHS_MISSING
#   5. else                                             -> SUCCESS
```

From src/wanctl/dashboard/widgets/history_browser.py:
```python
class HistoryBrowserWidget(Widget):
    HANDOFF_TEXT: str = HISTORY_COPY.HANDOFF  # exposed for Phase 185 assertions

    def compose(self) -> ComposeResult:
        yield Static(HISTORY_COPY.BANNER_SUCCESS, id="source-banner")
        yield Static("", id="source-detail")
        yield Static(HISTORY_COPY.HANDOFF, id="source-handoff")
        yield Select(options=TIME_RANGES, value="1h", id="time-range")
        yield Static("Select a time range", id="summary-stats")
        yield DataTable(id="history-table")
        yield Static("", id="source-diagnostic", classes="dim")

    async def _fetch_and_populate(self, time_range: str) -> None: ...
    # Routes through classify_history_state; updates #source-banner,
    # #source-detail, #source-handoff (never mutated post-compose),
    # #source-diagnostic, #summary-stats, #history-table.
```

Phase 183 contract §F3 (D-12) regression surface areas:
  A. success with mode=local_configured_db (1-path render)
  B. plainly visible endpoint-local label (BANNER_SUCCESS)
  C. DB-path-derived source context (source-detail shows db_paths value)
  D. visible CLI handoff `python3 -m wanctl.history` (HANDOFF_TEXT verbatim)
  E. fetch-error failure state
  F. missing-metadata.source degraded state
  G. missing-metadata.source.mode degraded state
  H. missing/empty metadata.source.db_paths degraded state
</interfaces>

<notes>
Backend payload contract (metadata.source.mode / metadata.source.db_paths)
is already covered by tests/test_health_check.py (see
test_history_uses_local_configured_db_when_controller_present around line
3163 and the shape check at line 3148-3153). Per CONTEXT.md D-03 discretion,
no additional tests/test_health_check.py assertions are needed in this plan.
This plan is narrowly scoped to the dashboard consumer side of the contract.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure classifier tests at tests/dashboard/test_history_state.py</name>
  <files>tests/dashboard/test_history_state.py</files>
  <read_first>
    - src/wanctl/dashboard/widgets/history_state.py (entire file — 92 lines)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (F1, F2, F3 and Acceptance Criteria 1, 3, 4, 5, 6, 8, 9, 10, 11)
    - .planning/phases/184-dashboard-history-source-surfacing/184-CONTEXT.md (D-15 precedence rule)
  </read_first>
  <behavior>
    Pure classifier tests only. No Textual mount, no httpx, no asyncio.
    All assertions import HISTORY_COPY and HistoryState directly from
    wanctl.dashboard.widgets.history_state — NO duplicated copy strings.

    Test functions (exact names required):
      - test_classify_history_state_success_returns_success
          Input: {"data": [], "metadata": {"source": {"mode": "local_configured_db", "db_paths": ["/var/lib/wanctl/metrics-att.db"]}}}
          Asserts: classify_history_state(input) is HistoryState.SUCCESS
      - test_classify_history_state_success_merged_discovery
          Input: metadata.source.mode="merged_discovery", db_paths=["/a.db","/b.db"]
          Asserts: classify_history_state(input) is HistoryState.SUCCESS
      - test_classify_history_state_fetch_error_for_exception
          Input: RuntimeError("boom")
          Asserts: is HistoryState.FETCH_ERROR
      - test_classify_history_state_fetch_error_for_httpx_timeout
          Input: simulate any Exception subclass (plain Exception("timeout") is fine — classifier uses isinstance(Exception))
          Asserts: is HistoryState.FETCH_ERROR
      - test_classify_history_state_source_missing_when_payload_not_dict
          Input: "not a dict"
          Asserts: is HistoryState.SOURCE_MISSING
      - test_classify_history_state_source_missing_when_metadata_not_dict
          Input: {"metadata": "nope"}
          Asserts: is HistoryState.SOURCE_MISSING
      - test_classify_history_state_source_missing_when_source_absent
          Input: {"metadata": {}}
          Asserts: is HistoryState.SOURCE_MISSING
      - test_classify_history_state_source_missing_when_source_not_dict
          Input: {"metadata": {"source": "local_configured_db"}}
          Asserts: is HistoryState.SOURCE_MISSING
      - test_classify_history_state_mode_missing_when_mode_absent
          Input: {"metadata": {"source": {"db_paths": ["/a.db"]}}}
          Asserts: is HistoryState.MODE_MISSING
      - test_classify_history_state_mode_missing_when_mode_unknown
          Input: mode="frankenmode", db_paths=["/a.db"]
          Asserts: is HistoryState.MODE_MISSING
      - test_classify_history_state_mode_missing_when_mode_not_string
          Input: mode=42, db_paths=["/a.db"]
          Asserts: is HistoryState.MODE_MISSING
      - test_classify_history_state_db_paths_missing_when_absent
          Input: mode="local_configured_db" (no db_paths key)
          Asserts: is HistoryState.DB_PATHS_MISSING
      - test_classify_history_state_db_paths_missing_when_empty_list
          Input: mode="local_configured_db", db_paths=[]
          Asserts: is HistoryState.DB_PATHS_MISSING
      - test_classify_history_state_db_paths_missing_when_not_list
          Input: mode="local_configured_db", db_paths="/a.db"
          Asserts: is HistoryState.DB_PATHS_MISSING
      - test_classify_precedence_fetch_error_beats_source_missing
          Input: an Exception (even an irrelevant one) must win over dict-shape checks
          Asserts: is HistoryState.FETCH_ERROR
      - test_history_copy_handoff_mentions_python3_m_wanctl_history_verbatim
          Asserts: "python3 -m wanctl.history" in HISTORY_COPY.HANDOFF
          Asserts: HISTORY_COPY.HANDOFF == "For merged cross-WAN proof, run: python3 -m wanctl.history"
      - test_history_copy_banner_success_is_endpoint_local
          Asserts: "endpoint-local" in HISTORY_COPY.BANNER_SUCCESS.lower()
          Asserts: "connected autorate daemon" in HISTORY_COPY.BANNER_SUCCESS.lower()
      - test_history_copy_details_reference_merged_cli
          Asserts: "python3 -m wanctl.history" in HISTORY_COPY.DETAIL_FETCH_ERROR
          Asserts: "python3 -m wanctl.history" in HISTORY_COPY.DETAIL_AMBIGUOUS
      - test_history_copy_no_parity_language_in_banners
          For each of BANNER_SUCCESS, BANNER_FETCH_ERROR, BANNER_SOURCE_MISSING,
          BANNER_MODE_MISSING, BANNER_DB_PATHS_MISSING:
            assert "authoritative" not in banner.lower()
            assert "wanctl-history" not in banner.lower()
            assert "cross-wan history" not in banner.lower()
          (This enforces 183-contract L2 at the constants layer.)
  </behavior>
  <action>
    Create tests/dashboard/test_history_state.py as a new pure-Python pytest
    module (no Textual, no asyncio, no httpx). File header docstring MUST
    reference the contract sections it covers:

      """Pure regression coverage for history_state classifier + HISTORY_COPY.

      Traces to Phase 183 contract
      (.planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md)
      sections L1, L2, F1, F2, F3 (D-12) and Acceptance Criteria 1, 7, 8, 9, 10.
      Phase 185 Plan 01 — DASH-04.
      """

    Import surface (top of file):
      from wanctl.dashboard.widgets.history_state import (
          HISTORY_COPY,
          HistoryState,
          KNOWN_SOURCE_MODES,
          classify_history_state,
      )

    Organize tests into three top-level classes:
      class TestClassifyHistoryStateSuccess   # SUCCESS branch + merged_discovery
      class TestClassifyHistoryStateFailure   # FETCH_ERROR + precedence
      class TestClassifyHistoryStateAmbiguous # SOURCE_MISSING / MODE_MISSING / DB_PATHS_MISSING
      class TestHistoryCopyContract           # copy invariants (no parity, handoff verbatim, banner endpoint-local)

    Every test function name listed in <behavior> MUST appear verbatim.
    Every test MUST assert against HISTORY_COPY / HistoryState members — do
    NOT inline the operator copy strings.

    The MODE_MISSING precedence test (F2b) MUST feed both a known mode with
    empty db_paths (expects DB_PATHS_MISSING) AND an unknown mode with a
    valid db_paths (expects MODE_MISSING) to prove the ordering in D-15.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/dashboard/test_history_state.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File tests/dashboard/test_history_state.py exists
    - `grep -c "def test_classify_history_state_success_returns_success" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_classify_history_state_fetch_error_for_exception" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_classify_history_state_source_missing_when_source_absent" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_classify_history_state_mode_missing_when_mode_unknown" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_classify_history_state_db_paths_missing_when_empty_list" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_history_copy_handoff_mentions_python3_m_wanctl_history_verbatim" tests/dashboard/test_history_state.py` == 1
    - `grep -c "def test_history_copy_no_parity_language_in_banners" tests/dashboard/test_history_state.py` == 1
    - `grep -c "from wanctl.dashboard.widgets.history_state import" tests/dashboard/test_history_state.py` >= 1
    - NO duplicated HISTORY_COPY string literals in the test body (grep -c '"Endpoint-local history from the connected autorate daemon."' tests/dashboard/test_history_state.py == 0 — must go through HISTORY_COPY.BANNER_SUCCESS)
    - `.venv/bin/pytest tests/dashboard/test_history_state.py -q` exits 0
    - `grep -c "183-dashboard-source-contract" tests/dashboard/test_history_state.py` >= 1 (traceability docstring present)
  </acceptance_criteria>
  <done>
    tests/dashboard/test_history_state.py exists with all 18 listed test
    functions, passes under pytest, references the Phase 183 contract in its
    docstring, and contains no duplicated copy string literals.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand tests/dashboard/test_history_browser.py with state-matrix widget regressions</name>
  <files>tests/dashboard/test_history_browser.py</files>
  <read_first>
    - tests/dashboard/test_history_browser.py (entire existing file — 213 lines)
    - src/wanctl/dashboard/widgets/history_browser.py (compose + _fetch_and_populate — lines 121-235)
    - src/wanctl/dashboard/widgets/history_state.py (HistoryState, HISTORY_COPY)
    - .planning/phases/183-dashboard-history-contract-audit/183-dashboard-source-contract.md (F1, F2, F3, Acceptance Criteria 1, 3, 4, 6, 8, 9, 10)
    - .planning/phases/184-dashboard-history-source-surfacing/184-CONTEXT.md (D-10, D-12 state matrix)
  </read_first>
  <behavior>
    Textual-mounted widget tests. Follow the existing pattern in this file:
    asyncio.run(_test()) wrapping an `async with app.run_test(size=(120, 40))`
    block with an AsyncMock http client assigned to widget._http_client.

    Assertions MUST import HISTORY_COPY and read HistoryBrowserWidget.HANDOFF_TEXT
    directly — NO copy string duplication.

    New test class: class TestHistoryBrowserSourceContract

    Required test methods (exact names):

      test_success_state_renders_banner_detail_and_handoff
        - Mock payload: {"data": [one row], "metadata": {"source":
          {"mode": "local_configured_db", "db_paths": ["/var/lib/wanctl/metrics-att.db"]}}}
        - After await widget._fetch_and_populate("1h"):
            - query_one("#source-banner", Static).render() str contains HISTORY_COPY.BANNER_SUCCESS
            - query_one("#source-detail", Static).render() str contains HISTORY_COPY.MODE_PHRASE_LOCAL
            - query_one("#source-detail", Static).render() str contains "/var/lib/wanctl/metrics-att.db"
            - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT
            - query_one("#source-handoff", Static).render() str contains "python3 -m wanctl.history"

      test_success_state_merged_discovery_lists_db_basenames
        - Mock payload with mode="merged_discovery", db_paths=["/x/metrics-spectrum.db","/x/metrics-att.db","/x/metrics.db"]
        - query_one("#source-detail", Static).render() str contains HISTORY_COPY.MODE_PHRASE_MERGED
        - query_one("#source-detail", Static).render() str contains "3 databases"
        - query_one("#source-detail", Static).render() str contains "metrics-spectrum.db"
        - query_one("#source-detail", Static).render() str contains "metrics-att.db"
        - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT

      test_fetch_error_state_clears_table_and_shows_unavailable_banner
        - Mock client.get raises httpx.TimeoutException (or simple Exception via side_effect)
        - After _fetch_and_populate:
            - query_one("#history-table", DataTable).row_count == 0
            - query_one("#source-banner", Static).render() str contains HISTORY_COPY.BANNER_FETCH_ERROR
            - query_one("#source-detail", Static).render() str contains HISTORY_COPY.DETAIL_FETCH_ERROR
            - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT (unchanged)
            - query_one("#summary-stats", Static).render() str contains HISTORY_COPY.SUMMARY_NO_DATA

      test_source_missing_state_shows_ambiguous_banner_and_preserves_handoff
        - Mock payload: {"data": [], "metadata": {}}  (source absent)
        - query_one("#source-banner", Static).render() str contains HISTORY_COPY.BANNER_SOURCE_MISSING
        - query_one("#source-detail", Static).render() str contains HISTORY_COPY.DETAIL_AMBIGUOUS
        - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT

      test_mode_missing_state_shows_mode_ambiguous_banner
        - Mock payload: metadata.source present but mode="frankenmode", db_paths=["/a.db"]
        - query_one("#source-banner", Static).render() str contains HISTORY_COPY.BANNER_MODE_MISSING
        - query_one("#source-detail", Static).render() str contains HISTORY_COPY.DETAIL_AMBIGUOUS
        - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT

      test_db_paths_missing_state_shows_db_paths_ambiguous_banner
        - Mock payload: mode="local_configured_db", db_paths=[]
        - query_one("#source-banner", Static).render() str contains HISTORY_COPY.BANNER_DB_PATHS_MISSING
        - query_one("#source-detail", Static).render() str contains HISTORY_COPY.DETAIL_AMBIGUOUS
        - query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT

      test_handoff_text_is_verbatim_across_all_states
        - A single test method that runs all six mock payloads (success local,
          success merged, fetch_error, source_missing, mode_missing,
          db_paths_missing) sequentially against the same mounted widget,
          asserting after each that
          query_one("#source-handoff", Static).render() str == HistoryBrowserWidget.HANDOFF_TEXT
          and that HistoryBrowserWidget.HANDOFF_TEXT contains "python3 -m wanctl.history".
  </behavior>
  <action>
    Expand tests/dashboard/test_history_browser.py WITHOUT removing any
    existing tests (TestHistoryBrowserCompose, TestComputeSummary,
    TestFetchAndPopulate, TestSelectChangedTriggerseFetch all stay).

    Add a new top-level class at the bottom of the file:

      class TestHistoryBrowserSourceContract:
          """Regression coverage for Phase 183 contract sections L1, L2, S1-S3,
          H1-H3, F1, F2 via HistoryBrowserWidget state matrix.

          Traces to 183-dashboard-source-contract.md §F3 (D-12) and
          184-CONTEXT.md D-12 state matrix. Phase 185 Plan 01 — DASH-04.
          """

    Each test method MUST use the existing fixture pattern (no new helpers —
    copy the `async def _test(): ... asyncio.run(_test())` structure already
    in TestFetchAndPopulate). Each test MUST mount the widget fresh to avoid
    stale state.

    Mock pattern for success/F2 payloads: construct a dict, then
      mock_client = AsyncMock()
      mock_resp = MagicMock()
      mock_resp.raise_for_status = MagicMock()
      mock_resp.json.return_value = <payload_dict>
      mock_client.get = AsyncMock(return_value=mock_resp)
      widget._http_client = mock_client

    Mock pattern for fetch_error: set mock_client.get = AsyncMock(side_effect=Exception("fetch failed"))
    (A plain Exception is sufficient — classify_history_state only checks isinstance(Exception).)

    For the test_handoff_text_is_verbatim_across_all_states test, define a
    local list of (label, payload_or_exc) tuples and iterate them inside the
    same `async with app.run_test()` block, re-assigning widget._http_client's
    get return_value/side_effect before each _fetch_and_populate call.

    Imports at top of new tests (add if not present):
      from wanctl.dashboard.widgets.history_state import HISTORY_COPY
      # HistoryBrowserWidget already imported by existing tests

    Every render-string assertion uses `str(widget.render()) if applicable`
    or `str(static.render())` following the existing
    `rendered = str(summary.render())` pattern in test_handles_http_error_gracefully.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/dashboard/test_history_browser.py::TestHistoryBrowserSourceContract -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestHistoryBrowserSourceContract" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_success_state_renders_banner_detail_and_handoff" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_success_state_merged_discovery_lists_db_basenames" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_fetch_error_state_clears_table_and_shows_unavailable_banner" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_source_missing_state_shows_ambiguous_banner_and_preserves_handoff" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_mode_missing_state_shows_mode_ambiguous_banner" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_db_paths_missing_state_shows_db_paths_ambiguous_banner" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "def test_handoff_text_is_verbatim_across_all_states" tests/dashboard/test_history_browser.py` == 1
    - `grep -c "HistoryBrowserWidget.HANDOFF_TEXT" tests/dashboard/test_history_browser.py` >= 6 (one per state plus the matrix test)
    - `grep -c "HISTORY_COPY" tests/dashboard/test_history_browser.py` >= 6
    - `grep -c "183-dashboard-source-contract" tests/dashboard/test_history_browser.py` >= 1
    - Existing tests still present: `grep -c "class TestHistoryBrowserCompose" tests/dashboard/test_history_browser.py` == 1 AND `grep -c "class TestFetchAndPopulate" tests/dashboard/test_history_browser.py` == 1
    - `.venv/bin/pytest tests/dashboard/test_history_browser.py -q` exits 0
  </acceptance_criteria>
  <done>
    tests/dashboard/test_history_browser.py contains the new
    TestHistoryBrowserSourceContract class with all seven named test methods,
    every previous test still passes, HANDOFF_TEXT is asserted verbatim in
    every state, and the full widget test file passes under pytest.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full dashboard regression slice and confirm traceability</name>
  <files></files>
  <read_first>
    - tests/dashboard/test_history_state.py (Task 1 output)
    - tests/dashboard/test_history_browser.py (Task 2 output)
  </read_first>
  <action>
    Run the dashboard regression slice and verify all new tests pass alongside
    the pre-existing widget tests:

      .venv/bin/pytest tests/dashboard/ -q

    Then run the focused hot-path slice sanity check (must not regress):

      .venv/bin/pytest -o addopts='' tests/dashboard/test_history_state.py tests/dashboard/test_history_browser.py -q

    If either command fails, STOP and return failure with the pytest output.
    Do NOT modify the history_state classifier or HistoryBrowserWidget to make
    tests pass — the contract is locked; tests adapt to the contract, not the
    other way around.

    On success, produce a short traceability note in the Plan 01 summary file
    mapping each Phase 183 contract item to the test function that covers it:
      - Acceptance Criterion 1 (endpoint-local)       -> test_history_copy_banner_success_is_endpoint_local + test_success_state_renders_banner_detail_and_handoff
      - Acceptance Criterion 3 (mode-specific label)  -> test_success_state_renders_banner_detail_and_handoff
      - Acceptance Criterion 4 (db_paths context)     -> test_success_state_renders_banner_detail_and_handoff + test_success_state_merged_discovery_lists_db_basenames
      - Acceptance Criterion 6 (CLI handoff)          -> test_handoff_text_is_verbatim_across_all_states + test_history_copy_handoff_mentions_python3_m_wanctl_history_verbatim
      - Acceptance Criterion 7 (no parity)            -> test_history_copy_no_parity_language_in_banners
      - Acceptance Criterion 8 (fetch failure)        -> test_fetch_error_state_clears_table_and_shows_unavailable_banner
      - Acceptance Criterion 9 (source missing)       -> test_source_missing_state_shows_ambiguous_banner_and_preserves_handoff
      - Acceptance Criterion 10 (db_paths missing)    -> test_db_paths_missing_state_shows_db_paths_ambiguous_banner
      - F3 mode_missing                               -> test_mode_missing_state_shows_mode_ambiguous_banner
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/dashboard/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/dashboard/ -q` exits 0
    - pytest output reports both tests/dashboard/test_history_state.py and tests/dashboard/test_history_browser.py ran
    - No modifications made to src/wanctl/dashboard/widgets/history_state.py (verify with git diff)
    - No modifications made to src/wanctl/dashboard/widgets/history_browser.py (verify with git diff)
  </acceptance_criteria>
  <done>
    Full dashboard test slice is green, classifier and widget contract are
    unchanged, and the plan summary records which test covers each Phase 183
    acceptance criterion.
  </done>
</task>

</tasks>

<verification>
Phase verification for this plan:
  - tests/dashboard/test_history_state.py exists and passes
  - tests/dashboard/test_history_browser.py::TestHistoryBrowserSourceContract passes
  - .venv/bin/pytest tests/dashboard/ -q exits 0
  - No source file under src/wanctl/dashboard/ was modified
  - No backend file under src/wanctl/health_check.py was modified
  - HANDOFF_TEXT verbatim invariant proven across all five widget states
  - HISTORY_COPY parity-language invariant proven at the constants layer
</verification>

<success_criteria>
DASH-04 regression coverage exists in code and traces 1:1 to the Phase 183
contract's F3 regression surface areas and Acceptance Criteria 1, 3, 4, 6,
7, 8, 9, 10. No backend, storage, retention, or control-loop code changed.
No Phase 184 UI decisions reopened.
</success_criteria>

<output>
After completion, create `.planning/phases/185-verification-and-operator-alignment/185-01-SUMMARY.md`
</output>
