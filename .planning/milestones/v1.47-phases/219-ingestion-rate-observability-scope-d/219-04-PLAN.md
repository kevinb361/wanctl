---
phase: 219
plan: 04
type: execute
wave: 3
depends_on: [219-02]
files_modified:
  - scripts/phase219_ingestion_digest.py
  - docs/CONFIGURATION.md
  - tests/test_phase219_ingestion_digest.py
autonomous: false
requirements: [INGEST-05, SAFE-11]
tags: [ingestion-rate, cron, snapshot, atomic-write, retention, docs, cycle-budget]

must_haves:
  truths:
    - "scripts/phase219_ingestion_digest.py (underscore per D-19) is invokable via `python -m scripts.phase219_ingestion_digest` AND direct invocation; cron-compatible"
    - "Atomic write: payload lands at /var/lib/wanctl/snapshots/ingestion/<unix_ts>.json via REUSED `wanctl.state_utils.atomic_write_json` (D-21; collision-safe mkstemp-based temp file)"
    - "Subprocess stdout is JSON-validated (json.loads) BEFORE atomic write (D-23); malformed payload -> stderr log + return 1, no garbage file"
    - "Count-based retention keeps the 288 most recent files; no per-cron age-based stat sweep (flash-wear-safe)"
    - "Subprocess to wanctl-history is bounded (timeout=30) and failure-tolerant: TimeoutExpired and CalledProcessError caught; script returns 1, never raises (cron-safe)"
    - "docs/CONFIGURATION.md carries a Phase 219 paragraph documenting _snapshot_unix / _snapshot_age_sec staleness semantics matching the v1.38 measurement_stale precedent"
    - "Tests pinned at tests/test_phase219_ingestion_digest.py turn green (xfail markers removed)"
    - "Post-deploy production cycle budget verified per Phase 217 baseline (D-27; Task 4 checkpoint)"
  artifacts:
    - path: scripts/phase219_ingestion_digest.py
      provides: "Cron-callable snapshot writer; calls wanctl-history --ingestion-rate --by-table --rolling=60,300,3600 --json and persists output atomically via wanctl.state_utils.atomic_write_json"
      contains: "MAX_SNAPSHOTS_DEFAULT = 288"
      min_lines: 80
    - path: docs/CONFIGURATION.md
      provides: "Phase 219 staleness-semantics paragraph + cron stanza documentation"
      contains: "_snapshot_unix"
    - path: tests/test_phase219_ingestion_digest.py
      provides: "Atomic write + retention + failure tolerance tests (xfail flipped to green); deterministic subprocess failure test via cwd-isolated env (M9)"
      contains: "MAX_SNAPSHOTS_DEFAULT = 288"
  key_links:
    - from: scripts/phase219_ingestion_digest.py
      to: wanctl.history --ingestion-rate --by-table --rolling=60,300,3600 --json
      via: "subprocess.run([sys.executable, '-m', 'wanctl.history', ...], capture_output=True, text=True, timeout=30, check=True)"
      pattern: "subprocess\\.run"
    - from: scripts/phase219_ingestion_digest.py
      to: wanctl.state_utils.atomic_write_json
      via: "import + call; collision-safe temp file via mkstemp (D-21)"
      pattern: "from wanctl\\.state_utils import atomic_write_json"
    - from: scripts/phase219_ingestion_digest.py
      to: /var/lib/wanctl/snapshots/ingestion/<unix_ts>.json
      via: "atomic_write_json(target, payload_dict)"
      pattern: "atomic_write_json\\("
---

<objective>
Ship the cron-callable snapshot primitive that persists `wanctl-history --ingestion-rate --by-table
--rolling=60,300,3600 --json` to disk so Phase 218 audits have an evidence trail even when they fire mid-event.
This is the final Phase 219 surface and the file the operator wires into crontab.

Purpose: Satisfy INGEST-05 (cron-callable, count-based retention, atomic write). Honor flash-wear posture by
avoiding per-cron mtime stat sweeps. Document the staleness contract in docs/CONFIGURATION.md so operators
understand what `_snapshot_age_sec` means when reading a stored file later. **Per D-27 (resolves Codex H6),
Task 4 closes ROADMAP success criterion #5 with explicit production cycle-budget verification — this is the
reason plan 04 is `autonomous: false`.**

Output: A ~120 line stdlib+wanctl.state_utils-only script + docs paragraph + the previously-xfailed tests turn
green + post-deploy cycle-budget evidence recorded in the phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md
@src/wanctl/check_cake_fix.py
@src/wanctl/state_utils.py
@docs/CONFIGURATION.md
@docs/PROFILING.md
@CLAUDE.md

<interfaces>
<!-- Extracted from RESEARCH §C, §"Cron Script Pattern", and reference analogs + D-19, D-21, D-23, D-27 -->

Analog 1 — retention pattern (src/wanctl/check_cake_fix.py:61-76):
  def _prune_snapshots(wan_name: str) -> None:
      if not SNAPSHOT_DIR.exists():
          return
      files = sorted(SNAPSHOT_DIR.glob(f"*_{wan_name}.json"))
      while len(files) > MAX_SNAPSHOTS:
          oldest = files.pop(0)
          oldest.unlink()
# Phase 219 clones this; filename pattern is `<unix_ts>.json` (lexicographic = chronological); no per-WAN suffix.

Analog 2 — atomic write (D-21: REUSED, not inlined):
  from wanctl.state_utils import atomic_write_json
  # Signature: atomic_write_json(file_path: Path, data: dict[str, Any]) -> None
  # Implementation at src/wanctl/state_utils.py:24-70 uses tempfile.mkstemp(suffix=".tmp", prefix=name+".", dir=parent)
  # → collision-safe for concurrent same-second writers (resolves Codex H4).
  # Note: helper takes a DICT, not a string. We must json.loads the subprocess stdout first (D-23 composes here).
  # Note: helper writes compact JSON (no indent). That is fine for snapshot evidence — readers parse, not eyeball.

Locked constants (CONTEXT.md §Snapshot persistence):
  SNAPSHOT_DIR_DEFAULT = Path("/var/lib/wanctl/snapshots/ingestion")
  MAX_SNAPSHOTS_DEFAULT = 288
  WANCTL_HISTORY_ARGV = [
      sys.executable, "-m", "wanctl.history",
      "--ingestion-rate", "--by-table", "--rolling=60,300,3600", "--json",
  ]

Script CLI flags (for testability):
  --snapshot-dir PATH    # default: SNAPSHOT_DIR_DEFAULT; tests override to tmp_path
  --max-snapshots N      # default: 288; tests override to small N
  --payload-from-stdin   # test affordance: skip subprocess, read JSON from stdin
  --inject-timestamp N   # test affordance: filename = N.json (deterministic retention tests)
# NB: NO --window-sec flag. Rolling windows 60/300/3600 are CONTEXT-locked.

Subprocess failure handling (RESEARCH R5):
  try:
      result = subprocess.run(argv, capture_output=True, text=True, timeout=30, check=True)
  except subprocess.TimeoutExpired as exc: ...; return 1
  except subprocess.CalledProcessError as exc: ...; return 1
# NEVER let an exception bubble out of main() — cron must exit cleanly.

JSON validation (D-23):
  try:
      payload_dict = json.loads(payload)
  except json.JSONDecodeError as exc:
      print(f"phase219-ingestion-digest: malformed JSON from wanctl-history: {exc}", file=sys.stderr)
      return 1
# Then atomic_write_json(target, payload_dict). Composes with D-21.

Cron stanza (documentation only):
  */5 * * * * wanctl /opt/wanctl/.venv/bin/python -m scripts.phase219_ingestion_digest >> /var/log/wanctl/ingestion-digest.log 2>&1
# `python -m scripts.phase219_ingestion_digest` works because D-19 ensures the filename is a valid identifier.

Cycle-budget reference (D-27):
  scripts/profiling_collector_json.py — Phase 217 baseline collector
  scripts/analyze_profiling.py — Phase 217 analyzer; gates `cycle_total.avg_ms ≤ 3.0` AND `p99_ms ≤ 7.5`
  docs/PROFILING.md — operator procedure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/phase219_ingestion_digest.py (cron snapshot writer; D-19 underscore + D-21 atomic_write_json import + D-23 JSON validate)</name>
  <files>scripts/phase219_ingestion_digest.py</files>
  <read_first>
    - src/wanctl/check_cake_fix.py (lines 1-100; lift the _prune_snapshots pattern at :61-76 verbatim — adjust max from 20 to 288 and remove the per-WAN suffix)
    - src/wanctl/state_utils.py (lines 24-70; the atomic_write_json signature this script IMPORTS per D-21)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §C "scripts/phase219_ingestion_digest.py (new)" (the structural skeleton table at lines 196-208) AND §"Cron Script Pattern" (lines 596-633)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Snapshot persistence" + §"Post-Review Amendments" D-19, D-21, D-23
    - tests/test_phase219_ingestion_digest.py (from plan 01; the xfailed contract this script must satisfy)
    - scripts/ directory: `ls scripts/phase21*.py | head -5` then read the first one to lift docstring/argparse style
  </read_first>
  <behavior>
    - `main()` returns int (0 success, 1 failure); NEVER raises
    - SNAPSHOT_DIR is created with mode=0o755 if missing; PermissionError logged + write attempted anyway
    - Single subprocess call to wanctl-history with bounded timeout=30 and check=True; both TimeoutExpired and CalledProcessError caught -> return 1
    - **D-23: subprocess stdout is parsed via `json.loads(payload)` BEFORE the write call. JSONDecodeError -> stderr log + return 1.** Prevents audit-evidence garbage from malformed wanctl-history output.
    - **D-21: atomic write uses `wanctl.state_utils.atomic_write_json(target, payload_dict)`** — NOT a hand-rolled `<target>.tmp` + os.replace. The helper uses `tempfile.mkstemp(suffix=".tmp", prefix=name+".", dir=parent)` which is collision-safe for concurrent same-second writers. Hand-rolling is forbidden by D-21.
    - Retention: sorted glob of *.json; pop oldest until len <= max_snapshots; lexicographic sort = chronological because filenames are <unix_ts>.json. Skip *.tmp files in the glob so an in-flight write from a concurrent process is never unlinked mid-rename. (atomic_write_json's mkstemp temp files use a prefix that won't match `*.json`, so this is doubly safe.)
    - Argparse: --snapshot-dir, --max-snapshots, --payload-from-stdin, --inject-timestamp
    - When --inject-timestamp N is set, the filename uses N instead of int(time.time())
    - No --window-sec flag; rolling windows 60/300/3600 are CONTEXT-locked
    - sys.exit(main()) at __name__=="__main__"
  </behavior>
  <action>
    Create `scripts/phase219_ingestion_digest.py` (underscore per D-19 — `python -m scripts.phase219_ingestion_digest` works). Stdlib + ONE wanctl import:
    ```
    import argparse, json, os, subprocess, sys, time
    from pathlib import Path
    from wanctl.state_utils import atomic_write_json   # D-21
    ```

    Module docstring lifted in style from `scripts/phase21*.py` (match conventions). One-line summary:
    "Phase 219 ingestion-rate snapshot writer (cron-callable; D-21 atomic write via wanctl.state_utils)."

    Module constants:
      `SNAPSHOT_DIR_DEFAULT = Path("/var/lib/wanctl/snapshots/ingestion")`
      `MAX_SNAPSHOTS_DEFAULT = 288`
      `SUBPROCESS_TIMEOUT_SEC = 30`

    Helper functions (top-level, all private with leading underscore):

      `_ensure_dir(snapshot_dir: Path) -> None`:
        `snapshot_dir.mkdir(parents=True, exist_ok=True, mode=0o755)`. Wrap in try/except PermissionError; on PermissionError log warning to stderr and return (caller will attempt write; the write may also fail, that's fine — return 1 path covers it).

      `_call_history() -> str`:
        Build argv: `[sys.executable, "-m", "wanctl.history", "--ingestion-rate", "--by-table", "--rolling=60,300,3600", "--json"]`. Rolling windows are CONTEXT-locked.
        `result = subprocess.run(argv, capture_output=True, text=True, timeout=SUBPROCESS_TIMEOUT_SEC, check=True)`. Return `result.stdout`. Let TimeoutExpired and CalledProcessError propagate — main() catches them.

      `_prune_old(snapshot_dir: Path, max_files: int) -> None`:
        `files = sorted(snapshot_dir.glob("*.json"))`
        `while len(files) > max_files:`
            `oldest = files.pop(0)`
            `oldest.unlink()`
        # NB: glob pattern is `*.json` only — `atomic_write_json`'s temp files use a different suffix (.tmp / mkstemp-generated) and are not matched.

      `_build_parser() -> argparse.ArgumentParser`:
        Add `--snapshot-dir` (type=Path, default=SNAPSHOT_DIR_DEFAULT)
        Add `--max-snapshots` (type=int, default=MAX_SNAPSHOTS_DEFAULT)
        Add `--payload-from-stdin` (action="store_true", help="Test affordance: read JSON payload from stdin instead of forking wanctl-history")
        Add `--inject-timestamp` (type=int, default=None, help="Test affordance: use this integer as the snapshot unix-ts (filename = <N>.json)")
        Total of FOUR flags. NO `--window-sec`.

      `main(argv: list[str] | None = None) -> int`:
        Parse args. Compute `snapshot_ts = args.inject_timestamp if args.inject_timestamp is not None else int(time.time())`. Call `_ensure_dir(args.snapshot_dir)`.

        Acquire payload:
        ```
        if args.payload_from_stdin:
            payload = sys.stdin.read()
        else:
            try:
                payload = _call_history()
            except subprocess.TimeoutExpired as exc:
                print(f"phase219-ingestion-digest: TIMEOUT after {exc.timeout}s", file=sys.stderr)
                return 1
            except subprocess.CalledProcessError as exc:
                print(f"phase219-ingestion-digest: wanctl-history failed rc={exc.returncode}: {exc.stderr}", file=sys.stderr)
                return 1
            except OSError as exc:
                print(f"phase219-ingestion-digest: subprocess OSError: {exc}", file=sys.stderr)
                return 1
        ```

        **D-23 JSON validation before write:**
        ```
        try:
            payload_dict = json.loads(payload)
        except json.JSONDecodeError as exc:
            print(f"phase219-ingestion-digest: malformed JSON from wanctl-history: {exc}", file=sys.stderr)
            return 1
        ```

        **D-21 atomic write:**
        ```
        target = args.snapshot_dir / f"{snapshot_ts}.json"
        try:
            atomic_write_json(target, payload_dict)
        except OSError as exc:
            print(f"phase219-ingestion-digest: atomic write failed: {exc}", file=sys.stderr)
            return 1
        ```
        (Note: atomic_write_json writes with mode 0o600 by default; this is acceptable for snapshot evidence. If broader read access is required by an operator process, document the chmod adjustment in plan 04 SUMMARY rather than overriding here. The D-21 win is collision-safety, not perm policy.)

        Retention:
        ```
        try:
            _prune_old(args.snapshot_dir, args.max_snapshots)
        except OSError as exc:
            print(f"phase219-ingestion-digest: retention sweep warning: {exc}", file=sys.stderr)
            return 0  # The snapshot landed successfully; retention is best-effort.
        ```
        Return 0.

    Final block:
      `if __name__ == "__main__": sys.exit(main())`

    Add a `# resolves_phase: 219` comment near the top.

    Make the file `chmod +x` after writing.

    **Boundary note (D-21 caveat):** This file imports from `wanctl.state_utils`. RESEARCH §F prefers no scripts/→src/wanctl imports, but D-21 relaxes the rule for this single utility import because the safety win (collision-safe temp files) is decisive and `atomic_write_json` is a thin utility, not controller logic. Document this in the phase SUMMARY.
  </action>
  <verify>
    <automated>echo '{"schema_version":1,"rows":[]}' | .venv/bin/python scripts/phase219_ingestion_digest.py --snapshot-dir /tmp/p219-smoke --max-snapshots 2 --payload-from-stdin &amp;&amp; ls /tmp/p219-smoke/*.json | wc -l &amp;&amp; .venv/bin/ruff check scripts/phase219_ingestion_digest.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase219_ingestion_digest.py` exists (D-19 underscore form) and is executable
    - `test ! -e scripts/phase219-ingestion-digest.py` (no hyphenated stub left behind)
    - **D-19 module-invokable:** `.venv/bin/python -m scripts.phase219_ingestion_digest --help` exits 0 (would fail with `SyntaxError` if filename had a hyphen)
    - `grep -c "MAX_SNAPSHOTS_DEFAULT = 288" scripts/phase219_ingestion_digest.py` returns 1
    - **D-21 import:** `grep -c "from wanctl.state_utils import atomic_write_json" scripts/phase219_ingestion_digest.py` returns 1
    - **D-21 no hand-rolled atomic write:** `grep -c "os.replace" scripts/phase219_ingestion_digest.py` returns 0 (use of os.replace forbidden — the helper handles it); `grep -cE '\\.tmp"' scripts/phase219_ingestion_digest.py` returns 0 (no hand-rolled `<target>.tmp` filenames)
    - **D-21 helper call:** `grep -c "atomic_write_json(" scripts/phase219_ingestion_digest.py` returns at least 1
    - **D-23 JSON validate before write:** the json.loads call appears BEFORE the atomic_write_json call (textual ordering): `awk '/^def main/,/^if __name__/' scripts/phase219_ingestion_digest.py | grep -n -E "json\\.loads\\(|atomic_write_json\\(" | head -2` shows `json.loads` at a lower line number than `atomic_write_json`
    - **D-23 error path:** `grep -c "json.JSONDecodeError" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c "subprocess.TimeoutExpired" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c "subprocess.CalledProcessError" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c "0o755" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c "resolves_phase: 219" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c -- "--inject-timestamp" scripts/phase219_ingestion_digest.py` returns at least 1
    - `grep -c -- "--window-sec" scripts/phase219_ingestion_digest.py` returns 0 (no operator-time override)
    - The inline smoke command creates exactly 1 file in /tmp/p219-smoke/ and `ruff check` exits 0
    - `.venv/bin/mypy scripts/phase219_ingestion_digest.py` exits 0 (or matches an explicit baseline)
    - `.venv/bin/python scripts/phase219_ingestion_digest.py --help` exits 0 and prints exactly four flags: `--snapshot-dir`, `--max-snapshots`, `--payload-from-stdin`, `--inject-timestamp`
  </acceptance_criteria>
  <done>Script lands at the underscore path; smoke produces one atomic file via the imported helper; D-23 JSON validation gate active; all subprocess failure paths return 1 without raising; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip tests/test_phase219_ingestion_digest.py from xfail to green (M9 cwd-isolated failure test)</name>
  <files>tests/test_phase219_ingestion_digest.py</files>
  <read_first>
    - tests/test_phase219_ingestion_digest.py (from plan 01; the existing xfailed scaffolds)
    - scripts/phase219_ingestion_digest.py (just-shipped from Task 1; confirm helper names and the --payload-from-stdin / --inject-timestamp affordances)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Validation Architecture" (lines 654-687; INGEST-05 test command map)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Post-Review Amendments" D-19, D-21, D-23 (the contracts the tests pin)
  </read_first>
  <behavior>
    - All previously-xfailed tests turn green
    - Tests drive the script via `subprocess.run([sys.executable, "scripts/phase219_ingestion_digest.py", ...], input='{"schema_version":1,"rows":[]}', text=True)` with --payload-from-stdin so no real wanctl-history fork is needed
    - Retention test invokes 5 times with --inject-timestamp; assert final glob count == max_snapshots
    - **M9: subprocess failure test runs with `cwd=tmp_path` (away from the repo root, so the `src/wanctl` shadow can't pollute sys.path) AND env containing only `PATH=os.defpath` + a `PYTHONPATH=/nonexistent` overlay.** This makes the "wanctl.history import fails" assertion deterministic.
  </behavior>
  <action>
    Update `tests/test_phase219_ingestion_digest.py` (from plan 01). Remove `@pytest.mark.xfail(...)` markers from
    all five test methods + module-skip if applicable. Update the implementation to drive the script via `subprocess.run`:

      Helper at module top: `SCRIPT = Path(__file__).resolve().parent.parent / "scripts" / "phase219_ingestion_digest.py"` (D-19 underscore).

      Method 1 `test_atomic_rename_writes_exactly_one_file(self, tmp_path)`:
        Run script with `--snapshot-dir tmp_path --max-snapshots 10 --payload-from-stdin`, stdin=`'{"schema_version":1,"rows":[]}'`. Assert exit code 0. Assert `len(list(tmp_path.glob("*.json"))) == 1`. Assert `len(list(tmp_path.glob("*.tmp"))) == 0` AND `len(list(tmp_path.glob(".*"))) == 0` (no mkstemp residue from atomic_write_json). Assert file content round-trips as JSON with `schema_version == 1`.

      Method 2 `test_retention_keeps_max_files(self, tmp_path)`:
        Invoke the script 5 times sequentially with `--max-snapshots 3 --payload-from-stdin --inject-timestamp <N>` (N = 1000, 1001, 1002, 1003, 1004). Assert final `len(list(tmp_path.glob("*.json"))) == 3`. Assert the 3 retained filenames are `1002.json`, `1003.json`, `1004.json`.

      Method 3 `test_subprocess_failure_returns_1_does_not_raise(self, tmp_path)` (**M9 deterministic env**):
        Run the script WITHOUT `--payload-from-stdin`. Set `cwd=tmp_path` (away from repo root) and `env={"PATH": os.defpath, "PYTHONPATH": "/nonexistent"}` so `python -m wanctl.history` import fails reliably (no shadow `src/wanctl` reachable via cwd). Use the script's absolute path for the subprocess argv. Assert exit code is non-zero. Assert `len(list(tmp_path.glob("*.tmp"))) == 0`. Assert `len(list(tmp_path.glob("*.json"))) == 0`.
        Implementation hint:
        ```
        result = subprocess.run(
            [sys.executable, str(SCRIPT), "--snapshot-dir", str(tmp_path)],
            cwd=str(tmp_path),
            env={"PATH": os.defpath, "PYTHONPATH": "/nonexistent"},
            capture_output=True, text=True, timeout=15,
        )
        assert result.returncode != 0
        ```

      Method 4 `test_snapshot_dir_created_with_mode_0o755(self, tmp_path)`:
        Run script with `--snapshot-dir tmp_path/nested/ingestion --payload-from-stdin`, stdin=valid JSON. Assert exit 0. Assert `(tmp_path / "nested" / "ingestion").stat().st_mode & 0o777 == 0o755`.

      Method 5 `test_default_max_snapshots_is_288(self)`:
        Import the script module via `importlib.util.spec_from_file_location("phase219_digest", SCRIPT)`; assert `phase219_digest.MAX_SNAPSHOTS_DEFAULT == 288`.

      Method 6 `test_malformed_json_payload_returns_1_no_file_written(self, tmp_path)` (**NEW D-23 pin**):
        Run script with `--snapshot-dir tmp_path --payload-from-stdin`, stdin=`'this is not json'`. Assert exit code 1. Assert stderr contains "malformed JSON" (or the actual message from Task 1's D-23 error path). Assert `len(list(tmp_path.glob("*.json"))) == 0` (no garbage written). This is the regression test for D-23 — if a future refactor removes the json.loads validation, this test fails LOUDLY.

    Update the module-level `MAX_SNAPSHOTS_DEFAULT` constant to mirror the script's value (still 288).

    The `--inject-timestamp` flag used by method 2 is landed in Task 1's argparse (single-task discipline preserved — Task 2 only consumes it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase219_ingestion_digest.py -v -x 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase219_ingestion_digest.py -x` exits 0 with 6 passed (zero xfailed)
    - `grep -c "xfail" tests/test_phase219_ingestion_digest.py` returns 0 (all markers removed)
    - `.venv/bin/ruff check tests/test_phase219_ingestion_digest.py scripts/phase219_ingestion_digest.py` exits 0
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0 (no controller-path diff)
    - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0 (hot-path slice)
    - **M9 cwd-isolated env:** `grep -c "cwd=str(tmp_path)\\|cwd=tmp_path" tests/test_phase219_ingestion_digest.py` returns at least 1 AND `grep -c "PATH.*os.defpath" tests/test_phase219_ingestion_digest.py` returns at least 1
    - **D-23 malformed-JSON test exists:** `grep -c "test_malformed_json_payload_returns_1_no_file_written" tests/test_phase219_ingestion_digest.py` returns 1
    - `grep -c "phase219_ingestion_digest" tests/test_phase219_ingestion_digest.py` returns at least 1 (D-19 underscore path); `grep -c "phase219-ingestion-digest" tests/test_phase219_ingestion_digest.py` returns 0
  </acceptance_criteria>
  <done>6 cron-script tests green, no xfail residue, mutation-boundary green, hot-path slice green, M9 cwd-isolated subprocess test deterministic, D-23 malformed-JSON regression test in place.</done>
</task>

<task type="auto">
  <name>Task 3: Document staleness semantics + cron stanza in docs/CONFIGURATION.md</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>
    - docs/CONFIGURATION.md (entire file; find the v1.38 measurement_stale / measurement_staleness_sec section)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"JSON output shape" and §"Snapshot persistence" + §"Post-Review Amendments" D-17, D-19, D-21
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Cron stanza (documentation only, NOT installed)" (lines 211-213; the verbatim stanza to embed — update to underscore form per D-19)
  </read_first>
  <action>
    Add a new section to `docs/CONFIGURATION.md` titled `### Ingestion-Rate Snapshot Staleness (Phase 219)` placed
    immediately after the existing `measurement_stale` / `measurement_staleness_sec` section (find via `grep -n "measurement_stale" docs/CONFIGURATION.md`). CLI-tool-description only — no threshold-tuning language, no future-tuning prediction, no recommended-value tables (SAFE-11 enforcement).

    Required content (~25-40 lines):

      1. One paragraph describing the `--ingestion-rate --by-table` and `--ingestion-rate --rolling` envelope: `{schema_version: 1, rows: [...]}` with `_snapshot_unix` and `_snapshot_age_sec` on every row. **Clarify per D-17 that the default mode (`--ingestion-rate` alone with neither flag) preserves the v1.44 envelope unchanged** — operators with existing scripts consuming the legacy `{window, totals, wans}` shape see no behavior change. Explain that `_snapshot_unix` is captured once per invocation; `_snapshot_age_sec = now - _snapshot_unix` at emit. For direct CLI use the age is ~0; the field becomes meaningful when reading a persisted snapshot file later. Include the RESEARCH R1 disambiguation sentence verbatim: **"`table_name` in the JSON envelope is the `metric_name` of the row in the metrics SQL table."**

      2. One paragraph describing the snapshot writer (`scripts/phase219_ingestion_digest.py`): persistence path `/var/lib/wanctl/snapshots/ingestion/<unix_ts>.json`, atomic write via reused `wanctl.state_utils.atomic_write_json` (per D-21; collision-safe under concurrent writers), count-based retention (288 most recent files, ~24h at 5-min cadence, ~3MB ceiling). Note this is cron-orchestrated (not systemd) per INGEST-05. **Mention that subprocess output is JSON-validated before disk write (D-23) — malformed payloads are logged to stderr and the script returns 1 without writing a partial or garbage file.**

      3. One literal cron stanza block (fenced ```cron) using the underscore filename per D-19:
         ```cron
         */5 * * * * wanctl /opt/wanctl/.venv/bin/python -m scripts.phase219_ingestion_digest >> /var/log/wanctl/ingestion-digest.log 2>&1
         ```
         Followed by one sentence noting that the snapshot directory must exist with the correct ownership before the first cron tick: `sudo install -d -m 0755 -o wanctl -g wanctl /var/lib/wanctl/snapshots/ingestion`.

      4. One paragraph cross-referencing the v1.38 `measurement_stale` precedent: "The staleness contract here mirrors the v1.38 `measurement_stale` / `measurement_staleness_sec` pattern — readers compute age relative to a captured timestamp rather than rely on file mtime."

    DO NOT add any text suggesting threshold values, retention tuning, or "consider increasing/decreasing" language. The Phase 219 mutation-boundary test scans this file for forbidden tuning tokens via `FORBIDDEN_MUTATION_RE` — keep the section descriptive only.
  </action>
  <verify>
    <automated>grep -c "_snapshot_unix" docs/CONFIGURATION.md &amp;&amp; grep -c "phase219_ingestion_digest" docs/CONFIGURATION.md &amp;&amp; grep -c "schema_version" docs/CONFIGURATION.md &amp;&amp; .venv/bin/pytest tests/test_phase219_mutation_boundary.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_snapshot_unix" docs/CONFIGURATION.md` returns at least 1
    - `grep -c "_snapshot_age_sec" docs/CONFIGURATION.md` returns at least 1
    - `grep -c "phase219_ingestion_digest" docs/CONFIGURATION.md` returns at least 1 (D-19 underscore)
    - `grep -c "phase219-ingestion-digest" docs/CONFIGURATION.md` returns 0 (no hyphenated leak)
    - `grep -c "schema_version" docs/CONFIGURATION.md` returns at least 1
    - `grep -cE "(consider increasing|consider decreasing|tune|tuning|recommended value|optimal value)" docs/CONFIGURATION.md` returns 0 for the Phase 219 section specifically (run via `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -cE "consider increasing|consider decreasing|tune|tuning|recommended value|optimal value"` returns 0)
    - R1 disambiguation present: `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -c "metric_name"` returns at least 1 AND `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -c "table_name in the JSON envelope is the"` returns at least 1
    - **D-17 back-compat note:** `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -cE "(default mode|legacy envelope|unchanged|v1\\.44)"` returns at least 1
    - **D-23 validation note:** `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -cE "(JSON-validated|malformed|json\\.loads)"` returns at least 1
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0 (FORBIDDEN_MUTATION_RE scan finds no tuning tokens)
    - `grep -c "measurement_stale" docs/CONFIGURATION.md` returns at least 1 (the cross-reference paragraph)
    - Section heading exists: `grep -c "### Ingestion-Rate Snapshot Staleness" docs/CONFIGURATION.md` returns 1
  </acceptance_criteria>
  <done>Docs paragraph added, cross-references v1.38 precedent + D-17 back-compat + D-21 atomic_write_json reuse + D-23 JSON validation, no tuning language, mutation-boundary docs test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>.planning/phases/219-ingestion-rate-observability-scope-d/219-04-SUMMARY.md</files>
  <action>See <task type="checkpoint:human-verify" gate="blocking">lt;how-to-verify<task type="checkpoint:human-verify" gate="blocking">gt; below; this is a checkpoint task — Claude pauses, operator runs the listed commands on production, then records evidence per the steps in <task type="checkpoint:human-verify" gate="blocking">lt;how-to-verify<task type="checkpoint:human-verify" gate="blocking">gt; before approving resume.</action>
  <name>Task 4: Post-deploy production cycle-budget verification (D-27 — resolves Codex H6)</name>
  <what-built>
    Phase 219 is otherwise complete: `wanctl-history --ingestion-rate --by-table/--rolling` ships, `wanctl-operator-summary --digest` emits the ingestion block, `scripts/phase219_ingestion_digest.py` is cron-callable, and `docs/CONFIGURATION.md` documents the staleness contract. The ONE remaining gate is ROADMAP success criterion #5: post-deploy production observation must confirm the 50ms control loop's cycle budget is unchanged within Phase 217 baseline tolerance.

    Phase 217 baseline thresholds inherited by this gate:
    - `cycle_total.avg_ms ≤ 3.0`
    - `cycle_total.p99_ms ≤ 7.5`

    No Phase 219 code touches the controller hot path (SAFE-11 enforces this at the test layer), so the expectation is "no measurable cycle-budget change." This checkpoint exists to make that expectation an explicit, auditable artifact rather than a narrative assumption.
  </what-built>
  <how-to-verify>
    Operator runs these steps on the deployed production host AFTER plans 02 + 03 + 04 (Tasks 1–3) have landed and at least one cron tick has fired (so any subprocess overhead from `phase219_ingestion_digest.py` is observable):

    1. On the production host: capture ≥1 hour of profiling data:
       ```
       ssh <prod_host> 'sudo /opt/wanctl/.venv/bin/python /opt/wanctl/scripts/profiling_collector_json.py --duration 3600 --out /var/lib/wanctl/profiling/phase219-postdeploy-<unix_ts>.json'
       ```
       (See `docs/PROFILING.md` for the canonical collector procedure if the flag set has evolved.)

    2. Analyze the capture:
       ```
       /opt/wanctl/.venv/bin/python /opt/wanctl/scripts/analyze_profiling.py /var/lib/wanctl/profiling/phase219-postdeploy-<unix_ts>.json
       ```
       Verify the analyzer's gate output includes:
       - `cycle_total.avg_ms`: actual value ≤ 3.0
       - `cycle_total.p99_ms`: actual value ≤ 7.5

    3. Compare with the Phase 217 baseline capture (path documented in `.planning/milestones/v1.46-phases/217-production-cycle-budget-baseline/`). The Phase 219 numbers should be within 5% drift of the baseline; larger drift requires investigation (likely unrelated to Phase 219 since SAFE-11 forbids controller-path edits, but flag it anyway).

    4. Record evidence in `.planning/phases/219-ingestion-rate-observability-scope-d/219-04-SUMMARY.md` under a new section `## Production Verification (D-27)`:
       - Profiling capture path on production host (full path)
       - Capture start/end timestamps (unix + ISO)
       - `cycle_total.avg_ms` actual value
       - `cycle_total.p99_ms` actual value
       - Phase 217 baseline reference (capture path + values)
       - Drift % vs baseline
       - Pass/fail verdict against gates 3.0 / 7.5
       - One sentence noting whether the cron-script `phase219_ingestion_digest.py` was running during the capture window (it should be — confirms the snapshot writer doesn't perturb the hot path even via subprocess overhead, which it shouldn't since the script runs out-of-band)

    5. If either gate fails: the failing numbers + the suspected cause go into a new gap-closure task (likely `/gsd-plan-phase 219 --gaps`). Phase 219 cannot be marked complete until gates pass.
  </how-to-verify>
  <resume-signal>
    Type `approved` after the `## Production Verification (D-27)` section is committed to `219-04-SUMMARY.md` with values that pass both gates. Type `regression` if either gate fails — that opens a gap-closure plan.
  </resume-signal>
  <verify>Operator-driven; Task 4 is satisfied when 219-04-SUMMARY.md contains a `## Production Verification (D-27)` section with both gates passing.</verify>
  <done>Production cycle-budget evidence recorded in 219-04-SUMMARY.md; `cycle_total.avg_ms ≤ 3.0` AND `cycle_total.p99_ms ≤ 7.5`; operator typed `approved`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron -> phase219_ingestion_digest.py | Cron invokes script with fixed argv; no operator argv at runtime |
| subprocess argv -> python -m wanctl.history | Argv is LIST-form (NEVER shell=True); no user-controlled string interpolation per T-219-03 |
| subprocess stdout -> json.loads | D-23 validation gate: malformed payload caught + return 1 BEFORE atomic write |
| atomic_write_json -> filesystem | D-21 helper uses mkstemp (collision-safe); writes mode 0o600 by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-01 | Information Disclosure | /var/lib/wanctl/snapshots/ingestion/ | mitigate | mkdir(mode=0o755); atomic_write_json writes mode 0o600 (helper default; tighter than original plan's 0o644 — acceptable since wanctl-process readers handle parsing); ingestion-rate JSON contains no PII or secrets — only metric_name + row counts |
| T-219-03 | Injection | subprocess invocation of wanctl-history | mitigate | argv passed as LIST: `[sys.executable, "-m", "wanctl.history", ...]`; shell=False (default); no f-string interpolation of user input into argv |
| T-219-08 | Denial of Service | unbounded subprocess hang | mitigate | timeout=30s on subprocess.run; TimeoutExpired caught + return 1; flash-wear retention bounded at 288 files |
| T-219-09 | Tampering | concurrent write race | mitigate | D-21: reused `wanctl.state_utils.atomic_write_json` uses `tempfile.mkstemp(suffix=".tmp", prefix=name+".", dir=parent)` for unique temp filenames + `os.replace` = POSIX atomic. Concurrent same-second writers never collide (resolves Codex H4 anti-pattern of hand-rolling `<target>.tmp`). |
| T-219-12 | Integrity | malformed subprocess stdout written as audit evidence | mitigate | D-23: json.loads(payload) validates structure BEFORE atomic_write_json; JSONDecodeError -> stderr + return 1 + no file written. Test 6 in tests/test_phase219_ingestion_digest.py is the regression guard. |
| T-219-13 | Performance regression | cron subprocess perturbs hot path | mitigate | D-27: Task 4 checkpoint captures ≥1h post-deploy profiling; gates `cycle_total.avg_ms ≤ 3.0` and `p99_ms ≤ 7.5`. Script runs out-of-band (cron, not control thread), so impact should be unmeasurable — checkpoint makes that explicit. |
</threat_model>

<verification>
After all four tasks (Tasks 1–3 automated; Task 4 checkpoint):

```bash
.venv/bin/pytest tests/test_phase219_ingestion_digest.py -x
.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x
.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
.venv/bin/ruff check scripts/phase219_ingestion_digest.py tests/test_phase219_ingestion_digest.py
.venv/bin/python scripts/phase219_ingestion_digest.py --help | grep -E -- "--snapshot-dir|--max-snapshots|--payload-from-stdin|--inject-timestamp"
.venv/bin/python scripts/phase219_ingestion_digest.py --help 2>&1 | grep -c -- "--window-sec"  # must be 0
.venv/bin/python -m scripts.phase219_ingestion_digest --help 2>&1 | grep -q "snapshot"  # D-19 module-invokable proof
```

All eight MUST succeed (penultimate finds the four real flags; the trailing `--window-sec` grep must return 0; the final command exits 0 and emits help text via the `-m` invocation path).

Full Phase 219 regression slice:

```bash
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py tests/test_phase219_ingestion_digest.py tests/test_phase219_mutation_boundary.py tests/test_history_cli.py -x
```

MUST exit 0 — confirms all Phase 219 tests green AND v1.44 envelope preservation (D-17) holds (no TestIngestionRateCli edits).

Task 4 verification artifact: `## Production Verification (D-27)` section present in `219-04-SUMMARY.md` with passing gate values.
</verification>

<success_criteria>
- INGEST-05 satisfied: cron-callable script exists at the D-19 underscore path; atomic write via D-21 helper; count-based retention=288; failure-tolerant; D-23 JSON-validates subprocess output
- docs/CONFIGURATION.md carries the Phase 219 staleness-semantics paragraph + cron stanza (underscore form) + back-compat note + D-23 mention
- All Phase 219 automated tests green (bucketed + cron-script + mutation-boundary)
- v1.44 envelope-pinning tests still green via D-17 preservation (no regression)
- Hot-path regression slice still green
- Mutation-boundary green: scripts/phase219_* and docs/CONFIGURATION.md edits allowlisted; no controller-path diff
- SAFE-11 docs scan finds no threshold-tuning tokens in the new section
- D-27 production cycle-budget gate verified post-deploy and recorded in 219-04-SUMMARY.md (Task 4 checkpoint)
</success_criteria>

<output>
Create `.planning/phases/219-ingestion-rate-observability-scope-d/219-04-SUMMARY.md`. Document:
- The folded todo `2026-04-17-ingestion-rate-tool` resolves with this plan (smoothed tail-rate via --rolling=60,300,3600)
- D-19 underscore filename rationale (resolves Codex H3; enables `python -m scripts.phase219_ingestion_digest`)
- D-21 atomic_write_json reuse (resolves Codex H4; collision-safe under concurrent writers; documents the relaxed scripts/→src/wanctl boundary)
- D-23 JSON validation before write (resolves Codex M8; audit evidence integrity)
- D-27 production verification section (`## Production Verification (D-27)`) with cycle-budget evidence — gates `cycle_total.avg_ms ≤ 3.0` and `p99_ms ≤ 7.5`
- Cron stanza is documentation only — operator installs the crontab entry manually
</output>
