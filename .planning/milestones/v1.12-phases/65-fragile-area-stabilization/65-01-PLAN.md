---
phase: 65-fragile-area-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_daemon_interaction.py
  - src/wanctl/steering/steering_confidence.py
  - tests/test_steering_daemon.py
autonomous: true
requirements:
  - FRAG-01
  - FRAG-02
  - FRAG-03

must_haves:
  truths:
    - "Renaming ewma.baseline_rtt in WANControllerState.save() causes a test failure"
    - "Renaming congestion.dl_state in WANControllerState.save() causes a test failure"
    - "evaluate() docstring explicitly documents check_flapping side-effect contract"
    - "WAN config misconfiguration tests assert WARNING level, not just substring match"
  artifacts:
    - path: "tests/test_daemon_interaction.py"
      provides: "Schema-pinning contract tests for autorate-steering state file"
      contains: "TestAutorateSteeringStateContract"
    - path: "src/wanctl/steering/steering_confidence.py"
      provides: "Explicit check_flapping side-effect documentation in evaluate()"
      contains: "check_flapping"
    - path: "tests/test_steering_daemon.py"
      provides: "Level-asserting WARNING tests for WAN config misconfiguration"
      contains: "levelname"
  key_links:
    - from: "tests/test_daemon_interaction.py"
      to: "src/wanctl/wan_controller_state.py"
      via: "json.loads of raw state file output"
      pattern: 'json\.loads.*read_text'
    - from: "tests/test_steering_daemon.py"
      to: "src/wanctl/steering/daemon.py"
      via: "caplog.records levelname assertion"
      pattern: 'levelname.*WARNING'
---

<objective>
Stabilize three fragile areas: add schema-pinning contract tests for the autorate-steering state file interface, document the check_flapping side-effect contract in evaluate(), and strengthen WAN config misconfiguration tests to assert WARNING log level explicitly.

Purpose: Prevent silent breakage of the inter-daemon state file schema, make implicit API contracts explicit, and ensure log level assertions catch accidental downgrades.
Output: New contract test class in test_daemon_interaction.py, updated evaluate() docstring, strengthened tests in test_steering_daemon.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-fragile-area-stabilization/65-RESEARCH.md

@tests/test_daemon_interaction.py
@src/wanctl/wan_controller_state.py
@src/wanctl/steering/steering_confidence.py
@tests/test_steering_daemon.py

<interfaces>
<!-- Key contracts the executor needs -->

From src/wanctl/wan_controller_state.py - WANControllerState.save():
```python
def save(
    self,
    download: dict[str, Any],
    upload: dict[str, Any],
    ewma: dict[str, float],
    last_applied: dict[str, int | None],
    congestion: dict[str, str] | None = None,
    force: bool = False,
) -> bool:
```
Produces JSON with keys: download, upload, ewma, last_applied, timestamp, congestion (optional).
Steering reads: state["ewma"]["baseline_rtt"] and state.get("congestion", {}).get("dl_state", None).

From tests/test_daemon_interaction.py - existing fixtures:
```python
@pytest.fixture
def state_file(tmp_path: Path) -> Path
@pytest.fixture
def make_writer(state_file, logger)
@pytest.fixture
def make_loader(state_file, logger)
def _write_state(writer, baseline_rtt, load_rtt=30.0) -> None
```

From src/wanctl/steering/steering_confidence.py - ConfidenceController.evaluate() line 611:
```python
# Check flap penalty (result unused but call updates internal state)
_ = self.flap_detector.check_flapping(self.timer_state, self.base_steer_threshold)
```

From tests/test_steering_daemon.py - TestWanStateConfig helper:
```python
def _make_config(self, tmp_path, config_dict) -> SteeringConfig
```
Current WARNING tests use `caplog.at_level(logging.WARNING)` and assert `in caplog.text` -- need strengthening to assert `r.levelname == "WARNING"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add state file schema-pinning contract tests</name>
  <files>tests/test_daemon_interaction.py</files>
  <behavior>
    - Test: ewma key exists in raw JSON after save -> assert "ewma" in json.loads(file)
    - Test: baseline_rtt key exists under ewma -> assert "baseline_rtt" in raw["ewma"]
    - Test: congestion key exists when congestion arg passed -> assert "congestion" in raw
    - Test: dl_state key exists under congestion -> assert "dl_state" in raw["congestion"]
    - Test: download/upload/last_applied top-level keys exist -> all 4 tracked sections present
    - Test: all expected subkeys in download section -> green_streak, soft_red_streak, red_streak, current_rate
    - Test: timestamp key exists -> assert "timestamp" in raw
  </behavior>
  <action>
Add a new test class `TestAutorateSteeringStateContract` to `tests/test_daemon_interaction.py` BELOW the existing `TestAutorateSteeringStateInterface` class. These are SCHEMA tests, not behavioral tests -- they inspect raw JSON via `json.loads(state_file.read_text())`, NOT through `BaselineLoader`.

Use the existing fixtures (`make_writer`, `state_file`, `_write_state` helper). Add `import json` at the top of the file.

Contract tests to add:
1. `test_ewma_baseline_rtt_key_path_exists` -- write state, assert `"ewma" in raw` and `"baseline_rtt" in raw["ewma"]`
2. `test_ewma_load_rtt_key_path_exists` -- write state, assert `"load_rtt" in raw["ewma"]`
3. `test_congestion_dl_state_key_path_exists` -- write state with congestion arg, assert `"congestion" in raw` and `"dl_state" in raw["congestion"]`
4. `test_congestion_ul_state_key_path_exists` -- same but assert `"ul_state" in raw["congestion"]`
5. `test_top_level_tracked_sections_exist` -- assert download, upload, ewma, last_applied, timestamp all present
6. `test_download_section_has_required_keys` -- assert green_streak, soft_red_streak, red_streak, current_rate
7. `test_upload_section_has_required_keys` -- same subkeys as download

Add a class docstring explaining these are pinning tests that fail if key paths are renamed, unlike the behavioral tests above which test read/write round-trips.

IMPORTANT: Do NOT route data through BaselineLoader. The point is to assert raw JSON structure so renaming a key on both sides simultaneously still fails the contract test.

For tests needing congestion data, call `writer.save()` directly (not `_write_state` helper) because `_write_state` does not pass the congestion kwarg.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_daemon_interaction.py -x -q</automated>
  </verify>
  <done>New TestAutorateSteeringStateContract class with 7 schema-pinning tests. All pass. Renaming "baseline_rtt" to "rtt_baseline" in wan_controller_state.py would cause test_ewma_baseline_rtt_key_path_exists to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Document check_flapping contract and strengthen WARNING-level test assertions</name>
  <files>src/wanctl/steering/steering_confidence.py, tests/test_steering_daemon.py</files>
  <action>
**FRAG-02 -- Docstring update (steering_confidence.py):**

Add a `Note:` paragraph to `ConfidenceController.evaluate()` docstring (between the `Returns:` section and the method body) documenting the check_flapping side-effect contract:

```
Note:
    check_flapping() is called for its side effects on timer_state
    (flap penalty activation and expiry cleanup). The returned effective
    threshold is intentionally discarded; steering decisions use
    self.base_steer_threshold directly via timer_mgr.
```

Do NOT change the `_ = self.flap_detector.check_flapping(...)` call itself. No behavioral change. Option A from research (docstring only).

**FRAG-03 -- Strengthen WARNING-level assertions (test_steering_daemon.py):**

In `TestWanStateConfig`, update 5 existing tests that assert warning log output. For each test:
1. Change `caplog.at_level(logging.WARNING)` to `caplog.at_level(logging.DEBUG)` so ALL levels are captured
2. Add explicit assertion: `warning_records = [r for r in caplog.records if r.levelname == "WARNING"]` then `assert len(warning_records) > 0, "Expected WARNING-level log"`
3. Move the existing substring assertion to check against warning_records specifically

Tests to update:
- `test_wrong_type_enabled_warns_and_disables` (line ~3020)
- `test_wrong_type_red_weight_warns_and_disables` (line ~3030)
- `test_red_weight_clamped_to_steer_threshold_minus_one` (line ~3043)
- `test_wan_override_true_logs_warning` (line ~3087)
- `test_wan_override_true_plus_disabled_warns` (line ~3096)
- `test_unknown_keys_produce_warning` (line ~3109)

Pattern for each test:
```python
with caplog.at_level(logging.DEBUG):  # Capture all levels
    config = self._make_config(tmp_path, valid_config_dict)
warning_records = [r for r in caplog.records if r.levelname == "WARNING"]
assert len(warning_records) > 0, "Expected WARNING-level log for misconfiguration"
# Keep existing behavioral assertions (config.wan_state_config is None, etc.)
```

Do NOT modify any production code in steering/daemon.py. The research confirmed all misconfiguration paths already log at WARNING. Only the test assertions need strengthening.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_steering_daemon.py -k "TestWanStateConfig" -x -q && .venv/bin/pytest tests/test_steering_confidence.py -x -q</automated>
  </verify>
  <done>evaluate() docstring documents check_flapping side-effect contract. Six WAN config tests now assert levelname == "WARNING" explicitly. Changing any misconfiguration log from WARNING to INFO would cause test failure.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `.venv/bin/pytest tests/test_daemon_interaction.py tests/test_steering_daemon.py tests/test_steering_confidence.py -x -q` -- all new and modified tests pass
2. `.venv/bin/pytest tests/ -q` -- full suite green, no regressions
3. Contract test validates: temporarily renaming a key in wan_controller_state.py would fail the schema-pinning test (do NOT actually commit this -- just verify mentally)
</verification>

<success_criteria>
- TestAutorateSteeringStateContract class exists with 7 schema-pinning tests in test_daemon_interaction.py
- evaluate() docstring explicitly documents check_flapping side-effect contract
- 6 TestWanStateConfig tests assert r.levelname == "WARNING" (not just caplog.text substring)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/65-fragile-area-stabilization/65-01-SUMMARY.md`
</output>
