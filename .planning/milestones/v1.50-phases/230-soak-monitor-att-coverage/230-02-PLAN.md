---
phase: 230-soak-monitor-att-coverage
plan: 02
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - .planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md
  - .planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md
autonomous: false
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "A real read-only soak-monitor run shows the aggregate/per-WAN scan now targets the live ATT units, which the pre-fix wanctl@att.service-only scan would have missed (success criterion 3)"
    - "A representative ATT-unit error condition is demonstrated via a local fake-ssh run (no production mutation): post-fix soak-monitor surfaces the simulated error, the pre-fix script run against the same shim does not"
    - "The before/after unit-set contrast is recorded as a committed evidence artifact"
    - "SAFE-14 controller-path zero-diff holds at the phase boundary against SAFE_BASE=87980bdf; in-scope file accounting uses the separate PHASE230_START=4ad2986e ref (87980bdf predates Phase 229 scripts/tests changes and MUST NOT be used for scope accounting)"
  artifacts:
    - path: ".planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md"
      provides: "Criterion-3 read-only before/after unit-set contrast evidence (pre-fix wanctl@att.service vs post-fix live ATT units)"
      contains: "cake-autorate-att.service"
    - path: ".planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md"
      provides: "SAFE-14 controller-path zero-diff proof: protected-path git diff vs baseline + dirty-tree status"
      contains: "87980bdf"
  key_links:
    - from: "230-MON01-EVIDENCE.md"
      to: "scripts/soak-monitor.sh --json units list"
      via: "read-only soak-monitor run output capture"
      pattern: "cake-autorate-att-state-bridge.service"
---

<objective>
Prove the fix in the field and at the boundary. Two deliverables, both documentation/evidence only:

1. Criterion-3 evidence (MON-01): a read-only demonstration that, post-fix, soak-monitor's scan targets the live ATT units — contrasted with the pre-fix `wanctl@att.service`-only scan that would have missed any ATT-unit error. NO live mutation, NO fault injection on production hosts (RESEARCH Pitfall 5). The live `--json` `units`-list contrast proves targeting; the roadmap's "injected/representative ATT-unit error condition" is satisfied without touching production via a LOCAL fake-ssh representative run (review consensus MEDIUM): the same shim technique as Plan 01's behavior test, simulating one ATT-unit journal error, run against both the post-fix script (surfaces it) and the pre-fix script (misses it). Supplement with any naturally-occurring ATT `err` already present in the live 1h window.

2. SAFE-14 boundary proof with TWO pinned baselines (review consensus HIGH — a single baseline conflates two different proofs):
   - `SAFE_BASE=87980bdf` — controller-path zero-diff proof only. Valid: no controller `.py` has changed since Phase 229's pinned docs-only commit.
   - `PHASE230_START=4ad2986e` (`4ad2986ec2872317ffcb25604781932caaf1d862`, Phase 229 closure commit) — "only Phase 230 files changed" in-scope accounting. `git diff --stat 87980bdf -- scripts/ tests/` MUST NOT be used for scope accounting: it already contains Phase 229 changes (`scripts/deploy.sh`, `scripts/phase229-att-artifact-diff.sh`, `tests/test_att_cake_autorate_artifacts.py`) — verified locally 2026-06-09. `git diff --stat 4ad2986e -- scripts/ tests/` is empty pre-phase (verified), so at the boundary it shows exactly the Phase 230 surface.
   Record both refs, the protected-path diff output, and the dirty-tree status, mirroring the `check-safe07-source-diff.sh` committed-diff-vs-ref + fail-closed pattern.

Purpose: close MON-01 success criterion 3 with field evidence and hold SAFE-14 at the phase boundary.
Output: two evidence notes in the phase dir.

Operator/credential note (per MEMORY): the live read-only run hits cake-shaper journals. If a privileged journal read (`journalctl -p err` / `sudo -n`) is denied, do NOT escalate credentials — hand Kevin a `! <command>` to run at the keyboard and paste back. The `--json` `units`-list contrast does not require privileged journal access (it shows which units the scan targets) and is sufficient on its own for criterion 3 if journal err-counts are gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-03-SUMMARY.md

<interfaces>
<!-- Baselines + protected set, VERIFIED 2026-06-09. -->
TWO baselines — do not conflate (review HIGH):
  SAFE_BASE      = 87980bdf8ea52e5537110cd9bbc7a368f523d2e2  (Phase 229 last docs/planning-only commit; controller-path zero-diff proof ONLY.
                   NOT valid for scripts/tests scope accounting — diff vs scripts/ tests/ already contains Phase 229 files:
                   scripts/deploy.sh, scripts/phase229-att-artifact-diff.sh, tests/test_att_cake_autorate_artifacts.py)
  PHASE230_START = 4ad2986ec2872317ffcb25604781932caaf1d862  (Phase 229 closure commit "docs(phase-229): evolve PROJECT.md after phase completion";
                   "only Phase 230 files changed" scope accounting. git diff --stat 4ad2986e -- scripts/ tests/ verified EMPTY pre-phase;
                   commits between it and 230-01 execution are .planning-docs-only and cannot affect the scripts/ tests/ scope diff)
Protected controller-path set (per Decision 227-04, includes wan_controller_state.py):
  src/wanctl/wan_controller.py  src/wanctl/wan_controller_state.py
  src/wanctl/queue_controller.py  src/wanctl/cake_signal.py
  src/wanctl/alert_engine.py  src/wanctl/fusion_healer.py  src/wanctl/backends/
Pattern reference: scripts/check-safe07-source-diff.sh (committed-diff-vs-ref, dirty-tree pre-check, fail-closed at exit 1).
soak-monitor evidence surface: ./scripts/soak-monitor.sh --json -> the {"service_group":"all-claimed-services","units":[...]} object's units list (post-fix includes the three ATT live units; pre-fix listed wanctl@att.service).
Pre-fix script retrieval for contrast: git show 4ad2986e:scripts/soak-monitor.sh  (pinned ref — do NOT use plan-name pseudo-refs like 230-01~1; plan IDs are not git refs)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Criterion-3 read-only live evidence — ATT live-unit scan contrast</name>
  <files>.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md</files>
  <read_first>
    - scripts/soak-monitor.sh (post-Plan-01 state — the generalized --json aggregate units list is the evidence surface)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Pitfall 5 read-only evidence rule, Open Question 3 evidence form)
  </read_first>
  <action>
    Capture the criterion-3 read-only field evidence and record it in `.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md`. All steps READ-ONLY — no `systemctl start/stop`, no fault injection on live units.

    Step A (post-fix live capture): run `./scripts/soak-monitor.sh --json | python3 -m json.tool` and capture the `{"service_group":"all-claimed-services", ...}` object's `units` list and the `att` per-WAN row's `errors_1h`. Confirm the units list contains `cake-autorate-att.service`, `cake-autorate-att-state-bridge.service`, `silicom-bypass-watchdog-cake-autorate-att.service` and does NOT contain `wanctl@att.service` (ATT in external mode).

    Step A2 (mode-detection inputs, read-only — review LOW: if mode detection silently failed, the --json output would fall back to native and the evidence would be inconclusive): capture `ssh kevin@10.10.110.223 'systemctl is-active cake-autorate-att.service cake-autorate-att-state-bridge.service silicom-bypass-watchdog-cake-autorate-att.service wanctl@att.service'` and record the four statuses (expected: active/active/active/inactive) so the evidence shows WHY external mode was selected.

    Step B (pre-fix contrast WITHOUT reverting code): display the old hardcoded ATT branch via `git show 4ad2986e:scripts/soak-monitor.sh` (PHASE230_START — a real pinned git ref; do NOT use `230-01~1`, plan IDs are not git refs). Record the contrast — pre-fix ATT scan targeted only the disabled `wanctl@att.service`, so any error in the live ATT units would have been missed.

    Step B2 (representative ATT-unit error condition, LOCAL ONLY — closes the roadmap criterion-3 "injected/representative" wording without production mutation): using the same fake-ssh shim technique as Plan 01's behavior test (temp bin dir first on PATH; shim answers `systemctl is-active cake-autorate-` payloads with exit 0, prints a canned health JSON for `curl` payloads, and for `journalctl` payloads prints `3` when the unit args include an ATT live unit — e.g. the silicom watchdog — else `0`):
      - run the POST-FIX `bash scripts/soak-monitor.sh --json` against the shim → the `att` row's `errors_1h` is 3 (the simulated error SURFACES);
      - extract the PRE-FIX script (`git show 4ad2986e:scripts/soak-monitor.sh > /tmp/soak-monitor-prefix.sh`) and run it against the SAME shim → the `att` row's `errors_1h` is 0 (the pre-fix scan, pointed at `wanctl@att.service` only, MISSES the same condition).
      Capture both outputs verbatim. This is a real soak-monitor run surfacing a representative ATT-unit error condition that the pre-fix scan demonstrably misses — no live host is touched by the shim runs.

    Step C (credential handling): if a privileged journal read is denied during Step A/A2, do NOT escalate credentials — hand Kevin a `! <command>` to run at the keyboard and paste back (MEMORY). The `units`-list contrast plus the Step B2 local representative run do not need privileged journal access and together satisfy criterion 3.

    Step D (record): write `230-MON01-EVIDENCE.md` containing the captured post-fix `units` list, the Step A2 mode-detection statuses, the pre-fix contrast, the Step B2 representative-error run (both outputs), any naturally-present ATT-unit `err` in the live window, and a one-line verdict that the post-fix scan surfaces ATT-unit error conditions the pre-fix scan would have missed. Use only IPs/units already committed in the repo.

    This task is a human-verify checkpoint: after recording, pause for operator confirmation that the `--json` units list shows the live ATT units and the before/after contrast is correct.
  </action>
  <verify>
    <human-check>Operator confirms `./scripts/soak-monitor.sh --json` aggregate units list shows the three live ATT units (and no `wanctl@att.service` in external mode), that the local fake-ssh representative run shows post-fix errors_1h=3 vs pre-fix errors_1h=0 for the same simulated ATT-unit error, and that `230-MON01-EVIDENCE.md` records the full contrast.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md` exists and contains the literal `cake-autorate-att.service`, `cake-autorate-att-state-bridge.service`, and `silicom-bypass-watchdog-cake-autorate-att.service` in the captured post-fix units list.
    - The evidence records the Step A2 mode-detection inputs (systemctl is-active statuses for the three ATT live units + wanctl@att.service) proving external mode was genuinely detected, not defaulted.
    - The evidence records the pre-fix `wanctl@att.service`-only ATT scan as the contrast, retrieved via the pinned `git show 4ad2986e:scripts/soak-monitor.sh` (no `230-01~1`-style pseudo-refs anywhere in the note).
    - The evidence records the Step B2 representative-error demonstration: post-fix run surfaces the simulated ATT-unit error (errors_1h=3), pre-fix run against the same shim reports 0 — satisfying the roadmap's "injected/representative ATT-unit error condition" wording with zero production mutation.
    - A one-line verdict states the post-fix scan surfaces ATT-unit error conditions the pre-fix scan would have missed (criterion 3).
    - All live-host commands are read-only; the note contains no fault-injection or `systemctl start/stop` step; the only "injection" is into the local fake-ssh shim's canned output.
    - Operator types "approved" (or describes what is missing).
  </acceptance_criteria>
  <done>The criterion-3 evidence note records the read-only live unit-set contrast, the mode-detection inputs, and a local representative-error run proving the post-fix scan surfaces an ATT-unit error condition the pre-fix scan misses — and the operator has approved.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-14 controller-path zero-diff boundary proof</name>
  <files>.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md</files>
  <read_first>
    - .planning/phases/229-att-deploy-path-artifact-tests/229-03-SUMMARY.md (the SAFE-14 boundary proof shape: committed protected-path diff + dirty-tree status, baseline 87980bdf)
    - scripts/check-safe07-source-diff.sh (committed-diff-vs-ref + dirty-tree pre-check + fail-closed pattern to mirror)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Pitfall 4, SAFE-14 Code Examples L265-274)
  </read_first>
  <action>
    Produce the SAFE-14 boundary proof for Phase 230 and record it in `.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md`. TWO pinned baselines, two distinct proofs (review consensus HIGH — do not conflate them):

    1. Controller-path zero-diff vs `SAFE_BASE=87980bdf`: `git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/`. This MUST be empty.
    2. Run the dirty-tree pre-check over the same protected set: `git status --porcelain -- src/wanctl/` plus `git diff --quiet -- src/wanctl/` and `git diff --cached --quiet -- src/wanctl/` — confirm the controller path is clean. Fail-closed: any protected-path diff or dirty state is a VIOLATION and the phase boundary does not pass.
    3. In-scope file accounting vs `PHASE230_START=4ad2986e` (NOT 87980bdf — `git diff --stat 87980bdf -- scripts/ tests/` already contains the Phase 229 files `scripts/deploy.sh`, `scripts/phase229-att-artifact-diff.sh`, `tests/test_att_cake_autorate_artifacts.py`, verified 2026-06-09): `git diff --stat 4ad2986e -- scripts/ tests/` MUST show only `scripts/soak-monitor.sh` and `tests/test_soak_monitor_att_coverage.py`.
    4. Record into `230-SAFE14-BOUNDARY.md`: BOTH baseline SHAs (`SAFE_BASE=87980bdf8ea52e5537110cd9bbc7a368f523d2e2`, `PHASE230_START=4ad2986ec2872317ffcb25604781932caaf1d862`) with one line each on what each proves, the empty protected-path diff output, the clean dirty-tree status, the two-file in-scope change list from PHASE230_START, and a PASS/VIOLATION verdict. Mirror the 229-03 evidence shape.
    5. Also record Plan 01's verification outputs in the note (review suggestion — ties the boundary proof to the shipped state): `shellcheck -S error scripts/soak-monitor.sh` exit code, `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q` result (5 passed), and full `.venv/bin/pytest tests/ -q` result.

    Keep `SAFE_BASE` pinned at `87980bdf` for the controller proof — do not re-pin to a later commit, since the goal is to prove zero controller drift across the whole ATT-coverage work since Phase 229. `PHASE230_START` stays pinned at `4ad2986e` even though docs-only commits follow it — those cannot appear in a `-- scripts/ tests/` diff.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && test -z "$(git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/)" && echo "SAFE-14 PASS: controller-path zero-diff vs 87980bdf"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 87980bdf -- <protected controller set>` is EMPTY (zero controller-path diff). Verifiable by the automated command echoing `SAFE-14 PASS`.
    - The protected controller path has no dirty-tree state: `git status --porcelain -- src/wanctl/` shows no controller `.py` changes attributable to this phase.
    - `git diff --stat 4ad2986e -- scripts/ tests/` (PHASE230_START, NOT 87980bdf) shows only `scripts/soak-monitor.sh` and `tests/test_soak_monitor_att_coverage.py`. Verifiable: `git diff --name-only 4ad2986e -- scripts/ tests/` outputs exactly those two paths.
    - `.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md` exists, records BOTH refs — `SAFE_BASE=87980bdf8ea52e5537110cd9bbc7a368f523d2e2` (controller zero-diff) and `PHASE230_START=4ad2986ec2872317ffcb25604781932caaf1d862` (scope accounting) — with what each proves, the empty diff output, the clean dirty-tree status, the Plan 01 verification results (shellcheck, focused pytest, full pytest), and a PASS verdict, mirroring the 229-03 evidence shape.
  </acceptance_criteria>
  <done>SAFE-14 boundary proof recorded with an empty protected-path diff vs SAFE_BASE 87980bdf, clean controller dirty-tree, a two-file in-scope change list vs PHASE230_START 4ad2986e, Plan 01 verification results, and a PASS verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box -> cake-shaper host (ssh) | Criterion-3 evidence run crosses this read-only; `--json` units capture + optional `journalctl -p err` read only |
| repo working tree -> committed evidence | SAFE-14 proof reads git history/diff only; no source mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-05 | Tampering | criterion-3 live run | mitigate | Evidence run is strictly read-only (`--json` units capture, optional `journalctl -p err`); NO fault injection, NO `systemctl start/stop` on live ATT units (RESEARCH Pitfall 5) |
| T-230-06 | Elevation of Privilege | privileged journal read | mitigate | If `sudo -n`/journal read denied, hand operator a `! <command>` rather than escalating creds (MEMORY); units-list contrast needs no privileged access |
| T-230-07 | Information Disclosure | committed evidence note | mitigate | Evidence records only IPs/units already committed in the repo; no new secret/host exposure |
| T-230-08 | Tampering | SAFE-14 controller path | mitigate | Boundary proof is fail-closed: any non-empty protected-path diff or dirty controller state = VIOLATION, phase does not pass |
</threat_model>

<verification>
- `230-MON01-EVIDENCE.md` shows the post-fix `--json` aggregate `units` list containing the three ATT live units, the mode-detection inputs, the pre-fix `wanctl@att.service`-only contrast (via pinned `git show 4ad2986e:scripts/soak-monitor.sh`), and the local fake-ssh representative-error run (post-fix surfaces errors_1h=3, pre-fix reports 0).
- `230-SAFE14-BOUNDARY.md` records an empty protected-path diff vs `SAFE_BASE=87980bdf`, clean controller dirty-tree, a two-file in-scope list vs `PHASE230_START=4ad2986e`, Plan 01 verification results, and a PASS verdict.
- No live mutation or fault injection performed; all live-host contact read-only (the representative error exists only in a local fake-ssh shim's canned output).
</verification>

<success_criteria>
- MON-01 criterion 3: field evidence demonstrates the post-fix scan targets ATT live units, and a representative ATT-unit error condition is surfaced by a real (local shim) soak-monitor run that the pre-fix scan demonstrably misses.
- SAFE-14: controller-path zero-diff holds at the phase boundary (empty diff vs SAFE_BASE, clean dirty-tree, only two in-scope files changed vs PHASE230_START).
</success_criteria>

<output>
Create `.planning/phases/230-soak-monitor-att-coverage/230-02-SUMMARY.md` when done.
</output>
