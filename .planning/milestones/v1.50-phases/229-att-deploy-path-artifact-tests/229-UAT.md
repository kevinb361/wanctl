---
status: complete
phase: 229-att-deploy-path-artifact-tests
source: [229-01-SUMMARY.md, 229-02-SUMMARY.md, 229-03-SUMMARY.md]
started: 2026-06-10T19:01:06Z
updated: 2026-06-10T19:04:30Z
---

## Current Test

[testing complete]

## Tests

### 1. ATT dry-run deploy lists full artifact set
expected: `bash scripts/deploy.sh att cake-shaper --with-att-cake-autorate --dry-run` exits 0; output includes ATT config, qdisc-init, state-bridge, and all three ATT systemd units including the silicom bypass watchdog, plus next-step restart/monitor guidance.
result: pass
evidence: Exit 0; planned actions include "deploy ATT cake-autorate config, qdisc init, state bridge, both cake-autorate units, and silicom watchdog unit" and "keep wanctl@att.service disabled while cake-autorate owns ATT rates".

### 2. WAN-name gate rejects mismatched flag
expected: `bash scripts/deploy.sh spectrum cake-shaper --with-att-cake-autorate --dry-run` exits non-zero with error "--with-att-cake-autorate is only valid with WAN name 'att'".
result: pass
evidence: Exit 1 with exact error message.

### 3. ATT artifact-contract tests pass
expected: `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q` → 6 passed.
result: pass
evidence: 6 passed in 0.36s.

### 4. Spectrum+ATT parity suite passes together
expected: `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py tests/test_spectrum_cake_autorate_artifacts.py -q` → 11 passed, no startup-race flake on the ATT bridge health poll.
result: pass
evidence: 11 passed in 0.61s.

### 5. Live ATT artifact diff is read-only and ALL EQUAL
expected: `scripts/phase229-att-artifact-diff.sh` against cake-shaper (BatchMode SSH + sudo -n cat, no writes) reports all six ATT artifacts `equal` with overall verdict `ALL EQUAL`, matching 229-DEPLOY02-EVIDENCE.md.
result: pass
evidence: Live run 2026-06-10: all six artifacts equal, verdict ALL EQUAL, exit 0.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
