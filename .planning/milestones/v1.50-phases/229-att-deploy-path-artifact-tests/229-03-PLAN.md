---
phase: 229-att-deploy-path-artifact-tests
plan: 03
type: execute
wave: 3
depends_on: ["229-01", "229-02"]
files_modified:
  - scripts/phase229-att-artifact-diff.sh
  - .planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md
autonomous: false
requirements: [DEPLOY-02, SAFE-14]
user_setup: []
must_haves:
  truths:
    - "A read-only audit script compares the repo's 6 ATT artifacts against the live hand-deployed copies on cake-shaper using ssh BatchMode + sudo -n cat | sha256sum"
    - "The audit never mutates the target — only `cat` and `sha256sum` cross the SSH boundary"
    - "Any repo-vs-live hash mismatch is recorded honestly as a drift finding with repo declared as source of truth"
    - "SAFE-14 controller-path zero-diff is proven empty at the phase boundary (final task, last)"
  artifacts:
    - path: "scripts/phase229-att-artifact-diff.sh"
      provides: "read-only live-vs-repo sha256 diff for the 6 ATT artifacts"
      contains: "sudo -n cat"
    - path: ".planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md"
      provides: "captured per-artifact repo_sha / live_sha / verdict table + SAFE-14 boundary result"
      contains: "config.att.sh"
  key_links:
    - from: "scripts/phase229-att-artifact-diff.sh"
      to: "cake-shaper (read-only)"
      via: "ssh -o BatchMode=yes ... sudo -n cat <live> | sha256sum"
      pattern: "ssh -o BatchMode=yes"
---

<objective>
Verify the repo's ATT artifact set matches the live hand-deployed state on cake-shaper via a read-only sha256 diff (DEPLOY-02), then prove SAFE-14 controller-path zero-diff at the phase boundary. The repo is the declared source of truth; any drift is recorded as a finding, not silently reconciled.

Purpose: Close the loop on the hand-deployed ATT artifacts — make the repo authoritative over what is already running, with honest evidence of any divergence. SAFE-14 confirms the deploy/test surface never touched the controller path.

Output: `scripts/phase229-att-artifact-diff.sh` (read-only audit), `229-DEPLOY02-EVIDENCE.md` (captured verdict table + SAFE-14 result).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-RESEARCH.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md
@scripts/phase226-snapshot-a.sh

<interfaces>
<!-- Read-only audit contract. Reuse the phase226 precedent verbatim. -->

phase226-snapshot-a.sh precedent (read-only, VERIFIED):
  SSH_HOST="cake-shaper"
  ssh -o BatchMode=yes "$SSH_HOST" "sudo -n cat <live_path>" | sha256sum | cut -d' ' -f1
  artifact_sha256() { sha256sum "$1" | cut -d' ' -f1; }
  verdict: [[ "$live_sha" == "$repo_sha" ]] && "equal" || "diff"

The 6 ATT repo→live path pairs (all read-only):
  configs/cake-autorate/config.att.sh                              -> /etc/cake-autorate/config.att.sh
  deploy/scripts/cake-autorate-att-qdisc-init                      -> /usr/local/sbin/cake-autorate-att-qdisc-init
  deploy/scripts/cake-autorate-att-state-bridge                    -> /usr/local/sbin/cake-autorate-att-state-bridge
  deploy/systemd/cake-autorate-att.service                         -> /etc/systemd/system/cake-autorate-att.service
  deploy/systemd/cake-autorate-att-state-bridge.service            -> /etc/systemd/system/cake-autorate-att-state-bridge.service
  deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service -> /etc/systemd/system/silicom-bypass-watchdog-cake-autorate-att.service

Expected (per research A3): the state-bridge live copy may differ from repo (hand-deployed 2026-06-08; repo may carry later edits). Surface honestly; do NOT assume zero-diff.

SAFE-14 protected controller-path set (CORRECTED — linux_cake.py/netlink_cake.py live under backends/, NOT top-level):
  src/wanctl/wan_controller.py
  src/wanctl/wan_controller_state.py        (per Decision 227-04 — imported by wan_controller.py)
  src/wanctl/queue_controller.py
  src/wanctl/cake_signal.py
  src/wanctl/alert_engine.py
  src/wanctl/fusion_healer.py
  src/wanctl/backends/                       (covers base.py, linux_cake.py, linux_cake_adapter.py, netlink_cake.py, routeros.py)

SAFE-14 baseline ref: the milestone/phase boundary commit BEFORE phase 229 implementation work began. At planning time HEAD is 87980bdf (docs(phase-229): add research and validation strategy) on top of the v1.50 roadmap/requirements docs commits. Pin the baseline to the commit that is the last commit before any Plan 01/02/03 implementation commit — the executor pins the exact SHA in the evidence file (the docs-only HEAD 87980bdf is the correct baseline because all prior 229 commits are docs/planning only). The git-diff against that baseline over the protected set MUST be empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the read-only ATT live-vs-repo sha256 diff script</name>
  <files>scripts/phase229-att-artifact-diff.sh</files>
  <read_first>
    - scripts/phase226-snapshot-a.sh (the read-only contract precedent — L12 SSH_HOST, L118-120 artifact_sha256 helper, L188 read-only ssh sudo -n cat, L241-247 equality compare)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (DEPLOY-02 section; Read-only remote contract)
  </read_first>
  <action>
    Create `scripts/phase229-att-artifact-diff.sh` modeled on `scripts/phase226-snapshot-a.sh`'s read-only contract. Default `SSH_HOST="cake-shaper"` (overridable by `$1` or env). For each of the 6 ATT repo→live path pairs in `<interfaces>`: compute the repo sha256 with `sha256sum <repo_file> | cut -d' ' -f1`; compute the live sha256 with `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n cat <live_path>" | sha256sum | cut -d' ' -f1`; set per-artifact verdict `equal` if hashes match else `drift`.

    The ONLY operations crossing the SSH boundary are `sudo -n cat` reads. The script MUST NOT contain any write/mutate command targeting the host (no mv, cp, rm, tee, systemctl, sed -i, scp-to-host). Print a per-artifact table (artifact, repo_sha, live_sha, verdict) and an overall verdict line (`ALL EQUAL` if all 6 match, else `DRIFT FOUND: <list>`). Exit 0 on all-equal, exit 3 on any drift (drift is a finding, not a script error). If an `ssh`/`sudo -n cat` fails (unreachable host or denied privilege), print the artifact as `unavailable` and exit 2 with a message that read-only access to cake-shaper is required; do NOT escalate credentials.

    Make the script executable (chmod 755). Use `set -euo pipefail` with care around the per-artifact loop so one drift/unavailable row does not abort the whole sweep prematurely (collect verdicts, then decide exit code).
  </action>
  <verify>
    <automated>bash -n scripts/phase229-att-artifact-diff.sh && test -x scripts/phase229-att-artifact-diff.sh && ! grep -Eq 'ssh[^|]*(sudo[^|]*(mv|rm|cp|tee|sed -i|systemctl|chmod|chown)|>)' scripts/phase229-att-artifact-diff.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase229-att-artifact-diff.sh` exits 0.
    - The script is executable (`test -x` passes).
    - The script contains `ssh -o BatchMode=yes` and `sudo -n cat` and `sha256sum`.
    - The script contains NO host-write command: `grep -E 'sudo[^|]*(mv|rm |cp |tee|sed -i|systemctl|chmod|chown)' scripts/phase229-att-artifact-diff.sh` returns no match inside any ssh invocation, and there is no `scp` to the target. (Read-only contract.)
    - All 6 repo→live ATT path pairs from `<interfaces>` appear in the script.
    - The script references `cake-shaper` as the default host.
  </acceptance_criteria>
  <done>A read-only sha256 diff script exists, is executable, contacts cake-shaper only via `sudo -n cat`, emits a per-artifact verdict table, exits 0/2/3 for equal/unavailable/drift, and contains zero host-mutating commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the read-only DEPLOY-02 diff against cake-shaper and captures evidence</name>
  <what-built>
    A read-only sha256 diff script (`scripts/phase229-att-artifact-diff.sh`) that compares the repo's 6 ATT artifacts against the live hand-deployed copies on cake-shaper. It only `sudo -n cat`s the live files — no mutation. This task requires read-only SSH to the production cake-shaper host, which per the operator's credential-read rule (MEMORY) must run with the operator at the keyboard.
  </what-built>
  <how-to-verify>
    1. Confirm read-only access: the script crosses the SSH boundary only via `sudo -n cat`. Review `scripts/phase229-att-artifact-diff.sh` if you want to confirm no write commands.
    2. Run it: `bash scripts/phase229-att-artifact-diff.sh cake-shaper`
       (or, if `sudo -n` is denied non-interactively, the executor will hand you a `! <command>` form to run at your keyboard rather than escalating creds.)
    3. Expected: a per-artifact table of repo_sha / live_sha / verdict, then an overall verdict.
       - Per research A3, the state-bridge row MAY show `drift` (live hand-deployed 2026-06-08; repo possibly newer). That is an expected, honestly-recorded finding — repo is the declared source of truth.
       - If the qdisc-init / config / unit files show drift, note which and whether the live or repo side is correct.
    4. The executor records the full verdict table into `.planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md`, including: each artifact's repo_sha, live_sha, verdict; the overall verdict; and a one-line disposition for any drift row (reconcile-to-repo / intentional-live-divergence / deferred-blocker). Do NOT reconcile (no edits to live or repo artifacts) in this phase — record the finding only.
  </how-to-verify>
  <resume-signal>Type "approved" once the diff has run and the evidence table is captured, or report the drift findings / access blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record DEPLOY-02 verdict + prove SAFE-14 controller-path zero-diff at the phase boundary (LAST)</name>
  <files>.planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md</files>
  <read_first>
    - scripts/check-safe07-source-diff.sh (the SAFE-N protected-path git-diff precedent)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (SAFE-14 controller-path zero-diff section; corrected protected set)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md (the DEPLOY-02 table captured in Task 2)
  </read_first>
  <action>
    Finalize `229-DEPLOY02-EVIDENCE.md` with the DEPLOY-02 verdict table (from Task 2) and append a SAFE-14 boundary section. Per the SAFE-07..13 precedent and Decision 227-04, run the protected-path git diff over the CORRECTED set (note: `linux_cake.py`/`netlink_cake.py` are under `backends/`, so the `backends/` directory entry covers them — do NOT reference non-existent top-level `src/wanctl/linux_cake.py`):

    `git diff --stat <baseline-ref> -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/`

    Pin `<baseline-ref>` to the last docs/planning-only commit before any Plan 01/02/03 implementation commit (at planning time that is `87980bdf`; the executor confirms the exact SHA and records it). The diff MUST be empty (zero controller-path source change). Also run the dirty-tree check: `git status --porcelain -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/` MUST be empty (no staged/unstaged/untracked controller-path changes).

    Record in the evidence file: the pinned baseline SHA, the exact git-diff command run, the (empty) output, the dirty-tree result, and a `SAFE-14: PASS` / `FAIL` verdict. If non-empty, that is a phase-boundary failure — record it as FAIL and surface as a blocker (do NOT attempt to "fix" the controller path; it should already be untouched since this milestone's surface is deploy/test/ops/doc only).

    Run this task LAST in the phase (after Plans 01 and 02 are committed and after Task 2) so the boundary reflects the true final phase state, per the 225-05 / 227-04 SAFE-boundary-runs-last precedent.
  </action>
  <verify>
    <automated>test -f .planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md && grep -q 'SAFE-14' .planning/phases/229-att-deploy-path-artifact-tests/229-DEPLOY02-EVIDENCE.md && [ -z "$(git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `229-DEPLOY02-EVIDENCE.md` exists and contains the 6-artifact DEPLOY-02 verdict table (each row has repo_sha, live_sha, verdict) and an overall DEPLOY-02 verdict.
    - The evidence file contains a `SAFE-14` section with the pinned baseline SHA, the exact protected-path git-diff command, and a `SAFE-14: PASS` (or FAIL) verdict.
    - `git diff --stat <pinned-baseline> -- <SAFE-14 protected set>` produces empty output (zero controller-path diff).
    - `git status --porcelain -- <SAFE-14 protected set>` is empty.
    - The SAFE-14 protected set in the evidence file uses `src/wanctl/backends/` (directory) and does NOT list non-existent top-level `src/wanctl/linux_cake.py` or `src/wanctl/netlink_cake.py`.
    - Any DEPLOY-02 drift row has a recorded disposition; the phase makes NO edits to live or repo ATT artifacts to reconcile (drift is a finding only).
  </acceptance_criteria>
  <done>DEPLOY-02 verdict captured honestly with repo as source of truth, and SAFE-14 controller-path zero-diff proven empty at the phase boundary against a pinned baseline.</done>
</task>

</tasks>

<verification>
- `scripts/phase229-att-artifact-diff.sh`: `bash -n` clean, executable, read-only contract (no host-write commands), all 6 artifact pairs present.
- DEPLOY-02 evidence table captured with repo as source of truth; drift (if any, esp. state-bridge) recorded honestly with disposition.
- SAFE-14: `git diff --stat <pinned-baseline> -- <protected set>` empty AND `git status --porcelain -- <protected set>` empty.
- No edits to any `src/wanctl/` controller-path file; no production mutation (read-only SSH only).
</verification>

<success_criteria>
DEPLOY-02 met: a verified read-only diff confirms (or honestly surfaces drift in) the repo's ATT artifact set vs the live cake-shaper state, repo declared source of truth. SAFE-14 met: controller-path zero-diff proven empty at the phase boundary.
</success_criteria>

<output>
Create `.planning/phases/229-att-deploy-path-artifact-tests/229-03-SUMMARY.md` when done.
</output>
