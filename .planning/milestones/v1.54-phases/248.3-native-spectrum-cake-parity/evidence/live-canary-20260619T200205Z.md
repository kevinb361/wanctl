# Phase 248.3 Live Canary Evidence — 2026-06-19T200205Z

## Objective

Verify that native `wanctl@spectrum` can run with the same practical CAKE shape as external cake-autorate before future fping keep/default-flip decisions:

- download envelope: 550M green floor / 600M ceiling
- upload: 18M ceiling
- CAKE RTT: 25ms
- download: no-ack-filter
- upload: ack-filter
- wash: enabled

## Pre-state

```text
cake-autorate-spectrum.service=active
cake-autorate-spectrum-state-bridge.service=active
steering.service=active
wanctl@spectrum.service=inactive
```

Backup created:

```text
/etc/wanctl/spectrum.yaml.pre-phase2483-parity-20260619T200205Z
```

## Applied live canary config

The canary changed live `/etc/wanctl/spectrum.yaml` to:

```text
backend=fping
rtt=25ms
ack_filter_absent=true
dl=550/600
```

This kept live config formatting by doing targeted text edits rather than dumping YAML.

## Native canary startup

After stopping external cake-autorate owner/state bridge and starting native `wanctl@spectrum.service`:

```text
cake-autorate-spectrum.service=inactive
cake-autorate-spectrum-state-bridge.service=inactive
steering.service=active
wanctl@spectrum.service=active
NRestarts=0
ExecMainStatus=0
ActiveState=active
SubState=running
```

Native config proof:

```text
backend=fping
rtt=25ms
ack_filter_present=False
dl_floor_green=550
dl_ceiling=600
```

Native initial qdisc proof:

```text
spec-modem: 18Mbit, wash, ack-filter, rtt 25ms, overhead 18, mpu 64, memlimit 64Mb
spec-router: 600Mbit, wash, no-ack-filter, rtt 25ms, overhead 18, mpu 64, memlimit 64Mb
```

Native initial health:

```text
status=healthy source=None version=1.47.0 backend=fping active=fping stale=0.06 fallback=0 raw=22.5 cycle_p99=2.7 cadence=10.0
```

## Observation window

Observed for 75s after a 45s startup grace.

Selected samples:

```text
--- t=15s ---
health=healthy backend=fping active=fping stale=4.286 raw=21.1 cycle_p99=2.1 overrun=5

--- t=30s ---
health=healthy backend=fping active=fping stale=8.516 raw=20.8 cycle_p99=1.0 overrun=5

--- t=45s ---
health=healthy backend=fping active=fping stale=1.877 raw=33.7 cycle_p99=1.0 overrun=5

--- t=60s ---
health=healthy backend=fping active=fping stale=6.116 raw=18.9 cycle_p99=1.0 overrun=5

--- t=75s ---
health=healthy backend=fping active=fping stale=10.354 raw=20.0 cycle_p99=1.0 overrun=5
```

The staleness values up to 10.354s are expected under fping's 10s cadence plus Phase 248.2's 15s hard stale window.

Final native qdisc proof:

```text
spec-modem: 18Mbit, wash, ack-filter, rtt 25ms
spec-router: 600Mbit, wash, no-ack-filter, rtt 25ms
```

Gate scan:

```text
stale_count=0 fallback_count=10 restarts=0
```

Interpretation:

- The Phase 248.2 stale-window fix still held under the parity config.
- Native qdisc shape now matches the external cake-autorate semantics closely enough for future fping canaries: same CAKE RTT and ack-filter directionality, with a 550/600 download envelope instead of the old 920M ceiling.
- Startup first-sample fallback warnings still occurred before fping was available; this remains the next fping blocker.

## Rollback / final state

Rollback intentionally restored service ownership and `measurement.backend: icmplib` while leaving the verified parity config values in place for future native starts.

Rollback config proof:

```text
rollback_backend=icmplib
rollback_rtt=25ms
rollback_ack_filter_present=False
rollback_dl_ceiling=600
```

Rollback services:

```text
cake-autorate-spectrum.service=active
cake-autorate-spectrum-state-bridge.service=active
steering.service=active
wanctl@spectrum.service=inactive
```

Post-rollback health:

```text
healthy cake-autorate-state-bridge cake-autorate-trial
```

Final independent verification:

```text
backend=icmplib
rtt=25ms
ack_filter_present=False
dl_floor_green=550
dl_ceiling=600
healthy cake-autorate-state-bridge cake-autorate-trial
spec-modem: 18Mbit, wash, ack-filter, rtt 25ms
spec-router: ~544Mbit, wash, no-ack-filter, rtt 25ms
```

The final download qdisc bandwidth is controlled by active external cake-autorate and naturally drifted within its 500/550/600 envelope after rollback.

## Verdict

Phase 248.3 passes.

Native Spectrum is now aligned to the external cake-autorate operating shape for fair future fping canaries. Do not permanently keep native fping yet; startup first-sample fallback behavior still needs a targeted fix or explicit acceptance.
