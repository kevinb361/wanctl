---
phase: "247"
plan: "04"
type: execute
wave: 3
depends_on:
  - "247-03"
files_modified:
  - .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-shadow-summary.json
autonomous: false
requirements:
  - PROF-01
user_setup:
  - service: cake-shaper
    why: "Shadow capture script must run on cake-shaper (the live Spectrum WAN host); fping must use source_ip 10.10.110.223 which only routes correctly from that host"
    dashboard_config:
      - task: "SSH to cake-shaper and verify fping is installed"
        location: "ssh cake-shaper 'which fping && fping --version'"
      - task: "Transfer phase247-fping-shadow.py to /opt/wanctl/scripts/ on cake-shaper"
        location: "scp scripts/phase247-fping-shadow.py cake-shaper:/opt/wanctl/scripts/"
      - task: "Start overnight soak (nohup or tmux)"
        location: "ssh cake-shaper 'cd /opt/wanctl && nohup .venv/bin/python scripts/phase247-fping-shadow.py > /var/log/wanctl/phase247-shadow.log 2>&1 &'"
      - task: "After ~12h, collect NDJSON output"
        location: "scp cake-shaper:/var/lib/wanctl/phase247-fping-shadow.ndjson .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/"

must_haves:
  truths:
    - "phase247-fping-shadow.py runs on cake-shaper without error and produces NDJSON output"
    - "NDJSON output contains run_start, probe_cycle, and probe_stats records"
    - "Config path on cake-shaper verified before soak: /opt/wanctl/configs/spectrum.yaml exists AND matches the live cake-autorate service config (or the correct path override is used if they differ)"
    - "Before starting the soak, the fixed output path /var/lib/wanctl/phase247-fping-shadow.ndjson is checked: if it already exists from a prior aborted run, the operator must confirm removal OR a timestamped output path is used — runs must NOT be silently appended/mixed"
    - "Route preflight verifies ALL configured reflectors are reachable from source_ip 10.10.110.223 (not just 1.1.1.1)"
    - "Preflight gate is a 2-cycle dry-run that produces at least 2 probe_cycle NDJSON lines (validates config parse, source binding, fping permissions, and write path) — NOT just --help"
    - "Target soak window is overnight ~12h per D-07; runs >=3h but <12h are recorded as PARTIAL evidence, not full overnight soak"
    - "phase247-shadow-summary.json exists with probe_count, soak_duration_h, probe_cycle_count, probe_stats_snapshot_count, and an evidence_completeness field (full|partial)"
    - "phase247-shadow-summary.json includes rtt_p99_ms (full-window RTT p99 from probe_cycle.rtt_ms across SUCCESSFUL records)"
    - "phase247-shadow-summary.json includes probe_elapsed_p99_ms_full_window as a DISTINCT first-class field: the full-window probe-cycle TIMING p99 computed from probe_cycle.elapsed_ms across SUCCESSFUL records only (inferred/dropped records have elapsed_ms=null and are EXCLUDED) — PROF-01 requires both RTT p99 AND cycle-timing p99"
    - "phase247-shadow-summary.json includes per_reflector stats derived from probe_cycle.per_host_results so Phase 248 is self-sufficient without the gitignored raw NDJSON"
    - "Shadow process is explicitly stopped after soak (no orphaned tmux/nohup process left running)"
    - "No controller-path files were modified on cake-shaper (SAFE-18 verifier run before deploy AND after collection)"
  artifacts:
    - path: ".planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-shadow-summary.json"
      provides: "Soak run summary for Phase 248 — probe count, duration, RTT p99, cycle-timing p99 (probe_elapsed_p99_ms_full_window), per-reflector stats, completeness flag, probe_cycle_count (authoritative evidence count for Phase 248 p99 computation)"
      contains: "probe_elapsed_p99_ms_full_window"
  key_links:
    - from: "phase247-fping-shadow.ndjson (on cake-shaper)"
      to: "evidence/phase247-shadow-summary.json"
      via: "rollup script computing rtt_p99_ms and probe_elapsed_p99_ms_full_window from probe_cycle records"
      pattern: "probe_elapsed_p99_ms_full_window"
---

<objective>
Deploy scripts/phase247-fping-shadow.py to cake-shaper and run the overnight soak.
Collect the NDJSON output and produce a summary JSON artifact for Phase 248.

This plan has a human checkpoint because the soak requires SSH access to cake-shaper
and an overnight wait (~12h). Claude handles all automation possible (preflight checks,
summary generation); the operator handles SSH access and timing.

Purpose: PROF-01 — produce the raw RTT sample + probe_cycle data that Phase 248 will
use for the fping vs icmplib distribution comparison. PROF-01 requires BOTH RTT p99 and
cycle-timing p99, so the summary JSON commits two distinct full-window p99 fields:
rtt_p99_ms (from probe_cycle.rtt_ms) and probe_elapsed_p99_ms_full_window (from
probe_cycle.elapsed_ms, successful records only). Phase 248 computes these from probe_cycle
records (not probe_stats rolling snapshots), so the probe_cycle_count in the summary JSON is
the authoritative evidence count.

Output:
  evidence/phase247-shadow-summary.json (committed to planning artifacts)
  evidence/phase247-fping-shadow.ndjson (local copy of soak output, may be large — do not commit to git)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-CONTEXT.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-RESEARCH.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — verify cake-shaper readiness, config path, output-path collision, all-reflector route, run SAFE-18, transfer + 2-cycle dry-run</name>
  <files>(no files_modified — all SSH/remote commands)</files>
  <read_first>
    - scripts/phase247-fping-shadow.py — the script to transfer (just built in Plan 03); confirm it supports a bounded/time-limited run for the dry-run preflight
    - scripts/phase247-safe18-boundary-check.sh — run before deploy to confirm controller tree is clean
    - .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-RESEARCH.md section "Common Pitfalls", "Pitfall 2: Shadow Script Running Against Dev Machine"
    - configs/spectrum.yaml — verify ping_source_ip is 10.10.110.223 (must match cake-shaper's Spectrum WAN interface) and read the full continuous_monitoring.ping_hosts reflector list (needed for the all-reflector route check)
  </read_first>
  <action>
    Run these preflight checks before starting the soak. All commands via bash using
    the `cake-shaper` SSH alias (or whatever alias resolves to the Spectrum gateway host).

    0. Run the SAFE-18 verifier locally BEFORE deploy to confirm the controller tree is clean:
       bash scripts/phase247-safe18-boundary-check.sh
       Expected: exits 0. Do not deploy if SAFE-18 fails.

    1. Verify fping is available on cake-shaper:
       ssh cake-shaper 'which fping && fping --version'
       Expected: /usr/bin/fping, version 5.x

    2. Verify Python and wanctl venv on cake-shaper:
       ssh cake-shaper 'ls /opt/wanctl/src/wanctl/fping_measurement.py'
       ssh cake-shaper '/opt/wanctl/.venv/bin/python -c "from wanctl.fping_measurement import FpingThread; print(\"ok\")"'
       Expected: file exists, Python import succeeds

    3. Verify /var/lib/wanctl/ is writable:
       ssh cake-shaper 'ls -ld /var/lib/wanctl/'
       (if it doesn't exist: ssh cake-shaper 'sudo mkdir -p /var/lib/wanctl/')

    4. Verify source_ip routes correctly from cake-shaper for ALL configured reflectors
       (not just 1.1.1.1 — a single reflector check does not prove the source binding works
       for the full set). Read the reflector list from configs/spectrum.yaml
       continuous_monitoring.ping_hosts (e.g., 1.1.1.1, 9.9.9.9, 208.67.222.222) and check each:
         for each reflector R in ping_hosts:
           ssh cake-shaper "ip route get $R from 10.10.110.223 2>/dev/null | head -1"
       Expected: every reflector resolves via the Spectrum WAN interface (not a blackhole or
       dev lo route). Alternatively confirm reachability from the source IP in one shot:
         ssh cake-shaper 'fping -c 1 -S 10.10.110.223 1.1.1.1 9.9.9.9 208.67.222.222'
       (substitute the actual configured reflector list). If ANY configured reflector fails to
       route from the source IP, stop and diagnose before the soak.

    5a. Verify config path on cake-shaper:
        Check which config file the live service actually loads. The shadow script defaults
        to /opt/wanctl/configs/spectrum.yaml, but the live cake-autorate service may use
        /etc/wanctl/spectrum.yaml. If they differ, the shadow run must be told which path
        to use via --config.

        ssh cake-shaper 'ls -la /opt/wanctl/configs/spectrum.yaml /etc/wanctl/spectrum.yaml 2>/dev/null || echo "missing paths"'

        If both exist, check whether they match:
        ssh cake-shaper 'md5sum /opt/wanctl/configs/spectrum.yaml /etc/wanctl/spectrum.yaml 2>/dev/null'

        If they differ, identify which one the live cake-autorate service uses:
        ssh cake-shaper 'systemctl cat cake-autorate-spectrum.service 2>/dev/null | grep -i config || true'

        Decision rule:
        - If both paths exist and checksums match: no action needed; shadow script default path is fine.
        - If only /opt/wanctl/configs/spectrum.yaml exists: use default.
        - If only /etc/wanctl/spectrum.yaml exists: pass --config /etc/wanctl/spectrum.yaml to the shadow script in all subsequent steps and in the soak launch command.
        - If both exist and differ: use the path the live cake-autorate service explicitly references (from systemctl cat output), and note the discrepancy in the preflight log.

        Log the result of this check before proceeding. If uncertain, stop and ask operator.

    5b. Output-path collision check (MEDIUM-fix): before the soak, confirm the fixed soak
        output file is NOT already present from a prior aborted attempt — appending to a stale
        file would mix runs and corrupt Phase 248's full-window p99 computation:
          ssh cake-shaper 'ls -la /var/lib/wanctl/phase247-fping-shadow.ndjson 2>/dev/null && echo "EXISTS" || echo "absent"'
        Decision rule:
        - If absent: proceed; use the default fixed path.
        - If EXISTS: do NOT silently overwrite or append. Either (a) ask the operator to confirm
          removal (ssh cake-shaper 'rm -f /var/lib/wanctl/phase247-fping-shadow.ndjson'), OR
          (b) use a timestamped output path for this run, e.g.
          --output /var/lib/wanctl/phase247-fping-shadow-$(date -u +%Y%m%dT%H%M%SZ).ndjson,
          and record the chosen path in the preflight log so Task 3 collects the right file.
        Record the resolved soak output path for use in the checkpoint launch command and Task 3.

    5c. Transfer the shadow script:
        scp scripts/phase247-fping-shadow.py cake-shaper:/opt/wanctl/scripts/phase247-fping-shadow.py

    6. PREFLIGHT GATE — 2-cycle dry-run (NOT just --help). This is the real preflight: it
       validates config parse, source binding, fping permissions, and the NDJSON write path
       end-to-end by producing actual sample lines.

       Run a short bounded capture to a throwaway output file. At cadence_sec=10.0, allow
       ~30 seconds so at least 2 fping bursts complete and at least 2 probe_cycle lines are
       written. Use the config path determined in step 5a (add --config if needed):
         ssh cake-shaper 'cd /opt/wanctl && timeout 30 .venv/bin/python \
           scripts/phase247-fping-shadow.py \
           --output /tmp/phase247-preflight.ndjson \
           --stats-interval 1 ; true'
       (timeout sends SIGTERM; the script's shutdown handler writes probe_stats_final and exits 0.
        The trailing `; true` keeps the SSH command from failing on timeout's 124 exit code.)

       Then verify the preflight produced real probe_cycle records:
         ssh cake-shaper 'grep -c "\"type\": \"probe_cycle\"" /tmp/phase247-preflight.ndjson'
       Expected: >= 2 probe_cycle lines. If 0, diagnose (wrong source_ip, fping permission denied,
       config parse failure) and fix BEFORE starting the soak. Do not proceed on a failed dry-run.

       Also verify the run_start record is present:
         ssh cake-shaper 'grep -c "\"type\": \"run_start\"" /tmp/phase247-preflight.ndjson'
       Expected: 1 run_start record (first line in the NDJSON file).

       Clean up the preflight file:
         ssh cake-shaper 'rm -f /tmp/phase247-preflight.ndjson'

    If any preflight check fails, diagnose and fix before proceeding to soak. Do not
    start the soak against the wrong source IP, wrong config path, an existing output file,
    or with a broken import/write path.
  </action>
  <verify>
    <automated>
      bash scripts/phase247-safe18-boundary-check.sh
      scp scripts/phase247-fping-shadow.py cake-shaper:/opt/wanctl/scripts/phase247-fping-shadow.py && echo "transfer ok"
      ssh cake-shaper 'ls -la /opt/wanctl/configs/spectrum.yaml /etc/wanctl/spectrum.yaml 2>/dev/null || echo "missing paths"'
      ssh cake-shaper 'ls -la /var/lib/wanctl/phase247-fping-shadow.ndjson 2>/dev/null && echo "EXISTS — resolve collision" || echo "output path absent ok"'
      ssh cake-shaper 'fping -c 1 -S 10.10.110.223 1.1.1.1 9.9.9.9 208.67.222.222'
      ssh cake-shaper 'cd /opt/wanctl && timeout 30 .venv/bin/python scripts/phase247-fping-shadow.py --output /tmp/phase247-preflight.ndjson --stats-interval 1 ; true'
      ssh cake-shaper 'test "$(grep -c "\"type\": \"probe_cycle\"" /tmp/phase247-preflight.ndjson)" -ge 2 && echo "dry-run produced probe_cycle records" && rm -f /tmp/phase247-preflight.ndjson'
    </automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/phase247-safe18-boundary-check.sh exits 0 before deploy
    - fping confirmed available on cake-shaper (which fping succeeds)
    - Python import of FpingThread succeeds on cake-shaper
    - /var/lib/wanctl/ is writable on cake-shaper
    - ALL configured reflectors (from continuous_monitoring.ping_hosts) route via the Spectrum WAN interface from source_ip 10.10.110.223 (not lo/blackhole); fping -c 1 -S to the full reflector set succeeds
    - Config path verified on cake-shaper: /opt/wanctl/configs/spectrum.yaml exists AND matches the live cake-autorate service config (or the correct --config override is identified and will be passed to the shadow script)
    - Output-path collision resolved: /var/lib/wanctl/phase247-fping-shadow.ndjson is absent, OR operator confirmed removal, OR a timestamped output path was chosen and recorded for the soak
    - Script transferred to /opt/wanctl/scripts/phase247-fping-shadow.py
    - 2-cycle dry-run produces >= 2 probe_cycle NDJSON lines in /tmp/phase247-preflight.ndjson
    - 1 run_start record present as the first line of /tmp/phase247-preflight.ndjson
    - Preflight throwaway file removed after verification
  </acceptance_criteria>
  <done>
    SAFE-18 clean before deploy. fping confirmed on cake-shaper. FpingThread import succeeds.
    /var/lib/wanctl/ writable. source_ip 10.10.110.223 has a valid Spectrum route for ALL
    configured reflectors. Config path on cake-shaper verified (shadow script loads the same
    config as the live cake-autorate service, or the --config override is confirmed). Soak output
    path collision resolved (absent, removed, or timestamped). Script transferred. The 2-cycle
    dry-run wrote >= 2 probe_cycle lines and 1 run_start line (config parse, source binding, fping
    permissions, write path all validated). Preflight file cleaned up.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Shadow script transferred and validated on cake-shaper via a 2-cycle dry-run (real
    probe_cycle records produced, run_start record confirmed). Config path verified — shadow
    script loads the same config as the live cake-autorate service. All configured reflectors
    confirmed routable from the source IP. Soak output path collision resolved. SAFE-18 confirmed
    clean. Operator must start the overnight soak and wait ~12h before returning.
  </what-built>
  <how-to-verify>
    Start the soak in a tmux session or with nohup on cake-shaper. Use the config path
    confirmed in Task 1 (add --config /etc/wanctl/spectrum.yaml if that was the confirmed path)
    and the resolved soak output path from step 5b (default fixed path, or the timestamped path
    if a collision was found).
    Recommended:

    Option A — tmux (preferred, allows monitoring):
      ssh cake-shaper
      tmux new-session -s phase247-shadow
      cd /opt/wanctl
      .venv/bin/python scripts/phase247-fping-shadow.py \
        --output /var/lib/wanctl/phase247-fping-shadow.ndjson \
        --stats-interval 100
      # Detach with Ctrl-B D. Reattach later with: tmux attach -t phase247-shadow

    Option B — nohup (background, no monitoring):
      ssh cake-shaper "cd /opt/wanctl && nohup .venv/bin/python \
        scripts/phase247-fping-shadow.py \
        --output /var/lib/wanctl/phase247-fping-shadow.ndjson \
        --stats-interval 100 \
        > /var/log/wanctl/phase247-shadow.log 2>&1 &"
      # Note the PID printed; you'll need it to stop the run cleanly.

    Target soak window: 12h (overnight) per D-07 — spans idle and peak traffic patterns.
    Minimum usable: 3h (captures at least 1080 bursts, ~10 probe_stats snapshots) but a
    run <12h is PARTIAL evidence, not the full overnight soak target. Aim for the full 12h.

    Check progress (any time):
      ssh cake-shaper 'wc -l /var/lib/wanctl/phase247-fping-shadow.ndjson'
      ssh cake-shaper 'grep -c "probe_cycle" /var/lib/wanctl/phase247-fping-shadow.ndjson'
      ssh cake-shaper 'tail -5 /var/lib/wanctl/phase247-fping-shadow.ndjson'

    The live system (cake-autorate state bridges + steering) will continue running normally.
    The shadow script is read-only and does not interact with any wanctl service.

    STOP THE SOAK CLEANLY when done (do not leave an orphaned process):
      tmux case: reattach (tmux attach -t phase247-shadow), press Ctrl-C to send SIGINT,
        wait for the script to write probe_stats_final and exit, then: tmux kill-session -t phase247-shadow
      nohup case: ssh cake-shaper 'kill -INT <PID>' (SIGINT, NOT kill -9 — the handler must
        run to write probe_stats_final). Confirm exit: ssh cake-shaper 'ps aux | grep phase247-fping-shadow | grep -v grep'
        should return nothing.
    The final NDJSON line should be type="probe_stats_final".
  </how-to-verify>
  <resume-signal>Type "soak complete" when the soak has run, the process is stopped cleanly, and output is ready to collect. Note the approximate soak duration so completeness (full vs partial) can be recorded, and the exact output path used.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Collect NDJSON output, produce summary JSON (RTT p99 + cycle-timing p99 + per-reflector stats), re-run SAFE-18</name>
  <files>.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-shadow-summary.json</files>
  <read_first>
    - .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/ directory (check what files exist after soak)
    - scripts/phase247-safe18-boundary-check.sh — re-run after collection to confirm no controller drift
  </read_first>
  <action>
    After human confirms soak is complete and the process is stopped cleanly:

    1. Collect the NDJSON output from cake-shaper (use the exact output path recorded in Task 1
       step 5b — default fixed path or timestamped path):
       scp cake-shaper:/var/lib/wanctl/phase247-fping-shadow.ndjson \
         .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-fping-shadow.ndjson
       Note: do NOT git-add this file — it will be large (thousands of lines) and is an
       evidence artifact for local analysis only. Ensure the evidence/ dir has a .gitignore
       entry for *.ndjson (add one if absent) so the raw file is not accidentally committed.

    2. Produce the summary JSON using a short Python script that reads the NDJSON and computes
       the distribution fields. The summary must contain:
         - "schema_version": 1
         - "phase": "247"
         - "collected_at": ISO8601 UTC timestamp now
         - "ndjson_path": (relative path of the NDJSON file)
         - "ndjson_sha256": (sha256 of the raw NDJSON file, for provenance)
         - "total_lines": (wc -l of the file)
         - "run_start_count": (count of lines where type=="run_start"; should be 1)
         - "probe_cycle_count": (count of lines where type=="probe_cycle"; this is the authoritative burst count for Phase 248 p99 computation — includes successful, cached-all-loss, and inferred-dropped records)
         - "probe_cycle_observed_count": (count of probe_cycle lines where inferred==false; the real observed bursts)
         - "probe_cycle_inferred_count": (count of probe_cycle lines where inferred==true; dropped bursts with elapsed_ms=null)
         - "probe_stats_snapshot_count": (count of lines where type=="probe_stats" or "probe_stats_final")
         - "probe_cycle_success_count": (count of probe_cycle lines where success==true)
         - "probe_cycle_failure_count": (count of probe_cycle lines where success==false, including all_loss and dropped)
         - "soak_start_ts": (ts field of first probe_cycle line, wall-clock)
         - "soak_end_ts": (ts field of last probe_cycle line, wall-clock)
         - "soak_duration_h": ((end_ts - start_ts) / 3600, rounded to 2 decimal places)
         - "evidence_completeness": "full" if soak_duration_h >= 12.0 else "partial"
         - "source_ip": (from the run_start record in the NDJSON)
         - "rtt_p50_ms": (median of rtt_ms values across all SUCCESSFUL probe_cycle records; success==true, rtt_ms not null)
         - "rtt_p99_ms": (99th percentile of rtt_ms values across all SUCCESSFUL probe_cycle records — FULL-WINDOW RTT p99 from raw per-burst data, not the rolling-window probe_stats p99)
         - "probe_elapsed_p50_ms": (median of elapsed_ms values across all SUCCESSFUL probe_cycle records; exclude inferred/dropped records whose elapsed_ms is null)
         - "probe_elapsed_p99_ms_full_window": (HIGH-fix — 99th percentile of elapsed_ms values across all SUCCESSFUL probe_cycle records ONLY. This is the cycle-TIMING p99 PROF-01 requires, DISTINCT from rtt_p99_ms. Inferred/dropped records have elapsed_ms=null and MUST be excluded from this computation — including them would corrupt the timing distribution.)
         - "probe_elapsed_count": (number of SUCCESSFUL records contributing to the elapsed_ms percentiles; should equal probe_cycle_success_count and excludes inferred records)
         - "per_reflector": (MEDIUM-fix — a dict keyed by reflector IP, each value containing
             {"sample_count": int, "loss_count": int (per_host_results value null), "rtt_p50_ms": float|null, "rtt_p99_ms": float|null}
             computed by walking probe_cycle.per_host_results across all probe_cycle records.
             This makes Phase 248 self-sufficient for reflector-specific diagnostics without the
             gitignored raw NDJSON.)
         - "probe_stats_p99_ms_final": (p99_ms from the last probe_stats_final record, if present — retained for comparison ONLY; note this covers only the last rolling window, NOT full-soak — do NOT use for the PROF-01 cycle-timing finding)
         - "probe_count_final": (probe_count_at_snapshot from the last probe_stats_final record)

       Percentile method: use nearest-rank on the sorted value list (document the method in a
       "percentile_method": "nearest_rank" field) so Phase 248 stays consistent.

       Write the summary to:
         .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-shadow-summary.json

       Commit the summary JSON (not the raw NDJSON). The summary is the Phase 247 PROF-01
       evidence artifact that Phase 248 will reference. BOTH rtt_p99_ms (RTT distribution) and
       probe_elapsed_p99_ms_full_window (cycle-timing distribution), computed from probe_cycle
       records, are the authoritative full-soak PROF-01 outputs for Phase 248 to use.

    3. Run the SAFE-18 verifier one more time at phase close to confirm no controller drift:
       bash scripts/phase247-safe18-boundary-check.sh
       Expected: exits 0 (overwrites the existing safe18-boundary-247.json with fresh proof)
  </action>
  <verify>
    <automated>
      python3 -c "import json; d=json.load(open('.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-shadow-summary.json')); assert d['probe_cycle_count'] > 0; assert d['probe_stats_snapshot_count'] > 0; assert d['soak_duration_h'] >= 3.0; assert d['evidence_completeness'] in ('full','partial'); assert 'ndjson_sha256' in d; assert 'rtt_p99_ms' in d; assert 'probe_elapsed_p99_ms_full_window' in d; assert isinstance(d['per_reflector'], dict) and len(d['per_reflector']) > 0; print('summary ok')"
      bash scripts/phase247-safe18-boundary-check.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - phase247-shadow-summary.json exists and parses as JSON
    - probe_cycle_count > 0 (authoritative burst count for Phase 248 p99 computation)
    - probe_cycle_observed_count and probe_cycle_inferred_count present (inferred = dropped bursts with null elapsed_ms)
    - probe_stats_snapshot_count > 0
    - soak_duration_h >= 3.0
    - evidence_completeness is "full" (>=12h) or "partial" (<12h) and matches soak_duration_h
    - ndjson_sha256 present (provenance)
    - rtt_p99_ms present (full-window RTT p99 from probe_cycle records)
    - probe_elapsed_p99_ms_full_window present (full-window cycle-TIMING p99 from probe_cycle.elapsed_ms, successful records only — DISTINCT from rtt_p99_ms; inferred records excluded)
    - probe_elapsed_count present and equals probe_cycle_success_count (inferred records excluded from timing percentiles)
    - per_reflector dict present and non-empty (per-reflector sample_count, loss_count, rtt p50/p99 from per_host_results)
    - percentile_method field present (e.g., "nearest_rank")
    - probe_stats_p99_ms_final retained as rolling-window comparison only (clearly distinct from the full-window fields)
    - run_start_count == 1 (provenance metadata record present)
    - raw .ndjson is NOT staged for commit (covered by evidence/.gitignore *.ndjson)
    - bash scripts/phase247-safe18-boundary-check.sh exits 0 after collection
  </acceptance_criteria>
  <done>
    phase247-shadow-summary.json committed with probe_cycle_count > 0 (authoritative burst
    evidence count for Phase 248), observed/inferred split, probe_stats_snapshot_count > 0,
    soak_duration_h >= 3.0, an evidence_completeness flag (full/partial), ndjson_sha256 provenance,
    rtt_p99_ms (full-window RTT p99), probe_elapsed_p99_ms_full_window (full-window cycle-timing p99
    from successful probe_cycle.elapsed_ms, inferred records excluded), per_reflector stats
    (Phase 248 self-sufficient without raw NDJSON), and probe_stats_p99_ms_final (rolling-window
    p99 retained for comparison only). Raw NDJSON not committed. SAFE-18 verifier exits 0.
    Phase 247 PROF-01 evidence is committed and ready for Phase 248 distribution analysis.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → cake-shaper SSH | Script transfer via scp; cake-shaper is an operator-controlled host |
| cake-shaper → internet reflectors | fping sends ICMP probes to the configured reflectors from source_ip 10.10.110.223; read-only, no inbound connections opened |
| cake-shaper → local filesystem | NDJSON written to /var/lib/wanctl/ (same path as existing wanctl state files) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-247-04-01 | Tampering | Live wanctl services during soak | accept | Script is read-only; no systemd units, no DB writes, no RouterOS calls; cake-autorate state bridges continue unaffected; SAFE-18 verifier run before deploy and after collection |
| T-247-04-02 | Denial of Service | fping ICMP flood | accept | count=5, period_ms=200, cadence_sec=10 → 0.5 probes/sec per reflector; negligible compared to existing icmplib probe rate |
| T-247-04-03 | Information Disclosure | RTT timing data in NDJSON | accept | Written to local filesystem; same sensitivity as existing wanctl state files; not committed to git (large raw file, gitignored) |
| T-247-04-04 | Denial of Service | Orphaned shadow process after soak | mitigate | Checkpoint instructions require clean SIGINT stop (tmux Ctrl-C or kill -INT PID) and a ps confirmation that no shadow process remains |
| T-247-04-05 | Tampering | Config path mismatch (shadow runs different config than live service) | mitigate | Task 1 step 5a explicitly verifies /opt/wanctl/configs/spectrum.yaml vs /etc/wanctl/spectrum.yaml; checksums both if both exist; uses systemctl cat to confirm which the live service reads; passes --config override if needed |
| T-247-04-06 | Tampering | Stale output file mixing two soak runs | mitigate | Task 1 step 5b checks for an existing /var/lib/wanctl/phase247-fping-shadow.ndjson; requires operator-confirmed removal or a timestamped output path before the soak; prevents append/mix corruption of the full-window p99 |
| T-247-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages; scp transfer of a single Python script only |
</threat_model>

<verification>
After soak:
  wc -l .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/phase247-fping-shadow.ndjson   # > 1000 lines expected for 3h+ soak
  python3 -c "import json; [json.loads(l) for l in open('...evidence/phase247-fping-shadow.ndjson')]"              # all lines parse as valid JSON
  python3 -c "import json; d=json.load(open('...evidence/phase247-shadow-summary.json')); print(d)"                # summary readable, has evidence_completeness + ndjson_sha256 + rtt_p99_ms + probe_elapsed_p99_ms_full_window + per_reflector
  bash scripts/phase247-safe18-boundary-check.sh                                                                    # exits 0 (SAFE-18 clean)
  ssh cake-shaper 'ps aux | grep phase247-fping-shadow | grep -v grep'                                              # empty (no orphaned process)
</verification>

<success_criteria>
Overnight soak completed (target 12h per D-07; >=3h recorded as partial evidence).
phase247-shadow-summary.json committed with positive probe_cycle_count (authoritative burst
count for Phase 248), an observed/inferred split, probe_stats_snapshot_count, soak_duration_h >= 3.0,
an evidence_completeness flag, ndjson_sha256 provenance, rtt_p99_ms (full-window RTT p99) AND
probe_elapsed_p99_ms_full_window (full-window cycle-timing p99 from successful probe_cycle.elapsed_ms,
inferred records excluded — the two distinct PROF-01 outputs), per_reflector stats (Phase 248
self-sufficient without the gitignored raw NDJSON), and probe_stats_p99_ms_final (rolling-window
p99 retained for comparison only). Shadow process stopped cleanly (no orphan). Soak output path
collision resolved before the run. All configured reflectors confirmed routable from the source IP.
Config path on cake-shaper verified before soak. SAFE-18 verifier confirms zero diff in controller
files before deploy and after collection. Phase 247 is complete; Phase 248 can begin distribution
analysis using probe_cycle records.
</success_criteria>

<output>
Create .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-04-SUMMARY.md when done.
</output>
</content>
