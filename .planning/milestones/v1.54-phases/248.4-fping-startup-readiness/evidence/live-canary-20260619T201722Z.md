# Phase 248.4 Live Canary Evidence — 2026-06-19T201722Z

## Objective

Verify the native fping startup first-sample readiness fix under live Spectrum ownership, then roll back to external cake-autorate + `icmplib`.

## Deployed code proof

Patched controller deployed to `/opt/wanctl/wan_controller.py` with backup:

```text
/var/lib/wanctl/canary-backups/wan_controller.py.pre-phase2484-20260619T201640Z
```

Hashes:

```text
50c7322e0f962077e7da1cbc98e5490a8e06258e630231c0eaa9f1eb3eaed359  /opt/wanctl/wan_controller.py
d97366adbff01fb05919a810d0f2c4e7556934e62816c3dcdc048e551b9901b8  /var/lib/wanctl/canary-backups/wan_controller.py.pre-phase2484-20260619T201640Z
startup_fix_present=True
```

## Pre-state

```text
cake-autorate-spectrum.service=active
cake-autorate-spectrum-state-bridge.service=active
steering.service=active
wanctl@spectrum.service=inactive
```

Config backup:

```text
/etc/wanctl/spectrum.yaml.pre-phase2484-startup-20260619T201722Z
```

Canary config proof:

```text
backend=fping parity_config=true
```

The live config retained Phase 248.3 parity values:

- CAKE `rtt 25ms`
- download ceiling 600M
- download no-ack-filter / upload ack-filter by direction default

## Native canary observation

After switching ownership to native `wanctl@spectrum.service`, sampled startup every 2s for 20s.

At t=2s:

```text
NRestarts=0
ExecMainStatus=0
ActiveState=active
SubState=running
health=healthy backend=fping active=fping fallback=0 stale=0.046 raw=29.2 p99=0.9 overrun=0
INFO spectrum: Waiting for initial fping RTT sample; freezing startup cycle instead of running fallback checks
```

Selected later samples:

```text
--- t=4s ---
health=healthy backend=fping active=fping fallback=0 stale=2.118 raw=29.2 p99=1.9 overrun=0

--- t=10s ---
health=healthy backend=fping active=fping fallback=0 stale=8.353 raw=29.2 p99=1.9 overrun=0

--- t=14s ---
health=healthy backend=fping active=fping fallback=0 stale=1.665 raw=25.6 p99=1.8 overrun=0

--- t=20s ---
health=healthy backend=fping active=fping fallback=0 stale=7.887 raw=25.6 p99=1.8 overrun=0
```

Final native state:

```text
cake-autorate-spectrum.service=inactive
cake-autorate-spectrum-state-bridge.service=inactive
steering.service=active
wanctl@spectrum.service=active
NRestarts=0
ExecMainStatus=0
ActiveState=active
SubState=running
status=healthy backend=fping active=fping fallback=0 stale=7.99 raw=25.6 p99=1.8 overrun=0
spec-modem: 18Mbit, wash, ack-filter, rtt 25ms
spec-router: 600Mbit, wash, no-ack-filter, rtt 25ms
```

Gate scan:

```text
wait_count=1
no_data_count=0
stale_count=0
fallback_warn_count=0
restarts=0
```

Interpretation:

- The intended readiness message fired once.
- The old startup warnings were suppressed.
- No fallback checks ran during startup.
- fping published and remained active.
- No stale-window regression.
- No service restart.
- Cycle budget stayed healthy.

## Rollback / final live state

Rollback restored external cake-autorate ownership and `measurement.backend: icmplib`.

Rollback proof:

```text
rollback_backend=icmplib
rollback_rtt=25ms
rollback_dl_ceiling=600
cake-autorate-spectrum.service=active
cake-autorate-spectrum-state-bridge.service=active
steering.service=active
wanctl@spectrum.service=inactive
healthy cake-autorate-state-bridge cake-autorate-trial
spec-modem: 18Mbit, wash, ack-filter, rtt 25ms
spec-router: 550Mbit, wash, no-ack-filter, rtt 25ms
```

Independent final check:

```text
cake-autorate-spectrum.service=active
cake-autorate-spectrum-state-bridge.service=active
steering.service=active
wanctl@spectrum.service=inactive
backend=icmplib
rtt=25ms
dl_ceiling=600
healthy cake-autorate-state-bridge cake-autorate-trial
live_wan_controller_sha256=50c7322e0f962077e7da1cbc98e5490a8e06258e630231c0eaa9f1eb3eaed359
```

## Verdict

Phase 248.4 passes.

The startup first-sample fallback blocker is fixed for native fping. A future keep/default-flip canary no longer has the two known blockers from Phases 248.2–248.4:

- stale-window mismatch: fixed in Phase 248.2;
- native/external CAKE parity mismatch: fixed in Phase 248.3;
- startup first-sample fallback noise: fixed in Phase 248.4.

Permanent native fping keep still needs a deliberately approved keep canary, but the known mechanical blockers are now closed.
