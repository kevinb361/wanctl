---
phase: 124-production-validation
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - pyproject.toml
  - src/wanctl/__init__.py
  - CHANGELOG.md
  - CLAUDE.md
autonomous: true
requirements: [VALN-01, VALN-02]

must_haves:
  truths:
    - "pyproject.toml version is 1.24.0"
    - "__init__.py __version__ is 1.24.0"
    - "CHANGELOG.md has a [1.24.0] section with hysteresis entries for phases 121-124"
    - "CLAUDE.md version reference says v1.24.0"
    - "CLAUDE.md Known Issues section updated (EWMA flapping entry resolved)"
    - "Git tag v1.24 exists on the release commit"
    - "Tag and commits pushed to origin"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package version 1.24.0"
      contains: 'version = "1.24.0"'
    - path: "src/wanctl/__init__.py"
      provides: "Runtime version 1.24.0"
      contains: '__version__ = "1.24.0"'
    - path: "CHANGELOG.md"
      provides: "v1.24.0 release notes with hysteresis entries"
      contains: "[1.24.0]"
    - path: "CLAUDE.md"
      provides: "Updated version reference"
      contains: "v1.24.0"
  key_links:
    - from: "pyproject.toml version"
      to: "__init__.py __version__"
      via: "both must be 1.24.0"
      pattern: "1\\.24\\.0"
    - from: "CHANGELOG.md [Unreleased]"
      to: "CHANGELOG.md [1.24.0]"
      via: "move unreleased to versioned section"
      pattern: "\\[1\\.24\\.0\\]"
---

<objective>
Version bump to 1.24.0, update CHANGELOG with hysteresis entries, update CLAUDE.md, git tag v1.24, push to origin. Closes the v1.24 milestone.

Purpose: D-04 requires bundling all release artifacts into this phase after production validation passes
Output: Tagged v1.24 release pushed to origin
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-production-validation/124-CONTEXT.md
@.planning/phases/124-production-validation/124-01-SUMMARY.md

<interfaces>
From pyproject.toml line 3:
```
version = "1.20.0"
```

From src/wanctl/__init__.py line 3:
```
__version__ = "1.23.0"
```

From CHANGELOG.md structure:
```
## [Unreleased]
### Fixed
- **Spike detector confirmation counter** - ...
### Analysis Record (2026-03-30, 44h post-deploy)
- ...

## [1.23.0] - 2026-03-27
```

From CLAUDE.md:
```
**Version:** 1.23.0
**Current:** v1.23.0 (Self-Optimizing Controller)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump and CHANGELOG update</name>
  <files>pyproject.toml, src/wanctl/__init__.py, CHANGELOG.md</files>
  <read_first>
    - pyproject.toml
    - src/wanctl/__init__.py
    - CHANGELOG.md
    - .planning/phases/124-production-validation/124-01-SUMMARY.md
  </read_first>
  <action>
**1. Bump pyproject.toml** (line 3):
Change `version = "1.20.0"` to `version = "1.24.0"` (per D-04, pyproject.toml is 3 versions behind)

**2. Bump src/wanctl/__init__.py** (line 3):
Change `__version__ = "1.23.0"` to `__version__ = "1.24.0"`

**3. Update CHANGELOG.md:**

The `## [Unreleased]` section currently contains the spike detector confirmation counter fix and analysis record. These belong to v1.23.1 work but were never versioned. Per D-04, move them into the new [1.24.0] section alongside the hysteresis entries.

Replace the current `## [Unreleased]` block (everything from `## [Unreleased]` down to but not including `## [1.23.0]`) with:

```markdown
## [Unreleased]

## [1.24.0] - 2026-MM-DD

**EWMA Boundary Hysteresis** - Eliminates GREEN/YELLOW flapping at the EWMA threshold boundary
during prime-time DOCSIS load. Dwell timer + deadband margin on both download and upload state
machines, YAML-configurable with SIGUSR1 hot-reload, health endpoint observability.
4 phases (121-124), 5 plans, 11/11 requirements satisfied. ~3,900+ tests.

### Added

- **Dwell timer on state transitions** (Phase 121) - Require N consecutive above-threshold cycles
  before GREEN->YELLOW transition, preventing single-cycle EWMA jitter from triggering rate clamping
  - `dwell_cycles` parameter (default 3, range 0-20) in `continuous_monitoring.thresholds`
  - Counter resets to zero when delta drops below threshold, ensuring only sustained congestion triggers
  - Applied to both download (4-state) and upload (3-state) machines identically
- **Deadband margin on recovery transitions** (Phase 121) - YELLOW->GREEN requires delta dropping below
  (target_bloat_ms - deadband_ms), preventing oscillation at the exact threshold boundary
  - `deadband_ms` parameter (default 3.0, range 0.0-20.0) in `continuous_monitoring.thresholds`
  - Split threshold: higher to enter YELLOW, lower to exit back to GREEN
- **Hysteresis configuration** (Phase 122) - YAML config with sensible defaults and SIGUSR1 hot-reload
  - Parameters under `continuous_monitoring.thresholds`: `dwell_cycles`, `deadband_ms`
  - SIGUSR1 reloads hysteresis params alongside existing dry_run/fusion/wan_state chain
  - Defaults work without config changes; `dwell_cycles: 0` disables hysteresis (escape hatch)
- **Hysteresis observability** (Phase 123) - Health endpoint exposure and suppression logging
  - Health endpoint `/health` includes `hysteresis` section: `dwell_counter`, `deadband_margins`,
    `transitions_suppressed` count
  - Log message on suppressed transitions: "transition suppressed, dwell N/M"

### Fixed

- **Spike detector confirmation counter** - Require `accel_confirm_cycles` (default 3) consecutive
  spike cycles before forcing RED state, eliminating false positives from DOCSIS cable jitter
  - Root cause: `accel_threshold_ms: 12` triggered on single-sample RTT jitter (9,225 false
    positives/hr during idle on Spectrum cable at prime-time). Real congestion produces zero
    spike triggers -- it builds gradually through the EWMA.
  - DOCSIS cable jitter profile (idle, DL=940M): p90=21ms, p95=28ms, p99=44ms cycle-to-cycle
    delta at 50ms resolution. No single threshold eliminates false positives without also
    missing genuine events.
  - Fix: `_spike_streak` counter tracks consecutive spike cycles. Only forces RED after 3
    consecutive cycles (150ms at 50ms/cycle). Single-sample jitter resets counter to zero.
  - New config param `accel_confirm_cycles` (int, default 3, range 1-10) in
    `continuous_monitoring.thresholds`. No YAML changes needed -- default takes effect.

### Analysis Record (2026-MM-DD, production validation)

- Include soak results from Plan 01 SUMMARY (read 124-01-SUMMARY.md for actual data)
- Record: duration, flapping alerts observed (should be 0), transitions_suppressed count, RRUL timing
```

Replace `2026-MM-DD` with today's actual date when executing. Use soak evidence from 124-01-SUMMARY.md for the analysis record.
  </action>
  <verify>
    <automated>grep -n 'version.*1\.24\.0' pyproject.toml && grep -n '__version__.*1\.24\.0' src/wanctl/__init__.py && grep -c '\[1\.24\.0\]' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `version = "1.24.0"` on line 3
    - src/wanctl/__init__.py contains `__version__ = "1.24.0"`
    - CHANGELOG.md has `## [1.24.0]` section between `## [Unreleased]` and `## [1.23.0]`
    - CHANGELOG.md [Unreleased] section is empty (all content moved to [1.24.0])
    - CHANGELOG.md [1.24.0] section includes entries for phases 121-124 (dwell timer, deadband, config, observability)
    - CHANGELOG.md [1.24.0] section includes the spike detector fix (moved from old Unreleased)
    - CHANGELOG.md [1.24.0] section includes analysis record with soak evidence
  </acceptance_criteria>
  <done>All three version files updated to 1.24.0. CHANGELOG has complete release notes.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and tag release</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md
    - pyproject.toml
    - src/wanctl/__init__.py
  </read_first>
  <action>
**1. Update CLAUDE.md version references:**

Change line 10 `**Version:** 1.23.0` to `**Version:** 1.24.0`

Change the `## Version` section (near bottom):
- `**Current:** v1.23.0 (Self-Optimizing Controller)` becomes `**Current:** v1.24.0 (EWMA Boundary Hysteresis)`
- Replace the bullet list under Version with v1.24 features:
  - Dwell timer (dwell_cycles=3 default) absorbs transient EWMA threshold crossings
  - Deadband margin (deadband_ms=3.0 default) prevents oscillation at exact boundary
  - YAML-configurable under continuous_monitoring.thresholds with SIGUSR1 hot-reload
  - Health endpoint hysteresis section (dwell_counter, deadband_margins, transitions_suppressed)
  - Spike detector confirmation counter (accel_confirm_cycles) from v1.23.1 fix
  - Update test count from "3,928 unit tests" to current count (run pytest --co -q | tail -1 to get count)

**2. Update Known Issues section:**

The EWMA boundary flapping entry is now resolved by this release. Remove the current bullet about EWMA boundary flapping. If validation passed perfectly (per 124-01-SUMMARY.md), replace with no known issues note or leave empty. Use Claude's discretion on wording.

**3. Git commit all changes (version bump + CHANGELOG + CLAUDE.md):**
```bash
git add pyproject.toml src/wanctl/__init__.py CHANGELOG.md CLAUDE.md
git commit -m "release: v1.24.0 EWMA Boundary Hysteresis"
```

**4. Create annotated git tag:**
```bash
git tag -a v1.24 -m "v1.24.0 - EWMA Boundary Hysteresis

Eliminates GREEN/YELLOW flapping at EWMA threshold boundary during prime-time
DOCSIS load via dwell timer + deadband margin on both DL and UL state machines.

Phases 121-124, 5 plans, 11/11 requirements."
```

**5. Push commits and tag to origin:**
```bash
git push origin main
git push origin v1.24
```
  </action>
  <verify>
    <automated>grep -n 'v1\.24\.0' CLAUDE.md && git tag -l 'v1.24' && git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md line 10 says `**Version:** 1.24.0`
    - CLAUDE.md Version section says `**Current:** v1.24.0 (EWMA Boundary Hysteresis)`
    - CLAUDE.md Known Issues no longer mentions EWMA boundary flapping as open issue
    - Git tag v1.24 exists
    - Latest commit message contains "v1.24.0"
    - Tag and commits pushed to origin (git log --oneline origin/main shows the release commit)
  </acceptance_criteria>
  <done>CLAUDE.md updated, release committed, v1.24 tag created and pushed. Milestone complete.</done>
</task>

</tasks>

<verification>
- `grep 'version.*1\.24\.0' pyproject.toml` returns match
- `grep '__version__.*1\.24\.0' src/wanctl/__init__.py` returns match
- `grep '\[1\.24\.0\]' CHANGELOG.md` returns match
- `grep 'v1\.24\.0' CLAUDE.md` returns match
- `git tag -l 'v1.24'` returns v1.24
- `git log origin/main --oneline -1` shows release commit
</verification>

<success_criteria>
v1.24.0 released: version bumped in all 4 files, CHANGELOG has complete hysteresis entries with soak evidence, CLAUDE.md updated, git tagged v1.24, pushed to origin. v1.24 milestone complete.
</success_criteria>

<output>
After completion, create `.planning/phases/124-production-validation/124-02-SUMMARY.md`
</output>
