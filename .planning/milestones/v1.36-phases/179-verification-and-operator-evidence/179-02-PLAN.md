---
phase: 179-verification-and-operator-evidence
plan: 02
type: execute
wave: 2
depends_on:
  - 179-01-PLAN.md
files_modified:
  - .planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
requirements: [OPER-04]
autonomous: true
tags: [production, history, topology, operators]

must_haves:
  truths:
    - "Live `wanctl-history` follows the per-WAN DB discovery path when per-WAN DBs exist"
    - "Live `/metrics/history` follows the same topology and keeps the documented response envelope"
    - "Direct SQLite spot checks distinguish the shared steering DB from the per-WAN autorate DBs"
  artifacts:
    - path: ".planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md"
      provides: "live reader-topology and retained-window evidence"
      contains: "/metrics/history"
  key_links:
    - from: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md"
      to: ".planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md"
      via: "executed live reader proof path"
      pattern: "wanctl-history"
---

<objective>
Prove on the deployed host that the supported history readers follow the intended DB topology and that retained-history shape is consistent with the Phase 178 storage change.

Purpose: repo-side code says the readers prefer per-WAN DBs; this plan proves that the live deployment behaves the same way.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/179-verification-and-operator-evidence/179-RESEARCH.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
@src/wanctl/history.py
@src/wanctl/health_check.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live CLI and HTTP history-reader behavior</name>
  <files>.planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md</files>
  <read_first>
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
    - src/wanctl/history.py
    - src/wanctl/health_check.py
  </read_first>
  <action>
    Create `179-live-reader-topology-report.md` and record:

    - live `wanctl-history` output for a recent time window
    - live `/metrics/history` output for a recent time window
    - evidence that both readers follow the per-WAN DB set when present

    Keep the checks production-safe and read-only.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'wanctl-history --last 1h --metrics wanctl_rtt_ms --json | python3 -m json.tool | head -n 40' && ssh -o BatchMode=yes kevin@10.10.110.223 'curl -s http://127.0.0.1:9101/metrics/history?range=1h&limit=5 | python3 -m json.tool'</automated>
  </verify>
  <acceptance_criteria>
    - the report includes evidence from both supported readers
    - the report states whether the readers use the per-WAN DB set as intended
    - the HTTP response envelope is shown to remain compatible
  </acceptance_criteria>
  <done>Live CLI and HTTP reader behavior is captured against the authoritative topology.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check retained-window shape and steering separation directly</name>
  <files>.planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md</files>
  <read_first>
    - .planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
  </read_first>
  <action>
    Extend the report with read-only direct SQLite spot checks for:

    - `metrics-spectrum.db`
    - `metrics-att.db`
    - `metrics.db`

    Use these checks to confirm:

    - per-WAN DBs still hold the autorate retained window
    - the shared DB still reflects steering activity separately
    - the retained shape is consistent with the post-Phase-178 retention profile
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 -json /var/lib/wanctl/metrics-spectrum.db "SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp ASC LIMIT 1; SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp DESC LIMIT 1;" && echo --- && sudo -n sqlite3 -json /var/lib/wanctl/metrics-att.db "SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp ASC LIMIT 1; SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp DESC LIMIT 1;" && echo --- && sudo -n sqlite3 -json /var/lib/wanctl/metrics.db "SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp ASC LIMIT 1; SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp DESC LIMIT 1;"'</automated>
  </verify>
  <acceptance_criteria>
    - the report distinguishes per-WAN retained-window evidence from shared steering DB evidence
    - the retained shape is described in terms of raw vs aggregate data where possible
    - no mutating production commands are used
  </acceptance_criteria>
  <done>Direct DB spot checks confirm live reader evidence and steering separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live reader output -> operator trust | if the proof path is incomplete, operators may trust the wrong reader surface |
| direct SQLite checks -> topology interpretation | mixing shared and per-WAN evidence could produce a false conclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-03 | I (Integrity) | live reader proof | mitigate | capture evidence from both supported readers, not only one |
| T-179-04 | I (Integrity) | topology interpretation | mitigate | record per-WAN and shared DB spot checks separately and describe their roles explicitly |
| T-179-05 | T (Tampering) | production DB checks | mitigate | use read-only CLI, HTTP, and sqlite3 commands only |
</threat_model>

<verification>
- `ssh -o BatchMode=yes kevin@10.10.110.223 'wanctl-history --last 1h --metrics wanctl_rtt_ms --json | python3 -m json.tool | head -n 40'`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'curl -s http://127.0.0.1:9101/metrics/history?range=1h&limit=5 | python3 -m json.tool'`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 -json /var/lib/wanctl/metrics-spectrum.db "SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp ASC LIMIT 1; SELECT timestamp, granularity, metric_name FROM metrics ORDER BY timestamp DESC LIMIT 1;"'`
- `rg -n 'wanctl-history|/metrics/history|metrics-spectrum\\.db|metrics-att\\.db|metrics\\.db' .planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md`
</verification>

<success_criteria>
After this plan, the milestone has live proof that the supported history readers and direct DB spot checks all tell the same storage-topology story on the deployed host.
</success_criteria>

<output>
After completion, update `.planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md` and create `.planning/phases/179-verification-and-operator-evidence/179-02-SUMMARY.md`
</output>
