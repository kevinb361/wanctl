---
phase: 179-verification-and-operator-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md
requirements: [OPER-04]
autonomous: true
tags: [production, storage, evidence, footprint]

must_haves:
  truths:
    - "Live per-WAN DB sizes are captured against the 2026-04-13 baseline rather than described qualitatively"
    - "Live `storage.status` is recorded from supported operator surfaces"
    - "The shared steering DB is included in the inventory without being confused with the per-WAN autorate DBs"
  artifacts:
    - path: ".planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md"
      provides: "post-Phase-178 live storage footprint evidence"
      contains: "2026-04-13 baseline"
  key_links:
    - from: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-VERIFICATION.md"
      to: ".planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md"
      via: "deferred production footprint proof"
      pattern: "metrics-spectrum.db"
---

<objective>
Capture post-Phase-178 live DB sizes and storage status, then compare them directly to the 2026-04-13 baseline.

Purpose: Phase 178 changed the retention profile in repo/config, but Phase 179 must prove whether the deployed footprint actually dropped.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/179-verification-and-operator-evidence/179-RESEARCH.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-VERIFICATION.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
@scripts/soak-monitor.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live DB file sizes and compare to baseline</name>
  <files>.planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md</files>
  <read_first>
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-VERIFICATION.md
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
  </read_first>
  <action>
    Create `179-production-footprint-report.md` and record:

    - current live sizes for `metrics-spectrum.db`, `metrics-att.db`, and `metrics.db`
    - companion WAL sizes where available
    - explicit before/after comparison against the 2026-04-13 baseline
    - whether the change is materially smaller, unchanged, or worse

    Keep this evidence-oriented and read-only.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - the report includes explicit baseline and current values
    - the report states whether each active per-WAN DB is materially smaller or not
    - the shared steering DB is inventoried separately from the per-WAN DBs
  </acceptance_criteria>
  <done>Live DB size evidence is recorded against the fixed baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Record current storage status from supported operator surfaces</name>
  <files>.planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md</files>
  <read_first>
    - .planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md
  </read_first>
  <action>
    Extend the report with the current `storage.status` and related operator-visible storage fields from `./scripts/soak-monitor.sh --json`.

    Tie the recorded status to the file-size evidence, but do not over-interpret `ok` as proof that the footprint is ideal.
  </action>
  <verify>
    <automated>./scripts/soak-monitor.sh --json</automated>
  </verify>
  <acceptance_criteria>
    - the report includes current operator-visible `storage.status`
    - the report distinguishes "not failing" from "materially reduced"
    - evidence remains read-only and operator-aligned
  </acceptance_criteria>
  <done>Live storage status is recorded from the supported operator helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo evidence -> production host state | the phase must not infer live success from repo changes alone |
| production evidence -> milestone closure | a weak or qualitative comparison could falsely close the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | I (Integrity) | baseline comparison | mitigate | use fixed 2026-04-13 sizes explicitly in the report instead of qualitative wording |
| T-179-02 | T (Tampering) | live evidence collection | mitigate | use read-only SSH, stat, and helper-script commands only |
</threat_model>

<verification>
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null'`
- `./scripts/soak-monitor.sh --json`
- `rg -n '2026-04-13 baseline|metrics-spectrum\\.db|metrics-att\\.db|metrics\\.db|storage.status' .planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md`
</verification>

<success_criteria>
After this plan, the milestone has explicit live evidence showing whether the deployed footprint improved relative to the fixed April 13 baseline and what storage status operators see now.
</success_criteria>

<output>
After completion, update `.planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md` and create `.planning/phases/179-verification-and-operator-evidence/179-01-SUMMARY.md`
</output>
