---
phase: 182-att-footprint-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 182-01-PLAN.md
  - 182-02-PLAN.md
files_modified:
  - .planning/phases/182-att-footprint-closure/182-production-footprint-closeout.md
  - .planning/phases/182-att-footprint-closure/182-VERIFICATION.md
  - docs/DEPLOYMENT.md
  - docs/RUNBOOK.md
requirements: [STOR-06]
autonomous: true
tags: [verification, production, operator, audit]

must_haves:
  truths:
    - "Final closure compares both active per-WAN DBs to the fixed 2026-04-13 baseline"
    - "The phase closes STOR-06 only if ATT and Spectrum are both materially smaller"
    - "Operator verification stays aligned with the explicit CLI-vs-endpoint-local HTTP story"
  artifacts:
    - path: ".planning/phases/182-att-footprint-closure/182-production-footprint-closeout.md"
      provides: "final baseline comparison and STOR-06 decision"
      contains: "materially smaller"
    - path: ".planning/phases/182-att-footprint-closure/182-VERIFICATION.md"
      provides: "phase verification and milestone audit handoff"
      contains: "STOR-06"
---

<objective>
Capture the final production outcome after the ATT reduction run and close `STOR-06` only if the live evidence supports it.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.36-MILESTONE-AUDIT.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md
@.planning/phases/182-att-footprint-closure/182-att-reduction-execution.md
@scripts/soak-monitor.sh
@scripts/canary-check.sh
@docs/DEPLOYMENT.md
@docs/RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture final production footprint evidence for both per-WAN DBs</name>
  <files>.planning/phases/182-att-footprint-closure/182-production-footprint-closeout.md</files>
  <read_first>
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md
    - .planning/phases/182-att-footprint-closure/182-att-reduction-execution.md
  </read_first>
  <action>
    Create `182-production-footprint-closeout.md` and record:

    - current live sizes for Spectrum and ATT DBs and WALs
    - comparison to the fixed `2026-04-13` baseline
    - whether `STOR-06` is now honestly satisfied
    - current storage and runtime status on supported operator surfaces
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null' && ./scripts/soak-monitor.sh --ssh kevin@10.10.110.223 --json</automated>
  </verify>
  <acceptance_criteria>
    - before/after values are explicit
    - ATT and Spectrum are both evaluated
    - the phase states the requirement outcome plainly
  </acceptance_criteria>
  <done>The milestone has final production evidence for the remaining footprint claim.</done>
</task>

<task type="auto">
  <name>Task 2: Verify operator surfaces and write the final phase verification</name>
  <files>.planning/phases/182-att-footprint-closure/182-VERIFICATION.md docs/DEPLOYMENT.md docs/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/182-att-footprint-closure/182-production-footprint-closeout.md
    - docs/DEPLOYMENT.md
    - docs/RUNBOOK.md
  </read_first>
  <action>
    Verify canary, soak, and history proof paths after the ATT reduction step, align docs if needed, and write `182-VERIFICATION.md` as the milestone audit handoff.
  </action>
  <verify>
    <automated>./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0 --json && ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n env PYTHONPATH=/opt python3 -m wanctl.history --last 1h --metrics wanctl_rtt_ms --json' && rg -n 'wanctl.history|metrics/history|storage.status|metadata.source' docs/DEPLOYMENT.md docs/RUNBOOK.md .planning/phases/182-att-footprint-closure/182-VERIFICATION.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - operator docs match the proven live commands
    - the verification explicitly marks `STOR-06` satisfied or unsatisfied
    - the audit handoff is honest and complete
  </acceptance_criteria>
  <done>The phase is ready for re-audit with a truthful verification artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production truth -> milestone closeout | final closure must rely on the live post-ATT evidence only |
| docs -> operator actions | the final docs must match the commands that actually worked on host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-04 | I (Integrity) | final closeout | mitigate | compare directly to the fixed baseline and state the outcome plainly |
| T-182-05 | I (Integrity) | operator proof path | mitigate | rerun the supported canary, soak, CLI, and endpoint-local history checks after the ATT run |
</threat_model>

<verification>
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null'`
- `./scripts/soak-monitor.sh --ssh kevin@10.10.110.223 --json`
- `./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0 --json`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n env PYTHONPATH=/opt python3 -m wanctl.history --last 1h --metrics wanctl_rtt_ms --json'`
- `git diff --check`
</verification>

<success_criteria>
After this plan, the milestone can be re-audited with final production evidence for whether `STOR-06` is actually closed.
</success_criteria>

<output>
After completion, create the final production evidence and verification artifacts, update operator docs if needed, and create `.planning/phases/182-att-footprint-closure/182-03-SUMMARY.md`.
</output>
