---
phase: 178-retention-tightening-and-legacy-db-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 178-01-PLAN.md
files_modified:
  - configs/spectrum.yaml
  - configs/att.yaml
  - docs/CONFIG_SCHEMA.md
  - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md
requirements: [STOR-06, STOR-07]
autonomous: true
tags: [storage, retention, downsampling, production-safety]

must_haves:
  truths:
    - "The active per-WAN DB footprint is targeted through retention thresholds rather than controller behavior changes"
    - "The chosen retention cut preserves the 24h aggregate window required by tuning safety"
    - "The new storage settings remain consistent across shipped config and schema docs"
  artifacts:
    - path: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md"
      provides: "baseline-to-new retention comparison and rationale"
      contains: "raw_age_seconds"
  key_links:
    - from: "configs/spectrum.yaml and configs/att.yaml"
      to: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md"
      via: "conservative footprint reduction"
      pattern: "aggregate_1m_age_seconds"
---

<objective>
Materially reduce the active per-WAN DB footprint with the smallest safe retention change.

Purpose: Phase 177 showed the multi-GB footprint is mostly live retained content. This plan should reduce that content conservatively without changing control logic, tuning semantics, or storage maintenance safety bounds.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-RESEARCH.md
@configs/spectrum.yaml
@configs/att.yaml
@docs/CONFIG_SCHEMA.md
@src/wanctl/config_base.py
@src/wanctl/config_validation_utils.py
@src/wanctl/storage/downsampler.py
@src/wanctl/storage/retention.py
@src/wanctl/storage/maintenance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten per-WAN retention conservatively</name>
  <files>configs/spectrum.yaml configs/att.yaml .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md</files>
  <read_first>
    - configs/spectrum.yaml
    - configs/att.yaml
    - src/wanctl/config_validation_utils.py
    - src/wanctl/storage/downsampler.py
  </read_first>
  <action>
    Reduce the active per-WAN retention settings by the smallest safe amount that should materially lower the DB footprint.

    Favor reducing `raw_age_seconds` first while preserving the 24h aggregate window needed by tuning. If any additional reduction is needed, justify it explicitly in `178-retention-change-record.md` and keep the rationale tied to Phase 177 evidence.

    Record in the change record:

    - old vs new retention values
    - why the chosen values are expected to reduce DB size materially
    - why the change is considered safe for tuning and operator workflows
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_config_base.py tests/storage/test_storage_maintenance.py tests/tuning/test_tuning_history_reader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - spectrum and att configs both use the new retention profile
    - 24h aggregate lookback required by tuning is preserved
    - change record captures baseline vs new values and safety rationale
  </acceptance_criteria>
  <done>Per-WAN retention is tightened conservatively and documented against the Phase 177 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Align schema/docs with the applied retention contract</name>
  <files>docs/CONFIG_SCHEMA.md .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md</files>
  <read_first>
    - docs/CONFIG_SCHEMA.md
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md
  </read_first>
  <action>
    Update the storage schema documentation to reflect the retention contract operators should expect after this change.

    Keep docs precise about defaults versus the production values shipped in the active WAN configs. Avoid implying that every deployment must use the production-tightened values if the code defaults remain broader.
  </action>
  <verify>
    <automated>rg -n 'raw_age_seconds|aggregate_1m_age_seconds|aggregate_5m_age_seconds|maintenance_interval_seconds' configs/spectrum.yaml configs/att.yaml docs/CONFIG_SCHEMA.md src/wanctl/config_base.py && .venv/bin/pytest -o addopts='' tests/test_check_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - docs distinguish defaults from shipped production config where relevant
    - docs and configs no longer contradict the intended Phase 178 retention contract
    - maintenance and retention settings remain explicitly bounded
  </acceptance_criteria>
  <done>Retention docs and shipped config describe the same safe storage contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retention config -> live storage behavior | overly aggressive retention can silently reduce needed history |
| docs/schema -> operator action | mismatched documentation could cause unsafe follow-on edits in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03 | I (Integrity) | retention reduction | mitigate | preserve tuning-safe aggregate window and validate with focused tests |
| T-178-04 | D (Denial of Service) | maintenance/downsampling cadence | mitigate | keep maintenance interval and per-granularity logic within existing safe bounded patterns |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_config_base.py tests/storage/test_storage_maintenance.py tests/tuning/test_tuning_history_reader.py -q`
- `.venv/bin/pytest -o addopts='' tests/test_check_config.py -q`
- `rg -n 'raw_age_seconds|aggregate_1m_age_seconds|aggregate_5m_age_seconds|maintenance_interval_seconds' configs/spectrum.yaml configs/att.yaml docs/CONFIG_SCHEMA.md src/wanctl/config_base.py`
</verification>

<success_criteria>
After this plan, the repo ships a smaller, explicitly justified per-WAN retention profile that is still compatible with tuning and existing storage-maintenance safety bounds.
</success_criteria>

<output>
After completion, update `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-retention-change-record.md` and create `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-02-SUMMARY.md`
</output>
