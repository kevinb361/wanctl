---
phase: 178-retention-tightening-and-legacy-db-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/steering.yaml
  - src/wanctl/config_base.py
  - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md
requirements: [STOR-05]
autonomous: true
tags: [storage, topology, steering, legacy-db]

must_haves:
  truths:
    - "The runtime role of `/var/lib/wanctl/metrics.db` is explicit rather than inherited accidentally"
    - "Steering storage intent is represented in shipped config or code, not left as an implied default"
    - "Only clearly stale DB artifacts are removed or marked for cleanup; active DBs are not deleted speculatively"
  artifacts:
    - path: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md"
      provides: "authoritative decision record for shared legacy DB role and stale file cleanup"
      contains: "metrics.db"
  key_links:
    - from: "configs/steering.yaml"
      to: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md"
      via: "explicit steering storage role"
      pattern: "storage"
---

<objective>
Make the runtime role of the shared legacy `metrics.db` explicit and close the easy stale-file ambiguity before any retention change lands.

Purpose: Phase 177 showed that `metrics.db` is still active/shared while the per-WAN DBs are authoritative for autorate. Phase 178 must remove ambiguity without risking destructive cleanup.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-RESEARCH.md
@.planning/phases/177-live-storage-footprint-investigation/177-findings-and-recommendation.md
@configs/steering.yaml
@src/wanctl/config_base.py
@src/wanctl/steering/daemon.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the shared DB role explicit for steering</name>
  <files>configs/steering.yaml src/wanctl/config_base.py .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md</files>
  <read_first>
    - configs/steering.yaml
    - src/wanctl/config_base.py
    - src/wanctl/steering/daemon.py
  </read_first>
  <action>
    Update the shipped steering config and any closely-related storage-default documentation/comments so the runtime role of `/var/lib/wanctl/metrics.db` is explicit rather than an inherited default.

    Also create `178-storage-topology-decision.md` capturing:

    - why `metrics.db` remains active/shared today
    - whether Phase 178 keeps it intentionally for steering or retires it
    - what the authoritative active DB set is after this plan

    Keep the change conservative. Do not redesign storage discovery in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_config_base.py tests/steering/test_steering_metrics_recording.py -q && rg -n 'metrics\\.db|storage:' configs/steering.yaml src/wanctl/config_base.py src/wanctl/steering/daemon.py</automated>
  </verify>
  <acceptance_criteria>
    - steering config no longer relies on an invisible legacy default for its DB path
    - decision doc states the intended role of `metrics.db`
    - the repo makes clear which DBs are authoritative after this change
  </acceptance_criteria>
  <done>Legacy/shared DB role is explicit in config and decision artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Close the stale zero-byte file ambiguity</name>
  <files>.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md</files>
  <read_first>
    - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md
    - .planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md
  </read_first>
  <action>
    Extend the decision doc with the disposition of `spectrum_metrics.db` and `att_metrics.db`.

    If execution chooses to remove references to them or document host cleanup steps, keep that action limited to the clearly stale zero-byte artifacts. Do not touch `metrics.db`, `metrics-spectrum.db`, or `metrics-att.db` in this task.
  </action>
  <verify>
    <automated>rg -n 'spectrum_metrics\\.db|att_metrics\\.db|metrics\\.db|authoritative active DB set' .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - stale zero-byte artifact handling is documented explicitly
    - no active DB is marked for deletion by assumption
    - the doc distinguishes active shared DB from stale artifact files
  </acceptance_criteria>
  <done>Stale zero-byte DB artifacts are explicitly classified and handled conservatively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/defaults -> production storage behavior | making the wrong DB role explicit would cement the wrong runtime contract |
| stale-file cleanup decision -> later operator action | misclassifying an active DB as residue would be destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-01 | I (Integrity) | storage role closure | mitigate | require explicit config and decision record, not inference from defaults |
| T-178-02 | T (Tampering) | stale-file cleanup | mitigate | limit cleanup scope to clearly stale zero-byte artifacts and keep active DBs untouched |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_config_base.py tests/steering/test_steering_metrics_recording.py -q`
- `rg -n 'metrics\\.db|storage:' configs/steering.yaml src/wanctl/config_base.py src/wanctl/steering/daemon.py`
- `rg -n 'spectrum_metrics\\.db|att_metrics\\.db|metrics\\.db|authoritative active DB set' .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md`
</verification>

<success_criteria>
After this plan, the repo clearly states why `metrics.db` exists, who uses it, and which DB files are authoritative without relying on tribal knowledge.
</success_criteria>

<output>
After completion, update `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-storage-topology-decision.md` and create `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-01-SUMMARY.md`
</output>
