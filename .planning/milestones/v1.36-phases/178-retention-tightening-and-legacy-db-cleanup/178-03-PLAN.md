---
phase: 178-retention-tightening-and-legacy-db-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 178-01-PLAN.md
  - 178-02-PLAN.md
files_modified:
  - src/wanctl/health_check.py
  - docs/RUNBOOK.md
  - docs/DEPLOYMENT.md
  - .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md
requirements: [STOR-06, STOR-07]
autonomous: true
tags: [history, health, operators, verification]

must_haves:
  truths:
    - "Reader paths follow the authoritative DB topology instead of silently hard-coding the legacy shared default where that is no longer correct"
    - "Operator-facing validation paths remain usable after the retention and topology changes"
    - "The phase ends with a clear post-change verification path for DB role and footprint checks"
  artifacts:
    - path: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md"
      provides: "repeatable operator checks for topology and footprint after Phase 178"
      contains: "wanctl-history"
  key_links:
    - from: "src/wanctl/health_check.py"
      to: ".planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md"
      via: "authoritative read path"
      pattern: "/metrics/history"
---

<objective>
Align reader and operator surfaces with the updated storage topology and retention settings.

Purpose: a storage-layout change is not complete if `/metrics/history`, CLI reads, canary expectations, and operator docs still assume the wrong DB path or omit the new re-check procedure.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-RESEARCH.md
@src/wanctl/health_check.py
@src/wanctl/history.py
@src/wanctl/storage/db_utils.py
@docs/RUNBOOK.md
@docs/DEPLOYMENT.md
@scripts/soak-monitor.sh
@scripts/canary-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align `/metrics/history` with the authoritative DB topology</name>
  <files>src/wanctl/health_check.py .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md</files>
  <read_first>
    - src/wanctl/health_check.py
    - src/wanctl/history.py
    - src/wanctl/storage/db_utils.py
  </read_first>
  <action>
    Update `/metrics/history` behavior if needed so it follows the authoritative metrics DB topology selected by Phase 178 rather than blindly querying `DEFAULT_DB_PATH`.

    Keep the change narrow. Preserve existing pagination and response-shape contracts.

    Capture the final intended read behavior in `178-operator-verification-path.md`, including how operators should verify history against the active DB set.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_health_check.py tests/test_history_multi_db.py tests/test_history_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `/metrics/history` reads from the intended authoritative DB path set
    - existing history response shape and pagination behavior remain intact
    - operator verification doc explains the authoritative history read path
  </acceptance_criteria>
  <done>History read surfaces are aligned with the active storage topology.</done>
</task>

<task type="auto">
  <name>Task 2: Update operator docs and verification commands</name>
  <files>docs/RUNBOOK.md docs/DEPLOYMENT.md .planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md</files>
  <read_first>
    - docs/RUNBOOK.md
    - docs/DEPLOYMENT.md
    - .planning/phases/177-live-storage-footprint-investigation/177-findings-and-recommendation.md
  </read_first>
  <action>
    Update the operator-facing docs and phase verification artifact so they describe:

    - which DB files are active after Phase 178
    - how to check storage footprint and storage status safely
    - how to query retained history using the supported path

    Keep the commands production-safe and aligned with existing helper scripts.
  </action>
  <verify>
    <automated>rg -n 'metrics\\.db|metrics-spectrum\\.db|metrics-att\\.db|soak-monitor|wanctl-history|storage.status' docs scripts && bash -n scripts/soak-monitor.sh scripts/canary-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - docs explain the authoritative active DB set and storage-check path
    - verification commands are copy-pastable and production-safe
    - docs do not regress to ambiguous single-DB guidance if that is no longer authoritative
  </acceptance_criteria>
  <done>Operators have a clear post-change verification path for storage topology and footprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal reader behavior -> external health/history contract | changing data source selection can break operator expectations without changing response shape |
| docs/helper commands -> production operations | incorrect commands could send operators to the wrong DB or wrong conclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-05 | I (Integrity) | `/metrics/history` path selection | mitigate | preserve response contract and validate with focused health/history tests |
| T-178-06 | T (Tampering) | operator verification path | mitigate | keep commands read-only, helper-based, and consistent with shipped scripts/docs |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_health_check.py tests/test_history_multi_db.py tests/test_history_cli.py -q`
- `rg -n 'metrics\\.db|metrics-spectrum\\.db|metrics-att\\.db|soak-monitor|wanctl-history|storage.status' docs scripts`
- `bash -n scripts/soak-monitor.sh scripts/canary-check.sh`
</verification>

<success_criteria>
After this plan, both code and docs tell operators the same story about which DBs are active, how history is queried, and how storage footprint should be re-checked safely.
</success_criteria>

<output>
After completion, update `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-operator-verification-path.md` and create `.planning/phases/178-retention-tightening-and-legacy-db-cleanup/178-03-SUMMARY.md`
</output>
