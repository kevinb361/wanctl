---
phase: 181-production-footprint-reduction-and-reader-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 181-01-PLAN.md
  - 181-02-PLAN.md
files_modified:
  - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md
  - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md
  - docs/DEPLOYMENT.md
  - docs/RUNBOOK.md
requirements: [STOR-06]
autonomous: true
tags: [production, verification, operator, audit]

must_haves:
  truths:
    - "Production evidence is captured against the fixed 2026-04-13 baseline"
    - "The phase states plainly whether the per-WAN DB footprint is now materially smaller"
    - "Operators can verify the final storage and history-reader state with supported commands"
  artifacts:
    - path: ".planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md"
      provides: "final production outcome for STOR-06"
      contains: "materially smaller"
    - path: ".planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md"
      provides: "live CLI/HTTP proof path after Phase 181"
      contains: "/metrics/history"
  key_links:
    - from: ".planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md"
      to: ".planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md"
      via: "fixed production baseline"
      pattern: "2026-04-13 baseline"
    - from: ".planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md"
      to: ".planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md"
      via: "before/after parity evidence"
      pattern: "CLI"
---

<objective>
Verify the post-change production footprint and final reader-parity state, then close `STOR-06` only if the live evidence truly supports it.

Purpose: the milestone cannot close on repo-side plausibility alone; it needs production proof.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.36-MILESTONE-AUDIT.md
@.planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md
@.planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
@scripts/soak-monitor.sh
@scripts/canary-check.sh
@docs/DEPLOYMENT.md
@docs/RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture post-change production size evidence and compare to baseline</name>
  <files>.planning/phases/181-production-footprint-reduction-and-reader-parity/181-production-footprint-closeout.md</files>
  <read_first>
    - .planning/phases/179-verification-and-operator-evidence/179-production-footprint-report.md
    - scripts/soak-monitor.sh
  </read_first>
  <action>
    Create `181-production-footprint-closeout.md` and record:

    - current live sizes for the active per-WAN DBs and WALs
    - direct comparison to the fixed 2026-04-13 baseline
    - whether the result is materially smaller in operator terms
    - current `storage.status` and any relevant operator-visible storage fields
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null' && ./scripts/soak-monitor.sh --json</automated>
  </verify>
  <acceptance_criteria>
    - the report includes explicit before/after values
    - the phase states plainly whether STOR-06 is now met
    - storage status is recorded without being overstated
  </acceptance_criteria>
  <done>The milestone has final live footprint evidence against the fixed baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Capture final live reader-parity evidence and operator commands</name>
  <files>.planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md docs/DEPLOYMENT.md docs/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/179-verification-and-operator-evidence/179-live-reader-topology-report.md
    - docs/DEPLOYMENT.md
    - docs/RUNBOOK.md
  </read_first>
  <action>
    Create `181-live-reader-parity-report.md` and update docs so the final operator story matches live reality after Phase 181.

    Include:

    - supported CLI invocation
    - supported HTTP role
    - any still-narrowed boundary if the surfaces are not fully identical
    - canary and operator-surface evidence showing no workflow regression
  </action>
  <verify>
    <automated>./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0 --json && ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n env PYTHONPATH=/opt python3 -m wanctl.history --last 1h --metrics wanctl_rtt_ms --json' && rg -n 'metrics/history|wanctl.history|storage.status' docs/DEPLOYMENT.md docs/RUNBOOK.md .planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md</automated>
  </verify>
  <acceptance_criteria>
    - the final docs match the actual proven operator commands
    - the report states whether CLI and HTTP are now aligned or still deliberately narrowed
    - canary and operator helpers remain working after the storage change
  </acceptance_criteria>
  <done>Operators have a final, reality-based proof path for storage footprint and history-reader behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo changes -> production truth | final closure must rely on live evidence, not code intent |
| operator docs -> production procedures | docs must match the commands that actually succeeded on host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-05 | I (Integrity) | production closeout | mitigate | compare directly to the fixed 2026-04-13 baseline and state outcome plainly |
| T-181-06 | I (Integrity) | operator workflows | mitigate | verify supported operator surfaces after the reduction step |
| T-181-07 | I (Integrity) | live reader proof path | mitigate | capture fresh CLI/HTTP evidence and align docs to that exact outcome |
</threat_model>

<verification>
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n stat -c "%n %s %y" /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-att.db-wal /var/lib/wanctl/metrics.db /var/lib/wanctl/metrics.db-wal 2>/dev/null'`
- `./scripts/soak-monitor.sh --json`
- `./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0 --json`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n env PYTHONPATH=/opt python3 -m wanctl.history --last 1h --metrics wanctl_rtt_ms --json'`
- `git diff --check`
</verification>

<success_criteria>
After this plan, the milestone has honest production evidence for whether `STOR-06` is truly satisfied and a final operator proof path that matches live behavior.
</success_criteria>

<output>
After completion, create the final production evidence artifacts, update operator docs as needed, and create `.planning/phases/181-production-footprint-reduction-and-reader-parity/181-03-SUMMARY.md`
</output>
