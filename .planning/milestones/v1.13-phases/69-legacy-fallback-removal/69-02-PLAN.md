---
phase: 69-legacy-fallback-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/config_validation_utils.py
  - src/wanctl/calibrate.py
  - src/wanctl/steering/daemon.py
  - docs/CONFIG_SCHEMA.md
  - CHANGELOG.md
  - tests/test_config_validation_utils.py
  - tests/test_calibrate.py
  - tests/test_steering_daemon.py
autonomous: true
requirements: [LGCY-04, LGCY-07]

must_haves:
  truths:
    - "validate_sample_counts() accepts only red_samples_required and green_samples_required (no bad_samples/good_samples)"
    - "calibrate.py generates baseline_time_constant_sec and load_time_constant_sec instead of alpha_baseline and alpha_load"
    - "cake_aware key in config produces a deprecation warning and is ignored"
    - "CONFIG_SCHEMA.md documents modern param names and lists deprecated params with their replacements"
    - "All existing tests pass after changes"
  artifacts:
    - path: "src/wanctl/config_validation_utils.py"
      provides: "Cleaned validate_sample_counts with 2-tuple return"
      exports: ["validate_sample_counts"]
    - path: "src/wanctl/calibrate.py"
      provides: "Modern param names in generated config output"
    - path: "src/wanctl/steering/daemon.py"
      provides: "cake_aware deprecation warning on config load"
    - path: "docs/CONFIG_SCHEMA.md"
      provides: "Updated documentation with deprecation notes"
    - path: "tests/test_config_validation_utils.py"
      provides: "Updated tests for new validate_sample_counts signature"
    - path: "tests/test_calibrate.py"
      provides: "Test that calibrate generates modern param names"
    - path: "tests/test_steering_daemon.py"
      provides: "Test for cake_aware deprecation warning"
  key_links:
    - from: "src/wanctl/calibrate.py"
      to: "generated config output"
      via: "baseline_time_constant_sec key in dict"
      pattern: "baseline_time_constant_sec"
    - from: "tests/test_config_validation_utils.py"
      to: "src/wanctl/config_validation_utils.py"
      via: "import validate_sample_counts"
      pattern: "validate_sample_counts.*red_samples_required.*green_samples_required"
---

<objective>
Clean up validate_sample_counts legacy params (LGCY-04), update calibrate.py to generate modern config names, retire cake_aware with deprecation warning (LGCY-07), and update documentation.

Purpose: LGCY-04 requires removing bad_samples/good_samples from the validation API. LGCY-07 requires cake_aware retirement. These are independent of the config-loading deprecation wiring in Plan 01.
Output: Cleaned validate_sample_counts (2-tuple), calibrate.py generating modern names, cake_aware warning, updated docs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-legacy-fallback-removal/69-CONTEXT.md

<interfaces>
<!-- Current validate_sample_counts signature (to be changed) -->
From src/wanctl/config_validation_utils.py:
```python
def validate_sample_counts(
    bad_samples: int = 8,
    good_samples: int = 15,
    red_samples_required: int = 2,
    green_samples_required: int = 15,
    logger: logging.Logger | None = None,
) -> tuple[int, int, int, int]:
```
Target: Remove bad_samples and good_samples params, return tuple[int, int] (red_samples_required, green_samples_required).

No callers in src/ import validate_sample_counts -- it is only called from tests and exists as a public API for config validation. All test callers are in tests/test_config_validation_utils.py.

From src/wanctl/calibrate.py lines 558-563 (config output):
```python
"thresholds": {
    "target_bloat_ms": round(target_bloat, 0),
    "warn_bloat_ms": round(warn_bloat, 0),
    "hard_red_bloat_ms": round(hard_red_bloat, 0),
    "alpha_baseline": alpha_baseline,  # CHANGE to baseline_time_constant_sec
    "alpha_load": 0.20,               # CHANGE to load_time_constant_sec
},
```

From src/wanctl/calibrate.py imports (does NOT import from autorate_continuous):
```python
import argparse, json, re, statistics, subprocess, sys, time
from dataclasses import dataclass
from datetime import datetime
from pathlib import Path
from typing import Any
import yaml
from wanctl.rtt_measurement import parse_ping_output
from wanctl.signal_utils import is_shutdown_requested, register_signal_handlers
from wanctl.timeouts import ...
```

From configs/steering.yaml (cake_aware already removed in Phase 68):
No cake_aware key present -- just need runtime handling if someone has it in their config.

From src/wanctl/steering/daemon.py (cake_aware already removed from config loading in Phase 68):
No cake_aware loading code -- need to add a deprecation warning if key appears in raw data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean validate_sample_counts, update calibrate.py, add cake_aware warning</name>
  <files>
    src/wanctl/config_validation_utils.py,
    src/wanctl/calibrate.py,
    src/wanctl/steering/daemon.py,
    tests/test_config_validation_utils.py,
    tests/test_calibrate.py,
    tests/test_steering_daemon.py
  </files>
  <behavior>
    - validate_sample_counts(red_samples_required=2, green_samples_required=15) returns (2, 15) tuple
    - validate_sample_counts raises ConfigValidationError for values < 1 or unreasonably high
    - validate_sample_counts no longer accepts bad_samples or good_samples params
    - calibrate.py output dict has "baseline_time_constant_sec" key (not "alpha_baseline")
    - calibrate.py output dict has "load_time_constant_sec" key (not "alpha_load")
    - Steering daemon with cake_aware in config data logs deprecation warning and ignores the key
  </behavior>
  <action>
1. Update validate_sample_counts() in config_validation_utils.py:
   - Remove bad_samples and good_samples parameters entirely
   - Remove their validation logic (< 1 check, > 1000 check)
   - Remove them from the debug log message
   - Change return type to tuple[int, int]
   - Return (red_samples_required, green_samples_required)
   - Update docstring to reflect new signature

2. Update calibrate.py config generation (~line 558-563):
   - Change "alpha_baseline": alpha_baseline to "baseline_time_constant_sec": round(_CYCLE_INTERVAL_SEC / alpha_baseline, 1)
   - Change "alpha_load": 0.20 to "load_time_constant_sec": round(_CYCLE_INTERVAL_SEC / 0.20, 2)
   - Define a module-level constant `_CYCLE_INTERVAL_SEC = 0.05` at the top of calibrate.py (near other constants). Do NOT import CYCLE_INTERVAL_SECONDS from autorate_continuous -- that would create unwanted coupling between the calibration tool and the daemon runtime.
   - alpha_baseline is computed at line 515-521 as 0.01/0.02/0.015 depending on baseline RTT. The time_constant = 0.05 / alpha, yielding 5.0s, 2.5s, or 3.33s respectively.

3. Add cake_aware deprecation warning in steering/daemon.py:
   - In the SteeringConfig._load_specific_fields() or __init__ method (wherever config data is first accessed), add a check:
     ```python
     if "cake_aware" in self.data.get("mode", {}):
         logger.warning("Deprecated config parameter 'mode.cake_aware': CAKE three-state model is always active, this key is ignored")
     ```
   - This uses the same warn+ignore pattern from CONTEXT decisions

4. Update tests/test_config_validation_utils.py:
   - Remove all bad_samples and good_samples from test calls
   - Update TestValidateSampleCounts to test only red_samples_required and green_samples_required
   - Update return value assertions from 4-tuple to 2-tuple
   - Remove tests that specifically test bad_samples/good_samples validation

5. Add/update tests/test_calibrate.py:
   - Add a test that runs calibrate's config generation and asserts the output dict contains "baseline_time_constant_sec" and "load_time_constant_sec" keys (not alpha_baseline/alpha_load)

6. Add test for cake_aware deprecation warning in tests/test_steering_daemon.py:
   - Create a SteeringConfig with mode.cake_aware in data, assert warning is logged and config loads successfully
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/test_config_validation_utils.py tests/test_calibrate.py tests/test_steering_daemon.py -x -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <done>
    - validate_sample_counts has 2-param signature, returns 2-tuple
    - No references to bad_samples/good_samples remain in config_validation_utils.py
    - calibrate.py generates baseline_time_constant_sec and load_time_constant_sec using local _CYCLE_INTERVAL_SEC constant
    - cake_aware in config produces warning and is ignored
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONFIG_SCHEMA.md and CHANGELOG.md</name>
  <files>docs/CONFIG_SCHEMA.md, CHANGELOG.md</files>
  <action>
1. Update docs/CONFIG_SCHEMA.md:
   - In the thresholds table (around line 189-190), replace alpha_baseline and alpha_load rows with baseline_time_constant_sec and load_time_constant_sec rows. Mark them as the primary/recommended params.
   - Add a "Deprecated Parameters" section (or append to existing deprecation notes) listing ALL 8 legacy params with their modern replacements:
     | Deprecated | Replacement | Notes |
     | alpha_baseline | baseline_time_constant_sec | Auto-translated: tc = interval/alpha |
     | alpha_load | load_time_constant_sec | Auto-translated: tc = interval/alpha |
     | cake_state_sources.spectrum | cake_state_sources.primary | Identity rename |
     | spectrum_download | primary_download | Identity rename |
     | spectrum_upload | primary_upload | Identity rename |
     | bad_samples | (removed) | Use red_samples_required |
     | good_samples | (removed) | Use green_samples_required |
     | mode.cake_aware | (removed) | CAKE always active |
   - Update any example YAML snippets that still show alpha_baseline/alpha_load (lines ~199-200, 478-479) to use baseline_time_constant_sec/load_time_constant_sec
   - Remove any cake_aware references if present

2. Update CHANGELOG.md:
   - Add v1.13 section (if not exists) with Phase 69 entry
   - Document: legacy config parameter deprecation (warn+continue), validate_sample_counts cleanup, calibrate.py modern output, cake_aware retirement
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && grep -c "baseline_time_constant_sec" docs/CONFIG_SCHEMA.md && grep -c "Deprecated" docs/CONFIG_SCHEMA.md && grep -c "cake_aware" docs/CONFIG_SCHEMA.md | head -1</automated>
  </verify>
  <done>
    - CONFIG_SCHEMA.md shows modern param names in tables and examples
    - Deprecated parameters section lists all 8 legacy params with replacements
    - CHANGELOG.md has Phase 69 entry documenting the changes
    - No alpha_baseline/alpha_load in example YAML snippets
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/ -x --timeout=60 2>&1 | tail -10
```
All tests pass. No regressions.
</verification>

<success_criteria>
- validate_sample_counts has clean 2-param signature (LGCY-04)
- cake_aware retired with deprecation warning (LGCY-07)
- calibrate.py generates modern config param names using local constant (no autorate_continuous import)
- CONFIG_SCHEMA.md documents all deprecated params and their replacements
- CHANGELOG.md updated
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/69-legacy-fallback-removal/69-02-SUMMARY.md`
</output>
