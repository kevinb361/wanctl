---
phase: 68-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/steering/daemon.py
  - src/wanctl/steering_logger.py
  - configs/steering.yaml
  - configs/examples/steering.yaml.example
  - docs/CONFIG_SCHEMA.md
  - docs/CORE-ALGORITHM-ANALYSIS.md
  - tests/conftest.py
  - tests/test_steering_daemon.py
  - tests/test_steering_logger.py
  - tests/test_failure_cascade.py
autonomous: true
requirements: [LGCY-02]

must_haves:
  truths:
    - "No code path in the steering daemon branches on a cake_aware flag"
    - "CAKE-aware state machine logic is the only code path (no legacy RTT-only alternative)"
    - "All existing tests pass after removal"
    - "Steering daemon initializes CakeStatsReader and StateThresholds unconditionally"
  artifacts:
    - path: "src/wanctl/steering/daemon.py"
      provides: "Unified steering daemon without cake_aware branching"
      contains: "def _evaluate_degradation_condition"
    - path: "src/wanctl/steering_logger.py"
      provides: "Logger without cake_aware branching"
  key_links:
    - from: "src/wanctl/steering/daemon.py"
      to: "assess_congestion_state"
      via: "_evaluate_degradation_condition always calls it"
      pattern: "assess_congestion_state\\(signals"
    - from: "src/wanctl/steering/daemon.py"
      to: "StateThresholds"
      via: "unconditional initialization in __init__"
      pattern: "self\\.thresholds = StateThresholds"
---

<objective>
Eliminate the `cake_aware` mode flag from the steering daemon, inlining CAKE-aware logic as the sole code path and removing all legacy RTT-only branches.

Purpose: The `cake_aware: true` flag is always set in production. The legacy `cake_aware: false` branch is dead code that adds complexity and maintenance burden. Removing it satisfies LGCY-02.

Output: Steering daemon with no mode branching, unconditional CAKE initialization, and all legacy-only code paths removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67/AUDIT.md

@src/wanctl/steering/daemon.py
@src/wanctl/steering_logger.py
@tests/conftest.py
@tests/test_steering_daemon.py
@tests/test_steering_logger.py
@tests/test_failure_cascade.py
</context>

<interfaces>
<!-- Key code locations in daemon.py where cake_aware branching exists (17 occurrences). -->
<!-- Executor must modify all of these to remove the branching. -->

SteeringConfig._load_operational_mode (line 227-232):
  self.cake_aware = mode.get("cake_aware", True)  # REMOVE this field entirely

SteeringConfig._load_thresholds (lines 416-422):
  # Legacy RTT-only thresholds -- REMOVE these 4 fields:
  self.bad_threshold_ms, self.recovery_threshold_ms, self.bad_samples, self.good_samples

SteeringDaemon.__init__ (lines 809-828):
  if self.config.cake_aware:  # REMOVE branch, keep only the True body unconditionally
      self.cake_reader = CakeStatsReader(config, logger)
      self.thresholds = StateThresholds(...)

SteeringDaemon._evaluate_degradation_condition (lines 949-962):
  if self.config.cake_aware:  # REMOVE branch, keep only CAKE path
  else: ... legacy threshold comparison  # DELETE

SteeringDaemon._handle_good_state (lines 1062-1097):
  Multiple if self.config.cake_aware: branches for logging  # REMOVE, keep CAKE logging only

SteeringDaemon._handle_degraded_state (lines 1117-1147):
  Multiple if self.config.cake_aware: branches for logging  # REMOVE, keep CAKE logging only

SteeringDaemon._update_state_machine_unified (lines 1171-1204):
  if self.config.cake_aware: for counter selection  # REMOVE, keep only red_count/thresholds path

SteeringDaemon.collect_cake_stats (line 1379):
  if not self.config.cake_aware or not self.cake_reader:  # Simplify to just check cake_reader

SteeringDaemon.run_cycle (lines 1548, 1567, 1586):
  3 occurrences of if self.config.cake_aware:  # REMOVE branches, keep CAKE path only

steering_logger.py (lines 41, 63, 311, 315, 325):
  cake_aware parameter and branching in log_measurement and log_transition_detected

State schema (line 534):
  "bad_count": 0  # REMOVE from default state -- only red_count used

configs/steering.yaml (line 50):
  cake_aware: true  # REMOVE from config file

configs/examples/steering.yaml.example (line 87):
  cake_aware: true  # REMOVE from example
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove cake_aware branching from steering daemon and logger</name>
  <files>
    src/wanctl/steering/daemon.py
    src/wanctl/steering_logger.py
    configs/steering.yaml
    configs/examples/steering.yaml.example
  </files>
  <action>
Remove all `cake_aware` mode branching from the steering daemon. The CAKE-aware path is always-on; the legacy RTT-only path is dead code. Specific changes:

**SteeringConfig (daemon.py):**
1. In `_load_operational_mode()`: Remove `self.cake_aware = mode.get("cake_aware", True)` entirely. The field must not exist on the config object.
2. In `_load_thresholds()`: Remove the 4 legacy-only fields (`bad_threshold_ms`, `recovery_threshold_ms`, `bad_samples`, `good_samples`) and their loading code (lines 416-422). Keep all CAKE-aware thresholds.

**SteeringDaemon.__init__:**
3. Remove the `if self.config.cake_aware:` / `else:` branch. Initialize `self.cake_reader` and `self.thresholds` unconditionally (the always-True body). Remove the "Legacy RTT-only mode" log message.

**SteeringDaemon._evaluate_degradation_condition:**
4. Remove the `if self.config.cake_aware:` / `else:` branch. Keep only the CAKE path (assess_congestion_state). Remove the assertion (thresholds are now always set). The legacy threshold comparison (lines 958-962) is deleted entirely.

**SteeringDaemon._handle_good_state and _handle_degraded_state:**
5. In both methods, remove all `if self.config.cake_aware:` / `else:` log branches. Keep only the CAKE-format logging (the True branches). Delete the legacy delta-based log messages.

**SteeringDaemon._update_state_machine_unified:**
6. Remove the `if self.config.cake_aware:` / `else:` branch for counter selection (lines 1171-1179). Always use `red_count`, `self.thresholds.red_samples_required`, `self.thresholds.green_samples_required`. Remove the assertions (thresholds always set).
7. In the persist-counters block (lines 1200-1203), always write `state["red_count"]`. Remove the `bad_count` branch.

**SteeringDaemon.collect_cake_stats:**
8. Change `if not self.config.cake_aware or not self.cake_reader:` to just `if not self.cake_reader:` (cake_reader is now always set, but keep the None guard for defensive programming).

**SteeringDaemon.run_cycle (3 locations):**
9. Line ~1548: Remove `if self.config.cake_aware:` branch around EWMA smoothing. Always call `update_ewma_smoothing()`.
10. Line ~1567: Remove `if self.config.cake_aware:` / `else:` log branch. Keep only CAKE-format measurement logging.
11. Line ~1586: Remove `if self.config.cake_aware and` prefix from cake_state_history condition. Just check `"congestion_state" in state`.

**State schema (create_steering_state_schema):**
12. Remove `"bad_count": 0` from the default state dict. Only `red_count` and `good_count` should remain as counters.

**steering_logger.py:**
13. In `log_measurement()`: Remove the `cake_aware` parameter. Always use the CAKE-aware format (signals-based logging). Remove the legacy RTT-only format branch. If `signals` is None, log a simplified format without crashing.
14. In `log_transition_detected()`: Remove the `cake_aware` parameter. Always use the CAKE-aware format (signals-based). Remove the legacy delta-only format.

**Config files:**
15. Remove `cake_aware: true` from `configs/steering.yaml` (line 50) and `configs/examples/steering.yaml.example` (line 87).

**Update docstrings and comments:**
- Remove all references to "legacy mode", "RTT-only mode", "CAKE-aware mode" branching from docstrings
- Update `_evaluate_degradation_condition` docstring to describe only the CAKE assessment
- Update `update_state_machine` docstring to remove "CAKE-aware and legacy modes" language
- Update `_update_state_machine_unified` docstring to just say "state machine" (no "unified" needed for two modes)

**What NOT to change:**
- Do NOT remove `CakeStatsReader`, `StateThresholds`, `assess_congestion_state`, `CongestionState`, or `CongestionSignals` -- these are the ACTIVE code
- Do NOT remove `red_count`, `good_count` state fields -- these are CAKE mode counters
- Do NOT change any threshold values or algorithm behavior
- Do NOT change the `update_state_machine` method that routes to confidence controller -- just update its docstring
  </action>
  <verify>
    <automated>.venv/bin/ruff check src/wanctl/steering/daemon.py src/wanctl/steering_logger.py && .venv/bin/python -c "import ast; ast.parse(open('src/wanctl/steering/daemon.py').read()); ast.parse(open('src/wanctl/steering_logger.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - `grep -c cake_aware src/wanctl/steering/daemon.py` returns 0
    - `grep -c cake_aware src/wanctl/steering_logger.py` returns 0
    - `grep -c cake_aware configs/steering.yaml` returns 0
    - No `bad_threshold_ms`, `recovery_threshold_ms`, `bad_samples`, `good_samples` attributes on SteeringConfig
    - No `bad_count` in default state schema
    - CakeStatsReader and StateThresholds initialized unconditionally
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests and docs for cake_aware removal</name>
  <files>
    tests/conftest.py
    tests/test_steering_daemon.py
    tests/test_steering_logger.py
    tests/test_failure_cascade.py
    docs/CONFIG_SCHEMA.md
    docs/CORE-ALGORITHM-ANALYSIS.md
  </files>
  <action>
Update all tests and documentation that reference the removed `cake_aware` flag and legacy mode.

**tests/conftest.py (line 119):**
1. Remove `config.cake_aware = True` from the mock_steering_config fixture. The attribute no longer exists on SteeringConfig.

**tests/test_steering_daemon.py (many locations):**
2. Remove all lines setting `mock_config.cake_aware = False` or `mock_config.cake_aware = True` or `config.cake_aware = ...`. The attribute no longer exists.
3. For tests that set `cake_aware = False` to bypass CakeStatsReader initialization (lines 708-709, 1012-1015, 1287, 3179, 4836, 5308): Since CakeStatsReader is now always initialized, these tests need to either:
   - Mock/provide the CakeStatsReader dependency properly, OR
   - If the test was specifically testing legacy mode behavior, DELETE the test entirely (it tests dead code)
4. `test_cake_aware_disabled_returns_zeros` (line 103): DELETE this test -- it tests the legacy mode path that no longer exists.
5. `test_default_cake_aware_mode` (line 2667): DELETE this test -- it tests config parsing of a field that no longer exists.
6. `test_run_cycle_cake_aware_mode_logs_congestion_state` (line 3268): Rename to remove "cake_aware_mode" from the name. The behavior it tests (congestion state logging) still exists but is no longer mode-dependent.
7. For tests that set `cake_aware = False` just to avoid initializing CakeStatsReader (e.g., EWMA tests, shutdown tests): Set `mock_config.cake_aware` to... wait, the attribute is gone. Instead, ensure these tests provide a mock CakeStatsReader or mock the initialization. Look at each test carefully -- if it's testing behavior that exists in both modes, it should still work with CAKE-always-on. If it's testing legacy-only behavior, delete it.

**tests/test_steering_logger.py:**
8. Remove `cake_aware=False` and `cake_aware=True` parameters from all test calls to `log_measurement()` and `log_transition_detected()`.
9. Tests that specifically tested legacy format (e.g., the default test at line 55-68 with `cake_aware=False`): Update to use the new always-CAKE format, or delete if they test the removed format.
10. `test_log_measurement_cake_aware` (line 72): Rename -- it's now just the standard format.

**tests/test_failure_cascade.py (lines 231, 283):**
11. Remove `mock_config.cake_aware = False` lines. These tests set cake_aware=False to simplify setup. Update them to work with CAKE-always-on (provide necessary mocks for CakeStatsReader/thresholds).

**docs/CONFIG_SCHEMA.md (line 359):**
12. Remove the `cake_aware` row from the config schema table.

**docs/CORE-ALGORITHM-ANALYSIS.md (lines 150, 163, 167, 509):**
13. Remove or update references to `_update_state_machine_cake_aware`, legacy mode branching, and the complexity table entry. Note: these docs reference old method names that were already refactored into `_update_state_machine_unified` -- the references are stale regardless.

**Key principle:** If a test was testing CAKE-aware behavior (the True branch), it should still pass with minimal changes (just remove the `cake_aware` attribute setting). If a test was testing legacy behavior (the False branch), delete it -- it tests dead code.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -rn cake_aware tests/` returns 0 matches
    - `grep cake_aware docs/CONFIG_SCHEMA.md` returns 0 matches
    - All tests pass (same count minus deleted legacy-mode tests)
    - No test sets `config.cake_aware` or `mock_config.cake_aware`
  </done>
</task>

</tasks>

<verification>
1. `grep -rn cake_aware src/wanctl/` returns 0 matches
2. `grep -rn cake_aware tests/` returns 0 matches
3. `grep -rn cake_aware configs/` returns 0 matches
4. `.venv/bin/pytest tests/ -x -q` passes all tests
5. `.venv/bin/ruff check src/ tests/` passes
6. `.venv/bin/mypy src/wanctl/` passes (no type errors from removed attributes)
</verification>

<success_criteria>
- Zero occurrences of `cake_aware` in src/, tests/, and configs/
- No `_update_state_machine_legacy` or `_update_state_machine_cake_aware` methods exist (they were already refactored away; this plan removes the remaining mode branching)
- SteeringDaemon unconditionally initializes CakeStatsReader and StateThresholds
- SteeringConfig has no `cake_aware`, `bad_threshold_ms`, `recovery_threshold_ms`, `bad_samples`, or `good_samples` attributes
- State schema has no `bad_count` field
- All tests pass after changes
</success_criteria>

<output>
After completion, create `.planning/phases/68-dead-code-removal/68-01-SUMMARY.md`
</output>
