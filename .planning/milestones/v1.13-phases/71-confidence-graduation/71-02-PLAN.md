---
phase: 71-confidence-graduation
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - configs/steering.yaml
  - docs/STEERING.md
  - CHANGELOG.md
autonomous: false
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "configs/steering.yaml has dry_run: false under confidence section"
    - "Rollback procedure is documented with exact commands (edit YAML + kill -USR1)"
    - "Health endpoint shows mode: active when dry_run is false"
    - "Health endpoint shows confidence scores influencing live steering decisions"
  artifacts:
    - path: "configs/steering.yaml"
      provides: "Production config with dry_run: false"
      contains: "dry_run: false"
    - path: "docs/STEERING.md"
      provides: "Rollback procedure documentation"
      contains: "SIGUSR1"
    - path: "CHANGELOG.md"
      provides: "v1.13 confidence graduation entry"
      contains: "dry_run"
  key_links:
    - from: "configs/steering.yaml"
      to: "src/wanctl/steering/daemon.py"
      via: "config loaded at startup, dry_run field drives routing decisions"
      pattern: "dry_run.*false"
    - from: "docs/STEERING.md"
      to: "src/wanctl/signal_utils.py"
      via: "rollback procedure references SIGUSR1"
      pattern: "SIGUSR1"
---

<objective>
Graduate confidence-based steering from dry-run to live mode in the production config, document the rollback procedure using SIGUSR1, and verify the health endpoint confirms active mode.

Purpose: CONF-01 requires dry_run: false for live routing. CONF-02 requires health endpoint confirmation. CONF-03 requires documented rollback procedure.

Output: Updated steering.yaml, rollback docs in STEERING.md, CHANGELOG entry, human-verified health endpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-confidence-graduation/71-01-SUMMARY.md
@configs/steering.yaml
@docs/STEERING.md
@src/wanctl/steering/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update production config and write rollback documentation</name>
  <files>configs/steering.yaml, docs/STEERING.md, CHANGELOG.md</files>
  <action>
    **configs/steering.yaml:**
    1. Change `dry_run: true` to `dry_run: false` on line 68.
    2. Update the comment above the confidence section from "DRY-RUN MODE" to "LIVE MODE":
       - Old: `# Confidence-based steering (DRY-RUN MODE)`
       - New: `# Confidence-based steering (LIVE MODE)`
    3. Update the comment on the dry_run line:
       - Old: `dry_run: true  # VALIDATION MODE - logs only, no routing changes`
       - New: `dry_run: false  # LIVE MODE - confidence scoring drives routing decisions`
    4. Add a comment block above dry_run explaining the rollback:
       ```yaml
       # Rollback: set dry_run: true and send SIGUSR1 to the steering daemon PID
       # See docs/STEERING.md for full rollback procedure
       ```

    **docs/STEERING.md:**
    5. Add a new section "## Confidence-Based Steering" (or append to existing content) covering:
       - Overview: confidence scoring replaces binary RED->steer with multi-signal scoring (0-100 scale)
       - Current mode: LIVE (dry_run: false) -- confidence decisions actively route traffic
       - Rollback procedure with exact commands:
         ```
         ## Rollback to Dry-Run Mode

         To revert confidence steering to log-only mode without restarting the daemon:

         1. Edit the steering config:
            ssh cake-spectrum 'sudo sed -i "s/dry_run: false/dry_run: true/" /etc/wanctl/steering.yaml'

         2. Send SIGUSR1 to reload:
            ssh cake-spectrum 'sudo kill -USR1 $(pgrep -f "steering.*--config")'

         3. Verify via health endpoint:
            ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep mode'
            # Should show: "mode": "dry_run"

         To re-enable live mode:
         1. Edit: set dry_run: false
         2. Send SIGUSR1: kill -USR1 $(pgrep -f "steering.*--config")
         3. Verify: health endpoint shows "mode": "active"
         ```
       - Note: SIGUSR1 only reloads the dry_run flag. Other config changes require daemon restart.

    **CHANGELOG.md:**
    6. Add entry under the current unreleased section (or create one for v1.13):
       - Confidence-based steering graduated from dry-run to live mode
       - SIGUSR1 hot-reload for dry_run flag (enables rollback without daemon restart)
       - Note the WAN-aware steering remains at its current enabled state
  </action>
  <verify>
    <automated>grep "dry_run: false" configs/steering.yaml && grep "SIGUSR1" docs/STEERING.md && grep -c "dry_run" CHANGELOG.md</automated>
  </verify>
  <done>
    - configs/steering.yaml has dry_run: false with updated comments
    - docs/STEERING.md documents rollback procedure with exact SSH + kill commands
    - CHANGELOG.md records confidence graduation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live confidence steering and rollback on production</name>
  <files>none</files>
  <action>
    Confidence-based steering graduated to live mode:
    - Config updated to dry_run: false
    - SIGUSR1 hot-reload mechanism implemented (Plan 71-01)
    - Rollback procedure documented in docs/STEERING.md

    After deploying to cake-spectrum, verify:
    1. The health endpoint shows confidence scoring in active mode
    2. The rollback procedure works (toggle dry_run via SIGUSR1)

    **Pre-deployment (local verification):**
    1. Run full test suite: `.venv/bin/pytest tests/ -v --tb=short`
    2. Verify config change: `grep "dry_run" configs/steering.yaml` shows `dry_run: false`

    **Post-deployment verification (on cake-spectrum):**
    3. Deploy updated config to cake-spectrum
    4. Restart steering daemon: `ssh cake-spectrum 'sudo systemctl restart wanctl-steering'`
    5. Check health endpoint:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool'`
       - Verify `steering.mode` shows `"active"` (not `"dry_run"`)
       - Verify `confidence.primary` shows a numeric score (CONF-02)
    6. Monitor logs briefly for confidence decisions:
       `ssh cake-spectrum 'journalctl -u wanctl-steering -f --no-pager' | head -50`
       - Look for `[CONFIDENCE]` log lines showing live scoring

    **Rollback test (CONF-03):**
    7. Edit config to dry_run: true:
       `ssh cake-spectrum 'sudo sed -i "s/dry_run: false/dry_run: true/" /etc/wanctl/steering.yaml'`
    8. Send SIGUSR1:
       `ssh cake-spectrum 'sudo kill -USR1 $(pgrep -f "steering.*--config")'`
    9. Verify rollback via health endpoint:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep mode'`
       - Should show `"mode": "dry_run"`
    10. Re-enable live mode:
        `ssh cake-spectrum 'sudo sed -i "s/dry_run: true/dry_run: false/" /etc/wanctl/steering.yaml'`
        `ssh cake-spectrum 'sudo kill -USR1 $(pgrep -f "steering.*--config")'`
    11. Verify active again:
        `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep mode'`
        - Should show `"mode": "active"`
  </action>
  <verify>
    <automated>echo "Manual verification required -- see action steps above"</automated>
  </verify>
  <done>
    - Health endpoint shows mode: "active" with live confidence scores (CONF-02)
    - Rollback to dry_run: true via SIGUSR1 confirmed working (CONF-03)
    - Re-enable to dry_run: false via SIGUSR1 confirmed working
    - User approved production deployment
  </done>
</task>

</tasks>

<verification>
1. `grep "dry_run: false" configs/steering.yaml` -- config updated
2. `grep "SIGUSR1" docs/STEERING.md` -- rollback docs present
3. Health endpoint on cake-spectrum shows `mode: "active"` and `confidence.primary` with live scores
4. Rollback to dry_run: true via SIGUSR1 works and health endpoint reflects the change
</verification>

<success_criteria>
- configs/steering.yaml has dry_run: false (CONF-01)
- Health endpoint shows mode: active with live confidence scores (CONF-02)
- Rollback procedure documented and validated via SIGUSR1 toggle (CONF-03)
- All tests pass locally before deployment
</success_criteria>

<output>
After completion, create `.planning/phases/71-confidence-graduation/71-02-SUMMARY.md`
</output>
