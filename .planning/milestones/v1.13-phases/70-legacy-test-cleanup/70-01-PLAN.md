---
phase: 70-legacy-test-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_steering_daemon.py
  - tests/test_autorate_config.py
autonomous: true
requirements: [LGCY-06]
must_haves:
  truths:
    - "No test docstrings or comments reference 'legacy mode' or 'CAKE-aware mode' as if two modes still exist"
    - "No fixture names imply a CAKE vs non-CAKE alternative (e.g., daemon_cake, mock_config_cake)"
    - "Test coverage remains at 90%+ with zero test deletions or regressions"
    - "Before/after test count documented (expect 2277 before and 2277 after)"
  artifacts:
    - path: "tests/test_steering_daemon.py"
      provides: "Updated docstrings and fixture names reflecting current-only codebase"
    - path: "tests/test_autorate_config.py"
      provides: "Updated module docstring and fixture name"
  key_links:
    - from: "tests/test_steering_daemon.py"
      to: "tests/conftest.py"
      via: "mock_steering_config fixture (unchanged, still shared)"
      pattern: "mock_steering_config"
---

<objective>
Update stale test docstrings, comments, and fixture names that reference the retired legacy/CAKE-aware mode distinction. After Phase 68 removed `cake_aware` branching, CAKE three-state is the sole code path, but several test docstrings and fixture names still reference "CAKE-aware mode" as if an alternative exists.

Purpose: Satisfy LGCY-06 -- test suite reflects current-only codebase with no vestigial legacy-mode references in fixtures or docs.
Output: Cleaned test files with accurate docstrings and fixture names. Zero test deletions, zero regressions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (needed for understanding what was removed)
@.planning/phases/68-dead-code-removal/68-01-SUMMARY.md
@.planning/phases/69-legacy-fallback-removal/69-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale docstrings and comments in test_steering_daemon.py</name>
  <files>tests/test_steering_daemon.py</files>
  <action>
Update the following stale docstrings and comments that reference the retired CAKE-aware vs legacy mode distinction. CAKE three-state is now the sole code path (since Phase 68), so references to "legacy mode" as an alternative should be removed.

Specific changes:

1. **TestUnifiedStateMachine class docstring (line ~1237-1244):**
   - OLD: "Tests behavioral equivalence between CAKE-aware and legacy modes: - CAKE-aware: Assessment-based..."
   - NEW: Update to describe what the unified state machine actually tests now: assessment-based state transitions, counter management, asymmetric hysteresis. Remove "behavioral equivalence between CAKE-aware and legacy modes" framing.

2. **daemon_cake fixture docstring (line ~1289):**
   - OLD: "Create a SteeringDaemon in CAKE-aware mode."
   - NEW: "Create a SteeringDaemon with CakeStatsReader patched." (describes what it actually does)
   - Also rename fixture from `daemon_cake` to `daemon` (there is no non-CAKE variant)
   - Update ALL references to `daemon_cake` within TestUnifiedStateMachine to `daemon`

3. **mock_config_cake fixture (line ~1247):**
   - Rename from `mock_config_cake` to `mock_config` within TestUnifiedStateMachine
   - Update the fixture chain: `daemon` fixture should reference `mock_config` not `mock_config_cake`
   - Update docstring: remove "CAKE" qualifier

4. **Section comment (line ~1303-1305):**
   - OLD: "# CAKE-aware mode tests"
   - NEW: Remove the section header entirely OR change to "# State machine tests" (there is only one mode)

5. **TestRunCycle class docstring (line ~3104-3112):**
   - OLD: includes "CAKE-aware mode vs legacy mode logging differences"
   - NEW: Remove that bullet point. Replace with accurate description of what run_cycle tests cover.

6. **TestRunCycle mock_config_cake fixture (line ~3115):**
   - Rename from `mock_config_cake` to `mock_config` within TestRunCycle
   - Update docstring: "Delegate to shared mock_steering_config with run_cycle overrides."
   - Update all references within TestRunCycle that use `mock_config_cake` to use `mock_config`

7. **TestAnomalyCycleSkip mock_config_cake fixture (line ~4854):**
   - Rename from `mock_config_cake` to `mock_config` within TestAnomalyCycleSkip
   - Update docstring: remove "CAKE" qualifier — "Delegate to shared mock_steering_config with anomaly test overrides."
   - Update `daemon_for_anomaly` fixture parameter (line ~4892): change `mock_config_cake` to `mock_config`
   - Update `config=mock_config_cake` reference (line ~4898): change to `config=mock_config`

IMPORTANT: Do NOT touch:
- TestCakeAwareDeprecation class (tests valid current deprecation behavior)
- TestLegacyStateWarning class (tests valid current normalization behavior)
- Any test that exercises deprecation warnings (these test current production behavior)
- SPECTRUM_GOOD/SPECTRUM_DEGRADED state names (these are active production state names)
- bad_count, red_count fields (still live state fields)

After changes, run full test suite to confirm zero regressions and same test count (2277).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_steering_daemon.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>All stale CAKE-aware/legacy mode references in test_steering_daemon.py docstrings and fixture names updated, including TestAnomalyCycleSkip. Zero test failures, same test count.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_autorate_config.py docstrings and document before/after test count</name>
  <files>tests/test_autorate_config.py</files>
  <action>
1. **Module docstring (line 1-8):**
   - OLD: "Config._load_download_config (legacy and state-based floors)" and "Config._load_upload_config (legacy and state-based floors)"
   - NEW: "Config._load_download_config (single-floor and state-based floors)" and same for upload
   - Rationale: `floor_mbps` is still a supported config path (Phase 69 CONTEXT confirmed "floor_mbps stays -- legitimate feature, NOT legacy"), but the word "legacy" is misleading now

2. **base_config_yaml_legacy fixture (line ~71):**
   - Rename from `base_config_yaml_legacy` to `base_config_yaml_single_floor`
   - Update docstring: "Base config YAML with single floor_mbps (applies to all states)."
   - Update all references: `test_load_download_legacy_floor` and `test_load_upload_legacy_floor` parameter names

3. **test_load_download_legacy_floor (line ~123):**
   - Rename to `test_load_download_single_floor`
   - Update docstring: "Single floor_mbps applies to all states."

4. **test_load_upload_legacy_floor (line ~263):**
   - Rename to `test_load_upload_single_floor`
   - Update docstring: "Single floor_mbps applies to all states."

IMPORTANT: Do NOT touch:
- alpha_baseline/alpha_load deprecation warning tests (test valid current behavior)
- Any test that verifies deprecation warnings fire correctly
- floor_mbps values in YAML fixtures (floor_mbps is still a supported production config key)

After changes, run full test suite to confirm zero regressions and same test count (2277).

Then document the before/after count: create a brief note at the top of the SUMMARY (executor will handle this) confirming:
- Before: 2277 tests
- After: 2277 tests (zero deletions, zero additions -- this phase was docstring/fixture rename only)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Module docstrings updated to remove "legacy" label from floor_mbps. Fixture and test method names renamed from "legacy" to "single_floor". Before/after test count documented as 2277/2277.</done>
</task>

</tasks>

<verification>
1. `grep -rn "CAKE-aware mode\|CAKE-aware and legacy\|behavioral equivalence between CAKE" tests/` returns zero matches (stale references eliminated)
2. `grep -rn "base_config_yaml_legacy\|daemon_cake\|mock_config_cake" tests/` returns zero matches (renamed fixtures)
3. `.venv/bin/pytest tests/ -v --tb=short 2>&1 | tail -3` shows 2277 tests passed
4. `.venv/bin/pytest tests/ --cov=src/wanctl --cov-report=term-missing 2>&1 | grep TOTAL` shows 90%+ coverage
</verification>

<success_criteria>
- Zero stale docstrings referencing CAKE-aware vs legacy mode distinction in test_steering_daemon.py
- Fixture names reflect current-only architecture (no daemon_cake, mock_config_cake, base_config_yaml_legacy)
- 2277/2277 tests pass (zero deletions, zero regressions)
- 90%+ coverage maintained
- Before/after test count documented in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/70-legacy-test-cleanup/70-01-SUMMARY.md`
</output>
