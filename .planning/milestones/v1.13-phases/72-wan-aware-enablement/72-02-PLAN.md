---
phase: 72-wan-aware-enablement
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - configs/steering.yaml
autonomous: false
requirements: [WANE-01, WANE-02, WANE-03]
must_haves:
  truths:
    - "Health endpoint on cake-spectrum shows wan_awareness.enabled=true with live zone data"
    - "Confidence scoring includes non-zero WAN contribution when WAN zone is non-GREEN"
    - "Stale WAN zone falls back to GREEN (confidence_contribution=0)"
    - "Missing autorate state file causes WAN weight to be skipped"
    - "Rollback via SIGUSR1 disables WAN-aware steering instantly"
  artifacts:
    - path: "configs/steering.yaml"
      provides: "Production config with wan_state.enabled: true"
      contains: "enabled: true"
  key_links:
    - from: "configs/steering.yaml"
      to: "health endpoint /health"
      via: "daemon reads wan_state.enabled, health.py reflects it"
      pattern: "wan_awareness.*enabled.*true"
---

<objective>
Verify WAN-aware steering is live on production and validate graceful degradation under real network conditions.

Purpose: Confirm WANE-01 (config enabled), WANE-02 (health endpoint reflects live data), and WANE-03 (degradation paths work) on production cake-spectrum container.

Output: Production-verified WAN-aware steering with confirmed degradation safety.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-wan-aware-enablement/72-CONTEXT.md
@.planning/phases/72-wan-aware-enablement/72-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy SIGUSR1 reload extension and add rollback comment</name>
  <files>configs/steering.yaml</files>
  <action>
    The wan_state.enabled is already true in configs/steering.yaml. The code changes from Plan 01 (SIGUSR1 wan_state reload) need to be deployed.

    1. Verify local configs/steering.yaml has `wan_state.enabled: true` (should already be set)
    2. Add rollback comment block above wan_state section (matching dry_run pattern):
       ```yaml
       # Rollback: set enabled: false and send SIGUSR1 to the steering daemon PID
       # See docs/STEERING.md for full WAN-aware rollback procedure
       ```
    3. Deploy code to cake-spectrum:
       ```bash
       ssh cake-spectrum 'cd /opt/wanctl && sudo git pull'
       ssh cake-spectrum 'sudo systemctl restart wanctl-steering@spectrum'
       ```
    4. Wait 5 seconds for daemon startup + verify it came up:
       ```bash
       ssh cake-spectrum 'systemctl is-active wanctl-steering@spectrum'
       ```
       Expected: "active"

    Note: The wan_state section already exists in production steering.yaml with enabled: true (set during v1.11). This task focuses on deploying the new SIGUSR1 reload code.
  </action>
  <verify>
    <automated>ssh cake-spectrum 'systemctl is-active wanctl-steering@spectrum'</automated>
  </verify>
  <done>Steering daemon running on cake-spectrum with updated code that includes _reload_wan_state_config. Config has wan_state.enabled: true with rollback comment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify WAN-aware steering and degradation on production</name>
  <files>none</files>
  <action>
    WAN-aware steering graduated to production:
    - wan_state.enabled: true in steering.yaml (since v1.11)
    - SIGUSR1 hot-reload extended for wan_state.enabled (Plan 72-01)
    - Degradation runbook documented in docs/STEERING.md

    After deploying to cake-spectrum, verify:

    **Step 1: Verify WAN-aware steering is live (WANE-01 + WANE-02)**
    1. Check health endpoint:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep -A15 wan_awareness'`
       - Verify `"enabled": true`
       - Verify zone data present (zone field populated)
       - Verify staleness tracking active (staleness_age_sec field present)
       - Verify confidence_contribution shows numeric value

    **Step 2: Validate stale zone fallback (WANE-03 part 1)**
    2. Stop autorate briefly:
       `ssh cake-spectrum 'sudo systemctl stop wanctl@spectrum'`
    3. Wait 10 seconds (exceeds staleness_threshold_sec: 5):
       `sleep 10`
    4. Check health -- stale=true, contribution=0:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep -A15 wan_awareness'`
       - Should show `"stale": true`, `"confidence_contribution": 0`
    5. Restart autorate:
       `ssh cake-spectrum 'sudo systemctl start wanctl@spectrum'`

    **Step 3: Validate SIGUSR1 rollback (WANE-01 rollback)**
    6. Disable WAN-aware via SIGUSR1:
       `ssh cake-spectrum 'sudo sed -i "s/enabled: true/enabled: false/" /etc/wanctl/steering.yaml'`
       `ssh cake-spectrum 'sudo kill -USR1 $(pgrep -f "steering.*--config")'`
    7. Wait 2 seconds, then verify:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep -A5 wan_awareness'`
       - Should show `"enabled": false`

    **Step 4: Re-enable and verify grace period re-trigger**
    8. Re-enable WAN-aware via SIGUSR1:
       `ssh cake-spectrum 'sudo sed -i "s/enabled: false/enabled: true/" /etc/wanctl/steering.yaml'`
       `ssh cake-spectrum 'sudo kill -USR1 $(pgrep -f "steering.*--config")'`
    9. Immediately check grace period:
       `ssh cake-spectrum 'curl -s http://127.0.0.1:9102/health | python3 -m json.tool | grep -A10 wan_awareness'`
       - Should show `"enabled": true`, `"grace_period_active": true`
  </action>
  <verify>
    <automated>echo "Manual verification required -- see action steps above"</automated>
  </verify>
  <done>
    - Health endpoint shows wan_awareness.enabled: true with live zone data (WANE-02)
    - Stale zone correctly falls back to GREEN with confidence_contribution: 0 (WANE-03)
    - SIGUSR1 rollback disables WAN-aware steering instantly (WANE-01)
    - Re-enable via SIGUSR1 re-triggers grace period
    - User approved all production verification steps
  </done>
</task>

</tasks>

<verification>
- Health endpoint shows `wan_awareness.enabled: true` with live zone data (WANE-02)
- Stale zone falls back gracefully (confidence_contribution=0) (WANE-03)
- SIGUSR1 toggles wan_state.enabled without restart (WANE-01 rollback)
- Re-enable via SIGUSR1 re-triggers grace period
</verification>

<success_criteria>
- Production health endpoint confirms wan_awareness.enabled=true with live data
- Degradation test shows stale zone correctly handled
- SIGUSR1 rollback disables and re-enables WAN-aware steering
- Grace period re-triggers on re-enable
- Human approved all verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/72-wan-aware-enablement/72-02-SUMMARY.md`
</output>
