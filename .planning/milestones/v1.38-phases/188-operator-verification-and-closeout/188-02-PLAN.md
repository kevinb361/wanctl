---
phase: 188-operator-verification-and-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 188-01
files_modified:
  - .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md
autonomous: false
requirements:
  - VALN-01
  - MEAS-04
user_setup: []

must_haves:
  truths:
    - "188-VERIFICATION.md records the chosen evidence path (live preferred, replayable fallback per D-01) and justifies the choice against the actual production version state."
    - "188-VERIFICATION.md demonstrates by reproducible command that /health (or the local health builder) exposes measurement.state / successful_count / stale and that a zero-success cycle maps to state='collapsed' while SAFE-02 fallback does not escalate."
    - "188-VERIFICATION.md cross-references .planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md for the repo-side proof that stale cached RTT is no longer presented as healthy current measurement."
    - "188-VERIFICATION.md closes MEAS-04, OPER-01, and VALN-01 with an explicit per-requirement pass/fail row linking each requirement to a concrete artifact line (doc heading, test name, or evidence block)."
    - "188-VERIFICATION.md explicitly confirms no controller threshold, steering policy, or fallback-path change was introduced by Phase 188."
  artifacts:
    - path: ".planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md"
      provides: "Phase 188 closeout evidence and requirement traceability"
      contains: "MEAS-04"
      min_lines: 80
  key_links:
    - from: "188-VERIFICATION.md"
      to: "187-VERIFICATION.md"
      via: "cross-reference for SAFE-02 / Phase 187 repo-side proof"
      pattern: "187-VERIFICATION\\.md"
    - from: "188-VERIFICATION.md"
      to: "docs/RUNBOOK.md Measurement Health Inspection section"
      via: "cross-reference for OPER-01 doc surface"
      pattern: "RUNBOOK\\.md"
    - from: "188-VERIFICATION.md"
      to: "tests/test_health_check.py::TestMeasurementContract"
      via: "replayable regression witness"
      pattern: "TestMeasurementContract"
---

<objective>
Verify the v1.38 measurement-resilience milestone against the real
`tcp_12down` failure mode using the D-01 "live preferred, replayable fallback"
evidence strategy, and close MEAS-04, OPER-01, and VALN-01 with a traceable
phase closeout artifact.

Purpose: Prove the milestone works in the way Phase 188's must-haves demand
— measurement collapse is visible on `/health`, the operator rubric added in
Plan 188-01 maps to real output, SAFE-02 bounded safety is unchanged, and
every Phase 188 requirement has a concrete evidence line.

Output:
- `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md`
  with evidence path, command transcripts, cross-references, and a per-
  requirement pass/fail table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/188-operator-verification-and-closeout/188-CONTEXT.md
@.planning/phases/188-operator-verification-and-closeout/188-01-PLAN.md
@.planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md
@.planning/phases/186-measurement-degradation-contract/186-CONTEXT.md
@.planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md
@docs/RUNBOOK.md
@docs/DEPLOYMENT.md
@src/wanctl/health_check.py
@tests/test_health_check.py

<interfaces>
<!-- Authoritative measurement contract field names (must appear verbatim    -->
<!-- in 188-VERIFICATION.md). Derived from health_check.py and Phase 186.    -->

wan_health[wan].measurement:
- state: "healthy" | "reduced" | "collapsed"
- successful_count: int (0..3 on current deployment)
- stale: bool (true if sample age > 3 * cadence_sec, or cadence unknown)

Existing repo-side regression witnesses (do not rerun full suite — target these):
- tests/test_health_check.py::TestMeasurementContract::test_zero_success_cycle_reports_collapsed
- tests/test_wan_controller.py::TestZeroSuccessCycle
- tests/test_autorate_error_recovery.py::TestMeasurementFailureRecovery::test_zero_success_cycle_does_not_invoke_icmp_failure_fallback
- tests/test_rtt_measurement.py::TestBackgroundRTTThread::test_cycle_status_published_on_zero_success_preserves_cached

Production version state (from .planning/STATE.md, 2026-04-15):
- Current deployed version on cake-shaper VM 206: v1.32.2
- Phase 187 / v1.38 resilience changes are in the repo but NOT yet on production.
- This means live reproduction of measurement.state on production is NOT
  possible without first running deploy.sh for v1.38, which is explicitly
  out of scope for a closeout phase. Per D-01 and D-12 the executor should
  treat replayable evidence as the primary path for this phase, and document
  the live-path recipe as the procedure to run when v1.38 is actually deployed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the evidence path (live vs replayable) before writing 188-VERIFICATION.md</name>
  <read_first>
    - .planning/STATE.md (current deployed version on cake-shaper VM 206)
    - .planning/phases/188-operator-verification-and-closeout/188-CONTEXT.md (D-01, D-02, D-03, D-10, D-12)
    - .planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md (repo-side proof that Phase 187 already holds)
  </read_first>
  <decision>
    Which evidence path does Phase 188 record as primary in 188-VERIFICATION.md?
  </decision>
  <context>
    Phase 188 is a closeout phase. Production cake-shaper VM 206 currently runs
    v1.32.2 per STATE.md, so the new `measurement.state` field does not exist
    on the live `/health` payload yet. Per D-01 "live preferred, replayable
    fallback" and D-10 "tightly bounded reproduction," the executor must pick
    an explicit primary path and justify it. The phase must NOT trigger an
    ad-hoc v1.38 deploy to make a live reading possible — that is a deploy
    decision, not a verification decision.
  </context>
  <options>
    <option id="option-a">
      <name>Replayable primary, live recipe documented</name>
      <pros>
        - Matches D-01 fallback clause and D-03 (bounded verification).
        - Does not require pushing v1.38 to production.
        - Uses the existing repo-side proof in 187-VERIFICATION.md and the
          targeted test suite hits as the evidence core.
        - Lets Phase 188 close cleanly without reopening deploy scheduling.
      </pros>
      <cons>
        - Replayable evidence is one step removed from a live `tcp_12down` run.
      </cons>
    </option>
    <option id="option-b">
      <name>Live primary against a bounded canary run of v1.38</name>
      <pros>
        - Strongest possible proof against the real failure mode.
        - Directly ties docs/RUNBOOK.md rubric to observed production output.
      </pros>
      <cons>
        - Requires a v1.38 deploy (or at least a canary deploy) as a
          precondition, which is out of scope for a closeout phase.
        - Risks turning a closeout into a deploy + tuning session, which D-03
          explicitly prohibits.
        - Does not close any requirement that the replayable path cannot
          already close.
      </cons>
    </option>
  </options>
  <files>(decision only — no files modified in this task)</files>
  <action>
    Read `.planning/STATE.md` for the current deployed version, re-read
    188-CONTEXT.md D-01/D-10/D-12, and re-read 187-VERIFICATION.md. Then
    select option-a (replayable primary, live recipe documented) or
    option-b (live primary against a canary v1.38 deploy) and record the
    selection plus a one-line justification in the executor scratchpad so
    Task 2 can embed it verbatim in the 188-VERIFICATION.md frontmatter
    `evidence_path` field and "Evidence Path Selection" section. The
    planner's recommended default is option-a because production currently
    runs v1.32.2 and D-03 forbids reopening deploy scheduling inside a
    closeout phase — but the executor MUST confirm the recommendation
    against current STATE.md rather than assuming.
  </action>
  <verify>
    <automated>echo "checkpoint — executor must record evidence_path decision before Task 2 runs"</automated>
  </verify>
  <done>
    Evidence path is explicitly selected (option-a or option-b) and captured with a one-line justification ready to paste into 188-VERIFICATION.md.
  </done>
  <resume-signal>Select: option-a or option-b, with one-line justification</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 188-VERIFICATION.md with evidence and requirement traceability</name>
  <files>.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/188-operator-verification-and-closeout/188-CONTEXT.md (all decisions, especially D-07 four-part closeout criteria and D-09 requirement mapping)
    - .planning/phases/188-operator-verification-and-closeout/188-01-PLAN.md (doc section this verification must reference)
    - .planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md (cross-reference target for SAFE-02 proof)
    - .planning/phases/181-production-footprint-reduction-and-reader-parity/181-live-reader-parity-report.md (shape reference for a phase-specific evidence artifact)
    - docs/RUNBOOK.md (must cite the new `## Measurement Health Inspection` heading)
    - docs/DEPLOYMENT.md (must cite the new post-deploy measurement-health inspection step)
    - src/wanctl/health_check.py (cite line range for `_build_measurement_section`)
    - tests/test_health_check.py (cite the `TestMeasurementContract` class and the zero-success test by name)
  </read_first>
  <action>
    Create `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md`
    using the evidence path locked in Task 1. The file MUST contain, in order:

    1. **Frontmatter** with at minimum:
       ```yaml
       ---
       phase: 188-operator-verification-and-closeout
       verified: <ISO 8601 UTC timestamp>
       status: passed
       evidence_path: replayable | live   # from Task 1
       ---
       ```

    2. **# Phase 188: Operator Verification And Closeout — Verification Report**
       with a short paragraph restating the Phase 188 goal (from ROADMAP.md)
       and the D-07 four-part closeout criteria.

    3. **## Evidence Path Selection** section recording the Task 1 decision
       verbatim, the justification against D-01/D-12 and the current deployed
       version state, and an explicit statement that Phase 188 is closeout
       and did NOT reopen deploy scheduling or controller thresholds.

    4. **## Observable Truths** table mirroring the four must-haves from
       this plan, each with a Status column (`VERIFIED` or `FAILED`) and an
       Evidence column that points at a concrete artifact line (doc heading,
       test name, or command transcript block). All four rows MUST be
       `VERIFIED` for the phase to close.

    5. **## Replayable Evidence Block** — run these commands from the repo
       root and paste the literal output into the report:
       ```bash
       .venv/bin/pytest -o addopts='' tests/test_health_check.py::TestMeasurementContract -q
       .venv/bin/pytest -o addopts='' tests/test_wan_controller.py::TestZeroSuccessCycle -q
       .venv/bin/pytest -o addopts='' tests/test_autorate_error_recovery.py::TestMeasurementFailureRecovery::test_zero_success_cycle_does_not_invoke_icmp_failure_fallback -q
       .venv/bin/pytest -o addopts='' tests/test_rtt_measurement.py::TestBackgroundRTTThread::test_cycle_status_published_on_zero_success_preserves_cached tests/test_rtt_measurement.py::TestBackgroundRTTThread::test_cycle_status_published_on_successful_cycle -q
       ```
       Each block MUST show the exact command, the captured output (or an
       explicit `<command failed>` block if any test fails), and a one-line
       "what this proves" annotation mapped to MEAS-04 / OPER-01 / VALN-01.

    6. **## Doc Surface Evidence Block** — paste the literal headings and
       the first line of body text from each of these doc sections, captured
       by `grep`-style commands, so the verification artifact is self-
       contained:
       ```bash
       grep -n 'Measurement Health Inspection\|Measurement Contract\|Bounded Inspection Recipe\|Pass / Fail Correlation Rubric' docs/RUNBOOK.md
       grep -n 'measurement-health contract' docs/DEPLOYMENT.md
       grep -n 'Measurement Health Inspection' docs/GETTING-STARTED.md
       ```
       Include the literal command lines and their captured output. A
       missing heading from Plan 188-01 is a FAILED row and MUST block the
       report.

    7. **## Live Procedure (For Post-v1.38 Deploy)** section (always present,
       regardless of Task 1 selection) — document the exact commands a
       future operator will run against `kevin@10.10.110.223` once v1.38 is
       deployed, so the live-path recipe is captured even if this phase's
       primary evidence is replayable:
       ```bash
       ssh kevin@10.10.110.223 'curl -s http://10.10.110.223:9101/health' \
         | jq '.wans[0].measurement'
       ssh kevin@10.10.110.227 'curl -s http://10.10.110.227:9101/health' \
         | jq '.wans[0].measurement'
       ./scripts/soak-monitor.sh --json \
         | jq '.[] | select(.wan) | {wan, measurement: .health.wans[0].measurement}'
       ```
       State the expected healthy reading (`state="healthy"`,
       `successful_count=3`, `stale=false`) and the `tcp_12down` degraded
       reading (expect `state` to move off `"healthy"` and/or `stale=true`
       during a reproduction of the original failure mode).

    8. **## Requirements Coverage** table with one row per Phase 188
       requirement (`MEAS-04`, `OPER-01`, `VALN-01`). Each row MUST have:
       - Source Plan column citing `188-01-PLAN.md` and/or `188-02-PLAN.md`.
       - Status column (`SATISFIED` or `UNSATISFIED`).
       - Evidence column that is a concrete pointer: a doc heading, a test
         name, or a line in this verification artifact itself.

    9. **## Non-Regression Callout** paragraph explicitly stating:
       - SAFE-02 ICMP-failure fallback behavior is unchanged (cross-reference
         `.planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md`
         by relative path).
       - No controller thresholds were modified in Phase 188.
       - No steering policy or WAN-selection logic was modified in Phase 188.
       - No new YAML tunable, new alert type, or new CLI tool was introduced.

    10. **## Gaps Summary** with the literal text "No phase-scoped gaps
        remain." if all truths verified, or an explicit gap list otherwise.

    Style constraints:
    - Use the same section-heading and table style as
      `.planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md`.
    - Every file reference MUST use repo-relative paths.
    - Do NOT invent test names, doc headings, or code line ranges — only
      cite artifacts that actually exist after Task 2 runs.
  </action>
  <verify>
    <automated>test -f .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'MEAS-04' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'OPER-01' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'VALN-01' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'measurement.state' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'SAFE-02' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q '187-VERIFICATION.md' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'TestMeasurementContract' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'Measurement Health Inspection' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'Live Procedure' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md` exists and is at least 80 lines.
    - File frontmatter contains `phase:`, `verified:`, `status: passed`, and `evidence_path:` set to the value locked in Task 1.
    - File contains all three requirement IDs: `MEAS-04`, `OPER-01`, `VALN-01`, each mapped to a concrete artifact line in the Requirements Coverage table.
    - File contains the literal strings `measurement.state`, `measurement.successful_count`, `measurement.stale`.
    - File cross-references `.planning/phases/187-rtt-cache-and-fallback-safety/187-VERIFICATION.md` and contains the literal token `SAFE-02`.
    - File contains captured test command output for at least `TestMeasurementContract` and `TestZeroSuccessCycle`, and each captured block shows a PASS result.
    - File contains a `## Live Procedure (For Post-v1.38 Deploy)` section with the literal `ssh kevin@10.10.110.223` and `soak-monitor.sh --json` commands.
    - File contains a Non-Regression Callout stating no controller thresholds, steering policy, YAML tunables, alerts, or CLI tools were introduced by Phase 188.
    - File contains the string `Measurement Health Inspection` referencing the Plan 188-01 runbook section.
  </acceptance_criteria>
  <done>
    Phase 188 closeout artifact exists, maps every Phase 188 requirement to a concrete evidence line, cross-references the Phase 187 repo-side proof, preserves the live-path recipe for post-v1.38 operators, and explicitly records that no controller, steering, or fallback semantics were changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of 188-VERIFICATION.md before milestone closeout</name>
  <what-built>
    A replayable-evidence-first (or live-primary per Task 1) Phase 188
    verification artifact that closes MEAS-04, OPER-01, and VALN-01 and
    preserves the live-path recipe for post-v1.38 deploys.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md`.
    2. Confirm the frontmatter `evidence_path` matches the Task 1 decision.
    3. Confirm the Observable Truths table shows all four truths `VERIFIED`.
    4. Confirm the Replayable Evidence Block contains real captured test
       output (not a placeholder) and that every listed pytest invocation
       shows a PASS.
    5. Confirm the Requirements Coverage table has one row each for
       `MEAS-04`, `OPER-01`, and `VALN-01`, and that each row's Evidence
       column points at a concrete artifact (doc heading, test name, or a
       line in this file).
    6. Confirm the Non-Regression Callout is present and explicitly names
       SAFE-02, controller thresholds, steering policy, YAML tunables,
       alerts, and CLI tools as unchanged.
    7. Confirm the Live Procedure section is present even if evidence_path
       is `replayable`, so future operators have the recipe.
    8. Reject if any requirement row is `UNSATISFIED`, any captured test
       block shows a failure, or any doc cross-reference is broken.
  </how-to-verify>
  <files>(review only — no files modified in this task)</files>
  <action>
    Open `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md`
    and walk the checklist in the `<how-to-verify>` block above. Confirm
    every Observable Truths row is `VERIFIED`, every Requirements Coverage
    row is `SATISFIED`, the Non-Regression Callout is present, and the
    Live Procedure section is captured for post-v1.38 operators. If any
    item fails, describe the issue on resume so Task 2 can be re-run
    before closing Phase 188.
  </action>
  <verify>
    <automated>grep -q 'status: passed' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md && grep -q 'SATISFIED' .planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md</automated>
  </verify>
  <done>
    Human reviewer has typed "approved" and the verification artifact passes the final acceptance checklist.
  </done>
  <resume-signal>Type "approved" to close Phase 188, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/188-operator-verification-and-closeout/188-VERIFICATION.md` exists and passes the acceptance_criteria grep sweep for Task 2.
- `188-VERIFICATION.md` cross-references `187-VERIFICATION.md`, the new `docs/RUNBOOK.md` Measurement Health Inspection section from Plan 188-01, and at least two existing test suite names from the Phase 187 regression slice.
- Phase 188 did not modify any source under `src/wanctl/`, any YAML under `configs/`, any systemd unit under `deploy/systemd/`, or any tuning knob.
- Requirements traceability closes: `REQUIREMENTS.md` entries for `MEAS-04`, `OPER-01`, and `VALN-01` are backed by the rows in the verification artifact.
</verification>

<success_criteria>
- `VALN-01`: Regression coverage for zero-success, reduced-quorum, and degraded-measurement health behavior is captured in `188-VERIFICATION.md` by name and by captured test output, without changing any core congestion threshold.
- `MEAS-04` (evidence leg): `188-VERIFICATION.md` demonstrates that `/health` (via `_build_measurement_section`) exposes `measurement.state`, `successful_count`, and `stale`, and that a collapsed cycle is legible to an operator following the Plan 188-01 rubric.
- Phase 188 closes per D-07: operator path visible, failure mode no longer looks healthy, SAFE-02 unchanged, requirements closed.
- Phase 188 does not reopen Phase 187 semantics or the v1.38 deploy decision.
</success_criteria>

<output>
After completion, create `.planning/phases/188-operator-verification-and-closeout/188-02-SUMMARY.md` summarizing the evidence path chosen, the requirements closed, and the one-line confirmation that SAFE-02 and controller thresholds are unchanged.
</output>
