---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 04
type: execute
wave: 4
depends_on: ["238-02", "238-03"]
files_modified:
  - scripts/phase238-egress-proof.sh
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
autonomous: false
gap_closure: true
requirements: [PROV-03, SAFE-17]
must_haves:
  truths:
    - "Operator can re-run the committed PROV-03 proof and receive PASS for both WANs under the current host-route topology: expected source IP per WAN, host egress dev ens18, and distinct source-bound route keys."
    - "The provenance map documents why the prior spec-modem/att-modem host-dev criterion was invalid for this topology: those labels are downstream cake-autorate ul_if labels, while host ip route get reports the shaper host's egress NIC ens18 toward the router."
    - "SAFE-17 remains passed after the gap-closure script/evidence/map updates; no src/wanctl controller-path file changes."
  artifacts:
    - path: "scripts/phase238-egress-proof.sh"
      provides: "PROV-03 proof script using the corrected host-route topology criterion"
      contains: "ens18"
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json"
      provides: "Fresh operator-run PROV-03 evidence with both WAN verdicts PASS"
      contains: "\"verdict\": \"PASS\""
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md"
      provides: "Updated PROV-03 status and topology-criterion rationale"
      contains: "PROV-03"
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json"
      provides: "Refreshed SAFE-17 evidence after gap closure"
      contains: "\"passed\": true"
  key_links:
    - from: "scripts/phase238-egress-proof.sh"
      to: "live cake-shaper host route output"
      via: "ip route get <reflector> [from <source_ip>]"
      pattern: "dev ens18"
    - from: "238-PROVENANCE-MAP.md PROV-03 section"
      to: "Phase 245 live A/B precondition"
      via: "explicit PASS evidence and caveat that host route proof validates source-bound router-hop egress, not downstream modem-interface labels"
      pattern: "source-bound router-hop"
---

<objective>
Close the Phase 238 PROV-03 verification gap by correcting the proof criterion to match the current live host topology, re-running the read-only proof, and updating the phase evidence to record a passing source-bound router-hop proof.

Purpose: Verification found the original expected-dev criterion invalid for the observed topology: `ip route get` on the shaper host reports `dev ens18` for both WANs, while `spec-modem` / `att-modem` are cake-autorate downstream `ul_if` labels and cannot appear as host route output in this topology. PROV-03 only requires proving `fping -S <source_ip>` would egress the intended WAN under current host routing; this plan closes that gap by proving the source-bound host route to the router with the correct expected source, expected host egress dev, and distinct source-bound route keys.

Output: updated `scripts/phase238-egress-proof.sh`, fresh passing PROV-03 evidence, an updated `238-PROVENANCE-MAP.md`, and a refreshed SAFE-17 evidence record.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-VERIFICATION.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-03-SUMMARY.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
@scripts/phase238-egress-proof.sh
@configs/cake-autorate/config.spectrum.sh
@configs/cake-autorate/config.att.sh

<interfaces>
Existing proof script contracts:
- `generate_remote_commands` emits exactly six `ip route get` commands plus one `ip rule` for `--wan all`.
- `is_allowed_remote_command` accepts only full-line `ip route get <IPv4>`, `ip route get <IPv4> from <IPv4>`, or `ip rule` commands.
- `verdict_for_line "$wan" "$expected_src" "$expected_dev" "$route_line"` returns JSON with `pass`, `expected_src`, `expected_dev`, `parsed_src`, `parsed_dev`, `parsed_via`, and `route_line`.
- Current live evidence shows Spectrum route lines with `src 10.10.110.223 dev ens18 via 10.10.110.1`; ATT source-bound route lines with `from 10.10.110.227 dev ens18 via 10.10.110.1`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct PROV-03 criterion to current host-route topology</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <read_first>
    - scripts/phase238-egress-proof.sh
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-VERIFICATION.md lines 7-17 (gap definition)
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json (observed host route output)
    - configs/cake-autorate/config.spectrum.sh lines 5-10 and configs/cake-autorate/config.att.sh lines 5-10 (downstream ul_if labels and source bindings)
  </read_first>
  <action>
    Update `scripts/phase238-egress-proof.sh` to replace the invalid host-route expected-dev values `spec-modem` and `att-modem` with the live host route dev `ens18` for both WANs. Preserve `10.10.110.223` as Spectrum's expected source and `10.10.110.227` as ATT's expected source. Keep the source-bound ATT branch: generated ATT commands MUST remain `ip route get <reflector> from 10.10.110.227`; generated Spectrum commands remain `ip route get <reflector>`.

    Update comments and `--help` text to say exactly: the PASS criterion is the host-route proof `src 10.10.110.223 + dev ens18` for Spectrum and `from/src 10.10.110.227 + dev ens18` for ATT, plus distinct source-bound route keys. Add a clear comment that `spec-modem` and `att-modem` are cake-autorate `ul_if` labels from `configs/cake-autorate/config.{spectrum,att}.sh`, but they are downstream of the shaper host's route lookup in this topology and are documented as context, not used as `ip route get` expected devs.

    Update `--self-test` fixtures to prove the corrected criterion: Spectrum `dev ens18 src 10.10.110.223` PASS; ATT `from 10.10.110.227 dev ens18` with no separate `src` token PASS; ATT wrong source FAIL; ATT correct source but wrong dev (for example `dev att-modem` or `dev spec-modem`) FAIL. Preserve all command-injection rejection fixtures and the shared `generate_remote_commands` / `is_allowed_remote_command` / mandatory pre-SSH allowlist behavior. Do not edit `src/wanctl/` or mutate live routing/services.
  </action>
  <verify>
    <automated>bash -n scripts/phase238-egress-proof.sh && scripts/phase238-egress-proof.sh --self-test && scripts/phase238-egress-proof.sh --print-commands && grep -Fq 'ens18' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.223' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.227' scripts/phase238-egress-proof.sh && grep -Fq 'generate_remote_commands' scripts/phase238-egress-proof.sh && grep -Fq 'is_allowed_remote_command' scripts/phase238-egress-proof.sh && test -z "$(git status --porcelain -- src/wanctl/)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase238-egress-proof.sh --self-test` exits 0 with the corrected `ens18` host-route fixtures.
    - `--print-commands` still prints six `ip route get` commands and one `ip rule`, with ATT commands source-bound via `from 10.10.110.227`.
    - The script documents that `spec-modem` / `att-modem` are downstream `ul_if` context, not host `ip route get` expected devs.
    - `git status --porcelain -- src/wanctl/` is empty.
  </acceptance_criteria>
  <done>The proof script validates the current host-route topology: expected source per WAN, expected host egress dev `ens18`, distinct source-bound route keys, unchanged read-only command allowlist, and no controller-path edits.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator re-runs corrected PROV-03 proof</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <what-built>`scripts/phase238-egress-proof.sh` now uses the corrected host-route criterion: Spectrum `src 10.10.110.223 dev ens18`; ATT `from/src 10.10.110.227 dev ens18`; both must have distinct source-bound route keys.</what-built>
  <read_first>
    - scripts/phase238-egress-proof.sh
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-VERIFICATION.md (PROV-03 gap)
  </read_first>
  <action>
    Present the operator with the exact command to run from the dev host: `scripts/phase238-egress-proof.sh --json`. The script SSHes read-only to `kevin@10.10.110.223` and runs only allowlist-validated `ip route get` / `ip rule` commands. If SSH access trips a guardrail, use `scripts/phase238-egress-proof.sh --print-commands` and have the operator run the printed `! ssh cake-shaper '<command>'` equivalents. Capture the full stdout verbatim for Task 3. A passing run must show both WAN top-level `verdict` fields as `PASS`, each reflector `pass:true`, Spectrum parsed source `10.10.110.223`, ATT parsed source `10.10.110.227` (from `src` or `from`), parsed dev `ens18` for both WANs, and `distinct_paths_check.pass:true`.
  </action>
  <verify>
    <automated>scripts/phase238-egress-proof.sh --self-test && scripts/phase238-egress-proof.sh --print-commands</automated>
    <human-check>Operator ran the corrected proof read-only and pasted full JSON stdout showing PASS for both WANs.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator pasted full JSON stdout from a read-only proof run.
    - JSON contains `"wan": "spectrum"` and `"wan": "att"` rows.
    - Top-level verdict is PASS for both WANs and per-reflector pass is true.
    - Parsed dev is `ens18` for both WANs; parsed source is `10.10.110.223` for Spectrum and `10.10.110.227` for ATT.
    - No production mutation occurred; only `ip route get` / `ip rule` commands ran.
  </acceptance_criteria>
  <done>Operator-provided JSON stdout is available for Task 3 and shows a passing read-only PROV-03 proof under the corrected host-route criterion.</done>
  <resume-signal>Paste the full JSON stdout and type "captured".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit passing evidence, update map, and refresh SAFE-17</name>
  <files>.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json, .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md, .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json</files>
  <read_first>
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json
    - scripts/phase238-safe17-boundary-check.sh
  </read_first>
  <action>
    Replace `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json` with the operator-pasted passing JSON from Task 2, preserving valid JSON formatting. Update `238-PROVENANCE-MAP.md` PROV-03 section so it no longer says PROV-03 is non-pass. State the corrected criterion explicitly: host route proof validates source-bound router-hop egress (`dev ens18`) plus distinct source-bound route keys; `spec-modem` and `att-modem` remain downstream cake-autorate `ul_if` context and were not valid expected host-route devs. Update the requirements mapping row for PROV-03 to `Satisfied by corrected read-only host-route evidence`.

    Re-run `scripts/phase238-safe17-boundary-check.sh` after the script/evidence/map edits and commit the refreshed JSON evidence. If SAFE-17 fails, stop and investigate; do not force-pass. Do not edit any `src/wanctl/` file.
  </action>
  <verify>
    <automated>python3 -c "import json; rows=json.load(open('.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/egress-proof-live-20260614T222118Z.json')); assert {r['wan'] for r in rows} == {'spectrum','att'}; assert all(r['verdict']=='PASS' for r in rows); assert all(item['pass'] is True and item['parsed_dev']=='ens18' for r in rows for item in r['reflectors']); assert any(r['wan']=='att' and all(item['parsed_src']=='10.10.110.227' for item in r['reflectors']) for r in rows); assert any(r['wan']=='spectrum' and all(item['parsed_src']=='10.10.110.223' for item in r['reflectors']) for r in rows)" && grep -Fq 'Satisfied by corrected read-only host-route evidence' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -Fq 'source-bound router-hop' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && scripts/phase238-safe17-boundary-check.sh && python3 -c "import json; d=json.load(open('.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json')); assert d['passed'] is True" && test -z "$(git status --porcelain -- src/wanctl/)"</automated>
  </verify>
  <acceptance_criteria>
    - Egress evidence JSON is valid and has both WAN verdicts PASS.
    - Every reflector row has `pass:true` and `parsed_dev:"ens18"`; Spectrum parsed source is `10.10.110.223`; ATT parsed source is `10.10.110.227`.
    - Provenance map records PROV-03 as satisfied by corrected host-route evidence and explains the `spec-modem` / `att-modem` criterion error.
    - SAFE-17 evidence has `passed:true` after gap closure.
    - `git status --porcelain -- src/wanctl/` is empty.
  </acceptance_criteria>
  <done>PROV-03 has fresh passing read-only evidence under the corrected topology criterion, the provenance map reflects that closure, and SAFE-17 still passes with no controller-path drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Remote reads must remain read-only and limited to `ip route get` / `ip rule` |
| proof stdout → phase evidence JSON | Operator-pasted data must be valid JSON before commit |
| evidence artifact → public docs | Internal IPs and route output remain in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-GAP-01 | Tampering | corrected egress-proof remote commands | mitigate | Preserve `generate_remote_commands`, full-line `is_allowed_remote_command`, mandatory pre-SSH validation, and `--self-test` injection rejection; only `ip route get` / `ip rule` can run remotely |
| T-238-GAP-02 | Spoofing | PROV-03 proof verdict | mitigate | PASS requires concrete source IP per WAN, host dev `ens18`, and distinct source-bound route keys; Task 3 parses JSON and asserts every reflector row before accepting evidence |
| T-238-GAP-03 | Repudiation | topology reinterpretation | mitigate | Provenance map must explicitly document why `spec-modem`/`att-modem` were invalid host-route expected devs and record the corrected source-bound router-hop criterion |
| T-238-GAP-04 | Information Disclosure | internal route/evidence details | mitigate | Keep live route output and internal IPs inside `.planning/phases/238-...`; do not promote to public docs |
| T-238-GAP-05 | Tampering | controller-path drift | mitigate | Re-run `scripts/phase238-safe17-boundary-check.sh`; assert `passed:true` and empty `git status --porcelain -- src/wanctl/` |
</threat_model>

<verification>
- `scripts/phase238-egress-proof.sh --self-test` passes with corrected `ens18` fixtures and injection rejection.
- Operator-run JSON evidence validates both WANs PASS under the corrected criterion.
- `238-PROVENANCE-MAP.md` records PROV-03 as satisfied and explains the topology criterion correction.
- SAFE-17 boundary check passes after all gap-closure edits.
</verification>

<success_criteria>
- PROV-03 gap is closed with a passing read-only proof under current host topology.
- The old non-pass `spec-modem`/`att-modem` expected-dev assumption is documented as a plan-level criterion error, not hidden.
- No production mutation and no `src/wanctl/` controller-path mutation occurred.
</success_criteria>

<output>
After completion, create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-04-SUMMARY.md`
</output>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Documented evidence-backed RTT provenance and A/B target before backend code | 01-04 | COVERED | Prior plans cover PROV-01/02 and SAFE; this gap plan closes the remaining egress proof.
REQ | PROV-01 | Provenance map | 03 | COVERED | Already verified.
REQ | PROV-02 | A/B selection | 03 | COVERED | Already verified: Selection A.
REQ | PROV-03 | fping source-IP egress proof | 04 | COVERED | Gap closure corrects criterion and captures passing proof.
REQ | SAFE-17 | Controller-path boundary | 01,03,04 | COVERED | Gap plan refreshes SAFE evidence after edits.
RESEARCH | — | Both-WAN read-only egress proof | 04 | COVERED | Reuses existing script and route-output evidence.
CONTEXT | D-07 | Cover both WANs and capture ip rule | 04 | COVERED | Task 2 rerun includes both WANs and ip rule.
CONTEXT | D-08 | Committed read-only proof script, operator-run stdout | 04 | COVERED | Task 2/3 capture fresh stdout as evidence.
CONTEXT | D-09 | Lightweight SAFE-17 assertion only | 04 | COVERED | Task 3 reruns existing lightweight checker; no full verifier added.
</source_audit>
