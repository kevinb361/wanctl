---
phase: 245-live-a-b-rollback-anchor
plan: 04
type: execute
wave: 4
depends_on: ["245-01", "245-02", "245-03"]
files_modified:
  - .planning/phases/245-live-a-b-rollback-anchor/evidence/
  - .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md
autonomous: false
requirements: [AB-01, AB-02, AB-03, SAFE-17]
must_haves:
  truths:
    - "A read-only preflight confirms fping is present on cake-shaper, steering.service is active, both /health endpoints respond, and the PRODUCTION flat-file deploy at cake-shaper:/opt/wanctl matches Snapshot-A anchor ffaa8a0e by file-hash proof (not local HEAD)"
    - "The Spectrum config is deployed with the flip and steering shows producer='wanctl-backend' before the scored window begins"
    - "A pre-registered interleaved A/B runs on Spectrum (icmplib vs fping) with ATT held as control, evidence descending from the frozen-thresholds commit"
    - "The verdict is computed by phase245-gate-eval.py against the frozen thresholds and recorded as an evidence artifact"
    - "Steering is returned to the Snapshot-A config state (icmplib backend, Phase-245 code deployed) via the armed config-only rollback, verified by /health"
    - "The SAFE-17 boundary verifier passes at the phase boundary"
  artifacts:
    - path: ".planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md"
      provides: "Recorded A/B verdict + recommendation (keep-icmplib | switch-eligible) with provenance"
      contains: "outcome"
    - path: ".planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json"
      provides: "Phase-boundary SAFE-17 PASS evidence"
      contains: "passed"
  key_links:
    - from: "live A/B evidence"
      to: "frozen thresholds commit (Plan 01)"
      via: "phase245-prereg-provenance.sh assert-descends"
      pattern: "assert-descends"
    - from: "245-AB-VERDICT.md"
      to: "phase245-gate-eval.py output"
      via: "recorded verdict JSON → markdown summary"
      pattern: "outcome"
---

<objective>
Execute the pre-registered live A/B on production (`cake-shaper`): preflight, deploy the flip to Spectrum, confirm the seam is producing samples, run the interleaved icmplib-vs-fping comparison with ATT held as control, compute the verdict against the frozen thresholds, return steering to the Snapshot-A config state via the armed config-only rollback, and record the verdict + fresh SAFE-17 boundary evidence. "Keep icmplib" is a valid passing close — the verdict is honest, not biased toward a fping win.

Purpose: This is the only plan that touches production. It is operator-gated at every mutation point because wanctl is a 24/7 production network control system (CLAUDE.md: change conservatively; stability > safety). The actual default flip is Phase 246 — this plan produces the verdict, not the production default change.

Output: live A/B evidence JSONL, `245-AB-VERDICT.md`, fresh `safe17-boundary-245.json`, and steering returned to the Snapshot-A config state (icmplib backend under Phase-245 code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/245-live-a-b-rollback-anchor/245-RESEARCH.md
@.planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md

<interfaces>
<!-- Scripts built in Plans 01/03; live endpoints from Phase 238 captures. -->

scripts/phase245-prereg-provenance.sh — record | assert-descends | assert-blob-unchanged
scripts/phase245-ab-run.sh — interleaved A/B orchestration (--confirm to mutate); records baseline_nrestarts + planned_restarts
scripts/phase245-gate-eval.py — six-dimension verdict (--output 245-AB-VERDICT.json); restart dim gates unexpected restarts
scripts/phase245-rollback.sh — armed --confirm-gated config-only Snapshot-A revert (NOT a code rollback)
scripts/phase245-safe17-boundary-check.sh — phase-boundary verifier (anchor ffaa8a0e)

Live endpoints (Phase 238 verified):
  Spectrum (under test): http://10.10.110.223:9101/health
  ATT (control):         http://10.10.110.227:9101/health
  Host: cake-shaper. Unit: steering.service. fping expected 5.1.

Snapshot-A anchor semantics (Concern 3 + Concern 4):
  - The Snapshot-A code anchor is ffaa8a0e (Phase 244 close), the code state deployed
    on cake-shaper BEFORE Plans 01-03 land. Production uses the project deploy.sh
    flat-rsync layout (`src/wanctl/` -> `/opt/wanctl/`), not a git checkout, so the
    anchor check is a PRODUCTION flat-file hash proof: compare every tracked
    `src/wanctl/**/*.py` blob at git anchor ffaa8a0e to `/opt/wanctl/**/*.py` on
    cake-shaper after stripping the `src/wanctl/` prefix. Do NOT use
    `git -C /opt/wanctl rev-parse`; `/opt/wanctl` is intentionally not a git repo.
    Do NOT check local HEAD — after Plans 01-03 commit, local HEAD is a different ref.
  - "Snapshot-A rollback" is a CONFIG-ONLY operation: revert the Spectrum
    measurement.backend to icmplib under the deployed Phase-245 code. It is NOT a code
    rollback to ffaa8a0e. After deploying Plans 01-03, the production code ref will be
    different from ffaa8a0e, and that is expected — the Phase-245 daemon flip stays
    deployed; only the backend config reverts to icmplib.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Read-only production preflight</name>
  <action>Run only read-only commands against cake-shaper (no mutation) per the verification steps below. Save the transcript to evidence/. BLOCK the phase if fping is absent (no fping-arm contrast) or the production flat-file deploy at cake-shaper:/opt/wanctl does NOT match Snapshot-A anchor ffaa8a0e by hash proof (i.e. the Snapshot-A code is not on the host yet).</action>
  <what-built>The preflight uses only read-only commands (no mutation). It confirms the A/B can run: fping is present, steering.service is active, both /health endpoints respond, and the PRODUCTION flat-file deploy at cake-shaper:/opt/wanctl matches Snapshot-A anchor ffaa8a0e.</what-built>
  <note>The anchor check targets deployed flat files on cake-shaper, NOT the local implementation branch and NOT a git repo under /opt/wanctl. Production uses deploy.sh's flat rsync layout (`src/wanctl/` -> `/opt/wanctl/`), so prove the deployed bytes by hashing remote `/opt/wanctl` files against git anchor `ffaa8a0e:src/wanctl/...`. The BLOCK condition is: the production host does NOT have ffaa8a0e-equivalent flat files deployed (the Snapshot-A code is not on the host yet).</note>
  <how-to-verify>
    Run these read-only commands and confirm the expected results:
    1. `ssh cake-shaper 'fping --version'` → reports fping 5.x (the fping arm requires the binary; absent → the A/B has no contrast, BLOCK).
    2. `ssh cake-shaper 'systemctl is-active steering.service'` → "active".
    3. `ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.223:9101/health' | python3 -m json.tool` → returns Spectrum payload (currently producer likely null/bridge pre-deploy).
    4. `ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.227:9101/health' | python3 -m json.tool` → returns ATT payload.
    5. (REVIEW FIX, Concern 3 + flat-rsync production layout) Confirm the PRODUCTION deployed flat files on cake-shaper match Snapshot-A anchor `ffaa8a0e`: compare every tracked `src/wanctl/**/*.py` blob at `ffaa8a0e` to `/opt/wanctl/**/*.py` on cake-shaper after stripping the `src/wanctl/` prefix. The proof JSON must record `anchor: ffaa8a0e`, `passed: true`, `missing: []`, and `changed: []`. This checks deployed production bytes, NOT local repo HEAD and NOT `git -C /opt/wanctl`.
    Save the preflight transcript to .planning/phases/245-live-a-b-rollback-anchor/evidence/preflight-<timestamp>.txt.
  </how-to-verify>
  <done>Preflight transcript saved; fping present, steering active, both /health respond, and the production flat-file hash proof confirms cake-shaper:/opt/wanctl matches ffaa8a0e (Snapshot-A anchor).</done>
  <resume-signal>Type "approved" if fping present + steering active + both /health respond + the production flat-file hash proof for cake-shaper:/opt/wanctl confirms anchor ffaa8a0e, or describe what is missing (e.g., fping absent, or production host not on the Snapshot-A anchor).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy the flip to Spectrum and confirm the seam is live</name>
  <action>Deploy the Plan 02 daemon flip + Spectrum measurement.backend config to cake-shaper and restart steering once to apply, then confirm via /health that the seam is producing samples (producer="wanctl-backend", climbing wanctl_backend count). Record pre-deploy NRestarts so the verdict can subtract the intended apply-restart. STOP if the count stays 0 while autorate_health climbs (silent fallback, Pitfall 1). Follow the verification steps below; ATT control gets zero changes.</action>
  <what-built>The Plan 02 daemon flip plus the Spectrum config (measurement.backend) need to reach /opt/wanctl on cake-shaper and steering must restart once to apply. This is a production deploy + single restart — operator-executed. After this deploy, the production-deployed ref on cake-shaper will be the Plans 01-03 implementation ref, NOT ffaa8a0e — that is expected.</what-built>
  <how-to-verify>
    1. Reconcile repo==prod: confirm the daemon.py at the deploy target will match the repo flip after deploy (the 238 sha256sum discipline; check `git status` clean and the flip committed).
    2. Deploy: `scripts/deploy.sh spectrum cake-shaper` (or the project's standard deploy path for the Spectrum steering stack). This is the intended single apply-restart — record the pre-deploy NRestarts as baseline_nrestarts so the verdict can subtract it (this feeds the A/B run summary's baseline_nrestarts field).
    3. Restart steering: `ssh cake-shaper 'sudo systemctl restart steering.service'` (if deploy did not).
    4. Confirm the seam is producing: scrape Spectrum /health and assert rtt_source.producer == "wanctl-backend" and rtt_source.counts.wanctl_backend is climbing across two scrapes ~10s apart (Pitfall 1: if it stays 0 and autorate_health climbs, the seam is silently falling back — STOP, diagnose source_ip/host before the scored window).
    5. Confirm ATT (control) is UNTOUCHED: ATT /health producer is unchanged from preflight.
    Save the deploy + confirmation transcript to evidence/.
  </how-to-verify>
  <done>Spectrum /health shows producer="wanctl-backend" with a climbing wanctl_backend count; ATT untouched; pre-deploy NRestarts recorded as baseline_nrestarts.</done>
  <resume-signal>Type "approved" if Spectrum /health shows producer="wanctl-backend" with a climbing wanctl_backend count and ATT is untouched, or describe the fallback symptom to diagnose.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the pre-registered interleaved A/B + compute the verdict</name>
  <action>Record the prereg commit and assert the thresholds blob is unchanged since Plan 01, run scripts/phase245-ab-run.sh --confirm for the operator-chosen window (recommend >= one diurnal cycle), assert the evidence descends from the prereg commit, then compute the verdict with scripts/phase245-gate-eval.py. Follow the verification steps below. Both keep-icmplib and switch-eligible are valid passing closes; rollback_trigger means proceed to Task 4 immediately.</action>
  <what-built>scripts/phase245-ab-run.sh drives the alternating-window icmplib<->fping comparison on Spectrum (ATT held as control), records the prereg commit so evidence descends from the frozen thresholds, scrapes /health attribution and NRestarts per window into evidence JSONL, records baseline_nrestarts + planned_restarts, and emits an aggregated summary. scripts/phase245-gate-eval.py then computes the six-dimension verdict (the restart dimension gates UNEXPECTED restarts only — planned per-window apply-restarts are excluded).</what-built>
  <how-to-verify>
    1. Record prereg + assert the thresholds blob is unchanged since Plan 01:
       `scripts/phase245-prereg-provenance.sh record` and `scripts/phase245-prereg-provenance.sh assert-blob-unchanged <expected_blob_sha>`.
    2. Run the A/B (operator decides total length; recommend >= one diurnal cycle for diurnal control, AB-02):
       `scripts/phase245-ab-run.sh --confirm` (mutates Spectrum config per window; ATT untouched). Monitor that wanctl_backend counts climb under both backends and that unexpected restarts (total NRestarts beyond baseline_nrestarts + the recorded planned per-window apply-restarts) stay at 0.
    3. After the run, assert evidence descends from prereg:
       `scripts/phase245-prereg-provenance.sh assert-descends <prereg_commit> <evidence_commit>`.
    4. Compute the verdict:
       `.venv/bin/python scripts/phase245-gate-eval.py --thresholds scripts/phase245-thresholds.json --output .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.json <run-summary args>`.
    5. Confirm the verdict outcome is one of pass (keep-icmplib OR switch-eligible) / rollback_trigger / input_error, and the exit code matches (0/1/2). Both keep-icmplib and switch-eligible are valid passing closes — do NOT treat keep-icmplib as a failure.
  </how-to-verify>
  <done>The interleaved A/B ran with evidence descending from the prereg commit, and 245-AB-VERDICT.json records a verdict outcome (pass/rollback_trigger/input_error) with provenance SHAs.</done>
  <resume-signal>Type "approved" with the verdict outcome (e.g., "pass / keep-icmplib"), or "rollback_trigger" / "input_error" with the failing dimension. If rollback_trigger, proceed immediately to Task 4 rollback.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Return steering to the Snapshot-A config state via the armed config-only rollback</name>
  <action>Review the rollback plan (no --confirm prints the ordered steps and a read-only health probe only), then execute scripts/phase245-rollback.sh --confirm to restore Spectrum to the icmplib backend config, redeploy, restart steering, and verify /health shows backend=="icmplib". This is a CONFIG-ONLY revert — the Phase-245 daemon flip stays deployed; only the backend config reverts to icmplib. Phase 245 leaves steering on the Snapshot-A config state (icmplib backend, Phase-245 code deployed) regardless of the verdict — the production default flip is Phase 246. Follow the verification steps below; ATT control untouched.</action>
  <what-built>scripts/phase245-rollback.sh is the armed --confirm-gated Snapshot-A config-only revert: restore the Spectrum measurement.backend config to icmplib under the deployed Phase-245 code, redeploy, restart steering, verify /health shows backend=="icmplib". Phase 245 does NOT flip the production default (that is Phase 246) — at the end of 245 steering must be on the Snapshot-A config state (icmplib backend, Phase-245 code deployed), regardless of the verdict.</what-built>
  <note>Snapshot-A rollback is a config-only operation. The Phase-245 code (the daemon flip from Plan 02) REMAINS deployed; only the Spectrum measurement.backend config reverts to icmplib. ffaa8a0e in the preflight (Task 1) confirms what was deployed BEFORE Plans 01-03; after deploying Plans 01-03 in Task 2, the production ref will be different from ffaa8a0e, and that is expected. "Return to Snapshot-A" here means the Snapshot-A CONFIG state (icmplib backend), not a code rollback to the ffaa8a0e commit.</note>
  <how-to-verify>
    1. Review the plan first: `scripts/phase245-rollback.sh` (no --confirm) prints the ordered steps and a read-only health probe; mutates nothing. Confirm the plan describes a config-only revert (icmplib backend) under Phase-245 deployed code and does not attempt a git-ref check under `/opt/wanctl`.
    2. Execute the revert: `scripts/phase245-rollback.sh --confirm`.
    3. Verify: scrape Spectrum /health and assert rtt_source.backend == "icmplib" (or producer != "wanctl-backend" if Selection-A is fully reverted at the config level) and NRestarts shows no unexpected extra restarts beyond the rollback's single apply-restart.
    4. Confirm ATT control still untouched.
    5. Record the rollback proof JSON to evidence/.
  </how-to-verify>
  <done>Spectrum is back on the Snapshot-A config state (icmplib backend, Phase-245 code deployed), verified via /health; rollback proof JSON recorded; ATT untouched.</done>
  <resume-signal>Type "approved" if Spectrum is back on the icmplib backend config (Snapshot-A config state, Phase-245 code still deployed) and verified via /health, or describe the rollback issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Record the verdict artifact + fresh SAFE-17 boundary evidence</name>
  <files>.planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md, .planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json</files>
  <read_first>
    - .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.json (the gate-eval output from Task 3)
    - scripts/phase245-safe17-boundary-check.sh (the boundary verifier from Plan 01)
    - .planning/phases/245-live-a-b-rollback-anchor/245-RESEARCH.md (the AB-03 dimensions for the summary)
  </read_first>
  <action>
    Write .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md summarizing the recorded A/B: the outcome (pass/rollback_trigger/input_error), the recommendation (keep-icmplib | switch-eligible), each of the six AB-03 dimension results with its measured value vs threshold (for the restart dimension, report baseline_nrestarts, planned_restarts, and computed unexpected_restarts vs MAX_UNEXPECTED_RESTARTS), the provenance block (thresholds_blob_sha + prereg_commit_sha + evidence_commit), the run window/length, restart counts, and a one-line statement of which state steering was left in: the Snapshot-A config state (icmplib backend, Phase-245 code deployed). Reference the verdict JSON and evidence JSONL by path; do NOT duplicate threshold literals (they live in the frozen JSON). State explicitly that the production default flip is deferred to Phase 246 (FLIP-01), and that the end-of-245 state is a CONFIG-only revert to icmplib (the Phase-245 code remains deployed; it is NOT a code rollback to ffaa8a0e).
    Run the phase-boundary SAFE-17 verifier against the final tree: `bash scripts/phase245-safe17-boundary-check.sh` and confirm it writes evidence/safe17-boundary-245.json with passed:true and the controller-path diff bounded to src/wanctl/steering/daemon.py and src/wanctl/steering/health.py (the flip + the additive count exposure) with no classification drift.
  </action>
  <verify>
    <automated>bash scripts/phase245-safe17-boundary-check.sh && python3 -c "import json; v=json.load(open('.planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.json')); assert 'outcome' in v" && python3 -c "import json; s=json.load(open('.planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json')); assert s.get('passed') is True"</automated>
  </verify>
  <acceptance_criteria>
    - 245-AB-VERDICT.md exists and states the outcome, recommendation, all six AB-03 dimension results (incl. baseline/planned/unexpected restart breakdown), the provenance SHAs, and the final steering state
    - 245-AB-VERDICT.md states the final steering state is the Snapshot-A config state (icmplib backend, Phase-245 code deployed) — explicitly a config-only revert, NOT a code rollback to ffaa8a0e
    - 245-AB-VERDICT.md states the production default flip is deferred to Phase 246 (FLIP-01)
    - evidence/safe17-boundary-245.json has passed == true with the diff bounded to src/wanctl/steering/daemon.py and src/wanctl/steering/health.py
    - `bash scripts/phase245-safe17-boundary-check.sh` exits 0 at the phase boundary
    - The verdict JSON descends from the frozen-thresholds prereg commit (assert-descends passed in Task 3 and is referenced in the verdict markdown)
  </acceptance_criteria>
  <done>The A/B verdict is recorded with full provenance and the six-dimension breakdown (incl. the planned/unexpected restart accounting), the final steering state is documented as the Snapshot-A config state (config-only icmplib revert, Phase-245 code deployed), the production-default flip is explicitly deferred to Phase 246, and the phase-boundary SAFE-17 verifier passes with the flip diff bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production cake-shaper | Every mutation (deploy, config flip per window, restart, rollback) crosses into the 24/7 control system; each is a blocking operator checkpoint |
| live A/B evidence → verdict integrity | Evidence must descend from the frozen-thresholds commit or the verdict is invalid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-09 | Denial of Service (self) | live steering daemon under the flip | mitigate | Read-only preflight; deploy-time seam-live confirmation; probe() try/except (Plan 02) so a raised probe never crashes the daemon; FPING-05 stall/death tolerance; armed config-only Snapshot-A rollback; planned vs unexpected restart accounting bounded + a verdict metric. |
| T-245-10 | Tampering | A/B evidence vs frozen thresholds | mitigate | assert-blob-unchanged before the run + assert-descends after; verdict carries provenance SHAs. |
| T-245-11 | Repudiation of result | confounded verdict | mitigate | Interleaved alternating-window design (AB-02) + ATT control held byte-stable; min-backend-cycle-fraction guard proves the seam (not autorate) produced the samples; unexpected-restart gate distinguishes planned apply-restarts from instability. |
| T-245-SC | Tampering | npm/pip/cargo installs | accept | No package installs (fping vetted in 241-243). |
</threat_model>

<verification>
- Preflight transcript saved; fping present, steering active, both /health respond, and the production flat-file hash proof confirms cake-shaper:/opt/wanctl matches ffaa8a0e (Snapshot-A anchor).
- Spectrum /health shows producer="wanctl-backend" with climbing wanctl_backend count post-deploy; ATT untouched.
- Evidence descends from the frozen-thresholds prereg commit.
- Verdict recorded (pass/rollback_trigger/input_error); keep-icmplib is a valid pass.
- Steering returned to the Snapshot-A config state (icmplib backend, Phase-245 code deployed) via the armed config-only rollback, /health verified.
- `bash scripts/phase245-safe17-boundary-check.sh` passes at the phase boundary.
</verification>

<success_criteria>
- A pre-registered interleaved live A/B ran on Spectrum with ATT as control, with the Snapshot-A flat-file anchor confirmed on the production host (AB-01, AB-02).
- The verdict was computed against thresholds frozen before data, recorded with provenance; keep-icmplib is a valid passing close (AB-03).
- /health attribution confirmed the intended backend produced samples on the WAN under test.
- Steering was returned to the Snapshot-A config state (config-only icmplib revert); SAFE-17 passes at the phase boundary.
</success_criteria>

<output>
Create `.planning/phases/245-live-a-b-rollback-anchor/245-04-SUMMARY.md` when done.
</output>
