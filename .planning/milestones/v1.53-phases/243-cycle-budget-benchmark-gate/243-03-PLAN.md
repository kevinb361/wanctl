---
phase: 243-cycle-budget-benchmark-gate
plan: 03
type: execute
wave: 2
depends_on: ["243-01"]
files_modified:
  - scripts/phase243-gate-eval.py
  - tests/test_phase243_gate_eval.py
autonomous: true
requirements: [BENCH-02]
user_setup: []

must_haves:
  truths:
    - "D-02: The same-run icmplib control arm is the PRIMARY gate basis — the evaluator computes the fping-minus-icmplib delta on the identical host/run/load and blocks on >20% avg or p99 regression; the historical 2.85ms/6.9ms baseline drives a HARD representativeness VALIDITY GATE (not a warning)"
    - "HIGH-4: if the same-run icmplib arm lands outside the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band, the evaluator returns a non-pass blocking outcome (outcome: input_error), aborting the run rather than passing with a warning"
    - "The gate derives cpu_delta_pts from the per-arm CPUUsageNSec window deltas (fping arm minus icmplib arm, same WAN/load) using the frozen CPU_NORMALIZATION basis (per_core), and enforces the frozen CPU% bound"
    - "The n-floor uses the frozen CYCLE_HZ constant (max(MIN_CYCLES, CYCLE_HZ*60*MIN_MINUTES)), not an implicit 20Hz literal"
    - "The gate enforces the absolute p99 ceiling, CPU delta bound, zombie/fd/Tasks hygiene, STALL events, and the D-04c n-floor"
    - "Thresholds are loaded only from the frozen scripts/phase243-thresholds.json, never inline literals"
    - "Incomplete arms (missing autorate_cycle_total, missing cpu_nsec, missing invocation_id, or below n-floor) fail closed, not silently pass"
    - "The verdict records the frozen thresholds blob SHA + prereg commit SHA (via the Plan 01 provenance helper) so it cannot be rationalized against moved goalposts"
  artifacts:
    - path: "scripts/phase243-gate-eval.py"
      provides: "Frozen-threshold verdict over per-arm evidence -> 243-BENCHMARK-VERDICT.json, incl. a hard representativeness validity gate"
      contains: "load_thresholds"
    - path: "tests/test_phase243_gate_eval.py"
      provides: "Pass fixture + one fixture per fail mode (delta, ceiling, CPU, zombie, fd-trend, stall, n-floor) + representativeness input_error fixture"
  key_links:
    - from: "scripts/phase243-gate-eval.py"
      to: "scripts/phase243-thresholds.json"
      via: "load_thresholds() reads the frozen JSON (CPU_DELTA_PCT_POINTS, CYCLE_HZ, CPU_NORMALIZATION, ICMPLIB_REPRESENTATIVE_*_TOL_MS all frozen)"
      pattern: "phase243-thresholds\\.json"
    - from: "scripts/phase243-gate-eval.py"
      to: "same-run icmplib arm"
      via: "delta = (fping - icmplib) / icmplib"
      pattern: "delta"
    - from: "scripts/phase243-gate-eval.py"
      to: "representativeness validity gate"
      via: "abs(icmplib_avg - ICMPLIB_REPRESENTATIVE_AVG_MS) <= ICMPLIB_REPRESENTATIVE_AVG_TOL_MS else input_error"
      pattern: "input_error"
    - from: "scripts/phase243-gate-eval.py"
      to: "scripts/phase243-prereg-provenance.sh"
      via: "verdict records thresholds_blob_sha + prereg_commit_sha from the provenance helper"
      pattern: "prereg"
    - from: "scripts/phase243-gate-eval.py"
      to: "per-arm CPUUsageNSec window delta (Plan 02/04 evidence)"
      via: "cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct"
      pattern: "cpu_delta_pts"
---

<objective>
Build the pre-registered gate evaluator that turns per-arm benchmark evidence into a recorded pass/fail verdict against the frozen D-04 thresholds.

Purpose: BENCH-02 is the hard gate that blocks the Phase 245 live A/B on regression. The evaluator computes fping-vs-same-run-icmplib deltas (D-02 primary basis), derives the CPU% delta from the per-arm CPUUsageNSec window deltas captured by Plan 02/04, and enforces the absolute ceiling and hygiene/STALL/validity gates (D-04/D-04a/D-04b/D-04c), failing closed on incomplete arms. The cross-AI review (243-REVIEWS.md) found one HIGH this plan owns — HIGH-4: the icmplib representativeness check must be a HARD validity gate (return input_error / abort) reading a frozen tolerance band, not a soft warn — plus three MEDIUMs folded in: pre-register and apply the CPU% normalization basis (CPU_NORMALIZATION=per_core) from the frozen JSON; take the cadence from the frozen CYCLE_HZ (no implicit 20Hz); and record the thresholds blob SHA + prereg commit SHA in the verdict via the Plan 01 provenance helper. Thresholds come only from the frozen JSON committed in Plan 01.

Output: scripts/phase243-gate-eval.py + its full pass/fail-mode test matrix (incl. the representativeness input_error case).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md

<interfaces>
<!-- Frozen thresholds JSON (created in Plan 01, scripts/phase243-thresholds.json; frozen -->
<!-- values — load via load_thresholds, never inline): -->
CYCLE_AVG_REGRESSION_PCT, CYCLE_P99_REGRESSION_PCT, CYCLE_P99_ABS_CEILING_MS,
CPU_DELTA_PCT_POINTS (2.0), ZOMBIES_MAX, TASKS_BOUND, STALL_GAP_MS, MIN_CYCLES, MIN_MINUTES,
ICMPLIB_REPRESENTATIVE_AVG_MS (2.85), ICMPLIB_REPRESENTATIVE_P99_MS (6.9),
CYCLE_HZ (20), CYCLE_INTERVAL_MS (50.0),
ICMPLIB_REPRESENTATIVE_AVG_TOL_MS, ICMPLIB_REPRESENTATIVE_P99_TOL_MS,  <!-- HIGH-4 validity band -->
CPU_NORMALIZATION ("per_core")                                          <!-- MEDIUM: CPU% basis -->

<!-- Per-arm profile JSON shape (produced by phase243-cycle-rollup.py, Plan 02; -->
<!-- now also carries invocation_id + parse_counters): -->
{ "invocation_id": "<id>", "parse_counters": {...}, "autorate_cycle_total": {"count": <n>, "avg_ms": <f>, "p99_ms": <f>, ...}, "stall": {"stall_events": [...], "max_gap_ms": <f>} }

<!-- Per-arm evidence CPU fields (rolled in by the launcher, Plan 04, via systemctl CPUUsageNSec): -->
{ "cpu_nsec_start": <int>, "cpu_nsec_end": <int>, "cpu_nsec_delta": <int>, "window_wall_sec": <f>, "n_cores": <int> }
<!-- CPU_NORMALIZATION="per_core": cpu_pct = cpu_nsec_delta / (window_wall_sec * 1e9 * n_cores) * 100 -->
<!-- then cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load). -->

<!-- Per-arm hygiene NDJSON rows (phase243-hygiene-sampler.sh, Plan 02; cpu_nsec is the per-tick -->
<!-- redundant/finer CPU source — last minus first row is an alternate cpu_nsec_delta): -->
{"t":<epoch>,"fd":<n>,"tasks":<n>,"zombies":<n>,"cpu_nsec":<n>}

<!-- Prereg provenance helper (Plan 01) the verdict consumes: -->
scripts/phase243-prereg-provenance.sh record  -> {thresholds_blob_sha, prereg_commit_sha, thresholds_path}

<!-- phase206 / phase224 gate-eval patterns to reuse: -->
- scripts/phase206-gate-check.py :: load_thresholds(path) -> dict ; EXIT_PASS=0 EXIT_BLOCK=1 EXIT_ABORT=2 ; zero-baseline delta-% policy
- scripts/phase224-gate-eval.py :: GateEvalError(message, gate_id) ; _gate(verdict, value, **extra) ; per-gate dict + final outcome scan ; json.dumps(..., indent=2, sort_keys=True)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate evaluator — frozen-threshold verdict + hard representativeness validity gate + fail-closed incomplete-arm handling</name>
  <read_first>
    - scripts/phase206-gate-check.py lines 1-50 (load_thresholds() reading the JSON, EXIT_PASS/BLOCK/ABORT constants, structured error classes) and the delta-% with zero-baseline guard (the `((cur-pre)/pre)*100 if pre>0 else ...` policy)
    - scripts/phase224-gate-eval.py (entire file — GateEvalError lines 25-31, _gate helper lines 73-81, the per-gate dict + final rollback_trigger/outcome scan lines 211-244, json output lines 283-288, exit-code main() lines 261-293)
    - scripts/phase243-thresholds.json (the frozen thresholds created in Plan 01 — load via load_thresholds, never inline; incl. CYCLE_HZ, CPU_NORMALIZATION, ICMPLIB_REPRESENTATIVE_*_TOL_MS)
    - scripts/phase243-prereg-provenance.sh (Plan 01 — the helper whose `record` output the verdict embeds: thresholds_blob_sha + prereg_commit_sha)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md lines 56-74 + 133-148 (HIGH-4 representativeness-must-abort; the CPU-normalization, CYCLE_HZ, and verdict-records-SHA MEDIUMs)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 159-194 (gate-eval instructions: load_thresholds copy-verbatim; D-04 per-gate delta math; per-gate dict + outcome; fail-closed on incomplete arms; "keep icmplib" valid passing close)
  </read_first>
  <behavior>
    - Pass fixture: fping avg/p99 within 20% of same-run icmplib, p99 < 10ms, cpu_delta_pts < CPU_DELTA_PCT_POINTS, zero zombies, flat fd, bounded Tasks, zero stall events, icmplib arm WITHIN the representativeness band, count >= max(MIN_CYCLES, CYCLE_HZ*60*MIN_MINUTES) -> outcome "pass"
    - representativeness fixture (HIGH-4): same-run icmplib arm avg or p99 OUTSIDE the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band -> outcome "input_error" (blocking, non-pass), NOT "pass", regardless of the fping delta — the run is suspect and must abort
    - avg-regression fixture: fping avg > 1.20x icmplib avg -> a "fail" gate, outcome "rollback_trigger"
    - p99-regression fixture: fping p99 > 1.20x icmplib p99 -> fail
    - ceiling fixture: fping p99 >= 10.0ms (even if delta < 20%) -> fail
    - CPU fixture: cpu_delta_pts (fping_arm_cpu_pct - icmplib_arm_cpu_pct, per_core normalization) >= CPU_DELTA_PCT_POINTS (2.0) -> fail
    - zombie fixture: max(zombies) > 0 -> fail
    - fd-trend fixture: monotonic-upward fd across windows -> fail
    - stall fixture: stall_events non-empty (gap > 100ms) -> fail
    - n-floor fixture: count < max(MIN_CYCLES, CYCLE_HZ*60*MIN_MINUTES) -> fail-closed (incomplete arm), NOT silent pass
    - missing autorate_cycle_total OR missing cpu_nsec evidence OR missing invocation_id -> GateEvalError / fail-closed
  </behavior>
  <files>scripts/phase243-gate-eval.py</files>
  <action>
    Create scripts/phase243-gate-eval.py reusing the phase206/phase224 patterns. Copy load_thresholds() verbatim so ALL figures come ONLY from scripts/phase243-thresholds.json — no inline literals, including CPU_DELTA_PCT_POINTS, CYCLE_HZ, CPU_NORMALIZATION, and the ICMPLIB_REPRESENTATIVE_*_TOL_MS band. Define EXIT_PASS=0 / EXIT_BLOCK=1 / EXIT_ABORT=2 and a GateEvalError(message, gate_id) structured error. Use a third terminal outcome string "input_error" (mapped to EXIT_ABORT) distinct from "rollback_trigger" (mapped to EXIT_BLOCK) and "pass" (EXIT_PASS).
    REPRESENTATIVENESS VALIDITY GATE (HIGH-4 — runs FIRST, before the regression gates): for each WAN, check the same-run icmplib arm: if abs(icmplib.avg_ms - ICMPLIB_REPRESENTATIVE_AVG_MS) > ICMPLIB_REPRESENTATIVE_AVG_TOL_MS OR abs(icmplib.p99_ms - ICMPLIB_REPRESENTATIVE_P99_MS) > ICMPLIB_REPRESENTATIVE_P99_TOL_MS, set outcome "input_error" and return a blocking (non-pass) verdict — the dev host is unrepresentative so the whole run is suspect (D-02), abort rather than warn. This is no longer advisory.
    The evaluator takes, per WAN, the fping-arm and the same-run icmplib-arm profile JSONs plus their hygiene NDJSON and the per-arm CPU evidence fields, and computes (D-02 primary basis = same-run delta, NOT historical 2.85/6.9): avg_delta_pct and p99_delta_pct = ((fping - icmplib)/icmplib)*100 with the zero-baseline guard; require each <= the frozen regression pct; require fping.p99_ms < CYCLE_P99_ABS_CEILING_MS (absolute). CPU GATE (D-04): derive each arm's cpu_pct using the frozen CPU_NORMALIZATION basis — for "per_core", cpu_pct = cpu_nsec_delta / (window_wall_sec * 1e9 * n_cores) * 100 (percent of whole machine); the normalization is read from the JSON, not hardcoded, so it is auditable. Compute cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load); require cpu_delta_pts < CPU_DELTA_PCT_POINTS (frozen 2.0). require max(hygiene.zombies) <= ZOMBIES_MAX; require fd is not monotonic-upward (windowed-median test: split the fd series into N windows, fail if window medians are strictly increasing across all windows); require max(hygiene.tasks) <= tasks_baseline + TASKS_BOUND; require stall_events count == 0; require autorate_cycle_total.count >= max(MIN_CYCLES, CYCLE_HZ * 60 * MIN_MINUTES) — the cadence comes from the frozen CYCLE_HZ, not an implicit 20 (MEDIUM). FAIL CLOSED on incomplete arms — missing autorate_cycle_total, missing cpu_nsec evidence (cannot derive cpu_delta_pts), missing invocation_id (HIGH-3 contamination guard from Plan 02), or count below the floor must raise/return a fail, never silently pass (V5 control). Build a per-gate verdict dict via a _gate() helper; the final outcome is "input_error" if representativeness failed, else "rollback_trigger" if any gate fails, else "pass". PROVENANCE (MEDIUM): invoke scripts/phase243-prereg-provenance.sh record (subprocess) and embed thresholds_blob_sha + prereg_commit_sha into the verdict, so the verdict cannot be re-derived against moved goalposts; the runbook (Plan 04) asserts evidence commits descend from prereg_commit_sha. Write the verdict to 243-BENCHMARK-VERDICT.json via json.dumps(verdict, indent=2, sort_keys=True) + "\n", referencing the frozen-thresholds path and the derived per-arm cpu_pct + the per-arm invocation_ids. Note "keep icmplib" is a valid passing close at the AB level — this gate blocks only on regression or on an unrepresentable host. main() returns the exit code. Stdlib only.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_gate_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The evaluator imports its thresholds via load_thresholds() from scripts/phase243-thresholds.json (no inline literals); `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'load_thresholds'` >= 1
    - The representativeness check is a HARD gate: a fixture with the same-run icmplib arm outside the frozen tolerance band yields outcome "input_error" and a nonzero (EXIT_ABORT) exit — NOT "pass" and NOT a warning (HIGH-4); `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'input_error'` >= 1
    - The evaluator derives cpu_delta_pts from the per-arm cpu_nsec window deltas using the frozen CPU_NORMALIZATION and gates it against CPU_DELTA_PCT_POINTS (2.0); `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'cpu_delta_pts'` >= 1
    - The n-floor uses the frozen CYCLE_HZ (no inline `20`); `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'CYCLE_HZ'` >= 1
    - The verdict JSON embeds thresholds_blob_sha and prereg_commit_sha (from the provenance helper)
    - A pass fixture yields outcome "pass" and exit 0; an avg-regression fixture yields a failing gate and nonzero exit; the p99 absolute-ceiling gate fails when fping p99 >= 10.0ms even if the delta is under 20%
    - An arm with count below the floor OR missing cpu_nsec OR missing invocation_id fails closed (not a silent pass)
    - `.venv/bin/ruff check scripts/phase243-gate-eval.py` clean
  </acceptance_criteria>
  <done>Gate evaluator runs the hard representativeness validity gate (input_error) first, computes the same-run delta + cpu_delta_pts (per_core), enforces the n-floor via frozen CYCLE_HZ, embeds the prereg blob/commit SHAs, fails closed on incomplete arms, writes the verdict JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-eval test matrix — pass + representativeness input_error + one fixture per fail mode</name>
  <read_first>
    - scripts/phase243-gate-eval.py (the evaluator under test — created in Task 1)
    - scripts/phase243-thresholds.json (the frozen thresholds the tests assert against; incl. the ICMPLIB_REPRESENTATIVE_*_TOL_MS band and CPU_NORMALIZATION)
    - scripts/phase243-prereg-provenance.sh (Plan 01 — the provenance helper the evaluator shells out to via `record`; the test exercises it as a REAL subprocess, no mock, because the thresholds JSON is already committed in Wave 1)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md lines 56-74 (HIGH-4 + the CPU/CYCLE_HZ MEDIUMs the tests must lock in)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 287-292 (gate_eval test: synthetic same-run arms, one pass fixture + one per fail mode, mirroring the 241 fail-mode coverage style)
  </read_first>
  <files>tests/test_phase243_gate_eval.py</files>
  <action>
    Create tests/test_phase243_gate_eval.py with synthetic same-run-arm fixtures (built inline / tmp_path) exercising every behavior listed in Task 1's behavior block: one all-pass fixture (outcome "pass", exit 0) and one fixture per fail mode — avg-delta regression, p99-delta regression, p99 absolute ceiling, CPU delta, nonzero zombie, monotonic-upward fd trend, non-empty stall_events, and below-n-floor (assert fail-closed, not silent pass). ADD a representativeness fixture (HIGH-4): the same-run icmplib arm avg (or p99) set OUTSIDE the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band while the fping delta is WITHIN bounds — assert the outcome is "input_error" (blocking, EXIT_ABORT, nonzero exit), proving an unrepresentative host aborts rather than passes. The CPU fixture must supply per-arm cpu_nsec_delta/window_wall_sec/n_cores such that the per_core-normalized fping cpu_pct exceeds the icmplib cpu_pct by >= 2.0 points and assert the CPU gate fails; the pass fixture must keep cpu_delta_pts < 2.0. Add a fixture with missing cpu_nsec evidence and one with missing invocation_id and assert both fail closed. Each fail fixture must assert the corresponding gate verdict is "fail"/blocking and the process exit code is nonzero. Add an n-floor boundary test that proves the floor is computed from the frozen CYCLE_HZ (e.g. a fixture whose count is just under CYCLE_HZ*60*MIN_MINUTES fails-closed while just over passes the floor). Assert a boundary fixture at the 20% / 10ms / 2.0-pt edge flips the verdict (proving the figures are read from the frozen thresholds JSON). Invoke the evaluator either by importing its evaluate()/main() with argv or via a subprocess run() helper; be consistent.
    PROVENANCE SUBPROCESS HANDLING (no mock — the helper runs for real): the evaluator shells out to `scripts/phase243-prereg-provenance.sh record` to stamp thresholds_blob_sha + prereg_commit_sha into the verdict. Plan 03 runs in Wave 2, AFTER Plan 01 commits scripts/phase243-thresholds.json in Wave 1, so the thresholds file is a committed git blob and the helper resolves real SHAs — do NOT stub or monkeypatch the helper. In the all-pass fixture, after running the evaluator, assert the emitted 243-BENCHMARK-VERDICT.json carries a thresholds_blob_sha and prereg_commit_sha that are each 40-hex strings, and that `scripts/phase243-prereg-provenance.sh assert-blob-unchanged <thresholds_blob_sha from the verdict>` exits 0 (the frozen thresholds blob the verdict was computed against still matches HEAD). If the test environment somehow has an uncommitted thresholds file (helper cannot resolve a blob SHA), the evaluator must fail closed (GateEvalError / nonzero), not emit a verdict with empty SHAs — assert that posture too if convenient, otherwise rely on the committed-blob precondition stated above.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_gate_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase243_gate_eval.py -x` exits 0
    - There is one passing-arm test plus at least 8 distinct fail-mode tests (avg, p99, ceiling, CPU, zombie, fd-trend, stall, n-floor) PLUS a representativeness input_error test
    - The representativeness test asserts outcome "input_error" + nonzero exit when the icmplib arm is outside the frozen band (HIGH-4)
    - The CPU test supplies cpu_nsec window-delta evidence yielding cpu_delta_pts >= 2.0 (per_core) and asserts the CPU gate fails; a missing-cpu_nsec fixture and a missing-invocation_id fixture each assert fail-closed
    - The n-floor test asserts the floor is derived from the frozen CYCLE_HZ
    - A boundary test proves the 20% / 10ms / 2.0-pt figures are read from the frozen thresholds JSON
    - The pass fixture runs the REAL `scripts/phase243-prereg-provenance.sh record` (no mock) and asserts the verdict's thresholds_blob_sha and prereg_commit_sha are each 40-hex, and that `scripts/phase243-prereg-provenance.sh assert-blob-unchanged <verdict blob_sha>` exits 0
  </acceptance_criteria>
  <done>Full pass + fail-mode matrix green, incl. the hard representativeness input_error case, proving the gate honors the frozen D-04 thresholds (CPU% via per_core, n-floor via CYCLE_HZ) and fails closed on incomplete arms; the verdict's prereg blob/commit SHAs are validated against the real provenance helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-arm evidence JSON → gate-eval | incomplete/malformed/unrepresentative arms could yield a false pass |
| frozen thresholds JSON → gate-eval | thresholds must not be overridable by inline literals |
| prereg commit → verdict | the verdict must prove it gated against the pre-committed (unchanged) thresholds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-08 | Tampering / Repudiation | incomplete arm silently passes | mitigate | Fail-closed on missing autorate_cycle_total, missing cpu_nsec evidence, missing invocation_id, or count below the D-04c floor — GateEvalError / fail verdict, never silent pass (V5 input validation) |
| T-243-08a | Tampering / Repudiation | unrepresentative dev host masks/fabricates a regression | mitigate | HIGH-4: representativeness is a HARD validity gate — icmplib arm outside the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band returns outcome "input_error" (EXIT_ABORT), not a warning; the run is rejected before any fping verdict is trusted (D-02) |
| T-243-09 | Repudiation | post-hoc threshold rationalization | mitigate | load_thresholds() reads ONLY scripts/phase243-thresholds.json (committed in Plan 01 before evidence); the verdict embeds thresholds_blob_sha + prereg_commit_sha from scripts/phase243-prereg-provenance.sh so the gate can't be re-derived against moved goalposts (closes the review MEDIUM) |
| T-243-10 | Tampering | wrong-baseline gate (historical vs same-run) | mitigate | Delta computed against the same-run icmplib arm (D-02 primary); CPU% delta computed fping-arm vs same-run icmplib-arm with the frozen per_core normalization; historical 2.85/6.9 drives the validity gate, never the regression baseline |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs (stdlib only) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase243_gate_eval.py -q` green (pass + representativeness input_error + all fail modes incl. CPU delta)
- `.venv/bin/ruff check scripts/phase243-gate-eval.py` and `.venv/bin/mypy src/wanctl/` (no src/wanctl change; mypy unaffected)
- Zero src/wanctl edits (verified by the Plan 01 SAFE-17 verifier at wave merge)
</verification>

<success_criteria>
- Gate evaluator aborts (input_error) on an unrepresentative icmplib arm and blocks on any D-04/D-04a/D-04b/D-04c regression vs the same-run icmplib arm
- The CPU% gate derives cpu_delta_pts via the frozen per_core normalization and enforces the frozen 2.0-pt bound; the n-floor uses the frozen CYCLE_HZ
- Thresholds sourced only from the frozen JSON; the verdict embeds the prereg blob/commit SHAs; incomplete arms fail closed
- Full pass + fail-mode test matrix green incl. the representativeness input_error case
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-03-SUMMARY.md` when done
</output>
</content>
</invoke>
