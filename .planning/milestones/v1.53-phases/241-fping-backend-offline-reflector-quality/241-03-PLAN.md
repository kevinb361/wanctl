---
phase: 241-fping-backend-offline-reflector-quality
plan: 03
type: execute
wave: 2
depends_on: ["241-01"]
files_modified:
  - scripts/capture-fping-fixtures.sh
  - scripts/capture_fping_fixtures.py
  - tests/fixtures/fping/reply.txt
  - tests/fixtures/fping/total_loss.txt
  - tests/fixtures/fping/partial_loss.txt
  - tests/fixtures/fping/partial_line.txt
  - tests/fixtures/fping/banner_noise.txt
  - tests/fixtures/fping/process_death.txt
  - tests/test_fping_measurement.py
autonomous: false
requirements: [FPING-04, REFL-01]

must_haves:
  truths:
    - "A capture helper ships that the operator runs on the live host to capture real fping 5.1 output for the six D-03 scenarios"
    - "The capture command is byte-identical to FpingMeasurement._build_command, proven MECHANICALLY (not by grep): the helper has a --print-command mode whose output is compared against a Python call to FpingMeasurement._build_command() (resolves review M3)"
    - "The capture helper is non-mutating to production routing/shaping (D-08 safety)"
    - "Each fixture carries metadata (fping version, exact command, stdout/stderr split, returncode) so tests can reconstruct a real CompletedProcess(stdout=..., stderr=..., returncode=...) (resolves review M3)"
    - "PROCESS-DEATH RETURNCODE FIDELITY (resolves review NEW-HIGH #2): the process_death scenario is captured so the recorded returncode is the PYTHON-EQUIVALENT NEGATIVE value (e.g. -15 for SIGTERM), NOT the shell 128+N (e.g. 143). This is achieved by capturing the process-death scenario via a Python subprocess.Popen/subprocess.run helper (the returncode IS the genuine Python negative value); if any scenario is captured via bash, the helper MUST normalize shell `128+N` -> `-N` when writing the fixture's returncode metadata. Plan 01's negative-returncode process-death gate keys on returncode < 0, so the captured fixture MUST yield a negative returncode or that gate is never exercised."
    - "PER-SCENARIO SHAPE VALIDATION (resolves review NEW-HIGH #2 + partial-loss/TEST-NET determinism): the capture helper validates each captured scenario's shape and FAILS LOUDLY (non-zero exit, no fixture written) on a malformed capture, so a bad capture can NOT silently produce a fixture that passes 143 to a < 0 gate, nor a partial-loss capture that came back all-reply (0%) or all-loss (100%). Required shapes: process_death -> returncode is NEGATIVE (< 0 / == -15); partial_loss -> the SAME host line includes BOTH at least one real RTT float AND at least one `-` token; total_loss -> all tokens are `-`; reply -> all tokens are floats (loss 0); TEST-NET 192.0.2.1 may emit routing/ICMP-error lines (banner_noise/total_loss) and those are tolerated as non-host noise, not as a clean host line."
    - "Tests feed the parser via a simulated real CompletedProcess with the captured stdout/stderr/returncode split, not bare stdout text (resolves review M3 + ties to Plan 01 H2 combined-stream parse + Plan 01 negative-returncode gate)"
    - "Committed real fping 5.1 captures replace the bootstrap fixtures and the parser/scorer tests pass against the real captures"
  artifacts:
    - path: "scripts/capture-fping-fixtures.sh"
      provides: "operator-run live-host capture orchestrator for the six D-03 scenarios, with a --print-command mechanical-check mode; delegates process-death (and returncode metadata) to the Python capture helper for genuine negative-returncode fidelity"
      contains: "print-command"
    - path: "scripts/capture_fping_fixtures.py"
      provides: "Python subprocess.Popen/run capture helper that records the GENUINE Python returncode (negative on signal death) + stdout/stderr split + version metadata, and shape-validates each scenario, failing loudly on a malformed capture"
      contains: "returncode"
    - path: "tests/fixtures/fping/process_death.txt"
      provides: "real operator-captured fping 5.1 process-death sample whose recorded returncode is the Python-equivalent NEGATIVE value (e.g. -15), exercising Plan 01's returncode < 0 gate"
  key_links:
    - from: "scripts/capture-fping-fixtures.sh"
      to: "src/wanctl/fping_measurement.py::_build_command"
      via: "byte-identical fping invocation proven via --print-command vs _build_command()"
      pattern: "_build_command"
    - from: "scripts/capture_fping_fixtures.py"
      to: "src/wanctl/fping_measurement.py::probe (negative-returncode process-death gate)"
      via: "the process_death fixture's recorded returncode is NEGATIVE so Plan 01's `returncode < 0` gate is exercised against real captured metadata"
      pattern: "returncode"
---

<objective>
Ship the D-08 operator-run capture helper, have the operator (Kevin) run it on the
LIVE host to capture real fping 5.1 output for the six D-03 scenarios, commit those
real captures (with stdout/stderr/returncode/version metadata) as the binding test
fixtures (replacing Plan 01's synthetic bootstraps), prove the capture command is
byte-identical to `_build_command` MECHANICALLY, ensure the process-death returncode is
the genuine Python-equivalent NEGATIVE value (so Plan 01's `returncode < 0` gate is
actually exercised — review NEW-HIGH #2), shape-validate every scenario so a bad capture
fails loudly, and re-confirm the parser + scorer tests pass against real output fed
through a simulated real CompletedProcess.

This is an OPERATOR-IN-THE-LOOP plan: fping is absent on the dev VM, and D-08 requires
REAL 5.1 captures as the binding FPING-04 proof. The capture step is a blocking
human-action checkpoint — do not silently skip it.

Purpose: convert the FPING-04 proof from synthetic to real captured 5.1 samples (D-08)
with a mechanically-verified byte-identical command, Python-fidelity negative process-death
returncodes, per-scenario shape validation, and stdout/stderr-aware tests.
Output: `scripts/capture-fping-fixtures.sh` (+ `scripts/capture_fping_fixtures.py`), six
real fixtures with metadata, re-pointed tests that feed real CompletedProcess shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-CONTEXT.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-PATTERNS.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md
@src/wanctl/fping_measurement.py

<interfaces>
<!-- The capture command MUST equal FpingMeasurement._build_command (Pitfall 6 / review M3). -->
Default invocation (from Plan 01 builder, -D OMITTED per OQ3, argv[0] is the RESOLVED fping path):
  <resolved-fping-path> -C 5 -p 200 -q [-S <source_ip>] <reflector1> <reflector2> ...
Mechanical byte-identity check (M3): the script's --print-command output must equal
  `python -c "...FpingMeasurement(cfg, log)._build_command(hosts)..."` for the same
  source_ip + hosts (compare the argv lists, not a grep). Note: _build_command resolves the
  binary via shutil.which, so the comparison must account for the same resolution (compare
  the flag/host portion of the argv exactly; argv[0] is the resolved fping path on the host).
The six D-03 scenarios + safe operator capture methods (RESEARCH fixture table):
  reply         : normal reflectors                      -> all floats, loss 0.0
  total_loss    : TEST-NET blackhole 192.0.2.1           -> all '-', loss 100.0
  partial_loss  : distant/lossy target or tc-induced drop-> mix, loss = dash/C*100
  partial_line  : truncated/interrupted stdout           -> tolerate, never 0ms
  banner_noise  : stderr/ICMP-error interleaving         -> ignore non-host lines (note: a
                  VALID host line may land on stderr — capture both streams so Plan 01's
                  combined-stream parse (H2) is exercised against real output)
  process_death : mid-burst kill/SIGTERM                 -> short read + NEGATIVE returncode (-15) -> probe None
Fixture metadata to record per scenario (M3): fping version, exact command (from
  --print-command), the stdout text, the stderr text, and the process returncode — so a
  test can rebuild CompletedProcess(args=..., stdout=..., stderr=..., returncode=...).
RETURNCODE FIDELITY (review NEW-HIGH #2): the process_death returncode MUST be the Python
  CompletedProcess.returncode equivalent — NEGATIVE on signal death (e.g. -15 SIGTERM),
  NOT the shell 128+N (143). subprocess.Popen/run reports -N directly; a bash capture must
  normalize 128+N -> -N before writing the metadata.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: capture helper (D-08, mechanically byte-identical command, Python-fidelity returncodes, per-scenario shape validation, non-mutating)</name>
  <read_first>
    - src/wanctl/fping_measurement.py  (_build_command from Plan 01 — the capture helper MUST build the identical fping invocation; OMIT -D per OQ3; argv[0] is the RESOLVED binary path; the --print-command check compares against this; AND Plan 01's probe() negative-returncode gate keys on returncode < 0 — the process_death fixture must satisfy it)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (six D-03 fixture scenarios table; Pitfall 6 capture-vs-runtime drift; capture-script loss-induction discretion — blackhole/tc/kill, safe and non-mutating)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-PATTERNS.md  (capture-fping-fixtures.sh section — operator-run, safe/non-mutating to production routing)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md  (Plan 03 NEW-HIGH #2 process-death returncode + per-scenario shape validation; MEDIUM M3 --print-command vs _build_command(); MEDIUM partial-loss/TEST-NET determinism; LOW fixture source-IP redaction)
    - scripts/phase240-safe17-boundary-check.sh  (bash conventions reference: set -euo pipefail, usage(), require_command — match house style)
  </read_first>
  <action>
    Ship the capture helper as a small bash orchestrator + a Python capture core. The
    Python core is REQUIRED for process-death returncode fidelity (review NEW-HIGH #2):
    `subprocess.Popen`/`subprocess.run` reports a signal death as a NEGATIVE returncode
    (e.g. -15), exactly what Plan 01's `returncode < 0` gate expects, whereas a bare bash
    `$?` reports `128+N` (143).

    A. `scripts/capture_fping_fixtures.py` (the capture core, operator-run on the live host):
    - `set`-style guards: argparse with `--source-ip`, `--reflectors` (space/comma list),
      `--out-dir` (default `tests/fixtures/fping`), `--scenario` (one of the six or `all`),
      `--print-command`, and a `--lossy-reflector` (operator-supplied distant target for
      partial_loss). Require fping present (except in `--print-command` mode).
    - Build the fping command by IMPORTING `FpingMeasurement._build_command` from
      `src.wanctl.fping_measurement` (single source of truth) OR by mirroring it exactly;
      `--print-command` prints the resolved argv WITHOUT executing.
    - For each scenario run the burst via `subprocess.run(cmd, capture_output=True,
      text=True, timeout=...)` and record `result.returncode` (the GENUINE Python value),
      `result.stdout`, `result.stderr`, and `fping --version`. For `process_death`, start
      the burst via `subprocess.Popen`, sleep a fraction of the burst, send `proc.terminate()`
      (SIGTERM) mid-burst, `proc.wait()`, and record `proc.returncode` — which is the
      NEGATIVE Python value (-15). Do NOT shell out and read `$?` for process_death.
    - SHAPE-VALIDATE each captured scenario BEFORE writing the fixture, and raise/exit
      non-zero (no fixture written) on a malformed capture (review NEW-HIGH #2 +
      determinism):
        * process_death: assert `returncode < 0` (e.g. == -15). If a bash path were used,
          normalize 128+N -> -N first; here Popen already gives -N, so assert < 0.
        * partial_loss: assert at least one host line contains BOTH >=1 float token AND
          >=1 `-` token on the SAME line (a real partial loss, not 0%/100%). If the capture
          came back all-reply or all-loss, FAIL LOUDLY telling the operator to pick a lossier
          target (the script must NOT silently accept it).
        * total_loss: assert the TEST-NET host line is all `-` tokens (route/ICMP-error
          lines tolerated as non-host noise).
        * reply: assert all tokens on the host line(s) are floats (loss 0).
        * banner_noise: assert at least one valid host line AND at least one non-host
          (banner/ICMP-error) line are present across stdout+stderr.
        * partial_line: produced by truncating a captured reply mid-line (post-processing
          the saved file, not a routing change); assert the final line is incomplete.
    - Write each fixture as the real captured sample PLUS a structured metadata header (or a
      sidecar `<scenario>.meta.json`): `# fping-version:`, `# command:`, `# returncode:`,
      and a `# --- stdout ---` / `# --- stderr ---` delimiter, so Task 3 can reconstruct
      `CompletedProcess(args, stdout, stderr, returncode)`.
    - REDACTION (review LOW): provide a `--redact-source` flag that masks the recorded
      source IP / reflector identities in the metadata if repo policy requires (default off;
      document the choice). The fping output RTTs themselves are non-sensitive.
    - NON-MUTATING (D-08): the Python core performs NO routing/CAKE/tc/route mutation; any
      tc/netem for partial_loss is the operator's manual, reverted responsibility (told in
      help text). total_loss uses the TEST-NET blackhole 192.0.2.1 (no route change).

    B. `scripts/capture-fping-fixtures.sh` (thin operator wrapper, `set -euo pipefail`,
    `usage()`, `require_command`): prints the non-mutating preflight banner (no
    routing/CAKE/shaping mutation; tc/netem is the operator's manual responsibility), then
    invokes `scripts/capture_fping_fixtures.py` with the operator's flags. Provide
    `--print-command` passthrough so the mechanical command check works without fping
    installed. The wrapper itself mutates nothing and is abort-safe.

    Do NOT run the capture in this task (fping is absent on the dev VM and it targets the
    live host). Verify only: both files exist and are executable; `bash -n` on the wrapper;
    `python -m py_compile` (or ruff) on the Python core; `--print-command` works without
    fping present and matches `_build_command`'s flag/host portion; the Python core does NOT
    call tc/route mutation; the process_death path uses Popen+terminate (negative returncode)
    not bash `$?`.
  </action>
  <verify>
    <automated>bash -n scripts/capture-fping-fixtures.sh && test -x scripts/capture-fping-fixtures.sh && test -x scripts/capture_fping_fixtures.py && .venv/bin/python -m py_compile scripts/capture_fping_fixtures.py && grep -q "print-command" scripts/capture-fping-fixtures.sh && grep -Eq "terminate|Popen" scripts/capture_fping_fixtures.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-fping-fixtures.sh` exists, executable, passes `bash -n`; `scripts/capture_fping_fixtures.py` exists, executable, passes `.venv/bin/python -m py_compile`
    - process-death is captured via Python with GENUINE negative returncode fidelity: `grep -Eq "terminate|Popen" scripts/capture_fping_fixtures.py` AND a comment/code path asserts `returncode < 0` for process_death (NOT shell 128+N) — `grep -n "returncode < 0" scripts/capture_fping_fixtures.py` present (or an explicit 128+N -> -N normalization if any bash path writes returncode)
    - per-scenario shape validation present and fail-loud: `grep -Eqi "shape|validate|raise|sys.exit" scripts/capture_fping_fixtures.py` AND the partial_loss validator requires BOTH a float AND a `-` on the same host line (so a 0%/100% capture fails loudly)
    - `--print-command` works without fping installed and matches `_build_command`'s flags: `./scripts/capture-fping-fixtures.sh --print-command --reflectors "1.1.1.1 8.8.8.8"` emits an argv whose flag/host portion is `-C 5 -p 200 -q ... 1.1.1.1 8.8.8.8` (count/period byte-identical, OMITs -D)
    - the helper records stdout/stderr/returncode/version metadata per fixture (`grep -Eqi 'returncode|stderr|version' scripts/capture_fping_fixtures.py`)
    - operator-safety banner present: `grep -ci "mutat" scripts/capture-fping-fixtures.sh` >= 1 AND help text states no routing/CAKE/shaping mutation
    - neither file performs route/qdisc mutation: `grep -Ev '^[[:space:]]*#' scripts/capture-fping-fixtures.sh scripts/capture_fping_fixtures.py | grep -Eqc '(tc qdisc|ip route (add|del)|ip rule (add|del))'` returns 0 matches
  </acceptance_criteria>
  <done>An operator can run a safe, non-mutating helper on the live host that captures all six D-03 scenarios with a command MECHANICALLY proven byte-identical to the runtime backend invocation (via --print-command), records the GENUINE Python returncode (negative on process death, so Plan 01's `returncode < 0` gate is exercised), and shape-validates every scenario so a malformed capture (e.g. a 143 process-death, or a 0%/100% partial-loss) fails loudly rather than producing a misleading fixture.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: OPERATOR — capture real fping 5.1 fixtures on the live host (D-08)</name>
  <what-built>`scripts/capture-fping-fixtures.sh` + `scripts/capture_fping_fixtures.py` (Task 1) — a non-mutating capture helper that produces the six D-03 fixtures (with stdout/stderr/returncode/version metadata, and GENUINE Python negative returncode for process-death) using the byte-identical runtime fping invocation, shape-validating each scenario.</what-built>
  <how-to-verify>
    fping is absent on the dev VM; this MUST run on the live host where fping 5.1 is
    installed. Operator steps:
    1. Copy/run the capture helper on the live host (the cake-autorate host) with the real
       reflectors and the WAN source IP, e.g.:
       `./scripts/capture-fping-fixtures.sh --source-ip <wan_src_ip> --reflectors "<r1> <r2> <r3>" --lossy-reflector <distant_or_lossy_target>`
    2. Confirm the preflight banner states NO routing/CAKE/shaping mutation, then proceed.
    3. The helper SHAPE-VALIDATES each scenario and will FAIL LOUDLY if a capture is
       malformed (e.g. partial_loss came back 0%/100%, or process_death returncode is not
       negative). If it fails on partial_loss, supply a lossier `--lossy-reflector` and
       re-run that scenario.
    4. Confirm all six files are produced under `tests/fixtures/fping/`:
       reply.txt, total_loss.txt, partial_loss.txt, partial_line.txt, banner_noise.txt,
       process_death.txt — each containing REAL fping 5.1 output AND the metadata header
       (fping version, exact command, returncode, stdout/stderr split). Confirm
       process_death's recorded returncode is NEGATIVE (e.g. -15), not 143.
    5. Bring the six captured files (and any sidecar .meta.json) back into the repo
       (replacing the Plan 01 synthetic bootstraps) and confirm `fping --version` reported
       5.1 (or note the actual version recorded in the metadata).
    6. Confirm production routing/CAKE/shaping is unchanged after the run (e.g. the
       controller/bridge health endpoint is still healthy).
  </how-to-verify>
  <resume-signal>Type "captured" with the fping version and the process_death returncode, or describe what failed / what to adjust in the capture helper.</resume-signal>
  <acceptance_criteria>
    - all six `tests/fixtures/fping/*.txt` contain real operator-captured fping output (not the synthetic bootstrap header)
    - each fixture carries the metadata header (fping version, exact command, returncode, stdout/stderr split)
    - the process_death fixture's recorded returncode is NEGATIVE (Python-equivalent, e.g. -15), NOT shell 143
    - partial_loss carries a real mix (both RTTs and `-` on the same host line), not 0%/100%
    - the captured invocation matches `_build_command` (command recorded in each fixture header)
    - operator confirms production routing/CAKE/shaping unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-point parser + scorer tests to real captures via simulated CompletedProcess; mechanical command check; negative-returncode gate proof; full-suite green (FPING-04, REFL-01)</name>
  <read_first>
    - tests/test_fping_measurement.py  (the Plan 01 tests — confirm they read fixtures from tests/fixtures/fping/; adjust assertions that hard-coded a synthetic value; switch any bare-stdout patch to a real CompletedProcess(stdout, stderr, returncode))
    - tests/fixtures/fping/*.txt  (the now-real captures + metadata from Task 2)
    - src/wanctl/fping_measurement.py  (_build_command — the test compares the capture helper's --print-command against this; AND the negative-returncode process-death gate the process_death fixture must exercise)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (Validation Architecture — the FPING-04 test map and the keystone dash-never-zero assertion)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md  (Plan 03 M3 — feed CompletedProcess(stdout, stderr, returncode); mechanical _build_command vs --print-command check; NEW-HIGH #2 — the process_death fixture returncode is negative and exercises Plan 01's gate)
  </read_first>
  <action>
    With the real fixtures + metadata in place, re-confirm and tighten the parser + scorer
    tests in `tests/test_fping_measurement.py` against REAL fping 5.1 output:
    - Parse each fixture's metadata into a real
      `subprocess.CompletedProcess(args=[...], stdout=<captured stdout>, stderr=<captured
      stderr>, returncode=<captured rc>)` and patch `subprocess.run` to return it (M3) —
      do NOT pass fixture text as bare stdout. This exercises Plan 01's combined-stream
      parse (H2) and the negative-returncode process-death gate (banner_noise stderr,
      process_death NEGATIVE rc) against real output.
    - Add `test_capture_command_matches_build_command` (M3 mechanical check): invoke
      `scripts/capture-fping-fixtures.sh --print-command --source-ip <ip> --reflectors
      "<h1> <h2>"` via subprocess and assert its argv (flag/host portion) equals
      `FpingMeasurement({"source_ip": <ip>, ...}, logger)._build_command(["<h1>","<h2>"])`.
      This replaces the grep-only byte-identity assertion.
    - Add `test_process_death_fixture_returncode_is_negative` (NEW-HIGH #2 proof): read the
      process_death fixture's recorded returncode metadata and assert it is `< 0` (e.g.
      == -15), THEN feed it as `CompletedProcess(returncode=<that negative rc>, ...)` and
      assert `probe()` returns None via Plan 01's `returncode < 0` process-death gate (not
      via a parse). This is the explicit cross-reference that Plan 01's gate and Plan 03's
      fixture returncode stay aligned.
    - Adjust any assertion tuned to a synthetic exact RTT value so it asserts
      shape/behavior (loss-safe parse, dash-never-zero, total-loss→None+100.0, all-fail→None
      AFTER scorer feed, scorer-feed any-loss→fail) rather than a brittle synthetic constant.
    - Keep the keystone `test_dash_token_never_zero` against the real `partial_loss.txt`.
    - Keep `test_all_fail_feeds_scorer` (Plan 01 H1) running against the real
      `total_loss.txt` (returncode 1).
    - Confirm the six-scenario coverage maps to the six real fixtures.
    - Remove or rewrite the SYNTHETIC-BOOTSTRAP header comments so the committed files are
      the real captured samples + metadata (the binding proof, D-08).
    If the real 5.1 output reveals a format detail the parser missed (e.g. an unexpected
    token, a `-D` prefix the operator left on, host lines on stderr, banner shape), fix
    `_parse_fping` / `_parse_target_line` in `src/wanctl/fping_measurement.py` — but ONLY
    within that allowlisted module, never touching a frozen/non-allowlisted file. Editing
    fping_measurement.py again here is allowed (it is in the 241 allowlist).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_fping_measurement.py -q && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests feed a real `CompletedProcess(args, stdout, stderr, returncode)` (not bare stdout): `grep -n "CompletedProcess" tests/test_fping_measurement.py` shows stderr+returncode passed
    - `tests/test_fping_measurement.py::test_capture_command_matches_build_command` exits 0 (mechanical --print-command vs _build_command() equality)
    - `tests/test_fping_measurement.py::test_process_death_fixture_returncode_is_negative` exits 0 (the fixture returncode is `< 0` AND probe() returns None via Plan 01's negative-returncode gate — NEW-HIGH #2 cross-reference)
    - `tests/test_fping_measurement.py::test_dash_token_never_zero` exits 0 against the REAL partial_loss.txt
    - `tests/test_fping_measurement.py::test_all_fail_feeds_scorer` exits 0 against the REAL total_loss.txt
    - no fixture file still contains a "SYNTHETIC BOOTSTRAP" marker: `grep -rci "synthetic bootstrap" tests/fixtures/fping/` returns 0
    - full suite `.venv/bin/pytest tests/ -q` exits 0
    - `.venv/bin/ruff check src/ tests/` + `.venv/bin/mypy src/wanctl/` clean
  </acceptance_criteria>
  <done>The committed fixtures are real operator-captured fping 5.1 samples with stdout/stderr/returncode/version metadata; the process_death fixture's returncode is the Python-equivalent NEGATIVE value and exercises Plan 01's `returncode < 0` gate (tests feed a real CompletedProcess shape); the capture command is mechanically proven equal to _build_command via --print-command; the FPING-04 keystone, all-fail-scorer, negative-returncode, and six-scenario tests pass against the real captures; the full suite is green. D-08's "captured real 5.1 samples" proof is genuine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator capture script → production host | the helper runs on a live production host and MUST NOT mutate routing/CAKE/shaping (D-08) |
| captured stdout+stderr+returncode → committed fixture | real subprocess output (both streams + the genuine Python returncode) becomes the binding parser ground truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-08 | Tampering/DoS | capture helper on live host | mitigate | helper performs NO routing/CAKE/tc mutation (acceptance criterion greps for `tc qdisc`/`ip route add|del`/`ip rule add|del` across both files and requires zero matches); operator-safety banner; total-loss uses TEST-NET blackhole, not a route change; any tc/netem is the operator's manual, reverted responsibility. |
| T-241-09 | Tampering | capture-vs-runtime command drift (Pitfall 6) | mitigate | capture command MECHANICALLY proven byte-identical to `_build_command` via `--print-command` vs a Python `_build_command()` call (M3); fixture header records the exact command for audit. |
| T-241-10 | Spoofing | synthetic fixtures masquerading as real proof | mitigate | Task 3 requires the SYNTHETIC-BOOTSTRAP markers be gone; metadata records fping version + returncode + stdout/stderr split; the committed files are the binding D-08 captures. |
| T-241-17 | Tampering | a process-death fixture that records shell 143 instead of Python -15, silently bypassing Plan 01's `returncode < 0` gate | mitigate | process_death is captured via Python `Popen`+`terminate` so the returncode is the genuine NEGATIVE value; shape validation asserts `returncode < 0` and fails loudly otherwise; Task 3 cross-checks the fixture returncode is `< 0` AND exercises Plan 01's gate (review NEW-HIGH #2). |
| T-241-18 | Spoofing | a non-deterministic partial-loss capture (0% or 100%) masquerading as partial loss | mitigate | per-scenario shape validation requires BOTH a float AND a `-` on the same host line for partial_loss; a degenerate capture fails loudly, prompting a lossier target (review determinism). |
| T-241-SC | Tampering | npm/pip/cargo installs | N/A | no language-package install; bash wrapper + Python stdlib capture core + fixtures + tests only. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/ -q` green against the real captures (tests fed via real CompletedProcess incl. the negative-returncode process_death)
- `bash -n scripts/capture-fping-fixtures.sh` clean; `.venv/bin/python -m py_compile scripts/capture_fping_fixtures.py` clean; `--print-command` runs without fping; helper performs zero routing/CAKE mutation
- mechanical: capture `--print-command` equals `_build_command()` (test-proven)
- process_death fixture returncode is NEGATIVE and exercises Plan 01's gate (test-proven)
- operator-confirmed: production routing/CAKE/shaping unchanged after capture; fping version recorded in fixture metadata
- `git diff --stat -- tests/fixtures/fping/` shows the six real captures committed
</verification>

<success_criteria>
The FPING-04 proof is genuine: six real operator-captured fping 5.1 fixtures (with
stdout/stderr/returncode/version metadata) are committed, the process_death fixture's
returncode is the Python-equivalent NEGATIVE value that exercises Plan 01's
`returncode < 0` gate, every scenario is shape-validated (a malformed capture fails loudly),
the parser + scorer tests (incl. the dash-never-zero keystone and the all-fail-scorer proof)
pass against them fed through a real CompletedProcess shape, the capture helper is
non-mutating and mechanically byte-identical to the runtime invocation, and the full suite
is green.
</success_criteria>

<output>
Create `.planning/phases/241-fping-backend-offline-reflector-quality/241-03-SUMMARY.md` when done.
</output>
