---
phase: 242-backend-factory-loud-fallback
plan: 04
type: execute
wave: 4
depends_on: ["242-03"]
files_modified:
  - .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json
autonomous: true
requirements: [SAFE-17]
must_haves:
  truths:
    - "The verifier script, Plan 01 tests, Plan 03 test files (tests/test_wan_controller.py, tests/test_health_check.py), and all Plan 02/03 src/wanctl edits are committed BEFORE the gate runs, and git status --porcelain is clean for src/wanctl/, tests/, scripts/, and the relevant .planning/ files (so the evidence is reproducible)"
    - "The Phase 242 SAFE-17 boundary verifier passes: changed_paths is a SUBSET of the full v1.53 allowlist (NOT only the five 242 files — prior 239-241 allowlisted files also appear vs the v1.52 anchor), with the Phase 242 source files present as expected and zero out-of-allowlist drift"
    - "Protected byte-frozen bodies remain byte-identical; the 241-close guard emits an EXPLICIT no-drift field that Plan 04 asserts directly (not inferred from pass/fail)"
    - "A phase-local FUNCTIONAL assertion proves fping actually RUNS on the live path: with fping selected + binary present + a valid measurement.fping.cadence_sec, the REAL WANController.start_background_rtt() path yields backend_active == 'fping' (SAFE-17 is a path/byte gate and cannot catch the cadence-wiring defect on its own — this functional check closes that gap)"
    - "The full suite is classified: green, OR any failures are named concretely and shown pre-existing by reproducing them on the pre-242 anchor via a temporary `git worktree add --detach` (NOT checkout/stash in the main worktree) or cited from prior phase evidence — not waved off as 'legacy' in bulk; the hot-path slice is green; Plan 01's verifier --self-test evidence is kept SEPARATE from the real phase evidence so it cannot pollute it"
  artifacts:
    - path: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      provides: "fail-closed SAFE-17 boundary evidence with passed:true + explicit 241-close-guard field"
      contains: "\"passed\": true"
  key_links:
    - from: "scripts/phase242-safe17-boundary-check.sh"
      to: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      via: "verifier emits evidence on pass"
      pattern: "safe17-boundary-242"
    - from: "tests/test_wan_controller.py"
      to: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      via: "the phase-local functional fping-active assertion (backend_active=='fping' via real start_background_rtt) gates alongside SAFE-17"
      pattern: "backend_active"
---

<objective>
The hard gate. Run the Phase 242 SAFE-17 boundary verifier against committed HEAD
and prove zero out-of-allowlist controller-path drift, byte-identical protected
bodies, no new 242 diff to 241-frozen files (asserted via an EXPLICIT field), and
byte-preserved /health fields. Additionally prove a phase-local FUNCTIONAL property
SAFE-17 cannot: that fping actually RUNS on the live path (backend_active == "fping"
via the real start_background_rtt path with a valid measurement.fping.cadence_sec) —
because the cadence-wiring defect would leave every edit allowlisted and every
protected body byte-identical while fping is silently dead. This is Success
Criterion 4 plus the cycle-3 functional guard — a passing SAFE-17 verifier AND a
green live-fping functional assertion are both non-negotiable.

Purpose: Prove the centralization + observability work stayed inside the narrowed
v1.53 allowlist (factory + both call sites + wan_controller wiring + /health wiring),
touched no classification logic (state machine, thresholds, EWMA, dwell, deadband,
arbitration, fusion), AND actually wired fping live (not an all-green factory with
dead fping). Evidence must be reproducible: verifier + tests + source committed before
the evidence run, with a clean tree across src/wanctl/, tests/, and scripts/.
Output: passing safe17-boundary-242.json evidence + green phase-local live-fping
functional assertion + classified full-suite + hot-path green.

This plan runs a READ-ONLY git-diff verifier and tests. It mutates no controller
source, no external gear, no CAKE/service state, and no refs (beyond committing the
already-authored work + the evidence). Any pre-242-anchor reproduction uses a
throwaway detached worktree, never the main worktree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md

<interfaces>
<!-- The gate runs tools authored/edited in prior plans. -->

Verifier: scripts/phase242-safe17-boundary-check.sh --anchor v1.52 (authored Plan 01)
  - fails closed on: dirty src/wanctl tree, out-of-allowlist drift, RTT-seam drift since 239,
    reflector_scorer drift since 240, new 242 diff to fping_measurement.py/reflector_scorer.py
    since 241 close (PHASE241_CLOSE_ANCHOR d8179bb6), protected-body drift
  - emits .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json
    with "passed": true on success AND an explicit no-drift field for the 241-close guard
    (e.g. "phase241_frozen_no_new_diff": true) so it can be asserted directly (review LOW)
  - the verifier's --self-test path (Plan 01) writes NOTHING to the real evidence JSON — it is a
    separate negative-proof path. Do NOT run --self-test as part of the real evidence run; keep its
    output (if any) out of the phase evidence dir so it cannot pollute the real evidence (review LOW)
Anchor: v1.52 = c8c6a750... (the v1.53 SAFE-17 baseline)
Protected-body proof: scripts/phase239-protected-body-diff.py (called by the verifier; unmodified)

PHASE-LOCAL FUNCTIONAL ASSERTION (cycle-3 HIGH — SAFE-17 cannot catch this):
  SAFE-17 verifies allowlist membership + byte-identical protected bodies. The cadence-wiring
  defect leaves ALL edits allowlisted and ALL protected bodies byte-identical while fping is dead
  (backend_active permanently "icmplib"). Therefore the gate set MUST also assert the FUNCTIONAL
  property: fping selected + binary present + valid measurement.fping.cadence_sec + the REAL
  WANController.start_background_rtt() path -> backend_active == "fping". This is exactly
  test_start_background_rtt_keeps_fping_active + test_fping_uses_resolved_cadence (Plan 01) + the
  Plan 03 real-path test in tests/test_wan_controller.py. Plan 04 asserts these named tests are
  GREEN as a gate, not just the SAFE-17 byte/path checks.

EXPECTED changed_paths (review MEDIUM — corrected): vs the v1.52 anchor, changed_paths is a
  SUBSET of the FULL v1.53 SAFE-17 allowlist (the 241 files PLUS the 242 files), because prior
  Phase 239/240/241 allowlisted edits also appear in the v1.52..HEAD diff. The Phase 242 source
  files (rtt_backend_factory.py, autorate_continuous.py, steering/daemon.py, wan_controller.py,
  health_check.py) MUST be present; nothing OUTSIDE the full allowlist may appear. Do NOT expect
  "only the five 242 files." Test edits (tests/test_wan_controller.py, tests/test_health_check.py,
  tests/test_rtt_backend_factory.py) live OUTSIDE src/wanctl/ and are NOT in the allowlist scope —
  they are not part of changed_paths' allowlist check, but they MUST be committed (clean tree).

Precondition (the verifier's dirty-tree gate + Plan 04 reproducibility, review MEDIUM): the
  verifier script, the Plan 01 tests, the Plan 03 test files (tests/test_wan_controller.py,
  tests/test_health_check.py), and all Plan 02/03 src/wanctl edits MUST be committed and
  `git status --porcelain` clean for src/wanctl/, tests/, scripts/, and the relevant .planning/
  files before the evidence run — so the evidence is fully reproducible. The verifier's own
  dirty-tree gate covers src/wanctl/; Plan 04 additionally requires tests/ and scripts/ clean
  because the gate depends on the committed tests + verifier script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit prior work (src + tests + scripts), run the SAFE-17 boundary gate, assert the phase-local live-fping functional property, prove subset-of-allowlist + explicit 241-guard field + classified full-suite</name>
  <files>.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json</files>
  <read_first>
    - scripts/phase242-safe17-boundary-check.sh (the verifier authored in Plan 01 — its fail-closed gates, the explicit 241-close-guard field it emits, the --out confinement, and the --self-test path which must NOT touch the real evidence)
    - scripts/phase239-protected-body-diff.py (PROTECTED set :21-32 the verifier proves byte-identical)
    - tests/test_wan_controller.py (the real-path fping-active test asserting backend_active == "fping" after the real start_background_rtt path — the functional gate)
    - tests/test_rtt_backend_factory.py (test_fping_uses_resolved_cadence + test_start_background_rtt_keeps_fping_active — the cycle-3 functional regressions)
    - .planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md (the phase-gate sampling rule: full suite green + SAFE-17 PASS before verify-work)
    - .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json (the passing 241 evidence shape to match, including which prior files appear)
  </read_first>
  <action>
    REPRODUCIBILITY FIRST (review MEDIUM — widened scope): ensure the verifier script, the
    Plan 01 tests, the Plan 03 test files (tests/test_wan_controller.py,
    tests/test_health_check.py), and all Plan 02/03 src/wanctl edits are committed and
    `git status --porcelain` is clean for src/wanctl/, tests/, AND scripts/ (the verifier's
    own dirty-tree gate covers src/wanctl/, but the gate DEPENDS on the committed tests and
    verifier script, so tests/ and scripts/ must also be clean for the evidence to be
    reproducible). Commit any pending tracked work in those trees BEFORE running the evidence —
    the evidence run must be against committed HEAD.
    Then run scripts/phase242-safe17-boundary-check.sh --anchor v1.52 and confirm it exits 0
    and writes safe17-boundary-242.json with:
    (a) "passed": true;
    (b) changed_paths is a SUBSET of the FULL v1.53 allowlist (the 241 files plus the 242
        files), with the five Phase 242 source files (rtt_backend_factory.py,
        autorate_continuous.py, steering/daemon.py, wan_controller.py, health_check.py) PRESENT
        and NOTHING outside the full allowlist — do NOT expect "only the five 242 files" (prior
        239-241 allowlisted files legitimately appear vs the v1.52 anchor; test files are
        out-of-allowlist scope and are not part of this check);
    (c) disallowed_paths empty;
    (d) all_identical true (protected bodies byte-identical);
    (e) rtt_seam_unchanged_since_phase239 true, reflector_scorer_unchanged true;
    (f) the EXPLICIT 241-close-guard field (e.g. phase241_frozen_no_new_diff) is true — assert
        this field DIRECTLY, do not infer no-drift from overall pass/fail (review LOW).
    Do NOT run the verifier's --self-test as part of this real evidence run — it is a separate
    negative-proof path (Plan 01) and must not write to or pollute safe17-boundary-242.json
    (review LOW: keep self-test evidence separate from real phase evidence).
    If the verifier fails closed on an out-of-allowlist path, do NOT widen the allowlist to
    pass — investigate whether Plan 03 touched a file it should not have (a real SAFE-17
    violation to fix in the source, not the verifier).
    PHASE-LOCAL FUNCTIONAL GATE (cycle-3 HIGH — SAFE-17 path/byte gate is insufficient alone):
    run the named live-fping functional tests and require them GREEN as part of this gate:
    `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_start_background_rtt_keeps_fping_active
    tests/test_rtt_backend_factory.py::test_fping_uses_resolved_cadence -x -q` AND the Plan 03
    real-path equivalent in tests/test_wan_controller.py (the one asserting backend_active == "fping"
    after the real start_background_rtt path). These prove fping actually RUNS with a valid
    measurement.fping.cadence_sec — the cadence defect would leave the SAFE-17 byte/path checks all
    green while fping is dead, so this functional assertion is a required keeper. If any of these
    functional tests is RED, the gate FAILS even if the SAFE-17 byte/path checks pass.
    FULL-SUITE CLASSIFICATION (review MEDIUM — no broad wave-off, detached-worktree reproduction):
    run the full suite `.venv/bin/pytest tests/ -q` and the hot-path slice. If the suite is
    green, record green. If ANY test fails, record each failure BY NAME and classify it
    concretely: reproduce the same failure on the pre-242 anchor by creating a temporary
    `git worktree add --detach <tmpdir> <v1.52-or-241-close-commit>` and re-running that named
    test THERE (do NOT `git stash`/checkout in the main worktree — that invalidates the
    "evidence is the immediate next commit" discipline), then `git worktree remove --force` it;
    OR cite the specific prior-phase evidence that already documents it as pre-existing. A
    failure that cannot be shown pre-existing by name is treated as introduced by 242 and must
    be fixed — do NOT bucket failures as "legacy boundary-test hygiene" without per-failure
    evidence. The phase-local + hot-path proofs are the keepers only after each full-suite
    failure is individually accounted for.
    Commit the evidence JSON as the immediate next commit so HEAD^ == evidence.head_commit
    (the 241-04 durable-freshness convention).
  </action>
  <verify>
    <automated>scripts/phase242-safe17-boundary-check.sh --anchor v1.52 && python3 -c "import json; d=json.load(open('.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json')); assert d['passed'] is True and d['all_identical'] is True and d['rtt_seam_unchanged_since_phase239'] is True and d['reflector_scorer_unchanged'] is True and d.get('phase241_frozen_no_new_diff') is True, d" && .venv/bin/pytest tests/test_rtt_backend_factory.py::test_start_background_rtt_keeps_fping_active tests/test_rtt_backend_factory.py::test_fping_uses_resolved_cadence -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The verifier script, Plan 01 tests, Plan 03 test files (tests/test_wan_controller.py, tests/test_health_check.py), and Plan 02/03 src/wanctl edits are committed and `git status --porcelain` is clean for src/wanctl/, tests/, AND scripts/ BEFORE the verifier runs
    - `scripts/phase242-safe17-boundary-check.sh --anchor v1.52` exits 0
    - safe17-boundary-242.json has "passed": true, "all_identical": true, "rtt_seam_unchanged_since_phase239": true, "reflector_scorer_unchanged": true, and the explicit 241-close-guard field (phase241_frozen_no_new_diff) true
    - disallowed_paths is empty; changed_paths is a SUBSET of the full v1.53 allowlist with the five Phase 242 files present (NOT "only five files")
    - The phase-local live-fping functional tests are GREEN: test_start_background_rtt_keeps_fping_active and test_fping_uses_resolved_cadence (tests/test_rtt_backend_factory.py) plus the Plan 03 real-path equivalent in tests/test_wan_controller.py all assert backend_active == "fping" / the fping adapter on the real start_background_rtt path — proving fping actually runs (the cycle-3 HIGH functional guard SAFE-17 cannot provide); if any is RED the gate FAILS
    - The verifier's --self-test path is NOT run as part of the real evidence run and does not write to / pollute safe17-boundary-242.json (review LOW: self-test evidence kept separate)
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0; the hot-path slice is green
    - Full suite is either green OR every failure is named and shown pre-existing by reproduction on the pre-242 anchor via a temporary `git worktree add --detach` (not checkout/stash in the main worktree) or cited from prior evidence — no bulk "legacy" wave-off
    - Evidence committed as the immediate next commit (HEAD^ == evidence.head_commit)
  </acceptance_criteria>
  <done>SAFE-17 Success Criterion 4 is satisfied: the boundary verifier passes with byte-identical protected bodies, changed_paths is a correct subset of the full allowlist with the 242 files present, the 241-close guard's explicit no-drift field is asserted directly, the phase-local live-fping functional assertion proves backend_active == "fping" on the real start_background_rtt path (the cycle-3 guard SAFE-17 cannot give), the tree is clean across src/wanctl/+tests/+scripts/, the self-test evidence is kept separate, and every full-suite failure (if any) is individually accounted for via detached-worktree reproduction; phase-local + hot-path tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs a read-only git-diff verifier + the test suite. No mutation of source, gear, services, or refs beyond committing already-authored work + evidence. Pre-anchor reproduction uses a throwaway detached worktree, never the main worktree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-10 | Tampering | passing the gate by widening the allowlist instead of fixing source | mitigate | Acceptance criteria forbid widening the allowlist to pass; an out-of-allowlist hit is treated as a real SAFE-17 violation to fix in Plan 03 source, not in the verifier |
| T-242-11 | Repudiation | stale/forged or non-reproducible evidence | mitigate | Commit the verifier+tests+source BEFORE the evidence run with a clean tree across src/wanctl/+tests/+scripts/, and the evidence JSON as the immediate next commit so HEAD^ == evidence.head_commit (241-04 durable-freshness convention); the verifier records head_commit + anchor_sha + checked_at |
| T-242-16 | Repudiation | masking a 242-introduced regression as "legacy" | mitigate | Full-suite failures must be named and shown pre-existing per-failure (reproduced on the pre-242 anchor via a detached worktree, or cited from prior evidence); no bulk "legacy hygiene" wave-off (review MEDIUM) |
| T-242-20 | Tampering | main-worktree checkout/stash during reproduction invalidating the evidence-next-commit discipline | mitigate | Pre-anchor reproduction uses `git worktree add --detach` in a throwaway dir + `git worktree remove --force`; the main worktree is never checked out/stashed, so HEAD^ == evidence.head_commit holds (review MEDIUM) |
| T-242-21 | Repudiation/observability | an all-green SAFE-17 byte/path gate shipping with fping silently dead (the cycle-3 HIGH) | mitigate | The gate set adds a phase-local FUNCTIONAL assertion (real start_background_rtt path -> backend_active == "fping" with a valid measurement.fping.cadence_sec); SAFE-17 alone cannot catch this because all edits stay allowlisted and protected bodies byte-identical while fping is dead — the functional test is the required complement (review MEDIUM) |
| T-242-23 | Repudiation | self-test output polluting the real phase evidence | mitigate | The verifier --self-test path is NOT run during the real evidence run and writes nothing to safe17-boundary-242.json; the real evidence is the immediate next commit (review LOW) |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: read-only verification + tests. The gate itself is the SAFE-17 mitigation for the whole phase, now complemented by a functional live-fping assertion.
</threat_model>

<verification>
- `scripts/phase242-safe17-boundary-check.sh --anchor v1.52` exits 0 with passed:true evidence and the explicit 241-close-guard field true.
- changed_paths is a subset of the full v1.53 allowlist with the five 242 files present.
- The phase-local live-fping functional tests (test_start_background_rtt_keeps_fping_active, test_fping_uses_resolved_cadence, + the Plan 03 real-path equivalent) are GREEN — backend_active == "fping" on the real path.
- Tree clean across src/wanctl/, tests/, scripts/ before the evidence run.
- The verifier --self-test path is not run in the real evidence run and does not pollute the evidence JSON.
- Phase-local + hot-path tests green; full-suite run classified per-failure (each named failure shown pre-existing via detached-worktree reproduction, or fixed).
- Evidence committed so HEAD^ == evidence.head_commit.
</verification>

<success_criteria>
The SAFE-17 boundary verifier passes (Criterion 4), protected bodies are byte-identical,
the 241-close guard's explicit no-drift field is asserted directly, changed_paths is a
correct subset of the full allowlist (not "only five files"), the phase-local live-fping
functional assertion proves backend_active == "fping" on the real start_background_rtt
path (the cycle-3 functional guard SAFE-17 cannot provide), the tree is clean across
src/wanctl/+tests/+scripts/, the self-test evidence is kept separate, every full-suite
failure is individually accounted for via detached-worktree reproduction, and the
loud-fallback + subset byte-preservation + controller_measurement + fping-active tests
are green — the phase is ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-04-SUMMARY.md` when done
</output>
</content>
</invoke>
