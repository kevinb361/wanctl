---
phase: 242-backend-factory-loud-fallback
plan: 05
type: execute
wave: 5
depends_on: ["242-04"]
files_modified:
  - src/wanctl/rtt_backend_factory.py
  - src/wanctl/wan_controller.py
  - tests/test_rtt_backend_factory.py
  - scripts/phase242-safe17-boundary-check.sh
  - .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json
autonomous: true
gap_closure: true
requirements: [FALL-02, SAFE-17]
must_haves:
  truths:
    - "Factory-produced fping background samples retain backend='fping' attribution until WANController.measure_rtt() can distinguish them from icmplib snapshots."
    - "WANController.measure_rtt() does not call ReflectorScorer.record_results() for fping-backed background samples, while icmplib BackgroundRTTThread behavior stays unchanged."
    - "The regression test seeds the wrapped FpingThread cache, calls WANController.measure_rtt(), and proves record_results() is not called."
    - "SAFE-17 evidence is refreshed after the gap fix and explicitly documents the intentional fping-scorer guard in measure_rtt without allowing threshold/state-machine/timing drift."
  artifacts:
    - path: "src/wanctl/rtt_backend_factory.py"
      provides: "fping adapter preserves RttSample backend/source attribution instead of converting to plain RTTSnapshot too early"
      contains: "RttSample"
    - path: "src/wanctl/wan_controller.py"
      provides: "targeted scorer-update guard for fping-attributed background samples"
      contains: "backend"
    - path: "tests/test_rtt_backend_factory.py"
      provides: "behavioral regression for WR-01 / verification gap"
      contains: "record_results"
    - path: "scripts/phase242-safe17-boundary-check.sh"
      provides: "phase-boundary verifier updated to assert the exact intentional measure_rtt fping-scorer guard"
      contains: "measure_rtt_fping_scorer_guard"
    - path: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      provides: "fresh post-gap SAFE-17 evidence"
      contains: "\"passed\": true"
  key_links:
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/wan_controller.py"
      via: "_FpingDriverThread.get_latest() returns an attributed RttSample; measure_rtt reads backend attribution before scorer update"
      pattern: "getattr\(snapshot, .*backend"
    - from: "tests/test_rtt_backend_factory.py"
      to: "src/wanctl/wan_controller.py"
      via: "wrapped FpingThread cache seeded with RttSample(backend='fping'), then measure_rtt() invoked"
      pattern: "assert_not_called|call_count == 0"
---

<objective>
Close the Phase 242 verification gap: fping background samples currently lose
backend attribution when `_FpingDriverThread.get_latest()` converts `RttSample` to
plain `RTTSnapshot`, so `WANController.measure_rtt()` treats them like icmplib
snapshots and feeds `ReflectorScorer.record_results()`. Preserve attribution and
add the smallest scorer-update guard needed so fping does not drive the reflector
scorer in Phase 242.

Purpose: uphold the committed Phase 242 scope decision that fping is
background-thread-only and scorer consumption remains on the icmplib
`controller_measurement` path, while preserving the controller RTT value path,
fallback observability, and SAFE-17 guardrails.
Output: one source fix, one stronger regression test, refreshed SAFE-17 evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/242-backend-factory-loud-fallback/242-VERIFICATION.md
@.planning/phases/242-backend-factory-loud-fallback/242-02-SUMMARY.md
@.planning/phases/242-backend-factory-loud-fallback/242-03-SUMMARY.md
@.planning/phases/242-backend-factory-loud-fallback/242-04-SUMMARY.md

<interfaces>
Key existing contracts:

`src/wanctl/rtt_backend.py`:
```python
@dataclasses.dataclass(frozen=True, slots=True)
class RttSample:
    rtt_ms: float
    per_host_results: dict[str, float | None]
    timestamp: float
    measurement_ms: float
    active_hosts: tuple[str, ...] = ()
    successful_hosts: tuple[str, ...] = ()
    backend: str = "icmplib"
    source_ip: str | None = None
```

`src/wanctl/rtt_backend_factory.py` currently drops attribution:
```python
def get_latest(self) -> RTTSnapshot | None:
    sample = self._fping_thread.get_latest()
    if sample is None:
        return None
    return sample.to_snapshot()
```

`src/wanctl/wan_controller.py` currently scores any snapshot when `cycle_status` is
not a zero-success status:
```python
if not (cycle_status and self._should_skip_scorer_update(cycle_status)):
    self._reflector_scorer.record_results(
        {host: rtt_val is not None for host, rtt_val in snapshot.per_host_results.items()}
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve fping sample attribution and skip scorer updates only for fping background samples</name>
  <files>src/wanctl/rtt_backend_factory.py, src/wanctl/wan_controller.py, tests/test_rtt_backend_factory.py</files>
  <read_first>
    - .planning/phases/242-backend-factory-loud-fallback/242-VERIFICATION.md (gap lines 7-21 and truth 9 failure)
    - src/wanctl/rtt_backend_factory.py (_FpingDriverThread.get_latest lines 77-81; RttDriverThread Protocol lines 46-58)
    - src/wanctl/rtt_backend.py (RttSample fields and to_snapshot behavior lines 37-67)
    - src/wanctl/wan_controller.py (_BackgroundRttDriver Protocol lines 71-83; measure_rtt scorer update lines 1179-1203; zero-success helper lines 1265-1276)
    - tests/test_rtt_backend_factory.py (existing weak test_fping_selected_measure_rtt_no_attributeerror lines 322-342; FpingThread cache shape from fping_measurement.py lines 307-318)
  </read_first>
  <behavior>
    - The fping adapter returns an attributed `RttSample` (or an equivalent object retaining `backend='fping'` and `source_ip`) from `get_latest()` instead of converting to attribution-free `RTTSnapshot` before WANController sees it.
    - `WANController.measure_rtt()` still returns the fping RTT value from the background sample and still updates `_last_raw_rtt`, active hosts, successful hosts, and staleness exactly as before for successful samples.
    - `WANController.measure_rtt()` skips `ReflectorScorer.record_results()` when the background sample is attributed to backend `fping`; it continues to call `record_results()` for normal icmplib `BackgroundRTTThread` snapshots and for blocking icmplib paths.
    - The fping skip does not masquerade as a zero-success RTT cycle: do not increment `_zero_success_blackout_cycles`, do not set `_zero_success_blackout_active`, and do not emit the zero-success warning solely because backend is `fping`.
  </behavior>
  <action>
    In `src/wanctl/rtt_backend_factory.py`, import/use `RttSample` from
    `wanctl.rtt_backend` and widen the `RttDriverThread.get_latest()` return type to
    `RTTSnapshot | RttSample | None`. Change `_FpingDriverThread.get_latest()` so it returns
    the wrapped `FpingThread.get_latest()` `RttSample` directly when present, preserving
    `backend='fping'`, `source_ip`, `active_hosts`, `successful_hosts`, and
    `per_host_results`. Do not alter `FpingMeasurement`, `FpingThread`, or any timeout/cadence
    behavior.

    In `src/wanctl/wan_controller.py`, widen the private `_BackgroundRttDriver.get_latest()`
    Protocol return type the same way or use a structural type compatible with both
    `RTTSnapshot` and `RttSample`. In `measure_rtt()`, after `snapshot` is known non-None and
    before `record_results()`, compute `snapshot_backend = getattr(snapshot, "backend", "icmplib")`
    and `skip_scorer_for_backend = snapshot_backend == "fping"`. Split the existing scorer
    and zero-success logic so:
    - `zero_success_cycle = bool(cycle_status and self._should_skip_scorer_update(cycle_status))`
    - `record_results(...)` runs only when `not zero_success_cycle and not skip_scorer_for_backend`
    - the existing zero-success blackout branch runs only when `zero_success_cycle` is true,
      not when `skip_scorer_for_backend` is true.
    Keep all RTT age thresholds, stale handling, return values, `_record_rtt_sample(...)` data,
    controller state transitions, thresholds, EWMA, dwell, deadband, arbitration, and fusion logic
    unchanged. This is a targeted scorer side-effect guard, not a control-algorithm change.

    Replace the weak regression in `tests/test_rtt_backend_factory.py` with a stronger test:
    build a fping-selected handle with fping present, create a `WANController`, install the
    factory adapter via `handle.make_thread(...)`, seed the WRAPPED `FpingThread` cache
    (`controller._rtt_thread._fping_thread._cached_result` or an equivalent public test seam on the
    adapter) with `RttSample(rtt_ms=12.5, per_host_results={"h1": 12.5, "h2": None},
    backend="fping", source_ip="192.0.2.10", active_hosts=("h1", "h2"), successful_hosts=("h1",))`,
    replace `controller._reflector_scorer.record_results` with a mock, call
    `controller.measure_rtt()`, and assert: return value is `12.5`, `record_results` call count is
    zero, `_zero_success_blackout_cycles` remains `0`, and the controller's last active/successful
    reflector host lists include `h1`/`h2` and `h1` respectively. Add or keep a companion icmplib
    snapshot test proving `record_results()` is still called for `RTTSnapshot` without backend
    attribution.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fping_selected_measure_rtt_no_attributeerror -x -q && .venv/bin/pytest -o addopts='' tests/test_rtt_backend_factory.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_FpingDriverThread.get_latest()` no longer calls `sample.to_snapshot()` for fping samples before returning to `WANController.measure_rtt()`.
    - `WANController.measure_rtt()` contains an explicit backend-attribution guard using `getattr(snapshot, "backend", "icmplib")` or equivalent and skips `record_results()` only for backend `fping`.
    - The zero-success blackout branch is still controlled only by a real zero-success `RTTCycleStatus`; fping attribution alone does not increment blackout counters or log zero-success collapse.
    - The fping regression test seeds the wrapped FpingThread cache, returns RTT `12.5`, and asserts `ReflectorScorer.record_results` is not called.
    - A normal icmplib/RTTSnapshot path test still proves `record_results()` is called.
    - Focused factory/controller/health tests exit 0; ruff and mypy are clean for the edited source files.
  </acceptance_criteria>
  <done>Fping background samples remain usable RTT samples but no longer feed ReflectorScorer.record_results() in Phase 242; icmplib scorer behavior is unchanged and the verifier gap's missing regression exists.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh SAFE-17 boundary evidence with an exact guard for the intentional measure_rtt scorer skip</name>
  <files>scripts/phase242-safe17-boundary-check.sh, .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json</files>
  <read_first>
    - scripts/phase242-safe17-boundary-check.sh (current protected-body proof call at lines 392-397 and evidence emit fields)
    - scripts/phase239-protected-body-diff.py (protected-body checker used by the phase verifier)
    - .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json (current evidence shape)
    - src/wanctl/wan_controller.py (final Task 1 measure_rtt guard)
  </read_first>
  <action>
    Update `scripts/phase242-safe17-boundary-check.sh` so the Phase 242 boundary verifier
    remains fail-closed after the intentional Task 1 `measure_rtt()` scorer guard. Do not remove
    allowlist enforcement, dirty-tree checks, RTT-seam drift checks, reflector-scorer no-drift,
    Phase-241 frozen no-drift, or protected-body checks for the other protected bodies.

    Add a phase-specific static assertion named `measure_rtt_fping_scorer_guard` (implemented in
    the shell script's embedded Python or a small Python helper invoked by the script) that reads
    `src/wanctl/wan_controller.py` and requires all of these exact properties before `passed:true`:
    1. `measure_rtt()` contains a backend attribution read from the snapshot (`getattr(snapshot,
       "backend", "icmplib")` or an AST-equivalent expression).
    2. `record_results(...)` is guarded by both `not zero_success_cycle` and `not
       skip_scorer_for_backend` (or semantically equivalent names).
    3. the zero-success blackout branch is guarded by `zero_success_cycle`, not by the fping skip
       flag.
    4. no forbidden tokens are introduced in `measure_rtt()` for thresholds/state/timing changes:
       do not change `5.0`, `0.5`, EWMA, dwell, deadband, arbitration, fusion, or rate-change logic.

    If the existing `phase239-protected-body-diff.py` reports only the expected
    `WANController.measure_rtt` drift and the static assertion passes, the Phase 242 verifier may
    emit `passed:true` with `measure_rtt_fping_scorer_guard: true` and a note that this is the
    post-verification gap closure exception. If any other protected body drifts, or the static
    assertion fails, the verifier must fail closed. Keep `disallowed_paths` empty and all existing
    allowlist semantics intact.

    Run the updated verifier after committing source/test fixes, then commit the refreshed
    `.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json` as the
    immediate next commit. Assert the JSON has `passed: true`, `measure_rtt_fping_scorer_guard:
    true`, empty `disallowed_paths`, `rtt_seam_unchanged_since_phase239: true`,
    `reflector_scorer_unchanged: true`, and `phase241_frozen_no_new_diff: true`.
  </action>
  <verify>
    <automated>scripts/phase242-safe17-boundary-check.sh --anchor v1.52 && python3 -c "import json; d=json.load(open('.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json')); assert d['passed'] is True and d.get('measure_rtt_fping_scorer_guard') is True and not d['disallowed_paths'] and d['rtt_seam_unchanged_since_phase239'] is True and d['reflector_scorer_unchanged'] is True and d.get('phase241_frozen_no_new_diff') is True, d" && .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The verifier still fails closed on dirty `src/wanctl`, out-of-allowlist paths, RTT-seam drift, reflector-scorer drift, Phase-241 frozen-file drift, and any protected-body drift other than the exact Task 1 `WANController.measure_rtt` fping-scorer guard.
    - Evidence JSON contains `measure_rtt_fping_scorer_guard: true`, `passed: true`, empty `disallowed_paths`, and the existing SAFE-17 no-drift booleans remain true.
    - Hot-path slice exits 0 after the fix.
    - Evidence is committed immediately after the source/test/verifier commit so `HEAD^` equals `safe17-boundary-242.json.head_commit`.
  </acceptance_criteria>
  <done>SAFE-17 is freshly proven for the gap closure: the only intentional protected-body change is the exact fping scorer guard, every other boundary remains fail-closed, and hot-path tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fping background sample -> controller scorer side effect | Internal background RTT sample crosses into `WANController.measure_rtt()`; backend attribution decides whether reflector quality state may be updated. |
| source tree -> SAFE-17 evidence | Verifier converts git/source state into boundary evidence; stale or overly broad exceptions would hide controller-path drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-G05-01 | Tampering | ReflectorScorer quality state | mitigate | Skip `record_results()` for `backend='fping'` background samples in Phase 242; add regression proving zero calls while icmplib still records. |
| T-242-G05-02 | Repudiation | SAFE-17 verifier | mitigate | Verifier emits `measure_rtt_fping_scorer_guard:true` only when static guard patterns match; other protected drift remains fail-closed. |
| T-242-G05-03 | DoS | zero-success blackout handling | mitigate | Fping scorer skip must not use the zero-success blackout path; test asserts blackout counters stay at zero for a successful fping sample. |
| T-242-G05-04 | Tampering | control algorithm thresholds/timing | mitigate | Task forbids changes to thresholds, EWMA, dwell, deadband, arbitration, fusion, rate logic, and stale age constants; hot-path slice must pass. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fping_selected_measure_rtt_no_attributeerror -x -q` proves the gap is closed.
- `.venv/bin/pytest -o addopts='' tests/test_rtt_backend_factory.py tests/test_wan_controller.py tests/test_health_check.py -q` proves phase-local behavior remains green.
- `scripts/phase242-safe17-boundary-check.sh --anchor v1.52` emits fresh passing evidence with `measure_rtt_fping_scorer_guard:true`.
- Hot-path slice passes: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
</verification>

<success_criteria>
Phase 242 verification gap truth #9 is satisfied: fping background samples do not feed
`ReflectorScorer.record_results()`, the wrapped fping-thread cache regression catches the
failure mode, icmplib scorer behavior is unchanged, no zero-success blackout is falsely
triggered, and SAFE-17 evidence is fresh after the targeted gap fix.
</success_criteria>

<output>
After completion, create `.planning/phases/242-backend-factory-loud-fallback/242-05-SUMMARY.md`
</output>
