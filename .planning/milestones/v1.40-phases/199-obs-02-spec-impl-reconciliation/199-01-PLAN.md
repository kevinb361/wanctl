---
phase: 199-obs-02-spec-impl-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SUBSYSTEMS.md
  - docs/RUNBOOK.md
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "REQUIREMENTS.md OBS-02 row contains all four anchor phrases (`absent SQLite rows`, `cold-start`, `invalid-snapshot`, `wanctl_arbitration_active_primary`) plus the Phase 199 amendment annotation (verify-only — no edit)."
    - "docs/SUBSYSTEMS.md `## Health And Metrics` mentions `signal_arbitration` and the four OBS-01 contract field names (`active_primary_signal`, `rtt_confidence`, `cake_av_delay_delta_us`, `control_decision_reason`) and quotes the OBS-02 absent-row phrase verbatim."
    - "docs/RUNBOOK.md SQLite/history section names `wanctl_arbitration_active_primary` as the per-cycle denominator and quotes the OBS-02 absent-row phrase verbatim."
    - "Zero Python source change under `src/wanctl/` and `tests/` for this plan's edits."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "OBS-02 amended row (verify-only; pre-staged)"
      contains: "amended in Phase 199"
    - path: "docs/SUBSYSTEMS.md"
      provides: "signal_arbitration field-shape note under wans[]"
      contains: "signal_arbitration"
    - path: "docs/RUNBOOK.md"
      provides: "operator-query note naming wanctl_arbitration_active_primary as per-cycle denominator"
      contains: "wanctl_arbitration_active_primary"
  key_links:
    - from: "docs/SUBSYSTEMS.md"
      to: ".planning/REQUIREMENTS.md OBS-02"
      via: "verbatim quote of absent-row phrase + parenthetical back-reference"
      pattern: "Per REQUIREMENTS\\.md OBS-02"
    - from: "docs/RUNBOOK.md"
      to: ".planning/REQUIREMENTS.md OBS-02"
      via: "verbatim quote of absent-row phrase + parenthetical back-reference"
      pattern: "Per REQUIREMENTS\\.md OBS-02"
---

<objective>
Verify the pre-staged REQUIREMENTS.md OBS-02 wording is intact, then propagate the spec phrase verbatim into two operator-facing doc surfaces: a `signal_arbitration` field-shape note in `docs/SUBSYSTEMS.md` `## Health And Metrics`, and a per-cycle denominator note in `docs/RUNBOOK.md` SQLite/history section.

Purpose: Close the v1.40 OBS-02 audit caveat by establishing spec → operator-doc lockstep with verbatim quotes (D-04). Locked invariants: docs-only (D-07); no Python source change; do not re-amend REQUIREMENTS.md (D-01); do not enumerate `refractory_active` (RESEARCH.md Pitfall 5); preserve the em-dash (U+2014) when quoting the OBS-02 phrase.

Output: Two targeted doc edits (SUBSYSTEMS.md + RUNBOOK.md) plus a verified-clean REQUIREMENTS.md state. Plan 02 consumes this plan's `files_touched` to write `199-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/199-obs-02-spec-impl-reconciliation/199-CONTEXT.md
@.planning/phases/199-obs-02-spec-impl-reconciliation/199-RESEARCH.md
@.planning/phases/199-obs-02-spec-impl-reconciliation/199-PATTERNS.md
@.planning/phases/199-obs-02-spec-impl-reconciliation/199-VALIDATION.md

<interfaces>
<!-- The exact OBS-02 verbatim phrase the operator notes must reuse (D-04). -->
<!-- Quote with the em-dash character (U+2014) preserved exactly. -->
<!-- The phrase as it appears at .planning/REQUIREMENTS.md:27: -->

> cold-start and invalid-snapshot cycles produce absent SQLite rows and `/health` nulls — no NaN, -1, or sentinel emission.

<!-- Note the em-dash (—) before "no NaN". This file (PLAN.md) renders the em-dash; copy it byte-for-byte from REQUIREMENTS.md when editing the docs. -->

<!-- Four OBS-01 contract field names (RESEARCH.md Pitfall 5: do NOT add `refractory_active`): -->
- `active_primary_signal` (one of `"queue"`, `"rtt"`, `"none"`)
- `rtt_confidence` (float 0.0–1.0 or `null`)
- `cake_av_delay_delta_us` (int or `null`)
- `control_decision_reason` (short stable-vocabulary string)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify REQUIREMENTS.md OBS-02 staged wording (no edit)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 25-30 for OBS-02 row; line 122 for traceability table — current state, NOT to edit)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-CONTEXT.md (D-01 — verify-only, do not re-amend)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-PATTERNS.md (REQUIREMENTS.md verify-only section, lines 19-34)
  </read_first>
  <action>
**Verify-only task — DO NOT edit `.planning/REQUIREMENTS.md`.** Per D-01, the OBS-02 wording is pre-staged in commit `2e0211f`. Re-amending re-opens the wording the audit already accepted.

Run the four-anchor-phrase grep against the file as it stands. The acceptance criteria below mechanize this check; if any anchor phrase is missing, STOP and report — do not "fix" by editing.

Required anchor phrases (verbatim, exact strings):
1. `absent SQLite rows`
2. `cold-start`
3. `invalid-snapshot`
4. `wanctl_arbitration_active_primary`
5. `amended in Phase 199`

If all five greps return exit-0, this task is done. Record verification in the SUMMARY.md as: "REQUIREMENTS.md OBS-02 wording verified: 5/5 anchor phrases present at .planning/REQUIREMENTS.md:27."
  </action>
  <verify>
    <automated>for p in 'absent SQLite rows' 'cold-start' 'invalid-snapshot' 'wanctl_arbitration_active_primary' 'amended in Phase 199'; do grep -qF "$p" .planning/REQUIREMENTS.md || { echo "MISSING: $p"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `for p in 'absent SQLite rows' 'cold-start' 'invalid-snapshot' 'wanctl_arbitration_active_primary' 'amended in Phase 199'; do grep -qF "$p" .planning/REQUIREMENTS.md || { echo "MISSING: $p"; exit 1; }; done` exits 0
    - `git diff --name-only -- .planning/REQUIREMENTS.md` returns empty (no edit performed in this task)
  </acceptance_criteria>
  <done>All five anchor-phrase greps return exit-0; `.planning/REQUIREMENTS.md` is unchanged in the working tree (zero diff).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add signal_arbitration field-shape sub-bullet to docs/SUBSYSTEMS.md</name>
  <files>docs/SUBSYSTEMS.md</files>
  <read_first>
    - docs/SUBSYSTEMS.md (read lines 115-145 — `## Health And Metrics` section; need exact current "Major response sections" bullet block before editing)
    - .planning/REQUIREMENTS.md (line 27, OBS-02 row — copy the em-dash phrase byte-for-byte from here, NOT from PLAN.md)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-PATTERNS.md (SUBSYSTEMS.md section, lines 38-78 — style anchors and skeleton)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-RESEARCH.md (Pitfall 5, lines 265-269 — do NOT include `refractory_active`)
  </read_first>
  <action>
Insert a sub-bullet expansion immediately under the existing `wans[]` line in the "Major response sections" enumeration in `docs/SUBSYSTEMS.md` (insertion point: directly after the `wans[]` bullet at approximately line 132).

**Exact text to insert** (the new sub-bullet, indented two spaces to nest under `wans[]`):

```
  - `signal_arbitration` (per OBS-01): `active_primary_signal`, `rtt_confidence` (float 0.0–1.0 or `null`), `cake_av_delay_delta_us` (int or `null`), `control_decision_reason`. Per REQUIREMENTS.md OBS-02: cold-start and invalid-snapshot cycles produce absent SQLite rows and `/health` nulls — no NaN, -1, or sentinel emission. See RUNBOOK.md `/metrics/history` for the operator-side denominator note.
```

**Critical character preservation rules:**
1. The dash before "no NaN" is the Unicode em-dash U+2014 (—), NOT two hyphens (`--`) and NOT a single hyphen (`-`). Copy it from `.planning/REQUIREMENTS.md:27`, not from intermediate planning files (PATTERNS.md renders it as `--` for hook safety).
2. The dash in `0.0–1.0` is the Unicode en-dash U+2013 (–), matching the convention used in REQUIREMENTS.md OBS-01 (line 26).
3. All four field names must appear backticked exactly as: `active_primary_signal`, `rtt_confidence`, `cake_av_delay_delta_us`, `control_decision_reason`.
4. Do NOT enumerate a fifth field. `refractory_active` is emitted by `get_health_data()` at `wan_controller.py:4222-4239` but is outside the OBS-01 contract (RESEARCH.md Pitfall 5). Adding it would silently expand the spec under cover of a docs phase.
5. Do NOT reorganize `## Health And Metrics` or restructure the bullet list (D-08). The sub-bullet is the only change.

After inserting the sub-bullet, the surrounding enumeration must read (the lines immediately around the insertion):

```
Major response sections:

- `wans[]`: per-WAN rate, state, hysteresis, RTT, connectivity, measurement, IRTT, reflector, fusion, CAKE, tuning, storage, and runtime status.
  - `signal_arbitration` (per OBS-01): `active_primary_signal`, `rtt_confidence` (float 0.0–1.0 or `null`), `cake_av_delay_delta_us` (int or `null`), `control_decision_reason`. Per REQUIREMENTS.md OBS-02: cold-start and invalid-snapshot cycles produce absent SQLite rows and `/health` nulls — no NaN, -1, or sentinel emission. See RUNBOOK.md `/metrics/history` for the operator-side denominator note.
- `alerting`: alert engine enabled state, fired count, and active cooldowns.
```
  </action>
  <verify>
    <automated>for p in signal_arbitration active_primary_signal rtt_confidence cake_av_delay_delta_us control_decision_reason 'absent SQLite rows' 'cold-start' 'invalid-snapshot'; do grep -qF "$p" docs/SUBSYSTEMS.md || { echo "MISSING: $p"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `for p in signal_arbitration active_primary_signal rtt_confidence cake_av_delay_delta_us control_decision_reason; do grep -qF "$p" docs/SUBSYSTEMS.md || { echo "MISSING: $p"; exit 1; }; done` exits 0
    - `grep -qF 'absent SQLite rows' docs/SUBSYSTEMS.md` exits 0
    - `grep -qF 'cold-start and invalid-snapshot' docs/SUBSYSTEMS.md` exits 0
    - `grep -qF 'Per REQUIREMENTS.md OBS-02' docs/SUBSYSTEMS.md` exits 0
    - `grep -qF 'no NaN, -1, or sentinel emission' docs/SUBSYSTEMS.md` exits 0
    - `grep -cF 'refractory_active' docs/SUBSYSTEMS.md` returns 0 (the fifth field MUST NOT appear)
    - `git diff --name-only -- src/wanctl/ tests/` returns empty
    - The em-dash is preserved: `python3 -c "import sys; assert '—' in open('docs/SUBSYSTEMS.md').read(), 'em-dash U+2014 missing'"` exits 0
  </acceptance_criteria>
  <done>The `signal_arbitration` sub-bullet is present under `wans[]` in `## Health And Metrics`; all four contract field names are backticked; the verbatim OBS-02 phrase appears (em-dash preserved); `refractory_active` is NOT mentioned; no Python source changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add per-cycle denominator note to docs/RUNBOOK.md</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - docs/RUNBOOK.md (read lines 280-290 for the existing `>` blockquote callout idiom; read lines 340-375 for the `/metrics/history` reader block — the insertion site)
    - .planning/REQUIREMENTS.md (line 27, OBS-02 row — copy the em-dash phrase byte-for-byte from here)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-PATTERNS.md (RUNBOOK.md section, lines 81-131 — Pattern A blockquote idiom + insertion site description)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-CONTEXT.md (D-03 — operator-query note placement and content)
  </read_first>
  <action>
Insert a `>` blockquote callout in `docs/RUNBOOK.md` **immediately after the "endpoint-local vs. merged cross-WAN" paragraph that ends at line 365**, and **before** the per-WAN DB compaction subsection that begins at line 367 (the `If the per-WAN DB files stay materially larger than expected...` paragraph).

**Exact text to insert** (a blank line, the blockquote, then a blank line):

```
> Per-cycle SQLite denominator: `wanctl_arbitration_active_primary` is emitted on every CAKE-metrics-enabled cycle and is the reliable denominator for coverage queries against the per-WAN metrics SQLite store. `wanctl_rtt_confidence` and `wanctl_cake_avg_delay_delta_us` are emitted only when valid. Per REQUIREMENTS.md OBS-02: cold-start and invalid-snapshot cycles produce absent SQLite rows and `/health` nulls — no NaN, -1, or sentinel emission. Absent rows for those two metrics are expected, not data loss.
```

**Critical character preservation rules:**
1. The dash before "no NaN" is the Unicode em-dash U+2014 (—). Copy it from `.planning/REQUIREMENTS.md:27`, not from PATTERNS.md (which renders `--` for hook safety).
2. Every metric name must be backticked: `wanctl_arbitration_active_primary`, `wanctl_rtt_confidence`, `wanctl_cake_avg_delay_delta_us`.
3. Use the bare `>` blockquote idiom (no `> Note:` prefix). The leading capitalized phrase "Per-cycle SQLite denominator:" carries the role, matching the file's existing Pattern A style at lines 281-287.
4. Place the callout AFTER the "endpoint-local vs. merged cross-WAN" paragraph, NOT inside the curl example block and NOT before the bullet list. The insertion lands between the paragraph that ends `...identical in the TUI and in this runbook.` (line 365) and the paragraph that begins `If the per-WAN DB files stay materially larger...` (line 367).
5. Do NOT reorganize the `/metrics/history` reader block (D-08). The blockquote callout is the only change.

The word `denominator` MUST appear in the inserted text — Plan 02's VERIFICATION.md greps for it as a token-level acceptance check.
  </action>
  <verify>
    <automated>grep -qF 'wanctl_arbitration_active_primary' docs/RUNBOOK.md && grep -qF 'denominator' docs/RUNBOOK.md && grep -qF 'absent SQLite rows' docs/RUNBOOK.md && grep -qF 'Per REQUIREMENTS.md OBS-02' docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'wanctl_arbitration_active_primary' docs/RUNBOOK.md` exits 0
    - `grep -qF 'denominator' docs/RUNBOOK.md` exits 0
    - `grep -qF 'absent SQLite rows' docs/RUNBOOK.md` exits 0
    - `grep -qF 'cold-start and invalid-snapshot' docs/RUNBOOK.md` exits 0
    - `grep -qF 'Per REQUIREMENTS.md OBS-02' docs/RUNBOOK.md` exits 0
    - `grep -qF 'no NaN, -1, or sentinel emission' docs/RUNBOOK.md` exits 0
    - `grep -qF 'wanctl_rtt_confidence' docs/RUNBOOK.md && grep -qF 'wanctl_cake_avg_delay_delta_us' docs/RUNBOOK.md` exits 0
    - `git diff --name-only -- src/wanctl/ tests/` returns empty
    - The em-dash is preserved: `python3 -c "import sys; content=open('docs/RUNBOOK.md').read(); assert 'denominator' in content and '—' in content[content.index('denominator'):], 'em-dash U+2014 missing in inserted callout'"` exits 0
  </acceptance_criteria>
  <done>The `>` blockquote callout naming `wanctl_arbitration_active_primary` as the per-cycle denominator is present after the `/metrics/history` reader block; the verbatim OBS-02 phrase appears (em-dash preserved); the word `denominator` appears; no Python source changed.</done>
</task>

<task type="auto">
  <name>Task 4: Plan-01 docs-only invariant + test-pin sanity</name>
  <files>(verification-only — no file modified)</files>
  <read_first>
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-VALIDATION.md (Per-Task Verification Map — task 199-01-05 docs-only invariant and 199-01-06 pytest pin)
    - .planning/phases/199-obs-02-spec-impl-reconciliation/199-RESEARCH.md (Pitfall 3 — measured pytest runtime 0.40s)
  </read_first>
  <action>
After Tasks 1-3 land, run the docs-only invariant check and the absent-row test pin to confirm Plan 01 left zero Python source changes and the spec-pinned test still passes.

This is a verification-only task — no file is edited. The two automated commands below are the gates:

1. `git diff --name-only -- src/wanctl/ tests/` MUST return empty. If any path appears, Plan 01 violated D-07 (no Python behavior change) and the executor must STOP and report — do NOT delete the offending change without operator review.

2. `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py -k "skip_rtt_confidence_when_none" -q` MUST pass with `1 passed`. RESEARCH.md Pitfall 3 measured this at 0.40s; budget is 5s. If it fails, the absent-row test pin has drifted and the spec-impl-doc lockstep is broken — STOP and report.

Record both results in the plan's SUMMARY.md as: "Docs-only invariant: PASS (zero src/wanctl or tests/ diff). Test pin: PASS (skip_rtt_confidence_when_none, 1 passed in <Xs>s)."
  </action>
  <verify>
    <automated>[ -z "$(git diff --name-only -- src/wanctl/ tests/)" ] && .venv/bin/pytest -o addopts='' tests/test_wan_controller.py -k "skip_rtt_confidence_when_none" -q</automated>
  </verify>
  <acceptance_criteria>
    - `[ -z "$(git diff --name-only -- src/wanctl/ tests/)" ]` exits 0 (zero Python source / test diff for this plan's edits)
    - `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py -k "skip_rtt_confidence_when_none" -q` exits 0 with `1 passed` in stdout
    - `git diff --name-only` (untrimmed) lists ONLY `docs/SUBSYSTEMS.md` and `docs/RUNBOOK.md` as modified files for this plan (`.planning/REQUIREMENTS.md` is verify-only and must not appear in the diff)
  </acceptance_criteria>
  <done>Docs-only invariant proven (zero `src/wanctl/` or `tests/` diff); absent-row test pin passes; only `docs/SUBSYSTEMS.md` and `docs/RUNBOOK.md` show as modified.</done>
</task>

</tasks>

<verification>
After all four tasks complete, the following commands re-run from a fresh shell must all exit 0:

```bash
# Anchor phrases in REQUIREMENTS.md (verify-only)
for p in 'absent SQLite rows' 'cold-start' 'invalid-snapshot' 'wanctl_arbitration_active_primary' 'amended in Phase 199'; do
  grep -qF "$p" .planning/REQUIREMENTS.md || { echo "MISSING: $p"; exit 1; }
done

# SUBSYSTEMS.md field-shape note
for p in signal_arbitration active_primary_signal rtt_confidence cake_av_delay_delta_us control_decision_reason; do
  grep -qF "$p" docs/SUBSYSTEMS.md || { echo "MISSING: $p"; exit 1; }
done
grep -qF 'absent SQLite rows' docs/SUBSYSTEMS.md

# RUNBOOK.md operator-query note
grep -qF 'wanctl_arbitration_active_primary' docs/RUNBOOK.md
grep -qF 'denominator' docs/RUNBOOK.md
grep -qF 'absent SQLite rows' docs/RUNBOOK.md

# Docs-only invariant
[ -z "$(git diff --name-only -- src/wanctl/ tests/)" ]

# Test pin
.venv/bin/pytest -o addopts='' tests/test_wan_controller.py -k "skip_rtt_confidence_when_none" -q
```
</verification>

<success_criteria>
1. REQUIREMENTS.md OBS-02 row has all five anchor phrases present and unchanged in the working tree.
2. `docs/SUBSYSTEMS.md` `## Health And Metrics` contains a `signal_arbitration` sub-bullet under `wans[]` enumerating exactly the four OBS-01 contract fields and quoting the OBS-02 absent-row phrase verbatim (em-dash preserved); `refractory_active` is NOT mentioned.
3. `docs/RUNBOOK.md` SQLite/history section contains a `>` blockquote callout naming `wanctl_arbitration_active_primary` as the per-cycle denominator and quoting the OBS-02 absent-row phrase verbatim (em-dash preserved).
4. `git diff --name-only -- src/wanctl/ tests/` returns empty (docs-only invariant).
5. The absent-row test pin (`test_phase195_metrics_skip_rtt_confidence_when_none`) still passes in under 1s.
</success_criteria>

<output>
After completion, create `.planning/phases/199-obs-02-spec-impl-reconciliation/199-01-SUMMARY.md` recording:
- Files modified: `docs/SUBSYSTEMS.md`, `docs/RUNBOOK.md` (REQUIREMENTS.md verify-only, no edit)
- The exact insertion lines (post-edit) where the new sub-bullet and blockquote callout landed
- Output of `[ -z "$(git diff --name-only -- src/wanctl/ tests/)" ] && echo PASS` (must print `PASS`)
- Output of the absent-row pytest pin (must print `1 passed`)
- A confirmation that the em-dash U+2014 is byte-preserved in both doc edits

Plan 02 reads this SUMMARY.md to populate `199-VERIFICATION.md`'s `files_touched` and Observable Truths body.
</output>
