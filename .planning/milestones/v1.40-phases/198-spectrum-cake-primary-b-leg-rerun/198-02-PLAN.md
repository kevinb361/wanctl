---
phase: 198-spectrum-cake-primary-b-leg-rerun
plan: 02
type: execute
wave: 2
depends_on: ["198-01"]
files_modified:
  - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-start-*-summary.json
  - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-summary.json
  - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/raw/
  - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json
  - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/primary-signal-audit-phase197.json
autonomous: false
requirements: [VALN-04]
must_haves:
  truths:
    - "Soak start capture taken on Phase 197 build (Plan 01 verdict pass)"
    - "Wall-clock duration between start capture and finish capture is ≥86400 seconds (24h)"
    - "Capture script wrote per-call summary JSON for both start and finish"
    - "Raw PSV exports are present (granularity='raw' rows over the full window)"
    - "Phase 197 audit predicate run over raw rows only (granularity filter applied at parse time)"
    - "Audit accept-list pass: only {queue_distress, green_stable, queue_during_refractory} reasons; documented exceptions counted in a separate bucket"
    - "≥95% queue-primary coverage of raw rows (queue_samples + queue_samples_via_refractory_rtt_fallback) / total"
    - "No unexplained refractory fallback (rtt_fallback_during_refractory paired with refractory_active=true counts as documented exception, not failure)"
  artifacts:
    - path: ".planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-start-*-summary.json"
      provides: "T0 snapshot of /health, journal, raw + aggregate sqlite metrics"
      contains: "signal_arbitration"
    - path: ".planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-summary.json"
      provides: "T0+24h snapshot — comparator anchor for the audit"
      contains: "signal_arbitration"
    - path: ".planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json"
      provides: "Manual gate evidence: soak_start, soak_end, elapsed_seconds ≥ 86400"
      contains: "elapsed_seconds"
    - path: ".planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/primary-signal-audit-phase197.json"
      provides: "Phase 197 audit predicate verdict over raw SQLite rows"
      contains: "accept_list_pass"
  key_links:
    - from: "cake-primary-finish-*-summary.json"
      to: "primary-signal-audit-phase197.json"
      via: "raw PSV export filtered to granularity='raw' then classified by Phase 197 predicate"
      pattern: "raw_metric_total_samples"
    - from: "soak-window.json.elapsed_seconds"
      to: "cake-primary-start vs cake-primary-finish captured_at_iso"
      via: "(soak_end - soak_start) >= 86400"
      pattern: "elapsed_seconds"
---

<objective>
Run a clean ≥24h Spectrum cake-primary B-leg soak on the Phase 197 build (deployment proven by Plan 01), capture `/health` + journal + raw + aggregate SQLite metrics at start and finish, then run the Phase 197 audit predicate over the raw rows only (granularity='raw' filter mandatory at parse time per Pitfall 3) and emit `primary-signal-audit-phase197.json`.

Purpose: Phase 196 B-leg failed twice — once because the audit counted 1-minute aggregates, and once because refractory masking forced RTT fallback. Phase 197 split detection from arbitration so refractory windows now correctly classify as queue-primary (or as `rtt_fallback_during_refractory` documented exceptions). Phase 198 must prove this on a fresh ≥24h soak using the same deployment token.

Output: capture-script per-call summaries, raw PSV exports, soak-window.json (manual ≥86400s gate), and `primary-signal-audit-phase197.json` (verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-RESEARCH.md
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-PATTERNS.md
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-VALIDATION.md
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-01-PLAN.md
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/preflight.json
@.planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/safe05-baseline.json
@.planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/primary-signal-audit-phase197.md
@.planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/raw-only-primary-signal-audit.json
@.planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/cake-primary-finish-20260427T092154Z-summary.json
@.planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/rtt-blend/primary-signal-audit.json
@scripts/phase196-soak-capture.sh

<interfaces>
<!-- Capture-script invocation (must run with the same env vars exported in Plan 01 Task 2). -->
./scripts/phase196-soak-capture.sh cake-primary-start
./scripts/phase196-soak-capture.sh cake-primary-finish

<!-- Capture script writes:
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-summary.json
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-health.json
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-journal.log
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-sqlite-metrics.psv          (raw rows over last 24h)
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-sqlite-metrics-aggregate.psv (1m aggregates)
  ${PHASE196_OUT_DIR}/cake-primary/cake-primary-start-<TS>-fusion-transitions.log
  (and the matching cake-primary-finish-* set)
-->

<!-- Phase 197 audit predicate (locked at primary-signal-audit-phase197.md): -->
<!-- Health-sample classification: -->
ACCEPT_LIST_QUEUE = ["queue_distress", "green_stable", "queue_during_refractory"]
queue_primary               = active_primary_signal == "queue" AND control_decision_reason ∈ ACCEPT_LIST_QUEUE
queue_primary_refractory_rtt_fallback = control_decision_reason == "rtt_fallback_during_refractory" AND refractory_active == true
non_queue                   = otherwise

<!-- Metric-row classification (granularity='raw' filter MANDATORY before counting): -->
metric_queue_samples                          = wanctl_arbitration_active_primary == 1.0
metric_queue_samples_via_refractory_rtt_fallback = active_primary == 2.0 AND wanctl_arbitration_refractory_active == 1.0
metric_non_queue_samples                       = otherwise
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator launches survival-safe 24h capture window</name>
  <files>
    .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-start-*-summary.json
    .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json (initial fields)
  </files>
  <read_first>
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-RESEARCH.md  (Pattern: Mode-gate is already-set; Pitfall 4 loaded-window definition)
    - .planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/196-MODE-GATE.md  (operator procedure)
    - scripts/phase196-soak-capture.sh
  </read_first>
  <what>
    The 24h soak window cannot run inside Claude's process because Claude does not survive a ≥24h wall-clock wait. The operator must launch the start capture, record the start timestamp, and schedule (or manually run) the finish capture ≥24h later. This task is a `human-action` checkpoint — Claude cannot automate it.
  </what>
  <how-to-verify>
    Operator MUST do all of the following from a shell that has the four PHASE196_* env vars from Plan 01 Task 2 exported:

    1. Confirm preflight verdict pass:
       ```bash
       jq -e '.verdict == "pass"' .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/preflight.json
       ```

    2. Run the start capture:
       ```bash
       ./scripts/phase196-soak-capture.sh cake-primary-start
       ```
       Note the start summary path printed and the start ISO timestamp inside it.

    3. Record the soak start in `.planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json`:
       ```json
       {
         "phase": 198,
         "leg": "cake-primary",
         "soak_start_utc": "<ISO 8601 from cake-primary-start summary captured_at_iso>",
         "soak_start_capture_path": "<path to cake-primary-start-*-summary.json>",
         "soak_finish_utc": null,
         "soak_finish_capture_path": null,
         "elapsed_seconds": null,
         "duration_gate_passed": false
       }
       ```

    4. Use a survival-safe pattern to schedule the finish capture (operator picks ONE):
       - `at` job: `echo './scripts/phase196-soak-capture.sh cake-primary-finish' | at now + 24 hours 30 minutes` (set the env vars in the at-job script).
       - `tmux` window left open and a manual run after ≥24h.
       - `systemd-run --user --on-active=24h30m -- ./scripts/phase196-soak-capture.sh cake-primary-finish` with env in `--setenv` flags.
       - `nohup` + `sleep` script (least preferred — no failure visibility).

       The operator chooses the mechanism. The Phase 196 MODE-GATE pattern explicitly notes this concern; mirror that.

    5. Operator confirms in writing: "Start capture completed at <ISO>. Finish capture scheduled via <mechanism> for <ISO+24h+>."
  </how-to-verify>
  <acceptance_criteria>
    - File `cake-primary-start-*-summary.json` exists under `soak/cake-primary/`.
    - `soak-window.json` exists with `soak_start_utc` populated and `duration_gate_passed: false` (will be flipped after finish).
    - Operator confirms scheduling mechanism in chat.
  </acceptance_criteria>
  <resume-signal>Type "start capture done; finish scheduled at <ISO>" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator returns ≥24h later — finish capture + duration gate</name>
  <files>
    .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-summary.json
    .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json (final fields)
  </files>
  <read_first>
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json  (start values written by Task 1)
    - scripts/phase196-soak-capture.sh
  </read_first>
  <what>
    After ≥24h elapsed wall-clock, capture the finish snapshot and gate the duration. Cannot be automated by Claude.
  </what>
  <how-to-verify>
    Operator (returning to shell with PHASE196_* env vars exported):

    1. Run the finish capture:
       ```bash
       ./scripts/phase196-soak-capture.sh cake-primary-finish
       ```
       Note the finish summary path and finish ISO timestamp.

    2. Update `soak-window.json` with final values:
       ```json
       {
         "phase": 198,
         "leg": "cake-primary",
         "soak_start_utc": "<from Task 1>",
         "soak_start_capture_path": "<from Task 1>",
         "soak_finish_utc": "<ISO 8601 from cake-primary-finish summary>",
         "soak_finish_capture_path": "<path to cake-primary-finish-*-summary.json>",
         "elapsed_seconds": <int — finish epoch minus start epoch>,
         "duration_gate_passed": <bool — true iff elapsed_seconds >= 86400>
       }
       ```

    3. If `elapsed_seconds < 86400`, the gate fails. Re-run the finish capture later (the soak window must be ≥24h wall-clock); update the file when redone.

    4. Confirm zero concurrent Spectrum experiment ran during the window (operator attestation; if any experiment ran, treat the soak as contaminated and restart Plan 02 from Task 1).
  </how-to-verify>
  <acceptance_criteria>
    - `jq -e '.duration_gate_passed == true and .elapsed_seconds >= 86400' soak-window.json` returns true.
    - `cake-primary-finish-*-summary.json` exists.
    - PSV files exist: `cake-primary-finish-*-sqlite-metrics.psv` and `cake-primary-finish-*-sqlite-metrics-aggregate.psv`.
  </acceptance_criteria>
  <resume-signal>Type "finish captured, duration_gate_passed=true" or describe issues</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run Phase 197 audit predicate over raw rows + emit primary-signal-audit-phase197.json</name>
  <files>
    .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/primary-signal-audit-phase197.json
  </files>
  <read_first>
    - .planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/primary-signal-audit-phase197.md  (LOCKED contract — DO NOT modify)
    - .planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/raw-only-primary-signal-audit.json  (analog schema for verdict shape)
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-RESEARCH.md  (Pitfall 3: granularity='raw' filter mandatory)
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-PATTERNS.md  (Section 4 schema; Shared Pattern B)
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-summary.json
    - .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-sqlite-metrics.psv  (raw rows; PSV)
  </read_first>
  <action>
    Run from repo root, after Task 2 completes (soak-window.json `duration_gate_passed: true`).

    1. Resolve the finish capture summary path and the raw PSV path:
       ```bash
       FINISH_SUMMARY=$(ls -t .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/cake-primary-finish-*-summary.json | head -1)
       RAW_PSV=$(jq -r '.artifacts.sqlite_metrics' "$FINISH_SUMMARY")
       AGG_PSV=$(jq -r '.artifacts.sqlite_metrics_aggregate' "$FINISH_SUMMARY")
       SOAK_START=$(jq -r '.soak_start_utc' .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json)
       SOAK_END=$(jq -r '.soak_finish_utc' .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/soak-window.json)
       ```

    2. PIN granularity filter form. The capture script's SQL emits raw rows but the PSV does not always include a granularity column header — inspect the header once:
       ```bash
       head -1 "$RAW_PSV"
       ```
       If a `granularity` column is present, filter at parse time to rows where granularity == 'raw'. If not, the capture script's SQL already filtered (Phase 196 lineage proves this — `raw-only-primary-signal-audit.json` keys its 74697 sample count off the same PSV layout). Record the chosen filter form in the audit JSON under `granularity_filter_form`.

    3. Write a small Python audit script (do NOT modify any `src/wanctl/` file; this script lives only as a one-shot in your shell or under `.planning/phases/198-*/`). The script implements RESEARCH §"Code Example 3" verbatim. Pseudocode:
       ```python
       # .venv/bin/python3 - <<'PY'
       # Reads RAW_PSV, parses each row as a dict (PSV header row gives column names).
       # If 'granularity' column present: filter rows where granularity == 'raw'.
       # Counts:
       #   metric_queue_samples                              when wanctl_arbitration_active_primary == 1.0
       #   metric_queue_samples_via_refractory_rtt_fallback  when active_primary == 2.0 AND wanctl_arbitration_refractory_active == 1.0
       #   metric_non_queue_samples                          otherwise
       # Records non-queue timestamps + their value into raw_metric_non_queue_timestamps[].
       # PY
       ```

    4. Compute coverage:
       ```
       queue_primary_coverage_pct = (metric_queue_samples + metric_queue_samples_via_refractory_rtt_fallback) / raw_metric_total_samples * 100
       ```

    5. Apply verdict rules from `primary-signal-audit-phase197.md`:
       - `pass`: `metric_non_queue_samples == 0` AND `queue_primary_coverage_pct >= 95`.
       - `pass_with_documented_exceptions`: `metric_non_queue_samples > 0` but each non-queue row has documented context AND `queue_primary_coverage_pct >= 95`. (Phase 198 should aim for `pass`; if non-queue rows persist on Phase 197 build that is itself a finding.)
       - `fail`: otherwise (e.g., `queue_primary_coverage_pct < 95` or unexplained non-queue rows).

       The accept-list pass is encoded as a separate boolean: `accept_list_pass = (metric_non_queue_samples == 0)`. If false but Phase 197 reasons (`queue_during_refractory`, `rtt_fallback_during_refractory + refractory_active=true`) account for all non-queue rows, the verdict is `pass_with_documented_exceptions`.

    6. Write `.planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/primary-signal-audit-phase197.json` with this exact schema:
       ```json
       {
         "phase": 198,
         "leg": "cake-primary",
         "audit_predicate_path": ".planning/phases/196-spectrum-a-b-soak-and-att-regression-canary/soak/cake-primary/primary-signal-audit-phase197.md",
         "raw_metrics_path": "<RAW_PSV>",
         "aggregate_metrics_path": "<AGG_PSV>",
         "granularity_filter_form": "column:granularity=raw | sql_only | other",
         "source_window": {
           "start_utc": "<SOAK_START>",
           "end_utc": "<SOAK_END>",
           "start_timestamp": <epoch>,
           "end_timestamp": <epoch>
         },
         "raw_metric_total_samples": <int>,
         "raw_metric_queue_samples": <int>,
         "raw_metric_queue_samples_via_refractory_rtt_fallback": <int>,
         "raw_metric_non_queue_samples": <int>,
         "raw_metric_non_queue_timestamps": [
           { "sampled_utc": "...", "timestamp": <epoch>,
             "value": <float>, "context": "<short>" }
         ],
         "aggregate_metric_total_samples": <int>,
         "aggregate_metric_note": "1-minute aggregate rows are averages over raw categorical values; excluded from verdict per Phase 197 audit predicate.",
         "queue_primary_coverage_pct": <float>,
         "accept_list_pass": <bool>,
         "rtt_fallback_during_refractory_count": <int>,
         "verdict": "pass | pass_with_documented_exceptions | fail",
         "decision": "<one short sentence — e.g. 'Coverage 99.99%; zero unexplained refractory fallback; accept'>"
       }
       ```

    7. If verdict is `fail`, surface a clear message to the operator listing each non-queue timestamp + reason. Do not silently proceed to Plan 03.
  </action>
  <verify>
    <automated>jq -e '(.verdict == "pass" or .verdict == "pass_with_documented_exceptions") and .queue_primary_coverage_pct >= 95 and (.raw_metric_total_samples >= 50000) and ((.accept_list_pass == true) or (.rtt_fallback_during_refractory_count >= 0))' .planning/phases/198-spectrum-cake-primary-b-leg-rerun/soak/cake-primary/primary-signal-audit-phase197.json</automated>
  </verify>
  <acceptance_criteria>
    - `primary-signal-audit-phase197.json` exists under `soak/cake-primary/`.
    - `jq -e '.queue_primary_coverage_pct >= 95'` returns true.
    - `jq -e '.verdict == "pass" or .verdict == "pass_with_documented_exceptions"'` returns true.
    - `jq -e '.raw_metric_total_samples >= 50000'` returns true (≥24h at 50ms cycle should produce ≥70k raw rows; 50k is a defensive floor accommodating sampling losses).
    - For every entry in `raw_metric_non_queue_timestamps[]`, `context` field is non-empty (no silent failures).
    - `audit_predicate_path` references the locked Phase 197 predicate document (NOT a copy or fork).
  </acceptance_criteria>
  <done>
    Audit verdict pass (or pass_with_documented_exceptions). VALN-04 primary-signal invariant satisfied for the cake-primary B-leg. Plan 03 may proceed to throughput captures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator dev VM → cake-shaper SSH | Capture script reads journal + sqlite over SSH |
| Phase 196 capture-script PSV → audit script | Untrusted text input parsed by Python; must filter granularity at parse time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-198-04 | Tampering | Audit double-counting from non-raw rows (the 196-07 false-failure pattern) | mitigate | Task 3 mandates `granularity='raw'` filter at parse time AND records the filter form in the audit JSON. Aggregate PSV is captured but NOT counted in verdict. |
| T-198-07 | Repudiation | Operator runs partial soak (<24h) and forgets | mitigate | Task 2 emits `soak-window.json` with `duration_gate_passed` boolean computed from epoch math. Plan 03 verify gate keys off this field. |
| T-198-08 | Tampering | Concurrent Spectrum experiment during the 24h window pollutes counters | accept (operator attestation) | Plan 01 Task 2 already required operator confirmation; Plan 02 Task 2 re-attests. No technical lock available. |
</threat_model>

<verification>
- `cake-primary-start-*-summary.json` and `cake-primary-finish-*-summary.json` both exist with non-null `signal_arbitration` blocks.
- `soak-window.json` has `duration_gate_passed: true`.
- `primary-signal-audit-phase197.json` verdict is `pass` or `pass_with_documented_exceptions` with coverage ≥95%.
- No source code under `src/wanctl/` was modified during this plan (regression slice still passes):
  ```
  .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_phase_197_replay.py -q
  ```
</verification>

<success_criteria>
1. Wall-clock soak duration ≥86400 seconds (manual gate enforced by `soak-window.json.duration_gate_passed`).
2. Audit predicate accept-list pass OR documented-exception bucket only (no unexplained non-queue raw rows).
3. ≥95% queue-primary coverage in raw rows over the full window.
4. Aggregate rows excluded from verdict (granularity filter applied at parse time).
</success_criteria>

<output>
After completion, create `.planning/phases/198-spectrum-cake-primary-b-leg-rerun/198-02-SUMMARY.md` with:
- key-files.created: cake-primary-start-*-summary.json, cake-primary-finish-*-summary.json, soak-window.json, primary-signal-audit-phase197.json
- requirements-addressed: [VALN-04]
- next plan: 198-03 (three corrected source-bound flent runs + throughput verdict)
</output>
