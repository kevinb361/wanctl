# Phase 91: Container Networking Audit - Context

**Gathered:** 2026-03-16
**Status:** Ready for planning

<domain>
## Phase Boundary

The latency contribution of container networking (veth pairs, Linux bridge) to RTT measurements is measured, quantified, and documented. Delivers an automated measurement script, a comprehensive audit report with stats/interpretation/recommendation, and network topology documentation. This is a measurement and documentation phase -- code changes to the autorate daemon are only needed if overhead exceeds 0.5ms (deferred to future phase).

</domain>

<decisions>
## Implementation Decisions

### Measurement Methodology
- Host-to-container ping: from dev/host machine to each container IP (10.10.110.246 cake-spectrum, 10.10.110.247 cake-att)
- Measures full veth pair + bridge round-trip without any WAN component
- 1000+ samples per path at 10ms interval (~10 seconds per path)
- 5 runs per path for stability
- icmplib (already in venv) or standard ping via subprocess
- Script runs from host machine (not from inside containers)

### Automation
- Python script: `scripts/container_network_audit.py`
- Automated: runs measurements, computes stats, generates markdown report
- Reusable: can re-run after infrastructure changes to compare
- Outputs: `docs/CONTAINER_NETWORK_AUDIT.md`

### Audit Report Format
- Location: `docs/CONTAINER_NETWORK_AUDIT.md` (alongside PRODUCTION_INTERVAL.md, TRANSPORT_COMPARISON.md)
- Sections: Executive summary, per-container measurement tables, jitter analysis, comparison against 0.5ms threshold, recommendation
- Per-container metrics: mean, median, p95, p99, stddev, min, max, sample count
- Network topology section: veth pairs, bridge name, MTU, IPs (captured via SSH `ip link`/`ip addr`)

### Jitter Characterization (CNTR-02)
- Standard deviation of container RTT samples as jitter metric
- Compare container stddev against known WAN jitter (Spectrum ~3-5ms loaded, ATT ~2-3ms loaded, both ~0.5-1ms idle)
- Threshold: container jitter < 10% of WAN jitter = negligible
- Report includes ratio: container_stddev / wan_jitter_estimate

### Actionable Outcomes
- If mean RTT overhead < 0.5ms AND jitter < 10% of WAN jitter: close with report only (no code changes)
- If overhead >= 0.5ms: report includes investigation findings and recommendation options (macvlan migration, veth optimization, baseline RTT offset) -- implementation deferred to future phase
- Script also captures container network topology (bridge, veth pairs, MTU) via SSH for documentation context

### Claude's Discretion
- icmplib vs subprocess ping for measurement (whatever works cleanly from host)
- Script argument parsing (argparse vs hardcoded container IPs)
- Raw data storage format (if any beyond the report)
- Exact report markdown formatting
- How WAN jitter reference values are obtained (from production logs, health endpoint, or hardcoded estimates)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Measurement tools
- `src/wanctl/rtt_measurement.py` -- icmplib-based ping pattern (could reuse for host-to-container measurement)
- `src/wanctl/benchmark.py` -- subprocess.run() pattern for external tool invocation

### Container details
- Production containers: cake-spectrum (10.10.110.246), cake-att (10.10.110.247)
- Container access: `ssh cake-spectrum`, `ssh cake-att`

### Existing docs pattern
- `docs/PRODUCTION_INTERVAL.md` -- Example of a measurement-based operational doc with data tables and analysis
- `docs/TRANSPORT_COMPARISON.md` -- Example of a comparison-style doc

### Requirements
- `.planning/REQUIREMENTS.md` -- CNTR-01, CNTR-02, CNTR-03

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- icmplib ping (used in rtt_measurement.py): could be imported for host-to-container latency measurement
- subprocess.run ping pattern (used in benchmark.py): alternative if icmplib has privilege issues from host
- statistics module (used in signal_processing.py): mean, median, stdev computation

### Established Patterns
- Measurement-based docs: PRODUCTION_INTERVAL.md follows summary -> data -> analysis -> recommendation structure
- Script outputs: scripts/ directory exists for operational tooling (soak-monitor.sh, analyze_profiling.py)

### Integration Points
- `scripts/container_network_audit.py` -- new script
- `docs/CONTAINER_NETWORK_AUDIT.md` -- new doc (generated by script)
- No daemon code changes expected (report-only closure if < 0.5ms)

</code_context>

<specifics>
## Specific Ideas

- The 0.5ms threshold from STATE.md is the decision boundary: below this, container overhead is considered noise in the context of 30-50ms WAN RTT measurements
- Jitter comparison uses real WAN numbers from production: Spectrum ~37.6ms avg with ~3-5ms jitter under load, ATT ~29.0ms avg with ~2-3ms jitter
- Script should be self-contained and runnable without the daemon (no wanctl imports beyond stdlib + icmplib)
- Network topology capture via SSH gives operators the full picture for future optimization decisions

</specifics>

<deferred>
## Deferred Ideas

None -- discussion stayed within phase scope

</deferred>

---

*Phase: 91-container-networking-audit*
*Context gathered: 2026-03-16*
