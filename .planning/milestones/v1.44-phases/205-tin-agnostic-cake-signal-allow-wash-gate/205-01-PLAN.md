---
phase: 205
plan: 01
type: execute
wave: 2
depends_on: [205-00]
files_modified:
  - tests/test_cake_signal.py
  - tests/test_cake_params.py
  - tests/backends/test_linux_cake.py
  - tests/backends/test_netlink_cake.py
  - tests/test_check_config.py
autonomous: true
requirements:
  - TOPO-01
  - TOPO-02
must_haves:
  truths:
    - "RED-behavior test exists asserting 1-tin besteffort layout produces non-zero active drop_rate (currently FAILS; turns GREEN after Plan 02)."
    - "RED-behavior test exists asserting allow_wash=true permits wash through build_cake_params (currently FAILS; turns GREEN after Plan 03)."
    - "RED-behavior test exists asserting linux_cake.initialize_cake emits 'wash'/'nowash' tokens (currently FAILS; turns GREEN after Plan 03)."
    - "RED-behavior test exists asserting netlink_cake.initialize_cake passes wash kwarg to pyroute2 IPRoute.tc() (currently FAILS; turns GREEN after Plan 03)."
    - "RED-behavior test exists asserting cake_params.allow_wash and cake_params.wash do not produce unknown-key WARN (currently FAILS; turns GREEN after Plan 03)."
    - "RED-behavior test exists for Spectrum-shaped {overhead_keyword: docsis, wash: True} exercising the LinuxCakeBackend fallback at netlink_cake.py:442 (currently FAILS; turns GREEN after Plan 03)."
    - "GREEN-invariant guard: pre-existing TestBuildCakeParamsExcluded::test_wash_excluded MUST pass before AND after."
    - "GREEN-invariant guard: TestBuildCakeParamsAllowWash::{test_allow_wash_does_not_permit_nat, test_allow_wash_does_not_permit_autorate_ingress, test_allow_wash_false_rejects_wash, test_allow_wash_absent_rejects_wash} MUST pass before AND after — they cover EXCLUDED_PARAMS at cake_params.py:150 which is unchanged across Phase 205."
    - "GREEN-invariant guard: 4-tin diffserv4 byte-identity snapshot test pins exact CAKE signal field outputs before AND after Plan 02 helper extraction (Codex MEDIUM-4)."
  artifacts:
    - path: tests/test_cake_signal.py
      provides: "Parameterized make_mock_stats(tin_count=) + TestCakeSignalProcessorBestEffort + TestCakeSignalProcessorBestEffortStructuralOracle + TestCakeSignalProcessorDiffserv4ByteIdentity"
      contains: "class TestCakeSignalProcessorBestEffort"
    - path: tests/test_cake_params.py
      provides: "TestBuildCakeParamsAllowWash class (1 RED-BEHAVIOR + 4 GREEN-INVARIANT)"
      contains: "class TestBuildCakeParamsAllowWash"
    - path: tests/backends/test_linux_cake.py
      provides: "test_initialize_cake_emits_wash + _emits_nowash_on_explicit_false + _emits_wash_under_docsis_fallback"
      contains: "test_initialize_cake_emits_wash"
    - path: tests/backends/test_netlink_cake.py
      provides: "test_initialize_cake_passes_wash_kwarg + _passes_wash_false_kwarg + _falls_back_to_subprocess_for_docsis"
      contains: "test_initialize_cake_passes_wash_kwarg"
    - path: tests/test_check_config.py
      provides: "test_cake_params_allow_wash_no_unknown_key_warning in TestLinuxCakeValidation"
      contains: "test_cake_params_allow_wash_no_unknown_key_warning"
  key_links:
    - from: "tests/test_cake_signal.py::make_mock_stats"
      to: "src/wanctl/cake_signal.py::CakeSignalProcessor.update"
      via: "synthesized 1-tin tins_raw dict"
      pattern: "tin_count=1"
    - from: "tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity"
      to: "src/wanctl/cake_signal.py aggregation sites"
      via: "snapshot fixture asserting bit-equal field values pre-helper-extraction"
      pattern: "byte_identity"
    - from: "tests/test_cake_params.py::TestBuildCakeParamsAllowWash"
      to: "src/wanctl/cake_params.py::build_cake_params"
      via: "{'allow_wash': True, 'wash': True}"
      pattern: "allow_wash"
    - from: "tests/backends/test_netlink_cake.py::test_initialize_cake_falls_back_to_subprocess_for_docsis"
      to: "src/wanctl/backends/netlink_cake.py:442 super().initialize_cake() fallback"
      via: "{overhead_keyword: docsis, wash: True}"
      pattern: "overhead_keyword.*docsis"
---

<objective>
Wave 0 — author all behavior tests for Phase 205 (TOPO-01 + TOPO-02) BEFORE any production source is touched. EXPLICITLY split into two buckets per Codex review HIGH-1:

- **RED-BEHAVIOR tests** (Tasks 1 + 2): MUST FAIL at end of this plan; each turns GREEN after the corresponding Plan 02 / Plan 03 edit.
- **GREEN-INVARIANT guard tests** (Tasks 3 + 4): MUST PASS at end of this plan AND after Plans 02/03. Prove that pre-existing behavior (D-08 transparent-bridge protection at EXCLUDED_PARAMS:150 + diffserv4 numeric byte-identity) does not regress.

Codex HIGH-1: tests like `test_allow_wash_false_rejects_wash`, `test_allow_wash_does_not_permit_nat`, `test_allow_wash_does_not_permit_autorate_ingress` were authored as "RED" in v1, but `EXCLUDED_PARAMS = {"nat", "wash", "autorate-ingress"}` at cake_params.py:60 with the unconditional reject at line 150 means they pass GREEN today. They are GUARD tests, not RED tests. This rewrite tags every test with its expected color in the acceptance criteria.

Codex MEDIUM-4: Add a 4-tin numeric-snapshot fixture test in Task 3 that asserts every CAKE signal field is bit-equal before AND after Plan 02 helper extraction. Replay tests passing != byte-identical.

Codex MEDIUM-6: Spectrum's `overhead: "docsis"` triggers a netlink → subprocess fallback at netlink_cake.py:442-447. Task 2 adds a fallback-path RED test for `{overhead_keyword: "docsis", wash: True}`.

Codex LOW-9: rename "replay-oracle" → "structural aggregation oracle" (the synthesized 1-tin fixture is structural, not a captured replay).

Output: 5 modified test files; ~14 new test methods + 3 new test classes + 1 fixture extension. SAFE-09 phase-boundary diff bound applies only to `src/wanctl/`; tests are unrestricted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing fixture/class shape extracted from in-tree tests at HEAD=b82abf0. -->

From src/wanctl/cake_signal.py:183 (verified by grep):
```python
self._tin_names = tin_names or ["Bulk", "BestEffort", "Video", "Voice"]
```
Attribute name is `_tin_names` (with leading underscore). Codex LOW-8 was right — `self.tin_names` does NOT exist.

From tests/test_cake_signal.py:29-68 — make_mock_stats current signature: `(tin_drops, tin_backlog, tin_peak_delay, tin_avg_delay, tin_base_delay)` all `list[int] | None`. Defaults to `[0, 0, 0, 0]`. Returns `{ ..., "tins": [ {...} for i in range(4) ] }`. Extension: add `tin_count: int = 4` keyword; replace literal `[0, 0, 0, 0]` with `[0] * tin_count`; replace `range(4)` with `range(tin_count)`. Default 4 preserves existing 4-tin tests byte-identically.

From tests/test_cake_params.py:189-202 — TestBuildCakeParamsExcluded class (test_nat_excluded, test_wash_excluded, test_autorate_ingress_excluded). Mirror class shape.

From tests/backends/test_linux_cake.py:699-715 — `@patch("wanctl.backends.linux_cake.subprocess.run")` decorator + `cmd = mock_run.call_args[0][0]` extraction.

From tests/backends/test_netlink_cake.py:493-519 — `@patch("wanctl.backends.netlink_cake.IPRoute")` + `mock_instance.link_lookup.return_value = [42]` + `call_kwargs = mock_instance.tc.call_args[1]`.

From src/wanctl/backends/netlink_cake.py:442-447 — docsis fallback (if `overhead_keyword` in params, returns super().initialize_cake(params) → LinuxCakeBackend subprocess path). Spectrum config uses `overhead: "docsis"` → triggers this fallback at startup.

From tests/test_check_config.py:1306-1325 — class TestLinuxCakeValidation, helper `self._make_config(transport="linux-cake", cake_params={...})`. New test goes in same class.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED-BEHAVIOR tests for cake_signal.py besteffort + structural oracle (turns GREEN after Plan 02)</name>
  <files>tests/test_cake_signal.py</files>
  <read_first>
    - tests/test_cake_signal.py (full file)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"tests/test_cake_signal.py")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pitfall 2: Single-tin active = empty"; §"Open Questions Q4: Tin-name list for besteffort")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (HIGH-1 + LOW-8 + LOW-9)
  </read_first>
  <action>
    Step 1 — Parameterize `make_mock_stats`:
      - Add `tin_count: int = 4` as the LAST keyword argument (preserves all existing call sites).
      - Replace `tin_drops or [0, 0, 0, 0]` (and 4 sibling fallbacks) with `tin_drops or [0] * tin_count`.
      - Replace `for i in range(4)` in the `"tins": [...]` list-comp with `for i in range(tin_count)`.

    Step 2 — Add `TestCakeSignalProcessorBestEffort` (RED-BEHAVIOR — must FAIL today; GREEN after Plan 02). Place after `TestCakeSignalProcessorTinSeparation`:
      - `test_single_tin_drops_in_active_rate(self)`: cold-start with `make_mock_stats(tin_drops=[0], tin_count=1)`; then `snap = proc.update(make_mock_stats(tin_drops=[100], tin_count=1))`. Assert `snap.drop_rate > 0.0` AND `snap.total_drop_rate > 0.0`. **RED today** (range(1,1) empty → 0.0); **GREEN after Plan 02.**
      - `test_single_tin_backlog_aggregates(self)`: mirror `test_backlog_excludes_bulk`. Cold-start backlog=[0]; second update backlog=[50000]. Assert non-zero backlog signal. **RED today; GREEN after Plan 02.**
      - `test_single_tin_peak_delay_aggregates(self)`: mirror `test_peak_delay_excludes_bulk`. Cold-start peak=[0]; second peak=[5000]. Assert non-zero peak_delay. **RED today; GREEN after Plan 02.**
      - `test_single_tin_name_label_is_besteffort(self)` (Q4 + LOW-8): build processor with default tin_names. Update with `make_mock_stats(tin_count=1)` twice. Assert `snap.tins[0].name == "BestEffort"`. **RED today** (cake_signal.py:237 returns `self._tin_names[0]` which is `"Bulk"` — NOT `f"Tin{i}"` because i=0 < len(_tin_names)=4. Plan 02 Q4 heuristic flips it to `["BestEffort"]` for single-tin); **GREEN after Plan 02.**

    Step 3 — Add `TestCakeSignalProcessorBestEffortStructuralOracle` (RED-BEHAVIOR; LOW-9 naming honesty):
      - Class docstring: `"""Structural aggregation oracle — synthesized 1-tin fixture, not a captured replay. Confirms that the active-tin set on besteffort produces the same numeric output as the active-tin set on diffserv4 for matched logical load."""`
      - `test_besteffort_matches_diffserv4_for_same_active_load(self)`:
        - 4-tin: `make_mock_stats(tin_drops=[0, 100, 0, 0], tin_count=4)` (load in BestEffort tin index 1).
        - 1-tin: `make_mock_stats(tin_drops=[100], tin_count=1)`.
        - Two `CakeSignalProcessor` instances with identical `CakeSignalConfig`. Cold-start both with zero-drop variant; then update both with load variant.
        - Assert both snapshots' `drop_rate` are equal within 1e-9. Same for `total_drop_rate`.
        - Repeat for backlog (`tin_backlog=[0, 50000, 0, 0]` vs `[50000]`) and peak_delay (`tin_peak_delay=[0, 5000, 0, 0]` vs `[5000]`).
        - **RED today** (besteffort=0 vs diffserv4≈100/dt); **GREEN after Plan 02.**

    Do NOT touch src/wanctl/cake_signal.py in this task.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorBestEffort tests/test_cake_signal.py::TestCakeSignalProcessorBestEffortStructuralOracle -v 2>&1 | grep -E "FAILED|ERROR|PASSED" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestCakeSignalProcessorBestEffort\b" tests/test_cake_signal.py` returns 1
    - `grep -c "class TestCakeSignalProcessorBestEffortStructuralOracle" tests/test_cake_signal.py` returns 1
    - `grep -c "Structural aggregation oracle" tests/test_cake_signal.py` returns >= 1 (LOW-9)
    - `grep -cE 'snap\.tins\[0\]\.name' tests/test_cake_signal.py` returns >= 1 (LOW-8 + Q4)
    - `grep -v '^#' tests/test_cake_signal.py | grep -c "tin_count"` returns >= 4 (fixture sig + default-list replacements + range usage)
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorBestEffort tests/test_cake_signal.py::TestCakeSignalProcessorBestEffortStructuralOracle -v` shows ALL 5 new tests as FAILED (RED gate).
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorTinSeparation tests/test_cake_signal.py::TestCakeSignalProcessorEWMA -v` — pre-existing tests still PASS (no regression from fixture parameterization).
  </acceptance_criteria>
  <done>RED-BEHAVIOR cake_signal tests committed; all FAIL today; Plan 02 will turn them GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: RED-BEHAVIOR + GREEN-INVARIANT tests for allow_wash + wash emission + docsis fallback</name>
  <files>tests/test_cake_params.py, tests/backends/test_linux_cake.py, tests/backends/test_netlink_cake.py, tests/test_check_config.py</files>
  <read_first>
    - tests/test_cake_params.py (lines 189-202)
    - tests/backends/test_linux_cake.py (lines 699-715)
    - tests/backends/test_netlink_cake.py (lines 493-519)
    - tests/test_check_config.py (lines 1157-1325)
    - src/wanctl/backends/netlink_cake.py:442-447 (docsis fallback site)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (HIGH-1 + MEDIUM-6)
  </read_first>
  <action>
    Step 1 — `tests/test_cake_params.py`: Append `TestBuildCakeParamsAllowWash` after `TestBuildCakeParamsExcluded`. Mark each test with color in a comment above the method:

      - `# RED-BEHAVIOR (turns GREEN after Plan 03)`
        `test_allow_wash_true_permits_wash`: `params = build_cake_params("download", {"allow_wash": True, "wash": True})`. Assert `params.get("wash") is True` AND `"allow_wash" not in params`. **RED today** (wash unconditionally rejected by EXCLUDED_PARAMS); **GREEN after Plan 03.**

      - `# GREEN-INVARIANT (passes today; MUST still pass after Plan 03)`
        `test_allow_wash_false_rejects_wash`: `with pytest.raises(ConfigValidationError): build_cake_params("download", {"allow_wash": False, "wash": True})`. **GREEN today AND after Plan 03** (gate only opens on `is True`).

      - `# GREEN-INVARIANT`
        `test_allow_wash_absent_rejects_wash`: `with pytest.raises(ConfigValidationError): build_cake_params("download", {"wash": True})`. **GREEN today AND after Plan 03** (equivalent to test_wash_excluded).

      - `# GREEN-INVARIANT`
        `test_allow_wash_does_not_permit_nat`: `with pytest.raises(ConfigValidationError): build_cake_params("download", {"allow_wash": True, "nat": True})`. **GREEN today AND after Plan 03** (no allow_nat gate).

      - `# GREEN-INVARIANT`
        `test_allow_wash_does_not_permit_autorate_ingress`: `with pytest.raises(ConfigValidationError): build_cake_params("download", {"allow_wash": True, "autorate_ingress": True})`. **GREEN today AND after Plan 03.**

      Do NOT modify `TestBuildCakeParamsExcluded::test_wash_excluded`.

    Step 2 — `tests/backends/test_linux_cake.py`: Append three RED-BEHAVIOR tests in the same class as `test_initialize_cake_boolean_flags`. Mirror `@patch("wanctl.backends.linux_cake.subprocess.run")` + `cmd = mock_run.call_args[0][0]`:
      - `test_initialize_cake_emits_wash`: `backend.initialize_cake({"wash": True})`; assert `"wash" in cmd` AND `"nowash" not in cmd`. **RED today** (wash not in flag tuple at linux_cake.py:396); **GREEN after Plan 03.**
      - `test_initialize_cake_emits_nowash_on_explicit_false`: `backend.initialize_cake({"wash": False})`; assert `"nowash" in cmd` AND `all(t != "wash" for t in cmd)`. **RED today**; **GREEN after Plan 03.**
      - `test_initialize_cake_emits_wash_under_docsis_fallback` (MEDIUM-6): `backend.initialize_cake({"overhead_keyword": "docsis", "bandwidth": "920Mbit", "diffserv": "besteffort", "wash": True})`. Assert `"wash" in cmd` AND that the cmd shows evidence of docsis (`any("docsis" in str(t) for t in cmd)` OR `"overhead-keyword" in cmd`). The Spectrum-shaped subprocess path emits both. **RED today**; **GREEN after Plan 03.**

    Step 3 — `tests/backends/test_netlink_cake.py`: Append three RED-BEHAVIOR tests in the same class as `test_initialize_cake_maps_boolean_flags`. Mirror `@patch("wanctl.backends.netlink_cake.IPRoute")` + `mock_instance.link_lookup.return_value = [42]`:
      - `test_initialize_cake_passes_wash_kwarg`: `backend.initialize_cake({"wash": True})`; `call_kwargs = mock_instance.tc.call_args[1]`; assert `call_kwargs.get("wash") is True`. **RED today**; **GREEN after Plan 03.**
      - `test_initialize_cake_passes_wash_false_kwarg`: `backend.initialize_cake({"wash": False})`; assert `call_kwargs.get("wash") is False`. **RED today**; **GREEN after Plan 03.**
      - `test_initialize_cake_falls_back_to_subprocess_for_docsis` (MEDIUM-6): patch BOTH `wanctl.backends.netlink_cake.IPRoute` AND `wanctl.backends.linux_cake.subprocess.run`. `backend.initialize_cake({"overhead_keyword": "docsis", "bandwidth": "920Mbit", "diffserv": "besteffort", "wash": True})`. Assert `mock_instance.tc` was NOT called (netlink path skipped at netlink_cake.py:442 → super().initialize_cake) AND `subprocess.run` WAS called with a cmd containing `"wash"`. **RED today** (linux backend doesn't emit wash); **GREEN after Plan 03.**

    Step 4 — `tests/test_check_config.py`: Append RED-BEHAVIOR test in `TestLinuxCakeValidation` after `test_cake_params_no_unknown_key_warnings`:
      - `test_cake_params_allow_wash_no_unknown_key_warning`: use `self._make_config(transport="linux-cake", cake_params={"upload_interface": "enp8s0", "download_interface": "enp9s0", "allow_wash": True, "wash": True})`. Set `data["queues"]` and `data["continuous_monitoring"]` per the analog. Call `check_unknown_keys(data)`. Filter for `r.severity == Severity.WARN and ("allow_wash" in r.field or r.field.endswith(".wash"))`. Assert `len(unknown) == 0`. **RED today**; **GREEN after Plan 03.**

    Do NOT touch any production source.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_true_permits_wash tests/backends/test_linux_cake.py -k "emits_wash or emits_nowash or wash_under_docsis" tests/backends/test_netlink_cake.py -k "passes_wash or falls_back_to_subprocess_for_docsis" tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_allow_wash_no_unknown_key_warning -v 2>&1 | grep -E "FAILED|ERROR|PASSED" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestBuildCakeParamsAllowWash" tests/test_cake_params.py` returns 1
    - `grep -c "test_initialize_cake_emits_wash\b" tests/backends/test_linux_cake.py` returns 1
    - `grep -c "test_initialize_cake_emits_wash_under_docsis_fallback" tests/backends/test_linux_cake.py` returns 1
    - `grep -c "test_initialize_cake_falls_back_to_subprocess_for_docsis" tests/backends/test_netlink_cake.py` returns 1
    - `grep -c "test_cake_params_allow_wash_no_unknown_key_warning" tests/test_check_config.py` returns 1
    - `.venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_true_permits_wash -v` shows FAILED (RED).
    - `.venv/bin/pytest tests/backends/test_linux_cake.py -k "emits_wash or emits_nowash or wash_under_docsis" -v` shows all 3 FAILED (RED).
    - `.venv/bin/pytest tests/backends/test_netlink_cake.py -k "passes_wash or falls_back_to_subprocess_for_docsis" -v` shows all 3 FAILED (RED).
    - `.venv/bin/pytest tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_allow_wash_no_unknown_key_warning -v` shows FAILED (RED).
    - Pre-existing `tests/test_cake_params.py::TestBuildCakeParamsExcluded::test_wash_excluded` still PASSES.
  </acceptance_criteria>
  <done>RED-BEHAVIOR allow_wash/wash-emission/docsis-fallback tests committed; all FAIL today; Plan 03 will turn them GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN-INVARIANT diffserv4 byte-identity snapshot guard (MEDIUM-4 — pins numeric output before/after Plan 02)</name>
  <files>tests/test_cake_signal.py</files>
  <read_first>
    - tests/test_cake_signal.py (the parameterized make_mock_stats from Task 1; existing TestCakeSignalProcessorTinSeparation for shape reference)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (MEDIUM-4)
  </read_first>
  <action>
    Add `TestCakeSignalProcessorDiffserv4ByteIdentity` (GREEN-INVARIANT — passes today; MUST still pass after Plan 02 helper extraction). Place after `TestCakeSignalProcessorBestEffortStructuralOracle`.

    Class docstring: `"""Pins exact numeric output for a representative 4-tin diffserv4 fixture. The Phase 02 _active_tin_indices(4) helper MUST return range(1, 4) — byte-identical to the prior range(1, len(tins_raw)). This test asserts every CAKE signal field is bit-equal so any accumulation-order change in the helper would surface immediately. Replay tests passing != byte-identical (Codex MEDIUM-4)."""`

    Test method `test_diffserv4_signal_fields_bit_equal_to_pinned_snapshot`:
      - Construct a deterministic 4-tin fixture: `cold = make_mock_stats(tin_count=4)` (all zero); `loaded = make_mock_stats(tin_drops=[5, 100, 50, 25], tin_backlog=[1000, 50000, 20000, 5000], tin_peak_delay=[100, 5000, 2000, 500], tin_avg_delay=[80, 4000, 1500, 400], tin_base_delay=[50, 1000, 800, 200], tin_count=4)`.
      - Build `CakeSignalConfig(enabled=True, time_constant_sec=1.0)` and `CakeSignalProcessor(config=cfg)`.
      - Cold-start with `cold`; capture `snap0 = proc.update(loaded)`.
      - Assert every numeric field of `snap0` against PINNED LITERAL VALUES. The executor MUST run this test against current source (HEAD=b82abf0) once to capture the actual values, then bake them into the assertion as integer/float literals (use `pytest.approx(value, rel=0, abs=0)` for floats with int-equivalent values; for genuine floats use `rel=1e-12` to allow IEEE-754 reordering tolerance — but ZERO tolerance is the goal). Pinned fields:
        - `snap0.drop_rate` (active-tin sum: tins[1]+tins[2]+tins[3] = 175 / dt)
        - `snap0.total_drop_rate` (all-tin sum: 5+100+50+25 = 180 / dt)
        - `snap0.backlog_bytes` or equivalent active-backlog field (50000+20000+5000 = 75000)
        - `snap0.peak_delay_us` or equivalent (max of tins[1..3] peak = 5000)
        - `snap0.avg_delay_us` or equivalent (max of tins[1..3] avg = 4000)
        - `snap0.base_delay_us` or equivalent (max of tins[1..3] base = 1000)
        - `snap0.tins[0].name == "Bulk"` (4-tin diffserv4 default; LOW-8 attribute name verified)
        - `snap0.tins[1].name == "BestEffort"`
      - Inspect the actual `CakeSignalSnapshot` dataclass at `src/wanctl/cake_signal.py` to find the exact field names; the bullets above are the SEMANTIC fields and the executor uses the actual attribute names.
      - **Color: GREEN today AND GREEN after Plan 02** (Plan 02's `_active_tin_indices(4) == range(1, 4)` invariant is the exact contract this test enforces; Task 1 of Plan 02 will fail the plan's acceptance if this snapshot test goes RED).

    The pinned literal snapshot is the byte-identity contract. If Plan 02 changes accumulation order or float precision, these literal assertions fail.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestCakeSignalProcessorDiffserv4ByteIdentity" tests/test_cake_signal.py` returns 1
    - `grep -c "test_diffserv4_signal_fields_bit_equal_to_pinned_snapshot" tests/test_cake_signal.py` returns 1
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v` shows test PASSED (GREEN today — the pinned values were captured against current source).
    - The test contains literal numeric assertions (no `> 0` or `is not None` shortcuts): `grep -cE "assert .*== [0-9]" tests/test_cake_signal.py` (run scoped to the class block) returns >= 5 (one per pinned numeric field).
  </acceptance_criteria>
  <done>Diffserv4 byte-identity guard committed; PASSES today; Plan 02 acceptance requires it still PASSES after helper extraction (MEDIUM-4 closed).</done>
</task>

<task type="auto">
  <name>Task 4: GREEN-INVARIANT verification — confirm guard tests PASS at end of Plan 01</name>
  <files>(read-only audit; no source edits — runs the guard tests and confirms they pass at this stage)</files>
  <read_first>
    - tests/test_cake_params.py (TestBuildCakeParamsExcluded + TestBuildCakeParamsAllowWash GREEN-INVARIANT subset from Task 2)
    - tests/test_cake_signal.py (TestCakeSignalProcessorDiffserv4ByteIdentity from Task 3)
  </read_first>
  <action>
    Run the GREEN-INVARIANT guard subset and confirm ALL pass at end of Plan 01 (before any production source edit). This task does not write code; it asserts the boundary contract that the four GREEN-INVARIANT tests in Task 2 + the byte-identity guard in Task 3 form a baseline that Plans 02 + 03 must preserve.

    Step 1 — Run pre-existing exclusion guards:
      ```
      .venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsExcluded -v
      ```
      Must show all 3 PASSED (test_nat_excluded, test_wash_excluded, test_autorate_ingress_excluded).

    Step 2 — Run the GREEN-INVARIANT subset of TestBuildCakeParamsAllowWash:
      ```
      .venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_false_rejects_wash tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_absent_rejects_wash tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_does_not_permit_nat tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_does_not_permit_autorate_ingress -v
      ```
      Must show all 4 PASSED. These tests cover EXCLUDED_PARAMS at cake_params.py:150 — unchanged across Phase 205, so they pass today AND after Plan 03's gate edit.

    Step 3 — Run the diffserv4 byte-identity snapshot:
      ```
      .venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v
      ```
      Must PASS. This baseline locks in the exact numeric output that Plan 02's `_active_tin_indices(4) == range(1, 4)` invariant must preserve.

    Step 4 — Sanity-confirm the RED-BEHAVIOR subset (the inverse — these MUST fail):
      ```
      .venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_true_permits_wash tests/test_cake_signal.py::TestCakeSignalProcessorBestEffort tests/test_cake_signal.py::TestCakeSignalProcessorBestEffortStructuralOracle -v
      ```
      Must show all FAILED (the RED gate). If any pass GREEN today, the test was mis-tagged — abort the plan and re-classify.

    No source files are touched in this task. The verification gate is the pre-flight contract that Plans 02 + 03 will be measured against.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsExcluded tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_false_rejects_wash tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_absent_rejects_wash tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_does_not_permit_nat tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_does_not_permit_autorate_ingress tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 8 GREEN-INVARIANT tests PASS (3 pre-existing in TestBuildCakeParamsExcluded + 4 in TestBuildCakeParamsAllowWash + 1 in TestCakeSignalProcessorDiffserv4ByteIdentity).
    - The RED-BEHAVIOR sanity check (Step 4) confirms `test_allow_wash_true_permits_wash` and all `TestCakeSignalProcessorBestEffort*` tests FAIL today (no mis-tagging).
    - `git diff HEAD -- src/wanctl/` returns empty (no production source changed in this plan).
  </acceptance_criteria>
  <done>Wave 0 boundary contract established: 8 GREEN-INVARIANT guards PASS; ~10 RED-BEHAVIOR tests FAIL; Plans 02 + 03 inherit a known-good baseline. Codex HIGH-1 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only additions; no new attack surface. Tests run in dev/CI, not production daemon. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-01-01 | Tampering | tests/test_cake_signal.py make_mock_stats fixture parameterization | accept | Default `tin_count=4` preserves existing 4-tin call sites; Task 1 acceptance verifies pre-existing classes still PASS. |
| T-205-01-02 | Tampering | RED/GREEN mis-tagging silently masks broken gates | mitigate | Task 4 Step 4 explicitly asserts the RED subset is FAILED; if a test passes GREEN today, it's mis-tagged and the plan aborts. |
| T-205-01-03 | Tampering (regression mask) | Diffserv4 byte-identity guard pinned literals could be miscaptured | mitigate | Task 3 instructs the executor to run the test against current source ONCE to capture values, then bake them in. Plan 02 will fail loudly if helper extraction breaks the literals. |
| T-205-01-04 | Tampering (incomplete fallback coverage) | Spectrum docsis path never tested for wash | mitigate | Task 2 Step 3 docsis-fallback test patches BOTH netlink and subprocess; asserts the production-critical Spectrum path emits wash. Closes Codex MEDIUM-6. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_cake_signal.py tests/test_cake_params.py tests/backends/test_linux_cake.py tests/backends/test_netlink_cake.py tests/test_check_config.py -v --tb=no -q` shows pre-existing tests still PASS.
- All 10 RED-BEHAVIOR tests FAIL (gate confirmed).
- All 5 GREEN-INVARIANT tests (4 in TestBuildCakeParamsAllowWash GREEN subset + 1 byte-identity guard) PASS.
- `git diff HEAD -- src/wanctl/` empty (production source untouched).
</verification>

<success_criteria>
- Codex HIGH-1 (RED-test framing category error) closed: every test tagged RED-BEHAVIOR or GREEN-INVARIANT in plan + comments + Task 4 acceptance.
- Codex MEDIUM-4 (byte-identity not pinned) closed: TestCakeSignalProcessorDiffserv4ByteIdentity asserts literal numeric output.
- Codex MEDIUM-6 (docsis fallback uncovered) closed: 2 docsis-fallback tests cover the Spectrum-shaped path.
- Codex LOW-8 (tin-name attribute) closed: test uses `_tin_names` semantics; asserts `snap.tins[0].name == "BestEffort"` for single-tin.
- Codex LOW-9 (oracle naming) closed: class named `TestCakeSignalProcessorBestEffortStructuralOracle` with honest docstring.
- ~14 new tests authored across 5 test files.
- No production source changed.
</success_criteria>

<output>
After completion, create `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-01-SUMMARY.md` documenting:
- New test classes/methods (with file:line references)
- RED-BEHAVIOR vs GREEN-INVARIANT classification per test (table)
- Pinned literal values captured for diffserv4 byte-identity snapshot (so Plan 02 reviewer can cross-check)
- Hand-off to Plan 02 (which RED-BEHAVIOR tests must turn GREEN; which GREEN-INVARIANT must STAY GREEN)
- Hand-off to Plan 03 (which RED-BEHAVIOR tests must turn GREEN)
</output>
