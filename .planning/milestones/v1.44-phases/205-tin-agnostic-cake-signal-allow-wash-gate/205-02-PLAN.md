---
phase: 205
plan: 02
type: execute
wave: 3
depends_on: [205-01]
files_modified:
  - src/wanctl/cake_signal.py
autonomous: true
requirements:
  - TOPO-01
must_haves:
  truths:
    - "src/wanctl/cake_signal.py aggregation iterates over the actual tin set; diffserv4 (4-tin) outputs byte-identical to v1.43 close (6508d68) — verified by TestCakeSignalProcessorDiffserv4ByteIdentity literal-snapshot guard."
    - "Single-tin besteffort layout produces non-zero active drop_rate / backlog / peak_delay (was 0.0)."
    - "Structural aggregation oracle test (besteffort vs diffserv4 on matched logical load) numerically equal."
    - "Existing Phase 193/194/195 replay tests pass (no EWMA accumulation order change)."
    - "snap.tins[0].name == 'BestEffort' for single-tin layout when default _tin_names list is in effect (Q4 + LOW-8 closed)."
    - "Total-drops aggregation invariant: src/wanctl/cake_signal.py:308 (and adjacent total-sum sites at lines 251, 298, 340) continue to use range(len(tins_raw)) — covering ALL tins — UNCHANGED. The active-drops aggregation at line 307 switches to the helper; the total line stays."
  artifacts:
    - path: src/wanctl/cake_signal.py
      provides: "Tin-agnostic aggregation via _active_tin_indices(tin_count) helper at top of module; helper applied at 6 active-aggregation sites; total-drop sites untouched; Q4 _tin_names heuristic for single-tin label."
      contains: "_active_tin_indices"
  key_links:
    - from: "src/wanctl/cake_signal.py::CakeSignalProcessor.update active-aggregation sites"
      to: "src/wanctl/cake_signal.py::_active_tin_indices"
      via: "active_indices = _active_tin_indices(len(tins_raw))"
      pattern: "_active_tin_indices\\(len\\(tins_raw\\)\\)"
    - from: "src/wanctl/cake_signal.py::CakeSignalProcessor tin-name resolution at lines 237/326"
      to: "self._tin_names attribute (verified at line 183)"
      via: "single-tin → ['BestEffort']; else default ['Bulk', 'BestEffort', 'Video', 'Voice']"
      pattern: "BestEffort"
---

<objective>
TOPO-01 — Make `cake_signal.py` aggregation tin-agnostic so the controller ingests both single-tin besteffort and multi-tin diffserv4 CAKE state without per-deployment branching.

Purpose: Phase 209's Spectrum config flip (940 diffserv4 nowash → 920 besteffort wash) currently breaks the controller silently — `range(1, len(tins_raw))` collapses to empty for a 1-tin qdisc and `active_drops/backlog/peak_delay` go to permanent 0. ATT (diffserv4) MUST remain byte-identical (SAFE-09).

Codex HIGH-3 fix: the v1 acceptance criterion `grep -cE 'range(len(tins_raw))' ... returns 1` was wrong — the live source has THREE such uses (verified at lines 251, 298, 340 — the total-drops sites at line 308 plus 251/298/340 are the all-tin sums that MUST stay). Replaced with three-part assertion: positive (helper exists + applied), negative (no `range(1, len(tins_raw))` literal remaining), and total-drops invariant (the all-tin sums continue to iterate over all indices).

Codex MEDIUM-7 fix: file-scoped `git diff` checks instead of cross-cutting `--name-only` so Plan 02 + Plan 03 can run in parallel without racing each other's diff-scope assertions.

Codex LOW-8 fix: attribute name is `self._tin_names` (verified at cake_signal.py:183), NOT `self.tin_names`. Q4 heuristic uses the correct name.

Output: One file modified — `src/wanctl/cake_signal.py`. New helper `_active_tin_indices(tin_count) -> range`. Six active-aggregation sites switched to the helper. Total-drop sites at lines 251, 298, 308, 340 UNCHANGED. Q4 `_tin_names` heuristic applied. No EWMA / threshold / dwell / deadband / burst values changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-01-SUMMARY.md
@CLAUDE.md
@src/wanctl/cake_signal.py

<interfaces>
<!-- Verified at HEAD=b82abf0 by `grep -nE 'range\(1?,?\s*len\(tins_raw\)\)' src/wanctl/cake_signal.py`. -->

ACTIVE-AGGREGATION sites (use range(1, len(tins_raw)) — REPLACE with helper):
- Line 256: cold-start active_backlog
- Line 259: cold-start active_peak_delay
- Line 263: cold-start active_avg_delay
- Line 267: cold-start active_base_delay
- Line 277: cold-start active_max_delay_delta (inner generator inside max())
- Line 307: steady-state active_drops
- Line 345: steady-state active_backlog
- Line 348: steady-state active_peak_delay
- Line 352: steady-state active_avg_delay
- Line 356: steady-state active_base_delay
- Line 366: steady-state active_max_delay_delta (inner generator)

That's 11 line-level substitutions; PATTERNS §"Aggregation sites to rewrite" treats them as 6 logical sites (cold-start backlog, cold-start peak/avg/base, cold-start max_delay_delta, steady-state active_drops, steady-state backlog/peak/avg/base, steady-state max_delay_delta).

TOTAL-AGGREGATION sites (use range(len(tins_raw)) — DO NOT TOUCH):
- Line 251: cold-start total drops/total tin snapshot construction (covers ALL tins)
- Line 298: steady-state pre-loop total iteration (covers ALL tins)
- Line 308: steady-state total_drops (covers ALL tins)
- Line 340: steady-state tin snapshot construction (covers ALL tins)

These four lines all iterate `range(len(tins_raw))` (no offset 1) and are correct for both layouts. Plan 02 must NOT touch these.

TIN-NAME sites (Q4 + LOW-8):
- Line 183: `self._tin_names = tin_names or ["Bulk", "BestEffort", "Video", "Voice"]` (attribute name verified — `_tin_names` with leading underscore)
- Line 237: cold-start `name=self._tin_names[i] if i < len(self._tin_names) else f"Tin{i}"`
- Line 326: steady-state `name=self._tin_names[i] if i < len(self._tin_names) else f"Tin{i}"`

For single-tin layouts, `i=0 < 4`, so the name resolves to `self._tin_names[0]` which is `"Bulk"` — wrong for besteffort. Q4 heuristic: in `update()`, compute a local `tin_names` that is `["BestEffort"]` when (a) `len(tins_raw) == 1` AND (b) `self._tin_names == ["Bulk", "BestEffort", "Video", "Voice"]` (default — operator-supplied custom names pass through unchanged). Then reference the local at the two sites.

Helper insertion point: top of module after `u32_delta` (~line 65), before any class definitions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _active_tin_indices helper + apply at 6 active-aggregation sites + Q4 tin-name heuristic</name>
  <files>src/wanctl/cake_signal.py</files>
  <read_first>
    - src/wanctl/cake_signal.py (full file — 6 active sites + 4 total sites + 2 tin-name sites; line numbers verified above)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"src/wanctl/cake_signal.py"; §"Shared Patterns S2: Iteration-range-only refactor")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pitfall 3: Diffserv4 byte-identity regression"; §"Open Questions Q4")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (HIGH-3 grep fix; LOW-8 attribute name; LOW-9 naming)
    - tests/test_cake_signal.py (Wave 0 tests from Plan 01; the byte-identity snapshot is the hard gate)
  </read_first>
  <behavior>
    - GREEN: tests/test_cake_signal.py::TestCakeSignalProcessorBestEffort::test_single_tin_drops_in_active_rate
    - GREEN: TestCakeSignalProcessorBestEffort::test_single_tin_backlog_aggregates
    - GREEN: TestCakeSignalProcessorBestEffort::test_single_tin_peak_delay_aggregates
    - GREEN: TestCakeSignalProcessorBestEffort::test_single_tin_name_label_is_besteffort (Q4 + LOW-8)
    - GREEN: TestCakeSignalProcessorBestEffortStructuralOracle::test_besteffort_matches_diffserv4_for_same_active_load
    - STILL GREEN: TestCakeSignalProcessorDiffserv4ByteIdentity::test_diffserv4_signal_fields_bit_equal_to_pinned_snapshot (MEDIUM-4 hard gate — pinned literals must hold)
    - STILL GREEN: All pre-existing TestCakeSignalProcessorTinSeparation tests
    - STILL GREEN: All Phase 193/194/195 replay tests
  </behavior>
  <action>
    Step 1 — Insert helper at top-of-module (after `u32_delta` ~line 65, before class definitions):
    ```python
    def _active_tin_indices(tin_count: int) -> range:
        """Return the indices of tins that count as 'active load'.

        For multi-tin diffserv layouts (diffserv3/diffserv4/diffserv8), tin
        index 0 is Bulk and is intentionally deprioritised — its drops/backlog
        are expected under load and excluded from the active signal. For
        single-tin besteffort layouts, the one tin IS the active tin; there
        is no Bulk to exclude.

        Args:
            tin_count: len(tins_raw) from get_queue_stats().

        Returns:
            Index range to use for "active" aggregation.
            - tin_count >= 2: range(1, tin_count) — exclude Bulk
            - tin_count == 1: range(0, 1) — the only tin is active
            - tin_count == 0: range(0, 0) — empty (caller already early-returns)
        """
        if tin_count >= 2:
            return range(1, tin_count)
        return range(tin_count)
    ```

    Step 2 — Replace `range(1, len(tins_raw))` at the 6 ACTIVE-aggregation sites with `_active_tin_indices(len(tins_raw))`. Verified line numbers (HEAD=b82abf0):
      - Line 256 (cold-start active_backlog inner generator)
      - Line 259 (cold-start active_peak_delay inner generator)
      - Line 263 (cold-start active_avg_delay inner generator)
      - Line 267 (cold-start active_base_delay inner generator)
      - Line 277 (cold-start active_max_delay_delta — single inner reference)
      - Line 307 (steady-state active_drops)
      - Line 345 (steady-state active_backlog inner generator)
      - Line 348 (steady-state active_peak_delay inner generator)
      - Line 352 (steady-state active_avg_delay inner generator)
      - Line 356 (steady-state active_base_delay inner generator)
      - Line 366 (steady-state active_max_delay_delta — single inner reference)

      Optional refactor for clarity: compute `active_indices = _active_tin_indices(len(tins_raw))` once at the top of the cold-start block and once at the top of the steady-state block, then reuse the variable. Either inline-substitution or local-variable-then-reuse is acceptable; choose the smaller diff. Both forms are equivalent for byte-identity (range objects compare by start/stop/step).

    Step 3 — CRITICAL DO-NOT-TOUCH: the four TOTAL-aggregation sites at lines 251, 298, 308, 340 use `range(len(tins_raw))` (no offset 1). They cover ALL tins by design. Leave them exactly as-is. Plan 02 acceptance verifies this with a count gate.

    Step 4 — Q4 tin-name heuristic (closes LOW-8 + Q4). In `CakeSignalProcessor.update()`, near the top of the method (after `tins_raw` is extracted but before tin snapshots are built), insert:
    ```python
    # Phase 205 Q4: meaningful Prometheus label for single-tin besteffort
    # layouts. Operator-supplied custom tin_names pass through unchanged.
    default_tin_names = ["Bulk", "BestEffort", "Video", "Voice"]
    if len(tins_raw) == 1 and self._tin_names == default_tin_names:
        tin_names = ["BestEffort"]
    else:
        tin_names = self._tin_names
    ```
    Then at lines 237 and 326, replace `self._tin_names[i] if i < len(self._tin_names) else f"Tin{i}"` with `tin_names[i] if i < len(tin_names) else f"Tin{i}"` (using the local variable). Default behavior unchanged for diffserv4 (tin_names == self._tin_names when len != 1 OR when operator overrode the names).

    Step 5 — Diffserv4 byte-identity proof:
      - `_active_tin_indices(4)` returns `range(1, 4)` — identical to `range(1, len(tins_raw))` when `len == 4`. Inner expressions unchanged. Sum/max accumulation order unchanged.
      - The `TestCakeSignalProcessorDiffserv4ByteIdentity` snapshot test from Plan 01 Task 3 is the hard gate. If Plan 02 inadvertently changed accumulation order, the pinned literal assertions fail loudly.

    DO NOT change:
      - Any threshold value, EWMA time-constant, dwell timer, deadband, burst counter, or zone classification logic (SAFE-09).
      - Any other src/wanctl/ file (SAFE-09 phase-boundary diff bound for Plan 02 = cake_signal.py only).
      - The `total_drops` line at 308 or any of the other TOTAL sites (251, 298, 340).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_phase_193_replay.py tests/test_phase_194_replay.py tests/test_phase_195_replay.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    POSITIVE assertions (helper exists + applied):
    - `grep -c '^def _active_tin_indices' src/wanctl/cake_signal.py` returns 1
    - `grep -cE '_active_tin_indices\(len\(tins_raw\)\)' src/wanctl/cake_signal.py` returns >= 1 (helper applied; local-variable refactor may collapse multiple call sites into one assignment)

    NEGATIVE assertion (HIGH-3 fix — no remaining `range(1, len(tins_raw))` literal at the active-aggregation sites):
    - `grep -cE 'range\(1, ?len\(tins_raw\)\)' src/wanctl/cake_signal.py` returns 0

    TOTAL-DROPS INVARIANT (HIGH-3 fix — total-aggregation sites at 251, 298, 308, 340 must continue to use `range(len(tins_raw))`):
    - `grep -cE 'range\(len\(tins_raw\)\)' src/wanctl/cake_signal.py` returns 4 (the four total-iteration sites; UNCHANGED count from baseline `grep -cE 'range\(len\(tins_raw\)\)' src/wanctl/cake_signal.py` at HEAD=b82abf0 which was 4)

    Q4 + LOW-8 (tin-name attribute):
    - `grep -c '_tin_names' src/wanctl/cake_signal.py` returns >= 3 (init line 183 + the heuristic comparison + at least one preserved reference). Note: the heuristic uses `self._tin_names` (with underscore) — verify by grep.
    - `grep -cE 'self\.tin_names\b' src/wanctl/cake_signal.py` returns 0 (no use of the wrong attribute name — LOW-8 closed)

    Behavior gates:
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorBestEffort tests/test_cake_signal.py::TestCakeSignalProcessorBestEffortStructuralOracle -v` — ALL 5 GREEN.
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v` — STILL GREEN (MEDIUM-4 byte-identity guard holds).
    - `.venv/bin/pytest tests/test_phase_193_replay.py tests/test_phase_194_replay.py tests/test_phase_195_replay.py -v` — ALL GREEN (replay invariance).
    - Hot-path slice green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exit code 0.

    SAFE-09 file-scoped diff (MEDIUM-7 fix — file-local, not cross-cutting):
    - `git diff 6508d68 -- src/wanctl/cake_signal.py | head -1` is non-empty (this file changed).
    - `git diff 6508d68 -- src/wanctl/cake_params.py | wc -l` returns 0 OR is the Plan 03 diff (Plan 02 does not touch this file; if Plan 03 ran first in parallel, its diff is unrelated to Plan 02's acceptance — file-scoped diff isolates Plan 02's contribution).
    - Value-invariance grep on this plan's file-scoped diff:
      `git diff 6508d68 -- src/wanctl/cake_signal.py | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'` returns empty (no threshold/EWMA literal change in this file's diff).
  </acceptance_criteria>
  <done>cake_signal.py tin-agnostic; diffserv4 byte-identical (snapshot guard holds); besteffort produces non-zero signal; replay tests pass; Q4/LOW-8 attribute use is correct; Plan 01 RED-BEHAVIOR cake_signal tests turn GREEN. HIGH-3 grep fix applied (positive + negative + total-invariant). MEDIUM-7 file-scoped diff applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel CAKE qdisc → wanctl daemon (netlink/subprocess readback) | Kernel-reported tin count is trusted (root-only path); aggregation logic must not crash on len==0 or len==1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-02-01 | Denial of Service (signal blindness) | cake_signal.py aggregation on besteffort layout | mitigate | `_active_tin_indices(1)` returns `range(0, 1)`. Verified by TestCakeSignalProcessorBestEffort RED→GREEN flip. |
| T-205-02-02 | Tampering (silent regression on ATT) | cake_signal.py aggregation order/expression change breaking diffserv4 EWMA chain | mitigate | TestCakeSignalProcessorDiffserv4ByteIdentity asserts literal numeric output; refactor changes ONLY iteration range. Phase 193/194/195 replay tests are the secondary gate. |
| T-205-02-03 | Tampering (mis-touched total-drops site) | Accidental edit of line 308 / 251 / 298 / 340 | mitigate | Acceptance criterion `grep -cE 'range\(len\(tins_raw\)\)' returns 4` enforces invariant; local-variable refactor cannot reduce the count without explicit edit. |
| T-205-02-04 | Repudiation | tin-name label drift in Prometheus metric | accept | Q4 heuristic only fires when `_tin_names == default 4-name list AND len(tins_raw) == 1`. Custom operator-supplied tin_names pass through unchanged. The mutation is observable in metrics and intentional. |
| T-205-02-05 | Information Disclosure | n/a | n/a | No new data exposure. |
| T-205-02-06 | Elevation of Privilege | n/a | n/a | No auth surface. |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_phase_193_replay.py tests/test_phase_194_replay.py tests/test_phase_195_replay.py -q` exits 0.
- `.venv/bin/ruff check src/wanctl/cake_signal.py` clean.
- `.venv/bin/mypy src/wanctl/cake_signal.py` clean (helper has explicit `-> range` return type).
- `git diff 6508d68 --name-only -- src/wanctl/cake_signal.py` is non-empty (this plan's contribution); cross-plan scope check is deferred to Plan 04.
</verification>

<success_criteria>
- ROADMAP success criterion #1 satisfied: aggregation iterates over actual tin set; ATT diffserv4 byte-identical (literal-snapshot guard + replay tests green).
- ROADMAP success criterion #2 satisfied: structural aggregation oracle test exercises 1-tin besteffort fixture and produces signal values consistent with diffserv4 oracle on matched load.
- All 5 RED-BEHAVIOR cake_signal tests from Plan 01 turn GREEN.
- TestCakeSignalProcessorDiffserv4ByteIdentity guard STILL GREEN (literal pin holds — Codex MEDIUM-4 closed).
- Phase 193/194/195 replay tests still GREEN.
- Codex HIGH-3 closed: positive (helper applied) + negative (no `range(1, len)` literal remaining) + total-invariant (`range(len(tins_raw))` count = 4) acceptance.
- Codex MEDIUM-7 closed: acceptance uses file-scoped `git diff 6508d68 -- src/wanctl/cake_signal.py` (no cross-plan race).
- Codex LOW-8 closed: `self._tin_names` (correct attribute) used; `self.tin_names` count = 0.
- SAFE-09 file-scoped diff for Plan 02: `cake_signal.py` only.
</success_criteria>

<output>
After completion, create `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-02-SUMMARY.md` documenting:
- `_active_tin_indices` helper (location + verbatim signature)
- 6 logical aggregation sites updated (line refs after edit)
- 4 total-aggregation sites confirmed UNCHANGED (line refs)
- Q4 tin-name heuristic applied; verified `_tin_names` attribute use
- Replay test results (193/194/195 byte-identical)
- TestCakeSignalProcessorDiffserv4ByteIdentity literal-snapshot guard PASSED
- File-scoped diff scope confirmation (cake_signal.py only)
- Hand-off to Plan 03 (wash gate), Plan 04 (cross-plan SAFE-09 boundary)
</output>
