---
phase: 205
plan: 04
type: execute
wave: 4
depends_on: [205-02, 205-03]
files_modified:
  - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md
  - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
autonomous: true
requirements:
  - TOPO-01
  - TOPO-02
must_haves:
  truths:
    - "Cross-plan SAFE-09 boundary: control-path source diff vs v1.43 close (6508d68) is bounded to the operator-approved 5-file set (or 6-file set if Plan 00 chose Option A; or divergence documented if Plan 00 chose REJECT)."
    - "No threshold / EWMA / dwell / deadband / burst / zone-classifier value changed in any of the 5 changed files."
    - "Full pytest suite green."
    - "Phase 193/194/195 replay tests byte-identical (diffserv4 invariance)."
    - "TestCakeSignalProcessorDiffserv4ByteIdentity literal-snapshot guard STILL GREEN (Codex MEDIUM-4 closed end-to-end)."
    - "Hot-path slice green."
    - "_DIFFSERV_NAME_TO_INT and _VALIDATE_KEY_TO_TCA explicitly UNTOUCHED (Pitfall 5 + MEDIUM-5 Option B deferrals to Phase 209 confirmed)."
    - "build_expected_readback at cake_params.py:176 UNTOUCHED (MEDIUM-5 Option B)."
  artifacts:
    - path: .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md
      provides: "Cross-plan SAFE-09 boundary verification evidence; full-suite + replay + byte-identity confirmation; Phase 209 carry-forward notes."
      contains: "SAFE-09"
    - path: .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
      provides: "Per-Task Verification Map populated with all task rows across 5 plans; wave_0_complete and nyquist_compliant flipped."
      contains: "wave_0_complete: true"
  key_links:
    - from: "Plan 02 + Plan 03 source edits"
      to: "ROADMAP success criterion #4 (SAFE-09 phase-boundary check)"
      via: "git diff 6508d68 -- src/wanctl/ scope assertion against operator-approved allowlist"
      pattern: "git diff"
---

<objective>
Phase 205 boundary closeout — pure verification. The operator-checkpoint task that previously lived here has moved upstream to Plan 00 (Codex HIGH-2 fix; ROADMAP amendment landed BEFORE source mutation). This plan is now strictly:

1. Cross-plan SAFE-09 boundary diff scope check (verifies Plans 02 + 03 produced exactly the operator-approved file set; this is the cross-plan check that file-scoped checks in Plans 02/03 cannot do).
2. Value-invariance grep across all changed files (confirms no SAFE-09 behavioral violation).
3. Full pytest suite + replay byte-identity gate (TestCakeSignalProcessorDiffserv4ByteIdentity is the hard MEDIUM-4 gate).
4. Hot-path slice + lint + type clean.
5. Confirm Pitfall 5 / Option B deferrals: `_DIFFSERV_NAME_TO_INT`, `_VALIDATE_KEY_TO_TCA`, `build_expected_readback` UNTOUCHED.
6. Write 205-04-SUMMARY.md with all evidence + Phase 209 carry-forward notes.
7. Finalize 205-VALIDATION.md Per-Task Verification Map (rows for all 5 plans; flip `wave_0_complete: true` and `nyquist_compliant: true`).

Codex MEDIUM-7 reminder: per-task acceptance criteria in Plans 02 + 03 already used file-scoped diffs (no cross-plan race within those plans). Plan 04 runs AFTER Plans 02 + 03 complete (wave 4, depends_on: [205-02, 205-03]) so cross-cutting `--name-only` is safe here.

Output: One SUMMARY.md + one finalized VALIDATION.md. ZERO source mutation in this plan. ZERO planning-artifact mutation outside the two named files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-01-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-02-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-plan SAFE-09 boundary diff scope audit + value-invariance grep + Pitfall-deferral confirmation</name>
  <files>(read-only audit; no source edits)</files>
  <read_first>
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md (operator decision branch — approve / option-a / amend / reject; determines expected file count: 5 / 5 (existing files w/ readback edits) / TBD / N/A respectively)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-02-SUMMARY.md
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-03-SUMMARY.md
  </read_first>
  <action>
    Step 1 — Determine expected file set from Plan 00 operator decision:
      - APPROVE (Option B, 5 files): expected = `cake_signal.py`, `cake_params.py`, `backends/linux_cake.py`, `backends/netlink_cake.py`, `check_config_validators.py`
      - OPTION-A (6 files conceptually but same 5 file paths since readback work happens inside cake_params.py + netlink_cake.py): expected = same 5 file paths; additionally verify `build_expected_readback` and `_VALIDATE_KEY_TO_TCA` were edited in those files.
      - AMEND: read the operator's wording from Plan 00 SUMMARY for the expected set.
      - REJECT: ROADMAP unchanged; documented divergence — actual diff (5 files) exceeds documented (2 files). SUMMARY surfaces this with a prominent warning block.

    Step 2 — Run cross-plan SAFE-09 boundary diff scope check:
      ```bash
      git diff 6508d68 --name-only -- src/wanctl/ | sort -u
      ```
      Compare against expected file set from Step 1. If actual differs from expected (extra files, missing files), STOP and report scope creep / scope shortfall — that's a SAFE-09 violation that the planner did not anticipate. Capture the actual output verbatim for SUMMARY.

    Step 3 — Value-invariance grep audit. For each changed file, grep the diff for any numeric literal change in threshold / EWMA / dwell / deadband / burst contexts:
      ```bash
      git diff 6508d68 -- src/wanctl/cake_signal.py src/wanctl/cake_params.py src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py src/wanctl/check_config_validators.py | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'
      ```
      Expected output: empty. If anything matches, inspect the context — only docstring/comment additions referencing these terms are allowed. Numeric literal changes are SAFE-09 (behavioral) violations.

    Step 4 — Confirm Pitfall 5 + Option B deferrals untouched:
      ```bash
      git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c '_DIFFSERV_NAME_TO_INT'
      git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c '_VALIDATE_KEY_TO_TCA'
      git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c 'TCA_CAKE_WASH'
      git diff 6508d68 -- src/wanctl/cake_params.py | grep -cE 'def build_expected_readback|^[+-].*build_expected_readback'
      ```
      Expected for APPROVE branch: all 4 return 0 (constants/structures untouched per Option B).
      Expected for OPTION-A branch: `_VALIDATE_KEY_TO_TCA` and `TCA_CAKE_WASH` may show >0 (readback added); `_DIFFSERV_NAME_TO_INT` MUST still be 0 (Pitfall 5 always deferred); `build_expected_readback` may show >0 in cake_params.py diff.
      Expected for REJECT branch: same as APPROVE; document divergence.

    Step 5 — Run the full pytest suite:
      ```bash
      .venv/bin/pytest tests/ -q --tb=short 2>&1 | tail -30
      ```
      Expected: all green; the new test count = baseline + ~14 new tests from Plan 01.

    Step 6 — Run replay byte-identity gate explicitly:
      ```bash
      .venv/bin/pytest tests/test_phase_193_replay.py tests/test_phase_194_replay.py tests/test_phase_195_replay.py -v 2>&1 | tail -20
      ```
      Expected: all GREEN (diffserv4 EWMA chain unchanged).

    Step 7 — Run TestCakeSignalProcessorDiffserv4ByteIdentity (the MEDIUM-4 hard gate):
      ```bash
      .venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v 2>&1 | tail -10
      ```
      Expected: GREEN. The literal-snapshot pin from Plan 01 Task 3 is the strongest byte-identity proof; Plan 02's helper extraction must preserve it bit-for-bit.

    Step 8 — Run hot-path slice (per CLAUDE.md):
      ```bash
      .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q 2>&1 | tail -10
      ```
      Expected: all GREEN.

    Step 9 — Run linters/type-checker on the 5 changed files:
      ```bash
      .venv/bin/ruff check src/wanctl/cake_signal.py src/wanctl/cake_params.py src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py src/wanctl/check_config_validators.py
      .venv/bin/mypy src/wanctl/cake_signal.py src/wanctl/cake_params.py src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py
      ```
      Expected: clean.

    Capture all outputs verbatim for Task 2 SUMMARY.
  </action>
  <verify>
    <automated>git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l | awk '{if ($1 == 5) print "SAFE-09-scope=5-OK"; else print "SAFE-09-scope-mismatch="$1}'</automated>
  </verify>
  <acceptance_criteria>
    - APPROVE/OPTION-A/AMEND branch: `git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l` returns 5 (or operator-approved count from Plan 00).
    - REJECT branch: `wc -l` returns 5 but Task 2 SUMMARY contains divergence warning block.
    - Value-invariance grep returns empty (no threshold/EWMA/dwell/deadband/burst numeric literal changes across all 5 files).
    - APPROVE branch: `git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c '_DIFFSERV_NAME_TO_INT'` returns 0.
    - APPROVE branch: `git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c '_VALIDATE_KEY_TO_TCA'` returns 0.
    - APPROVE branch: `git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -c 'TCA_CAKE_WASH'` returns 0.
    - APPROVE branch: `git diff 6508d68 -- src/wanctl/cake_params.py | grep -cE 'build_expected_readback'` returns 0 (function untouched per Option B).
    - `.venv/bin/pytest tests/ -q` exits 0; pass count delta = +~14 (Plan 01 new tests).
    - `.venv/bin/pytest tests/test_phase_193_replay.py tests/test_phase_194_replay.py tests/test_phase_195_replay.py -v` all GREEN.
    - `.venv/bin/pytest tests/test_cake_signal.py::TestCakeSignalProcessorDiffserv4ByteIdentity -v` GREEN (MEDIUM-4 end-to-end hard gate).
    - Hot-path slice exit 0.
    - Ruff + mypy clean on the 5 changed files.
  </acceptance_criteria>
  <done>SAFE-09 cross-plan boundary verified mechanically; value invariance confirmed; deferral pattern (Pitfall 5 + Option B) confirmed untouched; full suite + replay + byte-identity green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 205-04-SUMMARY.md + finalize 205-VALIDATION.md Per-Task Verification Map</name>
  <files>.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md, .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md</files>
  <read_first>
    - Task 1 captured outputs (diff scope, grep results, pytest counts, lint/type results)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md (operator decision branch)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-01-SUMMARY.md (RED/GREEN classification table; pinned literals)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-02-SUMMARY.md (cake_signal.py changes)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-03-SUMMARY.md (4-file TOPO-02 changes)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md (current Per-Task Verification Map)
  </read_first>
  <action>
    Step 1 — Write `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` with this structure:

      ```
      ---
      phase: 205
      plan: 04
      status: complete
      completed: 2026-05-14
      ---

      # Phase 205 Closeout — Plan 04 SUMMARY

      ## Outcome Banner

      PASS / FAIL — &lt;one-line verdict&gt;

      ## SAFE-09 Cross-Plan Boundary Diff Scope

      Operator decision (Plan 00): &lt;approve / option-a / amend / reject&gt;
      Expected file set: &lt;list&gt;
      Actual diff scope (verbatim `git diff 6508d68 --name-only -- src/wanctl/ | sort -u`):
      ```
      &lt;output&gt;
      ```
      Match: ✓ / ✗

      ## Value-Invariance Grep

      Command: `git diff 6508d68 -- &lt;5 files&gt; | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'`
      Output: &lt;empty / lines&gt;
      SAFE-09 (behavioral) verdict: ✓ no numeric literal change / ✗ violation

      ## Deferral Confirmation (Pitfall 5 + MEDIUM-5 Option B)

      | Item | Expected | Actual | Status |
      |------|----------|--------|--------|
      | _DIFFSERV_NAME_TO_INT in netlink_cake.py diff | 0 | &lt;n&gt; | ✓ deferred to Phase 209 |
      | _VALIDATE_KEY_TO_TCA in netlink_cake.py diff (Option B) | 0 | &lt;n&gt; | ✓ deferred to Phase 209 |
      | TCA_CAKE_WASH in netlink_cake.py diff (Option B) | 0 | &lt;n&gt; | ✓ deferred to Phase 209 |
      | build_expected_readback in cake_params.py diff (Option B) | 0 | &lt;n&gt; | ✓ deferred to Phase 209 |

      (For OPTION-A branch, expected values change accordingly.)

      ## Test Results

      | Suite | Result | Pass Count |
      |-------|--------|------------|
      | Full pytest (`.venv/bin/pytest tests/ -q`) | ✓ green | baseline + ~14 |
      | Phase 193/194/195 replay | ✓ green | (baseline) |
      | TestCakeSignalProcessorDiffserv4ByteIdentity (MEDIUM-4 hard gate) | ✓ green | 1 |
      | Hot-path slice | ✓ green | (baseline) |
      | Ruff on 5 changed files | ✓ clean | — |
      | Mypy on 4 .py files | ✓ clean | — |

      ## Codex Review Resolution Status (final)

      All 9 concerns from 205-REVIEWS.md addressed:

      - HIGH-1 (RED-test framing): Plan 01 split into RED-BEHAVIOR (Tasks 1+2) and GREEN-INVARIANT (Task 3 byte-identity + Task 4 audit). Each test color-tagged in acceptance.
      - HIGH-2 (SAFE-09 ordering): Plan 00 operator gate landed amendment BEFORE source mutation. Branch chosen: &lt;...&gt;.
      - HIGH-3 (broken grep): Plan 02 acceptance replaced with positive (`_active_tin_indices` exists + applied) + negative (no `range(1, len(tins_raw))` literal) + total-invariant (`range(len(tins_raw))` count = 4 — covers lines 251/298/308/340).
      - MEDIUM-4 (byte-identity not pinned): TestCakeSignalProcessorDiffserv4ByteIdentity asserts literal numeric output; STILL GREEN after Plan 02.
      - MEDIUM-5 (readback gap): Operator chose Option B in Plan 00 — readback in Phase 209. Confirmed untouched in this plan.
      - MEDIUM-6 (docsis fallback): test_initialize_cake_emits_wash_under_docsis_fallback (linux_cake) + test_initialize_cake_falls_back_to_subprocess_for_docsis (netlink_cake) GREEN.
      - MEDIUM-7 (wave-2 diff race): Plans 02 + 03 used file-scoped diffs. Plan 04 (this plan) safely runs cross-cutting check after both plans complete.
      - LOW-8 (tin-name attribute): `self._tin_names` (correct attribute, verified at cake_signal.py:183) used in Plan 02 Q4 heuristic. test_single_tin_name_label_is_besteffort GREEN.
      - LOW-9 (oracle naming): TestCakeSignalProcessorBestEffortStructuralOracle with honest docstring.

      ## Phase 209 Carry-Forward Notes

      Items deferred from Phase 205 that Phase 209 plan MUST address:

      1. `_DIFFSERV_NAME_TO_INT["besteffort"] = 2 → 3` in `src/wanctl/backends/netlink_cake.py:62-66` (RESEARCH §"Pitfall 5"). Without this, Phase 209 canary log will show `WARN CAKE param mismatch on spec-router: diffserv expected=2 actual=3` once Spectrum flips.
      2. `OPTIMAL_WASH` per-WAN auditor in `src/wanctl/check_cake.py:65-68` (RESEARCH §"Pitfall 6"). Spectrum DL with wash=yes will report "suboptimal" against the current `{"download": "no"}` expectation.
      3. (If Plan 00 chose APPROVE / Option B) Add `wash` to `build_expected_readback()` at `src/wanctl/cake_params.py:176` so live qdisc readback can validate the new state.
      4. (If Plan 00 chose APPROVE / Option B) Add `("wash", "TCA_CAKE_WASH")` to `_VALIDATE_KEY_TO_TCA` at `src/wanctl/backends/netlink_cake.py:69-78`.
      5. ATT remains untouched (SAFE-08 invariant); spectrum.yaml flips to `920Mbit besteffort wash` + `allow_wash: true`.

      ## Plan Map

      | Plan | Wave | Files | Status |
      |------|------|-------|--------|
      | 00 | 1 | ROADMAP + VALIDATION + REVIEWS | ✓ complete (operator gate) |
      | 01 | 2 | 5 test files | ✓ complete (10 RED-BEHAVIOR + 5 GREEN-INVARIANT) |
      | 02 | 3 | cake_signal.py | ✓ complete (TOPO-01) |
      | 03 | 3 | cake_params.py + 2 backends + check_config_validators.py | ✓ complete (TOPO-02) |
      | 04 | 4 | (this) | ✓ complete |

      ## Ready for /gsd-verify-work

      Phase 205 closeout complete. Milestone progresses to Phase 206 (A/B replay harness + rollback gates).
      ```

    Step 2 — Finalize `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md`:
      - Replace placeholder rows in Per-Task Verification Map with actual rows for every task across all 5 plans:
        - Plan 00: 205-00-01 (operator checkpoint), 205-00-02 (amendment apply)
        - Plan 01: 205-01-01 (RED besteffort tests), 205-01-02 (RED allow_wash + docsis tests), 205-01-03 (GREEN byte-identity guard), 205-01-04 (GREEN-invariant audit)
        - Plan 02: 205-02-01 (cake_signal.py helper + 6 sites + Q4)
        - Plan 03: 205-03-01 (cake_params gate), 205-03-02 (linux_cake wash emission), 205-03-03 (netlink_cake wash kwarg), 205-03-04 (validator allowlist)
        - Plan 04: 205-04-01 (SAFE-09 audit), 205-04-02 (SUMMARY + VALIDATION finalize)
      - Each row: Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status
      - Mark all rows ⬜ pending → ✅ passed (this plan runs after all task waves complete; status reflects completion).
      - In frontmatter, flip `wave_0_complete: false` → `wave_0_complete: true` and `nyquist_compliant: false` → `nyquist_compliant: true`.

    Do NOT touch any file outside the two named in &lt;files&gt;.
  </action>
  <verify>
    <automated>test -f .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md && grep -q "SAFE-09" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md && grep -q "wave_0_complete: true" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md && echo "ARTIFACTS-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` exists.
    - SUMMARY contains all required sections: Outcome Banner, SAFE-09 Cross-Plan Boundary Diff Scope, Value-Invariance Grep, Deferral Confirmation, Test Results, Codex Review Resolution Status, Phase 209 Carry-Forward Notes, Plan Map.
    - `grep -c "SAFE-09" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` returns >= 3.
    - `grep -c "Codex Review Resolution Status" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` returns 1.
    - `grep -cE "HIGH-[123]|MEDIUM-[4567]|LOW-[89]" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` returns >= 9 (all 9 Codex concerns referenced).
    - `grep -c "_DIFFSERV_NAME_TO_INT" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` returns >= 1 (Phase 209 carry-forward).
    - `grep -c "wave_0_complete: true" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md` returns 1.
    - `grep -c "nyquist_compliant: true" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md` returns 1.
    - Per-Task Verification Map row count: `grep -cE "\| 205-0[01234]-0[1234]" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md` returns >= 11 (2 + 4 + 1 + 4 + 2 = 13 rows minimum; depending on Plan 03 task split count).
    - `git diff HEAD -- src/wanctl/ | wc -l` returns 0 (NO source mutation in Plan 04 — sanity check).
  </acceptance_criteria>
  <done>Phase 205 closed; SUMMARY records all evidence; VALIDATION.md finalized with full per-task map; nyquist_compliant flipped; ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 02 + Plan 03 source edits → SAFE-09 closeout invariant | Phase 209's mechanical closeout depends on the SAFE-09 file allowlist matching reality. Plan 00 amended ROADMAP upstream; Plan 04 verifies actual diff matches operator-approved scope. |
| Cross-plan diff scope (cumulative) → ROADMAP wording | A file added by Plan 02 or Plan 03 outside the operator-approved set would be a SAFE-09 (mechanical) violation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-04-01 | Tampering (silent SAFE-09 violation) | Cross-plan git diff scope | mitigate | Task 1 Step 2 hard-asserts file count and listing against operator-approved set from Plan 00. Mismatch aborts and surfaces specific filename. |
| T-205-04-02 | Tampering (covert threshold change) | Numeric literal grep audit across 5 files | mitigate | Task 1 Step 3 greps for threshold/EWMA/dwell/deadband/burst keywords across full diff; expected empty. Catches accidental SAFE-09 (behavioral) violations even within allowed file set. |
| T-205-04-03 | Tampering (deferral pattern broken) | Pitfall 5 + Option B deferrals | mitigate | Task 1 Step 4 confirms `_DIFFSERV_NAME_TO_INT`, `_VALIDATE_KEY_TO_TCA`, `TCA_CAKE_WASH`, `build_expected_readback` untouched (or for OPTION-A: only readback structures touched, not Pitfall 5 constants). |
| T-205-04-04 | Tampering (byte-identity regression masked by replay test) | Plan 02 helper extraction silently changes diffserv4 numeric output | mitigate | Task 1 Step 7 runs TestCakeSignalProcessorDiffserv4ByteIdentity (literal-snapshot guard from Plan 01 Task 3); failure aborts Plan 04. |
| T-205-04-05 | Information Disclosure | n/a | n/a | No new data exposure; meta-verification plan. |
| T-205-04-06 | Denial of Service | n/a | n/a | Read-only audit + planning artifact updates. |
| T-205-04-07 | Elevation of Privilege | n/a | n/a | No auth surface change. |
</threat_model>

<verification>
- Task 1 acceptance criteria all met (operator-approved diff scope, empty value-invariance grep, deferral pattern intact, replay green, full suite green, byte-identity guard green).
- Task 2 SUMMARY + VALIDATION.md updated; wave_0_complete and nyquist_compliant flipped to true.
- `git diff HEAD -- src/wanctl/` empty (zero source mutation — hard invariant).
</verification>

<success_criteria>
- ROADMAP success criterion #4 satisfied: SAFE-09 phase-boundary check passes mechanically; documented diff scope (post-Plan-00 amendment) matches actual diff scope.
- All four ROADMAP success criteria for Phase 205 mapped and verified across Plans 00–04.
- All 9 Codex review concerns documented as resolved in 205-04-SUMMARY.md.
- Phase 205 ready for `/gsd-verify-work` and milestone progression to Phase 206.
- Phase 209 carry-forward list captured (Pitfall 5 fix, OPTIMAL_WASH update, optionally readback work if Option B was chosen).
</success_criteria>

<output>
After completion, `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md` exists per Task 2, and `205-VALIDATION.md` Per-Task Verification Map is fully populated with `wave_0_complete: true` + `nyquist_compliant: true`. No source files in `src/wanctl/` were touched.
</output>
