---
phase: 205
plan: 03
type: execute
wave: 3
depends_on: [205-01]
files_modified:
  - src/wanctl/cake_params.py
  - src/wanctl/backends/linux_cake.py
  - src/wanctl/backends/netlink_cake.py
  - src/wanctl/check_config_validators.py
autonomous: true
requirements:
  - TOPO-02
must_haves:
  truths:
    - "cake_params.allow_wash reads as False by default; absent → wash rejected (D-08 transparent-bridge protection)."
    - "allow_wash: false (explicit) AND wash: true together still raise ConfigValidationError."
    - "allow_wash: true permits wash through build_cake_params; wash flag flows to params dict."
    - "linux_cake.initialize_cake emits 'wash' token when params['wash'] is True; emits 'nowash' when params['wash'] is False (mirrors no-ack-filter pattern)."
    - "Spectrum-shaped {overhead_keyword: docsis, wash: True} reaches the LinuxCakeBackend fallback at netlink_cake.py:442 → super().initialize_cake() → subprocess emits wash token (Codex MEDIUM-6)."
    - "netlink_cake.initialize_cake passes wash=True/False kwarg to pyroute2 IPRoute.tc() on the non-docsis netlink path."
    - "check_config_validators.KNOWN_AUTORATE_PATHS includes cake_params.allow_wash AND cake_params.wash; daemon startup does NOT emit 'Unknown config key' WARN for either."
    - "EXCLUDED_PARAMS = {'nat', 'wash', 'autorate-ingress'} unchanged at the membership site (line 60); gate is at the check site (line 153)."
    - "nat and autorate-ingress remain unconditionally rejected even when allow_wash=true."
    - "READBACK validation explicitly OUT OF SCOPE for Phase 205 (Codex MEDIUM-5 Option B per operator approval in Plan 00). build_expected_readback() at cake_params.py:176 and _VALIDATE_KEY_TO_TCA at netlink_cake.py:69 do NOT learn 'wash' in this phase. Phase 209 will add wash readback alongside the actual config flip; until then, Phase 205 validates EMISSION ONLY (command construction)."
  artifacts:
    - path: src/wanctl/cake_params.py
      provides: "build_cake_params allow_wash gate; control flag stripped before override loop; conditional wash bypass at EXCLUDED_PARAMS check site (strict-bool `is True`)."
      contains: "allow_wash"
    - path: src/wanctl/backends/linux_cake.py
      provides: "wash/nowash token emission in initialize_cake boolean-flag loop (mirrors ack-filter/no-ack-filter)."
      contains: '"wash"'
    - path: src/wanctl/backends/netlink_cake.py
      provides: "(wash, wash) tuple in initialize_cake pyroute2 kwarg mapping."
      contains: '("wash", "wash")'
    - path: src/wanctl/check_config_validators.py
      provides: "KNOWN_AUTORATE_PATHS includes cake_params.allow_wash and cake_params.wash."
      contains: "cake_params.allow_wash"
  key_links:
    - from: "src/wanctl/cake_params.py::build_cake_params"
      to: "src/wanctl/backends/linux_cake.py::initialize_cake (subprocess path; Spectrum-critical via docsis fallback)"
      via: "params dict {wash: True}"
      pattern: "wash"
    - from: "src/wanctl/cake_params.py::build_cake_params"
      to: "src/wanctl/backends/netlink_cake.py::initialize_cake (non-docsis netlink path)"
      via: "params dict {wash: True}"
      pattern: "wash"
    - from: "src/wanctl/backends/netlink_cake.py:442 docsis fallback"
      to: "src/wanctl/backends/linux_cake.py::initialize_cake subprocess wash emission"
      via: "super().initialize_cake(params) when overhead_keyword == 'docsis'"
      pattern: "super\\(\\)\\.initialize_cake"
    - from: "configs/spectrum.yaml (Phase 209)"
      to: "src/wanctl/check_config_validators.py::KNOWN_AUTORATE_PATHS"
      via: "cake_params.allow_wash key registered"
      pattern: "cake_params.allow_wash"
---

<objective>
TOPO-02 — End-to-end allow_wash gate (EMISSION ONLY; readback deferred to Phase 209 per Codex MEDIUM-5 Option B). Per-WAN YAML flag flows through `build_cake_params` → `initialize_cake` (both subprocess and netlink backends) → tc qdisc args. Validator allowlist updated so daemon startup does not WARN.

Purpose: Phase 209 needs `wash: true` permitted for Spectrum (carrier strips DSCP — wash is topology-correct). D-08 transparent-bridge protection still applies to ATT and any other deployment that doesn't opt in. RESEARCH §"Pitfall 1: Half-wired wash gate" requires the gate to land in `cake_params.py` + both backends + validator allowlist in the same wave or it's silently broken in production.

The 5-file SAFE-09 scope expansion was approved by the operator in Plan 00 (Codex HIGH-2 fix; ROADMAP amended at three sites BEFORE source mutation). This plan implements the 4 source-side edits; Plan 02 owns cake_signal.py (the 5th file).

Codex MEDIUM-5 Option B (operator-recommended): Phase 205 validates EMISSION ONLY (command construction); LIVE qdisc readback validation lands in Phase 209 alongside the actual config flip. `build_expected_readback()` at cake_params.py:176 and `_VALIDATE_KEY_TO_TCA` at netlink_cake.py:69 are NOT extended in this plan. The Phase 209 plan will add wash to both readback structures. This narrows the SAFE-09 scope and keeps Phase 205 focused on the controller-input side; readback only matters once Spectrum is actually using wash.

Codex MEDIUM-6 fix: Spectrum config uses `overhead: "docsis"` → triggers `NetlinkCakeBackend.initialize_cake()` → fall-through to `LinuxCakeBackend.initialize_cake()` at netlink_cake.py:442-447. The linux_cake.py subprocess wash emission is the production-critical path for Spectrum even though the deployment uses the netlink backend. Plan 01 Task 2 added the docsis-fallback RED tests; this plan turns them GREEN by editing the linux_cake.py boolean-flag loop.

Codex MEDIUM-7 fix: file-scoped diff acceptance (per-plan-base-commit + per-file) instead of cross-cutting `--name-only`. Plan 02 + Plan 03 can run in parallel without racing each other's diff-scope assertions.

Output: 4 files modified. All RED-BEHAVIOR allow_wash / wash emission / docsis-fallback / validator-allowlist tests turn GREEN. All GREEN-INVARIANT guards (test_wash_excluded, allow_wash_does_not_permit_*, allow_wash_false_rejects_wash, allow_wash_absent_rejects_wash) STILL pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-01-SUMMARY.md
@CLAUDE.md
@src/wanctl/cake_params.py
@src/wanctl/backends/linux_cake.py
@src/wanctl/backends/netlink_cake.py
@src/wanctl/check_config_validators.py

<interfaces>
<!-- Existing call shapes — verified at HEAD=b82abf0. -->

cake_params.py — current EXCLUDED_PARAMS membership (line 60) and override loop (lines 150-157):
```python
# line 60
EXCLUDED_PARAMS: set[str] = {"nat", "wash", "autorate-ingress"}

# lines 150-157
if cake_config:
    for key, value in cake_config.items():
        tc_key = YAML_TO_TC_KEY.get(key, key)
        if tc_key in EXCLUDED_PARAMS:
            raise ConfigValidationError(
                f"Excluded CAKE parameter: {key!r} -- not valid for transparent bridge topology"
            )
        params[tc_key] = value
```

cake_params.py:176 — build_expected_readback() — DO NOT TOUCH (Option B: readback in Phase 209).

linux_cake.py — current boolean-flag emission (lines 396-400):
```python
for flag in ("split-gso", "ack-filter", "ingress"):
    if params.get(flag):
        cmd_args.append(flag)
    elif flag == "ack-filter" and flag in params:
        cmd_args.append("no-ack-filter")
```

netlink_cake.py:69-78 — _VALIDATE_KEY_TO_TCA — DO NOT TOUCH (Option B: readback in Phase 209).

netlink_cake.py:442-447 — docsis fallback (verified):
```python
if "overhead_keyword" in params:
    # pyroute2 doesn't correctly handle overhead keywords like "docsis" —
    return super().initialize_cake(params)
```

netlink_cake.py:478-484 — current pyroute2 kwarg mapping:
```python
for tc_flag, pyroute2_kwarg in [
    ("split-gso", "split_gso"),
    ("ack-filter", "ack_filter"),
    ("ingress", "ingress"),
]:
    if tc_flag in params:
        kwargs[pyroute2_kwarg] = bool(params[tc_flag])
```

netlink_cake.py:62-66 — _DIFFSERV_NAME_TO_INT — DO NOT TOUCH (Pitfall 5 / Q3 deferred to Phase 209).

check_config_validators.py — KNOWN_AUTORATE_PATHS cake_params block (lines 154-163). Insertion: add `"cake_params.wash"` and `"cake_params.allow_wash"`.

pyroute2 wash kwarg verified by RESEARCH introspection 2026-05-13: `pyroute2.netlink.rtnl.tcmsg.sched_cake.options.nla_map` contains `TCA_CAKE_WASH`; `IPRoute.tc("replace", kind="cake", wash=True)` is supported.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cake_params.py allow_wash gate (strict-bool, control flag stripped before loop)</name>
  <files>src/wanctl/cake_params.py</files>
  <read_first>
    - src/wanctl/cake_params.py (full file — EXCLUDED_PARAMS line 60; override loop 150-157; build_expected_readback at 176 — last is DO-NOT-TOUCH)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"src/wanctl/cake_params.py"; §"Shared Patterns S4: Strict-bool guard")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pattern 2: Conditional excluded-params"; §"Anti-Patterns: Removing wash from EXCLUDED_PARAMS")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (MEDIUM-5 Option B: readback OUT OF SCOPE)
  </read_first>
  <behavior>
    - GREEN: tests/test_cake_params.py::TestBuildCakeParamsAllowWash::test_allow_wash_true_permits_wash (RED-BEHAVIOR → GREEN)
    - STILL GREEN: TestBuildCakeParamsAllowWash::{test_allow_wash_false_rejects_wash, test_allow_wash_absent_rejects_wash, test_allow_wash_does_not_permit_nat, test_allow_wash_does_not_permit_autorate_ingress} (GREEN-INVARIANT)
    - STILL GREEN: TestBuildCakeParamsExcluded::{test_wash_excluded, test_nat_excluded, test_autorate_ingress_excluded}
  </behavior>
  <action>
    Edit `build_cake_params` (function around lines 100-160). Apply this exact transformation:

    1. KEEP `EXCLUDED_PARAMS = {"nat", "wash", "autorate-ingress"}` at line 60 unchanged.

    2. INSIDE `build_cake_params`, BEFORE the existing `if cake_config:` override loop, add the strict-bool extraction:
       ```python
       # Phase 205 (TOPO-02): per-WAN allow_wash gate. Strict-bool guard
       # `is True` rejects strings outright (`bool("false") == True` is an
       # operator typo trap); symmetric with cake_signal.py's
       # isinstance(v, bool) precedent.
       allow_wash = (
           cake_config.get("allow_wash") is True if cake_config else False
       )
       ```

    3. REPLACE the override loop (lines 150-157) with:
       ```python
       if cake_config:
           for key, value in cake_config.items():
               if key == "allow_wash":
                   continue  # control flag, consumed above; not a tc param
               tc_key = YAML_TO_TC_KEY.get(key, key)
               if tc_key in EXCLUDED_PARAMS:
                   # D-08 transparent-bridge protection still rejects nat and
                   # autorate-ingress unconditionally; wash is the only param
                   # the operator may opt into via the per-WAN allow_wash gate.
                   if tc_key == "wash" and allow_wash:
                       pass  # fall through to assignment
                   else:
                       raise ConfigValidationError(
                           f"Excluded CAKE parameter: {key!r} -- not valid for "
                           f"transparent bridge topology"
                       )
               params[tc_key] = value
       ```

    4. DO NOT touch `build_expected_readback()` at line 176 — Codex MEDIUM-5 Option B defers readback to Phase 209.

    Strict-bool decision: use `is True`, NOT `bool(...)`. Documented in PATTERNS §S4 + RESEARCH §"Security Domain V5".
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_cake_params.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "allow_wash" src/wanctl/cake_params.py` returns >= 3 (extraction + continue + check-site condition)
    - `grep -cE 'EXCLUDED_PARAMS.*=.*\{' src/wanctl/cake_params.py` returns 1 (membership unchanged at line 60)
    - `grep -cE '\.get\("allow_wash"\) is True' src/wanctl/cake_params.py` returns 1 (strict-bool form, NOT `bool(...)`)
    - `grep -c "build_expected_readback" src/wanctl/cake_params.py` returns the same count as baseline (`grep -c build_expected_readback src/wanctl/cake_params.py` at HEAD=b82abf0 — function untouched per Option B). Baseline expected: 1.
    - `.venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsAllowWash -v` — all 5 tests GREEN (1 RED→GREEN flip + 4 GREEN-INVARIANT guards still GREEN).
    - `.venv/bin/pytest tests/test_cake_params.py::TestBuildCakeParamsExcluded -v` — all 3 tests still GREEN.
    - `.venv/bin/pytest tests/test_cake_params.py -v` — full file exit code 0.
  </acceptance_criteria>
  <done>cake_params.py allow_wash gate complete; default-false preserved; nat/autorate-ingress unconditionally excluded; build_expected_readback untouched (Option B).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: linux_cake.py wash/nowash subprocess emission (Spectrum-critical via docsis fallback)</name>
  <files>src/wanctl/backends/linux_cake.py</files>
  <read_first>
    - src/wanctl/backends/linux_cake.py (lines 363-410 — initialize_cake docstring + boolean-flag loop)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"src/wanctl/backends/linux_cake.py")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pitfall 1: Half-wired wash gate"; §"Open Questions Q2: nowash emission")
    - src/wanctl/backends/netlink_cake.py:442-447 (docsis fallback — confirms why linux_cake.py is Spectrum-critical)
  </read_first>
  <behavior>
    - GREEN: tests/backends/test_linux_cake.py::test_initialize_cake_emits_wash
    - GREEN: tests/backends/test_linux_cake.py::test_initialize_cake_emits_nowash_on_explicit_false
    - GREEN: tests/backends/test_linux_cake.py::test_initialize_cake_emits_wash_under_docsis_fallback (Codex MEDIUM-6)
    - STILL GREEN: existing test_initialize_cake_boolean_flags
  </behavior>
  <action>
    EDIT `src/wanctl/backends/linux_cake.py` boolean-flag loop (lines 396-400). Replace with:
    ```python
    for flag in ("split-gso", "ack-filter", "ingress", "wash"):
        if params.get(flag):
            cmd_args.append(flag)
        elif flag == "ack-filter" and flag in params:
            cmd_args.append("no-ack-filter")
        elif flag == "wash" and flag in params:
            cmd_args.append("nowash")
    ```
    Token: `nowash` (one word, no hyphen) — per `tc-cake(8)` and `check_cake.py:55-68`. RESEARCH Q2 recommends symmetric explicit-false emission for operator audit trail.

    Update the docstring at lines ~363-364 to add `"wash"` to the listed boolean flags.

    DO NOT touch any other function in this file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/backends/test_linux_cake.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"split-gso", "ack-filter", "ingress", "wash"' src/wanctl/backends/linux_cake.py` returns 1
    - `grep -c '"nowash"' src/wanctl/backends/linux_cake.py` returns 1 (only in the new elif branch)
    - `.venv/bin/pytest tests/backends/test_linux_cake.py -k "wash or boolean_flags" -v` — all 4 tests GREEN (3 new RED→GREEN + 1 pre-existing boolean_flags).
    - `.venv/bin/pytest tests/backends/test_linux_cake.py::test_initialize_cake_emits_wash_under_docsis_fallback -v` — GREEN (MEDIUM-6 closed for the linux subprocess path).
    - File-scoped diff (MEDIUM-7): value-invariance grep on this plan's diff for this file:
      `git diff 6508d68 -- src/wanctl/backends/linux_cake.py | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'` returns empty.
  </acceptance_criteria>
  <done>linux_cake.py wash/nowash subprocess emission complete; docsis fallback path emits wash for Spectrum; symmetric explicit-false (nowash) emission. MEDIUM-6 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: netlink_cake.py wash kwarg mapping + docsis-fallback test passes (no _VALIDATE_KEY_TO_TCA edit per Option B)</name>
  <files>src/wanctl/backends/netlink_cake.py</files>
  <read_first>
    - src/wanctl/backends/netlink_cake.py (lines 416-490 — initialize_cake docstring + pyroute2 kwarg mapping at 478-484; AND lines 62-66 _DIFFSERV_NAME_TO_INT (DO-NOT-TOUCH); AND lines 69-78 _VALIDATE_KEY_TO_TCA (DO-NOT-TOUCH per Option B); AND lines 442-447 docsis fallback)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"src/wanctl/backends/netlink_cake.py")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pitfall 5: _DIFFSERV_NAME_TO_INT besteffort=2 bug — DEFERRED to Phase 209")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (MEDIUM-5 Option B; MEDIUM-6 docsis)
  </read_first>
  <behavior>
    - GREEN: tests/backends/test_netlink_cake.py::test_initialize_cake_passes_wash_kwarg
    - GREEN: tests/backends/test_netlink_cake.py::test_initialize_cake_passes_wash_false_kwarg
    - GREEN: tests/backends/test_netlink_cake.py::test_initialize_cake_falls_back_to_subprocess_for_docsis (turns GREEN due to Task 2's linux_cake.py wash emission; this task does not need to edit netlink_cake.py for the docsis fallback because the fallback path delegates to LinuxCakeBackend)
    - STILL GREEN: existing test_initialize_cake_maps_boolean_flags / _maps_false_boolean_flags
  </behavior>
  <action>
    EDIT `src/wanctl/backends/netlink_cake.py` pyroute2 kwarg mapping (lines 478-484). Replace with:
    ```python
    for tc_flag, pyroute2_kwarg in [
        ("split-gso", "split_gso"),
        ("ack-filter", "ack_filter"),
        ("ingress", "ingress"),
        ("wash", "wash"),
    ]:
        if tc_flag in params:
            kwargs[pyroute2_kwarg] = bool(params[tc_flag])
    ```

    Update the docstring around lines 416-426 to add `wash → wash=True/False` to the kwarg map list.

    DO NOT touch:
      - `_DIFFSERV_NAME_TO_INT` at lines 62-66 (Pitfall 5 / Q3 deferred to Phase 209). Touching it inflates SAFE-09 diff scope and the besteffort=2 → 3 bug doesn't fire until Spectrum actually flips to besteffort.
      - `_VALIDATE_KEY_TO_TCA` at lines 69-78 (Codex MEDIUM-5 Option B — readback validation deferred to Phase 209).
      - The docsis fallback block at lines 442-447 (no change needed — the fallback already delegates to LinuxCakeBackend, which Task 2 taught about wash).
      - Any other function in this file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/backends/test_netlink_cake.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\("wash", "wash"\)' src/wanctl/backends/netlink_cake.py` returns 1
    - `grep -c '_DIFFSERV_NAME_TO_INT' src/wanctl/backends/netlink_cake.py` returns the same baseline count (untouched — should be exactly 2 occurrences: definition at line 62 + lookup site elsewhere; Phase 5 / Q3 deferred)
    - `grep -c '_VALIDATE_KEY_TO_TCA' src/wanctl/backends/netlink_cake.py` returns the same baseline count (untouched per Option B — should be exactly 2: definition at line 69 + lookup site at line 530)
    - `grep -cE 'TCA_CAKE_WASH' src/wanctl/backends/netlink_cake.py` returns 0 (NOT added per Option B — readback structures untouched)
    - `.venv/bin/pytest tests/backends/test_netlink_cake.py -k "wash or boolean_flags or falls_back_to_subprocess_for_docsis" -v` — all 5 tests GREEN (3 new RED→GREEN + 2 pre-existing).
    - `.venv/bin/pytest tests/backends/ -v --tb=no -q` — full backend suite exit code 0.
    - File-scoped diff (MEDIUM-7): value-invariance grep on this plan's diff for this file:
      `git diff 6508d68 -- src/wanctl/backends/netlink_cake.py | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'` returns empty.
  </acceptance_criteria>
  <done>netlink_cake.py wash kwarg mapping complete; docsis fallback path passes via Task 2's LinuxCakeBackend wash emission; _DIFFSERV_NAME_TO_INT and _VALIDATE_KEY_TO_TCA explicitly UNTOUCHED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: check_config_validators.py KNOWN_AUTORATE_PATHS allowlist (cake_params.allow_wash + cake_params.wash)</name>
  <files>src/wanctl/check_config_validators.py</files>
  <read_first>
    - src/wanctl/check_config_validators.py (lines 140-200 — KNOWN_AUTORATE_PATHS cake_params block at 154-163)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md (§"src/wanctl/check_config_validators.py")
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md (§"Pitfall 4: Validator unknown-key WARN spam")
  </read_first>
  <behavior>
    - GREEN: tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_allow_wash_no_unknown_key_warning
    - STILL GREEN: tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_no_unknown_key_warnings
  </behavior>
  <action>
    Locate `KNOWN_AUTORATE_PATHS` set/frozenset definition. The cake_params block is around lines 154-163. Add two entries (in the existing alphabetical or insertion order — either works; pick whatever produces the smallest diff):
    ```python
    "cake_params.wash",         # Phase 205 (TOPO-02) -- tc flag, gated by allow_wash
    "cake_params.allow_wash",   # Phase 205 (TOPO-02) -- per-WAN gate flag
    ```
    Insert BOTH (per PATTERNS — `cake_params.wash` is the actual tc flag that would also WARN if added bare to YAML).

    Preserve all other entries in `KNOWN_AUTORATE_PATHS` unchanged. Surgical insert — no other edits in this 36k-line file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_check_config.py::TestLinuxCakeValidation -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"cake_params.allow_wash"' src/wanctl/check_config_validators.py` returns 1
    - `grep -c '"cake_params.wash"' src/wanctl/check_config_validators.py` returns 1
    - `grep -c '"cake_params.ack_filter"' src/wanctl/check_config_validators.py` returns 1 (regression guard — pre-existing entry must remain)
    - `.venv/bin/pytest tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_allow_wash_no_unknown_key_warning -v` — GREEN.
    - `.venv/bin/pytest tests/test_check_config.py::TestLinuxCakeValidation::test_cake_params_no_unknown_key_warnings -v` — STILL GREEN.
    - `.venv/bin/pytest tests/test_check_config.py -v --tb=no -q` — full file exit code 0.
    - File-scoped diff (MEDIUM-7): value-invariance grep on this plan's diff for this file:
      `git diff 6508d68 -- src/wanctl/check_config_validators.py | grep -E '^[+-]' | grep -viE '^(\+\+\+|---)' | grep -iE 'threshold|ewma|dwell|deadband|burst|time_constant|alpha|beta'` returns empty.
  </acceptance_criteria>
  <done>Allowlist includes cake_params.allow_wash and cake_params.wash; daemon startup will not WARN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config (operator-authored) → wanctl daemon | Operator-controlled values cross into runtime; `bool("false") == True` typo trap mitigated via strict-`is True` parse. |
| wanctl daemon → kernel CAKE qdisc (subprocess tc / pyroute2 netlink) | wash flag now flows through; emission tokens are literals (`"wash"` / `"nowash"`) — not user-controlled, no injection risk. |
| Spectrum docsis fallback path (netlink_cake.py:442 → LinuxCakeBackend.initialize_cake) | Production-critical for Spectrum; without Task 2's linux_cake.py wash emission, the docsis-shaped path silently drops wash. |
| wanctl daemon startup → operator alerting | Unknown-key WARN noise erodes operator trust. Allowlist additions prevent benign WARN spam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-03-01 | Tampering (operator typo) | cake_params.allow_wash YAML parse | mitigate | Strict-bool guard `cake_config.get("allow_wash") is True`; rejects string `"true"`, `"false"`, `1`, `0`. Symmetric with cake_signal.py isinstance(v, bool) precedent. |
| T-205-03-02 | Tampering (silent gate bypass) | D-08 transparent-bridge protection | mitigate | EXCLUDED_PARAMS membership unchanged at line 60; only check-site behavior changes. Plan 01 GREEN-INVARIANT guards (test_allow_wash_does_not_permit_nat / _autorate_ingress + test_wash_excluded) enforce. |
| T-205-03-03 | Tampering (half-wired gate) | cake_params.py permits wash but backends drop it silently | mitigate | All 4 source sites edited in this plan (Tasks 1-4) + Plan 01 RED-BEHAVIOR docsis-fallback test confirms Spectrum-shaped path emits wash. |
| T-205-03-04 | Information Disclosure | n/a | n/a | wash is qdisc behavior, not data export. |
| T-205-03-05 | Denial of Service | tc subprocess / pyroute2 invocation | accept | wash kwarg verified supported by pyroute2 (RESEARCH 2026-05-13); `tc-cake(8)` accepts wash/nowash. Failure mode: kernel rejects qdisc replace, daemon logs error, existing health check surfaces it. |
| T-205-03-06 | Elevation of Privilege | n/a | n/a | No auth surface change. |
| T-205-03-07 | Repudiation (audit trail) | linux_cake nowash emission on explicit false | accept | Symmetric emission makes operator-explicit-false visible in qdisc args (`tc qdisc show` outputs `nowash`). |
| T-205-03-08 | Tampering (readback validation gap) | wash readback NOT in build_expected_readback or _VALIDATE_KEY_TO_TCA | accept | Codex MEDIUM-5 Option B operator-approved deferral. Phase 205 validates EMISSION ONLY; live qdisc readback validation lands in Phase 209 alongside actual config flip. Risk: between Phase 205 ship and Phase 209 ship, no live readback for wash — but Spectrum is not USING wash until Phase 209, so the gap is moot. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_cake_params.py tests/backends/test_linux_cake.py tests/backends/test_netlink_cake.py tests/test_check_config.py -v --tb=no -q` exits 0.
- `.venv/bin/ruff check src/wanctl/cake_params.py src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py src/wanctl/check_config_validators.py` clean.
- `.venv/bin/mypy src/wanctl/cake_params.py src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py` clean.
- Hot-path slice: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0.
- File-scoped diff scope (MEDIUM-7) — each task's acceptance verifies its own file's diff is clean of threshold/EWMA literal change. Cross-plan boundary verification is deferred to Plan 04.
</verification>

<success_criteria>
- ROADMAP success criterion #3 satisfied: cake_params.allow_wash default-false; absent → wash excluded; false → wash excluded; true → wash permitted; nat / autorate-ingress still unconditionally excluded.
- Half-wired-gate pitfall (RESEARCH §"Pitfall 1") avoided — all 4 source sites edited in this plan.
- Spectrum docsis fallback path emits wash (Codex MEDIUM-6 closed via Task 2's linux_cake.py edit).
- Validator allowlist updated; no daemon startup WARN.
- All 7 RED-BEHAVIOR tests for allow_wash / wash emission / docsis-fallback / validator-allowlist turn GREEN.
- All GREEN-INVARIANT guards (test_wash_excluded + 4 in TestBuildCakeParamsAllowWash) STILL GREEN.
- Codex MEDIUM-5 Option B applied: build_expected_readback + _VALIDATE_KEY_TO_TCA EXPLICITLY untouched (operator-approved in Plan 00; Phase 209 owns readback).
- Codex MEDIUM-6 closed: docsis-fallback path tests GREEN (Tasks 2 + 3).
- Codex MEDIUM-7 closed: each task's acceptance uses file-scoped `git diff 6508d68 -- &lt;file&gt;` (no cross-plan race).
- _DIFFSERV_NAME_TO_INT besteffort=2 bug NOT touched (deferred to Phase 209 per Pitfall 5).
</success_criteria>

<output>
After completion, create `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-03-SUMMARY.md` documenting:
- 4 files modified with line refs after edit
- Strict-bool decision (chose `is True` over `bool(...)`) — rationale
- Symmetric `nowash` emission decision (Q2 — chose to emit)
- _DIFFSERV_NAME_TO_INT explicitly NOT touched (Q3 / Pitfall 5 deferred to Phase 209)
- _VALIDATE_KEY_TO_TCA explicitly NOT touched (MEDIUM-5 Option B — readback in Phase 209)
- build_expected_readback explicitly NOT touched (MEDIUM-5 Option B)
- Spectrum docsis fallback path verified via test_initialize_cake_falls_back_to_subprocess_for_docsis GREEN
- Cross-phase carry-forward to Phase 209 RETRO:
  - Add wash to build_expected_readback() at cake_params.py:176
  - Add ("wash", "TCA_CAKE_WASH") to _VALIDATE_KEY_TO_TCA at netlink_cake.py:69
  - Fix `_DIFFSERV_NAME_TO_INT["besteffort"] = 2 → 3` (Pitfall 5)
  - OPTIMAL_WASH per-WAN auditor in check_cake.py (Pitfall 6)
- Hand-off to Plan 04 (cross-plan SAFE-09 boundary verification)
</output>
