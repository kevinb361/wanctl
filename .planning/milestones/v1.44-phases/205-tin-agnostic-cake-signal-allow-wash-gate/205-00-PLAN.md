---
phase: 205
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
  - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
autonomous: false
requirements:
  - TOPO-01
  - TOPO-02
must_haves:
  truths:
    - "Operator has explicitly approved (or amended/rejected) the SAFE-09 file allowlist expansion BEFORE any source mutation lands."
    - "ROADMAP.md SAFE-09 (behavioral) wording reflects the approved allowlist (or divergence is documented)."
    - "ROADMAP.md Phase 205 Success Criterion #4 wording matches the approved allowlist."
    - "ROADMAP.md Phase 209 Success Criterion #4 wording matches the approved allowlist."
    - "ROADMAP.md Progress table shows Phase 205 = 0/5 plans (was 0/0)."
    - "VALIDATION.md SAFE-09 boundary verifier line reflects the approved file count."
    - "REVIEWS.md status note appended recording how Codex's HIGH-2 (SAFE-09 ordering) was resolved."
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Amended SAFE-09 wording at three sites: Closeout Invariants, Phase 205 SC#4, Phase 209 SC#4. Progress table updated to 0/5 for Phase 205."
      contains: "backends/linux_cake.py"
    - path: .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
      provides: "SAFE-09 boundary verifier line lists the approved file set; Per-Task Verification Map seeded with Plan 00 row."
      contains: "Plan 00"
    - path: .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
      provides: "Resolution status appended for Codex HIGH-2 (SAFE-09 ordering)."
      contains: "HIGH-2 resolved"
  key_links:
    - from: ".planning/ROADMAP.md SAFE-09 (behavioral)"
      to: "Plan 02 + Plan 03 source diff scope"
      via: "operator-approved file allowlist"
      pattern: "backends/linux_cake.py"
    - from: ".planning/phases/205-.../205-VALIDATION.md SAFE-09 boundary verifier"
      to: ".planning/ROADMAP.md SAFE-09 (behavioral)"
      via: "matching file count + path list"
      pattern: "wc -l"
---

<objective>
Pure planning-artifact plan — NO source mutation. Surfaces the SAFE-09 file allowlist expansion (TOPO-02 actually touches 5 files, not the 2 the ROADMAP currently names) for operator approval BEFORE any of Plans 01–04 lands a single source edit. Resolves Codex review HIGH-2 (SAFE-09 ROADMAP amendment ordering inverted in v1 plan-set).

Purpose: The original Plan 04 surfaced this amendment AFTER Plans 02 + 03 had already expanded the source diff to 5 files. That's after-the-fact reconciliation, not pre-approval. Per Codex review HIGH-2, the operator must approve (or reject) the allowlist expansion before the expansion happens, so any divergence aborts at the planning layer rather than after the source edits land.

Output: ROADMAP.md amended (or divergence documented) at three SAFE-09 wording sites; Progress table updated; VALIDATION.md SAFE-09 verifier line reflects the approved scope; REVIEWS.md status note records the resolution. Source tree (`src/wanctl/`) is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md
@CLAUDE.md

<interfaces>
<!-- Three ROADMAP wording sites that need amendment. Located by grep at b82abf0; line numbers may shift slightly with future doc edits. -->

Site 1 — Closeout Invariants / SAFE-09 (behavioral) (currently around ROADMAP.md line 23):
> **SAFE-09 (behavioral):** No controller threshold / algorithm / EWMA / dwell / deadband / burst changes. Control-path source diff bounded to TOPO-01 (`cake_signal.py`), TOPO-02 (`cake_params.py` allow_wash gate), TOOL-03 (`operator_summary.py` perm handling), and the `__init__.py` version bump. Verified at every phase boundary; mechanical closeout in Phase 209.

Site 2 — Phase 205 Success Criteria #4 (currently around ROADMAP.md line 49):
> 4. SAFE-09 phase-boundary check: control-path source diff vs v1.43 close (`6508d68`) is bounded to `cake_signal.py` (TOPO-01) and `cake_params.py` (TOPO-02); no threshold/EWMA/dwell/deadband/burst values changed.

Site 3 — Phase 209 Success Criteria #4 (currently around ROADMAP.md line 107):
> 4. SAFE-09 closeout: end-to-end control-path source diff between v1.43 close (`6508d68`) and v1.44 close is bounded to `cake_signal.py` (TOPO-01), `cake_params.py` (TOPO-02), `operator_summary.py` (TOOL-03), and `src/wanctl/__init__.py` (version bump). HRDN-01's fail-closed verifier confirms mechanically.

Proposed amended wording (per RESEARCH §"Pitfall 1: Half-wired wash gate" + PATTERNS §"Shared Pattern S1: Gate-before-emit symmetry") — adds three files to TOPO-02:
- `backends/linux_cake.py` (wash/nowash subprocess token emission)
- `backends/netlink_cake.py` (pyroute2 wash kwarg mapping)
- `check_config_validators.py` (KNOWN_AUTORATE_PATHS allowlist)

The Spectrum `overhead: "docsis"` configuration causes `NetlinkCakeBackend.initialize_cake()` to fall back to `LinuxCakeBackend.initialize_cake()` at netlink_cake.py:442-447 (verified) — so the linux_cake.py subprocess wash emission is the production-critical path for Spectrum even though the deployment uses the netlink backend.

Phase 205 validates EMISSION ONLY (command construction); LIVE qdisc readback validation (`build_expected_readback()` at cake_params.py:176 + `_VALIDATE_KEY_TO_TCA` at netlink_cake.py:69) does NOT include wash and is explicitly out of Phase 205 scope — that lives in Phase 209 alongside the actual config flip. This is Codex MEDIUM-5 Option B (recommended for narrower SAFE-09 scope).

Progress table currently:
| 205 — Tin-agnostic CAKE signal + allow_wash gate | 0/0 | Not started | - |

Should become:
| 205 — Tin-agnostic CAKE signal + allow_wash gate | 0/5 | Not started | - |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator approval for SAFE-09 file allowlist expansion (Codex HIGH-2)</name>
  <what-built>
    The original v1 plan-set treated TOPO-02 as a 2-file edit (`cake_signal.py` + `cake_params.py`). RESEARCH §"Pitfall 1: Half-wired wash gate" and PATTERNS §"Shared Pattern S1: Gate-before-emit symmetry" both established that TOPO-02 actually requires 5 files to land in the same wave or the gate is silently broken in production:

    1. `src/wanctl/cake_signal.py` — TOPO-01 tin-agnostic refactor
    2. `src/wanctl/cake_params.py` — TOPO-02 allow_wash gate at the EXCLUDED_PARAMS check site
    3. `src/wanctl/backends/linux_cake.py` — wash/nowash subprocess token emission (production-critical for Spectrum: `overhead: "docsis"` falls back from netlink to subprocess at netlink_cake.py:442-447)
    4. `src/wanctl/backends/netlink_cake.py` — pyroute2 wash kwarg mapping (covers non-docsis netlink path)
    5. `src/wanctl/check_config_validators.py` — KNOWN_AUTORATE_PATHS allowlist for `cake_params.allow_wash` and `cake_params.wash` (without this, daemon startup spams `WARN Unknown config key`)

    Codex review HIGH-2 flagged that the v1 plan-set surfaced this expansion AFTER Plans 02 + 03 had already landed the 5-file diff. That's after-the-fact reconciliation. The operator must approve (or reject) the allowlist expansion BEFORE the source edits land — this plan is the gate.

    Codex MEDIUM-5 also flagged a related design choice for `wash` readback validation (currently `build_expected_readback()` at `cake_params.py:176` and `_VALIDATE_KEY_TO_TCA` at `backends/netlink_cake.py:69` do NOT include `wash`). Two options:
    - **Option A:** Extend Plan 03 to add `wash` to both readback structures (TOPO-02 grows to 6 files; SAFE-09 amendment must reflect 6).
    - **Option B (RECOMMENDED):** Phase 205 validates EMISSION ONLY (command construction); LIVE qdisc readback validation lands in Phase 209 alongside the actual config flip. SAFE-09 amendment reflects 5 files. Narrower scope; readback only matters once Spectrum is actually using wash, which is Phase 209's job.

    PROPOSED ROADMAP AMENDMENTS (three sites):

    Site 1 — Closeout Invariants / SAFE-09 (behavioral) — current line ~23:
    > **SAFE-09 (behavioral):** No controller threshold / algorithm / EWMA / dwell / deadband / burst changes. Control-path source diff bounded to TOPO-01 (`cake_signal.py`), TOPO-02 (`cake_params.py` allow_wash gate, `backends/linux_cake.py` + `backends/netlink_cake.py` wash token/kwarg emission, `check_config_validators.py` allowlist), TOOL-03 (`operator_summary.py` perm handling), and the `__init__.py` version bump. Verified at every phase boundary; mechanical closeout in Phase 209.

    Site 2 — Phase 205 Success Criteria #4 — current line ~49:
    > 4. SAFE-09 phase-boundary check: control-path source diff vs v1.43 close (`6508d68`) is bounded to `cake_signal.py` (TOPO-01) and the TOPO-02 set (`cake_params.py`, `backends/linux_cake.py`, `backends/netlink_cake.py`, `check_config_validators.py`); no threshold/EWMA/dwell/deadband/burst values changed. Wash readback validation (`build_expected_readback()` + `_VALIDATE_KEY_TO_TCA`) is explicitly out of Phase 205 scope — Phase 205 validates emission only; Phase 209 owns live qdisc readback.

    Site 3 — Phase 209 Success Criteria #4 — current line ~107:
    > 4. SAFE-09 closeout: end-to-end control-path source diff between v1.43 close (`6508d68`) and v1.44 close is bounded to `cake_signal.py` (TOPO-01), the TOPO-02 set (`cake_params.py`, `backends/linux_cake.py`, `backends/netlink_cake.py`, `check_config_validators.py`), `operator_summary.py` (TOOL-03), and `src/wanctl/__init__.py` (version bump). HRDN-01's fail-closed verifier confirms mechanically. Phase 209 also adds wash to `build_expected_readback()` + `_VALIDATE_KEY_TO_TCA` so live readback validates the new qdisc state.

    Progress table also needs `0/0 → 0/5` for Phase 205 (was 4 plans in v1; now 5 with this Plan 00 added).

    Decision options for the operator:
    - **approve** — Apply Option B (recommended, 5-file scope) wording verbatim at all three sites. Plan 03 stays at 4 source files (cake_params + linux_cake + netlink_cake + check_config_validators); readback work defers to Phase 209.
    - **option-a** — Apply 6-file wording: same three sites + add `cake_params.py:build_expected_readback` and `backends/netlink_cake.py:_VALIDATE_KEY_TO_TCA` to scope. Plan 03 grows to handle wash readback in Phase 205 itself. Replanner will revise Plan 03 acceptance criteria to require readback symmetry.
    - **amend &lt;wording&gt;** — Apply operator-supplied alternative wording.
    - **reject** — Leave ROADMAP unchanged. Plans 02 + 03 will land a 5-file diff that exceeds the documented allowlist; Phase 209 closeout reviewer must reconcile manually. Plan 04 will document the divergence prominently.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-RESEARCH.md` §"Pitfall 1: Half-wired wash gate" — confirm all 4 TOPO-02 sites must land together.
    2. Read `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-PATTERNS.md` §"Shared Pattern S1: Gate-before-emit symmetry" — same conclusion from the pattern angle.
    3. Read `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md` HIGH-2 + MEDIUM-5 — Codex's framing of why this needs operator pre-approval and the readback-validation choice.
    4. Inspect the three current ROADMAP wording sites:
       ```bash
       grep -n "SAFE-09 (behavioral)" .planning/ROADMAP.md
       grep -n "SAFE-09 phase-boundary check" .planning/ROADMAP.md
       grep -n "end-to-end control-path source diff" .planning/ROADMAP.md
       ```
    5. Decide: `approve` (Option B, 5 files), `option-a` (6 files), `amend &lt;wording&gt;`, or `reject`.
  </how-to-verify>
  <resume-signal>Type "approve" to apply Option B (5-file allowlist) verbatim; "option-a" to apply 6-file allowlist (readback in Phase 205); "amend &lt;wording&gt;" with alternative; "reject" to leave ROADMAP unchanged (divergence documented in Plan 04).</resume-signal>
  <action>Pause for operator decision per &lt;what-built&gt; / &lt;how-to-verify&gt;. Capture the resume-signal verbatim and pass it to Task 2 for branch dispatch.</action>
  <verify>Operator resume-signal received and recorded in execution log.</verify>
  <done>Operator decision captured; Task 2 has a clear branch to take.</done>
</task>

<task type="auto">
  <name>Task 2: Apply ROADMAP amendment + update VALIDATION.md + record resolution in REVIEWS.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md, .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md</files>
  <read_first>
    - Task 1 operator decision (resume-signal)
    - .planning/ROADMAP.md (full file — three SAFE-09 wording sites + Progress table)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md (SAFE-09 boundary verifier line at row ~26 + Per-Task Verification Map at row ~44)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md (HIGH-2 + MEDIUM-5 entries)
  </read_first>
  <action>
    Branch on the operator decision from Task 1.

    Branch APPROVE (Option B, 5-file allowlist — RECOMMENDED):
      - Edit ROADMAP.md Site 1 (Closeout Invariants / SAFE-09 (behavioral) at ~line 23) — replace with the 5-file wording from &lt;what-built&gt;.
      - Edit ROADMAP.md Site 2 (Phase 205 SC#4 at ~line 49) — replace with the 5-file Phase 205 wording (includes the "wash readback explicitly out of scope" sentence).
      - Edit ROADMAP.md Site 3 (Phase 209 SC#4 at ~line 107) — replace with the Phase 209 wording (includes the "Phase 209 also adds wash to build_expected_readback + _VALIDATE_KEY_TO_TCA" sentence).
      - Edit VALIDATION.md SAFE-09 boundary verifier line (row ~26 in Test Infrastructure table) — keep the "exactly 5 files" wording; remove the parenthetical "ROADMAP wording amendment surfaced in Plan 04 Task 2" since the amendment is now upstream in Plan 00.
      - Edit VALIDATION.md Manual-Only Verifications row — replace "(see Plan 04 Task 2 for ROADMAP wording amendment)" with "(ROADMAP amendment landed in Plan 00 Task 2 per operator approval)".

    Branch OPTION-A (6-file allowlist — readback in Phase 205):
      - Edit ROADMAP.md Site 1 — replace with 6-file wording: same as Branch APPROVE plus "wash readback (`cake_params.build_expected_readback`, `backends/netlink_cake._VALIDATE_KEY_TO_TCA`)".
      - Edit ROADMAP.md Site 2 — replace with 6-file Phase 205 wording (drop the "out of scope" sentence; readback is in scope).
      - Edit ROADMAP.md Site 3 — Phase 209 wording mentions the 6-file set is already in place; Phase 209 only confirms.
      - Edit VALIDATION.md SAFE-09 boundary verifier line — change "exactly 5 files" to "exactly 5 files (readback work in cake_params.py + netlink_cake.py is in-scope additions to the existing files; file count is still 5 because both files are already in the set)".
      - Add a NOTE in 205-VALIDATION.md prompting the replanner: Plan 03 must grow a Task 4 for wash readback symmetry. (The replanner should already have authored Plan 03 with this in mind because the operator chose option-a; if Plan 03 was authored under approve, this is a contradiction — abort and re-run /gsd-plan-phase 205 --reviews.)

    Branch AMEND &lt;wording&gt;:
      - Apply the operator's literal wording at all three ROADMAP sites.
      - Adjust VALIDATION.md SAFE-09 verifier line and Manual-Only row to match.

    Branch REJECT:
      - Leave ROADMAP wording unchanged at all three sites.
      - Edit VALIDATION.md SAFE-09 boundary verifier line — add a prominent "DIVERGENCE: actual diff scope (5 files) exceeds ROADMAP wording (2 files); Phase 209 closeout reviewer must reconcile manually."
      - Set the resolution note in REVIEWS.md status to "DIVERGED — operator declined amendment".

    REGARDLESS OF BRANCH:

    Step A — Update ROADMAP.md Progress table row for Phase 205:
      - Locate `| 205 — Tin-agnostic CAKE signal + allow_wash gate | 0/0 | Not started | - |`
      - Replace with `| 205 — Tin-agnostic CAKE signal + allow_wash gate | 0/5 | Not started | - |`

    Step B — Append a status note to .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md at the end (after the "## Next Step" section). Format:
      ```
      ---

      ## Resolution Status (2026-05-14)

      Replanned via `/gsd-plan-phase 205 --reviews`. Outcome:

      - **HIGH-1 (RED-test framing):** Resolved — Plan 01 split into RED-behavior tests (Task 1+2: 9 tests, must fail before Plan 02/03) and GREEN-invariant guard tests (Task 3+4: 5 tests, must pass before AND after Plan 02/03 to prove no regression). Each test tagged with expected color in acceptance criteria.
      - **HIGH-2 (SAFE-09 ordering):** Resolved &lt;BRANCH&gt; — operator chose &lt;decision&gt; in Plan 00 Task 1; ROADMAP amended (or divergence documented) BEFORE source mutation in Plans 02/03.
      - **HIGH-3 (broken grep acceptance):** Resolved — Plan 02 acceptance criteria replaced with positive (helper exists + applied at sites), negative (no `range(1, len(tins_raw))` literal remains), and total-drops invariant (`range(len(tins_raw))` count unchanged at 308 + adjacent total sites) checks.
      - **MEDIUM-4 (byte-identity not pinned):** Resolved — Plan 02 Task 2 adds 4-tin diffserv4 fixture snapshot test asserting bit-equal numeric output before AND after helper extraction.
      - **MEDIUM-5 (readback validation gap):** Resolved &lt;BRANCH&gt; — operator chose &lt;decision&gt;.
      - **MEDIUM-6 (docsis fallback uncovered):** Resolved — Plan 01 RED test for `{overhead_keyword: "docsis", wash: True}` exercises the LinuxCakeBackend fallback path (netlink_cake.py:442-447). Plan 03 turns it GREEN.
      - **MEDIUM-7 (wave-2 diff race):** Resolved — Plan 02 + Plan 03 acceptance criteria use file-scoped diff checks (`git diff &lt;commit&gt; -- &lt;file&gt;`) instead of cross-cutting `--name-only`. Parallelism preserved.
      - **LOW-8 (tin-name attribute):** Resolved — Plan 02 Task action uses `self._tin_names` (verified by grep at b82abf0 — Codex correct on attribute name); test added asserting `snap.tins[0].name == "BestEffort"` for single-tin path.
      - **LOW-9 (oracle naming):** Resolved — RESEARCH/PATTERNS/VALIDATION + new test class names use "structural aggregation oracle" terminology; class renamed `TestCakeSignalProcessorBestEffortStructuralOracle`.

      Plan-set is now 5 plans (was 4): 205-00 (operator gate), 205-01 (RED+GREEN tests), 205-02 (TOPO-01), 205-03 (TOPO-02), 205-04 (SAFE-09 audit only).
      ```
      Replace &lt;BRANCH&gt; / &lt;decision&gt; with the actual operator choice (approve / option-a / amend / reject and the chosen readback option).

    Step C — Add a row to 205-VALIDATION.md Per-Task Verification Map for Plan 00 (insert before the existing 205-01-01 row):
      ```
      | 205-00-01 | 00 | 1 | TOPO-01,02 | T-205-00-01 | operator approval of SAFE-09 file allowlist expansion captured before source mutation | checkpoint:human-verify | (operator resume-signal) | n/a (checkpoint) | ⬜ pending |
      | 205-00-02 | 00 | 1 | TOPO-01,02 | T-205-00-02 | ROADMAP amended (or divergence documented); VALIDATION.md + REVIEWS.md updated | scripted | `grep -c "backends/linux_cake.py" .planning/ROADMAP.md` returns >= 1 (or REJECT branch divergence note present) | n/a (artifact) | ⬜ pending |
      ```

    DO NOT touch any file in `src/wanctl/`. This plan is planning-artifact-only.
  </action>
  <verify>
    <automated>(grep -c "backends/linux_cake.py" .planning/ROADMAP.md | awk '$1 >= 1 {exit 0} {exit 1}' && echo "ROADMAP-AMENDED-OK") || (grep -q "DIVERGENCE" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md && echo "ROADMAP-REJECTED-DIVERGENCE-DOCUMENTED")</automated>
  </verify>
  <acceptance_criteria>
    - If APPROVE / OPTION-A / AMEND: `grep -c "backends/linux_cake.py" .planning/ROADMAP.md` returns >= 1 (was 0 before this plan).
    - If APPROVE / OPTION-A / AMEND: `grep -c "backends/netlink_cake.py" .planning/ROADMAP.md` returns >= 1.
    - If APPROVE / OPTION-A / AMEND: `grep -c "check_config_validators.py" .planning/ROADMAP.md` returns >= 1.
    - If REJECT: `grep -c "DIVERGENCE" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md` returns >= 1.
    - `grep -E "\| 205 — Tin-agnostic CAKE signal \+ allow_wash gate \| 0/5 \|" .planning/ROADMAP.md` matches one line (Progress table updated).
    - `grep -c "Resolution Status" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md` returns 1.
    - `grep -c "HIGH-2" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-REVIEWS.md` returns >= 2 (original mention + resolution line).
    - `git diff HEAD -- src/wanctl/ | wc -l` returns 0 (NO source mutation in this plan — sanity check).
    - 205-VALIDATION.md Per-Task Verification Map contains rows for Plan 00 (`grep -c "205-00-0" .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VALIDATION.md` returns >= 2).
  </acceptance_criteria>
  <done>SAFE-09 file allowlist expansion approved (or divergence documented) at the planning layer BEFORE source mutation; downstream plans inherit a clean baseline; Codex HIGH-2 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator decision → planning artifacts | Operator must explicitly approve or reject the SAFE-09 expansion; silent acceptance would replay Codex HIGH-2. |
| ROADMAP wording → Phase 209 closeout reviewer (future Claude / operator) | Drift between ROADMAP and actual diff scope is the threat this plan exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-00-01 | Repudiation (silent scope creep) | SAFE-09 file allowlist drift between ROADMAP and reality | mitigate | Operator checkpoint forces explicit approve/option-a/amend/reject decision before source mutation; resume-signal recorded in execution log. |
| T-205-00-02 | Tampering (planning-artifact bypass) | Plans 02/03 landing source edits before this plan completes | mitigate | depends_on chain enforces 00 → 01 → (02, 03) → 04. Wave assignment guarantees 00 runs alone in wave 1. |
| T-205-00-03 | Information Disclosure | n/a | n/a | Public-safe planning docs only; no secrets. |
| T-205-00-04 | Denial of Service | n/a | n/a | Read-only audit + planning artifact updates. |
| T-205-00-05 | Elevation of Privilege | n/a | n/a | No auth surface; ROADMAP edits are version-controlled. |
</threat_model>

<verification>
- Task 1 operator resume-signal captured.
- Task 2 ROADMAP amended (or divergence documented) at all three SAFE-09 wording sites; Progress table updated to 0/5; VALIDATION.md and REVIEWS.md updated.
- `git diff HEAD -- src/wanctl/` empty (zero source mutation in this plan — hard invariant).
</verification>

<success_criteria>
- Codex review HIGH-2 (SAFE-09 ROADMAP amendment ordering) closed: operator approval landed BEFORE Plans 02/03 source edits.
- ROADMAP wording at three SAFE-09 sites matches the operator-chosen scope (approve / option-a / amend) OR divergence is documented (reject).
- Progress table reflects 5-plan structure for Phase 205.
- REVIEWS.md status note records resolution of all 9 Codex concerns.
- VALIDATION.md SAFE-09 verifier line and Per-Task Verification Map rows updated.
- Plan 02, 03, 04 inherit a known-good ROADMAP baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-00-SUMMARY.md` documenting:
- Operator decision (approve / option-a / amend / reject) verbatim from resume-signal.
- ROADMAP wording diff (before/after) at all three sites.
- VALIDATION.md changes summary.
- REVIEWS.md resolution status note (the same block written in Task 2).
- Hand-off to Plan 01 (Wave 0 RED+GREEN tests inherit the approved scope).
</output>
