---
phase: 206
plan: 04
type: execute
wave: 3
depends_on:
  - 206-01
  - 206-02
  - 206-03
files_modified:
  - .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md
  - .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md
autonomous: true
requirements:
  - TOPO-04
  - TOPO-05
tags:
  - safe-09
  - phase-boundary
  - closeout
  - revision-2026-05-14

must_haves:
  truths:
    - "SAFE-09 boundary verified across ALL FOUR git surfaces (codex C3): committed diff vs 6508d68, staged diff, working-tree unstaged diff, AND untracked files under src/wanctl/."
    - "The union of committed+staged+untracked under src/wanctl/ equals exactly the Phase 205 5-file allowlist."
    - "Unstaged and untracked surfaces under src/wanctl/ are both empty (wc -l outputs 0)."
    - "Threshold drift cross-check: doc-inlined values match scripts/phase206-thresholds.json values byte-identically (W5 enforcement)."
    - "golden-fixture-provenance.md has the committed NDJSON's sha256 hex digest pinned (no placeholder remains)."
    - "Full pytest suite passes; hot-path slice passes byte-identically."
    - "206-VERIFICATION.md captures all four SAFE-09 surface checks + drift check + test evidence in fenced-block form."
  artifacts:
    - path: ".planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md"
      provides: "Phase 206 closeout verification report (SAFE-09 4-surface boundary + cross-plan drift + test evidence)."
      contains: "SAFE-09"
    - path: ".planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md"
      provides: "Updated provenance doc — placeholder SHA256 replaced with the actual digest."
  key_links:
    - from: ".planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md"
      to: "git diff/ls-files/status (four surfaces)"
      via: "verbatim outputs captured in fenced blocks"
      pattern: "git diff 6508d68"
    - from: ".planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md"
      to: "tests/fixtures/phase206_golden_capture.ndjson"
      via: "sha256sum"
      pattern: "sha256"
---

<objective>
Final-wave SAFE-09 phase-boundary verification + cross-plan drift detection + provenance SHA256 pin.

**Revision 2026-05-14 — addresses codex review C3 (HIGH):** the SAFE-09 boundary check now covers ALL FOUR git surfaces, not just `git diff 6508d68..HEAD`. Untracked files and uncommitted worktree edits are explicitly checked. This closes the codex-identified hole where Phase 209's closeout could falsely pass with uncommitted `src/wanctl/` changes present.

**Also addresses W5:** drift check is now JSON-vs-doc (instead of source-vs-doc), since Plan 02 now reads thresholds from `scripts/phase206-thresholds.json` at module import. The doc inlines the values for operator readability; this plan asserts byte equality between the JSON and the inlined numerics.

Purpose: Phase 206 success criterion #5 (SAFE-09 phase-boundary check: zero new control-path source diff). Also detects drift between Plan 03's doc thresholds and Plan 02's source-of-truth JSON.

Output:
- Updated `.planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md` (SHA256 placeholder replaced).
- New `.planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md` (closeout evidence with four-surface SAFE-09 check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/206-a-b-replay-harness-rollback-gates/206-01-PLAN.md
@.planning/phases/206-a-b-replay-harness-rollback-gates/206-02-PLAN.md
@.planning/phases/206-a-b-replay-harness-rollback-gates/206-03-PLAN.md
@.planning/phases/206-a-b-replay-harness-rollback-gates/206-REVIEWS.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md
@.planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VERIFICATION.md
@CLAUDE.md
</context>

<locked_decisions>
- **SAFE-09 allowlist (frozen at Phase 205 close):** exactly five files under `src/wanctl/`:
  - `src/wanctl/cake_signal.py`
  - `src/wanctl/cake_params.py`
  - `src/wanctl/backends/linux_cake.py`
  - `src/wanctl/backends/netlink_cake.py`
  - `src/wanctl/check_config_validators.py`
- **Baseline SHA:** `6508d68` (v1.43 close).
- **Phase 206 invariant:** Zero new entries under `src/wanctl/` beyond the allowlist. Verified across four git surfaces (codex C3):
  1. Committed diff vs `6508d68` — `git diff 6508d68 --name-only -- src/wanctl/`
  2. Staged diff vs `6508d68` — `git diff --cached 6508d68 --name-only -- src/wanctl/`
  3. Working-tree unstaged edits — `git diff --name-only -- src/wanctl/`
  4. Untracked files — `git ls-files --others --exclude-standard -- src/wanctl/`

  Union of #1, #2, #4 MUST equal exactly the 5-file allowlist. #3 (unstaged) and #4 (untracked) MUST both be empty.

<tasks>

<task type="auto">
  <name>Task 1: Pin SHA256 of committed NDJSON into golden-fixture-provenance.md</name>
  <read_first>
    - .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md (locate the placeholder `<pinned-after-plan-01>`)
    - tests/fixtures/phase206_golden_capture.ndjson (Plan 01 output)
  </read_first>
  <files>
    .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md
  </files>
  <action>
    Compute the SHA256 of the committed NDJSON:

    ```bash
    sha256sum tests/fixtures/phase206_golden_capture.ndjson
    ```

    Capture the hex digest (the first whitespace-separated field of the output).

    Edit `.planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md`:

    Replace the placeholder line:
    ```
    Current value: `<pinned-after-plan-01>`
    ```
    with:
    ```
    Current value: `<HEX-DIGEST>`
    ```
    where `<HEX-DIGEST>` is the computed 64-character lowercase hex string.

    Use Edit tool, not Bash sed/awk.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl &amp;&amp; grep -qE 'Current value: `[a-f0-9]{64}`' .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md &amp;&amp; ! grep -qF 'pinned-after-plan-01' .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md</automated>
  </verify>
  <acceptance_criteria>
    - Placeholder removed: `grep -F "pinned-after-plan-01" .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md` produces no output
    - Hex digest pinned, lowercase 64-char form: `grep -qE 'Current value: \`[a-f0-9]{64}\`' .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md` exits 0
    - Pinned digest matches the live file: `cd /home/kevin/projects/wanctl && grep -oE '[a-f0-9]{64}' .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md | head -n 1` returns the same hex as `sha256sum tests/fixtures/phase206_golden_capture.ndjson | cut -d' ' -f1`
  </acceptance_criteria>
  <done>
    Provenance doc has the actual SHA256 of the committed NDJSON pinned. No placeholder remains. Digest in doc matches digest of the live file.
  </done>
</task>

<task type="auto">
  <name>Task 2: SAFE-09 four-surface boundary verification + cross-plan drift detection + write 206-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-VERIFICATION.md (the verification-report shape to mirror)
    - .planning/phases/205-tin-agnostic-cake-signal-allow-wash-gate/205-04-SUMMARY.md (the closeout-evidence + fenced-block convention)
    - .planning/phases/206-a-b-replay-harness-rollback-gates/206-REVIEWS.md (C3: four-surface SAFE-09; W5: thresholds JSON drift check)
    - .planning/phases/206-a-b-replay-harness-rollback-gates/PHASE-205-ROLLBACK-GATES.md (Plan 03 output — for cross-plan drift check)
    - scripts/phase206-thresholds.json (Plan 02 source-of-truth — for cross-plan drift check)
  </read_first>
  <files>
    .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md
  </files>
  <action>
    **Step 1 — Run the four-surface SAFE-09 boundary checks and capture verbatim outputs (codex C3):**

    ```bash
    cd /home/kevin/projects/wanctl

    # Surface 1: committed diff vs v1.43 close
    git diff 6508d68 --name-only -- src/wanctl/ | sort -u > /tmp/p206_committed.txt

    # Surface 2: staged-but-not-committed
    git diff --cached 6508d68 --name-only -- src/wanctl/ | sort -u > /tmp/p206_staged.txt

    # Surface 3: working-tree unstaged edits
    git diff --name-only -- src/wanctl/ | sort -u > /tmp/p206_worktree_unstaged.txt

    # Surface 4: untracked files
    git ls-files --others --exclude-standard -- src/wanctl/ | sort -u > /tmp/p206_untracked.txt

    # Bonus diagnostic: short-status
    git status --short -- src/wanctl/ > /tmp/p206_status.txt
    ```

    Required outcomes:
    - `wc -l /tmp/p206_committed.txt` outputs `5`.
    - The five lines in `/tmp/p206_committed.txt` match the Phase 205 allowlist exactly (`diff` against an inline expected-list exits 0).
    - `wc -l /tmp/p206_staged.txt` outputs `0` OR matches the same 5 files (operator may stage exactly the allowlist; anything else is a breach).
    - **`wc -l /tmp/p206_worktree_unstaged.txt` outputs `0`** (no uncommitted edits to allowlisted files).
    - **`wc -l /tmp/p206_untracked.txt` outputs `0`** (no new untracked files under `src/wanctl/`).

    If ANY of the four checks fails (e.g., untracked file present, unstaged edit present, committed diff doesn't match allowlist), ABORT this plan: do NOT write 206-VERIFICATION.md as PASS. Surface the unexpected files/diffs to the operator. SAFE-09 boundary breach is a phase-failure condition.

    **Step 2 — Run the threshold-drift cross-check (W5):**

    The doc inlines `5.0`, `10.0`, `10.0` in markdown for operator readability. The JSON is the source of truth. Verify byte equality:

    ```bash
    .venv/bin/python3 - <<'PY'
    import json
    from pathlib import Path

    j = json.loads(Path("scripts/phase206-thresholds.json").read_text())
    doc = Path(".planning/phases/206-a-b-replay-harness-rollback-gates/PHASE-205-ROLLBACK-GATES.md").read_text()

    assert j["RRUL_P99_REGRESSION_PCT"] == 5.0, f"JSON has {j['RRUL_P99_REGRESSION_PCT']}, expected 5.0"
    assert j["RESTART_RATE_INCREASE_PCT"] == 10.0, j
    assert j["TRANSITION_RATE_INCREASE_PCT"] == 10.0, j

    # The doc must reference each constant name and contain the corresponding value.
    # Look for the value-as-inline-code in proximity to the constant name (loose check;
    # the doc table has both name and value).
    for const, val in [
        ("RRUL_P99_REGRESSION_PCT", "5.0"),
        ("RESTART_RATE_INCREASE_PCT", "10.0"),
        ("TRANSITION_RATE_INCREASE_PCT", "10.0"),
    ]:
        assert const in doc, f"{const} missing from doc"
        assert f"`{val}`" in doc, f"value `{val}` missing from doc"
    print("threshold drift check OK")
    PY
    ```

    **Step 3 — Run the full pytest suite plus the hot-path slice (per CLAUDE.md):**

    ```bash
    .venv/bin/pytest tests/ -q
    .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
    ```

    Both must exit 0.

    **Step 4 — Write `.planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md`** following the 205-VERIFICATION.md shape:

    ```markdown
    ---
    phase: 206-a-b-replay-harness-rollback-gates
    verified: <UTC ISO8601 timestamp at the moment of writing>
    status: passed
    score: "5/5 success criteria verified"
    requirements: [TOPO-04, TOPO-05]
    ---

    # Phase 206: A/B Replay Harness + Rollback Gates — Verification Report

    **Phase Goal:** A deterministic A/B replay harness captures pre-migration controller behavior against the 2026-04-22 out-of-band flent finding (substituted with 2026-04-29 per Locked Decision D1), and rollback criteria are encoded as a machine-readable predeploy gate script that fails closed.

    **Status:** passed
    **Re-verification:** No — initial verification (incorporates 2026-05-14 cross-AI review revisions)

    ## Success Criteria

    | # | Criterion (ROADMAP wording) | Status | Evidence |
    |---|------------------------------|--------|----------|
    | 1 | A/B replay harness reuses Phase 193/194/195 pattern; ingests deterministic golden fixture; emits RRUL p99 latency, throughput, jitter for pre + post in one run. | ✓ VERIFIED | Plan 01: `tests/test_phase_206_replay.py::TestPattern193Reuse` (Phase 193 primitives imported); `tests/test_phase_206_replay.py::TestReplaySamplesConsumesAllRows` (C2 fix: per-row CAKE trace consumed); `scripts/phase206-ab-replay.py::_parse_flent_rrul` parses real flent .gz when `--flent-gz-pre/--flent-gz-post` supplied (C1 fix); schema-v1 JSON emitted with `meta.metric_source ∈ {flent, controller_replay}`. |
    | 2 | A/B summary JSON schema stable enough for one-line consumer change. | ✓ VERIFIED | Schema v1 frozen top-level keys: `schema_version, phase, fixture_provenance, fixture_sha256, meta, pre, post, delta, gates`. Per-side controller-replay keys always present; flent keys added when flent inputs supplied. Asserted by `TestSchemaV1Stability`. |
    | 3 | PHASE-205-ROLLBACK-GATES.md documents three rollback triggers in operator-readable form. | ✓ VERIFIED | Plan 03 deliverable. Three trigger sections; thresholds reference `scripts/phase206-thresholds.json` as source of truth (W5); inlined values verified byte-identical to JSON (drift check below). |
    | 4 | Predeploy gate exits non-zero when any of three triggers breaches; operator dry-run on v1.43 baseline exits zero. | ✓ VERIFIED | `tests/test_phase206_predeploy_gate.py` — `TestGateDryRun` passes, three block cases trip rc=1, abort cases trip rc=2, four post-soak mode tests prove M5 full-enforcement, `gate_baseline` schema tests prove M6 separation. Baseline carries REAL v1.43 numerics (`transition_rate_per_hour_baseline: 77.17`, `restart_rate_per_hour_baseline: 0.0` with `_provenance`) per C4 fix. |
    | 5 | SAFE-09 phase-boundary check: zero control-path source diff introduced. | ✓ VERIFIED | See "SAFE-09 Four-Surface Boundary Diff" section below (C3 fix). |

    **Score:** 5/5 success criteria verified

    ## SAFE-09 Four-Surface Boundary Diff (codex C3)

    The pre-revision draft used only `git diff 6508d68..HEAD --name-only -- src/wanctl/`, which excludes uncommitted and untracked changes. This revision verifies ALL FOUR git surfaces.

    ### Surface 1 — Committed diff vs `6508d68`

    ```text
    $ git diff 6508d68 --name-only -- src/wanctl/ | sort -u
    <PASTE VERBATIM 5 LINES — must be exactly the Phase 205 allowlist>

    $ git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l
    5
    ```

    ### Surface 2 — Staged-but-not-committed diff

    ```text
    $ git diff --cached 6508d68 --name-only -- src/wanctl/ | sort -u
    <PASTE VERBATIM — should be empty OR exactly the allowlist>

    $ git diff --cached 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l
    <0 or 5>
    ```

    ### Surface 3 — Working-tree unstaged edits

    ```text
    $ git diff --name-only -- src/wanctl/ | sort -u
    <PASTE VERBATIM — MUST be empty>

    $ git diff --name-only -- src/wanctl/ | sort -u | wc -l
    0
    ```

    ### Surface 4 — Untracked files

    ```text
    $ git ls-files --others --exclude-standard -- src/wanctl/ | sort -u
    <PASTE VERBATIM — MUST be empty>

    $ git ls-files --others --exclude-standard -- src/wanctl/ | wc -l
    0
    ```

    ### Diagnostic: short status

    ```text
    $ git status --short -- src/wanctl/
    <PASTE VERBATIM — operator-visible state>
    ```

    Expected union of surfaces 1+2+4 (Phase 205 allowlist, unchanged):
    - `src/wanctl/backends/linux_cake.py`
    - `src/wanctl/backends/netlink_cake.py`
    - `src/wanctl/cake_params.py`
    - `src/wanctl/cake_signal.py`
    - `src/wanctl/check_config_validators.py`

    Surfaces 3 (unstaged) and 4 (untracked) must both be empty (wc -l = 0).

    No new entries → Phase 206 introduced zero control-path source diff across all four git surfaces (SAFE-09 invariant holds even when including uncommitted state).

    ## Cross-Plan Threshold Drift Check (W5)

    Thresholds live in `scripts/phase206-thresholds.json` (Plan 02 source of truth). Plan 03's doc inlines them for operator readability. Verified equal:

    ```text
    $ .venv/bin/python -c "import json; print(json.dumps(json.load(open('scripts/phase206-thresholds.json')), indent=2))"
    <PASTE VERBATIM JSON>
    ```

    | Constant | JSON value | Doc inlined value | Drift |
    |----------|------------|-------------------|-------|
    | `RRUL_P99_REGRESSION_PCT` | 5.0 | 5.0 | none |
    | `RESTART_RATE_INCREASE_PCT` | 10.0 | 10.0 | none |
    | `TRANSITION_RATE_INCREASE_PCT` | 10.0 | 10.0 | none |

    ## Fixture SHA256 Pin

    Committed NDJSON sha256 (from `sha256sum tests/fixtures/phase206_golden_capture.ndjson`):

    ```text
    <PASTE HEX DIGEST>
    ```

    Pinned in `.planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md`. Drift between the doc value and the live file would indicate the fixture changed without a documentation update — verified equal at verification time.

    ## Test Evidence

    Full pytest suite:

    ```text
    $ .venv/bin/pytest tests/ -q
    <PASTE LAST 5 LINES — summary>
    ```

    Hot-path slice (per CLAUDE.md):

    ```text
    $ .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
    <PASTE LAST 5 LINES — summary>
    ```

    Phase 206 focused slice:

    ```text
    $ .venv/bin/pytest tests/test_phase_206_replay.py tests/test_phase206_ab_replay_cli.py tests/test_phase206_predeploy_gate.py -v
    <PASTE LAST 10 LINES — including each test class/method result>
    ```

    ## Requirements Coverage

    | Requirement | Source Plans | Status | Evidence |
    |-------------|--------------|--------|----------|
    | TOPO-04 (A/B replay harness + deterministic golden fixture) | 206-01 | ✓ SATISFIED | Plan 01 SUMMARY + tests passing. C1 fix wires real flent parsing; C2 fix consumes per-row CAKE trace via `_replay_samples`. |
    | TOPO-05 (Rollback criteria + predeploy gate script) | 206-02, 206-03 | ✓ SATISFIED | Plan 02 + Plan 03 SUMMARYs; gate tests passing; M5 post-soak full-enforcement; M6 `gate_baseline` schema separation; M7 SSH owned by wrapper; W5 thresholds JSON-sourced; doc-vs-JSON drift check above. |

    ## Anti-Patterns Found

    None. Phase 206 is scripts/tests/docs only — no `src/wanctl/` edits across any of the four git surfaces, no controller threshold/algorithm changes, no `/health` field additions.

    ## Human Verification Required

    None. Phase 206 is offline harness + docs; no deploy or visual behavior to verify.

    ## Gaps Summary

    No blocking gaps. Phase 206 achieves its goal: a deterministic A/B replay harness exists (with real flent parsing as an option), three rollback triggers are documented and enforced (with full-enforcement post-soak mode), SAFE-09 invariant holds across all four git surfaces at the phase boundary.

    Operator follow-up before Phase 209's production canary (not blocking for Phase 206 close):
    - Refresh the golden fixture with a fresh capture per the re-derivation procedure in golden-fixture-provenance.md (optional; the 2026-04-29 capture is acceptable as the design-time deterministic fixture).
    - Re-derive `restart_rate_per_hour_baseline` if the v1.43 reference soak's zero-restart assumption no longer holds for the live host being canaried.

    ---

    _Verified: <UTC ISO8601 timestamp>_
    _Verifier: Plan 04 closeout task (mechanical, 2026-05-14 revision)_
    ```

    Fill the `<PASTE …>` placeholders with the verbatim command outputs captured during this task. Use Edit/Write — not Bash heredoc — for the final file write.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl &amp;&amp; test "$(git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l)" = "5" &amp;&amp; test "$(git diff --name-only -- src/wanctl/ | wc -l)" = "0" &amp;&amp; test "$(git ls-files --others --exclude-standard -- src/wanctl/ | wc -l)" = "0" &amp;&amp; test -f .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md &amp;&amp; grep -F "5/5 success criteria verified" .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md &amp;&amp; grep -F "status: passed" .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - **C3 / Surface 1 (committed diff, MANDATORY):** `cd /home/kevin/projects/wanctl && git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l` outputs exactly `5`.
    - **C3 / Surface 1 file set exact:** `git diff 6508d68 --name-only -- src/wanctl/ | sort -u | diff - <(printf '%s\n' src/wanctl/backends/linux_cake.py src/wanctl/backends/netlink_cake.py src/wanctl/cake_params.py src/wanctl/cake_signal.py src/wanctl/check_config_validators.py | sort -u)` exits 0.
    - **C3 / Surface 3 (unstaged worktree, MANDATORY):** `git diff --name-only -- src/wanctl/ | wc -l` outputs `0`.
    - **C3 / Surface 4 (untracked, MANDATORY):** `git ls-files --others --exclude-standard -- src/wanctl/ | wc -l` outputs `0`.
    - **C3 / Surface 2 (staged) consistency:** `git diff --cached 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l` outputs `0` or `5`. If `5`, the file set must equal the Phase 205 allowlist exactly.
    - **W5: doc/JSON drift check passes:** the inline Python drift-check block from the Action exits 0 (run as part of this task).
    - **W5: doc cites the JSON:** `grep -F "scripts/phase206-thresholds.json" .planning/phases/206-a-b-replay-harness-rollback-gates/PHASE-205-ROLLBACK-GATES.md` exits 0.
    - **Full pytest pass:** `.venv/bin/pytest tests/ -q` exits 0.
    - **Hot-path slice pass:** `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0.
    - **Verification doc exists and is structured:** `test -f .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md` exits 0; `grep -F "status: passed" ...` exits 0; `grep -F "5/5 success criteria verified" ...` exits 0.
    - **All four SAFE-09 surface sections present in verification doc:** `grep -cE '^### Surface [1-4]' .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md` outputs `4`.
    - **All five ROADMAP success criteria addressed:** `grep -cE "^\| [1-5] \|" .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md` outputs at least `5`.
    - **C3 / Surface 1 captured verbatim:** the verification doc contains a fenced code block with all five file paths.
  </acceptance_criteria>
  <done>
    SAFE-09 boundary verified across all four git surfaces (codex C3 fix): committed (5 files, allowlist match), staged (0 or 5), unstaged (0), untracked (0). Cross-plan threshold drift check verifies doc-inlined values match `scripts/phase206-thresholds.json` byte-identically (W5 fix). Full pytest + hot-path slice both green. `206-VERIFICATION.md` written with all evidence captured verbatim across four surface sections. Phase 206 closeout complete; Phase 209 has a clean foundation that includes uncommitted-state coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tree -> verifier | The boundary check trusts `git diff/ls-files/status` output against `6508d68`. If the SHA is wrong, the check is wrong. |
| plans 02 + 03 outputs -> drift check | Documentation and source-of-truth JSON must be checked TOGETHER; either alone is insufficient. |
| codex C3 closure | The pre-revision draft only checked committed surface; this revision closes the uncommitted/untracked hole. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-206-08 | Tampering (silent fail-open) | SAFE-09 boundary check | mitigate | C3 fix: four-surface check (committed + staged + unstaged + untracked). Single-surface coverage would miss uncommitted edits or untracked files under `src/wanctl/`. All four surfaces have explicit MANDATORY acceptance criteria. |
| T-206-09 | Tampering | Baseline SHA `6508d68` | accept | Verified twice in ROADMAP and in Phase 205 VERIFICATION.md. Stable repo invariant. |
| T-206-10 | Tampering (drift) | Threshold constants between doc and JSON source-of-truth | mitigate | Plan 04 Task 2 runs a Python drift-check that loads `scripts/phase206-thresholds.json` and asserts each value appears in the doc's inlined-value cells. Drift between JSON and doc surfaces immediately. |
| T-206-15 | Tampering (commit-time race) | Surface 3 (worktree unstaged) | mitigate | Surface 3 explicitly asserts `wc -l == 0`. An operator who edits a `src/wanctl/` file but forgets to commit/revert would trigger a Plan 04 abort — exactly the hole codex flagged. |
</threat_model>

<verification>
End-of-plan verification:

```
# SAFE-09 four surfaces (codex C3)
cd /home/kevin/projects/wanctl
git diff 6508d68 --name-only -- src/wanctl/ | sort -u                       # MUST be exactly the 5-file allowlist
git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l               # MUST output 5
git diff --cached 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l      # 0 or 5
git diff --name-only -- src/wanctl/ | wc -l                                 # MUST output 0
git ls-files --others --exclude-standard -- src/wanctl/ | wc -l             # MUST output 0
git status --short -- src/wanctl/                                           # diagnostic only

# Cross-plan drift (W5)
.venv/bin/python -c "import json; t=json.load(open('scripts/phase206-thresholds.json')); print('thresholds JSON:', t)"
grep -F "5.0" .planning/phases/206-a-b-replay-harness-rollback-gates/PHASE-205-ROLLBACK-GATES.md
grep -F "10.0" .planning/phases/206-a-b-replay-harness-rollback-gates/PHASE-205-ROLLBACK-GATES.md

# Fixture SHA pin
grep -oE '[a-f0-9]{64}' .planning/phases/206-a-b-replay-harness-rollback-gates/golden-fixture-provenance.md | head -n 1
sha256sum tests/fixtures/phase206_golden_capture.ndjson | cut -d' ' -f1
# (both must match)

# Full suite
.venv/bin/pytest tests/ -q

# Hot-path slice
.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q

# Verification doc structure
grep -F "status: passed" .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md
grep -F "5/5 success criteria verified" .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md
grep -cE '^### Surface [1-4]' .planning/phases/206-a-b-replay-harness-rollback-gates/206-VERIFICATION.md   # MUST output 4
```

All checks must succeed.
</verification>

<success_criteria>
- [ ] **C3:** SAFE-09 boundary check covers all four git surfaces (committed, staged, unstaged, untracked) with explicit acceptance criteria.
- [ ] Committed surface: exactly the Phase 205 5-file allowlist preserved.
- [ ] Unstaged surface under `src/wanctl/`: `wc -l` outputs 0.
- [ ] Untracked surface under `src/wanctl/`: `wc -l` outputs 0.
- [ ] `golden-fixture-provenance.md` SHA256 placeholder replaced with the actual digest.
- [ ] **W5:** doc-inlined thresholds match `scripts/phase206-thresholds.json` byte-identically (drift check passes).
- [ ] Full pytest suite passes; hot-path slice passes.
- [ ] `206-VERIFICATION.md` exists, status `passed`, score `5/5`, evidence captured verbatim in fenced blocks for all four surfaces.
</success_criteria>

<output>
The verification doc IS this plan's output. Additionally write `.planning/phases/206-a-b-replay-harness-rollback-gates/206-04-SUMMARY.md` capturing:
- Files modified (two): the updated provenance doc and the new 206-VERIFICATION.md.
- The five SAFE-09 file paths (Surface 1) and `wc -l` output.
- Surfaces 2/3/4 line counts (0, 0, 0 expected).
- The pinned SHA256 hex digest.
- Drift check result (PASS / which constant drifted).
- Pytest summary lines.
</output>
