---
phase: "191.1"
plan: "01"
subsystem: "att-config"
tags: ["config", "att", "v1.38-baseline", "fusion", "irtt"]
requirements: ["SAFE-03", "VALN-02"]
requires: []
provides: ["ATT config restored to v1.38 baseline for irtt.server and fusion.enabled"]
affects: ["configs/att.yaml"]
tech_stack:
  - "YAML"
patterns:
  - "config-only restoration"
  - "semantic baseline diff guard"
key_files:
  created:
    - ".planning/phases/191.1-att-config-drift-resolution-and-phase-191-closure/191.1-01-SUMMARY.md"
  modified:
    - "configs/att.yaml"
decisions:
  - "Restored irtt.server and fusion.enabled together in one config commit per D-03."
  - "Removed the inline fusion disable comment instead of replacing it, per D-USER-01."
metrics:
  duration: "3m"
  completed_at: "2026-04-20T17:19:12Z"
  tasks_completed: 2
---

# Phase 191.1 Plan 01: ATT Config Restoration Summary

Restored the ATT config drift that live Phase 191 evidence identified as the closure blocker: `irtt.server` is back to `104.200.21.31` and `fusion.enabled` is back to `true`, with no other semantic config changes relative to `v1.38`.

## Tasks Completed

| Task | Name | Outcome | Commit |
| --- | --- | --- | --- |
| 1 | Restore `irtt.server` and `fusion.enabled` to `v1.38` values | Completed | `6db6a14` |
| 2 | Semantic YAML key-level diff guard vs `v1.38` baseline | Completed | `19de929` |

## Changes Made

### Exact diff of `configs/att.yaml`

```diff
diff --git a/configs/att.yaml b/configs/att.yaml
index 2f99d38..8b0f53d 100644
--- a/configs/att.yaml
+++ b/configs/att.yaml
@@ -126,7 +126,7 @@ signal_processing:
 # IRTT UDP RTT measurement (v1.18+)
 irtt:
   enabled: true
-  server: "zylone.org"
+  server: "104.200.21.31"
   port: 2112
   duration_ms: 1000
   packet_size: 48
@@ -143,7 +143,7 @@ reflector_quality:
 # remains enabled, but WANController bypasses blending whenever absolute ICMP
 # and IRTT RTT disagreement exceeds ATT's green-threshold budget.
 fusion:
-  enabled: false   # Disabled 2026-04-17: ATT carrier deprioritizes ICMP (~10ms gap), fusion mathematically bypasses every cycle. Re-enable if Protocol deprioritization log rate drops.
+  enabled: true
   icmp_weight: 0.7
```

## Verification

### Task 1 guards

- YAML parse guard: `python3 -c "import yaml; yaml.safe_load(open('configs/att.yaml'))"` exited `0`
- Irtt server guard: `grep -E '^\s*server:\s*"104\.200\.21\.31"\s*$' configs/att.yaml` matched
- Fusion enabled guard: `grep -A1 '^fusion:' configs/att.yaml | grep -E '^\s*enabled:\s*true\s*$'` matched
- Drift removal guard: `grep -E '^\s*server:\s*"zylone\.org"' configs/att.yaml` returned no matches

### Task 2 guards

- Semantic YAML diff vs `v1.38`: `OK`
- SAFE-03 protected-token grep: `SAFE-03 CLEAN`

### Focused regression slice

- Command: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`
- Result: `462 passed in 40.86s`

## Decisions Made

- Applied the ATT `v1.38` restoration as one coordinated config edit so `irtt.server` and `fusion.enabled` move together, matching the Phase 191 probe evidence.
- Left all other ATT config keys untouched and relied on the semantic YAML diff guard to prove no broader SAFE-03 drift.
- Removed the previous inline operator disable comment rather than replacing it, because the plan explicitly required raw key/value lines only.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- Found summary artifact at `.planning/phases/191.1-att-config-drift-resolution-and-phase-191-closure/191.1-01-SUMMARY.md`
- Found task commits `6db6a14` and `19de929` in `git log`
- Stub scan across changed files returned no matches
