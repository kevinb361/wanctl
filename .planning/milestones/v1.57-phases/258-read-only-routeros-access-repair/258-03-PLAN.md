---
phase: 258-read-only-routeros-access-repair
plan: 03
type: execute
wave: 3
depends_on: ["258-01", "258-02"]
files_modified:
  - .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt
  - scripts/phase258-readonly-proof.py
  - .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md
autonomous: false
requirements: [ACCESS-02, ACCESS-03, SAFE-21]
user_setup:
  - service: routeros-cake-shaper
    why: "Live read-only RouterOS proof requires the production router + steering credential on cake-shaper; operator-at-keyboard per D3 / SAFE-21"
    env_vars:
      - name: ROUTER_PASSWORD
        source: "/etc/wanctl/secrets on cake-shaper (already present; read with steering.service env)"
must_haves:
  truths:
    - "The patched Plan-02 code (netwatch + script REST handlers) is DEPLOYED to /opt/wanctl on cake-shaper before the proof runs, confirmed by a grep/version check on the deployed file"
    - "The live proof runs through get_router_client (the wanctl factory), NOT raw curl, and loads the REAL deployed /etc/wanctl/steering.yaml via SteeringConfig(path) — the same class+loader the daemon uses — NOT a synthetic/hardcoded config object; it resolves ROUTER_PASSWORD identically (config ${ENV} expansion), validates commands via readonly_validator, and parses FULL JSON (no head -c truncation)"
    - "The harness runs with PYTHONPATH=/opt and ASSERTS at runtime that wanctl.__file__ AND the imported routeros_rest module's __file__ resolve UNDER /opt/wanctl, recording both paths; if either is not under /opt/wanctl it emits ACCESS02_PROOF_FAIL import-path and exits non-zero — proving the proof exercised the DEPLOYED code, not a stray copy"
    - "A live /ip route print AND /tool netwatch print AND /system script print each return exit 0 with non-empty parseable JSON via run_cmd (D4, extended for the guard's three reads)"
    - "Every live command passes the generalized read-only validator before execution"
    - "The A1 gate was already resolved in Plan 01 (BLOCKING preflight); this plan does NOT contain an inline SSH fork — if A1 had failed the phase would have stopped before here"
  artifacts:
    - path: scripts/phase258-readonly-proof.py
      provides: "Python proof harness that runs ON cake-shaper, loads the real /etc/wanctl/steering.yaml via SteeringConfig, calls get_router_client, asserts import paths under /opt/wanctl, validates + runs the three reads, parses full JSON, prints counts + redacted samples"
      contains: "get_router_client"
    - path: .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt
      provides: "validated read-only command file for the proof (COMMAND: lines for route + netwatch + script)"
      contains: "COMMAND:"
    - path: .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md
      provides: "D4 live proof evidence (all three reads exit 0, parseable) + deployed-code confirmation + import-path & real-config provenance + ACCESS-03 residual"
      contains: "netwatch"
  key_links:
    - from: "scripts/phase258-readonly-proof.py"
      to: "src/wanctl/router_client.get_router_client"
      via: "the proof goes through the transport factory, not raw curl"
      pattern: "get_router_client"
    - from: "scripts/phase258-readonly-proof.py"
      to: "src/wanctl/steering/daemon.SteeringConfig"
      via: "loads the REAL deployed /etc/wanctl/steering.yaml via SteeringConfig(path) — same loader the daemon uses — not a synthetic config"
      pattern: "SteeringConfig"
    - from: "scripts/phase258-readonly-proof.py"
      to: "src/wanctl/readonly_validator.py"
      via: "every command validated before run_cmd"
      pattern: "readonly_validator|validate_command"
    - from: "258-03-access02-proof.md"
      to: "/opt/wanctl deployed handler"
      via: "deployed-code confirmation (grep _handle_script_print on cake-shaper) before the proof"
      pattern: "_handle_script_print|deployed"
---

<objective>
Prove ACCESS-02 against the live router THROUGH wanctl's supported inspection path: with the patched
Plan-02 code DEPLOYED to /opt/wanctl on cake-shaper, a Python harness that loads the REAL deployed
`/etc/wanctl/steering.yaml` via `SteeringConfig(path)` (the same loader the daemon uses) and calls
`get_router_client` (the real factory) runs `/ip route print`, `/tool netwatch print`, AND
`/system script print`, each returning exit 0 with non-empty, parseable FULL JSON. The harness runs
with `PYTHONPATH=/opt` and asserts its imported `wanctl`/`routeros_rest` modules resolve under
`/opt/wanctl` so the proof is pinned to the deployed runtime. Enforce ACCESS-03/SAFE-21 by validating
every command through the generalized read-only validator first.

This plan was restructured per the cross-AI review:
- HIGH-2: the proof goes through `get_router_client` via a Python harness (config/env loading,
  password resolution, run_cmd contract) — NOT raw curl. Curl is no longer the D4 proof. Full JSON is
  parsed (no `head -c` truncation). The harness loads the REAL `/etc/wanctl/steering.yaml` via
  `SteeringConfig(path)` (not a synthetic config), runs with `PYTHONPATH=/opt`, and asserts
  `wanctl.__file__`/`routeros_rest.__file__` resolve under `/opt/wanctl` — proving the SAME deployed
  runtime/config path that `steering.service` uses, end-to-end.
- HIGH-3: an explicit operator-driven deploy step gets the patched code onto cake-shaper BEFORE the
  proof, with a deployed-code confirmation (grep the new handler on /opt/wanctl).
- HIGH-4: the A1 gate is RESOLVED IN PLAN 01 as a blocking preflight. There is NO inline SSH fork
  here. If A1 had failed, the phase stopped at Plan 01 and a separate SSH fallback phase is written.
  Per D1, SSH is dropped when REST is steering's path.

Purpose: D4 is the ACCESS-02 acceptance gate and cannot be satisfied by unit tests or raw curl alone
— it needs a live multi-read proof through the supported wanctl client, against the deployed fix,
loading the real deployed config. Proving the three reads the guard depends on de-risks Phase 259.

Output: a validated read-only command file, the proof harness, and the D4 proof evidence.

NOTE: This plan is operator-driven. Privileged reads and the deploy on cake-shaper are handed to
Kevin as `! <command>` per D3 — never via credential escalation, never folded into a privileged
deploy.sh path. SAFE-21: read-only `print`/GET only; no RouterOS mutation, no Netwatch change, no
steering.service restart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/258-read-only-routeros-access-repair/258-CONTEXT.md
@.planning/phases/258-read-only-routeros-access-repair/258-RESEARCH.md
@.planning/phases/258-read-only-routeros-access-repair/258-PATTERNS.md

<interfaces>
<!-- Proof goes through the transport factory, never a bespoke client and never raw curl (review HIGH-2). -->
<!-- src/wanctl/router_client.get_router_client(config, logger) -> RouterOSREST/RouterOSSSH by
     config.router_transport (default "rest"). client.run_cmd(cmd, capture=True, timeout=5) ->
     (rc, stdout, stderr); rc==0 + json.loads(stdout) non-empty == a passing read. -->

<!-- REAL-CONFIG PROVENANCE (review HIGH-2 / cycle-2): the harness does NOT build a synthetic/hardcoded
     config. It loads the ACTUAL deployed /etc/wanctl/steering.yaml through wanctl's real loader so the
     supported steering config path is proven end-to-end. VERIFIED FACT (confirmed against source):
       - src/wanctl/steering/daemon.py:153 defines `class SteeringConfig(BaseConfig)`.
       - src/wanctl/config_base.py:392 `BaseConfig.__init__(self, config_path: str)` opens and
         yaml.safe_load()s the file at config_path, then runs schema validation and calls
         _load_specific_fields().
       - For SteeringConfig that runs _load_router_transport() (daemon.py:181), which reads
         router.transport/host/user/password/port/verify_ssl and resolves ${ROUTER_PASSWORD} via the
         real ${ENV} expansion, plus _build_router_dict().
     Therefore `SteeringConfig("/etc/wanctl/steering.yaml")` constructs the SAME config object the
     daemon builds, exercising the real loader, schema validation, transport loading, and password
     resolution. The harness passes that config straight to `get_router_client(config, logger)` exactly
     as today. RouterOSREST.from_config / _resolve_password (router_client.py lines ~95-116) expand
     ${ROUTER_PASSWORD} identically. -->

<!-- RUNTIME / IMPORT PROVENANCE (review HIGH-2 / cycle-2): steering.service runs the daemon with
     WorkingDirectory=/opt/wanctl, ExecStart=/usr/bin/python3 -m wanctl.steering.daemon, and
     Environment=PYTHONPATH=/opt (deploy/systemd/steering.service:13,14,18), with
     EnvironmentFile=-/etc/wanctl/secrets (line 22) supplying ROUTER_PASSWORD. The harness MUST run with
     PYTHONPATH=/opt so `import wanctl` resolves the package under /opt/wanctl exactly as the daemon
     does, and MUST assert at runtime that wanctl.__file__ AND the imported routeros_rest module's
     __file__ (i.e. wanctl.routeros_rest, reachable via the get_router_client import chain) both resolve
     UNDER /opt/wanctl. It records both paths. If either is NOT under /opt/wanctl, the harness emits
     `ACCESS02_PROOF_FAIL import-path` and exits non-zero — this proves the proof exercised the deployed
     code, not a stray copy. -->

<!-- DEPLOY (review HIGH-3): the patched code must be on /opt/wanctl on cake-shaper before the proof.
     deploy.sh rsyncs src/wanctl/ to /opt/wanctl (deploy_code, lines 190-242) but is operator-run and
     NOT a credential path (D3 keeps secrets out of deploy). The operator deploy + confirm is handed
     to Kevin as `! <command>`. Confirmation = grep the new handler on the deployed file:
       ! ssh cake-shaper 'grep -c _handle_script_print /opt/wanctl/routeros_rest.py' -> must be >= 1. -->

<!-- A1 gate already resolved in Plan 01 (258-01-a1-preflight.md). This plan assumes A1-confirmed.
     No curl A1 probe and no SSH fork here. -->

<!-- Command-file format the validator expects (readonly_validator.iter_commands): each executable
     line starts with `COMMAND:`; blank lines ignored. The command lines are the RouterOS print verbs
     (/ip route print, /tool netwatch print, /system script print) — the same strings the harness
     passes to run_cmd. -->

<!-- CREDENTIAL HYGIENE (review MEDIUM): the harness must NOT put the password on any command line.
     ROUTER_PASSWORD is resolved from the environment by the real config's ${ROUTER_PASSWORD} expansion
     (source /etc/wanctl/secrets + PYTHONPATH=/opt makes it resolve identically to the daemon, which
     uses EnvironmentFile=-/etc/wanctl/secrets); it is never echoed; evidence redacts sensitive fields.
     No `curl -u admin:$PW`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + validate the read-only proof command file (route + netwatch + script)</name>
  <read_first>
    - .planning/phases/258-read-only-routeros-access-repair/258-PATTERNS.md (Live ACCESS-02 proof harness section — go through the factory)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/evidence/phase257-readonly-commands-20260620T120700Z.txt (command-file format analog)
    - src/wanctl/readonly_validator.py (the validator that must accept every proof command — created in Plan 02; anchored prefix predicate)
  </read_first>
  <action>
    Create `.planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt`
    in the `COMMAND:`-prefixed format `readonly_validator.iter_commands` expects. Include exactly the
    THREE RouterOS read verbs the guard + D4 proof need: `/ip route print`, `/tool netwatch print`,
    and `/system script print` (the forms the generalized validator's anchored READ_ONLY_ROUTEROS_OBJECTS
    prefix accepts). Validate the file:
    `.venv/bin/python -m wanctl.readonly_validator .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt`
    must print `READONLY_COMMANDS_VALIDATED`. This proves ACCESS-03/SAFE-21 procedural enforcement
    gates the proof commands before any live execution. Do NOT add any mutating verb; the validator
    will reject it (that rejection is the guarantee).
  </action>
  <verify>
    <automated>.venv/bin/python -m wanctl.readonly_validator .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt | grep -q READONLY_COMMANDS_VALIDATED && grep -q "netwatch" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt && grep -q "system script\|system/script" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt</automated>
  </verify>
  <acceptance_criteria>
    - `258-03-readonly-commands.txt` exists with `COMMAND:` lines for `/ip route print`, `/tool netwatch print`, and `/system script print`.
    - `.venv/bin/python -m wanctl.readonly_validator <file>` prints `READONLY_COMMANDS_VALIDATED` (exit 0).
    - No mutating verb or shell metacharacter present (validator would reject otherwise).
  </acceptance_criteria>
  <done>
    A read-only proof command file containing the three required reads exists and passes the
    generalized read-only validator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the get_router_client proof harness (real steering.yaml load, import-path pinned, full JSON, validator-gated, no password on cmdline)</name>
  <read_first>
    - src/wanctl/router_client.py (get_router_client factory + _resolve_password — the harness MUST go through this)
    - src/wanctl/steering/daemon.py (class SteeringConfig(BaseConfig); _load_router_transport — the real config the daemon builds)
    - src/wanctl/config_base.py (BaseConfig.__init__(config_path) — opens + yaml.safe_load + schema validate + _load_specific_fields, the real loader)
    - src/wanctl/routeros_rest.py (RouterOSREST.from_config, run_cmd contract, the new netwatch/script handlers; the imported module whose __file__ is asserted under /opt/wanctl)
    - src/wanctl/readonly_validator.py (validate_command / iter_commands — gate every command before run_cmd)
    - .planning/phases/258-read-only-routeros-access-repair/258-PATTERNS.md (proof harness section)
  </read_first>
  <action>
    Create `scripts/phase258-readonly-proof.py` — a standalone harness intended to run ON cake-shaper
    against the DEPLOYED /opt/wanctl code, under steering.service's environment (PYTHONPATH=/opt,
    /etc/wanctl/secrets sourced). It must:
    (1) Read the command file `258-03-readonly-commands.txt` via
    `wanctl.readonly_validator.iter_commands` and call `validate_command` on EACH line first; abort
    (non-zero exit, print the rejection) if any command fails validation — proving ACCESS-03 gates the
    proof.
    (2) Load the REAL deployed config via `SteeringConfig("/etc/wanctl/steering.yaml")` (imported from
    `wanctl.steering.daemon`) — the SAME class + loader the daemon uses (BaseConfig.__init__ opens and
    yaml.safe_load()s the path, runs schema validation, and runs _load_router_transport which reads
    router.transport/host/user/password/port/verify_ssl and resolves ${ROUTER_PASSWORD} via the real
    ${ENV} expansion). Do NOT build a synthetic or hardcoded config object. Then obtain the client via
    `get_router_client(config, logger)` — NOT a bespoke client, NOT curl. The password is resolved from
    the environment by the existing ${ENV} expansion inside the real config; the harness NEVER places
    the password on a command line and NEVER prints it.
    (3) IMPORT-PATH PROVENANCE: assert at runtime that `wanctl.__file__` AND the imported
    `routeros_rest` module's `__file__` (the `wanctl.routeros_rest` module reached via the
    get_router_client import chain — import it explicitly to read its `__file__`) both resolve UNDER
    `/opt/wanctl`. Record (and print) both resolved paths. If either is NOT under `/opt/wanctl`, emit
    `ACCESS02_PROOF_FAIL import-path` and exit non-zero — this proves the proof exercised the DEPLOYED
    code, not a stray copy. (The harness is expected to run with PYTHONPATH=/opt; this assertion makes
    that load-bearing rather than assumed.)
    (4) For each validated command, call `client.run_cmd(cmd, capture=True, timeout=5)`, assert rc==0,
    and `json.loads(stdout)` the FULL output (no truncation, no `head -c`). Record the parsed entry
    COUNT and a REDACTED sample (e.g. for netwatch: host + status with any sensitive field masked) —
    never dump raw credential-bearing fields.
    (5) Print a machine-greppable verdict line: `ACCESS02_PROOF_PASS route=<n> netwatch=<n> script=<n>`
    when all three reads return rc==0 and non-empty parseable JSON; otherwise
    `ACCESS02_PROOF_FAIL <reason>` and exit non-zero.
    Run `.venv/bin/ruff check scripts/phase258-readonly-proof.py` and (if scripts are type-checked)
    `.venv/bin/mypy` clean. The harness performs NO mutation: it only issues print/GET reads via
    run_cmd. SAFE-21: no set/add/remove/enable/disable, no service restart. Add a focused unit test
    `tests/test_phase258_readonly_proof.py` that mocks get_router_client/run_cmd to assert: a mutating
    command in the command file is rejected before any run_cmd call, and a happy-path mock yields the
    ACCESS02_PROOF_PASS verdict — so the harness logic is provable without the live router. STRUCTURE
    the harness so `run_proof(client, commands)` is a PURE, mockable function that takes an already-built
    client and the validated commands and returns the verdict — the real-config load
    (`SteeringConfig(...)` + `get_router_client(...)`) and the import-path/__file__ assertions live in
    `main(argv)` (or a small helper it calls), NOT inside `run_proof`. The mocked test exercises
    `run_proof` directly with a fake client, so it does not need the real /etc/wanctl/steering.yaml or
    /opt/wanctl on disk.
  </action>
  <verify>
    <automated>.venv/bin/ruff check scripts/phase258-readonly-proof.py && grep -q "get_router_client" scripts/phase258-readonly-proof.py && grep -q "SteeringConfig" scripts/phase258-readonly-proof.py && grep -q "/etc/wanctl/steering.yaml" scripts/phase258-readonly-proof.py && grep -q "__file__" scripts/phase258-readonly-proof.py && grep -q "/opt/wanctl" scripts/phase258-readonly-proof.py && grep -q "ACCESS02_PROOF_FAIL import-path" scripts/phase258-readonly-proof.py && grep -q "validate_command\|iter_commands" scripts/phase258-readonly-proof.py && grep -q "def run_proof" scripts/phase258-readonly-proof.py && ! grep -Eq "head -c|-u .admin:|:\\$ROUTER_PASSWORD" scripts/phase258-readonly-proof.py && (test -f tests/test_phase258_readonly_proof.py && .venv/bin/pytest -o addopts='' tests/test_phase258_readonly_proof.py -q || true)</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase258-readonly-proof.py` obtains its client via `get_router_client` (grep-confirmed) — not curl, not a bespoke client.
    - It loads the REAL deployed config via `SteeringConfig("/etc/wanctl/steering.yaml")` (grep-confirmed) — NOT a synthetic/hardcoded config object.
    - It asserts `wanctl.__file__` and the imported `routeros_rest` module's `__file__` resolve under `/opt/wanctl`, records both paths, and emits `ACCESS02_PROOF_FAIL import-path` + non-zero exit if not (grep-confirmed).
    - It validates EVERY command via `readonly_validator` before any `run_cmd`, and aborts on rejection.
    - It parses FULL JSON (no `head -c`, no truncation) and never puts the password on a command line (negative grep gate passes).
    - It prints `ACCESS02_PROOF_PASS route=<n> netwatch=<n> script=<n>` on success and `ACCESS02_PROOF_FAIL` otherwise.
    - `run_proof(client, commands)` is a pure, mockable function; the harness-logic test (mocked) passes if present.
    - `.venv/bin/ruff check scripts/phase258-readonly-proof.py` exits 0.
  </acceptance_criteria>
  <done>
    A validator-gated proof harness exists that loads the REAL /etc/wanctl/steering.yaml via
    SteeringConfig, goes through `get_router_client`, asserts its imported wanctl/routeros_rest modules
    resolve under /opt/wanctl, parses full JSON for all three reads, redacts samples, leaks no password
    to the command line, and emits a greppable PASS/FAIL verdict.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint B: Deploy patched code to cake-shaper, confirm it, then run the proof harness (PYTHONPATH=/opt, real config, import-path asserted)</name>
  <action>
    Operator (a) deploys the patched Plan-02 code to /opt/wanctl on cake-shaper, (b) confirms the new
    handler is present on the deployed file, (c) runs the harness on cake-shaper under the steering
    environment with `PYTHONPATH=/opt` so it imports the deployed wanctl and loads the REAL
    /etc/wanctl/steering.yaml, and pastes the verdict + import paths + redacted counts for Task 3 to
    record. All commands are handed as `! <command>` per D3. The harness validity is tied to TWO gates:
    the deployed-code confirmation (`_handle_script_print` on /opt/wanctl) AND the harness's own
    import-path assertion. If the grep does not find `_handle_script_print` on /opt/wanctl, or the
    harness emits `ACCESS02_PROOF_FAIL import-path` (imported modules not under /opt/wanctl), STOP —
    the proof would exercise stock or stray code, not the deployed fix.
  </action>
  <what-built>
    Plan 02 added the netwatch + script REST handlers and the generalized validator IN THE REPO. The
    proof must run against the DEPLOYED handler on cake-shaper (review HIGH-3), not stock /opt/wanctl.
    The proof goes through `get_router_client` (review HIGH-2), loading the REAL /etc/wanctl/steering.yaml
    via `SteeringConfig(path)` (same loader the daemon uses, NOT a synthetic config), running with
    `PYTHONPATH=/opt` and asserting its imported wanctl/routeros_rest modules resolve under /opt/wanctl
    — so ROUTER_PASSWORD resolves identically and the SAME deployed runtime/config path is proven
    end-to-end. Full JSON is parsed (no truncation).
  </what-built>
  <how-to-verify>
    Run these on your workstation / cake-shaper. SAFE-21: every step is read-only or a code rsync; no
    RouterOS mutation, no service restart. The password is never placed on a command line.

    1. Deploy the patched code (operator-run rsync; D3 — NOT folded into a privileged secret path):
       ! ./scripts/deploy.sh spectrum cake-shaper --with-steering --dry-run   # preview the rsync first
       ! ./scripts/deploy.sh spectrum cake-shaper --with-steering             # rsync src/wanctl -> /opt/wanctl
       (Use the deploy form already in use for cake-shaper; the only goal is to rsync the patched
       src/wanctl tree to /opt/wanctl. Do NOT restart steering.service — SAFE-21.)

    2. Confirm the patched handler is actually deployed (must print a count >= 1):
       ! ssh cake-shaper 'grep -c _handle_script_print /opt/wanctl/routeros_rest.py'
       If this prints 0, STOP: the fix is not deployed; re-run step 1. Do not proceed to step 3.

    3. Stage the proof command file + harness on cake-shaper (read-only copy), then run the harness
       under steering.service's environment WITH `PYTHONPATH=/opt` so it imports the DEPLOYED wanctl
       under /opt/wanctl and resolves ROUTER_PASSWORD identically (password sourced from
       /etc/wanctl/secrets, never echoed, never on the command line). The harness loads the REAL
       /etc/wanctl/steering.yaml via SteeringConfig and asserts/prints that wanctl.__file__ and the
       imported routeros_rest module's __file__ are under /opt/wanctl:
       ! scp scripts/phase258-readonly-proof.py .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt cake-shaper:/tmp/
       ! ssh cake-shaper 'source /etc/wanctl/secrets; cd /opt/wanctl && PYTHONPATH=/opt python3 /tmp/phase258-readonly-proof.py /tmp/258-03-readonly-commands.txt'

       The `PYTHONPATH=/opt` is the load-bearing fix (it matches Environment=PYTHONPATH=/opt in
       steering.service); `cd /opt/wanctl` keeps WorkingDirectory parity with the daemon. If the harness
       prints `ACCESS02_PROOF_FAIL import-path` (an imported module is NOT under /opt/wanctl), STOP and
       fix the runtime — the proof is not pinned to the deployed code.

    Expected: the harness prints the resolved `wanctl.__file__` / `routeros_rest.__file__` paths (both
    under /opt/wanctl), confirms the config was loaded from /etc/wanctl/steering.yaml via SteeringConfig,
    and prints `ACCESS02_PROOF_PASS route=<n> netwatch=<n> script=<n>` with each count >= 1 — proving the
    three reads each returned exit 0 with non-empty parseable JSON THROUGH get_router_client, on the
    deployed fix, over the real steering config path. Capture those lines plus the redacted sample output
    as the proof evidence.

    SAFE-21: these are print/GET reads and a code rsync only. No set/add/remove/enable/disable, no
    Netwatch change, no steering.service restart.
  </how-to-verify>
  <resume-signal>Paste the deployed-handler grep count, the printed wanctl.__file__/routeros_rest.__file__ paths (under /opt/wanctl), the harness verdict line (ACCESS02_PROOF_PASS/FAIL), and the redacted counts — or report the failure mode</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the D4 ACCESS-02 proof evidence (with deployed-code + import-path + real-config provenance)</name>
  <read_first>
    - .planning/phases/258-read-only-routeros-access-repair/258-CONTEXT.md (D4 proof definition; ACCESS-03 residual to surface; D3 operator-deploy)
    - .planning/phases/258-read-only-routeros-access-repair/258-RESEARCH.md (Security Domain threat note — D2 accepted residual)
    - .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt (the validated command file)
    - .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-a1-preflight.md (the A1 gate outcome from Plan 01)
  </read_first>
  <action>
    Create `.planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md`
    recording the operator-run D4 result. Include:
    (1) A1 provenance: reference that A1 was confirmed in Plan 01 (258-01-a1-preflight.md) — this plan
    assumed A1-confirmed and contains no inline SSH fork.
    (2) Deployed-code confirmation: the `grep -c _handle_script_print /opt/wanctl/routeros_rest.py`
    count (>= 1), proving the proof ran against the patched fix, not stock code (review HIGH-3).
    (3) Transport provenance: the proof went through `get_router_client` via
    scripts/phase258-readonly-proof.py under steering.service's environment (review HIGH-2), NOT raw
    curl; full JSON parsed (no truncation).
    (4) IMPORT-PATH provenance: the harness ran with `PYTHONPATH=/opt` and the asserted
    `wanctl.__file__` / `routeros_rest.__file__` paths (both under /opt/wanctl) as printed by the
    harness — proving the proof exercised the DEPLOYED runtime, not a stray copy.
    (5) REAL-CONFIG provenance: the config was loaded from the REAL deployed `/etc/wanctl/steering.yaml`
    via `SteeringConfig(path)` (the same class + loader the daemon uses), NOT a synthetic/hardcoded
    config object — proving the supported steering config path (real loader, schema validation,
    ${ROUTER_PASSWORD} expansion) end-to-end.
    (6) The validated command-file reference + its READONLY_COMMANDS_VALIDATED line.
    (7) The harness verdict `ACCESS02_PROOF_PASS route=<n> netwatch=<n> script=<n>` and the redacted
    sample fields (sensitive values masked).
    (8) An explicit ACCESS-03 residual statement per D2, accurately scoped: read-only is enforced
    PROCEDURALLY (GET-only handlers on the transport + the validator on the proof-command file), NOT
    by RouterOS RBAC; the reused steering credential CAN write at the RouterOS RBAC layer.
    State the explicit verdict: ACCESS-02 PROVED (all three reads exit 0, non-empty, parseable through
    get_router_client against deployed code, over the real steering config, import-path pinned to
    /opt/wanctl) or ACCESS-02 BLOCKED with the reason. SAFE-21 holds: every recorded command is
    print/GET or a code rsync; no mutation, no Netwatch change, no service restart occurred. No secret
    value appears in the evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -qi "netwatch" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -qi "_handle_script_print\|deployed" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -qi "get_router_client" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -q "/opt/wanctl" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -qi "steering.yaml" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -Eqi "ACCESS-02 PROVED|ACCESS-02 BLOCKED" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && grep -Eqi "residual|RBAC|can write|procedural" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md && ! grep -Eqi "ROUTER_PASSWORD=[^$]|BEGIN [A-Z]*PRIVATE KEY" .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-access02-proof.md</automated>
  </verify>
  <acceptance_criteria>
    - `258-03-access02-proof.md` records the A1 provenance (resolved in Plan 01), the deployed-code grep confirmation, the get_router_client transport provenance, the import-path provenance (PYTHONPATH=/opt + wanctl.__file__/routeros_rest.__file__ under /opt/wanctl), the real-config provenance (loaded from /etc/wanctl/steering.yaml via SteeringConfig, not synthetic), the validated command-file reference, and the three-read verdict.
    - Contains an explicit `ACCESS-02 PROVED` (or `ACCESS-02 BLOCKED` with reason) verdict line.
    - States the ACCESS-03 D2 residual accurately (reused credential CAN write at RouterOS RBAC; read-only enforced PROCEDURALLY via GET-only handlers + the proof-command validator, not RBAC).
    - No mutating command, no service restart, and no secret value recorded (SAFE-21 + negative grep gate).
  </acceptance_criteria>
  <done>
    The D4 ACCESS-02 proof is recorded: all three reads succeeded through get_router_client against the
    deployed patched code over the real /etc/wanctl/steering.yaml config (import-path pinned to
    /opt/wanctl), the deployed-code confirmation, import-path provenance, and real-config provenance are
    explicit, the read-only enforcement is shown via the validated command file, and the ACCESS-03
    residual is accurately scoped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator keyboard -> cake-shaper | privileged credential reads + the code rsync cross here (`! <command>`, D3) |
| cake-shaper -> RouterOS REST (443) | the live inspection path exercised by the proof harness (GET only via get_router_client) |
| evidence/proof command-file -> validator | every proof command validated before run_cmd (procedural enforcement, D2) |
| reused steering credential -> RouterOS | credential CAN write at RouterOS RBAC (D2 accepted residual) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-01 | Elevation of privilege | reused steering credential able to mutate RouterOS (D2) | mitigate | Every proof command passes `readonly_validator` (anchored prefix + FORBIDDEN_ROUTEROS_ACTIONS) before run_cmd, and the harness only issues print/GET reads via the GET-only handlers; residual surfaced explicitly in the ACCESS-03 evidence, accurately scoped as procedural not RBAC. |
| T-258-05 | Information disclosure | ROUTER_PASSWORD / netwatch host data in evidence or process listings | mitigate | The harness resolves ROUTER_PASSWORD via the real config's ${ROUTER_PASSWORD} expansion (config loaded from /etc/wanctl/steering.yaml with /etc/wanctl/secrets sourced, the way steering does) and NEVER puts it on a command line (no `curl -u admin:$PW`, no `ps` leak); it never echoes the password; recorded evidence redacts sensitive fields; negative grep gate fails the evidence task if a secret/key block appears. |
| T-258-06 | Tampering | proof run against stock /opt/wanctl or a stray copy instead of the deployed fix | mitigate | TWO gates: a blocking deployed-code confirmation (`grep -c _handle_script_print /opt/wanctl/routeros_rest.py` >= 1) AND the harness's own runtime assertion that `wanctl.__file__`/`routeros_rest.__file__` resolve under `/opt/wanctl` (run with `PYTHONPATH=/opt`) — emitting `ACCESS02_PROOF_FAIL import-path` and exiting non-zero otherwise. The import-path assertion strengthens this threat's mitigation: the proof cannot silently exercise a stray/stale wanctl copy. If either gate fails the operator STOPS before running/recording the proof. |
| T-258-07 | Tampering | accidental mutation during a live proof | mitigate | Command file is validator-gated; harness issues only print/GET via run_cmd; checkpoint instructions are GET-only; SAFE-21 forbids set/add/remove/enable/disable and steering.service restart. |
| T-258-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; the harness uses only stdlib + the already-vendored wanctl modules (imported from the deployed /opt/wanctl tree). |
</threat_model>

<verification>
- `.venv/bin/python -m wanctl.readonly_validator .planning/phases/258-read-only-routeros-access-repair/evidence/258-03-readonly-commands.txt` prints `READONLY_COMMANDS_VALIDATED`.
- `.venv/bin/ruff check scripts/phase258-readonly-proof.py` exits 0; the harness goes through `get_router_client`, loads the real config via `SteeringConfig("/etc/wanctl/steering.yaml")`, and asserts `__file__` under `/opt/wanctl` (grep-confirmed); the mocked `run_proof` test passes (if present).
- Deployed-code confirmation: `grep -c _handle_script_print /opt/wanctl/routeros_rest.py` on cake-shaper returns >= 1 before the proof.
- Import-path provenance: the harness, run with `PYTHONPATH=/opt`, prints `wanctl.__file__`/`routeros_rest.__file__` under `/opt/wanctl` (no `ACCESS02_PROOF_FAIL import-path`).
- Real-config provenance: the harness loaded `/etc/wanctl/steering.yaml` via `SteeringConfig(path)`, not a synthetic config.
- D4 live proof: the harness prints `ACCESS02_PROOF_PASS route=<n> netwatch=<n> script=<n>` with each count >= 1, recorded in 258-03-access02-proof.md.
- `258-03-access02-proof.md` carries an explicit ACCESS-02 verdict, the get_router_client transport provenance, the import-path provenance (/opt/wanctl), the real-config provenance (steering.yaml via SteeringConfig), the deployed-code confirmation, and the accurately-scoped ACCESS-03 residual.
</verification>

<success_criteria>
ACCESS-02 proven by a live multi-read D4 proof (route + netwatch + script, each exit 0, parseable)
THROUGH `get_router_client` via the proof harness, loading the REAL `/etc/wanctl/steering.yaml` via
`SteeringConfig(path)` (not a synthetic config), running with `PYTHONPATH=/opt` and asserting its
imported wanctl/routeros_rest modules resolve under `/opt/wanctl`, against the patched code DEPLOYED on
/opt/wanctl (deployed-code confirmed). ACCESS-03/SAFE-21 held: every live command passed the
generalized, anchored read-only validator and the harness issued only print/GET reads; full JSON
parsed (no truncation); no password on any command line; no mutation, Netwatch change, or service
restart. The A1 gate was resolved in Plan 01 (no inline SSH fork). The accepted D2 residual is
documented and accurately scoped as procedural, not RBAC.
</success_criteria>

<output>
Create `.planning/phases/258-read-only-routeros-access-repair/258-03-SUMMARY.md` when done.
</output>
</output>
</invoke>
