---
phase: 260-dry-run-observation-rerun-canary-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase260-observation.py
  - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt
autonomous: true
requirements: [OBSERVE-01, OBSERVE-02, OBSERVE-03, SAFE-21]

must_haves:
  truths:
    - "The harness validates every live RouterOS command against the read-only allowlist BEFORE constructing the live client, and prints the negative self-test token."
    - "The harness samples :9102/health ownership_inspection across a bounded ~10-min window and gates each sample on inspector_status==ok AND match==true."
    - "Any single bad sample (inspector_status!=ok, match==false, non-advancing last_inspected_at) forces OBSERVE_VERDICT: not-ready (fail-closed)."
    - "The harness takes one independent direct read-only RouterOS cross-check (netwatch + default routes) and records a divergence if it disagrees with ownership_inspection."
    - "The harness renders a standing per-route intent table (active_owner + reconciliation route targets) vs live default_routes[] WITHOUT forcing a failover."
    - "The harness emits the single greppable verdict line plus SAFE-21 booleans, and scans the issued-command transcript for mutation tokens."
  artifacts:
    - path: "scripts/phase260-observation.py"
      provides: "Reusable multi-sample bounded-window read-only observation harness + 257-shaped readiness-packet renderer"
      contains: "OBSERVE_VERDICT"
      min_lines: 200
    - path: ".planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt"
      provides: "Validated read-only command file (COMMAND:-prefixed) for the live cross-check"
      contains: "COMMAND:"
  key_links:
    - from: "scripts/phase260-observation.py"
      to: "wanctl.readonly_validator.validate_command"
      via: "validate_commands_before_run gate before live client construction"
      pattern: "validate_commands_before_run"
    - from: "scripts/phase260-observation.py"
      to: ":9102/health ownership_inspection"
      via: "per-sample HTTP read of the steering health endpoint"
      pattern: "ownership_inspection"
    - from: "scripts/phase260-observation.py"
      to: "wanctl.routeros_rest cross-check"
      via: "get_router_client + STATIC_READ_COMMANDS direct read"
      pattern: "STATIC_READ_COMMANDS"
---

<objective>
Build `scripts/phase260-observation.py` — the reusable, deployed, read-only observation harness that reruns the v1.56-blocked bounded dry-run. It generalizes `scripts/phase259-ownership-proof.py` from a single inspector refresh to a bounded ~10-min multi-sample window, adds one independent direct RouterOS read-only cross-check, renders a standing per-route intent-vs-live table, assembles the three-class divergence union, and emits a 257-shaped readiness verdict. Also write the validated read-only command file the harness consumes.

Purpose: This harness is the engine that produces the OBSERVE-01/02/03 evidence. It must be re-runnable before any future canary and testable offline, so it must NOT depend on live state for its structural correctness.
Output: `scripts/phase260-observation.py` and `evidence/phase260-readonly-commands.txt`.

SAFE-21: This harness is read-only end-to-end. It issues only GET/print commands validated against the allowlist. It MUST NOT mutate routes, Netwatch, scripts, CAKE/qdisc, or flip the route owner. `ready-for-approval` is a verdict only — it must NOT chain into any approval, mutation, or canary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-CONTEXT.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md

<interfaces>
<!-- Extracted contracts the executor needs. Use these directly; do not re-explore. -->

From scripts/phase259-ownership-proof.py (the analog to generalize):
- `DEPLOYED_ROOT = Path("/opt/wanctl").resolve()`; prepend its parent to sys.path before importing wanctl.
- Imports: `from wanctl.readonly_validator import iter_commands, validate_command`; `from wanctl.router_client import get_router_client`; `from wanctl.steering.daemon import SteeringConfig`; `from wanctl.steering.route_manager import RouteManager`; `from wanctl.steering.route_ownership_inspector import ROUTE_PRINT, RouteOwnershipInspector`.
- `DEFAULT_CONFIG = Path("/etc/wanctl/steering.yaml")`.
- `STATIC_READ_COMMANDS = ("/tool netwatch print detail", "/system script print detail", ROUTE_PRINT)`.
- `validate_commands_before_run(client, commands)` iterates `validate_command(command)` (raises ValueError on any mutating/forbidden command). The passed `_NoRunClient.run_cmd` raises `AssertionError("validate_commands_before_run must not execute commands")`.
- `_assert_deployed_imports()` prints `wanctl.__file__=...`, asserts modules resolve under `/opt/wanctl`, exits 1 with greppable token otherwise.
- `_route_manager_from_config(config, client)` builds `RouteManager(enabled=..., mode=..., routes=config.route_management_routes, router_client=client, ownership_guard_result=None)`.
- `main()`: parse `command_file` + `--config`; `_assert_deployed_imports()`; `commands = iter_commands(args.command_file)`; `validate_commands_before_run(_NoRunClient(), commands)`; build client via `get_router_client(config, logger)`; `try/finally` calls `client.close()`.

From src/wanctl/readonly_validator.py:
- `iter_commands(path) -> list[str]` — reads `COMMAND:`-prefixed lines, raises ValueError otherwise.
- `validate_command(command)` — raises ValueError("...mutating action..."/shell-metachar/not-read-only-object).
- `validate_path(path)` prints `READONLY_COMMANDS_VALIDATED`; `self_test()` prints `READONLY_COMMANDS_NEGATIVE_SELF_TEST_PASSED`.
- `READ_ONLY_ROUTEROS_OBJECTS` includes `/ip route print`, `/tool netwatch print`, `/system script print` (+ slash forms).

From src/wanctl/steering/health.py ownership_inspection section (sampled per tick):
```
ownership_inspection = {
  "observed_owner": str,          # expect "netwatch"
  "configured_owner": str,        # expect "netwatch"
  "match": bool,                  # D-01 authoritative; expect True every sample
  "inspector_status": str,        # D-01 authoritative; expect "ok" every sample
  "inspector_error": str | None,
  "last_inspected_at": str | None,# must ADVANCE across refreshes (staleness check)
  "netwatch": {"entries_count": int, "route_mutating_active_count": int},
  "routes": {"total_route_count": int,
             "default_routes": [{"gateway": str|None, "disabled": bool,
                                 "distance": int|None, "comment": str|None}]},
}
```

From src/wanctl/steering/health.py route_management section (SUPPLEMENTARY signal, D-01):
- Keys: enabled, mode, active_owner, active_allowed, guard.status, reconciliation.{status,route_count}, circuit_breaker.open, last_intended_action, last_applied_action, rollback_ready.
- Harness gates only that `guard.status` is NOT circuit-open/hard-fail — NOT that it equals "ok".

From src/wanctl/steering/route_manager.py:
- `status_snapshot()` returns active_owner (via `_active_owner`, "netwatch" unless mode=="active" and allowed), reconciliation.route_count, and reconciliation.routes map.
- `reconcile_startup()` resolves each configured `self.routes` key into `RouteState(key, route_id, disabled, anchor_type, anchor_value)`.

Expected live baseline (259 proof): observed_owner=netwatch, configured_owner=netwatch, match=True, total_routes=17, default_routes=4. A deviation is a divergence to RECORD, not silently accept.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness skeleton — deployed-import bootstrap, validate-before-run gate, live client, sampling loop</name>
  <read_first>
    - scripts/phase259-ownership-proof.py (the analog to generalize — read whole)
    - src/wanctl/readonly_validator.py (iter_commands, validate_command, self_test, validate_path)
    - src/wanctl/router_client.py (get_router_client factory dispatch)
    - src/wanctl/steering/daemon.py (SteeringConfig: route_management_enabled/mode/routes, router_transport)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md (sections 1-5, 7)
  </read_first>
  <files>scripts/phase260-observation.py</files>
  <action>
    Create `scripts/phase260-observation.py`. Copy verbatim from the analog: the `/opt/wanctl` sys.path bootstrap, the import block, `DEFAULT_CONFIG`, `STATIC_READ_COMMANDS`, the `RouterClientLike`/`RouteManagerLike` Protocols, `validate_commands_before_run`, `_assert_deployed_imports`, `_route_manager_from_config`, and the `_NoRunClient` class (its `run_cmd` MUST keep raising `AssertionError("validate_commands_before_run must not execute commands")`).

    Add a `sample_health(url)` helper that GETs `:9102/health` (default `http://127.0.0.1:9102/health`, expose via `--health-url`) using stdlib `urllib.request`, parses JSON, and returns the `ownership_inspection` and `route_management` sub-dicts. On HTTP/parse error it returns a sentinel that the gate treats as a bad sample (fail-closed).

    Add a `gate_sample(ownership_inspection, route_management, prev_last_inspected_at)` function implementing D-01/D-02: returns `(ok: bool, blocker: str|None)`. A sample is good ONLY if `ownership_inspection["inspector_status"] == "ok"` AND `ownership_inspection["match"] is True` AND `last_inspected_at` is non-None. Treat `route_management["guard"]["status"]` and `circuit_breaker["open"]` as SUPPLEMENTARY: a circuit-open or hard-fail guard is a blocker, but guard.status not being literally "ok" is NOT. Detect a non-advancing `last_inspected_at` across consecutive samples as a stuck-inspector blocker.

    In `main()`: parse positional `command_file`, `--config` (default DEFAULT_CONFIG), and discretionary flags `--health-url`, `--window-sec` (default 636), `--interval-sec` (default 60), `--evidence-dir`. Call `_assert_deployed_imports()`; `commands = iter_commands(args.command_file)`; `validate_commands_before_run(_NoRunClient(), commands)` BEFORE building the live client. Also call `readonly_validator.validate_path(args.command_file)` and `readonly_validator.self_test()` so the transcript carries `READONLY_COMMANDS_VALIDATED` and `READONLY_COMMANDS_NEGATIVE_SELF_TEST_PASSED` before any live execution. Build the client via `get_router_client(config, logger)` inside a `try/finally` that calls `client.close()`.

    Do NOT yet implement the cross-check, the per-route table, or the packet — those are Tasks 2 and 3. For now, the sampling loop collects per-tick samples and computes a provisional verdict; leave a clearly-marked extension point.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('p260', pathlib.Path('scripts/phase260-observation.py')); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert hasattr(m,'validate_commands_before_run') and hasattr(m,'gate_sample') and hasattr(m,'_NoRunClient')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase260-observation.py` imports cleanly and exposes `validate_commands_before_run`, `gate_sample`, `sample_health`, `_assert_deployed_imports`, `_route_manager_from_config`, `_NoRunClient`, `main`.
    - `_NoRunClient.run_cmd` raises `AssertionError("validate_commands_before_run must not execute commands")`.
    - `gate_sample` returns `(False, <blocker>)` when `inspector_status != "ok"` OR `match is not True`; returns `(True, None)` only on a clean sample.
    - `validate_commands_before_run(_NoRunClient(), commands)` is called in `main()` BEFORE `get_router_client` is invoked (source ordering, confirmable by reading the function body).
    - No fenced RouterOS mutation verbs (` set `/` add `/` enable `/` disable `/` run `/` remove `) appear as issued commands anywhere in the script.
  </acceptance_criteria>
  <done>Harness skeleton exists with the validate-before-live gate, the D-01/D-02 sample gate, and the bounded sampling loop; imports clean offline.</done>
</task>

<task type="auto">
  <name>Task 2: Independent RouterOS cross-check (D-04) + standing per-route intent table (D-05) + divergence union (D-07)</name>
  <read_first>
    - scripts/phase260-observation.py (the file being extended — read current state)
    - src/wanctl/routeros_rest.py (_handle_netwatch_print, _handle_route_print — the read-only GET handlers)
    - src/wanctl/steering/route_manager.py lines 130-185, 280-315 (reconcile_startup, status_snapshot, _active_owner)
    - src/wanctl/steering/route_ownership_inspector.py (snapshot shape, ROUTE_PRINT)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md (sections 6, 7; Shared Patterns)
  </read_first>
  <files>scripts/phase260-observation.py</files>
  <action>
    Extend `scripts/phase260-observation.py`.

    Add `cross_check(client)` (D-04): issue the allowlisted `STATIC_READ_COMMANDS` directly through `client.run_cmd` (these dispatch to `routeros_rest._handle_netwatch_print` and `_handle_route_print` — pure GET against `/tool/netwatch` and `/ip/route`). Parse the netwatch entries and default-route rows. Return a structure the verdict logic compares against the daemon's `ownership_inspection`. A mismatch on observed default-route count, gateways, or netwatch route-mutating-active count vs the sampled `ownership_inspection.routes` / `.netwatch` is a recorded divergence (D-07 class 3) — NOT a remediation trigger.

    Add `standing_intent_table(route_manager, ownership_inspection)` (D-05): build wanctl's standing intent from `route_manager.status_snapshot()` (`active_owner` + reconciliation `routes` map / route targets/distances) — construct `RouteManager` exactly as the analog's `_route_manager_from_config`. Render a per-route comparison of wanctl standing intent vs live `ownership_inspection.routes.default_routes[]` (gateway / disabled / distance / comment per route). Any per-route mismatch on target/distance/owner is a recorded divergence (D-07 class 2). Do NOT drive a failover or any action that would make `last_intended_action` non-null — D-05 explicitly rejects that as bordering SAFE-21.

    Add `assemble_divergences(samples, cross_check_result, table)` (D-07): the union of three classes — (1) any sample with `inspector_status != "ok"` or `match == false` or non-advancing `last_inspected_at`; (2) any per-route intended-vs-live mismatch; (3) any cross-check disagreement. Each divergence is a dict carrying its observed values for the packet. Also implement a `scan_mutation_tokens(issued_commands)` returning the list of any forbidden-action hits (mirrors `readonly_validator.FORBIDDEN_ROUTEROS_ACTIONS`); for a clean read-only run this MUST be `[]`.

    Wire these into the verdict computation: `OBSERVE_VERDICT: ready-for-approval` requires an empty divergence union AND an all-clean window AND an empty mutation-token list; otherwise `not-ready` with the divergences recorded as concrete blockers (fail-closed, D-10).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('p260', pathlib.Path('scripts/phase260-observation.py')); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert all(hasattr(m,n) for n in ('cross_check','standing_intent_table','assemble_divergences','scan_mutation_tokens'))"</automated>
  </verify>
  <acceptance_criteria>
    - `cross_check`, `standing_intent_table`, `assemble_divergences`, `scan_mutation_tokens` are defined and importable.
    - `cross_check` issues only the three `STATIC_READ_COMMANDS` (all pass `validate_command`); it performs no mutation and no route-owner flip.
    - `standing_intent_table` reads `route_manager.status_snapshot()` and live `ownership_inspection.routes.default_routes[]`; it never calls a failover/apply path (no `last_intended_action`-forcing call).
    - `assemble_divergences` returns the union of the three D-07 classes, each carrying observed values.
    - `scan_mutation_tokens([])` returns `[]`; a list containing `/ip route disable 0` returns a non-empty hit list.
    - Verdict is `ready-for-approval` only when divergences AND mutation-token hits are both empty AND the window is all-clean; any divergence forces `not-ready`.
  </acceptance_criteria>
  <done>Cross-check, standing-intent table, divergence union, and mutation-token scan are wired into a fail-closed verdict computation.</done>
</task>

<task type="auto">
  <name>Task 3: 257-shaped readiness-packet renderer + verdict tokens + raw/transcript emission + command file</name>
  <read_first>
    - scripts/phase260-observation.py (the file being extended — read current state)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/evidence/phase257-readiness-packet-20260620T122132Z.md (the format template, D-09)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/evidence/phase257-readonly-commands-20260620T120700Z.txt (the command-file template)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md (evidence bundle section; readiness packet structure 1-8)
  </read_first>
  <files>scripts/phase260-observation.py, .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt</files>
  <action>
    Extend `scripts/phase260-observation.py` with a `render_packet(...)` that writes the 257-shaped readiness packet into `--evidence-dir`, plus the raw JSON dump and the human transcript. Use a `YYYYMMDDTHHMMSSZ` UTC timestamp convention for filenames (operator discretion on exact names; mirror 257's four-artifact set: packet `.md`, raw `.json`, transcript `.md`, and the command file `.txt` consumed at runtime).

    The packet copies 257's section order verbatim (D-09): (1) Header with `Verdict:` + `OBSERVE_VERDICT` token + the SAFE-21 boolean block; (2) Evidence Inputs pointing at the four 260 artifacts + Phase 256 rollback anchors (`/var/lib/wanctl/phase256-backups/20260620T033704Z`) + the plan summary; (3) Readiness Criteria Matrix (criterion/observed/status/readiness-impact) — the renderer must produce the two now-passing rows that replace 257's failures: the old `guard.status=error` row becomes gated on `ownership_inspection.inspector_status=ok` + `match=true` (D-01, pass when clean), and the old "live route/Netwatch inventory returned 255 / router.key not accessible" row becomes "REST read-only inventory succeeded" (pass when clean); (4) Intended vs Live Summary rendering the D-05 standing-intent table; (5) SAFE-21 No-Mutation Proof (the boolean block + "only the predeclared COMMAND: lines were issued" + the two validator tokens; rename 257's SAFE-20 -> SAFE-21); (6) Rollback Evidence (Phase 256 anchor path); (7) Blockers and Remediation (each D-07 divergence as a concrete blocker with observed values when not-ready; when ready-for-approval, state none AND that this is NOT approval per SAFE-21/D-10); (8) Next Recommendation (Netwatch stays owner; a separate future milestone may request canary approval).

    The packet body must include a sentence stating it SUPERSEDES the Phase 257 not-ready packet.

    Emit the final greppable verdict block to stdout exactly: `OBSERVE_VERDICT: ready-for-approval` (or `not-ready`), then `APPROVED_ACTIVE_CANARY: false`, `NETWATCH_REMAINS_OWNER: true`, `NO_ROUTE_OWNER_FLIP: true`, `NO_ROUTEROS_ROUTE_MUTATION: true`, `NO_NETWATCH_MUTATION: true`, `NO_CAKE_QDISC_CHANGE: true`, and `MUTATION_TOKEN_HITS: []` (or the hit list).

    Separately, write `.planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt` as the runtime command file: `COMMAND:`-prefixed read-only prints — `/tool netwatch print detail`, `/system script print detail`, `/ip route print detail`. Each line MUST pass `validate_command`. Do NOT include any of 257's SSH-key lines that returned 255; this phase uses the 258-proven REST path.
  </action>
  <verify>
    <automated>.venv/bin/python -m wanctl.readonly_validator .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt && .venv/bin/python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('p260', pathlib.Path('scripts/phase260-observation.py')); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert hasattr(m,'render_packet')"</automated>
  </verify>
  <acceptance_criteria>
    - `render_packet` is defined and importable.
    - `evidence/phase260-readonly-commands.txt` exists, contains only `COMMAND:`-prefixed read-only prints, and `python -m wanctl.readonly_validator <file>` prints `READONLY_COMMANDS_VALIDATED` (exit 0).
    - The command file contains NO SSH-key lines and NO mutation verbs.
    - The packet renderer emits the exact verdict tokens `OBSERVE_VERDICT: ready-for-approval|not-ready`, `APPROVED_ACTIVE_CANARY: false`, `NETWATCH_REMAINS_OWNER: true`, `NO_ROUTE_OWNER_FLIP: true`, plus the no-mutation booleans and `MUTATION_TOKEN_HITS:`.
    - The rendered packet includes a "supersedes the Phase 257 not-ready packet" statement and the two updated matrix rows (inspector_status/match authoritative; REST inventory succeeded).
    - The packet states, when ready-for-approval, that this is NOT approval and no active canary is requested (SAFE-21/D-10).
  </acceptance_criteria>
  <done>Packet renderer, verdict-token block, raw/transcript emission, and the validated read-only command file all exist; the command file passes the deployed validator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → RouterOS (REST :443) | Read-only commands cross into the production router; only GET/print is permitted. |
| command-file → validator | Untrusted command text validated before any live execution. |
| harness → :9102/health | Daemon health JSON consumed as the authoritative readiness signal. |
| secrets (ROUTER_PASSWORD / REST creds) → harness | Credentials loaded from service-equivalent env / config to reach the router. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-01 | Tampering | read-only allowlist bypass / mutation command slips through validator | mitigate | Every live command passes `validate_command` via `validate_commands_before_run(_NoRunClient(), commands)` BEFORE the live client is constructed; `_NoRunClient.run_cmd` raises on any execution attempt; negative self-test (`self_test`) printed before live run; `scan_mutation_tokens` re-scans the issued transcript and must yield `MUTATION_TOKEN_HITS: []`. |
| T-260-02 | Information Disclosure | ROUTER_PASSWORD / REST credentials handled by the harness | mitigate | Secrets loaded only via `SteeringConfig`/service-equivalent env (same path as 259); never written into evidence artifacts; `client.close()` in `try/finally` so no session leaks; evidence dumps contain only command text + parsed read-only state, never the credential. |
| T-260-03 | Spoofing/Elevation | false-positive `ready-for-approval` from a stale/cached inspector or non-fail-closed error path | mitigate | D-01/D-02 gate every sample on `inspector_status==ok` AND `match==true`; non-advancing `last_inspected_at` is a stuck-inspector blocker; independent D-04 cross-check must agree; any single bad sample or any divergence forces `not-ready` (fail-closed). |
| T-260-04 | Elevation of Privilege | `ready-for-approval` chaining into an approval/mutation/canary | accept→mitigate | The harness emits a verdict string only; it has no apply/failover/owner-flip code path; the packet explicitly states `ready-for-approval` is NOT approval (SAFE-21/D-10). No mitigation code beyond the absence of any mutation path is required. |
| T-260-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase; harness uses only stdlib + deployed `wanctl`. No package legitimacy gate required. |
</threat_model>

<verification>
- `.venv/bin/python -m wanctl.readonly_validator .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt` prints `READONLY_COMMANDS_VALIDATED`.
- The harness imports cleanly offline and exposes all named functions (gate, cross_check, table, divergences, mutation scan, packet renderer).
- No mutation verbs are issued anywhere in the harness or command file.
</verification>

<success_criteria>
- `scripts/phase260-observation.py` generalizes the 259 analog to a bounded multi-sample window with the validate-before-live gate, the D-01/D-02 fail-closed sample gate, the D-04 cross-check, the D-05 standing-intent table, the D-07 divergence union, the mutation-token scan, and the 257-shaped packet renderer emitting the exact verdict tokens.
- `evidence/phase260-readonly-commands.txt` exists and passes the deployed validator.
- SAFE-21 holds: no mutation path exists; `ready-for-approval` is verdict-only.
</success_criteria>

<output>
Create `.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-01-SUMMARY.md` when done.
</output>
