---
phase: 260-dry-run-observation-rerun-canary-readiness
plan: 03
type: execute
wave: 3
depends_on: ["260-01", "260-02"]
files_modified:
  - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md
  - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-raw.json
  - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-transcript.md
autonomous: false
requirements: [OBSERVE-01, OBSERVE-02, OBSERVE-03, SAFE-21]

must_haves:
  truths:
    - "The harness runs live from the production steering host (cake-shaper) against the real :9102/health and the 258-proven REST path, exercising deployed /opt/wanctl code."
    - "The live bounded ~10-min window samples ownership_inspection; the verdict is computed from real samples (D-01/D-02)."
    - "The four-artifact evidence bundle (packet .md, raw .json, transcript .md, command file .txt) lands under the phase evidence/ dir, mirroring Phase 257."
    - "The readiness packet emits an explicit OBSERVE_VERDICT: ready-for-approval|not-ready, the SAFE-21 booleans, and states it SUPERSEDES the Phase 257 not-ready packet."
    - "If ready-for-approval, the packet states this is NOT approval and no active canary is started; Netwatch remains owner."
  artifacts:
    - path: ".planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md"
      provides: "The canary-readiness decision packet (OBSERVE-03 deliverable), 257-shaped, superseding 257"
      contains: "OBSERVE_VERDICT"
    - path: ".planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-raw.json"
      provides: "Per-sample raw dump of every ownership_inspection snapshot + cross-check"
    - path: ".planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-transcript.md"
      provides: "Human transcript of issued read-only commands + validator tokens"
  key_links:
    - from: "operator on cake-shaper"
      to: "scripts/phase260-observation.py"
      via: "live run against :9102/health + 258-proven REST cross-check"
      pattern: "phase260-observation"
    - from: "evidence/phase260-readiness-packet.md"
      to: "Phase 257 not-ready packet"
      via: "explicit supersession statement + two now-passing matrix rows"
      pattern: "supersede"
---

<objective>
Run the observation harness LIVE from the production steering host (`cake-shaper`) over a bounded ~10-min window, capture the real four-artifact evidence bundle, and finalize the canary-readiness decision packet. This is the OBSERVE-01/02/03 deliverable: the rerun of the v1.56-blocked dry-run observation, now that 258 (REST read-only path) and 259 (live `ownership_inspection`) made it possible.

Purpose: Produce the authoritative `ready-for-approval` | `not-ready` packet that supersedes the Phase 257 not-ready packet, with the two previously-failing matrix rows now passing under the new authoritative signal.
Output: `phase260-readiness-packet.md`, `phase260-observation-raw.json`, `phase260-observation-transcript.md` (the command file from 260-01 completes the four-artifact set).

SAFE-21: The live run is read-only. Netwatch remains the active owner through closeout. No route mutation, Netwatch change, CAKE/qdisc change, or owner flip. A `ready-for-approval` verdict is informational only and MUST NOT chain into approval or a canary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-CONTEXT.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md
@.planning/phases/259-read-only-netwatch-route-ownership-inspection/259-02-SUMMARY.md
@.planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/evidence/phase257-readiness-packet-20260620T122132Z.md

<deployment_notes>
- Production steering runs on `cake-shaper` and exposes `:9102/health` with the `ownership_inspection` section live (259-02 proof: observed_owner=netwatch, match=True, total_routes=17, default_routes=4).
- wanctl@ controllers are disabled in prod (both WANs on cake-autorate); steering consumes bridge-written state. The harness must run FROM cake-shaper to reach `127.0.0.1:9102` and the 258-proven REST path to RouterOS.
- The harness must exercise DEPLOYED code under `/opt/wanctl` (`_assert_deployed_imports` enforces this) — so `scripts/phase260-observation.py` and the validated command file must be present on cake-shaper before the live run. Confirm whether a deploy step (operator-confirmed) is needed, mirroring the Phase 258-03 deploy-then-prove pattern.
- Phase 256 rollback anchors: `/var/lib/wanctl/phase256-backups/20260620T033704Z`.
</deployment_notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs the live bounded observation on cake-shaper</name>
  <read_first>
    - scripts/phase260-observation.py (the harness — confirm CLI flags before running)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt (the validated command file)
    - .planning/phases/259-read-only-netwatch-route-ownership-inspection/259-02-SUMMARY.md (the live proof + how 259 was run on cake-shaper)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/257-01-SUMMARY.md (the 257 background-supervision flakiness the harness avoids)
  </read_first>
  <files>(operator-run; no repo file written in this task)</files>
  <action>
    OPERATOR-EXECUTED checkpoint (non-autonomous): the live production sample on cake-shaper must be run by the operator, not the agent. Run scripts/phase260-observation.py against the real :9102/health and the 258-proven REST path over the bounded ~10-min window, then return the verdict block. See <how-to-verify> for the exact command and confirmation steps.
  </action>
  <what-built>
    The reusable read-only observation harness (`scripts/phase260-observation.py`) and validated command file (260-01), proven offline by the test slice (260-02). The harness needs a single live bounded run from the production steering host to capture real `ownership_inspection` samples + the independent RouterOS cross-check.
  </what-built>
  <how-to-verify>
    1. Ensure the harness + command file are present under the deployed location on `cake-shaper` (mirror the 258-03 operator-confirmed deploy to `/opt/wanctl` / scripts location; `_assert_deployed_imports` will fail loudly if imports do not resolve under `/opt/wanctl`).
    2. From `cake-shaper`, run the harness against the real endpoint, e.g.:
       python3 scripts/phase260-observation.py PHASE260_COMMAND_FILE --config /etc/wanctl/steering.yaml --health-url http://127.0.0.1:9102/health --window-sec 636 --interval-sec 60 --evidence-dir PHASE260_EVIDENCE_DIR
    3. Confirm the harness prints wanctl.__file__=/opt/wanctl/..., then READONLY_COMMANDS_VALIDATED and READONLY_COMMANDS_NEGATIVE_SELF_TEST_PASSED BEFORE any live command.
    4. Let the ~10-min window complete; confirm it emits the greppable verdict block (OBSERVE_VERDICT, the SAFE-21 booleans, MUTATION_TOKEN_HITS: []).
    5. Confirm the four evidence artifacts were written (packet .md, raw .json, transcript .md, command file .txt).
    6. Confirm no mutation occurred: only the predeclared COMMAND: lines were issued; Netwatch still owns the routes; :9102/health route_management.last_applied_action is still null.
  </how-to-verify>
  <acceptance_criteria>
    - The harness ran live on cake-shaper exercising /opt/wanctl deployed code (printed wanctl.__file__ under /opt/wanctl).
    - The validator tokens printed before any live command.
    - The window completed and emitted a single OBSERVE_VERDICT line plus the SAFE-21 boolean block and MUTATION_TOKEN_HITS: [].
    - The raw JSON + transcript artifacts were written and contain the per-sample ownership_inspection snapshots + the cross-check.
    - No route mutation / Netwatch change / owner flip occurred (Netwatch still owner).
  </acceptance_criteria>
  <resume-signal>Paste the harness verdict block + confirm the four evidence artifacts exist, or describe any failure / divergence observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Finalize and commit the readiness packet (supersede 257) + reconcile expected baseline</name>
  <read_first>
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md (the packet the harness wrote — read whole)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-raw.json (the real samples)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/evidence/phase257-readiness-packet-20260620T122132Z.md (the format + the two rows being superseded)
    - .planning/phases/258-read-only-routeros-access-repair/258-VERIFICATION.md (ACCESS02_PROOF_PASS route=17 netwatch=3 script=20)
  </read_first>
  <files>.planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md, .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-raw.json, .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-observation-transcript.md</files>
  <action>
    Review the harness-written packet against the real raw samples. Confirm it: (a) carries the explicit OBSERVE_VERDICT verdict matching the samples; (b) states it SUPERSEDES the Phase 257 not-ready packet; (c) shows the two previously-failing matrix rows now passing — the guard-status row now gated on ownership_inspection.inspector_status=ok + match=true (D-01), and the live-inventory row now "REST read-only inventory succeeded" citing 258's ACCESS02_PROOF_PASS route=17 netwatch=3 script=20; (d) renders the D-05 standing-intent table from the real samples; (e) records every D-07 divergence (if not-ready) as a concrete blocker with observed values, or (if ready-for-approval) states none AND that this is NOT approval (SAFE-21/D-10); (f) points Evidence Inputs at the four real 260 artifacts + Phase 256 rollback anchors.

    Reconcile against the expected baseline (259 proof: observed_owner=netwatch, match=True, total_routes=17, default_routes=4). If the live run deviates from this baseline, that deviation MUST be RECORDED in the packet as a divergence (D-07), not silently accepted — confirm the harness captured it and the verdict reflects it.

    Fix any rendering gaps in the packet text directly (the deliverable is the packet, not re-running the harness). Do NOT alter the verdict to be more favorable than the samples support — fail-closed and honest not-ready is a successful safe outcome (D-10).

    Commit the four-artifact evidence bundle (the command file from 260-01 plus the packet/raw/transcript from this plan).
  </action>
  <verify>
    <automated>grep -Eq '^OBSERVE_VERDICT: (ready-for-approval|not-ready)' .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md && grep -iq 'supersede' .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md && grep -q 'NETWATCH_REMAINS_OWNER: true' .planning/phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readiness-packet.md</automated>
  </verify>
  <acceptance_criteria>
    - The packet's `OBSERVE_VERDICT:` line is `ready-for-approval` or `not-ready` and matches what the real samples support (no upgrade beyond the evidence).
    - The packet contains an explicit "supersedes the Phase 257 not-ready packet" statement.
    - The packet shows the two previously-failing matrix rows now passing (inspector_status/match authoritative per D-01; REST inventory succeeded citing 258).
    - The packet's SAFE-21 boolean block is present (`APPROVED_ACTIVE_CANARY: false`, `NETWATCH_REMAINS_OWNER: true`, `NO_ROUTE_OWNER_FLIP: true` + no-mutation booleans).
    - If verdict is ready-for-approval, the packet states it is NOT approval and no active canary is started.
    - Any deviation from the 259 baseline is recorded as a D-07 divergence, not silently accepted.
    - The four-artifact evidence bundle is committed.
  </acceptance_criteria>
  <done>The readiness packet is finalized, supersedes 257, matches the real samples, holds SAFE-21, and the evidence bundle is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness (on cake-shaper) → RouterOS REST :443 | Live read-only commands cross into the production router; only GET/print permitted. |
| harness → :9102/health | Live daemon health consumed as the authoritative readiness signal. |
| secrets (ROUTER_PASSWORD) → harness | Production credential loaded from /etc/wanctl config to reach the router. |
| verdict → operator decision | `ready-for-approval` must not auto-chain into a canary or owner flip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-L1 | Tampering | a mutation command issued during the live run | mitigate | Only the predeclared validated `COMMAND:` lines are issued; harness validates before any live client; transcript scan emits `MUTATION_TOKEN_HITS: []`; operator confirms Netwatch still owner + `last_applied_action` null post-run. |
| T-260-L2 | Information Disclosure | ROUTER_PASSWORD leaking into evidence artifacts | mitigate | Evidence dumps contain parsed read-only state + command text only, never the credential; `client.close()` in try/finally; operator reviews artifacts before commit. |
| T-260-L3 | Spoofing | a stale/cached inspector yields a false `ready-for-approval` on the live run | mitigate | D-01/D-02 gate every live sample (`inspector_status==ok` AND `match==true`, advancing `last_inspected_at`); D-04 independent REST cross-check must agree; any divergence forces `not-ready`. |
| T-260-L4 | Elevation of Privilege | `ready-for-approval` triggering an active canary / owner flip | mitigate | Verdict is informational; the packet explicitly states it is NOT approval and Netwatch remains owner; the active canary is a separate future operator-gated milestone (SAFE-21/D-10). The checkpoint is `gate="blocking"` and non-autonomous. |
| T-260-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- The packet's `OBSERVE_VERDICT:` line, supersession statement, and SAFE-21 booleans are present (automated grep gate above).
- The four-artifact evidence bundle exists under the phase evidence/ dir.
- The live run exercised /opt/wanctl deployed code and issued only the predeclared read-only commands.
</verification>

<success_criteria>
- A canary-readiness decision packet (OBSERVE-03) exists with an explicit `ready-for-approval` | `not-ready` verdict matching the real samples, superseding the Phase 257 not-ready packet with the two previously-failing rows now passing.
- OBSERVE-01/02 satisfied: the bounded dry-run observation ran live with RouterOS inspection succeeding, intended decisions were compared against live Netwatch/default-route state, and divergences (if any) are recorded as evidence, not mutations.
- SAFE-21 holds through closeout: no mutation, no owner flip, Netwatch remains owner, no active canary started.
</success_criteria>

<output>
Create `.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-03-SUMMARY.md` when done.
</output>
