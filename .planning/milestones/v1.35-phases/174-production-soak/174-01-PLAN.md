---
phase: 174-production-soak
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/174-production-soak/174-soak-evidence-canary.json
  - .planning/phases/174-production-soak/174-soak-evidence-monitor.json
  - .planning/phases/174-production-soak/174-soak-evidence-journalctl.txt
  - .planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt
  - .planning/phases/174-production-soak/174-soak-evidence-operator-att.txt
  - .planning/phases/174-production-soak/174-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [STOR-03, SOAK-01]
must_haves:
  truths:
    - "Storage pressure is ok or warning (not critical) for both WANs at soak end"
    - "Zero unexpected service restarts over 24h soak window"
    - "Zero unhandled errors in journalctl over 24h soak window"
    - "canary-check.sh exits 0 at soak end"
    - "soak-monitor.sh produces valid health/uptime/CAKE JSON at soak end"
    - "wanctl-operator-summary produces valid output for both WANs"
  artifacts:
    - path: ".planning/phases/174-production-soak/174-soak-evidence-canary.json"
      provides: "Canary check JSON output at 24h mark"
    - path: ".planning/phases/174-production-soak/174-soak-evidence-monitor.json"
      provides: "Soak monitor JSON output at 24h mark"
    - path: ".planning/phases/174-production-soak/174-soak-evidence-journalctl.txt"
      provides: "journalctl error scan for both WANs over 24h window"
    - path: ".planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt"
      provides: "Operator summary output for Spectrum"
    - path: ".planning/phases/174-production-soak/174-soak-evidence-operator-att.txt"
      provides: "Operator summary output for ATT"
    - path: ".planning/phases/174-production-soak/174-01-SUMMARY.md"
      provides: "Phase summary with timestamps, results, DB sizes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "STOR-03 and SOAK-01 marked satisfied"
  key_links:
    - from: "canary-check.sh"
      to: "health endpoints 10.10.110.223:9101 and 10.10.110.227:9101"
      via: "SSH + curl"
      pattern: "exit 0"
    - from: "soak-monitor.sh --json"
      to: "health endpoints"
      via: "SSH + curl"
      pattern: "healthy.*uptime"
    - from: "journalctl"
      to: "wanctl@spectrum and wanctl@att"
      via: "SSH + journalctl -p err"
      pattern: "zero error lines"
---

<objective>
Validate that v1.35.0 production services (Spectrum, ATT, steering) run cleanly for a consecutive 24h soak period, proving storage and runtime stability.

Purpose: This is the final gate for the v1.35 milestone. The soak proves that Phase 172 storage fixes (per-WAN DB split, 24h retention) and Phase 173 clean deploy hold up under real production load. No code changes -- observation and evidence capture only.

Output: Evidence files (JSON + text) in the phase directory, a SUMMARY.md with soak results, and REQUIREMENTS.md updated with STOR-03 and SOAK-01 marked satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/174-production-soak/174-CONTEXT.md
@.planning/phases/173-clean-deploy-canary-validation/173-03-SUMMARY.md
@scripts/canary-check.sh
@scripts/soak-monitor.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm soak window eligibility</name>
  <read_first>
    .planning/phases/173-clean-deploy-canary-validation/173-03-SUMMARY.md
    .planning/phases/174-production-soak/174-CONTEXT.md
  </read_first>
  <files>none -- observation only</files>
  <what-built>
Phase 173 completed with canary-check.sh exit 0 on 2026-04-12. Per D-01, the soak clock started at that canary pass. Per D-03, valid uptime accumulated since then counts toward the 24h window. Per D-08, any unexpected service restart resets the clock.

Before proceeding to the 24h validation, confirm:
1. Run a quick uptime check via SSH to verify services have been running continuously since Phase 173 canary:
   ```
   ssh kevin@10.10.110.223 'systemctl show wanctl@spectrum.service --property=ActiveEnterTimestamp && systemctl show wanctl@att.service --property=ActiveEnterTimestamp && systemctl show steering.service --property=ActiveEnterTimestamp'
   ```
2. Verify that each service's ActiveEnterTimestamp is at or before the Phase 173 canary pass time (2026-04-12) and that elapsed time since then is >= 24 hours.
3. Quick error scan to confirm no unexpected restarts occurred:
   ```
   ssh kevin@10.10.110.223 'journalctl -u wanctl@spectrum -u wanctl@att --since "24 hours ago" -p err --no-pager | head -20'
   ```

If uptime >= 24h since Phase 173 canary with no unexpected restarts: proceed to Task 2.
If a restart occurred: investigate root cause, document it, then the 24h clock resets from the last clean start (per D-08). Wait for the new 24h window to elapse before continuing.
If uptime < 24h and no restart: wait until 24h have passed since the Phase 173 canary timestamp.
  </what-built>
  <action>
Run the SSH commands above to check service uptimes and scan for errors. Present the results to the user for confirmation. Do NOT run the full validation suite yet -- this task only confirms that the 24h window has elapsed cleanly.
  </action>
  <how-to-verify>
    1. SSH to cake-shaper: ssh kevin@10.10.110.223
    2. Check service uptimes: systemctl show wanctl@spectrum.service wanctl@att.service steering.service --property=ActiveEnterTimestamp
    3. Verify each timestamp is >= 24 hours ago
    4. Check for errors: journalctl -u wanctl@spectrum -u wanctl@att --since '24 hours ago' -p err --no-pager
    5. If zero errors and uptime >= 24h: type "approved" to proceed
  </how-to-verify>
  <verify>
    <automated>ssh kevin@10.10.110.223 'systemctl show wanctl@spectrum.service --property=ActiveEnterTimestamp'</automated>
  </verify>
  <acceptance_criteria>
    - All three services (wanctl@spectrum, wanctl@att, steering) show ActiveEnterTimestamp >= 24 hours ago
    - No unexpected errors in journalctl scan
    - User confirms "approved" to proceed to full validation
  </acceptance_criteria>
  <done>User confirmed 24h soak window elapsed with no unexpected restarts or errors.</done>
  <resume-signal>Type "approved" if 24h window is satisfied, or describe what happened (restart, short uptime, errors found)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run 4-tool validation suite and capture evidence</name>
  <read_first>
    .planning/phases/174-production-soak/174-CONTEXT.md
    scripts/canary-check.sh
    scripts/soak-monitor.sh
  </read_first>
  <files>
    .planning/phases/174-production-soak/174-soak-evidence-canary.json
    .planning/phases/174-production-soak/174-soak-evidence-monitor.json
    .planning/phases/174-production-soak/174-soak-evidence-journalctl.txt
    .planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt
    .planning/phases/174-production-soak/174-soak-evidence-operator-att.txt
  </files>
  <action>
Run the 4-tool validation suite (per D-06) and save all output as evidence files in the phase directory. Execute these commands and capture their output:

**Tool 1 -- Canary check (per D-06 item 1, D-07):**
```bash
./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0 --json > .planning/phases/174-production-soak/174-soak-evidence-canary.json
echo "EXIT_CODE=$?" >> .planning/phases/174-production-soak/174-soak-evidence-canary.json
```
MUST exit 0. Storage status in the JSON output must show "ok" or "warning" for both WANs (not "critical") -- this satisfies STOR-03.

Also run the human-readable version and record its output for the SUMMARY:
```bash
./scripts/canary-check.sh --ssh kevin@10.10.110.223 --expect-version 1.35.0
```

**Tool 2 -- Soak monitor (per D-06 item 2):**
```bash
./scripts/soak-monitor.sh --json > .planning/phases/174-production-soak/174-soak-evidence-monitor.json
```
Capture final health state, version, uptime, CAKE signal, error counts. Verify the JSON contains valid health data for at least the Spectrum WAN.

Also run human-readable version and record output for the SUMMARY:
```bash
./scripts/soak-monitor.sh
```

**Tool 3 -- journalctl error scan (per D-06 item 3, D-09):**
```bash
ssh kevin@10.10.110.223 'journalctl -u wanctl@spectrum -u wanctl@att --since "24 hours ago" -p err --no-pager' > .planning/phases/174-production-soak/174-soak-evidence-journalctl.txt
```
The file must be empty or contain only "-- No entries --". Zero unhandled errors satisfies the SOAK-01 error requirement. If there ARE error lines, analyze whether they are known-benign (e.g., DOCSIS MAP jitter logs per D-09) or genuine failures.

**Tool 4 -- Operator summary (per D-06 item 4):**
```bash
ssh kevin@10.10.110.223 'cd /opt/wanctl && sudo -u wanctl .venv/bin/wanctl-operator-summary --wan spectrum' > .planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt
ssh kevin@10.10.110.223 'cd /opt/wanctl && sudo -u wanctl .venv/bin/wanctl-operator-summary --wan att' > .planning/phases/174-production-soak/174-soak-evidence-operator-att.txt
```
Verify both produce valid multi-line output (not error tracebacks). This validates the v1.34 operator surfaces are functional.

**Bonus diagnostics (Claude's discretion):**
Also capture DB file sizes for the SUMMARY:
```bash
ssh kevin@10.10.110.223 'ls -lh /var/lib/wanctl/metrics-spectrum.db /var/lib/wanctl/metrics-att.db /var/lib/wanctl/metrics-spectrum.db-wal /var/lib/wanctl/metrics-att.db-wal 2>/dev/null'
```

**PASS criteria (all must hold):**
- canary-check.sh exit code 0
- Both WANs storage.status is "ok" or "warning" (not "critical")
- journalctl error scan shows zero unhandled errors
- Both operator summaries produce valid output
- soak-monitor.sh JSON contains valid health data

**FAIL handling:** If any tool fails, document the failure in the evidence files and STOP. Do not proceed to Task 3. Report the failure and await investigation.
  </action>
  <verify>
    <automated>ls -la .planning/phases/174-production-soak/174-soak-evidence-canary.json .planning/phases/174-production-soak/174-soak-evidence-monitor.json .planning/phases/174-production-soak/174-soak-evidence-journalctl.txt .planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt .planning/phases/174-production-soak/174-soak-evidence-operator-att.txt</automated>
  </verify>
  <acceptance_criteria>
    - 174-soak-evidence-canary.json exists and contains JSON with "pass" results for both WANs
    - 174-soak-evidence-monitor.json exists and contains valid health JSON with "healthy" status
    - 174-soak-evidence-journalctl.txt exists and is empty or contains only "-- No entries --"
    - 174-soak-evidence-operator-spectrum.txt exists and contains multi-line operator summary (not a traceback)
    - 174-soak-evidence-operator-att.txt exists and contains multi-line operator summary (not a traceback)
    - Storage status for both WANs is "ok" or "warning" in canary JSON (STOR-03)
    - Zero unhandled errors in journalctl (SOAK-01)
  </acceptance_criteria>
  <done>All 5 evidence files captured with passing results. Storage pressure not critical. Zero unhandled errors. All operator surfaces functional.</done>
</task>

<task type="auto">
  <name>Task 3: Write SUMMARY.md and update REQUIREMENTS.md</name>
  <read_first>
    .planning/phases/174-production-soak/174-soak-evidence-canary.json
    .planning/phases/174-production-soak/174-soak-evidence-monitor.json
    .planning/phases/174-production-soak/174-soak-evidence-journalctl.txt
    .planning/phases/174-production-soak/174-soak-evidence-operator-spectrum.txt
    .planning/phases/174-production-soak/174-soak-evidence-operator-att.txt
    .planning/REQUIREMENTS.md
  </read_first>
  <files>
    .planning/phases/174-production-soak/174-01-SUMMARY.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
**Part A: Write 174-01-SUMMARY.md (per D-11 item 1)**

Create `.planning/phases/174-production-soak/174-01-SUMMARY.md` with standard GSD SUMMARY frontmatter. Use the 173-03-SUMMARY.md as a formatting reference. The SUMMARY frontmatter must include these fields:
  phase: 174-production-soak
  plan: 01
  subsystem: production
  tags: [soak, production, storage, observability, validation]
  requires: (phase 173 plan 03 providing "Dual-WAN canary exit 0, v1.35.0 deployed")
  provides: ["24h production soak passed for STOR-03 and SOAK-01", "Storage pressure stable at ok/warning for 24h", "Zero unhandled errors over 24h soak", "All v1.34 operator surfaces validated functional"]
  affects: [production, storage, observability]
  tech-stack: added=[], patterns=["Bookend soak validation", "4-tool evidence capture"]
  key-files: created=(all 5 evidence files + SUMMARY), modified=(REQUIREMENTS.md)
  key-decisions: ["Counted uptime from Phase 173 canary pass toward the 24h soak window (per D-03)", "Used 4-tool validation suite as evidence for both STOR-03 and SOAK-01"]
  patterns-established: ["Soak phases use bookend validation with evidence file capture"]
  requirements-completed: [STOR-03, SOAK-01]
  duration: (actual duration)
  completed: (actual date)

Body sections:
1. **Title line** summarizing pass/fail
2. **Soak Window** -- start timestamp (from Phase 173 canary), end timestamp (when Task 2 ran), total uptime
3. **Validation Results** -- one subsection per tool with pass/fail and key metrics:
   - Canary: exit code, version, storage status per WAN
   - Soak monitor: health status, uptime, CAKE signal summary
   - Journalctl: error count (should be 0)
   - Operator summary: functional for both WANs (yes/no)
4. **Storage Health** -- DB file sizes at soak end, storage.status per WAN
5. **Requirements Satisfied** -- STOR-03 (storage pressure ok/warning for 24h), SOAK-01 (full stack clean for 24h)
6. **Self-Check** -- bullet list confirming each must-have truth

Populate all timestamps, metrics, and results from the evidence files captured in Task 2. Use actual values, not placeholders.

**Part B: Update REQUIREMENTS.md (per D-11 item 3)**

Read `.planning/REQUIREMENTS.md`. Change:
- `- [ ] **STOR-03**:` to `- [x] **STOR-03**:`
- `- [ ] **SOAK-01**:` to `- [x] **SOAK-01**:`

In the Traceability table, update:
- STOR-03 row: Status from `Pending` to `Satisfied`
- SOAK-01 row: Status from `Pending` to `Satisfied`

Also update STOR-01, STOR-02, DEPL-01, DEPL-02 if they are still showing `Pending` but were completed in Phases 172/173 (check the 173-03-SUMMARY.md which shows DEPL-01 was completed).

**Part C: Commit**

Git add all evidence files, SUMMARY.md, and REQUIREMENTS.md. Commit with message:
`docs(174): 24h production soak passed -- STOR-03 and SOAK-01 satisfied`
  </action>
  <verify>
    <automated>test -f .planning/phases/174-production-soak/174-01-SUMMARY.md && grep -q '\[x\] \*\*STOR-03\*\*' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*SOAK-01\*\*' .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 174-01-SUMMARY.md exists with complete frontmatter including requirements-completed: [STOR-03, SOAK-01]
    - SUMMARY body contains actual timestamps, metrics, and DB sizes from evidence files (no placeholders)
    - REQUIREMENTS.md has STOR-03 and SOAK-01 checked off with [x]
    - REQUIREMENTS.md traceability table shows Satisfied for both
    - All files committed to git
  </acceptance_criteria>
  <done>SUMMARY.md written with full soak results. REQUIREMENTS.md updated with STOR-03 and SOAK-01 satisfied. All committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> cake-shaper VM | SSH session to production host for observation (read-only commands) |
| cake-shaper VM -> health endpoints | localhost/LAN curl to health API (no external exposure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Information Disclosure | SSH session reads health JSON | accept | Health data is operational metrics (no PII). SSH uses key auth per existing security model. Evidence files stored in local git repo only. |
| T-174-02 | Tampering | Evidence files could be modified after capture | accept | Evidence is for internal milestone tracking, not compliance. Git commit provides integrity record. |
| T-174-03 | Elevation of Privilege | operator-summary runs via sudo -u wanctl | accept | Existing production pattern. wanctl user has minimal privileges (read state files and metrics DB only). No new privilege grants in this phase. |
</threat_model>

<verification>
Phase-level verification checklist:
1. All 5 evidence files exist and contain valid data
2. canary-check.sh exit code was 0
3. Storage pressure for both WANs is "ok" or "warning" (not "critical") -- STOR-03
4. journalctl error scan shows zero unhandled errors -- SOAK-01
5. Both operator summaries produce valid output -- SOAK-01
6. SUMMARY.md contains actual metrics from evidence files
7. REQUIREMENTS.md shows STOR-03 and SOAK-01 as satisfied
</verification>

<success_criteria>
- 24h consecutive uptime since Phase 173 canary pass confirmed
- Storage pressure stable (not critical) for entire soak window (STOR-03)
- Zero unexpected restarts and zero unhandled errors (SOAK-01)
- All v1.34 operator surfaces produce valid output
- Evidence files and SUMMARY committed to git
- REQUIREMENTS.md updated
- Ready for /gsd-complete-milestone to archive v1.35
</success_criteria>

<output>
After completion, create `.planning/phases/174-production-soak/174-01-SUMMARY.md`
Then run `/gsd-complete-milestone` to archive v1.35.
</output>
