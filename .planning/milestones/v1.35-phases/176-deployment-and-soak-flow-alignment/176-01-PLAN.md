---
phase: 176-deployment-and-soak-flow-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install.sh
  - scripts/deploy.sh
  - scripts/wanctl-operator-summary
requirements: [DEPL-01, SOAK-01]
autonomous: true
tags: [deploy, install, cli, operator-summary]

must_haves:
  truths:
    - "`scripts/install.sh` reports version 1.35.0 instead of stale 1.32.2"
    - "A deploy-surfaced `wanctl-operator-summary` command exists on target hosts without requiring an unpublished packaging step"
    - "`scripts/deploy.sh` deploys and exposes the operator-summary wrapper alongside existing helper CLIs"
  artifacts:
    - path: "scripts/wanctl-operator-summary"
      provides: "deployable wrapper for the operator summary CLI"
      contains: "from wanctl.operator_summary import main"
  key_links:
    - from: "pyproject.toml"
      to: "scripts/wanctl-operator-summary"
      via: "same CLI capability exposed through deploy-supported wrapper"
      pattern: "wanctl-operator-summary"
---

<objective>
Close the install/deploy parity gaps for release metadata and operator-summary CLI surfacing.

Purpose: the audit found that `install.sh` still advertises `1.32.2` and that the intended `wanctl-operator-summary` operator workflow is not actually deployed to target systems. This plan fixes those gaps using the repo's existing wrapper-and-symlink deployment style, without changing controller runtime logic or introducing a new packaging/install system.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/176-deployment-and-soak-flow-alignment/176-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update install-facing release metadata to 1.35.0</name>
  <files>scripts/install.sh</files>
  <read_first>
    - scripts/install.sh
    - pyproject.toml
    - src/wanctl/__init__.py
  </read_first>
  <action>
    Edit `scripts/install.sh` so the top-level `VERSION=` constant exactly matches the shipped runtime/package version `1.35.0`.

    Do not change installer behavior, dependency lists, directory paths, or service setup logic in this task. The scope is metadata parity only.
  </action>
  <verify>
    <automated>rg -n 'VERSION="1.35.0"' scripts/install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/install.sh` contains `VERSION="1.35.0"`
    - `scripts/install.sh` no longer contains `VERSION="1.32.2"`
  </acceptance_criteria>
  <done>Install script metadata matches v1.35.0.</done>
</task>

<task type="auto">
  <name>Task 2: Add a deployable wrapper for `wanctl-operator-summary`</name>
  <files>scripts/wanctl-operator-summary</files>
  <read_first>
    - src/wanctl/operator_summary.py
    - scripts/analyze_baseline.py
  </read_first>
  <action>
    Create a new executable wrapper script at `scripts/wanctl-operator-summary` using the same import-path pattern as `scripts/analyze_baseline.py`.

    The wrapper must:
    - start with `#!/usr/bin/env python3`
    - prepend both repo-style `src` and deployed `/opt` import roots to `sys.path`
    - import `main` from `wanctl.operator_summary`
    - call `main()` under `if __name__ == "__main__":`

    Do not add new CLI flags. Preserve the current source contract: positional health JSON sources and optional `--json`.
  </action>
  <verify>
    <automated>test -f scripts/wanctl-operator-summary && rg -n 'from wanctl.operator_summary import main|__name__ == "__main__"' scripts/wanctl-operator-summary</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/wanctl-operator-summary` exists
    - file starts with `#!/usr/bin/env python3`
    - file contains `from wanctl.operator_summary import main`
    - file contains `if __name__ == "__main__":`
  </acceptance_criteria>
  <done>Operator-summary has a wrapper script that works from both the repo checkout and deployed `/opt/wanctl` layout.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and surface the operator-summary wrapper in `deploy.sh`</name>
  <files>scripts/deploy.sh</files>
  <read_first>
    - scripts/deploy.sh
    - scripts/wanctl-operator-summary
    - pyproject.toml
  </read_first>
  <action>
    Update `scripts/deploy.sh` so it deploys `scripts/wanctl-operator-summary` into `$TARGET_CODE_DIR/scripts/wanctl-operator-summary` and creates a stable `/usr/local/bin/wanctl-operator-summary` symlink, matching the existing `wanctl-history` deployment pattern.

    Keep the current `wanctl-history` behavior intact.

    Also update the success output string so the script explicitly states that `wanctl-operator-summary` is available as a command after deploy.
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh scripts/wanctl-operator-summary && rg -n '/usr/local/bin/wanctl-operator-summary|scripts/wanctl-operator-summary|available as .wanctl-operator-summary. command' scripts/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy.sh` references `scripts/wanctl-operator-summary`
    - `scripts/deploy.sh` contains `/usr/local/bin/wanctl-operator-summary`
    - `bash -n scripts/deploy.sh scripts/wanctl-operator-summary` exits 0
  </acceptance_criteria>
  <done>`deploy.sh` now installs and surfaces `wanctl-operator-summary` on target hosts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scripts -> target host filesystem | deployment writes executables and symlinks into production paths |
| wrapper script -> Python module import path | wrapper must import the intended local module and not a stray system package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | T (Tampering) | install metadata | mitigate | update the exact version constant to `1.35.0` and verify with grep |
| T-176-02 | T (Tampering) | operator-summary CLI surfacing | mitigate | deploy a wrapper that imports `wanctl.operator_summary` from the repo/deployed tree and verify symlink creation textually |
</threat_model>

<verification>
- `rg -n 'VERSION="1.35.0"' scripts/install.sh`
- `test -f scripts/wanctl-operator-summary`
- `bash -n scripts/deploy.sh scripts/wanctl-operator-summary`
- `rg -n 'wanctl-operator-summary' scripts/deploy.sh scripts/wanctl-operator-summary pyproject.toml`
</verification>

<success_criteria>
The install/deploy workflow reports the correct v1.35.0 release metadata and exposes `wanctl-operator-summary` through a deploy-supported wrapper and command path.
</success_criteria>

<output>
After completion, create `.planning/phases/176-deployment-and-soak-flow-alignment/176-01-SUMMARY.md`
</output>
