---
phase: 176-deployment-and-soak-flow-alignment
plan: 03
type: execute
wave: 3
depends_on: [176-01, 176-02]
files_modified:
  - scripts/soak-monitor.sh
  - docs/RUNBOOK.md
  - docs/DEPLOYMENT.md
requirements: [STOR-03, SOAK-01]
autonomous: true
tags: [soak, observability, steering, evidence]

must_haves:
  truths:
    - "`scripts/soak-monitor.sh` is no longer hardcoded to Spectrum-only evidence coverage"
    - "The repeatable error-scan path can include `steering.service` in addition to WAN services"
    - "Operational docs describe the all-services soak evidence contract"
  artifacts:
    - path: "scripts/soak-monitor.sh"
      provides: "repeatable soak health and error evidence helper"
      contains: "steering.service"
  key_links:
    - from: "scripts/soak-monitor.sh"
      to: "docs/RUNBOOK.md"
      via: "same named services and evidence commands"
      pattern: "steering.service"
---

<objective>
Make the soak evidence path cover all claimed services, including `steering.service`, instead of remaining Spectrum-centric.

Purpose: the audit gap is specifically about repeatable evidence coverage. This plan updates the soak helper and operator docs so ATT and steering participate in the same evidence flow as Spectrum.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/v1.35-MILESTONE-AUDIT.md
@.planning/phases/174-production-soak/174-01-SUMMARY.md
@.planning/phases/176-deployment-and-soak-flow-alignment/176-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize `scripts/soak-monitor.sh` for multi-target and multi-service evidence</name>
  <files>scripts/soak-monitor.sh</files>
  <read_first>
    - scripts/soak-monitor.sh
    - .planning/v1.35-MILESTONE-AUDIT.md
    - .planning/phases/174-production-soak/174-01-SUMMARY.md
  </read_first>
  <action>
    Refactor `scripts/soak-monitor.sh` conservatively so its default evidence path is no longer Spectrum-only.

    The updated script must:
    - define both Spectrum and ATT autorate targets in the default target list, using the existing `name|health_ip|ssh_target` style already present in the script
    - keep the current formatted-table and `--json` modes
    - expand the recent-error scan to support multiple services rather than only `wanctl@${wan_name}`
    - include `steering.service` in the default service set or in an opt-in service list used by the main run path

    Preserve existing shell-only operation; do not rewrite the tool in Python.
  </action>
  <verify>
    <automated>bash -n scripts/soak-monitor.sh && rg -n 'spectrum|att|steering.service|journalctl .* -u ' scripts/soak-monitor.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/soak-monitor.sh` contains both `spectrum` and `att`
    - `scripts/soak-monitor.sh` contains `steering.service`
    - `scripts/soak-monitor.sh` contains a `journalctl` invocation that can cover multiple `-u` units
    - `bash -n scripts/soak-monitor.sh` exits 0
  </acceptance_criteria>
  <done>The soak monitor script now supports the full claimed service set instead of only the Spectrum autorate service.</done>
</task>

<task type="auto">
  <name>Task 2: Document the all-services soak evidence contract in `docs/RUNBOOK.md`</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - docs/RUNBOOK.md
    - scripts/soak-monitor.sh
    - .planning/v1.35-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Update the operator tools / soak-related sections in `docs/RUNBOOK.md` so they explicitly describe:

    - `scripts/soak-monitor.sh` as the multi-service soak helper
    - that 24h err-level review must include both WAN services and `steering.service` when steering is enabled
    - `wanctl-operator-summary` examples that use health URLs, consistent with the actual CLI

    Keep wording production-oriented and concise.
  </action>
  <verify>
    <automated>rg -n 'soak-monitor.sh|steering.service|wanctl-operator-summary http' docs/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RUNBOOK.md` contains `soak-monitor.sh`
    - `docs/RUNBOOK.md` contains `steering.service`
    - `docs/RUNBOOK.md` contains `wanctl-operator-summary http://`
  </acceptance_criteria>
  <done>Runbook guidance reflects the all-services soak evidence contract and the actual operator-summary CLI shape.</done>
</task>

<task type="auto">
  <name>Task 3: Keep deployment docs consistent with the soak evidence update</name>
  <files>docs/DEPLOYMENT.md</files>
  <read_first>
    - docs/DEPLOYMENT.md
    - docs/RUNBOOK.md
    - scripts/soak-monitor.sh
  </read_first>
  <action>
    Update `docs/DEPLOYMENT.md` monitoring or troubleshooting sections so they point operators at the updated soak evidence path:

    - `scripts/soak-monitor.sh`
    - `journalctl -u wanctl@... -u steering.service`
    - `wanctl-operator-summary` for health-summary snapshots

    Keep this consistent with `docs/RUNBOOK.md`; do not duplicate large blocks of text.
  </action>
  <verify>
    <automated>rg -n 'soak-monitor.sh|steering.service|wanctl-operator-summary' docs/DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DEPLOYMENT.md` contains `soak-monitor.sh`
    - `docs/DEPLOYMENT.md` contains `steering.service`
    - `docs/DEPLOYMENT.md` contains `wanctl-operator-summary`
  </acceptance_criteria>
  <done>Deployment documentation stays aligned with the updated soak evidence flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soak helper output -> operator confidence | incomplete service coverage can produce false clean-soak conclusions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-04 | I (Information Disclosure / Integrity of evidence) | soak evidence coverage | mitigate | make target/service coverage explicit in script defaults and docs so `steering.service` is not silently omitted |
</threat_model>

<verification>
- `bash -n scripts/soak-monitor.sh`
- `rg -n 'spectrum|att|steering.service' scripts/soak-monitor.sh`
- `rg -n 'soak-monitor.sh|steering.service|wanctl-operator-summary' docs/RUNBOOK.md docs/DEPLOYMENT.md`
</verification>

<success_criteria>
The repo now provides a repeatable soak evidence path that covers both WAN services and `steering.service`, and the operator docs describe that contract accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/176-deployment-and-soak-flow-alignment/176-03-SUMMARY.md`
</output>
