---
phase: 214-measurement-collapse-investigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase214-flent-matrix.sh
autonomous: true
requirements: [MEAS-01]
tags: [matrix, harness, orchestration, window-gate, mutation-guard, journal-capture]

must_haves:
  truths:
    - "Operator runs a single command per window and gets a Phase-214-owned evidence tree (per D-01/D-02/D-03)."
    - "Wrapper refuses out-of-window hours via the window-hour gate, mirroring phase198-rerun-flent-3run.sh."
    - "Wrapper refuses any run that detects an `src/wanctl/` diff via TRIPLE-CHECK (unstaged + staged + committed-since-base) per D-14 / MEAS-03 hard guard. Codex review HIGH-2: `git diff BASE..HEAD` alone misses uncommitted and staged working-tree changes."
    - "Wrapper delegates traffic generation, health polling, alert pull, and steering snapshot to phase213-baseline-capture.sh (no re-implementation)."
    - "After phase213-baseline-capture.sh returns, wrapper pulls a bounded `journalctl --since=@<test_start_unix> --until=@<test_end_unix> -u wanctl@spectrum -o json` into `<test_dir>/journal-window.ndjson` for each per-test directory. This artifact does NOT exist in Phase 213 (verified: phase213-baseline-capture.sh:318 captures steering-post, not journal). Plans 214-03/04/06 depend on it (Codex HIGH-1)."
    - "Each invocation produces a `phase214-window.json` sidecar recording {window, flent_duration_sec, phase, git_head_sha, journalctl_command} at run root AND at each per-test dir (Codex LOW-15: per-test analyzers should not need to walk up to run root)."
    - "Live runs are manual/operator-driven; the autonomous artifact is the wrapper script itself plus a `--dry-run --test-hour H window` regression hook."
  artifacts:
    - path: "scripts/phase214-flent-matrix.sh"
      provides: "Per-window thin wrapper around phase213-baseline-capture.sh with window-hour gate, D-14 src/wanctl/ triple-check diff guard, post-run journal capture, and sidecar manifest write (at run root + per-test dir)"
      contains: "phase214-window.json"
  key_links:
    - from: "scripts/phase214-flent-matrix.sh"
      to: "scripts/phase213-baseline-capture.sh"
      via: "bash subcommand with --bind-map spectrum=10.10.110.226 --wans spectrum --tests tcp_12down --evidence-root .planning/phases/214-.../evidence"
      pattern: "phase213-baseline-capture\\.sh.*--evidence-root.*214-measurement-collapse"
    - from: "scripts/phase214-flent-matrix.sh"
      to: ".planning/phases/214-measurement-collapse-investigation/evidence/RUN-<ts>/phase214-window.json"
      via: "jq -n sidecar write after orchestrator returns + per-test cp"
      pattern: "phase214-window\\.json"
    - from: "scripts/phase214-flent-matrix.sh"
      to: ".planning/phases/214-measurement-collapse-investigation/evidence/RUN-<ts>/spectrum/tcp_12down/journal-window.ndjson"
      via: "ssh cake-shaper sudo -n journalctl --since=@<t_start> --until=@<t_end> -u wanctl@spectrum -o json"
      pattern: "journal-window\\.ndjson"
---

<objective>
Build the per-window matrix wrapper that operators run three times across off-peak/daytime/prime-time to capture Spectrum `tcp_12down` evidence (D-01/D-02/D-03). The wrapper is a thin composition over `scripts/phase213-baseline-capture.sh` — it adds (a) a window-hour gate with `--dry-run`/`--test-hour`, (b) a D-14 hard guard that refuses any live run if `src/wanctl/` has ANY diff (unstaged, staged, OR committed since the phase base SHA), (c) a post-run bounded journal capture per per-test directory, and (d) a Phase-214-owned sidecar manifest written both at run root and per-test dir.

The journal-capture step exists because Phase 213's `phase213-baseline-capture.sh:318` does NOT pull journalctl output, yet Plans 214-03/04/06 all depend on `journal-window.ndjson` being present per test directory (Codex review HIGH-1).

Purpose: MEAS-01 requires a bounded reproduction matrix; the wrapper enforces the bounds without duplicating Phase 213 orchestration logic, AND fills the journal-capture gap that Phase 213 left.
Output: `scripts/phase214-flent-matrix.sh` (executable) + a dry-run regression hook callable from CI/pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-measurement-collapse-investigation/214-CONTEXT.md
@.planning/phases/214-measurement-collapse-investigation/214-RESEARCH.md
@.planning/phases/214-measurement-collapse-investigation/214-PATTERNS.md
@.planning/phases/214-measurement-collapse-investigation/214-VALIDATION.md
@.planning/phases/214-measurement-collapse-investigation/214-REVIEWS.md
@scripts/phase198-rerun-flent-3run.sh
@scripts/phase213-baseline-capture.sh

<interfaces>
<!-- Window-hour gate contract from phase198-rerun-flent-3run.sh:111-156 (verbatim reuse pattern) -->
Window labels and acceptable local-hour ranges:
  off-peak    -> hours 01..05  (exit 2 outside this range)
  daytime     -> hours 10..16  (exit 2 outside this range)
  prime-time  -> hours 19..22  (exit 2 outside this range)

Wrapper CLI shape:
  scripts/phase214-flent-matrix.sh <off-peak|daytime|prime-time>
  scripts/phase214-flent-matrix.sh --dry-run --test-hour <H> <window>
  scripts/phase214-flent-matrix.sh --help | -h

Env overrides honored by the wrapper:
  PHASE214_FLENT_DURATION  default 30   (D-03; recorded in sidecar)
  PHASE214_BASE_SHA        required for the D-14 src/wanctl/ diff guard
                           (recommend operator export = git HEAD at planning time)
  PHASE214_CAKE_SHAPER     default cake-shaper (ssh alias used for journal capture)

Delegate invocation:
  bash scripts/phase213-baseline-capture.sh \
    --bind-map spectrum=10.10.110.226 \
    --wans spectrum \
    --tests tcp_12down \
    --flent-duration "$DURATION" \
    --host dallas \
    --evidence-root ".planning/phases/214-measurement-collapse-investigation/evidence"

Post-run journal capture (NEW — fills Phase 213 gap per Codex HIGH-1):
  For each per-test directory under the newest RUN-*/, read manifest.json's
  test_start_unix and test_end_unix (phase213-baseline-capture.sh:322-323 writes
  these per-test). Then:
    ssh "$PHASE214_CAKE_SHAPER" "sudo -n journalctl \
       --since=@<test_start_unix> --until=@<test_end_unix> \
       -u wanctl@spectrum -o json" > <test_dir>/journal-window.ndjson
  Verify the file is non-empty AND each line parses as JSON before the wrapper
  exits 0. If journalctl returns empty (legitimately possible — no events in
  window) emit a 1-line stderr warning but still exit 0; the file must exist
  even if it's empty so 214-03/04/06 don't error on missing-input.

Exit codes:
  0  success
  2  refused: out-of-window hour
  4  refused: D-14 src/wanctl/ diff non-empty (any of unstaged/staged/committed)
  5  failed: journal capture errored (ssh failure or malformed JSON output)
  7  refused: --test-hour without --dry-run
  >0 delegated orchestrator failure (preserved from phase213-baseline-capture.sh)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement phase214-flent-matrix.sh wrapper with window gate, D-14 src/wanctl/ triple-check guard, post-run journal capture, and per-test sidecar manifest</name>
  <files>scripts/phase214-flent-matrix.sh</files>
  <read_first>
    - scripts/phase198-rerun-flent-3run.sh:1-30, 49-156, 169-176, 181-210 (window-hour gate, --dry-run/--test-hour pattern, SAFE-05 protected-source diff, trap/partial-summary)
    - scripts/phase213-baseline-capture.sh:1-30, 269-345 (delegate orchestrator CLI surface, egress probe ownership, manifest emission). CRITICAL: line 318 captures steering-post snapshot; line 322-323 writes per-test `manifest.json` with `test_start_unix`/`test_end_unix`. There is NO journalctl pull anywhere in this file — verified by Codex review and re-verified during this revision. The wrapper MUST add it.
    - scripts/phase191-flent-capture.sh:147-187 (flent output written under `${CAPTURE_DIR}` which phase213-baseline-capture.sh:282-283 then symlinks to `${test_dir}/flent`; downstream plans MUST resolve flent via `${test_dir}/flent/*.flent.gz`, NOT `${test_dir}/*.flent.gz`)
    - .planning/phases/214-measurement-collapse-investigation/214-PATTERNS.md §`scripts/phase214-flent-matrix.sh` (full pattern map)
    - .planning/phases/214-measurement-collapse-investigation/214-RESEARCH.md §Pattern 2 (per-window evidence-tree call sketch)
    - .planning/phases/214-measurement-collapse-investigation/214-CONTEXT.md D-01, D-02, D-03, D-11, D-14
    - .planning/phases/214-measurement-collapse-investigation/214-REVIEWS.md §HIGH-1 (journal capture gap) and §HIGH-2 (triple-check diff guard) and §LOW-15 (per-test sidecar)
  </read_first>
  <action>
    Create `scripts/phase214-flent-matrix.sh` (executable, mode 0755) as a thin wrapper around `scripts/phase213-baseline-capture.sh`. Mirror the phase198 strict-mode header (`#!/usr/bin/env bash` + `set -euo pipefail`).

    Implement per D-01/D-02/D-03:
    - Positional arg: `<window>` ∈ {`off-peak`, `daytime`, `prime-time`}; reject unknown labels with usage to stderr.
    - Flags: `--dry-run`, `--test-hour <H>` (only honored with `--dry-run`; without `--dry-run` exit 7 per phase198:111-118 pattern), `--help|-h` (heredoc usage).
    - Env defaults: `DURATION="${PHASE214_FLENT_DURATION:-30}"` (D-03 30s default), `WANS=spectrum` (D-01 hardcoded), `TESTS=tcp_12down` (D-02 hardcoded), `CAKE_SHAPER="${PHASE214_CAKE_SHAPER:-cake-shaper}"` (ssh alias for journal capture).
    - Window-hour gate: lift phase198-rerun-flent-3run.sh:124-156 verbatim, extended for all three labels (off-peak 01..05, daytime 10..16, prime-time 19..22). Use `LOCAL_HOUR="${TEST_HOUR:-$(date +%H)}"`. Refuse with exit 2 outside the band.

    Implement per D-14 (MEAS-03 hard guard) — **TRIPLE-CHECK per Codex review HIGH-2** (mirror phase198:169-176 BUT extend to catch dirty tree):
    - Require `PHASE214_BASE_SHA` env var; refuse with exit 4 + clear stderr message if unset.
    - Refuse with exit 4 if ANY of the following return nonzero (all three must exit 0 for the guard to pass):
      1. `git diff --quiet -- src/wanctl/`                       (unstaged working-tree)
      2. `git diff --cached --quiet -- src/wanctl/`              (staged but uncommitted)
      3. `git diff --quiet "${PHASE214_BASE_SHA}..HEAD" -- src/wanctl/`  (committed since phase base)
    - This guard runs BEFORE delegating to the orchestrator. Rationale: a developer mid-edit (`git status` shows modified `src/wanctl/wan_controller.py`) or with staged changes (`git add src/wanctl/...`) would slip through the BASE..HEAD diff alone — Codex review HIGH-2 verbatim. The dry-run path should still verify all three checks when `PHASE214_BASE_SHA` is set (so the regression hook catches a dirty tree before an operator burns a window).
    - Emit a stderr message naming WHICH of the three checks failed, so the operator can immediately fix (e.g. "REFUSED: src/wanctl/ has unstaged changes; run `git stash` or revert before live run").

    Delegate to phase213-baseline-capture.sh per D-11 (do not duplicate orchestration). Pass exactly: `--bind-map spectrum=10.10.110.226 --wans spectrum --tests tcp_12down --flent-duration "$DURATION" --host dallas --evidence-root ".planning/phases/214-measurement-collapse-investigation/evidence"`. Under `--dry-run`, print the planned command to stdout prefixed with `DRY-RUN:` and skip invocation.

    **NEW — Post-run journal capture (fills Codex HIGH-1 gap):** After successful orchestrator return (non-dry-run), locate the newest `RUN-*` dir under the evidence root (`RUN_DIR="$(ls -1d .planning/phases/214-measurement-collapse-investigation/evidence/RUN-* | sort | tail -n 1)"`). For each per-test directory matching `${RUN_DIR}/spectrum/tcp_12down/` (currently 1 — `tcp_12down` is hardcoded per D-02, but use a loop so this generalizes if D-04 ATT contrast is added later):
    - Read the per-test `manifest.json` (written by phase213-baseline-capture.sh:322-323) to extract `test_start_unix` and `test_end_unix` via `jq -r '.test_start_unix'` and `jq -r '.test_end_unix'`. Refuse with exit 5 if either is missing or non-numeric.
    - Build the journalctl command: `JOURNAL_CMD="sudo -n journalctl --since=@${T_START} --until=@${T_END} -u wanctl@spectrum -o json"`.
    - Run: `ssh "$CAKE_SHAPER" "$JOURNAL_CMD" > "${test_dir}/journal-window.ndjson"`. If ssh exits nonzero, exit 5 + stderr message naming the host and time window.
    - Verify the artifact: `test -e "${test_dir}/journal-window.ndjson"` (must exist, may be empty if no events fired in window — emit a 1-line warning to stderr in that case but proceed). If non-empty, validate first line parses as JSON: `head -n 1 "${test_dir}/journal-window.ndjson" | jq -e . >/dev/null 2>&1` — exit 5 if validation fails.
    - Record the exact journalctl command string in the sidecar (next step) for provenance (Codex LOW-4: report should cite the command).

    **Sidecar manifest at RUN_DIR root:** Write `${RUN_DIR}/phase214-window.json` via `jq -n` containing `{phase: 214, window, flent_duration_sec, started_utc, ended_utc, git_head_sha, mutation_posture: "read-only", phase214_base_sha: "$PHASE214_BASE_SHA", journalctl_command: "$JOURNAL_CMD"}`. Mirror the sidecar field shape from phase213-baseline-capture.sh:182-209 (`make_manifest`).

    **Sidecar copy per-test (Codex LOW-15):** After writing the run-root sidecar, `cp "${RUN_DIR}/phase214-window.json" "${test_dir}/phase214-window.json"` for each per-test dir. Per-test analyzers (214-03 align CLI invoked from 214-06 Task 1 post-processing) can then locate the window metadata in the same directory as the flent/health/journal artifacts without walking up to the run root.

    Do NOT re-implement the source-bind egress probe — phase213-baseline-capture.sh:329-334 already refuses on mismatch and propagates exit code (see 214-PATTERNS.md §Shared Patterns "Source-bind egress refusal").

    Do NOT modify any file under `src/wanctl/`, do NOT call RouterOS, do NOT restart services (D-14). Per D-11, do NOT back-edit `scripts/phase213-*.sh`. The journal capture is an ADDITIVE post-step in the wrapper, NOT a back-edit of phase213-baseline-capture.sh.
  </action>
  <verify>
    <automated>bash scripts/phase214-flent-matrix.sh --help</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/phase214-flent-matrix.sh` and is executable (`test -x scripts/phase214-flent-matrix.sh`).
    - `bash scripts/phase214-flent-matrix.sh --help` exits 0 and prints usage listing all three window labels.
    - `bash scripts/phase214-flent-matrix.sh --dry-run --test-hour 03 off-peak` exits 0 (when `PHASE214_BASE_SHA` set to a clean SHA AND `src/wanctl/` working tree clean) and prints a `DRY-RUN:` line containing `phase213-baseline-capture.sh`, `--wans spectrum`, `--tests tcp_12down`, `--bind-map spectrum=10.10.110.226`, `--evidence-root .planning/phases/214-measurement-collapse-investigation/evidence`.
    - `bash scripts/phase214-flent-matrix.sh --dry-run --test-hour 09 off-peak` exits 2 (out-of-window).
    - `bash scripts/phase214-flent-matrix.sh --dry-run --test-hour 14 daytime` exits 0.
    - `bash scripts/phase214-flent-matrix.sh --dry-run --test-hour 20 prime-time` exits 0.
    - `bash scripts/phase214-flent-matrix.sh --test-hour 03 off-peak` (no `--dry-run`) exits 7.
    - **Triple-check diff guard verified (Codex HIGH-2):** With `PHASE214_BASE_SHA="$(git rev-parse HEAD)"` set and clean tree, dry-run exits 0. After `touch src/wanctl/_dirty_test.py` (unstaged), dry-run exits 4 with stderr naming the unstaged-check. After `git add src/wanctl/_dirty_test.py` (staged), dry-run exits 4 with stderr naming the staged-check. After cleanup (`rm -f src/wanctl/_dirty_test.py && git reset HEAD src/wanctl/_dirty_test.py`), dry-run exits 0 again.
    - `grep -E -c "(systemctl restart|mikrotik|routeros|/etc/wanctl/.*\.yaml)" scripts/phase214-flent-matrix.sh` returns 0 (mutation-guard hygiene; the script writes no production state — note the regex is anchored on `systemctl restart` not bare `systemctl`, since the script CAN reference `journalctl` which is a systemd utility).
    - Script invokes `phase213-baseline-capture.sh` and writes `phase214-window.json` sidecar via `jq` (verified by `grep -q 'phase214-window.json' scripts/phase214-flent-matrix.sh && grep -q 'phase213-baseline-capture.sh' scripts/phase214-flent-matrix.sh`).
    - **Journal capture wired (Codex HIGH-1):** `grep -q 'journal-window.ndjson' scripts/phase214-flent-matrix.sh && grep -q 'journalctl' scripts/phase214-flent-matrix.sh && grep -q 'test_start_unix' scripts/phase214-flent-matrix.sh && grep -q 'test_end_unix' scripts/phase214-flent-matrix.sh` all succeed.
    - **Triple-check guard literally present:** `grep -c 'git diff --quiet' scripts/phase214-flent-matrix.sh` returns 3 (one for each check).
    - **Per-test sidecar copy present (Codex LOW-15):** `grep -E -c 'cp.*phase214-window.json' scripts/phase214-flent-matrix.sh` returns at least 1.
    - **Provenance recorded (Codex LOW-4):** `grep -q 'journalctl_command' scripts/phase214-flent-matrix.sh` succeeds.
  </acceptance_criteria>
  <done>
    Dry-run gate matrix above passes. `bash -n scripts/phase214-flent-matrix.sh` is clean. The wrapper delegates orchestration to phase213-baseline-capture.sh and never re-implements egress probe, health polling, alert pull, or steering snapshot — but DOES add the missing journal capture (post-run, per-test) and the per-test sidecar copy. Triple-check D-14 guard catches dirty tree, staged changes, AND committed changes since phase base.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → wrapper CLI | Untrusted positional/flag input; risk of shell injection if interpolated into `bash -c` or `eval`. |
| wrapper → phase213-baseline-capture.sh | Trusted local subprocess; arguments are constants except `$DURATION` (numeric). |
| wrapper → `git diff` | Local read-only git invocation; output not interpreted. |
| wrapper → `jq -n` (sidecar write) | Local write to `evidence/RUN-<ts>/` AND per-test dirs; outside `src/wanctl/`. |
| wrapper → ssh cake-shaper | Read-only `sudo -n journalctl` invocation; stdout captured to local file; no remote mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-01-01 | Tampering | `src/wanctl/` modification during live run | mitigate | D-14 TRIPLE-CHECK hard guard (Codex HIGH-2): unstaged + staged + committed-since-base. All three `git diff --quiet` invocations must exit 0 or wrapper exits 4 before delegating. |
| T-214-01-02 | Tampering | Shell injection via positional args | mitigate | Strict `case "$WINDOW" in off-peak\|daytime\|prime-time) ... *) usage; exit 64;;` — no interpolation of `$WINDOW` into shell commands except inside the validated `case` block. Numeric `$DURATION` is passed as a discrete argv element, never interpolated into a string. `$T_START`/`$T_END` from manifest.json are read via `jq -r` and validated as numeric before interpolation into the journalctl command. |
| T-214-01-03 | Elevation of Privilege | Window-hour bypass to fire an out-of-budget run | mitigate | `--test-hour` is only honored when `--dry-run` is also set (exit 7 otherwise), mirroring phase198:111-118. Production runs read live `date +%H`. |
| T-214-01-04 | Spoofing | Operator misreads dry-run as a real run and consumes the off-peak window | mitigate | `--dry-run` prefixes the planned command with literal `DRY-RUN:` and skips invocation; usage block describes the difference explicitly. |
| T-214-01-05 | Information Disclosure | Sidecar manifest leaks credentials OR journal capture leaks PII | accept | Sidecar fields are static (`phase`, `window`, `flent_duration_sec`, timestamps, `git_head_sha`, `phase214_base_sha`, `journalctl_command`, `mutation_posture`); no env vars or secrets are emitted. Journal events from `wanctl@spectrum` are operationally non-sensitive (reflector public IPs, controller state). Steering redaction is owned by `phase213-steering-snapshot.sh` (D-08, already enforced by the delegated orchestrator). |
| T-214-01-06 | Denial of Service | Failed orchestrator leaves stale `RUN-*` dir and burns the operator's window budget | accept | Phase 213 orchestrator's existing `trap` cleanup handles poller PIDs; partial RUN dirs are diagnostic, not destructive. Operator can re-run the window per D-02 (one retry allowed for invalid artifacts/netperf failure). |
| T-214-01-07 | Spoofing | `ssh cake-shaper` connects to a wrong host (e.g. operator's `~/.ssh/config` aliases drifted) | accept | Operator's ssh config is the trust anchor; wrapper does not embed an IP. If the host is wrong, journalctl will likely fail or return unrelated data; the wrapper's JSON-parse check on `head -n 1` of the captured file will catch most format-mismatch cases. Full host-fingerprint validation is out of scope for this wrapper. |
| T-214-01-08 | Tampering | Journal capture silently empty (host clock skew puts events outside window) | mitigate | Wrapper exits 0 on empty journal file but emits a stderr warning. 214-03 aligner's ±1s tolerance (Pitfall 4) absorbs minor skew. The `phase214-window.json` sidecar records the exact `journalctl_command` used so 214-06 REPORT can cite it (Codex LOW-4). |
</threat_model>

<verification>
- `bash -n scripts/phase214-flent-matrix.sh` clean.
- All `--dry-run` gate-matrix cases above behave as specified.
- `PHASE214_BASE_SHA="$(git rev-parse HEAD)" bash scripts/phase214-flent-matrix.sh --dry-run --test-hour 03 off-peak` exits 0 with a clean working tree under `src/wanctl/`.
- Triple-check diff guard verified end-to-end with synthetic dirty/staged/committed scenarios above.
- `grep -q 'phase214-window.json' scripts/phase214-flent-matrix.sh` && `grep -q 'PHASE214_BASE_SHA' scripts/phase214-flent-matrix.sh` && `grep -q 'journal-window.ndjson' scripts/phase214-flent-matrix.sh`.
</verification>

<success_criteria>
- Wrapper script ships with executable bit set.
- Window-hour gate refuses all three out-of-window cases verified.
- D-14 triple-check src/wanctl/ guard fires on unstaged, staged, OR committed-since-base diff (Codex HIGH-2).
- `--test-hour` without `--dry-run` exits 7.
- Wrapper delegates to phase213-baseline-capture.sh with exactly the documented argv (verified by `grep`).
- After orchestrator return, wrapper pulls `journal-window.ndjson` per per-test directory using `manifest.json`-derived time window (Codex HIGH-1).
- `phase214-window.json` sidecar written at run root AND copied per-test (Codex LOW-15).
- `journalctl_command` recorded in sidecar for REPORT provenance (Codex LOW-4).
- No `systemctl restart`/`mikrotik`/`routeros`/`/etc/wanctl/*.yaml` tokens present (mutation-guard hygiene; verified by exact `grep -E -c "(systemctl restart|mikrotik|routeros|/etc/wanctl/.*\.yaml)" scripts/phase214-flent-matrix.sh` returning 0 — `-E -c` counts matching lines without printing them; bare `grep -c` is forbidden because it inflates on comments; `systemctl restart` is anchored because the wrapper legitimately uses `journalctl` which is a systemd utility).
</success_criteria>

<output>
After completion, create `.planning/phases/214-measurement-collapse-investigation/214-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`.
</output>
