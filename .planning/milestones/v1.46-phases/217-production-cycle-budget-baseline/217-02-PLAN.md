---
phase: 217-production-cycle-budget-baseline
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - .planning/perf/capture/spectrum_debug.ndjson
  - .planning/perf/capture/spectrum_journal.ndjson
  - .planning/perf/capture/spectrum-health-on-window.ndjson
  - .planning/perf/capture/spectrum-health-off-window.ndjson
  - .planning/perf/capture/pilot-spectrum_debug.ndjson
  - .planning/perf/217-capture-window.json
autonomous: false
requirements: [PERF-01]
must_haves:
  truths:
    - "D-01: Capture targets Spectrum only — the active v1.46 quality-concern link (ATT is a different transport and not the milestone subject)"
    - "D-05: Spectrum runs --profile --debug AND WANCTL_LOG_FORMAT=json via a transient systemd drop-in for the capture window only (not a permanent edit to deploy/systemd/wanctl@.service)"
    - "Pilot first: a 5-minute pilot run gates the full window on (a) jq confirming cycle_total_ms appears as a top-level JSON key in 'Cycle timing' records and (b) absent watchdog overruns/restarts; the full 1h window only proceeds if the pilot passes"
    - "At least one hour of cycle-budget data is captured on production Spectrum as NDJSON 'Cycle timing' records with cycle_total_ms and subsystem *_ms extras"
    - "D-02: A driven RRUL/upload segment runs inside the window so the change-gated router-write labels are non-zero (organic steady-state alone would under-sample the flash-wear-guarded write path)"
    - "D-05: The drop-in is reverted and production returns to steady-state INFO operation with no --profile/--debug and no WANCTL_LOG_FORMAT override — verified by systemctl cat + ps -ef"
    - "Capture window start/end UTC timestamps are recorded to a structured artifact (217-capture-window.json) so Plan 03 storage-attribution uses an explicit --from/--to matching the capture window (research Pitfall 4)"
  artifacts:
    - path: ".planning/perf/capture/spectrum_debug.ndjson"
      provides: "Retrieved DEBUG/JSON Spectrum log spanning the >=1h window (concatenated rotated set, oldest→newest) — the source for Plan 03 analysis. Gitignored — NOT committed."
      contains: "Cycle timing"
    - path: ".planning/perf/capture/spectrum_journal.ndjson"
      provides: "journalctl belt-and-braces dump for the same window, rotation-immune. Gitignored — NOT committed."
    - path: ".planning/perf/217-capture-window.json"
      provides: "Committed structured record of capture window start_utc/end_utc, driven segment timestamps, and the pilot pass/fail summary — feeds Plan 03 wanctl-history --from/--to"
      min_lines: 5
  key_links:
    - from: "systemd drop-in override.conf"
      to: "wanctl@spectrum ExecStart"
      via: "--profile --debug + WANCTL_LOG_FORMAT=json appended for the window then systemctl revert"
      pattern: "--profile --debug"
    - from: "Phase 213 harness"
      to: "Spectrum link under load"
      via: "flent rrul/tcp_upload driven inside the capture window"
      pattern: "router_write"
---

<objective>
Capture ≥1h of live cycle-budget data on production Spectrum (`1.45.0`) as JSON-format
NDJSON `"Cycle timing"` records via a transient systemd `--profile --debug` +
`WANCTL_LOG_FORMAT=json` drop-in (D-05). Gate the full window behind a 5-minute pilot that
empirically confirms `cycle_total_ms` actually appears as a top-level JSON key under
production conditions and that the DEBUG I/O cost does not trigger the watchdog. Drive a
short RRUL/upload segment inside the window (D-02). Cleanly revert the drop-in. Retrieve
the rotated `spectrum_debug.log*` set + journald dump to gitignored `.planning/perf/capture/`.
Optionally collect adjacent ON/OFF `/health` polls for the observer-effect estimate.

Purpose: Produce the raw measurement substrate using the corrected JSON data path. This is
the only plan that touches the live 24/7 production host, so every step is operator-gated
and the revert is the single most important safety gate. Pilot-first gating prevents
committing a full 1h window on assumptions that fail under production conditions
(research Open Question 1).

Output: gitignored raw NDJSON under `.planning/perf/capture/`, plus a committed
`.planning/perf/217-capture-window.json` recording window timestamps + pilot status so
Plan 03 can run windowed `wanctl-history --ingestion-rate --from/--to`. NO `src/` changes;
NO permanent unit edits; NO control-path or threshold changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md
@.planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md
@.planning/phases/217-production-cycle-budget-baseline/217-REVIEWS.md

<interfaces>
<!-- Drop-in (verbatim, from research §"Capture mechanics"): -->
<!--   Path: /etc/systemd/system/wanctl@spectrum.service.d/override.conf -->
<!--   Body: -->
<!--     [Service] -->
<!--     Environment=WANCTL_LOG_FORMAT=json -->
<!--     ExecStart= -->
<!--     ExecStart=/usr/bin/python3 /opt/wanctl/autorate_continuous.py --config /etc/wanctl/spectrum.yaml --profile --debug -->
<!-- DEBUG sink (NOT spectrum.log — that is INFO only, src/wanctl/logging_utils.py:199-208): -->
<!--   /var/log/wanctl/spectrum_debug.log (+ rotated .log.1/.log.2/.log.3) -->
<!--   configs/spectrum.yaml:153-154 -->
<!-- Belt-and-braces (rotation-immune): -->
<!--   journalctl -u wanctl@spectrum --since "<start>" --until "<end>" -o cat -->
<!-- Unit safety: WatchdogSec=30s, Restart=on-failure, CPUAffinity=1-2, MemoryHigh=512M/MemoryMax=640M -->
<!--   (deploy/systemd/wanctl@.service) -->
<!-- Per-record shape (the pilot jq gate verifies this): -->
<!--   {"timestamp":"...","level":"DEBUG","logger":"cake_continuous_spectrum","message":"Cycle timing", -->
<!--    "cycle_total_ms":<f>,"overrun":<b>,"rtt_measurement_ms":<f>,"cake_stats_ms":<f>, -->
<!--    "router_communication_ms":<f>,"logging_metrics_ms":<f>, ... (15 subsystem *_ms keys)} -->
<!-- Phase 213 driven-segment invocation: -->
<!--   scripts/phase213-baseline-capture.sh --host dallas --flent-duration 60 --tests tcp_upload,rrul --wans spectrum -->
<!-- /health endpoint: research notes Phase 213 used bound IP :9101 (loopback was not bound). -->
<!--   Use `ssh <spectrum-host> 'curl -s http://127.0.0.1:9101/health'` and fall back to bound IP if loopback not listening. -->
<!-- Revert verification (the canonical safety gate, research §"Runtime State Inventory"): -->
<!--   systemctl cat wanctl@spectrum  (no [Service] override block; matches checked-in base unit) -->
<!--   ps -ef | grep [a]utorate_continuous  (no --profile, no --debug, no WANCTL_LOG_FORMAT) -->
<!-- Sample-count floor (research §"Acceptance criteria #3", replaces broken grep-based count): -->
<!--   jq -c 'select(.message=="Cycle timing") | .cycle_total_ms' <ndjson> | wc -l  >= 60000 -->
<!-- Capture-window record schema (committed): -->
<!--   { "phase":"217", "wan":"spectrum", "wanctl_version":"1.45.0", -->
<!--     "window": {"start_utc":"<ISO>", "end_utc":"<ISO>"}, -->
<!--     "driven_segment": {"start_utc":"<ISO>", "end_utc":"<ISO>", "tests":["tcp_upload","rrul"], "host":"dallas"}, -->
<!--     "pilot": {"duration_min":5, "cycle_total_ms_present":true, "overrun_warnings":0, "unit_restarts":0}, -->
<!--     "observer_effect": {"on_window_collected":<bool>, "off_window_collected":<bool>}, -->
<!--     "revert_verified": true } -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-capture safety check + run 5-min pilot (gate the full window)</name>
  <read_first>
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Open Questions" Q1+Q2 — pilot gates JSON-key presence + disk cost; §"Capture mechanics"; §"Assumptions A1/A2/A4/A5/A8/A9"; §"Common Pitfalls" 1, 2, 6)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-05 capture safety)
    - docs/PROFILING.md (Plan 01 output — follow its Pilot section)
  </read_first>
  <what-built>
    Operator runs read-only pre-flight checks on the Spectrum production host, then a
    5-minute pilot capture with the drop-in installed. The pilot serves two purposes:
    (a) empirically confirm that `cycle_total_ms` actually appears as a top-level JSON key
    in `"Cycle timing"` records under production conditions (research Open Question 1 —
    static analysis says yes via JSONFormatter Assumption A2, but the pilot turns that
    into a runtime fact); (b) bound the disk-cost / watchdog risk by tailing journald for
    `Cycle overrun:` warnings or unit restarts at 20Hz DEBUG (research Open Question 2).
    If either gate fails, the operator aborts and surfaces — does NOT proceed to the
    full 1h window. The pilot drop-in is reverted after the 5 minutes regardless of
    outcome.
  </what-built>
  <how-to-verify>
    Operator sequence:

    1. **Pre-flight reads (read-only, no mutation):**
       - `ssh <spectrum-host> 'systemctl is-active wanctl@spectrum && /usr/bin/python3 /opt/wanctl/autorate_continuous.py --help | grep -E -- "--profile|--debug"'` — confirms unit active AND both flags wired.
       - `ssh <spectrum-host> 'sudo systemctl cat wanctl@spectrum | head -20'` — confirms base unit matches checked-in `deploy/systemd/wanctl@.service`, no pre-existing drop-in.
       - `ssh <spectrum-host> 'ls -la /var/log/wanctl/spectrum_debug.log*'` — note current rotation state (if `spectrum_debug.log` doesn't exist yet, that's expected — it's only created when `--debug` is on).
       - `ssh <spectrum-host> 'curl -s http://127.0.0.1:9101/health | head -c 200'` — confirm `/health` reachable. If loopback not bound (Phase 211 finding), fall back to bound IP and record which.

    2. **Pilot enable (5-minute window):**
       - `ssh <spectrum-host> 'sudo systemctl edit wanctl@spectrum'` — paste the drop-in EXACTLY from docs/PROFILING.md §"Enable":
         ```
         [Service]
         Environment=WANCTL_LOG_FORMAT=json
         ExecStart=
         ExecStart=/usr/bin/python3 /opt/wanctl/autorate_continuous.py --config /etc/wanctl/spectrum.yaml --profile --debug
         ```
       - `ssh <spectrum-host> 'sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum'`. Record pilot start UTC.

    3. **Watch journald during the 5 minutes:**
       - In one terminal: `ssh <spectrum-host> 'journalctl -u wanctl@spectrum -f'`. Count any `Cycle overrun:` warnings and any `Started wanctl@spectrum.service` lines after the initial restart (= unit restart). Target: ZERO unit restarts; overruns acceptable if isolated (rate-limited at `perf_profiler.py:279-284`) but a sustained cluster is a fail signal.

    4. **Post-pilot JSON-key gate:**
       - `ssh <spectrum-host> 'jq -c "select(.message == \"Cycle timing\") | keys" /var/log/wanctl/spectrum_debug.log | head -1'`. Expected output: a JSON array of keys that INCLUDES `cycle_total_ms` (and ideally one or more `*_ms` subsystem extras, e.g. `rtt_measurement_ms`).
       - Belt-and-braces: `ssh <spectrum-host> 'jq -c "select(.message == \"Cycle timing\") | .cycle_total_ms" /var/log/wanctl/spectrum_debug.log | head -5'` should print numeric values, not `null`.

    5. **Pilot revert (mandatory regardless of outcome):**
       - `ssh <spectrum-host> 'sudo systemctl revert wanctl@spectrum && sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum'`.
       - Verify revert: `ssh <spectrum-host> 'sudo systemctl cat wanctl@spectrum | grep -cE "^Environment=WANCTL_LOG_FORMAT|--profile|--debug"'` → must print `0`.

    6. **Retrieve pilot artifact** (used by Task 2 to size the disk-cost estimate for the full window):
       - `scp '<spectrum-host>:/var/log/wanctl/spectrum_debug.log*' /tmp/pilot/` then `cat /tmp/pilot/spectrum_debug.log* > .planning/perf/capture/pilot-spectrum_debug.ndjson` (the `.planning/perf/capture/` dir is gitignored from Plan 01).

    **Gate decision (operator-stated):**
    - PASS: `cycle_total_ms` present in pilot JSON keys AND zero unit restarts AND no sustained overrun cluster → proceed to Task 2 (full window).
    - FAIL: any of the above → record which gate failed in the resume signal; do NOT proceed to Task 2. The drop-in is already reverted by step 5; production is back to baseline.
  </how-to-verify>
  <files>.planning/perf/capture/pilot-spectrum_debug.ndjson (gitignored, retrieved from host)</files>
  <action>Operator runs the read-only pre-flight in step 1, installs the transient JSON drop-in for a 5-minute pilot (steps 2-4), then mandatorily reverts (step 5) and retrieves the pilot NDJSON (step 6). The pilot is a hard gate on the full 1h window: it must empirically confirm `cycle_total_ms` appears as a top-level JSON key in `"Cycle timing"` records (research Open Question 1) AND that the production disk sustains ~240 DEBUG records/sec without unit restarts or sustained overrun warnings (research Open Question 2). If either gate fails, Task 2 does NOT run. Claude provides the exact commands; the operator executes host-side steps. Per docs/PROFILING.md §"Pilot first".</action>
  <verify>Operator confirms: (a) `jq keys` output included `cycle_total_ms`; (b) zero unit restarts during the 5 min; (c) post-revert `systemctl cat` shows no drop-in lines and no `--profile`/`--debug` in running ExecStart; (d) `.planning/perf/capture/pilot-spectrum_debug.ndjson` exists with `jq -c 'select(.message=="Cycle timing")' | wc -l` >= 5000 (5 min × 20Hz × 0.83 safety).</verify>
  <done>Pilot complete and pilot revert verified. Operator declares PASS (proceed to Task 2) or FAIL (record which gate; do not proceed).</done>
  <acceptance_criteria>
    - Pre-flight: `systemctl is-active wanctl@spectrum` returns `active`; `--help` lists `--profile` and `--debug`; base unit matches checked-in file (no pre-existing override).
    - Pilot drop-in installed at `/etc/systemd/system/wanctl@spectrum.service.d/override.conf` with the verbatim body from docs/PROFILING.md; service restarted.
    - During the 5-minute pilot: zero unit restarts in `journalctl -u wanctl@spectrum`; no sustained overrun cluster.
    - JSON-key gate: `jq -c 'select(.message=="Cycle timing") | keys' /var/log/wanctl/spectrum_debug.log | head -1` lists `cycle_total_ms` (or it appears under an `extra` sub-object — the operator records which).
    - Pilot reverted: `systemctl cat wanctl@spectrum` shows no `[Service]` override block; running ExecStart contains neither `--profile` nor `--debug` nor `WANCTL_LOG_FORMAT=json`.
    - `.planning/perf/capture/pilot-spectrum_debug.ndjson` exists and contains ≥5000 `Cycle timing` records (~5 min × 20Hz × 0.83 safety).
    - Operator explicitly states PASS or FAIL in the resume signal.
  </acceptance_criteria>
  <resume-signal>Type "pilot PASS — cycle_total_ms present, no restarts, drop-in reverted" (proceeds to Task 2) OR "pilot FAIL — <reason>, drop-in reverted" (Task 2 skipped; phase replanned).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Full ≥1h capture with driven segment, optional observer-effect ON/OFF, revert + verify</name>
  <read_first>
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Capture mechanics"; §"Q4 Capture mechanics & safety"; §"Q5 Observer-effect"; §"Q6 Driven segment"; §"Runtime State Inventory" — the canonical revert verification; Pitfalls 5, 6, 7)
    - docs/PROFILING.md (the runbook from Plan 01 — Enable → Drive → Capture → Revert → Retrieve sections)
    - .planning/perf/capture/pilot-spectrum_debug.ndjson (Task 1 pilot evidence)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-02 load conditions, D-05 capture safety, D-06 artifact path)
  </read_first>
  <what-built>
    Operator-driven full capture on the production host: re-install the transient
    `--profile --debug` + `WANCTL_LOG_FORMAT=json` drop-in (gated by Task 1 PASS), run for
    ≥1h with the Phase 213 RRUL/upload driven segment landed inside the window, optionally
    collect adjacent ON/OFF `/health` poll windows for the observer-effect estimate, then
    MANDATORY revert + retrieve. Claude cannot SSH/sudo production; operator executes the
    documented sequence and validates the retrieved artifacts afterward.
  </what-built>
  <how-to-verify>
    Operator sequence (use docs/PROFILING.md as the canonical reference):

    1. **(Optional but recommended) `/health` OFF baseline** — research §"Q5 Observer-effect option (a)". Before installing the drop-in, run a 5-minute `/health` poll while the daemon is in steady-state INFO operation:
       ```bash
       for i in $(seq 1 30); do
         ssh <spectrum-host> 'curl -s http://127.0.0.1:9101/health'
         sleep 10
       done > .planning/perf/capture/spectrum-health-off-window.ndjson
       ```
       (the `.planning/perf/capture/` dir is gitignored from Plan 01)
       Record the OFF-window start/end UTC.

    2. **Install full-capture drop-in** (same body as the pilot — `docs/PROFILING.md §"Enable"`):
       - `ssh <spectrum-host> 'sudo systemctl edit wanctl@spectrum'` → paste the verbatim drop-in.
       - `ssh <spectrum-host> 'sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum'`. Record window start UTC.
       - Quick smoke: `ssh <spectrum-host> 'systemctl show wanctl@spectrum -p ExecStart | grep -- "--debug"'` (non-empty → drop-in took).
       - `ssh <spectrum-host> 'jq -c "select(.message == \"Cycle timing\") | .cycle_total_ms" /var/log/wanctl/spectrum_debug.log | tail -3'` → numeric values printed (confirms structured records flowing).

    3. **(Optional) `/health` ON window** (~5 min, in parallel with the start of the capture):
       ```bash
       for i in $(seq 1 30); do
         ssh <spectrum-host> 'curl -s http://127.0.0.1:9101/health'
         sleep 10
       done > .planning/perf/capture/spectrum-health-on-window.ndjson
       ```
       Record the ON-window start/end UTC.

    4. **Watch journald during the window:** `ssh <spectrum-host> 'journalctl -u wanctl@spectrum -f'` in a background terminal. Watch for `Cycle overrun:` warnings (rate-limited; isolated firings are fine, sustained clusters are not) and `Started wanctl@spectrum.service` (= unit restart, INVALIDATES that segment of the capture). If a restart happens: STOP, abort the window, revert (step 7), and replan.

    5. **Driven segment INSIDE the window (D-02, Pitfall 5):** from the dev VM, ~30 min into the 1h window:
       ```bash
       scripts/phase213-baseline-capture.sh --host dallas --flent-duration 60 --tests tcp_upload,rrul --wans spectrum
       ```
       Record driven_segment.start_utc and driven_segment.end_utc. This populates the change-gated `autorate_router_write_*` labels.

    6. **Let the window run ≥1h total** (~72000 cycles at 20Hz). Record window end UTC.

    7. **REVERT (MANDATORY, single most important safety gate):**
       - `ssh <spectrum-host> 'sudo systemctl revert wanctl@spectrum && sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum'`.
       - **Verify revert** (research §"Runtime State Inventory" canonical check):
         - `ssh <spectrum-host> 'sudo systemctl cat wanctl@spectrum'` MUST show base unit only — no `[Service]` override block, no `override.conf` mentioned, no `Environment=WANCTL_LOG_FORMAT=json`, no `--profile` or `--debug` in the ExecStart.
         - `ssh <spectrum-host> 'ps -ef | grep [a]utorate_continuous'` MUST show NEITHER `--profile` NOR `--debug` NOR `WANCTL_LOG_FORMAT=json` in the running command line.
         - `ssh <spectrum-host> 'systemctl is-active wanctl@spectrum'` → `active`.
         - `ssh <spectrum-host> 'curl -s http://127.0.0.1:9101/health | jq .version'` → `"1.45.0"`.

    8. **Retrieve (DEBUG sink + journald belt-and-braces, research §"Retrieval"):**
       - `ssh <spectrum-host> 'ls -la /var/log/wanctl/spectrum_debug.log*'` — note the rotated set.
       - `scp '<spectrum-host>:/var/log/wanctl/spectrum_debug.log*' .planning/perf/capture/`. Concatenate chronologically (oldest→newest): `cat .planning/perf/capture/spectrum_debug.log.3 .planning/perf/capture/spectrum_debug.log.2 .planning/perf/capture/spectrum_debug.log.1 .planning/perf/capture/spectrum_debug.log > .planning/perf/capture/spectrum_debug.ndjson 2>/dev/null` (skip any backup numbers that don't exist).
       - Belt-and-braces (rotation-immune): `ssh <spectrum-host> 'journalctl -u wanctl@spectrum --since "<window start UTC>" --until "<window end UTC>" -o cat' > .planning/perf/capture/spectrum_journal.ndjson`.
       - Both files land in the gitignored `.planning/perf/capture/` dir — they are NOT committed (Pitfall 7).

    9. **Record window metadata (committed):** write `.planning/perf/217-capture-window.json` with the schema in the `<interfaces>` block (window start/end, driven_segment timestamps, pilot result, observer_effect flags, revert_verified). This file IS committed — it's how Plan 03 sources its `wanctl-history --from/--to` window.
  </how-to-verify>
  <files>/etc/systemd/system/wanctl@spectrum.service.d/override.conf (transient host state — reverted), .planning/perf/capture/spectrum_debug.ndjson (gitignored), .planning/perf/capture/spectrum_journal.ndjson (gitignored), .planning/perf/capture/spectrum-health-on-window.ndjson (gitignored, optional), .planning/perf/capture/spectrum-health-off-window.ndjson (gitignored, optional), .planning/perf/217-capture-window.json (committed)</files>
  <action>Operator runs the full enable→drive→capture→revert→retrieve sequence in <how-to-verify> using docs/PROFILING.md as the canonical reference. Re-install the verbatim transient drop-in from Task 1 (now gated PASS), optionally bracket the window with a ~5-min `/health` OFF baseline before and ~5-min `/health` ON window at the start for the observer-effect estimate, run for ≥1h with the Phase 213 RRUL/upload driven segment landed mid-window, MANDATORY `systemctl revert` then verify `systemctl cat` shows no drop-in AND `ps -ef` shows no `--profile`/`--debug`/`WANCTL_LOG_FORMAT=json` in the running ExecStart AND `/health.version == 1.45.0`. Retrieve the rotated `spectrum_debug.log*` set + journald dump into gitignored `.planning/perf/capture/`. Write `.planning/perf/217-capture-window.json` recording the window timestamps + driven-segment timestamps + pilot summary + revert_verified flag (committed; Plan 03 uses it to derive `wanctl-history --from/--to`, addressing research Pitfall 4). If `/health` ON/OFF windows were not collected, mark `observer_effect.on_window_collected: false` and `off_window_collected: false` — Plan 03 will fall through to the documented-caveat observer-effect shape (research §"Q5 option (b)"). Raw NDJSON stays gitignored — only `217-capture-window.json` is committed from this plan. Claude provides exact commands; operator executes host-side steps.</action>
  <verify>Operator confirms: (a) `systemctl cat wanctl@spectrum` shows NO override block and running ExecStart has neither `--profile` nor `--debug` nor `WANCTL_LOG_FORMAT=json`; (b) `.planning/perf/capture/spectrum_debug.ndjson` exists and `jq -c 'select(.message=="Cycle timing") | .cycle_total_ms' .planning/perf/capture/spectrum_debug.ndjson | wc -l` ≥ 60000; (c) `.planning/perf/217-capture-window.json` exists, parses as JSON, contains `window.start_utc`, `window.end_utc`, `revert_verified: true`.</verify>
  <done>≥1h Spectrum capture retrieved into gitignored `.planning/perf/capture/`, driven segment landed inside the window, drop-in reverted and verified, window metadata committed.</done>
  <acceptance_criteria>
    - Drop-in created at `/etc/systemd/system/wanctl@spectrum.service.d/override.conf` with the exact body from docs/PROFILING.md; ExecStart contains `--profile --debug`; `Environment=WANCTL_LOG_FORMAT=json` present; service restarted.
    - During the window, `jq -c 'select(.message=="Cycle timing")' /var/log/wanctl/spectrum_debug.log | head -3` produces at least 3 valid JSON records each with `cycle_total_ms` populated.
    - The Phase 213 driven segment ran INSIDE the window (start_utc and end_utc both between window.start_utc and window.end_utc); recorded in `217-capture-window.json`.
    - Window spans ≥1h (`window.end_utc - window.start_utc >= 3600 seconds`).
    - **Revert verified (the single most important gate):** `systemctl cat wanctl@spectrum` shows no `[Service]` override block AND running `ExecStart` from `ps -ef` contains NEITHER `--profile` NOR `--debug` NOR `WANCTL_LOG_FORMAT=json` AND `systemctl is-active` is `active` AND `/health.version == "1.45.0"`. `revert_verified: true` recorded in `217-capture-window.json`.
    - `.planning/perf/capture/spectrum_debug.ndjson` exists and `jq -c 'select(.message=="Cycle timing") | .cycle_total_ms' | wc -l` ≥ 60000 (research §"Acceptance criteria #3"; ≥~50min at 20Hz tolerates ≤10min rotation loss; target ~72000 for a full hour).
    - `.planning/perf/capture/spectrum_journal.ndjson` exists (rotation-immune belt-and-braces).
    - `.planning/perf/217-capture-window.json` exists, parses as JSON, contains all required keys per the `<interfaces>` schema, includes `revert_verified: true`.
    - Raw `*.ndjson` files in `.planning/perf/capture/` are NOT in `git status` (gitignored per Plan 01 Task 1).
  </acceptance_criteria>
  <resume-signal>Type "full capture retrieved + revert verified" once `spectrum_debug.ndjson` is in `.planning/perf/capture/`, `217-capture-window.json` is written, and `systemctl cat` + `ps -ef` confirm no drop-in residue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate retrieved NDJSON spans the window with router-write coverage</name>
  <read_first>
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Acceptance criteria the planner can encode" #3 + #5; §"Validation Architecture"; Pitfall 5)
    - .planning/perf/capture/spectrum_debug.ndjson (the retrieved capture)
    - .planning/perf/217-capture-window.json (the committed window metadata from Task 2)
  </read_first>
  <files>.planning/perf/217-capture-window.json (annotated with sample-count + router-write coverage)</files>
  <action>Validate the retrieved NDJSON is analysis-ready before Plan 03 consumes it. Run the canonical jq checks (research §"Acceptance criteria" #3 + #5):
(1) Cycle-timing record count: `jq -c 'select(.message=="Cycle timing") | .cycle_total_ms' .planning/perf/capture/spectrum_debug.ndjson | wc -l` MUST be ≥ 60000.
(2) Router-write coverage: `jq -c 'select(.message=="Cycle timing") | select((.router_write_download_ms // 0) > 0 or (.router_write_upload_ms // 0) > 0)' .planning/perf/capture/spectrum_debug.ndjson | wc -l` — record the value. If 0, the driven segment did not populate the change-gated router-write labels; flag as a gap.
(3) cycle_total_ms numeric: `jq -c 'select(.message=="Cycle timing") | .cycle_total_ms | type' .planning/perf/capture/spectrum_debug.ndjson | sort -u` MUST contain `"number"` only (not `"null"` — that would mean the JSON-key gate from Task 1 was wrong).
(4) Update `.planning/perf/217-capture-window.json` to ADD a new top-level `validation` block with keys: `cycle_timing_records` (int from check 1), `router_write_records` (int from check 2), `router_write_coverage` (string: `"present"` if router_write_records > 0, else `"absent_gap"`), `cycle_total_types` (array from check 3).
Do NOT modify the raw NDJSON. If `router_write_coverage == "absent_gap"`, record it — Plan 03 will couple it to the verdict (unconditional no-action requires the driven segment landed; pure steady-state cannot return unconditional no-action since the router-communication category cannot be evaluated under change events). This task only inspects committed/gitignored artifacts; it makes no host calls.</action>
  <verify>
    <automated>set -e; test -f .planning/perf/capture/spectrum_debug.ndjson && test -f .planning/perf/217-capture-window.json; N=$(jq -c 'select(.message=="Cycle timing") | .cycle_total_ms' .planning/perf/capture/spectrum_debug.ndjson | wc -l); echo "cycle_timing samples: $N"; [ "$N" -ge 60000 ]; RW=$(jq -c 'select(.message=="Cycle timing") | select((.router_write_download_ms // 0) > 0 or (.router_write_upload_ms // 0) > 0)' .planning/perf/capture/spectrum_debug.ndjson | wc -l); echo "router_write records: $RW"; TYPES=$(jq -c 'select(.message=="Cycle timing") | .cycle_total_ms | type' .planning/perf/capture/spectrum_debug.ndjson | sort -u); echo "cycle_total types: $TYPES"; echo "$TYPES" | grep -q '"number"' && ! echo "$TYPES" | grep -q '"null"'; jq -e '.validation.cycle_timing_records >= 60000 and (.validation.router_write_coverage == "present" or .validation.router_write_coverage == "absent_gap")' .planning/perf/217-capture-window.json</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf/capture/spectrum_debug.ndjson` exists and contains ≥ 60000 `"Cycle timing"` records with numeric `cycle_total_ms`.
    - `.planning/perf/217-capture-window.json` exists and has been annotated with a `validation` block containing `cycle_timing_records`, `router_write_records`, `router_write_coverage` (one of `"present"` or `"absent_gap"`), and `cycle_total_types` (array including `"number"`, NOT including `"null"`).
    - `cycle_total_ms` values are numeric, not null (proves JSONFormatter preserved the `extra=` key as expected per research Assumption A2 — empirically confirmed at the full-window scale).
    - No modification to the raw NDJSON content.
  </acceptance_criteria>
  <done>The raw capture is confirmed analysis-ready (≥60000 cycle samples, router-write coverage status recorded as `present` or `absent_gap` in the committed window metadata).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live production host | systemd drop-in transiently changes a 24/7 daemon's ExecStart and log format |
| production NDJSON → committed repo artifact | JSON-format DEBUG logs may contain richer structured operator detail than text — must NOT be committed; only the aggregate `.profile.json` + `217-capture-window.json` cross the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-03 | Denial of service | wanctl@spectrum control loop | mitigate | DEBUG + JSON-format adds ~240 structured log writes/sec; `WatchdogSec=30s` could restart the loop if I/O stalls it. **Pilot-first design (Task 1) is the primary mitigation** — empirically bounds disk cost on a 5-min window before committing to 1h. During the full window operator tails `journalctl -u wanctl@spectrum -f`; on unit restart or sustained overrun cluster, abort and revert. Capture is time-bounded; revert is mandatory regardless of outcome. |
| T-217-04 | Elevation / persistence | systemd drop-in left enabled | mitigate | The drop-in is the only persistent host state. Mandatory `systemctl revert` after both pilot AND full window; canonical verification gate is `systemctl cat wanctl@spectrum` shows no override block AND `ps -ef` shows no `--profile`/`--debug`/`WANCTL_LOG_FORMAT=json` in the running ExecStart AND `/health.version == 1.45.0`. This is the phase's single most important safety gate (D-05). `revert_verified: true` is committed to `217-capture-window.json` as the durable attestation. |
| T-217-05 | Information disclosure | committed artifacts (217-capture-window.json + later .profile.json from Plan 03) | mitigate | Per CLAUDE.md public-safe policy: raw `spectrum_debug.ndjson` and `spectrum_journal.ndjson` stay in `.planning/perf/capture/` which is gitignored from Plan 01 (Pitfall 7). The committed `217-capture-window.json` contains only window timestamps, sample counts, and the pilot/revert booleans — no operating points, IPs, or credentials by construction. Plan 03 enforces a pre-commit secret scan on the aggregate `.profile.json` before commit. |
| T-217-08 | Loss of evidence / capture integrity | `RotatingFileHandler` eviction (10MB×3 default) at JSON-DEBUG/20Hz silently truncating the window | mitigate | At ~240 structured JSON records/sec × ~250 bytes/record ≈ ~60 KB/sec, a 10MB file rolls every ~3 min; with `backupCount=3` the file-based window is ~12 min. Mitigation: operator concatenates all rotated backups `.log.3` → `.log.2` → `.log.1` → `.log` chronologically (Task 2 step 8), AND captures `journalctl --since/--until` belt-and-braces (rotation-immune). Task 3's `≥60000 cycle_total samples` floor catches truncated windows; below the floor the capture is rejected and rerun rather than silently passing. |
| T-217-09 | Information disclosure (JSON capture richer than text) | JSON-format `extra=` keys preserved verbatim | mitigate | JSONFormatter preserves all non-internal `record.__dict__` keys (research Assumption A2), so JSON-format DEBUG logs may carry richer structured operator detail than the text format would. The mitigation has two layers: (a) raw NDJSON never enters git (`.planning/perf/capture/` gitignored); (b) the Plan 03 pre-commit scrub explicitly inspects the aggregate `.profile.json` (which is timing stats only — `count/min/p50/avg/max/p95/p99` per label, no operating points) and any sanitized excerpts in the summary. |

No HIGH threats remain mitigated-to-acceptable: the capture is pilot-gated, transient, watchdog-supervised, reverted-and-verified, raw NDJSON is gitignored, and the only data leaving the host into git is window metadata + (in Plan 03) aggregate timing stats.
</threat_model>

<verification>
- Pilot (Task 1): deployed binary supports `--profile` + `--debug`; unit root-editable + active; pilot drop-in installed; `jq keys` lists `cycle_total_ms`; zero unit restarts; pilot reverted (canonical check).
- Full window (Task 2): drop-in re-installed with verbatim body; `Cycle timing` records contain numeric `cycle_total_ms`; driven segment runs inside the window; window ≥1h; revert verified (`systemctl cat` no override block + `ps -ef` no `--profile`/`--debug`/`WANCTL_LOG_FORMAT=json` + `/health.version == "1.45.0"`); `217-capture-window.json` committed with all required keys including `revert_verified: true`.
- Validation (Task 3): NDJSON has ≥60000 `Cycle timing` records with numeric `cycle_total_ms`; router-write coverage is `present` OR explicitly `absent_gap`; `217-capture-window.json` annotated with `validation` block.
- No `src/` files modified; no permanent unit edit.
- Raw `.ndjson` files are NOT in `git status` (they live in the gitignored capture dir).
</verification>

<success_criteria>
- ≥1h of Spectrum cycle-budget data captured under `--profile --debug` + `WANCTL_LOG_FORMAT=json` as structured NDJSON `Cycle timing` records (PERF-01 capture, corrected data path).
- Pilot-first gating prevented committing a full window on assumptions that fail under production conditions (research Open Question 1/2 resolved empirically).
- Driven segment landed inside the window (router-write labels populated, or gap explicitly recorded).
- Drop-in reverted and verified — production back to steady-state INFO operation, no observer-effect left running.
- Committed `.planning/perf/217-capture-window.json` provides Plan 03 with the explicit `--from/--to` window for `wanctl-history --ingestion-rate` (research Pitfall 4 closed).
- Raw NDJSON stays out of git (Pitfall 7 closed).
</success_criteria>

<output>
After completion, create `.planning/phases/217-production-cycle-budget-baseline/217-02-SUMMARY.md`
recording: pilot pass/fail summary, window start/end UTC, driven-segment start/end UTC + outcome,
whether `--debug` held or fell back, observer-effect ON/OFF windows collected or skipped,
revert-verification output, raw NDJSON path (gitignored) + sample count, and the committed
`217-capture-window.json` path.
</output>
</content>
</invoke>