---
phase: 113-network-engineering-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/113-network-engineering-audit/113-01-findings.md
autonomous: true
requirements: [NETENG-01, NETENG-02]

must_haves:
  truths:
    - "CAKE parameters on production VM match config YAML for both WANs"
    - "wanctl-check-cake tool produces matching results on production"
    - "DSCP mangle rules on MikroTik are documented with CAKE tin mapping"
    - "End-to-end DSCP path from MikroTik through bridge to CAKE tins is proven correct"
  artifacts:
    - path: ".planning/phases/113-network-engineering-audit/113-01-findings.md"
      provides: "CAKE parameter verification + DSCP trace documentation"
      contains: "NETENG-01"
  key_links:
    - from: "/etc/wanctl/spectrum.yaml"
      to: "tc -j qdisc show"
      via: "CAKE param readback comparison"
      pattern: "overhead|diffserv|memlimit|ack-filter|split-gso"
    - from: "/ip/firewall/mangle"
      to: "CAKE diffserv4 tins"
      via: "DSCP classification chain"
      pattern: "EF|AF41|CS1|Voice|Video|Bulk"
---

<objective>
Verify CAKE configuration and DSCP classification on the production VM.

Purpose: Confirm that CAKE parameters per WAN match what config YAML specifies, and document the full DSCP end-to-end path from MikroTik mangle rules through the transparent bridge into CAKE diffserv4 tins.
Output: 113-01-findings.md with CAKE parameter audit tables and DSCP trace documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-network-engineering-audit/113-CONTEXT.md
@src/wanctl/cake_params.py
@src/wanctl/check_cake.py
@src/wanctl/backends/linux_cake.py
@docs/CONFIG_SCHEMA.md

<interfaces>
<!-- Key types and contracts from cake_params.py -->

From src/wanctl/cake_params.py:
```python
UPLOAD_DEFAULTS = {
    "diffserv": "diffserv4",
    "split-gso": True,
    "ack-filter": True,   # Upload only
    "ingress": False,
    "ecn": False,
}

DOWNLOAD_DEFAULTS = {
    "diffserv": "diffserv4",
    "split-gso": True,
    "ack-filter": False,
    "ingress": True,       # Download only
    "ecn": True,           # Download only
}

TUNABLE_DEFAULTS = {
    "memlimit": "32mb",    # 33554432 bytes in tc readback
    "rtt": "100ms",        # 100000 us in tc readback
}

OVERHEAD_READBACK = {
    "docsis": {"overhead": 18},
    "bridged-ptm": {"overhead": 22},
    "ethernet": {"overhead": 38},
}
```

From src/wanctl/check_cake.py:
```
CLI: wanctl-check-cake <config.yaml>
Compares expected CAKE params from config against router readback.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CAKE parameter verification via tc readback and wanctl-check-cake (NETENG-01)</name>
  <files>.planning/phases/113-network-engineering-audit/113-01-findings.md</files>
  <read_first>
    - src/wanctl/cake_params.py
    - src/wanctl/check_cake.py
    - src/wanctl/backends/linux_cake.py
  </read_first>
  <action>
Per D-01, D-02, D-03:

1. SSH to production VM and read CAKE config from both WANs:
   ```
   ssh kevin@10.10.110.223 "sudo cat /etc/wanctl/spectrum.yaml"
   ssh kevin@10.10.110.223 "sudo cat /etc/wanctl/att.yaml"
   ```

2. Extract expected CAKE parameters from each config's `cake_params:` section. Merge with direction-aware defaults from cake_params.py (UPLOAD_DEFAULTS, DOWNLOAD_DEFAULTS, TUNABLE_DEFAULTS). Parameters to verify per D-03: overhead, diffserv4, ack-filter, split-gso, memlimit.

3. Get actual CAKE readback from production qdiscs via tc JSON:
   ```
   ssh kevin@10.10.110.223 "sudo tc -j qdisc show"
   ```
   Parse JSON output to find CAKE qdiscs on bridge interfaces (br-spectrum, br-att). Extract overhead, diffserv_mode, ack_filter, split_gso, memory_limit, rtt values.

4. Per D-02, also run wanctl-check-cake on the VM:
   ```
   ssh kevin@10.10.110.223 "cd /opt/wanctl && sudo -E python3 -m wanctl.check_cake /etc/wanctl/spectrum.yaml --json"
   ssh kevin@10.10.110.223 "cd /opt/wanctl && sudo -E python3 -m wanctl.check_cake /etc/wanctl/att.yaml --json"
   ```

5. Compare expected vs actual for each WAN and each direction (upload/download). Document in a table format:
   | Parameter | Expected (config+defaults) | Actual (tc readback) | Match? |

6. Document any discrepancies. If all match, state "All CAKE parameters verified correct."

Write findings to .planning/phases/113-network-engineering-audit/113-01-findings.md under a "## NETENG-01: CAKE Parameter Verification" heading.
  </action>
  <verify>
    <automated>grep -c "NETENG-01" .planning/phases/113-network-engineering-audit/113-01-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## NETENG-01" heading
    - File contains parameter comparison table for Spectrum (upload + download)
    - File contains parameter comparison table for ATT (upload + download)
    - File contains wanctl-check-cake output for both WANs
    - Each table has columns: Parameter, Expected, Actual, Match
    - File states overall pass/fail verdict for CAKE param verification
  </acceptance_criteria>
  <done>CAKE parameters for both WANs verified against config+defaults with comparison tables, wanctl-check-cake output captured, and pass/fail verdict stated</done>
</task>

<task type="auto">
  <name>Task 2: DSCP end-to-end trace documentation (NETENG-02)</name>
  <files>.planning/phases/113-network-engineering-audit/113-01-findings.md</files>
  <read_first>
    - src/wanctl/cake_params.py
    - src/wanctl/backends/linux_cake.py
  </read_first>
  <action>
Per D-04, D-05, D-06:

1. Per D-05, read MikroTik mangle rules via REST API from the workstation:
   ```
   curl -sk -u admin:$(ssh kevin@10.10.110.223 'sudo grep ROUTER_PASSWORD /etc/wanctl/secrets | cut -d= -f2') \
     "https://10.10.99.1/rest/ip/firewall/mangle" | python3 -m json.tool
   ```
   If curl is awkward with secrets, alternatively SSH to VM and use the wanctl router client:
   ```
   ssh kevin@10.10.110.223 "curl -sk -u admin:\$ROUTER_PASSWORD https://10.10.99.1/rest/ip/firewall/mangle"
   ```
   Or use `sudo -E` to load the environment file first.

2. Per D-04, this is a design path trace -- no live test traffic. Read the mangle rule output and identify:
   - Rules that set DSCP marks (action=mark-packet with new-dscp-value or passthrough with set-priority)
   - Which traffic classes get which DSCP values
   - The chain these rules are in (prerouting, forward, etc.)

3. Per D-06, map the full classification chain:
   - MikroTik mangle marks -> bridge passthrough behavior (transparent bridge preserves DSCP)
   - CAKE diffserv4 tin classification mapping:
     * Tin 0 (Bulk): CS1, LE
     * Tin 1 (Best Effort): CS0 (default)
     * Tin 2 (Video): AF41, AF42, AF43, CS4
     * Tin 3 (Voice): EF, VA, CS6, CS7
   - Document: EF -> Voice tin, AF41 -> Video tin, CS1 -> Bulk tin

4. Document the complete path as a flow diagram (text-based):
   ```
   Client traffic -> MikroTik mangle (DSCP marking) -> bridge interface -> CAKE diffserv4
     EF (DSCP 46) -> Tin 3 (Voice) -- lowest latency, strict priority
     AF41 (DSCP 34) -> Tin 2 (Video) -- bandwidth-weighted
     CS1 (DSCP 8) -> Tin 0 (Bulk) -- lowest priority
     CS0 (DSCP 0) -> Tin 1 (Best Effort) -- default
   ```

5. Note: The transparent bridge does NOT modify DSCP values -- they pass through unchanged from MikroTik to the CAKE qdisc on the bridge member port.

Append findings to .planning/phases/113-network-engineering-audit/113-01-findings.md under "## NETENG-02: DSCP End-to-End Trace".
  </action>
  <verify>
    <automated>grep -c "NETENG-02" .planning/phases/113-network-engineering-audit/113-01-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "## NETENG-02" heading
    - File contains MikroTik mangle rule listing (actual rules from router)
    - File documents DSCP-to-CAKE-tin mapping (EF->Voice, AF41->Video, CS1->Bulk)
    - File contains end-to-end flow diagram showing traffic path
    - File notes transparent bridge DSCP passthrough behavior
  </acceptance_criteria>
  <done>DSCP end-to-end trace documented showing MikroTik mangle rules, bridge passthrough, and CAKE tin classification with EF/AF41/CS1 mappings verified</done>
</task>

</tasks>

<verification>
- 113-01-findings.md exists with both NETENG-01 and NETENG-02 sections
- CAKE parameter tables show expected vs actual for both WANs
- DSCP trace shows complete path from MikroTik to CAKE tins
- wanctl-check-cake output captured for both WANs
</verification>

<success_criteria>
- CAKE parameters verified correct (or discrepancies documented) for Spectrum and ATT WANs
- DSCP classification chain fully documented from MikroTik mangle rules through bridge to CAKE tins
- EF->Voice, AF41->Video, CS1->Bulk mappings confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/113-network-engineering-audit/113-01-SUMMARY.md`
</output>
