---
phase: 106-cake-optimization-parameters
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - src/wanctl/backends/linux_cake.py
  - tests/test_linux_cake_backend.py
autonomous: true
requirements: [CAKE-05, CAKE-06, CAKE-10]

must_haves:
  truths:
    - "initialize_cake with overhead_keyword='docsis' produces tc command with standalone 'docsis' token (not 'overhead docsis')"
    - "initialize_cake with overhead_keyword='bridged-ptm' produces tc command with standalone 'bridged-ptm' token"
    - "initialize_cake still supports numeric overhead as fallback (backward compatibility)"
    - "overhead_keyword takes priority over numeric overhead when both present"
    - "build_cake_params output feeds directly into initialize_cake without transformation"
  artifacts:
    - path: "src/wanctl/backends/linux_cake.py"
      provides: "Extended initialize_cake with overhead_keyword support"
      contains: "overhead_keyword"
    - path: "tests/test_linux_cake_backend.py"
      provides: "Tests for overhead_keyword in initialize_cake"
      contains: "overhead_keyword"
  key_links:
    - from: "src/wanctl/cake_params.py"
      to: "src/wanctl/backends/linux_cake.py"
      via: "params dict with overhead_keyword key consumed by initialize_cake"
      pattern: 'params\\["overhead_keyword"\\]'
    - from: "tests/test_linux_cake_backend.py"
      to: "src/wanctl/cake_params.py"
      via: "integration test imports build_cake_params to verify output feeds initialize_cake"
      pattern: "from wanctl.cake_params import"
---

<objective>
Extend LinuxCakeBackend.initialize_cake() to handle overhead keywords as standalone tc tokens, and add integration tests verifying that build_cake_params() output feeds directly into initialize_cake().

Purpose: The tc-cake(8) overhead keywords (docsis, bridged-ptm) are standalone tokens on the command line, not key-value pairs. The current initialize_cake only handles numeric overhead. This plan adds keyword support and verifies the full params->tc pipeline.
Output: Modified linux_cake.py + extended test_linux_cake_backend.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-cake-optimization-parameters/106-CONTEXT.md
@.planning/phases/106-cake-optimization-parameters/106-RESEARCH.md
@.planning/phases/106-cake-optimization-parameters/106-01-SUMMARY.md

@src/wanctl/backends/linux_cake.py
@tests/test_linux_cake_backend.py
@src/wanctl/cake_params.py

<interfaces>
<!-- From Plan 01 output (cake_params.py) -->
```python
from wanctl.cake_params import build_cake_params, build_expected_readback

# build_cake_params("upload", {"overhead": "docsis"}, bandwidth_kbit=500000) returns:
# {
#     "diffserv": "diffserv4",
#     "split-gso": True,
#     "ack-filter": True,
#     "ingress": False,
#     "ecn": False,
#     "memlimit": "32mb",
#     "rtt": "100ms",
#     "overhead_keyword": "docsis",
#     "bandwidth": "500000kbit",
# }

# build_expected_readback(params) returns:
# {"overhead": 18, "diffserv": "diffserv4", "rtt": 100000, "memlimit": 33554432}
```

<!-- Current initialize_cake params handling (to be extended) -->
From src/wanctl/backends/linux_cake.py:
```python
# Lines 327-344: Current param handling
# Key-value: bandwidth, diffserv, overhead (int), mpu (int), memlimit (str), rtt (str)
# Boolean flags: split-gso, ack-filter, ingress, ecn
# MISSING: overhead_keyword (standalone token)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend initialize_cake for overhead_keyword support</name>
  <files>src/wanctl/backends/linux_cake.py, tests/test_linux_cake_backend.py</files>
  <read_first>
    - src/wanctl/backends/linux_cake.py (full file, especially initialize_cake lines 304-355)
    - tests/test_linux_cake_backend.py (full file, especially TestInitializeCake class)
    - .planning/phases/106-cake-optimization-parameters/106-RESEARCH.md (overhead keyword syntax section and anti-patterns)
  </read_first>
  <behavior>
    Overhead keyword as standalone token:
    - initialize_cake({"overhead_keyword": "docsis"}) produces tc command containing "docsis" as standalone arg
    - initialize_cake({"overhead_keyword": "docsis"}) does NOT produce "overhead docsis" (key-value pair)
    - initialize_cake({"overhead_keyword": "bridged-ptm"}) produces tc command containing "bridged-ptm" as standalone arg

    Priority:
    - initialize_cake({"overhead_keyword": "docsis", "overhead": 18}) uses overhead_keyword (standalone), ignores numeric overhead
    - initialize_cake({"overhead": 18}) still works (backward compat, produces "overhead 18")

    Full param set from builder:
    - initialize_cake({"diffserv": "diffserv4", "split-gso": True, "ack-filter": True, "overhead_keyword": "docsis", "memlimit": "32mb", "rtt": "100ms", "bandwidth": "500000kbit"}) produces:
      tc qdisc replace dev eth0 root cake bandwidth 500000kbit diffserv4 docsis memlimit 32mb rtt 100ms split-gso ack-filter

    Existing behavior preserved:
    - initialize_cake({"bandwidth": "500000kbit"}) still works exactly as before
    - initialize_cake({"diffserv": "diffserv4", "overhead": 18, "mpu": 64}) still works
    - Boolean flags still appended when truthy
  </behavior>
  <action>
    Modify src/wanctl/backends/linux_cake.py initialize_cake() method:

    1. Update the docstring params section to document the new overhead_keyword key:
       ```
       - overhead_keyword: str (standalone tc token like "docsis", "bridged-ptm")
       ```

    2. Add overhead_keyword handling BEFORE the existing numeric overhead handler (priority per D-09).
       Insert between the diffserv and overhead blocks (after line 332, before line 333):

       ```python
       if "overhead_keyword" in params:
           cmd_args.append(str(params["overhead_keyword"]))
       elif "overhead" in params:
           cmd_args.extend(["overhead", str(params["overhead"])])
       ```

       This replaces the existing `if "overhead" in params:` block. The elif ensures overhead_keyword takes priority.

    3. The mpu block stays unchanged (mpu is only used with numeric overhead, keywords include their own mpu internally).

    Add tests to tests/test_linux_cake_backend.py TestInitializeCake class:

    4. test_initialize_cake_overhead_keyword_standalone:
       - Call initialize_cake({"overhead_keyword": "docsis"})
       - Assert "docsis" in cmd
       - Assert cmd does NOT contain ["overhead", "docsis"] as consecutive elements

    5. test_initialize_cake_overhead_keyword_bridged_ptm:
       - Call initialize_cake({"overhead_keyword": "bridged-ptm"})
       - Assert "bridged-ptm" in cmd

    6. test_initialize_cake_overhead_keyword_priority:
       - Call initialize_cake({"overhead_keyword": "docsis", "overhead": 99})
       - Assert "docsis" in cmd
       - Assert "99" NOT in cmd (numeric overhead skipped)

    7. test_initialize_cake_numeric_overhead_fallback:
       - Call initialize_cake({"overhead": 18})
       - Assert "overhead" in cmd AND "18" in cmd (backward compat)

    8. test_initialize_cake_full_spectrum_upload:
       - Call initialize_cake with full Spectrum upload param set from builder
       - Assert all expected tokens in cmd: bandwidth, diffserv4, docsis, memlimit, 32mb, rtt, 100ms, split-gso, ack-filter
       - Assert "ingress" NOT in cmd, "ecn" NOT in cmd

    9. test_initialize_cake_full_att_download:
       - Call initialize_cake with full ATT download param set from builder
       - Assert all expected tokens in cmd: bandwidth, diffserv4, bridged-ptm, memlimit, 32mb, rtt, 100ms, split-gso, ingress, ecn
       - Assert "ack-filter" NOT in cmd

    Also add an integration test that imports build_cake_params:

    10. Add a new TestCakeParamsIntegration class:
        - test_builder_output_accepted_by_initialize_cake: call build_cake_params("upload", {"overhead": "docsis"}, 500000), pass result to initialize_cake(), assert success (rc=0)
        - test_builder_readback_matches_expected: call build_cake_params + build_expected_readback, verify the readback dict has correct numeric types

    Follow existing test patterns: @patch subprocess.run, check cmd list contents.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_linux_cake_backend.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/wanctl/backends/linux_cake.py contains 'if "overhead_keyword" in params:'
    - src/wanctl/backends/linux_cake.py contains 'cmd_args.append(str(params["overhead_keyword"]))'
    - src/wanctl/backends/linux_cake.py contains 'elif "overhead" in params:' (not standalone if)
    - tests/test_linux_cake_backend.py contains "test_initialize_cake_overhead_keyword_standalone"
    - tests/test_linux_cake_backend.py contains "test_initialize_cake_overhead_keyword_bridged_ptm"
    - tests/test_linux_cake_backend.py contains "test_initialize_cake_overhead_keyword_priority"
    - tests/test_linux_cake_backend.py contains "test_initialize_cake_full_spectrum_upload"
    - tests/test_linux_cake_backend.py contains "test_initialize_cake_full_att_download"
    - tests/test_linux_cake_backend.py contains "TestCakeParamsIntegration" or "test_builder_output_accepted_by_initialize_cake"
    - .venv/bin/pytest tests/test_linux_cake_backend.py -x -v passes with 0 failures
    - .venv/bin/pytest tests/test_cake_params.py tests/test_linux_cake_backend.py -x -v passes (cross-file)
    - .venv/bin/ruff check src/wanctl/backends/linux_cake.py passes
    - .venv/bin/mypy src/wanctl/backends/linux_cake.py passes
  </acceptance_criteria>
  <done>
    initialize_cake handles overhead_keyword as standalone tc token with priority over numeric overhead.
    Full integration tests verify builder output feeds directly into initialize_cake.
    All existing tests still pass (backward compatibility preserved).
    Ruff and mypy clean.
  </done>
</task>

</tasks>

<verification>
1. `.venv/bin/pytest tests/test_linux_cake_backend.py tests/test_cake_params.py -x -v` -- all tests pass
2. `.venv/bin/ruff check src/wanctl/backends/linux_cake.py src/wanctl/cake_params.py` -- lint clean
3. `.venv/bin/mypy src/wanctl/backends/linux_cake.py src/wanctl/cake_params.py` -- type check clean
4. Full test suite: `.venv/bin/pytest tests/ -x -v` -- no regressions
5. Verify overhead_keyword produces standalone token: grep 'cmd_args.append.*overhead_keyword' in linux_cake.py
6. Verify elif chain: grep 'elif "overhead"' in linux_cake.py (not standalone if)
</verification>

<success_criteria>
- initialize_cake handles overhead_keyword as standalone tc token (docsis, bridged-ptm)
- Numeric overhead fallback preserved for backward compatibility
- Integration tests prove build_cake_params -> initialize_cake pipeline works
- Full Spectrum upload and ATT download scenarios verified end-to-end
- All existing tests pass (no regressions)
- Ruff and mypy clean
</success_criteria>

<output>
After completion, create `.planning/phases/106-cake-optimization-parameters/106-02-SUMMARY.md`
</output>
