---
phase: 114-code-quality-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/114-code-quality-safety/114-01-exception-triage.md
  - src/wanctl/autorate_continuous.py
  - src/wanctl/steering/daemon.py
  - src/wanctl/health_check.py
  - src/wanctl/webhook_delivery.py
  - src/wanctl/routeros_rest.py
  - src/wanctl/routeros_ssh.py
  - src/wanctl/calibrate.py
  - src/wanctl/storage/writer.py
autonomous: true
requirements:
  - CQUAL-01
  - CQUAL-02

must_haves:
  truths:
    - "Every `except Exception` catch in the codebase has a documented disposition (safety-net or bug-swallowing)"
    - "All bug-swallowing catches now log the exception via logger.exception() or logger.error()"
    - "No exception catches silently swallow errors without at least a log statement"
  artifacts:
    - path: ".planning/phases/114-code-quality-safety/114-01-exception-triage.md"
      provides: "Full triage of all 96 except Exception catches with dispositions"
      contains: "disposition"
    - path: "src/wanctl/autorate_continuous.py"
      provides: "Fixed bug-swallowing catches (if any found)"
  key_links:
    - from: "114-01-exception-triage.md"
      to: "source files"
      via: "file:line references for each catch"
      pattern: "bug-swallowing|safety-net"
---

<objective>
Triage all 96 `except Exception` catches across the codebase and fix any bug-swallowing catches.

Purpose: Distinguish legitimate safety nets (which log the exception) from bug-swallowing catches (which silently pass or only increment counters), per D-01. Fix bug-swallowing catches by adding `logger.exception()` per D-02. Do NOT narrow exception types (too risky in 50ms control loop per D-02).

Output: Triage report (114-01-exception-triage.md) + source file fixes for silent catches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-code-quality-safety/114-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage all except Exception catches</name>
  <files>
    .planning/phases/114-code-quality-safety/114-01-exception-triage.md
  </files>
  <read_first>
    src/wanctl/autorate_continuous.py
    src/wanctl/steering/daemon.py
    src/wanctl/health_check.py
    src/wanctl/webhook_delivery.py
    src/wanctl/routeros_rest.py
    src/wanctl/routeros_ssh.py
    src/wanctl/calibrate.py
    src/wanctl/storage/writer.py
    src/wanctl/error_handling.py
    src/wanctl/config_base.py
    src/wanctl/router_client.py
    src/wanctl/state_utils.py
    src/wanctl/metrics.py
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
    Run `grep -rn "except Exception" src/wanctl/ --include="*.py"` to get all 96 catches.

    For EACH catch, classify per D-01:
    - **safety-net**: Catch block contains logger.error(), logger.warning(), logger.exception(), or logger.info() call that logs the exception
    - **bug-swallowing**: Catch block uses bare `pass`, only increments a counter, only sets a boolean, or returns a value without logging

    Also check catches in the `@handle_errors` decorator and `safe_operation` context manager in error_handling.py -- these are by-design safety nets (document as "framework safety net").

    Create `114-01-exception-triage.md` with this structure:
    ```
    # Exception Handling Triage (CQUAL-01)
    ## Summary
    - Total catches: N
    - Safety nets: N (with logging)
    - Framework safety nets: N (via @handle_errors/safe_operation/safe_call)
    - Bug-swallowing: N (silent -- MUST FIX)

    ## Safety Nets (no action needed)
    | File:Line | Context | Logging |
    |-----------|---------|---------|

    ## Framework Safety Nets
    | Pattern | Count | Notes |

    ## Bug-Swallowing Catches (MUST FIX per D-02)
    | File:Line | Context | Current behavior | Fix |
    |-----------|---------|------------------|-----|
    ```

    Include the actual code snippet (2-3 lines) for each bug-swallowing catch so Task 2 can fix them precisely.
  </action>
  <verify>
    <automated>test -f .planning/phases/114-code-quality-safety/114-01-exception-triage.md && grep -c "disposition\|safety-net\|bug-swallowing" .planning/phases/114-code-quality-safety/114-01-exception-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - File 114-01-exception-triage.md exists
    - Every `except Exception` catch from `grep -rn "except Exception" src/wanctl/` appears in the triage
    - Each catch has a disposition: safety-net, framework safety net, or bug-swallowing
    - Bug-swallowing catches include code snippets and proposed fix
  </acceptance_criteria>
  <done>All 96 except Exception catches triaged with dispositions documented. Bug-swallowing catches identified with code snippets for fixing.</done>
</task>

<task type="auto">
  <name>Task 2: Fix bug-swallowing exception catches</name>
  <files>
    src/wanctl/autorate_continuous.py
    src/wanctl/steering/daemon.py
    src/wanctl/health_check.py
    src/wanctl/webhook_delivery.py
    src/wanctl/routeros_rest.py
    src/wanctl/routeros_ssh.py
    src/wanctl/calibrate.py
    src/wanctl/storage/writer.py
  </files>
  <read_first>
    .planning/phases/114-code-quality-safety/114-01-exception-triage.md
    src/wanctl/autorate_continuous.py
    src/wanctl/steering/daemon.py
  </read_first>
  <action>
    Using the bug-swallowing catches identified in Task 1's triage report:

    For each bug-swallowing catch per D-02:
    1. Add `logger.exception("Context message", exc_info=True)` or `logger.error("Context: %s", e)` depending on whether a traceback is useful
    2. Do NOT change the exception type (keep `except Exception`)
    3. Do NOT change the control flow (keep `pass`, `return`, `continue` as-is -- just add logging before it)
    4. Use the existing logger available in each context (class method has `self.logger`, module-level has `logger = logging.getLogger(__name__)`)

    If a catch only has `pass` with no logger available in scope, add `import logging` and use `logging.getLogger(__name__).exception("...")`.

    After all fixes, run:
    - `.venv/bin/ruff check src/wanctl/ --select E,W,F,I`
    - `.venv/bin/pytest tests/ -x -q --timeout=120` (full suite, fail-fast)

    Note: The files listed above are the most likely locations. Only modify files that actually have bug-swallowing catches per the triage. Some listed files may not need changes.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -x -q --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - Every catch identified as bug-swallowing in 114-01-exception-triage.md now contains a logging call
    - `grep -rn "except Exception" src/wanctl/ | wc -l` still returns 96 (no catches removed, only logging added)
    - All tests pass
    - Ruff reports no new violations
  </acceptance_criteria>
  <done>All bug-swallowing exception catches fixed with logging. No exception types narrowed. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "except Exception" src/wanctl/ --include="*.py" | wc -l` still returns 96 (unchanged count)
2. `114-01-exception-triage.md` exists with all 96 catches documented
3. Every catch classified as bug-swallowing now has a logger call (verify by re-running triage grep)
4. `.venv/bin/pytest tests/ -x -q --timeout=120` passes
5. `.venv/bin/ruff check src/wanctl/` passes
</verification>

<success_criteria>
- All 96 `except Exception` catches have documented dispositions
- Bug-swallowing catches fixed with logger.exception() calls
- No exception types narrowed (per D-02)
- All 3,723+ tests pass
- Triage report committed as findings artifact
</success_criteria>

<output>
After completion, create `.planning/phases/114-code-quality-safety/114-01-SUMMARY.md`
</output>
