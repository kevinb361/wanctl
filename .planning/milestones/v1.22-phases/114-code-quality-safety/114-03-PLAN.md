---
phase: 114-code-quality-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/114-code-quality-safety/114-03-thread-safety-audit.md
  - .planning/phases/114-code-quality-safety/114-03-sigusr1-catalog.md
  - tests/test_sigusr1_e2e.py
autonomous: true
requirements:
  - CQUAL-04
  - CQUAL-06

must_haves:
  truths:
    - "All 9 threaded files have shared mutable state cataloged with protection status"
    - "SIGUSR1 reload chain is fully cataloged for both daemons"
    - "Every SIGUSR1 reload target has E2E test coverage (existing or newly added)"
  artifacts:
    - path: ".planning/phases/114-code-quality-safety/114-03-thread-safety-audit.md"
      provides: "Thread safety audit with race condition catalog"
      contains: "shared mutable state"
    - path: ".planning/phases/114-code-quality-safety/114-03-sigusr1-catalog.md"
      provides: "Complete SIGUSR1 reload chain catalog"
      contains: "reload target"
    - path: "tests/test_sigusr1_e2e.py"
      provides: "E2E tests for SIGUSR1 reload chain"
      contains: "SIGUSR1"
  key_links:
    - from: "tests/test_sigusr1_e2e.py"
      to: "src/wanctl/signal_utils.py"
      via: "Tests exercise full signal -> event -> reload path"
      pattern: "is_reload_requested"
    - from: "114-03-thread-safety-audit.md"
      to: "threaded source files"
      via: "File:line references for shared state"
      pattern: "threading.Lock|threading.Event|shared"
---

<objective>
Audit thread safety across all 9 threaded files and catalog the complete SIGUSR1 reload chain with E2E test coverage.

Purpose: Identify unprotected shared mutable state and potential race conditions (CQUAL-04 per D-07/D-08/D-09). Catalog all SIGUSR1 reload targets and verify or add E2E test coverage (CQUAL-06 per D-13/D-14/D-15).

Output: Thread safety audit document, SIGUSR1 catalog, and new E2E tests for any uncovered reload targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-code-quality-safety/114-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread safety audit</name>
  <files>
    .planning/phases/114-code-quality-safety/114-03-thread-safety-audit.md
  </files>
  <read_first>
    src/wanctl/autorate_continuous.py
    src/wanctl/health_check.py
    src/wanctl/irtt_thread.py
    src/wanctl/metrics.py
    src/wanctl/signal_utils.py
    src/wanctl/steering/daemon.py
    src/wanctl/steering/health.py
    src/wanctl/storage/writer.py
    src/wanctl/webhook_delivery.py
    .planning/codebase/ARCHITECTURE.md
  </read_first>
  <action>
    Static code review per D-07 (no dynamic testing, no production log analysis).

    For each of the 9 threaded files identified in D-08:
    1. `autorate_continuous.py` -- main loop thread + IRTT thread interaction
    2. `health_check.py` -- HTTP server thread + main daemon data access
    3. `irtt_thread.py` -- background IRTT measurement thread
    4. `metrics.py` -- MetricsRegistry with threading.Lock + HTTP server thread
    5. `signal_utils.py` -- signal handler thread + main thread Event coordination
    6. `steering/daemon.py` -- main loop + health thread + webhook thread
    7. `steering/health.py` -- steering health HTTP server thread
    8. `storage/writer.py` -- MetricsWriter singleton with write lock
    9. `webhook_delivery.py` -- webhook delivery background thread

    For each file per D-09:
    - **Identify shared mutable state**: Instance variables, module-level globals, singletons accessed from multiple threads
    - **Document protection**: Which accesses are protected (threading.Lock, threading.Event, atomic operations, GIL-safe)
    - **Document unprotected accesses**: Which accesses lack explicit synchronization
    - **Catalog potential race conditions**: Where could concurrent access cause incorrect behavior?

    Also check `rtt_measurement.py` which uses `concurrent.futures.ThreadPoolExecutor` for parallel pings.

    Create `114-03-thread-safety-audit.md`:
    ```
    # Thread Safety Audit (CQUAL-04)

    ## Summary
    - Files audited: 9 (+ rtt_measurement.py ThreadPoolExecutor)
    - Shared mutable state instances: N
    - Protected (lock/event/atomic): N
    - Unprotected: N
    - Potential race conditions: N

    ## Thread Topology
    (Describe which threads exist in each daemon process and their lifecycle)

    ### Autorate Daemon Threads
    1. Main thread (run_cycle loop)
    2. IRTT thread (irtt_thread.py)
    3. Health check HTTP thread (health_check.py)
    4. Metrics HTTP server thread (metrics.py)

    ### Steering Daemon Threads
    1. Main thread (run_cycle loop)
    2. Health check HTTP thread (steering/health.py)
    3. Webhook delivery thread (webhook_delivery.py)

    ## Per-File Analysis

    ### autorate_continuous.py

    #### Shared Mutable State
    | Variable | Accessed by | Protected | Mechanism |
    |----------|-------------|-----------|-----------|

    #### Potential Race Conditions
    | Access pattern | Risk | Severity | Notes |
    |----------------|------|----------|-------|

    (repeat for each file)

    ## Risk Summary
    | Race Condition | Severity | Likelihood | Impact |
    |----------------|----------|------------|--------|

    ## Recommendations for v1.23
    (fixes for any high-severity race conditions found)
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/114-code-quality-safety/114-03-thread-safety-audit.md && grep -c "shared mutable state\|Shared Mutable State" .planning/phases/114-code-quality-safety/114-03-thread-safety-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - 114-03-thread-safety-audit.md exists
    - All 9 files from D-08 have per-file analysis sections
    - Each file has shared mutable state table and race condition assessment
    - Thread topology section describes all threads in both daemons
    - Risk summary table present with severity ratings
  </acceptance_criteria>
  <done>Thread safety audit complete for all 9 threaded files. Shared mutable state cataloged. Race conditions identified and rated by severity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SIGUSR1 reload chain catalog + E2E tests</name>
  <files>
    .planning/phases/114-code-quality-safety/114-03-sigusr1-catalog.md
    tests/test_sigusr1_e2e.py
  </files>
  <read_first>
    src/wanctl/signal_utils.py
    src/wanctl/autorate_continuous.py
    src/wanctl/steering/daemon.py
    tests/test_signal_utils.py
    tests/test_fusion_reload.py
    tests/test_tuning_reload.py
    tests/test_steering_daemon.py
    tests/test_webhook_integration.py
    tests/test_tuning_safety_wiring.py
  </read_first>
  <behavior>
    - Test: SIGUSR1 signal sets reload event and each reload target method is called in autorate main loop
    - Test: SIGUSR1 signal sets reload event and each reload target method is called in steering main loop
    - Test: After all reload methods complete, reload_event is cleared (ready for next signal)
    - Test: Reload targets that encounter YAML errors do not crash the daemon loop
  </behavior>
  <action>
    **SIGUSR1 Catalog (CQUAL-06, per D-13):**

    Catalog ALL reload targets across both daemons:

    Autorate daemon (`autorate_continuous.py` main loop, lines ~4194-4200):
    - `_reload_fusion_config()` -- re-reads fusion section from YAML
    - `_reload_tuning_config()` -- re-reads tuning section from YAML

    Steering daemon (`steering/daemon.py` main loop, lines ~2143-2149):
    - `_reload_dry_run_config()` -- re-reads dry_run flag from YAML
    - `_reload_wan_state_config()` -- re-reads wan_state.enabled from YAML
    - `_reload_webhook_url_config()` -- re-reads alerting.webhook_url from YAML

    Signal chain: SIGUSR1 -> `_reload_signal_handler()` -> `_reload_event.set()` -> `is_reload_requested()` returns True -> daemon calls reload methods -> `reset_reload_state()` clears event.

    Create `114-03-sigusr1-catalog.md`:
    ```
    # SIGUSR1 Reload Chain Catalog (CQUAL-06)

    ## Signal Chain
    SIGUSR1 -> signal_utils._reload_signal_handler -> _reload_event.set()
    -> main loop checks is_reload_requested() -> calls reload methods -> reset_reload_state()

    ## Reload Targets

    ### Autorate Daemon
    | Target | Method | What it reloads | File:Line |
    |--------|--------|-----------------|-----------|

    ### Steering Daemon
    | Target | Method | What it reloads | File:Line |
    |--------|--------|-----------------|-----------|

    ## Test Coverage Matrix
    | Reload Target | Unit Tests | E2E Tests | Coverage Status |
    |---------------|------------|-----------|-----------------|
    | _reload_fusion_config | test_fusion_reload.py | ? | |
    | _reload_tuning_config | test_tuning_reload.py | ? | |
    | _reload_dry_run_config | test_steering_daemon.py | ? | |
    | _reload_wan_state_config | test_steering_daemon.py | ? | |
    | _reload_webhook_url_config | test_webhook_integration.py | ? | |

    ## Gaps
    (any reload targets missing test coverage)
    ```

    **E2E Tests (per D-14/D-15):**

    Review existing test coverage. The following tests already exist:
    - `test_signal_utils.py::TestReloadSignal` -- signal infrastructure tests
    - `test_fusion_reload.py` -- _reload_fusion_config unit tests
    - `test_tuning_reload.py` -- _reload_tuning_config unit tests
    - `test_steering_daemon.py::TestReloadDryRunConfig` -- _reload_dry_run_config + run_daemon_loop integration
    - `test_steering_daemon.py` -- _reload_wan_state_config tests
    - `test_webhook_integration.py` -- _reload_webhook_url_config tests
    - `test_tuning_safety_wiring.py` -- tuning reload safety state clearing

    Create `tests/test_sigusr1_e2e.py` with E2E tests that verify the COMPLETE chain:
    1. Signal fires -> event set -> daemon loop detects -> ALL reload methods called -> event cleared
    2. Test for autorate daemon: simulate SIGUSR1, verify both _reload_fusion_config AND _reload_tuning_config called
    3. Test for steering daemon: simulate SIGUSR1, verify all 3 reload methods called (_reload_dry_run_config, _reload_wan_state_config, _reload_webhook_url_config)
    4. Test that reload_event is cleared after handling (ready for next signal)
    5. Test error resilience: one reload method raises Exception, others still execute and daemon loop continues

    Use the same mocking patterns as existing tests: mock the daemon instance, set `_reload_event`, call the main loop's reload block.

    After writing tests:
    - `.venv/bin/pytest tests/test_sigusr1_e2e.py -v`
    - `.venv/bin/ruff check tests/test_sigusr1_e2e.py`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_sigusr1_e2e.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 114-03-sigusr1-catalog.md exists with all 5 reload targets documented
    - Test coverage matrix shows existing + new test coverage for each target
    - tests/test_sigusr1_e2e.py exists and passes
    - E2E tests verify complete chain: signal -> event -> reload methods -> event cleared
    - grep "SIGUSR1" tests/test_sigusr1_e2e.py returns matches
    - grep "_reload_fusion_config\|_reload_tuning_config\|_reload_dry_run_config\|_reload_wan_state_config\|_reload_webhook_url_config" tests/test_sigusr1_e2e.py returns matches for all 5 targets
  </acceptance_criteria>
  <done>SIGUSR1 reload chain fully cataloged. E2E tests written and passing for complete signal-to-reload chain in both daemons. All reload targets have test coverage.</done>
</task>

</tasks>

<verification>
1. Thread safety audit covers all 9 files with per-file shared state analysis
2. SIGUSR1 catalog lists all 5 reload targets across both daemons
3. `tests/test_sigusr1_e2e.py` passes with all E2E tests green
4. `.venv/bin/pytest tests/ -x -q --timeout=120` full suite still passes
5. `.venv/bin/ruff check tests/test_sigusr1_e2e.py` passes
</verification>

<success_criteria>
- Thread safety audit complete with race conditions cataloged and rated
- SIGUSR1 reload chain fully documented with test coverage matrix
- E2E tests added for any uncovered reload chain paths
- All tests pass (existing + new)
- Thread safety findings are document-only per D-19
- SIGUSR1 tests are fix/add per D-15/D-18
</success_criteria>

<output>
After completion, create `.planning/phases/114-code-quality-safety/114-03-SUMMARY.md`
</output>
