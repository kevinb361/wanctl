---
phase: 112-foundation-scan
plan: 04
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - .vulture_whitelist.py
  - .planning/phases/112-foundation-scan/112-04-findings.md
autonomous: true
requirements: [FSCAN-03]

must_haves:
  truths:
    - "Vulture has scanned all source code with min-confidence 80"
    - "A .vulture_whitelist.py documents all validated false positives"
    - "No code has been removed -- identification only"
    - "All 15+ 'looks dead but isn't' patterns from PITFALLS.md are validated"
  artifacts:
    - path: ".vulture_whitelist.py"
      provides: "Vulture false positive whitelist per D-05"
      min_lines: 10
    - path: ".planning/phases/112-foundation-scan/112-04-findings.md"
      provides: "Structured dead code inventory"
      contains: "## Dead Code Inventory"
  key_links:
    - from: ".vulture_whitelist.py"
      to: "vulture scan"
      via: "Whitelist suppresses known false positives"
      pattern: "vulture_whitelist"
    - from: "PITFALLS.md checklist"
      to: "vulture findings"
      via: "Cross-reference all 15 'looks dead but isn't' patterns"
      pattern: "false positive"
---

<objective>
Vulture dead code inventory with false positive validation

Purpose: Run vulture to produce a comprehensive dead code inventory. Cross-reference every finding against the 15+ known false positive patterns from PITFALLS.md and all 8 CLI entry points. No code removal (per D-07) -- identification and structured reporting only.
Output: .vulture_whitelist.py, structured dead code inventory report
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-foundation-scan/112-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run vulture scan and validate findings against false positive patterns</name>
  <files>.vulture_whitelist.py, .planning/phases/112-foundation-scan/112-04-findings.md</files>
  <read_first>
    - .planning/research/PITFALLS.md (entire file -- the 15+ "looks dead but isn't" patterns are CRITICAL)
    - pyproject.toml (all 8 CLI entry points in [project.scripts])
    - src/wanctl/autorate_continuous.py (lines 1200-1210 for RouterOS class, lines 3470-3480 for LinuxCakeAdapter conditional import)
    - src/wanctl/router_client.py (lazy imports for routeros_ssh)
    - src/wanctl/signal_utils.py (_reload_event pattern)
    - src/wanctl/metrics.py (_reset_instance method)
    - src/wanctl/wan_controller_state.py (dirty tracking exclusion, line ~32)
    - src/wanctl/config_validation_utils.py (deprecate_param function)
    - src/wanctl/steering/daemon.py (first 50 lines for RouterOSController)
  </read_first>
  <action>
CRITICAL: Per D-06 and D-07, this is IDENTIFICATION ONLY. No code removal.

1. Run initial vulture scan at 80% confidence (per STACK.md):
   `.venv/bin/vulture src/wanctl/ --min-confidence 80`
   Capture full output.

2. Also run at 60% confidence to see lower-confidence findings for completeness:
   `.venv/bin/vulture src/wanctl/ --min-confidence 60`
   Capture full output separately.

3. For EVERY finding, cross-reference against the PITFALLS.md "Looks Dead But Isn't" checklist (per D-06). The 15 known false positive patterns are:

   a. RouterOS class in autorate_continuous.py -- used when router_transport != "linux-cake"
   b. routeros_ssh.py -- lazy-imported by router_client.py during REST failover
   c. LinuxCakeAdapter -- conditional import at runtime (line ~3474)
   d. _reload_*_config() methods -- called via threading.Event flag, no static caller
   e. MetricsWriter._reset_instance() -- only called by tests
   f. congestion.dl_state/ul_state in state JSON -- read by steering (separate process)
   g. dirty tracking exclusion for congestion field -- intentional
   h. Context manager __exit__ that does not close -- MetricsWriter singleton
   i. deprecate_param() translations -- config backward compatibility
   j. container_install_*.sh scripts -- pre-v1.21 deployment reference
   k. verify_steering.sh / verify_steering_new.sh -- different verification approaches
   l. CAP_NET_RAW in systemd -- required for icmplib
   m. isinstance(stats, dict) guard -- MagicMock safety
   n. exclude_params in tuning config -- DOCSIS-specific
   o. PYTHONPATH=/opt in systemd -- required for non-pip deployment

4. Validate findings against ALL 8 CLI entry points:
   - wanctl (autorate_continuous:main)
   - wanctl-calibrate (calibrate:main)
   - wanctl-steering (steering.daemon:main)
   - wanctl-history (history:main)
   - wanctl-dashboard (dashboard.app:main)
   - wanctl-check-config (check_config:main)
   - wanctl-check-cake (check_cake:main)
   - wanctl-benchmark (benchmark:main)

   For each vulture finding, check if the flagged code is reachable from any of these 8 entry points.

5. Also check both transport configurations:
   - router_transport: "linux-cake" (current production)
   - router_transport: "rest" (RouterOS mode, still supported)

6. Create `.vulture_whitelist.py` (per D-05) documenting all validated false positives:
   ```python
   """Vulture whitelist -- known false positives for wanctl dead code analysis.

   These items appear unused to static analysis but are reachable through:
   - Runtime configuration (transport mode selection)
   - Lazy/conditional imports
   - Inter-process communication (autorate -> steering via state files)
   - Test-only utilities
   - Signal handler chains (SIGUSR1 reload)

   See .planning/research/PITFALLS.md for detailed rationale.
   """

   # RouterOS transport class -- used when router_transport != "linux-cake"
   RouterOS  # autorate_continuous.py

   # Lazy import for SSH failover
   import routeros_ssh

   # Conditional runtime import
   LinuxCakeAdapter  # autorate_continuous.py

   # SIGUSR1 reload targets -- called via threading.Event, no static caller
   _reload_fusion_config  # autorate_continuous.py
   _reload_tuning_config  # autorate_continuous.py
   # ... (add all validated false positives)
   ```

7. Run vulture with the whitelist to get the TRUE findings:
   `.venv/bin/vulture src/wanctl/ .vulture_whitelist.py --min-confidence 80`

8. Classify each TRUE finding (not in whitelist) into categories:
   - **Likely dead:** High confidence, not reachable from any entry point or config
   - **Needs investigation:** Medium confidence, might be reachable through dynamic dispatch
   - **Test-only:** Used only in tests, not production code (acceptable)

9. Write the structured report to `.planning/phases/112-foundation-scan/112-04-findings.md`:
   ```markdown
   # Dead Code Inventory (FSCAN-03)

   **Tool:** vulture 2.16, min-confidence 80
   **Scope:** src/wanctl/ (28,629 LOC)
   **Date:** {date}

   ## Summary
   - Total vulture findings (raw): {N}
   - False positives (whitelisted): {N}
   - True findings after whitelist: {N}
     - Likely dead: {N}
     - Needs investigation: {N}
     - Test-only (acceptable): {N}

   ## False Positives Validated
   | Item | File | Why It Is NOT Dead | PITFALLS.md Pattern |
   |------|------|--------------------|--------------------|
   | RouterOS class | autorate_continuous.py | Used with rest transport | Pitfall 1 |
   | ... | ... | ... | ... |

   ## Likely Dead Code
   | Item | File | Line | Confidence | Reachable From | Recommendation |
   |------|------|------|------------|----------------|----------------|
   | ... | ... | ... | ... | None of 8 entry points | Safe to remove in future |

   ## Needs Investigation
   | Item | File | Line | Confidence | Why Uncertain |
   |------|------|------|------------|---------------|
   | ... | ... | ... | ... | ... |

   ## Entry Point Coverage
   | Entry Point | Exercised During Validation | Code Paths Checked |
   |-------------|----------------------------|--------------------|
   | wanctl | Yes | main -> WANController -> ... |
   | wanctl-steering | Yes | main -> SteeringDaemon -> ... |
   | ... | ... | ... |

   ## IMPORTANT: No Removal Policy (D-07)
   This inventory is for identification only. No dead code has been removed.
   Removal decisions require transport validation (linux-cake AND rest modes)
   and should happen in a dedicated cleanup phase, not during audit.
   ```
  </action>
  <verify>
    <automated>.venv/bin/vulture src/wanctl/ .vulture_whitelist.py --min-confidence 80 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.vulture_whitelist.py` exists in project root with documented false positives
    - `.vulture_whitelist.py` contains entries for at least: RouterOS, routeros_ssh, LinuxCakeAdapter, _reload_fusion_config, _reset_instance (the 5 most critical false positives from PITFALLS.md)
    - `.planning/phases/112-foundation-scan/112-04-findings.md` exists with sections: "## Summary", "## False Positives Validated", "## Likely Dead Code", "## Entry Point Coverage"
    - The report states "No Removal Policy (D-07)" explicitly
    - All 8 CLI entry points are listed in the Entry Point Coverage table
    - No source files in src/wanctl/ have been deleted or had code removed
  </acceptance_criteria>
  <done>Complete dead code inventory with validated false positives, structured report, and whitelist file. Zero code removed.</done>
</task>

</tasks>

<verification>
- `.vulture_whitelist.py` exists and is parseable by vulture: `.venv/bin/vulture src/wanctl/ .vulture_whitelist.py --min-confidence 80` runs without error
- `.planning/phases/112-foundation-scan/112-04-findings.md` contains all required sections
- `git diff --stat src/wanctl/` shows zero file deletions (no code removed)
- The whitelist covers the 15 PITFALLS.md patterns
</verification>

<success_criteria>
- Vulture scan completed at 80% and 60% confidence levels
- All 15+ PITFALLS.md false positive patterns validated and whitelisted
- All 8 CLI entry points checked for code reachability
- Both transport modes (linux-cake, rest) considered
- Structured inventory report with clear categorization (likely dead / needs investigation / test-only)
- Zero code removed (per D-07)
- .vulture_whitelist.py committed as reusable artifact for future scans
</success_criteria>

<output>
After completion, create `.planning/phases/112-foundation-scan/112-04-SUMMARY.md`
</output>
