---
phase: 135-upload-recovery-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md
autonomous: false
requirements: [TUNE-01]

must_haves:
  truths:
    - "Baseline run with current params (step_up=2, factor_down=0.85) recorded for comparison"
    - "All 6 matrix configs tested: step_up={3,4,5} x factor_down={0.80,0.90}, 3 runs each"
    - "Each run has documented ICMP median/p99, UL throughput sum, and DL throughput as regression check"
    - "DL config confirmed unchanged after every config change"
  artifacts:
    - path: ".planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md"
      provides: "Raw test results for all 21 flent runs (3 baseline + 18 matrix)"
      contains: "Baseline"
  key_links:
    - from: "/etc/wanctl/spectrum.yaml"
      to: "wanctl@spectrum"
      via: "SIGUSR1 hot-reload"
      pattern: "systemctl kill -s SIGUSR1"
---

<objective>
Run the full UL parameter matrix: 3 baseline runs with current production params, then 6 configs (step_up_mbps={3,4,5} x factor_down={0.80,0.90}) with 3 RRUL flent runs each. Record all metrics in a structured results document.

Purpose: Generate the raw data needed to identify the best UL step_up/factor_down combination on linux-cake transport. The matrix design tests interaction effects between the two parameters (per D-01, D-02, D-03, D-04).
Output: 135-UL-RESULTS.md with all 21 flent run results in comparison tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-upload-recovery-tuning/135-CONTEXT.md
@configs/spectrum-vm.yaml

## Operational Reference

**SSH:** `ssh kevin@10.10.110.223` (cake-shaper VM)
**Config:** `/etc/wanctl/spectrum.yaml` (sudo required to edit)
**SIGUSR1:** `sudo systemctl kill -s SIGUSR1 wanctl@spectrum`
**Health:** `curl -s http://127.0.0.1:9101/health | python3 -m json.tool`
**flent RRUL:** `flent rrul -H 104.200.21.31 -l 60` (from dev machine, per D-06)

## Current Production UL Values

Under `continuous_monitoring.upload:` in /etc/wanctl/spectrum.yaml:

```yaml
upload:
  floor_mbps: 8
  ceiling_mbps: 38
  step_up_mbps: 2          # Current baseline
  factor_down: 0.85        # Current baseline
  green_required: 3        # NOT being tested (per D-01)
```

## Test Matrix (per D-02, D-03)

| Config | step_up_mbps | factor_down |
|--------|-------------|-------------|
| Baseline | 2 | 0.85 |
| A1 | 3 | 0.80 |
| A2 | 4 | 0.80 |
| A3 | 5 | 0.80 |
| B1 | 3 | 0.90 |
| B2 | 4 | 0.90 |
| B3 | 5 | 0.90 |

3 runs per config = 21 total flent runs (per D-04).

## Sed Safety for UL Section

The upload section sits between `upload:` and `thresholds:` in spectrum.yaml. Always scope sed to the UL section:

```bash
# Safe UL-scoped sed pattern:
sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: OLD/step_up_mbps: NEW/' /etc/wanctl/spectrum.yaml

# ALWAYS verify BOTH sections after sed:
sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
```

DL values that MUST NOT change: step_up_mbps=10, factor_down=0.85, factor_down_yellow=0.92
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate check + 3 baseline runs</name>
  <files>.planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md</files>
  <read_first>
    - configs/spectrum-vm.yaml
    - .planning/phases/135-upload-recovery-tuning/135-CONTEXT.md
  </read_first>
  <action>
    Verify environment and run 3 baseline RRUL tests with current production UL params (step_up=2, factor_down=0.85). Per D-05, use SIGUSR1 for all reloads. Per D-06, run flent from dev machine only.

    STEP 1 -- Gate check on VM:
    ```
    ssh kevin@10.10.110.223
    ```
    On VM:
    ```
    # Verify transport is linux-cake (per feedback_verify_transport_before_tuning)
    curl -s http://127.0.0.1:9101/health | python3 -c "import sys,json; d=json.load(sys.stdin); print('transport:', d.get('transport','?'))"

    # Verify current UL config
    sudo sed -n '/^\s*upload:/,/^\s*thresholds:/p' /etc/wanctl/spectrum.yaml
    # Expected: step_up_mbps: 2, factor_down: 0.85, green_required: 3

    # Verify DL config unchanged
    sudo sed -n '/download:/,/upload:/p' /etc/wanctl/spectrum.yaml
    # Expected: step_up_mbps: 10, factor_down: 0.85

    # Verify CAKE qdiscs active (per feedback_check_cake_qdiscs)
    tc -s qdisc show dev ens16 | head -5
    tc -s qdisc show dev ens17 | head -5
    # Both must show CAKE qdiscs

    # Verify wanctl healthy
    curl -s http://127.0.0.1:9101/health | python3 -c "import sys,json; d=json.load(sys.stdin); print('status:', d.get('status','?')); print('UL state:', d.get('upload',{}).get('state','?')); print('UL rate:', d.get('upload',{}).get('current_rate_mbps','?'))"
    ```

    STEP 2 -- Run 3 baseline flent tests (from dev machine):
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-baseline-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-baseline-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-baseline-run3"
    ```
    For each run, record: ICMP median, ICMP p99, DL throughput sum, UL throughput sum, time of day.

    STEP 3 -- Create results document:
    Create .planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md with:
    ```markdown
    # Phase 135: UL Recovery Tuning Results

    **Date:** 2026-04-0X (fill in)
    **Transport:** linux-cake
    **Methodology:** flent rrul -H 104.200.21.31 -l 60, 3 runs per config (per D-04)
    **Conditions:** (note time of day and cable plant conditions)
    **Host:** cake-shaper VM 206 (10.10.110.223)
    **Params tested:** step_up_mbps x factor_down matrix (per D-01, D-02, D-03)
    **Success threshold:** 15% UL throughput OR 20% latency improvement (per D-07)

    ## Baseline (step_up=2, factor_down=0.85)

    | Run | ICMP Median | ICMP p99 | DL Throughput | UL Throughput | Time |
    |-----|-------------|----------|---------------|---------------|------|
    | 1   |             |          |               |               |      |
    | 2   |             |          |               |               |      |
    | 3   |             |          |               |               |      |
    | **Avg** |         |          |               |               |      |
    ```
  </action>
  <verify>Gate passes (linux-cake transport, CAKE qdiscs active, health 200), 3 baseline runs recorded</verify>
  <done>Environment verified, 3 baseline RRUL runs completed and recorded in 135-UL-RESULTS.md with per-run metrics and averages</done>
  <acceptance_criteria>
    - Transport confirmed as linux-cake (not REST)
    - CAKE qdiscs verified active on ens16 and ens17
    - Current UL config confirmed: step_up=2, factor_down=0.85, green_required=3
    - DL config confirmed unchanged: step_up=10, factor_down=0.85
    - Health endpoint returns 200 with valid UL state
    - 3 baseline flent runs completed with all metrics recorded
    - 135-UL-RESULTS.md created with baseline section including per-run and average values
  </acceptance_criteria>
  <resume-signal>Paste gate check output and all 3 baseline run metrics. Type "ready" to proceed to matrix testing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Test factor_down=0.80 row (configs A1, A2, A3 -- 9 flent runs)</name>
  <files>.planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md</files>
  <read_first>
    - .planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md
  </read_first>
  <action>
    Test 3 step_up values with factor_down=0.80 (more aggressive decay than current 0.85). Per D-02, D-03, D-04. Per D-05, apply via SIGUSR1.

    NOTE: factor_down=0.80 means 20% rate cut per RED cycle (vs current 15%). This is more aggressive -- tests whether faster shedding during congestion improves UL recovery speed on linux-cake.

    CONFIG A1: step_up=3, factor_down=0.80
    On VM (ssh kevin@10.10.110.223):
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 2/step_up_mbps: 3/' /etc/wanctl/spectrum.yaml
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/factor_down: 0.85/factor_down: 0.80/' /etc/wanctl/spectrum.yaml
    # Verify BOTH sections (DL must be step_up=10, factor_down=0.85):
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A1-step3-fd080-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A1-step3-fd080-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A1-step3-fd080-run3"
    ```
    Record: ICMP median, p99, DL throughput, UL throughput, time.

    CONFIG A2: step_up=4, factor_down=0.80
    On VM:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 3/step_up_mbps: 4/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A2-step4-fd080-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A2-step4-fd080-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A2-step4-fd080-run3"
    ```
    Record same metrics.

    CONFIG A3: step_up=5, factor_down=0.80
    On VM:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 4/step_up_mbps: 5/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A3-step5-fd080-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A3-step5-fd080-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-A3-step5-fd080-run3"
    ```
    Record same metrics.

    After all 9 runs, append to 135-UL-RESULTS.md:
    ```markdown
    ## factor_down=0.80 Row

    ### Config A1: step_up=3, factor_down=0.80

    | Run | ICMP Median | ICMP p99 | DL Throughput | UL Throughput | Time |
    |-----|-------------|----------|---------------|---------------|------|
    | 1   |             |          |               |               |      |
    | 2   |             |          |               |               |      |
    | 3   |             |          |               |               |      |
    | **Avg** |         |          |               |               |      |

    ### Config A2: step_up=4, factor_down=0.80
    (same table format)

    ### Config A3: step_up=5, factor_down=0.80
    (same table format)
    ```

    RESTORE original UL params before moving to next task:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 5/step_up_mbps: 2/' /etc/wanctl/spectrum.yaml
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/factor_down: 0.80/factor_down: 0.85/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
  </action>
  <verify>9 flent runs completed (3 per config), all metrics recorded, UL params restored to baseline</verify>
  <done>Configs A1, A2, A3 all tested with 3 runs each. Results appended to 135-UL-RESULTS.md. Production UL params restored to step_up=2, factor_down=0.85.</done>
  <acceptance_criteria>
    - All 3 configs tested: (3,0.80), (4,0.80), (5,0.80)
    - 3 flent runs per config = 9 total runs
    - DL params verified unchanged after each config change (step_up=10, factor_down=0.85)
    - Per-run metrics recorded: ICMP median, p99, DL throughput, UL throughput, time of day
    - Per-config averages calculated
    - Results appended to 135-UL-RESULTS.md
    - UL params restored to production baseline (step_up=2, factor_down=0.85) after testing
  </acceptance_criteria>
  <resume-signal>Paste all 9 run results (or summary table). Confirm UL params restored. Type "ready" for factor_down=0.90 row.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Test factor_down=0.90 row (configs B1, B2, B3 -- 9 flent runs)</name>
  <files>.planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md</files>
  <read_first>
    - .planning/phases/135-upload-recovery-tuning/135-UL-RESULTS.md
  </read_first>
  <action>
    Test 3 step_up values with factor_down=0.90 (gentler decay than current 0.85). Per D-02, D-03, D-04. Per D-05, apply via SIGUSR1.

    NOTE: factor_down=0.90 means 10% rate cut per RED cycle (vs current 15%). This is gentler -- tests whether slower shedding preserves more UL throughput while linux-cake's faster feedback handles congestion. DL uses 0.85 on linux-cake, but UL has very different dynamics (38 vs 940 Mbps).

    CONFIG B1: step_up=3, factor_down=0.90
    On VM (ssh kevin@10.10.110.223):
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 2/step_up_mbps: 3/' /etc/wanctl/spectrum.yaml
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/factor_down: 0.85/factor_down: 0.90/' /etc/wanctl/spectrum.yaml
    # Verify BOTH sections (DL must be step_up=10, factor_down=0.85):
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B1-step3-fd090-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B1-step3-fd090-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B1-step3-fd090-run3"
    ```
    Record: ICMP median, p99, DL throughput, UL throughput, time.

    CONFIG B2: step_up=4, factor_down=0.90
    On VM:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 3/step_up_mbps: 4/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B2-step4-fd090-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B2-step4-fd090-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B2-step4-fd090-run3"
    ```
    Record same metrics.

    CONFIG B3: step_up=5, factor_down=0.90
    On VM:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 4/step_up_mbps: 5/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
    Wait 5s. From dev machine, run 3 times:
    ```
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B3-step5-fd090-run1"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B3-step5-fd090-run2"
    flent rrul -H 104.200.21.31 -l 60 -t "UL-B3-step5-fd090-run3"
    ```
    Record same metrics.

    After all 9 runs, append to 135-UL-RESULTS.md:
    ```markdown
    ## factor_down=0.90 Row

    ### Config B1: step_up=3, factor_down=0.90

    | Run | ICMP Median | ICMP p99 | DL Throughput | UL Throughput | Time |
    |-----|-------------|----------|---------------|---------------|------|
    | 1   |             |          |               |               |      |
    | 2   |             |          |               |               |      |
    | 3   |             |          |               |               |      |
    | **Avg** |         |          |               |               |      |

    ### Config B2: step_up=4, factor_down=0.90
    (same table format)

    ### Config B3: step_up=5, factor_down=0.90
    (same table format)
    ```

    RESTORE original UL params after testing:
    ```
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/step_up_mbps: 5/step_up_mbps: 2/' /etc/wanctl/spectrum.yaml
    sudo sed -i '/^\s*upload:/,/^\s*thresholds:/ s/factor_down: 0.90/factor_down: 0.85/' /etc/wanctl/spectrum.yaml
    sudo grep -E 'step_up_mbps|factor_down' /etc/wanctl/spectrum.yaml
    sudo systemctl kill -s SIGUSR1 wanctl@spectrum
    ```
  </action>
  <verify>9 flent runs completed (3 per config), all metrics recorded, UL params restored to baseline</verify>
  <done>Configs B1, B2, B3 all tested with 3 runs each. Results appended to 135-UL-RESULTS.md. Production UL params restored to step_up=2, factor_down=0.85. All 18 matrix runs complete.</done>
  <acceptance_criteria>
    - All 3 configs tested: (3,0.90), (4,0.90), (5,0.90)
    - 3 flent runs per config = 9 total runs
    - DL params verified unchanged after each config change (step_up=10, factor_down=0.85)
    - Per-run metrics recorded: ICMP median, p99, DL throughput, UL throughput, time of day
    - Per-config averages calculated
    - Results appended to 135-UL-RESULTS.md
    - UL params restored to production baseline (step_up=2, factor_down=0.85) after testing
    - Total: 21 flent runs recorded (3 baseline + 9 fd=0.80 + 9 fd=0.90)
  </acceptance_criteria>
  <resume-signal>Paste all 9 run results (or summary table). Confirm UL params restored. Type "done" to proceed to analysis plan.</resume-signal>
</task>

</tasks>

<verification>
1. 135-UL-RESULTS.md exists with baseline section (3 runs) and 6 config sections (3 runs each = 18)
2. Total: 21 flent runs documented with per-run metrics
3. Each config has: ICMP median, p99, DL throughput (regression check), UL throughput, time of day
4. DL config verified unchanged throughout all testing
5. Production UL params restored to baseline (step_up=2, factor_down=0.85) after testing
</verification>

<success_criteria>
- All 6 matrix configs tested with 3 RRUL flent runs each (satisfies TUNE-01 testing requirement)
- Baseline established with 3 runs for comparison (per Claude's discretion)
- Every run documented with ICMP median, p99, DL throughput, UL throughput
- DL config never changed during testing (regression safety)
- Production restored to baseline after testing (analysis/deploy in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/135-upload-recovery-tuning/135-01-SUMMARY.md`
</output>
