---
milestone: v1.57
milestone_name: Supported read-only RouterOS ownership inspection
audited: 2026-06-26
status: passed
scores:
  requirements: 10/10
  phases: 3/3
  integration: 4/4
  flows: 3/3
nyquist:
  compliant_phases: [258, 259, 260]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 258
    items:
      - "ACCESS-03 explicit residual: the read-only inspection path reuses the steering RouterOS credential, which can write at RouterOS RBAC. Read-only is enforced procedurally (GET-only REST handlers + readonly_validator allowlist), not by a least-privilege RouterOS user. Accepted residual, not a gap."
  - phase: 260
    items:
      - "D-07 cross-check detector was a false-divergence bug: the harness cross-check counted only inline Netwatch route mutations and missed `/system script run NAME` indirection that the live guard resolves (cross-check reported 0 vs inspector 4). Fixed post-phase-close in commit 7a96aa8f by sharing one `detect_netwatch_route_conflicts()`; verified live on cake-shaper 2026-06-26 (cross-check now 4, divergences=[], verdict ready-for-approval)."
      - "Deployed `route_ownership_guard.py` on cake-shaper is one file ahead of the last full deploy.sh (the behavior-preserving D-07 fix). Reconcile /opt/wanctl via a full deploy before any mutating canary milestone (SEED-008). Backup retained at route_ownership_guard.py.bak-d07."
notes:
  - "Phase 259 uses VERIFICATION.md (no NNN- prefix); phases 258 and 260 use NNN-VERIFICATION.md. Acceptance checked against the actual artifact shape."
  - "Milestone outcome was enhanced after phase close: the original Phase 260 packet was `not-ready`, which already satisfies OBSERVE-03 (a verdict of ready-for-approval OR not-ready). The D-07 fix + live re-run improved the verdict to `ready-for-approval`; it was an enhancement, not a gap-closure required for completion."
  - "Internal consistency: `route_mutating_active=4` was already proven live at Phase 259 (INSPECT_PROOF_PASS), one phase before D-07 surfaced. The corrected Phase 260 cross-check aligns to that already-established value."
  - "Full 258→259→260 chain was exercised end-to-end live during the 2026-06-26 re-run, producing ready-for-approval — direct E2E integration evidence, not just per-phase verification."
  - "Pre-existing deferred todos/seeds carried forward per v1.56 close. SEED-008 planted for v1.58 (active route-management canary)."
---

# v1.57 Milestone Audit — Supported read-only RouterOS ownership inspection

Status: passed

Audit date: 2026-06-26

## Result

v1.57 achieved its definition of done: repair and prove a *supported* read-only RouterOS ownership-inspection path from `cake-shaper`, surface live Netwatch and default-route ownership distinctly from the cake-autorate bridge (`:9101`) and steering route-management (`:9102`) health, then rerun the v1.56-blocked bounded dry-run observation and produce a canary-readiness packet — all with no route ownership change.

The failure chain that blocked v1.56 is fully resolved: Phase 258 repaired the inaccessible inspection credential by adding a supported GET-only REST read-only path (replacing the broken nested-SSH `router.key`), Phase 259 read live Netwatch + default-route state over that path and attributed Netwatch as owner, and Phase 260 reran the observation to a readiness packet. The final verdict is `ready-for-approval` after the post-close D-07 cross-check detector fix (see Tech Debt); even the pre-fix `not-ready` packet satisfied OBSERVE-03.

## Scores

| Area | Score | Status |
|------|-------|--------|
| Requirements | 10/10 | passed |
| Phases | 3/3 | passed |
| Integration | 4/4 | passed |
| E2E flows | 3/3 | passed |

## Requirement Coverage

| REQ-ID | Phase | Status | Evidence |
|--------|-------|--------|----------|
| ACCESS-01 | 258 | passed | Root cause documented: daemon REST handler gap + nested-SSH router.key rc=255 (`evidence/258-01-root-cause.md`). |
| ACCESS-02 | 258 | passed | GET-only `_handle_netwatch_print`/`_handle_script_print`; live `ACCESS02_PROOF_PASS route=17 netwatch=3 script=20`. |
| ACCESS-03 | 258 | passed (residual) | `readonly_validator` rejects mutating verbs/metachars/bypasses; residual: credential can write at RBAC (procedural read-only). |
| INSPECT-01 | 259 | passed | Live Netwatch read surfaced as ownership-inspection evidence. |
| INSPECT-02 | 259 | passed | `INSPECT_PROOF_PASS observed_owner=netwatch ... route_mutating_active=4 total_routes=17`. |
| INSPECT-03 | 259 | passed | Ownership inspection attributed distinctly from `:9101` bridge / `:9102` steering; no payload-shape regression. |
| OBSERVE-01 | 260 | passed | Bounded dry-run observation reran with live inspection; no route mutation. |
| OBSERVE-02 | 260 | passed | Intended decisions compared vs live state; divergences recorded as evidence. |
| OBSERVE-03 | 260 | passed | Readiness packet with explicit verdict (`ready-for-approval` after D-07 fix), blockers, rollback readiness. |
| SAFE-21 | 258/259/260 | passed | No route mutation, Netwatch change, CAKE/qdisc change, threshold retune, owner flip, or service restart across the milestone. |

## Cross-Phase Integration

| Integration point | Status |
|-------------------|--------|
| 258 GET-only REST path → 259 RouteOwnershipInspector consumes it | wired |
| 259 `ownership_inspection` signal → 260 sample gate consumes it | wired |
| 258 read-only path → 260 independent D-04 cross-check | wired (detector unified with guard post-fix) |
| Health surface separation `:9101` bridge vs `:9102` steering/ownership (INSPECT-03) | wired |

E2E: the full 258→259→260 chain ran live on cake-shaper 2026-06-26 and produced `ready-for-approval` with SAFE-21 held.

## E2E Flows

1. Read-only access proof (258) — supported REST GET path returns live RouterOS state. ✓
2. Live ownership inspection (259) — Netwatch + default routes read, owner attributed, surfaced on `:9102`. ✓
3. Dry-run observation → readiness packet (260) — bounded sampling, divergence union, verdict. ✓

## Verdict

PASSED. All 10 requirements satisfied, 3/3 phases verified, cross-phase chain proven live end-to-end. Two accepted tech-debt items carried (ACCESS-03 procedural-read-only residual; cake-shaper one-file-ahead deploy reconciliation before any mutating canary). Active route-management canary deferred to v1.58 (SEED-008) behind an explicit reversible operator gate.
