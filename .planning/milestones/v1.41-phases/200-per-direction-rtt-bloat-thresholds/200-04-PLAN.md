---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 04
type: execute
wave: 4
depends_on: [200-01, 200-02, 200-03]
files_modified:
  - pyproject.toml
  - src/wanctl/__init__.py
  - docker/Dockerfile
  - configs/spectrum.yaml
  - CHANGELOG.md
  - docs/CONFIGURATION.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "Version is `1.41.0` in pyproject.toml, src/wanctl/__init__.py.__version__, and docker/Dockerfile LABEL"
    - "configs/spectrum.yaml upload section adopts ceiling_mbps=18, factor_down_yellow=0.98, target_bloat_ms=42, warn_bloat_ms=105 (D-05)"
    - "CHANGELOG.md has a `## [1.41.0] - <date>` (or under [Unreleased] until ship) section documenting the new optional UL threshold keys, the SAFE-06 unknown-key warning, the SAFE-05 expected-count update, and the Spectrum YAML adoption"
    - "docs/CONFIGURATION.md documents `continuous_monitoring.upload.target_bloat_ms` and `warn_bloat_ms` with explicit migration note that SIGUSR1 does NOT reload these keys (service restart required)"
    - "docs/CONFIGURATION.md migration note cites the SIGUSR1 reload scope (wan_controller.py:1894+ handles dwell/deadband only — the Phase 200 keys load only at __init__ time)"
    - "All three version surfaces print the same string when interrogated: pyproject.toml `version`, `python -c 'import wanctl; print(wanctl.__version__)'`, and `grep version= docker/Dockerfile`"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project version"
      contains: 'version = "1.41.0"'
    - path: "src/wanctl/__init__.py"
      provides: "Runtime version string"
      contains: '__version__ = "1.41.0"'
    - path: "docker/Dockerfile"
      provides: "Image label version"
      contains: 'version="1.41.0"'
    - path: "configs/spectrum.yaml"
      provides: "Spectrum production YAML adopting D-05 UL settings"
      contains: "target_bloat_ms: 42"
    - path: "CHANGELOG.md"
      provides: "v1.41.0 release notes"
      contains: "1.41.0"
    - path: "docs/CONFIGURATION.md"
      provides: "Per-direction UL threshold docs + SIGUSR1 migration note"
      contains: "SIGUSR1"
  key_links:
    - from: "docs/CONFIGURATION.md migration note"
      to: "src/wanctl/wan_controller.py::_reload_hysteresis_config"
      via: "explicit reference to SIGUSR1 reload scope"
      pattern: "SIGUSR1.*restart|restart.*SIGUSR1"
---

<objective>
Bump version to 1.41.0 across all three version surfaces (pyproject.toml, `src/wanctl/__init__.py`, `docker/Dockerfile`), apply the locked Spectrum YAML adoption (D-05) to `configs/spectrum.yaml`, add a CHANGELOG.md entry covering the v1.41 changes (D-12), and add a migration note to `docs/CONFIGURATION.md` explaining the SIGUSR1-vs-restart asymmetry for the new threshold keys (DOCS-03).

Purpose: Ship coherence. v1.41 ROADMAP.md and the CONTEXT.md require the version-to-milestone mapping (D-11) and the SIGUSR1 migration note (D-12 / DOCS-03). The Spectrum YAML adoption (D-05) is the operator-evidence-driven settings change shipped from `.planning/spectrum-inline-native-18-upload-test-2026-04-29.md`.

Output: Six files changed in one atomic commit. No code-path changes (those landed in Plans 01-03). This is the documentation/configuration ship-coherence commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md
@.planning/spectrum-inline-native-18-upload-test-2026-04-29.md
@.planning/spectrum-lower-ceiling-sweep-2026-04-29.md
@pyproject.toml
@src/wanctl/__init__.py
@docker/Dockerfile
@configs/spectrum.yaml
@CHANGELOG.md
@docs/CONFIGURATION.md

<interfaces>
<!-- Current version surface state (v1.40 archived as 1.39.0 per STATE.md) -->

pyproject.toml:3        version = "1.39.0"        → "1.41.0"
src/wanctl/__init__.py:3 __version__ = "1.39.0"   → "1.41.0"
docker/Dockerfile:13    LABEL version="1.39.0"    → version="1.41.0"

<!-- Spectrum YAML target state (D-05) -->
<!-- The working tree already has these values per Phase 200 partial diff. -->
<!-- Verify, do not duplicate the adoption. -->

configs/spectrum.yaml:upload section:
  ceiling_mbps: 18
  factor_down_yellow: 0.98
  target_bloat_ms: 42
  warn_bloat_ms: 105

<!-- SIGUSR1 reload scope (for migration note) -->

src/wanctl/wan_controller.py:
  _reload_hysteresis_config (line 1894)  — reloads dwell_cycles, deadband_ms ONLY
  _reload_fusion_config (line 1748)
  _reload_tuning_config (line 1839)
  _reload_cake_signal_config (line 2177)
  Combined reload (line 3979): _reload_all_hot_reloadable_config_sections

  NONE of the SIGUSR1 reloaders touch:
    upload_target_bloat_ms / upload_warn_bloat_ms
    factor_down_yellow / step_up_mbps / ceiling_mbps
  These are __init__-only — service restart is required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 1.41.0 + apply Spectrum YAML adoption (D-05) sanity check</name>
  <files>
    pyproject.toml
    src/wanctl/__init__.py
    docker/Dockerfile
    configs/spectrum.yaml
  </files>
  <action>
    Step 1 — Version bump. Use the Edit tool, NOT sed:

    `pyproject.toml`:
      `version = "1.39.0"` → `version = "1.41.0"`

    `src/wanctl/__init__.py`:
      `__version__ = "1.39.0"` → `__version__ = "1.41.0"`

    `docker/Dockerfile`:
      `LABEL version="1.39.0"` → `LABEL version="1.41.0"`

    If any of these surfaces is already at a non-1.39.0 string (e.g., a stale prior bump), STOP and surface the discrepancy to the operator. Do NOT auto-correct — the working tree was last shipped at 1.39.0 per STATE.md.

    Step 2 — Verify the Spectrum YAML adoption is already in the working tree (it is, per the partial Phase 200 diff). Do NOT duplicate; just verify:

    ```bash
    yq -r '.continuous_monitoring.upload | .ceiling_mbps, .factor_down_yellow, .target_bloat_ms, .warn_bloat_ms' configs/spectrum.yaml
    ```

    Expected output (line-by-line):
    ```
    18
    0.98
    42
    105
    ```

    If `yq` is not installed, fall back to `python3 -c 'import yaml; ...'`. If the values do not match, edit `configs/spectrum.yaml` to match D-05 exactly, preserving the existing operator-comment trail (the `# Latency-first gaming-server soak 2026-04-29...` comments encode evidence provenance and must be kept).

    Step 3 — Verify the three version surfaces agree:

    ```bash
    grep '^version' pyproject.toml
    grep '__version__' src/wanctl/__init__.py
    grep 'LABEL version' docker/Dockerfile
    ```

    All three must show `1.41.0`.
  </action>
  <verify>
    <automated>test "$(grep -E '^version = ' pyproject.toml | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')" = "1.41.0" && test "$(grep -E '^__version__' src/wanctl/__init__.py | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')" = "1.41.0" && test "$(grep -E 'LABEL version' docker/Dockerfile | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')" = "1.41.0" && python3 -c "import yaml; ul=yaml.safe_load(open('configs/spectrum.yaml'))['continuous_monitoring']['upload']; assert ul['ceiling_mbps']==18 and ul['factor_down_yellow']==0.98 and ul['target_bloat_ms']==42 and ul['warn_bloat_ms']==105, ul"</automated>
  </verify>
  <done>
    All three version surfaces report 1.41.0; configs/spectrum.yaml carries D-05 settings (ceiling 18 / factor_down_yellow 0.98 / target_bloat_ms 42 / warn_bloat_ms 105).
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md entry + docs/CONFIGURATION.md migration note (DOCS-03)</name>
  <files>
    CHANGELOG.md
    docs/CONFIGURATION.md
  </files>
  <action>
    Step 1 — Edit `CHANGELOG.md`. Add a new section immediately above the `## [Unreleased]` header (or replace the v1.41 portion under `[Unreleased]` if convention is to ship under a frozen version header). Phrase consistent with v1.39.0 / v1.38.0 entries:

    ```markdown
    ## [1.41.0] - <ship-date — leave as YYYY-MM-DD placeholder, set to actual ship date at deploy time>

    ### Added

    - Optional per-direction RTT bloat thresholds for the legacy 3-state upload
      controller: `continuous_monitoring.upload.target_bloat_ms` (1-200 ms) and
      `continuous_monitoring.upload.warn_bloat_ms` (1-250 ms). When absent,
      upload behavior is byte-identical with v1.40 (DL globals shared as before).
      When present, the live-tuner cannot silently overwrite operator-explicit
      UL thresholds — per-key presence flags gate writes to `target_delta` and
      `warn_delta` independently. Phase 200, ARB-05, D-03.
    - Startup-time WARNING log emission for any unknown `continuous_monitoring.*`
      key in the deployment YAML. Closes the v1.40 silent-ignore gap that
      allowed prod `/etc/wanctl/spectrum.yaml` to carry 4 unrecognized UL keys
      for 3 days. SAFE-06, D-08.
    - `tests/test_wan_controller.py::test_upload_thresholds_explicit_when_value_equal_to_global`
      and `test_upload_thresholds_explicit_per_key_independence` — D-03 invariant tests.
    - `tests/test_autorate_config.py::TestSafe06UnknownKeyWarning` — SAFE-06 regression test.

    ### Changed

    - `configs/spectrum.yaml` upload section adopts D-05 latency-first gaming-server
      soak settings: `ceiling_mbps: 28 → 18`, `factor_down_yellow: 0.94 → 0.98`,
      `target_bloat_ms: 42` (new), `warn_bloat_ms: 105` (new). Operator evidence
      trail in `.planning/spectrum-{upload-ceiling,lower-ceiling,inline-native-18-upload}-2026-04-29.md`.
    - `tests/test_phase_195_replay.py::test_safe05_threshold_name_counts_are_unchanged`
      bumps `warn_bloat` and `target_bloat` expected counts to reflect the v1.41
      per-direction wiring. v1.40 SAFE-05 pin is intentionally superseded; the
      other 7 pinned counts remain unchanged. ARB-05, D-09.

    ### Migration

    - **Service restart required for new keys.** The new `continuous_monitoring.upload.target_bloat_ms`
      and `warn_bloat_ms` keys load only at `Config.__init__` time. SIGUSR1 hot-reload
      handlers (`_reload_hysteresis_config`, `_reload_fusion_config`,
      `_reload_tuning_config`, `_reload_cake_signal_config`) do NOT pick up changes
      to these keys. Operators changing UL thresholds in YAML must run
      `sudo systemctl restart wanctl@<wan>.service` for the change to take effect.
      DOCS-03, D-12.
    ```

    Step 2 — Edit `docs/CONFIGURATION.md`. Find the existing UL config section (around line 65-80, where `target_bloat_ms` / `warn_bloat_ms` first appear in the example YAML) and add a new subsection AFTER the existing thresholds documentation. Use the same Markdown style as the rest of the file (no emojis, sentence-style headers, code fences for examples). Suggested placement: directly after the "Threshold Tuning Guidance" or equivalent subsection. If unsure, place it as a new top-level `## Per-Direction Upload Thresholds (v1.41+)` section above any future-roadmap section.

    Content (adjust phrasing to fit the existing doc voice):

    ```markdown
    ## Per-Direction Upload Thresholds (v1.41+)

    The legacy 3-state upload controller normally shares the same RTT bloat thresholds
    as the 4-state download controller: `continuous_monitoring.thresholds.target_bloat_ms`
    (default 15 ms) and `continuous_monitoring.thresholds.warn_bloat_ms` (default 45 ms).

    On deployments where upload saturation under DL-shared thresholds causes the
    upload controller to oscillate between ceiling and floor (Spectrum cable production
    pattern observed 2026-04-29: 31 UL hysteresis suppressions per 60 s with DL=0),
    you can override the upload thresholds independently:

    ```yaml
    continuous_monitoring:
      upload:
        # Existing keys preserved; new optional keys below.
        target_bloat_ms: 42   # GREEN -> YELLOW (UL-only)
        warn_bloat_ms: 105    # YELLOW -> RED   (UL-only)
    ```

    Bounds:

    - `target_bloat_ms`: 1-200 ms
    - `warn_bloat_ms`: 1-250 ms
    - Ordering: `target_bloat_ms` MUST be strictly less than `warn_bloat_ms`
      (validated at config load; `Config(...)` raises `ValueError` if violated)

    When both keys are absent, upload thresholds fall back to the global thresholds
    byte-identically — non-Spectrum deployments are unaffected.

    ### Migration note: service restart required (NOT SIGUSR1)

    The `target_bloat_ms` and `warn_bloat_ms` keys under `continuous_monitoring.upload`
    are loaded only at daemon startup (`Config.__init__`). The SIGUSR1 hot-reload
    handlers cover only `dwell_cycles`, `deadband_ms`, fusion settings, tuning
    settings, and CAKE-signal settings — they do NOT pick up changes to these
    upload-threshold keys.

    To change UL thresholds in production:

    1. Edit `/etc/wanctl/<wan>.yaml`
    2. Run `sudo systemctl restart wanctl@<wan>.service`
    3. Verify via `curl -s http://127.0.0.1:9101/health | jq` (the daemon will load
       the new values; live-tuner cannot silently overwrite them — per-key presence
       flags gate writes when the keys are explicitly set in YAML)

    Sending SIGUSR1 alone will NOT apply changes to these keys.
    ```

    Step 3 — Sanity grep:

    ```bash
    grep -E "1\.41\.0" CHANGELOG.md  # at least one match expected
    grep -E "SIGUSR1" docs/CONFIGURATION.md  # at least one new match expected
    grep -E "service restart|systemctl restart" docs/CONFIGURATION.md  # at least one match
    grep -E "continuous_monitoring\.upload\.target_bloat_ms" docs/CONFIGURATION.md  # at least one match
    ```

    All four must succeed. If any fails, the corresponding section was inserted into the wrong place — fix.
  </action>
  <verify>
    <automated>grep -qE "1\.41\.0" CHANGELOG.md && grep -qE "SIGUSR1" docs/CONFIGURATION.md && grep -qE "systemctl restart wanctl@" docs/CONFIGURATION.md && grep -qE "continuous_monitoring.upload.target_bloat_ms|target_bloat_ms.*upload|upload.*target_bloat_ms" docs/CONFIGURATION.md</automated>
  </verify>
  <done>
    CHANGELOG.md carries the v1.41.0 entry covering Added/Changed/Migration sections with explicit DOCS-03/ARB-05/SAFE-06/D-03/D-08/D-09 citations. docs/CONFIGURATION.md documents the new keys, their bounds, ordering rule, and the SIGUSR1-vs-restart migration note with the explicit `systemctl restart wanctl@<wan>.service` command.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator behavior | Operators rely on docs for upgrade procedure; an inaccurate migration note becomes a production-incident vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-09 | Tampering | Migration note implies SIGUSR1 reloads new keys; operators send SIGUSR1, threshold change silently does not apply, latency regression | mitigate | Task 2 explicitly states "Sending SIGUSR1 alone will NOT apply changes" with `systemctl restart wanctl@<wan>.service` as the documented procedure. |
| T-200-10 | Spoofing | Version surfaces drift (e.g., pyproject.toml=1.41.0 but Dockerfile still 1.39.0); deployed image misreports version | mitigate | Task 1 verify command checks all three surfaces atomically; abort if any disagrees. |
| T-200-11 | Information Disclosure | CHANGELOG documents internal flag attribute names (`_upload_target_bloat_ms_explicit`) | accept | Operator-facing audit trail benefits from precise terms; no secret material exposed. |
</threat_model>

<verification>
- All three version surfaces report 1.41.0.
- `configs/spectrum.yaml` upload section matches D-05 exactly.
- `CHANGELOG.md` 1.41.0 entry exists with Added/Changed/Migration sections.
- `docs/CONFIGURATION.md` carries the migration note with explicit SIGUSR1-vs-restart guidance.
- Hot-path test slice still passes (this plan touches no code paths): `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_autorate_config.py -q`

**Rollback:** `git revert <plan-04-commit>` restores 1.39.0 surfaces and pre-D-05 spectrum.yaml. Production state is not touched until Plan 06 runs the deploy.
</verification>

<success_criteria>
1. Version 1.41.0 in all three surfaces.
2. configs/spectrum.yaml carries D-05 UL settings.
3. CHANGELOG.md 1.41.0 entry references ARB-05, SAFE-06, D-03, D-08, D-09 explicitly.
4. docs/CONFIGURATION.md documents the new keys and the SIGUSR1-vs-restart asymmetry.
5. Sanity greps for "1.41.0", "SIGUSR1", "systemctl restart", and the new key paths succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-04-SUMMARY.md` with:
- key-files.modified: pyproject.toml, src/wanctl/__init__.py, docker/Dockerfile, configs/spectrum.yaml, CHANGELOG.md, docs/CONFIGURATION.md
- requirements-addressed: [DOCS-03]
- decisions-applied: [D-05, D-11, D-12]
- next plan: 200-05 (pre-deploy canary script for VALN-06)
</output>
