---
milestone: v1.35
audited: 2026-04-13T20:08:00-05:00
status: gaps_found
scores:
  requirements: 6/6
  phases: 4/5
  integration: 0/4
  flows: 0/3
gaps:
  requirements: []
  integration:
    - from: "172"
      to: "173"
      requirement_ids: ["STOR-01", "DEPL-01"]
      issue: "scripts/migrate-storage.sh exists, but scripts/deploy.sh still does not wire or document the required deploy -> migrate -> restart -> canary operator path."
    - from: "173"
      to: "runtime/install"
      requirement_ids: ["DEPL-01"]
      issue: "scripts/install.sh still advertises VERSION=\"1.32.2\" while the shipped runtime/package version is 1.35.0."
    - from: "173"
      to: "174"
      requirement_ids: ["DEPL-01", "SOAK-01"]
      issue: "The packaged operator-summary entry point exists in pyproject.toml, but install/deploy do not place that CLI on target systems, so the soak flow fell back to direct python invocation."
    - from: "174"
      to: "176"
      requirement_ids: ["STOR-03", "SOAK-01"]
      issue: "Soak monitoring and 24h journal evidence do not cover all claimed services; steering.service remains outside the captured error-scan path."
  flows:
    - name: "deploy -> migrate storage -> restart -> canary"
      requirement_ids: ["STOR-01", "DEPL-01"]
      issue: "Production succeeded only with an operator-side manual migration step that the repo's active deploy flow does not express."
    - name: "install/deploy -> operator summary during soak"
      requirement_ids: ["DEPL-01", "SOAK-01"]
      issue: "The intended wanctl-operator-summary CLI is not actually wired into the deployed target workflow."
    - name: "24h soak evidence -> all claimed services"
      requirement_ids: ["STOR-03", "SOAK-01"]
      issue: "Canary evidence covered Spectrum, ATT, and steering, but soak-monitor and journal evidence remained incomplete for ATT/steering service coverage."
tech_debt:
  - phase: "172"
    items:
      - "172-VALIDATION.md still exists in draft / nyquist_compliant: false state."
  - phase: "173"
    items:
      - "173-VALIDATION.md still exists in draft / nyquist_compliant: false state."
  - phase: "174"
    items:
      - "174-VALIDATION.md is bookkeeping for re-audit, not a Nyquist validation strategy document."
  - phase: "176"
    items:
      - "Phase directory exists in ROADMAP.md, but no plan, summary, verification, validation, or execution artifacts exist yet."
nyquist:
  compliant_phases: ["175"]
  partial_phases: ["172", "173", "174"]
  missing_phases: ["176"]
  overall: "partial"
---

# Milestone v1.35 Audit

## Result

**Status:** gaps_found  
**Requirements score:** 6/6 satisfied  
**Phase completion score:** 4/5 phases complete  
**Integration score:** 0/4 cross-phase checks fully closed  
**Flow score:** 0/3 end-to-end operational flows fully wired

Milestone `v1.35` now has complete requirement verification and traceability through Phases `172`-`175`, but it is not ready for archival. The unfinished Phase `176` still carries the operational integration work that the earlier phases exposed: deploy/migration flow alignment, install metadata parity, operator-summary packaging, and full soak evidence coverage including `steering.service`.

## Milestone Scope

- Version: `v1.35`
- Name: `Storage Health & Stabilization`
- Phases in scope: `172` through `176`
- Completed phases: `172`, `173`, `174`, `175`
- Pending phase: `176`
- Requirements in scope: `STOR-01`, `STOR-02`, `STOR-03`, `DEPL-01`, `DEPL-02`, `SOAK-01`

## Requirements Coverage

| REQ-ID | Description | Assigned / Owning Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final status |
| --- | --- | --- | --- | --- | --- | --- |
| STOR-01 | Reduce metrics DB below warning threshold | Phase 172 (re-verified Phase 175) | checked / Satisfied | satisfied in `172-VERIFICATION.md` | listed in 172 and 175 summaries | satisfied |
| STOR-02 | Fix periodic maintenance error | Phase 172 | checked / Satisfied | satisfied in `172-VERIFICATION.md` | listed in 172 summary | satisfied |
| STOR-03 | 24h soak keeps storage non-critical | Phase 174 (verified Phase 175) | checked / Satisfied | satisfied in `174-VERIFICATION.md` | listed in 175 summary | satisfied |
| DEPL-01 | Clean deploy and canary exit 0 | Phase 173 (verified Phase 175) | checked / Satisfied | satisfied in `173-VERIFICATION.md` | listed in 173 and 175 summaries | satisfied |
| DEPL-02 | `analyze_baseline.py` deployable on production | Phase 172 | checked / Satisfied | satisfied in `172-VERIFICATION.md` | listed in 172 summaries | satisfied |
| SOAK-01 | Full observability stack clean for 24h | Phase 174 (verified Phase 175) | checked / Satisfied | satisfied in `174-VERIFICATION.md` with residual | listed in 175 summary | satisfied |

All six milestone requirements are verified and none are orphaned. The audit fails on milestone completeness and cross-phase integration, not on requirements coverage.

## Phase Verification Status

| Phase | ROADMAP Status | Verification | Validation / Nyquist | Result |
| --- | --- | --- | --- | --- |
| 172 Storage Health & Code Fixes | Complete | `172-VERIFICATION.md` present, satisfied | `172-VALIDATION.md` present, partial (`nyquist_compliant: false`) | complete |
| 173 Clean Deploy & Canary Validation | Complete | `173-VERIFICATION.md` present, satisfied | `173-VALIDATION.md` present, partial (`nyquist_compliant: false`) | complete |
| 174 Production Soak | Complete | `174-VERIFICATION.md` present, satisfied with residual | `174-VALIDATION.md` present, bookkeeping-only / partial for Nyquist | complete |
| 175 Verification And Evidence Closeout | Complete | `175-VERIFICATION.md` present, passed | `175-VALIDATION.md` present, compliant | complete |
| 176 Deployment And Soak Flow Alignment | Pending | missing | missing | blocker |

## Cross-Phase Integration

The milestone’s documentation and verification chain is now internally consistent, but the integration checker still found unresolved operational wiring gaps:

- `172 -> 173` (`STOR-01`, `DEPL-01`): [scripts/deploy.sh](/home/kevin/projects/wanctl/scripts/deploy.sh:463) does not include or document the migration step exported by Phase `172`, so the actual production path remained `deploy -> manual migrate -> restart -> canary`.
- `173 -> runtime/install` (`DEPL-01`): [scripts/install.sh](/home/kevin/projects/wanctl/scripts/install.sh:20) still publishes `VERSION="1.32.2"` while the runtime and package version shipped in v1.35 is `1.35.0`.
- `173 -> 174` (`DEPL-01`, `SOAK-01`): `wanctl-operator-summary` is declared in [pyproject.toml](/home/kevin/projects/wanctl/pyproject.toml:15), but the install/deploy path does not place that entry point on target machines, so Phase `174` had to invoke the script directly from Python instead of the intended CLI.
- `174 -> 176` (`STOR-03`, `SOAK-01`): [scripts/soak-monitor.sh](/home/kevin/projects/wanctl/scripts/soak-monitor.sh:10) remains Spectrum-centric and its error polling checks only `wanctl@${wan_name}`, leaving ATT and `steering.service` outside the full soak evidence path.

## End-to-End Flows

### Flow 1: Deploy -> migrate -> restart -> canary

**Status:** incomplete

Phase `172` produced the migration artifact and Phase `173` depended on live migration evidence, but the active deploy workflow never incorporated that step. The flow worked in production only because an operator supplied the missing migration action manually.

### Flow 2: Install / deploy -> operator summary during soak

**Status:** incomplete

The repo declares a packaged operator-summary command, but the deployed operator path still fell back to `python3 operator_summary.py ...` during Phase `174`. The intended CLI and the actual production instructions are not aligned.

### Flow 3: 24h soak evidence -> all claimed services

**Status:** incomplete

Canary evidence covered `spectrum`, `att`, and `steering`, but the soak-monitor/journal chain did not. The milestone still lacks a single repeatable soak flow that captures err-level evidence for all claimed services.

## Critical Gaps

### Milestone Completion Blocker

- **Phase 176 is still pending.** The ROADMAP explicitly keeps `176-deployment-and-soak-flow-alignment` in scope for v1.35, and that phase owns the remaining integration work. The milestone definition of done is therefore not yet met.

### Cross-Phase Issues

- **Deploy flow mismatch (`STOR-01`, `DEPL-01`)**: repo flow omits migration orchestration/guidance before canary.
- **Install metadata mismatch (`DEPL-01`)**: install-facing version metadata is stale relative to shipped runtime/package metadata.
- **Operator-summary flow mismatch (`DEPL-01`, `SOAK-01`)**: packaged CLI exists logically but is not actually installed or surfaced in the operator workflow.
- **Soak coverage mismatch (`STOR-03`, `SOAK-01`)**: steering-service and full multi-service soak evidence remain incomplete.

## Nyquist Discovery

| Phase | VALIDATION.md | Compliant | Action |
| --- | --- | --- | --- |
| 172 | exists | false / partial | `/gsd-validate-phase 172` |
| 173 | exists | false / partial | `/gsd-validate-phase 173` |
| 174 | exists | partial bookkeeping only | `/gsd-validate-phase 174` |
| 175 | exists | true | none |
| 176 | missing | missing | `/gsd-validate-phase 176` after execution |

## Tech Debt

Non-blocking debt remains, but it is secondary to the unfinished Phase `176` blocker:

- `scripts/deploy.sh` post-deploy monitoring guidance still points only at `wanctl@${wan_name}` and never surfaces `steering.service` logs when steering is enabled.
- `scripts/soak-monitor.sh` is still host-specific and does not read like a reusable dual-WAN production soak harness.
- The verification / requirements layer is now clean; remaining debt is operational flow alignment, not audit bookkeeping.

## Conclusion

Milestone `v1.35` has achieved full requirement verification, but it has **not** achieved its archive-ready definition of done. The remaining work is no longer evidentiary; it is operational integration work already captured as Phase `176`.
