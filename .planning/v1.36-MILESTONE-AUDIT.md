---
milestone: v1.36
audited: 2026-04-13T23:59:00Z
status: gaps_found
scores:
  requirements: 3/5
  phases: 2/3
  integration: 2/4
  flows: 2/4
gaps:
  requirements:
    - id: "STOR-04"
      status: "orphaned"
      phase: "177"
      claimed_by_plans:
        - "177-01-PLAN.md"
        - "177-02-PLAN.md"
        - "177-03-PLAN.md"
      completed_by_plans:
        - "177-01-SUMMARY.md"
        - "177-02-SUMMARY.md"
        - "177-03-SUMMARY.md"
      verification_status: "orphaned"
      evidence: "Phase 177 has summaries and REQUIREMENTS.md marks STOR-04 satisfied, but .planning/phases/177-live-storage-footprint-investigation/177-VERIFICATION.md is missing. STOR-04 is therefore absent from all phase VERIFICATION.md files."
    - id: "STOR-06"
      status: "unsatisfied"
      phase: "178"
      claimed_by_plans:
        - "178-02-PLAN.md"
        - "178-03-PLAN.md"
      completed_by_plans:
        - "178-02-SUMMARY.md"
        - "178-03-SUMMARY.md"
      verification_status: "contradicted_by_phase_179"
      evidence: "Phase 179 production footprint report states metrics-spectrum.db and metrics-att.db are effectively unchanged versus the 2026-04-13 baseline and explicitly says the deployed per-WAN footprint is not materially smaller."
  integration:
    - from: "Phase 178 /metrics/history topology change"
      to: "Phase 179 live HTTP reader verification"
      requirement_ids:
        - "STOR-06"
        - "OPER-04"
      issue: "Repo-side intent was shared CLI/HTTP per-WAN discovery, but Phase 179 live evidence found /metrics/history preserved its envelope while failing to prove merged cross-WAN history."
    - from: "Phase 177 evidence handoff"
      to: "Milestone verification trail"
      requirement_ids:
        - "STOR-04"
      issue: "Phase 177 evidence is usable by later phases, but the absence of 177-VERIFICATION.md leaves the requirement unverified at milestone audit time."
  flows:
    - name: "Retention reduction -> production footprint improvement"
      requirement_ids:
        - "STOR-06"
      breaks_at: "Phase 179 live re-check"
      issue: "The storage-retention change shipped, but production DB sizes remained effectively flat against baseline."
    - name: "Operator proof flow: inventory -> storage.status -> history readers -> footprint interpretation"
      requirement_ids:
        - "OPER-04"
        - "STOR-06"
      breaks_at: "HTTP reader parity"
      issue: "The module-based CLI proof path works, but live /metrics/history is only suitable for envelope/availability checks and not authoritative cross-WAN proof."
tech_debt:
  - phase: "178"
    items:
      - "178-VERIFICATION.md remains status: human_needed even though Phase 179 supplied the live follow-up evidence."
  - phase: "179"
    items:
      - "Live /metrics/history HTTP behavior still drifts from the intended merged multi-WAN topology and should be tracked as follow-up correctness work."
      - "The documented wanctl-history wrapper was not actually available on the live host; operators had to use the module invocation path instead."
nyquist:
  compliant_phases:
    - "177"
    - "178"
    - "179"
  partial_phases: []
  missing_phases: []
  overall: "compliant"
---

# Milestone Audit: v1.36 Storage Retention And DB Footprint

## Result

Milestone `v1.36` is **not ready** for completion. The audit lands at `gaps_found`.

The blockers are:

- `STOR-04` is orphaned because Phase `177` never produced a verification artifact.
- `STOR-06` is contradicted by the live production evidence captured in Phase `179`.

## Requirement Coverage

| Requirement | Planned Phase | REQUIREMENTS.md | VERIFICATION.md coverage | SUMMARY frontmatter | Final status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| STOR-04 | 177 | `[x]` | missing | listed in all 177 summaries | orphaned | No `177-VERIFICATION.md` exists, so the requirement is absent from all milestone verification files. |
| STOR-05 | 178 | `[x]` | present, satisfied | listed | satisfied | Phase 178 verification explicitly marks the shared `metrics.db` role closed out. |
| STOR-06 | 178 | `[x]` | present, repo-side only | listed | unsatisfied | Phase 179 live footprint report says per-WAN DBs are unchanged and not materially smaller than baseline. |
| STOR-07 | 178 | `[x]` | present, satisfied | listed | satisfied | Phase 178 verification confirms retention/downsampling bounds remained conservative and production-safe. |
| OPER-04 | 179 | `[x]` | present, satisfied | listed | satisfied | Phase 179 verification and closeout provide a repeatable operator proof path for DB inventory, `storage.status`, and footprint interpretation. |

## Phase Verification Status

| Phase | VERIFICATION.md | Status | Audit interpretation |
| --- | --- | --- | --- |
| 177 | missing | missing | blocker |
| 178 | exists | `human_needed` | phase artifact exists; repo-side verification passed and deferred live proof to Phase 179 |
| 179 | exists | `verified` | complete |

## Cross-Phase Integration

### Verified

- Phase 177 findings fed directly into Phase 178’s storage-topology and retention changes.
- Phase 179 completed the live re-check path Phase 178 deferred for production evidence and operator proof.

### Failed / degraded

- **Phase 177 verification trail -> milestone requirement audit** (`STOR-04`)
  - The evidence chain is present in Phase 177 summaries and later phases rely on it successfully.
  - The milestone still fails audit because the phase never produced `177-VERIFICATION.md`, so `STOR-04` is absent from all verification tables.

- **Phase 178 `/metrics/history` topology intent -> Phase 179 live endpoint proof** (`STOR-06`, `OPER-04`)
  - Repo-side code and tests say CLI and HTTP readers should follow the same per-WAN discovery rule.
  - Phase 179 live evidence showed the CLI proof path works cross-WAN, but live `/metrics/history` did not return ATT rows during capture.

## End-to-End Flows

### Working

- **Operator proof flow** (`OPER-04`)
  - Active DB inventory -> `storage.status` -> CLI history proof -> HTTP envelope check -> direct DB spot-check is documented and repeatable.

### Broken

- **Retention tightening -> materially smaller per-WAN DB footprint** (`STOR-06`)
  - The shipped retention change did not produce a materially smaller production footprint by the time of the Phase 179 live audit.

### Degraded

- **Operator proof flow** (`OPER-04`, `STOR-06`)
  - DB inventory and `storage.status` work.
  - The CLI proof path works via `sudo -n env PYTHONPATH=/opt python3 -m wanctl.history ...`.
  - The bare `wanctl-history` wrapper was not available on the host, and live `/metrics/history` is not yet a reliable merged cross-WAN proof surface.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 177 | exists | true | none |
| 178 | exists | true | none |
| 179 | exists | true | none |

## Tech Debt

**Phase 178**
- `178-VERIFICATION.md` still carries `human_needed` status even though the later milestone phase supplied the live evidence it deferred to.

**Phase 179**
- Live `/metrics/history` correctness still needs follow-up if merged cross-WAN HTTP history is expected to match the CLI path.
- The audit’s integration section is based on phase artifacts and live evidence already captured in milestone docs; the dedicated integration-checker subagent did not return during this audit window.

## Routing

This milestone should stay open.

Immediate actions:

1. Add a formal `177-VERIFICATION.md` so `STOR-04` is no longer orphaned.
2. Re-scope `STOR-06` honestly:
   - either do the production work needed to materially reduce the per-WAN DB footprint,
   - or revise the requirement before milestone completion if “materially reduced” is no longer the intended bar.
3. Treat live `/metrics/history` drift as follow-up correctness work if HTTP cross-WAN parity is still expected.
