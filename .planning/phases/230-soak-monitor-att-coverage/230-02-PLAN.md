---
phase: 230-soak-monitor-att-coverage
plan: 02
type: execute
wave: 2
depends_on: [230-01]
files_modified:
  - .planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md
  - .planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md
autonomous: false
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "A real read-only soak-monitor run shows the aggregate/per-WAN scan now targets the live ATT units, which the pre-fix wanctl@att.service-only scan would have missed (success criterion 3)"
    - "The before/after unit-set contrast is recorded as a committed evidence artifact"
    - "SAFE-14 controller-path zero-diff holds at the phase boundary against the pinned baseline"
  artifacts:
    - path: ".planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md"
      provides: "Criterion-3 read-only before/after unit-set contrast evidence (pre-fix wanctl@att.service vs post-fix live ATT units)"
      contains: "cake-autorate-att.service"
    - path: ".planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md"
      provides: "SAFE-14 controller-path zero-diff proof: protected-path git diff vs baseline + dirty-tree status"
      contains: "87980bdf"
  key_links:
    - from: "230-MON01-EVIDENCE.md"
      to: "scripts/soak-monitor.sh --json units list"
      via: "read-only soak-monitor run output capture"
      pattern: "cake-autorate-att-state-bridge.service"
---

<objective>
Prove the fix in the field and at the boundary. Two deliverables, both documentation/evidence only:

1. Criterion-3 evidence (MON-01): a read-only demonstration that, post-fix, soak-monitor's scan targets the live ATT units — contrasted with the pre-fix `wanctl@att.service`-only scan that would have missed any ATT-unit error. NO live mutation, NO fault injection on production hosts (RESEARCH Pitfall 5). The cleanest no-mutation proof is the `--json` `units`-list before/after contrast; supplement with any naturally-occurring ATT `err` already present in the 1h window.

2. SAFE-14 boundary proof: confirm only `scripts/soak-monitor.sh` + the new test changed and the controller `.py` path is byte-identical to baseline `87980bdf` (Phase 229's pinned last docs/planning-only commit, `git rev-parse 87980bdf` resolves). Record the protected-path diff output and the dirty-tree status, mirroring the `check-safe07-source-diff.sh` committed-diff-vs-ref + fail-closed pattern.

Purpose: close MON-01 success criterion 3 with field evidence and hold SAFE-14 at the phase boundary.
Output: two evidence notes in the phase dir.

Operator/credential note (per MEMORY): the live read-only run hits cake-shaper journals. If a privileged journal read (`journalctl -p err` / `sudo -n`) is denied, do NOT escalate credentials — hand Kevin a `! <command>` to run at the keyboard and paste back. The `--json` `units`-list contrast does not require privileged journal access (it shows which units the scan targets) and is sufficient on its own for criterion 3 if journal err-counts are gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-03-SUMMARY.md

<interfaces>
<!-- Baseline + protected set, VERIFIED 2026-06-09. -->
SAFE-14 baseline commit: 87980bdf8ea52e5537110cd9bbc7a368f523d2e2 (Phase 229 last docs/planning-only commit; git rev-parse 87980bdf resolves)
Protected controller-path set (per Decision 227-04, includes wan_controller_state.py):
  src/wanctl/wan_controller.py  src/wanctl/wan_controller_state.py
  src/wanctl/queue_controller.py  src/wanctl/cake_signal.py
  src/wanctl/alert_engine.py  src/wanctl/fusion_healer.py  src/wanctl/backends/
Pattern reference: scripts/check-safe07-source-diff.sh (committed-diff-vs-ref, dirty-tree pre-check, fail-closed at exit 1).
soak-monitor evidence surface: ./scripts/soak-monitor.sh --json -> the {"service_group":"all-claimed-services","units":[...]} object's units list (post-fix includes the three ATT live units; pre-fix listed wanctl@att.service).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Criterion-3 read-only live evidence — ATT live-unit scan contrast</name>
  <files>.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md</files>
  <read_first>
    - scripts/soak-monitor.sh (post-Plan-01 state — the generalized --json aggregate units list is the evidence surface)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Pitfall 5 read-only evidence rule, Open Question 3 evidence form)
  </read_first>
  <action>
    Capture the criterion-3 read-only field evidence and record it in `.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md`. All steps READ-ONLY — no `systemctl start/stop`, no fault injection on live units.

    Step A (post-fix capture): run `./scripts/soak-monitor.sh --json | python3 -m json.tool` and capture the `{"service_group":"all-claimed-services", ...}` object's `units` list and the `att` per-WAN row's `errors_1h`. Confirm the units list contains `cake-autorate-att.service`, `cake-autorate-att-state-bridge.service`, `silicom-bypass-watchdog-cake-autorate-att.service` and does NOT contain `wanctl@att.service` (ATT in external mode).

    Step B (pre-fix contrast WITHOUT reverting code): display the old hardcoded ATT branch via `git show 230-01~1:scripts/soak-monitor.sh` (or use the recorded pre-fix `wanctl@att.service`-only scan from RESEARCH). Record the contrast — pre-fix ATT scan targeted only the disabled `wanctl@att.service`, so any error in the live ATT units would have been missed.

    Step C (credential handling): if a privileged journal read is denied during Step A, do NOT escalate credentials — hand Kevin a `! <command>` to run at the keyboard and paste back (MEMORY). The `units`-list contrast does not need privileged journal access and is sufficient for criterion 3.

    Step D (record): write `230-MON01-EVIDENCE.md` containing the captured post-fix `units` list, the pre-fix contrast, any naturally-present ATT-unit `err` in the window, and a one-line verdict that the post-fix scan targets units the pre-fix scan would have missed. Use only IPs/units already committed in the repo.

    This task is a human-verify checkpoint: after recording, pause for operator confirmation that the `--json` units list shows the live ATT units and the before/after contrast is correct.
  </action>
  <verify>
    <human-check>Operator confirms `./scripts/soak-monitor.sh --json` aggregate units list shows the three live ATT units (and no `wanctl@att.service` in external mode), and that `230-MON01-EVIDENCE.md` records the pre-fix vs post-fix unit-set contrast.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md` exists and contains the literal `cake-autorate-att.service`, `cake-autorate-att-state-bridge.service`, and `silicom-bypass-watchdog-cake-autorate-att.service` in the captured post-fix units list.
    - The evidence records the pre-fix `wanctl@att.service`-only ATT scan as the contrast (the scan path the fix replaced).
    - A one-line verdict states the post-fix scan targets ATT units the pre-fix scan would have missed (criterion 3).
    - All captured commands are read-only; the note contains no fault-injection or `systemctl start/stop` step.
    - Operator types "approved" (or describes what is missing).
  </acceptance_criteria>
  <done>The criterion-3 evidence note records a read-only before/after unit-set contrast proving the post-fix scan targets live ATT units the pre-fix scan would have missed, and the operator has approved.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-14 controller-path zero-diff boundary proof</name>
  <files>.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md</files>
  <read_first>
    - .planning/phases/229-att-deploy-path-artifact-tests/229-03-SUMMARY.md (the SAFE-14 boundary proof shape: committed protected-path diff + dirty-tree status, baseline 87980bdf)
    - scripts/check-safe07-source-diff.sh (committed-diff-vs-ref + dirty-tree pre-check + fail-closed pattern to mirror)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Pitfall 4, SAFE-14 Code Examples L265-274)
  </read_first>
  <action>
    Produce the SAFE-14 boundary proof for Phase 230 and record it in `.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md`.

    1. Run the committed protected-path diff vs the pinned baseline `87980bdf` over the protected set: `git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/`. This MUST be empty.
    2. Run the dirty-tree pre-check over the same protected set: `git status --porcelain -- src/wanctl/` plus `git diff --quiet -- src/wanctl/` and `git diff --cached --quiet -- src/wanctl/` — confirm the controller path is clean. Fail-closed: any protected-path diff or dirty state is a VIOLATION and the phase boundary does not pass.
    3. Confirm the only files changed since baseline in this phase are `scripts/soak-monitor.sh` and `tests/test_soak_monitor_att_coverage.py` (plus `.planning/` evidence): `git diff --stat 87980bdf -- scripts/ tests/` should show only those two paths.
    4. Record into `230-SAFE14-BOUNDARY.md`: the baseline SHA `87980bdf8ea52e5537110cd9bbc7a368f523d2e2`, the empty protected-path diff output, the clean dirty-tree status, the two-file in-scope change list, and a PASS/VIOLATION verdict. Mirror the 229-03 evidence shape.

    Pin the baseline to `87980bdf` per RESEARCH (Phase 229's last docs/planning-only commit) — do not re-pin to a later commit, since the goal is to prove zero controller drift across the whole ATT-coverage work since Phase 229.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && test -z "$(git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/)" && echo "SAFE-14 PASS: controller-path zero-diff vs 87980bdf"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 87980bdf -- <protected controller set>` is EMPTY (zero controller-path diff). Verifiable by the automated command echoing `SAFE-14 PASS`.
    - The protected controller path has no dirty-tree state: `git status --porcelain -- src/wanctl/` shows no controller `.py` changes attributable to this phase.
    - `git diff --stat 87980bdf -- scripts/ tests/` shows only `scripts/soak-monitor.sh` and `tests/test_soak_monitor_att_coverage.py`.
    - `.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md` exists, records baseline `87980bdf8ea52e5537110cd9bbc7a368f523d2e2`, the empty diff output, the clean dirty-tree status, and a PASS verdict, mirroring the 229-03 evidence shape.
  </acceptance_criteria>
  <done>SAFE-14 boundary proof recorded with an empty protected-path diff vs `87980bdf`, clean controller dirty-tree, two-file in-scope change list, and a PASS verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box -> cake-shaper host (ssh) | Criterion-3 evidence run crosses this read-only; `--json` units capture + optional `journalctl -p err` read only |
| repo working tree -> committed evidence | SAFE-14 proof reads git history/diff only; no source mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-05 | Tampering | criterion-3 live run | mitigate | Evidence run is strictly read-only (`--json` units capture, optional `journalctl -p err`); NO fault injection, NO `systemctl start/stop` on live ATT units (RESEARCH Pitfall 5) |
| T-230-06 | Elevation of Privilege | privileged journal read | mitigate | If `sudo -n`/journal read denied, hand operator a `! <command>` rather than escalating creds (MEMORY); units-list contrast needs no privileged access |
| T-230-07 | Information Disclosure | committed evidence note | mitigate | Evidence records only IPs/units already committed in the repo; no new secret/host exposure |
| T-230-08 | Tampering | SAFE-14 controller path | mitigate | Boundary proof is fail-closed: any non-empty protected-path diff or dirty controller state = VIOLATION, phase does not pass |
</threat_model>

<verification>
- `230-MON01-EVIDENCE.md` shows the post-fix `--json` aggregate `units` list containing the three ATT live units and the pre-fix `wanctl@att.service`-only contrast.
- `230-SAFE14-BOUNDARY.md` records an empty protected-path diff vs `87980bdf`, clean controller dirty-tree, and a PASS verdict.
- No live mutation or fault injection performed; all host contact read-only.
</verification>

<success_criteria>
- MON-01 criterion 3: field evidence demonstrates the post-fix scan targets ATT live units the pre-fix scan would have missed.
- SAFE-14: controller-path zero-diff holds at the phase boundary (empty diff vs baseline, clean dirty-tree, only two in-scope files changed).
</success_criteria>

<output>
Create `.planning/phases/230-soak-monitor-att-coverage/230-02-SUMMARY.md` when done.
</output>
