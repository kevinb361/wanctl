---
phase: 230-soak-monitor-att-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/soak-monitor.sh
  - tests/test_soak_monitor_att_coverage.py
autonomous: true
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "soak-monitor error-scan reads the live ATT units (cake-autorate-att.service, cake-autorate-att-state-bridge.service, silicom-bypass-watchdog-cake-autorate-att.service) instead of the disabled wanctl@att.service"
    - "soak-monitor mode detection is WAN-parameterized (is_external_cake_mode) with no Spectrum-only hardcoding; ATT enters external-controller mode at parity with Spectrum"
    - "All four Spectrum-hardcoded call sites (per-WAN JSON, per-WAN table, aggregate JSON, aggregate non-JSON) route ATT through the live-unit set"
    - "scripts/soak-monitor.sh stays shellcheck -S error clean after the edit"
    - "A regression test asserts ATT live-unit coverage and the generalized predicate, and fails before the fix"
  artifacts:
    - path: "tests/test_soak_monitor_att_coverage.py"
      provides: "Static read-and-assert regression test for ATT live-unit coverage + generalized mode detection + shellcheck guard"
      contains: "test_soak_monitor_scans_live_att_units"
    - path: "scripts/soak-monitor.sh"
      provides: "WAN-parameterized external-mode predicate + per-WAN live-unit map, all four call sites generalized"
      contains: "is_external_cake_mode"
  key_links:
    - from: "scripts/soak-monitor.sh run_check per-WAN error branch"
      to: "external_units_for / is_external_cake_mode"
      via: "external-mode dispatch replacing the [[ wan == spectrum ]] guard"
      pattern: "is_external_cake_mode"
    - from: "tests/test_soak_monitor_att_coverage.py"
      to: "scripts/soak-monitor.sh"
      via: "Path read_text substring assertions"
      pattern: "cake-autorate-att-state-bridge.service"
---

<objective>
Close the ATT error-scan blind spot in `scripts/soak-monitor.sh`. After the 2026-06-08 migration both WANs run cake-autorate external-controller mode and `wanctl@att.service` is disabled, but soak-monitor still scans that disabled unit for ATT — so any error in the live ATT units is invisible. This plan generalizes the Spectrum-only mode detection into a per-WAN `is_external_cake_mode` predicate, adds a per-WAN live-unit map (`external_units_for`) that special-cases the ATT-only silicom watchdog, and updates all four Spectrum-hardcoded call sites so ATT scans its real units at Spectrum parity.

Purpose: MON-01 (error-scan covers live ATT units) + MON-02 (no Spectrum-only hardcoding in mode detection).
Output: Generalized `scripts/soak-monitor.sh` (still shellcheck-clean), new regression test `tests/test_soak_monitor_att_coverage.py`.

Scope guardrails (from RESEARCH): observability-only. NO controller `.py` edits (SAFE-14). All host probes stay read-only (`systemctl is-active`, `journalctl -p err`, `curl /health`) — no `systemctl start/stop`, no fault injection. Do NOT rebuild ATT health acquisition: ATT `/health` on the bridge endpoint already works via `check_target`; do NOT add a Spectrum-style `sudo -n python3` ATT state-fallback block. Do NOT introduce `bats` (not installed). Do NOT do a generic `$wan` symmetry refactor — parameterize only as far as MON-01/02 require.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md
@.planning/phases/230-soak-monitor-att-coverage/230-PATTERNS.md

<interfaces>
<!-- Key existing shapes the executor must reuse. Extracted from the codebase 2026-06-09. -->

scripts/soak-monitor.sh — header literals (L10-19):
  TARGETS=( "kevin@10.10.110.223|spectrum|10.10.110.223"  "kevin@10.10.110.223|att|10.10.110.227" )
  SERVICE_UNITS=( "wanctl@spectrum.service" "wanctl@att.service" "steering.service" )
  HEALTH_PORT=9101  (set -euo pipefail at top)

scripts/soak-monitor.sh — existing Spectrum-only predicate (L275-280), to generalize:
  is_spectrum_cake_trial_active() { ssh ... 'test "$(systemctl is-active cake-autorate-spectrum.service)" = active && test "$(systemctl is-active wanctl@spectrum.service)" != active' ; }

scripts/soak-monitor.sh — check_errors() (L283-297) is variadic over unit names; REUSE VERBATIM, only change which units are passed.

scripts/soak-monitor.sh — the FOUR Spectrum-hardcoded call sites to generalize:
  L327-331  per-WAN JSON branch:   if [[ "$wan_name" == "spectrum" ]] && is_spectrum_cake_trial_active ... else check_errors "$ssh_target" "wanctl@${wan_name}.service"
  L340-344  per-WAN table branch:  identical shape to L327
  L398-413  aggregate JSON:        is_spectrum_cake_trial_active "kevin@10.10.110.223" builds service_errors + service_units_json (HARDCODES wanctl@att.service even in trial path = MON-01 aggregate bug)
  L417-431  aggregate non-JSON:    same binary + service_label string + final investigate hint "journalctl -u wanctl@spectrum.service -u wanctl@att.service -u steering.service"

Live ATT unit set (deploy/systemd/*.service, VERIFIED):
  cake-autorate-att.service / cake-autorate-att-state-bridge.service / silicom-bypass-watchdog-cake-autorate-att.service (ATT-ONLY, no Spectrum sibling)
Spectrum external set: cake-autorate-spectrum.service, cake-autorate-spectrum-state-bridge.service (no watchdog)
</interfaces>

@tests/test_spectrum_cake_autorate_artifacts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing regression test for ATT live-unit coverage and generalized mode detection</name>
  <files>tests/test_soak_monitor_att_coverage.py</files>
  <read_first>
    - tests/test_spectrum_cake_autorate_artifacts.py (read-and-assert analog: imports, REPO_ROOT path anchor, read_text + substring asserts, subprocess.run pattern)
    - scripts/soak-monitor.sh (current state — to confirm the asserted strings are ABSENT now, so the test fails RED before Task 2/3)
    - .planning/phases/230-soak-monitor-att-coverage/230-PATTERNS.md (test section, L126-198)
  </read_first>
  <behavior>
    - test_soak_monitor_scans_live_att_units: soak-monitor.sh text contains all three ATT live units: "cake-autorate-att.service", "cake-autorate-att-state-bridge.service", "silicom-bypass-watchdog-cake-autorate-att.service". FAILS now.
    - test_soak_monitor_mode_detection_not_spectrum_hardcoded: soak-monitor.sh text contains "is_external_cake_mode". FAILS now.
    - test_soak_monitor_external_units_for_att_has_watchdog: soak-monitor.sh text contains "external_units_for" AND the att branch includes "silicom-bypass-watchdog-cake-autorate-att.service". FAILS now.
    - test_soak_monitor_shellcheck_clean: shutil.which-gated; if shellcheck present, "shellcheck -S error scripts/soak-monitor.sh" returns 0; else pytest.skip. PASSES now and must keep passing.
  </behavior>
  <action>
    Create `tests/test_soak_monitor_att_coverage.py` mirroring `tests/test_spectrum_cake_autorate_artifacts.py`. Use `from __future__ import annotations`, import `shutil`, `subprocess`, and `from pathlib import Path`. Anchor `REPO_ROOT = Path(__file__).resolve().parents[1]` and `SOAK = REPO_ROOT / "scripts" / "soak-monitor.sh"`.

    Write four test functions exactly as named in the behavior block. For the three text-assertion tests, call `text = SOAK.read_text(encoding="utf-8")` then `assert "<literal>" in text` for each unit/identifier listed. For `test_soak_monitor_shellcheck_clean`, guard with `if not shutil.which("shellcheck"): import pytest; pytest.skip("shellcheck not installed")`, then `r = subprocess.run(["shellcheck", "-S", "error", str(SOAK)], capture_output=True)` and `assert r.returncode == 0, r.stdout.decode() + r.stderr.decode()`.

    Do NOT add a brittle `assert "wanctl@att" not in text` negative — `wanctl@att.service` legitimately persists as the native-mode fallback (Open Question 2, Phase 231 rollback support). Prove coverage via the positive `is_external_cake_mode` + `external_units_for` + ATT-unit assertions instead. Keep the test free of any host contact (no ssh/curl) and no `integration` marker so it stays in the default `-m 'not integration'` suite under the 30s timeout.

    This is the RED step: run the suite and confirm the three text-assertion tests FAIL while the shellcheck test passes. Commit RED before implementing.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/test_soak_monitor_att_coverage.py --collect-only -q 2>&1 | grep -c "test_soak_monitor"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_soak_monitor_att_coverage.py` exists with exactly four test functions named: test_soak_monitor_scans_live_att_units, test_soak_monitor_mode_detection_not_spectrum_hardcoded, test_soak_monitor_external_units_for_att_has_watchdog, test_soak_monitor_shellcheck_clean.
    - Before Task 2/3 land: the three text-assertion tests FAIL (RED), the shellcheck test PASSES. Verifiable: `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q` reports 3 failed, 1 passed.
    - No ssh/curl/socket host contact in the test; no `integration` marker; uses `shutil.which` gating for shellcheck.
    - `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py --collect-only -q` lists exactly four test_soak_monitor functions (collection clean, no import errors).
  </acceptance_criteria>
  <done>The regression test exists, collects four tests cleanly, and the three coverage tests are RED while shellcheck is green — proving the test discriminates the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add generalized external-mode predicate + per-WAN unit map; fix both per-WAN error branches</name>
  <files>scripts/soak-monitor.sh</files>
  <read_first>
    - scripts/soak-monitor.sh (L275-297 predicate + check_errors; L320-344 per-WAN loop and both error branches)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Patterns 1-2, Code Examples L209-237, Pitfall 2 silicom-only, Pitfall 3 call sites)
    - .planning/phases/230-soak-monitor-att-coverage/230-PATTERNS.md (mode predicate + error scanner + call site 1/2 generalization, L39-106)
    - tests/test_soak_monitor_att_coverage.py (the GREEN target written in Task 1)
  </read_first>
  <action>
    Add two new bash helpers near the existing `is_spectrum_cake_trial_active` (L275-280):

    1. `is_external_cake_mode()` — takes `ssh_target` ($1) and `wan` ($2). Generalize the Spectrum predicate: ssh with `-o ConnectTimeout=5 -o BatchMode=yes`, remote test that `systemctl is-active cake-autorate-${wan}.service` equals active AND `systemctl is-active wanctl@${wan}.service` is not active, redirect `>/dev/null 2>&1`. Use a DOUBLE-quoted ssh payload with escaped `\$(...)` and `\"` so `${wan}` interpolates locally while the command-substitutions run remotely (see PATTERNS L49-56 for exact escaping). `${wan}` comes only from in-repo TARGETS literals — do not interpolate untrusted input (V5).

    2. `external_units_for()` — takes `wan` ($1). Body: `case "$1" in att) echo "cake-autorate-att.service cake-autorate-att-state-bridge.service silicom-bypass-watchdog-cake-autorate-att.service" ;; *) echo "cake-autorate-$1.service cake-autorate-$1-state-bridge.service" ;; esac`. The att branch MUST include the silicom watchdog (Pitfall 2 — ATT-only).

    Keep `is_spectrum_cake_trial_active` defined in place (lower-risk); remove only if zero call sites reference it after Task 2/3.

    Fix the TWO per-WAN error branches in `run_check()`:
    - L327-331 (JSON per-WAN): replace the `[[ "$wan_name" == "spectrum" ]] && is_spectrum_cake_trial_active "$ssh_target"` guard with `is_external_cake_mode "$ssh_target" "$wan_name"`, and replace the Spectrum-literal `check_errors ... "cake-autorate-spectrum.service" "cake-autorate-spectrum-state-bridge.service"` with `check_errors "$ssh_target" $(external_units_for "$wan_name")`. Keep the `else` branch `check_errors "$ssh_target" "wanctl@${wan_name}.service"` as native-mode fallback. Annotate the word-split call with `# shellcheck disable=SC2046` (quoting would pass the whole list as one `-u` arg).
    - L340-344 (table per-WAN): identical shape — same generalization, same `# shellcheck disable=SC2046` annotation.

    Run the test suite and shellcheck. After this task the three coverage tests pass (the literals `cake-autorate-att*`, `silicom-bypass-watchdog-cake-autorate-att.service`, `is_external_cake_mode`, `external_units_for` all appear); the aggregate scan is fixed in Task 3.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && shellcheck -S error scripts/soak-monitor.sh && .venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/soak-monitor.sh` defines `is_external_cake_mode()` (positional args ssh_target + wan) and `external_units_for()`. Verifiable: `grep -c "is_external_cake_mode\|external_units_for" scripts/soak-monitor.sh` >= 3.
    - The att branch of `external_units_for` includes `silicom-bypass-watchdog-cake-autorate-att.service`. Verifiable: `grep -c "silicom-bypass-watchdog-cake-autorate-att.service" scripts/soak-monitor.sh` >= 1.
    - Both per-WAN error branches dispatch via `is_external_cake_mode "$ssh_target" "$wan_name"` with the `[[ "$wan_name" == "spectrum" ]]` guard removed. Verifiable: `grep -c 'is_external_cake_mode "\$ssh_target" "\$wan_name"' scripts/soak-monitor.sh` >= 2.
    - `shellcheck -S error scripts/soak-monitor.sh` returns 0 (word-split calls carry `# shellcheck disable=SC2046`).
    - `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q` reports 4 passed.
  </acceptance_criteria>
  <done>The generalized predicate and unit map exist, both per-WAN branches route ATT to its live units, the script is shellcheck-clean, and all four regression tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generalize the aggregate summary scan (JSON + non-JSON) to iterate WANs through external mode</name>
  <files>scripts/soak-monitor.sh</files>
  <read_first>
    - scripts/soak-monitor.sh (L398-413 aggregate JSON branch; L417-431 aggregate non-JSON branch incl. service_label and the final investigate hint on L431)
    - .planning/phases/230-soak-monitor-att-coverage/230-RESEARCH.md (Pattern 3 aggregate scan, Pitfall 3 — aggregate hardcodes wanctl@att.service even in trial path)
    - .planning/phases/230-soak-monitor-att-coverage/230-PATTERNS.md (call site 3/4, L108-122)
    - tests/test_soak_monitor_att_coverage.py
  </read_first>
  <action>
    Fix the two aggregate summary scans so the all-services error scan and the reported unit list reflect the live units for BOTH WANs, not the Spectrum-trial-vs-SERVICE_UNITS binary that hardcodes `wanctl@att.service`.

    Build the aggregate unit set by iterating the TARGETS WANs: for each `wan_name`, if `is_external_cake_mode "$ssh_target" "$wan_name"` use `external_units_for "$wan_name"`, else use `wanctl@${wan_name}.service`. Always append `steering.service` exactly once (runs in both modes per CLAUDE.md service model). Use the ssh target derived from TARGETS rather than re-hardcoding `kevin@10.10.110.223`; if a single aggregate `check_errors` call is retained, pass the shared ssh target read from TARGETS (all current TARGETS share `kevin@10.10.110.223`) — do not introduce a new literal.

    - L398-413 (JSON aggregate): replace the `is_spectrum_cake_trial_active` binary. Accumulate the iterated unit list into a bash array, pass it to `check_errors` for `service_errors`, and build `service_units_json` from the SAME array so `--json` reports the real units. This `units` array is the criterion-3 before/after evidence surface (Plan 02 consumes it) — in external mode it must contain the three ATT units and must NOT contain `wanctl@att.service`.
    - L417-431 (non-JSON aggregate): replace the same binary; build `service_label` from the iterated unit list (comma-joined). Update the final investigate hint on L431 from the hardcoded `journalctl -u wanctl@spectrum.service -u wanctl@att.service -u steering.service` to a hint built from the iterated live units (or a generic instruction referencing the live unit set).

    Keep `SERVICE_UNITS` defined as the native-mode reference/fallback (Open Question 2) but do not let the external-mode aggregate path emit `wanctl@att.service`. Annotate any intentional word-split with `# shellcheck disable=SC2046`.

    Run shellcheck and the full regression suite. Do NOT make a live ssh call here — live evidence is Plan 02.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && shellcheck -S error scripts/soak-monitor.sh && .venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q && test "$(grep -c 'is_spectrum_cake_trial_active \"kevin@10.10.110.223\"' scripts/soak-monitor.sh)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Neither aggregate branch dispatches via `is_spectrum_cake_trial_active "kevin@10.10.110.223"`. Verifiable: `grep -c 'is_spectrum_cake_trial_active "kevin@10.10.110.223"' scripts/soak-monitor.sh` == 0.
    - The aggregate scan iterates WANs and includes `steering.service` exactly once; the external-mode path emits the three ATT live units and not `wanctl@att.service`. Verifiable by reading the rebuilt block: the unit accumulation references `external_units_for` and `is_external_cake_mode`.
    - The non-JSON final investigate hint no longer hardcodes `-u wanctl@att.service`. Verifiable: `grep -c "journalctl -u wanctl@spectrum.service -u wanctl@att.service -u steering.service" scripts/soak-monitor.sh` == 0.
    - `shellcheck -S error scripts/soak-monitor.sh` returns 0.
    - `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q` reports 4 passed and full `.venv/bin/pytest tests/ -q` stays green.
  </acceptance_criteria>
  <done>Both aggregate scans iterate WANs through external mode, the JSON `units` list and non-JSON label/hint reflect the live ATT units, no `wanctl@att.service` leaks into the external-mode aggregate path, script is shellcheck-clean, and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box -> cake-shaper host (ssh) | Read-only `systemctl is-active` / `journalctl -p err` / `curl /health` cross this boundary; no mutation |
| in-repo TARGETS literals -> ssh command string | WAN names (`spectrum`/`att`) interpolate into the `cake-autorate-${wan}` systemctl string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-01 | Tampering | `is_external_cake_mode` ssh payload | mitigate | `${wan}` sourced only from in-repo `TARGETS[]` literals (`spectrum`/`att`); never interpolate untrusted/external input into the systemctl string (ASVS V5) |
| T-230-02 | Tampering | soak-monitor host probes | mitigate | Read-only probe contract preserved — only `systemctl is-active`, `journalctl -p err`, `curl /health`; no `systemctl start/stop`, no writes, no fault injection on live units (ASVS V4) |
| T-230-03 | Elevation of Privilege | ssh access path | accept | SSH key auth only (`-o BatchMode=yes`), already the established contract; no new credentials introduced |
| T-230-04 | Information Disclosure | committed evidence/IPs | mitigate | Health IPs already committed in script/units; do not expand exposure in tests or comments |
| T-230-SC | Tampering | npm/pip/cargo installs | N/A | No external packages installed this phase (RESEARCH Package Legitimacy Audit = N/A); no slopcheck gate |
</threat_model>

<verification>
- `shellcheck -S error scripts/soak-monitor.sh` returns 0 after all edits.
- `.venv/bin/pytest tests/test_soak_monitor_att_coverage.py -q` reports 4 passed.
- `.venv/bin/pytest tests/ -q` stays green (no regression in the existing suite).
- `grep -c 'is_spectrum_cake_trial_active "kevin@10.10.110.223"' scripts/soak-monitor.sh` == 0.
- Read-only contract preserved: no `systemctl start/stop`, no writes, no fault injection in the diff.
</verification>

<success_criteria>
- MON-01: all four call sites route ATT through `external_units_for "att"` (the three live ATT units) when in external mode; `wanctl@att.service` survives only as native-mode fallback.
- MON-02: mode detection is the WAN-parameterized `is_external_cake_mode`; ATT enters external mode at Spectrum parity with no Spectrum-only hardcoding gating it.
- Regression test discriminates the fix (RED before Task 2/3, GREEN after) and guards shellcheck cleanliness.
- No controller `.py` files touched (SAFE-14 holds; proven at the phase boundary in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/230-soak-monitor-att-coverage/230-01-SUMMARY.md` when done.
</output>
