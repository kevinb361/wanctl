# Wave 3: Health + Reload Integration

**Phase**: 267
**Wave**: 3
**Scope**: Health endpoint per-WAN + config reload

## Tasks

1. Health endpoint refactor:
   - `failover` section becomes a dict per WAN
   - Each WAN: enabled, red_count, green_count, last_decision
   - `_build_failover_section` in health.py handles dict format

2. Config reload:
   - On USR1, detect failover config changes
   - Rebuild entire `FailoverBridgeGroup` with new settings
   - Preserve bridge state (don't reset counters on reload)

3. Tests:
   - Integration test: daemon cycle with dual bridges
   - Test config reload preserves bridge state
   - Test health endpoint shows per-WAN status

## Verification

- Health endpoint: `failover.spectrum` and `failover.att` sections
- Config reload: bridges reconfigured without counter reset
- All regression tests pass
