---
phase: 215-spectrum-upload-reclaim-canary
plan: 02
type: execute
wave: 2
depends_on: ["215-01"]
files_modified:
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/
autonomous: true
requirements: [RECLAIM-01, RECLAIM-02]
must_haves:
  truths:
    - "Snapshot A captures the pre-mutation production state (repo + deployed config, state file, /health, config-snapshot DB row) so revert is possible"
    - "A fresh in-session ceiling-18 leg-A baseline is captured via the Phase 213 harness for apples-to-apples comparison against leg-B"
    - "All committed evidence is D-08 redacted (no secrets)"
  artifacts:
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/"
      provides: "Snapshot A artifact set: pre-config copy (ceiling=18), redacted deployed config, state file, /health baseline, version/uptime, config-snapshot DB row showing 18"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/"
      provides: "Leg-A tcp_upload flent + paired /health NDJSON captured at ceiling 18 in the canary A/B session"
  key_links:
    - from: "evidence/leg-a-ceiling18 flent artifact"
      to: "scripts/phase214-extract.py (latency + extract_flent_upload_throughput)"
      via: "offline extraction to confirm leg-A is non-VOID and reproduces the baseline regime"
      pattern: "phase214-extract"
    - from: "Snapshot A config-snapshot DB row"
      to: "/var/lib/wanctl/metrics-spectrum.db upload_ceiling_mbps"
      via: "read-only query proving 18 was the loaded ceiling pre-mutation"
      pattern: "upload_ceiling_mbps"
---

<objective>
Capture Snapshot A (the revert anchor) and a fresh in-session ceiling-18 leg-A baseline BEFORE any production mutation. Per D-06 Snapshot A reuses the Phase 211 pattern; per D-10 a same-session 18-leg is preferred over reusing the day-old RUN-20260527T222043Z reference to control for Phase 214 time-of-day/path sensitivity. This plan performs NO config mutation, NO deploy, NO service restart — read-only production evidence + dev-VM traffic capture only.

Purpose: Snapshot A is the hard dependency that makes the Plan 03 mutation reversible. Leg-A is the in-session comparison baseline the gate scores leg-B against.
Output: evidence/snapshot-a/ artifact set and evidence/leg-a-ceiling18/ flent + health NDJSON, both D-08 redacted and confirmed non-VOID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md

<!-- Harness + extractor reused for leg-A capture (read-only / dev-VM traffic gen). -->
@scripts/phase213-baseline-capture.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Snapshot A (pre-mutation revert anchor, D-06)</name>
  <files>.planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/</files>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 4 "Snapshot A capture (D-06) — exact contents" table + the Phase 211 sequence in Code Examples)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-06, D-08)
    - .planning/STATE.md (lines 141-143 Phase 211 deploy/Snapshot-A facts; bound endpoint 10.10.110.223:9101, loopback not listening)
    - configs/spectrum.yaml (the ceiling=18 pre-mutation repo config to copy)
  </read_first>
  <action>
    Into evidence/snapshot-a/ (timestamped subdir), capture exactly the D-06 Snapshot A set, all read-only: (1) a copy of the repo configs/spectrum.yaml at ceiling=18; (2) the deployed config via `ssh cake-shaper 'sudo -n cat /etc/wanctl/spectrum.yaml'` D-08-redacted (strip ${ROUTER_PASSWORD}/${DISCORD_WEBHOOK_URL} and any secret-shaped values, mirroring the Phase 212/213 redaction approach); (3) the state file via `ssh cake-shaper 'sudo -n cat /var/lib/wanctl/spectrum_state.json'`; (4) the /health baseline `curl -s http://10.10.110.223:9101/health` written to snapshot-a-health.json (use the BOUND IP, never loopback 127.0.0.1); (5) version + uptime extracted from /health.version and /health.uptime_seconds; (6) the latest config-snapshot row's upload_ceiling_mbps from the metrics DB via a read-only query `ssh cake-shaper 'sudo -n sqlite3 -readonly "file:/var/lib/wanctl/metrics-spectrum.db?mode=ro" "SELECT ... ORDER BY ... DESC LIMIT 1"'` confirming it reads 18. Write snapshot-a/MANIFEST.md recording the timestamp, each artifact path, version/uptime, the confirmed loaded ceiling (18), and the exact revert command sequence (git checkout configs/spectrum.yaml then scripts/deploy.sh spectrum cake-shaper then ssh cake-shaper sudo systemctl restart wanctl@spectrum.service then verify bound /health then scripts/canary-check.sh --ssh cake-shaper). Do NOT mutate any config, deploy, or restart in this task.
  </action>
  <verify>
    <automated>ls .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/*/snapshot-a-health.json && ! grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-a-health.json exists and was fetched from the bound endpoint 10.10.110.223:9101 (the source URL/host is recorded in MANIFEST.md, not 127.0.0.1).
    - The captured config-snapshot DB row / deployed config shows upload_ceiling_mbps == 18 (pre-mutation ceiling confirmed loaded), recorded in MANIFEST.md.
    - `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/` returns nothing (D-08 redaction holds).
    - MANIFEST.md contains the full revert command sequence and confirms NO mutation/deploy/restart was performed in this task.
    - No production source/config writes occurred: `git diff --name-only | grep -cE '^(src/wanctl/|configs/)'` returns 0.
  </acceptance_criteria>
  <done>evidence/snapshot-a/ holds the complete D-06 revert anchor (repo + redacted deployed config, state file, /health from the bound endpoint, version/uptime, DB row = ceiling 18) plus a MANIFEST with the exact revert sequence. Nothing was mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Capture leg-A (ceiling-18) in-session baseline + confirm non-VOID</name>
  <files>.planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/</files>
  <read_first>
    - scripts/phase213-baseline-capture.sh (orchestrator: --bind-map, --wans, --tests, --flent-duration, --pre-buf/--post-buf, egress guard, per-test manifest/NDJSON outputs)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 3 "Wiring the A/B run" steps 1-4; recommend --flent-duration >=120; bind egress 70.123.224.169; host dallas; Open Questions 1-3)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-07, D-09, D-10)
    - scripts/phase214-extract.py (extract_flent_latency + extract_flent_upload_throughput from Plan 01, for the non-VOID confirmation)
  </read_first>
  <action>
    Run `scripts/phase213-baseline-capture.sh --bind-map spectrum=<spectrum-dev-bind-ip> --wans spectrum --tests tcp_upload --flent-duration 120 --evidence-root .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18` while the production daemon is still at ceiling 18 (verified in Task 1). This is dev-VM traffic generation + read-only /health polling only — the harness forbids service/config/deploy changes and is egress-guarded to 70.123.224.169. Use the same bind, host (dallas), and --flent-duration that Plan 03 leg-B will use, to honor D-10 apples-to-apples. After capture run `.venv/bin/python3 scripts/phase214-extract.py --flent-gz <leg-a flent> --output-json .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json` and confirm the window is NON-VOID per D-09 (latency extraction succeeds with no FlentExtractionError; the upload_throughput series resolves; sustained signal_outlier_rate / measurement_state from the paired health NDJSON is not collapsed). Record in leg-a-ceiling18/MANIFEST.md: chosen --flent-duration, bind IP, host, time-of-day, the extracted p50/p95/p99/median-upload numbers, and a cross-check note vs the re-extracted RUN-20260527T222043Z reference (p95 53.4 / p99 69.0 / median 11.43). If the window is VOID, re-run per D-09 and document the re-run; never score a VOID leg.
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "import json,glob; p=glob.glob('.planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json'); assert p, 'leg-a-extract.json missing'; d=json.load(open(p[0])); assert d['latency']['p95_ms']>0 and d['latency']['p99_ms']>0, 'VOID/zero latency'; assert d['upload_throughput']['throughput_median_mbps']>0, 'no upload throughput'"</automated>
  </verify>
  <acceptance_criteria>
    - leg-a-extract.json exists with latency p95_ms > 0, p99_ms > 0 and upload_throughput.throughput_median_mbps > 0 (proves a non-VOID, non-zero-filled extraction succeeded).
    - The leg-A flent and paired health-spectrum.ndjson are present under evidence/leg-a-ceiling18/.
    - MANIFEST.md records --flent-duration, bind IP, host=dallas, time-of-day, the extracted numbers, and the cross-check vs the RUN-20260527T222043Z reference.
    - No production source/config writes occurred: `git diff --name-only | grep -cE '^(src/wanctl/|configs/)'` returns 0 (harness is read-only/dev-VM-only).
    - D-08: `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/` returns nothing.
  </acceptance_criteria>
  <done>A fresh ceiling-18 leg-A tcp_upload capture exists with paired /health NDJSON, the offline extractor confirms it is non-VOID with real (non-zero) p95/p99/median-upload numbers, and the MANIFEST cross-checks it against the baseline reference. No production mutation occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production host (cake-shaper) → evidence repo | read-only sudo reads of deployed config / state / DB cross into committed artifacts |
| dev-VM traffic gen → Spectrum WAN | bind IP must egress Spectrum (70.123.224.169) or the capture is not comparable |
| flent/health window → leg-A baseline | a collapsed window must not become the comparison baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-05 | Information Disclosure | secrets in committed Snapshot A / leg-A evidence | mitigate | D-08 redaction strips ${ROUTER_PASSWORD}/${DISCORD_WEBHOOK_URL}/secret-shaped values before commit; acceptance grep asserts no secret tokens in either evidence dir. |
| T-215-06 | Tampering | accidental production mutation during "read-only" capture | mitigate | Only `sudo -n cat` / `sqlite3 -readonly` / `curl` reads; harness self-forbids service/config/deploy changes; acceptance asserts zero src/wanctl + configs git diff. |
| T-215-07 | Repudiation/Spoofing | leg-A baseline silently collapsed (false comparison floor) | mitigate | D-09 non-VOID confirmation via fail-closed extractor (no zero-fill) + signal_outlier_rate/measurement_state screen; VOID legs re-run, never scored. |
| T-215-08 | Tampering | querying loopback /health (not listening) yields empty/stale baseline | mitigate | Snapshot uses the bound endpoint 10.10.110.223:9101 per STATE 211-01; source host recorded in MANIFEST. |
</threat_model>

<verification>
- Snapshot A directory contains the full D-06 set with a MANIFEST and a confirmed loaded ceiling = 18.
- leg-a-extract.json shows real (non-zero) p95/p99 and upload median; the leg is non-VOID.
- `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]'` over evidence/ returns nothing.
- `git diff --name-only | grep -cE '^(src/wanctl/|configs/)'` returns 0 (no mutation in this plan).
</verification>

<success_criteria>
- Snapshot A is a complete, redacted, revertible pre-mutation anchor with the exact revert sequence documented.
- A fresh in-session ceiling-18 leg-A baseline exists, is non-VOID, and is cross-checked against the re-extracted RUN-20260527T222043Z reference.
- No production config/service/code was mutated.
</success_criteria>

<output>
After completion, create `.planning/phases/215-spectrum-upload-reclaim-canary/215-02-SUMMARY.md`
</output>
