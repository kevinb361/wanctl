---
phase: 215-spectrum-upload-reclaim-canary
plan: 02
type: execute
wave: 2
depends_on: ["215-01"]
files_modified:
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/
autonomous: true
requirements: [RECLAIM-01, RECLAIM-02]
must_haves:
  truths:
    - "D-06: Snapshot A captures the pre-mutation production state (repo + redacted deployed config, state file, /health from the bound endpoint, version/uptime, config-snapshot DB row) via the Phase 211 pattern"
    - "D-06 (REVIEW-4): the recorded revert sequence performs a TARGETED restore of ONLY continuous_monitoring.upload.ceiling_mbps back to 18 (not a whole-file git checkout that could discard unrelated worktree edits), then deploy.sh -> restart wanctl@spectrum -> verify bound /health"
    - "The config-snapshot DB row is read with the EXACT verified read-only query json_extract(labels, '$.autorate.upload_ceiling_mbps') on metric_name='wanctl_config_snapshot' AND wan_name='spectrum' (REVIEW-8) proving 18 was the loaded ceiling pre-mutation"
    - "All committed evidence is D-08 redacted (.redacted.yaml / .redacted.json only; ROUTER_PASSWORD / DISCORD_WEBHOOK_URL placeholders stripped so the acceptance grep cannot trip - REVIEW-12)"
    - "REVIEW-5: leg-A capture is NOT performed in this plan; it is captured in Plan 03 immediately before the mutation so leg-A and leg-B stay back-to-back in the same session (D-10) regardless of when human approval lands"
  artifacts:
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/"
      provides: "Snapshot A artifact set: pre-config copy (ceiling=18), redacted deployed config, state file, /health baseline, version/uptime, config-snapshot DB row showing 18, MANIFEST with the targeted revert sequence"
  key_links:
    - from: "Snapshot A config-snapshot DB row"
      to: "/var/lib/wanctl/metrics-spectrum.db metric_name=wanctl_config_snapshot labels.autorate.upload_ceiling_mbps"
      via: "read-only json_extract query proving 18 was the loaded ceiling pre-mutation"
      pattern: "upload_ceiling_mbps"
---

<objective>
Capture Snapshot A (the revert anchor) BEFORE any production mutation. Per D-06 Snapshot A reuses the Phase 211 pattern. This plan performs NO config mutation, NO deploy, NO service restart, NO traffic generation - read-only production evidence only.

This plan was revised from cross-AI review (REVIEW-4/5/8/12 in 215-REVIEWS.md):
- REVIEW-5 (same-session A/B drift): leg-A capture has been REMOVED from this plan. Because the mutation is behind a human checkpoint in Wave 3, capturing leg-A here risks D-10 "same session" being violated if approval lands hours later. Leg-A is now captured in Plan 03 immediately before the mutation, keeping leg-A and leg-B back-to-back. Plan 02 is reduced to the Snapshot A revert anchor only. This is the approach that best preserves apples-to-apples (D-07/D-10): leg-A and leg-B are guaranteed adjacent in time, same bind/host/duration, with no human-checkpoint gap between them.
- REVIEW-4 (rollback too broad): the recorded revert is a TARGETED single-key restore of ceiling_mbps to 18, not `git checkout configs/spectrum.yaml`.
- REVIEW-8 (DB query under-specified): the exact verified json_extract query is pinned.
- REVIEW-12 (evidence hygiene): only `.redacted.yaml`/`.redacted.json` evidence is committed.

Purpose: Snapshot A is the hard dependency that makes the Plan 03 mutation reversible.
Output: evidence/snapshot-a/ artifact set, D-08 redacted, with a MANIFEST recording the exact targeted revert sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md

<verified_facts>
<!-- Verified against the live codebase during review revision (do NOT re-guess). -->
- Config-snapshot DB read (REVIEW-8): table `metrics`, metric_name `wanctl_config_snapshot`, labels JSON `{"trigger":..., "autorate": {"upload_ceiling_mbps": N, ...}, "steering": {...}}` (src/wanctl/storage/config_snapshot.py:39). Exact read-only query:
  `SELECT json_extract(labels, '$.autorate.upload_ceiling_mbps') FROM metrics WHERE metric_name='wanctl_config_snapshot' AND wan_name='spectrum' ORDER BY timestamp DESC LIMIT 1;`
  run via `sqlite3 -readonly "file:/var/lib/wanctl/metrics-spectrum.db?mode=ro"`.
- Targeted revert (REVIEW-4): change ONLY the one YAML key. The deploy precedent is unchanged (scripts/deploy.sh spectrum cake-shaper does NOT restart; an explicit `systemctl restart wanctl@spectrum.service` is mandatory). The bound /health endpoint is 10.10.110.223:9101 (loopback not listening, STATE 211-01).
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Snapshot A (pre-mutation revert anchor, D-06) - redacted, with targeted revert sequence</name>
  <files>.planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/</files>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 4 "Snapshot A capture (D-06) - exact contents" table + the Phase 211 sequence in Code Examples)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-06, D-08)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md (REVIEW-4 targeted revert, REVIEW-8 DB query, REVIEW-12 redaction)
    - .planning/STATE.md (lines 141-143 Phase 211 deploy/Snapshot-A facts; bound endpoint 10.10.110.223:9101, loopback not listening)
    - configs/spectrum.yaml (the ceiling=18 pre-mutation repo config to copy)
  </read_first>
  <action>
    Into evidence/snapshot-a/ (timestamped subdir), capture exactly the D-06 Snapshot A set, all read-only, all committed as `.redacted.*` only (REVIEW-12):
    (1) a redacted copy of the repo configs/spectrum.yaml at ceiling=18, named repo-spectrum.redacted.yaml;
    (2) the deployed config via `ssh cake-shaper 'sudo -n cat /etc/wanctl/spectrum.yaml'` piped through the redaction step, named deployed-spectrum.redacted.yaml. REVIEW-12: the redaction MUST strip both interpolated secret VALUES and the literal placeholder tokens for the router password and discord webhook (replace with the marker REDACTED), so the acceptance grep for those tokens cannot trip; mirror the Phase 212/213 redaction approach;
    (3) the state file via `ssh cake-shaper 'sudo -n cat /var/lib/wanctl/spectrum_state.json'`, named state.redacted.json;
    (4) the /health baseline `curl -s http://10.10.110.223:9101/health`, named snapshot-a-health.redacted.json (use the BOUND IP, never loopback 127.0.0.1);
    (5) version + uptime extracted from /health.version and /health.uptime_seconds, recorded in MANIFEST;
    (6) the loaded ceiling via the EXACT verified read-only query (REVIEW-8): `sudo -n sqlite3 -readonly "file:/var/lib/wanctl/metrics-spectrum.db?mode=ro"` running `SELECT json_extract(labels, '$.autorate.upload_ceiling_mbps') FROM metrics WHERE metric_name='wanctl_config_snapshot' AND wan_name='spectrum' ORDER BY timestamp DESC LIMIT 1;` over SSH to cake-shaper, confirming it reads 18.
    Write snapshot-a/MANIFEST.md recording the timestamp, each artifact path, version/uptime, the confirmed loaded ceiling (18), the exact DB query used, and the TARGETED REVERT SEQUENCE (REVIEW-4): restore ONLY continuous_monitoring.upload.ceiling_mbps back to 18 - either by editing that single YAML key in configs/spectrum.yaml or by copying just that one value from repo-spectrum.redacted.yaml - explicitly NOT `git checkout configs/spectrum.yaml` (which would discard unrelated worktree edits); then `scripts/deploy.sh spectrum cake-shaper`; then `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'`; then verify the bound /health (10.10.110.223:9101) shows the reverted version; then re-run the DB query and confirm it reads 18; then `scripts/canary-check.sh --ssh cake-shaper` exit 0.
    Do NOT mutate any config, deploy, restart, or generate traffic in this task.
  </action>
  <verify>
    <automated>ls .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/*/snapshot-a-health.redacted.json && ! grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-a-health.redacted.json exists and was fetched from the bound endpoint 10.10.110.223:9101 (the source URL/host is recorded in MANIFEST.md, not 127.0.0.1).
    - The config-snapshot DB row read via the exact json_extract query shows upload_ceiling_mbps == 18 (pre-mutation ceiling confirmed loaded), recorded in MANIFEST.md along with the literal query string.
    - All committed evidence files use the `.redacted.yaml` / `.redacted.json` naming and `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/` returns nothing (D-08 + REVIEW-12 redaction holds, placeholder tokens stripped).
    - MANIFEST.md contains the TARGETED revert sequence (single-key ceiling restore to 18) and explicitly states the revert does NOT use `git checkout configs/spectrum.yaml`: `grep -c 'git checkout configs/spectrum.yaml' <MANIFEST>` appears only in the negative/"do not use" context — the actual revert step edits one key (REVIEW-4).
    - MANIFEST.md confirms NO mutation/deploy/restart/traffic was performed in this task.
    - No production source/config writes occurred: `git diff --name-only | grep -cE '^(src/wanctl/|configs/)'` returns 0.
  </acceptance_criteria>
  <done>evidence/snapshot-a/ holds the complete D-06 revert anchor (redacted repo + deployed config, state file, /health from the bound endpoint, version/uptime, DB row = ceiling 18 read via the exact json_extract query) plus a MANIFEST with the TARGETED single-key revert sequence (not a whole-file git checkout). Only redacted evidence is committed. Nothing was mutated and no leg-A traffic was generated (leg-A moves to Plan 03 per REVIEW-5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production host (cake-shaper) -> evidence repo | read-only sudo reads of deployed config / state / DB cross into committed artifacts |
| committed evidence -> git | config/state/health snapshots may carry secret-shaped fields or placeholder tokens |
| recorded revert sequence -> future rollback | an over-broad revert (git checkout) could discard unrelated worktree edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-05 | Information Disclosure | secrets/placeholder tokens in committed Snapshot A evidence | mitigate | D-08 + REVIEW-12: only `.redacted.*` evidence committed; redaction strips interpolated secret values AND the ROUTER_PASSWORD/DISCORD_WEBHOOK_URL placeholder tokens; acceptance grep asserts no secret tokens. |
| T-215-06 | Tampering | accidental production mutation during "read-only" capture | mitigate | Only `sudo -n cat` / `sqlite3 -readonly` / `curl` reads; no deploy/restart/traffic; acceptance asserts zero src/wanctl + configs git diff. |
| T-215-08 | Tampering | querying loopback /health (not listening) yields empty/stale baseline | mitigate | Snapshot uses the bound endpoint 10.10.110.223:9101 per STATE 211-01; source host recorded in MANIFEST. |
| T-215-16 | Tampering/Repudiation | over-broad revert discarding unrelated worktree edits | mitigate | REVIEW-4: MANIFEST records a TARGETED single-key restore of ceiling_mbps to 18, explicitly NOT `git checkout configs/spectrum.yaml`. |
</threat_model>

<verification>
- Snapshot A directory contains the full D-06 set with a MANIFEST and a confirmed loaded ceiling = 18 (read via the exact json_extract query).
- The MANIFEST records the TARGETED single-key revert sequence (not a whole-file git checkout).
- `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]'` over evidence/snapshot-a/ returns nothing; all evidence files use `.redacted.*` naming.
- `git diff --name-only | grep -cE '^(src/wanctl/|configs/)'` returns 0 (no mutation in this plan; no leg-A traffic).
</verification>

<success_criteria>
- Snapshot A is a complete, redacted, revertible pre-mutation anchor with the exact TARGETED revert sequence and the exact verified DB query documented.
- Leg-A is intentionally NOT captured here (REVIEW-5) - it moves to Plan 03 immediately before the mutation to keep leg-A/leg-B back-to-back in the same session (D-10).
- No production config/service/code was mutated; no traffic was generated.
</success_criteria>

<output>
After completion, create `.planning/phases/215-spectrum-upload-reclaim-canary/215-02-SUMMARY.md`
</output>
