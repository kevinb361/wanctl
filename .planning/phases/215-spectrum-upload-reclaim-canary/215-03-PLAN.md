---
phase: 215-spectrum-upload-reclaim-canary
plan: 03
type: execute
wave: 3
depends_on: ["215-01", "215-02"]
files_modified:
  - configs/spectrum.yaml
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json
  - .planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md
autonomous: false
requirements: [RECLAIM-01, RECLAIM-02, RECLAIM-03]
user_setup: []
must_haves:
  truths:
    - "D-01: exactly one knob changes - ceiling_mbps 18 -> 20; setpoint_mbps, floor_mbps, step_up_mbps and all other parameters stay frozen (one-knob discipline)"
    - "D-02: the ceiling (not setpoint) is the lever - Spectrum upload was pegged near ceiling=18 for 81.46% of tcp_upload samples, so the ceiling is the binding constraint and a setpoint raise would be a near-no-op; this justifies the single-knob scope"
    - "D-03: the magnitude is +2 (->20), not +4 (->22) - the smallest controlled probe given step_up_mbps=5; a raise to 22 is deferred to a possible follow-up only if 20 passes"
    - "REVIEW-6: before deploy a pre/post YAML semantic-delta assertion proves the ONLY changed key is continuous_monitoring.upload.ceiling_mbps (18->20), AND configs/spectrum.yaml + src/wanctl/ have a clean / allowlisted worktree (no unrelated drift gets deployed)"
    - "REVIEW-5/D-07/D-10: leg-A (ceiling 18) is captured in THIS plan immediately before the mutation, then leg-B (ceiling 20) right after, back-to-back same session (same bind/host/duration/time-of-day) for apples-to-apples scoring"
    - "After deploy the daemon is restarted and the loaded ceiling=20 is proven via the config-snapshot DB row (json_extract) AND the real CAKE init log line containing 20000kbit (REVIEW-7) - not a nonexistent /health.ceiling field and not the wrong bandwidth_kbit=20000 string"
    - "D-09 + REVIEW-3: leg-B VOID is bounded - re-run at most 2 times in-session; if still VOID the SAFE DEFAULT is rollback to ceiling 18 via Snapshot A (production is never stranded at 20 on an unscorable window)"
    - "REVIEW-2: the gate is invoked with its return code captured (set +e; gate; rc=$?; set -e) and the branch decision is made on the parsed verdict.json value, never letting a nonzero gate exit abort the script before the rollback/report branch runs"
    - "REVIEW-4/D-06: on gate FAIL or bounded-VOID-exhausted or operator-reject the rollback is a TARGETED single-key restore of ceiling_mbps to 18 (not git checkout), then deploy/restart, then canary-check confirms reverted ceiling=18; on PASS the win is kept with evidence"
    - "D-11: scripts/libreqos-cli.mjs runs as non-gating perceived-quality corroboration only - it does not enter the pass/fail verdict"
  artifacts:
    - path: "configs/spectrum.yaml"
      provides: "The single mutated knob ceiling_mbps: 20 (kept only on a WIN; targeted-restored to 18 on rollback)"
      contains: "ceiling_mbps"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/"
      provides: "Leg-A tcp_upload flent + paired /health NDJSON at ceiling 18, captured in-session immediately before the mutation (REVIEW-5)"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/"
      provides: "Leg-B tcp_upload flent + paired /health NDJSON at ceiling 20 + deploy proof (DB row 20 + CAKE 20000kbit log)"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json"
      provides: "Gate verdict (pass|fail|void|abort) with derived bounds, per-gate values and rollback protocol"
      contains: "verdict"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md"
      provides: "Operator decision report: keep-or-rollback with evidence, RECLAIM-01/02/03 closeout"
  key_links:
    - from: "configs/spectrum.yaml ceiling_mbps=20"
      to: "deployed /etc/wanctl/spectrum.yaml + restarted wanctl@spectrum.service"
      via: "deploy.sh spectrum cake-shaper then systemctl restart, proven by config-snapshot DB row=20 (json_extract) + CAKE 20000kbit log"
      pattern: "upload_ceiling_mbps"
    - from: "evidence/leg-a-ceiling18 + leg-b-ceiling20 flent + health NDJSON"
      to: "scripts/phase215-reclaim-gate.sh"
      via: "gate derives bounds from leg-A (--baseline-extract/--baseline-health) and scores leg-B (--candidate-extract/--candidate-health), emits verdict.json"
      pattern: "phase215-reclaim-gate"
---

<objective>
Execute the single production canary mutation and resolve it. Capture leg-A (ceiling 18) in-session, then raise `configs/spectrum.yaml` upload.ceiling_mbps 18 to 20 (one knob, D-01) behind a worktree + semantic-delta guard, deploy via the Phase 211 sequence, restart the daemon (mandatory - deploy.sh does NOT restart), prove the new ceiling loaded, capture leg-B back-to-back (D-10), run the Plan-01 gate with leg-A-derived bounds to emit verdict.json, then keep the WIN or roll back via a targeted Snapshot A restore - both with evidence.

This plan was revised from cross-AI review (REVIEW-2/3/4/5/6/7/8 in 215-REVIEWS.md):
- REVIEW-5: leg-A is now captured HERE immediately before the mutation (moved out of Plan 02) so leg-A and leg-B are guaranteed back-to-back, never split by the human checkpoint.
- REVIEW-6: a pre/post full-YAML semantic-delta assertion + clean/allowlisted worktree guard replaces the incomplete floor/setpoint/step grep.
- REVIEW-7: the CAKE deploy-proof grep is the real `20000kbit` string (the old `bandwidth_kbit=20000` was wrong - verified against src/wanctl/backends/linux_cake.py:411 and netlink_cake.py:503).
- REVIEW-2: the gate is invoked with rc captured under set +e/set -e and the branch decision reads verdict.json - a nonzero gate exit can never short-circuit the rollback branch.
- REVIEW-3: VOID is bounded (re-run at most 2x) then the SAFE DEFAULT is rollback to 18.
- REVIEW-4: rollback is a targeted single-key restore of ceiling_mbps to 18, not git checkout.
- REVIEW-8: the config-snapshot DB read uses the exact verified json_extract query.

Purpose: This is the ONLY production mutation in the phase. It is gated by Snapshot A (Plan 02) as a hard dependency and is autonomous:false - the operator approves the live change at deploy time. Honors "one knob per cycle"; ceiling to 22 and setpoint are explicitly out of scope (Deferred in CONTEXT.md).
Output: leg-A + leg-B evidence, the mutated (or targeted-reverted) config, verdict.json, and 215-REPORT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-02-SUMMARY.md

<verified_facts>
<!-- Verified against the live codebase during review revision (do NOT re-guess). -->
- CAKE init log (REVIEW-7): the real proof string is `20000kbit`. Subprocess backend logs `Initialized CAKE on <iface>: bandwidth 20000kbit ...` (src/wanctl/backends/linux_cake.py:411); netlink backend logs `Netlink: initialized CAKE on <iface>: {...'bandwidth': '20000kbit'...}` (src/wanctl/backends/netlink_cake.py:503). The grep is `20000kbit` (matches both). The previously-planned `bandwidth_kbit=20000` does NOT appear anywhere in the code and would silently never match.
- Config-snapshot DB read (REVIEW-8): `SELECT json_extract(labels, '$.autorate.upload_ceiling_mbps') FROM metrics WHERE metric_name='wanctl_config_snapshot' AND wan_name='spectrum' ORDER BY timestamp DESC LIMIT 1;` via `sqlite3 -readonly "file:/var/lib/wanctl/metrics-spectrum.db?mode=ro"` (src/wanctl/storage/config_snapshot.py:39).
- deploy.sh does NOT restart the daemon; an explicit `systemctl restart wanctl@spectrum.service` is mandatory (Pitfall 3). Bound /health endpoint: 10.10.110.223:9101 (loopback not listening, STATE 211-01).
</verified_facts>

<!-- Deploy + post-restart health gate (Phase 211 proven sequence). -->
@scripts/deploy.sh
@scripts/canary-check.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator approval to mutate ceiling and deploy</name>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/ (confirm the Plan-02 Snapshot A revert anchor exists and shows loaded ceiling=18)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-01, D-02, D-03, D-06; operator approves final mutation at deploy time)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md (REVIEW-5: leg-A is captured in this plan right after approval, immediately before the mutation)
  </read_first>
  <what-built>
    Plan 01 landed the upload-throughput extractor + leg-A-derived gate (green offline, pinned exit codes). Plan 02 captured Snapshot A (revert anchor, loaded ceiling=18, targeted revert sequence). The remaining production actions in this plan are: capture leg-A in-session, the single-knob mutation ceiling 18 to 20 + deploy + restart, capture leg-B, gate, then keep-or-rollback. Per D-02 the ceiling is the binding constraint (upload pegged near 18 for 81.46% of samples), per D-03 the magnitude is the smallest controlled +2 probe (not +4). Per REVIEW-5 leg-A is captured immediately AFTER this approval so leg-A and leg-B stay back-to-back regardless of approval timing.
  </what-built>
  <how-to-verify>
    1. Confirm Snapshot A exists and its MANIFEST shows loaded ceiling=18 plus the full TARGETED revert sequence: review .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/*/MANIFEST.md.
    2. Confirm this is a single-knob change (only ceiling_mbps; setpoint=12/floor=8/step_up=5 frozen) and that the predeploy gate + semantic-delta guard will pass it.
    3. Confirm the worktree for configs/spectrum.yaml and src/wanctl/ is clean or only carries the intended canary changes (REVIEW-6).
    4. Approve the live production mutation + deploy + daemon restart on the Spectrum WAN. (Leg-A traffic capture and the mutation will run back-to-back after approval.)
  </how-to-verify>
  <acceptance_criteria>
    - Operator explicitly approves the live mutation, OR aborts the phase (valid RECLAIM-02 "decide not to tune" outcome - record the abort in 215-REPORT.md).
    - Snapshot A presence + loaded-ceiling=18 confirmed before proceeding.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with leg-A capture + ceiling raise + deploy, or "abort" to record an evidenced no-tune decision.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Capture leg-A in-session, guard the worktree, mutate one knob, deploy, restart, prove ceiling=20</name>
  <files>configs/spectrum.yaml, .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/</files>
  <read_first>
    - scripts/phase213-baseline-capture.sh (orchestrator: --bind-map, --wans, --tests, --flent-duration, --pre-buf/--post-buf, egress guard, per-test manifest/NDJSON outputs; egress-guarded to 70.123.224.169, host dallas)
    - scripts/phase214-extract.py (extract_flent_latency + extract_flent_upload_throughput from Plan 01, for leg-A non-VOID confirmation)
    - configs/spectrum.yaml (line ~77 ceiling_mbps: 18 - the ONLY line that changes; setpoint_mbps:12, floor_mbps:8, step_up_mbps:5 stay frozen)
    - scripts/deploy.sh (the spectrum cake-shaper sequence: runs phase201-predeploy-gate automatically, pushes config, does NOT restart the daemon)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 1 runtime-verification table + Question 4 deploy sequence; Pitfall 3 mandatory restart, Pitfall 4 bound endpoint)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-01, D-02, D-03, D-06, D-07, D-09, D-10)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md (REVIEW-5 leg-A here, REVIEW-6 semantic-delta + worktree guard, REVIEW-7 CAKE log string, REVIEW-8 DB query)
  </read_first>
  <action>
    STEP A - leg-A (ceiling 18) in-session capture (REVIEW-5, D-07/D-10): while production is still at ceiling 18 (Snapshot A confirmed it), run `scripts/phase213-baseline-capture.sh --bind-map spectrum=<spectrum-dev-bind-ip> --wans spectrum --tests tcp_upload --flent-duration 120 --evidence-root .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18` (dev-VM traffic gen + read-only /health polling only; egress-guarded to 70.123.224.169; host dallas). Then `.venv/bin/python3 scripts/phase214-extract.py --flent-gz <leg-a flent> --output-json .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json` and confirm leg-A is NON-VOID per D-09 (latency extracts with no FlentExtractionError; upload_throughput resolves; signal_outlier_rate / measurement_state from the paired health NDJSON is not collapsed). If VOID, re-run leg-A (bounded, at most 2x) before proceeding; never use a VOID leg as the baseline. Record the leg-A bind/host/duration/time-of-day in evidence/leg-a-ceiling18/MANIFEST.md - leg-B in Task 3 MUST reuse the identical args.

    STEP B - worktree + semantic-delta guard (REVIEW-6): BEFORE editing, capture a pre-image of configs/spectrum.yaml. Assert the worktree for src/wanctl/ is clean (`git diff --name-only -- src/wanctl/` empty) and that configs/spectrum.yaml carries no unrelated staged/unstaged drift beyond what this canary intends (allowlist: only the ceiling line may differ post-edit). Abort the task if src/wanctl/ has uncommitted changes.

    STEP C - single-knob mutation (D-01, D-03): edit configs/spectrum.yaml changing exactly upload.ceiling_mbps from 18 to 20 (setpoint_mbps 12, floor_mbps 8, step_up_mbps 5 and every other key unchanged). Then assert the FULL pre/post YAML semantic delta is exactly one key: parse both with python yaml/`.venv/bin/python3` and assert the only differing leaf path is `continuous_monitoring.upload.ceiling_mbps` with value 18 -> 20 (REVIEW-6 - proves no other key, anywhere in the file, changed). If more than that one leaf differs, abort and restore the pre-image.

    STEP D - deploy + mandatory restart: run `scripts/deploy.sh spectrum cake-shaper` (the Phase 201 predeploy gate auto-runs and passes - the ceiling-20 change touches none of the blocked keys). Then run the MANDATORY restart `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'` (deploy.sh does NOT restart; without it the 20Hz daemon keeps the old ceiling - Pitfall 3).

    STEP E - prove ceiling=20 loaded (authoritative real proofs, NOT a /health.ceiling field which does not exist): (a) run the exact verified DB query (REVIEW-8) `ssh cake-shaper 'sudo -n sqlite3 -readonly "file:/var/lib/wanctl/metrics-spectrum.db?mode=ro" "SELECT json_extract(labels, '"'"'$.autorate.upload_ceiling_mbps'"'"') FROM metrics WHERE metric_name='"'"'wanctl_config_snapshot'"'"' AND wan_name='"'"'spectrum'"'"' ORDER BY timestamp DESC LIMIT 1;"'` and confirm it reads 20; (b) confirm the real CAKE init log line containing `20000kbit` (REVIEW-7) in `ssh cake-shaper 'journalctl -u wanctl@spectrum --since <restart-ts>'` or /var/log/wanctl/spectrum.log - grep for `20000kbit`, NOT the wrong `bandwidth_kbit=20000`. Then `curl -s http://10.10.110.223:9101/health` (BOUND IP) confirming status healthy + new version, and run `scripts/canary-check.sh --ssh cake-shaper` (exit 0). Save the post-deploy /health + DB-row + CAKE-log proof under evidence/leg-b-ceiling20/deploy-proof/ as `.redacted.*` (REVIEW-12).
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "import sys,yaml; pre=yaml.safe_load(open('/tmp/spectrum.pre.yaml')) if __import__('os').path.exists('/tmp/spectrum.pre.yaml') else None; post=yaml.safe_load(open('configs/spectrum.yaml')); cm=post['continuous_monitoring']['upload']; assert cm['ceiling_mbps']==20, cm; assert cm['setpoint_mbps']==12 and cm['floor_mbps']==8 and cm['step_up_mbps']==5, cm; print('single-knob ok')" && .venv/bin/python3 -c "import json,glob; p=glob.glob('.planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json'); assert p,'leg-a-extract.json missing'; d=json.load(open(p[0])); assert d['latency']['p95_ms']>0 and d['latency']['p99_ms']>0 and d['upload_throughput']['throughput_median_mbps']>0, 'leg-A VOID/zero'"</automated>
  </verify>
  <acceptance_criteria>
    - leg-a-extract.json exists with latency p95_ms > 0, p99_ms > 0 and upload_throughput.throughput_median_mbps > 0 (leg-A non-VOID, captured in-session before the mutation - REVIEW-5).
    - The pre/post full-YAML semantic delta is exactly one leaf: `continuous_monitoring.upload.ceiling_mbps` 18 -> 20, and setpoint_mbps=12/floor_mbps=8/step_up_mbps=5 are unchanged (REVIEW-6). The verify command asserts this via parsed YAML, not a line grep.
    - The src/wanctl/ worktree was clean before deploy: `git diff --name-only -- src/wanctl/` returns nothing (REVIEW-6 worktree guard).
    - The config-snapshot DB row upload_ceiling_mbps reads 20 after restart, read via the exact json_extract query and recorded under evidence/leg-b-ceiling20/deploy-proof/.
    - The real CAKE init log line containing `20000kbit` is present in the proof (REVIEW-7): `grep -c '20000kbit' <saved-cake-log-proof>` >= 1; the wrong string is absent: `grep -c 'bandwidth_kbit=20000' <saved-cake-log-proof>` == 0.
    - `scripts/canary-check.sh --ssh cake-shaper` exits 0 (post-restart health gate via the bound endpoint).
    - deploy-proof + leg-A artifacts are D-08 redacted: `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/ .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/deploy-proof/` returns nothing.
  </acceptance_criteria>
  <done>Leg-A (ceiling 18) was captured non-VOID in-session immediately before the mutation; the worktree + full-YAML semantic-delta guard proved exactly one key changed; configs/spectrum.yaml has ceiling 20; deploy + mandatory restart ran; and the loaded ceiling=20 is proven by the json_extract DB row + the real `20000kbit` CAKE init log + healthy bound /health + canary-check exit 0. Proofs saved and redacted.</done>
</task>

<task type="auto">
  <name>Task 3: Capture leg-B, run the gate (rc captured), bounded-VOID, keep-or-targeted-rollback, author the report</name>
  <files>.planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/, .planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json, .planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md, configs/spectrum.yaml</files>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json + leg-A MANIFEST (the in-session leg-A baseline + the exact harness args to reuse)
    - scripts/phase215-reclaim-gate.sh (the Plan-01 gate: --baseline-extract/--baseline-health = leg-A, --candidate-extract/--candidate-health = leg-B; EXIT_PASS=0/FAIL=1/ABORT=2, VOID->2; derives p95/p99 +10% and throughput +1.5 bounds from leg-A; env-preflight expects deployed ceiling=20)
    - scripts/phase213-baseline-capture.sh (same harness/args as leg-A for apples-to-apples leg-B)
    - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/*/MANIFEST.md (the TARGETED revert sequence to execute on rollback)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-04, D-05, D-06, D-07, D-09, D-10, D-11)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-REVIEWS.md (REVIEW-2 rc capture, REVIEW-3 bounded VOID, REVIEW-4 targeted rollback)
  </read_first>
  <action>
    STEP A - leg-B capture (D-07 primary instrument; D-10 same-session apples-to-apples): run `scripts/phase213-baseline-capture.sh` with the IDENTICAL args recorded in the leg-A MANIFEST (same --bind-map spectrum=<bind-ip>, --wans spectrum, --tests tcp_upload, --flent-duration 120, host dallas) but now at ceiling 20, `--evidence-root .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20`, back-to-back with leg-A.

    STEP B - gate with rc captured (REVIEW-2): invoke the gate passing leg-A as baseline and leg-B as candidate (REVIEW-1):
      `set +e`
      `scripts/phase215-reclaim-gate.sh --baseline-extract <leg-a-extract.json> --baseline-health <leg-a health NDJSON> --candidate-extract <leg-b-extract.json> --candidate-health <leg-b health NDJSON> --expected-ceiling 20 --output evidence/verdict.json`
      `rc=$?`
      `set -e`
    Then read the verdict from evidence/verdict.json (NOT from rc alone) - rc is a cross-check, the parsed `verdict` field is authoritative. A nonzero rc must NOT abort the script: the branch logic below always runs.

    STEP C - bounded VOID handling (REVIEW-3 + D-09): if verdict == "void", re-capture leg-B and re-run the gate at most 2 times (total 3 attempts) within the session. If still "void" after the bounded retries, the SAFE DEFAULT is rollback to ceiling 18 (do not strand production at 20 on an unscorable window) - proceed to STEP D rollback and record the bounded-VOID-exhausted reason.

    STEP D - branch on the (final) verdict:
      - verdict == "pass" (WIN): keep ceiling=20 in configs/spectrum.yaml; record the kept win.
      - verdict == "fail" OR bounded-VOID-exhausted OR operator-rejects: execute the Snapshot A TARGETED revert (REVIEW-4 / D-06, per the Snapshot A MANIFEST): restore ONLY continuous_monitoring.upload.ceiling_mbps back to 18 (edit the single YAML key - NOT `git checkout configs/spectrum.yaml`), then `scripts/deploy.sh spectrum cake-shaper`, then `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'`, then verify the bound /health shows the reverted version, then re-run the exact json_extract DB query and confirm upload_ceiling_mbps == 18, then `scripts/canary-check.sh --ssh cake-shaper` exit 0.

    STEP E - optionally run `scripts/libreqos-cli.mjs` as NON-gating corroboration only (D-11 - does not enter pass/fail).

    STEP F - author 215-REPORT.md: the RECLAIM-01 evaluation (operating points vs evidence), the RECLAIM-02 single-knob + Snapshot-A + gate discipline narrative (including the worktree/semantic-delta guard and targeted-revert mechanics), the RECLAIM-03 outcome (WIN with throughput delta vs the leg-A baseline + no D-05 regression, OR clean targeted rollback with evidence), the leg-A vs leg-B numbers, the derived bounds used, the verdict + bounded-VOID history if any, and the libreqos non-gating note (D-11). All committed evidence stays D-08 redacted.
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "import json; v=json.load(open('.planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json')); assert v['verdict'] in ('pass','fail','void','abort'), v; assert 'derived_p95_bound' in v and 'derived_win_bound' in v, 'gate did not record leg-A-derived bounds'; print('verdict', v['verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - evidence/verdict.json exists with verdict in {pass, fail, void, abort}, the leg-A-DERIVED bounds (derived_p95_bound, derived_p99_bound, derived_win_bound) and per-gate values (candidate_p95_ms, candidate_p99_ms, candidate_throughput_median_mbps, floor_hit_delta, alert_fire_delta).
    - The gate was invoked with rc captured (set +e/set -e) and the branch read verdict.json; a nonzero gate exit did not prevent the rollback/report branch from running (REVIEW-2). The 215-REPORT.md narrative records the captured rc alongside the verdict.
    - Leg-A and leg-B used identical harness args (recorded in both MANIFESTs) and were captured back-to-back (D-10); leg-B confirmed non-VOID before scoring (or bounded-VOID-exhausted -> rollback, with the retry count recorded - REVIEW-3).
    - If verdict != pass (or bounded-VOID-exhausted): configs/spectrum.yaml is back to ceiling_mbps: 18 via a TARGETED single-key restore (`grep -E 'ceiling_mbps: 18' configs/spectrum.yaml` matches), the json_extract DB row reads 18, and `scripts/canary-check.sh --ssh cake-shaper` exits 0 (clean targeted rollback proven - REVIEW-4). The rollback did NOT use `git checkout configs/spectrum.yaml`.
    - If verdict == pass: configs/spectrum.yaml retains ceiling_mbps: 20 and 215-REPORT.md records the throughput WIN (leg-B median >= leg-A median + 1.5) with no D-05 regression.
    - 215-REPORT.md closes RECLAIM-01/02/03 with the leg-A vs leg-B numbers, the derived bounds, the verdict, and the bounded-VOID history; libreqos is documented as non-gating (D-11).
    - D-08: `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/` returns nothing.
  </acceptance_criteria>
  <done>Leg-B captured non-VOID (or bounded-VOID-exhausted), the gate ran with rc captured and emitted a verdict.json with leg-A-derived bounds, the canary was resolved (WIN kept OR safe targeted rollback to ceiling 18 via Snapshot A with canary-check exit 0), and 215-REPORT.md closes all three RECLAIM requirements with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config -> deployed /etc/wanctl/spectrum.yaml | the single-knob mutation crosses into the live 24/7 controller |
| deploy.sh push -> running daemon | config is read only at process start; un-restarted daemon serves stale config |
| dirty worktree -> deploy | unrelated src/wanctl or config edits could ride along on the canary deploy |
| leg-B measurement -> gate verdict | a regressing or collapsed window must not be scored a false pass |
| gate exit code -> rollback branch | a nonzero exit under set -e could skip rollback |
| evidence artifacts -> git | deploy proof / health snapshots may carry secret-shaped fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-09 | Tampering | wrong value / multi-knob mutation reaching production | mitigate | REVIEW-6: pre/post FULL-YAML semantic-delta assertion proves the only changed leaf is upload.ceiling_mbps 18->20; phase201-predeploy-gate auto-runs in deploy.sh and fail-closes on blocked keys. |
| T-215-10 | Tampering/Repudiation | un-restarted daemon serving stale ceiling=18 while leg-B "measures" 20 | mitigate | Mandatory systemctl restart + load proof via json_extract DB row=20 + real `20000kbit` CAKE init log (REVIEW-7, Pitfall 3); leg-B not captured until ceiling=20 is proven loaded. |
| T-215-11 | Spoofing | false "pass" keeping a regressing change | mitigate | gate env-preflight ABORTs on deployed-ceiling mismatch; D-05 latency-first FAIL gates derived from leg-A; D-09 numeric VOID; verdict != pass forces the targeted Snapshot A revert. |
| T-215-12 | Information Disclosure | secrets in deploy-proof / leg-A / leg-B / verdict evidence | mitigate | D-08 redaction on all committed evidence (`.redacted.*`); acceptance grep asserts no ROUTER_PASSWORD/DISCORD_WEBHOOK/password tokens across evidence/. |
| T-215-13 | Denial of Service | mutation degrades the live WAN with no clean recovery | mitigate | Snapshot A (Plan 02) is a hard dependency; TARGETED revert restores ceiling=18 + restart + canary-check exit 0 proof; checkpoint requires operator approval; bounded-VOID safe-default is rollback (REVIEW-3) so production is never stranded at 20 on an unscorable window. |
| T-215-14 | Elevation of Privilege | new network-facing surface | accept | No new endpoint added; only an existing config value changes and existing read-only /health is polled. No attack surface added. |
| T-215-17 | Tampering | unrelated worktree drift deployed alongside the canary | mitigate | REVIEW-6: src/wanctl/ worktree asserted clean and configs/spectrum.yaml allowlisted to the single ceiling change before deploy; abort if dirty. |
| T-215-18 | Repudiation | nonzero gate exit short-circuiting rollback under set -e | mitigate | REVIEW-2: gate invoked with `set +e; gate; rc=$?; set -e`; branch reads verdict.json; rollback/report branch always runs. |
</threat_model>

<verification>
- The pre/post full-YAML semantic delta is exactly one leaf (upload.ceiling_mbps 18->20); src/wanctl/ worktree clean before deploy (REVIEW-6).
- Loaded ceiling=20 proven via json_extract DB row + real `20000kbit` CAKE log (REVIEW-7/8); the wrong `bandwidth_kbit=20000` string is absent from the proof.
- evidence/verdict.json has a valid verdict, leg-A-derived bounds, and per-gate values; the gate was run with rc captured (REVIEW-2).
- On non-pass / bounded-VOID-exhausted: config back to ceiling 18 via TARGETED single-key restore (not git checkout), DB row=18, canary-check exit 0 (REVIEW-3/4).
- On pass: config retains ceiling 20 with documented WIN (leg-B median >= leg-A median + 1.5) and no D-05 regression.
- `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]'` over evidence/ returns nothing.
- No `src/wanctl/` modified: `git diff --name-only | grep -c '^src/wanctl/'` returns 0.
</verification>

<success_criteria>
- Leg-A captured non-VOID in-session immediately before the mutation; leg-A and leg-B back-to-back with identical harness args (D-07/D-10, REVIEW-5).
- Exactly one knob mutated (ceiling 18 to 20) behind a full-YAML semantic-delta + clean-worktree guard (REVIEW-6), deployed, restarted, and proven loaded via json_extract DB row + real `20000kbit` CAKE log (REVIEW-7/8).
- Gate run with rc captured and leg-A-derived bounds; bounded-VOID safe-default rollback (REVIEW-2/3).
- Canary resolved: WIN kept with evidence OR cleanly rolled back via a TARGETED Snapshot A restore (canary-check exit 0, ceiling=18, no git checkout - REVIEW-4).
- 215-REPORT.md closes RECLAIM-01/02/03 with leg-A vs leg-B numbers, the derived bounds, the verdict, and the non-gating libreqos note.
</success_criteria>

<output>
After completion, create `.planning/phases/215-spectrum-upload-reclaim-canary/215-03-SUMMARY.md`
</output>
