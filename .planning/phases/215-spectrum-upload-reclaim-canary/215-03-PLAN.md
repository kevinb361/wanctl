---
phase: 215-spectrum-upload-reclaim-canary
plan: 03
type: execute
wave: 3
depends_on: ["215-01", "215-02"]
files_modified:
  - configs/spectrum.yaml
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/
  - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json
  - .planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md
autonomous: false
requirements: [RECLAIM-01, RECLAIM-02, RECLAIM-03]
user_setup: []
must_haves:
  truths:
    - "Exactly one knob changes: ceiling_mbps 18 to 20; setpoint/floor/step_up frozen"
    - "After deploy the daemon is restarted and the loaded ceiling=20 is proven via the config-snapshot DB row / CAKE init log (not a nonexistent /health.ceiling field)"
    - "Leg-B (ceiling 20) is captured non-VOID and the gate emits verdict.json scoring D-04/D-05"
    - "On gate FAIL/VOID the operator rolls back via Snapshot A and canary-check confirms reverted ceiling=18; on PASS the win is kept with evidence"
  artifacts:
    - path: "configs/spectrum.yaml"
      provides: "The single mutated knob ceiling_mbps: 20 (kept only on a WIN; reverted to 18 on rollback)"
      contains: "ceiling_mbps"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/"
      provides: "Leg-B tcp_upload flent + paired /health NDJSON at ceiling 20 + deploy proof"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json"
      provides: "Gate verdict (pass|fail|void) with per-gate values and rollback protocol"
      contains: "verdict"
    - path: ".planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md"
      provides: "Operator decision report: keep-or-rollback with evidence, RECLAIM-01/02/03 closeout"
  key_links:
    - from: "configs/spectrum.yaml ceiling_mbps=20"
      to: "deployed /etc/wanctl/spectrum.yaml + restarted wanctl@spectrum.service"
      via: "deploy.sh spectrum cake-shaper then systemctl restart, proven by config-snapshot DB row=20"
      pattern: "upload_ceiling_mbps"
    - from: "evidence/leg-b-ceiling20 flent + health NDJSON"
      to: "scripts/phase215-reclaim-gate.sh"
      via: "gate scores p95/p99/floor-hit/alert/throughput, emits verdict.json"
      pattern: "phase215-reclaim-gate"
---

<objective>
Execute the single production canary mutation and resolve it. Raise `configs/spectrum.yaml` upload.ceiling_mbps 18 to 20 (one knob, D-01), deploy via the Phase 211 sequence, restart the daemon (mandatory — deploy.sh does NOT restart), prove the new ceiling loaded, capture leg-B in the same A/B session (D-10), run the Plan-01 gate to emit verdict.json, then keep the WIN or roll back via Snapshot A — both with evidence.

Purpose: This is the ONLY production mutation in the phase. It is gated by Snapshot A (Plan 02) as a hard dependency and is autonomous:false — the operator approves the live change at deploy time. Honors "one knob per cycle"; ceiling to 22 and setpoint are explicitly out of scope (Deferred in CONTEXT.md).
Output: the mutated (or reverted) config, leg-B evidence, verdict.json, and 215-REPORT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md
@.planning/phases/215-spectrum-upload-reclaim-canary/215-02-SUMMARY.md

<!-- Deploy + post-restart health gate (Phase 211 proven sequence). -->
@scripts/deploy.sh
@scripts/canary-check.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator approval to mutate ceiling and deploy</name>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/ (confirm the Plan-02 Snapshot A revert anchor exists and shows loaded ceiling=18)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-01, D-03, D-06; operator approves final mutation at deploy time)
  </read_first>
  <what-built>
    Plan 01 landed the upload-throughput extractor + gate (green offline). Plan 02 captured Snapshot A (revert anchor, loaded ceiling=18) and a non-VOID leg-A baseline. The only remaining production action is the single-knob mutation ceiling 18 to 20 + deploy + restart.
  </what-built>
  <how-to-verify>
    1. Confirm Snapshot A exists and its MANIFEST shows loaded ceiling=18 plus the full revert sequence: review .planning/phases/215-spectrum-upload-reclaim-canary/evidence/snapshot-a/*/MANIFEST.md.
    2. Confirm leg-A is non-VOID: review evidence/leg-a-ceiling18/leg-a-extract.json (real p95/p99/median-upload).
    3. Confirm this is a single-knob change (only ceiling_mbps; setpoint=12/floor=8/step_up=5 frozen) and that the predeploy gate will pass it.
    4. Approve the live production mutation + deploy + daemon restart on the Spectrum WAN.
  </how-to-verify>
  <acceptance_criteria>
    - Operator explicitly approves the live mutation, OR aborts the phase (valid RECLAIM-02 "decide not to tune" outcome — record the abort in 215-REPORT.md).
    - Snapshot A presence + loaded-ceiling=18 confirmed before proceeding.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the ceiling raise + deploy, or "abort" to record an evidenced no-tune decision.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Mutate one knob, deploy, restart, prove ceiling=20 loaded</name>
  <files>configs/spectrum.yaml</files>
  <read_first>
    - configs/spectrum.yaml (line 77 ceiling_mbps: 18 — the ONLY line that changes; setpoint_mbps:12, floor_mbps:8, step_up_mbps:5 stay frozen)
    - scripts/deploy.sh (the spectrum cake-shaper sequence: runs phase201-predeploy-gate automatically, pushes config, does NOT restart the daemon)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 1 runtime-verification table + Question 4 deploy sequence; Pitfall 3 mandatory restart, Pitfall 4 bound endpoint)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-01, D-06)
  </read_first>
  <action>
    Edit configs/spectrum.yaml: change exactly the upload.ceiling_mbps value from 18 to 20 (leave setpoint_mbps 12, floor_mbps 8, step_up_mbps 5 and every other key unchanged — single knob per D-01). Run `scripts/deploy.sh spectrum cake-shaper` (the Phase 201 predeploy gate auto-runs and passes — the ceiling-20 change touches none of the blocked keys). Then run the MANDATORY restart `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'` (deploy.sh does NOT restart; without it the 20Hz daemon keeps the old ceiling — Pitfall 3). Prove the new ceiling loaded using the authoritative real proofs (NOT a /health.ceiling field — it does not exist): (a) query the latest config-snapshot row upload_ceiling_mbps from /var/lib/wanctl/metrics-spectrum.db via `ssh cake-shaper 'sudo -n sqlite3 -readonly ...'` and confirm it reads 20; (b) confirm the CAKE init log line bandwidth_kbit=20000 in journalctl -u wanctl@spectrum (or /var/log/wanctl/spectrum.log). Then `curl -s http://10.10.110.223:9101/health` (BOUND IP) confirming status healthy + new version, and run `scripts/canary-check.sh --ssh cake-shaper` (exit 0). Save the post-deploy /health + DB-row + CAKE-log proof under evidence/leg-b-ceiling20/deploy-proof/ (D-08 redacted).
  </action>
  <verify>
    <automated>git diff configs/spectrum.yaml | grep -E '^[+-][[:space:]]' | grep -v 'ceiling_mbps' | grep -cE '(setpoint_mbps|floor_mbps|step_up_mbps)' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `git diff configs/spectrum.yaml` shows ONLY the ceiling_mbps line changed 18 to 20; the above grep confirms no setpoint/floor/step_up line was touched (single-knob, D-01).
    - The config-snapshot DB row upload_ceiling_mbps reads 20 after restart (recorded under evidence/leg-b-ceiling20/deploy-proof/).
    - The CAKE init log line shows bandwidth_kbit=20000 (proof saved).
    - `scripts/canary-check.sh --ssh cake-shaper` exits 0 (post-restart health gate via the bound endpoint).
    - deploy-proof artifacts are D-08 redacted: `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/deploy-proof/` returns nothing.
  </acceptance_criteria>
  <done>configs/spectrum.yaml has exactly one changed knob (ceiling 20), the deploy + mandatory restart ran, and the loaded ceiling=20 is proven by the config-snapshot DB row + CAKE init log + healthy bound /health + canary-check exit 0. Proof artifacts saved and redacted.</done>
</task>

<task type="auto">
  <name>Task 3: Capture leg-B, run the gate, keep-or-rollback, author the report</name>
  <files>.planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20/, .planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json, .planning/phases/215-spectrum-upload-reclaim-canary/215-REPORT.md</files>
  <read_first>
    - .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-a-ceiling18/leg-a-extract.json (leg-A baseline numbers to score leg-B against)
    - scripts/phase215-reclaim-gate.sh (the Plan-01 gate: env-preflight expects deployed ceiling=20, scores p95>58.7/p99>75.9/floor-hit>0/alert>3/bloat>75, WIN >=12.9, D-09 VOID)
    - scripts/phase213-baseline-capture.sh (same harness/args as leg-A for apples-to-apples leg-B)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-CONTEXT.md (D-04, D-05, D-06, D-09, D-10, D-11)
    - .planning/phases/215-spectrum-upload-reclaim-canary/215-RESEARCH.md (Question 4 exact revert path + Question 5 gate semantics)
  </read_first>
  <action>
    Capture leg-B with the SAME harness invocation as leg-A but now at ceiling 20: `scripts/phase213-baseline-capture.sh --bind-map spectrum=<same-bind-ip> --wans spectrum --tests tcp_upload --flent-duration 120 --evidence-root .planning/phases/215-spectrum-upload-reclaim-canary/evidence/leg-b-ceiling20` (same bind, host dallas, duration, time-of-day window per D-10, back-to-back with leg-A). Run `scripts/phase215-reclaim-gate.sh` against the leg-B flent + paired /health NDJSON, emitting verdict.json into evidence/verdict.json; the gate aborts if the deployed ceiling is not 20 (stale-config guard) and emits verdict=void if the leg-B window is collapsed per D-09 (re-run a VOID leg, never score it). Optionally run `scripts/libreqos-cli.mjs` as NON-gating corroboration only (D-11 — does not enter pass/fail). Branch on the verdict: (WIN/pass) keep ceiling=20 in configs/spectrum.yaml and record the kept win; (FAIL or VOID-after-reruns or operator-rejects) execute the Snapshot A revert sequence exactly — git checkout configs/spectrum.yaml (back to 18) then scripts/deploy.sh spectrum cake-shaper then ssh cake-shaper sudo systemctl restart wanctl@spectrum.service then verify the bound /health shows reverted version + config-snapshot row upload_ceiling_mbps==18 then scripts/canary-check.sh --ssh cake-shaper exit 0. Author 215-REPORT.md: the RECLAIM-01 evaluation (operating points vs evidence), the RECLAIM-02 single-knob + Snapshot-A + gate discipline narrative, the RECLAIM-03 outcome (WIN with throughput delta + no regression, OR clean rollback with evidence), the leg-A vs leg-B numbers, the verdict, and the libreqos non-gating note. All committed evidence stays D-08 redacted.
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "import json; v=json.load(open('.planning/phases/215-spectrum-upload-reclaim-canary/evidence/verdict.json')); assert v['verdict'] in ('pass','fail','void','abort'), v; print('verdict', v['verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - evidence/verdict.json exists with verdict in {pass, fail, void, abort} and per-gate values (p95_ms, p99_ms, throughput_median_mbps, floor_hit_delta, alert_fire_delta).
    - Leg-B flent + paired health NDJSON present under evidence/leg-b-ceiling20/ and confirmed non-VOID before scoring.
    - If verdict != pass: configs/spectrum.yaml is back to ceiling_mbps: 18 (`grep -E 'ceiling_mbps: 18' configs/spectrum.yaml` matches), the config-snapshot row reads 18, and `scripts/canary-check.sh --ssh cake-shaper` exits 0 (clean rollback proven).
    - If verdict == pass: configs/spectrum.yaml retains ceiling_mbps: 20 and 215-REPORT.md records the throughput WIN (leg-B median >= 12.9) with no D-05 regression.
    - 215-REPORT.md closes RECLAIM-01/02/03 with the leg-A vs leg-B numbers and the verdict; libreqos is documented as non-gating (D-11).
    - D-08: `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]' .planning/phases/215-spectrum-upload-reclaim-canary/evidence/` returns nothing.
  </acceptance_criteria>
  <done>Leg-B captured non-VOID, gate emitted a verdict.json, the canary was resolved (WIN kept or cleanly rolled back via Snapshot A with canary-check exit 0), and 215-REPORT.md closes all three RECLAIM requirements with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → deployed /etc/wanctl/spectrum.yaml | the single-knob mutation crosses into the live 24/7 controller |
| deploy.sh push → running daemon | config is read only at process start; un-restarted daemon serves stale config |
| leg-B measurement → gate verdict | a regressing or collapsed window must not be scored a false pass |
| evidence artifacts → git | deploy proof / health snapshots may carry secret-shaped fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-09 | Tampering | wrong value / multi-knob mutation reaching production | mitigate | Single-knob diff guard (acceptance grep asserts only ceiling_mbps changed); phase201-predeploy-gate auto-runs in deploy.sh and fail-closes on blocked keys. |
| T-215-10 | Tampering/Repudiation | un-restarted daemon serving stale ceiling=18 while leg-B "measures" 20 | mitigate | Mandatory systemctl restart + load proof via config-snapshot DB row=20 + CAKE bandwidth_kbit=20000 log line (Pitfall 3); leg-B not captured until ceiling=20 is proven loaded. |
| T-215-11 | Spoofing | false "pass" keeping a regressing change | mitigate | phase215-reclaim-gate env-preflight ABORTs on deployed-ceiling mismatch; D-05 latency-first FAIL gates; D-09 VOID on collapse; verdict != pass forces the Snapshot A revert. |
| T-215-12 | Information Disclosure | secrets in deploy-proof / leg-B / verdict evidence | mitigate | D-08 redaction on all committed evidence; acceptance grep asserts no ROUTER_PASSWORD/DISCORD_WEBHOOK/password tokens across evidence/. |
| T-215-13 | Denial of Service | mutation degrades the live WAN with no clean recovery | mitigate | Snapshot A (Plan 02) is a hard dependency; revert sequence restores ceiling=18 + restart + canary-check exit 0 proof; checkpoint requires operator approval before any mutation. |
| T-215-14 | Elevation of Privilege | new network-facing surface | accept | No new endpoint added; only an existing config value changes and existing read-only /health is polled. No attack surface added. |
</threat_model>

<verification>
- `git diff configs/spectrum.yaml` shows only ceiling_mbps changed (single knob).
- evidence/verdict.json has a valid verdict and per-gate values.
- On non-pass: config back to ceiling 18, DB row=18, canary-check exit 0.
- On pass: config retains ceiling 20 with documented WIN and no D-05 regression.
- `grep -RiE 'ROUTER_PASSWORD|DISCORD_WEBHOOK|password:[^#]*[A-Za-z0-9]'` over evidence/ returns nothing.
- No `src/wanctl/` modified: `git diff --name-only | grep -c '^src/wanctl/'` returns 0.
</verification>

<success_criteria>
- Exactly one knob mutated (ceiling 18 to 20), deployed, restarted, and proven loaded via DB row + CAKE log.
- Leg-B captured non-VOID in the same A/B session; gate emitted verdict.json scoring D-04/D-05.
- Canary resolved: WIN kept with evidence OR cleanly rolled back via Snapshot A (canary-check exit 0, ceiling=18).
- 215-REPORT.md closes RECLAIM-01/02/03 with leg-A vs leg-B numbers, the verdict, and the non-gating libreqos note.
</success_criteria>

<output>
After completion, create `.planning/phases/215-spectrum-upload-reclaim-canary/215-03-SUMMARY.md`
</output>
