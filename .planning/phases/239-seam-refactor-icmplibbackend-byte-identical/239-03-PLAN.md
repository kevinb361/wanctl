---
phase: 239-seam-refactor-icmplibbackend-byte-identical
plan: 03
type: execute
wave: 3
depends_on: ["239-01", "239-02"]
files_modified:
  - scripts/phase239-safe17-boundary-check.sh
autonomous: true
requirements: [SAFE-17]
must_haves:
  truths:
    - "A fail-closed SAFE-17 allowlist verifier exists, anchored at tag v1.52"
    - "The verifier passes when src/wanctl/ diff vs v1.52 is bounded to rtt_backend.py + rtt_measurement.py"
    - "The verifier fails closed on any out-of-allowlist controller-path drift, dirty tree, or unresolved anchor"
    - "JSON evidence is written to the 239 phase evidence dir and records passed:true at the phase boundary"
  artifacts:
    - path: "scripts/phase239-safe17-boundary-check.sh"
      provides: "fail-closed v1.53 Phase-239 allowlist source-diff verifier"
      contains: "V153_ALLOWLIST_RE"
    - path: ".planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json"
      provides: "phase-boundary SAFE-17 evidence record"
      contains: "passed"
  key_links:
    - from: "scripts/phase239-safe17-boundary-check.sh"
      to: "git diff --name-only v1.52..HEAD -- src/wanctl/"
      via: "allowlist regex over changed paths"
      pattern: "V153_ALLOWLIST_RE"
    - from: "scripts/phase239-safe17-boundary-check.sh"
      to: ".planning/phases/239-.../evidence/safe17-boundary-239.json"
      via: "realpath-confined JSON evidence writer"
      pattern: "safe17-boundary-239.json"
---

<objective>
Build the fail-closed SAFE-17 allowlist source-diff verifier for v1.53 Phase 239 and run it at the phase boundary. This is the safety invariant that replaces the SAFE-07..16 zero-diff streak: instead of asserting "no controller-path diff," it asserts "any controller-path diff is bounded to the narrowed v1.53 allowlist." It must PASS on the intended rtt_backend.py + rtt_measurement.py edits and FAIL closed on anything else.

Purpose: SAFE-17 — prove no out-of-allowlist controller-path drift (state machine, thresholds, EWMA, dwell, deadband, arbitration, fusion) entered the tree while the seam landed. This is the dominant threat for the phase: controller-path drift hiding inside a refactor.

Output: `scripts/phase239-safe17-boundary-check.sh` (new fail-closed verifier) and its `passed:true` JSON evidence at `.planning/phases/239-.../evidence/safe17-boundary-239.json`. No `src/wanctl/` file is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md

<interfaces>
<!-- Clone sources. Two-source clone: allowlist semantics + hardening/evidence machinery. -->

Anchor: tag v1.52 (VERIFIED present via `git tag`). Last clean controller-path commit.
Allowlist regex (Phase 239 surface ONLY): ^src/wanctl/(rtt_backend\.py|rtt_measurement\.py)$
Evidence path convention: .planning/phases/238-.../evidence/safe17-boundary-238.json (VERIFIED)
  -> mirror as .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json

From scripts/check-safe07-source-diff.sh (allowlist semantics + dirty-tree precheck, lines 133-203):
  - dirty-tree fail-closed: git diff --quiet / --cached --quiet / git ls-files --others --exclude-standard -- src/wanctl/
  - CHANGED_PATHS=$(git diff --name-only "${REF}..HEAD" -- src/wanctl/)
  - DISALLOWED_PATHS=$(printf '%s\n' "$CHANGED_PATHS" | grep -Ev "$ALLOWLIST_RE")
  - empty DISALLOWED => OK exit 0; non-empty => VIOLATION exit 1

From scripts/phase238-safe17-boundary-check.sh (arg hardening + JSON evidence, lines 11-90+):
  - set -euo pipefail; ANCHOR="v1.52"; --anchor/--out flags
  - --out rejected if contains 'src/wanctl' or '..'; realpath-confined to ALLOWED_OUT_PREFIX
  - ANCHOR_SHA=$(git rev-parse --verify --end-of-options "${ANCHOR}^{commit}")
  - python3 heredoc emits JSON: per-path diff, dirty_tree, passed, checked_at (ISO-Z)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the fail-closed SAFE-17 v1.53 allowlist verifier</name>
  <files>scripts/phase239-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/check-safe07-source-diff.sh (lines 130-203) — allowlist regex + DISALLOWED_PATHS fail-closed logic + dirty-tree precheck (clone the allowlist semantics, NOT the version-bump assertion)
    - scripts/phase238-safe17-boundary-check.sh (lines 1-90 and the python3 JSON heredoc through the end) — arg hardening, --out realpath confinement, anchor rev-parse, JSON evidence emitter (clone verbatim)
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 170-220) — two-source clone instructions and the "do NOT reuse 238 zero-diff" warning
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md (Pitfall 4 lines 259-262; Pitfall 5 lines 264-267) — anchor v1.52, drop __version__ bump assertion
  </read_first>
  <action>
    Create scripts/phase239-safe17-boundary-check.sh as a two-source clone. Start from scripts/phase238-safe17-boundary-check.sh's skeleton (`set -euo pipefail`, `--anchor`/`--out` flags, usage, require_command, arg hardening, anchor rev-parse, python3 JSON evidence heredoc) and graft in the ALLOWLIST semantics from scripts/check-safe07-source-diff.sh lines 133-203.

    Set ANCHOR to v1.52 (Pitfall 4 / Assumption A6 — VERIFIED tag present; last clean controller-path commit). Set the default OUT and ALLOWED_OUT_PREFIX to the 239 phase evidence dir, file safe17-boundary-239.json (mirror the 238 convention exactly).

    Keep the dirty-tree fail-closed precheck verbatim (unstaged plus staged plus untracked under src/wanctl/ triggers exit 1). Keep the --out realpath confinement (reject paths containing src/wanctl or the parent-dir token; require realpath under ALLOWED_OUT_PREFIX) and the anchor resolution via git rev-parse --verify --end-of-options on the anchor commit.

    Replace the SAFE-09 zero-diff/version-bump logic with the v1.53 ALLOWLIST logic. Define the allowlist regex variable V153_ALLOWLIST_RE matching exactly src/wanctl/rtt_backend.py and src/wanctl/rtt_measurement.py anchored start-to-end — the Phase 239 surface, the narrowest in the milestone (PATTERNS line 190; allowlist grows in later phases). Compute CHANGED_PATHS via git diff --name-only on the resolved anchor SHA to HEAD over src/wanctl/, then DISALLOWED_PATHS via grep -Ev against V153_ALLOWLIST_RE. Empty CHANGED_PATHS OR empty DISALLOWED yields SAFE-17 OK exit 0; non-empty DISALLOWED yields SAFE-17 VIOLATION, print the offending paths to stderr, exit 1.

    Do NOT clone the 238 zero-diff script unchanged — it FAILS the instant rtt_measurement.py is edited, which is the intended change (Pitfall 4, PATTERNS line 220). Do NOT add a __version__ bump assertion and do NOT add __init__.py to the allowlist — v1.53 is mid-milestone, version string lags tags, defer any bump to Phase 246 (Pitfall 5 / Assumption A3). Do NOT add wan_controller.py / interfaces.py / irtt_measurement.py to the allowlist — out-of-allowlist drift in those files MUST trip the verifier.

    Emit the JSON evidence record (clone the 238 python3 heredoc) including anchor plus anchor_sha, the changed src/wanctl/ paths, the allowlist regex, disallowed paths (empty on pass), dirty_tree status, passed boolean, and checked_at ISO-Z timestamp. Use exit codes 0 clean / 1 violation / 2 usage-or-git-error to match the analog.
  </action>
  <verify>
    <automated>bash -n scripts/phase239-safe17-boundary-check.sh && grep -q 'V153_ALLOWLIST_RE' scripts/phase239-safe17-boundary-check.sh && grep -q 'rtt_backend' scripts/phase239-safe17-boundary-check.sh && grep -q 'rtt_measurement' scripts/phase239-safe17-boundary-check.sh && grep -q 'v1.52' scripts/phase239-safe17-boundary-check.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase239-safe17-boundary-check.sh` exits 0 (valid shell syntax)
    - `grep -q 'V153_ALLOWLIST_RE' scripts/phase239-safe17-boundary-check.sh` matches and the regex names exactly rtt_backend.py and rtt_measurement.py (no wan_controller.py / interfaces.py / __init__.py)
    - `grep -c '__version__' scripts/phase239-safe17-boundary-check.sh` returns 0 (no version-bump assertion — Pitfall 5)
    - Anchor default is v1.52 and is resolved via `git rev-parse --verify --end-of-options`
    - Script retains the dirty-tree precheck and the --out realpath confinement from the analogs
    - Exit codes follow 0 clean / 1 violation / 2 usage-or-git-error
  </acceptance_criteria>
  <done>scripts/phase239-safe17-boundary-check.sh exists, syntax-valid, fail-closed allowlist semantics anchored at v1.52, no version-bump assertion, dirty-tree + --out hardening preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run the verifier at the phase boundary and record passed:true evidence</name>
  <files>.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json</files>
  <read_first>
    - scripts/phase239-safe17-boundary-check.sh (the verifier from Task 1)
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json — the evidence-record shape to mirror
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 203-218) — evidence convention
  </read_first>
  <action>
    Run scripts/phase239-safe17-boundary-check.sh with default args at the phase boundary AFTER Plans 01 and 02 are committed (so the only src/wanctl/ diff vs v1.52 is rtt_backend.py + rtt_measurement.py). The script must exit 0 and write safe17-boundary-239.json with passed:true.

    If the verifier exits non-zero, STOP and investigate: a non-zero exit means either out-of-allowlist controller-path drift slipped in (a real SAFE-17 violation — find and revert the offending file), a dirty tree (commit/stash first), or an unresolved anchor (confirm v1.52 tag). Do NOT widen the allowlist to make it pass — the allowlist is the contract. Do NOT touch any src/wanctl/ file from this plan.

    This plan runs LAST (wave 3) precisely because the allowlist proof is only meaningful once the intended controller-path edits from Plans 01-02 are committed. The verifier proves they are the ONLY controller-path changes.
  </action>
  <verify>
    <automated>bash scripts/phase239-safe17-boundary-check.sh && python3 -c "import json; d=json.load(open('.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json')); assert d.get('passed') is True, d; print('OK', d.get('anchor'))"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/phase239-safe17-boundary-check.sh` exits 0 at the phase boundary
    - `.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json` exists with `passed: true`
    - The evidence record lists only src/wanctl/rtt_backend.py and src/wanctl/rtt_measurement.py as changed controller-path files (disallowed paths empty)
    - The evidence anchor field is the v1.52 commit (anchor + anchor_sha recorded)
    - dirty_tree status in the evidence is clean
    - No src/wanctl/ file was modified by this plan (`git diff --name-only` lists only the script + evidence JSON)
  </acceptance_criteria>
  <done>The SAFE-17 verifier runs clean at the phase boundary; safe17-boundary-239.json records passed:true with the diff bounded to the two allowlisted files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed tree → SAFE-17 verifier | The verifier is the gate that proves no out-of-allowlist controller-path drift crossed into the tree |
| verifier CLI args → filesystem | --out/--anchor are untrusted-shaped; must not write into controller source or run against an unresolved ref |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-08 | Tampering | out-of-allowlist controller-path drift (EWMA/dwell/deadband/arbitration/fusion) | mitigate | V153_ALLOWLIST_RE pins the allowed set to rtt_backend.py + rtt_measurement.py; any other src/wanctl/ change fails closed (exit 1) — this IS the SAFE-17 mitigation |
| T-239-09 | Tampering | verifier --out path traversal into controller source | mitigate | realpath confinement to phase evidence dir + reject 'src/wanctl' and parent-dir tokens (cloned from 238 script) |
| T-239-10 | Tampering | unresolved/attacker-shaped git anchor | mitigate | git rev-parse --verify --end-of-options on the anchor commit before diffing (cloned from 238 script) |
| T-239-11 | Repudiation | unrecorded boundary check | mitigate | JSON evidence record with passed boolean + checked_at ISO-Z written to the phase evidence dir |
| T-239-12 | Tampering | dirty working tree masking real drift | mitigate | dirty-tree fail-closed precheck (unstaged/staged/untracked) before trusting committed diff |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (shell verifier only) |
</threat_model>

<verification>
- `bash -n scripts/phase239-safe17-boundary-check.sh` exits 0
- `bash scripts/phase239-safe17-boundary-check.sh` exits 0 at the phase boundary
- safe17-boundary-239.json exists with passed:true, anchor v1.52, diff bounded to rtt_backend.py + rtt_measurement.py
- The verifier fails closed (exit 1) if any other src/wanctl/ file is changed vs v1.52
</verification>

<success_criteria>
- The narrowed v1.53 SAFE-17 allowlist is defined and the fail-closed verifier runs at the phase boundary, proving no out-of-allowlist controller-path drift (SAFE-17)
- The verifier PASSES on the intended rtt_backend.py + rtt_measurement.py edits and FAILS closed on anything else
- JSON evidence records passed:true anchored at v1.52
- No src/wanctl/ file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-03-SUMMARY.md` when done
</output>
