---
phase: 239-seam-refactor-icmplibbackend-byte-identical
plan: 03
type: execute
wave: 3
depends_on: ["239-01", "239-02"]
files_modified:
  - scripts/phase239-safe17-boundary-check.sh
  - scripts/phase239-protected-body-diff.py
  - tests/test_phase239_safe17_verifier.py
autonomous: true
requirements: [SAFE-17]
must_haves:
  truths:
    - "A fail-closed SAFE-17 allowlist verifier exists, anchored at tag v1.52"
    - "The verifier passes when src/wanctl/ diff vs v1.52 is bounded to rtt_backend.py + rtt_measurement.py AND every protected function body is byte-identical to v1.52"
    - "An intra-file function-body drift guard fails closed if any protected function body in rtt_measurement.py or wan_controller.py changed vs v1.52 — path allowlisting alone is insufficient"
    - "The verifier fails closed on out-of-allowlist controller-path drift, a protected-body drift, a dirty tree, or an unresolved anchor — proven by negative tests that do NOT leave the real tree dirty"
    - "Plans 01 and 02 are committed BEFORE the verifier runs (hard precondition; the dirty-tree precheck enforces it)"
    - "JSON evidence is written to the 239 phase evidence dir and records passed:true at the phase boundary"
  artifacts:
    - path: "scripts/phase239-safe17-boundary-check.sh"
      provides: "fail-closed v1.53 Phase-239 allowlist + intra-file body-drift source-diff verifier"
      contains: "V153_ALLOWLIST_RE"
    - path: "scripts/phase239-protected-body-diff.py"
      provides: "AST function-body extractor comparing protected functions HEAD-vs-v1.52"
      contains: "PROTECTED"
    - path: "tests/test_phase239_safe17_verifier.py"
      provides: "negative tests proving the verifier fails closed (tree-safe)"
      contains: "def test_"
    - path: ".planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json"
      provides: "phase-boundary SAFE-17 evidence record"
      contains: "passed"
  key_links:
    - from: "scripts/phase239-safe17-boundary-check.sh"
      to: "git diff --name-only v1.52..HEAD -- src/wanctl/"
      via: "allowlist regex over changed paths"
      pattern: "V153_ALLOWLIST_RE"
    - from: "scripts/phase239-safe17-boundary-check.sh"
      to: "scripts/phase239-protected-body-diff.py"
      via: "intra-file protected-body drift gate invoked before passed:true"
      pattern: "phase239-protected-body-diff.py"
    - from: "scripts/phase239-protected-body-diff.py"
      to: "git show v1.52:src/wanctl/rtt_measurement.py"
      via: "AST extraction of protected function bodies HEAD-vs-v1.52"
      pattern: "git show"
---

<objective>
Build the fail-closed SAFE-17 verifier for v1.53 Phase 239 and run it at the phase boundary. This is the safety invariant that replaces the SAFE-07..16 zero-diff streak. It has TWO layers, both fail-closed:

1. **Path allowlist** — any controller-path diff vs v1.52 must be bounded to the narrowed v1.53 allowlist (rtt_backend.py + rtt_measurement.py).
2. **Intra-file body-drift guard** — because the WHOLE of rtt_measurement.py is allowlisted, path allowlisting alone cannot prove byte-identity INSIDE that file. A function-body drift guard compares the protected function bodies HEAD-vs-v1.52 and FAILS CLOSED on any change. The only ALLOWED change to rtt_measurement.py this phase is the additive probe() method + its local RttSample import; the protected bodies must remain byte-identical to v1.52.

Purpose: SAFE-17 — prove no out-of-allowlist controller-path drift (state machine, thresholds, EWMA, dwell, deadband, arbitration, fusion) AND no hidden behavioral drift inside the allowlisted rtt_measurement.py. The dominant residual threat for this phase (per the cross-AI review) is exactly that hidden intra-file drift.

HARD PRECONDITION (commit-ordering): Plans 01 and 02 MUST be committed before this plan's Task 3 runs. The dirty-tree precheck enforces it (it fails closed on any uncommitted/staged/untracked src/wanctl/ change), but it is stated here as a contract, not a note: running all three plans in one uncommitted wave will FAIL by design.

Output: `scripts/phase239-safe17-boundary-check.sh` (allowlist + body-drift verifier), `scripts/phase239-protected-body-diff.py` (AST body extractor), `tests/test_phase239_safe17_verifier.py` (tree-safe negative tests), and the `passed:true` JSON evidence at `.planning/phases/239-.../evidence/safe17-boundary-239.json`. No `src/wanctl/` file is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md

<interfaces>
<!-- Clone sources + protected-function set. -->

Anchor: tag v1.52 (VERIFIED present via `git tag`). Last clean controller-path commit.
Path allowlist regex (Phase 239 surface ONLY): ^src/wanctl/(rtt_backend\.py|rtt_measurement\.py)$
Evidence path convention: .planning/phases/238-.../evidence/safe17-boundary-238.json (VERIFIED)
  -> mirror as .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json

Protected function set (byte-identical-to-v1.52 required; names VERIFIED in the live file):
  src/wanctl/rtt_measurement.py:
    - module-level function: ping_host                       (def at line 174)
    - module-level/method:  _aggregate_rtts                  (def at line 259, RTTMeasurement method)
    - method:               ping_hosts_with_results          (def at line 287, RTTMeasurement method)
    - method:               BackgroundRTTThread._run         (def at line 470)
    - method:               BackgroundRTTThread._ping_with_persistent_pool (def at line 531; PRESENT)
  src/wanctl/wan_controller.py:
    - method:               WANController.measure_rtt         (def at line 1137)
  ALLOWED change to rtt_measurement.py: the new RTTMeasurement.probe() method + its local `from wanctl.rtt_backend import RttSample`.
  Everything in the protected set above must hash-equal its v1.52 source segment.

From scripts/check-safe07-source-diff.sh (allowlist semantics + dirty-tree precheck, lines 133-203):
  - dirty-tree fail-closed: git diff --quiet / --cached --quiet / git ls-files --others --exclude-standard -- src/wanctl/
  - CHANGED_PATHS=$(git diff --name-only "${REF}..HEAD" -- src/wanctl/)
  - DISALLOWED_PATHS=$(printf '%s\n' "$CHANGED_PATHS" | grep -Ev "$ALLOWLIST_RE")
  - empty DISALLOWED => OK exit 0; non-empty => VIOLATION exit 1

From scripts/phase238-safe17-boundary-check.sh (arg hardening + JSON evidence, lines 11-90+):
  - set -euo pipefail; ANCHOR="v1.52"; --anchor/--out flags
  - --out rejected if contains 'src/wanctl' or '..'; realpath-confined to ALLOWED_OUT_PREFIX
  - ANCHOR_SHA=$(git rev-parse --verify --end-of-options "${ANCHOR}^{commit}")
  - python3 heredoc emits JSON: per-path diff, dirty_tree, passed, checked_at (ISO-Z)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AST protected-body drift extractor (HEAD vs v1.52)</name>
  <files>scripts/phase239-protected-body-diff.py</files>
  <read_first>
    - src/wanctl/rtt_measurement.py (lines 174-258 ping_host; 259-286 _aggregate_rtts; 287-324 ping_hosts_with_results; 470-529 BackgroundRTTThread._run; 531-575 _ping_with_persistent_pool) — the protected segments
    - src/wanctl/wan_controller.py (lines 1137-1312 measure_rtt) — the protected segment in the second file
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md (Pitfall 3 lines 254-257) — _cached type cascade rationale for why these bodies are protected
  </read_first>
  <action>
    Create scripts/phase239-protected-body-diff.py: a standalone Python script (stdlib only: ast, subprocess, sys, json) that proves the protected function bodies are byte-identical between v1.52 and HEAD, fail-closed.

    Define a PROTECTED mapping of file -> list of qualified function names exactly:
      "src/wanctl/rtt_measurement.py": ["ping_host", "RTTMeasurement._aggregate_rtts", "RTTMeasurement.ping_hosts_with_results", "BackgroundRTTThread._run", "BackgroundRTTThread._ping_with_persistent_pool"]
      "src/wanctl/wan_controller.py": ["WANController.measure_rtt"]
    (Resolve method qualnames by walking the AST: for each ClassDef, match its FunctionDef children by Class.method; for module-level functions match the bare name.)

    Extraction approach (concrete, robust): for each protected file, obtain the v1.52 source via `git show v1.52:<path>` (capture stdout) and the HEAD/worktree source by reading the file. Parse BOTH with `ast.parse`, locate each protected FunctionDef node by qualname, and extract its EXACT source segment with `ast.get_source_segment(source_text, node)` (preserves the body verbatim). Compare the v1.52 segment string to the HEAD segment string for each protected function. Any inequality, missing function (renamed/removed), or parse failure is a FAIL.

    The script must:
      - resolve the anchor via `git rev-parse --verify --end-of-options v1.52^{commit}` (accept an optional --anchor arg defaulting to v1.52; reject an unresolved anchor with a non-zero exit)
      - print a per-function PASS/FAIL line to stderr and, when invoked with --json, emit a JSON object {anchor, anchor_sha, protected: [{file, function, identical: bool}], all_identical: bool} to stdout
      - exit 0 only if EVERY protected function is identical; exit 1 on ANY drift/missing/parse failure; exit 2 on usage/anchor/git error
    Do NOT modify any src/wanctl/ file. Do NOT auto-fix drift — report it.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/phase239-protected-body-diff.py --anchor v1.52 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/phase239-protected-body-diff.py --anchor v1.52` exits 0 at the phase boundary (all protected bodies identical to v1.52 — only probe() was added)
    - `grep -c 'WANController.measure_rtt\|BackgroundRTTThread._run\|BackgroundRTTThread._ping_with_persistent_pool\|ping_hosts_with_results\|_aggregate_rtts\|ping_host' scripts/phase239-protected-body-diff.py` >= 6 (the full protected set is named)
    - `grep -q 'ast.get_source_segment' scripts/phase239-protected-body-diff.py` and `grep -q 'git' scripts/phase239-protected-body-diff.py` (AST extraction + git show v1.52 are the concrete mechanism)
    - `grep -q 'rev-parse' scripts/phase239-protected-body-diff.py` (anchor resolved, unresolved anchor fails)
    - Exit codes follow 0 identical / 1 drift-or-missing / 2 usage-or-git-error
    - The script reads (never writes) src/wanctl/ files
  </acceptance_criteria>
  <done>scripts/phase239-protected-body-diff.py exists, extracts the six protected function bodies via AST get_source_segment, compares HEAD vs v1.52, and fails closed (exit 1) on any drift/missing/parse failure.</done>
</task>

<task type="auto">
  <name>Task 2: Write the fail-closed SAFE-17 v1.53 allowlist verifier (path allowlist + body-drift gate)</name>
  <files>scripts/phase239-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/check-safe07-source-diff.sh (lines 130-203) — allowlist regex + DISALLOWED_PATHS fail-closed logic + dirty-tree precheck (clone the allowlist semantics, NOT the version-bump assertion)
    - scripts/phase238-safe17-boundary-check.sh (lines 1-90 and the python3 JSON heredoc through the end) — arg hardening, --out realpath confinement, anchor rev-parse, JSON evidence emitter (clone verbatim)
    - scripts/phase239-protected-body-diff.py (Task 1 — the intra-file gate this script invokes)
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 170-220) — two-source clone instructions and the "do NOT reuse 238 zero-diff" warning
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md (Pitfall 4 lines 259-262; Pitfall 5 lines 264-267) — anchor v1.52, drop __version__ bump assertion
  </read_first>
  <action>
    Create scripts/phase239-safe17-boundary-check.sh as a two-source clone PLUS the intra-file gate. Start from scripts/phase238-safe17-boundary-check.sh's skeleton (`set -euo pipefail`, `--anchor`/`--out` flags, usage, require_command, arg hardening, anchor rev-parse, python3 JSON evidence heredoc) and graft in the ALLOWLIST semantics from scripts/check-safe07-source-diff.sh lines 133-203.

    Set ANCHOR to v1.52 (Pitfall 4 / Assumption A6 — VERIFIED tag; last clean controller-path commit). Set the default OUT and ALLOWED_OUT_PREFIX to the 239 phase evidence dir, file safe17-boundary-239.json (mirror the 238 convention exactly).

    Keep the dirty-tree fail-closed precheck verbatim (unstaged + staged + untracked under src/wanctl/ triggers exit 1) — this IS the commit-ordering enforcement (HARD PRECONDITION: Plans 01/02 committed first). Keep the --out realpath confinement (reject paths containing src/wanctl or the parent-dir token; require realpath under ALLOWED_OUT_PREFIX) and the anchor resolution via git rev-parse --verify --end-of-options.

    LAYER 1 (path allowlist): Replace the SAFE-09 zero-diff/version-bump logic with the v1.53 ALLOWLIST logic. Define V153_ALLOWLIST_RE matching exactly src/wanctl/rtt_backend.py and src/wanctl/rtt_measurement.py anchored start-to-end. Compute CHANGED_PATHS via git diff --name-only on the resolved anchor SHA to HEAD over src/wanctl/, then DISALLOWED_PATHS via grep -Ev against V153_ALLOWLIST_RE. Non-empty DISALLOWED => SAFE-17 VIOLATION, print offending paths to stderr, exit 1.

    LAYER 2 (intra-file body-drift gate): AFTER the path allowlist passes and BEFORE emitting passed:true, invoke `python3 scripts/phase239-protected-body-diff.py --anchor "$ANCHOR"`. If it exits non-zero, this is a SAFE-17 VIOLATION (a protected function body drifted inside the allowlisted file, or measure_rtt drifted) — print its stderr, set passed:false, exit 1. Only when BOTH layers pass is passed:true. Capture the body-drift result (the --json output) into the evidence record.

    Do NOT clone the 238 zero-diff script unchanged — it FAILS the instant rtt_measurement.py is edited, which is the intended change (Pitfall 4, PATTERNS line 220). Do NOT add a __version__ bump assertion and do NOT add __init__.py to the allowlist — v1.53 is mid-milestone; defer any bump to Phase 246 (Pitfall 5 / Assumption A3). Do NOT add wan_controller.py / interfaces.py / irtt_measurement.py to the path allowlist — out-of-allowlist drift in those files MUST trip Layer 1 (and measure_rtt drift trips Layer 2).

    Emit the JSON evidence record (clone the 238 python3 heredoc) including anchor + anchor_sha, the changed src/wanctl/ paths, the path-allowlist regex, disallowed paths (empty on pass), the protected-body-drift result (all_identical + per-function), dirty_tree status, passed boolean (path-allowlist AND body-identical AND clean tree), and checked_at ISO-Z. Use exit codes 0 clean / 1 violation / 2 usage-or-git-error.
  </action>
  <verify>
    <automated>bash -n scripts/phase239-safe17-boundary-check.sh && grep -q 'V153_ALLOWLIST_RE' scripts/phase239-safe17-boundary-check.sh && grep -q 'phase239-protected-body-diff.py' scripts/phase239-safe17-boundary-check.sh && grep -q 'rtt_backend' scripts/phase239-safe17-boundary-check.sh && grep -q 'rtt_measurement' scripts/phase239-safe17-boundary-check.sh && grep -q 'v1.52' scripts/phase239-safe17-boundary-check.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase239-safe17-boundary-check.sh` exits 0 (valid shell syntax)
    - `grep -q 'V153_ALLOWLIST_RE' scripts/phase239-safe17-boundary-check.sh` matches and the regex names exactly rtt_backend.py and rtt_measurement.py (no wan_controller.py / interfaces.py / __init__.py)
    - `grep -q 'phase239-protected-body-diff.py' scripts/phase239-safe17-boundary-check.sh` (Layer 2 body-drift gate is wired in and gates passed:true)
    - `grep -c '__version__' scripts/phase239-safe17-boundary-check.sh` returns 0 (no version-bump assertion — Pitfall 5)
    - Anchor default is v1.52 and is resolved via `git rev-parse --verify --end-of-options`
    - Script retains the dirty-tree precheck and the --out realpath confinement from the analogs
    - Exit codes follow 0 clean / 1 violation / 2 usage-or-git-error
  </acceptance_criteria>
  <done>scripts/phase239-safe17-boundary-check.sh exists, syntax-valid, with BOTH a fail-closed path allowlist (anchored v1.52, narrowed to the two files) and a fail-closed intra-file body-drift gate invoking phase239-protected-body-diff.py; no version-bump assertion; dirty-tree + --out hardening preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Tree-safe negative tests + run the verifier at the phase boundary (passed:true)</name>
  <files>tests/test_phase239_safe17_verifier.py, .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json</files>
  <read_first>
    - scripts/phase239-safe17-boundary-check.sh (Task 2 — the verifier under test)
    - scripts/phase239-protected-body-diff.py (Task 1 — the body-drift gate under test)
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json — the evidence-record shape to mirror
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 203-218) — evidence convention
  </read_first>
  <behavior>
    - test_verifier_passes_at_boundary: with the real (clean, committed) tree, scripts/phase239-safe17-boundary-check.sh exits 0 and writes evidence with passed:true
    - test_fails_on_out_of_allowlist_change: an out-of-allowlist src/wanctl/wan_controller.py change makes the verifier exit 1 (Layer 1) — run inside a throwaway git worktree so the real tree is never dirtied
    - test_fails_on_protected_body_drift: a drift inside a protected function body in rtt_measurement.py makes phase239-protected-body-diff.py (and the verifier) exit 1 (Layer 2) — also inside the throwaway worktree
    - test_fails_on_unresolved_anchor: invoking the verifier with --anchor <nonexistent-ref> exits non-zero (anchor rev-parse fail-closed)
  </behavior>
  <action>
    TREE-SAFE MECHANISM (mandatory): the negative tests MUST NOT leave the real working tree or index dirty on this 50ms production codebase. Use a disposable linked worktree: in a tmp dir, run `git worktree add --detach <tmpdir> HEAD`, make the mutating change THERE (edit wan_controller.py for the out-of-allowlist case; edit a protected body in rtt_measurement.py for the body-drift case), `git -C <tmpdir> commit -am ...` so the change is committed (the verifier's dirty-tree precheck requires committed state to reach the allowlist/body layers), run the verifier/extractor with cwd=<tmpdir>, assert the non-zero exit, then ALWAYS `git worktree remove --force <tmpdir>` in a finally/fixture teardown. The v1.52 tag is visible from the linked worktree (shared object store), so `git show v1.52:...` and rev-parse work there. Do NOT use `git stash` on the real tree and do NOT scratch-commit-then-reset the real HEAD — the worktree keeps the real tree and real HEAD untouched.

    Write tests/test_phase239_safe17_verifier.py with a pytest fixture that yields a fresh detached worktree at HEAD and force-removes it on teardown. Implement the four named tests. For test_fails_on_unresolved_anchor, just invoke the verifier with `--anchor v0.0.0-does-not-exist` and assert returncode != 0 (no worktree needed; anchor resolution fails before any mutation). Use subprocess.run with cwd set appropriately and capture returncode.

    THEN run the verifier for real at the phase boundary AFTER Plans 01 and 02 are committed: `bash scripts/phase239-safe17-boundary-check.sh` with default args. It must exit 0 and write safe17-boundary-239.json with passed:true. If it exits non-zero, STOP and investigate (out-of-allowlist drift => revert the offending file; protected-body drift => the refactor changed a protected body, revert it; dirty tree => commit/stash first; unresolved anchor => confirm v1.52). Do NOT widen the allowlist or weaken the body-drift guard to make it pass — they are the contract. Do NOT touch any src/wanctl/ file from this plan.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_phase239_safe17_verifier.py -q && bash scripts/phase239-safe17-boundary-check.sh && python3 -c "import json; d=json.load(open('.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/evidence/safe17-boundary-239.json')); assert d.get('passed') is True, d; print('OK', d.get('anchor'))" && git diff --quiet -- src/wanctl/ && echo TREE-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -o addopts='' tests/test_phase239_safe17_verifier.py -q` exits 0 with the four named tests passing
    - `grep -c 'def test_verifier_passes_at_boundary\|def test_fails_on_out_of_allowlist_change\|def test_fails_on_protected_body_drift\|def test_fails_on_unresolved_anchor' tests/test_phase239_safe17_verifier.py` returns 4
    - `grep -q 'git worktree add' tests/test_phase239_safe17_verifier.py` and `grep -q 'worktree remove' tests/test_phase239_safe17_verifier.py` (tree-safe disposable-worktree mechanism; no stash/reset of the real tree)
    - `git diff --quiet -- src/wanctl/` exits 0 AFTER the negative tests run (the real tree is NOT dirtied — TREE-CLEAN printed)
    - `bash scripts/phase239-safe17-boundary-check.sh` exits 0 at the phase boundary
    - `.planning/phases/239-.../evidence/safe17-boundary-239.json` exists with `passed: true`, lists only rtt_backend.py + rtt_measurement.py as changed, records all_identical:true for the protected bodies, anchor v1.52 + anchor_sha, and dirty_tree clean
    - No src/wanctl/ file was modified by this plan (`git diff --name-only` lists only the two scripts, the test file, and the evidence JSON)
  </acceptance_criteria>
  <done>The four tree-safe negative tests prove the verifier fails closed (out-of-allowlist, protected-body drift, unresolved anchor) without dirtying the real tree, and the verifier runs clean at the phase boundary writing safe17-boundary-239.json passed:true with the protected bodies proven identical to v1.52.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed tree → SAFE-17 verifier | The verifier gates against out-of-allowlist controller-path drift AND intra-file behavioral drift |
| allowlisted rtt_measurement.py → protected function bodies | Path allowlisting trusts the whole file; the body-drift gate re-establishes byte-identity for the protected functions |
| verifier CLI args → filesystem | --out/--anchor are untrusted-shaped; must not write into controller source or run against an unresolved ref |
| negative tests → real working tree | Mutating tests must run in a disposable worktree; the real tree/HEAD must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-08 | Tampering | out-of-allowlist controller-path drift (EWMA/dwell/deadband/arbitration/fusion) | mitigate | V153_ALLOWLIST_RE pins the allowed set to rtt_backend.py + rtt_measurement.py; any other src/wanctl/ change fails closed (Layer 1, exit 1) |
| T-239-15 | Tampering | intra-file behavioral drift inside allowlisted rtt_measurement.py (ping_host/_aggregate_rtts/_run/blackout) | mitigate | AST body-drift gate compares the protected function bodies HEAD-vs-v1.52 and fails closed on any change (Layer 2); test_fails_on_protected_body_drift proves it |
| T-239-16 | Tampering | measure_rtt() drift in wan_controller.py masked by path allowlist | mitigate | wan_controller.py is OUT of the path allowlist (Layer 1 trips) AND WANController.measure_rtt is in the protected-body set (Layer 2 trips); both proven by negative test |
| T-239-09 | Tampering | verifier --out path traversal into controller source | mitigate | realpath confinement to phase evidence dir + reject 'src/wanctl' and parent-dir tokens (cloned from 238 script) |
| T-239-10 | Tampering | unresolved/attacker-shaped git anchor | mitigate | git rev-parse --verify --end-of-options on the anchor in BOTH the shell verifier and the body-diff extractor; test_fails_on_unresolved_anchor proves fail-closed |
| T-239-11 | Repudiation | unrecorded boundary check | mitigate | JSON evidence record with passed boolean + protected-body result + checked_at ISO-Z written to the phase evidence dir |
| T-239-12 | Tampering | dirty working tree masking real drift / out-of-order run | mitigate | dirty-tree fail-closed precheck (unstaged/staged/untracked) enforces the Plans-01/02-committed-first hard precondition before trusting the committed diff |
| T-239-17 | Tampering | negative tests dirtying the production tree | mitigate | disposable `git worktree add --detach` + force-remove teardown; real tree/HEAD untouched; `git diff --quiet -- src/wanctl/` asserted clean after the tests |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (shell + stdlib python verifier only) |
</threat_model>

<verification>
- `bash -n scripts/phase239-safe17-boundary-check.sh` exits 0
- `.venv/bin/python scripts/phase239-protected-body-diff.py --anchor v1.52` exits 0 at the boundary
- `.venv/bin/pytest -o addopts='' tests/test_phase239_safe17_verifier.py -q` exits 0 (four tree-safe negative/positive tests)
- `git diff --quiet -- src/wanctl/` exits 0 after the negative tests (real tree clean)
- `bash scripts/phase239-safe17-boundary-check.sh` exits 0 at the phase boundary; safe17-boundary-239.json passed:true, anchor v1.52, diff bounded to rtt_backend.py + rtt_measurement.py, protected bodies all_identical:true
- The verifier fails closed (exit 1) on out-of-allowlist drift (Layer 1), protected-body drift (Layer 2), and an unresolved anchor — proven by the negative tests
</verification>

<success_criteria>
- The narrowed v1.53 SAFE-17 allowlist is defined and the fail-closed verifier runs at the phase boundary with TWO layers: path allowlist + intra-file protected-body drift guard (SAFE-17)
- The verifier PASSES on the intended rtt_backend.py + rtt_measurement.py edits (with protected bodies identical to v1.52) and FAILS closed on out-of-allowlist drift, protected-body drift, dirty tree, or unresolved anchor
- Negative tests prove the fail-closed behavior without dirtying the real production tree (disposable worktree)
- Plans 01/02 committed first is a hard precondition enforced by the dirty-tree precheck
- JSON evidence records passed:true anchored at v1.52 with protected-body all_identical:true
- No src/wanctl/ file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-03-SUMMARY.md` when done
</output>
</content>
