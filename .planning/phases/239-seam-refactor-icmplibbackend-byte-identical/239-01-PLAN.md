---
phase: 239-seam-refactor-icmplibbackend-byte-identical
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/rtt_backend.py
  - tests/test_rtt_backend.py
autonomous: true
requirements: [SEAM-01, SEAM-03, SEAM-04]
must_haves:
  truths:
    - "A single RttBackend Protocol type exists and is importable from one module"
    - "The RttBackend.probe() contract returns RttSample | None — None means no successful measurement (mirrors the existing no-snapshot-on-all-fail path); a returned RttSample always carries a real rtt_ms"
    - "RttSample carries every RTTSnapshot field plus backend / source_ip / per_host_loss, with per_host_loss in defined percent units (0.0 = no loss .. 100.0 = total loss)"
    - "RttSample.to_snapshot() returns a byte-equal RTTSnapshot for the subset fields"
    - "An IRTT adapter skeleton proves the Protocol can absorb IRTT: a pure, tested sample_from_irtt_result() mapping helper exists, while live IRTT probing stays unwired"
    - "SEAM-01 is satisfied structurally this phase: RTTMeasurement conforms to RttBackend (Plan 02) and steering/autorate already consume RTTMeasurement structurally; no consumer is rewired in Phase 239"
    - "rtt_backend.py module top-level has NO unconditional import of RTTSnapshot from rtt_measurement — the import is TYPE_CHECKING-only at module scope and a real local import inside to_snapshot()"
  artifacts:
    - path: "src/wanctl/rtt_backend.py"
      provides: "RttBackend Protocol + RttSample dataclass + sample_from_irtt_result() helper + IrttRttBackend stub"
      contains: "class RttBackend"
      min_lines: 70
    - path: "tests/test_rtt_backend.py"
      provides: "Protocol conformance + superset + snapshot-equivalence + IRTT-mapping + acyclic-import tests"
      contains: "to_snapshot"
  key_links:
    - from: "src/wanctl/rtt_backend.py to_snapshot()"
      to: "src/wanctl/rtt_measurement.py"
      via: "local (in-function) import of RTTSnapshot, designed acyclic"
      pattern: "from wanctl.rtt_measurement import RTTSnapshot"
    - from: "src/wanctl/rtt_backend.py"
      to: "src/wanctl/irtt_measurement.py"
      via: "sample_from_irtt_result() maps IRTTResult -> RttSample"
      pattern: "IRTTResult"
---

<objective>
Define the seam contract: a single `RttBackend` Protocol, an `RttSample` value type that is a strict superset of `RTTSnapshot`, a pure `sample_from_irtt_result()` IRTT mapping helper, and an unwired IRTT adapter — all in the new `src/wanctl/rtt_backend.py`. This is the interface-first task: it creates the contracts that Plan 02 implements against and ships the unit tests that prove SEAM-01/03/04.

Purpose: Establishes the abstraction (SEAM-01), the metadata-carrying superset value type (SEAM-03), and the IRTT-absorption shape (SEAM-04) with zero behavior change to the live path. The imports are acyclic BY DESIGN (no reactive "if it surfaces" handling). No consumer is rewired in this plan.

SEAM-01 interpretation (binding for the checker and the SAFE-17 verifier): Phase 239 satisfies SEAM-01 **structurally**. The Protocol is defined here; Plan 02 makes `RTTMeasurement` conform; steering (`SteeringDaemon`) and autorate (`autorate_continuous.py`) already construct and consume a concrete `RTTMeasurement`, which now *is* an `RttBackend`. No consumer is re-typed to the Protocol or otherwise rewired this phase — that would widen the SAFE-17 allowlist and is explicitly out of scope. The phase goal "a single RttBackend Protocol consumed by both steering and autorate" is met by the existing consumers already holding the conforming type, proven by an isinstance() test (Plan 02), not by an import-graph rewrite.

Output: `src/wanctl/rtt_backend.py` (new contract module) and `tests/test_rtt_backend.py` (new test file proving conformance, superset, snapshot equivalence, IRTT-mapping, and acyclic imports).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Use these directly; do not explore the codebase to rediscover them. -->

From src/wanctl/rtt_measurement.py (RTTSnapshot — UNCHANGED, the coercion target, defined at line 93):
  @dataclasses.dataclass(frozen=True, slots=True)
  class RTTSnapshot:
      rtt_ms: float
      per_host_results: dict[str, float | None]
      timestamp: float            # time.monotonic() when measured
      measurement_ms: float       # how long measurement took (ms)
      active_hosts: tuple[str, ...] = ()
      successful_hosts: tuple[str, ...] = ()

  NOTE: the existing live path returns NO snapshot when zero hosts succeed
  (BackgroundRTTThread._run keeps stale _cached; WANController.measure_rtt
  returns None on "All pings failed"). The Protocol mirrors this: probe()
  returns None on zero-success.

From src/wanctl/interfaces.py (Protocol house style, lines 11-13, 20-28):
  from __future__ import annotations
  from typing import Any, Protocol, runtime_checkable
  @runtime_checkable
  class X(Protocol):
      def method(self) -> ...: ...   # bodies are `...`, docstring names implementers

From src/wanctl/irtt_measurement.py (IRTTResult — the adapter source type, defined at line 27;
loss fields are in PERCENT 0..100 per the dataclass comments):
  rtt_mean_ms, rtt_median_ms, ipdv_mean_ms,
  send_loss (upstream_loss_percent), receive_loss (downstream_loss_percent),
  packets_sent, packets_received, server, port, timestamp, success
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define RttBackend Protocol + RttSample superset + pure IRTT mapping helper + IRTT adapter stub</name>
  <files>src/wanctl/rtt_backend.py</files>
  <read_first>
    - src/wanctl/interfaces.py (lines 1-30, 85-117) — Protocol house style: `from __future__ import annotations`, `@runtime_checkable`, `class X(Protocol)`, method bodies `...`, docstring names implementers
    - src/wanctl/rtt_measurement.py (lines 92-107) — RTTSnapshot field order/types, the strict-superset base and `.to_snapshot()` coercion target; lines 470-513 BackgroundRTTThread._run for the no-snapshot-on-zero-success precedent
    - src/wanctl/irtt_measurement.py (lines 27-46) — IRTTResult field set the IRTT helper maps FROM (loss fields are PERCENT 0..100)
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 22-121) — sub-patterns A/B/C with exact apply-to code
  </read_first>
  <behavior>
    - RttBackend is @runtime_checkable Protocol with one method `probe(self, hosts: list[str]) -> RttSample | None` (body `...`); isinstance() works structurally. Docstring states: None means "no successful measurement" (mirrors the existing no-snapshot-on-all-fail behavior); a returned RttSample always carries a real (non-None) rtt_ms.
    - RttSample is frozen+slots dataclass with the six RTTSnapshot fields in identical order/types (rtt_ms is a NON-optional float — a constructed RttSample always has a real rtt), plus backend: str = "icmplib", source_ip: str | None = None, per_host_loss: dict[str, float | None] = field(default_factory=dict). per_host_loss is documented as PERCENT loss per host: 0.0 = no loss, 100.0 = total loss; None = not measured. The icmplib default backend may leave per_host_loss empty/defaulted (filled when fping lands in Phase 241); the UNIT is fixed now.
    - RttSample.to_snapshot() returns RTTSnapshot populated only from the six subset fields (no superset fields leak)
    - sample_from_irtt_result(result: IRTTResult) -> RttSample is a PURE mapping function (no I/O, no thread): rtt_median_ms -> rtt_ms; server -> source_ip; combined send_loss/receive_loss -> a single per_host_loss entry keyed by server (percent units); timestamp -> timestamp; backend="irtt". It is unit-tested in Task 2.
    - IrttRttBackend.probe() stays UNWIRED: it raises NotImplementedError("IRTT-MIG-01"). The pure mapping helper above is what proves the Protocol absorbs IRTT (SEAM-04), not a raising-only stub.
  </behavior>
  <action>
    Create src/wanctl/rtt_backend.py. Start with `from __future__ import annotations` then `import dataclasses` and `from typing import Protocol, runtime_checkable, TYPE_CHECKING`. Under `if TYPE_CHECKING:` import the annotation-only types: `from wanctl.rtt_measurement import RTTSnapshot` and `from wanctl.irtt_measurement import IRTTResult`. Because `from __future__ import annotations` is present, every annotation is a string, so these TYPE_CHECKING-only imports satisfy type checkers without creating a module-load cycle.

    DESIGNED-ACYCLIC IMPORT RULE (HIGH-1): do NOT place an unconditional `from wanctl.rtt_measurement import RTTSnapshot` at module top level. The REAL runtime import of RTTSnapshot happens LOCALLY inside the `to_snapshot()` method body only. This breaks the rtt_backend <-> rtt_measurement load-order cycle by construction (Plan 02 mirrors this by importing RttSample locally inside probe()).

    Define `RttBackend` as a `@runtime_checkable` Protocol per SEAM-01 with the single method `probe(self, hosts: list[str]) -> "RttSample | None": ...`; docstring must (a) name the implementers (RTTMeasurement = icmplib default; fping Phase 241; IRTT adapter SEAM-04) and (b) state the zero-success contract: return None when no host yields a successful RTT (mirrors BackgroundRTTThread keeping stale _cached and WANController.measure_rtt returning None on all-fail), and return an RttSample with a real rtt_ms only when at least one host succeeded. Place the Protocol in THIS file, NOT in interfaces.py (per RESEARCH anti-pattern line 226 and Assumption A1 — putting it in interfaces.py would widen the SAFE-17 allowlist to a high-traffic shared file).

    Define `RttSample` as `@dataclasses.dataclass(frozen=True, slots=True)` per SEAM-03 with the six RTTSnapshot fields first in identical order/types (rtt_ms: float NON-optional, per_host_results, timestamp, measurement_ms, active_hosts=(), successful_hosts=()), then the strict-superset additions: backend (default "icmplib"), source_ip (default None), per_host_loss (dataclasses.field(default_factory=dict)). Add a field comment/docstring fixing per_host_loss units to PERCENT (0.0 no loss .. 100.0 total loss; None = not measured) so it is unambiguous when fping populates it. Add `to_snapshot(self) -> "RTTSnapshot"` whose BODY does the local `from wanctl.rtt_measurement import RTTSnapshot` and constructs RTTSnapshot from only the six subset fields. frozen+slots is load-bearing for the GIL-atomic pointer swap, not stylistic (PATTERNS Shared Patterns line 263).

    Define `sample_from_irtt_result(result: "IRTTResult") -> "RttSample"` per SEAM-04: a PURE, side-effect-free mapping function (no thread, no subprocess, no socket). Map rtt_median_ms -> rtt_ms, server -> source_ip, timestamp -> timestamp, backend="irtt", and combine send_loss/receive_loss into one per_host_loss entry keyed by result.server (percent units; e.g. the larger of send/receive loss, documented in the function docstring). per_host_results may be `{result.server: result.rtt_median_ms}`; measurement_ms may be 0.0 if not derivable. This function is unit-tested in Task 2 and is the real SEAM-04 proof.

    Define `IrttRttBackend` as the unwired adapter class: its `probe()` raises NotImplementedError("IRTT-MIG-01"). Do NOT wire IRTTThread; do NOT perform the migration (deferred IRTT-MIG-01 future requirement). The shape proof is sample_from_irtt_result(), not the raising stub.

    Do NOT touch interfaces.py, rtt_measurement.py, irtt_measurement.py, or wan_controller.py in this task. RttSample becomes the live carrier only later (Phase 244); in Phase 239 it is defined but not yet swapped into the publish path.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from wanctl.rtt_backend import RttBackend, RttSample, IrttRttBackend, sample_from_irtt_result; import dataclasses; f={x.name for x in dataclasses.fields(RttSample)}; assert {'rtt_ms','per_host_results','timestamp','measurement_ms','active_hosts','successful_hosts','backend','source_ip','per_host_loss'} <= f, f; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import wanctl.rtt_backend; import wanctl.rtt_measurement"` exits 0 AND `.venv/bin/python -c "import wanctl.rtt_measurement; import wanctl.rtt_backend"` exits 0 (BOTH import orders succeed — acyclic by design, HIGH-1)
    - `grep -nE '^(from|import) ' src/wanctl/rtt_backend.py | grep -v 'TYPE_CHECKING' | grep -c 'from wanctl.rtt_measurement import RTTSnapshot'` returns 0 (module-scope body has NO unconditional RTTSnapshot import; it lives only under TYPE_CHECKING and inside to_snapshot()) — assert with: the only `from wanctl.rtt_measurement import RTTSnapshot` lines are inside the `if TYPE_CHECKING:` block and the `to_snapshot` method body
    - `grep -n 'def probe' src/wanctl/rtt_backend.py` shows the Protocol method annotated `-> "RttSample | None"` (or `RttSample | None`) — the zero-success contract is in the signature (HIGH-2)
    - `grep -n 'class RttBackend(Protocol)' src/wanctl/rtt_backend.py` matches and the class carries `@runtime_checkable`
    - `grep -n 'frozen=True' src/wanctl/rtt_backend.py` and `grep -n 'slots=True' src/wanctl/rtt_backend.py` both match the RttSample dataclass decorator
    - `grep -n 'def sample_from_irtt_result' src/wanctl/rtt_backend.py` matches (pure IRTT mapping helper exists; SEAM-04)
    - `grep -n 'NotImplementedError("IRTT-MIG-01")' src/wanctl/rtt_backend.py` matches (unwired live-probe deferral marker)
    - per_host_loss unit is documented as percent in the source (`grep -niE 'per_host_loss.*percent|percent.*loss' src/wanctl/rtt_backend.py` returns >= 1)
    - `grep -c 'interfaces' src/wanctl/rtt_backend.py` returns 0 (Protocol NOT placed in interfaces.py; no interfaces.py edit)
    - RttSample fields are a strict superset of RTTSnapshot fields (the verify command assertion passes)
  </acceptance_criteria>
  <done>rtt_backend.py exists with the acyclic-by-design RttBackend Protocol (probe -> RttSample | None), RttSample superset dataclass with percent per_host_loss and a local-import to_snapshot(), the pure sample_from_irtt_result() helper, and the unwired IrttRttBackend; both import orders succeed; interfaces.py untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for conformance, superset, snapshot equivalence, IRTT mapping, acyclic imports</name>
  <files>tests/test_rtt_backend.py</files>
  <read_first>
    - tests/test_rtt_measurement.py (lines 1-40) — import block, MagicMock logger fixture, RTTMeasurement fixture structure
    - tests/helpers.py (line 18) — make_host_result deterministic per-host input helper
    - src/wanctl/rtt_backend.py — the module created in Task 1 (the system under test)
    - src/wanctl/rtt_measurement.py (lines 92-107) — RTTSnapshot for the hand-built equivalence comparison
    - src/wanctl/irtt_measurement.py (lines 27-46) — IRTTResult fields for the mapping test
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 224-257) — test-group spec
  </read_first>
  <behavior>
    - test_protocol_conformance (SEAM-01): an object exposing probe() satisfies isinstance(obj, RttBackend) via @runtime_checkable; a bare object without probe() does not
    - test_rttsample_superset_fields (SEAM-03): set of RttSample dataclass field names is a strict superset of RTTSnapshot field names and includes backend/source_ip/per_host_loss
    - test_rttsample_to_snapshot_byte_identical (SEAM-02): build an RttSample with deterministic values, assert .to_snapshot() == a hand-built RTTSnapshot with the same six subset values; frozen-dataclass == is field-wise so equality is the byte-identity proof
    - test_sample_from_irtt_result_mapping (SEAM-04): build an IRTTResult with deterministic fields, assert sample_from_irtt_result() maps rtt_median_ms->rtt_ms, server->source_ip, backend=="irtt", and per_host_loss[server] reflects the documented percent combination of send_loss/receive_loss
    - test_irtt_backend_unwired (SEAM-04): IrttRttBackend().probe([...]) raises NotImplementedError whose message contains "IRTT-MIG-01"
    - test_imports_acyclic_both_orders (HIGH-1): both `import wanctl.rtt_backend; import wanctl.rtt_measurement` and the reverse succeed in a fresh subprocess (return code 0)
  </behavior>
  <action>
    Create tests/test_rtt_backend.py mirroring the import/fixture style of tests/test_rtt_measurement.py (lines 1-40). Import RttBackend, RttSample, IrttRttBackend, sample_from_irtt_result from wanctl.rtt_backend; RTTSnapshot from wanctl.rtt_measurement; IRTTResult from wanctl.irtt_measurement. Use a MagicMock logger fixture where needed and tests/helpers.make_host_result for deterministic per-host inputs.

    Write the test groups named exactly: test_protocol_conformance, test_rttsample_superset_fields, test_rttsample_to_snapshot_byte_identical, test_sample_from_irtt_result_mapping, test_irtt_backend_unwired, test_imports_acyclic_both_orders (these names are referenced by the 239-RESEARCH Test Map and 239-VALIDATION). For the snapshot-equivalence test, assert `sample.to_snapshot() == RTTSnapshot(rtt_ms=..., per_host_results=..., timestamp=..., measurement_ms=..., active_hosts=..., successful_hosts=...)` using identical deterministic values, plus `isinstance(sample.to_snapshot(), RTTSnapshot)`. For conformance, assert `isinstance(<probe-bearing object>, RttBackend)` is True and a probe-less stand-in is False. For the IRTT mapping test, assert the concrete field mapping and percent-unit per_host_loss. For the acyclic-import test, use subprocess.run([sys.executable, "-c", "import wanctl.rtt_backend; import wanctl.rtt_measurement"]) and the reverse, asserting returncode == 0 for both.

    Do NOT modify tests/test_rtt_measurement.py in this task. Do NOT add live-network or icmplib socket calls — these are pure-data and shape assertions.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q` exits 0 with all six named tests collected and passing
    - `grep -c 'def test_rttsample_to_snapshot_byte_identical' tests/test_rtt_backend.py` returns 1
    - `grep -c 'def test_protocol_conformance\|def test_rttsample_superset_fields\|def test_sample_from_irtt_result_mapping\|def test_irtt_backend_unwired\|def test_imports_acyclic_both_orders' tests/test_rtt_backend.py` returns 5
    - test_imports_acyclic_both_orders runs BOTH import orders in subprocesses and asserts returncode 0 for each (HIGH-1 proof)
    - The snapshot-equivalence test uses RTTSnapshot `==` comparison (field-wise equality proof), not attribute-by-attribute getattr loops
    - No test opens a raw socket or calls icmplib.ping (pure-data assertions only)
  </acceptance_criteria>
  <done>tests/test_rtt_backend.py exists, the six tests pass, snapshot equivalence is proven via frozen-dataclass equality, the IRTT mapping helper is field-verified, and both import orders are proven acyclic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-refactor controller path → seam | The new contract type must not alter any value RTTSnapshot carries into the EWMA/scorer path |
| rtt_backend.py ↔ rtt_measurement.py | Module-load boundary: imports must be acyclic by design (TYPE_CHECKING + local import), not reactively patched |
| rtt_backend.py → interfaces.py | Placement boundary: keeping the Protocol out of interfaces.py keeps the SAFE-17 allowlist narrow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-01 | Tampering | RttSample.to_snapshot() | mitigate | Snapshot-equivalence test asserts frozen-dataclass `==` to a hand-built RTTSnapshot; any field drift fails the test |
| T-239-02 | Tampering | Protocol placement (interfaces.py) | mitigate | Protocol defined in rtt_backend.py only; `grep -c interfaces src/wanctl/rtt_backend.py == 0`; interfaces.py byte-unchanged keeps SAFE-17 allowlist tight (Plan 03 verifies) |
| T-239-03 | Elevation | IRTT adapter doing real migration | mitigate | IrttRttBackend.probe() raises NotImplementedError("IRTT-MIG-01"); only the pure sample_from_irtt_result() helper exists; no IRTTThread wiring; migration deferred |
| T-239-13 | Denial of service | circular import rtt_backend ↔ rtt_measurement | mitigate | Designed acyclic: RTTSnapshot under TYPE_CHECKING + local import inside to_snapshot(); test_imports_acyclic_both_orders proves both load orders exit 0; grep gate proves no module-scope unconditional import |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure-stdlib refactor; RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `.venv/bin/python -c "import wanctl.rtt_backend; import wanctl.rtt_measurement"` exits 0 AND the reverse order exits 0 (acyclic by design)
- `.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q` exits 0
- interfaces.py, rtt_measurement.py, irtt_measurement.py, wan_controller.py are byte-unchanged after this plan (confirmed by `git diff --name-only` showing only rtt_backend.py + tests/test_rtt_backend.py)
</verification>

<success_criteria>
- RttBackend Protocol (probe -> RttSample | None), RttSample superset dataclass, pure sample_from_irtt_result() helper, and unwired IrttRttBackend exist in src/wanctl/rtt_backend.py (SEAM-01, SEAM-03, SEAM-04)
- Imports are acyclic by design; both import orders exit 0
- RttSample is a strict superset of RTTSnapshot with percent-unit per_host_loss; .to_snapshot() proven byte-equal by test
- Six SEAM/import unit tests pass; no live consumer is rewired (SEAM-01 satisfied structurally)
- No file outside src/wanctl/rtt_backend.py and tests/test_rtt_backend.py is modified
</success_criteria>

<output>
Create `.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-01-SUMMARY.md` when done
</output>
</content>
</invoke>
