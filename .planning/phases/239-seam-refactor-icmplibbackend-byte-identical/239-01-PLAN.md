---
phase: 239-seam-refactor-icmplibbackend-byte-identical
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/rtt_backend.py
  - tests/test_rtt_backend.py
autonomous: true
requirements: [SEAM-01, SEAM-03, SEAM-04]
must_haves:
  truths:
    - "A single RttBackend Protocol type exists and is importable from one module"
    - "RttSample carries every RTTSnapshot field plus backend / source_ip / per_host_loss"
    - "RttSample.to_snapshot() returns a byte-equal RTTSnapshot for the subset fields"
    - "An IRTT adapter skeleton proves the Protocol can absorb IRTT without doing the migration"
  artifacts:
    - path: "src/wanctl/rtt_backend.py"
      provides: "RttBackend Protocol + RttSample dataclass + IrttRttBackend stub"
      contains: "class RttBackend"
      min_lines: 60
    - path: "tests/test_rtt_backend.py"
      provides: "Protocol conformance + superset + snapshot-equivalence + IRTT-shape tests"
      contains: "to_snapshot"
  key_links:
    - from: "src/wanctl/rtt_backend.py"
      to: "src/wanctl/rtt_measurement.py"
      via: "RttSample.to_snapshot() returns RTTSnapshot"
      pattern: "from wanctl.rtt_measurement import RTTSnapshot"
    - from: "src/wanctl/rtt_backend.py"
      to: "src/wanctl/irtt_measurement.py"
      via: "IrttRttBackend maps IRTTResult -> RttSample"
      pattern: "IRTTResult"
---

<objective>
Define the seam contract: a single `RttBackend` Protocol, an `RttSample` value type that is a strict superset of `RTTSnapshot`, and an IRTT adapter skeleton — all in the new `src/wanctl/rtt_backend.py`. This is the interface-first task: it creates the contracts that Plan 02 implements against and ships the unit tests that prove SEAM-01/03/04.

Purpose: Establishes the abstraction (SEAM-01), the metadata-carrying superset value type (SEAM-03), and the IRTT-absorption shape (SEAM-04) with zero behavior change to the live path. No consumer is rewired in this plan.

Output: `src/wanctl/rtt_backend.py` (new contract module) and `tests/test_rtt_backend.py` (new test file proving conformance, superset, snapshot equivalence, and IRTT adapter shape).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-RESEARCH.md
@.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Use these directly; do not explore the codebase to rediscover them. -->

From src/wanctl/rtt_measurement.py (RTTSnapshot — UNCHANGED, the coercion target, defined at line 93):
  @dataclasses.dataclass(frozen=True, slots=True)
  class RTTSnapshot:
      rtt_ms: float
      per_host_results: dict[str, float | None]
      timestamp: float            # time.monotonic() when measured
      measurement_ms: float       # how long measurement took (ms)
      active_hosts: tuple[str, ...] = ()
      successful_hosts: tuple[str, ...] = ()

From src/wanctl/interfaces.py (Protocol house style, lines 11-13, 20-28):
  from __future__ import annotations
  from typing import Any, Protocol, runtime_checkable
  @runtime_checkable
  class X(Protocol):
      def method(self) -> ...: ...   # bodies are `...`, docstring names implementers

From src/wanctl/irtt_measurement.py (IRTTResult — the adapter source type, defined at line 27):
  rtt_mean_ms, rtt_median_ms, ipdv_mean_ms, send_loss, receive_loss,
  packets_sent, packets_received, server, port, timestamp, success
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define RttBackend Protocol + RttSample superset + IRTT adapter stub</name>
  <files>src/wanctl/rtt_backend.py</files>
  <read_first>
    - src/wanctl/interfaces.py (lines 1-30, 85-117) — Protocol house style: `from __future__ import annotations`, `@runtime_checkable`, `class X(Protocol)`, method bodies `...`, docstring names implementers
    - src/wanctl/rtt_measurement.py (lines 92-107) — RTTSnapshot field order/types, the strict-superset base and `.to_snapshot()` coercion target
    - src/wanctl/irtt_measurement.py (lines 27-46) — IRTTResult field set the IRTT adapter maps FROM
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 22-121) — sub-patterns A/B/C with exact apply-to code
  </read_first>
  <behavior>
    - RttBackend is @runtime_checkable Protocol with one method `probe(self, hosts: list[str]) -> RttSample` (body `...`); isinstance() works structurally
    - RttSample is frozen+slots dataclass with the six RTTSnapshot fields in identical order/types, plus backend: str = "icmplib", source_ip: str | None = None, per_host_loss: dict[str, float | None] = field(default_factory=dict)
    - RttSample.to_snapshot() returns RTTSnapshot populated only from the six subset fields (no superset fields leak)
    - IrttRttBackend is a shape-proving stub: constructs the IRTTResult->RttSample field mapping (rtt_median_ms->rtt_ms, send_loss/receive_loss->per_host_loss, server->source_ip) then raises NotImplementedError("IRTT-MIG-01")
  </behavior>
  <action>
    Create src/wanctl/rtt_backend.py. Start with `from __future__ import annotations` then `import dataclasses` and `from typing import Protocol, runtime_checkable`. Import the coercion target with `from wanctl.rtt_measurement import RTTSnapshot` (and `IRTTResult` from `wanctl.irtt_measurement` for the adapter type hint only).

    Define `RttBackend` as a `@runtime_checkable` Protocol per SEAM-01 with the single method `probe(self, hosts: list[str]) -> "RttSample": ...`; docstring must name the implementers (RTTMeasurement = icmplib default; fping Phase 241; IRTT adapter SEAM-04). Place the Protocol in THIS file, NOT in interfaces.py (per RESEARCH anti-pattern line 226 and Assumption A1 — putting it in interfaces.py would widen the SAFE-17 allowlist to a high-traffic shared file).

    Define `RttSample` as `@dataclasses.dataclass(frozen=True, slots=True)` per SEAM-03 with the six RTTSnapshot fields first in identical order/types (rtt_ms, per_host_results, timestamp, measurement_ms, active_hosts=(), successful_hosts=()), then the strict-superset additions: backend (default "icmplib"), source_ip (default None), per_host_loss (dataclasses.field(default_factory=dict)). Add `to_snapshot(self) -> RTTSnapshot` that constructs RTTSnapshot from only the six subset fields. frozen+slots is load-bearing for the GIL-atomic pointer swap, not stylistic (PATTERNS Shared Patterns line 263).

    Define `IrttRttBackend` per SEAM-04 (Pattern 3 / Open Question 2): a shape-proving stub whose `probe()` builds the IRTTResult->RttSample mapping in a comment/expression then raises `NotImplementedError("IRTT-MIG-01")`. Do NOT wire IRTTThread; do NOT perform the migration (that is the deferred IRTT-MIG-01 future requirement). The stub proves the Protocol absorbs IRTT.

    Do NOT touch interfaces.py, rtt_measurement.py, irtt_measurement.py, or wan_controller.py in this task. RttSample becomes the live carrier only later (Phase 244); in Phase 239 it is defined but not yet swapped into the publish path.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from wanctl.rtt_backend import RttBackend, RttSample, IrttRttBackend; import dataclasses; f={x.name for x in dataclasses.fields(RttSample)}; assert {'rtt_ms','per_host_results','timestamp','measurement_ms','active_hosts','successful_hosts','backend','source_ip','per_host_loss'} <= f, f; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import wanctl.rtt_backend"` exits 0 (module imports cleanly under src layout, no reinstall needed)
    - `grep -n 'class RttBackend(Protocol)' src/wanctl/rtt_backend.py` matches and the class carries `@runtime_checkable`
    - `grep -n 'frozen=True' src/wanctl/rtt_backend.py` and `grep -n 'slots=True' src/wanctl/rtt_backend.py` both match the RttSample dataclass decorator
    - `grep -n 'NotImplementedError("IRTT-MIG-01")' src/wanctl/rtt_backend.py` matches (SEAM-04 deferral marker)
    - `grep -c 'interfaces' src/wanctl/rtt_backend.py` returns 0 (Protocol NOT placed in interfaces.py; no interfaces.py edit)
    - RttSample fields are a strict superset of RTTSnapshot fields (the verify command assertion passes)
  </acceptance_criteria>
  <done>rtt_backend.py exists with RttBackend Protocol, RttSample superset dataclass with to_snapshot(), and the IrttRttBackend stub; module imports clean; interfaces.py untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for conformance, superset, snapshot equivalence, IRTT shape</name>
  <files>tests/test_rtt_backend.py</files>
  <read_first>
    - tests/test_rtt_measurement.py (lines 1-40) — import block, MagicMock logger fixture, RTTMeasurement fixture structure
    - tests/helpers.py (line 18) — make_host_result deterministic per-host input helper
    - src/wanctl/rtt_backend.py — the module created in Task 1 (the system under test)
    - src/wanctl/rtt_measurement.py (lines 92-107) — RTTSnapshot for the hand-built equivalence comparison
    - .planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-PATTERNS.md (lines 224-257) — four test-group spec
  </read_first>
  <behavior>
    - test_protocol_conformance (SEAM-01): RttSample-producing object satisfies isinstance(obj, RttBackend) via @runtime_checkable; a bare object without probe() does not
    - test_rttsample_superset_fields (SEAM-03): set of RttSample dataclass field names is a strict superset of RTTSnapshot field names and includes backend/source_ip/per_host_loss
    - test_rttsample_to_snapshot_byte_identical (SEAM-02): build an RttSample with deterministic values, assert .to_snapshot() == a hand-built RTTSnapshot with the same six subset values; frozen-dataclass == is field-wise so equality is the byte-identity proof
    - test_irtt_adapter_shape (SEAM-04): IrttRttBackend().probe([...]) raises NotImplementedError whose message contains "IRTT-MIG-01"
  </behavior>
  <action>
    Create tests/test_rtt_backend.py mirroring the import/fixture style of tests/test_rtt_measurement.py (lines 1-40). Import RttBackend, RttSample, IrttRttBackend from wanctl.rtt_backend and RTTSnapshot from wanctl.rtt_measurement. Use a MagicMock logger fixture where needed and tests/helpers.make_host_result for deterministic per-host inputs.

    Write the four test groups named exactly: test_protocol_conformance, test_rttsample_superset_fields, test_rttsample_to_snapshot_byte_identical, test_irtt_adapter_shape (these names are referenced by the 239-RESEARCH Test Map and 239-VALIDATION). For the snapshot-equivalence test, assert `sample.to_snapshot() == RTTSnapshot(rtt_ms=..., per_host_results=..., timestamp=..., measurement_ms=..., active_hosts=..., successful_hosts=...)` using identical deterministic values, plus `isinstance(sample.to_snapshot(), RTTSnapshot)`. For conformance, assert `isinstance(<probe-bearing object>, RttBackend)` is True and a probe-less stand-in is False.

    Do NOT modify tests/test_rtt_measurement.py in this task. Do NOT add live-network or icmplib socket calls — these are pure-data and shape assertions.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q` exits 0 with all four named tests collected and passing
    - `grep -c 'def test_rttsample_to_snapshot_byte_identical' tests/test_rtt_backend.py` returns 1
    - `grep -c 'def test_protocol_conformance\|def test_rttsample_superset_fields\|def test_irtt_adapter_shape' tests/test_rtt_backend.py` returns 3
    - The snapshot-equivalence test uses RTTSnapshot `==` comparison (field-wise equality proof), not attribute-by-attribute getattr loops
    - No test opens a raw socket or calls icmplib.ping (pure-data assertions only)
  </acceptance_criteria>
  <done>tests/test_rtt_backend.py exists, the four SEAM tests pass, snapshot equivalence is proven via frozen-dataclass equality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-refactor controller path → seam | The new contract type must not alter any value RTTSnapshot carries into the EWMA/scorer path |
| rtt_backend.py → interfaces.py | Placement boundary: keeping the Protocol out of interfaces.py keeps the SAFE-17 allowlist narrow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-01 | Tampering | RttSample.to_snapshot() | mitigate | Snapshot-equivalence test asserts frozen-dataclass `==` to a hand-built RTTSnapshot; any field drift fails the test |
| T-239-02 | Tampering | Protocol placement (interfaces.py) | mitigate | Protocol defined in rtt_backend.py only; `grep -c interfaces src/wanctl/rtt_backend.py == 0`; interfaces.py byte-unchanged keeps SAFE-17 allowlist tight (Plan 03 verifies) |
| T-239-03 | Elevation | IRTT adapter doing real migration | mitigate | IrttRttBackend is a NotImplementedError("IRTT-MIG-01") stub; no IRTTThread wiring; migration explicitly deferred |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure-stdlib refactor; RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `.venv/bin/python -c "import wanctl.rtt_backend"` exits 0
- `.venv/bin/pytest -o addopts='' tests/test_rtt_backend.py -q` exits 0
- interfaces.py, rtt_measurement.py, irtt_measurement.py, wan_controller.py are byte-unchanged after this plan (confirmed by `git diff --name-only` showing only rtt_backend.py + tests/test_rtt_backend.py)
</verification>

<success_criteria>
- RttBackend Protocol, RttSample superset dataclass, and IrttRttBackend stub exist in src/wanctl/rtt_backend.py (SEAM-01, SEAM-03, SEAM-04)
- RttSample is a strict superset of RTTSnapshot and .to_snapshot() is proven byte-equal by test
- Four SEAM unit tests pass; no live consumer is rewired
- No file outside src/wanctl/rtt_backend.py and tests/test_rtt_backend.py is modified
</success_criteria>

<output>
Create `.planning/phases/239-seam-refactor-icmplibbackend-byte-identical/239-01-SUMMARY.md` when done
</output>
