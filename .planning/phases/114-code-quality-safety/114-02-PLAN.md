---
phase: 114-code-quality-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/114-code-quality-safety/114-02-mypy-probe.md
  - .planning/phases/114-code-quality-safety/114-02-complexity-analysis.md
  - .planning/phases/114-code-quality-safety/114-02-import-graph.md
autonomous: true
requirements:
  - CQUAL-03
  - CQUAL-05
  - CQUAL-07

must_haves:
  truths:
    - "MyPy disallow_untyped_defs has been probed on 5 leaf modules with per-module results documented"
    - "Top 5 complexity hotspot files have extraction recommendations documented"
    - "Import graph has been analyzed for circular dependencies"
  artifacts:
    - path: ".planning/phases/114-code-quality-safety/114-02-mypy-probe.md"
      provides: "Per-module mypy strictness probe results"
      contains: "disallow_untyped_defs"
    - path: ".planning/phases/114-code-quality-safety/114-02-complexity-analysis.md"
      provides: "Complexity hotspot analysis with extraction recommendations"
      contains: "extraction"
    - path: ".planning/phases/114-code-quality-safety/114-02-import-graph.md"
      provides: "Import graph circular dependency analysis"
      contains: "circular"
  key_links:
    - from: "114-02-complexity-analysis.md"
      to: "112-03-findings.md"
      via: "References Phase 112 complexity baseline"
      pattern: "C901|complexity"
---

<objective>
Probe MyPy strictness on 5 leaf modules, analyze complexity hotspots in top 5 files, and check import graph for circular dependencies. All document-only -- no code changes in this plan.

Purpose: Establish baselines for future strictness migration (CQUAL-03), document extraction opportunities for v1.23 (CQUAL-05 per D-10), and identify import cycle risks (CQUAL-07).

Output: Three findings documents (mypy probe, complexity analysis, import graph).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-code-quality-safety/114-CONTEXT.md
@.planning/phases/112-foundation-scan/112-03-findings.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MyPy strictness probe + import graph analysis</name>
  <files>
    .planning/phases/114-code-quality-safety/114-02-mypy-probe.md
    .planning/phases/114-code-quality-safety/114-02-import-graph.md
  </files>
  <read_first>
    pyproject.toml
    src/wanctl/path_utils.py
    src/wanctl/rate_utils.py
    src/wanctl/timeouts.py
    src/wanctl/pending_rates.py
    src/wanctl/daemon_utils.py
    .planning/phases/114-code-quality-safety/114-CONTEXT.md
  </read_first>
  <action>
    **MyPy Probe (CQUAL-03, per D-04/D-05/D-06):**

    Probe these 5 leaf modules (smallest, most self-contained, fewest cross-file imports per D-05):
    1. `src/wanctl/path_utils.py` (124 LOC, 0 wanctl imports)
    2. `src/wanctl/rate_utils.py` (227 LOC, 0 wanctl imports)
    3. `src/wanctl/timeouts.py` (152 LOC, 0 wanctl imports)
    4. `src/wanctl/pending_rates.py` (76 LOC, 0 wanctl imports)
    5. `src/wanctl/daemon_utils.py` (41 LOC, 0 wanctl imports)

    For each module, run:
    ```bash
    .venv/bin/mypy --disallow-untyped-defs --no-error-summary src/wanctl/{module}.py
    ```

    Create `114-02-mypy-probe.md`:
    ```
    # MyPy Strictness Probe (CQUAL-03)

    ## Methodology
    - Flag: `disallow_untyped_defs` only (per D-04)
    - 5 leaf modules selected (per D-05): smallest, self-contained, zero intra-package imports

    ## Results

    | Module | LOC | Errors | Pass/Fail | Strategy |
    |--------|-----|--------|-----------|----------|
    | path_utils.py | 124 | N | pass/fail | fix/suppress |

    ## Per-Module Detail

    ### path_utils.py
    - Errors: (list each error)
    - Strategy: fix (add annotations) / suppress (type: ignore)
    - Effort estimate: trivial/moderate/significant

    ## Recommendation for v1.23
    - Modules ready for strict: [list]
    - Modules needing work: [list]
    - Suggested migration order: [list]
    ```

    Per D-06: Do NOT add type annotations. Document only.

    **Import Graph (CQUAL-07, per D-16/D-17):**

    Analyze import graph using grep:
    ```bash
    grep -rn "^from wanctl\.\|^import wanctl\." src/wanctl/ --include="*.py"
    ```

    Build adjacency list from the output. Check for cycles by tracing import chains.

    Create `114-02-import-graph.md`:
    ```
    # Import Graph Analysis (CQUAL-07)

    ## Methodology
    - Extracted all `from wanctl.X import` and `import wanctl.X` statements
    - Built directed dependency graph
    - Checked for circular import chains

    ## Circular Dependencies Found
    (list any cycles with the full chain: A -> B -> C -> A)
    (or "None found")

    ## Import Statistics
    - Total modules: N
    - Total import edges: N
    - Most imported module: X (imported by N modules)
    - Most importing module: X (imports N modules)

    ## Hub Modules (imported by 5+ modules)
    | Module | Imported by |
    |--------|-------------|

    ## Recommendations for v1.23
    (if cycles found: describe cycle, recommend fix approach per D-17)
    (if no cycles: note health of import graph)
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/114-code-quality-safety/114-02-mypy-probe.md && test -f .planning/phases/114-code-quality-safety/114-02-import-graph.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - 114-02-mypy-probe.md exists with results for exactly 5 modules
    - Each module has pass/fail status, error count, and strategy (fix or suppress)
    - 114-02-import-graph.md exists with adjacency analysis
    - Circular dependency section present (even if none found)
    - grep "disallow_untyped_defs" 114-02-mypy-probe.md returns a match
  </acceptance_criteria>
  <done>MyPy probe completed for 5 leaf modules with per-module strategy documented. Import graph analyzed for circular dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Complexity hotspot analysis</name>
  <files>
    .planning/phases/114-code-quality-safety/114-02-complexity-analysis.md
  </files>
  <read_first>
    .planning/phases/112-foundation-scan/112-03-findings.md
    src/wanctl/autorate_continuous.py
    src/wanctl/steering/daemon.py
    src/wanctl/check_config.py
    src/wanctl/check_cake.py
    src/wanctl/calibrate.py
  </read_first>
  <action>
    Analyze top 5 files by LOC per D-12:
    1. `autorate_continuous.py` (4,342 LOC)
    2. `steering/daemon.py` (2,411 LOC)
    3. `check_config.py` (1,472 LOC)
    4. `check_cake.py` (1,249 LOC)
    5. `calibrate.py` (1,131 LOC)

    For each file, use the Phase 112 complexity baseline from 112-03-findings.md (16 functions >15, 4 >20) as the starting point per D-11.

    For each file, analyze:
    1. **Responsibility inventory**: What distinct responsibilities does this file contain? (e.g., autorate_continuous.py: Config loading, QueueController FSM, WANController cycle, main() CLI)
    2. **High-complexity functions**: List from 112-03 baseline, describe what makes them complex (nested conditionals, state branches, early returns)
    3. **Extraction candidates**: Which responsibilities could be moved to separate modules? What would the new module boundary look like?
    4. **Risk assessment**: What is the risk of extracting each candidate? (behavioral regression in 50ms loop = high risk; CLI argument parsing = low risk)

    Create `114-02-complexity-analysis.md`:
    ```
    # Complexity Hotspot Analysis (CQUAL-05)

    ## Summary
    - Files analyzed: 5 (top by LOC)
    - Total LOC: ~10,605
    - Functions exceeding complexity 15: 16 (from Phase 112 baseline)
    - Functions exceeding complexity 20: 4

    ## Per-File Analysis

    ### autorate_continuous.py (4,342 LOC)

    #### Responsibilities
    1. Config class (lines X-Y) -- config loading and validation
    2. QueueController class (lines X-Y) -- bandwidth state machine
    3. WANController class (lines X-Y) -- control loop orchestration
    4. main() (lines X-Y) -- CLI entry point + daemon setup

    #### High-Complexity Functions
    | Function | Complexity | Lines | Why complex |
    |----------|-----------|-------|-------------|

    #### Extraction Recommendations (deferred to v1.23 per D-10)
    | Candidate | Target module | Risk | Benefit |
    |-----------|---------------|------|---------|

    (repeat for each file)

    ## Priority Ranking
    1. [highest impact extraction]
    2. ...

    ## v1.23 Recommendation
    (suggested order of extraction, estimated effort, risk mitigation)
    ```

    Per D-10: Document only. No code changes. Recommendations deferred to v1.23.
  </action>
  <verify>
    <automated>test -f .planning/phases/114-code-quality-safety/114-02-complexity-analysis.md && grep -c "extraction\|Extraction" .planning/phases/114-code-quality-safety/114-02-complexity-analysis.md</automated>
  </verify>
  <acceptance_criteria>
    - 114-02-complexity-analysis.md exists
    - All 5 files analyzed (autorate_continuous.py, steering/daemon.py, check_config.py, check_cake.py, calibrate.py)
    - Each file has responsibility inventory, high-complexity functions, and extraction recommendations
    - References Phase 112 complexity baseline numbers
    - grep "deferred to v1.23" returns a match (confirming document-only scope)
  </acceptance_criteria>
  <done>Top 5 complexity hotspot files analyzed with extraction recommendations documented for v1.23. No code changes made.</done>
</task>

</tasks>

<verification>
1. Three findings files exist in `.planning/phases/114-code-quality-safety/`
2. MyPy probe covers exactly 5 leaf modules with per-module results
3. Import graph analysis has circular dependency section
4. Complexity analysis covers all 5 files with extraction recommendations
5. No source code files modified (document-only plan)
</verification>

<success_criteria>
- MyPy probe completed for 5 leaf modules with fix/suppress strategy per module
- Import graph analyzed with circular dependencies documented (or confirmed absent)
- Top 5 complexity hotspot files analyzed with extraction recommendations
- All findings reference Phase 112 baseline where applicable
- No source code changes (per D-06, D-10, D-16)
</success_criteria>

<output>
After completion, create `.planning/phases/114-code-quality-safety/114-02-SUMMARY.md`
</output>
