---
phase: 261-pre-flip-deploy-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - 261-01
  - 261-02
files_modified:
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-observation-raw.json
autonomous: false
requirements:
  - RECON-03
user_setup: []

must_haves:
  truths:
    - "The post-deploy :9102 smoke assertion PASSES all four checks: route_management.mode==dry_run, route_management.active_owner==netwatch, ownership_inspection.inspector_status==ok, ownership_inspection.match==true; all expected services active"
    - "The smoke gate is the binary PASS/FAIL for RECON-03 and asserts mode explicitly (catching any steering.yaml clobber that active_owner alone would miss)"
    - "The smoke gate rejects stale health: ownership_inspection.last_inspected_at is proven newer than the Plan 02 restart boundary via --min-inspected-after (MEDIUM)"
    - "AFTER the gate passes, the Phase 260 observation harness rerun on the reconciled on-host tree still yields OBSERVE_VERDICT: ready-for-approval with MUTATION_TOKEN_HITS empty (confirmatory evidence, not a second gate)"
    - "The confirmatory harness is staged OUTSIDE the deploy-managed /opt/wanctl tree (under /var/lib/wanctl/phase261) and cleaned up, so it does not pollute the repo==prod surface (LOW)"
    - "No ownership behavior changed: Netwatch remains owner, mode stays dry_run, SAFE-22 holds at the phase boundary"
  artifacts:
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md"
      provides: "D-04 PASS/FAIL gate result + raw :9102 JSON + units-active check + freshness proof"
      contains: "PASS route_management.mode==dry_run"
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md"
      provides: "Confirmatory ready-for-approval packet on the reconciled tree"
      contains: "OBSERVE_VERDICT: ready-for-approval"
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-observation-raw.json"
      provides: "Raw harness output for the confirmatory rerun"
  key_links:
    - from: "scripts/phase261-smoke-assertion.py"
      to: ":9102/health post-reconcile state"
      via: "curl + sibling-key extraction, mode asserted explicitly, freshness via --min-inspected-after"
      pattern: "PASS route_management.mode==dry_run"
    - from: "scripts/phase260-observation.py staged on-host outside /opt/wanctl"
      to: "reconciled /opt/wanctl tree"
      via: "_assert_deployed_imports + bounded observation -> OBSERVE_VERDICT"
      pattern: "OBSERVE_VERDICT: ready-for-approval"
---

<objective>
Run the post-deploy clean-proof for RECON-03: the targeted :9102 smoke assertion is the binary
PASS/FAIL gate (D-04, now rejecting stale health), and AFTER it passes, re-run the Phase 260
observation harness on the reconciled on-host tree (staged outside /opt/wanctl) as confirmatory
evidence that ready-for-approval still holds.

Purpose: RECON-03 requires the route-management surface + :9102 health to come up clean in the
existing dry-run/safe state after the reconcile — proving the deploy changed NO ownership behavior.
The v1.57 readiness packet was produced on the pre-reconcile prod; re-validating it on the freshly
deployed tree is the point. The smoke gate explicitly asserts mode=="dry_run" to catch any
steering.yaml clobber (active_owner is "netwatch" for both off and dry_run) and requires the
inspector reading to be fresher than the restart boundary so a stale-but-ok sample cannot pass.

Output: the smoke-assertion gate result (all four checks PASS + units active + freshness proof), the
confirmatory ready-for-approval packet, and the raw harness JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-pre-flip-deploy-reconciliation/261-CONTEXT.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-RESEARCH.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-PATTERNS.md
@.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md
@.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readonly-commands.txt

<interfaces>
D-04 smoke gate (the RECON-03 PASS/FAIL): all four checks must PASS —
  route_management.mode == "dry_run"        (CRITICAL: assert explicitly; clobber detector)
  route_management.active_owner == "netwatch"
  ownership_inspection.inspector_status == "ok"
  ownership_inspection.match is True
PLUS all expected units active: steering.service, cake-autorate-{spectrum,att}.service,
cake-autorate-{spectrum,att}-state-bridge.service. route_management and ownership_inspection are
SIBLING top-level keys in :9102/health (never nested). Pitfall 5: if :9102 returns
{"status":"starting"}, re-poll until ready before asserting.

Health freshness (MEDIUM): the smoke script's --min-inspected-after EPOCH flag (added in Plan 01)
requires ownership_inspection.last_inspected_at to be STRICTLY NEWER than the supplied epoch. Pass
the Plan 02 restart-boundary epoch (recorded in phase261-deploy-restart.md) so a stale-but-ok
inspector reading taken before the restart cannot pass the gate.

Confirmatory harness (D-04, NOT a second gate) — scripts/phase260-observation.py reused AS-IS:
- It enforces _assert_deployed_imports (imports must resolve under /opt/wanctl) -> MUST run on-host
  against the reconciled tree (that is the point — re-validate the freshly deployed tree). Running it
  with cwd OUTSIDE /opt/wanctl still resolves wanctl imports from /opt/wanctl (the guard checks the
  import path, not cwd), so the harness can be staged under /var/lib/wanctl/phase261 without weakening
  the guard.
- It is NOT in deploy.sh's installed set, so the full deploy did NOT place it under
  /opt/wanctl/scripts -> the plan must STAGE scripts/phase260-observation.py + the Plan-01
  phase261-readonly-commands.txt onto the host, OUTSIDE the deploy-managed tree (LOW: stage under
  /var/lib/wanctl/phase261, not /opt/wanctl/scripts, so it cannot pollute the repo==prod surface;
  clean it up after).
- CLI: positional command_file, --config /etc/wanctl/steering.yaml,
  --health-url http://127.0.0.1:9102/health, --window-sec 636, --interval-sec 60, --evidence-dir.
- Emits OBSERVE_VERDICT: ready-for-approval (exit 0) ONLY on zero divergences AND zero
  mutation-token hits (MUTATION_TOKEN_HITS: []); preserves Phase 260's all-samples-clean /
  authoritative-signal rule. Anything else -> not-ready (exit 1).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: D-04 smoke-assertion gate on the reconciled :9102 health, with stale-health rejection (RECON-03 PASS/FAIL)</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md</files>
  <read_first>
    - scripts/phase261-smoke-assertion.py (the gate built in Plan 01, incl. --min-inspected-after)
    - 261-RESEARCH.md Code Examples §Smoke gate + Pitfall 1 (mode is the clobber discriminator) + Pitfall 5 (readiness timing)
    - 261-PATTERNS.md "scripts/phase261-smoke-assertion.py" section (gate fields, field provenance, fail-closed sibling extraction)
    - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md (confirm steering.yaml was restored before this runs; read the restart-boundary epoch for --min-inspected-after)
  </read_first>
  <action>
    Run scripts/phase261-smoke-assertion.py against the reconciled cake-shaper :9102/health
    (read-only). This is the RECON-03 binary gate. Depends on Plan 02 (the deploy + steering.yaml
    restore must have completed); if the deploy-restart evidence is absent, STOP.

    Assert all four checks: route_management.mode=="dry_run" (EXPLICITLY — this is the steering.yaml
    clobber detector; active_owner is "netwatch" for both off and dry_run so it cannot catch a
    clobber), route_management.active_owner=="netwatch", ownership_inspection.inspector_status=="ok",
    ownership_inspection.match is True. Read route_management and ownership_inspection as SIBLING
    keys. Use the bounded readiness poll (Pitfall 5) — if :9102 reports {"status":"starting"},
    re-poll until status in (healthy,degraded) AND inspector_status=="ok" AND a fresh
    last_inspected_at before asserting; fail closed on timeout.

    STALE-HEALTH REJECTION (MEDIUM): pass --min-inspected-after <restart-boundary epoch> (read from
    phase261-deploy-restart.md). The gate must additionally require
    ownership_inspection.last_inspected_at to be STRICTLY NEWER than that epoch, failing closed if the
    field is missing/unparseable/older. This proves the gate is reading a POST-restart inspection, not
    a stale-but-ok sample from before the restart.

    Also assert all expected units active: steering.service, cake-autorate-{spectrum,att}.service,
    cake-autorate-{spectrum,att}-state-bridge.service.

    Write phase261-smoke-assertion.md with the per-check PASS/FAIL lines, the units-active result,
    the freshness check (last_inspected_at vs the restart-boundary epoch), and the RAW :9102 JSON
    (host IP/credential excluded). The gate PASSES iff all four checks PASS AND the freshness check
    PASSES AND all units active (exit 0). If mode != dry_run, the steering.yaml restore failed —
    STOP, re-restore the route_management block and re-restart steering before re-running. Do NOT
    proceed to Task 2 unless this gate is green.

    checkpoint:human-action because it reads the live production host post-deploy and is the gating
    decision; the operator runs the smoke assertion and pastes the result + raw JSON.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md && grep -q 'PASS route_management.mode==dry_run' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md && ! grep -q '^FAIL ' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md && grep -qi 'last_inspected_at' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md && echo SMOKE_GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - phase261-smoke-assertion.md shows PASS for all four checks: route_management.mode==dry_run,
      route_management.active_owner==netwatch, ownership_inspection.inspector_status==ok,
      ownership_inspection.match==true; no FAIL line present.
    - The freshness check is recorded: ownership_inspection.last_inspected_at is strictly newer than
      the Plan 02 restart-boundary epoch (--min-inspected-after); a stale reading would FAIL (MEDIUM).
    - All expected units reported active (steering + both shaper + both state-bridge).
    - Raw :9102 JSON is recorded; no host IP/credential present.
    - mode was asserted explicitly (the evidence shows the literal dry_run check, not just active_owner).
  </acceptance_criteria>
  <done>The D-04 smoke gate is green on the reconciled tree with a fresh (post-restart) inspector reading: dry-run/safe state intact, no ownership behavior changed; RECON-03 gate satisfied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Confirmatory Phase 260 harness rerun on the reconciled tree, staged outside /opt/wanctl (D-04 evidence)</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md, .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-observation-raw.json</files>
  <read_first>
    - scripts/phase260-observation.py (reused AS-IS; _assert_deployed_imports, compute_verdict, OBSERVE_VERDICT token + exit coupling, CLI defaults --window-sec 636 --interval-sec 60)
    - 261-RESEARCH.md Code Examples §Confirmatory rerun + the staging caveat (harness NOT deploy-installed)
    - 261-PATTERNS.md "Reuse verbatim ... confirmatory harness rerun" + verdict-token block to embed
    - .planning/milestones/v1.57-phases/260-dry-run-observation-rerun-canary-readiness/260-CONTEXT.md (all-samples-clean / authoritative-signal rule the rerun must preserve)
    - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-smoke-assertion.md (Task 1 gate must be green first)
  </read_first>
  <action>
    Re-run the Phase 260 observation harness on the reconciled on-host tree as CONFIRMATORY evidence
    that ready-for-approval still holds (this is NOT a second gate — the smoke assertion in Task 1
    is the gate). Only run after Task 1's gate is green.

    1. STAGE OUTSIDE /opt/wanctl (LOW): copy scripts/phase260-observation.py and the Plan-01
       phase261-readonly-commands.txt onto cake-shaper under a NON-deploy-managed dir
       /var/lib/wanctl/phase261/ (mkdir -p). Do NOT stage under /opt/wanctl/scripts — that would
       pollute the repo==prod surface the audit just proved. The harness's _assert_deployed_imports
       guard checks the IMPORT path (wanctl must resolve under /opt/wanctl), not cwd, so running it
       from /var/lib/wanctl/phase261 still re-validates the reconciled /opt/wanctl tree.

    2. RUN on-host from the staging dir (imports still resolve under /opt/wanctl — re-validating the
       freshly reconciled tree, which is the point):
         cd /var/lib/wanctl/phase261 && sudo python3 phase260-observation.py phase261-readonly-commands.txt \
           --config /etc/wanctl/steering.yaml --health-url http://127.0.0.1:9102/health \
           --evidence-dir <evidence-dir>
       Use the harness defaults (--window-sec 636 --interval-sec 60). The harness preserves Phase
       260's all-samples-clean / authoritative-signal rule: ready-for-approval (exit 0) ONLY on zero
       divergences AND MUTATION_TOKEN_HITS empty.

    3. CLEAN UP (LOW): after the run, remove the staged files (rm -rf /var/lib/wanctl/phase261) so no
       harness artifact lingers on the host; note the cleanup in the evidence. (The anchor backups
       under /var/lib/wanctl/phase261-backups are a DIFFERENT dir and must be kept.)

    4. Capture phase261-observation-raw.json (harness raw) and write phase261-readiness-packet.md
       mirroring the Phase 260 packet header convention: top Verdict line, the verbatim verdict-token
       block (OBSERVE_VERDICT: ready-for-approval, NETWATCH_REMAINS_OWNER: true, NO_ROUTE_OWNER_FLIP:
       true, NO_NETWATCH_MUTATION: true, NO_CAKE_QDISC_CHANGE: true, MUTATION_TOKEN_HITS: []), an
       Evidence Inputs list, and a SAFE-22 block. Host IP/credential excluded.

    If the verdict is not-ready, the reconciled tree diverges or a mutation token fired — STOP and
    investigate; do not paper over it (RECON-03 confirmatory evidence must be ready-for-approval).

    checkpoint:human-action because it runs sudo python3 on the live production host (read-only
    bounded observation); the operator stages, runs, cleans up, and pastes the verdict + raw JSON.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md && grep -q 'OBSERVE_VERDICT: ready-for-approval' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md && grep -q 'MUTATION_TOKEN_HITS: \[\]' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readiness-packet.md && test -s .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-observation-raw.json && echo CONFIRMATORY_READY</automated>
  </verify>
  <acceptance_criteria>
    - phase261-readiness-packet.md contains OBSERVE_VERDICT: ready-for-approval and
      MUTATION_TOKEN_HITS: [] (zero divergences, zero mutation-token hits) on the reconciled tree.
    - phase261-observation-raw.json exists and is non-empty (harness raw output).
    - The packet records NETWATCH_REMAINS_OWNER: true and NO_ROUTE_OWNER_FLIP: true (SAFE-22 held).
    - The harness ran on-host against /opt/wanctl (deployed-import guard satisfied) but was STAGED
      under /var/lib/wanctl/phase261 (NOT /opt/wanctl/scripts) and cleaned up afterward; evidence
      notes the staging + cleanup so the repo==prod surface is not polluted (LOW).
    - No host IP/credential present.
  </acceptance_criteria>
  <done>Confirmatory rerun on the reconciled tree yields ready-for-approval with no mutation-token hits; harness staged outside the deploy-managed tree and cleaned up; RECON-03 confirmatory evidence captured; SAFE-22 holds at the phase boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation -> cake-shaper (SSH) | Read-only :9102 reads + on-host harness run cross here |
| :9102 health payload -> smoke gate | A clobbered dry-run state OR a stale reading must be caught, not silently passed |
| staged harness -> /opt/wanctl repo==prod surface | The harness must not be placed inside the deploy-managed tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-09 | Tampering / DoS-of-safety | smoke gate passing despite a steering.yaml clobber | mitigate | Gate asserts route_management.mode=="dry_run" EXPLICITLY (active_owner is "netwatch" for both off and dry_run) — the discriminating field |
| T-261-10 | Spoofing of readiness | asserting :9102 before the daemon finished restarting, or on a stale pre-restart reading (false PASS) | mitigate | Bounded readiness poll until status healthy/degraded + inspector_status ok (Pitfall 5) AND --min-inspected-after requires last_inspected_at newer than the restart boundary (MEDIUM), fail closed |
| T-261-11 | Repudiation / stale evidence | confirmatory verdict run against the OLD tree, not the reconciled one | mitigate | Harness _assert_deployed_imports forces import resolution under /opt/wanctl even when staged elsewhere; stage explicitly + re-validate the reconciled tree |
| T-261-16 | Tampering | staged harness polluting the audited /opt/wanctl repo==prod surface | mitigate | Stage under /var/lib/wanctl/phase261 (not /opt/wanctl/scripts) and rm -rf after the run (LOW) |
| T-261-12 | Information Disclosure | host IP/credential leaking into evidence | mitigate | Smoke/packet exclude host IP/cred; secrets out of scope (MEMORY: prior leak) |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses existing harness + host coreutils only |
</threat_model>

<verification>
- Smoke gate green: all four checks PASS, units active, mode asserted explicitly (no clobber), last_inspected_at fresher than the restart boundary (no stale reading).
- Confirmatory harness rerun on the reconciled on-host tree (staged outside /opt/wanctl, cleaned up) -> ready-for-approval, MUTATION_TOKEN_HITS empty.
- Netwatch remains owner; mode stays dry_run; SAFE-22 holds at the phase boundary.
</verification>

<success_criteria>
- RECON-03 satisfied: route-management surface + :9102 health clean in the existing dry-run/safe
  state (smoke gate, freshness-checked), confirmed by the ready-for-approval harness rerun on the reconciled tree.
- No ownership behavior changed by the reconcile; phase boundary SAFE-22-clean.
</success_criteria>

<output>
Create `.planning/phases/261-pre-flip-deploy-reconciliation/261-03-SUMMARY.md` when done
</output>
</content>
