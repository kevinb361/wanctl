---
phase: 261-pre-flip-deploy-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - 261-01
files_modified:
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-audit.md
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-repo.txt
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-prod.txt
autonomous: false
requirements:
  - RECON-01
user_setup: []

must_haves:
  truths:
    - "The full deploy.sh pipeline (both per-WAN cake-autorate invocations + steering) ran end-to-end against cake-shaper, retiring the surgical single-file rsync shortcut"
    - "The host /etc/wanctl/steering.yaml route_management dry-run block survived the deploy (backed up before the steering scp, restored before the steering restart)"
    - "Service restarts were sequenced with steering LAST, each restart health-gated on :9102 before proceeding; only -state-bridge + steering units restarted (NOT the cake-autorate shaper units)"
    - "A per-file sha256 audit proves /opt/wanctl == repo over the D-01 set (PHASE261_AUDIT_SRC_TREE_EQUAL); the deploy-managed scripts subset matches; the .phase259-backup-* dir is swept; the stale extra script disposition is recorded"
  artifacts:
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-audit.md"
      provides: "D-01 per-file sha256 audit verdict (repo==prod) + scripts-subset result + extras disposition"
      contains: "PHASE261_AUDIT_SRC_TREE_EQUAL"
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md"
      provides: "D-03 deploy transcript + steering.yaml preserve/restore + sequenced-restart + :9102 gates"
      contains: "steering"
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-prod.txt"
      provides: "Raw prod-side sha256 manifest of /opt/wanctl src tree"
  key_links:
    - from: "/etc/wanctl/steering.yaml host route_management block"
      to: "post-deploy restored config before steering restart"
      via: "backup before --with-steering scp, sudo install restore before restart"
      pattern: "route_management|steering.yaml"
    - from: "deploy.sh --delete reconcile"
      to: "repo==prod sha256 equality"
      via: "phase261-sha256-audit.py manifest diff"
      pattern: "PHASE261_AUDIT_SRC_TREE_EQUAL"
---

<objective>
Run the full, reversible deploy.sh reconcile of /opt/wanctl on cake-shaper (RECON-01), preserving
the host-only steering.yaml route_management dry-run block across the steering scp, sequencing the
service restarts with steering LAST and health-gating each on :9102, then prove repo==prod with the
per-file sha256 audit built in Plan 01.

Purpose: the v1.57 D-07 fix reached prod via a surgical single-file rsync, never the full pipeline.
RECON-01 reconciles the whole tree so repo==prod is provable and the deploy path is known-good
before any later mutating phase. The rollback anchor from Plan 01 makes this reversible.

Output: the deploy+restart transcript, the steering.yaml preserve/restore record, the :9102 restart
gates, the per-file sha256 audit verdict (PHASE261_AUDIT_SRC_TREE_EQUAL) and both raw manifests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-pre-flip-deploy-reconciliation/261-CONTEXT.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-RESEARCH.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-PATTERNS.md
@.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md
@.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt

<interfaces>
deploy.sh "full prod surface" = TWO per-WAN invocations (NOT one):
  ./scripts/deploy.sh spectrum cake-shaper --with-steering --with-spectrum-cake-autorate
  ./scripts/deploy.sh att      cake-shaper --with-att-cake-autorate
--with-spectrum-cake-autorate requires WAN_NAME=spectrum; --with-att-cake-autorate requires
WAN_NAME=att (deploy.sh guards). The src/wanctl rsync runs in BOTH (idempotent on the 2nd).
deploy.sh rsync = -av --delete --exclude=__pycache__ --exclude=*.pyc (this is what makes repo==prod
provable). The Phase 201 predeploy gate runs only for WAN_NAME=spectrum.

LANDMINE (Pitfall 1, breaks RECON-03 downstream if unhandled): repo configs/steering.yaml has NO
route_management block; the live /etc/wanctl/steering.yaml has a host-only
route_management:{enabled:true, mode:"dry_run"} block (hand-added Phase 256, never committed).
--with-steering scp's the repo config over it -> mode reverts to off. RESOLUTION FOR THIS PHASE
(option 1, conservative, SAFE-22-clean, no repo source edit): back up the host steering.yaml BEFORE
the deploy, restore it (sudo install -m0640 -o root -g wanctl) AFTER the steering scp and BEFORE the
steering restart. Do NOT edit repo configs/steering.yaml in Phase 261 (committing the block is a
deferred reviewed source edit, out of scope).

Restart scope (Pitfall 6): restart ONLY cake-autorate-{spectrum,att}-state-bridge.service +
steering.service (LAST). Do NOT restart cake-autorate-{spectrum,att}.service (the shaper proper —
bouncing it disturbs live production shaping). The state-bridges + steering are the wanctl-side
consumers of the deployed /opt/wanctl code.

:9102 restart gate (Pitfall 5): after each restart, poll :9102 until status in (healthy,degraded)
AND ownership_inspection.inspector_status=="ok" with a fresh last_inspected_at, with timeout — a
restart transiently returns {"status":"starting"} with no route_management section; asserting too
early is a false FAIL.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Full deploy.sh reconcile with steering.yaml preserve/restore + sequenced steering-last restart (D-03)</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md</files>
  <read_first>
    - scripts/deploy.sh (rsync --delete + pyc excludes; per-WAN --with-{spectrum,att}-cake-autorate guards; --with-steering config scp; Phase 201 predeploy gate)
    - 261-RESEARCH.md Pitfall 1 (steering.yaml clobber), Pitfall 5 (restart readiness timing), Pitfall 6 (shaper vs state-bridge units), Code Examples §Deploy + §Sequenced restart
    - 261-PATTERNS.md "Deploy + Reconcile Reference" + "CRITICAL config-preservation"
    - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md (the anchor that makes this reversible; the recorded one-command revert)
    - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt (the witnessed --delete set)
  </read_first>
  <action>
    Run the full reversible reconcile. The Plan 01 anchor MUST exist first (depends_on 261-01); if
    it does not, STOP. Re-witness the live :9102 state immediately before starting (point-in-time
    facts can drift since research).

    1. PRESERVE: back up the host /etc/wanctl/steering.yaml to the anchor dir before the deploy
       (sudo cp -a /etc/wanctl/steering.yaml <ANCHOR_DIR>/steering.yaml.host-pre-deploy). This is
       the load-bearing safeguard for the route_management dry-run block.

    2. DEPLOY (full prod surface = two per-WAN invocations):
         ./scripts/deploy.sh spectrum cake-shaper --with-steering --with-spectrum-cake-autorate
         ./scripts/deploy.sh att      cake-shaper --with-att-cake-autorate
       The src/wanctl rsync (-av --delete, pyc excluded) runs in both (idempotent on the 2nd) and
       sweeps the witnessed .phase259-backup-* dir. Capture both transcripts. Ensure
       /etc/wanctl/spectrum.yaml is reconciled or the Spectrum Phase 201 predeploy gate fail-closes.

    3. RESTORE (before the steering restart): re-install the preserved host steering.yaml so the
       route_management dry-run block survives (sudo install -m0640 -o root -g wanctl
       <ANCHOR_DIR>/steering.yaml.host-pre-deploy /etc/wanctl/steering.yaml). Do NOT edit repo
       configs/steering.yaml.

    4. RESTART (sequenced, steering LAST, :9102-gated; ONLY state-bridge + steering, NEVER the
       shaper units):
         restart cake-autorate-spectrum-state-bridge.service -> gate :9102
         restart cake-autorate-att-state-bridge.service       -> gate :9102
         restart steering.service                             -> gate :9102   (LAST)
       Use the bounded :9102 poll (Pitfall 5): wait for status in (healthy,degraded) AND
       inspector_status=="ok" with a fresh last_inspected_at before proceeding. Do NOT restart
       cake-autorate-{spectrum,att}.service (the shaper — touches live shaping).

    SAFE-22: this is reconcile + restart only. No CAKE/qdisc/threshold change, no controller-path
    source diff, no Netwatch enable/disable/delete, no route-owner flip, no active route-management
    mode. Netwatch remains owner; mode stays dry_run. If anything goes wrong, the recorded
    one-command revert (Plan 01 anchor) restores /opt/wanctl.

    Write phase261-deploy-restart.md: both deploy transcripts, the preserve+restore record, the
    sequenced restart with each :9102 gate result, and confirmation the shaper units were NOT
    restarted. Keep host IP/credential out of the file.

    checkpoint:human-action because it mutates the live production host (the SAFE-22-permitted
    reconcile deploy) and bounces production-adjacent services; per CLAUDE.md "suggest, don't
    implement; minimal changes only when approved" the operator runs and witnesses each step.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md && grep -qi 'steering.yaml' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md && grep -qi 'state-bridge' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-restart.md && echo DEPLOY_RESTART_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both per-WAN deploy.sh invocations ran end-to-end (transcripts captured); the src rsync swept
      the .phase259-backup-* dir.
    - The host steering.yaml was backed up before deploy and restored (sudo install -m0640) before
      the steering restart; evidence records the preserve+restore commands.
    - Restarts sequenced steering LAST, each :9102-gated (status healthy/degraded + inspector_status
      ok recorded); evidence explicitly confirms cake-autorate-{spectrum,att}.service (shaper) were
      NOT restarted.
    - Evidence states SAFE-22 held (no owner flip, mode unchanged, no controller-path diff) and
      contains no host IP/credential.
  </acceptance_criteria>
  <done>Full reversible deploy.sh reconcile complete; steering.yaml dry-run block preserved; services restarted steering-last and :9102-gated; SAFE-22 held.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Per-file sha256 audit proving repo==prod over the D-01 set (RECON-01)</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-audit.md, .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-repo.txt, .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-prod.txt</files>
  <read_first>
    - scripts/phase261-sha256-audit.py (the audit proof built in Plan 01)
    - 261-RESEARCH.md Code Examples §Audit + Pitfall 3 (/opt/wanctl/scripts not in src rsync --delete) + Pitfall 4 (verify_deployment is filename-only, NOT the audit)
    - 261-CONTEXT.md D-01 (audit scope: src/wanctl tree + deploy.sh-installed scripts subset; exclude /etc/wanctl, /var/lib/wanctl, pyc)
  </read_first>
  <action>
    Run scripts/phase261-sha256-audit.py against the now-reconciled /opt/wanctl (read-only;
    prod hashes computed on-host via ssh sudo sha256sum). This proves the D-01 byte-equality —
    NOT deploy.sh's filename-only verify_deployment (Pitfall 4).

    The audit covers: (a) the src/wanctl tree mirror — all non-pyc files (live-verified 110) under
    /opt/wanctl with the scripts/ helper dir pruned, repo-vs-prod manifest diff -> expect
    PHASE261_AUDIT_SRC_TREE_EQUAL; (b) the deploy.sh-installed /opt/wanctl/scripts subset
    (analyze_baseline.py, validate-deployment.sh, wanctl-history, wanctl-operator-summary,
    compact-metrics-dbs.sh) each prod sha256 == its repo source -> expect all OK; (c) any EXTRA
    non-managed file under /opt/wanctl/scripts enumerated for disposition.

    Confirm post-deploy: the .phase259-backup-* dir is gone (swept by rsync --delete) and
    route_ownership_guard.py sha == repo (already matched pre-deploy; the audit re-proves it). For
    the stale extra /opt/wanctl/scripts/phase259-ownership-proof.py (Pitfall 3) — RECORD its
    disposition explicitly: sweep it (rm) as it is a stale evidence script not in the deploy-managed
    set, and note this in the evidence. (It is read-only / no controller effect — A5; sweeping is the
    clean disposition.)

    Write phase261-sha256-audit.md (verdict PHASE261_AUDIT_SRC_TREE_EQUAL + scripts-subset OK lines
    + extras disposition + .phase259-backup-* sweep confirmation) and both raw manifests
    (phase261-sha256-manifest-repo.txt, phase261-sha256-manifest-prod.txt). Keep host IP/credential
    out of the evidence. If the verdict is MISMATCH, STOP — repo!=prod fails RECON-01; investigate
    before proceeding to Plan 03.

    checkpoint:human-action because it runs ssh sudo against the live production host (read-only
    hashing) and sweeps the stale script; the operator runs the audit and pastes the verdict + manifests.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-audit.md && grep -q 'PHASE261_AUDIT_SRC_TREE_EQUAL' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-audit.md && test -s .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-repo.txt && test -s .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-sha256-manifest-prod.txt && echo AUDIT_EQUAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - phase261-sha256-audit.md contains PHASE261_AUDIT_SRC_TREE_EQUAL (repo==prod over the D-01 src tree).
    - Both raw manifests (repo, prod) exist and are non-empty.
    - The deploy.sh-installed /opt/wanctl/scripts subset all match repo sources (OK lines recorded).
    - The .phase259-backup-* sweep is confirmed and the stale phase259-ownership-proof.py disposition
      (sweep) is recorded; route_ownership_guard.py sha==repo re-proven.
    - Evidence contains no host IP/credential.
  </acceptance_criteria>
  <done>repo==prod proven over the D-01 set with a per-file sha256 audit; scripts subset matches; stale artifacts disposed; RECON-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation -> cake-shaper (SSH) | The SAFE-22-permitted reconcile deploy + sudo restarts cross here |
| repo configs/steering.yaml -> host /etc/wanctl/steering.yaml | Deploy scp can clobber the host-only dry-run safety block |
| -state-bridge units vs cake-autorate shaper units | Restarting the wrong family disturbs live production shaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-05 | Tampering / DoS-of-safety | steering.yaml route_management dry-run block clobbered by --with-steering scp | mitigate | Backup before deploy + sudo install restore before steering restart; downstream smoke gate (Plan 03) asserts mode=="dry_run" explicitly to detect any clobber |
| T-261-06 | DoS (production traffic) | restarting cake-autorate-{spectrum,att}.service (shaper) disturbs live shaping | mitigate | Restart ONLY -state-bridge + steering units; acceptance asserts shaper units NOT restarted (Pitfall 6) |
| T-261-07 | Tampering | rsync --delete removes live state mistaken for stale | mitigate | Plan 01 --dry-run witness pre-confirmed the deletion set; only the .phase259-backup-* dir is swept |
| T-261-08 | Elevation / unintended mutation | deploy touching controller-path source or flipping owner | mitigate | deploy is config/code reconcile only; mode stays dry_run, active_owner stays netwatch; SAFE-22 asserted in evidence |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | No package installs — deploy.sh + host coreutils only (RESEARCH: no Package Legitimacy Audit required) |
</threat_model>

<verification>
- Full deploy.sh ran end-to-end (both per-WAN invocations); .phase259-backup-* swept.
- steering.yaml route_management dry-run block preserved across the deploy.
- Restarts steering-last, :9102-gated; shaper units untouched.
- PHASE261_AUDIT_SRC_TREE_EQUAL; scripts subset matches; extras disposed.
- SAFE-22 held: no owner flip, mode unchanged, no controller-path diff.
</verification>

<success_criteria>
- RECON-01 satisfied: full deploy.sh reconcile + per-file sha256 audit proving repo==prod.
- The reconcile is reversible (Plan 01 anchor) and changed no ownership behavior.
- Plan 03 can now run the smoke gate + confirmatory rerun on the reconciled tree.
</success_criteria>

<output>
Create `.planning/phases/261-pre-flip-deploy-reconciliation/261-02-SUMMARY.md` when done
</output>
