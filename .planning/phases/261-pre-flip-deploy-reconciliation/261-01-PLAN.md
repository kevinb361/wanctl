---
phase: 261-pre-flip-deploy-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase261-sha256-audit.py
  - scripts/phase261-smoke-assertion.py
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-host-probe.txt
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md
  - .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readonly-commands.txt
autonomous: false
requirements:
  - RECON-02

user_setup: []

must_haves:
  truths:
    - "Two reusable proof scripts exist (sha256 audit, :9102 smoke assertion), each emitting one greppable verdict token coupled to its exit code"
    - "A pre-deploy timestamped tarball of /opt/wanctl exists on cake-shaper as the rollback anchor, with its sha256 recorded"
    - "The rollback anchor is proven restorable: a non-disruptive scratch-dir restore byte-matches the live tree without touching /opt/wanctl or bouncing any service"
    - "The deploy.sh --dry-run --delete reconcile delta is witnessed and every deletion is machine-checked against a fail-closed allowlist (only .phase259-backup-* permitted; PHASE261_DELETE_SET_ALLOWLISTED)"
    - "deploy.sh is statically proven to perform NO internal steering restart and NO executed shaper-unit restart (only printed next-steps); PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART recorded (HIGH-1 static witness)"
    - "Pre-deploy ActiveEnterTimestampMonotonic of cake-autorate-{spectrum,att}.service AND steering.service is captured as the baseline for Plan 02's no-restart gate (HIGH-2 / HIGH-1)"
    - "The deploy.sh write-set OUTSIDE /opt/wanctl is inventoried, and the non-/opt host paths it overwrites are backed up (or pre-deploy sha256-recorded) alongside the tarball anchor so the revert story covers the full mutated surface (HIGH-3)"
    - "The FULL deploy.sh write-set is machine-checked as rollback-covered: every enumerated path deploy.sh writes is classified into exactly one bucket (backed-up / sha-baselined / reproducible-from-repo / install-if-absent / documented-non-issue) with a fail-closed verdict token — PHASE261_FULL_WRITESET_ROLLBACK_COVERED on full classification, PHASE261_FULL_WRITESET_ROLLBACK_INCOMPLETE + STOP-the-phase on ANY unclassified path (HIGH-3 enforcement, not prose-only)"
  artifacts:
    - path: "scripts/phase261-sha256-audit.py"
      provides: "Per-file sha256 manifest audit over the D-01 set; emits PHASE261_AUDIT_SRC_TREE_EQUAL/_MISMATCH"
      min_lines: 60
    - path: "scripts/phase261-smoke-assertion.py"
      provides: ":9102 health smoke gate over route_management + ownership_inspection siblings; exit 0 iff all four checks PASS"
      min_lines: 50
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md"
      provides: "Anchor path/timestamp + scratch-drill verdict + recorded one-command revert + non-/opt write-set inventory and host-config backups + the machine-checked full-write-set classification table (HIGH-3)"
      contains: "PHASE261_RESTORE_DRILL_PASS"
      also_contains: "PHASE261_FULL_WRITESET_ROLLBACK_COVERED"
    - path: ".planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt"
      provides: "Dry-run --delete allowlist verdict + static deploy.sh no-internal-steering-restart witness"
      contains: "PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART"
  key_links:
    - from: "scripts/phase261-smoke-assertion.py"
      to: ":9102/health route_management + ownership_inspection"
      via: "curl + json sibling-key extraction"
      pattern: "route_management|ownership_inspection"
    - from: "rollback anchor tarball"
      to: "scratch-dir restore drill"
      via: "tar -xzf into mktemp dir + sha256 diff vs /opt/wanctl"
      pattern: "PHASE261_RESTORE_DRILL_(PASS|FAIL)"
    - from: "scripts/deploy.sh source"
      to: "steering/shaper restart behavior"
      via: "static grep proving restarts are print-only next-steps"
      pattern: "PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART"
    - from: "scripts/deploy.sh full write-set"
      to: "rollback coverage classification"
      via: "machine-checked per-path bucket assignment with fail-closed verdict"
      pattern: "PHASE261_FULL_WRITESET_ROLLBACK_(COVERED|INCOMPLETE)"
---

<objective>
Build the two reusable proof scripts this phase depends on (the per-file sha256 audit and the
:9102 smoke-assertion gate), probe the live cake-shaper host read-only, witness the
deploy.sh --dry-run reconcile delta (machine-checked against a fail-closed deletion allowlist),
statically witness that deploy.sh performs no internal steering/shaper restart, capture the
pre-deploy shaper+steering restart baseline, and capture + exercise the pre-deploy rollback anchor
(RECON-02) — extended to inventory, back up, AND machine-check full rollback coverage over the
deploy.sh write-set OUTSIDE /opt/wanctl. This is the Wave-0 prep + the reversibility proof that MUST
land before any deploy.

Purpose: RECON-02 requires a proven, exercised revert path captured BEFORE the reconcile
deploy mutates /opt/wanctl. Capturing the anchor first means the deploy is reversible from the
instant it runs. The two proof scripts are consumed by Plan 02 (audit) and Plan 03 (smoke gate),
so they are built here in Wave 1. The static restart witness + pre-deploy timestamp baseline + the
machine-checked full-write-set rollback-coverage classification close the three cross-AI HIGH
concerns (steering-restart timing, shaper non-restart, anchor scope) by converting them into Wave-1
witnessed checks — HIGH-3 specifically is now ENFORCED via a fail-closed coverage verdict token, not
documented in prose.

Output: scripts/phase261-sha256-audit.py, scripts/phase261-smoke-assertion.py, a host-probe
record (incl. pre-deploy monotonic baseline), a dry-run witness (incl. allowlist verdict + static
deploy.sh restart witness), the rollback-anchor evidence packet (with PHASE261_RESTORE_DRILL_PASS,
the non-/opt write-set inventory/backups, AND the machine-checked full-write-set classification table
emitting PHASE261_FULL_WRITESET_ROLLBACK_COVERED), and the staged readonly-commands file for the
Plan 03 confirmatory rerun.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/261-pre-flip-deploy-reconciliation/261-CONTEXT.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-RESEARCH.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-PATTERNS.md
@.planning/phases/261-pre-flip-deploy-reconciliation/261-VALIDATION.md

<interfaces>
:9102/health payload — route_management and ownership_inspection are TOP-LEVEL SIBLING keys
(not nested). Field provenance (read-only consumer surfaces, DO NOT modify these source files):
- route_management.{mode,active_owner,enabled} <- src/wanctl/steering/health.py
  _build_route_management_section (defaults mode="off", active_owner="unknown")
- ownership_inspection.{match,inspector_status,inspector_error,last_inspected_at} <-
  src/wanctl/steering/health.py _build_ownership_inspection_section
- _active_owner returns "netwatch" unless mode=="active" AND active_allowed
  (src/wanctl/steering/route_manager.py) — so active_owner is "netwatch" for BOTH off and
  dry_run; the smoke gate MUST assert mode explicitly to detect a steering.yaml clobber.

phase227-rollback.sh idioms to copy: usage(), require_command(), flag-loop, SSH+sha256 helper.
The phase261 drill is NON-DISRUPTIVE (no --confirm mutation gate needed) — scratch-dir restore
only, never touches live /opt/wanctl.

deploy.sh write-set (HIGH-3 inventory ground truth — deploy.sh writes MUCH more than /opt/wanctl;
this is the AUTHORITATIVE enumeration the Task 3 classification gate must cover with NO path left
unclassified):
- /opt/wanctl (rsync -av --delete; the tarball anchor covers THIS)
- /etc/wanctl/{spectrum,att}.yaml, /etc/wanctl/steering.yaml (steering.yaml = the clobber, handled
  by Plan 02 preserve/restore), /etc/cake-autorate/config.{spectrum,att}.sh
- /usr/local/sbin/cake-autorate-{spectrum,att}-{qdisc-init,state-bridge},
  wanctl-bpctl-watchdog-{petter,bypass}; /etc/wanctl/bpctl-watchdog/{att,spectrum}.env
  (install-if-absent only)
- /etc/systemd/system/*.service units (steering, cake-autorate-{spectrum,att},
  cake-autorate-{spectrum,att}-state-bridge, wanctl@, nic-tuning, bridge-qos,
  silicom-bypass-watchdog@) + systemctl daemon-reload (the ONLY unit-state op;
  reversible by another daemon-reload — non-destructive)
- /etc/sysctl.d/99-wanctl-network.conf, /usr/local/bin/wanctl-nic-tuning.sh,
  wanctl-bridge-qos.sh, /etc/wanctl/bridge-qos.nft
- /opt/scripts/{profiling_collector,analyze_profiling}.py, /opt/docs/PROFILING.md
- /opt/wanctl/scripts/{analyze_baseline.py, validate-deployment.sh, wanctl-history,
  wanctl-operator-summary, compact-metrics-dbs.sh} + /usr/local/bin symlinks
- NO venv/pip install (rsyncs source only — NO dependency-state mutation; document as a non-issue)
- NO unit enable/start/disable in main path (enable/disable are manual next-steps); daemon-reload
  is the only unit-state op (document as non-issue: reversible, non-destructive)

Rollback-coverage buckets (Task 3 step 5 classification — EVERY enumerated path lands in EXACTLY one):
- backed-up           : covered by the /opt/wanctl tarball anchor OR the host-config-pre-deploy/ backup dir
- sha-baselined       : pre-deploy sha256 recorded so a post-deploy compare proves unchanged/restorable
- reproducible-from-repo : tracked in repo + trivially re-deployable (helper scripts/units)
- install-if-absent   : deploy.sh writes it only if missing — never overwritten/destroyed
- documented-non-issue: e.g. systemctl daemon-reload (reversible by another daemon-reload); NO venv/pip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the sha256 audit + smoke-assertion proof scripts</name>
  <files>scripts/phase261-sha256-audit.py, scripts/phase261-smoke-assertion.py</files>
  <read_first>
    - scripts/phase260-observation.py (sample_health / gate_sample field extraction; fail-closed
      sentinel on OSError/Timeout/JSONDecodeError; argparse CLI shape; OBSERVE_VERDICT token+exit coupling)
    - scripts/phase259-ownership-proof.py (single greppable verdict token + exit-code coupling pattern)
    - scripts/phase227-rollback.sh (usage()/require_command()/flag-loop + SSH+sha256 helper idiom)
    - src/wanctl/steering/health.py _build_route_management_section + _build_ownership_inspection_section
      (the exact :9102 sibling-key shape the smoke gate reads)
    - 261-RESEARCH.md Code Examples (the verified §Audit and §Smoke gate shapes)
    - 261-PATTERNS.md Conventions 1-4 (verdict token, deployed-import guard, argparse, fail-closed sibling extraction)
  </read_first>
  <action>
    Write two standalone proof scripts following the established 258/259/260 proof family
    conventions (one greppable verdict line, exit code coupled to it; argparse with --ssh-host
    defaulting to cake-shaper, plus require_command guards).

    phase261-sha256-audit.py: builds a per-file sha256 manifest of the D-01 set and compares
    repo vs prod. Repo side = all non-pyc files under src/wanctl/ (the live-verified set is 110
    files: 109 .py + dashboard/dashboard.tcss), excluding __pycache__ and *.pyc to match
    deploy.sh's rsync excludes. Prod side = the same tree under /opt/wanctl with the scripts/
    helper dir pruned (find . -path ./scripts -prune -o ...). Sort manifests by path, diff them,
    emit token PHASE261_AUDIT_SRC_TREE_EQUAL on zero diff else PHASE261_AUDIT_SRC_TREE_MISMATCH
    (exit 0 iff EQUAL). Separately audit the deploy.sh-installed /opt/wanctl/scripts subset per
    Open-Q2 resolution: compare each of {analyze_baseline.py, validate-deployment.sh,
    wanctl-history, wanctl-operator-summary, compact-metrics-dbs.sh} prod sha256 to its repo
    source under scripts/, print OK/MISMATCH per file, and ENUMERATE any extra non-managed file
    under /opt/wanctl/scripts (live: phase259-ownership-proof.py) as a flagged-for-disposition
    line (do NOT treat the scripts/ dir as a 1:1 repo mirror — there is none). The script writes
    both raw manifests to the evidence dir paths it is given. Prod hashes are computed on-host
    via ssh sudo sha256sum / sudo find; do NOT hand-roll a copy-then-hash. Use the
    phase227-rollback.sh SSH+sha256 helper idiom.

    phase261-smoke-assertion.py: pure curl+json gate that does NOT import wanctl (so it sidesteps
    the deployed-import guard). Reads :9102/health, extracts route_management and
    ownership_inspection as SIBLING keys (never assume nesting), and asserts exactly four checks:
    route_management.mode=="dry_run", route_management.active_owner=="netwatch",
    ownership_inspection.inspector_status=="ok", ownership_inspection.match is True. Print
    "PASS <check>" / "FAIL <check>" per check; exit 0 iff all four PASS. CRITICAL: assert mode
    EXPLICITLY (active_owner is "netwatch" for both off and dry_run — it cannot detect the
    steering.yaml clobber). Include the bounded-poll readiness guard (Pitfall 5): if :9102 returns
    {"status":"starting"} or has no route_management section, re-poll until status in
    (healthy,degraded) AND inspector_status=="ok" AND a fresh last_inspected_at, with a timeout;
    fail closed on timeout. HEALTH FRESHNESS (MEDIUM): support a --min-inspected-after EPOCH flag —
    when supplied, the gate additionally requires ownership_inspection.last_inspected_at to parse to
    a timestamp STRICTLY NEWER than that epoch (the restart boundary), failing closed if the field is
    missing, unparseable, or older; this rejects a stale-but-ok inspector reading taken before the
    restart. Also support a flag to assert all expected units active: steering.service,
    cake-autorate-{spectrum,att}.service, cake-autorate-{spectrum,att}-state-bridge.service.

    Run repo CI on the new scripts: .venv/bin/ruff check scripts/phase261-*.py and
    .venv/bin/ruff format scripts/phase261-*.py. These are standalone proof scripts, NOT
    controller-path source — SAFE-22 clean. Do NOT edit any src/wanctl/ file.
  </action>
  <verify>
    <automated>.venv/bin/ruff check scripts/phase261-sha256-audit.py scripts/phase261-smoke-assertion.py && python3 -c "import ast,sys; [ast.parse(open(f).read()) for f in ('scripts/phase261-sha256-audit.py','scripts/phase261-smoke-assertion.py')]; print('PARSE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts exist, parse, and pass ruff check.
    - phase261-sha256-audit.py emits exactly one of PHASE261_AUDIT_SRC_TREE_EQUAL /
      PHASE261_AUDIT_SRC_TREE_MISMATCH and couples exit 0 to EQUAL (grep the source for both tokens).
    - phase261-sha256-audit.py prunes ./scripts from the src-tree manifest and audits the
      deploy.sh-installed scripts subset separately, flagging extras.
    - phase261-smoke-assertion.py asserts mode=="dry_run" explicitly (grep confirms a literal
      "dry_run" comparison, not only active_owner), reads route_management and
      ownership_inspection as siblings, exits 0 iff all four checks PASS, and does NOT import wanctl.
    - phase261-smoke-assertion.py exposes --min-inspected-after and, when set, fails closed unless
      ownership_inspection.last_inspected_at is strictly newer than the given epoch (grep confirms the
      flag and a strict newer-than comparison).
    - Neither script modifies any src/wanctl/ file (git diff --name-only shows only scripts/phase261-*.py).
  </acceptance_criteria>
  <done>Two reusable, CI-clean proof scripts exist with the correct verdict tokens, exit coupling, and the health-freshness guard; no controller-path source touched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Read-only host probe + restart-baseline + deploy.sh dry-run/static restart witness on cake-shaper</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-host-probe.txt, .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt, .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readonly-commands.txt</files>
  <read_first>
    - 261-RESEARCH.md Common Pitfalls 2 (rsync --delete witness) + Environment Availability + Wave 0 Gaps
    - 261-PATTERNS.md "Readonly command file to copy verbatim"
    - scripts/deploy.sh (deploy_steering_systemd: scp unit + repo steering.yaml + daemon-reload ONLY,
      NO executed steering restart; deploy_{spectrum,att}_cake_autorate: scp + daemon-reload ONLY, NO
      executed shaper restart; restarts are print_next_steps manual guidance)
    - .planning/milestones/v1.57-phases/260-dry-run-observation-rerun-canary-readiness/evidence/phase260-readonly-commands.txt
  </read_first>
  <action>
    Probe the live host read-only (no mutation) and witness the deploy delta + deploy.sh restart
    behavior BEFORE anchor/deploy.

    1. Probe cake-shaper for the tooling A1 assumes: confirm tar, mktemp, xargs, sha256sum, curl,
       python3 are present (command -v each). Snapshot the CURRENT :9102/health JSON as the
       pre-deploy baseline (this re-witnesses the point-in-time facts per RESEARCH "re-witness at
       execution"). Snapshot the live src/wanctl non-pyc file count under /opt/wanctl and list
       /opt/wanctl/scripts and any /opt/wanctl/.phase259-backup-* dirs. Write all of this to
       phase261-host-probe.txt. Keep the cake-shaper host IP/credentials OUT of the evidence file
       (MEMORY: prior cred leak in .planning) — refer to it as "cake-shaper".

    1b. SHAPER + STEERING NON-RESTART BASELINE (HIGH-2 + HIGH-1 pre-half). Capture the
       ActiveEnterTimestampMonotonic of the SHAPER-PROPER units AND steering BEFORE any deploy, so
       Plan 02 can prove deploy.sh did not bounce them:
         ssh cake-shaper systemctl show -p ActiveEnterTimestampMonotonic \
           cake-autorate-spectrum.service cake-autorate-att.service steering.service
       Record the three monotonic values verbatim in phase261-host-probe.txt under a clearly
       labelled "PRE-DEPLOY ActiveEnterTimestampMonotonic baseline" block. These are the load-bearing
       pre-values for the Plan 02 no-restart gate: SAFE-22 forbids bouncing the two shaper units
       (their monotonic MUST be byte-identical pre/post the whole Plan 02 sequence); the steering
       value lets Plan 02 prove deploy.sh performed no INTERNAL steering restart (steering monotonic
       must move ONLY at the operator's explicit post-restore restart, not during the deploy.sh run).

    2. Run the deploy dry-run witness (read-only, -n): the two per-WAN invocations with --dry-run
       so the rsync --delete set is previewed without writing anything. Capture every "deleting ..."
       line. MACHINE-CHECKED DELETION ALLOWLIST (MEDIUM, fail-closed — not eyeballed): the ONLY
       permitted deletion is the witnessed stale artifact under the synced tree, paths matching
       "^\.phase259-backup-". Filter the dry-run output to every "deleting " line, strip the literal
       "deleting " prefix, and assert EVERY remaining path matches the allowlist regex; if ANY
       deletion path falls outside the allowlist, emit PHASE261_DELETE_SET_UNEXPECTED and STOP (do
       not proceed to anchor or deploy). On a fully-allowlisted set emit
       PHASE261_DELETE_SET_ALLOWLISTED. Record the full deleting-line list, the allowlist regex, and
       the emitted token in phase261-deploy-dryrun-witness.txt.

    2b. STATIC deploy.sh STEERING/SHAPER RESTART WITNESS (HIGH-1 static half + HIGH-2 reinforcement).
       Prove from the deploy.sh SOURCE (local, read-only) that the only steering/shaper restart it
       references is a PRINTED next-step, never an executed command. Run, capturing output into
       phase261-deploy-dryrun-witness.txt:
         grep -nE 'restart[^|]*steering' scripts/deploy.sh
         grep -nE 'restart[^|]*cake-autorate-(spectrum|att)\.service' scripts/deploy.sh
       Confirm in the evidence that EVERY match sits inside a print/echo (the print_next_steps manual
       guidance) and that deploy_steering_systemd / deploy_{spectrum,att}_cake_autorate run only
       scp + `systemctl daemon-reload` (no executed `systemctl restart` of steering or the shaper
       units, no `ssh ... systemctl restart`). Emit PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART when
       confirmed; if any non-print executed steering/shaper restart is found, emit
       PHASE261_DEPLOY_INTERNAL_STEERING_RESTART and STOP — the steering.yaml preserve/restore
       safeguard and the SAFE-22 shaper-non-restart guarantee are unsound if deploy.sh restarts those
       units internally.

    3. Stage the readonly-commands file for Plan 03's confirmatory rerun by copying the Phase 260
       file verbatim (the three GET-only reads: netwatch print detail, system script print detail,
       ip route print detail) to phase261-readonly-commands.txt.

    This is a checkpoint:human-action because it reads the live production host; the operator runs
    the read-only ssh/curl/dry-run commands and pastes results. No service is touched, nothing is
    written to /opt/wanctl. The grep over scripts/deploy.sh is a local read-only static check.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-host-probe.txt && test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt && grep -q 'PRE-DEPLOY ActiveEnterTimestampMonotonic baseline' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-host-probe.txt && grep -q 'PHASE261_DELETE_SET_ALLOWLISTED' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt && grep -q 'PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-deploy-dryrun-witness.txt && grep -qi 'netwatch print detail' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-readonly-commands.txt && echo PROBE_EVIDENCE_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - phase261-host-probe.txt records tar/mktemp/xargs/sha256sum/curl/python3 availability, a fresh
      :9102 baseline JSON, the live /opt/wanctl non-pyc file count, and the /opt/wanctl/scripts +
      .phase259-backup-* inventory; contains NO host IP or credential.
    - phase261-host-probe.txt contains a "PRE-DEPLOY ActiveEnterTimestampMonotonic baseline" block
      recording the monotonic timestamps of cake-autorate-spectrum.service, cake-autorate-att.service,
      AND steering.service (the load-bearing pre-values for Plan 02's no-restart gate).
    - phase261-deploy-dryrun-witness.txt captures the --dry-run --delete preview, the allowlist regex,
      and emits PHASE261_DELETE_SET_ALLOWLISTED (every deletion matched ^\.phase259-backup-); any
      out-of-allowlist deletion instead emits PHASE261_DELETE_SET_UNEXPECTED and the operator STOPs.
    - phase261-deploy-dryrun-witness.txt records the two static greps over scripts/deploy.sh and emits
      PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART, confirming the only steering/shaper restarts in
      deploy.sh are printed next-steps (deploy_steering_systemd / deploy_*_cake_autorate do scp +
      daemon-reload only).
    - phase261-readonly-commands.txt contains exactly the three GET-only RouterOS read commands.
  </acceptance_criteria>
  <done>Live host probed read-only; the pre-deploy shaper+steering monotonic baseline captured; the --delete reconcile delta machine-checked against a fail-closed allowlist; deploy.sh statically witnessed to perform no internal steering/shaper restart; readonly-commands staged for Plan 03.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Capture rollback anchor + non-disruptive restore drill + machine-checked full-write-set rollback-coverage gate (RECON-02, HIGH-3)</name>
  <files>.planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md</files>
  <read_first>
    - 261-RESEARCH.md Code Examples §Anchor (verified tarball-capture + scratch-restore shape)
    - 261-PATTERNS.md "scripts/phase261-rollback-drill.sh" section (capture + scratch-restore + verdict; recorded one-command revert)
    - 261-CONTEXT.md D-02 (timestamped tarball, scratch-dir restore drill, NOT live restore-over-/opt/wanctl) and D-01 (audit scope)
    - scripts/deploy.sh (the full write-set: /etc/wanctl/*.yaml, /etc/cake-autorate/*.sh, /etc/systemd/system/*.service, /usr/local/sbin + /usr/local/bin + /opt/scripts + /opt/docs files; install-if-absent bpctl-watchdog env; daemon-reload only; NO venv/pip; NO unit enable/start in main path)
    - the <interfaces> deploy.sh write-set enumeration + rollback-coverage buckets above (the AUTHORITATIVE list the classification gate must cover)
  </read_first>
  <action>
    Capture the pre-deploy rollback anchor, PROVE it restorable WITHOUT touching the live tree, AND
    machine-check FULL rollback coverage over the deploy.sh write-set OUTSIDE /opt/wanctl (HIGH-3
    enforcement). This must complete BEFORE Plan 02's deploy so the reconcile is reversible from the
    start. D-02 LOCKS the code anchor mechanism to a /opt/wanctl tarball + scratch-dir drill — do NOT
    swap it; EXTEND coverage around it.

    1. Capture (code anchor — D-02, unchanged): create /var/lib/wanctl/phase261-backups/<UTC-timestamp>/
       on cake-shaper and write a timestamped tarball opt-wanctl.tgz of /opt/wanctl
       (sudo tar -C /opt -czf ... wanctl), plus opt-wanctl.tgz.sha256. Record the exact ANCHOR_DIR
       path + timestamp.

    2. Exercise (non-disruptive): extract the tarball into a fresh mktemp scratch dir and per-file
       sha256-diff the restored scratch tree against the live /opt/wanctl tree (both excluding *.pyc
       / __pycache__). Emit PHASE261_RESTORE_DRILL_PASS iff the diff is empty, else
       PHASE261_RESTORE_DRILL_FAIL; then rm -rf the scratch dir. This NEVER writes to /opt/wanctl and
       bounces NO service (D-02 — the rejected alternative is a live restore-over-/opt/wanctl that
       double-bounces steering). The drill also confirms tarball readability and that the scratch
       extract preserves file ownership/mode (LOW — trivial: note `tar -tzf` listed cleanly and the
       extracted tree matched), and that the backups dir had sufficient free disk for both the anchor
       and the extract (df check).

    3. NON-/opt WRITE-SET INVENTORY + BACKUP (HIGH-3, conservative hybrid). The /opt/wanctl tarball
       covers ONLY the code tree. deploy.sh also overwrites host paths outside /opt/wanctl. Into the
       SAME ANCHOR_DIR, back up (sudo cp -a, preserving owner/mode) the host-local config + unit files
       deploy.sh overwrites that are NOT covered by the tarball and are NOT runtime/excluded state:
         /etc/wanctl/steering.yaml            (also the Plan 02 preserve target — back it up here too)
         /etc/wanctl/spectrum.yaml, /etc/wanctl/att.yaml
         /etc/cake-autorate/config.spectrum.sh, /etc/cake-autorate/config.att.sh
         /etc/systemd/system/{steering.service, cake-autorate-spectrum.service,
           cake-autorate-att.service, cake-autorate-spectrum-state-bridge.service,
           cake-autorate-att-state-bridge.service}  (the units this deploy's invocations scp)
         /etc/sysctl.d/99-wanctl-network.conf
       Store these under <ANCHOR_DIR>/host-config-pre-deploy/ mirroring their absolute paths, and
       record an sha256 of each backed-up file. (Do NOT back up /etc/wanctl secrets/SSH keys — D-01
       excludes them and deploy.sh does not overwrite them.) The /usr/local/sbin, /usr/local/bin,
       /opt/scripts, /opt/docs script files deploy.sh installs are NON-controller helper scripts
       already tracked in repo; for those, record a pre-deploy sha256 list (sudo sha256sum of each) so
       a post-deploy compare can prove they were either unchanged or are trivially restorable from
       repo — do NOT tar them (keeps the anchor lean; they are reproducible from the repo).

    4. DOCUMENT THE NON-ISSUES (HIGH-3 discharge). In the evidence, explicitly state, grounded in the
       deploy.sh source: (a) deploy.sh performs NO venv/pip install — it rsyncs source only, so
       "venv/dependency state" is NOT mutated and needs no backup; (b) deploy.sh performs NO unit
       enable/start/disable in the main path — the only unit-state op is `systemctl daemon-reload`,
       which is non-destructive and reversible by another daemon-reload; (c) /etc/wanctl/steering.yaml
       is additionally handled by the Plan 02 preserve/restore safeguard; (d) the
       /etc/wanctl/bpctl-watchdog/{att,spectrum}.env files are install-if-absent only — deploy.sh
       never overwrites an existing one, so they are never destroyed.

    5. MACHINE-CHECKED FULL-WRITE-SET ROLLBACK-COVERAGE GATE (HIGH-3 ENFORCEMENT — fail-closed verdict
       token, NOT prose). Steps 1-4 produce the backups/sha-baselines/non-issue notes; this step PROVES
       they cover the ENTIRE deploy.sh write-set with no gap.
       a. ENUMERATE the deploy.sh write-set programmatically / from a recorded enumeration: derive the
          complete set of paths deploy.sh writes from scripts/deploy.sh itself (e.g. grep its rsync /
          scp / install / cp targets) cross-checked against the AUTHORITATIVE <interfaces> write-set
          list above. Record the enumerated path list verbatim in the evidence.
       b. CLASSIFY each enumerated path into EXACTLY ONE of the five buckets (definitions in the
          <interfaces> "Rollback-coverage buckets" block): backed-up / sha-baselined /
          reproducible-from-repo / install-if-absent / documented-non-issue. Write the per-path
          classification as a table (path -> bucket -> evidence pointer, e.g. "backed-up ->
          host-config-pre-deploy/etc/wanctl/steering.yaml + sha", "documented-non-issue -> daemon-reload
          reversible", "install-if-absent -> bpctl-watchdog env").
       c. ASSERT coverage is COMPLETE and fail-closed: every enumerated path from (a) MUST appear in
          the classification table in (b) in exactly one bucket. If ANY enumerated path is unclassified
          (in zero buckets) or ambiguous (in >1), emit PHASE261_FULL_WRITESET_ROLLBACK_INCOMPLETE and
          STOP the phase — do NOT proceed to Plan 02's deploy. Only when every path is classified into
          exactly one bucket, emit PHASE261_FULL_WRITESET_ROLLBACK_COVERED.
       d. Record the enumerated write-set, the per-path classification table, and the emitted verdict
          token in phase261-rollback-anchor.md.
       This step is SAFE-22 clean: read-only static enumeration of scripts/deploy.sh + the additive
       backup/sha work already done in steps 1-4 — NO controller-path source edit, NO ownership change.
       Keep host IP/credential out of the evidence (MEMORY: prior cred leak in .planning).

    Write phase261-rollback-anchor.md recording: the ANCHOR_DIR path + timestamp; the tarball sha256;
    the PHASE261_RESTORE_DRILL_PASS verdict (incl. readability/ownership/free-disk notes); the
    enumerated deploy.sh write-set + the per-path classification table + the
    PHASE261_FULL_WRITESET_ROLLBACK_COVERED verdict (or _INCOMPLETE + STOP); the host-config backup
    location + per-file sha256; the helper-script sha baseline; the documented non-issues; and the
    recorded ONE-COMMAND code revert (stop steering.service -> tar -C /opt -xzf
    <anchor>/opt-wanctl.tgz -> start steering.service) PLUS the documented host-config revert (restore
    the host-config-pre-deploy/ files with sudo install preserving owner/mode -> daemon-reload) so a
    real rollback covers the full mutated surface. Keep the host IP/credential out of the file.

    checkpoint:human-action because it writes a tarball + host-config backups to the live production
    host filesystem (additive, under /var/lib/wanctl — NOT /opt/wanctl, NO service touch) and runs
    sudo on prod. The operator runs the capture + drill + inventory + classification gate and pastes
    the verdicts.
  </action>
  <verify>
    <automated>test -f .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && grep -q 'PHASE261_RESTORE_DRILL_PASS' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && grep -q 'PHASE261_FULL_WRITESET_ROLLBACK_COVERED' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && ! grep -q 'PHASE261_FULL_WRITESET_ROLLBACK_INCOMPLETE' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && grep -qi 'phase261-backups' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && grep -qi 'host-config-pre-deploy' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && grep -qi 'daemon-reload' .planning/phases/261-pre-flip-deploy-reconciliation/evidence/phase261-rollback-anchor.md && echo ANCHOR_EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Anchor tarball + .sha256 exist on cake-shaper under /var/lib/wanctl/phase261-backups/<ts>/;
      the exact path/timestamp is recorded in the evidence file.
    - phase261-rollback-anchor.md contains PHASE261_RESTORE_DRILL_PASS (scratch restore byte-matched
      the live tree) and the recorded one-command code revert.
    - The drill did NOT modify /opt/wanctl and bounced no service (scratch-dir only); evidence states
      this, plus tarball readability + ownership/mode preservation + free-disk notes.
    - The evidence enumerates the FULL deploy.sh write-set (derived from scripts/deploy.sh, cross-checked
      against the authoritative interfaces list) and classifies EVERY enumerated path into exactly one
      bucket (backed-up / sha-baselined / reproducible-from-repo / install-if-absent / documented-non-issue)
      with NO unclassified path, recorded as a per-path classification table.
    - phase261-rollback-anchor.md emits PHASE261_FULL_WRITESET_ROLLBACK_COVERED (full classification);
      any unclassified path instead emits PHASE261_FULL_WRITESET_ROLLBACK_INCOMPLETE and STOPs the phase
      (the gate is fail-closed — grep proves COVERED present AND _INCOMPLETE absent).
    - The host-config backup under <ANCHOR_DIR>/host-config-pre-deploy/ exists with per-file sha256, the
      helper-script sha baseline is recorded, and the venv (none) / daemon-reload (reversible) /
      install-if-absent (never overwritten) non-issues are documented.
    - The documented host-config revert (restore host-config-pre-deploy/ + daemon-reload) is recorded
      alongside the one-command code revert.
    - Evidence file contains no host IP or credential.
  </acceptance_criteria>
  <done>Pre-deploy rollback anchor captured and proven restorable non-disruptively; the full deploy.sh write-set is ENUMERATED and machine-checked as rollback-covered (every path in exactly one bucket; PHASE261_FULL_WRITESET_ROLLBACK_COVERED, fail-closed STOP on any unclassified path); the deploy is reversible across its entire mutated surface before it runs (RECON-02 + HIGH-3 satisfied by enforcement).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation -> cake-shaper (SSH) | Privileged sudo ops on the production host cross here; key-auth only |
| repo source -> proof scripts | New scripts/ files must NOT touch controller-path source (SAFE-22) |
| live /opt/wanctl tree -> anchor/drill | The drill must stay off the live tree (scratch-dir only) |
| deploy.sh write-set outside /opt/wanctl | Anchor must machine-check-cover (or discharge) every host path deploy.sh mutates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-261-01 | Tampering | scratch-dir restore drill touching live /opt/wanctl | mitigate | Drill extracts into mktemp scratch dir + rm -rf after; never writes /opt/wanctl, bounces no service (D-02) |
| T-261-02 | Information Disclosure | host IP/credential leaking into .planning evidence | mitigate | Audit/anchor exclude /etc/wanctl/secrets; evidence files refer to "cake-shaper", no IP/cred pasted (MEMORY: prior leak) |
| T-261-03 | Tampering | rsync --delete sweeping live state mistaken for stale | mitigate | deploy.sh --dry-run witness in Task 2 machine-checks every deletion against a fail-closed allowlist (^\.phase259-backup-); out-of-allowlist deletion STOPs before any real run |
| T-261-04 | Tampering | proof script accidentally editing controller-path source | mitigate | Task 1 acceptance asserts git diff shows only scripts/phase261-*.py; SAFE-22 holds |
| T-261-13 | DoS-of-safety | deploy.sh restarting steering/shaper INTERNALLY, voiding the preserve/restore + SAFE-22 shaper guarantee | mitigate | Task 2 static greps prove the only steering/shaper restarts are printed next-steps (PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART); internal restart STOPs the phase (HIGH-1/HIGH-2) |
| T-261-14 | Repudiation / incomplete rollback | /opt/wanctl tarball under-covering the deploy.sh write-set | mitigate | Task 3 step 5 ENUMERATES the full deploy.sh write-set and machine-checks that every path is classified into exactly one rollback bucket, emitting the fail-closed verdict PHASE261_FULL_WRITESET_ROLLBACK_COVERED (or _INCOMPLETE + STOP) — backs up non-/opt host-config + units, sha-baselines helper scripts, and discharges venv/daemon-reload/install-if-absent as non-issues (HIGH-3 ENFORCED, not prose-only) |
| T-261-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — composes existing repo tooling + host coreutils only (RESEARCH: no Package Legitimacy Audit required) |
</threat_model>

<verification>
- Both proof scripts CI-clean (ruff) and parse; correct verdict tokens + exit coupling; smoke gate has the --min-inspected-after freshness guard.
- Host probe captured read-only; pre-deploy shaper+steering monotonic baseline recorded (HIGH-2/HIGH-1).
- Dry-run witness: deletion set machine-checked against the fail-closed allowlist (PHASE261_DELETE_SET_ALLOWLISTED); deploy.sh statically witnessed to perform no internal steering/shaper restart (PHASE261_DEPLOY_NO_INTERNAL_STEERING_RESTART) (HIGH-1).
- Rollback anchor captured + PHASE261_RESTORE_DRILL_PASS; the full deploy.sh write-set is enumerated and machine-checked as rollback-covered, emitting PHASE261_FULL_WRITESET_ROLLBACK_COVERED (fail-closed STOP on any unclassified path); full-surface revert recorded (RECON-02 + HIGH-3 ENFORCED).
- No src/wanctl/ source diff (SAFE-22).
</verification>

<success_criteria>
- RECON-02 satisfied: pre-deploy anchor captured and proven restorable non-disruptively, with revert coverage extended to — and machine-checked over — the full deploy.sh write-set (PHASE261_FULL_WRITESET_ROLLBACK_COVERED, HIGH-3 ENFORCED).
- The two proof scripts (audit + smoke, smoke now freshness-aware) exist and are ready for Plan 02 / Plan 03 consumption.
- The deploy --delete delta is allowlist-witnessed; the static deploy.sh restart witness holds (HIGH-1); the shaper+steering pre-baseline is staged for Plan 02 (HIGH-2); the readonly-commands file is staged.
</success_criteria>

<output>
Create `.planning/phases/261-pre-flip-deploy-reconciliation/261-01-SUMMARY.md` when done
</output>
