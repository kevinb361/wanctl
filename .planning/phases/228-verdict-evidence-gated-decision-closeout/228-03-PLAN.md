---
phase: 228-verdict-evidence-gated-decision-closeout
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - configs/spectrum.yaml
  - scripts/phase228-rollback-proof.py
  - tests/test_phase228_rollback_proof.py
  - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json
autonomous: false
requirements: [GATE-03]
must_haves:
  truths:
    - "The live Spectrum rollback to besteffort wash is executed via the existing operator-gated scripts/phase227-rollback.sh --confirm (real Spectrum WAN CAKE-mode mutation) — it is operator-at-keyboard, NOT fired unattended by the executor; the plan is autonomous:false and the executor stops at the confirm boundary for operator sign-off (D-04)"
    - "Sequence is verdict -> rollback -> verify -> closeout: the 228-02 reject verdict JSON is already committed BEFORE this rollback runs; this plan does the rollback + restoration proof only, and the closeout docs (228-04) come after (D-04)"
    - "The rollback command passes an EXPLICIT 228 --out so phase227-rollback.sh writes its run proof into the Phase 228 evidence dir (.../228-.../evidence/phase227-rollback-run-proof.json), NOT the Phase 227 default path — Phase 227 evidence is not polluted and the rollback run is traceable to Phase 228 (HIGH review finding)"
    - "configs/spectrum.yaml restoration framing is SINGLE and unambiguous: the diffserv/allow_wash/DL/UL/docsis_mode VALUES are semantically restored to Snapshot A besteffort (diffserv: besteffort, allow_wash: true, DL 920, UL 18, docsis_mode: true), AND the Phase 227 candidate COMMENT annotation is replaced with a Phase 228 rollback annotation — the repo proof therefore asserts VALUE-equivalence to Snapshot A on those keys plus a full no-unrelated-drift comparison; it does NOT claim byte-identical-including-comments (that would contradict the annotation rewrite). The 'Snapshot A byte-restore' wording is dropped in favor of 'value-restored + annotated'."
    - "Triple restoration proof emitted to phase228-rollback-proof.json: (1) repo — configs/spectrum.yaml value-compares to Snapshot A besteffort on the key fields (diffserv: besteffort, allow_wash: true, rates, docsis_mode) AND a full secret-safe redacted diff against the Snapshot A redacted yaml shows no UNRELATED drift (only the intended diffserv-value + annotation lines differ) — not just selected-line checks; (2) production — scripts/phase227-qdisc-verify.sh --expected-mode besteffort returns besteffort on BOTH spec-router AND spec-modem; (3) health — Spectrum /health reports an accepted-healthy state (status == \"healthy\", matching the existing health-script vocabulary; the script defines the accepted set rather than an undefined 'GREEN') with no crashloop post-restart (D-05)"
    - "The proof JSON mirrors the phase227 qdisc-verify / rollback-late.json artifact shape so it slots into the existing evidence convention and is machine-checkable, not prose"
    - "The rollback-proof tooling is testable WITHOUT a live host: the qdisc-mode check supports the existing --router-input/--modem-input file-fed path and the proof generator accepts injected qdisc-verify + health JSON so the proof logic is locked by test before the operator runs the real mutation"
    - "If the rollback verify fails (qdisc not besteffort on both NICs, or health not GREEN), the proof records a FAILED restoration and the plan does NOT report success — restoration is fail-closed, never assumed"
  artifacts:
    - path: configs/spectrum.yaml
      provides: "reverted to diffserv: besteffort (allow_wash/rates/docsis_mode unchanged); Phase 227 candidate annotation replaced with the Phase 228 rollback note"
      contains: "besteffort"
    - path: scripts/phase228-rollback-proof.py
      provides: "triple restoration proof generator (repo byte-compare + production qdisc-verify + health GREEN) -> phase228-rollback-proof.json"
      contains: "besteffort"
    - path: tests/test_phase228_rollback_proof.py
      provides: "proof-logic regression: all-three-pass => restored; any-fail => fail-closed; file-fed (no live host)"
      contains: "rollback_proof"
    - path: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json
      provides: "the committed triple restoration proof after the operator-gated rollback"
      contains: "besteffort"
    - path: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase227-rollback-run-proof.json
      provides: "phase227-rollback.sh run proof routed into Phase 228 evidence via explicit --out (not the Phase 227 default path)"
      contains: "besteffort"
  key_links:
    - from: scripts/phase228-rollback-proof.py
      to: scripts/phase227-qdisc-verify.sh
      via: "invokes the read-only fail-closed qdisc-mode gate with --expected-mode besteffort for the production proof"
      pattern: "phase227-qdisc-verify|expected-mode besteffort"
    - from: scripts/phase227-rollback.sh
      to: configs/spectrum.yaml
      via: "--confirm copies Snapshot A besteffort bytes to configs/spectrum.yaml + deploy + restart"
      pattern: "besteffort|spectrum.yaml"
---

<objective>
Execute the verdict-driven, operator-gated live rollback of Spectrum from diffserv4 back to
besteffort wash (Snapshot A), then emit a triple restoration proof. Production is LIVE on diffserv4
right now (Phase 227 D-08 left the candidate deployed so 228 could read the deployed mode); the
228-02 reject verdict therefore requires a real Spectrum WAN CAKE-mode change.

Purpose: GATE-03 (criterion 3) requires that if a rollback trigger fires, Spectrum is rolled back
to besteffort wash and BOTH production AND repo are verified restored to Snapshot A. D-04 fixes the
sequence: the verdict is already computed + committed (228-02); this plan runs the operator-gated
rollback and proves restoration; the closeout docs come after (228-04). D-04 also makes this an
operator-at-keyboard step — the real `phase227-rollback.sh --confirm` is NOT fired unattended, so
this plan is `autonomous: false`. D-05 requires a triple proof (repo byte-compare + production
qdisc-verify on both NICs + health GREEN), emitted as a machine-checkable JSON mirroring the
phase227 proof shape.

Output: configs/spectrum.yaml reverted to besteffort, a `phase228-rollback-proof.py` triple-proof
generator (file-fed testable), a regression test, and the committed phase228-rollback-proof.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md

# The operator-gated mutation path (read its usage + the besteffort assertion guard)
@scripts/phase227-rollback.sh
# The read-only fail-closed qdisc-mode gate reused for the production proof
@scripts/phase227-qdisc-verify.sh
# The config to revert
@configs/spectrum.yaml

<interfaces>
<!-- Verified facts. The mutation is operator-at-keyboard; the executor stops at the confirm gate. -->

`scripts/phase227-rollback.sh` (mutation only with --confirm):
- Required: `--raw-dir DIR` containing Snapshot A `deployed-spectrum.yaml` with `diffserv: besteffort`
  (operator-private raw restore source, mode 0600, outside git).
- `--ssh-host` (default cake-shaper), `--health-url` (default http://10.10.110.223:9101/health),
  `--out FILE`, `--dry-run` (print plan, mutate nothing), `--confirm` (real mutation).
- Plan: assert raw besteffort -> cp to configs/spectrum.yaml -> scripts/deploy.sh spectrum <host> ->
  ssh restart wanctl@spectrum.service -> verify besteffort on both NICs via phase227-qdisc-verify.sh.
- Without --confirm it REFUSES ("rollback mutates production/repo config; rerun with --confirm").

`scripts/phase227-qdisc-verify.sh` (read-only, fail-closed):
- `--expected-mode <besteffort|diffserv4>` (required), `--ssh-host`, `--router-iface spec-router`,
  `--modem-iface spec-modem`, `--out FILE`, `--dry-run`, and TEST-ONLY `--router-input FILE` /
  `--modem-input FILE` (parse qdisc from a file instead of SSH) + `--simulate-ssh-failed`.
- Emits a JSON proof with per-NIC got-mode + match; fail-closed on ssh failure or mode mismatch.

`configs/spectrum.yaml` current state (verified):
- line 44: `diffserv: diffserv4  # Phase 227 (AB-03): candidate diffserv4 wash A/B; leave live for Phase 228 verdict`
- line 45: `allow_wash: true  # ...` (UNCHANGED on rollback)
- DL 920 / UL 18, `docsis_mode: true` (UNCHANGED on rollback)
- Rollback flips line 44 back to `diffserv: besteffort` with a Phase 228 rollback annotation; nothing else changes.

Restoration framing (review MEDIUM — pick ONE, do not claim both): the rollback VALUE-restores
the besteffort config and REWRITES the comment annotation. So the proof asserts VALUE-equivalence to
Snapshot A on diffserv/allow_wash/rates/docsis_mode PLUS a full redacted diff that shows no
UNRELATED drift (the only differing lines are the intended diffserv value + the annotation comment).
Do NOT describe this as "byte-identical including comments" — that contradicts the annotation
rewrite. The Snapshot A redacted yaml (committable) is the repo comparison reference; the
operator-private raw `deployed-spectrum.yaml` (mode 0600, outside git) is the rollback SOURCE
(snapshot/rollback proof split: committable proof vs operator-private raw source).

`phase227-rollback.sh --out` (verified flag): writes the rollback RUN proof to a path. If omitted it
writes to the Phase 227 default proof path — which would pollute Phase 227 evidence. The 228 rollback
MUST pass `--out .../228-verdict-evidence-gated-decision-closeout/evidence/phase227-rollback-run-proof.json`.

Health vocabulary (review MEDIUM): the existing health scripts/payload use `status == "healthy"`,
NOT a "GREEN" string. The proof's health leg accepts the health-script vocabulary (status ==
"healthy" + no crashloop indicator) and DEFINES the accepted set explicitly — do not introduce an
undefined "GREEN" token.

Qdisc read timing (review LOW): a single immediate qdisc read right after restart can be racy. The
production leg either reuses phase227-rollback.sh's own qdisc proof OR does a bounded retry
(e.g. a few attempts with a short sleep) before declaring the production leg failed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the triple restoration-proof generator (file-fed testable)</name>
  <files>scripts/phase228-rollback-proof.py</files>
  <read_first>
    - scripts/phase227-qdisc-verify.sh (the production proof leg — invoke with --expected-mode besteffort; understand its JSON proof shape + the --router-input/--modem-input file-fed test path)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/qdisc-verify-rollback-late.json (shape reference for phase228-rollback-proof.json)
    - configs/spectrum.yaml (the repo leg — the diffserv/allow_wash/rate lines that must byte-match Snapshot A besteffort)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-05 triple proof; snapshot/rollback proof split)
  </read_first>
  <action>
    Create `scripts/phase228-rollback-proof.py` emitting a triple restoration proof JSON mirroring the
    phase227 qdisc-verify / rollback-late.json shape. Three legs:
    (1) `repo`: VALUE-compare the configs/spectrum.yaml key fields (diffserv mode, allow_wash, DL/UL
        rates, docsis_mode) against a Snapshot A besteffort reference — accept a `--snapshot-ref FILE`
        (the committable Snapshot A redacted yaml) and assert `diffserv: besteffort`, `allow_wash: true`,
        unchanged rates/docsis_mode — PLUS a full secret-safe redacted diff of the whole config against
        the snapshot-ref so UNRELATED drift is caught (the only allowed differing lines are the intended
        diffserv value + the annotation comment; any other differing line fails the repo leg). Record
        pass/fail + the compared values + the set of differing lines (redacted; no secret bytes). Do NOT
        claim byte-identical-including-comments — the framing is value-restored + annotated.
    (2) `production`: shell out to `scripts/phase227-qdisc-verify.sh --expected-mode besteffort` (pass
        through `--ssh-host`, `--out` to a temp), parse its JSON, require besteffort on BOTH spec-router
        AND spec-modem. Avoid a racy single immediate read: either reuse phase227-rollback.sh's own
        qdisc proof or do a bounded retry (a few attempts, short sleep) before failing the leg. For
        testability accept `--qdisc-proof FILE` to read a pre-captured qdisc-verify JSON instead of
        invoking SSH (so the proof logic is lockable without a live host).
    (3) `health`: accept a `--health-json FILE` (or `--health-url` to curl) and require the
        health-script's accepted-healthy vocabulary — `status == "healthy"` (define the accepted set
        explicitly; do NOT use an undefined "GREEN" token) with no crashloop indicator (e.g. NRestarts
        not climbing) post-restart.
    Overall `restored` = all three legs pass (fail-closed: any leg fail => restored:false, NOT assumed).
    `--output` (default -) writes the JSON sorted-keys. Add a docstring noting D-05 triple proof and that
    the real rollback mutation is the operator-gated phase227-rollback.sh --confirm, not this script.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/phase228-rollback-proof.py').read())" && .venv/bin/ruff check scripts/phase228-rollback-proof.py && python3 scripts/phase228-rollback-proof.py --help 2>&1 | grep -qi "snapshot-ref\|qdisc-proof\|health-json"</automated>
  </verify>
  <done>Module parses, passes ruff, `--help` shows the file-fed test inputs (--snapshot-ref / --qdisc-proof / --health-json); the proof is fail-closed (any leg fail => restored:false) and emits a phase227-shaped JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test — triple proof pass/fail, fail-closed, file-fed (no live host)</name>
  <files>tests/test_phase228_rollback_proof.py</files>
  <read_first>
    - scripts/phase228-rollback-proof.py (the three-leg logic under test)
    - scripts/phase227-qdisc-verify.sh (the --router-input/--modem-input file-fed path the test can drive to produce a real qdisc-verify JSON fixture, or hand-build the JSON fixture)
    - tests/ (closest existing proof/verify test as the style analog)
  </read_first>
  <action>
    Add pytest checks driven entirely by fixtures (no live host): (a) all three legs pass (repo
    value-matches besteffort snapshot-ref with only the intended diffserv/annotation lines differing,
    qdisc-proof shows besteffort on both NICs, health status=="healthy") => restored:true; (b) repo leg
    fail (spectrum.yaml still diffserv4) => restored:false; (b2) repo leg fail on UNRELATED drift — a
    config that has besteffort + correct rates but ALSO an unexpected differing line vs the snapshot-ref
    => restored:false (proves the full redacted diff catches drift, not only selected-line checks);
    (c) production leg fail (qdisc-proof shows diffserv4 or only one NIC besteffort) => restored:false;
    (d) health leg fail (status != "healthy" / crashloop indicator) => restored:false; (d2) health
    vocabulary: a payload with status=="healthy" passes and a payload using some other token does NOT
    silently pass (the accepted set is status=="healthy", not an undefined "GREEN"); (e) a
    missing/unparseable input is fail-closed (restored:false + error), never assumed-restored; (f) the
    emitted JSON shape matches the phase227 proof convention (per-leg pass/fail + overall restored).
    Build fixtures via the qdisc-verify --router-input/--modem-input file-fed path and temp yaml/health
    JSON files.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase228_rollback_proof.py -q</automated>
  </verify>
  <done>All tests pass; the suite fails if a future edit makes any leg non-fail-closed (assumes restored on bad/missing input) or breaks the all-three-pass requirement.</done>
</task>

<task type="manual">
  <name>Task 3: Operator-gated live rollback + commit the restoration proof</name>
  <files>
    configs/spectrum.yaml
    .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json
  </files>
  <read_first>
    - scripts/phase227-rollback.sh (the --confirm mutation path + its --raw-dir requirement — the operator supplies the operator-private Snapshot A raw dir)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json (confirm the committed reject verdict drives this rollback — verdict-first ordering, D-04)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-04 operator-at-keyboard; D-05 triple proof)
  </read_first>
  <action>
    OPERATOR-AT-KEYBOARD (autonomous:false — do NOT fire unattended). After the operator confirms the
    228-02 reject verdict is committed: (1) Dry-run first — `scripts/phase227-rollback.sh --raw-dir
    <operator-private-snapshot-a-dir> --dry-run` and show the ordered plan. (2) On operator approval,
    run the real mutation WITH AN EXPLICIT 228 --out so the rollback run proof lands in Phase 228
    evidence, not the Phase 227 default path:
    `scripts/phase227-rollback.sh --raw-dir <dir>
    --out .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase227-rollback-run-proof.json
    --confirm` (value-restores Snapshot A besteffort to configs/spectrum.yaml, deploys via
    scripts/deploy.sh spectrum, restarts wanctl@spectrum, verifies besteffort on both NICs). (3) Replace
    the Phase 227 candidate annotation on the diffserv line in configs/spectrum.yaml with a Phase 228
    rollback annotation (allow_wash / rates / docsis_mode unchanged) — value-restored + annotated, NOT
    byte-identical-including-comments. (4) Generate the triple proof:
    `scripts/phase228-rollback-proof.py --snapshot-ref <snapshot-a-redacted.yaml>
    --ssh-host cake-shaper --health-url http://10.10.110.223:9101/health
    --output .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json`.
    Confirm restored:true (repo value-restored with no unrelated drift + production besteffort on both
    NICs + health status=="healthy"). If the proof is restored:false, STOP and surface the failing leg —
    do not proceed to closeout. This is the one production-mutating step in the phase; treat it
    conservatively per the wanctl change policy.
  </action>
  <verify>
    <automated>grep -Eq '^[[:space:]]*diffserv:[[:space:]]+besteffort([[:space:]#]|$)' configs/spectrum.yaml && test -f .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json && test -f .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase227-rollback-run-proof.json && python3 -c "import json; p=json.load(open('.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-rollback-proof.json')); assert p['restored'] is True, p; print('restored to besteffort, triple proof passed; rollback run proof in 228 evidence')"</automated>
  </verify>
  <done>configs/spectrum.yaml is diffserv: besteffort (allow_wash/rates/docsis_mode unchanged); the rollback run proof is written to the Phase 228 evidence dir (explicit --out, not the Phase 227 default); phase228-rollback-proof.json shows restored:true across all three legs (repo value-restored no-unrelated-drift + both-NIC qdisc + health status=="healthy"); the rollback was operator-confirmed (not fired unattended); if any leg had failed, the executor stopped rather than reporting success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> Spectrum cake-shaper (SSH, MUTATING via --confirm) | The only production-mutating step in the phase; operator-at-keyboard, gated behind phase227-rollback.sh --confirm (D-04). |
| operator-private Snapshot A raw dir -> repo configs/spectrum.yaml | Raw besteffort bytes (mode 0600, outside git) value-restore the repo config; the committable proof value-compares + full-redacted-diffs against the redacted Snapshot A reference (value-restored + annotated, not byte-identical-including-comments). |
| rollback-proof.py -> phase227-qdisc-verify.sh (SSH read-only) | Production proof leg reads qdisc mode on both NICs; fail-closed on ssh failure or mode mismatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-08 | Tampering | Unattended/unauthorized production CAKE-mode change | mitigate | autonomous:false; phase227-rollback.sh REFUSES without --confirm; dry-run shown before the real mutation; operator-at-keyboard (D-04). |
| T-228-09 | Spoofing | Restoration assumed without proof | mitigate | Triple proof (repo + production both-NIC qdisc + health) fail-closed; restored:false on any leg fail; Task 2 (b)-(e) lock fail-closed (D-05). |
| T-228-10 | Information Disclosure | Operator-private raw Snapshot A bytes leaking into the proof/repo | mitigate | Proof value-compares + full-redacted-diffs against the redacted snapshot-ref and records compared values + redacted differing lines only (no secret bytes); raw dir stays mode 0600 outside git (snapshot/rollback proof split). |
| T-228-11 | Repudiation | No auditable record the rollback was verdict-driven | mitigate | Verdict-first ordering (D-04): phase228-verdict.json committed (228-02) before this rollback; proof JSON committed after. |
| T-228-21 | Repudiation | Rollback run proof pollutes Phase 227 evidence (default --out) | mitigate | Task 3 passes explicit --out into the Phase 228 evidence dir (phase227-rollback-run-proof.json); Task 3 verify asserts it exists there. (HIGH review finding.) |
| T-228-22 | Tampering | Unrelated Spectrum config drift slips through a selected-line-only repo check | mitigate | Repo leg does a full secret-safe redacted diff vs the snapshot-ref; only the intended diffserv-value + annotation lines may differ; Task 2 (b2) locks it. |
| T-228-23 | Spoofing | A non-healthy payload passes the health leg via an undefined "GREEN" token | mitigate | Health leg accepts the existing health-script vocabulary (status=="healthy") with a defined accepted set; Task 2 (d2) locks it. |
| T-228-SC | Tampering | npm/pip installs | accept | No new package installs — reuses the existing rollback/qdisc-verify scripts + stdlib python3. |
</threat_model>

<verification>
- `phase228-rollback-proof.py`: ast.parse + ruff clean; `--help` shows file-fed test inputs.
- `.venv/bin/pytest tests/test_phase228_rollback_proof.py -q` green (all-pass + each-leg fail-closed + unrelated-drift + health-vocabulary cases).
- After the operator-gated rollback: configs/spectrum.yaml is diffserv: besteffort (allow_wash/rates/docsis_mode unchanged), value-restored + annotated.
- The rollback run proof is written to the Phase 228 evidence dir via explicit --out (not the Phase 227 default).
- phase228-rollback-proof.json restored:true (repo value-restored no-unrelated-drift + both-NIC production qdisc + health status=="healthy").
- The rollback was operator-confirmed via --confirm (dry-run shown first); not fired unattended (D-04).
</verification>

<success_criteria>
- Live Spectrum rollback to besteffort wash executed via operator-gated phase227-rollback.sh --confirm WITH an explicit Phase 228 --out (D-04; HIGH review fix).
- configs/spectrum.yaml value-restored to besteffort (allow_wash/rates/docsis_mode unchanged) + annotated — single unambiguous framing, not byte-identical-including-comments.
- Triple restoration proof (repo value-compare + full redacted no-unrelated-drift diff + production qdisc-verify both NICs + health status=="healthy") emitted, restored:true (D-05).
- Proof is fail-closed and file-fed testable without a live host; tests lock the logic (incl. drift + health vocabulary) before the real mutation.
- Sequence verdict -> rollback -> verify holds; closeout (228-04) follows.
</success_criteria>

<output>
Create `.planning/phases/228-verdict-evidence-gated-decision-closeout/228-03-SUMMARY.md` when done
</output>
