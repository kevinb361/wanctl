---
phase: 228-verdict-evidence-gated-decision-closeout
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - .planning/decisions/phase-228-safe13-no-lift.md
  - docs/BRIDGE_QOS.md
  - CHANGELOG.md
  - .planning/todos/pending/2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes.md
  - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json
  - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md
autonomous: true
requirements: [GATE-02, GATE-03, SAFE-13]
must_haves:
  truths:
    - "A dedicated SAFE-13 no-lift decision record exists at .planning/decisions/phase-228-safe13-no-lift.md (mirroring the phase-224 risk-acceptance decision-record format): it states the evidence (reject verdict, EF degraded, no useful per-tin separation), the decision (NO controller-path change), and that the controller path stays zero-diff vs v1.48 — durable + greppable (D-06); it cleanly SEPARATES the two proofs it relies on (the SAFE-13 boundary check proves controller-path zero-diff + ATT byte-identical ONLY — it does NOT verify Spectrum config restoration; the 228-03 rollback proof is the separate Spectrum-restoration evidence) so neither proof is overstated to cover the other"
    - "docs/BRIDGE_QOS.md and CHANGELOG.md record the closeout as a measured harm finding: on the current CRS/Ruckus/bridge topology diffserv4 wash regressed RRUL p99 AND degraded the EF flow it should protect, so besteffort wash is upheld — framed as a STRONGER negative than v1.44 (now actively harmful, not inert classification theater); the v1.44 decision is reaffirmed-with-new-evidence (D-07)"
    - "The closeout cites the verdict JSON (phase228-verdict.json) as the source of the numbers and does NOT duplicate numeric literals beyond the headline — pre-registration single-source discipline is preserved (D-07)"
    - "The v1.49 thesis todo (2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes) is closed-with-prejudice using a DELIBERATE convention choice (NOTE: .planning/todos/ has three terminal dirs — closed/, completed/, done/; pick ONE per the repo's newest-active convention and record a moved-from/moved-to trail in the closed file so the move is auditable), with the verdict attached verbatim, and records an explicit reopen-gate: reopen permitted ONLY on a material topology change (CRS/Ruckus DSCP landing change, or a carrier change off DOCSIS) — not a casual re-test (D-09)"
    - "An EF root-cause research seed is captured (why did diffserv4 wash degrade EF on this topology? — wash bit clearing the mark / tin misclassification / DOCSIS interaction) as a dormant post-v1.49 note, recorded but NOT acted on (D-11)"
    - "SAFE-13 is verified at the phase boundary via scripts/phase225-safe13-boundary-check.sh --anchor v1.48: controller-path source (wan_controller.py, queue_controller.py, cake_signal.py, wan_controller_state.py, backends, alert_engine.py, fusion) is zero-diff vs v1.48 AND ATT config is byte-identical; the boundary proof JSON is committed (SAFE-13)"
    - "Phase 228 closes the PHASE only — the v1.49 milestone archive is explicitly NOT done here; it runs via /gsd-complete-milestone after 228 verifies (D-10)"
  artifacts:
    - path: .planning/decisions/phase-228-safe13-no-lift.md
      provides: "the durable SAFE-13 no-lift decision record (evidence + decision + zero-diff statement)"
      contains: "SAFE-13"
    - path: docs/BRIDGE_QOS.md
      provides: "closeout harm-finding + v1.44 reaffirmation, citing the verdict JSON"
      contains: "besteffort"
    - path: CHANGELOG.md
      provides: "v1.49 closeout entry (besteffort wash upheld; diffserv4 actively harmful on this topology)"
      contains: "besteffort"
    - path: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json
      provides: "controller zero-diff vs v1.48 + ATT byte-identical boundary proof"
      contains: "v1.48"
    - path: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md
      provides: "dormant post-v1.49 EF root-cause research seed (captured, not acted on)"
      contains: "EF"
  key_links:
    - from: docs/BRIDGE_QOS.md
      to: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json
      via: "cites the verdict JSON as the source of the headline numbers (no literal duplication)"
      pattern: "phase228-verdict|verdict"
    - from: .planning/decisions/phase-228-safe13-no-lift.md
      to: scripts/phase225-safe13-boundary-check.sh
      via: "the no-lift decision is corroborated by the zero-diff boundary proof"
      pattern: "SAFE-13|zero-diff|v1.48"
---

<objective>
Record the Phase 228 closeout: the SAFE-13 no-lift decision record, the harm-finding closeout in
docs/BRIDGE_QOS.md + CHANGELOG.md (citing the verdict JSON, not duplicating numbers), the
close-with-prejudice of the v1.49 thesis todo with an explicit reopen-gate, the dormant EF
root-cause seed, and the SAFE-13 phase-boundary proof. Phase 228 closes the PHASE only — the v1.49
milestone archive is a separate /gsd-complete-milestone step (D-10).

Purpose: the verdict (228-02) is reject and the rollback (228-03) restored besteffort; this plan
makes that decision durable and discoverable. GATE-03 (criterion 4) requires the verdict recorded in
docs/BRIDGE_QOS.md, configs/spectrum.yaml (done in 228-03), and CHANGELOG.md, including the
negative-result path as a valid close. D-06 puts the no-lift SAFE-13 call in a dedicated
.planning/decisions/ record (phase-224 precedent). D-07 frames the finding precisely — diffserv4
wash is ACTIVELY HARMFUL to EF on this topology (stronger than v1.44's inert "theater"), so v1.44 is
reaffirmed-with-new-evidence. D-09 closes the thesis todo with prejudice + a topology-change-only
reopen gate. D-11 seeds the EF root-cause question without acting on it. SAFE-13 is verified at the
boundary (controller zero-diff vs v1.48; ATT byte-identical).

Output: the decision record, the two closeout docs, the closed todo, the EF seed, and the committed
SAFE-13 boundary proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md
@.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json

# Decision-record format precedent
@.planning/decisions/phase-224-clean-restart-risk-acceptance.md
# Closeout doc targets
@docs/BRIDGE_QOS.md
@CHANGELOG.md
# The thesis todo to close-with-prejudice
@.planning/todos/pending/2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes.md
# SAFE-13 boundary check (reuse with --anchor v1.48)
@scripts/phase225-safe13-boundary-check.sh

<interfaces>
<!-- Verified facts. This plan is documentation + a read-only boundary check; no production touch. -->

`scripts/phase225-safe13-boundary-check.sh`:
- `--anchor REF` (default v1.48), `--out PATH`. Compares controller-path source + ATT config against
  the anchor; emits a JSON with controller-path zero-diff + ATT byte-identical proof. The v1.48 git
  tag exists. Controller targets it protects: wan_controller.py, wan_controller_state.py,
  queue_controller.py, cake_signal.py, backends, alert_engine.py, fusion (+ ATT config).

The verdict JSON (phase228-verdict.json, committed in 228-02) is the single source for the headline
numbers — the closeout cites it; do NOT restate the per-gate literals in prose beyond the one-line
headline (pre-registration single-source discipline, D-07).

The phase-224 decision record format (precedent for phase-228-safe13-no-lift.md): Symptom/Evidence
sections, a Default Disposition / Decision statement, Evidence Links, and a Sign-Off line. Adapt to:
evidence (reject verdict + EF degraded + no useful per-tin separation), decision (NO controller-path
change; controller path stays zero-diff vs v1.48), reopen condition.

The v1.49 thesis todo lives at
`.planning/todos/pending/2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes.md`. Closing
it = moving/marking it done with the verdict attached verbatim + the reopen-gate (check the repo's
todo close convention — pending/ vs a done/ or closed/ dir, or a status field).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SAFE-13 no-lift decision record</name>
  <files>.planning/decisions/phase-228-safe13-no-lift.md</files>
  <read_first>
    - .planning/decisions/phase-224-clean-restart-risk-acceptance.md (the decision-record format to mirror: Symptom/Evidence/Disposition/Evidence-Links/Sign-Off)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json (the evidence the record cites: outcome=reject, gate_rrul_p99 fail, safe13_lift=false)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-06 dedicated decision record; controller path zero-diff vs v1.48)
  </read_first>
  <action>
    Create `.planning/decisions/phase-228-safe13-no-lift.md` mirroring the phase-224 record structure.
    State: the EVIDENCE (the 228-02 reject verdict — RRUL p99 regression past the locked gate, EF flow
    actively degraded, no useful per-tin separation — cite phase228-verdict.json, do not restate every
    literal); the DECISION (NO SAFE-13 lift — zero controller-path source change; the per-tin backlog
    weighting idea in cake_signal.py and any other controller-path edit is NOT warranted by this
    evidence); the INVARIANT (controller path stays zero-diff vs v1.48, corroborated by the
    phase-boundary check in Task 4); and the OVERRIDE PATH (a lift would require a future explicit
    evidence-gated roadmap decision, not a casual edit). Include Evidence Links — and SEPARATE the two
    distinct proofs so neither is overstated: (i) the CONTROLLER no-lift proof = SAFE-13-BOUNDARY.json
    (controller-path zero-diff vs v1.48 + ATT byte-identical — this does NOT prove anything about
    Spectrum config restoration); (ii) the SPECTRUM rollback proof = phase228-rollback-proof.json
    (besteffort restored on the Spectrum WAN — this does NOT prove controller zero-diff); plus the
    verdict JSON. Add a Sign-Off line consistent with the phase-224 precedent. This is the durable,
    greppable record of the default + expected NO-lift outcome (D-06).
  </action>
  <verify>
    <automated>test -f .planning/decisions/phase-228-safe13-no-lift.md && grep -qi "no.lift\|zero-diff\|v1.48" .planning/decisions/phase-228-safe13-no-lift.md && grep -qi "phase228-verdict" .planning/decisions/phase-228-safe13-no-lift.md</automated>
  </verify>
  <done>The decision record exists, states the no-lift decision + controller zero-diff vs v1.48, cites the verdict JSON, and follows the phase-224 record structure.</done>
</task>

<task type="auto">
  <name>Task 2: Closeout in docs/BRIDGE_QOS.md and CHANGELOG.md (harm finding + v1.44 reaffirmation)</name>
  <files>docs/BRIDGE_QOS.md CHANGELOG.md</files>
  <read_first>
    - docs/BRIDGE_QOS.md (find the existing v1.44 diffserv4 / classification-theater discussion + the bridge-QoS topology section to attach the reaffirmation-with-new-evidence to)
    - CHANGELOG.md (the current top/unreleased section + the entry style to match)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json (cite this; headline only, no literal duplication)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-07 framing: actively harmful > inert theater; v1.44 reaffirmed-with-new-evidence)
  </read_first>
  <action>
    Record the closeout in both docs per the D-07 framing. In docs/BRIDGE_QOS.md: add a Phase 228 /
    v1.49 closeout note stating that on the current CRS/Ruckus/bridge topology, diffserv4 wash
    regressed RRUL p99 latency-under-load AND degraded the marked-EF flow it should protect (a
    realtime-protection FAILURE, not just neutral classification theater), so besteffort wash is
    UPHELD; explicitly reaffirm the v1.44 decision WITH NEW EVIDENCE (now actively harmful, a stronger
    negative). Cite phase228-verdict.json for the numbers; state the one-line headline only (e.g.
    "RRUL p99 +~11.5% past the locked >+5% gate; EF UDP loss ~0.15%->6.8%") and point to the verdict
    JSON + the AB-04 summary for the full figures — do not duplicate the full literal set (D-07
    single-source discipline). In CHANGELOG.md: add a v1.49 closeout entry — "Spectrum diffserv4 wash
    A/B: rejected; besteffort wash upheld; diffserv4 measured actively harmful to EF on this topology;
    controller path unchanged (SAFE-13 no lift)" — matching the existing changelog entry style and
    citing the decision record + verdict. Note the negative-result path is a VALID milestone close.
    PRECISION (review MEDIUM): when the closeout references restoration, state it as the 228-03 proof
    actually established — Spectrum besteffort VALUE-restored + annotated (per phase228-rollback-proof.json),
    NOT "byte-identical to Snapshot A". Do not overstate the rollback proof's scope.
  </action>
  <verify>
    <automated>grep -qi "besteffort" docs/BRIDGE_QOS.md && grep -qi "228\|v1.49\|verdict" docs/BRIDGE_QOS.md && grep -qi "besteffort\|diffserv4" CHANGELOG.md && grep -qi "SAFE-13\|no lift\|upheld\|228\|v1.49" CHANGELOG.md</automated>
  </verify>
  <done>docs/BRIDGE_QOS.md carries the harm-finding + v1.44 reaffirmation citing the verdict JSON (headline only); CHANGELOG.md has the v1.49 closeout entry (besteffort upheld, diffserv4 harmful, SAFE-13 no lift); neither duplicates the full numeric set beyond the headline.</done>
</task>

<task type="auto">
  <name>Task 3: Close-with-prejudice the thesis todo + seed the EF root-cause note</name>
  <files>
    .planning/todos/pending/2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes.md
    .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md
  </files>
  <read_first>
    - .planning/todos/pending/2026-06-03-retest-spectrum-diffserv4-wash-after-local-qos-changes.md (the thesis todo to close + its current frontmatter/status convention)
    - .planning/todos/ (VERIFIED: three terminal dirs exist — closed/, completed/, done/. This is ambiguous; you MUST pick ONE deliberately. Inspect the most-recently-populated dir and any status-field convention in recent closures to determine the repo's NEWEST-active convention, choose it, and record a moved-from/moved-to trail.)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-09 close-with-prejudice + topology-change-only reopen gate; D-11 EF seed captured-not-acted-on)
  </read_first>
  <action>
    (a) Close the v1.49 thesis todo with prejudice using a DELIBERATE convention choice. The repo has
    THREE terminal todo dirs (closed/, completed/, done/) — do NOT guess; inspect which is the
    newest-active convention (most recent closures / any status-field pattern) and pick ONE. Move the
    todo to that location (or set the status field if that is the convention) AND record an explicit
    moved-from/moved-to trail inside the closed file (e.g. "moved from todos/pending/ to todos/<chosen>/
    on 2026-06-04; closed-with-prejudice by Phase 228 verdict") so the close is auditable and the chosen
    convention is documented. Attach the verdict VERBATIM (the outcome + headline from
    phase228-verdict.json) and record the explicit reopen-gate: reopen permitted ONLY on a material
    topology change — e.g. CRS/Ruckus DSCP landing changes, or a carrier change off DOCSIS — NOT a
    casual re-test; note the EF-harm finding makes a flip unlikely absent a topology shift (D-09).
    (b) Create
    `.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md` as a
    DORMANT post-v1.49 research seed: "why did diffserv4 wash degrade the marked-EF flow on this
    topology?" with candidate hypotheses (wash bit clearing the DSCP mark, tin misclassification,
    DOCSIS interaction). Record the signal; explicitly mark it NOT acted on this milestone, do not
    expand v1.49 scope or reopen the rejected A/B (D-11).
  </action>
  <verify>
    <automated>test -f .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md && grep -qi "EF\|diffserv4\|topology" .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/ef-root-cause-seed.md && grep -rqi "reopen\|topology change\|closed\|verdict" .planning/todos/</automated>
  </verify>
  <done>The thesis todo is closed-with-prejudice per the repo convention with the verdict attached verbatim + the topology-change-only reopen gate (D-09); the EF root-cause seed exists as a dormant, not-acted-on note (D-11).</done>
</task>

<task type="auto">
  <name>Task 4: SAFE-13 phase-boundary proof (controller zero-diff vs v1.48 + ATT byte-identical)</name>
  <files>.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the boundary check + its --anchor / --out args and the controller targets it protects)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (SAFE-13 invariant: controller path zero-diff vs v1.48; ATT byte-identical the entire milestone)
  </read_first>
  <action>
    Run `scripts/phase225-safe13-boundary-check.sh --anchor v1.48 --out
    .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json` to
    prove SAFE-13 at the phase boundary: controller-path source (wan_controller.py,
    wan_controller_state.py, queue_controller.py, cake_signal.py, backends, alert_engine.py, fusion)
    is zero-diff vs the v1.48 close AND the ATT config is byte-identical. This is a read-only git/diff
    check — no production touch. The 228 phase changed scripts/ (gate-eval, summary, rollback-proof),
    docs, decisions, and configs/spectrum.yaml (the besteffort revert is a config flip, NOT a
    controller-path source change), so the controller targets must show zero diff. Confirm the emitted
    JSON shows controller zero-diff + ATT byte-identical; if it shows ANY controller-path diff, STOP —
    that would violate SAFE-13 and contradict the no-lift decision record (D-06). SCOPE NOTE (review
    LOW): this boundary check proves CONTROLLER-path + ATT only — it does NOT verify Spectrum config
    restoration (that is the separate 228-03 rollback proof). The closeout must not imply this JSON
    covers the Spectrum besteffort restore. This same check is re-run at v1.49 milestone close (out of
    this phase, D-10).
  </action>
  <verify>
    <automated>test -f .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json && grep -qi "v1.48" .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json && python3 -c "import json; d=json.load(open('.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/SAFE-13-BOUNDARY.json')); print('SAFE-13 boundary proof emitted vs v1.48')"</automated>
  </verify>
  <done>SAFE-13-BOUNDARY.json is emitted against v1.48 and shows controller-path zero-diff + ATT byte-identical; if any controller-path diff appeared the executor stopped (it would contradict the no-lift decision).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> repo docs/decisions/todos (write) | Documentation + decision-record + todo-close edits; no production touch. |
| boundary check -> git (read-only) | phase225-safe13-boundary-check.sh reads git diff vs v1.48 + ATT config; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-12 | Repudiation | No durable record of the no-lift decision | mitigate | Dedicated .planning/decisions/phase-228-safe13-no-lift.md (greppable) + the SAFE-13 boundary JSON corroborate the zero-diff claim (D-06). |
| T-228-13 | Tampering | Closeout silently restates/diverges numeric literals | mitigate | Closeout cites phase228-verdict.json; headline only, no full literal duplication (D-07 single-source discipline). |
| T-228-14 | Tampering | An undetected controller-path change ships under the "no lift" banner | mitigate | Task 4 phase225-safe13-boundary-check.sh --anchor v1.48 proves controller zero-diff; STOP on any diff (SAFE-13). |
| T-228-15 | Repudiation | Thesis todo silently reopenable / re-tested casually | mitigate | Close-with-prejudice + explicit topology-change-only reopen gate recorded verbatim (D-09). |
| T-228-SC | Tampering | npm/pip installs | accept | No new package installs — docs + the existing boundary-check script + stdlib python3. |
</threat_model>

<verification>
- .planning/decisions/phase-228-safe13-no-lift.md exists, states no-lift + zero-diff vs v1.48, cites the verdict JSON.
- docs/BRIDGE_QOS.md + CHANGELOG.md carry the harm-finding + v1.44 reaffirmation (headline only, verdict cited).
- The thesis todo is closed-with-prejudice with the verdict verbatim + topology-change-only reopen gate.
- The EF root-cause seed is captured as dormant / not-acted-on.
- SAFE-13-BOUNDARY.json shows controller zero-diff vs v1.48 + ATT byte-identical.
- No production touch in this plan; the v1.49 milestone archive is explicitly deferred to /gsd-complete-milestone (D-10).
</verification>

<success_criteria>
- SAFE-13 no-lift decision recorded durably in .planning/decisions/ (D-06).
- Closeout in docs/BRIDGE_QOS.md + CHANGELOG.md frames diffserv4 as actively harmful > v1.44 theater; v1.44 reaffirmed-with-new-evidence, citing the verdict JSON (D-07).
- v1.49 thesis todo closed-with-prejudice + topology-change-only reopen gate (D-09).
- EF root-cause seed captured, not acted on (D-11).
- SAFE-13 verified at the phase boundary: controller zero-diff vs v1.48, ATT byte-identical (SAFE-13).
- Phase 228 closes the PHASE only; milestone archive deferred (D-10).
</success_criteria>

<output>
Create `.planning/phases/228-verdict-evidence-gated-decision-closeout/228-04-SUMMARY.md` when done
</output>