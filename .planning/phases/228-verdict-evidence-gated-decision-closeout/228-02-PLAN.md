---
phase: 228-verdict-evidence-gated-decision-closeout
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/phase228-gate-eval.py
  - tests/test_phase228_gate_eval.py
  - .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "scripts/phase228-gate-eval.py ingests scripts/phase226-thresholds.json plus the two authoritative summaries (baseline besteffort + candidate diffserv4) and emits a verdict JSON with per-gate pass/fail across ALL FIVE GATE-01 gate families and an overall accept/reject outcome — the verdict reads the locked numbers, it cannot reverse-fit them (D-01, pre-registration discipline)"
    - "All five gate families are evaluated, none dropped: gate_rrul_p99 (RRUL_P99_REGRESSION_PCT), gate_restart_rate (RESTART_RATE_INCREASE_PCT), gate_transition_rate (TRANSITION_RATE_INCREASE_PCT), gate_ul_stability (UL_STABILITY floor_hit_cycles/soft_red_dwell no-increase + UL p99), gate_tin_separation (the 228-01 tin_separation block: non-BE occupancy + delay gap > NOISE_BAND_MS) — the softer rate gates are computed and labeled corroborating, not silently skipped (D-01, D-03)"
    - "The RRUL p99 gate computes candidate-vs-baseline regression percent from rrul_p99_latency_under_load_ms_mean in the two summaries and fails when regression exceeds RRUL_P99_REGRESSION_PCT (5.0); on the authoritative data this fails (345.1 -> 384.9 ms = +11.5%), so the overall outcome is reject -> rollback"
    - "The restart/transition rate gates derive from the per-summary baseline_window restart_rate / transition_rate (captured health-window NDJSON), and the verdict records the D-03 caveat that 3x60s is a coarse window and these gates are corroborating, not headline; no live journalctl read occurs at verdict time"
    - "The verdict JSON pins provenance uncontestably: the two authoritative dir names AND their artifact-sha256.txt content (or a hash thereof), plus an explicit excluded-attempt note for baseline-20260604T153654Z / candidate-20260604T154125Z (D-08)"
    - "The verdict JSON includes a safe13_lift field set to false with a rationale, recording that the evidence (reject + EF degraded + no useful per-tin separation) does NOT warrant any controller-path change (D-06) — the SAFE-13 decision is represented in the machine output, default + expected NO lift"
    - "Bad/missing input fails fast and structured (mirrors phase224-gate-eval GateEvalError): a missing summary field, an unparseable summary, or a thresholds-schema mismatch produces a structured error and a non-zero exit, never a silently fabricated pass"
    - "The committed phase228-verdict.json is reproducible: re-running the gate-eval over the same committed thresholds + summaries yields the same per-gate verdicts and the same overall outcome (deterministic, no wall-clock-dependent gate value beyond an evaluated_at timestamp)"
  artifacts:
    - path: scripts/phase228-gate-eval.py
      provides: "GATE-02 verdict evaluator: thresholds-JSON + two authoritative summaries -> per-gate verdict JSON with overall accept/reject, pinned provenance, safe13_lift:false"
      contains: "tin_separation"
    - path: tests/test_phase228_gate_eval.py
      provides: "verdict-logic regression: each gate's pass/fail boundary, the authoritative-data reject path, structured fail-fast on bad input, provenance pinning, safe13_lift:false"
      contains: "gate_rrul_p99"
    - path: .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json
      provides: "the committed GATE-02 verdict over the authoritative pair (expected: reject -> rollback)"
      contains: "rollback"
  key_links:
    - from: scripts/phase228-gate-eval.py
      to: scripts/phase226-thresholds.json
      via: "reads the locked GATE-01 numeric gates; numeric literals live only in the JSON"
      pattern: "RRUL_P99_REGRESSION_PCT|NOISE_BAND_MS"
    - from: scripts/phase228-gate-eval.py
      to: scripts/phase226-baseline-summary.py
      via: "reads the tin_separation block + baseline_window + rrul p99 from the 228-01 summaries"
      pattern: "tin_separation|baseline_window|rrul_p99"
---

<objective>
Build `scripts/phase228-gate-eval.py`: the GATE-02 verdict evaluator. It ingests the locked
thresholds JSON plus the two authoritative summaries (besteffort baseline + diffserv4 candidate,
both re-emitted with the tin_separation block in plan 228-01) and emits a verdict JSON with
per-gate pass/fail across all five GATE-01 gate families and an overall accept/reject outcome.

Purpose: GATE-02 (criterion 1) requires the verdict be a MACHINE OUTPUT computed against the
pre-registered GATE-01 thresholds, not a human assertion. The evidence already points to a clean
reject (RRUL p99 +11.5% past the >+5% gate; EF actively degraded), but the verdict must still be
SCRIPTED from the locked numbers it cannot reverse-fit (D-01). The script mirrors the
`phase224-gate-eval.py` structure (GateEvalError fail-fast, per-gate dict, overall outcome) and
the `phase206-gate-check.py` threshold-evaluation precedent. It pins provenance (D-08) and records
the SAFE-13 no-lift decision in the machine output (`safe13_lift: false`, D-06).

Output: the evaluator script, a regression test for every gate boundary + the reject path + fail-fast
+ provenance + safe13_lift, and the committed verdict JSON over the authoritative pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md

# Structural template — copy the GateEvalError fail-fast + per-gate dict + outcome shape
@scripts/phase224-gate-eval.py
# Threshold-evaluation precedent
@scripts/phase206-gate-check.py
# The locked numeric gates the verdict reads (single source of truth)
@scripts/phase226-thresholds.json

<interfaces>
<!-- Verified facts. The evaluator is summary-in -> verdict-out; it must NOT re-parse tc. -->

Authoritative summaries (after plan 228-01 re-emit) expose:
- `rrul_p99_latency_under_load_ms_mean` (float) — D-01 RRUL p99 gate input. baseline=345.148..., candidate~384.9.
- `baseline_window` { restart_rate, transition_rate, floor_hit_cycles, soft_red_dwell_s, mean_window_duration_s, windows } — D-02/D-03/D-04 inputs.
- `marked_ef` { jitter_ms, loss_pct, valid, ... } and `ref_udp_unmarked` { jitter_ms, loss_pct } — AB-04 realtime context (informational here; the AB-04 win/loss framing belongs to GATE-02 criterion 1 but the headline gates are RRUL p99 + UL stability).
- `tin_separation` { per iface: non_be_present, delay_gap_ms, noise_band_ms, useful_separation } — D-05/D-06 gate input (added in 228-01).
- `interfaces`, `provenance`, `schema_version`, `run_count`.

`scripts/phase226-thresholds.json` (read every literal from here; duplicate none into the .py):
- RRUL_P99_REGRESSION_PCT = 5.0 (candidate p99 may not exceed baseline by more than this percent).
- RESTART_RATE_INCREASE_PCT = 10.0, TRANSITION_RATE_INCREASE_PCT = 10.0 (corroborating rate gates).
- UL_STABILITY { UL_P99_REGRESSION_PCT, FLOOR_CHURN_NO_INCREASE, BASELINE_COMPARISON_METRICS [floor_hit_cycles, soft_red_dwell], FAIL_IF_ANY_METRIC_INCREASES_VS_BASELINE }.
- TIN_SEPARATION { OCCUPANCY_REQUIRED, DELAY_GAP_REQUIRED, MAGNITUDE_RULE, NOISE_BAND_MS.value=24.206 }.

phase224-gate-eval.py shape to mirror:
- `GateEvalError(ValueError)` with optional gate_id for structured fail-fast.
- `_gate(verdict, value, **extra)` -> {verdict, value, ...}.
- `evaluate(...)` -> { evaluated_at, gates: {...}, rollback_trigger, outcome }.
- `main()` returns 0 (kept_aligned/accept), 1 (rollback/reject or input_error with gate_id), 2 (continue).

Authoritative provenance to pin (D-08):
- baseline dir: `.../evidence/baseline-20260604T154929Z/` (+ its artifact-sha256.txt)
- candidate dir: `.../evidence/candidate-20260604T163152Z/` (+ its artifact-sha256.txt)
- EXCLUDED aborted attempt: baseline-20260604T153654Z / candidate-20260604T154125Z (qdisc-verify-rollback-late.json at 15:48Z marks the abort).

Gate semantics:
- accept diffserv4 ONLY if every gate passes (clear win, no regression). The first failing gate sets
  rollback_trigger and outcome=reject/rollback. On the authoritative data, gate_rrul_p99 fails first
  (+11.5% > 5.0), so outcome=reject. tin_separation also fails (no useful non-BE separation under
  besteffort; under diffserv4 the gap, if any, plus the EF-harm picture do not constitute a "win").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build phase228-gate-eval.py over all five gate families</name>
  <files>scripts/phase228-gate-eval.py</files>
  <read_first>
    - scripts/phase224-gate-eval.py (copy the GateEvalError fail-fast pattern, _gate helper, evaluate() -> gates/rollback_trigger/outcome shape, and main() exit-code convention — DO NOT copy its canary-specific gates; this is a CAKE A/B verdict)
    - scripts/phase206-gate-check.py (threshold-evaluation precedent for percent-regression gates)
    - scripts/phase226-thresholds.json (read RRUL_P99_REGRESSION_PCT, RESTART/TRANSITION_RATE_INCREASE_PCT, UL_STABILITY, TIN_SEPARATION + NOISE_BAND_MS — never hardcode these literals)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-01 all-five-gates; D-03 rate-gate caveat; D-06 safe13_lift:false; D-08 pinned provenance)
  </read_first>
  <action>
    Create `scripts/phase228-gate-eval.py` with `--thresholds`, `--baseline-summary`,
    `--candidate-summary`, `--baseline-dir`, `--candidate-dir` (for provenance + artifact-sha256
    pinning), and `--output` (default `-`). Reuse a `GateEvalError(ValueError)` with optional gate_id
    for structured fail-fast. `evaluate()` reads the two summaries + thresholds and computes five gates:
    (1) `gate_rrul_p99`: regression_pct = (candidate.rrul_p99_mean - baseline.rrul_p99_mean) /
        baseline.rrul_p99_mean * 100; fail if regression_pct > RRUL_P99_REGRESSION_PCT; HEADLINE.
    (2) `gate_restart_rate`: pct increase of candidate.baseline_window.restart_rate vs baseline; fail
        if > RESTART_RATE_INCREASE_PCT; tag corroborating:true + a D-03 caveat note ("3x60s coarse
        window; absolute counts tiny; corroborating not headline").
    (3) `gate_transition_rate`: same shape vs TRANSITION_RATE_INCREASE_PCT; corroborating:true + caveat.
    (4) `gate_ul_stability`: fail if candidate floor_hit_cycles or soft_red_dwell increases vs baseline
        (FAIL_IF_ANY_METRIC_INCREASES_VS_BASELINE) AND, if a UL p99 field is present, fail on
        UL_P99_REGRESSION_PCT; HEADLINE.
    (5) `gate_tin_separation`: read candidate.tin_separation per interface; pass ONLY if OCCUPANCY_REQUIRED
        satisfied (non_be_present) AND DELAY_GAP_REQUIRED satisfied (useful_separation true on at least the
        primary interface) per the MAGNITUDE_RULE (delay_gap_ms > NOISE_BAND_MS); else fail (no useful
        non-BE separation). Treat missing/None tin_separation as fail-closed with a gate_id.
    Compute rollback_trigger = first failing gate (deterministic gate order), outcome = "accept" if all
    pass else "reject" (map to exit 0 / 1). Emit a verdict dict: { evaluated_at, thresholds_schema_version,
    gates{...}, rollback_trigger, outcome, headline_gates:[gate_rrul_p99, gate_ul_stability],
    corroborating_gates:[gate_restart_rate, gate_transition_rate], safe13_lift:false with a rationale
    string (reject + EF degraded + no useful per-tin separation => no controller-path change, D-06),
    provenance:{ baseline_dir, candidate_dir, baseline_artifact_sha256, candidate_artifact_sha256 (read
    each dir's artifact-sha256.txt), excluded_attempt:[baseline-20260604T153654Z, candidate-20260604T154125Z]
    with the D-08 note } }. Fail fast + structured on any missing summary field, unparseable summary,
    or thresholds_schema_version mismatch. main() writes JSON (sorted keys) and returns 0/1/2 like the
    224 precedent. Add a top-of-file docstring stating the verdict reads locked numbers and cannot
    reverse-fit them (pre-registration discipline).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/phase228-gate-eval.py').read())" && .venv/bin/ruff check scripts/phase228-gate-eval.py && python3 scripts/phase228-gate-eval.py --help 2>&1 | grep -qi "candidate-summary"</automated>
  </verify>
  <done>Module parses, passes ruff, `--help` lists `--candidate-summary` / `--thresholds`; evaluate() computes all five gates reading literals only from the thresholds JSON; bad input raises a structured GateEvalError; no numeric gate literal is hardcoded in the .py.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gate-eval over the authoritative pair, commit the verdict JSON</name>
  <files>.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json</files>
  <read_first>
    - scripts/phase228-gate-eval.py (the evaluator from Task 1 + its exact CLI arg names)
    - .planning/phases/228-verdict-evidence-gated-decision-closeout/228-CONTEXT.md (D-08 the authoritative pair + excluded attempt; D-04 verdict computed/committed FIRST, before any rollback)
  </read_first>
  <action>
    Create the phase 228 evidence dir if absent. Run `scripts/phase228-gate-eval.py` with
    `--thresholds scripts/phase226-thresholds.json`,
    `--baseline-summary .../evidence/baseline-20260604T154929Z/baseline-summary.json`,
    `--candidate-summary .../evidence/candidate-20260604T163152Z/baseline-summary.json`,
    `--baseline-dir .../evidence/baseline-20260604T154929Z`,
    `--candidate-dir .../evidence/candidate-20260604T163152Z`,
    `--output .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json`.
    This is READ-ONLY over committed evidence — no live host, no production touch (D-04: verdict is
    computed and committed BEFORE any rollback). Confirm the emitted verdict: outcome=reject,
    rollback_trigger=gate_rrul_p99 (RRUL p99 +~11.5% > 5.0), safe13_lift=false, provenance pins both
    dir shas and the excluded attempt. The verdict JSON is the auditable, reproducible record the
    closeout (228-04) cites; do NOT hand-edit it.
  </action>
  <verify>
    <automated>test -f .planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json && python3 -c "import json; v=json.load(open('.planning/phases/228-verdict-evidence-gated-decision-closeout/evidence/phase228-verdict.json')); assert v['outcome']=='reject', v['outcome']; assert v['safe13_lift'] is False; assert v['gates']['gate_rrul_p99']['verdict']=='fail'; print('verdict reject confirmed, rollback_trigger=', v['rollback_trigger'])"</automated>
  </verify>
  <done>phase228-verdict.json exists with outcome=reject, gate_rrul_p99 failing, safe13_lift=false, and pinned provenance for the authoritative pair + excluded attempt; the file is a machine output (not hand-edited); no production touch occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Regression test — gate boundaries, reject path, fail-fast, provenance, safe13_lift</name>
  <files>tests/test_phase228_gate_eval.py</files>
  <read_first>
    - scripts/phase228-gate-eval.py (the evaluate() function + GateEvalError under test)
    - scripts/phase226-thresholds.json (the locked literals the boundary tests assert against)
    - tests/ (closest existing gate-eval / threshold test as the style analog; check for a test_phase224_gate_eval or similar)
  </read_first>
  <action>
    Add pytest checks with synthetic summary dicts (no live data needed): (a) RRUL p99 boundary —
    +4.9% passes gate_rrul_p99, +5.1% fails (proves it reads RRUL_P99_REGRESSION_PCT, not a hardcode);
    (b) the authoritative-shaped reject — candidate p99 ~11.5% over baseline => outcome=reject,
    rollback_trigger=gate_rrul_p99; (c) restart/transition rate gates compute the percent increase and
    carry corroborating:true + the D-03 caveat note; (d) gate_ul_stability fails when candidate
    floor_hit_cycles or soft_red_dwell increases vs baseline; (e) gate_tin_separation passes only with
    non-BE occupancy AND delay_gap_ms > NOISE_BAND_MS, fails on missing/None tin_separation (fail-closed
    with a gate_id); (f) an all-pass synthetic (candidate beats baseline on every gate, with useful tin
    separation) yields outcome=accept (proves accept is reachable — the script does not reverse-fit a
    reject); (g) fail-fast: a summary missing rrul_p99_mean, an unparseable summary, and a
    thresholds_schema_version mismatch each raise GateEvalError / non-zero exit, never a fabricated pass;
    (h) the verdict carries safe13_lift=false + a rationale and provenance pins both dirs + the excluded
    attempt. Use temp files / dict fixtures; assert literals come from a passed thresholds dict.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase228_gate_eval.py -q</automated>
  </verify>
  <done>All tests pass; the suite fails if a future edit hardcodes a gate literal, drops a gate family, makes accept unreachable (reverse-fit), removes the fail-closed tin-separation/fail-fast guards, or drops safe13_lift / provenance pinning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> committed evidence + thresholds (read-only) | The evaluator reads committed summaries + the locked thresholds JSON; no live host, no production touch at verdict time (D-04). |
| thresholds JSON -> verdict | The verdict reads pre-registered locked numbers; it cannot reverse-fit them (pre-registration discipline, D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-04 | Tampering | Verdict reverse-fit to a desired reject/accept | mitigate | Literals read only from the thresholds JSON; Task 3 (a)/(f) prove the boundary is threshold-driven and accept is reachable — the script computes, it does not assert. |
| T-228-05 | Spoofing | Bad/empty input silently producing a pass | mitigate | GateEvalError fail-fast on missing field / unparseable summary / schema mismatch (Task 3 g); fail-closed tin-separation. |
| T-228-06 | Repudiation | Verdict provenance contestable | mitigate | Verdict pins both authoritative dir names + their artifact-sha256 and the excluded aborted attempt (D-08). |
| T-228-07 | Tampering | Hand-edited verdict JSON | mitigate | Verdict is a machine output; Task 2 done-criterion forbids hand-edit; re-run reproducibility asserted. |
| T-228-SC | Tampering | npm/pip installs | accept | No new package installs — pure-stdlib python3 evaluator. |
</threat_model>

<verification>
- `ast.parse` + `ruff check` clean on phase228-gate-eval.py.
- `--help` lists `--candidate-summary` / `--thresholds`.
- Verdict over the authoritative pair: outcome=reject, rollback_trigger=gate_rrul_p99, safe13_lift=false, provenance pinned.
- `.venv/bin/pytest tests/test_phase228_gate_eval.py -q` green (all five gates, reject + accept paths, fail-fast).
- No production touch / live host at verdict time (D-04).
</verification>

<success_criteria>
- phase228-gate-eval.py evaluates all five GATE-01 gate families reading locked literals from the thresholds JSON only (D-01).
- Headline gates (RRUL p99, UL stability) vs corroborating rate gates labeled; D-03 caveat recorded.
- Verdict JSON: outcome=reject on the authoritative data, pinned provenance + excluded attempt (D-08), safe13_lift=false rationale (D-06).
- Structured fail-fast on bad input; accept path reachable (no reverse-fit).
- Regression test locks every gate boundary + reject/accept + fail-fast + provenance + safe13_lift.
</success_criteria>

<output>
Create `.planning/phases/228-verdict-evidence-gated-decision-closeout/228-02-SUMMARY.md` when done
</output>
