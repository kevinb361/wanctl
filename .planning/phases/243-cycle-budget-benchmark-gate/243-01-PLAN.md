---
phase: 243-cycle-budget-benchmark-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase243-thresholds.json
  - .planning/phases/243-cycle-budget-benchmark-gate/243-BENCHMARK-PREREGISTRATION.md
  - scripts/phase243-safe17-boundary-check.sh
  - tests/test_phase243_safe17_verifier.py
  - tests/test_phase243_prereg.py
autonomous: true
requirements: [BENCH-02, SAFE-17]
user_setup: []

must_haves:
  truths:
    - "Frozen D-04 gate thresholds exist in a committed JSON before any benchmark data is collected"
    - "A human-readable pre-registration narrates the gate and references the frozen thresholds without duplicating numeric literals"
    - "Any src/wanctl edit vs the Phase 242 close anchor fails the SAFE-17 boundary verifier"
    - "The SAFE-17 mirror test is pinned to the 243 close anchor, not HEAD"
  artifacts:
    - path: "scripts/phase243-thresholds.json"
      provides: "D-04/D-04a/D-04b/D-04c frozen gate thresholds (single source of truth)"
      contains: "CYCLE_P99_ABS_CEILING_MS"
    - path: ".planning/phases/243-cycle-budget-benchmark-gate/243-BENCHMARK-PREREGISTRATION.md"
      provides: "Pre-registration narrative committed before data collection"
      min_lines: 30
    - path: "scripts/phase243-safe17-boundary-check.sh"
      provides: "Fail-closed empty-src/wanctl-diff boundary verifier (measurement-only posture)"
      contains: "--self-test"
    - path: "tests/test_phase243_safe17_verifier.py"
      provides: "Mirror test pinned to 243 PHASE_CLOSE_ANCHOR"
      contains: "PHASE_CLOSE_ANCHOR"
    - path: "tests/test_phase243_prereg.py"
      provides: "Pre-registration presence/shape assertions"
  key_links:
    - from: "243-BENCHMARK-PREREGISTRATION.md"
      to: "scripts/phase243-thresholds.json"
      via: "narrative references the JSON as single source of truth"
      pattern: "phase243-thresholds\\.json"
    - from: "scripts/phase243-safe17-boundary-check.sh"
      to: "Phase 242 close anchor fcc2e15b"
      via: "git diff --name-only ANCHOR HEAD -- src/wanctl/ must be empty"
      pattern: "fcc2e15b"
---

<objective>
Establish the Phase 243 BENCH-02 pre-registration discipline and the SAFE-17 boundary gate before any benchmark scaffolding or evidence exists.

Purpose: BENCH-02 requires the gate thresholds to be committed BEFORE data collection so the verdict cannot be rationalized post-hoc. SAFE-17 requires a fail-closed verifier proving this measurement-only phase makes zero controller-path edits.

Output: A frozen thresholds JSON (single source of truth for D-04 figures), a human-readable pre-registration markdown, a simplified empty-diff SAFE-17 verifier, and the two mirror/presence unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md

<interfaces>
<!-- Frozen anchor facts the executor needs. Extracted from codebase + PATTERNS.md. -->
<!-- Phase 242 close anchor (verified via git log + safe17-boundary-242.json head_commit): -->
fcc2e15b  ("test(242-05): refresh SAFE-17 boundary evidence")
fcc2e15bd9d01ac13064a0072bea61ac40218e6f  (full SHA)

<!-- phase206-thresholds.json shape to mirror: -->
{ "thresholds_schema_version": 1, "<NUMERIC_KEY>": <value>, ..., "_notes": "single source of truth; do not duplicate literals in docs" }

<!-- D-04 frozen figures (from 243-CONTEXT.md decisions; commit exactly these, pre-committed): -->
CYCLE_AVG_REGRESSION_PCT = 20.0   (fping avg delta vs same-run icmplib arm, D-04)
CYCLE_P99_REGRESSION_PCT = 20.0   (fping p99 delta vs same-run icmplib arm, D-04)
CYCLE_P99_ABS_CEILING_MS = 10.0   (absolute p99 ceiling, 5x headroom on 50ms, D-04)
CPU_DELTA_PCT_POINTS = 2.0        (CPU% delta bound vs icmplib arm, D-04)
ZOMBIES_MAX = 0                   (D-04a strictly zero)
TASKS_BOUND = <pre-commit integer>  (D-04a Tasks/threads bound above baseline)
STALL_GAP_MS = 100.0              (D-04b: 2x the 50ms budget)
MIN_CYCLES = 10000               (D-04c floor)
MIN_MINUTES = 30                 (D-04c floor)
ICMPLIB_REPRESENTATIVE_AVG_MS = 2.85  (D-02 secondary anchor, Phase 217)
ICMPLIB_REPRESENTATIVE_P99_MS = 6.9   (D-02 secondary anchor, Phase 217)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze D-04 thresholds JSON + pre-registration markdown</name>
  <read_first>
    - scripts/phase206-thresholds.json (entire file — the exact shape to mirror: thresholds_schema_version + numeric literals + _notes single-source rule)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md lines 88-124 (D-04/D-04a/D-04b/D-04c locked figures)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 197-220 (suggested thresholds JSON keys) and lines 337-340 (no-analog pre-reg discipline: numeric literals live ONLY in the JSON)
  </read_first>
  <files>scripts/phase243-thresholds.json, .planning/phases/243-cycle-budget-benchmark-gate/243-BENCHMARK-PREREGISTRATION.md</files>
  <action>
    Create scripts/phase243-thresholds.json mirroring phase206-thresholds.json shape: a flat object with thresholds_schema_version (1), the eleven D-04 numeric keys named in the interfaces block (CYCLE_AVG_REGRESSION_PCT, CYCLE_P99_REGRESSION_PCT, CYCLE_P99_ABS_CEILING_MS, CPU_DELTA_PCT_POINTS, ZOMBIES_MAX, TASKS_BOUND, STALL_GAP_MS, MIN_CYCLES, MIN_MINUTES, ICMPLIB_REPRESENTATIVE_AVG_MS, ICMPLIB_REPRESENTATIVE_P99_MS), and a _notes value stating this is the BENCH-02 single source of truth committed before data, covering D-04/D-04a/D-04b/D-04c, and forbidding literal duplication in markdown. Pre-commit a concrete integer for TASKS_BOUND (e.g. 4) per Claude's discretion in 243-CONTEXT.md; document the choice in the markdown. Use the exact D-04 figures above — do NOT soften, label "v1", or defer any threshold.
    Create 243-BENCHMARK-PREREGISTRATION.md: narrate the 8-arm design (2 backends x {idle,under-load} x 2 WANs per D-01b), the D-02 PRIMARY basis (same-run fping minus icmplib delta) vs the SECONDARY historical 2.85ms/6.9ms representativeness anchor, each gate's pass condition in prose, and the D-04c validity floor. Reference scripts/phase243-thresholds.json as the single source of the numeric values; do NOT restate the numbers inline (single-source discipline). State that "keep icmplib" is a valid passing close (gate blocks only on regression). State the BENCH-02 ordering rule: this artifact and the thresholds JSON are committed before any evidence is collected.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('scripts/phase243-thresholds.json')); assert d['CYCLE_P99_ABS_CEILING_MS']==10.0 and d['ZOMBIES_MAX']==0 and d['MIN_CYCLES']==10000 and '_notes' in d, d"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('scripts/phase243-thresholds.json'))"` exits 0 (valid JSON)
    - The JSON contains all eleven D-04 keys plus thresholds_schema_version and _notes
    - CYCLE_P99_ABS_CEILING_MS == 10.0, ZOMBIES_MAX == 0, MIN_CYCLES == 10000, MIN_MINUTES == 30 (D-04 figures unsoftened)
    - 243-BENCHMARK-PREREGISTRATION.md contains the substring "phase243-thresholds.json"
    - 243-BENCHMARK-PREREGISTRATION.md does NOT inline the numeric thresholds (single-source discipline: e.g. `grep -c '10.0' 243-BENCHMARK-PREREGISTRATION.md` should not be used to restate ceilings)
    - 243-BENCHMARK-PREREGISTRATION.md states "keep icmplib" is a valid passing close
  </acceptance_criteria>
  <done>Thresholds JSON frozen with exact D-04 figures; pre-registration markdown narrates the gate and references the JSON as single source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Clone-and-simplify the SAFE-17 boundary verifier (empty-diff posture) + prereg test</name>
  <read_first>
    - scripts/phase242-safe17-boundary-check.sh (entire file — the clone source; note the multi-anchor block lines 17-30, emit_evidence heredoc lines 58-209, --out confinement lines 419-434, dirty-tree gate lines 464-493, core boundary check lines 495-504, --self-test lines 212-256)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 44-94 (CRITICAL simplification: drop protected-body/scorer-guard/frozen-file machinery; allowlist becomes empty set; ANCHOR="fcc2e15b"; emit_evidence keeps only the empty-diff fields)
    - .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json (the evidence JSON shape to mirror — strip phase239/240/241, rtt_seam_*, reflector_scorer_unchanged, protected_body, measure_rtt_fping_scorer_guard fields)
  </read_first>
  <files>scripts/phase243-safe17-boundary-check.sh, tests/test_phase243_prereg.py</files>
  <action>
    Clone scripts/phase242-safe17-boundary-check.sh to scripts/phase243-safe17-boundary-check.sh and SIMPLIFY to the measurement-only posture. Replace the multi-anchor block with a single ANCHOR="fcc2e15b" (Phase 242 close), OUT=".planning/phases/243-cycle-budget-benchmark-gate/evidence/safe17-boundary-243.json", and ALLOWED_OUT_PREFIX=".planning/phases/243-cycle-budget-benchmark-gate/evidence/". DROP: the PHASE239/240/241 anchor constants, check_measure_rtt_fping_scorer_guard, protected_body_ok_with_measure_rtt_exception, the phase239-protected-body-diff.py call, and all per-frozen-file git diff --quiet blocks. KEEP VERBATIM: the --out path confinement (reject src/wanctl targets, reject `..` components, realpath must stay inside ALLOWED_OUT_PREFIX), the fail-closed dirty-tree gate (unstaged/staged/untracked src/wanctl all fail), and the --self-test (git worktree add --detach at HEAD, commit a disallowed src/wanctl/queue_controller.py edit, assert it trips, clean up, assert live tree not left dirty). The core boundary check becomes: CHANGED_PATHS = git diff --name-only ANCHOR_SHA HEAD -- src/wanctl/ ; if non-empty, emit a "243 is measurement-only; no src/wanctl edits permitted" violation, emit_evidence false, exit 1. Reduce emit_evidence to keep only: anchor, anchor_sha, head_commit, changed_paths, disallowed_paths, controller_path_diff_count, dirty_tree, dirty_tree_clean, passed, checked_at, notes; write via out.write_text(json.dumps(record, indent=2, sort_keys=True) + "\n").
    Create tests/test_phase243_prereg.py asserting: scripts/phase243-thresholds.json exists and parses; it carries the D-04 keys (CYCLE_AVG_REGRESSION_PCT, CYCLE_P99_REGRESSION_PCT, CYCLE_P99_ABS_CEILING_MS, CPU_DELTA_PCT_POINTS, ZOMBIES_MAX, TASKS_BOUND, STALL_GAP_MS, MIN_CYCLES, MIN_MINUTES); 243-BENCHMARK-PREREGISTRATION.md exists and references "phase243-thresholds.json".
    Make the script executable (chmod +x).
  </action>
  <verify>
    <automated>chmod +x scripts/phase243-safe17-boundary-check.sh && bash scripts/phase243-safe17-boundary-check.sh --self-test && .venv/bin/pytest tests/test_phase243_prereg.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/phase243-safe17-boundary-check.sh --self-test` exits 0 and prints that the allowlist rejects a committed src/wanctl/queue_controller.py edit
    - The script contains the substrings `--self-test`, `fcc2e15b`, and `safe17-boundary-243.json`
    - The script does NOT reference `phase239-protected-body-diff.py` or `check_measure_rtt_fping_scorer_guard` (machinery dropped)
    - `grep -c 'PHASE239_CLOSE_ANCHOR\|PHASE240_CLOSE_ANCHOR\|PHASE241_CLOSE_ANCHOR' scripts/phase243-safe17-boundary-check.sh` returns 0
    - `.venv/bin/pytest tests/test_phase243_prereg.py -x` exits 0
  </acceptance_criteria>
  <done>Simplified empty-diff SAFE-17 verifier passes --self-test; prereg test confirms thresholds JSON + markdown exist with D-04 keys.</done>
</task>

<task type="auto">
  <name>Task 3: SAFE-17 mirror test pinned to the 243 close anchor</name>
  <read_first>
    - tests/test_phase241_safe17_verifier.py (entire file — the clone source: ROOT/VERIFIER/EVIDENCE constants lines 10-20, PHASE_CLOSE_ANCHOR pin lines 22-25, run() helper lines 37-47, detached_worktree fixture lines 50-61, commit_worktree_change helper lines 64-79, static contract test lines 82-111, fail-mode tests lines 119-135)
    - scripts/phase243-safe17-boundary-check.sh (the script under test — created in Task 2)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 97-128 (adaptations: assert "safe17-boundary-243.json"; assert empty-set posture not allowlist-regex; KEEP test_fails_on_out_of_allowlist_change + test_fails_on_dirty_src_wanctl_change; DROP protected-body / rtt-backend-drift / reflector-scorer tests)
  </read_first>
  <files>tests/test_phase243_safe17_verifier.py</files>
  <action>
    Clone tests/test_phase241_safe17_verifier.py to tests/test_phase243_safe17_verifier.py. Re-target VERIFIER to scripts/phase243-safe17-boundary-check.sh and EVIDENCE to the 243 evidence path. Set PHASE_CLOSE_ANCHOR to the 243 close commit SHA placeholder with a clear comment that it MUST be filled at phase close and pinned here (NOT HEAD) so the test stays green as phases 244+ legitimately change the controller (documented rot hazard from project memory). Until the real close SHA exists, pin PHASE_CLOSE_ANCHOR to the current HEAD-equivalent baseline that contains the 243 scaffolding so the worktree tests run; leave a TODO marking it must be repinned to the actual close commit at phase finalize. Copy verbatim the run() subprocess helper, detached_worktree fixture (git worktree add --detach ... PHASE_CLOSE_ANCHOR), and commit_worktree_change helper (uses SKIP_DOC_CHECK=1). Rewrite the static contract test to assert: VERIFIER exists and is executable; "safe17-boundary-243.json" in text; the 243 evidence prefix in text; ANCHOR="fcc2e15b" in text; the measurement-only violation message string ("no src/wanctl edits permitted" or equivalent) in text; and that protected-body machinery strings are absent (assert "phase239-protected-body-diff.py" not in text). KEEP test_fails_on_out_of_allowlist_change (edit any src/wanctl/*.py, commit, expect nonzero + violation message) and test_fails_on_dirty_src_wanctl_change (uncommitted src/wanctl edit, expect "uncommitted, staged, or untracked src/wanctl/ edit"). DROP test_fails_on_protected_body_drift, test_fails_on_rtt_backend_drift_since_phase239, test_reflector_scorer_edit_fails_closed.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_safe17_verifier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase243_safe17_verifier.py -x` exits 0
    - The test file contains a PHASE_CLOSE_ANCHOR constant with a comment that it pins to the 243 close commit (not HEAD)
    - The static contract test asserts both `safe17-boundary-243.json` and `fcc2e15b` appear in the verifier script
    - The fail-mode tests cover an out-of-allowlist committed edit and a dirty src/wanctl edit
    - No test references protected-body or rtt-backend-drift assertions
  </acceptance_criteria>
  <done>Mirror test green, pinned to the 243 close anchor, asserting the empty-diff posture and the two retained fail modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → SAFE-17 verifier `--out` | operator-supplied output path could escape the evidence dir |
| git working tree → boundary verifier | uncommitted controller edits could bypass the diff proof |
| pre-registration → verdict (future) | thresholds could be edited after data to rationalize a pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01 | Tampering | `phase243-safe17-boundary-check.sh --out` | mitigate | Copy verbatim the 242 path confinement: reject src/wanctl targets, reject `..` components, realpath must stay inside ALLOWED_OUT_PREFIX (V12 file/resource control) |
| T-243-02 | Tampering | git working tree (dirty src/wanctl) | mitigate | Copy verbatim the 242 fail-closed dirty-tree gate: unstaged/staged/untracked src/wanctl all fail before any diff proof is accepted |
| T-243-03 | Repudiation | post-hoc threshold rationalization | mitigate | BENCH-02 discipline: thresholds JSON + PREREGISTRATION.md committed in this Wave 1 plan BEFORE any evidence collection (Plan 04); _notes records single-source-of-truth rule |
| T-243-04 | Elevation | controller hot-path drift disguised as scaffolding | mitigate | Empty-src/wanctl-diff posture: ANY changed controller path vs fcc2e15b fails closed; mirror test proves it |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs this phase (RESEARCH Package Legitimacy Gate: N/A) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase243_safe17_verifier.py tests/test_phase243_prereg.py -q` green
- `bash scripts/phase243-safe17-boundary-check.sh --self-test` exits 0
- `.venv/bin/ruff check scripts/ tests/test_phase243_prereg.py tests/test_phase243_safe17_verifier.py` clean
- Zero src/wanctl edits in this plan (it is the verifier of that fact)
</verification>

<success_criteria>
- D-04 thresholds frozen in scripts/phase243-thresholds.json (BENCH-02 single source of truth)
- Pre-registration markdown committed before any benchmark evidence exists
- SAFE-17 boundary verifier reduced to the empty-diff measurement-only posture, --self-test green
- Mirror test pinned to the 243 close anchor (not HEAD)
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-01-SUMMARY.md` when done
</output>
