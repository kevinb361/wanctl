---
phase: 243-cycle-budget-benchmark-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase243-cycle-rollup.py
  - scripts/phase243-hygiene-sampler.sh
  - tests/test_phase243_cycle_rollup.py
  - tests/test_phase243_hygiene_sampler.py
autonomous: true
requirements: [BENCH-01]
user_setup: []

must_haves:
  truths:
    - "An operator can roll up a captured 'Cycle timing' NDJSON stream into per-arm avg/p99 cycle-budget stats"
    - "The rollup flags any inter-cycle gap >100ms as a STALL event (the TTY-vs-pipe fingerprint)"
    - "The rollup errors when the loop did not actually run (no autorate_cycle_total samples)"
    - "An operator can sample fd / zombie / Tasks / CPUUsageNSec counts as well-formed NDJSON over a soak window"
  artifacts:
    - path: "scripts/phase243-cycle-rollup.py"
      provides: "Cycle timing NDJSON -> avg/p99 per arm + STALL gap detector"
      contains: "autorate_cycle_total"
    - path: "scripts/phase243-hygiene-sampler.sh"
      provides: "1Hz fd/zombie/Tasks/cpu_nsec NDJSON sampler keyed on the bench unit MainPID"
      contains: "cpu_nsec"
    - path: "tests/test_phase243_cycle_rollup.py"
      provides: "Fixture NDJSON -> stats + stall-gap + no-cycle-samples guard tests"
    - path: "tests/test_phase243_hygiene_sampler.py"
      provides: "Hygiene NDJSON shape (incl. cpu_nsec) + trend assertions"
  key_links:
    - from: "scripts/phase243-cycle-rollup.py"
      to: "scripts/profiling_collector_json.py"
      via: "reuses the Cycle timing NDJSON parse + percentile contract"
      pattern: "Cycle timing"
    - from: "scripts/phase243-hygiene-sampler.sh"
      to: "bench unit MainPID"
      via: "systemctl show -p MainPID + /proc/<pid> sampling"
      pattern: "MainPID"
    - from: "scripts/phase243-hygiene-sampler.sh"
      to: "systemd cgroup CPU accounting"
      via: "systemctl show -p CPUUsageNSec emitted as cpu_nsec per tick"
      pattern: "CPUUsageNSec"
---

<objective>
Build the two measurement scaffolding scripts that capture cycle-budget statistics and subprocess-hygiene (incl. per-unit CPU accounting) over a benchmark arm, plus their unit tests against fixtures.

Purpose: BENCH-01 requires per-arm cycle avg/p99 and fd/zombie/Tasks/CPU evidence. The cycle-budget capture path already exists (Cycle timing NDJSON -> profiling_collector_json.py); this plan adds a thin rollup wrapper with the net-new STALL gap detector and a soak hygiene sampler that also carries the per-unit CPUUsageNSec counter (the producer for the D-04 CPU% delta gate). Zero src/wanctl edits — read-only reuse of existing instrumentation.

Output: scripts/phase243-cycle-rollup.py, scripts/phase243-hygiene-sampler.sh, and their fixture-driven unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md

<interfaces>
<!-- Cycle timing NDJSON record shape (emitted by perf_profiler record_cycle_profiling; -->
<!-- reuse via profiling_collector_json.py parse contract): -->
{ "message": "Cycle timing", "timestamp": <iso8601>, "cycle_total_ms": <float>, "<subsystem>_ms": <float>, ... }

<!-- profiling_collector_json.py contract the rollup wraps (do not rewrite): -->
- parse_ndjson(stream) -> (samples: dict[label, list[float]], cycle_records: int)
- canonical_label("cycle_total_ms") -> "autorate_cycle_total"
- calculate_statistics(samples) -> {count, min_ms, p50_ms, avg_ms, max_ms, p95_ms, p99_ms}
- main() returns exit 2 if no "Cycle timing" records OR no "autorate_cycle_total" samples

<!-- stdlib percentile helper (NumPy-free) if interpolated percentiles preferred: -->
scripts/soak_summary_aggregate.py :: percentile()

<!-- Per-unit CPU accounting source (RESEARCH Q2 recommendation, cgroup-clean per unit): -->
systemctl show -p CPUUsageNSec --value "$BENCH_UNIT"   # cumulative nanoseconds; emit raw as cpu_nsec per tick
<!-- The hygiene sampler emits the RAW cumulative counter per tick; the gate-eval (Plan 03) -->
<!-- converts to %: cpu_pct = (cpu_nsec_last - cpu_nsec_first) / (window_wall_ns * n_cores) * 100 -->
<!-- and cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cycle-budget rollup wrapper + STALL gap detector</name>
  <read_first>
    - scripts/profiling_collector_json.py (entire file — the exact-match parser to reuse: canonical_label, calculate_statistics, parse_ndjson, build_profile, the no-cycle-samples guard at lines 120-126, argparse shape lines 90-109, main() lines 112-132)
    - scripts/soak_summary_aggregate.py :: percentile() (the NumPy-free percentile helper, in case interpolated percentiles are preferred over sorted-index)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 131-157 (thin-wrapper instructions; the STALL gap detector is the ONLY net-new logic)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md lines 169-171 (Pattern 3: gap_ms = t[i]-t[i-1] from consecutive Cycle timing timestamps; stall_events = gaps > 100ms)
  </read_first>
  <files>scripts/phase243-cycle-rollup.py</files>
  <action>
    Create scripts/phase243-cycle-rollup.py as a thin wrapper over the profiling_collector_json.py contract — import or re-use its parse + statistics logic rather than reimplementing percentiles. The script reads a "Cycle timing" NDJSON stream (positional input, `-`/omitted = stdin) and writes a per-arm profile JSON (--output, omitted = stdout). Keep the built-in validity guard: exit 2 if there are zero "Cycle timing" records OR no autorate_cycle_total samples (free proof the loop ran). Add the net-new STALL gap detector: from each "Cycle timing" record's timestamp field, compute consecutive gap_ms = t[i] - t[i-1] in milliseconds, and emit into the profile JSON a "stall" block carrying stall_events (the list/count of gaps > 100.0 ms), max_gap_ms, and the gap threshold used. The autorate_cycle_total count must appear in the output (it is the D-04c n-floor source the gate reads). main() returns an int exit code. Stdlib only — no NumPy/pandas. Do NOT add any src/wanctl import that triggers controller behavior; this is an offline NDJSON transform.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_cycle_rollup.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The script parses fixture "Cycle timing" NDJSON to a profile with autorate_cycle_total carrying count/avg_ms/p99_ms
    - The profile JSON contains a "stall" block with stall_events derived from inter-cycle timestamp gaps > 100ms
    - Feeding NDJSON with zero "Cycle timing" records exits 2 (no-cycle-samples guard preserved)
    - Feeding "Cycle timing" records that lack cycle_total_ms (no autorate_cycle_total) exits 2
    - `.venv/bin/ruff check scripts/phase243-cycle-rollup.py` clean
  </acceptance_criteria>
  <done>Rollup produces per-arm avg/p99 + stall-gap detection, preserves the loop-ran guard, stdlib only.</done>
</task>

<task type="auto">
  <name>Task 2: Cycle-rollup unit tests (fixtures: stats, stall, guard)</name>
  <read_first>
    - tests/test_phase241_safe17_verifier.py lines 37-47 (the run() subprocess helper pattern + capture_output/check=False to copy for invoking the rollup as a subprocess, or invoke main() directly)
    - scripts/phase243-cycle-rollup.py (the script under test — created in Task 1)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 283-285 (cycle_rollup test expectations: feed fixture Cycle timing NDJSON; assert avg_ms/p99_ms + stall detector; assert the no-autorate_cycle_total guard exit 2)
  </read_first>
  <files>tests/test_phase243_cycle_rollup.py</files>
  <action>
    Create tests/test_phase243_cycle_rollup.py with fixture-driven cases: (1) a normal "Cycle timing" NDJSON fixture (built inline or via tmp_path) with known cycle_total_ms values and ascending timestamps spaced ~50ms apart — assert the rollup emits autorate_cycle_total with the expected count and a p99_ms consistent with the inputs, and stall_events empty. (2) a fixture with one timestamp gap > 100ms between consecutive cycles — assert stall_events captures exactly that gap and max_gap_ms reflects it. (3) a fixture with "Cycle timing" records that omit cycle_total_ms (no autorate_cycle_total) — assert the rollup exits 2. (4) a fixture with zero "Cycle timing" records — assert exit 2. Invoke the rollup either via a subprocess run() helper (copied from the 241 pattern) or by importing main() with argv; pick one and be consistent.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_cycle_rollup.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase243_cycle_rollup.py -x` exits 0 with at least 4 test cases
    - A test asserts stall_events is populated when an inter-cycle gap exceeds 100ms
    - A test asserts exit code 2 on the no-autorate_cycle_total fixture
    - A test asserts exit code 2 on the empty-stream fixture
  </acceptance_criteria>
  <done>Cycle-rollup tests cover normal stats, stall detection, and both validity-guard exits.</done>
</task>

<task type="auto">
  <name>Task 3: Subprocess-hygiene + CPU soak sampler + its NDJSON-shape test</name>
  <read_first>
    - scripts/soak-capture.sh (entire file — the NDJSON-per-tick clone source: set -euo pipefail + env validation lines 23-44, the `while [ "$(date +%s)" -lt "$SOAK_END" ]; do ... sleep 1; done` skeleton lines 61-161, the row_total/row_failed + SOAK_FAIL_RATE_THRESHOLD bounded-failure gate)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 224-247 (hygiene sampler instructions: /proc + systemctl sampling keyed on the unit MainPID; zombie = state Z child whose PPID==MainPID; trend test consumed by gate-eval)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md lines 155-167 (Pattern 2 sampling commands and the flat/bounded trend definition) and lines 303-306 (Q2: CPUUsageNSec cgroup accounting is the recommended per-unit CPU source)
  </read_first>
  <files>scripts/phase243-hygiene-sampler.sh, tests/test_phase243_hygiene_sampler.py</files>
  <action>
    Create scripts/phase243-hygiene-sampler.sh cloning the soak-capture.sh shape: set -euo pipefail, env-driven required inputs (BENCH_UNIT name, SOAK_DURATION_SEC, CAPTURE_DIR), fail-closed numeric validation that exits 2 on bad input, and the 1Hz `while ... sleep 1` loop with the bounded-failure tolerance gate (row_total/row_failed vs a threshold). Per tick: resolve PID via `systemctl show -p MainPID --value "$BENCH_UNIT"`; fd = count of /proc/$PID/fd entries; tasks = `systemctl show -p TasksCurrent --value "$BENCH_UNIT"`; zombies = count of /proc/[0-9]*/stat entries in state Z whose PPID == $PID (any Z under the unit is a reaping bug); cpu_nsec = `systemctl show -p CPUUsageNSec --value "$BENCH_UNIT"` (RESEARCH Q2: per-unit cgroup CPU accounting, cleaner than /proc utime+stime — emit the RAW cumulative nanosecond counter; the gate-eval converts first/last samples into a windowed cpu_pct and then cpu_delta_pts vs the icmplib arm). Append one NDJSON row per tick of shape {"t":<epoch>,"fd":<n>,"tasks":<n>,"zombies":<n>,"cpu_nsec":<n>} to $CAPTURE_DIR/hygiene.ndjson. Keep the script side-effect-free against live state — it only reads /proc and systemctl; it MUST NOT touch CAKE, routing, qdisc, RouterOS, or any service. Make it executable.
    Create tests/test_phase243_hygiene_sampler.py asserting the script is executable and contains the MainPID resolution, the /proc fd-count, the TasksCurrent read, the zombie scan, and the CPUUsageNSec read; and validate the NDJSON row shape against a small synthetic hygiene.ndjson fixture (well-formed {t,fd,tasks,zombies,cpu_nsec}). Assert cpu_nsec is present and well-formed (integer-valued, monotonically non-decreasing across the fixture rows — it is a cumulative counter, so a decreasing cpu_nsec is malformed and must be detectable). Since live /proc sampling is not reproducible on the dev VM, the test focuses on shape/contract assertions plus a fixture round-trip that the gate-eval trend + CPU logic will consume (monotonic-upward fd rejected, nonzero zombie rejected, cpu_nsec delta computable from first/last rows — assert those conditions are detectable from the fixture rows).
  </action>
  <verify>
    <automated>chmod +x scripts/phase243-hygiene-sampler.sh && bash -n scripts/phase243-hygiene-sampler.sh && .venv/bin/pytest tests/test_phase243_hygiene_sampler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase243-hygiene-sampler.sh` exits 0 (valid shell syntax)
    - The script contains `MainPID`, `/proc/`, `TasksCurrent`, `CPUUsageNSec`, and a zombie/`Z`-state scan
    - The script begins with `set -euo pipefail` and validates inputs fail-closed (exit 2)
    - The script never references tc/qdisc/RouterOS/CAKE mutation verbs
    - `.venv/bin/pytest tests/test_phase243_hygiene_sampler.py -x` exits 0 asserting the {t,fd,tasks,zombies,cpu_nsec} NDJSON shape
    - A test asserts cpu_nsec is present, integer-valued, and a window delta (last − first) is computable from the fixture rows
  </acceptance_criteria>
  <done>Hygiene sampler emits well-formed fd/zombie/Tasks/cpu_nsec NDJSON keyed on the bench unit MainPID; shape test green (incl. CPU field); read-only against live state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| journal NDJSON → rollup | malformed/incomplete records could yield a silently-empty stat set |
| host /proc + systemctl → sampler | sampler reads live process state; must not mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-05 | Tampering / Repudiation | malformed journal record silently passes | mitigate | Keep the profiling_collector_json.py validity guard: exit 2 if no Cycle timing records or no autorate_cycle_total — incomplete arms cannot produce a passing rollup (V5 input validation) |
| T-243-06 | Tampering | hygiene sampler touches live cake-autorate/router state | mitigate | Sampler reads only /proc + systemctl show (incl. CPUUsageNSec); contains no tc/qdisc/RouterOS/CAKE mutation; bash -n + test assert read-only posture |
| T-243-07 | DoS (resource) | leftover bench process tree (zombies) undetected | mitigate | The sampler itself is the zombie detector (state Z child of MainPID = reaping bug = nonzero) feeding the D-04a gate |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs (stdlib + system tools only) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase243_cycle_rollup.py tests/test_phase243_hygiene_sampler.py -q` green
- `bash -n scripts/phase243-hygiene-sampler.sh` exits 0
- `.venv/bin/ruff check scripts/phase243-cycle-rollup.py` clean
- Zero src/wanctl edits (verified by the Plan 01 SAFE-17 verifier at wave merge)
</verification>

<success_criteria>
- Cycle rollup produces per-arm avg/p99 + STALL gap events, preserving the loop-ran guard
- Hygiene sampler emits well-formed fd/zombie/Tasks/cpu_nsec NDJSON, read-only against live state
- The cpu_nsec field is the producer for the D-04 CPU% delta gate (Plan 03 consumes it)
- Both scripts covered by fixture-driven unit tests
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-02-SUMMARY.md` when done
</output>
