---
phase: 243-cycle-budget-benchmark-gate
plan: 04
type: execute
wave: 3
depends_on: ["243-01", "243-02", "243-03"]
files_modified:
  - configs/bench/README.md
  - configs/bench/gen-bench-configs.sh
  - scripts/phase243-bench-preflight.sh
  - scripts/phase243-bench-run.sh
  - tests/test_phase243_bench_configs.py
  - docs/PHASE243-BENCHMARK-RUNBOOK.md
  - .planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json
autonomous: false

requirements: [BENCH-01, BENCH-02, SAFE-17]
user_setup: []

must_haves:
  truths:
    - "BENCH-01 reproducibility: committed, tested bench config templates (a committed generator) exist for all 8 arms, proving backend selection, source-IP binding, UNIQUE health/metrics ports, isolated lock/state/log/storage paths, AND writer isolation — none of which collide with the live/production configs (HIGH-2)"
    - "HIGH-1: a HARD, fail-closed isolation preflight proves the bench unit physically CANNOT write the active CAKE qdiscs or the RouterOS queues used by live shaping; if it cannot positively prove isolation, the run aborts (fail-closed) — a throwaway unit name alone is NOT sufficient"
    - "D-01: An operator can launch one benchmark arm as a throwaway transient systemd unit (systemd-run --collect) running the real autorate_continuous.py 50ms loop with journal-pipe stdout (never a TTY); not a wanctl@ revive"
    - "The launcher mirrors production-relevant unit properties (User=wanctl, WorkingDirectory, CPUAccounting=yes) and captures the _SYSTEMD_INVOCATION_ID, handing it to the Plan 02 cycle-rollup so the journal slice is one-run-only (HIGH-3 cross-plan)"
    - "D-03: The under-load arm drives real WAN saturation via the established flent-RRUL-over-netperf path to the Dallas Linode 104.200.21.31 (no iperf, no synthetic CPU load); the idle arm generates no load"
    - "The launcher records the per-arm CPUUsageNSec window delta into the per-arm evidence JSON so the D-04 CPU% gate has its input"
    - "The launcher's per-arm evidence JSON carries EXACTLY the cpu/window keys gate-eval reads (cpu_nsec_start/end/delta, window_wall_sec, n_cores, invocation_id), proven by a key-name schema check so a rename cannot silently trip gate-eval's fail-closed missing-cpu guard"
    - "The 8-arm run produces evidence and a recorded gate verdict against the pre-committed thresholds"
  artifacts:
    - path: "configs/bench/gen-bench-configs.sh"
      provides: "Committed generator that emits the isolated bench configs (unique ports/paths, throwaway qdisc interfaces, isolated router writer) — the BENCH-01 reproducibility proof"
      contains: "download_interface"
    - path: "scripts/phase243-bench-preflight.sh"
      provides: "Fail-closed writer/qdisc isolation preflight (HIGH-1): aborts unless the bench cannot touch live CAKE qdiscs / RouterOS queues"
      contains: "tc qdisc show"
    - path: "scripts/phase243-bench-run.sh"
      provides: "Per-arm transient-unit launcher + invocation-id capture + journal drain + per-arm CPUUsageNSec capture + teardown, gated on the isolation preflight"
      contains: "systemd-run"
    - path: "tests/test_phase243_bench_configs.py"
      provides: "Asserts every path/port in the bench configs differs from live/production configs and the writer/qdisc is the isolated one (HIGH-2); plus a key-name schema check that the launcher's per-arm evidence JSON exposes exactly the keys gate-eval consumes"
    - path: "docs/PHASE243-BENCHMARK-RUNBOOK.md"
      provides: "Operator runbook for the 8-arm live run, gated on the isolation preflight + maintenance window"
      min_lines: 40
    - path: ".planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json"
      provides: "Recorded gate verdict against the frozen thresholds"
  key_links:
    - from: "scripts/phase243-bench-preflight.sh"
      to: "live cake-autorate shaping qdiscs"
      via: "tc qdisc show dev <live_iface> ownership + cake-autorate-{spectrum,att}.service active-state check; abort fail-closed on collision"
      pattern: "cake-autorate"
    - from: "scripts/phase243-bench-run.sh"
      to: "scripts/phase243-bench-preflight.sh"
      via: "launcher refuses to start unless the isolation preflight exits 0"
      pattern: "phase243-bench-preflight"
    - from: "scripts/phase243-bench-run.sh"
      to: "scripts/phase243-cycle-rollup.py"
      via: "journalctl _SYSTEMD_INVOCATION_ID=<id> -o cat | cycle-rollup --invocation-id <id>"
      pattern: "_SYSTEMD_INVOCATION_ID"
    - from: "scripts/phase243-bench-run.sh"
      to: "scripts/phase243-gate-eval.py (Plan 03) evidence contract"
      via: "per-arm evidence JSON keys cpu_nsec_start/end/delta, window_wall_sec, n_cores, invocation_id match what gate-eval reads"
      pattern: "cpu_nsec_delta"
    - from: "configs/bench/gen-bench-configs.sh"
      to: "throwaway qdisc interfaces + isolated ports/paths"
      via: "cake_params.{download,upload}_interface point at bench-only netdevs; health/metrics/lock/state/storage all unique"
      pattern: "download_interface"
    - from: "scripts/phase243-bench-run.sh"
      to: "systemd cgroup CPU accounting"
      via: "systemd-run --property=CPUAccounting=yes; systemctl show -p CPUUsageNSec at window start/end rolled into per-arm evidence"
      pattern: "CPUAccounting"
---

<objective>
Build the committed bench config templates, the fail-closed writer/qdisc isolation preflight, the per-arm benchmark launcher, and the operator runbook; then (operator-gated) execute the 8-arm live run and record the gate verdict.

Purpose: BENCH-01 requires the benchmark to run under a real systemd unit (TTY-vs-pipe is the STALL fingerprint). The cross-AI review (243-REVIEWS.md) found the two blocking HIGHs here: HIGH-1 — launching the real autorate_continuous loop runs WANController.apply_rate_changes_if_needed() -> router.set_limits() (wan_controller.py:1607/1676) and the Linux CAKE adapter does `tc qdisc replace` at construction (linux_cake_adapter.py:12/288), so the bench can become a SECOND live shaper colliding with the production cake-autorate-{spectrum,att} units; and HIGH-2 — there are no committed/tested bench configs proving isolation. This revision adds committed bench config templates (a generator emitting all 8 arms) with a test asserting every port/path differs from live and the writer/qdisc is isolated, plus a hard fail-closed isolation preflight the launcher must pass before it starts. The live run produces the evidence the Plan 03 gate evaluates into the recorded BENCH-02 verdict.

Output: configs/bench/gen-bench-configs.sh (+ README), scripts/phase243-bench-preflight.sh, scripts/phase243-bench-run.sh, tests/test_phase243_bench_configs.py, docs/PHASE243-BENCHMARK-RUNBOOK.md, and the recorded 243-BENCHMARK-VERDICT.json.

NOTE: autonomous=false. The live 8-arm run requires the real .226 (Spectrum) / .233 (ATT) WAN hosts, fping, netperf reachability, `-S` source-IP binding, and an operator-approved isolation posture (throwaway qdisc interfaces or a maintenance window) — none reachable/decidable from this dev VM. Tasks 1-3 (configs+test, preflight, launcher+runbook) are autonomous; Task 4 is an operator-gated checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-BENCHMARK-PREREGISTRATION.md

<interfaces>
<!-- VERIFIED live-collision facts (orchestrator confirmed against source): -->
<!-- - WANController.apply_rate_changes_if_needed() -> self.router.set_limits(...) (wan_controller.py:1676) -->
<!-- - LinuxCakeAdapter.from_config() calls initialize_cake() => `tc qdisc replace` on the -->
<!--   configured cake_params.{download,upload}_interface AT CONSTRUCTION (linux_cake_adapter.py:12,288). -->
<!--   This is UNAVOIDABLE for transport=linux-cake — so the bench config MUST name throwaway -->
<!--   interfaces, never the live shaping interfaces. -->

<!-- Config keys that control isolation (config_base.py / autorate_config.py): -->
router.transport            (default "rest"; linux-cake/linux-cake-netlink do tc qdisc replace)
cake_params.download_interface / cake_params.upload_interface   (the interfaces `tc qdisc replace` targets)
health_check.port           (default 9101 — live; bench MUST be unique, e.g. 9201/9202/...)
metrics.port                (default 9100 — live; bench MUST be unique)
metrics.enabled             (set false / point storage.db_path at a throwaway temp DB — no production metrics.db writes)
storage.db_path             (default /var/lib/wanctl/metrics.db — bench MUST use a throwaway temp path)
lock_file                   (live e.g. /run/wanctl/<wan>.lock — bench MUST be unique, e.g. /run/wanctl/bench-<wan>.lock)
state_file                  (live e.g. /var/lib/wanctl/<wan>_state.json — bench MUST be a throwaway temp path)
ping_source_ip              (the WAN source IP for `-S` binding; real per WAN)

<!-- Per-arm evidence JSON contract — the EXACT key set scripts/phase243-gate-eval.py (Plan 03) reads -->
<!-- for the CPU% gate; the launcher (Task 3) MUST assemble these key names verbatim, else gate-eval's -->
<!-- fail-closed missing-cpu guard trips with a silent contract drift instead of a real signal: -->
{ "cpu_nsec_start": <int>, "cpu_nsec_end": <int>, "cpu_nsec_delta": <int>,
  "window_wall_sec": <f>, "n_cores": <int>, "invocation_id": "<id>" }

<!-- Live shapers the bench must NOT collide with (deploy/systemd): -->
cake-autorate-spectrum.service, cake-autorate-att.service (+ their -state-bridge units), steering.service

<!-- Isolation posture (HIGH-1) — the bench config selects ONE of: -->
<!-- A) transport=linux-cake bound to THROWAWAY interfaces (operator-created dummy/ifb netdevs, -->
<!--    e.g. bench-dl/bench-ul) so `tc qdisc replace` cannot touch the live shaping interfaces, AND -->
<!--    a router writer that cannot reach the production RouterOS queue paths; OR -->
<!-- B) an operator-approved maintenance window on an isolated host where the live -->
<!--    cake-autorate-{spectrum,att} units are STOPPED for the duration. -->
<!-- The preflight proves the chosen posture fail-closed before launch. -->

<!-- Throwaway transient unit invocation (RESEARCH Pattern 1; D-01; NEVER --pty), now with -->
<!-- production-relevant properties + CPUAccounting (review MEDIUM) + invocation-id capture (HIGH-3): -->
sudo systemd-run --unit="wanctl-bench-<WAN>-<BACKEND>-<LOAD>-$(date +%s)" --collect \
  --property=CPUAccounting=yes --uid=wanctl --working-directory=/opt/wanctl \
  --setenv=WANCTL_LOG_FORMAT=json \
  /opt/wanctl/.venv/bin/python -m wanctl.autorate_continuous \
    --debug --config configs/bench/<WAN>-bench-<BACKEND>.yaml
INVOCATION=$(systemctl show -p InvocationID --value <unit>)   # the _SYSTEMD_INVOCATION_ID

<!-- journal drain -> rollup (Plan 02), invocation-scoped (HIGH-3): -->
journalctl _SYSTEMD_INVOCATION_ID="$INVOCATION" -o cat --no-pager \
  | python3 scripts/phase243-cycle-rollup.py - --invocation-id "$INVOCATION" --output evidence/<arm>.profile.json

<!-- Per-arm CPU capture (RESEARCH Q2; cgroup-clean per-unit accounting): -->
cpu_start=$(systemctl show -p CPUUsageNSec --value <unit>)   # after MainPID is up
cpu_end=$(systemctl show -p CPUUsageNSec --value <unit>)     # just before teardown

<!-- Load generation (D-03; benchmark.py flent path; NO iperf): -->
flent rrul -H 104.200.21.31 -l <dur> -D <tmpdir>   (source-bound to the WAN IP via -S)
  Spectrum source IP: 10.10.110.226   ATT source IP: per att.yaml / phase220 matrix
  Dallas Linode netperf target: 104.200.21.31

<!-- Gate evaluator (Plan 03) + frozen thresholds (Plan 01) + provenance helper: -->
scripts/phase243-gate-eval.py  ;  scripts/phase243-thresholds.json  ;  scripts/phase243-prereg-provenance.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed bench config generator (8 isolated arms) + isolation test (HIGH-2) + evidence-key schema check</name>
  <read_first>
    - configs/examples/cable.yaml.example (the config shape to mirror: router.transport, cake_params.{download,upload}_interface, health_check.port, metrics, lock_file, state_file, storage.db_path, ping_source_ip — the keys the bench must make unique/isolated)
    - configs/examples/fiber.yaml.example, configs/examples/dsl.yaml.example (per-link example shapes so the generator emits link-agnostic configs per CLAUDE.md portability rule — deployment specifics in YAML, not Python)
    - deploy/systemd/cake-autorate-spectrum.service, deploy/systemd/cake-autorate-att.service (the LIVE shapers — to enumerate the live unit names / interfaces the bench must differ from)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md lines 82-91 + 125-129 (HIGH-2: committed bench configs proving backend/source-IP/unique-ports/isolated-paths + writer isolation; HIGH-1 writer isolation)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md lines 196-206 (Runtime State Inventory: unique unit/port/lock/state; no production metrics-DB writes)
    - scripts/phase243-gate-eval.py (Plan 03 — the CPU% gate's reader; note the exact per-arm cpu/window evidence key names it consumes, so this task's schema check asserts the same contract)
  </read_first>
  <files>configs/bench/gen-bench-configs.sh, configs/bench/README.md, tests/test_phase243_bench_configs.py</files>
  <action>
    Create configs/bench/gen-bench-configs.sh — a committed generator that emits the bench config YAMLs for the arms (2 backends icmplib|fping x 2 WANs spectrum|att; the {idle,under-load} distinction is a launcher/load-gen concern, so the generator emits one config per (WAN,BACKEND) = 4 configs that the launcher reuses for both load states; document this in README). set -euo pipefail. Each emitted config MUST be ISOLATED from live/production:
      - router.transport = linux-cake bound to THROWAWAY interfaces: cake_params.download_interface / upload_interface = bench-only netdev names (e.g. "bench-${WAN}-dl" / "bench-${WAN}-ul") that the operator creates as dummy/ifb devices for the run — NEVER the live shaping interfaces. (HIGH-1 writer isolation: `tc qdisc replace` then targets only the throwaway netdevs.)
      - health_check.port and metrics.port = UNIQUE bench ports distinct from live 9101/9100 (e.g. 9201/9200 + per-WAN offset) so no collision with cake-autorate state-bridge / steering health.
      - metrics.enabled = false (or storage.db_path pointed at a throwaway temp DB under /var/tmp) so the bench writes NO rows to the production /var/lib/wanctl/metrics.db.
      - lock_file = /run/wanctl/bench-${WAN}.lock ; state_file = a throwaway temp path (e.g. /var/tmp/wanctl-bench/${WAN}_state.json) — never the live lock/state.
      - ping_source_ip = the real WAN source IP for `-S` binding (the one real-world coupling; the fork/exec/parse cost must be real per D-01a).
      - backend selection per arm via the 242 factory config key (icmplib vs fping) so build_rtt_backend() picks the arm's backend.
    Keep the configs link-agnostic (CLAUDE.md NON-NEGOTIABLE: no Python branching; deployment specifics in YAML). Emit the configs to stdout or under configs/bench/generated/; the GENERATOR and README are the committed reproducibility artifact (the runtime /etc/wanctl/bench/ copies are operator-deployed, NOT committed — /etc is runtime-only). Create configs/bench/README.md documenting: the arm matrix, the isolation contract (throwaway interfaces, unique ports/paths, no production metrics DB), how the operator creates the throwaway dummy/ifb netdevs, and that these are bench-only configs that must never be deployed as a live shaper.
    Create tests/test_phase243_bench_configs.py: run the generator, parse the emitted YAML for every arm, and ASSERT isolation vs the live/example configs — for each bench config: cake_params.download_interface and upload_interface are bench-only names (match a "bench" marker) and are NOT any interface named in the live cake-autorate / examples; health_check.port and metrics.port differ from 9101/9100 and from each other across arms (no two arms share a health port); lock_file and state_file contain a "bench" marker and are NOT the live lock/state paths; storage writes are disabled or pointed at a temp path (no /var/lib/wanctl/metrics.db); router.transport is the linux-cake-on-throwaway-interface posture (not pointed at the production RouterOS queues); the backend key matches the arm (icmplib vs fping). The test is the HIGH-2 reproducibility proof — it must fail if any bench path/port equals a live one (include a negative assertion).
    ADD an EVIDENCE-KEY SCHEMA CHECK (WARNING 1 cross-plan contract): in the same tests/test_phase243_bench_configs.py, assert that the launcher's assembled per-arm evidence JSON exposes EXACTLY the key set the Plan 03 gate-eval reads for the CPU% gate — `cpu_nsec_start`, `cpu_nsec_end`, `cpu_nsec_delta`, `window_wall_sec`, `n_cores`, `invocation_id`. Implement it as a small contract assertion: define the EXPECTED_EVIDENCE_KEYS frozenset literally in the test, build (or load a committed fixture of) a representative merged per-arm evidence dict using those exact keys, and assert that set(evidence) is a superset of EXPECTED_EVIDENCE_KEYS AND that gate-eval consumes the same names — derive the gate-eval-required names by importing them from scripts/phase243-gate-eval.py if it exposes a constant (e.g. REQUIRED_CPU_EVIDENCE_KEYS), otherwise assert each EXPECTED key appears as a string literal in scripts/phase243-gate-eval.py source (grep-style). The point: a future rename on either side (launcher emit vs gate-eval read) fails this test loudly instead of silently tripping gate-eval's fail-closed missing-cpu guard, which would masquerade as a "missing cpu evidence" abort rather than a schema error. Keep this assertion in this Wave-3 test because gate-eval (Plan 03, Wave 2) already exists when this runs.
  </action>
  <verify>
    <automated>chmod +x configs/bench/gen-bench-configs.sh && bash -n configs/bench/gen-bench-configs.sh && .venv/bin/pytest tests/test_phase243_bench_configs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n configs/bench/gen-bench-configs.sh` exits 0 and the generator emits a parseable YAML config per arm
    - Each bench config's cake_params.download_interface / upload_interface are bench-only throwaway names, NOT the live shaping interfaces
    - Each bench config's health_check.port and metrics.port differ from the live 9101/9100 and no two arms share a health port
    - Each bench config's lock_file and state_file carry a bench marker and are NOT the live paths; the bench writes no rows to /var/lib/wanctl/metrics.db (metrics disabled or temp DB)
    - `.venv/bin/pytest tests/test_phase243_bench_configs.py -x` exits 0 and includes a negative assertion that FAILS if any bench path/port is changed to equal a live one
    - The test asserts the merged per-arm evidence JSON contract: the exact keys `cpu_nsec_start`, `cpu_nsec_end`, `cpu_nsec_delta`, `window_wall_sec`, `n_cores`, `invocation_id` are present and match what scripts/phase243-gate-eval.py reads (a rename on either side fails the test, not gate-eval's silent missing-cpu guard)
    - configs/bench/README.md documents the isolation contract and the throwaway-netdev creation step
  </acceptance_criteria>
  <done>Committed bench config generator emits the isolated arms (throwaway qdisc interfaces, unique ports/paths, no production metrics DB); the isolation test proves every port/path differs from live and the writer/qdisc is isolated (HIGH-2 reproducibility proof); a key-name schema check pins the launcher↔gate-eval evidence contract.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed writer/qdisc isolation preflight (HIGH-1)</name>
  <read_first>
    - src/wanctl/backends/linux_cake_adapter.py lines 10-20, 280-300 (READ-ONLY — the construction-time `tc qdisc replace` on cake_params.{download,upload}_interface that makes interface isolation mandatory; do NOT edit src/wanctl)
    - src/wanctl/wan_controller.py lines 1607, 1676 (READ-ONLY — apply_rate_changes_if_needed -> router.set_limits, the live-write path the preflight must prove is isolated)
    - deploy/systemd/cake-autorate-spectrum.service, deploy/systemd/cake-autorate-att.service (the live shaper units whose active-state + owned interfaces the preflight checks)
    - configs/bench/gen-bench-configs.sh (Task 1 — the bench configs whose interfaces/ports/paths the preflight reads to prove isolation)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md lines 82-96 + 117-129 (HIGH-1 the preflight must resolve fail-closed: prove the bench cannot write live CAKE qdiscs / RouterOS queues, or require an operator-approved isolated host/window; throwaway unit name is NOT enough)
  </read_first>
  <files>scripts/phase243-bench-preflight.sh</files>
  <action>
    Create scripts/phase243-bench-preflight.sh — a HARD, fail-closed isolation preflight that the launcher must pass before starting any bench unit (HIGH-1). set -euo pipefail. Inputs: the bench config path + the live WAN identifier + the operator-declared posture flag (throwaway-interface | maintenance-window). It must POSITIVELY PROVE isolation or ABORT (nonzero exit) — absence of proof = abort, never proceed:
      1. INTERFACE ISOLATION: parse the bench config's cake_params.download_interface / upload_interface; assert they are bench-only throwaway netdevs and are NOT the interfaces the live cake-autorate shapers own. Record the live shaping qdisc ownership via `tc qdisc show dev <live_iface>` (handle/kind) so a post-run check can prove it was untouched; if a bench interface name equals a live shaping interface, ABORT.
      2. LIVE-SHAPER COLLISION: check `systemctl is-active cake-autorate-spectrum.service cake-autorate-att.service` (and the state-bridge + steering units). Two operator-declared postures: (A) THROWAWAY-INTERFACE — the bench targets dummy/ifb netdevs, so the live units MAY remain active, but the preflight asserts the bench interfaces are disjoint from the live ones AND that the bench config's router writer cannot reach the production RouterOS queue paths (transport is linux-cake-on-throwaway-iface, NOT a REST/SSH writer pointed at the prod router); (B) MAINTENANCE-WINDOW — the operator has stopped the live cake-autorate-{spectrum,att} units; the preflight asserts they are inactive. If NEITHER posture is positively satisfied, ABORT fail-closed.
      3. PORT/LOCK/STATE ISOLATION: via `ss -ltnp`, assert the bench health/metrics ports are free and not the live 9101/9100; assert no live bench lock/state lingers from a prior arm.
      4. QDISC-OWNERSHIP SNAPSHOT: capture `tc qdisc show dev <live_iface>` for each live shaping interface into the evidence dir so the launcher's post-run residue check can diff it and prove the live qdiscs were untouched (any change = fail).
    Emit a JSON isolation-proof record (posture chosen, bench interfaces, live interfaces, live-unit active states, port checks, live qdisc snapshot, passed:true/false) into the evidence dir. Exit 0 ONLY if every check positively proves isolation. The preflight reads only `tc qdisc show`, `systemctl`, `ss`, and the config file — it MUST NOT mutate any qdisc/router/service. Make it executable.
  </action>
  <verify>
    <automated>chmod +x scripts/phase243-bench-preflight.sh && bash -n scripts/phase243-bench-preflight.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase243-bench-preflight.sh` exits 0 (valid shell syntax)
    - The script contains `tc qdisc show`, a `cake-autorate-spectrum`/`cake-autorate-att` active-state check, and an `ss` port check
    - The script supports BOTH the throwaway-interface posture and the maintenance-window posture, and ABORTS fail-closed if neither is positively satisfied
    - The script aborts if a bench cake_params interface name equals a live shaping interface
    - The script captures a pre-run `tc qdisc show dev <live_iface>` snapshot into the evidence dir (for the post-run untouched-proof)
    - The script never references a qdisc/router/service MUTATION verb (read-only: show/is-active/ss only)
  </acceptance_criteria>
  <done>Fail-closed isolation preflight positively proves the bench cannot write live CAKE qdiscs / RouterOS queues (throwaway-interface OR maintenance-window posture), snapshots live qdisc ownership for the post-run untouched-proof, and aborts when isolation is not provable (HIGH-1).</done>
</task>

<task type="auto">
  <name>Task 3: Per-arm bench-run launcher (preflight-gated, invocation-scoped, CPUAccounting) + operator runbook</name>
  <read_first>
    - scripts/soak-capture.sh (env/loop/teardown discipline + bounded-failure pattern to mirror)
    - src/wanctl/benchmark.py lines 424-434 (READ-ONLY — the established flent RRUL invocation for the under-load arm; reuse, do NOT introduce iperf)
    - scripts/phase243-bench-preflight.sh (Task 2 — the isolation gate the launcher must pass); scripts/phase243-cycle-rollup.py and scripts/phase243-hygiene-sampler.sh (Plan 02 — rollup now requires --invocation-id; sampler requires CPUAccounting=yes)
    - scripts/phase243-gate-eval.py (Plan 03 — the consumer of the per-arm evidence JSON; the launcher's merged evidence keys cpu_nsec_start/end/delta, window_wall_sec, n_cores, invocation_id MUST match what it reads, per the Task 1 schema check)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-REVIEWS.md lines 82-94 + 149-150 (HIGH-1 preflight gating; the systemd-run-properties MEDIUM: User=wanctl, WorkingDirectory, CPUAccounting, invocation IDs)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 251-274 (bench-run instructions: systemd-run --collect, --setenv WANCTL_LOG_FORMAT=json, NEVER --pty; flent reuse)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md lines 47-124 (D-01/D-01a/D-01b/D-03/D-04c) and .planning/phases/243-cycle-budget-benchmark-gate/243-VALIDATION.md lines 66-72 (manual-only verifications)
  </read_first>
  <files>scripts/phase243-bench-run.sh, docs/PHASE243-BENCHMARK-RUNBOOK.md</files>
  <action>
    Create scripts/phase243-bench-run.sh: a parameterized per-arm launcher taking WAN (spectrum|att), BACKEND (icmplib|fping), LOAD (idle|load), duration, and the bench config path. set -euo pipefail. HARD GATE (HIGH-1): the FIRST step is to run scripts/phase243-bench-preflight.sh for this bench config + WAN + posture; if it exits nonzero, the launcher ABORTS and does NOT start the unit (fail-closed isolation). LAUNCH: sudo systemd-run with a UNIQUE per-run unit name ("wanctl-bench-${WAN}-${BACKEND}-${LOAD}-$(date +%s)"), --collect, --property=CPUAccounting=yes (so CPUUsageNSec is numeric — review MEDIUM), --uid=wanctl, --working-directory=/opt/wanctl, --setenv=WANCTL_LOG_FORMAT=json, running /opt/wanctl/.venv/bin/python -m wanctl.autorate_continuous --debug --config the bench yaml. NEVER pass --pty (a TTY defeats the STALL fingerprint; D-01/D-04b). After MainPID is up, CAPTURE the invocation id: INVOCATION=$(systemctl show -p InvocationID --value <unit>) (the _SYSTEMD_INVOCATION_ID) — handed to the rollup so the journal slice is one-run-only (HIGH-3 cross-plan). For the under-load arm only, drive real WAN saturation via the established flent RRUL path to the Dallas Linode 104.200.21.31, source-bound to the WAN IP (reuse benchmark.py's invocation; NO iperf, NO synthetic CPU load; idle arm generates no load). Run scripts/phase243-hygiene-sampler.sh against the unit over the same window (it requires CPUAccounting=yes — already set; emits {t,fd,tasks,zombies,cpu_nsec}). CPU CAPTURE (D-04, RESEARCH Q2): record cpu_nsec_start + wall epoch after MainPID is up, cpu_nsec_end + wall epoch just before teardown; compute cpu_nsec_delta and window_wall_sec; resolve n_cores via `nproc`. After the window (>= the D-04c floor), DRAIN the journal INVOCATION-SCOPED: journalctl _SYSTEMD_INVOCATION_ID="$INVOCATION" -o cat --no-pager piped to scripts/phase243-cycle-rollup.py - --invocation-id "$INVOCATION" --output the per-arm profile.json (HIGH-3: NOT `journalctl -u <unit>` alone). ASSEMBLE the merged per-arm evidence JSON the Plan 03 gate reads by combining the Plan 02 rollup cycle-timing fields, the hygiene NDJSON summary, AND the CPU/window fields under the EXACT key names gate-eval consumes — `cpu_nsec_start`, `cpu_nsec_end`, `cpu_nsec_delta`, `window_wall_sec`, `n_cores`, and `invocation_id` (verbatim; the Task 1 schema check pins this contract — a rename here would silently trip gate-eval's fail-closed missing-cpu guard). TEARDOWN: rely on --collect for the transient unit; after teardown run a residue check asserting no leftover wanctl-bench-* unit, no leftover bench lock, no stray flent/netperf/fping child, AND re-run a `tc qdisc show dev <live_iface>` diff against the preflight snapshot to PROVE the live shaping qdiscs were untouched (any change = fail). The script reads /proc + journal + systemctl + drives the dev host's own bench unit only; it MUST NOT touch the live cake-autorate units, the state bridges, steering, or production state. Make it executable. (This script is exercised live in Task 4; here it must pass bash -n.)
    Create docs/PHASE243-BENCHMARK-RUNBOOK.md documenting the operator procedure for the 8-arm run (D-01b). Cover: (1) ISOLATION SETUP (HIGH-1) — choose the posture: create the throwaway dummy/ifb netdevs the bench configs name (Task 1 README), OR declare a maintenance window and stop the live cake-autorate-{spectrum,att} units; run scripts/phase243-bench-preflight.sh and confirm it PASSES (the launcher refuses to start otherwise). (2) pre-run host checks (fping --version + netperf reachability on BOTH .226/.233; ip route get 104.200.21.31 from the source IP egresses the intended WAN per PROV-03). (3) per-arm launch via scripts/phase243-bench-run.sh with the exact unit naming, WANCTL_LOG_FORMAT=json, CPUAccounting=yes, and journal-pipe (never --pty); the D-04c floor (>= max(10k cycles, 30 min); hygiene sampler at 1Hz). (4) CPU% capture: the launcher records CPUUsageNSec window deltas into each per-arm evidence JSON; the gate converts to per-arm CPU% (per_core normalization, frozen) and compares fping vs same-run icmplib — operator confirms cpu_nsec fields are non-zero/present before running the gate. (5) REPRESENTATIVENESS (HIGH-4) — the dev icmplib arm MUST land inside the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band or the gate returns input_error and the run aborts; re-run on a representative host. (6) run scripts/phase243-gate-eval.py over all arms to produce 243-BENCHMARK-VERDICT.json; the verdict embeds the prereg blob/commit SHAs (Plan 03) — confirm via scripts/phase243-prereg-provenance.sh that the evidence commit descends from the prereg commit. (7) teardown/residue confirmation incl. the post-run `tc qdisc show` untouched-proof. State explicitly that "keep icmplib" is a valid passing close and this gate only blocks the Phase 245 A/B on regression. Reference scripts/phase243-thresholds.json as the frozen single source; do not restate the thresholds inline. Keep this public-safe per CLAUDE.md.
  </action>
  <verify>
    <automated>chmod +x scripts/phase243-bench-run.sh && bash -n scripts/phase243-bench-run.sh && test -f docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q phase243-bench-preflight docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q phase243-gate-eval docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q CPUUsageNSec docs/PHASE243-BENCHMARK-RUNBOOK.md && ! grep -qiE 'iperf|--pty' docs/PHASE243-BENCHMARK-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase243-bench-run.sh` exits 0 (valid shell syntax)
    - The launcher's FIRST action is to run scripts/phase243-bench-preflight.sh and ABORT if it exits nonzero (HIGH-1 gating); `grep -c phase243-bench-preflight scripts/phase243-bench-run.sh` >= 1
    - The script contains `systemd-run`, `--collect`, `--property=CPUAccounting=yes`, `WANCTL_LOG_FORMAT=json`, and captures `InvocationID` / `_SYSTEMD_INVOCATION_ID`
    - The journal drain is invocation-scoped: `grep -c _SYSTEMD_INVOCATION_ID scripts/phase243-bench-run.sh` >= 1 and the rollup is called with `--invocation-id` (HIGH-3); it does NOT drain via `journalctl -u <unit>` alone
    - The script records a per-arm cpu_nsec window delta into the evidence JSON (D-04 CPU% gate producer)
    - The launcher assembles the merged per-arm evidence JSON using the exact gate-eval key names `cpu_nsec_start`, `cpu_nsec_end`, `cpu_nsec_delta`, `window_wall_sec`, `n_cores`, `invocation_id` (the Task 1 schema check covers the contract); `grep -c cpu_nsec_delta scripts/phase243-bench-run.sh` >= 1
    - `grep -c -- '--pty' scripts/phase243-bench-run.sh` returns 0 and `grep -ci iperf scripts/phase243-bench-run.sh` returns 0
    - The post-run residue check re-diffs `tc qdisc show dev <live_iface>` against the preflight snapshot (untouched-proof) and checks for leftover unit/lock/child
    - docs/PHASE243-BENCHMARK-RUNBOOK.md is >= 40 lines, references the preflight + launcher + gate-eval + CPUUsageNSec, documents the isolation posture + representativeness abort, states "keep icmplib" is a valid passing close, and mentions neither iperf nor --pty
  </acceptance_criteria>
  <done>Preflight-gated launcher orchestrates the throwaway journal-pipe unit (CPUAccounting=yes, production-relevant props), captures + invocation-scopes the journal drain, captures CPUUsageNSec, assembles the merged evidence JSON under the exact gate-eval key contract, proves the live qdiscs untouched on teardown; the runbook fully specifies the isolation-gated 8-arm run; syntax valid.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator runs the isolation-gated 8-arm live benchmark and records the verdict</name>
  <action>OPERATOR-EXECUTED (autonomous=false): Claude cannot reach the live WAN hosts or decide the isolation posture. The operator follows docs/PHASE243-BENCHMARK-RUNBOOK.md: pick the isolation posture, run scripts/phase243-bench-preflight.sh per arm and confirm it PASSES, launch all 8 arms via scripts/phase243-bench-run.sh (>= the D-04c floor each, journal-pipe not TTY), confirm representativeness lands inside the frozen band, run scripts/phase243-gate-eval.py to record 243-BENCHMARK-VERDICT.json against the frozen thresholds, and confirm teardown + the post-run tc-qdisc untouched-proof. See how-to-verify for the exact steps.</action>
  <what-built>
    The full benchmark harness: committed isolated bench config generator + isolation test, fail-closed writer/qdisc isolation preflight (HIGH-1), per-arm launcher (scripts/phase243-bench-run.sh) gated on the preflight with CPUAccounting + invocation-scoped journal drain, cycle rollup + STALL detector (invocation-scoped), hygiene sampler (fd/zombie/Tasks/cpu_nsec), frozen gate evaluator with the hard representativeness validity gate + thresholds, and the operator runbook. All unit tests green; SAFE-17 boundary verifier passes (zero src/wanctl edits).
  </what-built>
  <how-to-verify>
    This is a TRULY-manual step: the 8-arm live run requires the real .226 (Spectrum) and .233 (ATT) WAN hosts, fping installed, netperf reachability, `-S` source-IP binding, AND an operator-approved isolation posture — none reachable/decidable from this dev VM. Claude cannot execute it.

    Operator procedure (per docs/PHASE243-BENCHMARK-RUNBOOK.md):
    1. ISOLATION (HIGH-1): choose a posture — create the throwaway dummy/ifb netdevs the bench configs name, OR declare a maintenance window and stop the live cake-autorate-{spectrum,att} units. Run `scripts/phase243-bench-preflight.sh` for each arm and confirm it PASSES (the launcher refuses to start otherwise; it proves the bench cannot write the live CAKE qdiscs / RouterOS queues).
    2. On .226 and .233: confirm `fping --version` and netperf reachability to 104.200.21.31; confirm `ip route get 104.200.21.31 from <source_ip>` egresses the intended WAN (PROV-03).
    3. Run all 8 arms via `scripts/phase243-bench-run.sh` (icmplib/fping x idle/under-load x .226/.233), each for >= max(10k cycles, 30 min). Confirm stdout is the journal pipe (never a TTY), CPUAccounting=yes, and that the journal drain was invocation-scoped. Confirm each per-arm evidence JSON carries non-zero cpu_nsec window-delta fields and an invocation_id.
    4. Confirm the dev icmplib arm lands INSIDE the frozen ICMPLIB_REPRESENTATIVE_*_TOL_MS band (HIGH-4); if outside, the gate will return input_error — re-run on a representative host before trusting any verdict.
    5. Run `scripts/phase243-gate-eval.py` over all per-arm evidence to produce `.planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json` against the frozen `scripts/phase243-thresholds.json`. The verdict embeds the prereg blob/commit SHAs — confirm via `scripts/phase243-prereg-provenance.sh` that the evidence commit descends from the prereg commit.
    6. Confirm teardown residue is clean (no leftover wanctl-bench-* unit, lock, or stray flent/netperf/fping process) AND the post-run `tc qdisc show dev <live_iface>` matches the preflight snapshot (live qdiscs untouched).
    7. Commit the verdict JSON + per-arm evidence.

    "keep icmplib" is a valid passing close — this gate only blocks the Phase 245 A/B on regression.
  </how-to-verify>
  <resume-signal>Type "verdict recorded" with the outcome (pass / regression / input_error) once 243-BENCHMARK-VERDICT.json is committed, or describe any blockers (e.g. host unreachable, preflight could not prove isolation, unrepresentative icmplib arm).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| throwaway bench unit → live CAKE qdiscs / RouterOS queues | the unit's construction-time `tc qdisc replace` + apply_rate_changes -> router.set_limits could collide with / corrupt live cake-autorate shaping |
| bench config → live config | a bench config reusing a live port/lock/state/interface becomes a second live shaper |
| launcher evidence JSON → gate-eval | a key-name drift would silently trip gate-eval's fail-closed missing-cpu guard instead of a schema error |
| operator-run live arms → evidence/verdict | a partial, mis-bound, or unrepresentative arm could produce a misleading verdict |
| bench unit stdout → journal | TTY buffering would hide the STALL fingerprint the gate exists for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-11 | Tampering / Elevation | bench unit writes live CAKE qdiscs / RouterOS queues (VERIFIED live-write path) | mitigate | HIGH-1: fail-closed isolation preflight (scripts/phase243-bench-preflight.sh) proves the bench targets throwaway interfaces (or the live units are stopped) and cannot reach the prod RouterOS queues; the launcher refuses to start unless it passes; post-run `tc qdisc show` diff proves the live qdiscs were untouched (V4 access control) |
| T-243-11a | Tampering | bench config collides with live port/lock/state/interface | mitigate | HIGH-2: committed bench config generator emits unique ports/lock/state + throwaway interfaces + no production metrics DB; tests/test_phase243_bench_configs.py fails if any bench path/port equals a live one |
| T-243-11b | Tampering / Repudiation | launcher↔gate-eval evidence key drift → silent fail-closed misread | mitigate | tests/test_phase243_bench_configs.py schema check pins the exact merged-evidence key set (cpu_nsec_start/end/delta, window_wall_sec, n_cores, invocation_id); a rename on either side fails the test loudly instead of masquerading as a missing-cpu abort (WARNING 1) |
| T-243-12 | DoS (resource) | leftover bench process tree / orphaned fping after the run | mitigate | `systemd-run --collect` teardown + launcher residue check (no leftover unit/lock/child); hygiene sampler proves zero zombies during the run |
| T-243-13 | Repudiation | STALL hidden by TTY buffering, or prior-arm journal contamination → false pass | mitigate | NEVER `--pty`; journal drain is `_SYSTEMD_INVOCATION_ID`-scoped (HIGH-3); cycle-rollup requires the invocation id and flags any >100ms inter-cycle gap |
| T-243-14 | Information disclosure | secrets / private host detail in evidence JSON or runbook | mitigate | Evidence carries only timing/hygiene/CPU counts + isolation proofs; runbook is public-safe per CLAUDE.md (no secrets; dev WAN source IPs already in repo configs) |
| T-243-15 | Elevation | controller hot-path mutation introduced under cover of benchmark wiring | mitigate | Zero src/wanctl edits (all new files under configs/, scripts/, tests/, docs/); the Plan 01 SAFE-17 verifier re-runs at the phase boundary and fails closed on any controller-path drift |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs; system tools (fping/flent/netperf/systemd) already in the deploy baseline (RESEARCH Package Legitimacy Gate: N/A) |
</threat_model>

<verification>
- `bash -n scripts/phase243-bench-preflight.sh scripts/phase243-bench-run.sh` exits 0; `bash -n configs/bench/gen-bench-configs.sh` exits 0
- `.venv/bin/pytest tests/test_phase243_bench_configs.py -q` green (every bench path/port differs from live; writer/qdisc isolated; evidence-key contract pinned)
- `.venv/bin/ruff check tests/test_phase243_bench_configs.py` clean (Python-only; the .sh artifacts are covered by the `bash -n` syntax checks in the T2/T3 verify blocks, not ruff)
- docs/PHASE243-BENCHMARK-RUNBOOK.md present, references the preflight + launcher + gate-eval + CPUUsageNSec capture, documents the isolation posture, no iperf/--pty
- `bash scripts/phase243-safe17-boundary-check.sh --out .planning/phases/243-cycle-budget-benchmark-gate/evidence/safe17-boundary-243.json` PASSES (zero src/wanctl drift; all new files are under configs/, scripts/, tests/, docs/) at the phase boundary
- 243-BENCHMARK-VERDICT.json recorded against the frozen thresholds (operator-run live)
</verification>

<success_criteria>
- Committed, tested bench config generator proves isolation (unique ports/paths, throwaway qdisc interfaces, no production metrics DB) — the BENCH-01 reproducibility proof (HIGH-2)
- Fail-closed isolation preflight proves the bench cannot write live CAKE qdiscs / RouterOS queues, gating the launcher (HIGH-1)
- Bench-run launcher uses a throwaway journal-pipe transient unit (never a TTY, CPUAccounting=yes, production-relevant props), invocation-scoped journal drain (HIGH-3), distinct from live state, with collision preflight + residue + untouched-qdisc proof
- The launcher captures per-arm CPUUsageNSec window deltas into the per-arm evidence (D-04 CPU% gate producer) under the exact gate-eval key contract, pinned by the Task 1 schema check
- Under-load arm reuses the flent-RRUL-over-netperf path (no iperf, no synthetic load)
- Operator runbook fully specifies the isolation-gated 8-arm run and verdict generation
- The recorded verdict is computed against the pre-committed frozen thresholds (BENCH-02)
- SAFE-17 boundary verifier passes (zero controller-path drift; all new files outside src/wanctl)
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-04-SUMMARY.md` when done
</output>
</content>
