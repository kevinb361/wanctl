---
phase: 243-cycle-budget-benchmark-gate
plan: 04
type: execute
wave: 3
depends_on: ["243-01", "243-02", "243-03"]
files_modified:
  - scripts/phase243-bench-run.sh
  - docs/PHASE243-BENCHMARK-RUNBOOK.md
  - .planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json
autonomous: false
requirements: [BENCH-01, BENCH-02, SAFE-17]
user_setup: []

must_haves:
  truths:
    - "D-01: An operator can launch one benchmark arm as a throwaway transient systemd unit (systemd-run --collect) running the real autorate_continuous.py 50ms loop with journal-pipe stdout (never a TTY); not a wanctl@ revive"
    - "The bench unit uses a distinct unit name / health port / lock / state path so it never collides with live cake-autorate, the state bridges, or steering"
    - "D-03: The under-load arm drives real WAN saturation via the established flent-RRUL-over-netperf path to the Dallas Linode 104.200.21.31 (no iperf, no synthetic CPU load); the idle arm generates no load"
    - "The launcher records the per-arm CPUUsageNSec window delta into the per-arm evidence JSON so the D-04 CPU% gate has its input"
    - "The 8-arm run produces evidence and a recorded gate verdict against the pre-committed thresholds"
  artifacts:
    - path: "scripts/phase243-bench-run.sh"
      provides: "Per-arm transient-unit launcher + journal drain + per-arm CPUUsageNSec capture + teardown"
      contains: "systemd-run"
    - path: "docs/PHASE243-BENCHMARK-RUNBOOK.md"
      provides: "Operator runbook for the 8-arm live run on .226/.233"
      min_lines: 40
    - path: ".planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json"
      provides: "Recorded gate verdict against the frozen thresholds"
  key_links:
    - from: "scripts/phase243-bench-run.sh"
      to: "build_rtt_backend() via per-WAN bench config"
      via: "autorate_continuous --config selects the backend per arm"
      pattern: "autorate_continuous"
    - from: "scripts/phase243-bench-run.sh"
      to: "scripts/phase243-cycle-rollup.py"
      via: "journalctl -o cat | cycle-rollup"
      pattern: "phase243-cycle-rollup"
    - from: "scripts/phase243-bench-run.sh"
      to: "systemd cgroup CPU accounting"
      via: "systemctl show -p CPUUsageNSec at window start/end rolled into per-arm evidence"
      pattern: "CPUUsageNSec"
---

<objective>
Build the per-arm benchmark launcher and the operator runbook, then (operator-gated) execute the 8-arm live run on the real WAN hosts and record the gate verdict.

Purpose: BENCH-01 requires the benchmark to run under a real systemd unit (TTY-vs-pipe is the STALL fingerprint). The launcher orchestrates the throwaway transient unit, journal capture, load generation, hygiene + CPU sampling, and teardown. The live run produces the evidence the Plan 03 gate evaluates into the recorded BENCH-02 verdict — including the per-arm CPUUsageNSec window delta that feeds the D-04 CPU% gate.

Output: scripts/phase243-bench-run.sh, docs/PHASE243-BENCHMARK-RUNBOOK.md, and the recorded 243-BENCHMARK-VERDICT.json.

NOTE: autonomous=false. The live 8-arm run requires the real .226 (Spectrum) / .233 (ATT) WAN hosts, fping, netperf reachability, and `-S` source-IP binding — none reachable from this dev VM. Tasks 1-2 (launcher + runbook) are autonomous; Task 3 is an operator-gated checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-BENCHMARK-PREREGISTRATION.md

<interfaces>
<!-- Throwaway transient unit invocation (RESEARCH Pattern 1; D-01; NEVER --pty): -->
sudo systemd-run --unit="wanctl-bench-<WAN>-<BACKEND>-<LOAD>" --collect \
  --setenv=WANCTL_LOG_FORMAT=json \
  /opt/wanctl/.venv/bin/python -m wanctl.autorate_continuous \
    --debug --config /etc/wanctl/bench/<WAN>-bench-<BACKEND>.yaml

<!-- journal drain -> rollup (Plan 02): -->
journalctl -u <unit> -o cat --no-pager | python3 scripts/phase243-cycle-rollup.py - --output evidence/<arm>.profile.json

<!-- Per-arm CPU capture (RESEARCH Q2; cgroup-clean per-unit accounting): -->
<!-- read CPUUsageNSec at window start and end; the delta over the soak window is the arm's CPU spend -->
cpu_start=$(systemctl show -p CPUUsageNSec --value <unit>)   # at launch, after MainPID is up
cpu_end=$(systemctl show -p CPUUsageNSec --value <unit>)     # just before teardown
<!-- Roll cpu_nsec_start / cpu_nsec_end / cpu_nsec_delta / window_wall_sec / n_cores into the per-arm -->
<!-- evidence JSON. The gate-eval (Plan 03) computes cpu_pct = cpu_nsec_delta / (window_wall_sec*1e9*n_cores)*100 -->
<!-- and cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load). -->
<!-- The hygiene sampler (Plan 02) already emits cpu_nsec per tick as a redundant/finer source. -->

<!-- Load generation (D-03; benchmark.py flent path; NO iperf): -->
flent rrul -H 104.200.21.31 -l <dur> -D <tmpdir>   (source-bound to the WAN IP via -S)
  Spectrum source IP: 10.10.110.226   ATT source IP: per att.yaml / phase220 matrix
  Dallas Linode netperf target: 104.200.21.31

<!-- Collision avoidance (RESEARCH Pitfall 2): bench config MUST use a unique -->
<!-- unit name + health port + /run/wanctl lock + state path vs live -->
<!-- cake-autorate-{spectrum,att}.service / -state-bridge.service / steering.service. -->

<!-- Gate evaluator (Plan 03) + frozen thresholds (Plan 01): -->
scripts/phase243-gate-eval.py  ;  scripts/phase243-thresholds.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-arm bench-run launcher (transient unit + journal drain + CPU capture + teardown + collision preflight)</name>
  <read_first>
    - scripts/soak-capture.sh (env/loop/teardown discipline + bounded-failure pattern to mirror)
    - src/wanctl/benchmark.py lines 424-434 (the established flent RRUL invocation for the under-load arm — reuse, do NOT introduce iperf)
    - scripts/phase243-cycle-rollup.py and scripts/phase243-hygiene-sampler.sh (Plan 02 outputs this launcher drives; the hygiene sampler emits cpu_nsec per tick)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 251-274 (bench-run instructions: systemd-run --collect, --setenv WANCTL_LOG_FORMAT=json, NEVER --pty; flent reuse; collision avoidance preflight with ss -ltnp)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md lines 196-206 (Runtime State Inventory: unique unit/port/lock/state; confirm no production metrics-DB writes; --collect teardown residue check) and lines 303-306 (Q2: CPUUsageNSec is the per-arm CPU source)
  </read_first>
  <files>scripts/phase243-bench-run.sh</files>
  <action>
    Create scripts/phase243-bench-run.sh: a parameterized per-arm launcher taking WAN (spectrum|att), BACKEND (icmplib|fping), LOAD (idle|load), duration, and the bench config path. set -euo pipefail. PREFLIGHT (collision avoidance, RESEARCH Pitfall 2): before launch, verify via `ss -ltnp` that the bench config's health port is free, and that no live wanctl-bench-* unit/lock/state from a prior arm lingers — abort fail-closed if a collision is detected. LAUNCH: sudo systemd-run with --unit="wanctl-bench-${WAN}-${BACKEND}-${LOAD}", --collect, --setenv=WANCTL_LOG_FORMAT=json, running /opt/wanctl/.venv/bin/python -m wanctl.autorate_continuous --debug --config the bench yaml. NEVER pass --pty (a TTY is line-buffered and defeats the STALL fingerprint — this is the whole point of the unit; D-01/D-04b). For the under-load arm only, drive real WAN saturation via the established flent RRUL path to the Dallas Linode 104.200.21.31, source-bound to the WAN IP (reuse benchmark.py's invocation; NO iperf, NO synthetic CPU load; idle arm generates no load). Run scripts/phase243-hygiene-sampler.sh against the unit MainPID over the same window (it emits the per-tick {t,fd,tasks,zombies,cpu_nsec} NDJSON). CPU CAPTURE (D-04, RESEARCH Q2): immediately after the unit's MainPID is up record cpu_nsec_start via `systemctl show -p CPUUsageNSec --value <unit>` and the wall epoch; just before teardown record cpu_nsec_end and the wall epoch; compute cpu_nsec_delta = end-start and window_wall_sec = end_epoch-start_epoch; resolve n_cores via `nproc`. After the window (>= the D-04c floor), DRAIN the journal via journalctl -u <unit> -o cat --no-pager piped to scripts/phase243-cycle-rollup.py - --output the per-arm profile.json, and roll the hygiene NDJSON AND the CPU fields (cpu_nsec_start, cpu_nsec_end, cpu_nsec_delta, window_wall_sec, n_cores) into the per-arm evidence JSON the Plan 03 gate reads — this is the producer for cpu_delta_pts. TEARDOWN: rely on --collect for the transient unit; after teardown run a residue check asserting no leftover wanctl-bench-* unit, no leftover /run/wanctl lock, no stray flent/netperf/fping child. The script reads /proc + journal + systemctl + drives the dev host's own bench unit only; it MUST NOT touch the live cake-autorate units, the state bridges, steering, or production state. Make it executable. (This script is exercised live in Task 3; here it must pass bash -n.)
  </action>
  <verify>
    <automated>chmod +x scripts/phase243-bench-run.sh && bash -n scripts/phase243-bench-run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase243-bench-run.sh` exits 0 (valid shell syntax)
    - The script contains `systemd-run`, `--collect`, `WANCTL_LOG_FORMAT=json`, and pipes to `phase243-cycle-rollup`
    - The script contains `CPUUsageNSec` and records a per-arm cpu_nsec window delta into the evidence JSON (D-04 CPU% gate producer)
    - `grep -c -- '--pty' scripts/phase243-bench-run.sh` returns 0 (a TTY would defeat the STALL fingerprint)
    - `grep -ci iperf scripts/phase243-bench-run.sh` returns 0 (D-03 forbids iperf substitution)
    - The script contains a collision preflight (`ss` and a port/lock check) that aborts fail-closed
    - The script contains a post-run residue check for leftover unit/lock/child processes
  </acceptance_criteria>
  <done>Bench-run launcher orchestrates the throwaway journal-pipe unit, flent load, hygiene + CPUUsageNSec capture, journal drain, and teardown with collision preflight + residue check; the per-arm evidence carries the CPU window delta; syntax valid.</done>
</task>

<task type="auto">
  <name>Task 2: Operator runbook for the 8-arm live run</name>
  <read_first>
    - scripts/phase243-bench-run.sh (the launcher the runbook drives — created in Task 1)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-CONTEXT.md lines 47-124 (D-01/D-01a/D-01b host + arm matrix; D-03 load paths; D-04 CPU% bound; D-04c floor)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md lines 311-326 (Environment Availability: fping/netperf must be confirmed on .226/.233, not the dev VM) and lines 303-306 (Q2: CPUUsageNSec capture method)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-VALIDATION.md lines 66-72 (manual-only verifications table)
  </read_first>
  <files>docs/PHASE243-BENCHMARK-RUNBOOK.md</files>
  <action>
    Create docs/PHASE243-BENCHMARK-RUNBOOK.md documenting the operator procedure for the 8-arm run (2 backends x {idle,under-load} x 2 WANs, D-01b). Cover: pre-run host checks (confirm fping --version and netperf reachability on BOTH .226 Spectrum and .233 ATT; confirm ip route get 104.200.21.31 from the source IP egresses the intended WAN per PROV-03; confirm the bench config health port/lock/state do not collide with live cake-autorate/bridge/steering via ss -ltnp); per-arm launch via scripts/phase243-bench-run.sh with the exact unit naming and the WANCTL_LOG_FORMAT=json env; the D-04c sampling floor (>= max(10k cycles, 30 min) per arm; hygiene sampler at 1Hz over the same window). Document the CPU% capture: the launcher records CPUUsageNSec at window start/end (systemctl show -p CPUUsageNSec) and rolls the cpu_nsec window delta into each per-arm evidence JSON; the gate converts that to a per-arm CPU% (delta / (wall*cores)) and compares the fping vs same-run icmplib arm to produce cpu_delta_pts against the frozen CPU% bound — note the operator does not hand-compute CPU%, the launcher captures it automatically, but they must confirm the cpu_nsec fields are non-zero and present in each evidence JSON before running the gate. Also cover: confirming stdout is the journal pipe not a TTY (systemctl show -p StandardOutput, never --pty); the representativeness sanity check (the dev icmplib arm should land near the historical anchor per D-02 — re-run on a better host if wildly off); collecting per-arm profile.json + hygiene.ndjson + CPU fields into the evidence dir; running scripts/phase243-gate-eval.py over all arms to produce 243-BENCHMARK-VERDICT.json; and the teardown/residue confirmation (no leftover wanctl-bench-* unit, lock, or stray flent/netperf/fping process). State explicitly that "keep icmplib" is a valid passing close and that this gate only blocks the Phase 245 A/B on regression. Reference scripts/phase243-thresholds.json as the frozen single source of the numbers; do not restate the thresholds inline. Keep this public-safe per CLAUDE.md (no secrets; source IPs are dev WAN host addresses already documented in repo configs).
  </action>
  <verify>
    <automated>test -f docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q 'phase243-bench-run.sh' docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q 'phase243-gate-eval' docs/PHASE243-BENCHMARK-RUNBOOK.md && grep -q 'CPUUsageNSec' docs/PHASE243-BENCHMARK-RUNBOOK.md && ! grep -qiE 'iperf|--pty' docs/PHASE243-BENCHMARK-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/PHASE243-BENCHMARK-RUNBOOK.md exists and is >= 40 lines
    - It references scripts/phase243-bench-run.sh and scripts/phase243-gate-eval.py
    - It documents the 8-arm matrix, the journal-pipe (not TTY) requirement, and the D-04c sampling floor
    - It documents the CPUUsageNSec per-arm capture and that cpu_nsec fields must be present/non-zero in each evidence JSON before running the gate
    - It does NOT mention iperf or --pty
    - It states "keep icmplib" is a valid passing close
  </acceptance_criteria>
  <done>Runbook fully specifies the 8-arm live run, host preflight, journal-pipe requirement, CPUUsageNSec capture, sampling floor, verdict generation, and teardown.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    The full benchmark harness: per-arm launcher (scripts/phase243-bench-run.sh) with CPUUsageNSec capture, cycle rollup + STALL detector, hygiene sampler (fd/zombie/Tasks/cpu_nsec), frozen gate evaluator + thresholds, and the operator runbook. All unit tests green; SAFE-17 boundary verifier passes (zero src/wanctl edits).
  </what-built>
  <how-to-verify>
    This is a TRULY-manual step: the 8-arm live run requires the real .226 (Spectrum) and .233 (ATT) WAN hosts, fping installed, netperf reachability, and `-S` source-IP binding — none reachable from this dev VM. Claude cannot execute it.

    Operator procedure (per docs/PHASE243-BENCHMARK-RUNBOOK.md):
    1. On .226 and .233: confirm `fping --version` and netperf reachability to 104.200.21.31; confirm `ip route get 104.200.21.31 from <source_ip>` egresses the intended WAN (PROV-03).
    2. Confirm the bench config health port/lock/state do not collide with live cake-autorate/bridge/steering (`ss -ltnp`).
    3. Run all 8 arms via `scripts/phase243-bench-run.sh` (icmplib/fping x idle/under-load x .226/.233), each for >= max(10k cycles, 30 min). Confirm stdout is the journal pipe, never a TTY. Confirm each per-arm evidence JSON carries non-zero cpu_nsec window-delta fields (D-04 CPU% gate input).
    4. Sanity-check the dev icmplib arm lands near the historical anchor (D-02); if wildly off, re-run on a better host before trusting the verdict.
    5. Run `scripts/phase243-gate-eval.py` over all per-arm evidence to produce `.planning/phases/243-cycle-budget-benchmark-gate/evidence/243-BENCHMARK-VERDICT.json` against the frozen `scripts/phase243-thresholds.json` (the CPU% gate compares fping vs same-run icmplib cpu_delta_pts against the frozen 2.0-pt bound).
    6. Confirm teardown residue is clean (no leftover wanctl-bench-* unit, lock, or stray flent/netperf/fping process).
    7. Commit the verdict JSON + per-arm evidence.

    "keep icmplib" is a valid passing close — this gate only blocks the Phase 245 A/B on regression.
  </how-to-verify>
  <resume-signal>Type "verdict recorded" with the outcome (pass / regression) once 243-BENCHMARK-VERDICT.json is committed, or describe any blockers (e.g. host unreachable, unrepresentative icmplib arm).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| throwaway bench unit → live host state | the unit could collide with / corrupt live cake-autorate, bridge, or steering state |
| operator-run live arms → evidence/verdict | a partial or mis-bound arm could produce a misleading verdict |
| bench unit stdout → journal | TTY buffering would hide the STALL fingerprint the gate exists for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-11 | Tampering | bench unit collides with live cake-autorate / bridge / steering | mitigate | Unique unit name + health port + /run/wanctl lock + state path; launcher preflights with `ss -ltnp` and aborts fail-closed on collision (V4 access control) |
| T-243-12 | DoS (resource) | leftover bench process tree / orphaned fping after the run | mitigate | `systemd-run --collect` teardown + launcher residue check (no leftover unit/lock/child); hygiene sampler proves zero zombies during the run |
| T-243-13 | Repudiation | STALL hidden by TTY buffering → false pass | mitigate | NEVER `--pty`; runbook confirms `systemctl show -p StandardOutput` is the journal; cycle-rollup STALL detector flags any >100ms inter-cycle gap |
| T-243-14 | Information disclosure | secrets / private host detail in evidence JSON or runbook | mitigate | Evidence carries only timing/hygiene/CPU counts; runbook is public-safe per CLAUDE.md (no secrets; dev WAN source IPs already in repo configs) |
| T-243-15 | Elevation | controller hot-path mutation introduced under cover of benchmark wiring | mitigate | Zero src/wanctl edits; the Plan 01 SAFE-17 verifier re-runs at the phase boundary and fails closed on any controller-path drift |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs; system tools (fping/flent/netperf/systemd) already in the deploy baseline (RESEARCH Package Legitimacy Gate: N/A) |
</threat_model>

<verification>
- `bash -n scripts/phase243-bench-run.sh` exits 0
- `.venv/bin/ruff check scripts/phase243-bench-run.sh` clean (shellcheck-equivalent if configured)
- docs/PHASE243-BENCHMARK-RUNBOOK.md present, references the launcher + gate-eval + CPUUsageNSec capture, no iperf/--pty
- `bash scripts/phase243-safe17-boundary-check.sh --out .planning/phases/243-cycle-budget-benchmark-gate/evidence/safe17-boundary-243.json` PASSES (zero src/wanctl drift) at the phase boundary
- 243-BENCHMARK-VERDICT.json recorded against the frozen thresholds (operator-run live)
</verification>

<success_criteria>
- Bench-run launcher uses a throwaway journal-pipe transient unit (never a TTY), distinct from live state, with collision preflight + residue check
- The launcher captures per-arm CPUUsageNSec window deltas into the per-arm evidence (D-04 CPU% gate producer)
- Under-load arm reuses the flent-RRUL-over-netperf path (no iperf, no synthetic load)
- Operator runbook fully specifies the 8-arm run and verdict generation
- The recorded verdict is computed against the pre-committed frozen thresholds (BENCH-02)
- SAFE-17 boundary verifier passes (zero controller-path drift)
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-04-SUMMARY.md` when done
</output>
