---
phase: 243-cycle-budget-benchmark-gate
plan: 03
type: execute
wave: 2
depends_on: ["243-01"]
files_modified:
  - scripts/phase243-gate-eval.py
  - tests/test_phase243_gate_eval.py
autonomous: true
requirements: [BENCH-02]
user_setup: []

must_haves:
  truths:
    - "The gate evaluator computes the fping-minus-icmplib delta on the same-run arms and blocks on >20% avg or p99 regression"
    - "The gate derives cpu_delta_pts from the per-arm CPUUsageNSec window deltas (fping arm minus icmplib arm, same WAN/load) and enforces the frozen CPU% bound"
    - "The gate enforces the absolute p99 ceiling, CPU delta bound, zombie/fd/Tasks hygiene, STALL events, and the D-04c n-floor"
    - "Thresholds are loaded only from the frozen scripts/phase243-thresholds.json, never inline literals"
    - "Incomplete arms (missing autorate_cycle_total, missing cpu_nsec, or below n-floor) fail closed, not silently pass"
  artifacts:
    - path: "scripts/phase243-gate-eval.py"
      provides: "Frozen-threshold verdict over per-arm evidence -> 243-BENCHMARK-VERDICT.json"
      contains: "load_thresholds"
    - path: "tests/test_phase243_gate_eval.py"
      provides: "Pass fixture + one fixture per fail mode (delta, ceiling, CPU, zombie, fd-trend, stall, n-floor)"
  key_links:
    - from: "scripts/phase243-gate-eval.py"
      to: "scripts/phase243-thresholds.json"
      via: "load_thresholds() reads the frozen JSON (CPU_DELTA_PCT_POINTS=2.0 frozen)"
      pattern: "phase243-thresholds\\.json"
    - from: "scripts/phase243-gate-eval.py"
      to: "same-run icmplib arm"
      via: "delta = (fping - icmplib) / icmplib"
      pattern: "delta"
    - from: "scripts/phase243-gate-eval.py"
      to: "per-arm CPUUsageNSec window delta (Plan 02/04 evidence)"
      via: "cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct"
      pattern: "cpu_delta_pts"
---

<objective>
Build the pre-registered gate evaluator that turns per-arm benchmark evidence into a recorded pass/fail verdict against the frozen D-04 thresholds.

Purpose: BENCH-02 is the hard gate that blocks the Phase 245 live A/B on regression. The evaluator computes fping-vs-same-run-icmplib deltas (D-02 primary basis), derives the CPU% delta from the per-arm CPUUsageNSec window deltas captured by Plan 02/04, and enforces the absolute ceiling and hygiene/STALL/validity gates (D-04/D-04a/D-04b/D-04c), failing closed on incomplete arms. Thresholds come only from the frozen JSON committed in Plan 01.

Output: scripts/phase243-gate-eval.py + its full pass/fail-mode test matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md
@.planning/phases/243-cycle-budget-benchmark-gate/243-RESEARCH.md

<interfaces>
<!-- Frozen thresholds JSON (created in Plan 01, scripts/phase243-thresholds.json; -->
<!-- CPU_DELTA_PCT_POINTS is frozen at 2.0 — do NOT change the frozen value): -->
CYCLE_AVG_REGRESSION_PCT, CYCLE_P99_REGRESSION_PCT, CYCLE_P99_ABS_CEILING_MS,
CPU_DELTA_PCT_POINTS, ZOMBIES_MAX, TASKS_BOUND, STALL_GAP_MS, MIN_CYCLES, MIN_MINUTES,
ICMPLIB_REPRESENTATIVE_AVG_MS, ICMPLIB_REPRESENTATIVE_P99_MS

<!-- Per-arm profile JSON shape (produced by phase243-cycle-rollup.py, Plan 02): -->
{ "autorate_cycle_total": {"count": <n>, "avg_ms": <f>, "p99_ms": <f>, ...}, "stall": {"stall_events": [...], "max_gap_ms": <f>} }

<!-- Per-arm evidence CPU fields (rolled in by the launcher, Plan 04, via systemctl CPUUsageNSec): -->
{ "cpu_nsec_start": <int>, "cpu_nsec_end": <int>, "cpu_nsec_delta": <int>, "window_wall_sec": <f>, "n_cores": <int> }
<!-- Derive per-arm cpu_pct = cpu_nsec_delta / (window_wall_sec * 1e9 * n_cores) * 100 -->
<!-- then cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load). -->

<!-- Per-arm hygiene NDJSON rows (phase243-hygiene-sampler.sh, Plan 02; cpu_nsec is the per-tick -->
<!-- redundant/finer CPU source — last minus first row is an alternate cpu_nsec_delta): -->
{"t":<epoch>,"fd":<n>,"tasks":<n>,"zombies":<n>,"cpu_nsec":<n>}

<!-- phase206 / phase224 gate-eval patterns to reuse: -->
- scripts/phase206-gate-check.py :: load_thresholds(path) -> dict ; EXIT_PASS=0 EXIT_BLOCK=1 EXIT_ABORT=2 ; zero-baseline delta-% policy
- scripts/phase224-gate-eval.py :: GateEvalError(message, gate_id) ; _gate(verdict, value, **extra) ; per-gate dict + final outcome scan ; json.dumps(..., indent=2, sort_keys=True)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate evaluator — frozen-threshold verdict with fail-closed incomplete-arm handling</name>
  <read_first>
    - scripts/phase206-gate-check.py lines 1-50 (load_thresholds() reading the JSON, EXIT_PASS/BLOCK/ABORT constants, structured error classes) and the delta-% with zero-baseline guard (the `((cur-pre)/pre)*100 if pre>0 else ...` policy)
    - scripts/phase224-gate-eval.py (entire file — GateEvalError lines 25-31, _gate helper lines 73-81, the per-gate dict + final rollback_trigger/outcome scan lines 211-244, json output lines 283-288, exit-code main() lines 261-293)
    - scripts/phase243-thresholds.json (the frozen thresholds created in Plan 01 — load via load_thresholds, never inline; CPU_DELTA_PCT_POINTS frozen at 2.0)
    - scripts/phase243-hygiene-sampler.sh / scripts/phase243-bench-run.sh (Plan 02/04 — the producers of the cpu_nsec per-tick field and the per-arm cpu_nsec_delta/window_wall_sec/n_cores evidence fields this evaluator consumes)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 159-194 (gate-eval instructions: load_thresholds copy-verbatim; D-04 per-gate delta math; per-gate dict + outcome; fail-closed on incomplete arms; "keep icmplib" valid passing close)
  </read_first>
  <behavior>
    - Pass fixture: fping avg/p99 within 20% of same-run icmplib, p99 < 10ms, cpu_delta_pts < CPU_DELTA_PCT_POINTS, zero zombies, flat fd, bounded Tasks, zero stall events, count >= max(10000, 30min cycles) -> outcome "pass"
    - avg-regression fixture: fping avg > 1.20x icmplib avg -> a "fail" gate, outcome "rollback_trigger"
    - p99-regression fixture: fping p99 > 1.20x icmplib p99 -> fail
    - ceiling fixture: fping p99 >= 10.0ms (even if delta < 20%) -> fail
    - CPU fixture: cpu_delta_pts (fping_arm_cpu_pct - icmplib_arm_cpu_pct, derived from cpu_nsec_delta/window_wall_sec/n_cores) >= CPU_DELTA_PCT_POINTS (2.0) -> fail
    - zombie fixture: max(zombies) > 0 -> fail
    - fd-trend fixture: monotonic-upward fd across windows -> fail
    - stall fixture: stall_events non-empty (gap > 100ms) -> fail
    - n-floor fixture: count < max(10000, cycles for 30min) -> fail-closed (incomplete arm), NOT silent pass
    - missing autorate_cycle_total OR missing cpu_nsec evidence -> GateEvalError / fail-closed
  </behavior>
  <files>scripts/phase243-gate-eval.py</files>
  <action>
    Create scripts/phase243-gate-eval.py reusing the phase206/phase224 patterns. Copy load_thresholds() verbatim so D-04 figures come ONLY from scripts/phase243-thresholds.json — no inline literals, including the frozen CPU_DELTA_PCT_POINTS=2.0. Define EXIT_PASS=0 / EXIT_BLOCK=1 / EXIT_ABORT=2 and a GateEvalError(message, gate_id) structured error. The evaluator takes, per WAN, the fping-arm and the same-run icmplib-arm profile JSONs plus their hygiene NDJSON and the per-arm CPU evidence fields, and computes (D-02 primary basis = same-run delta, NOT historical 2.85/6.9): avg_delta_pct and p99_delta_pct = ((fping - icmplib)/icmplib)*100 with the zero-baseline guard; require each <= the frozen regression pct; require fping.p99_ms < CYCLE_P99_ABS_CEILING_MS (absolute). CPU GATE (D-04, the producer fix): derive each arm's cpu_pct = cpu_nsec_delta / (window_wall_sec * 1e9 * n_cores) * 100 from the per-arm evidence (the cpu_nsec_delta the launcher rolled in, or last-minus-first cpu_nsec from the hygiene NDJSON as a fallback source); compute cpu_delta_pts = fping_arm_cpu_pct - icmplib_arm_cpu_pct (same WAN/load); require cpu_delta_pts < CPU_DELTA_PCT_POINTS (frozen 2.0). require max(hygiene.zombies) <= ZOMBIES_MAX; require fd is not monotonic-upward (windowed-median test: split the fd series into N windows, fail if window medians are strictly increasing across all windows); require max(hygiene.tasks) <= tasks_baseline + TASKS_BOUND; require stall_events count == 0; require autorate_cycle_total.count >= max(MIN_CYCLES, cycles for MIN_MINUTES at 20Hz). FAIL CLOSED on incomplete arms — missing autorate_cycle_total, missing cpu_nsec evidence (cannot derive cpu_delta_pts), or count below the floor must raise/return a fail, never silently pass (V5 control). Build a per-gate verdict dict via a _gate() helper; the final outcome is "rollback_trigger" if any gate fails, else "pass". Add a SECONDARY representativeness check: icmplib arm avg within a band of ICMPLIB_REPRESENTATIVE_AVG_MS and p99 within a band of ICMPLIB_REPRESENTATIVE_P99_MS — surface it as advisory (warn) so an unrepresentative host is flagged before trusting the verdict (D-02). Note "keep icmplib" is a valid passing close at the AB level — this gate blocks only on regression. Write the verdict to 243-BENCHMARK-VERDICT.json via json.dumps(verdict, indent=2, sort_keys=True) + "\n", referencing the frozen-thresholds path/commit and the derived per-arm cpu_pct in the verdict so the gate can't be rationalized post-hoc. main() returns the exit code. Stdlib only.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_gate_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The evaluator imports its thresholds via load_thresholds() from scripts/phase243-thresholds.json (no inline D-04 literals)
    - `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'load_thresholds'` >= 1
    - The evaluator derives cpu_delta_pts from the per-arm cpu_nsec window deltas and gates it against the frozen CPU_DELTA_PCT_POINTS (2.0); `grep -v '^#' scripts/phase243-gate-eval.py | grep -c 'cpu_delta_pts'` >= 1
    - A pass fixture yields outcome "pass" and exit 0; an avg-regression fixture yields a failing gate and nonzero exit
    - The p99 absolute-ceiling gate fails when fping p99 >= 10.0ms even if the delta is under 20%
    - An arm with count below max(MIN_CYCLES, 30min) OR missing cpu_nsec evidence fails closed (not a silent pass)
    - `.venv/bin/ruff check scripts/phase243-gate-eval.py` clean
  </acceptance_criteria>
  <done>Gate evaluator computes the same-run delta + derives cpu_delta_pts from CPUUsageNSec evidence + all D-04/D-04a/D-04b/D-04c gates from the frozen JSON, fails closed on incomplete arms, writes the verdict JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-eval test matrix — pass + one fixture per fail mode</name>
  <read_first>
    - scripts/phase243-gate-eval.py (the evaluator under test — created in Task 1)
    - scripts/phase243-thresholds.json (the frozen thresholds the tests assert against; CPU_DELTA_PCT_POINTS=2.0)
    - .planning/phases/243-cycle-budget-benchmark-gate/243-PATTERNS.md lines 287-292 (gate_eval test: synthetic same-run arms, one pass fixture + one per fail mode, mirroring the 241 fail-mode coverage style)
  </read_first>
  <files>tests/test_phase243_gate_eval.py</files>
  <action>
    Create tests/test_phase243_gate_eval.py with synthetic same-run-arm fixtures (built inline / tmp_path) exercising every behavior listed in Task 1's behavior block: one all-pass fixture (outcome "pass", exit 0) and one fixture per fail mode — avg-delta regression, p99-delta regression, p99 absolute ceiling, CPU delta, nonzero zombie, monotonic-upward fd trend, non-empty stall_events, and below-n-floor (assert fail-closed, not silent pass). The CPU fixture must supply per-arm cpu_nsec_delta/window_wall_sec/n_cores such that the derived fping cpu_pct exceeds the icmplib cpu_pct by >= 2.0 points (the frozen bound) and assert the CPU gate fails; the pass fixture must keep cpu_delta_pts < 2.0. Add a fixture with missing cpu_nsec evidence and assert fail-closed (cannot derive cpu_delta_pts -> not a silent pass). Each fail fixture must assert the corresponding gate verdict is "fail"/rollback and the process exit code is nonzero. Invoke the evaluator either by importing its evaluate()/main() with argv or via a subprocess run() helper; be consistent. Assert that thresholds come from the frozen JSON (e.g. a fixture sitting exactly at the boundary +epsilon flips the verdict, proving the 20%/10ms/2.0-pt figures are honored).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase243_gate_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase243_gate_eval.py -x` exits 0
    - There is one passing-arm test plus at least 8 distinct fail-mode tests (avg, p99, ceiling, CPU, zombie, fd-trend, stall, n-floor)
    - The CPU test supplies cpu_nsec window-delta evidence yielding cpu_delta_pts >= 2.0 and asserts the CPU gate fails; a missing-cpu_nsec fixture asserts fail-closed
    - The n-floor test asserts fail-closed behavior on an incomplete arm
    - A boundary test proves the 20% / 10ms / 2.0-pt figures are read from the frozen thresholds JSON
  </acceptance_criteria>
  <done>Full pass + fail-mode matrix green, proving the gate honors the frozen D-04 thresholds (incl. the CPU% bound from CPUUsageNSec evidence) and fails closed on incomplete arms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-arm evidence JSON → gate-eval | incomplete/malformed arms could yield a false pass |
| frozen thresholds JSON → gate-eval | thresholds must not be overridable by inline literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-08 | Tampering / Repudiation | incomplete arm silently passes | mitigate | Fail-closed on missing autorate_cycle_total, missing cpu_nsec evidence, or count below the D-04c floor — GateEvalError / fail verdict, never silent pass (V5 input validation) |
| T-243-09 | Repudiation | post-hoc threshold rationalization | mitigate | load_thresholds() reads ONLY scripts/phase243-thresholds.json (committed in Plan 01 before evidence, CPU_DELTA_PCT_POINTS frozen at 2.0); verdict references the frozen path so the gate can't be re-derived against moved goalposts |
| T-243-10 | Tampering | wrong-baseline gate (historical vs same-run) | mitigate | Delta computed against the same-run icmplib arm (D-02 primary); CPU% delta computed fping-arm vs same-run icmplib-arm; historical 2.85/6.9 is advisory-only representativeness, never the gate baseline |
| T-243-SC | Tampering | npm/pip/cargo installs | N/A | Zero new package installs (stdlib only) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase243_gate_eval.py -q` green (pass + all fail modes incl. CPU delta)
- `.venv/bin/ruff check scripts/phase243-gate-eval.py` and `.venv/bin/mypy src/wanctl/` (no src/wanctl change; mypy unaffected)
- Zero src/wanctl edits (verified by the Plan 01 SAFE-17 verifier at wave merge)
</verification>

<success_criteria>
- Gate evaluator blocks on any D-04/D-04a/D-04b/D-04c regression vs the same-run icmplib arm
- The CPU% gate derives cpu_delta_pts from the per-arm CPUUsageNSec window deltas and enforces the frozen 2.0-pt bound
- Thresholds sourced only from the frozen JSON; incomplete arms fail closed
- Full pass + fail-mode test matrix green
</success_criteria>

<output>
Create `.planning/phases/243-cycle-budget-benchmark-gate/243-03-SUMMARY.md` when done
</output>
