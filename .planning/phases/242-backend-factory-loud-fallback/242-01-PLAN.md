---
phase: 242-backend-factory-loud-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rtt_backend_factory.py
  - tests/test_health_check.py
  - scripts/phase242-safe17-boundary-check.sh
autonomous: true
requirements: [FALL-01, FALL-02, SAFE-17]
must_haves:
  truths:
    - "A test file asserts the loud-fallback behavior (WARN + per-WAN counter + /health signal) and is RED until the factory lands"
    - "A test pins the COMMON THREAD PROTOCOL: the handle's make_thread() output exposes get_cycle_status() AND get_latest() returns the RTTSnapshot shape measure_rtt() consumes (NOT just the fallback signal)"
    - "A test proves fping-selected + factory-produced thread handle can drive WANController.measure_rtt() WITHOUT AttributeError (the regression that would have caught the contract gap)"
    - "A test asserts the three steering-consumed /health fields stay byte-identical (subset assertion) after additive keys"
    - "A Phase-242 SAFE-17 boundary verifier exists with an expanded allowlist covering the factory + four call-site/health files, and a negative self-test proves it fails CLOSED on an out-of-allowlist edit"
  artifacts:
    - path: "tests/test_rtt_backend_factory.py"
      provides: "FALL-01 + FALL-02 unit tests + the thread-protocol contract assertion + the measure_rtt-no-AttributeError regression"
      contains: "def test_fallback_is_loud"
    - path: "scripts/phase242-safe17-boundary-check.sh"
      provides: "Phase 242 fail-closed boundary verifier (clone of 241 + expanded allowlist) with a negative self-test"
      contains: "rtt_backend_factory"
    - path: "tests/test_health_check.py"
      provides: "subset byte-preservation assertion for available/raw_rtt_ms/staleness_sec"
      contains: "byte_preserved"
  key_links:
    - from: "scripts/phase242-safe17-boundary-check.sh"
      to: "scripts/phase239-protected-body-diff.py"
      via: "protected-body proof call (kept intact)"
      pattern: "phase239-protected-body-diff"
    - from: "tests/test_rtt_backend_factory.py"
      to: "src/wanctl/wan_controller.py"
      via: "fping-selected thread handle drives measure_rtt() without AttributeError"
      pattern: "measure_rtt"
---

<objective>
Wave 0 scaffold: author the test contracts and the SAFE-17 boundary verifier
BEFORE any factory or call-site code exists. This is the Nyquist/TDD anchor for
the phase — the FALL-01/FALL-02 tests, the THREAD-PROTOCOL contract assertion,
the measure_rtt()-no-AttributeError regression, and the subset byte-preservation
assertion go RED now and turn GREEN as Plans 02/03 land. The boundary verifier is
authored here with its expanded allowlist AND a negative self-test so Plan 04's
gate has a tool that is proven to fail closed.

Purpose: Lock the observable contract (loud fallback, the common thread protocol
measure_rtt requires, frozen /health fields, fail-closed allowlist boundary)
before implementation so executors build against fixed acceptance criteria. The
review's load-bearing HIGH (factory hands a raw FpingThread to _rtt_thread →
AttributeError in measure_rtt) is closed HERE by pinning the protocol in tests so
unit tests cannot stay GREEN while the live path breaks.
Output: tests/test_rtt_backend_factory.py (RED), an extended
tests/test_health_check.py subset byte-preservation test, and
scripts/phase242-safe17-boundary-check.sh (executable, expanded allowlist,
negative self-test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md
@.planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md

<interfaces>
<!-- The factory under test does not exist yet. Tests are written against the
     contract the factory MUST satisfy (per CONTEXT D-02/D-03/D-04 + the review's
     RttBackendHandle remediation). The import WILL fail until Plan 02 — that is
     the intended RED state. Do NOT stub the factory. -->

Factory contract (target — does not exist yet, defined by the review remediation):
  build_rtt_backend(config, source_ip, logger, *, wan_key) -> RttBackendHandle
  RttBackendHandle (dataclass) fields:
    - backend            : the RttBackend Protocol object (RTTMeasurement or FpingMeasurement)
    - backend_active     : str  — what actually ran after any fallback ("icmplib"|"fping")
    - fell_back          : bool — True iff fping was requested but the factory fell back
    - fallback_count     : int  — per-handle counter (per-WAN, NOT process-global)
  RttBackendHandle.make_thread(hosts_fn, shutdown_event, *, pool=None, cadence_sec) -> driver thread
    - DEFERRED build: shutdown_event/pool/hosts_fn do NOT exist at construction; the
      autorate caller invokes make_thread later (Pitfall 1 timing). Do NOT build the
      thread at build_rtt_backend() time.

COMMON THREAD PROTOCOL (what measure_rtt() consumes — measure_rtt is PROTECTED, byte-frozen):
  make_thread() output MUST expose:
    - start()                                  (lifecycle)
    - stop()                                   (lifecycle)
    - get_latest() -> RTTSnapshot | None       (measure_rtt reads .timestamp, .per_host_results,
                                                .rtt_ms, .active_hosts, .successful_hosts)
    - get_cycle_status() -> RTTCycleStatus | None  (measure_rtt isinstance-checks; None tolerated)
    - get_profile_stats() -> dict
    - cadence_sec (property/attr)
  icmplib path  -> BackgroundRTTThread already satisfies this verbatim.
  fping path    -> an ADAPTER (built in Plan 02, lives in rtt_backend_factory.py, NOT in
                   241-frozen fping_measurement.py) wraps FpingThread:
                     get_cycle_status() returns None  (measure_rtt tolerates None)
                     get_latest() returns RttSample.to_snapshot()  (RTTSnapshot shape)
                   FpingThread.get_latest() returns RttSample, which has .to_snapshot()
                   producing the RTTSnapshot measure_rtt expects.

  Resolution cases:
  - backend "fping" requested + shutil.which("fping") is None -> handle.make_thread builds the
    icmplib BackgroundRTTThread; backend_active "icmplib"; fell_back True
  - backend "fping" requested + fping present                 -> handle.make_thread builds the
    fping adapter (FpingThread wrapped); backend_active "fping"; fell_back False
  - measurement.backend absent                                -> icmplib; fell_back False; no warn

Frozen /health measurement fields (238 mandate — must stay byte-identical, SUBSET assertion):
  available, raw_rtt_ms, staleness_sec

Existing analog tests to mirror (fixture + logger-injection + monkeypatch style):
  tests/test_fping_measurement.py, tests/test_rtt_backend.py
  tests/test_health_check.py::test_measurement_section_present_with_live_snapshot (line ~1916)

WARN-once idiom precedent: src/wanctl/fping_measurement.py:64,265-267 (_first_failure_logged)
MEASUREMENT_BACKENDS = ("icmplib","fping")  in src/wanctl/check_config_validators.py:30
RttSample.to_snapshot(): src/wanctl/rtt_backend.py:~56 (legacy RTTSnapshot from a RttSample)
measure_rtt consumes get_cycle_status() at wan_controller.py:1152 (PROTECTED — read-only here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the FALL-01 + FALL-02 factory tests + the THREAD-PROTOCOL contract + the measure_rtt-no-AttributeError regression (RED)</name>
  <files>tests/test_rtt_backend_factory.py</files>
  <read_first>
    - tests/test_fping_measurement.py (fixture + logger-injection + monkeypatch shutil.which style to mirror)
    - tests/test_rtt_backend.py (RttBackend/RttSample assertion style; RttSample.to_snapshot usage)
    - src/wanctl/fping_measurement.py (FpingMeasurement/FpingThread constructor shapes the factory will build; FpingThread.get_latest returns RttSample :316; NO get_cycle_status; _first_failure_logged WARN idiom at :64,:265; timeout<cadence ValueError at :299)
    - src/wanctl/rtt_measurement.py (RTTMeasurement.__init__ source_ip param at :139; BackgroundRTTThread at :412 — already exposes start/stop/get_latest/get_cycle_status/get_profile_stats/cadence_sec; RTTSnapshot fields :101-106; RTTCycleStatus fields :119-122)
    - src/wanctl/rtt_backend.py (RttSample :37 + RttSample.to_snapshot returning RTTSnapshot — the adapter's get_latest translation target)
    - src/wanctl/wan_controller.py (measure_rtt :1137-1235 PROTECTED — read ONLY to learn what it consumes from the thread: get_latest()→snapshot.{timestamp,per_host_results,rtt_ms,active_hosts,successful_hosts}; get_cycle_status()→RTTCycleStatus|None tolerated via isinstance; __init__ :327 to learn how to construct a WANController in a test; start_background_rtt :1080 to learn how a thread gets installed)
    - src/wanctl/check_config_validators.py (MEASUREMENT_BACKENDS at :30, absent->icmplib resolution rule at validate_measurement_backend :960)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Pitfall 1 deferred-build timing; Open Questions RESOLVED block)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the "two highest-value tests" section ~line 134)
  </read_first>
  <behavior>
    - test_fallback_is_loud: backend "fping" selected + shutil.which("fping") monkeypatched to None -> (a) handle.backend is an icmplib RTTMeasurement, (b) a WARN-level record is emitted (caplog at WARNING), (c) handle.fallback_count >= 1, (d) handle.fell_back True and handle.backend_active "icmplib". The "loud, never silent" proof for FALL-02.
    - test_fallback_builds_icmplib: same monkeypatch scenario, asserts the FALL-01 construction half: handle.backend is RTTMeasurement; no FpingMeasurement constructed.
    - test_fping_selected_builds_fping: shutil.which("fping") monkeypatched to a truthy path + backend "fping" -> handle.backend is FpingMeasurement, fell_back False, backend_active "fping", no WARN record.
    - test_default_icmplib: measurement.backend key absent from config -> icmplib backend, fell_back False, no WARN (matches the validator's absent->icmplib rule).
    - test_thread_protocol_contract (THE HIGH-CLOSING TEST): for BOTH the fallback case (fping requested, binary absent) AND the fping-present case, call handle.make_thread(hosts_fn, shutdown_event, pool=<a real or dummy ThreadPoolExecutor>, cadence_sec=<a value strictly greater than the fping timeout>) and assert the produced thread (a) exposes callables start, stop, get_latest, get_cycle_status, get_profile_stats and a cadence_sec attribute; (b) get_cycle_status() returns either an RTTCycleStatus or None (never raises AttributeError); (c) get_latest() returns either None or an object with attributes rtt_ms, per_host_results, timestamp, active_hosts, successful_hosts (the RTTSnapshot shape) — proving the fping adapter translates RttSample->RTTSnapshot. Assert the protocol on the produced thread, NOT just on the handle's fallback signal.
    - test_fping_selected_measure_rtt_no_attributeerror (THE REGRESSION THAT CATCHES THE GAP): with fping present, build the handle, build the thread via make_thread, install it on a WANController instance as its background RTT thread (assign to _rtt_thread the way start_background_rtt would, OR construct the controller with the thread factory and call start_background_rtt), then call WANController.measure_rtt() and assert it returns (None or a float) WITHOUT raising AttributeError on get_cycle_status. Seed the adapter's cached sample if needed so get_latest returns a snapshot. This is the live-autorate-path regression the review demanded.
    - test_fping_timeout_ge_cadence_falls_back (timeout-vs-cadence DISPOSITION = loud fallback, NOT crash): with fping present but a config whose fping timeout >= the make_thread cadence (the FpingThread ValueError condition), make_thread must NOT propagate ValueError — it must fall back to icmplib LOUDLY (WARN + fell_back True + backend_active "icmplib"). Asserts the decided disposition.
  </behavior>
  <action>
    Create tests/test_rtt_backend_factory.py importing build_rtt_backend (and the
    RttBackendHandle type if exported) from the target factory module
    src/wanctl/rtt_backend_factory.py. The import WILL fail until Plan 02 — that is
    the intended RED state; do NOT stub the factory. Use pytest caplog at
    logging.WARNING and monkeypatch.setattr on the factory module's shutil.which
    reference per the analog tests. Build a minimal config object exposing
    config.data.get("measurement", {}).get("backend") and the ping knobs (timeout_ping,
    ping_source_ip) so the cases drive absent/fping/icmplib resolution. Assert backend
    type via isinstance against RTTMeasurement / FpingMeasurement. For the thread-protocol
    and measure_rtt tests, invoke handle.make_thread(...) with a real
    concurrent.futures.ThreadPoolExecutor (or a small dummy that satisfies the icmplib
    BackgroundRTTThread constructor) and a hosts_fn returning a fixed host list; choose
    a cadence_sec strictly greater than the fping timeout for the success path and one
    that violates it for the fallback path. Name the tests exactly test_fallback_is_loud,
    test_fallback_builds_icmplib, test_fping_selected_builds_fping, test_default_icmplib,
    test_thread_protocol_contract, test_fping_selected_measure_rtt_no_attributeerror,
    test_fping_timeout_ge_cadence_falls_back so the VALIDATION test map commands resolve.
    Reference FALL-01/FALL-02 in test docstrings. Do NOT inline factory implementation
    in the test (no production logic in the test file). Do NOT edit measure_rtt or any
    PROTECTED body — the measure_rtt test only CALLS measure_rtt, it does not modify it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_rtt_backend_factory.py -q 2>&1 | grep -qiE "error|failed|no module|cannot import|attributeerror"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_rtt_backend_factory.py defines all seven named tests: test_fallback_is_loud, test_fallback_builds_icmplib, test_fping_selected_builds_fping, test_default_icmplib, test_thread_protocol_contract, test_fping_selected_measure_rtt_no_attributeerror, test_fping_timeout_ge_cadence_falls_back
    - The file is RED for the right reason: collection/import failure on the missing build_rtt_backend symbol, NOT a syntax error in the test file itself
    - test_fallback_is_loud asserts all four loud pillars: icmplib backend built, WARN record present, per-handle fallback_count incremented, fell_back/backend_active signal
    - test_thread_protocol_contract asserts the PRODUCED THREAD (make_thread output) exposes get_cycle_status (callable, returns RTTCycleStatus|None, never AttributeError) AND get_latest returns the RTTSnapshot shape (rtt_ms/per_host_results/timestamp/active_hosts/successful_hosts) — for both icmplib and fping paths
    - test_fping_selected_measure_rtt_no_attributeerror calls WANController.measure_rtt() with the factory-produced fping thread installed and asserts no AttributeError
    - test_fping_timeout_ge_cadence_falls_back asserts loud fallback to icmplib (NOT a propagated ValueError)
    - No production code is created or modified by this task; measure_rtt is only called, never edited
  </acceptance_criteria>
  <done>The seven named tests exist and fail on the missing factory import; FALL-01, FALL-02, the common thread protocol, the measure_rtt-no-AttributeError regression, and the timeout-vs-cadence disposition are all encoded as executable assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_health_check.py with the SUBSET /health byte-preservation assertion</name>
  <files>tests/test_health_check.py</files>
  <read_first>
    - tests/test_health_check.py (test_measurement_section_present_with_live_snapshot at ~:1916 — the fixture + live-snapshot + start_health_server pattern to copy)
    - src/wanctl/health_check.py (_build_measurement_section at :454; the three frozen fields at :516-518: available/raw_rtt_ms/staleness_sec)
    - src/wanctl/wan_controller.py (producer "measurement" dict at :4487 — raw_rtt_ms/staleness_sec source values)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (/health additive-key section ~line 240)
  </read_first>
  <behavior>
    - test_measurement_byte_preserved: with a live RTT snapshot set on the mock WAN, the /health measurement section's available, raw_rtt_ms, and staleness_sec values match the exact pre-242 expected values (available True, raw_rtt_ms rounded to 2dp, staleness_sec rounded to 3dp) as a SUBSET assertion — assert these three keys' exact values and rounding, NOT object bytes or full-dict equality, so the additive 242 keys (backend_active/fell_back/fallback_count) may be present or absent without breaking the test. Passes now (no additive keys yet) and MUST keep passing after Plan 03 adds keys — it is the standing regression guard.
  </behavior>
  <action>
    Add a test named test_measurement_byte_preserved (or a method whose -k token is
    byte_preserved so `pytest tests/test_health_check.py -k byte_preserved` resolves)
    that mirrors test_measurement_section_present_with_live_snapshot: set _last_raw_rtt
    and the staleness inputs, start the health server, GET /health, and assert the
    three frozen fields equal their exact expected rounded values via a SUBSET check
    (assert section["available"] == ..., section["raw_rtt_ms"] == round(..., 2),
    section["staleness_sec"] == round(..., 3)). Do NOT assert dict equality on the
    whole measurement object and do NOT assert the additive keys are absent — the
    assertion must remain forward-compatible with Plan 03's additive keys. This locks
    the 238 byte-preservation mandate as a standing subset regression.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_health_check.py contains a test matching -k byte_preserved
    - The test asserts available, raw_rtt_ms, and staleness_sec exact values + rounding from a live snapshot AS A SUBSET (no full-dict / object-bytes equality)
    - `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` exits 0 (passes now, before any additive keys)
    - The assertion does not require the additive 242 keys to be absent (forward-compatible with Plan 03)
  </acceptance_criteria>
  <done>A standing SUBSET byte-preservation regression for the three frozen /health fields exists and is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/phase242-safe17-boundary-check.sh (clone of 241, expanded allowlist) + a negative self-test that proves it fails CLOSED</name>
  <files>scripts/phase242-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/phase241-safe17-boundary-check.sh (the full file to clone — allowlist regex at :24, allowed_paths set at :112-119, close-anchor const pattern at :20-21, RTT-seam guard at :290-298, reflector_scorer guard at :300-307, dirty-tree gate at :248-277, protected-body call at :316-321, --out path-confinement at :209-224)
    - scripts/phase239-protected-body-diff.py (PROTECTED set at :21-32 — kept intact, NOT modified)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the phase242 verifier section ~line 151 with the exact expansion list)
    - .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json (for the 241 head_commit reference)
  </read_first>
  <action>
    Copy phase241-safe17-boundary-check.sh to phase242-safe17-boundary-check.sh and
    make exactly these changes, preserving every fail-closed gate:
    (1) Expand V153_ALLOWLIST_RE to add: rtt_backend_factory\.py, autorate_continuous\.py,
        health_check\.py, wan_controller\.py, and the steering subpath as
        steering/daemon\.py — the regex anchors on ^src/wanctl/ so use an alternation
        that matches steering/daemon\.py explicitly. Keep the existing six 241 files.
    (2) Expand the allowed_paths Python set (the :112-119 union) to the identical set
        including "src/wanctl/rtt_backend_factory.py", "src/wanctl/autorate_continuous.py",
        "src/wanctl/steering/daemon.py", "src/wanctl/health_check.py",
        "src/wanctl/wan_controller.py". Regex and set MUST stay in sync.
    (3) Add a PHASE241_CLOSE_ANCHOR const set to commit d8179bb6 (the last 241 src/wanctl
        commit "feat(241-02): validate fping measurement config") and a no-drift guard
        block mirroring the existing reflector_scorer guard (:300-307) that runs
        `git diff --quiet <PHASE241_CLOSE_ANCHOR> HEAD -- src/wanctl/fping_measurement.py src/wanctl/reflector_scorer.py`
        and fails closed if 242 added any new diff to the 241-frozen files. Resolve the
        anchor to a SHA via the same git rev-parse --verify pattern used for the 239/240
        anchors; emit it in the evidence JSON.
    (4) Update OUT and ALLOWED_OUT_PREFIX to the 242 evidence dir
        (.planning/phases/242-backend-factory-loud-fallback/evidence/).
    (5) Keep INTACT and unweakened: the dirty-tree fail-closed gate, the RTT-seam
        no-drift-since-239 guard, the reflector_scorer-unchanged-since-240 guard, the
        protected-body proof call to phase239-protected-body-diff.py, and the --out
        path-confinement guards. Do NOT edit phase239-protected-body-diff.py.
    (6) NEGATIVE SELF-TEST (closes the MEDIUM "grep-only is weak / does it fail closed?"):
        Add a self-test path to the script, gated behind an explicit flag (e.g.
        `--self-test`), that proves the allowlist fails CLOSED. The self-test must, in a
        DETACHED git worktree or a temporary throwaway checkout (NEVER mutating the live
        tree or src/wanctl in place), introduce a synthetic out-of-allowlist edit to a
        src/wanctl file that is NOT in the allowlist (e.g. append a comment line to a
        controller file like src/wanctl/queue_controller.py), run the verifier's
        allowlist check against that synthetic diff, assert the verifier returns NON-ZERO
        / reports the path under disallowed_paths, then tear the worktree down. The
        self-test must restore any temporary state and leave the live tree clean. Keep
        this path strictly separate from the real --anchor evidence run. chmod +x the
        new script. Do NOT run the real evidence path against post-edit HEAD yet (no
        src/wanctl edits exist this plan) — running `bash -n`, confirming the allowlist
        regex compiles, and running `--self-test` proving it fails closed is sufficient
        here.
  </action>
  <verify>
    <automated>bash -n scripts/phase242-safe17-boundary-check.sh && grep -q 'rtt_backend_factory' scripts/phase242-safe17-boundary-check.sh && grep -q 'steering/daemon' scripts/phase242-safe17-boundary-check.sh && grep -q 'PHASE241_CLOSE_ANCHOR' scripts/phase242-safe17-boundary-check.sh && grep -q 'phase239-protected-body-diff' scripts/phase242-safe17-boundary-check.sh && grep -q 'self-test' scripts/phase242-safe17-boundary-check.sh && test -x scripts/phase242-safe17-boundary-check.sh && scripts/phase242-safe17-boundary-check.sh --self-test</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase242-safe17-boundary-check.sh passes `bash -n` (no syntax errors)
    - The allowlist regex AND the allowed_paths set both include rtt_backend_factory.py, autorate_continuous.py, steering/daemon.py, health_check.py, wan_controller.py plus the original six 241 files
    - A PHASE241_CLOSE_ANCHOR const + a no-drift guard for fping_measurement.py/reflector_scorer.py is present
    - The protected-body call to phase239-protected-body-diff.py, the dirty-tree gate, the RTT-seam guard, and the --out path-confinement guards are all still present (grep-confirmed)
    - `scripts/phase242-safe17-boundary-check.sh --self-test` exits 0 by PROVING the verifier returns non-zero / lists the path under disallowed_paths for a synthetic out-of-allowlist edit made in a detached worktree, then leaves the live tree clean (`git status --porcelain -- src/wanctl/` empty after)
    - The script is executable; phase239-protected-body-diff.py is unmodified
  </acceptance_criteria>
  <done>The Phase 242 boundary verifier exists with the union allowlist, the 241 close-anchor guard, and a negative self-test proving it fails CLOSED on out-of-allowlist edits; Plan 04 can run it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds tests + a read-only git-diff verifier script. No network input, no auth, no external I/O, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-01 | Tampering | phase242 verifier --out path | mitigate | Reuse the 241 --out path-confinement guards verbatim (reject src/wanctl, reject `..`, realpath-confine to the 242 evidence dir) — do not weaken them |
| T-242-02 | Repudiation | boundary verifier weakened/bypassed or fails OPEN | mitigate | Keep all four fail-closed gates from 241 intact; verify-by-grep the protected-body call and dirty-tree gate survive the clone; the NEW negative self-test proves the allowlist actually fails CLOSED (not just that grep tokens exist) |
| T-242-12 | Tampering | self-test mutating the live src/wanctl tree | mitigate | The self-test runs ONLY in a detached/throwaway worktree, restores state, and asserts `git status --porcelain -- src/wanctl/` is empty afterward — it never edits the live tree in place |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — additive observable signal only, no new dependencies) |

No high-severity threats: this plan is test scaffolding + a read-only diff verifier. The verifier reduces risk (it is the SAFE-17 mitigation); the negative self-test strengthens that mitigation.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_rtt_backend_factory.py -q` is RED on the missing factory import (expected), and the seven named tests (including the thread-protocol + measure_rtt-no-AttributeError regressions) are collected.
- `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` is GREEN (subset assertion).
- `bash -n scripts/phase242-safe17-boundary-check.sh` exits 0; allowlist + 241 close-anchor guard grep-confirmed; `--self-test` proves fail-closed and leaves the tree clean.
</verification>

<success_criteria>
The FALL-01/FALL-02 contracts, the COMMON THREAD PROTOCOL contract, the
measure_rtt()-no-AttributeError regression, and the timeout-vs-cadence disposition
are all RED-pending in tests/test_rtt_backend_factory.py; the subset byte-preservation
regression is GREEN; and a Phase-242 SAFE-17 verifier with the expanded allowlist
exists, parses, and is proven to fail CLOSED by a negative self-test. Wave 0 gaps
from VALIDATION.md are closed and the review's load-bearing HIGH is pinned in tests.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-01-SUMMARY.md` when done
</output>
</output>
