---
phase: 242-backend-factory-loud-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rtt_backend_factory.py
  - tests/test_health_check.py
  - scripts/phase242-safe17-boundary-check.sh
autonomous: true
requirements: [FALL-01, FALL-02, SAFE-17]
must_haves:
  truths:
    - "A test file asserts the loud-fallback behavior (WARN + counter + /health signal) and is RED until the factory lands"
    - "A test asserts the three steering-consumed /health fields stay byte-identical after additive keys"
    - "A Phase-242 SAFE-17 boundary verifier exists with an expanded allowlist covering all four call-site/health files"
  artifacts:
    - path: "tests/test_rtt_backend_factory.py"
      provides: "FALL-01 + FALL-02 highest-value unit tests"
      contains: "def test_fallback_is_loud"
    - path: "scripts/phase242-safe17-boundary-check.sh"
      provides: "Phase 242 fail-closed boundary verifier (clone of 241 + expanded allowlist)"
      contains: "rtt_backend_factory"
    - path: "tests/test_health_check.py"
      provides: "byte-preservation assertion for available/raw_rtt_ms/staleness_sec"
      contains: "byte_preserved"
  key_links:
    - from: "scripts/phase242-safe17-boundary-check.sh"
      to: "scripts/phase239-protected-body-diff.py"
      via: "protected-body proof call (kept intact)"
      pattern: "phase239-protected-body-diff"
---

<objective>
Wave 0 scaffold: author the test contracts and the SAFE-17 boundary verifier
BEFORE any factory or call-site code exists. This is the Nyquist/TDD anchor for
the phase — the FALL-01/FALL-02 tests and the byte-preservation assertion go RED
now and turn GREEN as Plans 02/03 land. The boundary verifier is authored here
with its expanded allowlist so Plan 04's gate has a tool to run.

Purpose: Lock the observable contract (loud fallback, frozen /health fields,
allowlist boundary) before implementation so executors build against fixed
acceptance criteria, not the other way around.
Output: tests/test_rtt_backend_factory.py (RED), an extended
tests/test_health_check.py byte-preservation test, and
scripts/phase242-safe17-boundary-check.sh (executable, expanded allowlist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md
@.planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md

<interfaces>
<!-- The factory under test does not exist yet. Tests are written against the
     contract the factory MUST satisfy (per CONTEXT D-02/D-03/D-04). Executor
     writes tests that import the not-yet-existing build_rtt_backend and assert
     the contract; tests are expected to FAIL on import/attribute until Plan 02. -->

Factory contract (target — does not exist yet):
  build_rtt_backend(config, source_ip, shutdown_event, logger, ...) -> (backend, thread) + fallback signal
  - backend == "fping" requested + shutil.which("fping") is None  -> builds icmplib RTTMeasurement + BackgroundRTTThread, fell_back=True
  - backend == "fping" requested + fping present                  -> builds FpingMeasurement + FpingThread, fell_back=False
  - measurement.backend absent                                    -> icmplib, fell_back=False, no warn

Frozen /health measurement fields (238 mandate — must stay byte-identical):
  available, raw_rtt_ms, staleness_sec

Existing analog tests to mirror (fixture + logger-injection + monkeypatch style):
  tests/test_fping_measurement.py, tests/test_rtt_backend.py
  tests/test_health_check.py::test_measurement_section_present_with_live_snapshot (line ~1916)

WARN-once idiom precedent: src/wanctl/fping_measurement.py:64,265-267 (_first_failure_logged)
MEASUREMENT_BACKENDS = ("icmplib","fping")  in src/wanctl/check_config_validators.py:30
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the FALL-01 + FALL-02 factory tests (RED)</name>
  <files>tests/test_rtt_backend_factory.py</files>
  <read_first>
    - tests/test_fping_measurement.py (fixture + logger-injection + monkeypatch shutil.which style to mirror)
    - tests/test_rtt_backend.py (RttBackend/RttSample assertion style)
    - src/wanctl/fping_measurement.py (FpingMeasurement/FpingThread constructor shapes the factory will build; _first_failure_logged WARN idiom at :64,:265)
    - src/wanctl/rtt_measurement.py (RTTMeasurement.__init__ source_ip param at :139; BackgroundRTTThread at :412)
    - src/wanctl/check_config_validators.py (MEASUREMENT_BACKENDS at :30, absent->icmplib resolution rule at validate_measurement_backend :960)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the "two highest-value tests" section ~line 134)
  </read_first>
  <behavior>
    - test_fallback_is_loud: backend "fping" selected + shutil.which("fping") monkeypatched to None -> (a) factory returns an icmplib RTTMeasurement-typed backend and a BackgroundRTTThread-typed (or builder) thread, (b) a WARN-level record is emitted (caplog at WARNING), (c) fallback_count is incremented to >=1, (d) the fallback signal exposes fell_back True and backend_active "icmplib". This is the "loud, never silent" proof for FALL-02.
    - test_fallback_builds_icmplib: same monkeypatch scenario, asserts the FALL-01 construction half only (icmplib backend + driver thread built; no fping objects constructed).
    - test_fping_selected_builds_fping: shutil.which("fping") monkeypatched to a truthy path + backend "fping" -> FpingMeasurement + FpingThread built, fell_back False, no WARN record.
    - test_default_icmplib: measurement.backend key absent from config -> icmplib backend, fell_back False, no WARN (matches the validator's absent->icmplib rule).
  </behavior>
  <action>
    Create tests/test_rtt_backend_factory.py importing build_rtt_backend from the
    target factory module (the import WILL fail until Plan 02 — that is the
    intended RED state; do NOT stub the factory). Use pytest caplog at
    logging.WARNING and monkeypatch.setattr on shutil.which (or the factory
    module's which reference) per the analog tests. Build a minimal fake/real
    config object exposing config.data.get("measurement", {}).get("backend") so
    the four cases drive the absent/fping/icmplib resolution. Assert backend type
    via isinstance against RTTMeasurement / FpingMeasurement and thread type/shape
    against BackgroundRTTThread / FpingThread (or the documented builder per the
    Plan 02 D-04 return shape — read 242-RESEARCH Pitfall 1 / Open Question 1 so
    the thread assertion matches the chosen return shape; if uncertain, assert on
    the bundle's backend_active/fell_back fields which are shape-independent). Name
    the four tests exactly test_fallback_is_loud, test_fallback_builds_icmplib,
    test_fping_selected_builds_fping, test_default_icmplib so the VALIDATION test
    map commands resolve. Reference FALL-01/FALL-02 in test docstrings.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_rtt_backend_factory.py -q 2>&1 | grep -qiE "error|failed|no module|cannot import|attributeerror"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_rtt_backend_factory.py defines test_fallback_is_loud, test_fallback_builds_icmplib, test_fping_selected_builds_fping, test_default_icmplib
    - The file is RED for the right reason: collection/import failure on the missing build_rtt_backend symbol, NOT a syntax error in the test file itself
    - test_fallback_is_loud asserts all four loud pillars: icmplib backend built, WARN record present, fallback_count incremented, fell_back/backend_active signal
    - No production code is created or modified by this task
  </acceptance_criteria>
  <done>The four named tests exist and fail on the missing factory import; FALL-01 and FALL-02 contracts are encoded as executable assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_health_check.py with the /health byte-preservation assertion</name>
  <files>tests/test_health_check.py</files>
  <read_first>
    - tests/test_health_check.py (test_measurement_section_present_with_live_snapshot at ~:1916 — the fixture + live-snapshot + start_health_server pattern to copy)
    - src/wanctl/health_check.py (_build_measurement_section at :454; the three frozen fields at :516-518: available/raw_rtt_ms/staleness_sec)
    - src/wanctl/wan_controller.py (producer "measurement" dict at :4487 — raw_rtt_ms/staleness_sec source values)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (/health additive-key section ~line 240)
  </read_first>
  <behavior>
    - test_measurement_byte_preserved: with a live RTT snapshot set on the mock WAN, the /health measurement section's available, raw_rtt_ms, and staleness_sec values match the exact pre-242 expected values (available True, raw_rtt_ms rounded to 2dp, staleness_sec rounded to 3dp) regardless of whether the additive 242 keys (backend_active/fell_back/fallback_count) are present. The test passes now (no additive keys yet) and MUST keep passing after Plan 03 adds keys — it is the regression guard, not a RED test.
  </behavior>
  <action>
    Add a test named test_measurement_byte_preserved (or a method whose -k token
    is byte_preserved so the VALIDATION command
    `pytest tests/test_health_check.py -k byte_preserved` resolves) that mirrors
    test_measurement_section_present_with_live_snapshot: set _last_raw_rtt and the
    staleness inputs, start the health server, GET /health, and assert the three
    frozen fields equal their exact expected rounded values. Make the assertion
    independent of additive keys (assert the three keys' values directly; do not
    assert dict equality on the whole measurement object). This locks the 238
    byte-preservation mandate as a standing regression that survives Plan 03's
    additive keys.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_health_check.py contains a test matching -k byte_preserved
    - The test asserts available, raw_rtt_ms, and staleness_sec exact values from a live snapshot
    - `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` exits 0 (passes now, before any additive keys)
    - The assertion does not require the additive 242 keys to be absent (forward-compatible with Plan 03)
  </acceptance_criteria>
  <done>A standing byte-preservation regression for the three frozen /health fields exists and is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/phase242-safe17-boundary-check.sh (clone of 241, expanded allowlist)</name>
  <files>scripts/phase242-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/phase241-safe17-boundary-check.sh (the full file to clone — allowlist regex at :24, allowed_paths set at :112-119, close-anchor const pattern at :20-21, RTT-seam guard at :290-298, reflector_scorer guard at :300-307, dirty-tree gate at :248-277, protected-body call at :316-321, --out path-confinement at :209-224)
    - scripts/phase239-protected-body-diff.py (PROTECTED set at :21-32 — kept intact, NOT modified)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the phase242 verifier section ~line 151 with the exact expansion list)
    - .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json (for anchor_sha 69f39db1 and the 241 head_commit reference)
  </read_first>
  <action>
    Copy phase241-safe17-boundary-check.sh to phase242-safe17-boundary-check.sh and
    make exactly these changes, preserving every fail-closed gate:
    (1) Expand V153_ALLOWLIST_RE to add: rtt_backend_factory\.py, autorate_continuous\.py,
        health_check\.py, wan_controller\.py, and the steering subpath as
        steering/daemon\.py — the regex anchors on ^src/wanctl/ so use an
        alternation that matches steering/daemon\.py explicitly. Keep the existing
        six 241 files.
    (2) Expand the allowed_paths Python set (the :112-119 union) to the identical
        set including "src/wanctl/rtt_backend_factory.py",
        "src/wanctl/autorate_continuous.py", "src/wanctl/steering/daemon.py",
        "src/wanctl/health_check.py", "src/wanctl/wan_controller.py". Regex and set
        MUST stay in sync.
    (3) Add a PHASE241_CLOSE_ANCHOR const set to commit d8179bb6 (the last 241
        src/wanctl commit) and a no-drift guard block mirroring the existing
        reflector_scorer guard (:300-307) that runs
        `git diff --quiet <PHASE241_CLOSE_ANCHOR> HEAD -- src/wanctl/fping_measurement.py src/wanctl/reflector_scorer.py`
        and fails closed if 242 added any new diff to the 241-frozen files. Resolve
        the anchor to a SHA via the same git rev-parse --verify pattern used for the
        239/240 anchors; emit it in the evidence JSON.
    (4) Update OUT and ALLOWED_OUT_PREFIX to the 242 evidence dir
        (.planning/phases/242-backend-factory-loud-fallback/evidence/).
    (5) Keep INTACT and unweakened: the dirty-tree fail-closed gate, the RTT-seam
        no-drift-since-239 guard, the reflector_scorer-unchanged-since-240 guard,
        the protected-body proof call to phase239-protected-body-diff.py, and the
        --out path-confinement guards. Do NOT edit phase239-protected-body-diff.py.
    chmod +x the new script. Do NOT run it yet against post-edit HEAD (no src/wanctl
    edits exist this plan) — a self-test against the clean anchor proving it loads
    and the allowlist regex compiles is sufficient here.
  </action>
  <verify>
    <automated>bash -n scripts/phase242-safe17-boundary-check.sh && grep -q 'rtt_backend_factory' scripts/phase242-safe17-boundary-check.sh && grep -q 'steering/daemon' scripts/phase242-safe17-boundary-check.sh && grep -q 'PHASE241_CLOSE_ANCHOR' scripts/phase242-safe17-boundary-check.sh && grep -q 'phase239-protected-body-diff' scripts/phase242-safe17-boundary-check.sh && test -x scripts/phase242-safe17-boundary-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase242-safe17-boundary-check.sh passes `bash -n` (no syntax errors)
    - The allowlist regex AND the allowed_paths set both include rtt_backend_factory.py, autorate_continuous.py, steering/daemon.py, health_check.py, wan_controller.py plus the original six 241 files
    - A PHASE241_CLOSE_ANCHOR const + a no-drift guard for fping_measurement.py/reflector_scorer.py is present
    - The protected-body call to phase239-protected-body-diff.py, the dirty-tree gate, the RTT-seam guard, and the --out path-confinement guards are all still present (grep-confirmed)
    - The script is executable; phase239-protected-body-diff.py is unmodified
  </acceptance_criteria>
  <done>The Phase 242 boundary verifier exists with the union allowlist and the 241 close-anchor guard; Plan 04 can run it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds tests + a read-only git-diff verifier script. No network input, no auth, no external I/O, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-01 | Tampering | phase242 verifier --out path | mitigate | Reuse the 241 --out path-confinement guards verbatim (reject src/wanctl, reject `..`, realpath-confine to the 242 evidence dir) — do not weaken them |
| T-242-02 | Repudiation | boundary verifier weakened/bypassed | mitigate | Keep all four fail-closed gates from 241 intact; verify-by-grep in acceptance criteria that the protected-body call and dirty-tree gate survive the clone |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — additive observable signal only, no new dependencies) |

No high-severity threats: this plan is test scaffolding + a read-only diff verifier. The verifier reduces risk (it is the SAFE-17 mitigation), it does not add surface.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_rtt_backend_factory.py -q` is RED on the missing factory import (expected).
- `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` is GREEN.
- `bash -n scripts/phase242-safe17-boundary-check.sh` exits 0; allowlist + 241 close-anchor guard grep-confirmed.
</verification>

<success_criteria>
The FALL-01/FALL-02 contracts are RED-pending in tests/test_rtt_backend_factory.py,
the byte-preservation regression is GREEN, and a Phase-242 SAFE-17 verifier with
the expanded allowlist exists and parses. Wave 0 gaps from VALIDATION.md are closed.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-01-SUMMARY.md` when done
</output>
