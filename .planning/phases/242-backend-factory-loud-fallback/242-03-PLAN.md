---
phase: 242-backend-factory-loud-fallback
plan: 03
type: execute
wave: 3
depends_on: ["242-02"]
files_modified:
  - src/wanctl/autorate_continuous.py
  - src/wanctl/steering/daemon.py
  - src/wanctl/wan_controller.py
  - src/wanctl/health_check.py
  - tests/test_wan_controller.py
  - tests/test_health_check.py
autonomous: true
requirements: [FALL-02, SAFE-17]
must_haves:
  truths:
    - "D-01: route construction only — both autorate_continuous.py and steering/daemon.py construct via build_rtt_backend() (Criterion 3, single construction site); steering's pinger stays DEAD, still consuming the autorate /health baseline (live consumption deferred to Phase 245)"
    - "autorate_continuous.py constructs its RTT backend via build_rtt_backend() (LIVE path); the live autorate path is behavior-identical (icmplib by default)"
    - "autorate's _create_wan_components constructs WANController(config.wan_name, config, router, handle.controller_measurement, logger, rtt_thread_factory=handle, rtt_backend_status=handle) — the positional rtt_measurement slot is filled with handle.controller_measurement (ALWAYS an icmplib RTTMeasurement), NEVER handle.backend. self.rtt_measurement is therefore NEVER a FpingMeasurement, so the three controller helper paths (ping_hosts_with_results :1269, ping_host :1474, maybe_probe :3127) never AttributeError under fping mode (cycle-2 HIGH closed)"
    - "WANController gains OPTIONAL constructor state (rtt_thread_factory=None, rtt_backend_status=None) so existing call sites/tests do not churn; measure_rtt stays PROTECTED/untouched; start_background_rtt (non-protected) uses make_thread when a factory handle is present, else builds BackgroundRTTThread as today"
    - "start_background_rtt passes self._background_rtt_cadence_sec() (the controller background-RTT cadence ≈0.25s) to handle.make_thread, but the fping branch of make_thread IGNORES that param and builds FpingThread with the handle's resolved measurement.fping.cadence_sec (default 10.0) — so the LIVE fping path stays backend_active 'fping' and does NOT fall back to icmplib at startup (cycle-3 HIGH closed). The icmplib branch uses the controller cadence as today"
    - "The factory-produced thread (icmplib thread OR fping adapter) is what gets assigned to _rtt_thread — a raw FpingThread is NEVER assigned; measure_rtt() runs without AttributeError (cycle-1 HIGH stays closed)"
    - "steering/daemon.py constructs its RTT backend via build_rtt_backend() with a Config-like object loaded from the PRIMARY WAN's autorate config (the file SteeringConfig.primary_wan_config points to, already opened in _derive_primary_health_url) via an EXPLICIT helper, NOT from steering.yaml; both measurement.backend AND source_ip resolve from that primary WAN config; non-None per-WAN source_ip (D-01a), LOUD on failure to resolve; consumption stays dead; the steering backend source-of-truth is made explicit for Phase 245"
    - "D-03: minimal additive /health fallback signal only (backend_active, fell_back, fallback_count) read PER-CONTROLLER from this WAN's handle (NOT a module-level global); the three steering-consumed fields (available, raw_rtt_ms, staleness_sec) stay byte-preserved"
    - "SCOPE (242 explicit): the reflector scorer runs via maybe_probe(now, self.rtt_measurement) on the icmplib controller_measurement; fping does NOT drive the scorer in 242 — fping's loss-aware scoring on the controller path is a Phase 245 A/B concern, documented as a deliberate scope decision"
  artifacts:
    - path: "src/wanctl/autorate_continuous.py"
      provides: "first factory call site (LIVE path), zero backend-type branching; binds handle.controller_measurement to the WANController rtt_measurement slot"
      contains: "build_rtt_backend"
    - path: "src/wanctl/steering/daemon.py"
      provides: "second factory call site (DEAD path) with loud non-None source_ip + backend both resolved from the primary WAN config via an explicit Config-like helper"
      contains: "build_rtt_backend"
    - path: "src/wanctl/wan_controller.py"
      provides: "optional rtt_thread_factory/rtt_backend_status constructor state + start_background_rtt uses make_thread (fping cadence honored by the handle) + producer writes per-WAN fallback keys"
      contains: "rtt_thread_factory"
    - path: "src/wanctl/health_check.py"
      provides: "_build_measurement_section reflects the additive keys; three frozen fields untouched"
      contains: "backend_active"
    - path: "tests/test_wan_controller.py"
      provides: "regression: handle.controller_measurement bound to rtt_measurement lets the three controller helper paths run without AttributeError under fping mode; the REAL start_background_rtt path with fping selected + valid cadence keeps backend_active 'fping'; factory-produced fping thread lets measure_rtt() run clean"
      contains: "controller_measurement"
    - path: "tests/test_health_check.py"
      provides: "additive-key + per-WAN independence assertions; byte-preservation regression stays GREEN"
      contains: "backend_active"
  key_links:
    - from: "src/wanctl/autorate_continuous.py"
      to: "src/wanctl/rtt_backend_factory.py"
      via: "build_rtt_backend(...) call in _create_wan_components returning a handle; handle.controller_measurement fills the WANController rtt_measurement positional slot"
      pattern: "controller_measurement"
    - from: "src/wanctl/wan_controller.py"
      to: "src/wanctl/rtt_backend_factory.py"
      via: "start_background_rtt invokes handle.make_thread(pool, hosts_fn, shutdown_event, cadence_sec); the fping branch uses handle.fping_cadence_sec not this cadence"
      pattern: "make_thread"
    - from: "src/wanctl/steering/daemon.py"
      to: "src/wanctl/rtt_backend_factory.py"
      via: "build_rtt_backend(primary_wan_config_like, source_ip, ...) — backend+source_ip resolved from the primary WAN config via an explicit Config-like helper, not steering.yaml"
      pattern: "build_rtt_backend"
    - from: "src/wanctl/wan_controller.py"
      to: "src/wanctl/health_check.py"
      via: "producer measurement dict (per-WAN handle status) -> _build_measurement_section additive keys"
      pattern: "fell_back|fallback_count"
---

<objective>
Wire the factory into both construction sites and thread the loud-fallback signal
through to /health — the controller-path surface of the phase. autorate's
_create_wan_components calls build_rtt_backend and receives an RttBackendHandle; it
constructs the WANController with handle.controller_measurement (an icmplib
RTTMeasurement) in the existing positional rtt_measurement slot — NEVER handle.backend
— plus rtt_thread_factory=handle and rtt_backend_status=handle as optional trailing
params so the handle and its per-WAN fallback status ride into the controller without
churning existing call sites/tests. start_background_rtt (NOT protected) builds its
background thread via handle.make_thread(...) when a factory handle is present — so the
thread that lands on _rtt_thread is the protocol-satisfying icmplib thread OR the fping
adapter, NEVER a raw FpingThread. measure_rtt stays byte-frozen and untouched.

CYCLE-3 HIGH (live-wiring manifestation): start_background_rtt passes
self._background_rtt_cadence_sec() (the controller background-RTT cadence ≈0.25s) into
handle.make_thread as before. The fix lives in the handle (Plan 02): the fping branch of
make_thread IGNORES that controller cadence and builds FpingThread with the handle's
resolved measurement.fping.cadence_sec (default 10.0). The live fping path therefore stays
backend_active "fping" and does NOT fall back to icmplib at startup. A REAL-path test on
WANController.start_background_rtt asserts this. The icmplib branch keeps using the
controller cadence as today.

Steering gets a CORRECT, non-None per-WAN source_ip AND a backend both resolved from the
PRIMARY WAN config now (D-01a), via an EXPLICIT Config-like helper (not steering.yaml, not
a raw-dict shape build_rtt_backend cannot consume), LOUD if it cannot resolve, while its
consumption stays dead (Phase 245 flips that). The /health producer reads the per-WAN
handle status; the three frozen fields stay byte-identical.

Purpose: Satisfy Criterion 3 (single construction site for both consumers), FALL-02
pillar 3 (/health attribution, per-WAN), and D-01a (correct source_ip for 245), while
holding the 238 byte-preservation mandate AND closing the review findings: (cycle-1) no
raw FpingThread on _rtt_thread; (cycle-2) self.rtt_measurement is ALWAYS an icmplib
RTTMeasurement (handle.controller_measurement) so ping_host/ping_hosts_with_results/
maybe_probe never AttributeError under fping mode; (cycle-3 HIGH) the live fping path uses
the resolved fping cadence so fping actually runs; (cycle-3 MEDIUM) steering passes a
Config-like object via an explicit helper. Optional constructor state avoids call-site churn.
Output: collapsed call sites in autorate + steering binding controller_measurement,
optional thread-factory wiring in wan_controller's start_background_rtt, per-WAN additive
/health keys via the producer + health_check reflector, the controller-helper-paths
regression test, the REAL start_background_rtt fping-active test, all inside the SAFE-17
allowlist.

EVERY edit lands inside the 242 allowlist (autorate_continuous.py, steering/daemon.py,
wan_controller.py, health_check.py + the factory module; test edits land in tests/ which
is OUTSIDE src/wanctl/ and therefore outside the allowlist regex anchor — they are
allowed and the dirty-tree gate covers them per Plan 04). Do NOT touch any PROTECTED
byte-frozen body: RTTMeasurement.*, BackgroundRTTThread._run,
BackgroundRTTThread._ping_with_persistent_pool, WANController.measure_rtt
(phase239-protected-body-diff.py:21-32). start_background_rtt is NOT in the protected
set — edit it minimally and deliberately. Construct/wire, never modify protected bodies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md

<interfaces>
<!-- Concrete call-site + producer shapes + the wan_controller wiring points. Extracted from codebase. -->

factory handle (from Plan 02) — src/wanctl/rtt_backend_factory.py:
  handle = build_rtt_backend(config, source_ip, logger, *, wan_key) -> RttBackendHandle
  handle.backend / handle.controller_measurement / handle.backend_active / handle.fell_back /
    handle.fallback_count / handle.fping_cadence_sec
  handle.controller_measurement is ALWAYS an icmplib RTTMeasurement (the object that fills
    WANController's positional rtt_measurement slot — NEVER handle.backend)
  handle.fping_cadence_sec is the RESOLVED measurement.fping.cadence_sec (default 10.0) — the
    cadence the fping branch of make_thread builds FpingThread with (cycle-3 HIGH)
  handle.make_thread(hosts_fn, shutdown_event, *, pool, cadence_sec) -> protocol-satisfying thread
    The fping branch IGNORES the caller-supplied cadence_sec and uses handle.fping_cadence_sec.
    The icmplib branch uses the caller-supplied cadence_sec (the controller background cadence).

THE CYCLE-3 HIGH (live wiring — VERIFIED against live source) — what this plan must pin:
  start_background_rtt today builds BackgroundRTTThread with
    cadence_sec=self._background_rtt_cadence_sec() (wan_controller.py:1101).
    _background_rtt_cadence_sec() = max(self._cycle_interval_ms/1000, BACKGROUND_RTT_MIN_CADENCE_SECONDS)
    = ~0.25s on the 50ms loop (wan_controller.py:90,:1105-1110, VERIFIED).
  If that 0.25s were passed to the fping FpingThread, FpingThread.__init__ would raise ValueError
    (3.0s default timeout >= 0.25s — fping_measurement.py:299) and fping would fall back to icmplib
    EVERY startup. The Plan 02 handle prevents this: make_thread's fping branch uses
    handle.fping_cadence_sec (default 10.0 > 3.0s), NOT the passed cadence. This plan keeps passing
    self._background_rtt_cadence_sec() to make_thread (correct for the icmplib branch) and relies on
    the handle to use the fping cadence for the fping branch. A REAL start_background_rtt test asserts
    backend_active stays "fping" — proving fping actually runs, not just that a manual cadence>timeout
    unit test passes.

THE CYCLE-2 HIGH (verified against live code) — what this plan must pin:
  WANController.__init__ takes positional rtt_measurement: RTTMeasurement (:331) stored at :338.
  self.rtt_measurement is consumed on THREE controller helper paths:
    - self.rtt_measurement.ping_hosts_with_results(...)  # wan_controller.py:1269 (blocking fallback)
    - self.rtt_measurement.ping_host(gateway_ip, count=1) # wan_controller.py:1474 (gateway check)
    - self._reflector_scorer.maybe_probe(now, self.rtt_measurement)  # wan_controller.py:3127
      (reflector_scorer.py:277 — maybe_probe(self, now, rtt_measurement: RTTMeasurement))
  FpingMeasurement has NONE of ping_host / ping_hosts_with_results / the maybe_probe contract.
  THEREFORE the positional rtt_measurement slot MUST be filled with handle.controller_measurement
  (icmplib RTTMeasurement), NOT handle.backend (which is a FpingMeasurement when fping is selected).

autorate call site — src/wanctl/autorate_continuous.py:120-167:
  _create_wan_components(config, logger) -> (router, rtt_measurement)
  current: RTTMeasurement(logger, timeout_ping=config.timeout_ping, aggregation_strategy=RTTAggregationStrategy.AVERAGE, log_sample_stats=True, source_ip=config.ping_source_ip)  # :145
  current construction: WANController(config.wan_name, config, router, rtt_measurement, logger)  # :167
  TARGET construction: WANController(config.wan_name, config, router, handle.controller_measurement,
    logger, rtt_thread_factory=handle, rtt_backend_status=handle)
  thread start: autorate_continuous.py wan_info["controller"].start_background_rtt(rtt_shutdown)

WANController — src/wanctl/wan_controller.py:
  __init__ :327-339 (wan_name, config, router, rtt_measurement, logger) — ADD optional
    rtt_thread_factory=None, rtt_backend_status=None as the LAST params so existing
    positional call sites/tests do not churn (review HIGH). Default None preserves today's behavior.
    The positional rtt_measurement param is now fed handle.controller_measurement by autorate.
  _rtt_thread/_rtt_pool init :772-773
  start_background_rtt :1080-1103 (NOT protected): builds the pool :1091 then
    BackgroundRTTThread :1095 with hosts_fn=self._reflector_scorer.get_active_hosts,
    cadence_sec=self._background_rtt_cadence_sec(). EDIT minimally: when
    self._rtt_thread_factory is not None, build via
    self._rtt_thread_factory.make_thread(hosts_fn=self._reflector_scorer.get_active_hosts,
    shutdown_event=shutdown_event, pool=self._rtt_pool, cadence_sec=self._background_rtt_cadence_sec())
    and assign to self._rtt_thread; else keep the existing BackgroundRTTThread construction verbatim.
    The passed cadence is correct for the icmplib branch; the fping branch uses handle.fping_cadence_sec
    inside make_thread (cycle-3 HIGH) — do NOT special-case the fping cadence here at the call site.
  measure_rtt :1137-1235 (PROTECTED — DO NOT TOUCH): reads self._rtt_thread.get_latest()
    + get_cycle_status(); the factory-produced thread satisfies both.
  controller helper paths :1269 / :1474 / :3127 (NOT protected — but DO NOT edit them here;
    they are correct as-is once self.rtt_measurement is the icmplib controller_measurement)
  producer "measurement" dict :4487-4501: add backend_active/fell_back/fallback_count
    sourced from self._rtt_backend_status (the per-WAN handle status passed at construction).

steering call site — src/wanctl/steering/daemon.py:2544-2561 (DEAD path):
  _create_steering_components(config, logger) -> (state_mgr, router, rtt_measurement, baseline_loader)
  current: RTTMeasurement(logger, timeout_ping=config.timeout_ping, aggregation_strategy=RTTAggregationStrategy.MEDIAN, log_sample_stats=False)  # :2554 — NO source_ip (the D-01a gap)
  PRIMARY-WAN source-of-truth: SteeringConfig.primary_wan_config (loaded in _load_topology
    :192-194, a Path to the primary WAN autorate YAML). Steering already opens it in
    _derive_primary_health_url (:202-221) with yaml.safe_load + try/except.
  CONFIG-SHAPE FIX (review MEDIUM): build_rtt_backend(config, ...) reads config.data["measurement"]
    and config.timeout_ping — it expects a Config-LIKE object, NOT a raw YAML dict. Steering's
    primary WAN YAML is a raw dict from yaml.safe_load. THEREFORE add an EXPLICIT helper (e.g.
    _load_primary_wan_config_for_rtt_backend() -> (config_like, source_ip, warnings)) that loads
    the primary WAN YAML and returns a Config-like object whose .data["measurement"] and
    .timeout_ping build_rtt_backend can consume, plus the resolved source_ip. Do NOT pass a raw dict
    that build_rtt_backend cannot read (executors would otherwise drift to reading the backend from
    steering.yaml or dropping fping/timeout params). The helper resolves BOTH measurement.backend AND
    source_ip from that primary WAN config so Phase 245 inherits a coherent backend+source_ip pairing.
  Make a None/missing source_ip LOUD (WARN), do NOT swallow it silently (review MEDIUM: a
    swallowed exception silently passing None defeats D-01a). The helper returns warnings the caller
    logs at WARN. Prefer reusing the yaml.safe_load load already in _derive_primary_health_url to
    reduce drift (reviewer suggestion).

reflector — src/wanctl/health_check.py:454,516-518 (_build_measurement_section):
  FROZEN: "available": raw_rtt is not None, "raw_rtt_ms": round(raw_rtt,2)..., "staleness_sec": round(staleness,3)...
  (Phase 186 precedent already ADDS state/successful_count/stale beside them — same additive shape)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse both call sites to build_rtt_backend(); bind handle.controller_measurement to WANController's rtt_measurement slot; add optional thread-factory state; wire make_thread; add the REAL start_background_rtt fping-active test; plumb LOUD steering source_ip+backend from the PRIMARY WAN config via an explicit Config-like helper</name>
  <files>src/wanctl/autorate_continuous.py, src/wanctl/steering/daemon.py, src/wanctl/wan_controller.py, tests/test_wan_controller.py</files>
  <read_first>
    - src/wanctl/autorate_continuous.py (_create_wan_components :120-152 — the RTTMeasurement(...) call at :145 to collapse; the WANController construction at :167 — bind handle.controller_measurement here, NOT handle.backend; start_background_rtt call site)
    - src/wanctl/wan_controller.py (__init__ :327-339 — add the two optional trailing params + store them; the positional rtt_measurement slot is fed handle.controller_measurement; self.rtt_measurement consumers at :1269 ping_hosts_with_results, :1474 ping_host, :3127 maybe_probe — confirm these need the icmplib RTTMeasurement; _rtt_thread/_rtt_pool :772-773; BACKGROUND_RTT_MIN_CADENCE_SECONDS=0.25 :90 + _background_rtt_cadence_sec :1105-1110 — the controller cadence still passed to make_thread, IGNORED by the fping branch; start_background_rtt :1080-1103 — the minimal edit point; measure_rtt :1137 is PROTECTED — do NOT touch; producer dict :4487-4501 for Task 2 context)
    - src/wanctl/reflector_scorer.py (maybe_probe :277 — `def maybe_probe(self, now, rtt_measurement: RTTMeasurement)`; confirms the icmplib contract the scorer needs)
    - src/wanctl/fping_measurement.py (FpingMeasurement defines ONLY probe/is_available/record_results — NO ping_host/ping_hosts_with_results; confirms why handle.backend cannot fill the rtt_measurement slot; _timeout :57 + FpingThread ValueError :299 — why the fping cadence must be the resolved 10.0, not 0.25)
    - src/wanctl/steering/daemon.py (_create_steering_components :2544-2561 — the dead RTTMeasurement with NO source_ip at :2554; SteeringConfig.primary_wan_config set in _load_topology :192-194; _derive_primary_health_url :202-221 for the primary-WAN YAML load pattern to reuse; _load_rtt_measurement :248 — confirms no ping_source_ip field exists)
    - src/wanctl/rtt_backend_factory.py (the build_rtt_backend signature, RttBackendHandle incl. controller_measurement + fping_cadence_sec, and make_thread from Plan 02; confirm build_rtt_backend reads config.data["measurement"] + config.timeout_ping — i.e. a Config-LIKE object, which dictates the steering helper's return shape)
    - src/wanctl/check_config_validators.py (measurement.fping.cadence_sec recognized key :258 + cadence example default 10 :1045 — confirms the independent fping cadence the handle resolves and the fping branch of make_thread uses, NOT the ~0.25s controller cadence)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Pitfall 1 thread-timing/deferred build, Pitfall 4 steering source_ip, Pitfall 5 no config.backend accessor, Assumption A1)
    - tests/test_rtt_backend_factory.py (the handle/make_thread/controller_measurement shape the call sites must consume; test_fping_selected_measure_rtt_no_attributeerror + test_controller_measurement_is_rttmeasurement + test_fping_uses_resolved_cadence show the install + binding path)
    - tests/test_wan_controller.py (existing construction call sites — confirm the optional trailing params keep them GREEN unchanged; how a WANController + start_background_rtt is exercised in tests; the suite this plan extends with the controller-helper-paths regression and the real-path fping-active test)
  </read_first>
  <behavior>
    - autorate _create_wan_components stops constructing RTTMeasurement directly and calls handle = build_rtt_backend(config, source_ip=config.ping_source_ip, logger, wan_key=<wan name>). The WANController is constructed as WANController(config.wan_name, config, router, handle.controller_measurement, logger, rtt_thread_factory=handle, rtt_backend_status=handle) — the positional rtt_measurement slot is filled with handle.controller_measurement (an icmplib RTTMeasurement), NEVER handle.backend. The live RTT path behaves identically (icmplib by default since prod configs omit measurement.backend) — no behavior change, only construction relocation.
    - self.rtt_measurement is therefore ALWAYS an icmplib RTTMeasurement, so the three controller helper paths (ping_hosts_with_results :1269, ping_host :1474, maybe_probe :3127) run without AttributeError even when fping is the selected background backend (cycle-2 HIGH closed). These three call sites are NOT edited — they are correct once the bound object is icmplib.
    - WANController.__init__ accepts rtt_thread_factory=None and rtt_backend_status=None as trailing optional params (default None preserves today's behavior; existing positional callers and tests are unaffected). start_background_rtt, when self._rtt_thread_factory is not None, builds the background thread via handle.make_thread(hosts_fn=self._reflector_scorer.get_active_hosts, shutdown_event=shutdown_event, pool=self._rtt_pool, cadence_sec=self._background_rtt_cadence_sec()) and assigns the result to self._rtt_thread; otherwise it constructs BackgroundRTTThread exactly as today. The thread on _rtt_thread is therefore always the protocol-satisfying icmplib thread or the fping adapter — never a raw FpingThread — so measure_rtt() runs without AttributeError (cycle-1 HIGH stays closed).
    - CYCLE-3 HIGH (live path): start_background_rtt passes the controller background cadence (~0.25s) to make_thread. For the fping branch make_thread IGNORES it and uses handle.fping_cadence_sec (the resolved measurement.fping.cadence_sec, default 10.0), so the live fping path stays backend_active "fping" and does NOT fall back to icmplib at startup. The call site does NOT special-case the fping cadence — the handle owns that. A real start_background_rtt test proves it.
    - steering _create_steering_components resolves a Config-like primary WAN config via an EXPLICIT helper (not steering.yaml, not a raw dict build_rtt_backend cannot read) and calls build_rtt_backend with it, resolving BOTH source_ip AND measurement.backend from the PRIMARY WAN's autorate config (the file SteeringConfig.primary_wan_config points to; review MEDIUM source-of-truth + Config-shape). LOUD (WARN) if the source_ip is missing/None — never silently None. It preserves the MEDIAN strategy intent on the controller_measurement (or documents the strategy choice as a Phase-245 decision point per Plan 02's note). Consumption stays DEAD (steering still reads autorate's /health baseline). The source-of-truth is made explicit in a comment: steering resolves measurement.backend AND source_ip from the primary WAN config so Phase 245 inherits a coherent backend+source_ip pairing.
    - SCOPE (242 explicit, review MEDIUM): the reflector scorer runs via maybe_probe(now, self.rtt_measurement) on the icmplib controller_measurement; fping does NOT drive the scorer in 242. fping's loss-aware scoring on the controller path is a Phase 245 A/B concern — documented as a deliberate scope decision in a comment near the binding.
    - Neither call site branches on backend type (D-04).
  </behavior>
  <action>
    autorate: collapse _create_wan_components (:145) to call build_rtt_backend, passing
    source_ip=config.ping_source_ip and wan_key. Honor Pitfall 1: do NOT construct any
    thread at the call site (the pool + hosts_fn do not exist until start_background_rtt).
    Construct the WANController as WANController(config.wan_name, config, router,
    handle.controller_measurement, logger, rtt_thread_factory=handle,
    rtt_backend_status=handle) — the positional rtt_measurement slot gets
    handle.controller_measurement (an icmplib RTTMeasurement), NEVER handle.backend; this is
    the cycle-2 HIGH fix. Add a brief comment that controller_measurement is always icmplib so
    the controller helper paths (ping_host/ping_hosts_with_results/maybe_probe) keep full
    RTTMeasurement semantics, and that fping (when selected) feeds only the background thread.
    If _create_wan_components currently returns (router, rtt_measurement), adjust it minimally
    to return the handle (or (router, handle)) and update the single caller — keep the change
    surgical. wan_controller.py: add rtt_thread_factory=None and rtt_backend_status=None as the
    LAST __init__ params (store as self._rtt_thread_factory / self._rtt_backend_status); this
    keeps every existing positional caller and test GREEN. In start_background_rtt, after
    building self._rtt_pool, branch: if self._rtt_thread_factory is not None, assign
    self._rtt_thread = self._rtt_thread_factory.make_thread(
    hosts_fn=self._reflector_scorer.get_active_hosts, shutdown_event=shutdown_event,
    pool=self._rtt_pool, cadence_sec=self._background_rtt_cadence_sec()) then .start(); else
    keep the existing BackgroundRTTThread construction verbatim. Pass the controller cadence as
    today — the handle's fping branch uses handle.fping_cadence_sec internally (cycle-3 HIGH),
    so do NOT add any fping-cadence special-casing at this call site. Do NOT edit measure_rtt
    (PROTECTED) and do NOT edit the three controller-helper call sites at :1269/:1474/:3127.
    steering: replace the no-source_ip RTTMeasurement at :2554 with build_rtt_backend, sourcing
    BOTH source_ip AND measurement.backend from the PRIMARY WAN's autorate config via an EXPLICIT
    helper (e.g. _load_primary_wan_config_for_rtt_backend() returning a Config-like object whose
    .data["measurement"] and .timeout_ping build_rtt_backend can consume, the resolved source_ip,
    and a warnings list) — reuse the yaml.safe_load load already in _derive_primary_health_url
    (:202-221) to read the primary WAN YAML, then wrap it as the Config-like object build_rtt_backend
    expects (do NOT pass a raw dict, and do NOT let build_rtt_backend resolve the backend from
    steering.yaml; review MEDIUM Config-shape). If that read yields a missing/None source_ip, log a
    WARN (loud — do NOT swallow into a silent None; RESEARCH A1/Pitfall 4 — None defeats D-01a). Bind
    handle.controller_measurement into the steering RTTMeasurement slot the same way (keep the MEDIAN
    strategy intent documented; note the AVERAGE-vs-MEDIAN aggregation shape is a Phase-245 decision
    point per Plan 02). Add a comment stating steering resolves BOTH backend and source_ip from the
    primary WAN config via the explicit Config-like helper so 245 inherits a coherent pairing, and
    that consumption stays dead until Phase 245. Add tests in tests/test_wan_controller.py: (a) a
    regression that a WANController built with handle.controller_measurement (under a fping-SELECTED
    config) can call all three controller helper paths — ping_host via the gateway-check path
    (:1474), ping_hosts_with_results via the blocking-fallback path (:1269), and
    maybe_probe(now, self.rtt_measurement) (:3127) — WITHOUT AttributeError, with network I/O
    mocked/monkeypatched so the test exercises method-presence, not real pings (cite
    wan_controller.py:1269/:1474/:3127); (b) a REAL-PATH fping-active test (the cycle-3 HIGH
    regression): build the handle via build_rtt_backend under a fping-SELECTED config with fping
    present (shutil.which patched truthy at both sites) and a valid measurement.fping.cadence_sec
    (default 10.0 or an explicit value > the fping timeout), construct a WANController with
    rtt_thread_factory=handle, call the REAL WANController.start_background_rtt(shutdown_event), and
    assert AFTER thread construction that handle.backend_active == "fping" (NOT "icmplib") and that
    self._rtt_thread is the fping adapter (not a BackgroundRTTThread and not a raw FpingThread) — i.e.
    fping actually runs on the live path rather than silently falling back. Stop the thread / set the
    shutdown event in teardown; (c) a factory-produced fping thread handle installed via
    start_background_rtt lets measure_rtt() run without AttributeError; and a steering test asserting
    _create_steering_components plumbs a NON-None source_ip AND resolves the backend from the primary
    WAN config via the explicit Config-like helper (not literal None, not steering.yaml) and does not
    consume the constructed pinger. Run ruff/mypy.
  </action>
  <verify>
    <automated>grep -q 'build_rtt_backend' src/wanctl/autorate_continuous.py && grep -q 'controller_measurement' src/wanctl/autorate_continuous.py && grep -q 'build_rtt_backend' src/wanctl/steering/daemon.py && grep -q 'rtt_thread_factory' src/wanctl/wan_controller.py && grep -q 'make_thread' src/wanctl/wan_controller.py && grep -q 'controller_measurement' tests/test_wan_controller.py && grep -qE 'start_background_rtt' tests/test_wan_controller.py && .venv/bin/pytest -o addopts='' tests/test_wan_controller.py tests/test_rtt_backend_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - build_rtt_backend is called in both _create_wan_components (autorate) and _create_steering_components (steering)
    - autorate constructs WANController with handle.controller_measurement in the positional rtt_measurement slot (grep shows controller_measurement in autorate_continuous.py) — NOT handle.backend; self.rtt_measurement is never a FpingMeasurement
    - WANController.__init__ accepts rtt_thread_factory=None and rtt_backend_status=None as optional trailing params (existing positional callers/tests unchanged and GREEN)
    - start_background_rtt assigns self._rtt_thread from handle.make_thread(...) when a factory handle is present (grep shows make_thread); it passes self._background_rtt_cadence_sec() (the controller cadence) and does NOT special-case the fping cadence at the call site; the thread on _rtt_thread is the protocol-satisfying thread/adapter, never a raw FpingThread
    - A REAL-PATH test in tests/test_wan_controller.py drives WANController.start_background_rtt() with a factory handle built under a fping-SELECTED config (fping present, valid measurement.fping.cadence_sec) and asserts handle.backend_active == "fping" AFTER thread construction AND that self._rtt_thread is the fping adapter (not a BackgroundRTTThread, not a raw FpingThread) — proving fping actually runs on the live path and does NOT fall back to icmplib at startup (cycle-3 HIGH regression). The manual make_thread(cadence>timeout) unit test in Plan 01 is insufficient alone; this real-path test is the keeper.
    - A regression test in tests/test_wan_controller.py builds a WANController with handle.controller_measurement under a fping-selected config and calls ping_host, ping_hosts_with_results, and maybe_probe(now, self.rtt_measurement) WITHOUT AttributeError (network I/O mocked); the three live call sites wan_controller.py:1269/:1474/:3127 are cited in the test
    - The three controller-helper call sites at :1269/:1474/:3127 and measure_rtt are NOT edited
    - The steering call resolves BOTH source_ip AND measurement.backend from the PRIMARY WAN config via an EXPLICIT Config-like helper (not steering.yaml, not a raw dict), passes a non-None source_ip, and emits a WARN if it cannot resolve one (grep shows primary-WAN resolution helper + a WARN on the None path; not literal None silently, not config.data from steering.yaml)
    - A comment documents that steering resolves backend+source_ip from the primary WAN config via the explicit Config-like helper (explicit source-of-truth for Phase 245) and that fping does not drive the reflector scorer in 242
    - Neither call site contains a backend-type branch (no `if backend == "fping"` at the call sites)
    - No edit to any PROTECTED body: `git diff` shows RTTMeasurement.*, BackgroundRTTThread._run/_ping_with_persistent_pool, and WANController.measure_rtt unchanged
    - `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py tests/test_rtt_backend_factory.py -q` exits 0; ruff + mypy clean on the edited src files
  </acceptance_criteria>
  <done>Both consumers construct their RTT backend through the single factory with zero backend-type branching; WANController.rtt_measurement is bound to handle.controller_measurement (always icmplib) so the three controller helper paths never AttributeError under fping mode (cycle-2 HIGH closed); start_background_rtt installs the protocol-satisfying thread and the live fping path stays backend_active "fping" because the handle uses the resolved fping cadence (cycle-3 HIGH closed, proven by a real-path test); WANController carries the optional handle without churning call sites; steering carries a correct, loud-on-failure per-WAN source_ip AND backend both resolved from the primary WAN config via an explicit Config-like helper; the live autorate path is behavior-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the PER-WAN additive fallback signal through the wan_controller producer into /health</name>
  <files>src/wanctl/wan_controller.py, src/wanctl/health_check.py, tests/test_health_check.py</files>
  <read_first>
    - src/wanctl/wan_controller.py (the producer "measurement" dict at :4487-4501 — where health_data[measurement] is assembled; this is where the additive keys must be WRITTEN, sourced from self._rtt_backend_status set in Task 1. measure_rtt :1137 is PROTECTED — do not touch it)
    - src/wanctl/health_check.py (_build_measurement_section :454; the three FROZEN fields at :516-518; the Phase 186 additive-key precedent that already adds state/successful_count/stale)
    - src/wanctl/rtt_backend_factory.py (RttBackendHandle.backend_active/fell_back/fallback_count — the PER-WAN source values; NOT a module-level global accessor)
    - tests/test_health_check.py (test_measurement_byte_preserved from Plan 01 — must stay GREEN; test_measurement_section_present_with_live_snapshot :1916 for the producer->reflector flow)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Open Question 3 — the producer must write the keys before _build_measurement_section can reflect them; D-03 minimal-signal scope)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the additive /health section ~line 240, frozen-field warning)
  </read_first>
  <behavior>
    - The wan_controller producer dict (health_data["measurement"]) gains additive keys carrying THIS WAN's fallback signal sourced from self._rtt_backend_status: backend_active (the backend that actually ran), fell_back (bool), fallback_count (int). When no handle was passed (rtt_backend_status is None — e.g. tests/legacy callers), the producer writes a safe default (backend_active "icmplib", fell_back False, fallback_count 0) so the keys are always present and never read from a module global. _build_measurement_section reflects these additive keys into the /health response.
    - Because the status is read per-controller (per-WAN), a fallback on the second WAN (ATT) is attributed correctly and does not read the first WAN's (Spectrum) state (review MEDIUM: dual-WAN attribution).
    - The three frozen fields (available, raw_rtt_ms, staleness_sec) are byte-identical: same keys, same rounding (2dp / 3dp), same computation. test_measurement_byte_preserved stays GREEN.
    - A new assertion proves the additive keys appear in /health and that backend_active reflects the active backend, and that two distinct controllers report independent fallback status.
  </behavior>
  <action>
    In wan_controller.py, extend ONLY the producer "measurement" dict at :4487 (additive
    keys, do not alter the existing raw_rtt_ms/staleness_sec/host/cadence keys) so it writes
    backend_active, fell_back, and fallback_count sourced from self._rtt_backend_status (the
    per-WAN handle reference stored in Task 1). Read PER-CONTROLLER state — do NOT add or
    call a module-level global fallback accessor (review MEDIUM: /health attribution must be
    per-WAN). When self._rtt_backend_status is None, write the safe defaults (backend_active
    "icmplib", fell_back False, fallback_count 0) so the keys are always present. In
    health_check.py, extend _build_measurement_section (:454) to reflect the three additive
    keys in the returned dict, ADDING beside available/raw_rtt_ms/staleness_sec exactly as
    the Phase 186 precedent added state/successful_count/stale — do NOT mutate the three
    frozen fields' keys, values, or rounding. Use planner-discretion key names that are
    additive and 244-forward-consistent (backend_active, fell_back, fallback_count per D-03a).
    Add an assertion in tests/test_health_check.py that /health exposes
    backend_active/fell_back/fallback_count, that the three frozen fields are unchanged
    (extend or add beside the byte_preserved test), and that two controllers with different
    handle status report independent fallback_count/backend_active (per-WAN proof). Run
    ruff/mypy.
  </action>
  <verify>
    <automated>grep -q 'fell_back\|backend_active' src/wanctl/wan_controller.py && grep -q 'fell_back\|backend_active' src/wanctl/health_check.py && .venv/bin/pytest tests/test_health_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The wan_controller producer "measurement" dict writes backend_active, fell_back, fallback_count (additive) sourced from self._rtt_backend_status (per-WAN), with safe defaults when the status is None — NOT from a module-level global
    - health_check._build_measurement_section reflects those keys; the three frozen fields available/raw_rtt_ms/staleness_sec keep identical keys/values/rounding
    - `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` exits 0 (still GREEN — the subset regression holds)
    - `.venv/bin/pytest tests/test_health_check.py -q` exits 0 including a new assertion that the additive fallback keys appear in /health and that two controllers report independent per-WAN fallback status
    - No PROTECTED body edited (measure_rtt unchanged); ruff + mypy clean
  </acceptance_criteria>
  <done>FALL-02 pillar 3 is live and PER-WAN: /health carries the additive fallback signal read from each controller's own handle status, dual-WAN attribution is independent, and the three frozen fields remain byte-identical (238 mandate held).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller -> /health response | The additive keys are computed in-process from THIS WAN's factory handle status; no external/untrusted input crosses into them. /health is an existing localhost observability endpoint. |
| primary WAN config file -> steering factory call | Steering reads the primary WAN's YAML via an explicit Config-like helper to source BOTH source_ip and measurement.backend (D-01a); this file is operator-owned config already trusted by _derive_primary_health_url. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-06 | Information Disclosure | /health leaking source_ip/internal data via the new keys | mitigate | D-03 ships ONLY backend_active (a backend name string), fell_back (bool), fallback_count (int) — NO source_ip, no per-sample attribution (that is Phase 244). The additive keys carry no PII or network-topology secret. |
| T-242-07 | Tampering | mutating the three byte-frozen /health fields | mitigate | Add keys ONLY beside available/raw_rtt_ms/staleness_sec; the standing subset test_measurement_byte_preserved regression (Plan 01) + the SAFE-17 boundary verifier (Plan 04) both fail closed on any frozen-field drift |
| T-242-08 | Repudiation | additive keys written but never surfaced (silent counter) | mitigate | The producer writes AND _build_measurement_section reflects, with a test asserting the keys reach the /health response — the counter is not write-only |
| T-242-14 | Repudiation | second-WAN fallback hidden by reading process-global state | mitigate | The producer reads self._rtt_backend_status (per-controller), never a module global; a test asserts two controllers report independent fallback status, so an ATT fallback is not masked by Spectrum's state (review MEDIUM) |
| T-242-15 | Spoofing/Repudiation | steering silently binding source_ip=None defeating D-01a | mitigate | Steering reads the primary WAN config's ping_source_ip via the explicit Config-like helper and emits a WARN if it cannot resolve a non-None value (loud, not swallowed); a test asserts a non-None source_ip is plumbed (review MEDIUM) |
| T-242-17 | Tampering | WANController.rtt_measurement bound to a FpingMeasurement -> AttributeError on ping_host/ping_hosts_with_results/maybe_probe (cycle-2 HIGH) | mitigate | autorate binds handle.controller_measurement (always icmplib RTTMeasurement) into the positional rtt_measurement slot — never handle.backend; a regression test exercises all three controller helper paths under a fping-selected config without AttributeError |
| T-242-18 | Repudiation | steering resolving measurement.backend from steering.yaml instead of the primary WAN config -> incoherent backend+source_ip for Phase 245 | mitigate | Steering resolves BOTH backend and source_ip from the primary WAN config via an explicit Config-like helper (the file _derive_primary_health_url opens), made explicit in a comment + test; build_rtt_backend does not read steering.yaml for the backend and does not receive a raw dict it cannot parse |
| T-242-21 | Repudiation/observability | live fping path silently falling back to icmplib at startup (cycle-3 HIGH live manifestation) | mitigate | start_background_rtt passes the controller cadence to make_thread but the fping branch uses handle.fping_cadence_sec (resolved 10.0 > 3.0s timeout); a real-path start_background_rtt test asserts backend_active stays "fping" and _rtt_thread is the fping adapter — proving fping runs live |
| T-242-09 | DoS | unbounded fallback_count growth | accept | fallback_count is a small per-handle integer incremented once per construction-time fallback decision (not per-sample); construction happens at most a few times per process lifetime — no unbounded growth surface |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: additive observable signal only; the new keys are a backend name + bool + small int with no external input and no secret content.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_health_check.py -q` GREEN including the subset byte-preservation regression, the new additive-key assertion, and the per-WAN independence assertion.
- `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py tests/test_rtt_backend_factory.py -q` GREEN (optional constructor state + factory-thread install + measure_rtt-no-AttributeError + the controller-helper-paths-under-fping regression + the real-path start_background_rtt fping-active regression).
- Hot-path slice GREEN: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
- `git diff` shows src edits confined to the four allowlisted src/wanctl files + no PROTECTED body change; test edits land in tests/ (outside the allowlist anchor, covered by Plan 04's dirty-tree gate).
- ruff + mypy clean on all four edited src files.
</verification>

<success_criteria>
Both consumers construct via the single factory (Criterion 3); WANController.rtt_measurement
is bound to handle.controller_measurement (always icmplib) so the three controller helper
paths never AttributeError under fping mode (cycle-2 HIGH closed); start_background_rtt
installs the protocol-satisfying thread and the live fping path stays backend_active "fping"
because the handle uses the resolved measurement.fping.cadence_sec (cycle-3 HIGH closed,
proven by a real-path test); WANController carries the optional handle without churning call
sites; measure_rtt runs clean (cycle-1 HIGH stays closed); steering carries a correct
loud-on-failure source_ip AND backend both resolved from the primary WAN config via an
explicit Config-like helper (D-01a, explicit source-of-truth for Phase 245); /health exposes
the loud fallback signal per-WAN (FALL-02 pillar 3); and the three steering-consumed /health
fields are byte-identical. All src edits sit inside the SAFE-17 allowlist with no
PROTECTED-body drift.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-03-SUMMARY.md` when done
</output>
</content>
</invoke>
