---
phase: 242-backend-factory-loud-fallback
plan: 03
type: execute
wave: 3
depends_on: ["242-02"]
files_modified:
  - src/wanctl/autorate_continuous.py
  - src/wanctl/steering/daemon.py
  - src/wanctl/wan_controller.py
  - src/wanctl/health_check.py
autonomous: true
requirements: [FALL-02, SAFE-17]
must_haves:
  truths:
    - "D-01: route construction only — both autorate_continuous.py and steering/daemon.py construct via build_rtt_backend() (Criterion 3, single construction site); steering's pinger stays DEAD, still consuming the autorate /health baseline (live consumption deferred to Phase 245)"
    - "autorate_continuous.py constructs its RTT backend via build_rtt_backend() (Criterion 3, live path)"
    - "steering/daemon.py constructs its RTT backend via build_rtt_backend() with a correct per-WAN source_ip (Criterion 3 + D-01a; consumption stays dead)"
    - "D-03: minimal additive /health fallback signal only (backend_active, fell_back, fallback_count); full per-sample attribution stays Phase 244; the three steering-consumed fields (available, raw_rtt_ms, staleness_sec) stay byte-preserved"
    - "/health exposes the additive fallback signal (backend_active, fell_back, fallback_count) via the wan_controller producer (FALL-02 pillar 3)"
    - "The three steering-consumed /health fields (available, raw_rtt_ms, staleness_sec) remain byte-identical"
  artifacts:
    - path: "src/wanctl/autorate_continuous.py"
      provides: "first factory call site (LIVE path), zero backend-type branching"
      contains: "build_rtt_backend"
    - path: "src/wanctl/steering/daemon.py"
      provides: "second factory call site (DEAD path) with source_ip binding"
      contains: "build_rtt_backend"
    - path: "src/wanctl/wan_controller.py"
      provides: "producer that writes the additive fallback keys into health_data[measurement]"
      contains: "fell_back"
    - path: "src/wanctl/health_check.py"
      provides: "_build_measurement_section reflects the additive keys; three frozen fields untouched"
      contains: "backend_active"
  key_links:
    - from: "src/wanctl/autorate_continuous.py"
      to: "src/wanctl/rtt_backend_factory.py"
      via: "build_rtt_backend(...) call in _create_wan_components"
      pattern: "build_rtt_backend"
    - from: "src/wanctl/steering/daemon.py"
      to: "src/wanctl/rtt_backend_factory.py"
      via: "build_rtt_backend(...) call in _create_steering_components"
      pattern: "build_rtt_backend"
    - from: "src/wanctl/wan_controller.py"
      to: "src/wanctl/health_check.py"
      via: "producer measurement dict -> _build_measurement_section additive keys"
      pattern: "fell_back|fallback_count"
---

<objective>
Wire the factory into both construction sites and thread the loud-fallback signal
through to /health — the controller-path surface of the phase. Both call sites
collapse to `backend, thread = build_rtt_backend(...)` with zero backend-type
branching (D-04). Steering gets a CORRECT per-WAN source_ip now (D-01a) while its
consumption stays dead (Phase 245 flips that). The /health producer in
wan_controller.py gains the additive backend_active/fell_back/fallback_count keys;
the three frozen fields stay byte-identical.

Purpose: Satisfy Criterion 3 (single construction site for both consumers),
FALL-02 pillar 3 (/health attribution), and D-01a (correct source_ip binding for
245), while holding the 238 byte-preservation mandate.
Output: collapsed call sites in autorate + steering, additive /health keys via the
wan_controller producer + health_check reflector, all inside the SAFE-17 allowlist.

EVERY edit in this plan lands inside the 242 allowlist (autorate_continuous.py,
steering/daemon.py, wan_controller.py, health_check.py + the factory module).
Do NOT touch any PROTECTED byte-frozen body: RTTMeasurement.*, BackgroundRTTThread._run,
BackgroundRTTThread._ping_with_persistent_pool, WANController.measure_rtt
(phase239-protected-body-diff.py:21-32). Construct these, never modify them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md

<interfaces>
<!-- Concrete call-site + producer shapes. Extracted from codebase. -->

autorate call site — src/wanctl/autorate_continuous.py:120-152:
  _create_wan_components(config, logger) -> (router, rtt_measurement)
  current: RTTMeasurement(logger, timeout_ping=config.timeout_ping, aggregation_strategy=RTTAggregationStrategy.AVERAGE, log_sample_stats=True, source_ip=config.ping_source_ip)
  thread built LATER: wan_controller.start_background_rtt (:1080, builds BackgroundRTTThread :1095 with pool + self._reflector_scorer.get_active_hosts) — NOT protected, but wan_controller.py is now allowlisted (Pitfall 1)
  thread start: autorate_continuous.py:515 wan_info["controller"].start_background_rtt(rtt_shutdown)

steering call site — src/wanctl/steering/daemon.py:2544-2561 (DEAD path):
  _create_steering_components(config, logger) -> (state_mgr, router, rtt_measurement, baseline_loader)
  current: RTTMeasurement(logger, timeout_ping=config.timeout_ping, aggregation_strategy=RTTAggregationStrategy.MEDIAN, log_sample_stats=False)  # NO source_ip — the D-01a gap
  D-01a source-of-truth: SteeringConfig.primary_wan_config (:192, a Path to the primary WAN autorate YAML) — read that YAML's ping_source_ip (autorate_config.py:643) for the per-WAN source. Steering already opens primary_wan_config in _derive_primary_health_url (:209) — reuse that load pattern.

producer — src/wanctl/wan_controller.py:4487-4501:
  "measurement": { "raw_rtt_ms": self._last_raw_rtt, "staleness_sec": (...), "active_reflector_hosts": ..., "successful_reflector_hosts": ..., "cadence_sec": ... }

reflector — src/wanctl/health_check.py:454,516-518 (_build_measurement_section):
  FROZEN: "available": raw_rtt is not None, "raw_rtt_ms": round(raw_rtt,2)..., "staleness_sec": round(staleness,3)...
  (Phase 186 precedent already ADDS state/successful_count/stale beside them — same additive shape)

fallback signal source: rtt_backend_factory.py exposes backend_active/fell_back/fallback_count (Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse both call sites to build_rtt_backend() + plumb steering source_ip (D-01a)</name>
  <files>src/wanctl/autorate_continuous.py, src/wanctl/steering/daemon.py</files>
  <read_first>
    - src/wanctl/autorate_continuous.py (_create_wan_components :120-152 — the RTTMeasurement(...) call to collapse; start_background_rtt call at :515)
    - src/wanctl/steering/daemon.py (_create_steering_components :2544-2561 — the dead RTTMeasurement with NO source_ip; SteeringConfig.primary_wan_config :192; _derive_primary_health_url :209 for the primary-WAN YAML load pattern; _load_rtt_measurement :248 — confirms no ping_source_ip field exists)
    - src/wanctl/rtt_backend_factory.py (the build_rtt_backend signature + return bundle from Plan 02)
    - src/wanctl/wan_controller.py (start_background_rtt :1080, BackgroundRTTThread :1095 — confirm where the icmplib thread is actually built so the call-site collapse matches the factory's thread-builder return shape; measure_rtt :1137 is PROTECTED — do not touch)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Pitfall 1 thread-timing, Pitfall 4 steering source_ip, Assumption A1)
    - tests/test_rtt_backend_factory.py (the bundle shape the call sites must consume)
  </read_first>
  <behavior>
    - autorate _create_wan_components stops constructing RTTMeasurement directly and instead receives the backend (+ thread or thread-builder) from build_rtt_backend(config, source_ip=config.ping_source_ip, ...). The live RTT path behaves identically (icmplib by default since prod configs omit measurement.backend) — no behavior change, only construction relocation.
    - steering _create_steering_components calls build_rtt_backend with a correct per-WAN source_ip sourced from the primary WAN config (D-01a), preserving the MEDIAN strategy. Consumption stays DEAD (steering still reads autorate's /health baseline).
    - Neither call site branches on backend type (D-04): both are `backend, thread = build_rtt_backend(...)` (or the documented bundle unpack).
  </behavior>
  <action>
    Collapse autorate _create_wan_components (:145) to call build_rtt_backend,
    passing source_ip=config.ping_source_ip and the existing AVERAGE strategy /
    log_sample_stats=True knobs through the factory. Honor Pitfall 1: do NOT
    construct BackgroundRTTThread at the call site (the pool + hosts_fn do not exist
    until start_background_rtt). Match whatever thread-builder/return shape Plan 02
    chose so the existing start_background_rtt continues to build the actual thread;
    if Plan 02 returns a builder closure, hand it through to where start_background_rtt
    can invoke it WITHOUT editing measure_rtt (PROTECTED). Touch start_background_rtt
    in wan_controller.py only if strictly required and only its non-protected body.
    For steering _create_steering_components (:2554): replace the no-source_ip
    RTTMeasurement with build_rtt_backend, sourcing source_ip from the primary WAN's
    autorate config — reuse the primary_wan_config YAML load already present in
    _derive_primary_health_url (:209) to read that WAN's ping_source_ip (RESEARCH A1:
    read primary WAN config, not None — passing None defeats D-01a). Keep the MEDIAN
    strategy. Add a brief comment that consumption stays dead until Phase 245. Run
    ruff/mypy after editing.
  </action>
  <verify>
    <automated>grep -q 'build_rtt_backend' src/wanctl/autorate_continuous.py && grep -q 'build_rtt_backend' src/wanctl/steering/daemon.py && .venv/bin/pytest -o addopts='' tests/test_wan_controller.py tests/test_rtt_backend_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - build_rtt_backend is called in both _create_wan_components (autorate) and _create_steering_components (steering)
    - The steering call passes a non-None source_ip sourced from the primary WAN config (grep shows source_ip plumbed at the steering call; not literal None)
    - Neither call site contains a backend-type branch (no `if backend == "fping"` at the call sites — the factory owns that)
    - No edit to any PROTECTED body: `git diff` shows RTTMeasurement.*, BackgroundRTTThread._run/_ping_with_persistent_pool, and WANController.measure_rtt unchanged
    - `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py tests/test_rtt_backend_factory.py -q` exits 0
    - ruff + mypy clean on both edited files
  </acceptance_criteria>
  <done>Both consumers construct their RTT backend through the single factory with zero backend-type branching; steering carries a correct per-WAN source_ip; the live autorate path is behavior-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the additive fallback signal through the wan_controller producer into /health</name>
  <files>src/wanctl/wan_controller.py, src/wanctl/health_check.py</files>
  <read_first>
    - src/wanctl/wan_controller.py (the producer "measurement" dict at :4487-4501 — where health_data[measurement] is assembled; this is where the additive keys must be WRITTEN. measure_rtt :1137 is PROTECTED — do not touch it)
    - src/wanctl/health_check.py (_build_measurement_section :454; the three FROZEN fields at :516-518; the Phase 186 additive-key precedent that already adds state/successful_count/stale)
    - src/wanctl/rtt_backend_factory.py (the fallback_count accessor + backend_active/fell_back exposed in Plan 02 — the source values)
    - tests/test_health_check.py (test_measurement_byte_preserved from Plan 01 — must stay GREEN; test_measurement_section_present_with_live_snapshot :1916 for the producer->reflector flow)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Open Question 3 — the producer must write the keys before _build_measurement_section can reflect them; D-03 minimal-signal scope)
    - .planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md (the additive /health section ~line 240, frozen-field warning)
  </read_first>
  <behavior>
    - The wan_controller producer dict (health_data["measurement"]) gains additive keys carrying the factory's fallback signal: backend_active (the backend that actually ran), fell_back (bool), fallback_count (int). _build_measurement_section reflects these additive keys into the /health response.
    - The three frozen fields (available, raw_rtt_ms, staleness_sec) are byte-identical: same keys, same rounding (2dp / 3dp), same computation. test_measurement_byte_preserved stays GREEN.
    - A new assertion proves the additive keys appear in /health when the backend fell back, and that backend_active reflects the active backend.
  </behavior>
  <action>
    In wan_controller.py, extend ONLY the producer "measurement" dict at :4487
    (additive keys, do not alter the existing raw_rtt_ms/staleness_sec/host keys) so
    it writes backend_active, fell_back, and fallback_count sourced from the factory's
    exposed fallback signal (the WANController must hold a reference to those values
    from when build_rtt_backend constructed its backend — wire that reference through
    construction, not through measure_rtt which is PROTECTED). In health_check.py,
    extend _build_measurement_section (:454) to reflect the three additive keys in the
    returned dict, ADDING beside available/raw_rtt_ms/staleness_sec exactly as the
    Phase 186 precedent added state/successful_count/stale — do NOT mutate the three
    frozen fields' keys, values, or rounding. Use planner-discretion key names that
    are additive and 244-forward-consistent (backend_active, fell_back, fallback_count
    per D-03a). Add an assertion in tests/test_health_check.py that /health exposes
    backend_active/fell_back/fallback_count and that the three frozen fields are
    unchanged (extend or add beside the byte_preserved test). Run ruff/mypy.
  </action>
  <verify>
    <automated>grep -q 'fell_back\|backend_active' src/wanctl/wan_controller.py && grep -q 'fell_back\|backend_active' src/wanctl/health_check.py && .venv/bin/pytest tests/test_health_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The wan_controller producer "measurement" dict writes backend_active, fell_back, fallback_count (additive)
    - health_check._build_measurement_section reflects those keys; the three frozen fields available/raw_rtt_ms/staleness_sec keep identical keys/values/rounding
    - `.venv/bin/pytest tests/test_health_check.py -k byte_preserved -x -q` exits 0 (still GREEN — the regression holds)
    - `.venv/bin/pytest tests/test_health_check.py -q` exits 0 including a new assertion that the additive fallback keys appear in /health
    - No PROTECTED body edited (measure_rtt unchanged); ruff + mypy clean
  </acceptance_criteria>
  <done>FALL-02 pillar 3 is live: /health carries the additive fallback signal through the producer, and the three frozen fields remain byte-identical (238 mandate held).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller -> /health response | The additive keys are computed in-process from the factory's fallback signal; no external/untrusted input crosses into them. /health is an existing localhost observability endpoint. |
| primary WAN config file -> steering factory call | Steering reads the primary WAN's YAML to source source_ip (D-01a); this file is operator-owned config already trusted by _derive_primary_health_url. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-06 | Information Disclosure | /health leaking source_ip/internal data via the new keys | mitigate | D-03 ships ONLY backend_active (a backend name string), fell_back (bool), fallback_count (int) — NO source_ip, no per-sample attribution (that is Phase 244). The additive keys carry no PII or network-topology secret. |
| T-242-07 | Tampering | mutating the three byte-frozen /health fields | mitigate | Add keys ONLY beside available/raw_rtt_ms/staleness_sec; the standing test_measurement_byte_preserved regression (Plan 01) + the SAFE-17 boundary verifier (Plan 04) both fail closed on any frozen-field drift |
| T-242-08 | Repudiation | additive keys written but never surfaced (silent counter) | mitigate | The producer writes AND _build_measurement_section reflects, with a test asserting the keys reach the /health response — the counter is not write-only |
| T-242-09 | DoS | unbounded fallback_count growth | accept | fallback_count is a small in-process integer incremented once per construction-time fallback decision (not per-sample); construction happens at most a few times per process lifetime — no unbounded growth surface |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: additive observable signal only; the new keys are a backend name + bool + small int with no external input and no secret content.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_health_check.py -q` GREEN including the byte-preservation regression and the new additive-key assertion.
- Hot-path slice GREEN: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
- `git diff` shows edits confined to the four allowlisted files + no PROTECTED body change.
- ruff + mypy clean on all four edited files.
</verification>

<success_criteria>
Both consumers construct via the single factory (Criterion 3), steering carries a
correct source_ip for Phase 245 (D-01a), /health exposes the loud fallback signal
(FALL-02 pillar 3), and the three steering-consumed /health fields are byte-identical.
All edits sit inside the SAFE-17 allowlist with no PROTECTED-body drift.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-03-SUMMARY.md` when done
</output>
