---
phase: 242-backend-factory-loud-fallback
plan: 04
type: execute
wave: 4
depends_on: ["242-03"]
files_modified:
  - .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json
autonomous: true
requirements: [SAFE-17]
must_haves:
  truths:
    - "All Phase 242 src/wanctl edits are committed and the src/wanctl tree is clean before the gate runs"
    - "The Phase 242 SAFE-17 boundary verifier passes: zero out-of-allowlist controller-path drift vs v1.52"
    - "Protected byte-frozen bodies remain byte-identical; 241-frozen files show no new 242 diff"
    - "The full suite is green and the hot-path slice is green"
  artifacts:
    - path: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      provides: "fail-closed SAFE-17 boundary evidence with passed:true"
      contains: "\"passed\": true"
  key_links:
    - from: "scripts/phase242-safe17-boundary-check.sh"
      to: ".planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json"
      via: "verifier emits evidence on pass"
      pattern: "safe17-boundary-242"
---

<objective>
The hard gate. Run the Phase 242 SAFE-17 boundary verifier against committed HEAD
and prove zero out-of-allowlist controller-path drift, byte-identical protected
bodies, no new 242 diff to 241-frozen files, and byte-preserved /health fields.
This is Success Criterion 4 — a passing SAFE-17 verifier is a non-negotiable gate.

Purpose: Prove the centralization + observability work stayed inside the narrowed
v1.53 allowlist (factory + both call sites + /health wiring) and touched no
classification logic (state machine, thresholds, EWMA, dwell, deadband, arbitration,
fusion).
Output: passing safe17-boundary-242.json evidence + full-suite + hot-path green.

This plan runs a READ-ONLY git-diff verifier and tests. It mutates no controller
source, no external gear, no CAKE/service state, and no refs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md

<interfaces>
<!-- The gate runs tools authored/edited in prior plans. -->

Verifier: scripts/phase242-safe17-boundary-check.sh --anchor v1.52 (authored Plan 01)
  - fails closed on: dirty src/wanctl tree, out-of-allowlist drift, RTT-seam drift since 239, reflector_scorer drift since 240, new 242 diff to fping_measurement.py/reflector_scorer.py since 241 close (d8179bb6), protected-body drift
  - emits .planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json with "passed": true on success
Anchor: v1.52 = 69f39db1...
Protected-body proof: scripts/phase239-protected-body-diff.py (called by the verifier; unmodified)

Precondition (the verifier's own dirty-tree gate): all src/wanctl edits from Plans 02/03 MUST be committed and the src/wanctl worktree clean — staged/unstaged/untracked src/wanctl changes fail closed before any allowlist proof.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SAFE-17 boundary gate and prove pass + full-suite green</name>
  <files>.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json</files>
  <read_first>
    - scripts/phase242-safe17-boundary-check.sh (the verifier authored in Plan 01 — its four fail-closed gates and the --out confinement)
    - scripts/phase239-protected-body-diff.py (PROTECTED set :21-32 the verifier proves byte-identical)
    - .planning/phases/242-backend-factory-loud-fallback/242-VALIDATION.md (the phase-gate sampling rule: full suite green + SAFE-17 PASS before verify-work)
    - .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json (the passing 241 evidence shape to match)
  </read_first>
  <action>
    Precondition check first: confirm all Plan 02/03 src/wanctl edits are committed
    and `git status --porcelain -- src/wanctl/` is empty (the verifier's dirty-tree
    gate fails closed otherwise; commit any pending src/wanctl edits before running).
    Then run scripts/phase242-safe17-boundary-check.sh --anchor v1.52 and confirm it
    exits 0 and writes safe17-boundary-242.json with "passed": true,
    controller_path_diff_count covering only the five allowlisted files
    (rtt_backend_factory.py, autorate_continuous.py, steering/daemon.py,
    wan_controller.py, health_check.py), all_identical true (protected bodies),
    rtt_seam_unchanged_since_phase239 true, reflector_scorer_unchanged true, and the
    new 241-close-anchor guard showing no new diff to fping_measurement.py /
    reflector_scorer.py. If the verifier fails closed on an out-of-allowlist path,
    do NOT widen the allowlist to pass — investigate whether Plan 03 touched a file
    it should not have (that is a real SAFE-17 violation to fix in the source, not
    the verifier). Then run the full suite `.venv/bin/pytest tests/ -q` and the
    hot-path slice; record both as green. Note: per the v1.53 decisions, known legacy
    full-suite boundary-test failures from earlier phases are out-of-scope historical
    test hygiene — if they appear, record them as pre-existing (not introduced by 242)
    with the SAFE-17/phase-local/hot-path proofs as the keepers, mirroring the 241-04
    precedent. Commit the evidence JSON as the immediate next commit so HEAD^ ==
    evidence.head_commit (the 241-04 durable-freshness convention).
  </action>
  <verify>
    <automated>scripts/phase242-safe17-boundary-check.sh --anchor v1.52 && python3 -c "import json; d=json.load(open('.planning/phases/242-backend-factory-loud-fallback/evidence/safe17-boundary-242.json')); assert d['passed'] is True and d['all_identical'] is True and d['rtt_seam_unchanged_since_phase239'] is True and d['reflector_scorer_unchanged'] is True, d"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain -- src/wanctl/` is empty before the verifier runs
    - `scripts/phase242-safe17-boundary-check.sh --anchor v1.52` exits 0
    - safe17-boundary-242.json has "passed": true, "all_identical": true, "rtt_seam_unchanged_since_phase239": true, "reflector_scorer_unchanged": true
    - disallowed_paths is empty; changed_paths contains only allowlisted files
    - The 241-close-anchor guard shows no new 242 diff to fping_measurement.py / reflector_scorer.py
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py tests/test_health_check.py -q` exits 0; the hot-path slice is green
    - Any full-suite failures present are pre-existing legacy boundary-test hygiene (not introduced by 242), recorded as such
  </acceptance_criteria>
  <done>SAFE-17 Success Criterion 4 is satisfied: the boundary verifier passes with byte-identical protected bodies and zero out-of-allowlist drift; phase-local + hot-path tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs a read-only git-diff verifier + the test suite. No mutation of source, gear, services, or refs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-10 | Tampering | passing the gate by widening the allowlist instead of fixing source | mitigate | Acceptance criteria forbid widening the allowlist to pass; an out-of-allowlist hit is treated as a real SAFE-17 violation to fix in Plan 03 source, not in the verifier |
| T-242-11 | Repudiation | stale/forged evidence | mitigate | Commit the evidence JSON as the immediate next commit so HEAD^ == evidence.head_commit (241-04 durable-freshness convention); the verifier records head_commit + anchor_sha + checked_at |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: read-only verification + tests. The gate itself is the SAFE-17 mitigation for the whole phase.
</threat_model>

<verification>
- `scripts/phase242-safe17-boundary-check.sh --anchor v1.52` exits 0 with passed:true evidence.
- Phase-local + hot-path tests green; full-suite run recorded (legacy failures, if any, flagged pre-existing).
- Evidence committed so HEAD^ == evidence.head_commit.
</verification>

<success_criteria>
The SAFE-17 boundary verifier passes (Criterion 4), protected bodies are byte-identical,
241-frozen files show no new 242 diff, and the loud-fallback + byte-preservation tests
are green — the phase is ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-04-SUMMARY.md` when done
</output>
