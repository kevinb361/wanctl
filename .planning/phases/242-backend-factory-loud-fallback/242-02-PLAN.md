---
phase: 242-backend-factory-loud-fallback
plan: 02
type: execute
wave: 2
depends_on: ["242-01"]
files_modified:
  - src/wanctl/rtt_backend_factory.py
autonomous: true
requirements: [FALL-01, FALL-02]
must_haves:
  truths:
    - "A single build_rtt_backend() resolves measurement.backend (absent->icmplib) and selects the backend"
    - "When fping is selected but the binary is absent, the factory falls back to icmplib automatically (FALL-01)"
    - "The fallback is loud: WARN-once + an incrementable fallback counter + a fell_back/backend_active signal (FALL-02)"
    - "The factory returns a (backend, thread/builder) bundle so call sites need zero backend-type branching (D-04)"
  artifacts:
    - path: "src/wanctl/rtt_backend_factory.py"
      provides: "build_rtt_backend() — the single RTT-backend construction site"
      contains: "def build_rtt_backend"
      min_lines: 40
  key_links:
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "shutil.which"
      via: "construction-time fping presence probe (D-02)"
      pattern: "which\\(.fping.\\)"
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/rtt_measurement.py"
      via: "icmplib half: RTTMeasurement + BackgroundRTTThread"
      pattern: "RTTMeasurement"
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/fping_measurement.py"
      via: "fping half: FpingMeasurement + FpingThread"
      pattern: "FpingMeasurement"
---

<objective>
Build the factory module — the only genuinely new code in the phase (~40 lines:
resolution + fallback branch + loud signal + bundle return). It selects icmplib
vs fping from measurement.backend, falls back to icmplib LOUDLY at construction
time when fping is selected but absent, and returns a (backend, thread) bundle.
This plan makes the Plan 01 factory unit tests GREEN.

Purpose: Centralize backend construction in one place with zero call-site
branching (D-04), and make fallback observable so a misconfigured backend can
never degrade silently (FALL-02).
Output: src/wanctl/rtt_backend_factory.py with build_rtt_backend() and the
WARN-once + counter helpers; no call-site edits in this plan.

This plan creates a NEW module only — it does not edit any existing src/wanctl
file, so it does NOT touch the controller path beyond adding one allowlisted file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md

<interfaces>
<!-- Concrete constructor shapes the factory composes. Extracted from codebase. -->

icmplib half — src/wanctl/rtt_measurement.py:
  RTTMeasurement(logger, timeout_ping=..., aggregation_strategy=RTTAggregationStrategy.<AVERAGE|MEDIAN>, log_sample_stats=..., source_ip=...)
  BackgroundRTTThread(...)  # built later for autorate (needs pool + hosts_fn — see Pitfall 1 / Open Q1)

fping half — src/wanctl/fping_measurement.py:
  FpingMeasurement(config: dict, logger)   # config keys: source_ip, count, period_ms, loss_fail_threshold, scorer, ...
  FpingThread(measurement, hosts_fn, cadence_sec, shutdown_event, logger)  # raises ValueError if timeout >= cadence (already enforced — do NOT re-validate)

Resolution (Pitfall 5 — NO config.backend accessor exists):
  requested = (config.data.get("measurement", {}) or {}).get("backend", "icmplib")
  MEASUREMENT_BACKENDS = ("icmplib","fping")  # src/wanctl/check_config_validators.py:30

WARN-once idiom (FALL-02 pillar 1) — src/wanctl/fping_measurement.py:64,265-267:
  flag-guarded WARN via a _first_failure_logged-style boolean; WARN level (not DEBUG — Pitfall 3).
  Factory is a module-level function: use a module-scoped set/dict keyed by WAN name (or a small counter object).

Seam — src/wanctl/rtt_backend.py: RttBackend Protocol (:20), RttSample (:37) carries backend/source_ip. No new runtime type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement build_rtt_backend() with resolution + loud fallback + bundle return</name>
  <files>src/wanctl/rtt_backend_factory.py</files>
  <read_first>
    - src/wanctl/rtt_backend.py (RttBackend Protocol :20, RttSample :37 — the seam the factory returns against; do NOT add the factory here, it is byte-frozen/shape-checked per RESEARCH anti-pattern)
    - src/wanctl/rtt_measurement.py (RTTMeasurement.__init__ :139 source_ip param; BackgroundRTTThread :412 — the icmplib half + fallback target; PROTECTED bodies, construct never modify)
    - src/wanctl/fping_measurement.py (FpingMeasurement :47 config dict shape; FpingThread :281 timeout<cadence assert at :299; _first_failure_logged WARN idiom :64,:265 — copy this exact idiom)
    - src/wanctl/autorate_continuous.py (_create_wan_components :120 — the exact RTTMeasurement(...) call at :145 to copy; tuple-return idiom at :152)
    - src/wanctl/check_config_validators.py (MEASUREMENT_BACKENDS :30; validate_measurement_backend :960 absent->icmplib rule to match)
    - src/wanctl/irtt_measurement.py (second WARN-once precedent :71-75)
    - tests/test_rtt_backend_factory.py (the contract this implements — written in Plan 01)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Pitfall 1 / Open Question 1 — the autorate thread is built later in wan_controller.start_background_rtt; decide the return shape accordingly)
  </read_first>
  <behavior>
    - Resolves requested backend via (config.data.get("measurement",{}) or {}).get("backend","icmplib"); validates against MEASUREMENT_BACKENDS; an absent key resolves to icmplib with no warn and fell_back False (test_default_icmplib).
    - requested == "fping" and shutil.which("fping") is None -> WARN-once at WARNING level, increments the fallback counter, builds the icmplib RTTMeasurement + its driver thread (or thread builder), returns fell_back True and backend_active "icmplib" (test_fallback_is_loud, test_fallback_builds_icmplib).
    - requested == "fping" and fping present -> builds FpingMeasurement + FpingThread, fell_back False, no warn (test_fping_selected_builds_fping).
    - The return bundle exposes backend, the thread/builder, and the fallback signal (backend_active, fell_back, fallback_count) so call sites do zero backend-type branching.
  </behavior>
  <action>
    Create the NEW module src/wanctl/rtt_backend_factory.py (a new file — do NOT
    add build_rtt_backend inside rtt_backend.py, which is byte-frozen and import-
    light per RESEARCH anti-pattern). Implement build_rtt_backend(config, source_ip,
    shutdown_event, logger, ...) following the Plan 01 test contract:
    (1) Resolve requested = (config.data.get("measurement", {}) or {}).get("backend",
        "icmplib"); reuse MEASUREMENT_BACKENDS for the valid set; absent -> "icmplib".
    (2) Fallback branch (D-02, construction-time only): if requested == "fping" and
        shutil.which("fping") is None, emit a WARN-once (module-scoped flag/set keyed
        by WAN name, mirroring _first_failure_logged — WARNING level per Pitfall 3),
        increment a module-scoped fallback counter, set active="icmplib",
        fell_back=True. Do NOT build any runtime hot-swap / demotion watcher (D-02a,
        rejected). Runtime fping burst failure stays 241's FpingThread job.
    (3) Build the icmplib half by mirroring the exact RTTMeasurement(...) call from
        autorate_continuous.py:145 (pass source_ip through — this is the D-01a binding
        mechanism). Build the fping half as FpingMeasurement(config-dict, logger) +
        FpingThread; FpingThread already enforces timeout<cadence — do not re-validate.
    (4) Return the (backend, thread-or-builder) bundle plus the fallback signal
        (backend_active, fell_back, fallback_count). Resolve the autorate thread-
        timing wrinkle (Pitfall 1: BackgroundRTTThread needs pool + hosts_fn that
        only exist later in wan_controller.start_background_rtt) by returning a
        thread-BUILDER closure/params for the icmplib path rather than a fully-built
        BackgroundRTTThread, so the call sites do not need to construct the thread
        before the pool/hosts_fn exist. Document this decision in a module comment.
        Provide a way to read the current fallback_count for the /health producer
        (a module-level accessor or a field on the returned bundle).
    Keep the module small and explicit with decision-rationale comments matching the
    rtt_backend.py docstring tone.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_rtt_backend_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - build_rtt_backend is defined in src/wanctl/rtt_backend_factory.py (a new module, NOT inside rtt_backend.py)
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fallback_is_loud -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fallback_builds_icmplib -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fping_selected_builds_fping -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_default_icmplib -x` exits 0
    - The fallback uses shutil.which("fping") (construction-time only) and a WARN-once flag at WARNING level; no runtime hot-swap/demotion watcher exists
    - The fallback counter is readable for the /health producer (Plan 03 consumes it)
    - No existing src/wanctl file is modified by this plan
  </acceptance_criteria>
  <done>build_rtt_backend() exists as a new module, the four FALL-01/FALL-02 factory tests are GREEN, and the fallback signal is exposed for the /health producer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config (YAML, already validated) -> factory | measurement.backend is already enum-validated against MEASUREMENT_BACKENDS in Phase 240; the factory must resolve it identically (absent->icmplib) and not trust arbitrary strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-03 | Tampering | backend resolution from config string | mitigate | Resolve only against MEASUREMENT_BACKENDS ("icmplib","fping"); a typo'd/unknown backend must not silently route to an unexpected path — match the Phase 240 validator's enum + absent->icmplib default exactly |
| T-242-04 | Repudiation/observability | silent degradation hiding a misconfigured backend | mitigate | FALL-02 itself is the mitigation: WARN-once at WARNING level (not DEBUG, Pitfall 3) + an in-process counter + the /health fell_back/fallback_count signal (Plan 03). A test asserts all three pillars fire, not just the construction. |
| T-242-05 | Tampering | fping subprocess argv injection | accept | The factory constructs the FpingMeasurement object only; Phase 241 already builds the fping argv from validated hosts. No new exec surface is added here. (RESEARCH Security Domain, VERIFIED.) |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs — the factory composes existing in-tree components (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: the factory constructs internal Python objects from already-validated config. No auth, session, access-control, crypto, or new network/input surface (RESEARCH Security Domain).
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_rtt_backend_factory.py -q` is fully GREEN.
- `.venv/bin/ruff check src/wanctl/rtt_backend_factory.py` and `.venv/bin/mypy src/wanctl/rtt_backend_factory.py` pass.
- No diff to any existing src/wanctl file (`git diff --name-only -- src/wanctl/` shows only the new module).
</verification>

<success_criteria>
build_rtt_backend() is the single construction site (ready for call-site wiring in
Plan 03), FALL-01 (auto fallback) and FALL-02 (loud, never silent) are proven by
GREEN unit tests, and the module adds zero diff to existing controller-path files.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-02-SUMMARY.md` when done
</output>
