---
phase: 242-backend-factory-loud-fallback
plan: 02
type: execute
wave: 2
depends_on: ["242-01"]
files_modified:
  - src/wanctl/rtt_backend_factory.py
autonomous: true
requirements: [FALL-01, FALL-02]
must_haves:
  truths:
    - "A single build_rtt_backend() resolves measurement.backend (absent->icmplib) and returns an RttBackendHandle dataclass (backend, controller_measurement, backend_active, fell_back, fallback_count, make_thread) — NOT a raw thread (D-04 / D-04a chosen shape)"
    - "RttBackendHandle.controller_measurement is ALWAYS an icmplib RTTMeasurement (never a FpingMeasurement) regardless of which backend the background thread runs — it is the object WANController.rtt_measurement binds to for the controller helper paths (ping_host / ping_hosts_with_results / maybe_probe). When backend_active is 'icmplib' it MAY be the same object as handle.backend; when backend_active is 'fping' it is a SEPARATE icmplib RTTMeasurement while handle.backend is the FpingMeasurement"
    - "make_thread(hosts_fn, shutdown_event, *, pool, cadence_sec) is the DEFERRED thread builder: the thread is built only when pool+hosts_fn exist, never at build_rtt_backend() time (resolves Pitfall 1 timing)"
    - "make_thread() output satisfies the COMMON THREAD PROTOCOL measure_rtt() requires: start/stop/get_latest/get_cycle_status/get_profile_stats/cadence_sec; an fping ADAPTER (inside this module) wraps FpingThread so get_cycle_status() returns None and get_latest() returns the RTTSnapshot shape via RttSample.to_snapshot() — a raw FpingThread is NEVER returned"
    - "When fping is selected but the binary is absent, the factory falls back to icmplib automatically (FALL-01); when fping timeout >= cadence, make_thread falls back LOUDLY to icmplib instead of propagating FpingThread's ValueError (decided disposition). On the timeout fallback the handle is kept COHERENT: handle.backend is replaced with the icmplib RTTMeasurement so backend never advertises a FpingMeasurement while backend_active == 'icmplib'"
    - "The fallback is loud and PER-WAN: WARN-once keyed by wan_key + a per-handle fallback_count + the fell_back/backend_active fields (FALL-02) — fallback state is NOT process-global"
    - "SCOPE (242 explicit): fping governs ONLY the background thread that feeds measure_rtt() via get_latest(). It does NOT drive the controller helper paths — ping_host (gateway check), ping_hosts_with_results (blocking fallback), and maybe_probe (reflector scorer) ALWAYS run on the icmplib controller_measurement. fping-driven loss-aware scoring on the controller/scorer path is a future-phase (245 A/B) concern, NOT 242 — this preserves today's behavior on those paths (CONTEXT 'no behavior change')"
  artifacts:
    - path: "src/wanctl/rtt_backend_factory.py"
      provides: "build_rtt_backend() + RttBackendHandle (with controller_measurement) + the fping adapter (RttDriverThread protocol) — the single RTT-backend construction site"
      contains: "def build_rtt_backend"
      min_lines: 60
  key_links:
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "shutil.which"
      via: "construction-time fping presence probe (D-02); SINGLE probe owned by the factory (no double which())"
      pattern: "which\\(.fping.\\)"
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/rtt_measurement.py"
      via: "icmplib half + controller_measurement: RTTMeasurement (always) + BackgroundRTTThread (built in make_thread)"
      pattern: "RTTMeasurement"
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/fping_measurement.py"
      via: "fping half: FpingMeasurement + FpingThread wrapped by the in-module adapter"
      pattern: "FpingThread"
    - from: "src/wanctl/rtt_backend_factory.py"
      to: "src/wanctl/rtt_backend.py"
      via: "fping adapter get_latest() returns RttSample.to_snapshot() (RTTSnapshot shape)"
      pattern: "to_snapshot"
---

<objective>
Build the factory module — the only genuinely new code in the phase. It selects
icmplib vs fping from measurement.backend, falls back to icmplib LOUDLY at
construction time when fping is selected but absent, and returns an RttBackendHandle
(NOT a raw thread). The handle exposes a DEFERRED make_thread() builder that the
autorate caller invokes later (when the pool + hosts_fn exist) and whose output
satisfies the COMMON THREAD PROTOCOL WANController.measure_rtt() requires. For the
fping path, make_thread builds an in-module ADAPTER wrapping FpingThread so a raw
FpingThread is never assigned to _rtt_thread.

The handle ALSO carries a separate controller_measurement field that is ALWAYS an
icmplib RTTMeasurement — this is the object WANController.rtt_measurement binds to so
the controller's helper paths (ping_host gateway check, ping_hosts_with_results
blocking fallback, maybe_probe reflector scorer — none of which exist on
FpingMeasurement) keep full RTTMeasurement semantics even when the background RTT
thread runs fping. This plan makes the Plan 01 factory unit tests — including the
thread-protocol, the measure_rtt-no-AttributeError, and the
controller_measurement-is-RTTMeasurement regressions — GREEN.

Purpose: Centralize backend construction in one place with zero call-site branching
(D-04), make fallback observable per-WAN (FALL-02), and CLOSE both review HIGHs: (1)
the factory hands the call sites a protocol-satisfying thread handle/adapter, not a
raw FpingThread that would AttributeError in measure_rtt (cycle-1 HIGH, already
closed); and (2) the factory hands a guaranteed-icmplib controller_measurement so
WANController.rtt_measurement never becomes a FpingMeasurement and the three live
controller helper paths never AttributeError under fping mode (cycle-2 HIGH).
Output: src/wanctl/rtt_backend_factory.py with build_rtt_backend(), RttBackendHandle
(with controller_measurement), the fping adapter, the deferred make_thread builder,
and the WARN-once + per-WAN counter helpers; no call-site edits in this plan.

This plan creates a NEW module only — it does not edit any existing src/wanctl file,
so it does NOT touch the controller path beyond adding one allowlisted file. The
fping adapter lives HERE (inside rtt_backend_factory.py, already allowlisted), NOT
in the 241-frozen fping_measurement.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/242-backend-factory-loud-fallback/242-CONTEXT.md
@.planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md
@.planning/phases/242-backend-factory-loud-fallback/242-PATTERNS.md

<interfaces>
<!-- Concrete shapes the factory composes + the protocol it must satisfy. Extracted from codebase. -->

CHOSEN RETURN SHAPE (D-04a — review-endorsed RttBackendHandle dataclass):
  @dataclass
  class RttBackendHandle:
      backend: <RttBackend Protocol object — RTTMeasurement | FpingMeasurement>
      controller_measurement: RTTMeasurement   # ALWAYS icmplib; bound to WANController.rtt_measurement
      backend_active: str            # "icmplib" | "fping" — what actually ran after fallback
      fell_back: bool                # True iff fping requested but factory fell back
      fallback_count: int            # PER-HANDLE (per-WAN) counter, NOT process-global
      def make_thread(self, hosts_fn, shutdown_event, *, pool=None, cadence_sec) -> <RttDriverThread>
  build_rtt_backend(config, source_ip, logger, *, wan_key) -> RttBackendHandle
  NOTE: shutdown_event / pool / hosts_fn are NOT params of build_rtt_backend — they
  do not exist at _create_wan_components time (Pitfall 1). They are make_thread params.

WHY controller_measurement EXISTS (cycle-2 HIGH — verified against live code):
  WANController stores its constructor rtt_measurement at wan_controller.py:338 and
  consumes it on THREE controller helper paths beyond thread construction:
    - self.rtt_measurement.ping_hosts_with_results(...)  # wan_controller.py:1269 (blocking fallback)
    - self.rtt_measurement.ping_host(gateway_ip, count=1) # wan_controller.py:1474 (gateway connectivity check)
    - self._reflector_scorer.maybe_probe(now, self.rtt_measurement)  # wan_controller.py:3127
      (ReflectorScorer.maybe_probe(self, now, rtt_measurement: RTTMeasurement) at reflector_scorer.py:277)
  FpingMeasurement (fping_measurement.py) defines ONLY probe()/is_available()/record_results()
  — it has NEITHER ping_host NOR ping_hosts_with_results NOR the maybe_probe contract.
  RTTMeasurement (rtt_measurement.py) defines ping_host (:174), ping_hosts_with_results (:287),
  probe (:325), ping_hosts_concurrent (:361). Therefore controller_measurement MUST be an
  icmplib RTTMeasurement so those three paths never AttributeError, even when the background
  thread runs fping. The fping/icmplib choice governs ONLY the background thread (what feeds
  measure_rtt() via get_latest()), NOT the controller helper paths.

CHOSEN BINDING (icmplib case): when backend_active == "icmplib", controller_measurement
  MAY be the SAME object as handle.backend (both are the one RTTMeasurement) — avoid building
  two icmplib pingers needlessly. When backend_active == "fping", controller_measurement is a
  SEPARATE icmplib RTTMeasurement while handle.backend is the FpingMeasurement.

CONTROLLER_MEASUREMENT CONSTRUCTION (mirror autorate_continuous.py:145 exactly):
  RTTMeasurement(logger, timeout_ping=config.timeout_ping,
                 aggregation_strategy=RTTAggregationStrategy.AVERAGE,
                 log_sample_stats=True, source_ip=source_ip)
  i.e. identical params to today's autorate call so the helper paths' behavior is unchanged.

COMMON THREAD PROTOCOL (RttDriverThread — what measure_rtt() consumes; measure_rtt is PROTECTED):
  start() ; stop() ; cadence_sec (attr)
  get_latest() -> RTTSnapshot | None
      measure_rtt reads: .timestamp, .per_host_results, .rtt_ms, .active_hosts, .successful_hosts
  get_cycle_status() -> RTTCycleStatus | None
      measure_rtt does `if not isinstance(cycle_status, RTTCycleStatus): cycle_status = None`
      and `if not (cycle_status and self._should_skip_scorer_update(cycle_status)):` — so
      returning None is SAFE and tolerated. (wan_controller.py:1152-1154,1171)
  get_profile_stats() -> dict

icmplib half — src/wanctl/rtt_measurement.py:
  RTTMeasurement(logger, timeout_ping=..., aggregation_strategy=RTTAggregationStrategy.<AVERAGE|MEDIAN>, log_sample_stats=..., source_ip=...)  # :139
  BackgroundRTTThread(rtt_measurement, hosts_fn, shutdown_event, logger, pool, cadence_sec=...)  # :412,:433
      Already satisfies RttDriverThread verbatim (get_latest->RTTSnapshot, get_cycle_status->RTTCycleStatus, :463/:472).

fping half — src/wanctl/fping_measurement.py (241-FROZEN — construct, do NOT edit):
  FpingMeasurement(config: dict, logger)   # :47 ; config keys: source_ip, count, period_ms, loss_fail_threshold, scorer, ...
      defines ONLY probe()/is_available()/record_results() — NO ping_host, NO ping_hosts_with_results
  FpingThread(measurement, hosts_fn, cadence_sec, shutdown_event, logger)  # :291
      raises ValueError if measurement._timeout >= cadence_sec (:299)
      get_latest() -> RttSample | None (:316)  — NOT RTTSnapshot, NO get_cycle_status()

ADAPTER target (lives in THIS module): RttSample.to_snapshot() -> RTTSnapshot (src/wanctl/rtt_backend.py)
  The fping adapter delegates start/stop/get_profile_stats/cadence_sec to the wrapped
  FpingThread, returns None from get_cycle_status(), and returns
  wrapped.get_latest().to_snapshot() (or None) from get_latest().

Resolution (Pitfall 5 — NO config.backend accessor exists):
  requested = (config.data.get("measurement", {}) or {}).get("backend", "icmplib")
  MEASUREMENT_BACKENDS = ("icmplib","fping")  # src/wanctl/check_config_validators.py:30

SINGLE shutil.which PROBE (review MEDIUM — double-probe ambiguity): the factory owns the
  ONE fping-presence probe. Resolve the binary path/presence ONCE in build_rtt_backend via
  shutil.which("fping") and pass the resolved value into FpingMeasurement so it does not
  re-probe (or, if FpingMeasurement re-probes internally, document that the factory's probe
  is authoritative for fell_back/backend_active and the test patches BOTH probe sites). The
  "no WARN on fping-present" assertion must be deterministic — a single authoritative probe
  is the preferred way to make it so.

WARN-once idiom (FALL-02 pillar 1) — src/wanctl/fping_measurement.py:64,265-267:
  flag-guarded WARN at WARNING level (NOT DEBUG — Pitfall 3). Because the factory is a
  module function, scope the once-flag PER WAN (a module-scoped set keyed by wan_key)
  so a fallback on the second WAN is not hidden by the first WAN's WARN (review MEDIUM).

INVALID DIRECT INPUT (review LOW, defense-in-depth): validators normally catch a bad
  measurement.backend upstream, but build_rtt_backend should RAISE (ValueError) on an
  unknown backend string rather than silently defaulting to icmplib — an absent key still
  resolves to icmplib (the documented default), but a present-but-invalid value is an error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RttBackendHandle (with controller_measurement) + the fping adapter + build_rtt_backend() with resolution, loud per-WAN fallback, coherent timeout fallback, and deferred make_thread</name>
  <files>src/wanctl/rtt_backend_factory.py</files>
  <read_first>
    - src/wanctl/rtt_backend.py (RttBackend Protocol :20, RttSample :37 + RttSample.to_snapshot returning RTTSnapshot — the adapter's get_latest translation; do NOT add the factory here, it is byte-frozen/shape-checked per RESEARCH anti-pattern)
    - src/wanctl/rtt_measurement.py (RTTMeasurement.__init__ :139 — logger, timeout_ping, aggregation_strategy, log_sample_stats, source_ip; ping_host :174, ping_hosts_with_results :287 — the controller-helper methods controller_measurement MUST expose; BackgroundRTTThread :412 constructor signature :433 — pool+hosts_fn+cadence_sec; it already exposes get_latest/get_cycle_status/get_profile_stats/cadence_sec/start/stop; PROTECTED bodies, construct never modify; RTTSnapshot :93, RTTCycleStatus :109)
    - src/wanctl/fping_measurement.py (FpingMeasurement :47 config dict shape — defines ONLY probe/is_available/record_results, NO ping_host/ping_hosts_with_results; FpingThread :291 constructor + timeout<cadence ValueError at :299; get_latest :316 returns RttSample; NO get_cycle_status; any internal shutil.which probe in __init__; _first_failure_logged WARN idiom :64,:265 — copy the level/style; 241-FROZEN — do NOT edit this file, wrap it from the factory)
    - src/wanctl/wan_controller.py (measure_rtt :1137-1235 PROTECTED — read ONLY to confirm exactly what the produced thread must expose; __init__ :327-339 storing self.rtt_measurement at :338; the THREE controller-helper consumers self.rtt_measurement.ping_hosts_with_results :1269, self.rtt_measurement.ping_host :1474, self._reflector_scorer.maybe_probe(now, self.rtt_measurement) :3127 — the call sites controller_measurement must satisfy; start_background_rtt :1080-1103 builds BackgroundRTTThread today — the build point make_thread plugs into)
    - src/wanctl/reflector_scorer.py (maybe_probe :277 — `def maybe_probe(self, now, rtt_measurement: RTTMeasurement)`; confirms the icmplib RTTMeasurement contract the scorer needs)
    - src/wanctl/autorate_continuous.py (_create_wan_components :120 — the exact RTTMeasurement(...) call at :145 to mirror for BOTH controller_measurement AND the make_thread icmplib branch; tuple-return idiom at :152)
    - src/wanctl/check_config_validators.py (MEASUREMENT_BACKENDS :30; validate_measurement_backend :960 absent->icmplib rule to match)
    - src/wanctl/irtt_measurement.py (second WARN-once precedent :71-75)
    - tests/test_rtt_backend_factory.py (the contract this implements — written in Plan 01, including test_thread_protocol_contract, test_fping_selected_measure_rtt_no_attributeerror, test_fping_timeout_ge_cadence_falls_back, and test_controller_measurement_is_rttmeasurement)
    - .planning/phases/242-backend-factory-loud-fallback/242-RESEARCH.md (Pitfall 1 deferred-build; Pitfall 3 WARN level; Pitfall 5 no config.backend accessor; Open Questions RESOLVED)
  </read_first>
  <behavior>
    - build_rtt_backend resolves requested = (config.data.get("measurement",{}) or {}).get("backend","icmplib"); an absent key resolves to icmplib with no warn and fell_back False (test_default_icmplib); a present-but-unknown backend string RAISES ValueError (review LOW — defense-in-depth, not a silent default). It returns an RttBackendHandle (backend, controller_measurement, backend_active, fell_back, fallback_count, make_thread) — never a raw thread.
    - controller_measurement is ALWAYS an icmplib RTTMeasurement constructed with the autorate_continuous.py:145 params (timeout_ping, AVERAGE, log_sample_stats=True, source_ip). When backend_active == "icmplib", controller_measurement and handle.backend MAY be the same RTTMeasurement object (do not build two icmplib pingers). When backend_active == "fping", controller_measurement is a SEPARATE icmplib RTTMeasurement while handle.backend is the FpingMeasurement (test_controller_measurement_is_rttmeasurement).
    - requested == "fping" and shutil.which("fping") is None -> WARN-once at WARNING level keyed by wan_key (PER-WAN, not process-global), increments this handle's fallback_count, builds the icmplib RTTMeasurement as handle.backend (and reuses it as controller_measurement), sets fell_back True, backend_active "icmplib" (test_fallback_is_loud, test_fallback_builds_icmplib).
    - requested == "fping" and fping present -> builds FpingMeasurement as handle.backend AND a separate icmplib RTTMeasurement as controller_measurement; fell_back False, backend_active "fping", no warn (test_fping_selected_builds_fping). The fping-presence probe is the factory's SINGLE authoritative shutil.which call (no double probe) — the resolved path is passed to FpingMeasurement or FpingMeasurement's internal probe is documented as subordinate.
    - test_default_icmplib: measurement.backend key absent -> icmplib backend; controller_measurement is the same RTTMeasurement; fell_back False; no warn.
    - handle.make_thread(hosts_fn, shutdown_event, *, pool, cadence_sec) is DEFERRED: it builds the actual driver thread only when called. For backend_active "icmplib" it builds BackgroundRTTThread(rtt_measurement=handle.backend, ...). For backend_active "fping" it builds FpingThread(handle.backend, ...) wrapped in the in-module adapter (get_cycle_status()->None, get_latest()->RttSample.to_snapshot()).
    - make_thread timeout-vs-cadence DISPOSITION (decided = loud fallback, NOT crash, AND COHERENT): if building the FpingThread would raise ValueError (timeout >= cadence), make_thread catches it, emits a WARN-once, increments fallback_count, sets fell_back True / backend_active "icmplib", AND replaces handle.backend with the icmplib RTTMeasurement (so backend never advertises a FpingMeasurement while backend_active == "icmplib" — review MEDIUM coherence), then builds the icmplib BackgroundRTTThread. controller_measurement is already icmplib so the controller paths are safe regardless. A warned-but-valid config never becomes a startup crash (test_fping_timeout_ge_cadence_falls_back).
    - The produced thread (either branch) drives WANController.measure_rtt() without AttributeError (test_fping_selected_measure_rtt_no_attributeerror), and a WANController built with handle.controller_measurement runs ping_host / ping_hosts_with_results / maybe_probe without AttributeError under a fping-selected config (test_controller_measurement_is_rttmeasurement / the no-AttributeError controller-paths test in Plan 01).
    - SCOPE (242 explicit, review MEDIUM on fping scorer/loss semantics): fping does NOT drive the reflector scorer or the controller helper paths in 242 — those run on the icmplib controller_measurement via maybe_probe(now, self.rtt_measurement). fping feeds ONLY the background thread's measure_rtt() snapshot. fping's loss-aware scoring on the controller path is a future-phase (245 A/B) concern. Document this as a deliberate scope decision in module comments.
  </behavior>
  <action>
    Create the NEW module src/wanctl/rtt_backend_factory.py (a new file — do NOT add
    build_rtt_backend inside rtt_backend.py, which is byte-frozen and import-light per
    RESEARCH anti-pattern). Implement:
    (1) An RttBackendHandle dataclass with fields backend, controller_measurement,
        backend_active, fell_back, fallback_count and a make_thread(self, hosts_fn,
        shutdown_event, *, pool=None, cadence_sec) method. fallback_count is per-handle
        (per-WAN), NOT a module global. controller_measurement is typed RTTMeasurement.
    (2) An in-module fping ADAPTER class (e.g. _FpingDriverThread) wrapping a FpingThread:
        get_cycle_status() returns None (measure_rtt tolerates None); get_latest() returns
        self._fping_thread.get_latest().to_snapshot() (or None if no sample yet);
        start/stop/get_profile_stats delegate to the wrapped FpingThread; cadence_sec
        returns the wrapped thread's cadence_sec. This keeps fping_measurement.py
        byte-frozen (241 close) and keeps the adapter inside the SAFE-17 allowlist.
    (3) A small private helper (e.g. _build_controller_measurement(source_ip, logger,
        timeout_ping)) that constructs the icmplib RTTMeasurement mirroring
        autorate_continuous.py:145 (timeout_ping, RTTAggregationStrategy.AVERAGE,
        log_sample_stats=True, source_ip=source_ip). Used for controller_measurement AND
        for the icmplib backend so the icmplib case can share ONE object.
    (4) build_rtt_backend(config, source_ip, logger, *, wan_key): resolve requested
        (absent -> "icmplib"); validate against MEASUREMENT_BACKENDS — an unknown
        present value RAISES ValueError (review LOW), it does NOT silently default.
        Probe fping presence ONCE via shutil.which("fping") (the factory's single
        authoritative probe; pass the resolved path to FpingMeasurement so it does not
        re-probe, or document the factory probe as authoritative). icmplib case
        (requested "icmplib" OR fallback): build ONE RTTMeasurement and set BOTH
        handle.backend and handle.controller_measurement to it; backend_active "icmplib".
        fping-present case: build FpingMeasurement as handle.backend AND a SEPARATE
        icmplib RTTMeasurement via _build_controller_measurement as handle.controller_measurement;
        backend_active "fping", fell_back False. Fallback branch (D-02, construction-time
        only): if requested == "fping" and fping absent, emit WARN-once keyed PER wan_key
        (module-scoped set/dict keyed by wan_key, WARNING level per Pitfall 3), set
        fell_back True, backend_active "icmplib", fallback_count 1; build the icmplib
        RTTMeasurement as BOTH backend and controller_measurement (the D-01a source_ip
        binding flows in). Do NOT build any runtime hot-swap / demotion watcher (D-02a,
        rejected); runtime fping burst failure stays 241's FpingThread job.
    (5) make_thread: for the icmplib path build BackgroundRTTThread with the passed
        pool/hosts_fn/shutdown_event/cadence_sec against handle.backend. For the fping
        path, construct FpingThread(handle.backend, hosts_fn, cadence_sec, shutdown_event,
        logger) inside a try/except ValueError: on ValueError (timeout >= cadence) WARN-once,
        increment fallback_count, flip the handle to backend_active "icmplib"/fell_back True,
        AND replace handle.backend with the icmplib RTTMeasurement (keep the handle coherent —
        do NOT leave a FpingMeasurement on .backend while backend_active == "icmplib";
        controller_measurement is already icmplib), then build an icmplib BackgroundRTTThread.
        On success, wrap the FpingThread in the in-module adapter and return it. Document the
        deferred-build rationale (Pitfall 1), the timeout-fallback coherence, and the 242
        scope decision (fping feeds only the background thread, never the controller helper
        paths / reflector scorer — those always run on controller_measurement) in module comments.
    (6) Expose this handle's fallback signal (backend_active, fell_back, fallback_count)
        as plain fields on the handle so the /health producer (Plan 03) reads PER-WAN
        state off the controller's own handle reference — NOT a module-level global
        accessor (review MEDIUM: per-WAN /health attribution).
    Keep the module small and explicit with decision-rationale comments matching the
    rtt_backend.py docstring tone. Run ruff + mypy after.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_rtt_backend_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - build_rtt_backend + RttBackendHandle (with a controller_measurement field) + the fping adapter are defined in src/wanctl/rtt_backend_factory.py (a new module, NOT inside rtt_backend.py)
    - RttBackendHandle.controller_measurement is ALWAYS an icmplib RTTMeasurement instance (NEVER a FpingMeasurement) in all four cases (icmplib, fping-present, fping-absent fallback, timeout fallback); when backend_active == "icmplib" it MAY be the same object as handle.backend, when "fping" it is a separate RTTMeasurement
    - build_rtt_backend returns an RttBackendHandle (never a raw thread); make_thread is the deferred builder and takes pool/hosts_fn/shutdown_event/cadence_sec (NOT build_rtt_backend)
    - The fping path's make_thread returns the in-module ADAPTER (get_cycle_status()->None, get_latest()->RttSample.to_snapshot()), never a raw FpingThread
    - On the timeout-vs-cadence fallback, handle.backend is replaced with the icmplib RTTMeasurement (NOT left as a FpingMeasurement while backend_active == "icmplib") — the handle stays coherent
    - A present-but-unknown measurement.backend string raises ValueError; an absent key resolves to icmplib silently (no warn)
    - The factory owns a SINGLE shutil.which("fping") probe (no double-probe ambiguity); the no-WARN fping-present assertion is deterministic
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fallback_is_loud -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_thread_protocol_contract -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fping_selected_measure_rtt_no_attributeerror -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_controller_measurement_is_rttmeasurement -x` exits 0
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py::test_fping_timeout_ge_cadence_falls_back -x` exits 0 (loud fallback, not a propagated ValueError; handle.backend coherent icmplib after)
    - `.venv/bin/pytest tests/test_rtt_backend_factory.py -q` is fully GREEN
    - WARN-once is keyed PER wan_key and fallback_count is per-handle (no process-global fallback state)
    - Module comments document the 242 scope decision: fping feeds ONLY the background thread / measure_rtt snapshot; the controller helper paths + reflector scorer always run on controller_measurement (icmplib); fping loss-aware scoring on the controller path is deferred to Phase 245
    - No existing src/wanctl file is modified by this plan (fping_measurement.py stays byte-frozen; the adapter lives in the factory module)
  </acceptance_criteria>
  <done>build_rtt_backend() exists as a new module returning an RttBackendHandle with (a) a deferred protocol-satisfying make_thread (icmplib thread or fping adapter) and (b) a controller_measurement that is ALWAYS an icmplib RTTMeasurement; the FALL-01/FALL-02/protocol/regression/controller_measurement tests are GREEN; the timeout fallback keeps the handle coherent; and the per-WAN fallback signal is exposed on the handle for the /health producer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config (YAML, already validated) -> factory | measurement.backend is already enum-validated against MEASUREMENT_BACKENDS in Phase 240; the factory must resolve it identically (absent->icmplib) and raise on an unknown present value, not trust arbitrary strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-03 | Tampering | backend resolution from config string | mitigate | Resolve only against MEASUREMENT_BACKENDS ("icmplib","fping"); absent->icmplib default; a present-but-unknown backend RAISES ValueError (review LOW defense-in-depth) — it never silently routes to an unexpected path; match the Phase 240 validator's enum exactly |
| T-242-04 | Repudiation/observability | silent degradation hiding a misconfigured backend, OR hiding a fallback on the SECOND WAN | mitigate | FALL-02 itself is the mitigation: WARN-once at WARNING level (not DEBUG, Pitfall 3) keyed PER wan_key + a per-handle counter + the /health fell_back/fallback_count signal (Plan 03). Per-WAN scoping prevents the first WAN's WARN from masking a second-WAN fallback (review MEDIUM). A test asserts all three pillars fire. |
| T-242-13 | Denial of Service | a warned-but-valid fping timeout>=cadence config crashing the daemon at startup | mitigate | DECIDED disposition: make_thread catches FpingThread's ValueError and falls back LOUDLY+COHERENTLY to icmplib (replaces handle.backend with the icmplib RTTMeasurement) instead of propagating — a misconfig degrades observably, never a startup crash. test_fping_timeout_ge_cadence_falls_back proves it. |
| T-242-17 | Tampering | WANController.rtt_measurement bound to a FpingMeasurement -> AttributeError on ping_host/ping_hosts_with_results/maybe_probe (the cycle-2 HIGH failure class) | mitigate | controller_measurement is ALWAYS an icmplib RTTMeasurement on the handle; Plan 03 binds it to the WANController positional rtt_measurement slot; a regression test exercises all three controller helper paths under a fping-selected config without AttributeError |
| T-242-05 | Tampering | fping subprocess argv injection | accept | The factory constructs the FpingMeasurement object only; Phase 241 already builds the fping argv from validated hosts. No new exec surface is added here. (RESEARCH Security Domain, VERIFIED.) |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | No package installs — the factory composes existing in-tree components (RESEARCH Package Legitimacy Audit: N/A) |

No high-severity threats: the factory constructs internal Python objects from already-validated config. No auth, session, access-control, crypto, or new network/input surface (RESEARCH Security Domain).
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_rtt_backend_factory.py -q` is fully GREEN, including test_thread_protocol_contract, test_fping_selected_measure_rtt_no_attributeerror, test_controller_measurement_is_rttmeasurement, and test_fping_timeout_ge_cadence_falls_back.
- `.venv/bin/ruff check src/wanctl/rtt_backend_factory.py` and `.venv/bin/mypy src/wanctl/rtt_backend_factory.py` pass.
- No diff to any existing src/wanctl file (`git diff --name-only -- src/wanctl/` shows only the new module).
</verification>

<success_criteria>
build_rtt_backend() is the single construction site returning an RttBackendHandle
with (a) a deferred, protocol-satisfying make_thread (ready for call-site wiring in
Plan 03) and (b) a controller_measurement that is ALWAYS an icmplib RTTMeasurement,
FALL-01 (auto fallback) and FALL-02 (loud, never silent, PER-WAN) are proven by GREEN
unit tests, the common-thread-protocol + measure_rtt-no-AttributeError +
controller_measurement-is-RTTMeasurement regressions pass (both review HIGHs closed),
the timeout fallback keeps the handle coherent, and the module adds zero diff to
existing controller-path files.
</success_criteria>

<output>
Create `.planning/phases/242-backend-factory-loud-fallback/242-02-SUMMARY.md` when done
</output>
</content>
</invoke>
