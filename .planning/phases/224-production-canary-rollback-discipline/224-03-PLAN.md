---
phase: 224-production-canary-rollback-discipline
plan: 03
type: execute
wave: 2
depends_on:
  - 224-01
  - 224-02
files_modified:
  - .planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/
  - .planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/
  - .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/
  - .planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/
requirements:
  - CANARY-01
  - CANARY-02
autonomous: false
must_haves:
  truths:
    - "Operator has captured a Snapshot A under evidence/snapshot-a/<TS>/ with a complete MANIFEST.md citing pre-deploy /health.version, deployed/repo config equality, AND the pre-deploy git commit SHA."
    - "Operator has signed `.planning/decisions/phase-224-clean-restart-risk-acceptance.md` before the deploy gate, with the signed artifact captured as evidence."
    - "Plan 01 rehearsal-budget.md exists with `pending: false` AND `within_budget: true` (measured staging rehearsal succeeded). Plan 03 hard-blocks if either is missing."
    - "Operator has executed `scripts/deploy.sh <wan> <host> --with-steering` followed by `systemctl restart steering.service` against the production target host AS A BLOCKING HUMAN CHECKPOINT (the single production-touch event of Phase 224 is operator-driven, not automated)."
    - "Post-deploy raw `/health` (from `ssh host curl http://127.0.0.1:9102/health`) shows version alignment with the expected v1.48 release line, status=healthy, and decision/steering/rtt_source sections populate."
    - "Spine probe run immediately post-deploy returns spine.binary_on_off, only_new_connections, spectrum_state_not_written_by_daemon all match=true (or restart-window symptom for binary_on_off / spectrum_state within the first 15 cycles, per D-11)."
  artifacts:
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/"
      provides: "Pre-deploy Snapshot A directory tree (one TS-named subdir)"
      contains: "MANIFEST.md"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/"
      provides: "Pre-deploy /health baseline + spine probe output"
      contains: "health-baseline.redacted.json"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/deploy/"
      provides: "Deploy command log + deploy.sh exit code + restart confirmation"
      contains: "deploy-stdout.redacted.log"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/"
      provides: "Post-deploy raw /health (PRIMARY) + spine probe + canary-check summary (secondary signal)"
      contains: "health.steering.postdeploy.json"
  key_links:
    - from: "evidence/snapshot-a/<TS>/MANIFEST.md"
      to: "evidence/deploy/deploy-stdout.redacted.log"
      via: "deploy log cites snapshot anchor TS"
      pattern: "snapshot-a/[0-9]+T[0-9]+Z"
    - from: "evidence/deploy/"
      to: "evidence/leg-b-postalignment/health.steering.postdeploy.json"
      via: "post-deploy raw /health captured via ssh+curl (gate-eval primary input)"
      pattern: "health\\.steering\\.postdeploy\\.json"
    - from: "evidence/leg-b-postalignment/spine-probe.json"
      to: "scripts/phase224-spine-probe.sh"
      via: "spine probe produced this output"
      pattern: "spine"
---

<objective>
Execute the production deploy of the aligned steering daemon under Snapshot A rollback anchor:
capture Snapshot A, obtain operator sign-off on the risk-acceptance decision artifact, deploy
via the paved path, restart the steering service, and immediately capture post-deploy /health
+ spine probe as Leg B evidence (CANARY-01, CANARY-02).

Purpose: This is the single production-mutation event of Phase 224. Plan 04 then observes,
gates, and decides keep vs rollback. Plan 03 establishes the snapshot anchor and proves the
deploy itself succeeded with a healthy /health endpoint emitting the expected version.

Output: snapshot-a/<TS>/ directory with manifest; leg-a-prealignment baseline; deploy log;
leg-b-postalignment /health + spine probe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/224-production-canary-rollback-discipline/224-CONTEXT.md
@.planning/phases/224-production-canary-rollback-discipline/224-RESEARCH.md
@.planning/phases/224-production-canary-rollback-discipline/224-01-PLAN.md
@.planning/phases/224-production-canary-rollback-discipline/224-02-PLAN.md
@.planning/decisions/phase-224-clean-restart-risk-acceptance.md
@scripts/phase224-snapshot-a.sh
@scripts/phase224-spine-probe.sh
@scripts/canary-check.sh
@scripts/deploy.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator sign-off on clean-restart risk-acceptance artifact</name>
  <files>.planning/decisions/phase-224-clean-restart-risk-acceptance.md, .planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md</files>
  <what-built>
    Decision artifact `.planning/decisions/phase-224-clean-restart-risk-acceptance.md` was committed in Phase 223 with a Default Disposition accepting the bounded ~15-cycle / ~0.75s post-restart effective-steering window. Plan 04 gate-eval treats this window as a non-rollback symptom. Before any production touch, the operator must sign or annotate this artifact, OR explicitly invoke the Override Path documented therein (which exits Phase 224 entirely until a daemon fix is shipped).
  </what-built>
  <how-to-verify>
    1. Open `.planning/decisions/phase-224-clean-restart-risk-acceptance.md`.
    2. Read Symptom, Blast Radius, Default Disposition, Override Path.
    3. If accepting the default disposition: edit the Sign-Off line to fill in `Accepted`, `Date`, and `Operator`.
    4. If invoking override: STOP Phase 224. Open a Phase 224 pre-work item for the daemon-side fix. Do NOT proceed with Plan 03.
    5. Commit the signed artifact (or the override decision note) under SKIP_DOC_CHECK if needed.
    6. Capture a redacted copy of the signed artifact at `.planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md` so the canary report can cite it.
  </how-to-verify>
  <action>
    BLOCKING HUMAN CHECKPOINT — no Claude automation. Operator reads `.planning/decisions/phase-224-clean-restart-risk-acceptance.md` in full, then either signs the Sign-Off line (Accepted / Date / Operator), commits the change with SKIP_DOC_CHECK if the pre-commit hook fires, copies a redacted version into `evidence/risk-acceptance-signed.redacted.md`, and resumes; OR invokes the Override Path (abandons Phase 224 and opens a pre-work item for a daemon-side fix). No probe, snapshot, deploy, or restart is allowed before this checkpoint clears.
  </action>
  <verify>
    <automated>test -f .planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md</automated>
  </verify>
  <done>Signed artifact captured at `evidence/risk-acceptance-signed.redacted.md`, OR Phase 224 abandoned via Override Path.</done>
  <resume-signal>Type "signed" with the commit SHA, or "override" with the Phase 224 pre-work item link, or "stop" to abandon the phase.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Capture Snapshot A + Leg A pre-deploy baseline</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/, .planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/</files>
  <read_first>
    - scripts/phase224-snapshot-a.sh (Plan 01 output — invocation contract)
    - scripts/phase224-spine-probe.sh (Plan 02 output — invocation contract)
    - scripts/canary-check.sh (`--expect-version <v> --json` post-snapshot baseline)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md (must exist per Task 1)
  </read_first>
  <action>
    Execute pre-deploy capture against the production target host (default `cake-shaper` — operator confirms host at runtime). Steps:
    - **GATE: Plan 01 rehearsal-budget.** Read `.planning/phases/224-production-canary-rollback-discipline/evidence/rehearsal-budget.md`. Refuse to proceed if YAML frontmatter `pending != false` OR `within_budget != true` OR `duration_seconds > 300`. Print explicit error and exit non-zero if gate fails. The rollback path is unproven until rehearsal is measured.
    - Set `TS=$(date -u +%Y%m%dT%H%M%SZ)`.
    - Set `SNAPSHOT_DIR=.planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/${TS}`.
    - Set `LEGA_DIR=.planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment`.
    - Run `scripts/phase224-snapshot-a.sh --output-dir "${SNAPSHOT_DIR}" --raw-dir "${PHASE224_RAW_DIR:?must be set to operator-private path outside git tree}" --ssh-host cake-shaper` and verify the script exits 0 and `MANIFEST.md` exists with the Pre-Deploy Git Ref field populated.
    - Capture raw pre-deploy `/health` (steering loopback): `ssh kevin@cake-shaper curl -s http://127.0.0.1:9102/health > "${LEGA_DIR}/health.steering.pre-deploy.json"`. Verify the JSON parses and contains `version`, `status`, `decision`, `rtt_source` top-level keys.
    - Capture pre-deploy canary-check summary as a SECONDARY signal (NOT a raw-health substitute): `scripts/canary-check.sh --ssh kevin@cake-shaper --expect-version "$(grep __version__ src/wanctl/__init__.py | cut -d\" -f2)" --json > "${LEGA_DIR}/canary-check.pre-deploy.summary.json"`. (This is the `[{target,service,result,detail}]` summary array — gate-eval does NOT consume it; it is a redundant pass/fail signal for the report only.)
    - Run `scripts/phase224-spine-probe.sh --ssh-host cake-shaper --output "${LEGA_DIR}/spine-probe.pre-deploy.json"` and verify all three spine matches == true (binary_on_off, only_new_connections, spectrum_state_not_written_by_daemon). If any are false BEFORE deploy, STOP the canary — that's a pre-existing drift not caused by Phase 224.
    - Confirm `MANIFEST.md` in `${SNAPSHOT_DIR}` cites: pre-deploy version (matches the raw `/health.version`), deployed/repo config equality status, persisted state sha256, AND pre-deploy git commit SHA.
    - Verify the operator-signed `risk-acceptance-signed.redacted.md` exists from Task 1.
    Anti-pattern to avoid: do NOT proceed if the rehearsal-budget gate is `pending: true`. The rollback path must be measured before deploy.
    Anti-pattern to avoid: do NOT proceed if the pre-deploy spine probe shows a violation. That is a pre-existing condition that must be triaged before Phase 224 can proceed.
    Anti-pattern to avoid: do NOT modify `MANIFEST.md` after capture — it's the rollback anchor.
    Anti-pattern to avoid: do NOT consume `canary-check.sh --json` summary as the pre-deploy `/health` baseline. The raw `ssh ... curl` payload is the source of truth; canary-check summary is secondary signal only.
  </action>
  <verify>
    <automated>
      python3 -c "
import re, sys, pathlib
p = pathlib.Path('.planning/phases/224-production-canary-rollback-discipline/evidence/rehearsal-budget.md')
assert p.is_file(), 'rehearsal-budget.md missing — Plan 01 prerequisite failed'
text = p.read_text()
assert re.search(r'^pending:\s*false', text, re.M), 'rehearsal-budget pending != false — Plan 03 BLOCKED'
assert re.search(r'^within_budget:\s*true', text, re.M), 'rehearsal-budget within_budget != true — Plan 03 BLOCKED'
" &amp;&amp;
      TS_DIR=$(ls -1dt .planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/*/ 2>/dev/null | head -1) &amp;&amp;
      test -f "${TS_DIR}/MANIFEST.md" &amp;&amp;
      grep -q '# Snapshot A Manifest' "${TS_DIR}/MANIFEST.md" &amp;&amp;
      grep -q '## Targeted Revert Sequence' "${TS_DIR}/MANIFEST.md" &amp;&amp;
      grep -q 'Pre-Deploy Git Ref' "${TS_DIR}/MANIFEST.md" &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/health.steering.pre-deploy.json &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/canary-check.pre-deploy.summary.json &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/spine-probe.pre-deploy.json &amp;&amp;
      python3 -c "import json; d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/health.steering.pre-deploy.json')); assert 'version' in d and 'status' in d and 'decision' in d and 'rtt_source' in d, 'raw /health missing required keys'" &amp;&amp;
      python3 -c "import json; d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/leg-a-prealignment/spine-probe.pre-deploy.json')); assert d['spine']['binary_on_off']['match'] in (True, None), 'pre-deploy binary_on_off violated — STOP'; assert d['spine']['spectrum_state_not_written_by_daemon']['match'] in (True, None), 'pre-deploy spectrum_state invariant violated — STOP'"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md` exists.
    - `.planning/phases/224-production-canary-rollback-discipline/evidence/rehearsal-budget.md` exists with YAML `pending: false` AND `within_budget: true`.
    - Exactly one `snapshot-a/<TS>/` subdirectory exists containing `MANIFEST.md`, `deployed-steering.redacted.yaml`, `repo-steering.redacted.yaml`, `snapshot-a-health.bound.redacted.json`, `snapshot-a-health.steering.redacted.json`, `repo-version.txt`, `pre-deploy-git-ref.txt`.
    - `evidence/leg-a-prealignment/health.steering.pre-deploy.json` exists (RAW `/health` payload, single object with `version`/`status`/`decision`/`rtt_source` keys).
    - `evidence/leg-a-prealignment/canary-check.pre-deploy.summary.json` exists as secondary signal (the `[{target,service,result,detail}]` summary array).
    - `evidence/leg-a-prealignment/spine-probe.pre-deploy.json` exists with valid JSON.
    - Pre-deploy spine probe shows `spine.binary_on_off.match`, `spine.only_new_connections.match`, `spine.spectrum_state_not_written_by_daemon.match` all true OR null (read-error); none must be `false` pre-deploy.
    - MANIFEST.md `## Targeted Revert Sequence` cites `steering.service` (NOT `wanctl-steering.service`) and `canary-check.sh`.
  </acceptance_criteria>
  <done>
    Pre-deploy snapshot + leg-A baseline captured; rollback anchor established; operator has signed the risk-acceptance artifact; no pre-existing spine drift detected.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Deploy aligned steering daemon + capture Leg B post-deploy proof (BLOCKING HUMAN CHECKPOINT)</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/deploy/, .planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/</files>
  <what-built>
    The single production-mutation event of Phase 224. Operator executes `scripts/deploy.sh spectrum cake-shaper --with-steering` and `ssh cake-shaper 'sudo systemctl restart steering.service'` AT THE KEYBOARD, then runs the post-deploy capture commands. Automation prepares the commands and validates the evidence after the operator returns; it does NOT execute the deploy or restart.
  </what-built>
  <how-to-verify>
    1. Confirm prerequisites: Task 1 signed risk-acceptance, Task 2 snapshot + leg-A baseline captured, Plan 01 rehearsal-budget shows `pending: false` + `within_budget: true`.
    2. Set:
       - `DEPLOY_DIR=.planning/phases/224-production-canary-rollback-discipline/evidence/deploy`
       - `LEGB_DIR=.planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment`
       - `SNAP_TS=$(ls -1dt .planning/phases/224-production-canary-rollback-discipline/evidence/snapshot-a/*/ | head -1 | xargs basename)`
       - `EXPECT_VER=$(grep __version__ src/wanctl/__init__.py | cut -d'"' -f2)`
       - `mkdir -p "${DEPLOY_DIR}" "${LEGB_DIR}"`
       - `echo "snapshot_anchor=${SNAP_TS}" > "${DEPLOY_DIR}/anchor.txt"`
    3. Verify git tree is clean for the controller-path allowlist subset (`git status --porcelain -- src/wanctl/wan_controller.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/backends/ src/wanctl/fusion_healer.py` is empty).
    4. **OPERATOR RUNS (at keyboard, with eyes on terminal):**
       - `scripts/deploy.sh spectrum cake-shaper --with-steering 2>&1 | tee "${DEPLOY_DIR}/deploy-stdout.redacted.log"` — confirm exit 0 before continuing.
       - `ssh cake-shaper 'sudo systemctl restart steering.service' 2>&1 | tee "${DEPLOY_DIR}/restart-stdout.redacted.log"` — confirm exit 0.
       - Record `RESTART_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)`.
    5. Operator polls `ssh cake-shaper curl -s http://127.0.0.1:9102/health | jq -r '.status'` every 2 seconds for up to 30 seconds (WatchdogSec budget). On first `healthy` response, record TS and break. Write the poll log to `${DEPLOY_DIR}/restart-to-healthy.log`. If 30-second poll budget exhausted without `healthy`, IMMEDIATELY invoke `scripts/phase224-rollback.sh --snapshot evidence/snapshot-a/${SNAP_TS} --raw-dir "${PHASE224_RAW_DIR}" --ssh-host cake-shaper` and fail this task with rollback-fired evidence under `evidence/rollback/`.
    6. On healthy: operator captures post-deploy proof:
       - **Raw `/health` payload (PRIMARY — gate-eval consumes this):** `ssh cake-shaper 'curl -s http://127.0.0.1:9102/health' > "${LEGB_DIR}/health.steering.postdeploy.json"`
       - **Bound raw `/health`:** `curl -s http://10.10.110.223:9101/health > "${LEGB_DIR}/health.bound.postdeploy.redacted.json"` (redact router password if present in payload).
       - **Spine probe:** `scripts/phase224-spine-probe.sh --ssh-host cake-shaper --output "${LEGB_DIR}/spine-probe.json"`.
       - **Canary-check summary (SECONDARY signal only, NOT a raw-health substitute):** `scripts/canary-check.sh --ssh kevin@cake-shaper --expect-version "${EXPECT_VER}" --json > "${LEGB_DIR}/canary-check.summary.json"`.
    7. Operator writes `${DEPLOY_DIR}/deploy-summary.json` with: `snapshot_anchor` (TS), `pre_deploy_version`, `post_deploy_version` (from raw `/health.version`), `pre_deploy_commit_sha`, `deploy_exit_code`, `restart_exit_code`, `restart_completed_ts` (used as `observation_start_ts` by Plan 04), `restart_to_healthy_seconds`, `leg_b_canary_check_summary_exit_code`, `leg_b_spine_all_match` (bool, derived from binary_on_off + only_new_connections + spectrum_state_not_written_by_daemon, with restart-window symptoms permitted).
    8. Type `deployed <commit-sha> <restart-to-healthy-seconds>` to resume the workflow.
    Anti-pattern to avoid: do NOT proceed to observation (Plan 04) if Leg B raw `/health.status != healthy` or spine has any `match: false` outside the restart window. Hand off to the gate evaluator for the canary report.
    Anti-pattern to avoid: do NOT touch `wanctl@spectrum.service` (controller path) — only `steering.service`.
    Anti-pattern to avoid: do NOT modify any controller-path source file. SAFE-12 boundary check will catch this in Plan 05.
    Anti-pattern to avoid: do NOT consume `canary-check.sh --json` as the post-deploy `/health` payload. The raw `ssh ... curl` JSON is the source of truth for gate-eval.
  </how-to-verify>
  <action>
    BLOCKING HUMAN CHECKPOINT — automation MUST NOT execute `scripts/deploy.sh` or `systemctl restart steering.service`. These are the single production-touch events of Phase 224 and are reserved for the operator. Automation may prepare environment variables, validate prerequisites, and write `${DEPLOY_DIR}/anchor.txt` ahead of time. After operator returns with `deployed <commit-sha> <restart-to-healthy-seconds>`, automation validates the evidence shape per the verify block.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-stdout.redacted.log &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/restart-stdout.redacted.log &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-summary.json &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/health.steering.postdeploy.json &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/spine-probe.json &amp;&amp;
      python3 -c "import json; d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/leg-b-postalignment/health.steering.postdeploy.json')); assert 'version' in d and 'status' in d and 'decision' in d and 'rtt_source' in d, 'raw /health missing required keys'" &amp;&amp;
      python3 -c "import json; d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-summary.json')); assert d['deploy_exit_code']==0 and d['restart_exit_code']==0 and d['restart_to_healthy_seconds']&lt;=30 and d.get('restart_completed_ts') and d.get('pre_deploy_commit_sha'), 'deploy gate failed'" &amp;&amp;
      grep -q 'systemctl restart steering.service' .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/restart-stdout.redacted.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/deploy/deploy-stdout.redacted.log` exists and cites a snapshot anchor TS that matches the directory created in Task 2.
    - `evidence/deploy/restart-stdout.redacted.log` exists and references `steering.service` (NOT `wanctl-steering.service`).
    - `evidence/deploy/deploy-summary.json` has `deploy_exit_code == 0`, `restart_exit_code == 0`, `restart_to_healthy_seconds <= 30`, `restart_completed_ts` (ISO8601), `pre_deploy_commit_sha` (git SHA).
    - `evidence/leg-b-postalignment/health.steering.postdeploy.json` exists as a single JSON object with keys `version`, `status`, `decision`, `rtt_source` (raw `/health` shape — NOT the `[{target,service,result,detail}]` array).
    - `evidence/leg-b-postalignment/canary-check.summary.json` exists (secondary signal only).
    - `evidence/leg-b-postalignment/spine-probe.json` exists with `spine.spectrum_state_not_written_by_daemon.match == true` AND `spine.only_new_connections.match == true` (binary_on_off and spectrum_state may be in restart-window symptom state at first sample — that's OK; Plan 04 gate-eval distinguishes).
    - No controller-path source file (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, `backends/`, `alert_engine.py`, `fusion_healer.py`) has been modified during this plan (verify with `git diff --name-only HEAD~ HEAD | grep -E '(wan_controller|queue_controller|cake_signal|alert_engine|fusion_healer)' | wc -l` returning 0).
    - No `wanctl@*.service` was restarted during this plan (only `steering.service`).
    - Task is marked `type: checkpoint:human-action` and the production-touch commands were run by the operator at the keyboard, not by automation.
  </acceptance_criteria>
  <done>
    Operator-driven deploy of the aligned steering daemon completed against production with the snapshot anchor cited; service restarted inside watchdog budget; raw `/health` returns healthy + version-aligned; spine probe captured for Plan 04 to evaluate over the observation window.
  </done>
  <resume-signal>Type "deployed" followed by the pre-deploy commit SHA and the measured restart-to-healthy seconds (e.g., `deployed abc123 8`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → production target host | Single mutation event: deploy + restart. SSH key only. |
| Operator → /health (bound + loopback) | Read-only post-deploy proof. |
| Operator → controller-path source | NO MUTATION — SAFE-12 invariant. Verified at Plan 05 boundary. |
| Operator → risk-acceptance decision artifact | Write (sign-off) is a planning-level commit, not a production mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-10 | Tampering | Deploy accidentally modifies controller-path source | mitigate | Task 3 verifies no controller-path files in `git diff` during plan execution; Plan 05 runs `safe12-boundary-check`. |
| T-224-11 | Denial of service | Steering daemon restart exceeds WatchdogSec=30s, watchdog loop | mitigate | Task 3 polls /health.status for up to 30s; auto-invokes rollback if budget exceeded. Plan 01 staging rehearsal validated this in advance. |
| T-224-12 | Information disclosure | Deploy log captures router password | mitigate | Tee through redaction; any value matching `password|token|secret` field in deploy output is replaced with `REDACTED` before write. |
| T-224-13 | Repudiation | Snapshot anchor not cited in deploy log | mitigate | Task 3 explicitly writes the snapshot TS into `deploy-summary.json` and the deploy-stdout log header. |
| T-224-14 | Elevation of privilege | Operator bypasses risk-acceptance sign-off | mitigate | Task 1 is a blocking-human gate; Task 2 verifies `risk-acceptance-signed.redacted.md` exists before snapshot capture. |
| T-224-SC | Supply chain | Deploy pulls unverified package | accept | `scripts/deploy.sh` is the project's vetted paved path; no new packages introduced in this plan. |
</threat_model>

<verification>
- Risk-acceptance artifact signed before deploy gate.
- Plan 01 rehearsal-budget shows `pending: false` AND `within_budget: true` before snapshot capture.
- Snapshot A exists with manifest (including Pre-Deploy Git Ref), redaction proven (no raw secrets); raw artifacts captured to operator-private `--raw-dir`.
- Deploy + restart executed AS A BLOCKING HUMAN CHECKPOINT (operator at keyboard); evidence captured by automation after operator returns.
- Deploy exit 0; restart exit 0; restart-to-healthy ≤ 30 seconds; `restart_completed_ts` recorded for Plan 04 `observation_start_ts`.
- Leg B RAW `/health` (from `ssh ... curl http://127.0.0.1:9102/health`) returns healthy + version-aligned. Canary-check `--json` summary captured separately as secondary signal only.
- Leg B spine probe captures three invariants (binary_on_off, only_new_connections, spectrum_state_not_written_by_daemon) for Plan 04 gate-eval.
- No controller-path source file (incl. `fusion_healer.py`) modified.
- No `wanctl@*.service` restarted; `steering.service` is the only restarted unit.
</verification>

<success_criteria>
1. Operator has signed the risk-acceptance decision artifact (CANARY-01 governance precondition).
2. Snapshot A directory exists and serves as the rollback anchor for the entire observation window.
3. Aligned steering daemon is deployed; /health returns the expected version; daemon comes up healthy inside 30s watchdog budget.
4. Leg B evidence is captured for Plan 04 gate-eval to consume across the observation window.
5. SAFE-12 holds — no controller-path source mutation during deploy plan execution.
</success_criteria>

<output>
Create `.planning/phases/224-production-canary-rollback-discipline/224-03-SUMMARY.md` when done, citing: snapshot anchor TS, pre-deploy version, post-deploy version, deploy exit code, restart-to-healthy seconds, signed risk-acceptance artifact path, Leg B canary-check + spine-probe paths.
</output>
