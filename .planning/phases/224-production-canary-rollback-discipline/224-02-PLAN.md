---
phase: 224-production-canary-rollback-discipline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase224-spine-probe.sh
  - scripts/phase224-gate-eval.py
  - tests/test_phase224_gate_eval.py
requirements:
  - CANARY-02
  - CANARY-03
autonomous: true
must_haves:
  truths:
    - "Operator can run `scripts/phase224-spine-probe.sh --ssh-host <host> --health-url <url>` against a target host and get a JSON record of: binary on/off probe, only-new-connections rule-selector probe, daemon-not-writing-spectrum_state.json probe (the narrowed Phase 223 invariant-3 proxy)."
    - "Operator can run `python scripts/phase224-gate-eval.py --health <raw-health-json> --spine <spine-json> --expected-version <v>` consuming RAW `/health` payloads from `ssh host curl http://127.0.0.1:9102/health` (NOT `canary-check.sh --json` summary which is `[{target,service,result,detail}]`), and get a verdict.json with a per-gate boolean and a fired-gate id (or null)."
    - "Gate evaluator explicitly distinguishes the accepted post-restart window (≤15 cycles, recovers to GOOD) from steady-state spine violation per D-11/D-12 — restart-window symptoms do NOT fire rollback."
    - "Pytest unit coverage for the gate evaluator passes against fixture inputs covering kept-aligned, restart-window symptom (must NOT fire), steady-state violation (MUST fire), and window-end-timestamp boundary (kept_aligned ONLY at window close)."
  artifacts:
    - path: "scripts/phase224-spine-probe.sh"
      provides: "Read-only spine invariant probe against live target"
      contains: "router_reachable"
    - path: "scripts/phase224-gate-eval.py"
      provides: "Stdlib-only Python gate evaluator producing verdict.json; consumes RAW /health JSON (not canary-check.sh --json summary)"
      contains: "restart_window"
    - path: "tests/test_phase224_gate_eval.py"
      provides: "Pytest coverage of gate-eval logic (kept, restart-window, steady-state)"
      contains: "def test_"
  key_links:
    - from: "scripts/phase224-gate-eval.py"
      to: "scripts/phase224-spine-probe.sh"
      via: "consumes spine-probe JSON output"
      pattern: "spine"
    - from: "scripts/phase224-gate-eval.py"
      to: ".planning/decisions/phase-224-clean-restart-risk-acceptance.md"
      via: "restart-window classification logic"
      pattern: "restart_window|clean_restart"
    - from: "tests/test_phase224_gate_eval.py"
      to: "scripts/phase224-gate-eval.py"
      via: "imports + invokes evaluator"
      pattern: "phase224_gate_eval|phase224-gate-eval"
---

<objective>
Build the spine invariant probe and the gate evaluator that drives the rollback decision during
canary observation (CANARY-02, CANARY-03). The evaluator must explicitly treat the bounded
~15-cycle post-restart effective-steering window as an accepted symptom, NOT a rollback trigger
(per D-11/D-12 and the committed risk-acceptance artifact).

Purpose: Phase 215 had a simple verdict script. Phase 224's spine has three live invariants plus
a decision-continuity dimension plus a clean-restart symptom that LOOKS LIKE a violation but
isn't. The evaluator is the place where that distinction lives.

Output: One read-only bash probe, one stdlib-only Python gate evaluator, one pytest module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/224-production-canary-rollback-discipline/224-CONTEXT.md
@.planning/phases/224-production-canary-rollback-discipline/224-RESEARCH.md
@.planning/decisions/phase-224-clean-restart-risk-acceptance.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md
@src/wanctl/steering/health.py
@src/wanctl/steering/daemon.py
@scripts/canary-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/phase224-spine-probe.sh</name>
  <files>scripts/phase224-spine-probe.sh</files>
  <read_first>
    - src/wanctl/steering/health.py (CONFIRMED field names: `decision.last_transition_time` line 281, `decision.time_in_state_seconds` line 282, top-level `rtt_source.current` / `rtt_source.last_successful` / `rtt_source.last_measurement_age_sec` lines 196 + 367 — NO `decision.last_cycle_ts`, NO `decision.rtt_source`. Probe MUST consume these actual field names.)
    - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md (the three spine invariants verbatim and the fixture shape Phase 223 used; Phase 223 narrowed invariant-3 to "daemon does NOT write `spectrum_state.json` during steady state" — the probe consumes this narrowed proxy, NOT a fuzzy `rtt_source.current` string match)
    - scripts/canary-check.sh (WARNING: `--json` output is `[{target, service, result, detail}]` summary array per line 159-164, NOT raw `/health` payload. Probe MUST NOT consume canary-check --json for raw health fields. Probe directly invokes `ssh <host> curl -s http://127.0.0.1:9102/health` for the raw payload.)
    - scripts/phase213-steering-snapshot.sh (the SSH-with-sudo-n redaction pattern this probe reuses for the router rule read)
  </read_first>
  <action>
    Implement `scripts/phase224-spine-probe.sh` as a read-only bash script that produces a single
    JSON document on stdout (and optionally writes it to `--output <path>`). It MUST:
    - Accept `--ssh-host <host>` (default `cake-shaper`), `--health-url <url>` (default `http://10.10.110.223:9101/health`), `--steering-health-url <url>` (default `http://127.0.0.1:9102/health` invoked over SSH), `--state-dir <path>` (default `/var/lib/wanctl`, used for spectrum_state.json presence check), `--output <path>` (optional; if absent, write to stdout).
    - **First action:** fetch the raw steering `/health` JSON via `ssh <ssh-host> curl -s http://127.0.0.1:9102/health` and store as `<output>.raw-steering-health.json` alongside the spine JSON (or in a sibling temp file when stdout-only). Probe operates on the raw payload directly — NEVER from `canary-check.sh --json` summary.
    - Read three live signals from the raw steering `/health` JSON (field names confirmed against `src/wanctl/steering/health.py`):
      1. **Binary on/off:** read `steering.enabled` (top-level `/health.steering.enabled` from `_build_steering_status_section`) AND the live router rule presence (REST `/rest/ip/firewall/mangle` or `/rest/ip/route/rule` per project convention, SSH fallback). Match: `enabled == true` ↔ `rule_present == true` (and inverse). Emit: `binary_on_off: { enabled: bool, rule_present: bool, match: bool }`.
      2. **Only-new-connections selector:** read the live router mangle/route rule selector shape (read-only via REST preferred, SSH fallback). Expected selector is the spec-frozen "only new latency-sensitive connections" rule recorded in Phase 222 audit / Phase 223 spine evidence. Compare live selector to recorded shape field-by-field. Emit: `only_new_connections: { live_selector, expected_selector, match: bool }`.
      3. **Daemon-not-writing-spectrum_state.json (narrowed Phase 223 invariant-3 proxy):** check whether `<state-dir>/spectrum_state.json` exists on the target host (`ssh <ssh-host> 'sudo -n test -f /var/lib/wanctl/spectrum_state.json && echo present || echo absent'`). For steady-state (≥30s post-restart), expected value is `absent` — the steering daemon must NOT write `spectrum_state.json` (that path is owned by the controller; daemon-side writes are a clean-restart spillover symptom Phase 223 narrowed the invariant to). If file is present and its mtime is within the last 30s, classify as `restart_window_symptom: true` (gate-eval will distinguish). Also capture the file mtime so gate-eval can compute age. Emit: `spectrum_state_not_written_by_daemon: { file_present: bool, mtime_iso: "..." | null, mtime_age_seconds: int | null, match: bool }`.
    - Also record raw health pointer fields: `health.version`, `health.status`, `health.uptime_seconds`, `health.decision.last_transition_time`, `health.decision.time_in_state_seconds`, `health.rtt_source.current`, `health.rtt_source.last_successful`, `health.rtt_source.last_measurement_age_sec` (these are the ACTUAL field names per health.py:196,266,281,282,361-371). Do NOT reference `decision.last_cycle_ts` or `decision.rtt_source` — those fields DO NOT EXIST in the current payload.
    - Output JSON shape:
      ```
      {
        "captured_at": "<ISO8601 UTC>",
        "target_host": "<host>",
        "raw_health_path": "<path to ssh+curl raw health output>",
        "health": {
          "version": "...",
          "status": "...",
          "uptime_seconds": ...,
          "decision": {
            "last_transition_time": "...",
            "time_in_state_seconds": ...
          },
          "rtt_source": {
            "current": "...",
            "last_successful": "...",
            "last_measurement_age_sec": ...
          }
        },
        "spine": {
          "binary_on_off": { "enabled": bool, "rule_present": bool, "match": bool },
          "only_new_connections": { "live_selector": {...}, "expected_selector": {...}, "match": bool },
          "spectrum_state_not_written_by_daemon": { "file_present": bool, "mtime_iso": "..." | null, "mtime_age_seconds": int | null, "match": bool }
        }
      }
      ```
    - Read-only: NO `sudo` writes, NO `systemctl`, NO router-mutation calls. RouterOS read uses REST API (`/rest/...`) per CLAUDE.md preference; SSH read is fallback.
    - `set -euo pipefail`, shellcheck-clean. Stdlib + curl + jq + ssh only.
    - If any signal cannot be read (e.g., router unreachable), emit `match: null` (NOT `false`) and a `read_error` field so the gate evaluator can distinguish "unreadable" from "violated".
    Anti-pattern to avoid: do NOT classify or judge in the probe — just read and emit. Gate logic lives in `phase224-gate-eval.py`.
    Anti-pattern to avoid: do NOT touch the controller path. Controller health is not in spine-probe scope (separate `/health` endpoint on a different port).
  </action>
  <verify>
    <automated>
      shellcheck scripts/phase224-spine-probe.sh &amp;&amp;
      bash -n scripts/phase224-spine-probe.sh &amp;&amp;
      scripts/phase224-spine-probe.sh --help 2>&amp;1 | grep -E 'ssh-host|health-url|output'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/phase224-spine-probe.sh` exists, is executable, passes `shellcheck` and `bash -n`.
    - Script `--help` output names `--ssh-host`, `--health-url`, `--steering-health-url`, `--state-dir`, `--output` flags.
    - Script JSON output contains top-level keys `captured_at`, `target_host`, `raw_health_path`, `health`, `spine`.
    - Script `spine.binary_on_off`, `spine.only_new_connections`, `spine.spectrum_state_not_written_by_daemon` each contain a `match` field (note: invariant 3 renamed from autorate_baseline to spectrum_state_not_written_by_daemon per Phase 223 narrowing).
    - Script `health` block contains `decision.last_transition_time`, `decision.time_in_state_seconds`, `rtt_source.current`, `rtt_source.last_successful`, `rtt_source.last_measurement_age_sec` (the ACTUAL fields per `src/wanctl/steering/health.py`).
    - Script does NOT reference `decision.last_cycle_ts` or `decision.rtt_source` (those fields do not exist).
    - Script invokes `ssh <host> curl -s http://127.0.0.1:9102/health` to fetch raw payload; does NOT consume `canary-check.sh --json` output for raw health fields.
    - Script never contains the strings `wan_controller.py`, `queue_controller.py`, `cake_signal.py`, `fusion_healer.py`, `alert_engine.py`, `systemctl restart`, `sudo tee`, `sudo cp` (SAFE-12 + read-only; sole `sudo` use is `sudo -n test -f` for the spectrum_state.json presence probe which is read-only).
  </acceptance_criteria>
  <done>
    Spine probe exists, shellcheck-clean, read-only, emits the three invariant matches plus decision-continuity timestamp in a single JSON document.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement scripts/phase224-gate-eval.py with restart-window distinction</name>
  <files>scripts/phase224-gate-eval.py, tests/test_phase224_gate_eval.py</files>
  <read_first>
    - .planning/decisions/phase-224-clean-restart-risk-acceptance.md (the ~15-cycle / ~0.75-second bounded window definition — gate code must NOT misclassify this)
    - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.md (exact "reproduced-bug" cycle window and recovery semantics — fixture source for tests)
    - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md (per-invariant verdicts shape — gate output mirrors this; narrowed invariant 3 is `spectrum_state.json` write absence)
    - src/wanctl/steering/health.py (CONFIRMED decision field names: `decision.last_transition_time` line 281, `decision.time_in_state_seconds` line 282, `rtt_source.last_measurement_age_sec` line 367 — the staleness signal that actually exists. Gate consumes these.)
    - tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml (existing fixture shape — gate-eval test fixtures can reference this)
  </read_first>
  <action>
    Implement `scripts/phase224-gate-eval.py` as a stdlib-only Python script (per `scripts/phase219_ingestion_digest.py` precedent — argparse, json, sys, datetime; no third-party imports). It MUST:
    - Accept `--health <path-or-->` (RAW `/health` payload from `ssh host curl http://127.0.0.1:9102/health`), `--spine <path-or-->` (output from `scripts/phase224-spine-probe.sh`), `--expected-version <v>`, `--snapshot <dir>`, `--observation-start-ts <iso8601>`, `--observation-end-ts <iso8601>` (REQUIRED for `kept_aligned` verdict; gate refuses to emit `kept_aligned` unless `captured_at >= observation_end_ts`), `--restart-window-cycles <int default 15>`, `--rtt-staleness-seconds <float default 5.0>` (replaces 4-cycle/200ms gate; uses real `rtt_source.last_measurement_age_sec` field — see note below), `--output <path-or-->` flags.
    - Validate input shape: the `--health` JSON MUST be a single object with keys `version`, `status`, `decision`, `rtt_source` (raw `/health` shape per `src/wanctl/steering/health.py`). If the input is a JSON array (i.e., `canary-check.sh --json` summary), fail explicitly with: "gate-eval requires raw /health JSON, not canary-check --json summary; invoke ssh host curl http://127.0.0.1:9102/health".
    - Read the raw health JSON and the spine JSON.
    - Compute the following gate verdicts (field names match `src/wanctl/steering/health.py`):
      - `gate_version_alignment`: `health.version == expected_version` AND `health.status == "healthy"`.
      - `gate_binary_on_off`: `spine.binary_on_off.match == true`.
      - `gate_only_new_connections`: `spine.only_new_connections.match == true`.
      - `gate_spectrum_state_not_written_by_daemon`: `spine.spectrum_state_not_written_by_daemon.match == true` (narrowed Phase 223 invariant-3 proxy — replaces fuzzy "autorate-baseline-authoritative" string match).
      - `gate_rtt_source_fresh`: `health.rtt_source.last_measurement_age_sec` is non-null AND `<= rtt-staleness-seconds`. This is the REAL decision-freshness signal exposed by the live payload (per `src/wanctl/steering/health.py:367`). NOTE: the original 4-cycle/200ms staleness gate is not implementable from the current payload because no `decision.last_cycle_ts` or per-cycle counter exists. The 5-second default reflects an RTT measurement cadence that is meaningfully observable from 60s-cadence samples; staleness on a per-cycle basis (50ms granularity) would require either an additive health field (filed as a Phase 224 follow-up) or a dedicated high-rate probe loop (out of scope for this canary). Plan 04 documents that this gate is the staleness signal in use.
      - `gate_daemon_not_degraded`: `health.status == "healthy"` AND `health.decision.time_in_state_seconds` is non-null. If single point-in-time read, the gate uses `status == "healthy"` directly.
    - **Restart-window distinction (CRITICAL — D-11/D-12):** Compute `seconds_since_restart = (captured_at - observation_start_ts).total_seconds()` (interpreting `observation_start_ts` as the post-deploy restart-completed TS Plan 03 writes). If `seconds_since_restart <= restart-window-cycles * 0.05` (default 0.75s) AND `health.decision.time_in_state_seconds <= seconds_since_restart` (the daemon literally just restarted), mark any FAIL on `gate_binary_on_off` or `gate_spectrum_state_not_written_by_daemon` as `restart_window_symptom: true` and DO NOT fire rollback. Emit `note: "restart_window_symptom_per_decision_artifact"` and continue observation. Only when a FAIL persists BEYOND the restart window (or applies to a non-restart-window gate) does it become a rollback trigger.
    - Output verdict JSON shape:
      ```
      {
        "evaluated_at": "<iso8601>",
        "observation_start_ts": "<iso8601>",
        "snapshot_anchor": "<dir or null>",
        "expected_version": "...",
        "gates": {
          "gate_version_alignment": { "verdict": "pass|fail|restart_window_symptom", "value": "..." },
          "gate_binary_on_off": { "verdict": "...", "value": "..." },
          ...
        },
        "rollback_trigger": null | "<gate_id>",
        "outcome": "kept_aligned | continue_observation | rollback"
      }
      ```
    - `outcome == "rollback"` ONLY when at least one gate has `verdict == "fail"` (NOT `restart_window_symptom`).
    - `outcome == "kept_aligned"` only when all gates pass AND `captured_at >= observation_end_ts` (the explicit window-end timestamp). If `observation_end_ts` is null OR not yet reached, the maximum allowed outcome is `continue_observation`. Emit the comparison verbatim in the verdict: `"window_end_reached": bool, "captured_at": "...", "observation_end_ts": "..."`.
    - Otherwise `outcome == "continue_observation"`.
    - Exit codes: `0` for `kept_aligned`, `1` for `rollback`, `2` for `continue_observation` (mirrors `canary-check.sh` 0/1/2 contract).
    Then implement `tests/test_phase224_gate_eval.py` with at minimum these tests:
    - `test_all_gates_pass_returns_kept_aligned_at_end_of_window` (fixture: all matches true, `captured_at >= observation_end_ts`, ≥15 samples ≥900s window — outcome MUST be `kept_aligned`).
    - `test_all_gates_pass_before_window_end_returns_continue_observation` (fixture: all matches true, `captured_at < observation_end_ts` — outcome MUST be `continue_observation`, NOT `kept_aligned`).
    - `test_restart_window_binary_violation_does_not_trigger_rollback` (fixture: cycle index 5 of 15 post-restart (0.25s elapsed), `binary_on_off.match=false`, `decision.time_in_state_seconds=0.25` — outcome MUST be `continue_observation`, `restart_window_symptom: true`, NOT rollback).
    - `test_steady_state_binary_violation_triggers_rollback` (fixture: 60 seconds post-restart, `binary_on_off.match=false`, daemon healthy — outcome MUST be `rollback`).
    - `test_rtt_source_stale_triggers_rollback` (fixture: `rtt_source.last_measurement_age_sec=10.0`, default staleness threshold 5.0 — `gate_rtt_source_fresh` fails, outcome MUST be `rollback`).
    - `test_rtt_source_fresh_passes` (fixture: `rtt_source.last_measurement_age_sec=1.2`, threshold 5.0 — gate passes).
    - `test_health_input_as_canary_check_summary_array_fails_explicitly` (fixture: gate input is `[{"target":...,"service":...,"result":...,"detail":...}]` array — gate-eval MUST raise a clear error naming the required raw `/health` shape; no silent misinterpretation).
    Tests use stdlib `unittest` or pytest (project uses pytest — match precedent in `tests/test_phase_203_capture_projection.py`). Tests MUST pass via `.venv/bin/pytest tests/test_phase224_gate_eval.py -v`.
    Anti-pattern to avoid: do NOT use any third-party Python package. Stdlib only.
    Anti-pattern to avoid: do NOT classify the restart-window symptom as a "pass" — it's a `restart_window_symptom` distinct verdict.
    Anti-pattern to avoid: do NOT consume `canary-check.sh --json` summary as the `--health` input. The summary schema `[{target, service, result, detail}]` has none of the fields gate-eval needs.
  </action>
  <verify>
    <automated>
      .venv/bin/python -m py_compile scripts/phase224-gate-eval.py &amp;&amp;
      .venv/bin/ruff check scripts/phase224-gate-eval.py tests/test_phase224_gate_eval.py &amp;&amp;
      .venv/bin/pytest tests/test_phase224_gate_eval.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/phase224-gate-eval.py` exists, compiles, ruff-clean.
    - `tests/test_phase224_gate_eval.py` exists, ruff-clean.
    - `.venv/bin/pytest tests/test_phase224_gate_eval.py -v` exits 0 with at least the seven test functions named in the action block passing.
    - The tests `test_restart_window_binary_violation_does_not_trigger_rollback`, `test_steady_state_binary_violation_triggers_rollback`, `test_all_gates_pass_before_window_end_returns_continue_observation`, `test_rtt_source_stale_triggers_rollback`, and `test_health_input_as_canary_check_summary_array_fails_explicitly` exist by name and pass.
    - Gate-eval `--help` lists `--observation-end-ts` and `--rtt-staleness-seconds` flags.
    - Gate-eval source contains the verdict keys `gate_rtt_source_fresh`, `gate_spectrum_state_not_written_by_daemon`, and `window_end_reached` (replaces the original autorate/4-cycle-staleness gates).
    - Gate-eval source does NOT reference `decision.last_cycle_ts` or `decision.rtt_source` (those fields do not exist in live payload).
    - Gate-eval rejects array-shaped `--health` input with an explicit error naming raw `/health` requirement.
    - The verdict JSON contains a `restart_window_symptom` field on any gate that triggered the restart-window distinction AND a top-level `window_end_reached` boolean.
    - `scripts/phase224-gate-eval.py` source contains a comment or docstring referencing `.planning/decisions/phase-224-clean-restart-risk-acceptance.md`.
    - No third-party imports (`grep '^import\|^from' scripts/phase224-gate-eval.py` returns only stdlib modules).
  </acceptance_criteria>
  <done>
    Gate evaluator exists, stdlib-only, ruff-clean, with passing pytest coverage proving the restart-window vs steady-state distinction works as governed by the risk-acceptance decision artifact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 224 probe → /health endpoints | Read-only HTTP. Bound and loopback surfaces. |
| Phase 224 probe → RouterOS REST/SSH | Read-only queries. No mutation calls. |
| Gate evaluator → snapshot directory | Read-only file reads (manifest, version files). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-05 | Tampering | Spine probe accidentally writes to RouterOS or target host | mitigate | shellcheck + grep-gate verifies no `sudo tee`, `sudo cp`, `systemctl`, `/rest/.../set`, `/rest/.../add` in probe source. |
| T-224-06 | Information disclosure | Probe captures router credentials in JSON output | mitigate | Probe uses REST endpoint with operator-side auth; credentials NEVER appear in output JSON. Audit by grep against committed fixtures. |
| T-224-07 | Information disclosure | Gate-eval logs sensitive `/health` fields | mitigate | Gate-eval logs only verdicts and gate IDs; raw health JSON is consumed but not echoed. |
| T-224-08 | Elevation of privilege | Gate-eval misclassifies restart-window as pass | mitigate | `restart_window_symptom` verdict is distinct from `pass`. Tests cover both directions. The decision artifact link is in code. |
| T-224-09 | Repudiation | Gate-eval output lacks evaluator version / source-of-truth pointer | mitigate | Output JSON includes `snapshot_anchor` field and an `evaluated_at` UTC timestamp. |
| T-224-SC | Supply chain | Test imports introduce third-party deps | mitigate | Tests use stdlib + pytest (already in project). No new requirements.txt entries. |
</threat_model>

<verification>
- `scripts/phase224-spine-probe.sh` is read-only by grep gate.
- `scripts/phase224-gate-eval.py` is stdlib-only by import audit.
- `tests/test_phase224_gate_eval.py` covers kept-aligned (with window-end gate), continue-before-window-end, restart-window symptom (non-trigger), steady-state violation (trigger), rtt-source-stale trigger, rtt-source-fresh pass, and array-input rejection.
- Neither script references controller-path source files (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, `fusion_healer.py`, `alert_engine.py`).
- Neither script references the non-existent fields `decision.last_cycle_ts` or `decision.rtt_source`.
- Gate-eval consumes RAW `/health` JSON (single object), explicitly rejects `canary-check.sh --json` array summaries.
</verification>

<success_criteria>
1. Probe emits a single JSON document covering the three spine invariants (binary_on_off, only_new_connections, spectrum_state_not_written_by_daemon) from any target host AND captures the raw `/health` JSON alongside.
2. Gate-eval consumes raw `/health` + spine JSON and produces a verdict.json with explicit kept/rollback/continue outcomes plus a `window_end_reached` boolean.
3. Restart-window symptom NEVER triggers rollback (proven by pytest).
4. Steady-state spine violation ALWAYS triggers rollback (proven by pytest).
5. RTT-source staleness gate uses the real `rtt_source.last_measurement_age_sec` field (5s default), not a fictional `decision.last_cycle_ts`.
6. `kept_aligned` outcome is gated on explicit `observation_end_ts` — never emitted mid-window.
</success_criteria>

<output>
Create `.planning/phases/224-production-canary-rollback-discipline/224-02-SUMMARY.md` when done, citing probe path, gate-eval path, test file path, and a summary of the three pytest verdicts.
</output>
