---
phase: 224-production-canary-rollback-discipline
plan: 04
type: execute
wave: 3
depends_on:
  - 224-03
files_modified:
  - .planning/phases/224-production-canary-rollback-discipline/evidence/observation/
  - .planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json
  - .planning/phases/224-production-canary-rollback-discipline/evidence/rollback/
requirements:
  - CANARY-02
  - CANARY-03
autonomous: false
must_haves:
  truths:
    - "Operator can observe the aligned steering daemon over a bounded ≥15-minute window, with `scripts/phase224-spine-probe.sh` + `scripts/canary-check.sh --json` samples captured at a documented cadence (default ≥1 sample/minute)."
    - "Operator can run `scripts/phase224-gate-eval.py` over the observation samples and produce `evidence/verdict.json` with a kept-aligned, rollback, or continue-observation outcome per the gate definitions in Plan 02."
    - "Restart-window symptoms (≤15 cycles after a steering daemon restart) NEVER trigger rollback per D-11/D-12; only steady-state spine violations do."
    - "If a steady-state violation fires, `scripts/phase224-rollback.sh --snapshot <Plan 03 anchor>` is executed against the production host inside the measured ≤5-minute budget from Plan 01, with post-revert `/health` proof captured under evidence/rollback/."
  artifacts:
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/observation/"
      provides: "Per-sample /health + spine-probe JSON across the observation window"
      contains: "sample-"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json"
      provides: "Final canary verdict per gate-eval"
      contains: "outcome"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/rollback/"
      provides: "Post-rollback evidence if rollback fired; empty otherwise"
      contains: "(empty or rollback-summary.json)"
  key_links:
    - from: "evidence/observation/sample-*"
      to: "scripts/phase224-spine-probe.sh"
      via: "samples produced by spine probe"
      pattern: "spine-probe"
    - from: "evidence/verdict.json"
      to: "scripts/phase224-gate-eval.py"
      via: "verdict produced by gate-eval"
      pattern: "phase224-gate-eval|phase224_gate_eval"
    - from: "evidence/rollback/"
      to: "scripts/phase224-rollback.sh"
      via: "rollback executed by phase224-rollback.sh if needed"
      pattern: "phase224-rollback"
---

<objective>
Run the canary observation window: sample /health + spine probe at a documented cadence over a
bounded ≥15-minute window; feed samples to the gate evaluator; produce verdict.json with a
kept-aligned, continue-observation, or rollback outcome. If a steady-state violation fires,
execute the Plan 01 rollback within the measured budget. Restart-window symptoms are NOT
rollback triggers (CANARY-02, CANARY-03).

Purpose: Phase 215 had a single end-of-window verdict. Phase 224 has a 15-minute window with
multiple samples because steady-state drift can appear gradually and the restart-window
symptom must be filtered out. The gate evaluator from Plan 02 is the source of truth.

Output: observation/ samples, verdict.json, and optionally rollback/ evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/224-production-canary-rollback-discipline/224-CONTEXT.md
@.planning/phases/224-production-canary-rollback-discipline/224-RESEARCH.md
@.planning/phases/224-production-canary-rollback-discipline/224-01-PLAN.md
@.planning/phases/224-production-canary-rollback-discipline/224-02-PLAN.md
@.planning/phases/224-production-canary-rollback-discipline/224-03-PLAN.md
@.planning/decisions/phase-224-clean-restart-risk-acceptance.md
@scripts/phase224-spine-probe.sh
@scripts/phase224-gate-eval.py
@scripts/phase224-rollback.sh
@scripts/canary-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run observation window sampling loop</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/observation/</files>
  <read_first>
    - scripts/phase224-spine-probe.sh (sample command)
    - scripts/canary-check.sh (sample command, `--json`)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-summary.json (Plan 03 output — read `snapshot_anchor`, restart_to_healthy timing, observation_start_ts derived from deploy completion)
    - src/wanctl/steering/health.py (decision section field names — cadence anchor)
  </read_first>
  <action>
    Implement and run the observation loop. Use a small bash driver inline:
    - Set `OBS_DIR=.planning/phases/224-production-canary-rollback-discipline/evidence/observation`.
    - Read `snapshot_anchor` and Plan 03 deploy completion TS from `evidence/deploy/deploy-summary.json`. Set `OBS_START_TS=$(jq -r '.observation_start_ts // .restart_completed_ts // .deploy_completed_ts' evidence/deploy/deploy-summary.json)` (use the most appropriate field that exists).
    - Set window duration `OBS_DURATION_MIN=15` (operator may override at run time via env `PHASE224_WINDOW_MIN`).
    - Set sample cadence `SAMPLE_INTERVAL_SEC=60` (one sample per minute — operator may override).
    - Loop: at each interval, write `${OBS_DIR}/sample-${N}-health.json` from `scripts/canary-check.sh --ssh kevin@cake-shaper --expect-version "$(grep __version__ src/wanctl/__init__.py | cut -d\" -f2)" --json`, and `${OBS_DIR}/sample-${N}-spine.json` from `scripts/phase224-spine-probe.sh --ssh-host cake-shaper`.
    - Between samples, also run `scripts/phase224-gate-eval.py --health ${OBS_DIR}/sample-${N}-health.json --spine ${OBS_DIR}/sample-${N}-spine.json --expected-version <v> --snapshot evidence/snapshot-a/<TS> --observation-start-ts "${OBS_START_TS}" --output ${OBS_DIR}/sample-${N}-verdict.json`. Capture exit code: 0 = kept-aligned (only at window close), 1 = rollback (BREAK loop and proceed to Task 3), 2 = continue.
    - On any sample returning `outcome=rollback` mid-window: STOP sampling, write `${OBS_DIR}/breakout-sample.json` referring to the offending sample N, and signal Task 3.
    - Continue until `OBS_DURATION_MIN` elapsed OR a rollback gate fires.
    - At window close, run a final gate-eval with the final sample and write `${OBS_DIR}/final-verdict.json`.
    - Write `${OBS_DIR}/observation-log.json` summarizing: start_ts, end_ts, sample_count, breakout_sample (N or null), final_outcome.
    Anti-pattern to avoid: do NOT call `scripts/canary-check.sh` without `--json` — gate-eval consumes structured JSON.
    Anti-pattern to avoid: do NOT continue sampling after a rollback gate fires — that's the trigger.
    Anti-pattern to avoid: do NOT consider restart-window symptom samples as failures — gate-eval handles that distinction.
  </action>
  <verify>
    <automated>
      OBS=.planning/phases/224-production-canary-rollback-discipline/evidence/observation &amp;&amp;
      test -d "$OBS" &amp;&amp;
      test -f "$OBS/observation-log.json" &amp;&amp;
      ls "$OBS"/sample-*-health.json | wc -l | python3 -c "import sys; n=int(sys.stdin.read()); assert n &gt;= 1, f'no samples captured (got {n})'"
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/observation/observation-log.json` exists with `start_ts`, `end_ts`, `sample_count`, `final_outcome` fields.
    - At least one `sample-*-health.json` and matching `sample-*-spine.json` exist.
    - Either `final_outcome == "kept_aligned"` (Task 3 skipped) OR `breakout_sample` is non-null (Task 3 executes rollback).
    - No samples with `outcome == "rollback"` AND the loop continuing past them (loop must break immediately on rollback verdict).
  </acceptance_criteria>
  <done>
    Observation window completed; samples captured at documented cadence; verdict trail recorded; rollback signal (if any) is unambiguous.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compute final canary verdict</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json</files>
  <read_first>
    - .planning/phases/224-production-canary-rollback-discipline/evidence/observation/observation-log.json (Task 1 output)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/observation/final-verdict.json (Task 1 output)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-summary.json (snapshot anchor + version fields)
    - scripts/phase224-gate-eval.py (verdict schema)
  </read_first>
  <action>
    Assemble the final canary verdict at `evidence/verdict.json`. Read `observation/final-verdict.json` (or `observation/breakout-sample.json` if a rollback fired). Write `evidence/verdict.json` with:
    - `snapshot_anchor`: from deploy-summary.json.
    - `observation_start_ts`, `observation_end_ts`: from observation-log.json.
    - `sample_count`: from observation-log.json.
    - `pre_deploy_version`, `post_deploy_version`: from deploy-summary.json.
    - `outcome`: one of `kept_aligned` or `rollback`. (If observation-log final_outcome was `continue_observation` at window close, that's a logic error in Task 1 — treat as `rollback` and report).
    - `gates`: copy from the deciding sample's verdict file (the sample that established the final outcome).
    - `rollback_trigger`: from the deciding sample (gate id or null).
    - `restart_window_symptoms_observed`: count of samples where gate-eval emitted `restart_window_symptom` for any gate (informational; not a verdict).
    - `decision_artifact_signed`: true if `evidence/risk-acceptance-signed.redacted.md` exists (from Plan 03 Task 1).
    Use stdlib Python (per `scripts/phase219_ingestion_digest.py` precedent). Write as JSON, pretty-printed.
    Anti-pattern to avoid: do NOT fabricate a kept-aligned outcome if observation-log says otherwise. The verdict echoes the gate-eval, not the operator's preference.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json &amp;&amp;
      python3 -c "
import json
d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json'))
assert d['outcome'] in ('kept_aligned','rollback'), f'invalid outcome {d[\"outcome\"]}'
assert d['snapshot_anchor'], 'snapshot_anchor missing'
assert d['decision_artifact_signed'] is True, 'risk-acceptance not signed'
assert isinstance(d['restart_window_symptoms_observed'], int), 'restart_window_symptoms_observed missing'
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/verdict.json` exists with `outcome` ∈ `{kept_aligned, rollback}`.
    - `verdict.json` cites `snapshot_anchor` (the Plan 03 snapshot TS).
    - `verdict.json` includes `restart_window_symptoms_observed` (integer, may be 0).
    - `verdict.json` records `decision_artifact_signed: true`.
    - If `outcome == rollback`, `rollback_trigger` is non-null (a gate id).
    - If `outcome == kept_aligned`, all gate verdicts are `pass` (no fail, no restart_window_symptom at window close).
  </acceptance_criteria>
  <done>
    Final canary verdict written; outcome is one of two values; rollback trigger (if any) is named.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Execute rollback (only if Task 2 outcome == rollback)</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/rollback/</files>
  <what-built>
    Task 2 verdict.json says `outcome: rollback` with a named `rollback_trigger`. Operator must execute the rollback path within the ≤5-minute budget measured in Plan 01.
  </what-built>
  <how-to-verify>
    1. SKIP this task entirely if `evidence/verdict.json` `outcome == kept_aligned`.
    2. If `outcome == rollback`:
       - Read `snapshot_anchor` from verdict.json.
       - Execute: `scripts/phase224-rollback.sh --snapshot evidence/snapshot-a/<TS> --ssh-host cake-shaper --target-wan spectrum`.
       - Confirm script prints `ROLLBACK_DURATION_SECONDS=<n>` and `n <= 300`.
       - Confirm post-revert `scripts/canary-check.sh --ssh kevin@cake-shaper --expect-version <pre_deploy_version> --json` exits 0 PASS.
       - Write `evidence/rollback/rollback-summary.json` with: snapshot_anchor, rollback_started_ts, rollback_completed_ts, rollback_duration_seconds, post_revert_canary_check_exit_code, post_revert_health_version, gate_that_fired.
       - Write `evidence/rollback/post-revert-health.bound.redacted.json` from `curl http://10.10.110.223:9101/health`.
       - Write `evidence/rollback/post-revert-spine-probe.json` from `scripts/phase224-spine-probe.sh --ssh-host cake-shaper`.
       - Confirm post-revert spine probe shows all three matches true (production back to pre-Phase-224 state).
    3. Commit `evidence/rollback/` (with SKIP_DOC_CHECK).
  </how-to-verify>
  <action>
    BLOCKING HUMAN CHECKPOINT — conditional execution. If `evidence/verdict.json` `outcome == kept_aligned`, this task is a no-op and the operator types `kept-aligned`. If `outcome == rollback`, the operator reads `snapshot_anchor` and `rollback_trigger` from verdict.json, executes `scripts/phase224-rollback.sh --snapshot evidence/snapshot-a/<TS> --ssh-host cake-shaper --target-wan spectrum`, confirms `ROLLBACK_DURATION_SECONDS <= 300`, captures post-revert `/health` and spine-probe evidence under `evidence/rollback/`, writes `rollback-summary.json`, and commits with SKIP_DOC_CHECK. No probe-and-rollback automation runs without the operator at the keyboard for this gate.
  </action>
  <verify>
    <automated>
      python3 -c "
import json, os, sys
v=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json'))
if v['outcome']=='kept_aligned':
    sys.exit(0)
assert os.path.isfile('.planning/phases/224-production-canary-rollback-discipline/evidence/rollback/rollback-summary.json'), 'verdict says rollback but evidence/rollback/rollback-summary.json missing'
r=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/rollback/rollback-summary.json'))
assert r['rollback_duration_seconds'] &lt;= 300, f'rollback exceeded 5-minute budget: {r[\"rollback_duration_seconds\"]}'
assert r['post_revert_canary_check_exit_code'] == 0, 'post-revert canary-check did not PASS'
"
    </automated>
  </verify>
  <done>Either `verdict.outcome == kept_aligned` (no rollback needed), OR `evidence/rollback/rollback-summary.json` exists with duration ≤ 300s and post-revert canary-check exit 0.</done>
  <resume-signal>Type "rolled-back" with the rollback duration and post-revert /health.version, OR "kept-aligned" if Task 2 outcome did not require rollback.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observation samplers → /health + RouterOS | Read-only. Bound and loopback /health surfaces; REST/SSH router reads. |
| Gate-eval → sample JSON | Pure-function over JSON inputs. |
| Rollback → production host | Single mutation event under operator control; bounded to the snapshot restore + restart + verify sequence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-15 | Tampering | Observation loop accidentally writes to host | mitigate | All commands are `curl` reads + `ssh ... curl` (read-only). No `sudo`, no `systemctl`. Task 1 grep-gate excludes mutation verbs. |
| T-224-16 | Denial of service | Sampling cadence overloads /health endpoint | accept | Default 60-second cadence at 9101 is well below the health endpoint's documented capacity; no risk at this rate. |
| T-224-17 | Elevation of privilege | Rollback skips verification step | mitigate | Task 3 requires operator-typed resume signal naming both rollback_duration AND post-revert /health.version. |
| T-224-18 | Repudiation | Verdict.json fabricated by operator preference | mitigate | Task 2 reads gate-eval output verbatim; no operator-editable fields. Verdict.json schema is checked in CI for shape. |
| T-224-19 | Information disclosure | Sample JSONs contain router credentials | mitigate | Samples come from /health (no credentials surfaced) and from spine-probe (redacted by Plan 02 design). |
| T-224-SC | Supply chain | New observation loop introduces packages | accept | Stdlib + bash + curl + jq. No new packages. |
</threat_model>

<verification>
- ≥1 sample captured during observation window; observation-log.json shape valid.
- verdict.json outcome is one of two strings; rollback trigger is named when present.
- Restart-window symptoms count is reported (may be 0).
- Risk-acceptance signed = true (precondition from Plan 03).
- If rollback fired: rollback-summary.json shows duration ≤ 300s; post-revert /health version matches pre-deploy version; post-revert spine probe shows all matches true.
- If kept-aligned: all final-sample gates pass; sample count ≥ ceil(window_minutes / cadence_minutes).
</verification>

<success_criteria>
1. Operator can read evidence/verdict.json and know the canary outcome without ambiguity.
2. Restart-window symptoms are visible in the verdict trail but NEVER cause rollback (CANARY-03 governance via D-11/D-12).
3. If rollback fired, production is restored to pre-deploy state inside the measured ≤5-minute budget, with /health proof.
4. Snapshot anchor is cited end-to-end (Plan 03 → Plan 04 verdict → Plan 05 report).
</success_criteria>

<output>
Create `.planning/phases/224-production-canary-rollback-discipline/224-04-SUMMARY.md` when done, citing: outcome, sample count, window duration, restart-window symptoms observed, rollback duration (if applicable), post-revert /health version (if applicable).
</output>
