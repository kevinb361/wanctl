---
phase: 224-production-canary-rollback-discipline
plan: 05
type: execute
wave: 3
depends_on:
  - 224-04
files_modified:
  - .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json
  - .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.md
  - .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md
requirements:
  - CANARY-01
  - CANARY-02
  - CANARY-03
  - SAFE-12
autonomous: true
must_haves:
  truths:
    - "Operator can read `.planning/phases/224-production-canary-rollback-discipline/224-REPORT.md` and see the canary verdict (kept-aligned or rolled-back), snapshot anchor, gate verdicts, and (if applicable) rollback reason."
    - "Operator can verify SAFE-12 holds at Phase 224 boundary: zero controller-path source diff vs v1.47 close, with `evidence/safe12-boundary-check.json` showing `passed: true`."
    - "Operator can confirm SAFE-12 holds at v1.48 milestone close (re-run boundary check against the milestone delta), or hand off the milestone-close re-run as a documented follow-up if Plan 04 outcome was rollback (the milestone may not close cleanly under rollback)."
    - "Operator can locate the signed risk-acceptance decision artifact and confirm it governs the canary report's treatment of any observed restart-window symptoms."
  artifacts:
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json"
      provides: "SAFE-12 phase-boundary check (Phase 223 schema)"
      contains: "passed"
    - path: ".planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.md"
      provides: "Human-readable SAFE-12 boundary summary"
      contains: "controller-path"
    - path: ".planning/phases/224-production-canary-rollback-discipline/224-REPORT.md"
      provides: "Phase 224 canary report (Phase 215 REPORT format)"
      contains: "Verdict"
  key_links:
    - from: "224-REPORT.md"
      to: "evidence/verdict.json"
      via: "report cites verdict outcome"
      pattern: "verdict"
    - from: "224-REPORT.md"
      to: ".planning/decisions/phase-224-clean-restart-risk-acceptance.md"
      via: "report cites risk-acceptance governance"
      pattern: "risk-acceptance|clean-restart"
    - from: "evidence/safe12-boundary-check.json"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json"
      via: "schema reuse"
      pattern: "passed.*committed_clean.*steering_daemon_clean"
---

<objective>
Publish the Phase 224 canary report citing the snapshot anchor, gate verdicts, rollback reason
(if any), and the risk-acceptance decision artifact governance; produce the SAFE-12 phase-
boundary check using the Phase 223 schema (CANARY-01/02/03 + SAFE-12).

Purpose: Phase 215 established the canary report shape (Verdict, REQ closeout, Evidence Index,
Notes). Phase 223 established the SAFE-12 boundary check shape. Plan 05 produces both for
Phase 224 and closes the phase. If the v1.48 milestone is also closing in this commit (i.e.,
Plan 04 outcome was kept_aligned), Plan 05 also re-runs SAFE-12 against the v1.48 milestone
delta. If outcome was rollback, milestone close is a separate follow-up decision.

Output: 224-REPORT.md + evidence/safe12-boundary-check.{json,md}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/224-production-canary-rollback-discipline/224-CONTEXT.md
@.planning/phases/224-production-canary-rollback-discipline/224-RESEARCH.md
@.planning/phases/224-production-canary-rollback-discipline/224-03-PLAN.md
@.planning/phases/224-production-canary-rollback-discipline/224-04-PLAN.md
@.planning/decisions/phase-224-clean-restart-risk-acceptance.md
@.planning/milestones/v1.46-phases/215-spectrum-upload-reclaim-canary/215-REPORT.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json
@.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate SAFE-12 phase-boundary check</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json, .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.md</files>
  <read_first>
    - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json (schema: `passed`, `committed_clean`, `dirty_tree_clean`, `steering_daemon_clean`, `controller_path_paths`, `steering_paths`, allowlist)
    - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md (human summary shape)
    - .planning/REQUIREMENTS.md (SAFE-12 statement: controller-path source byte-identical to v1.47 close)
    - .planning/PROJECT.md (v1.47 close commit reference)
  </read_first>
  <action>
    Produce `evidence/safe12-boundary-check.json` and `evidence/safe12-boundary-check.md` using the Phase 223 schema, against the current HEAD. Steps:
    - Identify v1.47 close anchor: read PROJECT.md / STATE.md for the v1.47 close commit SHA. If a `v1.47-close` git tag exists (`git tag --list 'v1.47*'`), use it; otherwise use the last commit before the v1.48 milestone began (operator may need to provide the SHA — fall back to reading `.planning/milestones/v1.47-MILESTONE-AUDIT.md` for the close commit).
    - Run `git diff --name-only <v1.47-anchor> HEAD -- src/wanctl/wan_controller.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/backends/ src/wanctl/fusion/`. Result MUST be empty.
    - Run `git status --porcelain -- src/wanctl/wan_controller.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/backends/ src/wanctl/fusion/`. Result MUST be empty (no dirty tree on controller path).
    - Run the same check against `src/wanctl/steering/` (this IS in scope for mutation but must report cleanly — `steering_daemon_clean: true` means committed, no dirty unstaged/untracked).
    - Compute byte-by-byte sha256 of each controller-path file at HEAD vs at the v1.47 anchor; record per-file hash equality in the JSON.
    - Write JSON with keys mirroring Phase 223 schema:
      ```
      {
        "phase": "224",
        "checked_at": "<iso8601>",
        "v147_close_anchor": "<sha or tag>",
        "head_sha": "<sha>",
        "controller_path_paths": [...],
        "steering_paths": ["src/wanctl/steering/"],
        "passed": true|false,
        "committed_clean": true|false,
        "dirty_tree_clean": true|false,
        "steering_daemon_clean": true|false,
        "per_file_sha256_equal": { "<path>": true|false, ... },
        "allowlist_summary": "...",
        "notes": "..."
      }
      ```
    - Write Markdown sibling with the same fields in human-readable form, mirroring Phase 223's `safe12-boundary-check.md` section structure.
    - If `passed: false`, STOP — the phase cannot close cleanly. Surface the failing file(s) in the Markdown summary and exit non-zero.
    - If `passed: true` AND Plan 04 outcome was `kept_aligned`: ALSO compute the v1.48 milestone-close check (full milestone delta from v1.47 anchor to HEAD across the entire `src/wanctl/` tree, allowlist-aware). Write a `milestone_close_passed` field to the same JSON.
    - If Plan 04 outcome was `rollback`: skip the milestone-close re-run; note in JSON that `milestone_close_deferred: true` and that the v1.48 milestone-close decision is a follow-up.
    Anti-pattern to avoid: do NOT modify any controller-path file to "fix" SAFE-12 violations. The boundary check is the truth; if it fails, the phase fails and the offending change must be reverted (or escalated as a separate decision).
    Anti-pattern to avoid: do NOT use the Phase 222 schema if it differs from Phase 223 — Phase 223 is the more recent + complete schema.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json &amp;&amp;
      test -f .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.md &amp;&amp;
      python3 -c "
import json
d=json.load(open('.planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json'))
assert d['passed'] is True, f'SAFE-12 boundary check FAILED at phase 224 close: {d}'
assert d['committed_clean'] is True
assert d['dirty_tree_clean'] is True
assert d['steering_daemon_clean'] is True
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` exists with `passed: true`, `committed_clean: true`, `dirty_tree_clean: true`, `steering_daemon_clean: true`.
    - `evidence/safe12-boundary-check.md` exists with the same field values in human-readable form.
    - JSON includes a `per_file_sha256_equal` map showing each controller-path file's hash matches the v1.47 anchor.
    - JSON cites `v147_close_anchor` (commit SHA or tag).
    - If Plan 04 outcome was kept_aligned, JSON includes `milestone_close_passed` (bool).
    - If Plan 04 outcome was rollback, JSON includes `milestone_close_deferred: true`.
  </acceptance_criteria>
  <done>
    SAFE-12 boundary check produced; phase 224 holds against v1.47 close; v1.48 milestone close either verified or explicitly deferred per outcome.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 224-REPORT.md (canary report)</name>
  <files>.planning/phases/224-production-canary-rollback-discipline/224-REPORT.md</files>
  <read_first>
    - .planning/phases/224-production-canary-rollback-discipline/evidence/verdict.json (Plan 04 outcome)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/deploy/deploy-summary.json (Plan 03 deploy fields)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/observation/observation-log.json (window + samples)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/safe12-boundary-check.json (Task 1 output)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/rehearsal-budget.md (Plan 01 measured rollback budget)
    - .planning/phases/224-production-canary-rollback-discipline/evidence/risk-acceptance-signed.redacted.md (Plan 03 sign-off)
    - .planning/decisions/phase-224-clean-restart-risk-acceptance.md (governance reference)
    - .planning/milestones/v1.46-phases/215-spectrum-upload-reclaim-canary/215-REPORT.md (REPORT format precedent — Verdict, RECLAIM closeout, Evidence Index, Notes)
  </read_first>
  <action>
    Write `.planning/phases/224-production-canary-rollback-discipline/224-REPORT.md` following the Phase 215 format. Required sections:
    - `# Phase 224 Production Canary + Rollback Discipline Report`
    - `## Verdict` — one-line outcome (kept-aligned or rolled-back) with snapshot anchor TS, pre-deploy/post-deploy versions, rollback duration (if applicable). Single sentence.
    - `## CANARY Closeout`
      - `### CANARY-01 — Snapshot A rollback anchor`: snapshot TS, manifest path, measured rehearsal budget (from rehearsal-budget.md), confirmation of signed risk-acceptance.
      - `### CANARY-02 — Version alignment + contract invariant proof`: pre-deploy and post-deploy /health.version, leg-A and leg-B paths, spine.binary_on_off / only_new_connections / autorate_baseline match values from the final sample.
      - `### CANARY-03 — Rollback discipline`: gate verdicts at observation close (or at breakout sample if rollback fired), gate that fired (if any), measured rollback wall-clock duration vs budget (if rollback fired), post-revert /health proof (if rollback fired).
    - `## SAFE-12 Boundary` — cite `evidence/safe12-boundary-check.json` `passed`, `committed_clean`, `dirty_tree_clean`, `steering_daemon_clean`. State whether `milestone_close_passed` is true, false, or deferred.
    - `## Clean-Restart Window Governance` — cite `.planning/decisions/phase-224-clean-restart-risk-acceptance.md`, report the count of restart-window symptoms observed during the canary (from verdict.json `restart_window_symptoms_observed`), and confirm that none caused rollback per D-11/D-12.
    - `## Evidence Index` — list of evidence paths (snapshot-a/, leg-a-prealignment/, deploy/, leg-b-postalignment/, observation/, verdict.json, safe12-boundary-check.{json,md}, rollback/ if exists, risk-acceptance-signed.redacted.md, rehearsal-budget.md).
    - `## Notes` — any non-blocking observations: cadence chosen, observation window duration, version-bump decision (`__version__` bumped vs not), staging rehearsal hostname (redacted), any deviation from the planned defaults.
    The report MUST cite verdict.json verbatim — do NOT paraphrase the outcome. The report MUST NOT introduce new claims not backed by evidence in the phase directory.
    Anti-pattern to avoid: do NOT write a "lessons learned" section — Phase 215's report does not have one and Phase 224 follows that precedent. Lessons go in LEARNINGS.md after phase close via `/gsd:extract-learnings`.
    Anti-pattern to avoid: do NOT minimize a rollback outcome with hedging language. If rollback fired, the report cites the gate and the reason in plain language.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^## Verdict' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^### CANARY-01' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^### CANARY-02' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^### CANARY-03' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^## SAFE-12 Boundary' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q 'phase-224-clean-restart-risk-acceptance' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md &amp;&amp;
      grep -q '^## Evidence Index' .planning/phases/224-production-canary-rollback-discipline/224-REPORT.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `224-REPORT.md` exists with required section headers: `# Phase 224 ...`, `## Verdict`, `## CANARY Closeout`, `### CANARY-01`, `### CANARY-02`, `### CANARY-03`, `## SAFE-12 Boundary`, `## Clean-Restart Window Governance`, `## Evidence Index`, `## Notes`.
    - Report cites snapshot anchor TS literally (matching `evidence/snapshot-a/<TS>/`).
    - Report cites verdict.json outcome literally (no paraphrase).
    - Report cites `.planning/decisions/phase-224-clean-restart-risk-acceptance.md` by path.
    - Report cites measured rehearsal budget from `evidence/rehearsal-budget.md`.
    - If verdict.outcome == rollback, report names the firing gate.
    - SAFE-12 section reports `passed: true` from `safe12-boundary-check.json`.
  </acceptance_criteria>
  <done>
    Phase 224 canary report published, Phase 215 format, evidence-grounded, no hedging.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report writer → evidence directory | Read-only consumption of all upstream evidence. |
| SAFE-12 boundary check → git working tree | Read-only `git diff`, `git status`, `git tag` queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-20 | Repudiation | Report fabricates outcome inconsistent with verdict.json | mitigate | Task 2 verify gate cross-checks that verdict.outcome literal appears in the report. Acceptance criteria forbid paraphrase. |
| T-224-21 | Tampering | SAFE-12 boundary check skipped or weakened | mitigate | Task 1 verify gate asserts `passed`, `committed_clean`, `dirty_tree_clean`, `steering_daemon_clean` all true. Failure halts the phase. |
| T-224-22 | Information disclosure | Report includes raw `/health` payloads with credentials | mitigate | Report cites paths to redacted artifacts; never inlines raw /health JSON. Acceptance criteria forbid inline JSON. |
| T-224-23 | Elevation of privilege | Phase close skips clean-restart governance section | mitigate | Task 2 verify gate asserts the section heading and the decision-artifact citation. |
| T-224-SC | Supply chain | Report introduces new packages | accept | Report is markdown. SAFE-12 check is stdlib + git. No new packages. |
</threat_model>

<verification>
- SAFE-12 boundary check passes (passed: true).
- 224-REPORT.md exists with all required sections.
- Report outcome matches verdict.json outcome verbatim.
- Clean-restart governance section cites the decision artifact and reports the symptom count.
- Evidence Index lists all evidence paths produced across Plans 01-04.
- If milestone-close re-run was performed (kept-aligned outcome), milestone_close_passed is recorded in safe12-boundary-check.json AND surfaced in 224-REPORT.md SAFE-12 Boundary section.
</verification>

<success_criteria>
1. Operator reads 224-REPORT.md and immediately knows: outcome, snapshot anchor, gate verdicts, rollback reason (if any), SAFE-12 status.
2. SAFE-12 holds at phase boundary — zero controller-path diff vs v1.47 close.
3. Clean-restart symptom count is reported but NEVER cited as a rollback reason.
4. Phase 224 is closeable: verification phase can run against this report and the upstream evidence.
</success_criteria>

<output>
Create `.planning/phases/224-production-canary-rollback-discipline/224-05-SUMMARY.md` when done, citing: report path, SAFE-12 passed boolean, milestone_close_passed or _deferred, restart-window symptom count, final outcome.
</output>
