---
phase: 258-read-only-routeros-access-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md
autonomous: true
requirements: [ACCESS-01]
must_haves:
  truths:
    - "The v1.56 not-ready blocker is documented as TWO distinct broken layers, not one"
    - "The daemon-path failure (REST has no /tool netwatch handler -> guard.status=error) is documented as the real inspection blocker"
    - "The manual-evidence-path failure (nested-SSH router.key inaccessible -> returncode 255) is documented as a parallel, now-avoidable surface"
    - "The chosen supported path (keep REST, add read-only netwatch handler) is stated, with the dropped SSH-key repair noted per D1"
  artifacts:
    - path: .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md
      provides: "ACCESS-01 root-cause writeup covering both failure layers + chosen path"
      contains: "netwatch"
      min_lines: 30
  key_links:
    - from: "258-01-root-cause.md"
      to: "src/wanctl/routeros_rest.py (missing /tool netwatch handler)"
      via: "documented daemon-path root cause"
      pattern: "_handle_netwatch_print|tool netwatch|Unsupported command"
    - from: "258-01-root-cause.md"
      to: "/etc/wanctl/ssh/router.key"
      via: "documented manual-evidence-path root cause"
      pattern: "router.key|returncode 255|255"
---

<objective>
Document the root cause of the v1.56 not-ready RouterOS-inspection blocker (ACCESS-01). The
research is unambiguous: the failure is TWO independent broken layers, and a writeup that names
only `router.key` would leave the real daemon-path blocker undocumented.

Purpose: ACCESS-01 requires a documented root cause covering presence, path, ownership,
permissions, and the service user for the inspection credential — AND the distinct daemon-path
failure that `router.key` repair alone would NOT fix. This writeup is the source of truth that
justifies the chosen supported path (keep REST, add a read-only netwatch handler) and the dropped
SSH-key repair line per D1.

Output: `.planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/258-read-only-routeros-access-repair/258-CONTEXT.md
@.planning/phases/258-read-only-routeros-access-repair/258-RESEARCH.md

<interfaces>
<!-- The two failure layers the writeup must document. Extracted from RESEARCH + 257 evidence. -->
<!-- Layer (a) DAEMON PATH — the real inspection blocker:
       src/wanctl/routeros_rest.py::_execute_single_command (lines 260-296) dispatches
       /ip route print -> _handle_route_print (works), but has NO /tool netwatch branch.
       A "/tool netwatch print detail" falls through to:
         self.logger.warning(f"Unsupported command for REST API: {cmd}"); return None
       -> run_cmd returns (1, "", "Command failed")
       -> RouteOwnershipGuard fails closed with status="error".
     This is the exact 257 readiness-packet failure: guard.status=error,
     "failed to read RouterOS netwatch: Command failed".

     Layer (b) MANUAL-EVIDENCE PATH — a parallel, now-avoidable surface:
       The Phase 257 operator evidence script used a nested SSH command:
         sudo -n ssh -i /etc/wanctl/ssh/router.key ... admin@10.10.99.1 "/tool netwatch print detail"
       which failed returncode=255 because /etc/wanctl/ssh/router.key was inaccessible on
       cake-shaper. This is NOT how the daemon talks to RouterOS. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two-layer ACCESS-01 root-cause writeup</name>
  <read_first>
    - .planning/phases/258-read-only-routeros-access-repair/258-RESEARCH.md (Summary, Pitfall 1, Architectural Responsibility Map, Open Questions operator commands)
    - .planning/phases/258-read-only-routeros-access-repair/258-CONTEXT.md (D1-D4 locked decisions)
    - .planning/milestones/v1.56-phases/257-dry-run-observation-canary-readiness-decision/257-01-SUMMARY.md (the not-ready blocker record: guard.status=error, router.key returncode 255)
    - src/wanctl/routeros_rest.py (lines 260-296 — the dispatch fall-through that rejects /tool netwatch)
  </read_first>
  <action>
    Create `.planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md`.
    Document BOTH failure layers per RESEARCH Pitfall 1 (the ACCESS-01 trap is documenting only
    the key). Required sections:
    (1) Daemon path (the real inspection blocker): steering uses REST (transport: rest,
    10.10.99.1:443 per configs/steering.yaml); routeros_rest._execute_single_command has no
    /tool netwatch branch; the command falls through to "Unsupported command for REST API" -> None
    -> run_cmd rc=1 -> RouteOwnershipGuard status=error. Cite routeros_rest.py lines 260-296 and the
    257 guard.status=error packet line.
    (2) Manual-evidence path (parallel, now-avoidable): the 257 operator nested-SSH command using
    /etc/wanctl/ssh/router.key failed returncode 255 because the key was inaccessible on cake-shaper.
    Record the ACCESS-01 credential facts: path /etc/wanctl/ssh/router.key, and the
    presence/owner/perms/service-user fields. Where a fact requires a privileged live read on
    cake-shaper, mark it [OPERATOR-VERIFY] and reference the read-only `! <command>` from RESEARCH
    Open Questions block (3) — do NOT attempt the read yourself.
    (3) Chosen supported path + dropped work: state that the resolution keeps REST and adds a
    read-only `_handle_netwatch_print` GET handler (Plan 02), and that per D1 the router.key SSH
    repair is DROPPED (router.key remains a configured SSH-fallback credential but is not the
    inspection path). Note that the netwatch handler is what fixes the daemon-path blocker; a
    router.key chmod/chown alone would NOT.
    Do NOT include fenced shell code that mutates anything; all referenced operator commands are
    read-only `print`/GET. Per SAFE-21, no diagnosis step mutates RouterOS or restarts steering.service.
  </action>
  <verify>
    <automated>test -f .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md && grep -qi "netwatch" .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md && grep -Eqi "router\.key|returncode 255|255" .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md && grep -Eqi "Unsupported command|_handle_netwatch_print|no .*netwatch.*handler" .planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/258-read-only-routeros-access-repair/evidence/258-01-root-cause.md` exists, >= 30 lines.
    - Documents the daemon-path failure (REST has no /tool netwatch handler -> guard.status=error) AS the real inspection blocker.
    - Documents the manual-evidence-path failure (router.key inaccessible -> returncode 255) as a parallel, now-avoidable surface.
    - Records ACCESS-01 credential facts (path + presence/owner/perms/service-user), with live-only facts marked [OPERATOR-VERIFY] tied to a read-only `! <command>`.
    - States the chosen path (keep REST + add netwatch handler) and the dropped router.key repair per D1.
    - No mutating command appears; no steering.service restart referenced.
  </acceptance_criteria>
  <done>
    ACCESS-01 root-cause writeup exists documenting both failure layers, the chosen supported path,
    and the dropped SSH-key repair, with live-only facts flagged for operator verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cake-shaper -> RouterOS (10.10.99.1) | inspection host crosses to the production router; this plan only documents, does not connect |
| operator keyboard -> cake-shaper | privileged credential/file reads cross here (`! <command>`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-01 | Information disclosure | router.key / ROUTER_PASSWORD facts in the writeup | mitigate | Writeup records presence/owner/perms/service-user only; never the key material or password value. Live reads are operator-run read-only `! <command>` per D3. |
| T-258-02 | Tampering | RouterOS state during diagnosis | accept | This plan writes only a Markdown doc; it issues no RouterOS command. SAFE-21 held by construction. |
| T-258-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_routeros_rest.py -q` exits 0 (no regression; this plan adds no code).
- Root-cause doc present and covers both failure layers (grep gate above).
</verification>

<success_criteria>
ACCESS-01 satisfied: the inaccessible-inspection-path root cause is documented as two distinct
layers (daemon REST netwatch-handler gap + manual router.key inaccessibility), with the chosen
supported path and the dropped SSH-key repair recorded, and live-only facts flagged for the operator.
</success_criteria>

<output>
Create `.planning/phases/258-read-only-routeros-access-repair/258-01-SUMMARY.md` when done.
</output>
