---
phase: 241-fping-backend-offline-reflector-quality
plan: 03
type: execute
wave: 2
depends_on: ["241-01"]
files_modified:
  - scripts/capture-fping-fixtures.sh
  - tests/fixtures/fping/reply.txt
  - tests/fixtures/fping/total_loss.txt
  - tests/fixtures/fping/partial_loss.txt
  - tests/fixtures/fping/partial_line.txt
  - tests/fixtures/fping/banner_noise.txt
  - tests/fixtures/fping/process_death.txt
  - tests/test_fping_measurement.py
autonomous: false
requirements: [FPING-04, REFL-01]

must_haves:
  truths:
    - "A capture helper ships that the operator runs on the live host to capture real fping 5.1 output for the six D-03 scenarios"
    - "The capture command is byte-identical to FpingMeasurement._build_command, proven MECHANICALLY (not by grep): the helper has a --print-command mode whose output is compared against a Python call to FpingMeasurement._build_command() (resolves review M3)"
    - "The capture helper is non-mutating to production routing/shaping (D-08 safety)"
    - "Each fixture carries metadata (fping version, exact command, stdout/stderr split, returncode) so tests can reconstruct a real CompletedProcess(stdout=..., stderr=..., returncode=...) (resolves review M3)"
    - "Tests feed the parser via a simulated real CompletedProcess with the captured stdout/stderr/returncode split, not bare stdout text (resolves review M3 + ties to Plan 01 H2 combined-stream parse)"
    - "Committed real fping 5.1 captures replace the bootstrap fixtures and the parser/scorer tests pass against the real captures"
  artifacts:
    - path: "scripts/capture-fping-fixtures.sh"
      provides: "operator-run live-host capture helper for the six D-03 scenarios, with a --print-command mechanical-check mode"
      contains: "print-command"
    - path: "tests/fixtures/fping/partial_loss.txt"
      provides: "real operator-captured fping 5.1 partial-loss sample with metadata header (binding proof)"
  key_links:
    - from: "scripts/capture-fping-fixtures.sh"
      to: "src/wanctl/fping_measurement.py::_build_command"
      via: "byte-identical fping invocation proven via --print-command vs _build_command()"
      pattern: "_build_command"
---

<objective>
Ship the D-08 operator-run capture helper, have the operator (Kevin) run it on the
LIVE host to capture real fping 5.1 output for the six D-03 scenarios, commit those
real captures (with stdout/stderr/returncode/version metadata) as the binding test
fixtures (replacing Plan 01's synthetic bootstraps), prove the capture command is
byte-identical to `_build_command` MECHANICALLY, and re-confirm the parser + scorer
tests pass against real output fed through a simulated real CompletedProcess.

This is an OPERATOR-IN-THE-LOOP plan: fping is absent on the dev VM, and D-08 requires
REAL 5.1 captures as the binding FPING-04 proof. The capture step is a blocking
human-action checkpoint — do not silently skip it.

Purpose: convert the FPING-04 proof from synthetic to real captured 5.1 samples (D-08)
with a mechanically-verified byte-identical command and stdout/stderr-aware tests.
Output: `scripts/capture-fping-fixtures.sh`, six real fixtures with metadata, re-pointed
tests that feed real CompletedProcess shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-CONTEXT.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-PATTERNS.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md
@src/wanctl/fping_measurement.py

<interfaces>
<!-- The capture command MUST equal FpingMeasurement._build_command (Pitfall 6 / review M3). -->
Default invocation (from Plan 01 builder, -D OMITTED per OQ3):
  fping -C 5 -p 200 -q [-S <source_ip>] <reflector1> <reflector2> ...
Mechanical byte-identity check (M3): the script's --print-command output must equal
  `python -c "...FpingMeasurement(cfg, log)._build_command(hosts)..."` for the same
  source_ip + hosts (compare the argv lists, not a grep).
The six D-03 scenarios + safe operator capture methods (RESEARCH fixture table):
  reply         : normal reflectors                      -> all floats, loss 0.0
  total_loss    : TEST-NET blackhole 192.0.2.1           -> all '-', loss 100.0
  partial_loss  : distant/lossy target or tc-induced drop-> mix, loss = dash/C*100
  partial_line  : truncated/interrupted stdout           -> tolerate, never 0ms
  banner_noise  : stderr/ICMP-error interleaving         -> ignore non-host lines (note: a
                  VALID host line may land on stderr — capture both streams so Plan 01's
                  combined-stream parse (H2) is exercised against real output)
  process_death : mid-burst kill/SIGTERM                 -> short read + negative returncode -> probe None
Fixture metadata to record per scenario (M3): fping version, exact command (from
  --print-command), the stdout text, the stderr text, and the process returncode — so a
  test can rebuild CompletedProcess(args=..., stdout=..., stderr=..., returncode=...).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: capture-fping-fixtures.sh operator helper (D-08, mechanically byte-identical command, non-mutating)</name>
  <read_first>
    - src/wanctl/fping_measurement.py  (_build_command from Plan 01 — the capture script MUST build the identical fping invocation; OMIT -D per OQ3; the --print-command check compares against this)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (six D-03 fixture scenarios table; Pitfall 6 capture-vs-runtime drift; capture-script loss-induction discretion — blackhole/tc/kill, safe and non-mutating)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-PATTERNS.md  (capture-fping-fixtures.sh section — operator-run, safe/non-mutating to production routing)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md  (Plan 03 MEDIUM M3 — add --print-command and compare to _build_command(); store stdout/stderr/returncode/version metadata in fixtures)
    - scripts/phase240-safe17-boundary-check.sh  (bash conventions reference: set -euo pipefail, usage(), require_command — match house style)
  </read_first>
  <action>
    Create `scripts/capture-fping-fixtures.sh`, an operator-run helper for the live host
    (`set -euo pipefail`, `usage()`, `require_command fping`). It captures the six D-03
    scenarios into `tests/fixtures/fping/<scenario>.txt`, building the fping command
    byte-identically to `FpingMeasurement._build_command` (count=5, period_ms=200, `-q`,
    optional `-S <source_ip>` from a flag/env). Provide a `--source-ip`, `--reflectors`,
    `--out-dir` (default `tests/fixtures/fping`), and a `--print-command` interface.

    MECHANICAL byte-identity (review M3): add a `--print-command` mode that prints the
    exact fping argv the script would run (one shell-quoted line, OR space-joined argv)
    WITHOUT executing it. This is the audit handle the test in Task 3 compares against a
    Python call to `FpingMeasurement(...)._build_command(hosts)`. Keep `_build_command` in
    fping_measurement.py the single source of truth; the script mirrors it and
    `--print-command` makes the mirror checkable without running fping.

    Per-scenario capture (operator-safe, NON-MUTATING to production routing/shaping —
    D-08; this runs on a production host so it MUST NOT change routes, CAKE, or shaping).
    For EACH scenario capture and record into the fixture (M3 metadata): the fping
    version (`fping --version`), the exact command (from --print-command), the stdout, the
    stderr (kept SEPARATE — do not merge at capture time), and the process returncode:
    - reply: run against the configured live reflectors.
    - total_loss: run against TEST-NET blackhole `192.0.2.1` (no route mutation).
    - partial_loss: run against a deliberately distant/lossy reflector the operator
      supplies via flag (do NOT add tc/netem automatically — if tc is used, the operator
      applies and reverts it manually and is told so in the help text; the script itself
      mutates nothing).
    - partial_line: capture then truncate the saved stdout mid-line (post-processing on
      the captured file, not a routing change).
    - banner_noise: induce ICMP-error/banner output (e.g. an unreachable host that emits
      stderr) and capture stderr+stdout SEPARATELY (so a valid host line landing on stderr
      is preserved for Plan 01's combined-stream parse).
    - process_death: start the burst and send SIGTERM mid-burst (`kill` the fping PID),
      saving the short read AND the negative returncode.
    Print a clear preflight banner stating the script does not mutate routing/CAKE/shaping
    and that any tc/netem is the operator's manual responsibility. Record the metadata in a
    structured header in each fixture (e.g. a `# fping-version:`, `# command:`,
    `# returncode:`, and a `# --- stdout ---` / `# --- stderr ---` delimiter, or a
    sidecar `<scenario>.meta.json`) so Task 3 can reconstruct the CompletedProcess. The
    script must be safe to abort at any point.

    Do NOT run the capture in this task (fping is absent on the dev VM and it targets the
    live host). Verify only that the script exists, is executable, passes `bash -n`
    syntax check, that `--print-command` works without fping present, and that the printed
    command matches `_build_command`'s flags.
  </action>
  <verify>
    <automated>bash -n scripts/capture-fping-fixtures.sh && test -x scripts/capture-fping-fixtures.sh && grep -q "print-command" scripts/capture-fping-fixtures.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-fping-fixtures.sh` exists, is executable, passes `bash -n`
    - the script supports `--print-command` and printing the command does NOT require fping installed (the dev VM lacks fping): `grep -q "print-command" scripts/capture-fping-fixtures.sh`
    - the printed command matches `_build_command`'s flags: `./scripts/capture-fping-fixtures.sh --print-command --reflectors "1.1.1.1 8.8.8.8"` emits a line matching `fping -C 5 -p 200 -q` followed by the reflectors (count/period byte-identical, OMITs -D)
    - the script records stdout/stderr/returncode/version metadata per fixture (`grep -Eqi 'returncode|stderr|version' scripts/capture-fping-fixtures.sh`)
    - `grep -ci "mutat" scripts/capture-fping-fixtures.sh` >= 1 AND help text states no routing/CAKE/shaping mutation (operator-safety banner present)
    - the script does NOT itself call tc/route mutation: `grep -Ev '^[[:space:]]*#' scripts/capture-fping-fixtures.sh | grep -Eqc '(tc qdisc|ip route (add|del)|ip rule (add|del))'` returns 0 matches
  </acceptance_criteria>
  <done>An operator can run a safe, non-mutating helper on the live host that captures all six D-03 scenarios with a command MECHANICALLY proven byte-identical to the runtime backend invocation (via --print-command), recording stdout/stderr/returncode/version metadata per fixture.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: OPERATOR — capture real fping 5.1 fixtures on the live host (D-08)</name>
  <what-built>`scripts/capture-fping-fixtures.sh` (Task 1) — a non-mutating capture helper that produces the six D-03 fixtures (with stdout/stderr/returncode/version metadata) using the byte-identical runtime fping invocation.</what-built>
  <how-to-verify>
    fping is absent on the dev VM; this MUST run on the live host where fping 5.1 is
    installed. Operator steps:
    1. Copy/run `scripts/capture-fping-fixtures.sh` on the live host (the cake-autorate
       host) with the real reflectors and the WAN source IP, e.g.:
       `./scripts/capture-fping-fixtures.sh --source-ip <wan_src_ip> --reflectors "<r1> <r2> <r3>"`
    2. Confirm the preflight banner states NO routing/CAKE/shaping mutation, then proceed.
    3. Confirm all six files are produced under `tests/fixtures/fping/`:
       reply.txt, total_loss.txt, partial_loss.txt, partial_line.txt, banner_noise.txt,
       process_death.txt — each containing REAL fping 5.1 output AND the metadata header
       (fping version, exact command, returncode, stdout/stderr split).
    4. Bring the six captured files (and any sidecar .meta.json) back into the repo
       (replacing the Plan 01 synthetic bootstraps) and confirm `fping --version` reported
       5.1 (or note the actual version recorded in the metadata).
    5. Confirm production routing/CAKE/shaping is unchanged after the run (e.g. the
       controller/bridge health endpoint is still healthy).
  </how-to-verify>
  <resume-signal>Type "captured" with the fping version, or describe what failed / what to adjust in the capture script.</resume-signal>
  <acceptance_criteria>
    - all six `tests/fixtures/fping/*.txt` contain real operator-captured fping output (not the synthetic bootstrap header)
    - each fixture carries the metadata header (fping version, exact command, returncode, stdout/stderr split)
    - the captured invocation matches `_build_command` (command recorded in each fixture header)
    - operator confirms production routing/CAKE/shaping unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-point parser + scorer tests to real captures via simulated CompletedProcess; mechanical command check; full-suite green (FPING-04, REFL-01)</name>
  <read_first>
    - tests/test_fping_measurement.py  (the Plan 01 tests — confirm they read fixtures from tests/fixtures/fping/; adjust assertions that hard-coded a synthetic value; switch any bare-stdout patch to a real CompletedProcess(stdout, stderr, returncode))
    - tests/fixtures/fping/*.txt  (the now-real captures + metadata from Task 2)
    - src/wanctl/fping_measurement.py  (_build_command — the test compares the capture script's --print-command against this)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (Validation Architecture — the FPING-04 test map and the keystone dash-never-zero assertion)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md  (Plan 03 M3 — feed CompletedProcess(stdout, stderr, returncode); mechanical _build_command vs --print-command check)
  </read_first>
  <action>
    With the real fixtures + metadata in place, re-confirm and tighten the parser + scorer
    tests in `tests/test_fping_measurement.py` against REAL fping 5.1 output:
    - Parse each fixture's metadata into a real
      `subprocess.CompletedProcess(args=[...], stdout=<captured stdout>, stderr=<captured
      stderr>, returncode=<captured rc>)` and patch `subprocess.run` to return it (M3) —
      do NOT pass fixture text as bare stdout. This exercises Plan 01's combined-stream
      parse (H2) and the negative-returncode process-death gate (banner_noise stderr,
      process_death negative rc) against real output.
    - Add `test_capture_command_matches_build_command` (M3 mechanical check): invoke
      `scripts/capture-fping-fixtures.sh --print-command --source-ip <ip> --reflectors
      "<h1> <h2>"` via subprocess and assert its argv equals
      `FpingMeasurement({"source_ip": <ip>, ...}, logger)._build_command(["<h1>","<h2>"])`.
      This replaces the grep-only byte-identity assertion.
    - Adjust any assertion tuned to a synthetic exact RTT value so it asserts
      shape/behavior (loss-safe parse, dash-never-zero, total-loss→None+100.0, all-fail→None
      AFTER scorer feed, scorer-feed any-loss→fail) rather than a brittle synthetic constant.
    - Keep the keystone `test_dash_token_never_zero` against the real `partial_loss.txt`.
    - Keep `test_all_fail_feeds_scorer` (Plan 01 H1) running against the real
      `total_loss.txt` (returncode 1).
    - Confirm the six-scenario coverage maps to the six real fixtures.
    - Remove or rewrite the SYNTHETIC-BOOTSTRAP header comments so the committed files are
      the real captured samples + metadata (the binding proof, D-08).
    If the real 5.1 output reveals a format detail the parser missed (e.g. an unexpected
    token, a `-D` prefix the operator left on, host lines on stderr, banner shape), fix
    `_parse_fping` / `_parse_target_line` in `src/wanctl/fping_measurement.py` — but ONLY
    within that allowlisted module, never touching a frozen/non-allowlisted file. Editing
    fping_measurement.py again here is allowed (it is in the 241 allowlist).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_fping_measurement.py -q && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests feed a real `CompletedProcess(args, stdout, stderr, returncode)` (not bare stdout): `grep -n "CompletedProcess" tests/test_fping_measurement.py` shows stderr+returncode passed
    - `tests/test_fping_measurement.py::test_capture_command_matches_build_command` exits 0 (mechanical --print-command vs _build_command() equality)
    - `tests/test_fping_measurement.py::test_dash_token_never_zero` exits 0 against the REAL partial_loss.txt
    - `tests/test_fping_measurement.py::test_all_fail_feeds_scorer` exits 0 against the REAL total_loss.txt
    - no fixture file still contains a "SYNTHETIC BOOTSTRAP" marker: `grep -rci "synthetic bootstrap" tests/fixtures/fping/` returns 0
    - full suite `.venv/bin/pytest tests/ -q` exits 0
    - `.venv/bin/ruff check src/ tests/` + `.venv/bin/mypy src/wanctl/` clean
  </acceptance_criteria>
  <done>The committed fixtures are real operator-captured fping 5.1 samples with stdout/stderr/returncode/version metadata; tests feed a real CompletedProcess shape; the capture command is mechanically proven equal to _build_command via --print-command; the FPING-04 keystone, all-fail-scorer, and six-scenario tests pass against the real captures; the full suite is green. D-08's "captured real 5.1 samples" proof is genuine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator capture script → production host | the helper runs on a live production host and MUST NOT mutate routing/CAKE/shaping (D-08) |
| captured stdout+stderr → committed fixture | real subprocess output (both streams + returncode) becomes the binding parser ground truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-08 | Tampering/DoS | capture script on live host | mitigate | script itself performs NO routing/CAKE/tc mutation (acceptance criterion greps for `tc qdisc`/`ip route add|del`/`ip rule add|del` and requires zero matches); operator-safety banner; total-loss uses TEST-NET blackhole, not a route change; any tc/netem is the operator's manual, reverted responsibility. |
| T-241-09 | Tampering | capture-vs-runtime command drift (Pitfall 6) | mitigate | capture command MECHANICALLY proven byte-identical to `_build_command` via `--print-command` vs a Python `_build_command()` call (M3); fixture header records the exact command for audit. |
| T-241-10 | Spoofing | synthetic fixtures masquerading as real proof | mitigate | Task 3 requires the SYNTHETIC-BOOTSTRAP markers be gone; metadata records fping version + returncode + stdout/stderr split; the committed files are the binding D-08 captures. |
| T-241-SC | Tampering | npm/pip/cargo installs | N/A | no language-package install; bash helper + fixtures + tests only. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/ -q` green against the real captures (tests fed via real CompletedProcess)
- `bash -n scripts/capture-fping-fixtures.sh` clean; `--print-command` runs without fping; helper performs zero routing/CAKE mutation
- mechanical: capture `--print-command` equals `_build_command()` (test-proven)
- operator-confirmed: production routing/CAKE/shaping unchanged after capture; fping version recorded in fixture metadata
- `git diff --stat -- tests/fixtures/fping/` shows the six real captures committed
</verification>

<success_criteria>
The FPING-04 proof is genuine: six real operator-captured fping 5.1 fixtures (with
stdout/stderr/returncode/version metadata) are committed, the parser + scorer tests
(incl. the dash-never-zero keystone and the all-fail-scorer proof) pass against them fed
through a real CompletedProcess shape, the capture helper is non-mutating and
mechanically byte-identical to the runtime invocation, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/241-fping-backend-offline-reflector-quality/241-03-SUMMARY.md` when done.
</output>
