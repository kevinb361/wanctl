---
phase: 241-fping-backend-offline-reflector-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase241-safe17-boundary-check.sh
  - tests/test_phase241_safe17_verifier.py
  - src/wanctl/check_config_validators.py
  - tests/test_check_config_validators_fping.py
autonomous: true
requirements: [SAFE-17, FPING-01]

must_haves:
  truths:
    - "phase241-safe17-boundary-check.sh expands the allowlist to add fping_measurement.py and reflector_scorer.py"
    - "The verifier still fails closed on a dirty/staged/untracked src/wanctl tree"
    - "The verifier still fails closed on out-of-allowlist drift, protected-body drift, and RTT-seam drift since Phase 239"
    - "Light range validators reject fping count/period/cadence/threshold typos (OQ1) additively, without failing absent keys"
  artifacts:
    - path: "scripts/phase241-safe17-boundary-check.sh"
      provides: "expanded-allowlist fail-closed SAFE-17 boundary verifier for Phase 241"
      contains: "fping_measurement"
    - path: "tests/test_phase241_safe17_verifier.py"
      provides: "verifier behavior test mirroring test_phase240_safe17_verifier.py"
      contains: "def test_static_phase241_script_contract"
    - path: "src/wanctl/check_config_validators.py"
      provides: "additive light fping sub-param range validators"
  key_links:
    - from: "scripts/phase241-safe17-boundary-check.sh"
      to: "scripts/phase239-protected-body-diff.py"
      via: "protected-body layer invocation (reused unchanged)"
      pattern: "phase239-protected-body-diff\\.py"
---

<objective>
Ship the Phase 241 SAFE-17 boundary verifier as an allowlist-extended clone of the
Phase 240 script: add `fping_measurement.py` (the new backend) and `reflector_scorer.py`
(the accepted REFL-01 exception) to the path allowlist, keep every other fail-closed
layer intact, and mirror its behavior test. Also resolve Open Question 1 by adding
LIGHT additive range validators for the new fping sub-params in the already-allowlisted
`check_config_validators.py`.

This plan does NOT run the boundary gate — that is Plan 04 (the `[BLOCKING]` gate after
all edits are committed). This plan only BUILDS the verifier + validators.

Purpose: SAFE-17 tooling + operator-typo safety for the fping config knobs.
Output: `scripts/phase241-safe17-boundary-check.sh`,
`tests/test_phase241_safe17_verifier.py`, additive validators in
`check_config_validators.py` + their test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-PATTERNS.md

<interfaces>
<!-- Anchors verified against the live tree (2026-06-15) -->
Phase 239 close anchor (RTT-seam no-drift layer): 03c82de0
SAFE-17 anchor for controller-path diff: v1.52
Phase 240 allowlist regex (the line to extend):
  V153_ALLOWLIST_RE='^src/wanctl/(rtt_backend\.py|rtt_measurement\.py|check_config_validators\.py|check_steering_validators\.py)$'
Phase 241 must ADD to the alternation: fping_measurement\.py AND reflector_scorer\.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase241-safe17-boundary-check.sh — expanded-allowlist clone + mirror test (SAFE-17)</name>
  <read_first>
    - scripts/phase240-safe17-boundary-check.sh  (FULL — copy verbatim; the ONLY changes are the allowlist regex, the OUT/ALLOWED_OUT_PREFIX evidence paths → phase 241 dir, the Python `allowed_paths` set, and the human-readable allowed-files echo)
    - scripts/phase239-protected-body-diff.py  (read the PROTECTED dict + CLI — reused UNCHANGED by the new script; do NOT edit it)
    - tests/test_phase240_safe17_verifier.py  (FULL — mirror structure: static contract test, passes-at-boundary, fails-on-out-of-allowlist, fails-on-dirty, fails-on-protected-body-drift, fails-on-rtt-seam-drift; detached_worktree fixture)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (SAFE-17 Boundary Discipline section; A3/A4 — confirm fping_measurement.py + reflector_scorer.py are the correct additions and rtt seam stays byte-unchanged)
  </read_first>
  <action>
    Create `scripts/phase241-safe17-boundary-check.sh` by copying
    `scripts/phase240-safe17-boundary-check.sh` and changing ONLY:
    - `V153_ALLOWLIST_RE` → add `fping_measurement\.py` and `reflector_scorer\.py` to
      the alternation:
      `^src/wanctl/(rtt_backend\.py|rtt_measurement\.py|check_config_validators\.py|check_steering_validators\.py|fping_measurement\.py|reflector_scorer\.py)$`
    - `OUT` default →
      `.planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json`
    - `ALLOWED_OUT_PREFIX` →
      `.planning/phases/241-fping-backend-offline-reflector-quality/evidence/`
    - the Python `allowed_paths` set → add
      `"src/wanctl/fping_measurement.py"` and `"src/wanctl/reflector_scorer.py"`
    - the human-readable "Allowed ... src/wanctl files" echo string → include the two
      new files.
    Keep ALL other layers byte-for-byte: fail-closed-on-dirty-tree, the
    no-RTT-seam-drift-since-Phase-239 layer keyed on `rtt_backend.py`+`rtt_measurement.py`
    (passes as-is because 241 leaves both byte-unchanged), the
    `phase239-protected-body-diff.py` protected-body layer, and the JSON evidence emit.
    Keep `PHASE239_CLOSE_ANCHOR="03c82de0"`. Keep `--out` path-confinement guards
    (retarget the evidence prefix). `chmod +x` the new script.

    NOTE on `reflector_scorer.py`: it is in the path allowlist for membership/safety but
    is BYTE-UNCHANGED this phase (the REFL-01 feed lives in fping_measurement.py). Its
    presence in the allowlist means the verifier will not flag it if a future plan must
    touch it — but Plan 04 should confirm it shows zero diff.

    Create `tests/test_phase241_safe17_verifier.py` mirroring
    `tests/test_phase240_safe17_verifier.py`: update `VERIFIER` path, evidence path, and
    `ALLOWED_SRC_PATHS` (add the two new files); the static-contract test asserts the
    expanded regex contains `fping_measurement\.py` and `reflector_scorer\.py`; reuse the
    detached_worktree negative tests (out-of-allowlist drift on `wan_controller.py`
    rejected; dirty-tree rejected; protected-body drift rejected; rtt-seam drift rejected).
    Do NOT include the "passes-at-boundary" green assertion in THIS plan if the fping
    module is not yet committed — gate that to Plan 04. Instead assert the script is
    executable and the contract holds (static + negative-path tests only here).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_phase241_safe17_verifier.py -k "contract or out_of_allowlist or dirty or protected or seam" -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase241-safe17-boundary-check.sh` exists and is executable (`test -x`)
    - `grep -c "fping_measurement" scripts/phase241-safe17-boundary-check.sh` >= 2 (regex + allowed_paths)
    - `grep -c "reflector_scorer" scripts/phase241-safe17-boundary-check.sh` >= 2
    - `grep -n "phase239-protected-body-diff.py" scripts/phase241-safe17-boundary-check.sh` present (protected-body layer retained)
    - `grep -n "03c82de0" scripts/phase241-safe17-boundary-check.sh` present (Phase 239 close anchor retained)
    - the verify-command test selection exits 0
  </acceptance_criteria>
  <done>The Phase 241 boundary verifier is a faithful clone of Phase 240's with exactly the allowlist + evidence-path changes; its mirror test proves the expanded contract and that out-of-allowlist / dirty / protected-body / rtt-seam drift still fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Light additive fping sub-param range validators (OQ1, CFG-style safety)</name>
  <read_first>
    - src/wanctl/check_config_validators.py  (FULL — the existing measurement.backend validator from Phase 240; the registry/validator-registration pattern; absent-key-emits-no-result convention per decision [240-01])
    - .planning/phases/240-config-validator/240-01-SUMMARY.md  (how measurement.backend validation was kept out of Config.SCHEMA and absent keys emit no result — match that convention)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md  (Open Question 1 — recommendation: light range validators now; count>=1, period_ms>=10, threshold 0-100, timeout<cadence)
  </read_first>
  <action>
    In `src/wanctl/check_config_validators.py` (already in the SAFE-17 allowlist), ADD
    light, additive range validators for the new `measurement.fping` sub-params,
    following the EXACT registration + absent-key convention Phase 240 established
    (absent keys emit NO result; do not add to Config.SCHEMA):
    - `measurement.fping.count` (alias `-C`): integer >= 1.
    - `measurement.fping.period_ms` (alias `-p`): integer >= 10 (fping minimum period).
    - `measurement.fping.cadence_sec`: number > 0.
    - `measurement.fping.loss_fail_threshold`: number in [0, 100].
    - cross-field: WARN (not fail) if a derivable `timeout = (count*period_ms/1000)+grace`
      would be >= `cadence_sec` (burst-pile guard, Pitfall 5) — keep it a WARN so a
      partial config without all keys does not hard-fail.
    Keep every validator additive and gated to the `fping` block only; an icmplib or
    absent `measurement.fping` config must validate unchanged (CFG-03 spirit). Do NOT
    touch `check_steering_validators.py` (no new fping knobs there this phase).

    Create `tests/test_check_config_validators_fping.py`: assert valid fping knobs pass;
    `count=0`, `period_ms=5`, `loss_fail_threshold=150` each produce a rejection; an
    absent `measurement.fping` block produces no result; a config where timeout>=cadence
    produces a WARN (not a hard error). Confirm an existing real config
    (e.g. `configs/spectrum.yaml` if present, else a representative fixture) still
    validates unchanged.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_check_config_validators_fping.py -q</automated>
  </verify>
  <acceptance_criteria>
    - the new validators reject `count=0`, `period_ms=5`, `loss_fail_threshold=150` (tests assert each)
    - an absent `measurement.fping` block emits no validator result (test asserts)
    - `timeout >= cadence_sec` yields a WARN, not a hard failure (test asserts)
    - `.venv/bin/pytest -o addopts='' tests/test_check_config_validators.py -q` (existing Phase 240 tests) still exits 0 — no regression
    - `.venv/bin/ruff check src/wanctl/check_config_validators.py` + `.venv/bin/mypy src/wanctl/check_config_validators.py` exit 0
  </acceptance_criteria>
  <done>fping sub-param typos (bad count/period/threshold) are caught additively by the already-allowlisted validator, burst-pile risk is WARNed, and absent/icmplib configs validate unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator YAML → validator | fping sub-param values are operator input validated before use |
| git working tree → boundary verifier | the verifier inspects committed controller-path diff read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-05 | Tampering | SAFE-17 allowlist scope | mitigate | allowlist expands by EXACTLY two named files (fping_measurement.py, reflector_scorer.py); the verifier still fails closed on any other drift, dirty tree, protected-body, and rtt-seam drift. Mirror test proves the negative paths. |
| T-241-06 | Tampering | `--out` evidence path redirection | mitigate | retains Phase 240's path-confinement guards (no `src/wanctl`, no `..`, must live under the phase evidence prefix). |
| T-241-07 | DoS | bad fping knob (e.g. period_ms<10, timeout>=cadence) | mitigate | additive range validators + burst-pile WARN catch operator typos before they reach the subprocess (defense-in-depth with the runtime guard in Plan 01). |
| T-241-SC | Tampering | npm/pip/cargo installs | N/A | no language-package install; scripts/tests/validators only. |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_phase241_safe17_verifier.py tests/test_check_config_validators_fping.py tests/test_check_config_validators.py -q` green
- `test -x scripts/phase241-safe17-boundary-check.sh`
- `.venv/bin/ruff check src/ tests/` + `.venv/bin/mypy src/wanctl/` clean
- `git diff --quiet -- scripts/phase240-safe17-boundary-check.sh scripts/phase239-protected-body-diff.py src/wanctl/check_steering_validators.py` exits 0 (Phase 240/239 tooling unchanged; no steering-validator edit)
</verification>

<success_criteria>
SAFE-17 tooling for Phase 241 exists and is proven by mirror tests; the allowlist
expands by exactly the two SAFE-17-named files; fping sub-param typos are caught
additively without breaking any existing config. The gate is built but not yet run
(Plan 04 runs it after all edits land).
</success_criteria>

<output>
Create `.planning/phases/241-fping-backend-offline-reflector-quality/241-02-SUMMARY.md` when done.
</output>
