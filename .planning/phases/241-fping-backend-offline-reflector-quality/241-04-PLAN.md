---
phase: 241-fping-backend-offline-reflector-quality
plan: 04
type: execute
wave: 3
depends_on: ["241-01", "241-02", "241-03"]
files_modified:
  - .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json
autonomous: false
requirements: [SAFE-17]

must_haves:
  truths:
    - "All Phase 241 src/wanctl edits are committed and the src/wanctl tree is clean before the gate runs"
    - "The SAFE-17 boundary verifier proves ZERO out-of-allowlist controller-path drift vs the verifier baseline (anchor_sha 69f39db1)"
    - "Protected bodies (BackgroundRTTThread._run, ping_hosts_with_results, WANController.measure_rtt, RTTMeasurement.*) are byte-identical"
    - "The RTT seam (rtt_backend.py, rtt_measurement.py) shows zero drift since the Phase 239 close anchor"
    - "CUMULATIVE changed_paths (vs the verifier's v1.52/anchor_sha 69f39db1 baseline) is the inherited Phase 239/240 set {check_config_validators.py, check_steering_validators.py, rtt_backend.py, rtt_measurement.py} PLUS src/wanctl/fping_measurement.py = exactly 5 paths (resolves review H3 — the cumulative diff is NOT 2 files)"
    - "PHASE-LOCAL diff (anchored at the Phase 240 close commit a181ca27, NOT the v1.52 SAFE-17 anchor) shows ONLY src/wanctl/fping_measurement.py + src/wanctl/check_config_validators.py changed THIS phase; reflector_scorer.py / wan_controller.py / autorate_continuous.py / irtt_thread.py / rtt_backend.py / rtt_measurement.py / check_steering_validators.py all show zero phase-local diff (resolves review H3 secondary)"
    - "Phase-local 'unchanged this phase' is proven by diff against the a181ca27 baseline commit, NOT by tautological `git diff --quiet HEAD -- file` (which always passes once edits are committed) (resolves review H3 MEDIUM)"
  artifacts:
    - path: ".planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json"
      provides: "fail-closed SAFE-17 boundary evidence with passed:true and zero disallowed paths"
      contains: "\"passed\": true"
  key_links:
    - from: "scripts/phase241-safe17-boundary-check.sh"
      to: ".planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json"
      via: "fail-closed verifier emit"
      pattern: "safe17-boundary-241"
---

<objective>
[BLOCKING SAFE-17 BOUNDARY GATE] Run the Phase 241 fail-closed boundary verifier after
all controller-path edits are committed and prove (1) ZERO out-of-allowlist CUMULATIVE
drift vs the verifier baseline, (2) byte-identical protected bodies, (3) zero RTT-seam
drift since Phase 239, and (4) a PHASE-LOCAL diff (vs the Phase 240 close commit) showing
only fping_measurement.py + check_config_validators.py changed THIS phase. This is the
phase boundary proof — the first controller-path-touching milestone in 10 phases makes
this the keeper gate.

Purpose: prove SAFE-17 held for Phase 241 — controller-path edits stayed inside the
v1.53 allowlist (fping_measurement.py + the accepted reflector-scorer membership), with
no state-machine / threshold / EWMA / dwell / deadband / arbitration / fusion drift.
Output: `safe17-boundary-241.json` evidence with `passed: true` and the correct
cumulative `changed_paths`, plus a phase-local diff check anchored at a181ca27.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-RESEARCH.md
@.planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md
@.planning/phases/240-config-validator/evidence/safe17-boundary-240.json
@scripts/phase241-safe17-boundary-check.sh

<interfaces>
<!-- Anchors verified against the live tree + Phase 240 evidence JSON (2026-06-15) -->
SAFE-17 verifier baseline (the verifier's own anchor_sha / baseline_commit): 69f39db1
  (labelled "v1.52" in the evidence notes; this is the SHA the cumulative changed_paths is
   computed against — confirmed in .planning/phases/240-config-validator/evidence/safe17-boundary-240.json)
Phase 239 close anchor (RTT-seam no-drift): 03c82de0
Phase 240 close commit (the PRE-PHASE-241 phase-local baseline): a181ca27

CUMULATIVE changed_paths the Phase 241 verifier MUST report (review H3, VERIFIED against
the Phase 240 evidence JSON whose changed_paths at Phase 240 was the 4 inherited files):
  src/wanctl/check_config_validators.py     (inherited, Phase 240 — re-touched this phase too)
  src/wanctl/check_steering_validators.py   (inherited, Phase 240 — byte-unchanged this phase)
  src/wanctl/rtt_backend.py                 (inherited, Phase 239 — byte-unchanged this phase)
  src/wanctl/rtt_measurement.py             (inherited, Phase 239 — byte-unchanged this phase)
  src/wanctl/fping_measurement.py           (NEW this phase)
  => exactly 5 cumulative paths. NOT {fping_measurement.py, check_config_validators.py}.

PHASE-LOCAL diff (anchored at a181ca27, the Phase 240 close) the gate MUST also prove
(review H3 secondary): ONLY these two changed THIS phase:
  src/wanctl/fping_measurement.py           (new file)
  src/wanctl/check_config_validators.py     (fping validators + KNOWN_AUTORATE_PATHS entries)
  All other src/wanctl files show ZERO diff vs a181ca27 — in particular
  reflector_scorer.py, wan_controller.py, autorate_continuous.py, irtt_thread.py,
  rtt_backend.py, rtt_measurement.py, check_steering_validators.py.

Protected bodies (must be byte-identical — phase239-protected-body-diff.py PROTECTED):
  rtt_measurement.py: RTTSnapshot, RTTMeasurement.__init__/.ping_host/._aggregate_rtts/
    .ping_hosts_with_results, BackgroundRTTThread._run/._ping_with_persistent_pool
  wan_controller.py: WANController.measure_rtt
  Allowed added qualname on rtt_measurement.py: exactly RTTMeasurement.probe (already present)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all controller-path edits and prove a clean tree + correct phase-local diff (vs a181ca27)</name>
  <read_first>
    - scripts/phase241-safe17-boundary-check.sh  (the dirty-tree fail-closed precondition — the gate refuses to run on uncommitted/staged/untracked src/wanctl changes; and how it computes cumulative changed_paths vs its baseline anchor)
    - .planning/phases/240-config-validator/evidence/safe17-boundary-240.json  (the GROUNDING evidence: anchor_sha 69f39db1, head_commit a181ca27, cumulative changed_paths = the 4 inherited files — review H3 is verified against this)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-REVIEWS.md  (Plan 04 HIGH H3 — cumulative changed_paths is 5 not 2; add phase-local diff vs a181ca27; replace tautological git diff --quiet HEAD checks)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-01-SUMMARY.md  (what Plan 01 committed under src/wanctl)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-02-SUMMARY.md  (Plan 02 validator + KNOWN_AUTORATE_PATHS edit)
    - .planning/phases/241-fping-backend-offline-reflector-quality/241-03-SUMMARY.md  (any Plan 03 fping_measurement.py parser fix)
  </read_first>
  <action>
    Confirm every Phase 241 `src/wanctl/` edit from Plans 01-03 is committed (the
    MANDATORY `project-finalizer` agent must have run before each commit per CLAUDE.md).
    The boundary verifier fails closed on any unstaged/staged/untracked `src/wanctl/`
    change, so the tree must be clean. If anything is uncommitted, run project-finalizer
    and commit it now (commit message scoped to the phase).

    Then prove the PHASE-LOCAL diff against the Phase 240 close commit `a181ca27` (the
    pre-Phase-241 baseline — review H3). This is the correct anchor for "what changed THIS
    phase," distinct from the verifier's cumulative v1.52/69f39db1 anchor:
    - `git diff --name-only a181ca27 HEAD -- src/wanctl/` MUST list EXACTLY
      `src/wanctl/check_config_validators.py` and `src/wanctl/fping_measurement.py`
      (the only two files this phase changed) and NOTHING ELSE.
    - In particular, `git diff --quiet a181ca27 HEAD -- src/wanctl/reflector_scorer.py
      src/wanctl/wan_controller.py src/wanctl/autorate_continuous.py
      src/wanctl/irtt_thread.py src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py
      src/wanctl/check_steering_validators.py` MUST exit 0 (zero phase-local diff). This
      REPLACES the old tautological `git diff --quiet HEAD -- file` checks, which always
      pass once edits are committed and prove nothing about what Phase 241 changed.
    Do not edit any src/wanctl file in this task — this is a verification-only
    commit/clean step.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- src/wanctl/)" && [ "$(git diff --name-only a181ca27 HEAD -- src/wanctl/ | sort | tr '\n' ',')" = "src/wanctl/check_config_validators.py,src/wanctl/fping_measurement.py," ] && git diff --quiet a181ca27 HEAD -- src/wanctl/reflector_scorer.py src/wanctl/wan_controller.py src/wanctl/autorate_continuous.py src/wanctl/irtt_thread.py src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py src/wanctl/check_steering_validators.py && echo PHASE_LOCAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain -- src/wanctl/` is empty (clean tree)
    - PHASE-LOCAL diff (vs a181ca27): `git diff --name-only a181ca27 HEAD -- src/wanctl/` is EXACTLY `src/wanctl/check_config_validators.py` and `src/wanctl/fping_measurement.py` (sorted), nothing else
    - `git diff --quiet a181ca27 HEAD -- src/wanctl/reflector_scorer.py src/wanctl/wan_controller.py src/wanctl/autorate_continuous.py src/wanctl/irtt_thread.py src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py src/wanctl/check_steering_validators.py` exits 0 (zero phase-local diff on every byte-unchanged file — replaces the tautological HEAD checks)
    - the RTT seam shows zero drift since Phase 239 close: `git diff --quiet 03c82de0 HEAD -- src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py` exits 0
  </acceptance_criteria>
  <done>All Phase 241 controller-path work is committed, the src/wanctl tree is clean, and the phase-local diff vs the a181ca27 baseline proves ONLY fping_measurement.py + check_config_validators.py changed this phase (every other controller-path file shows zero phase-local diff).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run the SAFE-17 boundary verifier and confirm passed:true with the correct cumulative changed_paths</name>
  <what-built>`scripts/phase241-safe17-boundary-check.sh` (Plan 02) — the fail-closed verifier with the expanded allowlist (+fping_measurement.py +reflector_scorer.py), run after all edits are committed. Its cumulative changed_paths is computed vs the verifier baseline (anchor_sha 69f39db1), so it reports the inherited Phase 239/240 set PLUS the new fping module.</what-built>
  <how-to-verify>
    1. Run the gate from the repo root:
       `bash scripts/phase241-safe17-boundary-check.sh`
       (the dev VM lacks fping but the verifier is git/source-diff only — no fping needed).
    2. Expect exit 0 and the line `SAFE-17 boundary check passed: .planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json`.
    3. Open `.planning/phases/241-fping-backend-offline-reflector-quality/evidence/safe17-boundary-241.json`
       and confirm:
       - `"passed": true`
       - `"disallowed_paths": []`
       - `"dirty_tree_clean": true`
       - `"rtt_seam_unchanged_since_phase239": true`
       - `"all_identical": true` and `"allowed_shape_ok": true` (protected bodies +
         allowed-shape guard)
       - `changed_paths` is the CUMULATIVE set vs the verifier baseline = exactly the 5
         paths: `src/wanctl/check_config_validators.py`,
         `src/wanctl/check_steering_validators.py`, `src/wanctl/rtt_backend.py`,
         `src/wanctl/rtt_measurement.py`, `src/wanctl/fping_measurement.py`. (Review H3:
         do NOT expect only 2 — the verifier reports the cumulative v1.53 diff, which at
         Phase 240 was the 4 inherited files; Phase 241 adds fping_measurement.py.)
       - `"head_commit"` equals current `git rev-parse HEAD` (no stale evidence).
    4. Also run the verifier behavior test:
       `.venv/bin/pytest -o addopts='' tests/test_phase241_safe17_verifier.py -q`
       and confirm it is green (including the now-runnable passes-at-boundary case).
    5. If the gate FAILS: it has correctly caught controller-path drift. Do NOT override.
       Report the disallowed path / protected-body diff so the offending edit can be moved
       back inside the allowlist (a frozen-body edit means the work must be redone on the
       fping side).
  </how-to-verify>
  <resume-signal>Type "passed" with the evidence path, or paste the SAFE-17 VIOLATION output if the gate failed.</resume-signal>
  <acceptance_criteria>
    - `bash scripts/phase241-safe17-boundary-check.sh` exits 0
    - `safe17-boundary-241.json` has `"passed": true`, `"disallowed_paths": []`, `"dirty_tree_clean": true`, `"rtt_seam_unchanged_since_phase239": true`, `"all_identical": true`, `"allowed_shape_ok": true`
    - CUMULATIVE `changed_paths` is exactly the 5-path set {`src/wanctl/check_config_validators.py`, `src/wanctl/check_steering_validators.py`, `src/wanctl/rtt_backend.py`, `src/wanctl/rtt_measurement.py`, `src/wanctl/fping_measurement.py`} (review H3 — NOT a 2-path set)
    - `safe17-boundary-241.json` `head_commit` equals `git rev-parse HEAD` (no stale evidence)
    - `.venv/bin/pytest -o addopts='' tests/test_phase241_safe17_verifier.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed controller-path diff → boundary verifier | the verifier inspects the committed src/wanctl diff read-only and fails closed on any drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-11 | Tampering | undetected controller-path drift | mitigate | fail-closed verifier: out-of-allowlist path drift, dirty/staged/untracked tree, protected-body byte-diff, and RTT-seam drift since Phase 239 each abort with non-zero exit and `passed:false` evidence. |
| T-241-12 | Repudiation | unverifiable phase-boundary claim | mitigate | JSON evidence (`safe17-boundary-241.json`) records anchor SHAs, cumulative changed paths, protected-body result, and timestamp — an auditable record, not an assertion; head_commit is checked against HEAD to reject stale evidence. |
| T-241-13 | Elevation | a frozen body silently edited to "reuse" the icmplib thread/scorer | mitigate | the protected-body layer (phase239-protected-body-diff.py) proves BackgroundRTTThread._run, ping_hosts_with_results, WANController.measure_rtt byte-identical; gate is blocking and not auto-approvable. |
| T-241-15 | Repudiation | false "only 2 files changed" claim hiding inherited drift, OR a tautological HEAD check proving nothing | mitigate | cumulative changed_paths is asserted as the correct 5-path set vs the verifier baseline; a SEPARATE phase-local diff vs the a181ca27 Phase 240 close commit proves only fping_measurement.py + check_config_validators.py changed this phase (replacing the tautological `git diff --quiet HEAD` checks). |
| T-241-SC | Tampering | npm/pip/cargo installs | N/A | no language-package install anywhere in Phase 241. |
</threat_model>

<verification>
- `bash scripts/phase241-safe17-boundary-check.sh` exits 0 and emits `passed:true` evidence with the 5-path cumulative changed_paths
- phase-local diff vs a181ca27 proves only fping_measurement.py + check_config_validators.py changed this phase (Task 1)
- `.venv/bin/pytest -o addopts='' tests/test_phase241_safe17_verifier.py -q` green
- full suite `.venv/bin/pytest tests/ -q` green (regression check at the boundary)
- hot-path slice green (icmplib path unchanged):
  `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`
</verification>

<success_criteria>
SAFE-17 held at the Phase 241 boundary: the fail-closed verifier passes with zero
out-of-allowlist drift, byte-identical protected bodies, zero RTT-seam drift since
Phase 239, the CUMULATIVE changed_paths is the correct 5-path inherited+fping set (not a
spurious 2-path set), and a phase-local diff vs the Phase 240 close commit proves only
fping_measurement.py + check_config_validators.py changed this phase — all recorded in
committed JSON evidence whose head_commit matches HEAD.
</success_criteria>

<output>
Create `.planning/phases/241-fping-backend-offline-reflector-quality/241-04-SUMMARY.md` when done.
</output>
