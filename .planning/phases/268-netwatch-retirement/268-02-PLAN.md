# Wave 2: Netwatch Removal + Guard Update

**Phase**: 268
**Wave**: 2
**Scope**: Remove RouterOS netwatch entries, update guard and inspector

## Tasks

1. RouterOS netwatch removal:
   - Via SSH/REST: `/ip dhcp-client remove [numbers]` for netwatch-related
   - Actually: `/ping checks remove` — need to identify exact netwatch entry IDs
   - Script: `scripts/phase268-remove-netwatch.sh` — idempotent, verify before removal

2. Guard update:
   - `RouteOwnershipGuard`: remove netwatch conflict detection
   - Guard should no longer check for netwatch entries or report conflicts
   - Update guard status message to reflect "wanctl-only" ownership

3. Inspector update:
   - `RouteOwnershipInspector`: remove netwatch inspection from snapshot
   - Health endpoint: remove `ownership_inspection.netwatch` section
   - Update health.py `_build_ownership_inspection_section`

4. Config cleanup:
   - Remove any netwatch-related config options
   - Update example config

## Verification

- Netwatch entries removed from RouterOS
- Guard reports clean without netwatch checks
- Health endpoint no longer shows netwatch section
- Route reconciliation still works (4 routes, all managed)

## Rollback

- Netwatch entries can be recreated via RouterOS API
- Script: `scripts/phase268-restore-netwatch.sh` with known entries
