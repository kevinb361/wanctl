---
phase: 235-bypass-operator-cli-boot-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_silicom_bypass_cli.py
  - scripts/silicom-bypass
  - deploy/scripts/silicom-bypass.conf.example
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]
must_haves:
  truths:
    - "Operator runs `silicom-bypass status [pair|all]` and sees live per-pair state read back from bpctl every call (never cached)"
    - "A non-bypass-capable interface (empty get_bypass_slave) is refused with a clear error and non-zero exit"
    - "`off`/`conn` are idempotent no-ops (exit 0) when the pair is already in the target state"
    - "`on`/`disc` refuse to act without `--yes`"
    - "A destructive op that would place BOTH pairs into a non-NIC state (Bypass OR Disconnect) is refused without `--both-wan-confirm`"
    - "`silicom-bypass mark <label>` writes to the journal AND appends to the flat marks log"
    - "Every automated test injects a STATEFUL fake bpctl_util via BPCTL_UTIL — no test touches the live card, and read-back-asserting verbs see their own writes"
    - "The stateful fake models ALL EIGHT state keys (bypass, disc, std_nic, dis_bypass, bypass_pwoff, bypass_pwup, disc_pwup, plus the bypass_slave capability), each primeable to a known value so Plan 02's baseline tests can set up deterministic matched/mismatched states"
  artifacts:
    - path: "scripts/silicom-bypass"
      provides: "Guarded bash CLI: status/on/off/disc/conn/mark over bpctl_util"
      min_lines: 120
      contains: "BPCTL_UTIL"
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "Offline pytest with STATEFUL fake bpctl_util fixture covering TOOL-01..04, modeling all 8 baseline+TOOL state keys"
      contains: "BPCTL_UTIL"
    - path: "deploy/scripts/silicom-bypass.conf.example"
      provides: "Config example with live pair names"
      contains: "att-modem spec-modem"
  key_links:
    - from: "scripts/silicom-bypass"
      to: "/etc/silicom-bypass.conf (SILICOM_BYPASS_CONF)"
      via: "shell source of PAIRS"
      pattern: "SILICOM_BYPASS_CONF|\\. .*CONF"
    - from: "scripts/silicom-bypass"
      to: "$BPCTL_UTIL <iface> <verb>"
      via: "tool-path env seam"
      pattern: "BPCTL_UTIL"
    - from: "scripts/silicom-bypass"
      to: "logger / $LOGGER seam"
      via: "journal write for mark + state changes"
      pattern: "LOGGER|logger -t silicom-bypass"
    - from: "tests/test_silicom_bypass_cli.py"
      to: "scripts/silicom-bypass"
      via: "subprocess.run with injected BPCTL_UTIL + stateful fake"
      pattern: "subprocess\\.run"
---

<objective>
Build the guarded `silicom-bypass` operator CLI (status/on/off/disc/conn/mark) with its config example and an offline pytest harness that injects a STATEFUL fake `bpctl_util`. This is the single sanctioned mutation path for the Silicom bypass card, replacing hand-typed `bpctl_util` invocations and removing typo-induced dual-WAN loss.

Purpose: Operators get idempotent, journaled, read-back-verified, double-gated verbs. Covers TOOL-01..04. All acceptance criteria are provable OFFLINE via the BPCTL_UTIL fake seam — no live host required. The fake's state model is the shared seam Plan 02's baseline tests build on, so it MUST cover every baseline policy key, not just the TOOL keys.

Output: `scripts/silicom-bypass`, `deploy/scripts/silicom-bypass.conf.example`, `tests/test_silicom_bypass_cli.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-CONTEXT.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-RESEARCH.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-PATTERNS.md

<interfaces>
<!-- Concrete contracts the executor needs. Do not explore the codebase for these. -->

Tool-path seam (existing repo convention, scripts/wanctl-bpctl-watchdog-petter:6-12):
  : "${BPCTL_UTIL:=/opt/bpctl-silicom/bpctl_util}"
  util() { "$BPCTL_UTIL" "$IFACE" "$@"; }

Journal seam (Review fix: 235-REVIEWS LOW — prove logger is invoked, not just the flat log):
  : "${LOGGER:=logger}"   # PATH-injectable in tests via a fake `logger` on PATH or LOGGER=...
  journal() { "$LOGGER" -t silicom-bypass -- "$*"; }

Config/marks seams (research CLI skeleton):
  CONF="${SILICOM_BYPASS_CONF:-/etc/silicom-bypass.conf}"
  MARKS_LOG="${SILICOM_MARKS_LOG:-/var/log/silicom-bypass-marks.log}"
  [ -r "$CONF" ] && . "$CONF"   # provides PAIRS="att-modem spec-modem"

Heavy-bash arg-parse + exit-code grammar (scripts/phase225-safe13-boundary-check.sh:1-48):
  #!/usr/bin/env bash ; set -euo pipefail
  while [[ $# -gt 0 ]]; do case "$1" in ... esac done
  exit codes: 0 ok/no-op, 1 runtime/read-back failure, 2 usage/unknown-arg

bpctl verb surface (docs/SILICOM-BYPASS.md, authoritative for THIS card/firmware 0xaa, module 5.2.0.46):
  CLI verb        | mutator           | read-back verb | target substring (match non- form FIRST)
  on  <pair> (--yes)  set_bypass on    get_bypass     "Bypass"      (NOT "non-Bypass")
  off <pair>          set_bypass off   get_bypass     "non-Bypass"
  disc <pair> (--yes) set_disc on      get_disc       "Disconnect"  (NOT "non-Disconnect")
  conn <pair>         set_disc off     get_disc       "non-Disconnect"
  status (read-only)  —                get_bypass, get_disc, get_std_nic  (report all three per pair)
  capability probe    —                get_bypass_slave   non-empty slave else REFUSE iface

Non-NIC predicate (for the --both-wan-confirm dual-WAN gate — TOOL-03):
  A pair is "non-NIC" if get_bypass matches "Bypass" (non- excluded) OR get_disc matches "Disconnect"
  (non- excluded). Disconnect counts as non-NIC, not only Bypass. The gate reads BOTH the verb about to
  change AND the other pair's CURRENT bypass+disc state, then refuses if both pairs would end up non-NIC.
  (Review fix: 235-REVIEWS 235-01 MEDIUM — TOOL-03 must treat "other pair already disconnected" as non-NIC.)

STATEFUL FAKE bpctl_util contract (Review fix: 235-REVIEWS 235-01 HIGH #1 + HIGH #2, and 235-02 MEDIUM —
enumerate ALL baseline policy keys, not just TOOL keys). The fake MUST model card state transitions, not echo
canned strings, or a correct read-back-asserting CLI fails its own tests. It MUST track EIGHT state items so
that Plan 02's baseline tests can prime each independently into a matched-or-mismatched state:
  - The stub keeps per-iface state on disk (e.g. tmp_path/"state/<iface>.<key>"), keyed by iface ($1)
    and logical key. Eight logical keys are tracked — three TOOL keys plus four baseline-policy keys plus
    the capability probe:
      key          | set verb         | get verb         | get echoes (set-state -> string)              | DEFAULT state -> default string
      bypass       | set_bypass       | get_bypass       | on->"Bypass", off->"non-Bypass"               | off  -> "non-Bypass"
      disc         | set_disc         | get_disc         | on->"Disconnect", off->"non-Disconnect"       | off  -> "non-Disconnect"
      std_nic      | set_std_nic      | get_std_nic      | on->"Standard NIC", off->"not in Standard NIC mode" | off -> "not in Standard NIC mode"
      dis_bypass   | set_dis_bypass   | get_dis_bypass   | off->"Bypass mode enabled", on->"Bypass mode disabled" (double-negative: set_dis_bypass OFF clears the disable bit) | on -> "Bypass mode disabled"
      bypass_pwoff | set_bypass_pwoff | get_bypass_pwoff | on->"Bypass at power off", off->"non-Bypass at power off" | off -> "non-Bypass at power off"
      bypass_pwup  | set_bypass_pwup  | get_bypass_pwup  | on->"Bypass at power up", off->"non-Bypass at power up" | on -> "Bypass at power up"
      disc_pwup    | set_disc_pwup    | get_disc_pwup    | on->"Disconnect at power up", off->"non-Disconnect at power up" | on -> "Disconnect at power up"
    NOTE: the four baseline keys' DEFAULT states above are chosen so the fake's UNPRIMED default does NOT
    already match the Plan-02 baseline policy (baseline wants dis_bypass=off, bypass_pwoff=on, bypass_pwup=off,
    disc_pwup=off, std_nic=off). dis_bypass/bypass_pwup/disc_pwup default to the MISMATCHED side, bypass_pwoff
    defaults to the mismatched "off". This means Plan-02's baseline-applies test (running against an UNPRIMED
    fake) legitimately expects all five set_* writes, while std_nic's default ("off"/"not in Standard NIC mode")
    ALREADY matches baseline policy — so Plan 02 MUST prime a mismatched std_nic (set it "on") when it wants
    the std_nic set_* write to appear. Plan 02's tests prime these exact keys; keep this table authoritative.
  - set_* verbs WRITE the new state for that iface+key (set_bypass on -> bypass=on), then `exit 0`.
  - get_* verbs READ current state for that iface+key and echo the matching documented string per the table
    above; an UNSET key falls back to the DEFAULT state in the table.
  - get_bypass_slave MUST default to a NON-EMPTY paired iface for EVERY configured pair (so capability
    probing passes the happy path). A test that wants the "configured-but-not-capable" refusal overrides
    get_bypass_slave to empty for ONE iface only.
  - The fake MUST expose a priming hook (a helper that writes tmp_path/"state/<iface>.<key>" directly to a
    chosen on/off value before the CLI runs) so Plan 02 can deterministically set any of the eight keys to a
    matched or mismatched baseline state without first invoking a mutating CLI verb.
  - Every invocation still appends "$@" (with iface) to calls.log for call-trace assertions.
  This makes `on att-modem --yes` after `set_bypass on` read back "Bypass" and PASS, instead of a canned
  get_bypass="non-Bypass" failing a correct CLI; and it gives Plan 02 a fully primeable baseline-policy state.

Pytest subprocess+env runner (tests/test_check_safe07_source_diff.py:11-12,49-58):
  REPO_ROOT = Path(__file__).resolve().parents[1]
  subprocess.run(["bash", str(SCRIPT), *args], cwd=REPO_ROOT,
                 capture_output=True, text=True, env={**os.environ, **extra})

Static-artifact assertion style (tests/test_att_cake_autorate_artifacts.py:13-23): module-level Path constants + .exists()/.read_text() substring asserts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest harness with STATEFUL fake bpctl_util fixture (TOOL-01..04 stubs, RED)</name>
  <files>tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - tests/test_check_safe07_source_diff.py (subprocess+env runner shape, exit-code-by-class asserts)
    - tests/test_att_cake_autorate_artifacts.py (module-level Path constants, static .exists()/substring asserts)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-RESEARCH.md (pytest-with-fake-bpctl_util sketch; bpctl verb surface table)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-VALIDATION.md (per-task verification map; exact test names)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEWS.md (235-01 HIGH/MEDIUM concerns the fixtures must satisfy)
  </read_first>
  <action>
    Create tests/test_silicom_bypass_cli.py establishing the offline seam BEFORE the CLI exists (RED). Define REPO_ROOT = Path(__file__).resolve().parents[1] and CLI = REPO_ROOT/"scripts"/"silicom-bypass".
    Build a STATEFUL fake per the "STATEFUL FAKE bpctl_util contract" in <interfaces>. Add a helper `_fake_bpctl(tmp_path, slave_overrides=None)` that writes a bash stub to tmp_path/"bpctl_util" which:
      - appends every invocation's full args (including iface $1) to tmp_path/"calls.log";
      - persists per-iface state under tmp_path/"state/" using files named "<iface>.<key>" for ALL EIGHT keys bypass|disc|std_nic|dis_bypass|bypass_pwoff|bypass_pwup|disc_pwup (and the bypass_slave capability);
      - on any set_<key> verb WRITES on|off for that iface+key and exits 0;
      - on any get_<key> verb READS the iface+key (default to the DEFAULT state from the contract table when unset) and echoes the documented string for that key per the contract table;
      - on get_bypass_slave echoes a non-empty paired iface for every configured pair UNLESS slave_overrides maps that iface to "" (empty);
      chmod 0o755; return (fake_path, calls_log_path).
    Also add a priming helper `_prime(tmp_path, iface, key, value)` that writes tmp_path/"state/<iface>.<key>" directly to "on"/"off" so a test (here AND in Plan 02) can set any of the eight keys to a matched/mismatched baseline state WITHOUT invoking a mutating CLI verb. Plan 02 reuses this exact helper.
    Add a `_run(cli_args, env)` helper using subprocess.run(["bash", str(CLI), *cli_args], capture_output=True, text=True, env={**os.environ, "BPCTL_UTIL": str(fake), "PAIRS": "att-modem spec-modem", "SILICOM_BYPASS_CONF": "/dev/null", "SILICOM_MARKS_LOG": str(tmp_path/"marks.log"), "LOGGER": str(fake_logger), **extra}). Also write a PATH-injectable fake `logger` stub (tmp_path/"logger") that appends its args to tmp_path/"logger.log" and exits 0, so tests can prove journaling actually ran.
    Write these tests with the exact names from 235-VALIDATION.md (they MUST fail now since the CLI is absent). The stateful fake means each test SETS state via real CLI verbs or by priming the fake's state dir, never by canned get_* responses:
    - test_status_reads_live: default NIC posture; run `status att-modem`; assert rc==0 AND calls.log contains get_bypass/get_disc/get_std_nic for att-modem (proves live read, not cached). Run `status att-modem` again and assert get_* re-issued (no caching between calls).
    - test_off_idempotent_noop: default state (already non-Bypass); run `off att-modem`; assert rc==0, "no-op" in stdout, AND "set_bypass off" NOT in calls.log (idempotency).
    - test_on_requires_yes: run `on att-modem` (no --yes); assert rc!=0 AND no "set_bypass on" in calls.log; then run `on att-modem --yes`; assert rc==0 AND "set_bypass on" in calls.log; then run `status att-modem` and assert the stateful fake now reports "Bypass" (read-back saw the write — proves the fake is stateful and the CLI's own read-back assertion would have passed).
    - test_disc_requires_yes_and_applies: run `disc att-modem` (no --yes); assert rc!=0 and no "set_disc on" in calls.log; run `disc att-modem --yes`; assert rc==0, "set_disc on" in calls.log; then `status att-modem` reports "Disconnect".
    - test_disc_idempotent_noop: prime att-modem disc=on (run `disc att-modem --yes` first), then run `disc att-modem --yes` again; assert rc==0, "no-op" in stdout, and only ONE "set_disc on" across both runs (second run is a no-op).
    - test_conn_idempotent_noop: default state (already non-Disconnect); run `conn att-modem`; assert rc==0, "no-op" in stdout, "set_disc off" NOT in calls.log.
    - test_refuses_non_pair_iface: run `status eth99` (not in PAIRS); assert rc!=0 and an error mentioning the interface in stderr; assert no set_* in calls.log.
    - test_refuses_not_capable_iface: build fake with slave_overrides={"spec-modem": ""}; run `status spec-modem`; assert rc!=0 and an error indicating spec-modem is not bypass-capable; assert no set_* in calls.log. (Configured-but-not-capable refusal.)
    - test_both_wan_confirm_gate_bypass: prime att-modem to bypass (run `on att-modem --yes`), then run `on spec-modem --yes` (would make BOTH non-NIC); assert rc!=0 without --both-wan-confirm and rc==0 with `--yes --both-wan-confirm`.
    - test_both_wan_confirm_gate_disconnect: prime att-modem to DISCONNECT (run `disc att-modem --yes`), then run `disc spec-modem --yes`; assert rc!=0 without --both-wan-confirm (the other pair is Disconnect, not Bypass, but still non-NIC) and rc==0 with `--yes --both-wan-confirm`. (Review fix: Disconnect counts as non-NIC.)
    - test_mark_appends_log_and_journals: run `mark soak-start-1`; assert rc==0 AND the SILICOM_MARKS_LOG file contains "soak-start-1" AND the fake-logger log (logger.log) contains "soak-start-1" (proves logger was invoked, not just the flat log).
    Do NOT call /opt/bpctl-silicom/bpctl_util anywhere; every test injects BPCTL_UTIL. Do NOT add bats.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/test_silicom_bypass_cli.py -q 2>&1 | grep -qE 'error|failed' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_silicom_bypass_cli.py exists and is collected by pytest
    - The fake bpctl_util stub is STATEFUL: grep finds it writing/reading per-iface state files (a state dir under tmp_path), not only echoing canned constants
    - The fake models ALL EIGHT state keys (grep the fake stub finds handling for set_dis_bypass/get_dis_bypass, set_bypass_pwoff/get_bypass_pwoff, set_bypass_pwup/get_bypass_pwup, set_disc_pwup/get_disc_pwup in addition to bypass/disc/std_nic), with the DEFAULT-state strings from the contract table
    - A `_prime(tmp_path, iface, key, value)` helper exists that writes a state file directly so any of the eight keys can be set without a mutating CLI verb (Plan 02 reuses it)
    - The fake defaults get_bypass_slave to a NON-EMPTY value for configured pairs; a slave_overrides hook can force empty for one iface
    - All named tests are present (grep finds test_status_reads_live, test_off_idempotent_noop, test_on_requires_yes, test_disc_requires_yes_and_applies, test_disc_idempotent_noop, test_conn_idempotent_noop, test_refuses_non_pair_iface, test_refuses_not_capable_iface, test_both_wan_confirm_gate_bypass, test_both_wan_confirm_gate_disconnect, test_mark_appends_log_and_journals)
    - A LOGGER/logger seam is injected so test_mark_appends_log_and_journals can assert logger was invoked
    - File contains "BPCTL_UTIL" and never references /opt/bpctl-silicom/bpctl_util as a real path to call
    - Running the file FAILS (RED) because scripts/silicom-bypass does not yet exist — `.venv/bin/pytest tests/test_silicom_bypass_cli.py -q` reports failures/errors, not 0 passed
  </acceptance_criteria>
  <done>Test file present, all TOOL tests defined against a STATEFUL fake modeling all 8 keys with a reusable priming helper, non-empty slave defaults and a logger seam, suite is RED (CLI absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/silicom-bypass CLI + config example (GREEN, TOOL-01..04)</name>
  <files>scripts/silicom-bypass, deploy/scripts/silicom-bypass.conf.example, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - scripts/wanctl-bpctl-watchdog-petter (BPCTL_UTIL seam, util() wrapper, read-then-act gate)
    - scripts/wanctl-bpctl-watchdog-bypass (bare mutators set_dis_bypass/set_bypass)
    - scripts/phase225-safe13-boundary-check.sh (header, set -euo pipefail, usage() heredoc, arg-parse while/case, exit-code grammar 0/1/2)
    - deploy/scripts/bpctl-watchdog-spectrum.env.example (flat KEY=value config format; IFACE=spec-modem confirms live name)
    - tests/test_silicom_bypass_cli.py (the contract this CLI must satisfy — written in Task 1, including the STATEFUL fake and non-NIC=Bypass-OR-Disconnect dual-WAN gate)
    - docs/SILICOM-BYPASS.md (bpctl verb surface, read-back strings, set_dis_bypass double-negative)
  </read_first>
  <action>
    Create scripts/silicom-bypass: `#!/usr/bin/env bash` + `set -euo pipefail` (NOT /bin/sh — needs [[, arrays, case dispatch). Seams: CONF="${SILICOM_BYPASS_CONF:-/etc/silicom-bypass.conf}", `: "${BPCTL_UTIL:=/opt/bpctl-silicom/bpctl_util}"`, `: "${LOGGER:=logger}"`, MARKS_LOG="${SILICOM_MARKS_LOG:-/var/log/silicom-bypass-marks.log}", then `[ -r "$CONF" ] && . "$CONF"` providing PAIRS. Add `die()` (stderr + exit code) and `journal() { "$LOGGER" -t silicom-bypass -- "$*"; }` (use the LOGGER seam so tests can prove it ran).
    Helpers: `resolve_pair <pair>` validates the arg is in the PAIRS allowlist (else die rc!=0 with a clear message naming the interface), then runs `"$BPCTL_UTIL" "$pair" get_bypass_slave` as a capability probe — empty/failed slave ⇒ refuse the iface loudly (rc!=0). Centralize read-back want-strings in one place; match the `non-` form FIRST (`*non-Bypass*` before `*Bypass*`) per RESEARCH Pitfall 4.
    Add `is_non_nic <pair>` predicate used by the dual-WAN gate: reads get_bypass AND get_disc for the pair and returns true if the pair matches `*Bypass*` (non- excluded) OR `*Disconnect*` (non- excluded). Disconnect MUST count as non-NIC, not only Bypass (235-REVIEWS 235-01 MEDIUM).
    Subcommand dispatch (while/case, extensible so Phase 236 can add arm/disarm later):
    - `status [pair|all]`: for each target pair run get_bypass, get_disc, get_std_nic EVERY call (no caching, no state file) and print NIC/bypass/disconnect per pair.
    - `off <pair>`: read get_bypass; if already `*non-Bypass*` print "<pair> already non-bypass (NIC); no-op" and exit 0; else `set_bypass off`, re-read, assert `*non-Bypass*` (rc 1 on mismatch), journal.
    - `conn <pair>`: same shape against set_disc off / get_disc / `*non-Disconnect*`.
    - `on <pair>`: destructive — require `--yes` (else die rc!=0). Idempotent: no-op if get_bypass already `*Bypass*` (non- excluded). Dual-WAN gate: if applying this op AND the OTHER pair is currently non-NIC (via is_non_nic, i.e. Bypass OR Disconnect), require `--both-wan-confirm` (else die rc!=0). Then `set_bypass on`, re-read, assert non-`non-` `*Bypass*`, journal.
    - `disc <pair>`: destructive shape against set_disc on / get_disc / `Disconnect`. require `--yes`. Idempotent: no-op if get_disc already `*Disconnect*` (non- excluded). Same dual-WAN gate (other pair non-NIC ⇒ require `--both-wan-confirm`).
    - `mark <label>`: `journal "$label"` (via the LOGGER seam) AND append the label to "$MARKS_LOG" (create if absent). Quote the label; no eval; no unquoted word-splitting.
    All state changes journal. Quote all expansions; no `eval`. Exit-code grammar: 0 ok/no-op, 1 runtime/read-back failure, 2 usage/unknown subcommand.
    Create deploy/scripts/silicom-bypass.conf.example (flat shell-sourced) with exactly: PAIRS="att-modem spec-modem", WD_TIMEOUT_MS=10000 (comment: reserved for Phase 236), HEARTBEAT_MS=3000 (comment: reserved for Phase 236). NEVER emit sil-spare1/sil-spare2 — those names are stale.
    Do NOT modify scripts/wanctl-bpctl-watchdog-* or any deploy/systemd watchdog unit (Phase 236 scope). Do NOT touch src/wanctl/** (SAFE-16). Do NOT add a `baseline` subcommand here — that is Plan 02.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/test_silicom_bypass_cli.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/silicom-bypass exists, starts with `#!/usr/bin/env bash` and `set -euo pipefail`, and is executable
    - File contains BPCTL_UTIL, SILICOM_BYPASS_CONF, MARKS_LOG, and LOGGER seams and a get_bypass_slave capability probe
    - File contains an is_non_nic-style predicate that tests BOTH get_bypass (Bypass) AND get_disc (Disconnect) for the dual-WAN gate
    - deploy/scripts/silicom-bypass.conf.example contains `PAIRS="att-modem spec-modem"` and contains neither `sil-spare1` nor `sil-spare2` (`grep -v '^#' deploy/scripts/silicom-bypass.conf.example | grep -c 'sil-spare'` returns 0)
    - All TOOL tests pass including both dual-WAN gate variants (bypass and disconnect) and disc/conn idempotency: `.venv/bin/pytest tests/test_silicom_bypass_cli.py -x -q` exits 0
    - `grep -v '^#' scripts/silicom-bypass | grep -c 'sil-spare'` returns 0
    - No file under src/wanctl/ is modified (git status shows only scripts/silicom-bypass, deploy/scripts/silicom-bypass.conf.example, tests/test_silicom_bypass_cli.py changed)
  </acceptance_criteria>
  <done>CLI implements TOOL-01..04 against the stateful fake-bpctl harness; all TOOL tests green incl. disc/conn coverage and Disconnect-as-non-NIC gate; config ships live pair names; zero controller-path edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args → bpctl_util | Operator-supplied pair name/label crosses into card mutation commands |
| test harness → CLI | Executor runs CLI in CI; MUST NOT reach the live card |
| this phase → controller path | Scripts/tests must not mutate src/wanctl/** (SAFE-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-01 | Denial of Service | `on`/`disc` on a live pair | mitigate | Idempotent no-op + `--yes` gate; both-WAN backstop in T-235-03; all automated tests use stateful fake BPCTL_UTIL (executor never runs real tool) |
| T-235-02 | Tampering | unvalidated `<pair>` arg reaching bpctl_util | mitigate | resolve_pair allowlists against PAIRS + get_bypass_slave capability probe before any set_* |
| T-235-03 | Denial of Service | typo putting BOTH pairs non-NIC | mitigate | `--both-wan-confirm` dual gate (TOOL-03); is_non_nic reads other pair's Bypass OR Disconnect state before acting |
| T-235-04 | Tampering | shell injection via label/pair args | mitigate | `set -euo pipefail`, quoted expansions, `"$LOGGER" -- "$label"`, no `eval`, no unquoted word-splitting |
| T-235-05 | Tampering | controller-path mutation slips in | mitigate | Plan 03 runs `scripts/phase225-safe13-boundary-check.sh --anchor v1.51` at phase gate; this plan touches no src/wanctl/** |
| T-235-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — all tooling OS/repo-resident) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -x -q` exits 0 (all TOOL tests green incl. disc/conn + both dual-WAN gate variants)
- No automated step invokes /opt/bpctl-silicom/bpctl_util (every test injects BPCTL_UTIL with a stateful fake)
- The fake models all eight state keys with a reusable `_prime` helper (Plan 02 depends on this)
- The mark test proves the logger seam ran (logger.log contains the label), not only the flat marks log
- `grep -v '^#' deploy/scripts/silicom-bypass.conf.example | grep -c 'sil-spare'` returns 0
- `git status --porcelain src/wanctl/` is empty (no controller-path change)
</verification>

<success_criteria>
- `silicom-bypass status [pair|all]` reads live state per pair every call (TOOL-01)
- idempotent guarded `on/off/disc/conn`; `on`/`disc` need `--yes`; non-pair and not-capable iface refused (TOOL-02)
- dual-pair → non-NIC (Bypass OR Disconnect) needs `--both-wan-confirm` (TOOL-03)
- `mark <label>` → journal (logger) + flat log (TOOL-04)
- the stateful fake models all 8 baseline+TOOL keys with a primeable hook, so Plan 02 can build deterministic baseline tests on it
- config ships `att-modem spec-modem` live names
- zero src/wanctl/** diff
</success_criteria>

<output>
Create `.planning/phases/235-bypass-operator-cli-boot-baseline/235-01-SUMMARY.md` when done
</output>
</content>
</invoke>
