---
phase: 235-bypass-operator-cli-boot-baseline
plan: 04
type: execute
wave: 1
depends_on: [235-01, 235-02, 235-03]
files_modified:
  - scripts/deploy.sh
  - deploy/systemd/silicom-bypass-init.service
  - docs/SILICOM-BYPASS.md
  - tests/test_silicom_bypass_cli.py
autonomous: true
gap_closure: true
requirements: [BOOT-01]
# Note: CR-01 + WR-02 are deploy-surface safety hardening with no dedicated req ID;
# WR-01 protects the BOOT-01 boot-baseline runbook (accurate manual re-apply procedure).
# No src/wanctl files touched — SAFE-16 controller-path zero-diff invariant holds.

must_haves:
  truths:
    - "The Silicom standalone deploy stages root-installed artifacts in a per-deploy private remote temp directory (not predictable /tmp paths) and installs them atomically as root."
    - "The --silicom-bypass-only mode fails closed on an extra positional argument or an incompatible WAN deployment flag, before any SSH/mutation action."
    - "The silicom-bypass-init.service manual re-apply runbook is coherent with the unit's RemainAfterExit semantics — a documented manual command actually re-runs the baseline ExecStart."
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "Hardened deploy_silicom_bypass() (mktemp -d staging + install -o root -g root) and fail-closed --silicom-bypass-only arg validation"
      contains: "mktemp -d"
    - path: "deploy/systemd/silicom-bypass-init.service"
      provides: "Boot baseline oneshot unit"
      contains: "ExecStart=/usr/local/sbin/silicom-bypass baseline"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "Manual oneshot exercise runbook coherent with RemainAfterExit=yes"
      contains: "systemctl restart silicom-bypass-init.service"
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "Offline assertions for staging hardening, arg fail-closed, and unit/docs coherence"
      contains: "def test_"
  key_links:
    - from: "scripts/deploy.sh deploy_silicom_bypass()"
      to: "remote mktemp -d staging dir"
      via: "scp into private dir then sudo install -o root -g root"
      pattern: "mktemp -d"
    - from: "scripts/deploy.sh --silicom-bypass-only handler"
      to: "fail-closed validation"
      via: "reject non-empty TARGET_HOST and incompatible flags before check_prerequisites/deploy"
      pattern: "silicom-bypass-only cannot be combined"
    - from: "docs/SILICOM-BYPASS.md manual exercise"
      to: "silicom-bypass-init.service ExecStart"
      via: "systemctl restart (not start) given RemainAfterExit=yes"
      pattern: "systemctl restart silicom-bypass-init.service"
---

<objective>
Close the three code-review/verification gaps on the Phase 235 deploy and operator surface (235-VERIFICATION.md, 235-REVIEW.md): CR-01 (predictable /tmp privileged staging race), WR-02 (silent extra-positional-arg acceptance in standalone deploy), and WR-01 (manual oneshot runbook incoherent with RemainAfterExit=yes).

Purpose: The Phase 235 goal is explicitly safety-oriented — it installs root-run bypass control tooling on a live dual-WAN production host. The verifier blocked closure on CR-01 and flagged WR-01/WR-02 as warnings. All three are real defects in the actual codebase. This plan makes the deploy surface fail closed and removes the local race/tamper window, without touching the controller path.

Output: Hardened `deploy_silicom_bypass()` staging, fail-closed `--silicom-bypass-only` argument validation, a coherent manual-reapply runbook, and offline tests proving all three. No `src/wanctl` changes (SAFE-16 zero-diff invariant preserved).

Scope guard: This is deploy-script / systemd-unit / docs / test surface ONLY. Do not edit anything under `src/wanctl/`. Do not change `scripts/silicom-bypass`, baseline verb semantics, or any controller-path file. No live-host mutation is required to close these gaps — every fix is offline-verifiable via deploy.sh dry-run output, grep/regex source assertions, and the existing pytest harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-VERIFICATION.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEW.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-CONTEXT.md

<interfaces>
<!-- Current state of the surfaces being modified. Executor must read the files in
     <read_first>, but these anchors orient the edits. -->

scripts/deploy.sh — current deploy_silicom_bypass() (lines ~525-559) uses the predictable-/tmp pattern:
  scp "$PROJECT_ROOT/scripts/silicom-bypass" "$TARGET_HOST:/tmp/silicom-bypass"
  ssh "$TARGET_HOST" "sudo mv /tmp/silicom-bypass /usr/local/sbin/silicom-bypass && sudo chown root:root ... && sudo chmod 755 ..."
  ...same pattern for wanctl-bpctl-init, silicom-bypass.conf.example (install-if-absent), and each SILICOM_BYPASS_SYSTEMD unit via "/tmp/$basename".

scripts/deploy.sh — argument parser (lines ~737-744) declares:
  WAN_NAME="" ; TARGET_HOST="" ; WITH_STEERING=false ;
  WITH_SPECTRUM_CAKE_AUTORATE=false ; WITH_ATT_CAKE_AUTORATE=false ;
  INSTALL_ONLY=false ; SILICOM_BYPASS_ONLY=false ; DRY_RUN=false
  Positional loop (lines ~782-790): first positional -> WAN_NAME, second -> TARGET_HOST, third -> "Unexpected argument" error+exit.

scripts/deploy.sh — current --silicom-bypass-only handler (lines ~811-836):
  if [[ "$SILICOM_BYPASS_ONLY" == "true" ]]; then
      TARGET_HOST="$WAN_NAME"   # unconditional — silently drops a real second positional into ignored TARGET_HOST
      if [[ -z "$TARGET_HOST" ]]; then print_error ...; usage; exit 1; fi
      ... dry-run branch (print_silicom_bypass_plan; exit 0) ...
      check_prerequisites ; deploy_silicom_bypass ; exit 0
  fi
  Compare the --install-only handler (lines ~797-808) which has the same TARGET_HOST="$WAN_NAME" pattern — do NOT change --install-only in this plan; only --silicom-bypass-only.

deploy/systemd/silicom-bypass-init.service (full file):
  [Service]
  Type=oneshot
  RemainAfterExit=yes
  ExecStart=/usr/local/sbin/silicom-bypass baseline

docs/SILICOM-BYPASS.md — manual exercise block (lines ~153-158) currently says:
  "To manually exercise the oneshot during an approved maintenance window:"
  systemctl start silicom-bypass-init.service
  journalctl -u silicom-bypass-init.service --no-pager -l

tests/test_silicom_bypass_cli.py — existing harness conventions to reuse:
  - module constants: DEPLOY, INIT_SERVICE, BPCTL_SERVICE, REPO_ROOT
  - helper _silicom_deploy_function_body(deploy_text) extracts the deploy_silicom_bypass() body via regex
  - helper _silicom_systemd_array_entries(deploy_text) extracts SILICOM_BYPASS_SYSTEMD array entries
  - test_silicom_standalone_dry_run_is_non_mutating() runs: bash deploy.sh --silicom-bypass-only cake-shaper --dry-run
  - subprocess.run(["bash", str(DEPLOY), ...], cwd=REPO_ROOT, capture_output=True, text=True, check=False)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden deploy_silicom_bypass() staging (CR-01) and fail-closed --silicom-bypass-only arg validation (WR-02)</name>
  <files>scripts/deploy.sh, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - scripts/deploy.sh (read lines 480-560 for the existing /tmp staging pattern including the ATT bridge precedent at 490-504, and lines 737-836 for the arg parser + both --install-only and --silicom-bypass-only handlers)
    - tests/test_silicom_bypass_cli.py (read lines 1-30 for imports/constants, 195-225 for the subprocess helper conventions, and 488-581 for the existing deploy.sh regex helpers and standalone-mode tests this task extends)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEW.md (CR-01 fix pattern at lines 35-52, WR-02 fix pattern at lines 70-91)
  </read_first>
  <behavior>
    - Test (staging, CR-01): the deploy_silicom_bypass() function body contains "mktemp -d" and "install -o root -g root" and does NOT contain a literal predictable target path "/tmp/silicom-bypass" or "/tmp/wanctl-bpctl-init" used as an scp destination; it removes its temp dir (contains "rm -rf" of the temp-dir variable).
    - Test (staging chmod 700, CR-01): the function restricts the staging dir before copying into it (contains "chmod 700" applied to the mktemp-d variable, or mktemp invoked such that the dir is user-private).
    - Test (arg fail-closed, WR-02): running `bash deploy.sh --silicom-bypass-only cake-shaper extra-host` exits non-zero and prints a usage/error mentioning the standalone mode; it MUST NOT reach deploy or print the deploy-started banner. Use --dry-run is irrelevant — validation must fire before the dry-run branch too, so assert on `--silicom-bypass-only cake-shaper extra-host` (no dry-run) returning non-zero with the error string.
    - Test (flag conflict fail-closed, WR-02): running `bash deploy.sh --silicom-bypass-only cake-shaper --with-steering` exits non-zero with an error stating the standalone mode cannot be combined with WAN deployment options.
    - Test (happy path still works, regression): `bash deploy.sh --silicom-bypass-only cake-shaper --dry-run` still exits 0 and prints "DRY RUN" (existing test_silicom_standalone_dry_run_is_non_mutating must continue to pass unchanged).
  </behavior>
  <action>
    Two edits to scripts/deploy.sh, both within the deploy surface, plus tests.

    EDIT A (CR-01) — rewrite the body of `deploy_silicom_bypass()` to eliminate predictable-/tmp privileged staging. Create one per-deploy private remote directory via `remote_tmp=$(ssh "$TARGET_HOST" "mktemp -d /tmp/wanctl-silicom.XXXXXX")`, then immediately `ssh "$TARGET_HOST" "chmod 700 '$remote_tmp'"`. For each artifact (`scripts/silicom-bypass` -> /usr/local/sbin/silicom-bypass mode 0755; `scripts/wanctl-bpctl-init` -> /usr/local/sbin/wanctl-bpctl-init mode 0755; each unit in SILICOM_BYPASS_SYSTEMD -> $TARGET_SYSTEMD_DIR/$basename mode 0644), scp into `$remote_tmp/<name>` and install to the destination with `ssh "$TARGET_HOST" "sudo install -o root -g root -m <mode> '$remote_tmp/<name>' <dest>"`. Use `install` (not `mv`+`chown`+`chmod`) so ownership and mode are applied atomically at the destination. Preserve the existing install-if-absent semantics for `/etc/silicom-bypass.conf` (mode 0644): scp the example into `$remote_tmp/silicom-bypass.conf.example`, then guard with `if sudo test -e /etc/silicom-bypass.conf; then : ; else sudo install -o root -g root -m 0644 '$remote_tmp/silicom-bypass.conf.example' /etc/silicom-bypass.conf; fi`. Keep the existing echo progress lines, the SILICOM_BYPASS_SYSTEMD missing-file `print_error`+exit guard, the `/dev/bpctl*` presence warning, and the final `sudo systemctl daemon-reload` + `print_success`. At the end of the function (after daemon-reload, and on the error-exit guard path) clean up with `ssh "$TARGET_HOST" "rm -rf '$remote_tmp'"`. Do NOT alter `deploy_att_cake_autorate` or any other deploy function — only `deploy_silicom_bypass()` is in scope this plan.

    EDIT B (WR-02) — make the `--silicom-bypass-only` handler (currently lines ~811-818) fail closed BEFORE `check_prerequisites`/`deploy_silicom_bypass` and before the dry-run branch. Replace the unconditional `TARGET_HOST="$WAN_NAME"` with validation: require `WAN_NAME` non-empty AND `TARGET_HOST` empty (a non-empty TARGET_HOST means an extra positional was supplied) — if `[[ -z "$WAN_NAME" || -n "$TARGET_HOST" ]]` then `print_error "Usage: $0 --silicom-bypass-only <target_host> [--dry-run]"`, `usage`, `exit 1`. Then reject incompatible deployment flags: if `[[ "$WITH_STEERING" == "true" || "$WITH_SPECTRUM_CAKE_AUTORATE" == "true" || "$WITH_ATT_CAKE_AUTORATE" == "true" ]]` then `print_error "--silicom-bypass-only cannot be combined with WAN deployment options"`, `exit 1`. Only after both checks pass, set `TARGET_HOST="$WAN_NAME"`. Keep the rest of the handler (header echo, dry-run branch calling print_silicom_bypass_plan, check_prerequisites + deploy_silicom_bypass, exit 0) intact. Leave the `--install-only` handler (lines ~797-808) unchanged — it is out of scope.

    TESTS — extend tests/test_silicom_bypass_cli.py using the existing constants and helpers (DEPLOY, REPO_ROOT, _silicom_deploy_function_body, subprocess.run with cwd=REPO_ROOT, check=False). Add: a CR-01 source test asserting the deploy_silicom_bypass() body contains "mktemp -d", "install -o root -g root", "chmod 700", and "rm -rf", and asserting it does not scp to a literal "/tmp/silicom-bypass" destination; a WR-02 behavior test for the extra-positional case (`--silicom-bypass-only cake-shaper extra-host` returns non-zero, error mentions silicom-bypass-only usage, output does not contain "Silicom bypass artifacts deployed"); a WR-02 behavior test for the flag-conflict case (`--silicom-bypass-only cake-shaper --with-steering` returns non-zero with "cannot be combined"). Do not weaken or delete the existing standalone tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -q && bash scripts/deploy.sh --silicom-bypass-only cake-shaper extra-host; test $? -ne 0 && bash scripts/deploy.sh --silicom-bypass-only cake-shaper --dry-run >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -q` reports all tests passed (>= 23, including the new CR-01/WR-02 tests).
    - `grep -n 'mktemp -d' scripts/deploy.sh` matches inside deploy_silicom_bypass(); `grep -n 'install -o root -g root' scripts/deploy.sh` matches for the silicom artifacts.
    - The deploy_silicom_bypass() body contains no `scp ... "$TARGET_HOST:/tmp/silicom-bypass"` style line writing to a predictable shared /tmp path; staging destinations reference the mktemp-d variable.
    - `bash scripts/deploy.sh --silicom-bypass-only cake-shaper extra-host` exits non-zero and its combined output does NOT contain "Silicom bypass artifacts deployed".
    - `bash scripts/deploy.sh --silicom-bypass-only cake-shaper --with-steering` exits non-zero and prints "cannot be combined".
    - `bash scripts/deploy.sh --silicom-bypass-only cake-shaper --dry-run` still exits 0 and prints "DRY RUN".
    - `git status --porcelain src/wanctl` is empty (no controller-path changes).
  </acceptance_criteria>
  <done>
    CR-01 and WR-02 are closed: standalone deploy stages in a private per-deploy mktemp -d dir and installs atomically as root; --silicom-bypass-only rejects extra positionals and incompatible flags before any SSH/mutation; all offline tests pass; no src/wanctl diff.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make manual oneshot runbook coherent with RemainAfterExit=yes (WR-01)</name>
  <files>docs/SILICOM-BYPASS.md, deploy/systemd/silicom-bypass-init.service, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - deploy/systemd/silicom-bypass-init.service (full file — confirm Type=oneshot, RemainAfterExit=yes, ExecStart, and the Before=/After= ordering comment)
    - docs/SILICOM-BYPASS.md (read lines 135-200 for the enable instructions, the manual-exercise block at ~153-158, the equivalent direct-CLI block at ~165-169, and the rollback section that follows)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEW.md (WR-01 at lines 56-68 — note both fix options)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-VERIFICATION.md (gap 2 / truth 13 / WR-01 framing, and the note that live evidence in phase 235 used `restart` successfully)
    - tests/test_silicom_bypass_cli.py (lines 472-486 — test_init_service_artifact asserts on the unit; extend coherence assertions near here)
  </read_first>
  <action>
    Resolve WR-01 by the docs-side fix, NOT by removing RemainAfterExit=yes. Rationale to record in the SUMMARY: RemainAfterExit=yes is the conventional setting for a oneshot boot-ordering unit so that downstream units' `After=silicom-bypass-init.service`/`Before=` semantics see the baseline as a completed-and-held state rather than inactive; the unit's own comment documents it as an ordering anchor pulled in by WantedBy=multi-user.target. Phase 235 live evidence already used `systemctl restart` successfully (235-VERIFICATION truth 15). Removing RemainAfterExit=yes would change boot-transaction/ordering visibility for a production bypass-control unit — out of proportion to a runbook wording bug. Therefore keep the unit unchanged and fix the docs.

    Edit docs/SILICOM-BYPASS.md manual-exercise block (~lines 153-158): change the command from `systemctl start silicom-bypass-init.service` to `systemctl restart silicom-bypass-init.service`, and add one sentence immediately above the code block stating that the unit is a `oneshot` with `RemainAfterExit=yes`, so `systemctl start` is a no-op once the unit is already active after boot and `restart` is required to actually re-run the baseline ExecStart. Keep the following `journalctl -u silicom-bypass-init.service` line. Leave the "Equivalent direct CLI exercise" `silicom-bypass baseline` block (~165-169) as-is — that path always re-runs and remains the alternative; optionally note it is unaffected by RemainAfterExit. Do not change the `systemctl enable silicom-bypass-init.service` instruction at ~143. Do not modify the unit file (it is listed in <files> only so the executor confirms its current directives match the docs claim; the deploy/systemd/silicom-bypass-init.service file should end this task byte-identical to its starting state).

    Add an offline coherence test in tests/test_silicom_bypass_cli.py: assert that whenever the unit text contains "RemainAfterExit=yes", the docs manual-exercise procedure uses "systemctl restart silicom-bypass-init.service" and does NOT instruct "systemctl start silicom-bypass-init.service" in that manual-reapply context. Read the unit via the existing INIT_SERVICE constant and the docs via a new module constant pointing at docs/SILICOM-BYPASS.md (REPO_ROOT / "docs" / "SILICOM-BYPASS.md"). The test must fail if someone later reintroduces `systemctl start silicom-bypass-init.service` as the documented manual reapply command while RemainAfterExit=yes is still present, OR if RemainAfterExit is removed without the docs being revisited (i.e. gate the restart assertion on RemainAfterExit presence so the two stay coherent).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -q && grep -q 'systemctl restart silicom-bypass-init.service' docs/SILICOM-BYPASS.md && ! grep -qE '^[[:space:]]*systemctl start silicom-bypass-init\.service' docs/SILICOM-BYPASS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'systemctl restart silicom-bypass-init.service' docs/SILICOM-BYPASS.md` matches in the manual-exercise block.
    - `grep -vE '^\s*#' docs/SILICOM-BYPASS.md | grep -c 'systemctl start silicom-bypass-init.service'` returns 0 (no documented manual-reapply `start`).
    - docs/SILICOM-BYPASS.md mentions RemainAfterExit (or "no-op" rationale) near the manual-exercise block explaining why restart is required.
    - `grep -c 'RemainAfterExit=yes' deploy/systemd/silicom-bypass-init.service` returns 1 (unit unchanged; existing test_init_service_artifact still passes).
    - `git diff --stat deploy/systemd/silicom-bypass-init.service` shows no changes.
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -q` passes including the new coherence test.
    - `git status --porcelain src/wanctl` is empty.
  </acceptance_criteria>
  <done>
    WR-01 closed: the manual oneshot runbook uses `systemctl restart` and explains the RemainAfterExit=yes no-op-on-start hazard; the unit is unchanged; a coherence test locks the docs and unit together; no src/wanctl diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy operator workstation -> target host (SSH) | Operator-driven privileged install of root-run bypass-control artifacts onto a live production host |
| other local users/processes on target -> staged install artifacts | Any local principal on the target can observe/write world-accessible paths during the staging window |
| operator -> standalone deploy CLI args | Operator-supplied positional target host + mode flags select which host gets mutated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-01 | Tampering / Elevation | deploy_silicom_bypass() /tmp staging (CR-01) | mitigate | Stage in per-deploy `mktemp -d /tmp/wanctl-silicom.XXXXXX` with `chmod 700`, then `sudo install -o root -g root -m <mode>` atomically to destination and `rm -rf` the temp dir. Closes the predictable-path race window between scp and privileged install (Task 1, EDIT A). |
| T-235-02 | Spoofing / wrong-target deploy | --silicom-bypass-only arg handler (WR-02) | mitigate | Reject extra positional args and incompatible WAN flags before any SSH/mutation; fail closed with explicit usage/error (Task 1, EDIT B). Prevents ambiguous/silent selection of an unintended production host. |
| T-235-03 | Repudiation / false-confidence | silicom-bypass-init manual reapply runbook (WR-01) | mitigate | Document `systemctl restart` (not `start`) given RemainAfterExit=yes, so an operator's manual re-apply provably re-runs the baseline ExecStart instead of silently no-opping (Task 2). |
| T-235-04 | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; all changes are bash/systemd-unit/markdown/pytest edits to existing repo files. No new dependencies, no install tasks. |
| T-235-SC | Tampering | supply-chain package staging | accept | N/A — no packages added or installed; nothing to audit. RESEARCH.md Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -q` — full offline suite passes (existing 23 + new CR-01/WR-02/WR-01 tests).
- `bash scripts/deploy.sh --silicom-bypass-only cake-shaper extra-host` — exits non-zero, no deploy banner (WR-02 extra positional).
- `bash scripts/deploy.sh --silicom-bypass-only cake-shaper --with-steering` — exits non-zero, "cannot be combined" (WR-02 flag conflict).
- `bash scripts/deploy.sh --silicom-bypass-only cake-shaper --dry-run` — exits 0, "DRY RUN" (regression: happy path intact).
- `grep -n 'mktemp -d' scripts/deploy.sh` and `grep -n 'install -o root -g root' scripts/deploy.sh` — CR-01 hardening present.
- `grep -n 'systemctl restart silicom-bypass-init.service' docs/SILICOM-BYPASS.md` and `grep -vE '^\s*#' docs/SILICOM-BYPASS.md | grep -c 'systemctl start silicom-bypass-init.service'` == 0 — WR-01 fixed.
- `git status --porcelain src/wanctl` — empty (SAFE-16 controller-path zero-diff holds).
</verification>

<success_criteria>
All three 235-VERIFICATION gaps are closed and offline-verifiable:
- CR-01: no predictable /tmp privileged staging; per-deploy private mktemp -d + atomic `install -o root -g root`; temp dir cleaned up.
- WR-02: --silicom-bypass-only fails closed on extra positionals and incompatible WAN flags before any mutation.
- WR-01: manual oneshot runbook uses `systemctl restart` with documented RemainAfterExit rationale; unit unchanged; coherence test locks docs+unit.
- No `src/wanctl` files modified (SAFE-16 invariant preserved).
- No live-host mutation performed by the executor (any real re-deploy remains an operator-gated action outside this plan).
</success_criteria>

<output>
Create `.planning/phases/235-bypass-operator-cli-boot-baseline/235-04-SUMMARY.md` when done. Record the WR-01 decision rationale (kept RemainAfterExit=yes, fixed docs to restart) explicitly so the verifier sees the chosen-and-justified single fix.
</output>
