---
phase: 235-bypass-operator-cli-boot-baseline
plan: 03
type: execute
wave: 3
depends_on: ["235-01", "235-02"]
files_modified:
  - scripts/deploy.sh
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
  - .planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json
autonomous: false
requirements: [SAFE-16]
user_setup: []
must_haves:
  truths:
    - "The new 235 artifacts (silicom-bypass, conf example, init unit) are installable via a TRUE standalone deploy mode (`--silicom-bypass-only <host>`) that short-circuits BEFORE the wanctl release/restart path — it does NOT run deploy_code/deploy_config/verify/validation"
    - "docs/SILICOM-BYPASS.md documents the CLI usage and the operator-gated live procedures (status all, manual oneshot run, rollback) with VALID deploy syntax"
    - "SAFE-16 controller-path zero-diff is VERIFIED at the phase boundary via the turnkey checker against anchor v1.51 (passed=true, controller_path_diff_count=0)"
    - "Live-host verification (real bpctl_util, enabling/running the oneshot) is an operator checkpoint, never executed autonomously; the checkpoint distinguishes offline-acceptance-passed from live-verified vs live-waived"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "Standalone --silicom-bypass-only mode (short-circuit) + deploy_silicom_bypass() + SILICOM_BYPASS_SYSTEMD array"
      contains: "silicom-bypass-only"
    - path: ".planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json"
      provides: "SAFE-16 boundary proof JSON (passed=true)"
      contains: "passed"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "CLI usage + operator-gated live procedures + rollback (valid deploy syntax)"
      contains: "silicom-bypass"
  key_links:
    - from: "scripts/deploy.sh --silicom-bypass-only"
      to: "deploy_silicom_bypass()"
      via: "standalone short-circuit (mirrors --install-only) before WAN validation"
      pattern: "silicom-bypass-only"
    - from: "deploy_silicom_bypass()"
      to: "deploy/systemd/silicom-bypass-init.service"
      via: "SILICOM_BYPASS_SYSTEMD array + install loop"
      pattern: "SILICOM_BYPASS_SYSTEMD"
    - from: "phase225-safe13-boundary-check.sh --anchor v1.51"
      to: "src/wanctl/** zero-diff"
      via: "git anchor comparison"
      pattern: "controller_path_diff_count"
---

<objective>
Wire a TRUE standalone, operator-gated deploy mode for the new 235 artifacts (decoupled from the wanctl release/restart path), document the CLI and the operator-gated live procedures in docs/SILICOM-BYPASS.md with valid syntax, prove SAFE-16 controller-path zero-diff at the phase boundary, and run the operator-gated live verification (real `status all`, manual oneshot run). This plan contains the only live-host steps in the phase — all behind an operator checkpoint.

Purpose: Make the artifacts installable WITHOUT running the broader wanctl deploy pipeline (code rsync, config, verify, validation, next-steps), give the operator a documented runbook + rollback, and close the phase with a verified (not assumed) SAFE-16 boundary. Covers SAFE-16.

Output: extended `scripts/deploy.sh` (standalone mode), deploy-seam tests in `tests/test_silicom_bypass_cli.py`, extended `docs/SILICOM-BYPASS.md`, SAFE-16 evidence JSON, operator sign-off on live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-CONTEXT.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-RESEARCH.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-PATTERNS.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEWS.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-01-SUMMARY.md
@.planning/phases/235-bypass-operator-cli-boot-baseline/235-02-SUMMARY.md

<interfaces>
<!-- Concrete deploy + SAFE-16 contracts. -->

STANDALONE-MODE structure (Review fix: 235-REVIEWS 235-03 HIGH #4). The existing `--with-*` flags are
ADDITIVE flags inside the main flow: they still run deploy_code, deploy_config, verify_deployment,
validate-deployment, wanctl-history/operator-summary install, and the next-steps banner (scripts/deploy.sh
lines 800-832 call all of these unconditionally). An additive `--with-silicom-bypass` would therefore NOT
be "decoupled from the wanctl release/restart path" — it would run the whole pipeline. The CORRECT pattern
is the existing `--install-only` short-circuit (scripts/deploy.sh:704-707 parse, 737-749 handle): it sets a
flag, and a block near the top of "main" exits(0) BEFORE any WAN validation or deploy_* calls. Model the new
mode on THAT.

  Parse (add to the while/case at scripts/deploy.sh:690-734):
    --silicom-bypass-only) SILICOM_BYPASS_ONLY=true ; shift ;;
  Short-circuit (add a block alongside the `if [[ "$INSTALL_ONLY" == "true" ]]` block at lines 736-749,
  i.e. BEFORE "Validate arguments" at 751 and BEFORE the main deploy_* calls at 810+):
    if [[ "$SILICOM_BYPASS_ONLY" == "true" ]]; then
        TARGET_HOST="$WAN_NAME"        # first positional is the host, same convention as --install-only
        [host-required guard like --install-only]
        print_header "Silicom bypass standalone deploy"
        check_prerequisites
        deploy_silicom_bypass
        exit 0
    fi
  This guarantees deploy_code/deploy_config/verify_deployment/validate/next-steps DO NOT run.
  Initialize SILICOM_BYPASS_ONLY=false in the var block (lines 682-688).

Deploy array pattern (scripts/deploy.sh:62-71): `NAME_SYSTEMD=( "deploy/systemd/..." )`. Add:
  SILICOM_BYPASS_SYSTEMD=( "deploy/systemd/silicom-bypass-init.service" )

Install-function template (scripts/deploy.sh:456-504, deploy_att_cake_autorate):
  scp "$PROJECT_ROOT/<file>" "$TARGET_HOST:/tmp/<base>"
  ssh "$TARGET_HOST" "sudo mv /tmp/<base> <dest> && sudo chown root:root <dest> && sudo chmod <mode> <dest>"
  loop *_SYSTEMD[@] into $TARGET_SYSTEMD_DIR ; end with `ssh ... sudo systemctl daemon-reload`
  Soft-presence-guard model at lines 497-499: `if ! ssh ... test -x ...; then print_warning ...; fi` (absent ⇒ warn, don't fail).
  Install map for 235:
    scripts/silicom-bypass               -> /usr/local/sbin/silicom-bypass        chmod 755
    deploy/scripts/silicom-bypass.conf.example -> /etc/silicom-bypass.conf         chmod 644, install-if-absent (do NOT clobber operator edits)
    deploy/systemd/silicom-bypass-init.service -> $TARGET_SYSTEMD_DIR              (via array loop)
  deploy_silicom_bypass MUST NOT enable or start any unit (daemon-reload only). enable/run is the Task 3 operator checkpoint.

DRY-RUN coverage (Review fix: 235-REVIEWS 235-03 MEDIUM): the dry-run block (scripts/deploy.sh:778-797)
must report the standalone silicom path when SILICOM_BYPASS_ONLY=true (since the short-circuit runs before
the existing dry-run block, add a dry-run branch INSIDE the standalone short-circuit, or honor --dry-run in
deploy_silicom_bypass by printing planned actions and skipping scp/ssh).

VALID runbook syntax (Review fix: 235-REVIEWS 235-03 MEDIUM): the current parser requires `wan_name
target_host` except for --install-only; `deploy.sh --with-silicom-bypass` is INVALID. The standalone form is
  ./scripts/deploy.sh --silicom-bypass-only cake-shaper
(first positional is the host, mirroring `--install-only target-host`). Docs MUST use this exact form.

SAFE-16 turnkey checker (scripts/phase225-safe13-boundary-check.sh — supports --anchor REF / --out PATH):
  scripts/phase225-safe13-boundary-check.sh --anchor v1.51 \
    --out .planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json
  PASS == exit 0 + JSON passed=true + controller_path_diff_count=0. Protected paths: wan_controller.py, queue_controller.py, cake_signal.py, backends/, alert_engine.py, fusion.

Static deploy-seam test analog (tests/test_att_cake_autorate_artifacts.py:280-307 + the _*_systemd_array_entries regex helper + test_deploy_script_has_external_att_mode lines 40-47).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRUE standalone --silicom-bypass-only deploy mode in deploy.sh + deploy-seam tests</name>
  <files>scripts/deploy.sh, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - scripts/deploy.sh (lines 60-75 array declarations; 456-504 deploy_att_cake_autorate install template; 682-688 var init; 690-734 arg-parse; 736-749 --install-only short-circuit MODEL; 751-756 validation; 778-797 dry-run; 810-832 main deploy_* + verify/validate calls the standalone mode must NOT run)
    - scripts/silicom-bypass (the CLI being installed — confirm install path /usr/local/sbin/silicom-bypass)
    - deploy/systemd/silicom-bypass-init.service (the unit being installed)
    - deploy/scripts/silicom-bypass.conf.example (the conf example → /etc/silicom-bypass.conf install-if-absent)
    - tests/test_att_cake_autorate_artifacts.py (deploy-list regex cross-check + array-entry helper + has-mode static test)
    - tests/test_silicom_bypass_cli.py (extend with the deploy-seam tests)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-PATTERNS.md (deploy.sh self-extend guidance; Assumption A4 minimal seam)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEWS.md (235-03 HIGH #4: standalone mode required; MEDIUM: valid syntax + dry-run coverage)
  </read_first>
  <action>
    Extend scripts/deploy.sh with a TRUE standalone, operator-gated mode modeled on `--install-only` (NOT an additive --with-* flag — see <interfaces> STANDALONE-MODE; an additive flag would still run the full wanctl deploy pipeline and would NOT be decoupled from the release/restart path):
    - Add array `SILICOM_BYPASS_SYSTEMD=( "deploy/systemd/silicom-bypass-init.service" )` near the other *_SYSTEMD arrays (lines 62-71).
    - Initialize `SILICOM_BYPASS_ONLY=false` in the var block (lines 682-688).
    - Add `--silicom-bypass-only) SILICOM_BYPASS_ONLY=true; shift ;;` to the arg-parse while/case (lines 690-734), and add a usage() line documenting it (syntax: `--silicom-bypass-only <target_host>`).
    - Add a SHORT-CIRCUIT block alongside the `--install-only` handler (after lines 749, before "Validate arguments" at 751): when SILICOM_BYPASS_ONLY=true, set TARGET_HOST="$WAN_NAME" (first positional is the host), guard that a host was given (mirror --install-only's required-host check), then if DRY_RUN print the planned silicom actions and exit 0; else `check_prerequisites; deploy_silicom_bypass; exit 0`. This MUST exit before deploy_code/deploy_config/verify_deployment/validate/next-steps ever run.
    - Add a `deploy_silicom_bypass()` function modeled on deploy_att_cake_autorate(): scp+ssh-install scripts/silicom-bypass → /usr/local/sbin/silicom-bypass (chown root:root, chmod 755); install deploy/scripts/silicom-bypass.conf.example → /etc/silicom-bypass.conf with chmod 644 ONLY IF ABSENT (ssh test -e guard so operator edits are never clobbered); loop SILICOM_BYPASS_SYSTEMD[@] into $TARGET_SYSTEMD_DIR; end with `sudo systemctl daemon-reload`. Add a soft presence guard (model lines 497-499) that warns — does not fail — if the bpctl module/device path is absent on the host. Do NOT auto-enable the unit and do NOT start anything (enable/run is the operator checkpoint in Task 3).
    Then add static deploy-seam tests to tests/test_silicom_bypass_cli.py (mirror test_att_cake_autorate_artifacts.py:280-307,40-47):
    - test_artifacts_repo_owned: assert SILICOM_BYPASS_SYSTEMD entries each exist as repo files; assert deploy.sh references scripts/silicom-bypass, the conf example, and the init unit basename; assert deploy.sh references no nonexistent silicom-bypass file.
    - test_deploy_has_silicom_standalone_mode: assert deploy.sh text contains the flag string `--silicom-bypass-only`, the var `SILICOM_BYPASS_ONLY`, and the function name `deploy_silicom_bypass`.
    - test_silicom_standalone_short_circuits: read deploy.sh and assert the standalone handler block ends in `exit 0` and appears BEFORE the main `deploy_code` call (use line-index comparison: the index of the `SILICOM_BYPASS_ONLY` short-circuit `exit 0` is less than the index of the unconditional `deploy_code` invocation) — proving the standalone mode does not fall through into the full deploy pipeline. (This is the graded proof of HIGH #4.)
    Do NOT modify watchdog scripts/units or src/wanctl/** (SAFE-16).
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && .venv/bin/pytest tests/test_silicom_bypass_cli.py::test_artifacts_repo_owned tests/test_silicom_bypass_cli.py::test_deploy_has_silicom_standalone_mode tests/test_silicom_bypass_cli.py::test_silicom_standalone_short_circuits -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/deploy.sh contains `SILICOM_BYPASS_SYSTEMD=(`, a `SILICOM_BYPASS_ONLY` var initialized false, a `--silicom-bypass-only` case, and a `deploy_silicom_bypass()` function
    - The standalone handler sets TARGET_HOST from the first positional, guards host presence, and `exit 0`s BEFORE deploy_code is ever called (test_silicom_standalone_short_circuits proves the ordering)
    - deploy_silicom_bypass references /usr/local/sbin/silicom-bypass, /etc/silicom-bypass.conf (install-if-absent via an ssh test -e guard), and silicom-bypass-init.service, and enables/starts NO unit (daemon-reload only)
    - --dry-run under --silicom-bypass-only prints planned silicom actions and makes no scp/ssh changes
    - deploy.sh does NOT scp/ssh-install any file that does not exist in the repo
    - test_artifacts_repo_owned, test_deploy_has_silicom_standalone_mode, test_silicom_standalone_short_circuits pass
    - No file under src/wanctl/ and no watchdog file is modified
  </acceptance_criteria>
  <done>TRUE standalone --silicom-bypass-only mode wired (short-circuits before the wanctl deploy pipeline, proven by an ordering test); install does not enable/start units; dry-run covers the silicom path; not coupled to the release path.</done>
</task>

<task type="auto">
  <name>Task 2: Document CLI + operator-gated live procedures (valid syntax); prove SAFE-16 boundary</name>
  <files>docs/SILICOM-BYPASS.md, .planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json</files>
  <read_first>
    - docs/SILICOM-BYPASS.md (existing hardware RCA + validated baseline strings — append CLI usage + procedures; do NOT rewrite the RCA)
    - scripts/silicom-bypass (final verb surface + exit codes to document)
    - deploy/systemd/silicom-bypass-init.service (boot behavior + ordering-only/must-be-enabled note to document)
    - scripts/deploy.sh (the FINAL --silicom-bypass-only syntax to document — host as first positional)
    - scripts/phase225-safe13-boundary-check.sh (--anchor / --out flags; protected target list; JSON shape passed/controller_path_diff_count)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-RESEARCH.md (SAFE-16 boundary proof invocation; rollback = off+conn both pairs)
    - .planning/phases/235-bypass-operator-cli-boot-baseline/235-REVIEWS.md (235-03 MEDIUM: valid deploy syntax in the runbook)
  </read_first>
  <action>
    Append to docs/SILICOM-BYPASS.md (do not disturb the settled RCA content): a "silicom-bypass CLI" section documenting every verb (status/on/off/disc/conn/mark/baseline), the `--yes` and `--both-wan-confirm` gates (note that both-WAN-confirm triggers when the OTHER pair is already non-NIC via Bypass OR Disconnect), the exit-code grammar (0 ok/no-op, 1 runtime/read-back fail, 2 usage), the config (/etc/silicom-bypass.conf, PAIRS="att-modem spec-modem"), and the marks log path. Add an "Operator-gated live procedures" subsection:
      - Install via the STANDALONE mode using the VALID syntax `./scripts/deploy.sh --silicom-bypass-only cake-shaper` (first positional is the host; do NOT write `deploy.sh --with-silicom-bypass`, which is invalid for the parser). State plainly this mode installs only and does not run the wanctl release/restart pipeline, and enables/starts nothing.
      - How to run `silicom-bypass status all` on the live host (read-only).
      - That `silicom-bypass-init.service` is ordering-only and must be ENABLED on the host (`systemctl enable silicom-bypass-init`) to participate in boot — installation alone does not enable it.
      - How to manually run/enable the oneshot and inspect `journalctl -u silicom-bypass-init` — expect mandatory per-pair readiness wait, then all five settings read-back-asserted on both pairs, loud failure on mismatch or never-ready pair.
      - ROLLBACK path = `silicom-bypass off <pair>` + `silicom-bypass conn <pair>` on BOTH att-modem and spec-modem to restore NIC mode.
    State plainly that running real bpctl_util / enabling units on the live dual-WAN host is an operator action. Use the live pair names att-modem/spec-modem; never sil-spare1/sil-spare2.
    Then run the SAFE-16 boundary proof and capture evidence:
      mkdir -p .planning/phases/235-bypass-operator-cli-boot-baseline/evidence
      scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json
    Confirm the checker exits 0 and the JSON has passed=true and controller_path_diff_count=0. If it fails, a controller-path file was touched in this phase — stop and remove the offending diff (this phase is scripts/units/docs/tests only). Do NOT author a bespoke checker; use the turnkey one.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json && python3 -c "import json,sys; d=json.load(open('.planning/phases/235-bypass-operator-cli-boot-baseline/evidence/safe16-boundary-235.json')); sys.exit(0 if d.get('passed') is True and d.get('controller_path_diff_count')==0 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/SILICOM-BYPASS.md contains a CLI section documenting status/on/off/disc/conn/mark/baseline, the --yes and --both-wan-confirm gates, and the rollback (off+conn both pairs) procedure
    - docs uses the VALID standalone deploy syntax `--silicom-bypass-only cake-shaper` and does NOT contain the invalid `--with-silicom-bypass` form
    - docs notes the init unit must be `systemctl enable`d to participate in boot (ordering-only)
    - docs/SILICOM-BYPASS.md uses att-modem/spec-modem and contains no new sil-spare1/sil-spare2 references
    - evidence/safe16-boundary-235.json exists with `passed` true and `controller_path_diff_count` == 0
    - `scripts/phase225-safe13-boundary-check.sh --anchor v1.51` exits 0
    - No file under src/wanctl/ is modified
  </acceptance_criteria>
  <done>CLI + operator-gated live runbook (valid standalone syntax, enable-to-boot note) + rollback documented; SAFE-16 boundary verified (not assumed) with committed evidence JSON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-gated live verification on cake-shaper (LIVE HOST — never autonomous)</name>
  <action>Operator-only checkpoint — the executor performs NO action here. Pause and surface the live-host verification runbook below; all OFFLINE acceptance criteria are already satisfied. Resume only on operator sign-off, recording whether live steps were run, waived, or surfaced issues.</action>
  <what-built>
    A guarded `silicom-bypass` CLI (status/on/off/disc/conn/mark/baseline), a `silicom-bypass-init.service` boot oneshot that mandatorily waits for each pair to be bpctl-capable then applies + read-back-asserts the known-good baseline to both pairs, a TRUE standalone operator-gated deploy mode (`deploy.sh --silicom-bypass-only cake-shaper`), and a verified SAFE-16 controller-path zero-diff. All automated acceptance criteria already passed OFFLINE via the stateful fake bpctl_util seam.
  </what-built>
  <how-to-verify>
    These are LIVE-HOST steps on the production dual-WAN cake-shaper. Run only when you choose to; the executor must NOT run any of these.
    1. (Optional) Install: `./scripts/deploy.sh --silicom-bypass-only cake-shaper` to place /usr/local/sbin/silicom-bypass, /etc/silicom-bypass.conf (install-if-absent), and the init unit (daemon-reload only; nothing enabled/started, and the wanctl release pipeline is NOT run).
    2. Read-only smoke: `silicom-bypass status all` over SSH — expect live per-pair NIC/bypass/disconnect for att-modem and spec-modem. No state change.
    3. (Optional) Enable + baseline: `systemctl enable silicom-bypass-init`, then `systemctl start silicom-bypass-init` (or `silicom-bypass baseline`), then `journalctl -u silicom-bypass-init` — expect the per-pair readiness wait, all five settings read-back-asserted on both pairs, loud failure if any mismatch or never-ready pair.
    4. ROLLBACK if anything looks wrong: `silicom-bypass off <pair>` then `silicom-bypass conn <pair>` on BOTH att-modem and spec-modem to restore NIC mode.
    Do NOT run `on`/`disc` on a live pair unless you intend to drop that WAN; never put both pairs non-NIC without `--both-wan-confirm` and intent.
  </how-to-verify>
  <resume-signal>
    Type one of:
      - "approved: live verified" (you ran the live steps and they passed),
      - "approved: live waived" (you accept offline acceptance and explicitly defer live verification — recorded as an OPERATOR WAIVER, NOT as live-verified),
      - or describe issues to address.
    All OFFLINE acceptance criteria are already satisfied; the roadmap "oneshot proven after boot/manual run" criterion is only met by "live verified" — a waiver must be logged as such in the summary, not silently treated as verified. (Review fix: 235-REVIEWS 235-03 MEDIUM.)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.sh → live host (scp/ssh) | Installs artifacts via a standalone mode; must not run the release path or auto-enable/start units |
| operator checkpoint → live card | Only sanctioned place a real bpctl_util runs against live WANs |
| phase boundary → controller path | SAFE-16 zero-diff must be proven, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-235-10 | Denial of Service | deploy seam coupled to wanctl release/restart | mitigate | TRUE standalone --silicom-bypass-only mode short-circuits (exit 0) before deploy_code/verify/validate; ordering test proves it; function installs only, never enables/starts |
| T-235-11 | Tampering | conf install clobbers operator-edited /etc/silicom-bypass.conf | mitigate | install-if-absent (ssh test -e guard); never overwrite existing conf |
| T-235-12 | Denial of Service | executor runs real bpctl_util / enables oneshot on live host | mitigate | All live steps in a blocking checkpoint:human-verify (autonomous: false); offline tests satisfy every automated acceptance criterion |
| T-235-05 | Tampering | controller-path mutation slips into the phase | mitigate | scripts/phase225-safe13-boundary-check.sh --anchor v1.51 emits passed=true + controller_path_diff_count=0; evidence committed |
| T-235-13 | Repudiation | SAFE-16 claimed but not proven; OR live verification waiver recorded as verified | mitigate | Evidence JSON is a committed artifact; verify command re-runs the turnkey checker; checkpoint distinguishes "live verified" from "live waived" so a waiver is never logged as verified |
</threat_model>

<verification>
- `scripts/phase225-safe13-boundary-check.sh --anchor v1.51` exits 0; evidence JSON passed=true, controller_path_diff_count=0
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -q` exits 0 (full TOOL+BOOT+deploy suite green, incl. standalone short-circuit ordering test)
- deploy_silicom_bypass installs only — does not enable or start any unit; standalone mode exits before the main deploy pipeline
- docs use the valid `--silicom-bypass-only` syntax and live pair names only
- `git status --porcelain src/wanctl/` is empty
</verification>

<success_criteria>
- new 235 artifacts installable via a TRUE standalone operator-gated `deploy.sh --silicom-bypass-only cake-shaper`, short-circuiting before (decoupled from) the release path — proven by an ordering test
- CLI + operator-gated live procedures (valid syntax, enable-to-boot note) + rollback documented in docs/SILICOM-BYPASS.md
- SAFE-16 controller-path zero-diff VERIFIED at the phase boundary (evidence JSON)
- live-host verification is an operator checkpoint that distinguishes verified vs waived, never autonomous
</success_criteria>

<output>
Create `.planning/phases/235-bypass-operator-cli-boot-baseline/235-03-SUMMARY.md` when done
</output>
</content>
