---
phase: 213-experience-baseline-harness
plan: 04
type: execute
wave: 3
depends_on: [213-01, 213-02, 213-03]
files_modified:
  - scripts/phase213-baseline-capture.sh
  - scripts/phase213-classify.py
  - docs/RUNBOOKS/baseline.md
  - .planning/phases/213-experience-baseline-harness/evidence/README.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
tags: [baseline, harness, orchestrator, classifier, runbook, evidence-index]

must_haves:
  truths:
    - "Operator runs ONE command: `bash scripts/phase213-baseline-capture.sh`. Per D-09 the orchestrator wires every leaf script from Plans 02 and 03."
    - "Per-WAN sequencing D-11 enforced: Spectrum suite to completion, then ATT — never concurrent."
    - "Egress probe gate (Phase 198 pattern) refuses to run flent if --local-bind doesn't egress the intended WAN."
    - "Classifier emits a six-bucket signal-sheet.json + signal-sheet.md WITHOUT comparing steering /health values to threshold-field-name semantics (D-08/D-14)."
    - "Single ranked next-phase recommendation (one of 214/215/216) with runners-up appears in signal-sheet output (D-15)."
    - "evidence/README.md mirrors Phase 212's 6-column command-index table + redaction policy + production-boundary sections."
    - "docs/RUNBOOKS/baseline.md leads with the one-command invocation, lists D-10 forbidden operations, and points to the bucket-→-evidence map."
  artifacts:
    - path: "scripts/phase213-baseline-capture.sh"
      provides: "Top-level orchestrator. CLI per RESEARCH §Orchestrator skeleton."
      min_lines: 250
    - path: "scripts/phase213-classify.py"
      provides: "Offline NDJSON→signal-sheet emitter; reads run-dir, emits JSON+MD."
      min_lines: 300
    - path: "docs/RUNBOOKS/baseline.md"
      provides: "One-command operator runbook per D-09; mirrors docs/SOAK_HARNESS.md style."
      min_lines: 80
    - path: ".planning/phases/213-experience-baseline-harness/evidence/README.md"
      provides: "Phase 213 evidence-index template mirroring Phase 212 evidence/README.md shape (6-column table populated by orchestrator at run-time, plus standing redaction-policy + production-boundary text)."
      min_lines: 40
  key_links:
    - from: "scripts/phase213-baseline-capture.sh"
      to: "scripts/phase213-{health-poller,browse-loop,alert-window,steering-snapshot}.sh"
      via: "bash invocation within run_bracketed_test() per RESEARCH §Pattern 1"
      pattern: "scripts/phase213-(health-poller|browse-loop|alert-window|steering-snapshot)\\.sh"
    - from: "scripts/phase213-baseline-capture.sh"
      to: "scripts/phase191-flent-capture.sh"
      via: "wrapper call with --tests <one-test> --wan <wan> --local-bind <ip>"
      pattern: "phase191-flent-capture.sh"
    - from: "scripts/phase213-classify.py"
      to: "evidence/RUN-<ts>/<wan>/<test>/*.ndjson + alerts-*.json + flent summaries + browse.curl.csv"
      via: "argparse --run-dir; stdlib-only file reads"
      pattern: "--run-dir"
    - from: "scripts/phase213-classify.py"
      to: "signal-sheet.json + signal-sheet.md"
      via: "--output-json / --output-md flags"
      pattern: "--output-json"
---

<objective>
Wire Waves 0/1/2 surfaces into the single operator-invocable Phase 213 harness. Build the orchestrator that runs the per-WAN sequenced test suite (D-09, D-11), the offline classifier that emits the six-bucket signal sheet (BASE-03, D-13, D-14, D-15), the operator runbook (D-09), and the evidence-dir command index (D-12 mirroring Phase 212 convention).

Purpose: The orchestrator is the BASE-01 deliverable. The classifier is the BASE-03 deliverable. Together they convert "operator runs one command" into a per-run evidence tree + signal sheet + ranked next-phase recommendation. Plan 04 closes the codepath; Plan 05 executes it for real and authors the report.

Output: Four files. Three new scripts/docs + one evidence README. All four pre-existing Wave 0 tests (mutation-boundary, ndjson-schema, classify, manifest-schema) GREEN after this plan ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-02-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-03-SUMMARY.md
@scripts/phase198-rerun-flent-3run.sh
@scripts/phase198-loaded-window-audit.py
@scripts/phase191-flent-capture.sh
@scripts/analyze_baseline.py
@docs/SOAK_HARNESS.md
@.planning/phases/212-production-inventory-and-drift-audit/evidence/README.md

<interfaces>
<!-- Orchestrator CLI (from RESEARCH §Orchestrator skeleton): -->
<!-- scripts/phase213-baseline-capture.sh [--local-bind <ip>] [--host <name>] [--flent-duration <sec>] -->
<!--   [--browse-duration <sec>] [--pre-buf <sec>] [--post-buf <sec>] [--wans <csv>] [--tests <csv>] -->
<!--   [--evidence-root <dir>] [--dry-run] [--help] -->

<!-- Default test order per WAN: browse,tcp_upload,tcp_download,rrul,tcp_12down -->
<!-- Default WAN order: spectrum,att (D-11 serialized) -->

<!-- Classifier CLI: -->
<!-- .venv/bin/python3 scripts/phase213-classify.py --run-dir <RUN-dir> --output-json <path> --output-md <path> -->

<!-- Six bucket keys (exactly): -->
<!-- upload_ceiling_setpoint, download_recovery_lag, measurement_collapse, -->
<!-- steering_drift, refractory_semantics, external_isp -->

<!-- Bucket thresholds (RESEARCH §Bucket → Evidence Map; classifier MUST cite these as module-level constants): -->
<!-- BUCKET 1: pct_samples_at_ceiling > 0.80 during a tcp_upload test -->
<!-- BUCKET 2: time_to_green_after_red_sec > 30 OR cake_dl_peak_delay_us_p99 > 50000 -->
<!-- BUCKET 3: signal_outlier_rate_max > 0.30 AND flent_p99 > flent_median * 5 -->
<!-- BUCKET 4: ANY raw state transition or counter delta during test window (NO threshold-name compare per D-14) -->
<!-- BUCKET 5: pct_samples_refractory_active > 0.05 OR backlog_suppressed_delta > 100 in a test window -->
<!-- BUCKET 6: flent_throughput_drop_pct_vs_plan > 0.30 AND curl_ttfb_p99_ms > 2000 AND signal_outlier_rate_max < 0.10 -->

<!-- Plan-default upload/download caps for BUCKET 6 (RESEARCH §Bucket → Evidence Map): -->
<!-- Spectrum: 920 Mbps DL, 40 Mbps UL plan; ATT: 95 Mbps DL, 18 Mbps UL plan -->

<!-- Per-run top-level manifest.json schema (RESEARCH §Per-Run Artifact Layout): -->
<!-- phase, run_id, started_utc, ended_utc, host_dev_vm, source_ip, netperf_host, -->
<!-- flent_version, wanctl_version_dev_repo, wanctl_version_{spectrum,att,steering}_runtime, -->
<!-- git_head_sha, tests_ordered (array of {wan, test, test_start_unix, test_end_unix}), -->
<!-- redaction_posture, mutation_posture -->

<!-- Egress probe expected IPs (Phase 191/198 known mapping; orchestrator records observed and refuses on mismatch): -->
<!-- Spectrum egress: 70.123.224.169 (Phase 198 verified); ATT egress: record-only (no enforced value yet) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/phase213-baseline-capture.sh orchestrator (D-09, D-11, BASE-01)</name>
  <read_first>
    - scripts/phase198-rerun-flent-3run.sh (entire file 449 lines — PRIMARY analog: header docstring, set -euo pipefail, usage(), long-flag parsing, per-attempt subdir + parallel /health subshell, egress probe gate, jq -n manifest assembly)
    - scripts/phase191-flent-capture.sh (entire file 191 lines — confirm --tests/--label/--wan/--local-bind/--host/--duration/--output-dir/--ref CLI surface; this orchestrator INVOKES it per-test)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Orchestrator skeleton" + §"Pattern 1: Per-Test Bracketed Polling" + §"Per-Run Artifact Layout" (manifest schema)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-baseline-capture.sh" (analog assignment + 5 sub-sections of copy-from/change-from)
    - scripts/phase213-health-poller.sh (Plan 02 output — confirm CLI: --endpoint/--wan/--output/--duration)
    - scripts/phase213-browse-loop.sh (Plan 02 output — confirm CLI: --output/--duration/--local-bind/--sites)
    - scripts/phase213-alert-window.sh (Plan 03 output — confirm CLI: --start/--end/--output-dir)
    - scripts/phase213-steering-snapshot.sh (Plan 03 output — confirm CLI: --output prefix)
    - tests/fixtures/phase213/manifest-expected-keys.json (Plan 01 — the manifest test_phase213_manifest_schema.py asserts against)
  </read_first>
  <files>scripts/phase213-baseline-capture.sh</files>
  <action>
    Create a new bash script ~250-350 lines. `set -euo pipefail`. Top-of-file docstring states: phase 213 evidence baseline harness; D-09 single-command operator entry; D-10 mutation posture (allowed: dev-VM traffic; forbidden: service restart, /etc/wanctl write, steering toggle, RouterOS write, deploy, controller config change, profiling harness change). Implement `usage()` heredoc enumerating the 11 flags from the interfaces block. Implement long-flag parsing in the phase198 style (`while [[ $# -gt 0 ]]; do case "$1" in ...; esac done`). Implement prerequisite gates: `command -v flent jq curl >/dev/null`, `ssh -o BatchMode=yes cake-shaper 'true'`, `ssh cake-shaper 'sudo -n true'`, `ip -4 addr show | grep -qF " ${LOCAL_BIND}/"`. Build the per-test bracketed function `run_bracketed_test()` per RESEARCH §"Pattern 1" exactly: pre-snapshot via phase213-steering-snapshot.sh, dual-WAN background pollers (Spectrum + ATT, both polling regardless of which WAN is the test target) via phase213-health-poller.sh with `--duration 0` so the orchestrator manages kill, sleep pre-buf, record t_start, branch on test name (browse → phase213-browse-loop.sh, others → phase191-flent-capture.sh wrapper with `--tests "$test"`), record t_end, sleep post-buf, kill+wait both poller PIDs, post-snapshot, then phase213-alert-window.sh with start=t_start-pre_buf end=t_end+post_buf, then `jq -n` per-test manifest write. Implement the egress probe per Phase 198 lines 270-289: for Spectrum WAN, refuse if egress IP != Phase 198 known value `70.123.224.169` (record actual observed in probe-run JSON); for ATT, record observed without enforcement. Implement the outer per-WAN serialized loop (D-11): `for WAN in ${WANS_CSV//,/ }; do for TEST in ${TESTS_CSV//,/ }; do …; sleep 5; done; done`. Write the per-run top-level `manifest.json` via `jq -n` using `--arg`/`--argjson`/`--argjson tests_ordered` with EVERY key from manifest-expected-keys.json `required_top_level_keys`. Add a `--dry-run` branch that runs prerequisite gates, creates the RUN-<ts> dir, writes the manifest with empty `tests_ordered`, prints "DRY_RUN OK", and exits 0 (so test_phase213_manifest_schema.py can invoke). At the end of a real run, invoke `.venv/bin/python3 scripts/phase213-classify.py --run-dir "$RUN_DIR" --output-json "${EVIDENCE_ROOT}/signal-sheet-${RUN_TS}.json" --output-md "${EVIDENCE_ROOT}/signal-sheet-${RUN_TS}.md"`. Add SSH ControlMaster multiplexing flags to a script-local SSH wrapper. Append per-test commands to `.planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/.command-log` (one row per executed command for evidence README provenance). Do NOT include any of the 9 D-10 forbidden tokens. chmod +x.
  </action>
  <verify>
    <automated>test -x scripts/phase213-baseline-capture.sh &amp;&amp; bash -n scripts/phase213-baseline-capture.sh &amp;&amp; bash scripts/phase213-baseline-capture.sh --help 2&gt;&amp;1 | grep -E "\\-\\-(local-bind|host|flent-duration|browse-duration|pre-buf|post-buf|wans|tests|evidence-root|dry-run|help)" | wc -l &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_manifest_schema.py -q 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    Script exists, executable, `bash -n` clean. `--help` lists all 11 flags (test asserts >= 11 flag-name matches). test_phase213_mutation_boundary.py PASSES with the orchestrator no longer skipped. test_phase213_manifest_schema.py PASSES (manifest emitted by `--dry-run` contains every required key). No D-10 forbidden token present. SSH ControlMaster multiplexing flags present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/phase213-classify.py offline signal-sheet emitter (BASE-03, D-13, D-14, D-15)</name>
  <read_first>
    - scripts/phase198-loaded-window-audit.py (entire file 296 lines — PRIMARY analog: argparse, NDJSON-in, JSON-out, stdlib-only, docstring style)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map" (six bucket schemas + threshold values) and §"Signal-Sheet Output Schema" (verbatim JSON shape) and §"Signal-Sheet Markdown Rendering" (operator-readable rendering)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Open Questions" #2 (use controller's own green_streak >= green_required for stable-GREEN definition) and #5 (chronological order in signal-sheet.md)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-classify.py" (analog assignment + threshold-constant placement + D-14 safety guard)
    - tests/fixtures/phase213/signal-sheet-expected-empty.json (golden file: all six buckets present with flagged=false)
    - tests/fixtures/phase213/signal-sheet-expected-ul-ceiling-flagged.json (golden file: BUCKET 1 flagged shape)
    - tests/test_phase213_classify.py (Plan 01 — the four test functions this classifier must pass)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json (v1.39 threshold field names — record verbatim, NEVER use in comparison RHS)
  </read_first>
  <files>scripts/phase213-classify.py</files>
  <action>
    Create a new Python script ~300-450 lines using `#!/usr/bin/env python3`, `from __future__ import annotations`, stdlib-only imports (argparse, csv, json, pathlib, statistics, re, sys). Module-level docstring states purpose + inputs (run_dir tree per RESEARCH §Per-Run Artifact Layout) + outputs (signal-sheet.json + signal-sheet.md) + D-13/D-14/D-15 invariants. Define threshold constants near the top as a TYPED block: `BUCKET_1_PCT_AT_CEILING = 0.80` (D-18 reference comment), `BUCKET_2_TIME_TO_GREEN_SEC = 30`, `BUCKET_2_PEAK_DELAY_US = 50000`, `BUCKET_3_OUTLIER_RATE_MAX = 0.30`, `BUCKET_3_P99_VS_MEDIAN_MULT = 5`, `BUCKET_5_PCT_REFRACTORY_ACTIVE = 0.05`, `BUCKET_5_BACKLOG_SUPPRESSED_DELTA = 100`, `BUCKET_6_THROUGHPUT_DROP_PCT = 0.30`, `BUCKET_6_TTFB_P99_MS = 2000`, `BUCKET_6_OUTLIER_RATE_MAX = 0.10`. Define `SPECTRUM_PLAN_DL_MBPS = 920`, `SPECTRUM_PLAN_UL_MBPS = 40`, `ATT_PLAN_DL_MBPS = 95`, `ATT_PLAN_UL_MBPS = 18` for BUCKET 6 plan-vs-actual math. Implement six bucket-analysis functions, each taking parsed per-test NDJSON + alert dumps + flent summary + browse CSV and returning `{flagged: bool, evidence_rows: [...], operator_note: str}`. For BUCKET 1 (`upload_ceiling_setpoint`): compute `pct_samples_at_ceiling` = fraction of NDJSON rows during tcp_upload where `upload_rate_mbps >= ceiling - 0.5` (ceiling read from `upload_setpoint_mbps + 6` per Spectrum D-18 OR from row's own metadata if present); flag when > 0.80. For BUCKET 2 (`download_recovery_lag`): walk NDJSON rows during tcp_12down and rrul post-test buffer, compute `time_to_green_after_red_sec` between last RED/SOFT_RED row and first row where `download_state == GREEN` AND `download_green_streak >= download_green_required` (RESEARCH Open Q #2). For BUCKET 3 (`measurement_collapse`): take `signal_outlier_rate_max` and `signal_confidence_min` over all rows, cross-reference parsed flent summary p99 vs median throughput. For BUCKET 4 (`steering_drift`): compute raw counter deltas `red_count_delta`, `good_count_delta`, `cake_read_failures_delta` from steering-pre vs steering-post JSON files; emit pre_post_state_transition as a raw string (e.g., "SPECTRUM_GOOD → SPECTRUM_GOOD"); flag on ANY transition or non-zero counter delta. **CRITICAL D-14 INVARIANT:** This function MUST NOT contain any expression where `green_rtt_ms`, `yellow_rtt_ms`, `red_rtt_ms`, `red_samples_required`, or `green_samples_required` appears on the RHS of a comparison operator (`<`, `>`, `<=`, `>=`, `==`, `!=`). Field names may appear ONLY as dict keys when constructing evidence_row records and in inline comments. For BUCKET 5 (`refractory_semantics`): compute `pct_samples_refractory_active` from `arb_refractory_active` field, sum `cake_*_backlog_suppressed_count` deltas across test window. For BUCKET 6 (`external_isp`): compute `flent_throughput_drop_pct_vs_plan` using the four plan constants above, take `curl_ttfb_p99_ms` from browse.curl.csv parsed `time_starttransfer` column (p99 over all rows for that test), and the AND-of-three flag condition. Build `recommended_next_phase` per D-15: primary = bucket priority order based on which buckets flagged (mapping: bucket 1 → phase 215, bucket 2 → phase 214/216, bucket 3 → phase 214, bucket 5 → phase 216, bucket 4 → phase 216, bucket 6 → no v1.46 phase but record as runner-up); runners-up = remaining flagged buckets in priority order. If NO bucket flags, primary = 214 (default cautious-investigation choice) with explicit operator_note. Emit `signal-sheet.json` with the verbatim schema from RESEARCH §"Signal-Sheet Output Schema". Emit `signal-sheet.md` per RESEARCH §"Signal-Sheet Markdown Rendering" — one table per bucket, chronological row order per RESEARCH Open Q #5, summary header at top listing flagged buckets and the recommendation. CLI: `argparse` with `--run-dir <path>` (required), `--output-json <path>` (required), `--output-md <path>` (required), `--help`. The script must NOT import `wanctl.*` (couples to deployed version per RESEARCH §"Don't Hand-Roll"). Make script executable (chmod +x).
  </action>
  <verify>
    <automated>test -x scripts/phase213-classify.py &amp;&amp; .venv/bin/python3 -c "import ast, pathlib; ast.parse(pathlib.Path('scripts/phase213-classify.py').read_text())" &amp;&amp; .venv/bin/python3 scripts/phase213-classify.py --help 2&gt;&amp;1 | grep -E "\\-\\-(run-dir|output-json|output-md)" | wc -l &amp;&amp; .venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <done>
    Script exists, executable, parses as valid Python (no syntax errors). `--help` lists --run-dir, --output-json, --output-md. test_phase213_classify.py PASSES all four test functions: test_six_buckets_present, test_bucket_1_threshold_flags_on_ceiling_pegged, test_bucket_4_steering_drift_no_threshold_name_compare (the source-grep test passes — no threshold-field-name appears on a comparison RHS), test_ranked_next_phase_recommendation_present. test_phase213_mutation_boundary.py PASSES. No `from wanctl` or `import wanctl` line in the source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write docs/RUNBOOKS/baseline.md + evidence/README.md (D-09, D-12)</name>
  <read_first>
    - docs/SOAK_HARNESS.md (entire file — closest tone/style template for the runbook; mirror Purpose / Files / NDJSON Per-Row Schema / Output Schema section order)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Per-Run Artifact Layout" (artifact tree to document in runbook) and §"Bucket → Evidence Map" (signal-sheet reading guide)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"docs/RUNBOOKS/baseline.md" (analog + Phase 213 specifics: one-command lead, mutation-posture callout, artifact-tree section, signal-sheet reading guide)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md (Phase 212 evidence README — exact 6-column index table + redaction policy + production-boundary sections to mirror)
    - scripts/phase213-baseline-capture.sh (Task 1 — the CLI surface the runbook documents)
    - scripts/phase213-classify.py (Task 2 — the classifier behavior the runbook explains)
  </read_first>
  <files>docs/RUNBOOKS/baseline.md, .planning/phases/213-experience-baseline-harness/evidence/README.md</files>
  <action>
    Create `docs/RUNBOOKS/` directory if missing. Write `docs/RUNBOOKS/baseline.md` (~80-150 lines) mirroring `docs/SOAK_HARNESS.md` section order. Section 1 "Purpose": Phase 213 experience baseline harness; evidence-only per D-10. Section 2 "Quick start": ONE bash command — `bash scripts/phase213-baseline-capture.sh` with three flag-example variants (full default; dry-run; Spectrum-only). Section 3 "Mutation posture (D-10)" as a callout box: lists the seven forbidden operations (service restart, /etc/wanctl edit, steering toggle, RouterOS write, deploy, profiling harness change, controller config change); allowed = dev-VM traffic generation only. Section 4 "Files": table listing the six phase213 scripts + the runbook + the classifier with their roles. Section 5 "Per-run artifact tree": copy the tree from RESEARCH §"Per-Run Artifact Layout" verbatim. Section 6 "Reading the signal sheet": brief paragraph + reference to RESEARCH §"Bucket → Evidence Map". Section 7 "Carrying forward": note that operator authors `213-REPORT.md` citing signal-sheet rows (Plan 05). Do NOT inline the full extended NDJSON schema — link to RESEARCH.md for that. No `systemctl restart` examples in any code block.

    Write `.planning/phases/213-experience-baseline-harness/evidence/README.md` (~40-80 lines) mirroring `.planning/phases/212-.../evidence/README.md` exactly: opening paragraph stating Phase 213 evidence-only / D-09 / D-10 / D-08 redaction; "## Evidence Index" with 6-column table header `| Timestamp (UTC) | Source host | Command purpose | Redaction method | Output file | Mutation posture |` and one initial row noting "Populated at run-time by scripts/phase213-baseline-capture.sh into .command-log; collated per RUN-<ts> dir"; "## Redaction Policy" listing D-08 key pattern verbatim and the exclusions Phase 212 used; "## Production Boundary" listing the same four bullets Phase 212 used (production evidence comes from ssh cake-shaper; dev VM is not production evidence; no deploy/restart/config-write/steering-toggle/router-mutation; raw state.json gitignored). Add closing note: per-RUN evidence README is generated by orchestrator into `evidence/RUN-<ts>/README.md` (separate from this top-level index).
  </action>
  <verify>
    <automated>test -f docs/RUNBOOKS/baseline.md &amp;&amp; test -f .planning/phases/213-experience-baseline-harness/evidence/README.md &amp;&amp; grep -F "bash scripts/phase213-baseline-capture.sh" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F "D-10" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F "| Timestamp (UTC) | Source host | Command purpose | Redaction method | Output file | Mutation posture |" .planning/phases/213-experience-baseline-harness/evidence/README.md &amp;&amp; grep -F "password|secret|token|credential|auth|key|private" .planning/phases/213-experience-baseline-harness/evidence/README.md &amp;&amp; (grep -E "systemctl\\s+restart|deploy\\.sh" docs/RUNBOOKS/baseline.md &amp;&amp; { echo "FAIL: mutation token in runbook"; exit 1; } || echo "OK: runbook clean")</automated>
  </verify>
  <done>
    Both files exist. Runbook leads with the one-command invocation; references D-10 forbidden operations; references RESEARCH bucket map. evidence/README.md has the 6-column index table header verbatim, the D-08 redaction policy line, and the production-boundary section. Neither file contains any of the 9 D-10 forbidden tokens in code blocks (table cells and bullet text are OK because the mutation-boundary test scans scripts only, but use only references-to per the verify gate).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → orchestrator CLI | Inherits Plans 02/03 boundaries (dev VM → autorate, dev VM → cake-shaper SSH). |
| Classifier → run-dir filesystem | Offline read-only. No network. Stdlib-only. |
| Runbook + evidence README → operator | Read-only documentation; no executable content beyond example commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I | Evidence dir command-log + per-RUN README (orchestrator-written) | mitigate | Top-level evidence/README.md states D-08 redaction policy explicitly; orchestrator appends only command names + timestamps to .command-log (no env values, no secret-bearing args); per-RUN README is generated by orchestrator with the same 6-column schema as the top-level index. |
| T-213-02 | T | Orchestrator invokes phase191-flent-capture.sh and phase213-*.sh leaves | mitigate | All invocations are read-side (HTTP, SSH read-only) or dev-VM traffic generation (D-10 allowance). Mutation-boundary test scans the orchestrator source after this plan ships. |
| T-213-03 | I | Concurrent live-writer SQLite via phase213-alert-window.sh | mitigate | Inherits Plan 03's mode=ro URI gate. |
| T-213-04 | D | Per-WAN serialized full suite duration ~10 min/WAN | accept | D-11 sequencing already bounds blast radius; orchestrator records start/end timestamps so impact window is auditable. |
| T-213-05 | E | D-14 steering drift bucket source-grep enforcement | mitigate | Plan 01's test_phase213_classify.py::test_bucket_4_steering_drift_no_threshold_name_compare greps the classifier source for `green_rtt_ms` / `yellow_rtt_ms` / `red_rtt_ms` on comparison RHS; classifier MUST pass this test (it is in the verify gate of Task 2). |
| T-213-05 | E | Recommended next-phase bias (could push operator toward a phase that bypasses v1.39 alignment) | accept | D-15 requires explicit runners-up; classifier emits primary + at least one runner-up; operator-authored report (Plan 05) makes the final call. |
</threat_model>

<verification>
- `bash -n scripts/phase213-baseline-capture.sh` exits 0.
- `python -c "import ast; ast.parse(open('scripts/phase213-classify.py').read())"` exits 0.
- `.venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_classify.py tests/test_phase213_manifest_schema.py tests/test_phase213_ndjson_schema.py -q` ALL FOUR FILES GREEN.
- `bash scripts/phase213-baseline-capture.sh --help` lists all 11 flags.
- `.venv/bin/python3 scripts/phase213-classify.py --help` lists --run-dir, --output-json, --output-md.
- `grep -F "bash scripts/phase213-baseline-capture.sh" docs/RUNBOOKS/baseline.md` matches.
- `grep -F "| Timestamp (UTC) | Source host | Command purpose | Redaction method | Output file | Mutation posture |" .planning/phases/213-experience-baseline-harness/evidence/README.md` matches.
- `grep -E "from wanctl|import wanctl" scripts/phase213-classify.py` returns nothing (no deployed-wanctl coupling).
- `git diff --stat src/wanctl/` returns empty (SAFE-05 invariant).
</verification>

<success_criteria>
- All four artifacts shipped: orchestrator, classifier, runbook, evidence README.
- All four Wave 0 test files (mutation-boundary, classify, manifest-schema, ndjson-schema) GREEN.
- Classifier emits six buckets in stable order with thresholds as named module-level constants (no inline magic numbers in the bucket-flag conditions).
- D-14 invariant holds: classifier source contains zero threshold-field-name-on-comparison-RHS expressions (source-grep test enforces).
- D-15 invariant holds: recommended_next_phase has `primary in {214,215,216}` and `len(runners_up) >= 1`.
- evidence/README.md mirrors Phase 212 6-column command-index shape.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-04-SUMMARY.md` covering: orchestrator line count, classifier line count, runbook line count, all four Wave 0 tests now GREEN (paste pytest summary), the threshold constants emitted as named module-level constants in the classifier, the D-15 default-recommendation logic (which bucket maps to which next phase), and the artifact tree the orchestrator writes per RUN-<ts>. Surface the handoff to Plan 05: "Phase 213 codepath is complete and tested; Plan 05 executes it for real and authors 213-REPORT.md."
</output>
