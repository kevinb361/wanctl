---
phase: 213-experience-baseline-harness
plan: 04
type: execute
wave: 3
depends_on: [213-01, 213-02, 213-03]
files_modified:
  - scripts/phase213-baseline-capture.sh
  - scripts/phase213-classify.py
  - docs/RUNBOOKS/baseline.md
  - .planning/phases/213-experience-baseline-harness/evidence/README.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
tags: [baseline, harness, orchestrator, classifier, runbook, evidence-index]

must_haves:
  truths:
    - "Operator runs ONE command: `bash scripts/phase213-baseline-capture.sh`. Per D-09 the orchestrator wires every leaf script from Plans 02 and 03."
    - "Per-WAN sequencing D-11 enforced: Spectrum suite to completion, then ATT — never concurrent (serialized, not concurrent)."
    - "Per-WAN bind map (codex HIGH-1): orchestrator accepts `--bind-map spectrum=10.10.110.226,att=10.10.110.233` and uses the per-WAN bind for each WAN's flent/browse legs. Single `--local-bind` for both WANs is FORBIDDEN."
    - "Egress probe gate (Phase 198 pattern) refuses to run flent for a given WAN if egress IP via that WAN's bind doesn't match the expected gateway signature for that WAN."
    - "Orchestrator has TWO offline-vs-live mode splits (codex HIGH-3): `--check-manifest` (offline: emits schema-valid manifest, no SSH/curl, used by pytest) and `--check-prereqs` (live: SSH/sudo/egress probe against cake-shaper, used by operator pre-real-run)."
    - "Background pollers are tracked by PID array; `trap cleanup_pollers EXIT INT TERM` guarantees no leaked poller process on `set -e` mid-test failure (codex HIGH-4)."
    - "Classifier emits a six-bucket signal-sheet.json + signal-sheet.md inside the per-run dir at `evidence/RUN-<ts>/signal-sheet.{json,md}` (codex MEDIUM-5 canonical location). NEVER compares steering /health values to threshold-field-name semantics (D-08/D-14)."
    - "Bucket 1 reads upload ceiling from the per-WAN YAML config OR from a row metadata field — NEVER from `setpoint_mbps + 6` arithmetic (codex MEDIUM-2, link-agnostic architecture)."
    - "Flent artifact paths from phase191-flent-capture.sh are normalized via symlink into `evidence/RUN-<ts>/<wan>/<test>/` after each flent invocation (codex MEDIUM-3)."
    - "Single ranked next-phase recommendation (one of 214/215/216) with runners-up appears in signal-sheet output (D-15)."
    - "evidence/README.md mirrors Phase 212's 6-column command-index table + redaction policy + production-boundary sections."
    - "docs/RUNBOOKS/baseline.md leads with the one-command invocation, lists D-10 forbidden operations, documents `--bind-map` and `--check-manifest`/`--check-prereqs` modes, and points to the bucket-→-evidence map."
  artifacts:
    - path: "scripts/phase213-baseline-capture.sh"
      provides: "Top-level orchestrator. `--bind-map`, `--check-manifest`, `--check-prereqs`, per-WAN egress probe, trap-based poller cleanup."
      min_lines: 320
    - path: "scripts/phase213-classify.py"
      provides: "Offline NDJSON→signal-sheet emitter; reads ceiling from per-WAN YAML config (MEDIUM-2); emits signal-sheet.{json,md} INTO the per-RUN dir (MEDIUM-5)."
      min_lines: 320
    - path: "docs/RUNBOOKS/baseline.md"
      provides: "One-command operator runbook per D-09; documents `--bind-map`, the offline/live mode split, mirrors docs/SOAK_HARNESS.md style."
      min_lines: 90
    - path: ".planning/phases/213-experience-baseline-harness/evidence/README.md"
      provides: "Phase 213 evidence-index template mirroring Phase 212 evidence/README.md shape (6-column table populated by orchestrator at run-time, plus standing redaction-policy + production-boundary text)."
      min_lines: 40
  key_links:
    - from: "scripts/phase213-baseline-capture.sh"
      to: "scripts/phase213-{health-poller,browse-loop,alert-window,steering-snapshot}.sh"
      via: "bash invocation within run_bracketed_test() per RESEARCH §Pattern 1"
      pattern: "scripts/phase213-(health-poller|browse-loop|alert-window|steering-snapshot)\\.sh"
    - from: "scripts/phase213-baseline-capture.sh"
      to: "scripts/phase191-flent-capture.sh"
      via: "wrapper call with --tests <one-test> --wan <wan> --local-bind <per-wan-bind-from-bind-map>"
      pattern: "phase191-flent-capture.sh"
    - from: "scripts/phase213-baseline-capture.sh"
      to: "Per-WAN bind map"
      via: "--bind-map spectrum=<ip>,att=<ip> parsed into associative array BIND[wan]"
      pattern: "--bind-map"
    - from: "scripts/phase213-baseline-capture.sh"
      to: "Trap-based poller cleanup"
      via: "trap cleanup_pollers EXIT INT TERM; POLLER_PIDS=()"
      pattern: "trap .*cleanup.*EXIT"
    - from: "scripts/phase213-classify.py"
      to: "evidence/RUN-<ts>/<wan>/<test>/*.ndjson + alerts-*.json + flent summaries (symlinked or normalized) + browse.curl.csv"
      via: "argparse --run-dir; stdlib-only file reads; per-WAN YAML config read for ceiling"
      pattern: "--run-dir"
    - from: "scripts/phase213-classify.py"
      to: "evidence/RUN-<ts>/signal-sheet.json + signal-sheet.md (MEDIUM-5 canonical location)"
      via: "--output-json / --output-md flags pointing INTO the run dir"
      pattern: "--output-json"
---

<objective>
Wire Waves 0/1/2 surfaces into the single operator-invocable Phase 213 harness. Build the orchestrator (per-WAN sequenced suite with per-WAN bind map, dual offline/live mode split, trap-based poller cleanup), the offline classifier (six-bucket signal sheet with config-driven ceiling and canonical signal-sheet location), the operator runbook, and the evidence-dir command index.

Codex review patches landed here:
- HIGH-1: orchestrator accepts `--bind-map spectrum=<ip>,att=<ip>` and uses per-WAN bind for each WAN's flent/browse. Single `--local-bind` for both WANs is rejected (`--help` no longer advertises a single-bind flag for cross-WAN use). Egress probe runs PER WAN before that WAN's test suite.
- HIGH-3: dual mode split — `--check-manifest` (offline, pytest-invoked, no SSH/curl) emits schema-valid manifest and exits 0; `--check-prereqs` (live, operator-invoked, SSH+sudo+egress probe against cake-shaper) validates real-run readiness. The old `--dry-run` becomes an alias for `--check-prereqs` for backward compatibility but pytest uses `--check-manifest` only.
- HIGH-4: trap-based poller cleanup. `POLLER_PIDS=()` array; `cleanup_pollers()` function that kills+waits all recorded PIDs idempotently; `trap cleanup_pollers EXIT INT TERM` registered globally; per-test inner trap also fires.
- MEDIUM-2: classifier reads upload ceiling from per-WAN YAML config (`configs/spectrum.yaml`, `configs/att.yaml`) at runtime, NOT from `setpoint_mbps + 6` arithmetic. Source-grep test enforces no `setpoint_mbps + 6` literal in classifier source.
- MEDIUM-3: orchestrator post-step after each flent invocation normalizes `OUTPUT_ROOT/LABEL/WAN/TIMESTAMP/*.flent.gz` into `evidence/RUN-<ts>/<wan>/<test>/` via symlinks. Classifier reads from the normalized location only.
- MEDIUM-5: signal-sheet.{json,md} canonical location is INSIDE the per-RUN dir at `evidence/RUN-<ts>/signal-sheet.{json,md}` (NOT `evidence/signal-sheet-<ts>.md` at the top level).
- MEDIUM-7: orchestrator and runbook describe WAN order as "serialized in <wan-order>" (not "concurrent"). D-11 callout explicit.

Output: Four files. All five Wave 0 test files (mutation-boundary, ndjson-schema, classify [all six buckets], manifest-schema, alert-window) GREEN after this plan ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-REVIEWS.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-02-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-03-SUMMARY.md
@scripts/phase198-rerun-flent-3run.sh
@scripts/phase198-loaded-window-audit.py
@scripts/phase191-flent-capture.sh
@scripts/analyze_baseline.py
@docs/SOAK_HARNESS.md
@.planning/phases/212-production-inventory-and-drift-audit/evidence/README.md
@configs/spectrum.yaml
@configs/att.yaml
@src/wanctl/health_check.py

<interfaces>
<!-- Orchestrator CLI (codex HIGH-1 + HIGH-3 patched): -->
<!-- scripts/phase213-baseline-capture.sh \ -->
<!--   --bind-map spectrum=<ip>,att=<ip>             (REQUIRED, replaces --local-bind) -->
<!--   [--host <name>]                                (flent netperf host, default dallas) -->
<!--   [--flent-duration <sec>] [--browse-duration <sec>] [--pre-buf <sec>] [--post-buf <sec>] -->
<!--   [--wans <csv>]                                 (D-11 serialized order; default spectrum,att) -->
<!--   [--tests <csv>] -->
<!--   [--evidence-root <dir>] -->
<!--   [--check-manifest]                             (OFFLINE: emit manifest to tmp dir + exit; pytest mode) -->
<!--   [--check-prereqs | --dry-run]                  (LIVE: SSH/sudo/egress prereq probes; operator mode) -->
<!--   [--help] -->

<!-- The two modes are mutually exclusive. Without either, the script runs a real capture (operator only). -->

<!-- Default test order per WAN: browse,tcp_upload,tcp_download,rrul,tcp_12down -->
<!-- Default WAN order: spectrum,att (D-11 serialized — never concurrent) -->

<!-- Phase 198 known bind→egress mapping (orchestrator probes per-WAN, refuses on mismatch): -->
<!-- - spectrum bind 10.10.110.226 → egress 70.123.224.169 (Charter AS11427) -->
<!-- - att bind 10.10.110.233 → egress (recorded only; no enforced value yet — egress signature must come from operator config or 212-production-inventory.md if available, else recorded-only) -->

<!-- Classifier CLI: -->
<!-- .venv/bin/python3 scripts/phase213-classify.py --run-dir <RUN-dir> \ -->
<!--   --output-json <RUN-dir>/signal-sheet.json \ -->
<!--   --output-md <RUN-dir>/signal-sheet.md -->

<!-- Six bucket keys (exactly): -->
<!-- upload_ceiling_setpoint, download_recovery_lag, measurement_collapse, -->
<!-- steering_drift, refractory_semantics, external_isp -->

<!-- Bucket thresholds (RESEARCH §Bucket → Evidence Map; classifier MUST cite these as module-level constants): -->
<!-- BUCKET 1: pct_samples_at_ceiling > 0.80 during a tcp_upload test (ceiling READ FROM CONFIG, not setpoint+6) -->
<!-- BUCKET 2: time_to_green_after_red_sec > 30 OR cake_dl_peak_delay_us_p99 > 50000 -->
<!-- BUCKET 3: signal_outlier_rate_max > 0.30 AND flent_p99 > flent_median * 5 -->
<!-- BUCKET 4: ANY raw state transition or counter delta during test window (NO threshold-name compare per D-14) -->
<!-- BUCKET 5: pct_samples_refractory_active > 0.05 OR backlog_suppressed_delta > 100 in a test window -->
<!-- BUCKET 6: flent_throughput_drop_pct_vs_plan > 0.30 AND curl_ttfb_p99_ms > 2000 AND signal_outlier_rate_max < 0.10 -->

<!-- Per-WAN plan caps for BUCKET 6 (RESEARCH §Bucket → Evidence Map) — read from configs/<wan>.yaml: -->
<!-- Spectrum: plan_dl_mbps=920, plan_ul_mbps=40 -->
<!-- ATT: plan_dl_mbps=95, plan_ul_mbps=18 -->
<!-- (These mirror existing config; classifier loads the YAML at runtime, does not hardcode the numbers separately from config). -->

<!-- Per-run top-level manifest.json schema (NEW key per codex HIGH-1: bind_map): -->
<!-- phase, run_id, started_utc, ended_utc, host_dev_vm, source_ip, netperf_host, -->
<!-- flent_version, wanctl_version_dev_repo, wanctl_version_{spectrum,att,steering}_runtime, -->
<!-- git_head_sha, tests_ordered (array of {wan, test, test_start_unix, test_end_unix}), -->
<!-- redaction_posture, mutation_posture, bind_map (e.g. {"spectrum":"10.10.110.226","att":"10.10.110.233"}) -->

<!-- Flent artifact normalization (codex MEDIUM-3): -->
<!-- phase191-flent-capture.sh writes to ${OUTPUT_ROOT}/${LABEL}/${WAN}/${TIMESTAMP}/. -->
<!-- After each invocation, orchestrator runs: -->
<!--   ln -s "${OUTPUT_ROOT}/${LABEL}/${WAN}/${TIMESTAMP}" "evidence/RUN-<ts>/<wan>/<test>/flent" -->
<!-- (or `cp -r` if symlinks are problematic for downstream tooling). -->
<!-- Classifier looks at evidence/RUN-<ts>/<wan>/<test>/flent/*.flent.gz and flent-summary.json. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/phase213-baseline-capture.sh orchestrator (HIGH-1 bind-map + HIGH-3 mode split + HIGH-4 trap-based poller cleanup + MEDIUM-3 flent path normalization)</name>
  <read_first>
    - scripts/phase198-rerun-flent-3run.sh (entire file 449 lines — PRIMARY analog: header docstring, set -euo pipefail, usage(), long-flag parsing, per-attempt subdir + parallel /health subshell, egress probe gate, jq -n manifest assembly)
    - scripts/phase191-flent-capture.sh (entire file 191 lines — confirm --tests/--label/--wan/--local-bind/--host/--duration/--output-dir/--ref CLI surface; this orchestrator INVOKES it per-test; note the OUTPUT_ROOT/LABEL/WAN/TIMESTAMP layout for MEDIUM-3 normalization)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Orchestrator skeleton" + §"Pattern 1: Per-Test Bracketed Polling" + §"Per-Run Artifact Layout" (manifest schema)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-baseline-capture.sh"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md HIGH-1, HIGH-3, HIGH-4, MEDIUM-3, MEDIUM-5, MEDIUM-7 (all this task's patches)
    - scripts/phase213-health-poller.sh (Plan 02 output — confirm CLI: --endpoint/--wan/--output/--duration)
    - scripts/phase213-browse-loop.sh (Plan 02 output — confirm CLI: --output/--duration/--local-bind/--sites)
    - scripts/phase213-alert-window.sh (Plan 03 output — confirm CLI: --start/--end/--output-dir [SSH or --local-db])
    - scripts/phase213-steering-snapshot.sh (Plan 03 output — confirm CLI: --output prefix; raw temp lives in /tmp/ not evidence/)
    - tests/fixtures/phase213/manifest-expected-keys.json (Plan 01 — schema includes bind_map; orchestrator must emit it)
    - configs/spectrum.yaml, configs/att.yaml (per-WAN config files the classifier reads for ceiling/plan caps; orchestrator records their content hash in manifest provenance)
  </read_first>
  <acceptance_criteria>
    - File exists, executable, passes `bash -n`.
    - `--help` lists ALL of: `--bind-map`, `--host`, `--flent-duration`, `--browse-duration`, `--pre-buf`, `--post-buf`, `--wans`, `--tests`, `--evidence-root`, `--check-manifest`, `--check-prereqs`, `--help`. The literal token `--local-bind` MUST NOT appear as a top-level flag (only as the per-leg argument to phase191-flent-capture.sh, internally).
    - Source assertion (HIGH-1): `! grep -E "for WAN in.*--local-bind \\\\\$ONE_BIND" scripts/phase213-baseline-capture.sh` — there is NO loop that passes a single bind across both WANs.
    - Source assertion (HIGH-3): the `--check-manifest` branch (extract via awk from `case "$MODE" in check-manifest)` to `;;`) contains NEITHER `ssh ` NOR `curl ` invocations. Verified by test_phase213_manifest_schema.py::test_check_manifest_is_offline.
    - Source assertion (HIGH-4): `grep -E "^trap .*cleanup_pollers.*EXIT" scripts/phase213-baseline-capture.sh` matches.
    - Source assertion (HIGH-4): `POLLER_PIDS=()` array initialization present; `cleanup_pollers()` function defined and idempotent (safe to call twice).
    - `--check-manifest` exits 0 with NO network reachability required (offline). Emits a manifest at `<evidence-root>/RUN-CHECK-MANIFEST/manifest.json` containing every key in manifest-expected-keys.json `required_top_level_keys` (including `bind_map`).
    - `--check-prereqs` runs the live SSH/sudo/egress probe gates against cake-shaper (replaces the old `--dry-run`; `--dry-run` is accepted as an alias for `--check-prereqs`).
    - Per-WAN egress probe present: for each WAN in the bind map, `curl -s --interface <bind> https://ifconfig.io` (or equivalent) before that WAN's test suite; refuse the WAN's test if egress signature mismatches expected (Spectrum: 70.123.224.169). For ATT: record-only.
    - Post-flent-invocation normalization (MEDIUM-3): after each `phase191-flent-capture.sh` returns, the orchestrator creates `evidence/RUN-<ts>/<wan>/<test>/flent` as a symlink (or copy) to the actual `OUTPUT_ROOT/LABEL/WAN/TIMESTAMP` directory phase191 created.
    - Classifier invocation at end of real run: `.venv/bin/python3 scripts/phase213-classify.py --run-dir "$RUN_DIR" --output-json "${RUN_DIR}/signal-sheet.json" --output-md "${RUN_DIR}/signal-sheet.md"` — signal-sheet INSIDE the run dir (MEDIUM-5).
    - Manifest emits `bind_map` field as a JSON object (HIGH-1).
    - test_phase213_mutation_boundary.py, test_phase213_manifest_schema.py PASS.
  </acceptance_criteria>
  <files>scripts/phase213-baseline-capture.sh</files>
  <action>
    Create a new bash script ~320-400 lines. `set -euo pipefail`. Top-of-file docstring states: phase 213 evidence baseline harness; D-09 single-command operator entry; D-10 mutation posture (allowed: dev-VM traffic; forbidden: service restart, /etc/wanctl write, steering toggle command, RouterOS write, deploy, controller config change, profiling harness change); D-11 serialized per-WAN order; HIGH-1 per-WAN bind map; HIGH-3 dual offline/live mode split; HIGH-4 trap-based poller cleanup.

    **Argument parsing:**

    - `--bind-map spectrum=<ip>,att=<ip>`: parse into bash associative array `declare -A BIND` such that `BIND[spectrum]=10.10.110.226`, `BIND[att]=10.10.110.233`. REQUIRED unless `--check-manifest` (which can use fixture values).
    - `--check-manifest`: sets `MODE=check-manifest`. Emits manifest to `${EVIDENCE_ROOT}/RUN-CHECK-MANIFEST/manifest.json` (or the legacy `RUN-DRYRUN` path if the operator overrides). NO SSH/curl invocations in this code path. Exits 0.
    - `--check-prereqs` (or `--dry-run` as alias): sets `MODE=check-prereqs`. Runs SSH/sudo/egress probes against cake-shaper. Exits 0 on success, non-zero on gate failure.
    - Without either: `MODE=real` — runs the full capture.

    **Mode dispatch:**

    ```bash
    case "${MODE:-real}" in
      check-manifest)
        # OFFLINE — codex HIGH-3
        # No ssh, no curl, no sudo. Build the manifest from in-script fixture values
        # (host_dev_vm = $(hostname), source_ip = "OFFLINE", netperf_host = ${HOST:-dallas},
        #  flent_version = "$(flent --version 2>/dev/null || echo unknown)",
        #  wanctl_version_*_runtime = "offline-fixture",
        #  git_head_sha = "$(git rev-parse HEAD 2>/dev/null || echo offline)",
        #  bind_map = "$(printf '{"spectrum":"%s","att":"%s"}' ${BIND[spectrum]:-fixture} ${BIND[att]:-fixture})",
        #  tests_ordered = [], started_utc/ended_utc = same iso timestamp)
        mkdir -p "${EVIDENCE_ROOT}/RUN-CHECK-MANIFEST"
        jq -n --argjson bm "${BIND_MAP_JSON}" --arg ts "$(date -u -Iseconds)" ... > manifest.json
        # ALSO emit a per-RUN README that mirrors the top-level evidence/README.md shape.
        echo "CHECK_MANIFEST OK"
        exit 0
        ;;
      check-prereqs|dry-run)
        # LIVE — SSH/sudo/egress probes; gated for operator pre-real-run verification
        command -v flent jq curl >/dev/null || { echo "missing flent/jq/curl on PATH"; exit 1; }
        ssh -o BatchMode=yes cake-shaper 'true' || { echo "cake-shaper unreachable"; exit 1; }
        ssh -o BatchMode=yes cake-shaper 'sudo -n true' || { echo "sudo -n unavailable on cake-shaper"; exit 1; }
        for WAN in ${WANS_CSV//,/ }; do
          bind="${BIND[$WAN]}"
          ip -4 addr show | grep -qF " ${bind}/" || { echo "bind $bind for WAN $WAN missing on dev VM"; exit 1; }
          egress="$(curl -s --interface "$bind" --max-time 5 https://ifconfig.io || echo unknown)"
          if [[ "$WAN" == "spectrum" && "$egress" != "70.123.224.169" ]]; then
            echo "REFUSED: spectrum egress via bind=$bind is $egress (expected 70.123.224.169)"; exit 1
          fi
          echo "WAN=$WAN bind=$bind egress=$egress OK"
        done
        echo "CHECK_PREREQS OK"
        exit 0
        ;;
      real)
        # Real capture — see below
        ;;
    esac
    ```

    **Real-run body (HIGH-4 trap-based poller cleanup):**

    Initialize poller-PID array at top of real-run block: `POLLER_PIDS=()`. Define:

    ```bash
    cleanup_pollers() {
      local pid
      for pid in "${POLLER_PIDS[@]:-}"; do
        if [[ -n "$pid" ]] && kill -0 "$pid" 2>/dev/null; then
          kill "$pid" 2>/dev/null || true
          wait "$pid" 2>/dev/null || true
        fi
      done
      POLLER_PIDS=()
    }
    cleanup_temp() {
      # also cleans any orchestrator-owned temp files
      :
    }
    trap 'cleanup_pollers; cleanup_temp' EXIT INT TERM
    ```

    **Per-test bracket (`run_bracketed_test()`):** per RESEARCH §"Pattern 1":
    1. pre-snapshot via phase213-steering-snapshot.sh
    2. start dual-WAN background pollers (Spectrum + ATT) via phase213-health-poller.sh with `--duration 0` so the orchestrator manages kill; record EACH PID into `POLLER_PIDS+=("$!")` IMMEDIATELY after launch — before any subsequent step that could fail under `set -e`
    3. (Optional inner trap inside the function to catch failures specific to one test before the outer EXIT trap fires — `trap 'cleanup_pollers; return' RETURN` if running in a function, OR just rely on the global EXIT trap)
    4. sleep pre-buf
    5. record t_start (unix)
    6. branch on test name:
       - `browse` → `bash scripts/phase213-browse-loop.sh --output ... --duration ... --local-bind "${BIND[$WAN]}" --sites ...`
       - others → `bash scripts/phase191-flent-capture.sh --tests "$TEST" --label "phase213-${RUN_TS}" --wan "$WAN" --local-bind "${BIND[$WAN]}" --host "$HOST" --duration "$FLENT_DURATION" --output-dir "${FLENT_OUTPUT_ROOT}"`
    7. record t_end (unix)
    8. **MEDIUM-3 normalization:** after the flent invocation, locate the deepest timestamped dir phase191 created (`${FLENT_OUTPUT_ROOT}/phase213-${RUN_TS}/${WAN}/<flent-ts>/`) and `ln -s "<that-path>" "${RUN_DIR}/${WAN}/${TEST}/flent"`. For browse, the browse CSV is written directly into `${RUN_DIR}/${WAN}/${TEST}/browse.curl.csv` — no normalization needed.
    9. sleep post-buf
    10. kill specific test-window pollers and `wait` (the global trap also cleans up but explicit cleanup keeps the inner test's PIDs out of subsequent iterations)
    11. post-snapshot via phase213-steering-snapshot.sh
    12. phase213-alert-window.sh with `--start $((t_start - PRE_BUF)) --end $((t_end + POST_BUF))` (SSH mode against cake-shaper)
    13. write per-test manifest via `jq -n`

    **Outer per-WAN serialized loop (D-11):**

    ```bash
    # D-11 SERIALIZED — never concurrent (codex MEDIUM-7 wording)
    for WAN in ${WANS_CSV//,/ }; do
      # Per-WAN egress probe (HIGH-1)
      bind="${BIND[$WAN]}"
      egress="$(curl -s --interface "$bind" --max-time 5 https://ifconfig.io || echo unknown)"
      if [[ "$WAN" == "spectrum" && "$egress" != "70.123.224.169" ]]; then
        echo "REFUSED: spectrum egress signature mismatch via bind=$bind (got $egress, expected 70.123.224.169)"; exit 1
      fi
      # ATT: record-only; orchestrator logs egress to manifest provenance but doesn't refuse
      for TEST in ${TESTS_CSV//,/ }; do
        run_bracketed_test "$WAN" "$TEST"
        sleep 5
      done
    done
    ```

    **Per-run top-level manifest (HIGH-1 adds bind_map):** write at run end via `jq -n` using `--arg`/`--argjson`/`--argjson tests_ordered`/`--argjson bind_map` for EVERY key in manifest-expected-keys.json `required_top_level_keys`. Include `bind_map: {"spectrum": "${BIND[spectrum]}", "att": "${BIND[att]}"}`. Include both per-WAN egress observations under a `bind_map_egress_observed` field (NOT in required_keys but useful for provenance).

    **Classifier invocation (MEDIUM-5 — INSIDE RUN dir):**
    ```bash
    .venv/bin/python3 scripts/phase213-classify.py \
      --run-dir "${RUN_DIR}" \
      --output-json "${RUN_DIR}/signal-sheet.json" \
      --output-md "${RUN_DIR}/signal-sheet.md"
    ```

    Append per-test commands to `${RUN_DIR}/.command-log` (one row per executed command). Add SSH ControlMaster multiplexing flags to a script-local SSH wrapper. Do NOT include any of the tightened D-10 forbidden tokens. chmod +x.
  </action>
  <verify>
    <automated>test -x scripts/phase213-baseline-capture.sh &amp;&amp; bash -n scripts/phase213-baseline-capture.sh &amp;&amp; FLAG_COUNT=$(bash scripts/phase213-baseline-capture.sh --help 2&gt;&amp;1 | grep -E -- "--(bind-map|host|flent-duration|browse-duration|pre-buf|post-buf|wans|tests|evidence-root|check-manifest|check-prereqs|help)" | wc -l) &amp;&amp; [[ "$FLAG_COUNT" -ge 12 ]] &amp;&amp; echo "FLAGS_OK ($FLAG_COUNT)" &amp;&amp; grep -E "^trap .*cleanup_pollers.*EXIT" scripts/phase213-baseline-capture.sh &amp;&amp; grep -E "POLLER_PIDS=" scripts/phase213-baseline-capture.sh &amp;&amp; grep -E -- "--bind-map" scripts/phase213-baseline-capture.sh &amp;&amp; (grep -E "for WAN in.*--local-bind \\\$ONE_BIND|--local-bind \\\$\\{?ONE_BIND" scripts/phase213-baseline-capture.sh &amp;&amp; { echo "FAIL: single bind across both WANs"; exit 1; } || echo "OK: no single-bind anti-pattern") &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_manifest_schema.py -q 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    Script exists, executable, `bash -n` clean. `--help` lists all 12 required flags. test_phase213_mutation_boundary.py PASSES with the orchestrator no longer skipped. test_phase213_manifest_schema.py PASSES (both `--check-manifest` schema test AND `test_check_manifest_is_offline` source-grep). `trap cleanup_pollers EXIT` present. `--bind-map` present; single-bind anti-pattern absent. Per-WAN egress probe present in both `--check-prereqs` and real-run paths. Flent path normalization (ln -s) present after each phase191 invocation. Manifest emits `bind_map` field. Classifier invocation writes signal-sheet INTO the RUN dir.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/phase213-classify.py offline signal-sheet emitter (BASE-03, D-13, D-14, D-15, MEDIUM-1 all six buckets, MEDIUM-2 config-driven ceiling, MEDIUM-5 RUN-dir output location)</name>
  <read_first>
    - scripts/phase198-loaded-window-audit.py (entire file 296 lines — PRIMARY analog: argparse, NDJSON-in, JSON-out, stdlib-only, docstring style)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map" (six bucket schemas + threshold values) and §"Signal-Sheet Output Schema" and §"Signal-Sheet Markdown Rendering"
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Open Questions" #2 and #5
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-classify.py"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md MEDIUM-1, MEDIUM-2 (per-bucket testing + config-driven ceiling)
    - configs/spectrum.yaml, configs/att.yaml (per-WAN config — classifier reads upload ceiling AND plan caps from here; MEDIUM-2)
    - src/wanctl/health_check.py around line 343 (`setpoint_mbps` is exposed; ceiling is NOT — confirm classifier cannot read ceiling from /health and must use config)
    - tests/fixtures/phase213/signal-sheet-expected-empty.json
    - tests/fixtures/phase213/signal-sheet-expected-ul-ceiling-flagged.json
    - tests/fixtures/phase213/RUN-bucket-{2,3,5,6}/ (the per-bucket golden fixtures created in Plan 01 — classifier MUST flag the correct bucket for each)
    - tests/test_phase213_classify.py (Plan 01 — ALL eight test functions this classifier must pass: six_buckets_present, bucket_1_threshold, bucket_2/3/5/6 flag tests, bucket_4_no_threshold_name_compare, ranked_recommendation, bucket_1_ceiling_source_is_config_not_arithmetic)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json (v1.39 threshold field names — record verbatim, NEVER use in comparison RHS)
  </read_first>
  <acceptance_criteria>
    - File exists, executable, parses as valid Python.
    - `--help` lists --run-dir, --output-json, --output-md.
    - ALL eight tests in test_phase213_classify.py PASS:
      - test_six_buckets_present
      - test_bucket_1_threshold_flags_on_ceiling_pegged
      - test_bucket_2_flags_on_download_recovery_lag
      - test_bucket_3_flags_on_measurement_collapse
      - test_bucket_5_flags_on_refractory_semantics
      - test_bucket_6_flags_on_external_isp
      - test_bucket_4_steering_drift_no_threshold_name_compare
      - test_ranked_next_phase_recommendation_present
      - test_bucket_1_ceiling_source_is_config_not_arithmetic (MEDIUM-2 source-grep — `setpoint_mbps\s*\+\s*6` must NOT appear)
    - No `from wanctl` or `import wanctl` import line (no deployed-version coupling).
    - Bucket 1 reads ceiling via a function that loads `configs/<wan>.yaml` (using stdlib — either `tomllib`-style fallback parser OR a minimal YAML reader; if `yaml` module not available in stdlib, use a small key:value parser scoped to `upload:\n  ceiling_mbps: N` form) OR from a row metadata field on the NDJSON if present. MUST NOT compute ceiling = setpoint + 6.
  </acceptance_criteria>
  <files>scripts/phase213-classify.py</files>
  <action>
    Create a new Python script ~330-470 lines using `#!/usr/bin/env python3`, `from __future__ import annotations`, stdlib-only imports (argparse, csv, json, pathlib, statistics, re, sys). Add a minimal YAML reader (NOT a PyYAML import — keep stdlib-only per RESEARCH §"Don't Hand-Roll" rule against coupling to deployed wanctl): a small `_read_wan_config(wan_name: str) -> dict` function that parses `configs/<wan>.yaml` looking for `upload:` block keys `ceiling_mbps`, `setpoint_mbps`, and `plan:` block keys `download_mbps`, `upload_mbps`. Indent-based mini parser, ~30 lines.

    Module-level docstring states purpose + inputs (run_dir tree per RESEARCH §Per-Run Artifact Layout) + outputs (signal-sheet.json + signal-sheet.md WRITTEN INTO the run-dir per MEDIUM-5) + D-13/D-14/D-15 invariants + MEDIUM-2 ceiling-from-config invariant + MEDIUM-1 six-bucket coverage.

    Define threshold constants near the top: `BUCKET_1_PCT_AT_CEILING = 0.80`, `BUCKET_2_TIME_TO_GREEN_SEC = 30`, `BUCKET_2_PEAK_DELAY_US = 50000`, `BUCKET_3_OUTLIER_RATE_MAX = 0.30`, `BUCKET_3_P99_VS_MEDIAN_MULT = 5`, `BUCKET_5_PCT_REFRACTORY_ACTIVE = 0.05`, `BUCKET_5_BACKLOG_SUPPRESSED_DELTA = 100`, `BUCKET_6_THROUGHPUT_DROP_PCT = 0.30`, `BUCKET_6_TTFB_P99_MS = 2000`, `BUCKET_6_OUTLIER_RATE_MAX = 0.10`.

    DO NOT define hardcoded plan caps (`SPECTRUM_PLAN_DL_MBPS = 920` etc.); instead `BUCKET_6` reads `_read_wan_config(wan)["plan"]["download_mbps"]` at runtime.

    Implement six bucket-analysis functions, each taking parsed per-test NDJSON + alert dumps + flent summary + browse CSV and returning `{flagged: bool, evidence_rows: [...], operator_note: str}`.

    **BUCKET 1 (`upload_ceiling_setpoint`) — MEDIUM-2 fix:** Read ceiling via `_read_wan_config(wan)["upload"]["ceiling_mbps"]` for the WAN whose tcp_upload test is being analyzed. Compute `pct_samples_at_ceiling` = fraction of NDJSON rows during tcp_upload where `upload_rate_mbps >= ceiling - 0.5`. Flag when > 0.80. **CRITICAL:** The classifier source MUST NOT contain the literal substring `setpoint_mbps + 6` or `setpoint_mbps+6` anywhere (test_bucket_1_ceiling_source_is_config_not_arithmetic enforces). Document in a top-of-function comment: "Ceiling sourced from configs/<wan>.yaml `upload.ceiling_mbps` (link-agnostic per CLAUDE.md portable-architecture invariant; per codex MEDIUM-2)."

    **BUCKET 2 (`download_recovery_lag`):** Walk NDJSON rows during tcp_12down and rrul post-test buffer, compute `time_to_green_after_red_sec` between last RED/SOFT_RED row and first row where `download_state == GREEN` AND `download_green_streak >= download_green_required` (RESEARCH Open Q #2). Also compute `cake_dl_peak_delay_us_p99` via `statistics.quantiles`. Flag when either threshold exceeded.

    **BUCKET 3 (`measurement_collapse`):** Take `signal_outlier_rate_max` over all rows. Parse `flent-summary.json` (in the symlinked `flent/` subdir per MEDIUM-3) for `throughput_p99` and `throughput_median`. Flag when `signal_outlier_rate_max > 0.30 AND flent_p99 > flent_median * 5`.

    **BUCKET 4 (`steering_drift`):** Compute raw counter deltas `red_count_delta`, `good_count_delta`, `cake_read_failures_delta` from steering-pre.redacted.json vs steering-post.redacted.json. Emit pre_post_state_transition as a raw string. Flag on ANY transition or non-zero counter delta.

    **D-14 INVARIANT FOR BUCKET 4:** This function MUST NOT contain any expression where `green_rtt_ms`, `yellow_rtt_ms`, `red_rtt_ms`, `red_samples_required`, or `green_samples_required` appears on the RHS of a comparison operator. Field names may appear ONLY as dict keys when constructing evidence_row records and in inline comments.

    **BUCKET 5 (`refractory_semantics`):** Compute `pct_samples_refractory_active` from `arb_refractory_active` field, sum `cake_*_backlog_suppressed_count` deltas across test window. Flag on > 0.05 OR delta > 100.

    **BUCKET 6 (`external_isp`):** Compute `flent_throughput_drop_pct_vs_plan` using `_read_wan_config(wan)["plan"]["download_mbps"]` (MEDIUM-2). Take `curl_ttfb_p99_ms` from browse.curl.csv parsed `time_starttransfer` column. AND-of-three flag.

    **Recommendation (D-15):** primary = bucket priority order based on flagged buckets (mapping: 1→215, 2→214, 3→214, 4→216, 5→216, 6→runner-up); runners-up = remaining flagged buckets. If NO bucket flags, primary=214 with explicit operator_note. Output schema per RESEARCH §"Signal-Sheet Output Schema".

    CLI: `argparse` with `--run-dir <path>` (required), `--output-json <path>` (required — orchestrator passes `<RUN_DIR>/signal-sheet.json` per MEDIUM-5), `--output-md <path>` (required — `<RUN_DIR>/signal-sheet.md`), `--help`. The script must NOT import `wanctl.*`. chmod +x.
  </action>
  <verify>
    <automated>test -x scripts/phase213-classify.py &amp;&amp; .venv/bin/python3 -c "import ast, pathlib; ast.parse(pathlib.Path('scripts/phase213-classify.py').read_text())" &amp;&amp; .venv/bin/python3 scripts/phase213-classify.py --help 2&gt;&amp;1 | grep -E -- "--(run-dir|output-json|output-md)" | wc -l &amp;&amp; (grep -E "setpoint_mbps\\s*\\+\\s*6" scripts/phase213-classify.py &amp;&amp; { echo "FAIL: MEDIUM-2 violation — setpoint+6 arithmetic present"; exit 1; } || echo "OK: no setpoint+6 arithmetic") &amp;&amp; (grep -E "from wanctl|import wanctl" scripts/phase213-classify.py &amp;&amp; { echo "FAIL: wanctl import present"; exit 1; } || echo "OK: no wanctl coupling") &amp;&amp; .venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    Script exists, executable, parses as valid Python. `--help` lists --run-dir, --output-json, --output-md. ALL eight classifier tests PASS (six_buckets_present, bucket_1/2/3/5/6 flag tests, bucket_4_no_threshold_name_compare, ranked_recommendation, bucket_1_ceiling_source_is_config_not_arithmetic). test_phase213_mutation_boundary.py PASSES. No `from wanctl` or `import wanctl` line. No `setpoint_mbps + 6` literal anywhere (MEDIUM-2). Ceiling sourced from configs/<wan>.yaml.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write docs/RUNBOOKS/baseline.md + evidence/README.md (D-09, D-12, MEDIUM-5 + MEDIUM-7 wording)</name>
  <read_first>
    - docs/SOAK_HARNESS.md (closest tone/style template for the runbook)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Per-Run Artifact Layout" and §"Bucket → Evidence Map"
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"docs/RUNBOOKS/baseline.md"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md MEDIUM-5, MEDIUM-7
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md (Phase 212 evidence README — exact 6-column index table + redaction policy + production-boundary sections to mirror)
    - scripts/phase213-baseline-capture.sh (Task 1 — the CLI surface the runbook documents)
    - scripts/phase213-classify.py (Task 2 — the classifier behavior the runbook explains)
  </read_first>
  <acceptance_criteria>
    - docs/RUNBOOKS/baseline.md exists with all required sections.
    - Quick-start section uses `--bind-map spectrum=10.10.110.226,att=10.10.110.233` (NOT `--local-bind`).
    - Runbook documents BOTH `--check-manifest` (offline, pytest) AND `--check-prereqs` (live, operator pre-run) modes.
    - Runbook explicitly calls D-11 ordering "serialized" (NOT "concurrent"); calls out the prohibition on `--wans att,spectrum` being described as "concurrent" (MEDIUM-7).
    - Runbook states signal-sheet location as `evidence/RUN-<ts>/signal-sheet.{json,md}` (MEDIUM-5).
    - evidence/README.md has the 6-column index table header verbatim.
    - Neither file contains a forbidden D-10 token outside of explicit "FORBIDDEN" example callouts (the mutation-boundary test scans scripts only, not docs — so doc text mentioning `systemctl restart wanctl@` as a FORBIDDEN example is acceptable; runbook should use a clearly-labeled "Do NOT do this:" code block fence).
  </acceptance_criteria>
  <files>docs/RUNBOOKS/baseline.md, .planning/phases/213-experience-baseline-harness/evidence/README.md</files>
  <action>
    Create `docs/RUNBOOKS/` directory if missing. Write `docs/RUNBOOKS/baseline.md` (~90-160 lines) mirroring `docs/SOAK_HARNESS.md` section order.

    Section 1 "Purpose": Phase 213 experience baseline harness; evidence-only per D-10.

    Section 2 "Quick start": ONE bash command with the per-WAN bind map (HIGH-1 patched):
    ```
    bash scripts/phase213-baseline-capture.sh \
      --bind-map spectrum=10.10.110.226,att=10.10.110.233 \
      --host dallas \
      --wans spectrum,att
    ```
    Plus three variants: offline manifest check (`--check-manifest`); live prereq check (`--check-prereqs`); Spectrum-only (`--wans spectrum`).

    Section 3 "Operational modes": describe the three modes side-by-side: `--check-manifest` (offline, no SSH, used by pytest, exits 0 if schema valid); `--check-prereqs` / `--dry-run` (live, validates SSH + sudo + egress per WAN, exits 0 if all gates pass); real run (no mode flag, generates traffic, writes per-RUN evidence).

    Section 4 "Mutation posture (D-10)" as a callout: lists forbidden operations (service restart, /etc/wanctl edit, steering toggle command, RouterOS write, deploy, profiling harness change, controller config change); allowed = dev-VM traffic generation only.

    Section 5 "Per-WAN bind map and egress probe (codex HIGH-1)": explains why a single `--local-bind` across both WANs is forbidden (per-WAN labels invalidated). Documents Spectrum bind=10.10.110.226 → egress 70.123.224.169; ATT bind=10.10.110.233 → egress recorded-only.

    Section 6 "D-11 serialized order (codex MEDIUM-7)": explicit statement that `--wans spectrum,att` and `--wans att,spectrum` are BOTH serialized — they only differ in WHICH WAN runs first. Calls out that "concurrent" is the wrong word and that D-11 forbids dual-WAN concurrent load.

    Section 7 "Files": table listing the six phase213 scripts + runbook + classifier with roles.

    Section 8 "Per-run artifact tree (MEDIUM-5)": copy the tree from RESEARCH §"Per-Run Artifact Layout" verbatim. Emphasize that signal-sheet.{json,md} live INSIDE `evidence/RUN-<ts>/`, not at the top level.

    Section 9 "Reading the signal sheet": brief paragraph + reference to RESEARCH §"Bucket → Evidence Map".

    Section 10 "Carrying forward": operator authors `213-REPORT.md` citing signal-sheet rows (Plan 05).

    Write `.planning/phases/213-experience-baseline-harness/evidence/README.md` (~40-80 lines) mirroring Phase 212 evidence/README.md: opening paragraph stating Phase 213 evidence-only / D-09 / D-10 / D-08 redaction; "## Evidence Index" with the 6-column table header verbatim; "## Redaction Policy" with the D-08 key pattern; "## Production Boundary" mirroring Phase 212; closing note that per-RUN README is generated by orchestrator into `evidence/RUN-<ts>/README.md` (separate from this top-level index). Add a paragraph noting signal-sheet.{json,md} live INSIDE each `RUN-<ts>/` dir (MEDIUM-5).
  </action>
  <verify>
    <automated>test -f docs/RUNBOOKS/baseline.md &amp;&amp; test -f .planning/phases/213-experience-baseline-harness/evidence/README.md &amp;&amp; grep -F "bash scripts/phase213-baseline-capture.sh" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F -- "--bind-map" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F -- "--check-manifest" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F -- "--check-prereqs" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F "D-10" docs/RUNBOOKS/baseline.md &amp;&amp; grep -E "serialized" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F "evidence/RUN-" docs/RUNBOOKS/baseline.md &amp;&amp; grep -F "| Timestamp (UTC) | Source host | Command purpose | Redaction method | Output file | Mutation posture |" .planning/phases/213-experience-baseline-harness/evidence/README.md &amp;&amp; grep -F "password|secret|token|credential|auth|key|private" .planning/phases/213-experience-baseline-harness/evidence/README.md</automated>
  </verify>
  <done>
    Both files exist. Runbook quick-start uses `--bind-map` (not `--local-bind`). Documents `--check-manifest` AND `--check-prereqs`. Says "serialized" for D-11 ordering (not "concurrent"). References `evidence/RUN-<ts>/signal-sheet.*`. evidence/README.md has the 6-column index table header verbatim and the D-08 redaction policy line.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → orchestrator CLI | Inherits Plans 02/03 boundaries; HIGH-1 per-WAN bind map prevents WAN/source mislabeling. HIGH-3 offline mode prevents inadvertent SSH from pytest. |
| Classifier → run-dir filesystem | Offline read-only. No network. Stdlib-only. Reads configs/<wan>.yaml at runtime (MEDIUM-2). |
| Runbook + evidence README → operator | Read-only documentation; no executable content beyond example commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I | Evidence dir command-log + per-RUN README (orchestrator-written) | mitigate | Top-level evidence/README.md states D-08 redaction policy explicitly; orchestrator appends only command names + timestamps to .command-log (no env values, no secret-bearing args). |
| T-213-02 | T | Orchestrator invokes phase191-flent-capture.sh and phase213-*.sh leaves | mitigate | All invocations are read-side or dev-VM traffic generation (D-10 allowance). Mutation-boundary test scans the orchestrator source. |
| T-213-03 | I | Concurrent live-writer SQLite via phase213-alert-window.sh | mitigate | Inherits Plan 03's mode=ro URI gate. |
| T-213-04 | D | Per-WAN serialized full suite duration ~10 min/WAN | accept | D-11 sequencing already bounds blast radius; orchestrator records start/end timestamps. |
| T-213-05 | E | D-14 steering drift bucket source-grep enforcement | mitigate | Plan 01's test_phase213_classify.py::test_bucket_4_steering_drift_no_threshold_name_compare greps the classifier source. |
| T-213-06 (codex HIGH-3) | T (Tampering) | "Offline" pytest tests inadvertently SSH to cake-shaper via the orchestrator | mitigate | `--check-manifest` code branch contains NO `ssh ` / `curl ` invocations. test_phase213_manifest_schema.py::test_check_manifest_is_offline source-greps the branch to enforce. Pytest no longer requires production reachability. |
| T-213-07 (codex HIGH-1) | T (Tampering) | WAN/source-bind mislabeling — single bind across two WANs invalidates per-WAN labels | mitigate | `--bind-map` flag is REQUIRED; the orchestrator parses spectrum=<ip>,att=<ip> into an associative array and passes the per-WAN bind to each WAN's phase191/browse invocation. Per-WAN egress probe runs before each WAN's test suite and refuses on mismatch (Spectrum: hard-fail on egress != 70.123.224.169; ATT: record-only). Source-grep gate: `grep -E "for WAN in.*--local-bind \\$ONE_BIND" scripts/phase213-baseline-capture.sh` must return empty. |
| T-213-08 (codex HIGH-4) | D (Denial of service) | Background poller leak on `set -e` mid-test failure | mitigate | `POLLER_PIDS=()` array tracks every poller PID; `cleanup_pollers()` function kills+waits idempotently; `trap cleanup_pollers EXIT INT TERM` registered globally before any poller spawn. Inner per-test cleanup also fires. Source-grep gate enforces trap presence. |
| T-213-09 (codex MEDIUM-2) | E (Elevation of privilege) | Bucket 1 ceiling inferred from `setpoint + 6` arithmetic (Spectrum-specific) bakes a non-portable assumption into the classifier | mitigate | Classifier reads ceiling from configs/<wan>.yaml. Source-grep test enforces no `setpoint_mbps + 6` literal anywhere in classifier source. Link-agnostic architecture (CLAUDE.md portable-controller invariant) holds. |
</threat_model>

<verification>
- `bash -n scripts/phase213-baseline-capture.sh` exits 0.
- `python -c "import ast; ast.parse(open('scripts/phase213-classify.py').read())"` exits 0.
- `.venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_classify.py tests/test_phase213_manifest_schema.py tests/test_phase213_ndjson_schema.py tests/test_phase213_alert_window.py -q` ALL FIVE FILES GREEN.
- `bash scripts/phase213-baseline-capture.sh --help` lists `--bind-map`, `--check-manifest`, `--check-prereqs` and the other 9 flags (12 total).
- `bash scripts/phase213-baseline-capture.sh --check-manifest --evidence-root /tmp/p213-check --bind-map spectrum=fixture,att=fixture` exits 0 with NO network reachability required.
- `grep -F "trap" scripts/phase213-baseline-capture.sh | grep -F "cleanup_pollers" | grep -F "EXIT"` matches.
- `grep -E "setpoint_mbps\s*\+\s*6" scripts/phase213-classify.py` returns nothing.
- `.venv/bin/python3 scripts/phase213-classify.py --help` lists --run-dir, --output-json, --output-md.
- `grep -F "evidence/RUN-" docs/RUNBOOKS/baseline.md` matches (MEDIUM-5).
- `grep -F "| Timestamp (UTC) | Source host | Command purpose | Redaction method | Output file | Mutation posture |" .planning/phases/213-experience-baseline-harness/evidence/README.md` matches.
- `grep -E "from wanctl|import wanctl" scripts/phase213-classify.py` returns nothing.
- `git diff --stat src/wanctl/` returns empty (SAFE-05 invariant).
</verification>

<success_criteria>
- All four artifacts shipped: orchestrator, classifier, runbook, evidence README.
- All FIVE Wave 0 test files (mutation-boundary, classify, manifest-schema, ndjson-schema, alert-window) GREEN.
- All eight classifier tests PASS — buckets 1/2/3/5/6 flag tests AND bucket 4 no-threshold-name-compare AND recommendation AND ceiling-from-config (MEDIUM-1 + MEDIUM-2).
- `--check-manifest` exits 0 offline (HIGH-3); `--check-prereqs` runs live gates (replaces `--dry-run`).
- `--bind-map` parses per-WAN binds; single-bind-across-WANs anti-pattern absent (HIGH-1).
- `trap cleanup_pollers EXIT INT TERM` present (HIGH-4); POLLER_PIDS array tracks all background pollers.
- Flent paths normalized into `evidence/RUN-<ts>/<wan>/<test>/flent` symlink (MEDIUM-3).
- Signal-sheet emitted INTO `evidence/RUN-<ts>/signal-sheet.{json,md}` (MEDIUM-5).
- D-14 invariant holds: classifier source contains zero threshold-field-name-on-comparison-RHS expressions.
- D-15 invariant holds: recommended_next_phase has `primary in {214,215,216}` and `len(runners_up) >= 1`.
- evidence/README.md mirrors Phase 212 6-column command-index shape.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-04-SUMMARY.md` covering: orchestrator line count, classifier line count, runbook line count, all FIVE Wave 0 tests now GREEN (paste pytest summary), the threshold constants emitted as named module-level constants in the classifier, the D-15 default-recommendation logic, the artifact tree the orchestrator writes per RUN-<ts> (with signal-sheet INSIDE), and the codex review patches landed: HIGH-1 (bind-map), HIGH-3 (--check-manifest offline), HIGH-4 (trap cleanup), MEDIUM-1 (per-bucket tests), MEDIUM-2 (config-driven ceiling), MEDIUM-3 (flent path normalization), MEDIUM-5 (signal-sheet in RUN dir), MEDIUM-7 (serialized wording). Surface the handoff to Plan 05.
</output>
