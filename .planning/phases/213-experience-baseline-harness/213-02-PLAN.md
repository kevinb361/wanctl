---
phase: 213-experience-baseline-harness
plan: 02
type: execute
wave: 2
depends_on: [213-01]
files_modified:
  - scripts/phase213-health-poller.sh
  - scripts/phase213-browse-loop.sh
autonomous: true
requirements: [BASE-01, BASE-02]
tags: [baseline, harness, poller, browse, ndjson, dev-vm]

must_haves:
  truths:
    - "Per-WAN /health is polled at 1Hz emitting extended NDJSON rows with every key from ndjson-row-expected-keys.json."
    - "Poller survives transient curl/jq failures (HRDN-02 bounded-failure pattern) and writes a TSV sidecar for failed rows."
    - "Curl-browse loop fetches the planner-fixed seven-site rotation with `--interface <local-bind>` source binding and records ts_utc, site, http_code, time_starttransfer, time_total, size_download, exit_code per request as CSV."
    - "Both scripts originate from the dev VM only; neither reads /etc/wanctl, restarts services, or touches RouterOS."
  artifacts:
    - path: "scripts/phase213-health-poller.sh"
      provides: "Sibling of scripts/soak-capture.sh with extended jq projection (52 keys per RESEARCH §Pattern 2). Accepts --endpoint, --wan, --output, --duration."
      min_lines: 150
    - path: "scripts/phase213-browse-loop.sh"
      provides: "Curl-browse loop. Accepts --output, --duration, --local-bind, --sites. Emits CSV with 7-column header."
      min_lines: 60
  key_links:
    - from: "scripts/phase213-health-poller.sh"
      to: "tests/fixtures/phase213/ndjson-row-expected-keys.json"
      via: "jq projection emits every key in required_keys"
      pattern: "jq -c"
    - from: "scripts/phase213-browse-loop.sh"
      to: "Spectrum / ATT WAN egress (via --local-bind)"
      via: "curl --interface $LOCAL_BIND"
      pattern: "curl --interface"
    - from: "scripts/phase213-health-poller.sh"
      to: "TSV sidecar at $CAPTURE_DIR/poll-failures.tsv"
      via: "HRDN-02 bounded-failure tracking"
      pattern: "row_failed"
---

<objective>
Build the two independent dev-VM traffic/telemetry surfaces for Phase 213: `phase213-health-poller.sh` (extended-projection 1Hz NDJSON poll of one `/health` endpoint) and `phase213-browse-loop.sh` (curl-browse TTFB+total-time CSV loop). Both run from the dev VM, both honor the D-10 mutation boundary, and both follow PATTERNS.md analogs verbatim except for the documented changes.

Purpose: These are the leaf-level telemetry surfaces the Wave 3 orchestrator wires. The poller's extended jq projection is the BASE-02 contract that the Wave 0 schema test enforces — it must emit every key in `tests/fixtures/phase213/ndjson-row-expected-keys.json`. The browse loop is the D-02 "normal browsing" surface that runs in parallel with flent during each test window.

Output: Two new executable bash scripts. No tests written here — Plan 01 already authored `tests/test_phase213_ndjson_schema.py` to verify the projection and `tests/test_phase213_mutation_boundary.py` to verify D-10 compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@scripts/soak-capture.sh
@scripts/phase198-rerun-flent-3run.sh
@.planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json

<interfaces>
<!-- Health poller CLI contract (consumed by Wave 3 orchestrator): -->
<!-- bash scripts/phase213-health-poller.sh --endpoint <url> --wan <name> --output <ndjson-path> [--duration <sec-or-0>] -->
<!-- Exit 0 on SIGTERM (orchestrator-driven lifecycle); exit nonzero only on prerequisite failure (missing jq/curl). -->

<!-- Browse loop CLI contract: -->
<!-- bash scripts/phase213-browse-loop.sh --output <csv-path> [--duration <sec>] [--local-bind <ip>] [--sites <csv>] -->
<!-- CSV header (exact): ts_utc,site,http_code,time_starttransfer,time_total,size_download,exit_code -->

<!-- Forbidden tokens from Plan 01 D-10 enforcement (test_phase213_mutation_boundary.py): -->
<!-- "systemctl\\s+restart", "systemctl\\s+(start|stop)\\s+wanctl", ">\\s*/etc/wanctl/", -->
<!-- "deploy(_clean|_steering)?\\.sh", "install(-systemd)?\\.sh", "/ip/cake/", "/queue/tree/", -->
<!-- "ssh.*router.*set", "steering\\s+toggle|toggle\\s+steering" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/phase213-health-poller.sh with extended jq projection (BASE-02)</name>
  <read_first>
    - scripts/soak-capture.sh (entire file ~164 lines — analog source; copy HRDN-02 bounded-failure loop, body_tmp/out_tmp/err_tmp truncation pattern, four failure-mode classification, TSV sidecar emit, SOAK_FAIL_RATE_THRESHOLD/MIN_SAMPLES_BEFORE_EVAL gate)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Pattern 2: Sibling NDJSON Poller with Extended `jq` Projection" (verbatim jq projection block, ~58 lines)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Per-row NDJSON schema (extended)" (52-field source-path table)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-health-poller.sh" (analog assignment + changes-from-analog list)
    - tests/fixtures/phase213/ndjson-row-expected-keys.json (the key set this script's jq projection must emit — built in Plan 01)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json (verify every jq source path resolves against real /health payload)
  </read_first>
  <files>scripts/phase213-health-poller.sh</files>
  <action>
    Copy scripts/soak-capture.sh into scripts/phase213-health-poller.sh and edit per PATTERNS.md changes-from-analog list. Replace positional SOAK_TS arg parsing with long-flag parsing for --endpoint, --wan, --output, --duration (default 0 meaning "until SIGTERM"). When --duration is 0, omit the SOAK_END timer and run the poll loop until SIGTERM is received; install a `trap 'exit 0' TERM INT` so the orchestrator can `kill` the poller cleanly. When --duration is >0, behave like soak-capture.sh's SOAK_DURATION_SEC pattern. Replace the soak-capture jq projection block (lines ~94-124, upload-only) with the verbatim 52-field jq projection from RESEARCH §"Pattern 2" — keep `--arg twall "$(date -u -Iseconds)" --argjson tmono "$T_MONO_DELTA" --arg wan "$WAN"` argument injection. Preserve the four failure-mode classification (curl_exit_nonzero, curl_http_nonzero, empty_body, jq_parse_error), TSV sidecar at `${OUTPUT%/*}/poll-failures.tsv`, body_tmp/out_tmp/err_tmp truncation per iteration, and the HRDN-02 fail-rate gate (SOAK_FAIL_RATE_THRESHOLD env defaulting to 0.30; MIN_SAMPLES_BEFORE_EVAL env defaulting to 30). Output NDJSON rows append to the file named by --output. Add a top-of-file docstring stating: "Phase 213 sibling poller of soak-capture.sh; D-09/D-10 evidence-only; emits extended /health projection per RESEARCH §Pattern 2." Do NOT include any of the 9 D-10 forbidden tokens. Use `set -euo pipefail`. Make the script executable (chmod +x).
  </action>
  <verify>
    <automated>test -x scripts/phase213-health-poller.sh &amp;&amp; bash -n scripts/phase213-health-poller.sh &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -5 &amp;&amp; .venv/bin/pytest tests/test_phase213_ndjson_schema.py -q 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    Script exists, is executable, passes `bash -n` syntax check. tests/test_phase213_mutation_boundary.py PASSES with the poller no longer skipped. tests/test_phase213_ndjson_schema.py PASSES — the jq projection emits every key in ndjson-row-expected-keys.json when piped phase212_health_spectrum fixture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/phase213-browse-loop.sh curl-browse CSV emitter (D-02)</name>
  <read_first>
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Pattern 5: Curl-Browse Loop (D-02)" (recommended sites, cadence, full bash example ~30 lines)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Open Questions" #4 (cache-busting recommendation: `?cache_bust=$(date +%s%N)`)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-browse-loop.sh" (CLI flag style + curl --interface + --write-out discipline)
    - scripts/phase198-rerun-flent-3run.sh lines 270-289 (curl --interface egress-probe pattern)
    - scripts/soak-capture.sh lines 75-90 (curl --write-out + --output + exit-code handling)
  </read_first>
  <files>scripts/phase213-browse-loop.sh</files>
  <action>
    Create a new bash script (target ~60-80 lines) following RESEARCH §"Pattern 5" body. `set -euo pipefail`. CLI flags via the phase198/phase191 long-flag style: `--output <csv-path>` (required), `--duration <sec>` (default 60), `--local-bind <ip>` (default 10.10.110.233), `--sites <csv>` (optional override; default to the seven-site array). Default SITES array contains exactly: `https://www.google.com/`, `https://www.cloudflare.com/`, `https://github.com/`, `https://www.wikipedia.org/`, `https://news.ycombinator.com/`, `https://www.bbc.com/news`, `https://i.imgur.com/aB6Z9zN.jpg`. CSV header on first line of $OUTPUT: `ts_utc,site,http_code,time_starttransfer,time_total,size_download,exit_code` (no trailing newline before first data row). Loop until `$(date +%s) -lt $T_END`; per iteration: select `SITES[i % len]`, build URL with cache-bust query string per RESEARCH Open Question #4 (`?cache_bust=$(date +%s%N)` — append if URL has no `?`, else `&cache_bust=...`), record TS, run `curl --interface "$LOCAL_BIND" --silent --max-time 10 -w '%{http_code},%{time_starttransfer},%{time_total},%{size_download}' -o /dev/null "$URL" 2>/dev/null` capturing exit code; append CSV row `"${TS},${SITE},${OUT},${EC}"`. Increment i, `sleep 2`. Do NOT inherit soak-capture.sh's HRDN-02 fail-rate gate — per PATTERNS.md, failures ARE data here; record exit_code in the row and continue. Top-of-file docstring states D-02/D-09/D-10 posture. Do NOT include any of the 9 D-10 forbidden tokens. chmod +x.
  </action>
  <verify>
    <automated>test -x scripts/phase213-browse-loop.sh &amp;&amp; bash -n scripts/phase213-browse-loop.sh &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -5 &amp;&amp; bash scripts/phase213-browse-loop.sh --output /tmp/phase213-browse-smoke.csv --duration 4 --local-bind 127.0.0.1 --sites "https://example.com/" 2&gt;&amp;1 | tail -3 ; head -2 /tmp/phase213-browse-smoke.csv</automated>
  </verify>
  <done>
    Script exists, is executable, passes `bash -n` syntax check. tests/test_phase213_mutation_boundary.py still PASSES (browse-loop skip removed). Smoke run against 127.0.0.1 with a 4-second window writes CSV with the exact 7-column header on line 1 and at least one data row. Curl exit codes are captured (non-zero rows do not abort the loop). No D-10 forbidden token present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev VM → autorate /health (HTTP) | Read-only HTTP polling against bound endpoints on cake-shaper. No mutation; HTTP daemon-state evidence only (D-17). |
| Dev VM → public internet (curl-browse) | Outbound HTTP traffic with source-bind. Cache-bust query string per Open Q #4 prevents cache-hit noise but does not exfiltrate identifying data beyond standard HTTP fingerprint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I (Information disclosure) | NDJSON output containing /health payload | mitigate | jq projection is explicit allow-list — never `'.'` dump (Pitfall 5); 52 keys are operating-point/state evidence, not secrets. Output file is local; redaction policy applies only to evidence committed under .planning/phases/213/evidence/ (Plan 04 owns committed-artifact redaction). |
| T-213-02 | T (Tampering) | scripts/phase213-health-poller.sh under D-10 mutation boundary | mitigate | test_phase213_mutation_boundary.py (Plan 01) greps for the 9 forbidden tokens; Plan 02 commits cannot land with mutation patterns in either script. |
| T-213-04 | D (Denial of service) | Sustained 1Hz poll against autorate /health (Pitfall 5: ~30KB response × 1Hz × 2 WANs) | accept | 1Hz is documented soak cadence (RESEARCH §Pitfall 5 + soak-capture.sh precedent); poller body discards raw 30KB after jq projection. Bandwidth + daemon CPU impact identical to existing soak harness. |
| T-213-04 | D | Browse-loop traffic generation impact on other production users | accept | Single CDN fetch every 2s × 60s window = 30 fetches; impact negligible vs. flent. D-11 sequencing limits per-WAN duration; D-10 allows this as the only mutation kind permitted. |
</threat_model>

<verification>
- `bash -n scripts/phase213-health-poller.sh` exits 0.
- `bash -n scripts/phase213-browse-loop.sh` exits 0.
- `.venv/bin/pytest tests/test_phase213_mutation_boundary.py -q` PASSES (browse-loop and health-poller no longer skipped).
- `.venv/bin/pytest tests/test_phase213_ndjson_schema.py -q` PASSES (jq projection emits every required key).
- Smoke browse-loop run against `--sites "https://example.com/" --duration 4 --local-bind 127.0.0.1` produces CSV with 7-column header and at least one data row.
- `git diff --stat src/wanctl/` returns empty (SAFE-05 invariant).
</verification>

<success_criteria>
- Both new scripts executable, syntactically valid bash.
- jq projection in poller emits exactly the 52-key set defined in ndjson-row-expected-keys.json.
- Browse-loop CSV header matches the exact 7-column contract.
- mutation-boundary test and ndjson-schema test both GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-02-SUMMARY.md` covering: script line counts, jq projection key count (must = 52), HRDN-02 thresholds inherited from soak-capture.sh, browse-loop default site count (7), CLI contracts for both scripts (downstream orchestrator in Plan 04 will invoke these).
</output>
