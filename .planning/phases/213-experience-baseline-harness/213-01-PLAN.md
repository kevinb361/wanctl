---
phase: 213-experience-baseline-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/fixtures/phase213/health-spectrum-snapshot.json
  - tests/fixtures/phase213/health-att-snapshot.json
  - tests/fixtures/phase213/health-steering-snapshot.json
  - tests/fixtures/phase213/alerts-test.db
  - tests/fixtures/phase213/_generate_alerts_db.py
  - tests/fixtures/phase213/signal-sheet-expected-empty.json
  - tests/fixtures/phase213/signal-sheet-expected-ul-ceiling-flagged.json
  - tests/fixtures/phase213/ndjson-row-expected-keys.json
  - tests/fixtures/phase213/manifest-expected-keys.json
  - tests/fixtures/phase213/RUN-bucket-2/
  - tests/fixtures/phase213/RUN-bucket-3/
  - tests/fixtures/phase213/RUN-bucket-5/
  - tests/fixtures/phase213/RUN-bucket-6/
  - tests/fixtures/phase213/mutation-fixtures/legitimate-doc.sh
  - tests/fixtures/phase213/mutation-fixtures/forbidden-restart.sh
  - tests/test_phase213_ndjson_schema.py
  - tests/test_phase213_classify.py
  - tests/test_phase213_manifest_schema.py
  - tests/test_phase213_mutation_boundary.py
  - tests/test_phase213_alert_window.py
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
tags: [baseline, harness, evidence, classifier, fixtures, mutation-boundary]

must_haves:
  truths:
    - "Pytest fixtures load Phase 212 health JSONs without re-probing production (D-19)."
    - "Six-bucket classifier behavior is asserted before classifier source exists; tests RED until Wave 2."
    - "NDJSON per-row schema is fixed by an expected-key set referenced by downstream pollers."
    - "Per-run manifest schema is fixed by an expected-key set referenced by the orchestrator."
    - "Mutation-boundary grep guard runs offline, is parametrized per-script (no whole-test-skip-on-first-missing), uses tightened regex (no false positives on narrative strings), and is exercised by positive/negative mutation-fixture scripts."
    - "Alert-window SQL behavior is exercised against tests/fixtures/phase213/alerts-test.db via a `--local-db` mode (no SSH required for pytest)."
    - "All six classifier buckets have golden-file fixtures so buckets 2/3/5/6 cannot regress silently."
  artifacts:
    - path: "tests/fixtures/phase213/health-spectrum-snapshot.json"
      provides: "Spectrum /health fixture copied verbatim from Phase 212 evidence"
    - path: "tests/fixtures/phase213/health-att-snapshot.json"
      provides: "ATT /health fixture (verified IRTT-bearing payload)"
    - path: "tests/fixtures/phase213/health-steering-snapshot.json"
      provides: "v1.39 steering /health fixture, threshold field names verbatim (D-08 carry-forward)"
    - path: "tests/fixtures/phase213/alerts-test.db"
      provides: "Synthetic SQLite alerts fixture built from src/wanctl/storage/schema.py create_tables"
    - path: "tests/fixtures/phase213/ndjson-row-expected-keys.json"
      provides: "Canonical 40+ key set from RESEARCH §Per-row NDJSON schema (extended)"
    - path: "tests/fixtures/phase213/manifest-expected-keys.json"
      provides: "Canonical manifest key set from RESEARCH §Per-run top-level manifest.json schema"
    - path: "tests/fixtures/phase213/signal-sheet-expected-empty.json"
      provides: "Golden file: classifier output when no buckets flag"
    - path: "tests/fixtures/phase213/signal-sheet-expected-ul-ceiling-flagged.json"
      provides: "Golden file: classifier output when BUCKET 1 flags"
    - path: "tests/fixtures/phase213/RUN-bucket-2/"
      provides: "Minimal run-dir fixture flagging BUCKET 2 (download_recovery_lag)"
    - path: "tests/fixtures/phase213/RUN-bucket-3/"
      provides: "Minimal run-dir fixture flagging BUCKET 3 (measurement_collapse)"
    - path: "tests/fixtures/phase213/RUN-bucket-5/"
      provides: "Minimal run-dir fixture flagging BUCKET 5 (refractory_semantics)"
    - path: "tests/fixtures/phase213/RUN-bucket-6/"
      provides: "Minimal run-dir fixture flagging BUCKET 6 (external_isp)"
    - path: "tests/fixtures/phase213/mutation-fixtures/legitimate-doc.sh"
      provides: "Positive fixture: narrative text mentioning 'steering toggle' in a comment must NOT trip the mutation-boundary regex"
    - path: "tests/fixtures/phase213/mutation-fixtures/forbidden-restart.sh"
      provides: "Negative fixture: `systemctl restart wanctl@spectrum` MUST trip the mutation-boundary regex"
    - path: "tests/test_phase213_ndjson_schema.py"
      provides: "Asserts poller jq projection emits every key in ndjson-row-expected-keys.json"
    - path: "tests/test_phase213_classify.py"
      provides: "Per-bucket golden-file tests (all six buckets), D-14 no-threshold-name-compare, recommendation presence"
    - path: "tests/test_phase213_manifest_schema.py"
      provides: "Asserts orchestrator `--check-manifest` (offline) emits every key in manifest-expected-keys.json"
    - path: "tests/test_phase213_mutation_boundary.py"
      provides: "Parametrized per-script forbidden-pattern grep — independent skip per script + positive/negative mutation-fixture cases"
    - path: "tests/test_phase213_alert_window.py"
      provides: "Behavioral test of phase213-alert-window.sh against tests/fixtures/phase213/alerts-test.db via --local-db mode"
    - path: "tests/conftest.py"
      provides: "Extended with phase212_health_{spectrum,att,steering} session fixtures"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/phase213/health-*.json"
      via: "json.loads(... .read_text())"
      pattern: "fixtures/phase213/health-"
    - from: "tests/test_phase213_classify.py"
      to: "scripts/phase213-classify.py (built Wave 2)"
      via: "subprocess.run([sys.executable, 'scripts/phase213-classify.py', ...])"
      pattern: "phase213-classify"
    - from: "tests/test_phase213_mutation_boundary.py"
      to: "PHASE213_SCRIPTS list (files built Waves 1+2) — parametrized per script"
      via: "@pytest.mark.parametrize over the list; each script case skips independently"
      pattern: "@pytest.mark.parametrize"
    - from: "tests/test_phase213_alert_window.py"
      to: "scripts/phase213-alert-window.sh --local-db tests/fixtures/phase213/alerts-test.db"
      via: "subprocess.run(['bash', script, '--local-db', ..., '--start', ..., '--end', ..., '--output-dir', tmp])"
      pattern: "--local-db"
---

<objective>
Establish Wave 0 test scaffolding for Phase 213: pytest fixtures derived from Phase 212 evidence, golden-file outputs for the classifier (all six buckets), and behavior tests for the five downstream surfaces (NDJSON schema, classifier buckets, manifest schema, mutation-boundary grep, alert-window SQL). Tests are RED on commit and turn GREEN as Waves 1 and 2 add their script implementations.

Purpose: Lock the contracts before code lands. The NDJSON expected-key set, manifest expected-key set, six per-bucket golden-file fixtures, and D-10 forbidden-pattern list are the artifacts every downstream plan reads to know "what done looks like." This plan also enforces D-19 by copying Phase 212 evidence in once, ending re-probe drift.

Codex review patches landed here:
- HIGH-5: parametrized mutation-boundary test (per-script independent skip) + tightened regex (no false positives on narrative comment lines / heredoc bodies) + positive/negative mutation-fixture scripts.
- MEDIUM-1: per-bucket golden-file fixtures for buckets 2/3/5/6 so they cannot regress silently.
- MEDIUM-6: behavioral alert-window SQL test against the local fixture DB via the `--local-db` mode (added in Plan 03).

Output: 22 files under `tests/` and `tests/fixtures/phase213/`. No scripts touched, no production access, no `src/wanctl/` modification (CLAUDE.md SAFE-05 invariant holds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-VALIDATION.md
@.planning/phases/213-experience-baseline-harness/213-REVIEWS.md
@.planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json
@.planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json
@.planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json
@tests/conftest.py
@tests/test_analyze_baseline.py
@src/wanctl/storage/schema.py

<interfaces>
<!-- The 40+ NDJSON keys from RESEARCH §Pattern 2 must populate ndjson-row-expected-keys.json verbatim. -->
<!-- The manifest keys from RESEARCH §Per-Run Artifact Layout must populate manifest-expected-keys.json verbatim. -->

ndjson-row-expected-keys.json schema (RESEARCH §Pattern 2 "Per-row NDJSON schema (extended)"):
{
  "required_keys": [
    "t_wall", "t_monotonic_sec", "wan", "version", "status",
    "download_state", "download_state_reason", "download_rate_mbps",
    "download_green_streak", "download_green_required",
    "upload_state", "upload_state_reason", "upload_rate_mbps",
    "upload_setpoint_mbps", "upload_docsis_mode_active",
    "upload_floor_hit_cycles_total", "upload_headroom_state",
    "upload_headroom_exhausted_streak", "upload_red_streak", "upload_anti_windup_triggers",
    "cake_dl_peak_delay_us", "cake_dl_drop_rate",
    "cake_ul_peak_delay_us", "cake_ul_drop_rate",
    "cake_dl_backlog_suppressed_count", "cake_ul_backlog_suppressed_count",
    "cake_dl_refractory_remaining", "cake_ul_refractory_remaining",
    "cake_refractory_cycles", "cake_burst_active", "cake_burst_trigger_count",
    "arb_active_primary_signal", "arb_control_decision_reason",
    "arb_refractory_active", "arb_rtt_confidence",
    "signal_confidence", "signal_outlier_rate", "signal_warming_up",
    "measurement_state", "measurement_stale", "measurement_staleness_sec", "measurement_successful_count",
    "baseline_rtt_ms", "load_rtt_ms", "load_rtt_delta_us",
    "irtt_rtt_mean_ms", "irtt_loss_up_pct", "irtt_loss_down_pct", "irtt_asymmetry_ratio",
    "router_reachable", "alerting_fire_count", "alerting_active_cooldowns_count"
  ]
}

manifest-expected-keys.json schema (RESEARCH §Per-run top-level manifest.json schema):
{
  "required_top_level_keys": [
    "phase", "run_id", "started_utc", "ended_utc",
    "host_dev_vm", "source_ip", "netperf_host",
    "flent_version", "wanctl_version_dev_repo",
    "wanctl_version_spectrum_runtime", "wanctl_version_att_runtime", "wanctl_version_steering_runtime",
    "git_head_sha", "tests_ordered",
    "redaction_posture", "mutation_posture",
    "bind_map"
  ],
  "required_test_entry_keys": ["wan", "test", "test_start_unix", "test_end_unix"]
}

src/wanctl/storage/schema.py — alerts table columns (verified): timestamp INTEGER, alert_type TEXT, severity TEXT, wan_name TEXT, details TEXT.

D-10 forbidden patterns (tightened per codex HIGH-5 — see test source for exact regex literals):
- `\bsystemctl\s+restart\s+wanctl@`           (RESTART of wanctl@ unit, not bare "systemctl restart")
- `\bsystemctl\s+(start|stop)\s+wanctl`      (start/stop of wanctl unit)
- `>\s*/etc/wanctl/.*\.ya?ml`                 (write redirect to YAML config, not bare /etc/wanctl mention)
- `\bdeploy(_clean|_steering)?\.sh\b`        (deploy script invocation)
- `\binstall(-systemd)?\.sh\b`               (install script invocation)
- `ssh\s+\S+\s+["']?\s*/system/script\b|/ip/cake/|/queue/tree/`   (RouterOS write paths via SSH)
- COMMAND-LEVEL steering toggle: `\bsteering-(enable|disable)\b`, `\bwanctl\s+steering\s+toggle\b`
  (NOT the narrative phrase "steering toggle" — that may appear in docstrings/heredocs)

Comment-line filter: lines whose first non-whitespace char is `#` are excluded from the scan (avoids self-invalidating grep gate AND avoids docstring false positives that codex flagged).

bind_map (new manifest key per codex HIGH-1): {"spectrum": "<bind-ip>", "att": "<bind-ip>"} — orchestrator records the per-WAN bind in the manifest so cross-run comparability is auditable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed fixtures and conftest from Phase 212 evidence + canonical key sets + per-bucket run-dirs + mutation-fixtures</name>
  <read_first>
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json (source of truth for Spectrum schema; copy verbatim)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json (source of truth for ATT IRTT block presence)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json (verbatim v1.39 threshold field names — D-08/D-14 carry-forward)
    - tests/conftest.py (existing file; extend, do not replace)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Pattern 2: Sibling NDJSON Poller" (NDJSON key list)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Per-run top-level manifest.json schema" (manifest key list)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map" (threshold values for per-bucket fixtures)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"tests/conftest.py (additions only)" (extension pattern)
    - tests/test_analyze_baseline.py (pytest style template for fixture-DB build)
    - src/wanctl/storage/schema.py lines 68-87 (alerts table column definitions for fixture DB)
  </read_first>
  <files>
    tests/fixtures/phase213/health-spectrum-snapshot.json,
    tests/fixtures/phase213/health-att-snapshot.json,
    tests/fixtures/phase213/health-steering-snapshot.json,
    tests/fixtures/phase213/ndjson-row-expected-keys.json,
    tests/fixtures/phase213/manifest-expected-keys.json,
    tests/fixtures/phase213/_generate_alerts_db.py,
    tests/fixtures/phase213/alerts-test.db,
    tests/fixtures/phase213/RUN-bucket-2/spectrum/rrul/health-spectrum.ndjson,
    tests/fixtures/phase213/RUN-bucket-2/manifest.json,
    tests/fixtures/phase213/RUN-bucket-3/spectrum/tcp_download/health-spectrum.ndjson,
    tests/fixtures/phase213/RUN-bucket-3/spectrum/tcp_download/flent-summary.json,
    tests/fixtures/phase213/RUN-bucket-3/manifest.json,
    tests/fixtures/phase213/RUN-bucket-5/spectrum/tcp_upload/health-spectrum.ndjson,
    tests/fixtures/phase213/RUN-bucket-5/manifest.json,
    tests/fixtures/phase213/RUN-bucket-6/spectrum/tcp_download/health-spectrum.ndjson,
    tests/fixtures/phase213/RUN-bucket-6/spectrum/tcp_download/flent-summary.json,
    tests/fixtures/phase213/RUN-bucket-6/spectrum/browse/browse.curl.csv,
    tests/fixtures/phase213/RUN-bucket-6/manifest.json,
    tests/fixtures/phase213/mutation-fixtures/legitimate-doc.sh,
    tests/fixtures/phase213/mutation-fixtures/forbidden-restart.sh,
    tests/conftest.py
  </files>
  <action>
    Copy the three Phase 212 evidence health JSONs verbatim into tests/fixtures/phase213/ with the `-snapshot.json` suffix (use `cp` not heredoc; preserve exact byte content per D-19). Create ndjson-row-expected-keys.json with the verbatim 52-element required_keys array from the interfaces block (sourced from RESEARCH §Pattern 2 "Per-row NDJSON schema (extended)"). Create manifest-expected-keys.json with required_top_level_keys (17 entries — adds `bind_map` per codex HIGH-1) and required_test_entry_keys per the interfaces block. Create _generate_alerts_db.py as a small Python helper that uses `sqlite3.connect("tests/fixtures/phase213/alerts-test.db")`, imports `from wanctl.storage.schema import create_tables`, calls create_tables(conn), then INSERTs 8 synthetic alert rows spanning two WANs (spectrum, att), three alert_types (cake_drop_high, flapping_dl, headroom_exhausted), two severities (warning, error), with timestamps clustered in two windows (window A: 1717000000..1717000060, window B: 1717000100..1717000160) so the alert-window query test can assert exact row counts per window. Run the generator once and check alerts-test.db into the fixtures dir.

    **Per-bucket run-dir fixtures (MEDIUM-1):** Create four minimal evidence trees under tests/fixtures/phase213/RUN-bucket-N/ for N in {2,3,5,6}. Each is the smallest evidence dir that flags the named bucket per RESEARCH §"Bucket → Evidence Map":
    - RUN-bucket-2/spectrum/rrul/health-spectrum.ndjson: 40 rows. First 5 rows download_state=GREEN; rows 5–25 download_state=RED; rows 25–40 download_state=GREEN with download_green_streak < download_green_required for first 35 seconds, then meets threshold — yielding time_to_green_after_red_sec > 30. Include cake_dl_peak_delay_us > 50000 in 3+ rows. RUN-bucket-2/manifest.json: minimal {"phase":213,"run_id":"bucket-2-fixture","tests_ordered":[{"wan":"spectrum","test":"rrul","test_start_unix":...,"test_end_unix":...}], ...}.
    - RUN-bucket-3/spectrum/tcp_download/health-spectrum.ndjson: 60 rows with signal_outlier_rate ranging up to 0.45 in 5+ rows. RUN-bucket-3/spectrum/tcp_download/flent-summary.json: minimal flent-style summary JSON with throughput p99=900, throughput median=150 (ratio > 5×).
    - RUN-bucket-5/spectrum/tcp_upload/health-spectrum.ndjson: 60 rows; 8% of rows have arb_refractory_active=true; cake_ul_backlog_suppressed_count increments by >100 across the window.
    - RUN-bucket-6/spectrum/tcp_download/health-spectrum.ndjson: 60 rows with stable signal_outlier_rate < 0.10. RUN-bucket-6/spectrum/tcp_download/flent-summary.json: throughput median=400 (Spectrum DL plan is 920; drop=57%, > 30%). RUN-bucket-6/spectrum/browse/browse.curl.csv: 30 rows with time_starttransfer column p99 > 2.0 seconds (i.e. ttfb_p99_ms > 2000).
    Each RUN-bucket-N/ tree must include a per-WAN `<wan>/<test>/alerts-spectrum.json` empty stub `{"wan":"spectrum","db":"...","present":true,"rows":[],"summary":[]}` and a steering-pre.redacted.json + steering-post.redacted.json minimal stub (counters identical so bucket 4 does NOT flag). Authoring tip: write a tiny `_generate_bucket_fixtures.py` helper to produce the NDJSON rows deterministically; check the helper into the fixtures dir.

    **Mutation-fixture scripts (HIGH-5):**
    - mutation-fixtures/legitimate-doc.sh: a bash file whose heredoc/docstring narratively mentions "this script does NOT perform a steering toggle" and `# systemctl restart wanctl@spectrum -- DO NOT USE`. The mutation-boundary test MUST NOT flag this file (narrative text in comments + heredoc body).
    - mutation-fixtures/forbidden-restart.sh: a bash file containing the literal executable line `systemctl restart wanctl@spectrum` (no leading `#`). The mutation-boundary test MUST flag this file.

    In tests/conftest.py, append three session-scoped fixtures `phase212_health_spectrum`, `phase212_health_att`, `phase212_health_steering` per PATTERNS.md §"tests/conftest.py (additions only)" — use `json.loads((_FIXT / "<name>-snapshot.json").read_text())` with `_FIXT = Path(__file__).parent / "fixtures" / "phase213"`. Do NOT modify any existing conftest.py fixtures or imports unless adding the new Path/json imports if absent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.loads(open('tests/fixtures/phase213/ndjson-row-expected-keys.json').read()); assert len(d['required_keys']) &gt;= 50, f'expected &gt;=50 keys, got {len(d[\"required_keys\"])}'; print('keys', len(d['required_keys']))" &amp;&amp; .venv/bin/python -c "import json; m=json.loads(open('tests/fixtures/phase213/manifest-expected-keys.json').read()); assert 'bind_map' in m['required_top_level_keys'], 'missing bind_map key'; print('manifest keys', len(m['required_top_level_keys']))" &amp;&amp; .venv/bin/python -c "import sqlite3; c=sqlite3.connect('tests/fixtures/phase213/alerts-test.db'); n=c.execute('SELECT COUNT(*) FROM alerts').fetchone()[0]; assert n == 8, f'expected 8 rows, got {n}'; print('rows', n)" &amp;&amp; ls -d tests/fixtures/phase213/RUN-bucket-2 tests/fixtures/phase213/RUN-bucket-3 tests/fixtures/phase213/RUN-bucket-5 tests/fixtures/phase213/RUN-bucket-6 &amp;&amp; test -f tests/fixtures/phase213/mutation-fixtures/legitimate-doc.sh &amp;&amp; test -f tests/fixtures/phase213/mutation-fixtures/forbidden-restart.sh &amp;&amp; .venv/bin/pytest --collect-only tests/conftest.py -q 2&gt;&amp;1 | grep -E "phase212_health" || echo "OK conftest fixtures collectable"</automated>
  </verify>
  <done>
    Eight base fixture artifacts exist under tests/fixtures/phase213/. ndjson-row-expected-keys.json has >=50 keys. manifest-expected-keys.json contains the new `bind_map` key. alerts-test.db has 8 rows clustered in two windows. Four RUN-bucket-N/ fixture trees exist (buckets 2/3/5/6). Two mutation-fixture scripts exist (legitimate-doc.sh + forbidden-restart.sh). tests/conftest.py defines `phase212_health_spectrum`, `phase212_health_att`, `phase212_health_steering` session fixtures. No Phase 212 evidence JSON has been re-fetched from production.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write five offline test files — parametrized mutation-boundary, per-bucket classifier, manifest schema (offline mode), NDJSON schema, alert-window behavioral</name>
  <read_first>
    - tests/test_analyze_baseline.py (full file ~200 lines — pytest style template, subprocess invocation pattern)
    - tests/conftest.py (after Task 1 — confirms phase212_health_* fixtures available)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"tests/test_phase213_classify.py", §"tests/test_phase213_ndjson_schema.py", §"tests/test_phase213_manifest_schema.py", §"tests/test_phase213_mutation_boundary.py"
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map" (threshold values for classifier tests) and §"Signal-Sheet Output Schema" (golden-file shape)
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md HIGH-5, MEDIUM-1, MEDIUM-6 (the patches this task lands)
    - scripts/check-safe07-source-diff.sh (forbidden-pattern grep guard shape)
    - tests/fixtures/phase213/ndjson-row-expected-keys.json (the key set the schema test asserts on)
    - tests/fixtures/phase213/manifest-expected-keys.json (the key set the manifest test asserts on)
    - tests/fixtures/phase213/RUN-bucket-{2,3,5,6}/ (per-bucket golden fixtures created in Task 1)
  </read_first>
  <behavior>
    - test_phase213_ndjson_schema.py::test_jq_projection_emits_all_expected_keys: extracts the `jq -c` projection block from scripts/phase213-health-poller.sh (built in Plan 02), pipes phase212_health_spectrum fixture through it via `subprocess.run(["jq", "-c", PROJECTION, ...])`, parses output as JSON, asserts `set(row.keys()) == set(required_keys)`. RED until Plan 02 ships the poller.

    - test_phase213_classify.py::test_six_buckets_present: invokes `subprocess.run([sys.executable, "scripts/phase213-classify.py", "--run-dir", <synth>, "--output-json", <tmp>])`, loads tmp JSON, asserts `set(out["buckets"].keys()) == {"upload_ceiling_setpoint", "download_recovery_lag", "measurement_collapse", "steering_drift", "refractory_semantics", "external_isp"}` even when all flagged=false.
    - test_phase213_classify.py::test_bucket_1_threshold_flags_on_ceiling_pegged: synthesizes a minimal run-dir where `upload_rate_mbps` is within 0.5 of an 18 Mbps ceiling (read from the synthesized per-WAN config OR from row metadata — NOT from `setpoint + 6` arithmetic per MEDIUM-2) in >80% of rows; asserts `out["buckets"]["upload_ceiling_setpoint"]["flagged"] is True`.
    - test_phase213_classify.py::test_bucket_2_flags_on_download_recovery_lag: invokes the classifier against tests/fixtures/phase213/RUN-bucket-2/ and asserts `out["buckets"]["download_recovery_lag"]["flagged"] is True` with at least one evidence_row showing time_to_green_after_red_sec > 30.
    - test_phase213_classify.py::test_bucket_3_flags_on_measurement_collapse: invokes against RUN-bucket-3/ and asserts `out["buckets"]["measurement_collapse"]["flagged"] is True`.
    - test_phase213_classify.py::test_bucket_5_flags_on_refractory_semantics: invokes against RUN-bucket-5/ and asserts `out["buckets"]["refractory_semantics"]["flagged"] is True`.
    - test_phase213_classify.py::test_bucket_6_flags_on_external_isp: invokes against RUN-bucket-6/ and asserts `out["buckets"]["external_isp"]["flagged"] is True`.
    - test_phase213_classify.py::test_bucket_4_steering_drift_no_threshold_name_compare: greps scripts/phase213-classify.py source text for `green_rtt_ms`, `yellow_rtt_ms`, `red_rtt_ms`, `red_samples_required`, `green_samples_required`; asserts each occurrence is on the RHS of a dict-key assignment OR on a comment line, NEVER on the LHS of a comparison operator (`<`, `>`, `<=`, `>=`, `==`, `!=`). Acceptable forms: `"green_rtt_ms": value`, `# green_rtt_ms field`. Rejected forms: `if val < green_rtt_ms`.
    - test_phase213_classify.py::test_ranked_next_phase_recommendation_present: asserts `out["recommended_next_phase"]["primary"] in {214, 215, 216}` and `len(out["recommended_next_phase"]["runners_up"]) >= 1`.
    - test_phase213_classify.py::test_bucket_1_ceiling_source_is_config_not_arithmetic: greps scripts/phase213-classify.py for `setpoint_mbps\s*\+\s*6`. Must return zero matches. The classifier MUST read ceiling from a config-loaded value (per-WAN YAML lookup or row-metadata field), per MEDIUM-2.

    - test_phase213_manifest_schema.py::test_orchestrator_check_manifest_emits_manifest_keys: invokes `bash scripts/phase213-baseline-capture.sh --check-manifest --evidence-root <tmp>` (offline mode added in Plan 04 per HIGH-3), reads the emitted manifest.json, asserts every key in manifest-expected-keys.json `required_top_level_keys` is present (including the new `bind_map`). NOTE: `--check-manifest` is the offline mode that does NOT SSH; pytest must remain runnable when cake-shaper is unreachable.
    - test_phase213_manifest_schema.py::test_check_manifest_is_offline: greps `scripts/phase213-baseline-capture.sh` source. Locate the `--check-manifest` code path (the block from `case "$MODE" in check-manifest)` to its matching `;;`) and assert that block contains NEITHER `ssh ` NOR `curl ` invocations. Implementation: parse the file in awk to extract just the check-manifest block, then assert via `re.search(r"\bssh\b|\bcurl\b", block) is None`.

    - test_phase213_mutation_boundary.py:
      - Define PHASE213_SCRIPTS as a list of six paths (scripts/phase213-{baseline-capture,health-poller,browse-loop,alert-window,steering-snapshot}.sh + scripts/phase213-classify.py).
      - Define FORBIDDEN as the tightened regex set from the interfaces block (NOT the loose patterns from the original plan).
      - Define `strip_comments(body)` helper: returns `"\n".join(ln for ln in body.splitlines() if not ln.lstrip().startswith("#"))`. This filter prevents both self-invalidation (the test file contains the regex literals) AND false positives on narrative comments / heredoc docstring lines that codex HIGH-5 identified.
      - `@pytest.mark.parametrize("script_path", PHASE213_SCRIPTS)` over `test_no_forbidden_mutation_tokens_per_script(script_path)`: if file does not exist, `pytest.skip(f"{script_path} not built yet")` — INSIDE the parametrized function, so each script case skips independently (HIGH-5 fix). If file exists, read body, strip comments, assert `re.search(pat, stripped) is None` for each forbidden pattern.
      - `test_legitimate_doc_fixture_does_not_trip_guard`: reads tests/fixtures/phase213/mutation-fixtures/legitimate-doc.sh, applies strip_comments + FORBIDDEN scan, asserts ZERO patterns match. Positive control — narrative comments and heredoc bodies mentioning "steering toggle" must not trip.
      - `test_forbidden_fixture_does_trip_guard`: reads tests/fixtures/phase213/mutation-fixtures/forbidden-restart.sh, applies strip_comments + FORBIDDEN scan, asserts at least ONE pattern matches (specifically the `\bsystemctl\s+restart\s+wanctl@` pattern). Negative control.

    - test_phase213_alert_window.py::test_alert_window_local_db_window_a_returns_expected_rows: invokes `bash scripts/phase213-alert-window.sh --local-db tests/fixtures/phase213/alerts-test.db --start 1717000000 --end 1717000060 --output-dir <tmp>` (where `--local-db` is the new offline mode added in Plan 03 per MEDIUM-6). Loads emitted `<tmp>/alerts-spectrum.json`, asserts JSON shape `{wan,db,present,rows,summary}`, asserts row count matches the synthetic window A row count (computed from the alerts-test.db generator), and asserts `summary` group-by counts sum to the row count.
    - test_phase213_alert_window.py::test_alert_window_local_db_window_b: same as above for window B timestamps.
    - test_phase213_alert_window.py::test_alert_window_local_db_no_ssh_invoked: source-grep `scripts/phase213-alert-window.sh` — locate the `--local-db` branch and assert it does NOT contain `ssh ` (the `--local-db` mode must skip SSH entirely so pytest doesn't depend on cake-shaper reachability).
  </behavior>
  <files>
    tests/test_phase213_ndjson_schema.py,
    tests/test_phase213_classify.py,
    tests/test_phase213_manifest_schema.py,
    tests/test_phase213_mutation_boundary.py,
    tests/test_phase213_alert_window.py,
    tests/fixtures/phase213/signal-sheet-expected-empty.json,
    tests/fixtures/phase213/signal-sheet-expected-ul-ceiling-flagged.json
  </files>
  <action>
    Write the FIVE pytest test files (was four; adds tests/test_phase213_alert_window.py per MEDIUM-6) following PATTERNS.md analogs and the behavior block above. Use `subprocess.run([sys.executable, "scripts/phase213-classify.py", ...])` for classifier invocation rather than `importlib` due to the dash in the filename.

    **Mutation-boundary specifics (HIGH-5):** Replace the original single-test-with-in-loop-skip pattern with `@pytest.mark.parametrize("script_path", PHASE213_SCRIPTS)`. Each parametrized case independently calls `pytest.skip(...)` if the script doesn't exist yet — the per-case skip does NOT short-circuit subsequent cases. Tighten FORBIDDEN regex literals per the interfaces block (word-boundary anchored, command-context anchored). Add `strip_comments()` helper that filters out lines starting with `#` after optional whitespace. Implement positive (legitimate-doc) and negative (forbidden-restart) fixture-script tests as plain non-parametrized tests so they always run.

    **Per-bucket tests (MEDIUM-1):** Implement the four new per-bucket tests (bucket_2/3/5/6) by passing `--run-dir tests/fixtures/phase213/RUN-bucket-N/` to the classifier and asserting the corresponding bucket's `flagged` field is True. These tests are RED until Plan 04 Task 2 ships the classifier.

    **MEDIUM-2 enforcement test:** `test_bucket_1_ceiling_source_is_config_not_arithmetic` greps the classifier source for `setpoint_mbps\s*\+\s*6` — must return zero matches. The classifier must read ceiling from a per-WAN YAML config or a metadata field on the row.

    **Manifest schema test split (HIGH-3):** `test_orchestrator_check_manifest_emits_manifest_keys` invokes the new `--check-manifest` offline mode (added in Plan 04). `test_check_manifest_is_offline` source-greps the orchestrator's `--check-manifest` code block and asserts NO `ssh` / `curl` invocations inside that block. The existing `--dry-run` becomes the live mode (`--check-prereqs`) and is NOT invoked by pytest.

    **Alert-window behavioral test (MEDIUM-6):** Three tests in tests/test_phase213_alert_window.py exercising the `--local-db` mode added to Plan 03. Asserts the alert-window script can run offline against tests/fixtures/phase213/alerts-test.db with no SSH involvement.

    **Golden files:** Build the two signal-sheet golden files per RESEARCH §Signal-Sheet Output Schema — signal-sheet-expected-empty.json with all six buckets flagged=false and evidence_rows=[]; signal-sheet-expected-ul-ceiling-flagged.json with upload_ceiling_setpoint flagged=true and one evidence_row.

    All FORBIDDEN regex literals appear inside the test file body — they are inside Python string literals on non-comment lines, so the test file itself MUST be excluded from the PHASE213_SCRIPTS list (and it is — PHASE213_SCRIPTS only references scripts/phase213-*, not tests/*).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_classify.py tests/test_phase213_manifest_schema.py tests/test_phase213_ndjson_schema.py tests/test_phase213_alert_window.py -q --no-header 2&gt;&amp;1 | tail -30 ; .venv/bin/python -c "import json; e=json.loads(open('tests/fixtures/phase213/signal-sheet-expected-empty.json').read()); assert set(e['buckets'].keys()) == {'upload_ceiling_setpoint','download_recovery_lag','measurement_collapse','steering_drift','refractory_semantics','external_isp'}, list(e['buckets'].keys()); print('golden ok')" ; .venv/bin/pytest tests/test_phase213_mutation_boundary.py::test_legitimate_doc_fixture_does_not_trip_guard tests/test_phase213_mutation_boundary.py::test_forbidden_fixture_does_trip_guard -q 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    Five test files exist. mutation-boundary test PASSES at Wave 0 for the six PHASE213_SCRIPTS (parametrized skip-with-reason per script) AND the two mutation-fixture cases pass (legitimate-doc doesn't trip; forbidden-restart does trip). Classifier per-bucket tests (1/2/3/5/6) are RED (skipped or fail) because the classifier doesn't exist yet — expected Wave 0 state. Alert-window test is RED (skipped) until Plan 03 ships `--local-db` mode. Manifest-schema test is RED until Plan 04 ships `--check-manifest`. Two golden-file JSONs validate as well-formed and contain all six bucket keys. No D-10 forbidden token appears in any test file's executable (non-comment) lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture authorship → repo | All fixture content is either copied verbatim from already-redacted Phase 212 evidence (no fresh production access) or synthesized in-tree from `wanctl.storage.schema.create_tables`. No external input crosses into the repo. |
| Test execution → file system | Tests read fixtures and (in later waves) invoke scripts via `subprocess.run`. No network, no SSH, no production access in Wave 0. |
| Pytest execution → production (NEW per HIGH-3) | The manifest-schema test invokes the orchestrator's `--check-manifest` mode which MUST be offline (no SSH/curl). The live `--check-prereqs` mode is operator-only. Source-grep test enforces the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I (Information disclosure) | Phase 212 evidence JSON copies → tests/fixtures/phase213/ | mitigate | Phase 212 evidence was already redacted per D-08 (no router passwords, no tokens in committed files). Copy is `cp` byte-for-byte; no transformation that could leak. Mutation-boundary grep guard test_phase213_mutation_boundary.py also runs against any future fixture additions via downstream wave gates. |
| T-213-02 | T (Tampering) | tests/fixtures/phase213/alerts-test.db generated from wanctl.storage.schema | accept | DB built locally with synthetic data; no production DB content. Schema source is the same code path autorate uses, so drift = pytest fail (acceptable). |
| T-213-03 | I (Information disclosure) | SQLite read-only concurrent access (deferred to Plan 03; this plan creates only a fixture DB) | mitigate | Fixture DB is local-only; no live writer involved. Plan 03 implements the runtime `mode=ro` URI form AND the offline `--local-db` mode this plan's test exercises. |
| T-213-04 | D (Denial of service) | Test execution duration during CI | accept | Wave 0 tests are <1s offline; no traffic generation. |
| T-213-05 | E (Elevation of privilege) | D-14 v1.39 steering threshold-name interpretation | mitigate | test_phase213_classify.py::test_bucket_4_steering_drift_no_threshold_name_compare encodes D-14 invariant as a source-grep assertion against the classifier (enforced when classifier ships in Plan 04). Also enforced: test_bucket_1_ceiling_source_is_config_not_arithmetic (MEDIUM-2 — link-agnostic architecture). |
| T-213-06 (NEW per HIGH-3) | T (Tampering) | "Offline" pytest tests inadvertently SSH to cake-shaper | mitigate | test_phase213_manifest_schema.py::test_check_manifest_is_offline source-greps the orchestrator's `--check-manifest` code block and asserts NO `ssh` / `curl` invocations inside that block. Production reachability is no longer a pytest dependency. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase213_mutation_boundary.py -q` PASSES (parametrized — each missing script skips independently; mutation-fixture positive/negative cases pass).
- `.venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_manifest_schema.py tests/test_phase213_ndjson_schema.py tests/test_phase213_alert_window.py -q` collects tests; classifier/schema/alert-window tests RED (expected — scripts don't exist yet); no syntax errors.
- `.venv/bin/python -c "import json; json.loads(open('tests/fixtures/phase213/signal-sheet-expected-empty.json').read())"` exits 0.
- `.venv/bin/python -c "import sqlite3; c=sqlite3.connect('tests/fixtures/phase213/alerts-test.db'); print(c.execute('SELECT COUNT(*) FROM alerts').fetchone()[0])"` prints `8`.
- Four RUN-bucket-N/ trees exist with the documented per-bucket evidence content.
- Two mutation-fixture scripts exist; positive test passes; negative test passes.
- `git diff --stat src/wanctl/` returns empty (SAFE-05 invariant: no controller source touched).
</verification>

<success_criteria>
- 22 files created/modified exactly matching `files_modified` frontmatter (note bind_map in manifest schema, RUN-bucket-N fixtures, mutation-fixtures, new alert-window test).
- All four expected-key/schema artifacts (ndjson, manifest, two signal-sheet goldens) are well-formed JSON.
- `tests/conftest.py` exposes three new session fixtures without breaking existing fixtures.
- Parametrized mutation-boundary test PASSES on Wave 0 commit; positive+negative mutation-fixture tests PASS.
- `git diff --stat src/wanctl/ scripts/` returns empty.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md` covering: fixture sources (Phase 212 evidence paths), expected-key counts (NDJSON, manifest with bind_map), four per-bucket RUN fixture summaries, mutation-fixture positive/negative test outcomes, parametrized mutation-boundary script-skip count, alert-window behavioral test wiring (--local-db mode it expects in Plan 03), and Wave 0 → Wave 1 handoff contract (downstream plans MUST consume `ndjson-row-expected-keys.json` and `manifest-expected-keys.json` as the schema source of truth).
</output>
