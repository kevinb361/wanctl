---
phase: 213-experience-baseline-harness
plan: 05
type: execute
wave: 4
depends_on: [213-01, 213-02, 213-03, 213-04]
files_modified:
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/manifest.json
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/README.md
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/
  - .planning/phases/213-experience-baseline-harness/213-REPORT.md
autonomous: false
requirements: [BASE-01, BASE-02, BASE-03]
tags: [baseline, harness, evidence-capture, report, operator-verdict]

must_haves:
  truths:
    - "End-to-end dry-run completes without invoking flent or curl-browse traffic, exercising every prerequisite gate."
    - "One real evidence-capturing run captures the full per-WAN suite vs Spectrum to completion, then vs ATT (D-11)."
    - "Signal-sheet.json and signal-sheet.md exist for the real run with six buckets present."
    - "Operator authors 213-REPORT.md citing signal-sheet evidence rows for each bucket verdict and produces a SINGLE ranked next-phase recommendation (one of 214/215/216) with explicit runners-up (BASE-03 success criterion 4, D-13, D-15)."
    - "Evidence redaction posture audited: zero raw state.json artifacts committed; every committed *.redacted.json passes the D-08 key-pattern scan."
    - "Cross-run comparability holds: source IP family per D-04 recorded; flent --host dallas; same flent args for rrul/tcp_12down as Phase 191/198."
  artifacts:
    - path: ".planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/manifest.json"
      provides: "Dry-run manifest proving orchestrator prerequisite gates fire and manifest schema holds"
    - path: ".planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/"
      provides: "Full per-WAN evidence tree (two WAN × five test subdirs each), plus signal-sheet.json/.md at the parent evidence/ root with the RUN's timestamp suffix"
    - path: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      provides: "Operator-authored final report: per-bucket verdict with cited signal-sheet rows + ranked next-phase recommendation with runners-up (BASE-03 + D-13 + D-15)"
      min_lines: 80
  key_links:
    - from: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      to: "evidence/RUN-<ts>/signal-sheet.{json,md}"
      via: "row citations like 'see evidence/RUN-20260528T021500Z/spectrum/tcp_upload/health-spectrum.ndjson'"
      pattern: "evidence/RUN-"
    - from: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      to: "Next phase decision (214 OR 215 OR 216)"
      via: "explicit ## Recommended Next Phase section with primary + runners-up"
      pattern: "## Recommended Next Phase"
---

<objective>
Execute the Phase 213 codepath end-to-end for real and author the operator report that closes BASE-03 success criteria 3 and 4. This plan runs the orchestrator twice — first as `--dry-run` to prove every prerequisite gate fires, then as a real per-WAN sequenced suite vs Spectrum to completion, then vs ATT (D-11). After the real run completes and the signal sheet is emitted, the operator (Kevin) reads the signal sheet and authors `213-REPORT.md` citing evidence rows for each bucket verdict and producing a single ranked next-phase recommendation with runners-up.

Purpose: Plans 01–04 build the harness; Plan 05 USES it. This is the BASE-03 success-criterion-4 plan: "Baseline recommends whether to proceed to measurement investigation, upload reclaim, or another narrower phase first." It is also the gate that proves the harness actually works against production rather than only against fixtures.

Output: A `RUN-DRYRUN/` artifact directory, a full `RUN-<utc-ts>/` evidence tree from one real run, paired signal-sheet.{json,md} files, and the operator-authored final report. This plan is `autonomous: false` because the bucket-verdict and next-phase-ranking step is human judgment per D-13/D-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-VALIDATION.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-02-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-03-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-04-SUMMARY.md
@.planning/phases/212-production-inventory-and-drift-audit/212-REPORT.md
@scripts/phase213-baseline-capture.sh
@scripts/phase213-classify.py
@docs/RUNBOOKS/baseline.md

<interfaces>
<!-- Real-run target invocation: -->
<!--   bash scripts/phase213-baseline-capture.sh \ -->
<!--     --local-bind <observed-dev-VM-IP> --host dallas \ -->
<!--     --flent-duration 60 --browse-duration 60 --pre-buf 10 --post-buf 10 \ -->
<!--     --wans spectrum,att \ -->
<!--     --tests browse,tcp_upload,tcp_download,rrul,tcp_12down \ -->
<!--     --evidence-root .planning/phases/213-experience-baseline-harness/evidence -->

<!-- D-03 / D-04 carry-forward (RESEARCH §"User Constraints"): -->
<!-- - Source IP family matches Phase 191/198: 10.10.110.233 (Spectrum egress) or 10.10.110.226 (ATT-specific egress per Phase 198) — orchestrator's egress probe enforces. -->
<!-- - Flent netperf host = `dallas` (D-04 locked). -->

<!-- 213-REPORT.md outline (BASE-03 success criterion 4 + D-13 + D-15): -->
<!-- 1. ## Summary — what was captured, when, against which WAN(s) -->
<!-- 2. ## Per-Bucket Verdict (six subsections; each verdict cites at least one signal-sheet evidence row by path) -->
<!--    2.1 Upload ceiling / setpoint (BUCKET 1) -->
<!--    2.2 Download recovery lag (BUCKET 2) -->
<!--    2.3 Measurement collapse (BUCKET 3) -->
<!--    2.4 Steering drift (BUCKET 4) — verdict cites raw counter deltas only, never threshold-name comparison (D-14) -->
<!--    2.5 Refractory semantics (BUCKET 5) -->
<!--    2.6 External ISP conditions (BUCKET 6) -->
<!-- 3. ## Recommended Next Phase — primary (one of 214/215/216) + at least one runner-up + rationale citing specific evidence rows -->
<!-- 4. ## Downstream Constraints — facts the next phase MUST honor (e.g., observed Spectrum UL ceiling-pegged % per D-18; v1.39 steering threshold drift still unresolved per Phase 212 carry-forward) -->
<!-- 5. ## Run Metadata — RUN-<ts>, source IP, netperf host, flent version, dev VM hostname, wanctl runtime versions per WAN, signal-sheet path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run validation + evidence-redaction grep gate</name>
  <read_first>
    - scripts/phase213-baseline-capture.sh (Plan 04 output — the script being invoked)
    - .planning/phases/213-experience-baseline-harness/213-VALIDATION.md §"Manual-Only Verifications" (test instructions)
    - .planning/phases/213-experience-baseline-harness/evidence/README.md (Plan 04 output — committed evidence redaction policy)
    - tests/fixtures/phase213/manifest-expected-keys.json (Plan 01 — the manifest schema this dry-run must satisfy)
  </read_first>
  <files>.planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/manifest.json, .planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/README.md</files>
  <action>
    Confirm current dev VM source IPs by running `ip -4 addr show | grep -E " 10\.10\.110\.(226|233)/"` and record observed IPs in shell-local variable. Invoke `bash scripts/phase213-baseline-capture.sh --dry-run --evidence-root .planning/phases/213-experience-baseline-harness/evidence --local-bind <observed-spectrum-bind>`. The dry-run must (a) pass every prerequisite gate (flent + jq + curl on PATH, cake-shaper SSH reachable, sudo -n usable, --local-bind present on dev VM), (b) create `RUN-DRYRUN/` (or rename whatever timestamp the orchestrator picks to `RUN-DRYRUN`), (c) emit `manifest.json` containing every required_top_level_key from manifest-expected-keys.json, and (d) emit a per-RUN `README.md` mirroring the top-level evidence/README.md 6-column-index format (orchestrator implements this in Plan 04). After the dry-run, run the FULL Wave 0 test suite to confirm Plans 01–04 are still GREEN: `.venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_ndjson_schema.py tests/test_phase213_manifest_schema.py tests/test_phase213_mutation_boundary.py -q`. Then run a focused mutation-grep against the evidence dir to confirm no raw state.json artifact landed under evidence/ — `find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json"` must be empty. If the dry-run fails any gate, do NOT proceed to Task 2 — fix the orchestrator first and re-invoke this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/manifest.json &amp;&amp; .venv/bin/python -c "import json; m=json.loads(open('.planning/phases/213-experience-baseline-harness/evidence/RUN-DRYRUN/manifest.json').read()); req=json.loads(open('tests/fixtures/phase213/manifest-expected-keys.json').read())['required_top_level_keys']; missing=[k for k in req if k not in m]; print('missing_keys', missing); assert not missing, f'manifest missing {missing}'" &amp;&amp; .venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_ndjson_schema.py tests/test_phase213_manifest_schema.py tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -5 &amp;&amp; (find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json" | head -1 | grep -q . &amp;&amp; { echo "FAIL: raw state json committed"; exit 1; } || echo "OK: no raw state json under evidence/")</automated>
  </verify>
  <done>
    RUN-DRYRUN/manifest.json exists with every required_top_level_key. All four Wave 0 tests still GREEN. No `*.raw.json` artifact present under evidence/. Per-RUN README emitted by orchestrator mirrors the top-level 6-column shape. Dry-run did NOT execute any flent or curl-browse traffic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute one real evidence-capturing run (D-11 per-WAN sequencing)</name>
  <what-built>
    Plans 01–04 + Task 1 (dry-run) prove the codepath is complete. This step asks the operator to invoke the orchestrator for real, which generates production WAN traffic (D-10 allowance: dev-VM-originated traffic only) and writes a full per-test evidence tree under `evidence/RUN-<utc-ts>/`.
  </what-built>
  <how-to-verify>
    1. Confirm dev VM source IPs match Phase 191/198 expectations (`10.10.110.233` for Spectrum-egress; `10.10.110.226` for ATT-egress if used). Run `ip -4 addr show | grep " 10.10.110."` to record current state.
    2. Confirm cake-shaper is reachable: `ssh -o BatchMode=yes cake-shaper 'echo ok'`.
    3. Confirm autorate endpoints respond: `curl -fsS --max-time 5 http://10.10.110.223:9101/health | jq .version` and `curl -fsS --max-time 5 http://10.10.110.227:9101/health | jq .version`.
    4. Confirm no other operator is loading the WANs (check `journalctl -u wanctl@spectrum.service -n 20 -p info --no-pager | grep -i load` if uncertain).
    5. Run the real-run invocation per the interfaces block:
       ```
       bash scripts/phase213-baseline-capture.sh \
         --local-bind 10.10.110.233 --host dallas \
         --flent-duration 60 --browse-duration 60 --pre-buf 10 --post-buf 10 \
         --wans spectrum,att \
         --tests browse,tcp_upload,tcp_download,rrul,tcp_12down \
         --evidence-root .planning/phases/213-experience-baseline-harness/evidence
       ```
    6. Watch the per-test progress messages. Expected total wall-clock: ~12-15 minutes per WAN, ~25-30 minutes total. D-11 sequencing ensures Spectrum finishes fully before ATT starts.
    7. After completion, confirm artifact tree exists: `find .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts> -type f | wc -l` should be in the range 100-200 (5 tests × 2 WANs × ~10 artifacts/test + manifest + signal sheet).
    8. Confirm signal sheet emitted: `ls .planning/phases/213-experience-baseline-harness/evidence/signal-sheet-<ts>.{json,md}`. Inspect `signal-sheet.md` — it must list six buckets, a summary header, and a recommended_next_phase row.
    9. Audit redaction: `find .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts> -name "*.raw.json"` should return empty (gitignored, not committed). `find … -name "*.redacted.json" -exec grep -l -E '"(password|secret|token|credential|auth|key|private)":[^<]' {} \;` should return empty (every match must be `"<REDACTED>"`).
    10. If anything fails: orchestrator failed prerequisite check → operator decides whether to fix and rerun; if a per-test poller errored mid-run, examine `poll-failures.tsv` in the affected test dir and decide whether to discard the run and rerun or accept the partial evidence.
  </how-to-verify>
  <resume-signal>Type "approved" once the real run completed, signal sheet emitted, and redaction audit passes. If any audit failed, describe the failure and we'll fix the orchestrator before retrying. Per D-11, never rerun with `--wans att,spectrum` (loaded concurrently) — always Spectrum first or ATT first as a single ordered list.</resume-signal>
  <files>.planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/</files>
  <action>Human-only checkpoint — see &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; above. Operator runs the orchestrator interactively and verifies the artifact tree + redaction audit before unblocking Task 3.</action>
  <verify>
    <automated>test -d .planning/phases/213-experience-baseline-harness/evidence &amp;&amp; ls .planning/phases/213-experience-baseline-harness/evidence/signal-sheet-*.md 2&gt;/dev/null | head -1 | grep -q . &amp;&amp; (find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json" | head -1 | grep -q . &amp;&amp; { echo FAIL; exit 1; } || echo OK)</automated>
  </verify>
  <done>Operator typed "approved"; signal-sheet.{json,md} present for the RUN-&lt;ts&gt; just completed; redaction audit passed (no raw state.json under evidence/).</done>
</task>

<task type="auto">
  <name>Task 3: Operator-authored 213-REPORT.md (BASE-03 success criteria 3 + 4, D-13, D-15)</name>
  <read_first>
    - .planning/phases/213-experience-baseline-harness/evidence/signal-sheet-<ts>.md (the signal sheet generated by Task 2 — the source for every bucket verdict)
    - .planning/phases/213-experience-baseline-harness/evidence/signal-sheet-<ts>.json (the raw evidence rows the verdict cites)
    - .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/manifest.json (run metadata for §"Run Metadata" section)
    - .planning/phases/213-experience-baseline-harness/213-CONTEXT.md §"Symptom → Bucket Classification (BASE-03)" (D-13/D-14/D-15 wording the report follows)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map" (threshold context for each bucket verdict)
    - .planning/phases/212-production-inventory-and-drift-audit/212-REPORT.md (Phase 212 final report — STYLE template for tone, per-section structure, citation discipline)
    - .planning/ROADMAP.md §"Phase 214/215/216" (what each next-phase choice commits to)
  </read_first>
  <files>.planning/phases/213-experience-baseline-harness/213-REPORT.md</files>
  <action>
    Author `.planning/phases/213-experience-baseline-harness/213-REPORT.md` (target ~80-180 lines) following the five-section outline in the interfaces block. Mirror Phase 212's report tone (operator-first, evidence-cited, no speculative language). For each of the six buckets in §"Per-Bucket Verdict": state the verdict (flagged / not-flagged / inconclusive), cite at least one signal-sheet evidence row by `evidence_path` (per D-13), and add operator commentary explaining why the threshold did or did not fire. For BUCKET 4 (steering drift) the verdict MUST be expressed in raw counter-delta and state-transition terms ONLY — no comparison to v1.39 threshold field names per D-14 (CONTEXT.md). For §"Recommended Next Phase": ONE primary (must be 214, 215, or 216 per D-15 — no other phase IDs accepted), at least one runner-up, evidence-cited rationale for each. If the signal sheet flagged zero buckets, the primary recommendation defaults to Phase 214 (measurement collapse investigation is the conservative next step) with explicit operator note. For §"Downstream Constraints": list 3-5 facts the next phase must honor — at minimum: observed Spectrum UL ceiling-pegged % per D-18 (intentional config, not drift); v1.39 steering threshold-name interpretation remains forbidden until Phase 216 alignment; Phase 212 inventory facts are still the authoritative baseline (D-19). For §"Run Metadata": include RUN-<ts>, source_ip, netperf_host, flent_version, host_dev_vm, wanctl_version_{spectrum,att,steering}_runtime, signal-sheet absolute paths. End with a closing paragraph noting that Plan 05 has SAFETY-CHECKED the run and confirmed redaction posture holds. Update `.planning/ROADMAP.md` §"Phase 213" to mark `[x]` and append `(completed YYYY-MM-DD)` next to the phase header.
  </action>
  <verify>
    <automated>test -f .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Per-Bucket Verdict" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Recommended Next Phase" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Downstream Constraints" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Run Metadata" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -E "Phase 21[456]" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -Eo "evidence/RUN-[A-Z0-9]+" .planning/phases/213-experience-baseline-harness/213-REPORT.md | head -3 &amp;&amp; grep -F "[x] **Phase 213" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    213-REPORT.md exists with all five required sections (Summary, Per-Bucket Verdict, Recommended Next Phase, Downstream Constraints, Run Metadata). At least one of Phase 214 / 215 / 216 is named in the Recommended Next Phase section. At least three `evidence/RUN-<ts>/...` citations appear in the body. ROADMAP.md Phase 213 entry now `[x]` and dated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real-run dev VM → autorate /health + cake-shaper SSH | Inherits Plans 02/03/04 boundaries; this plan is the consumer, not introducer. |
| Operator authorship → committed report | Report is text; cites already-redacted evidence paths. No fresh production access at report-authoring time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I (Information disclosure) | Committed RUN-<ts> evidence tree | mitigate | Task 1 verify gate AND Task 2 step-9 audit confirm zero *.raw.json artifacts committed and zero unredacted secret-bearing keys in *.redacted.json. .gitignore (Plan 03) catches raw state.json. |
| T-213-02 | T (Tampering) | Orchestrator run mutates evidence dir only | mitigate | Plan 01 mutation-boundary test is part of Task 1's GREEN-gate rerun; orchestrator source already enforces no controller-config write per the same test. |
| T-213-04 | D (Denial of service) | ~25-30 min of dev-VM-originated traffic against both WANs | accept | D-11 sequencing prevents concurrent dual-WAN load. Total impact window is auditable from manifest.json `started_utc`/`ended_utc`. Task 2 step-4 asks operator to confirm no concurrent operator load exists before starting. |
| T-213-05 | E (Elevation of privilege) | Operator-authored bucket verdicts could implicitly interpret v1.39 steering thresholds | mitigate | Task 3 action text explicitly forbids BUCKET 4 from comparing values to v1.39 threshold field names; verdict expressed as raw counter deltas / state transitions only per D-14. Operator-pen-tested by Phase 212 D-08 carry-forward narrative in CONTEXT.md. |
| T-213-04 | D | Wrong next-phase recommendation pushes downstream effort in the wrong direction | accept | D-15 mandates runners-up explicitly so the next-phase decision is not single-point-of-failure; future operator can re-read 213-REPORT.md and pivot if bucket evidence shifts. |
</threat_model>

<verification>
- Task 1: dry-run manifest contains every required_top_level_key; all four Wave 0 tests GREEN; no raw state.json under evidence/.
- Task 2: operator-verified — full per-WAN suite ran to completion in D-11 order; signal-sheet.{json,md} present; redaction audit passes.
- Task 3: 213-REPORT.md has all five required sections; references Phase 214/215/216 in the recommendation; cites at least three evidence/RUN-<ts>/ paths; ROADMAP.md updated.
- Final state: `.venv/bin/pytest tests/test_phase213_*.py -q` ALL GREEN.
- `git status` shows the report, ROADMAP update, RUN-DRYRUN, RUN-<ts> tree, and signal-sheet files staged for commit.
</verification>

<success_criteria>
- One end-to-end dry-run completed with manifest schema GREEN.
- One real per-WAN sequenced evidence run committed under `evidence/RUN-<ts>/`.
- `signal-sheet.json` + `signal-sheet.md` emitted with six buckets present and a ranked next-phase recommendation (D-15).
- Operator-authored 213-REPORT.md exists with bucket verdicts citing evidence rows AND a single ranked next-phase (one of 214/215/216) with runners-up.
- All four Phase 213 Wave 0 tests still GREEN.
- ROADMAP.md Phase 213 marked complete.
- D-08/D-09/D-10/D-11/D-13/D-14/D-15/D-19 invariants all observably held in committed artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-05-SUMMARY.md` covering: dry-run prerequisite-gate results, real-run start/end timestamps and total artifact count, signal-sheet flagged-bucket list, operator-chosen primary next phase + runners-up, redaction-audit final state (zero unredacted hits, zero raw state.json artifacts committed), Phase 213 closeout note linking to ROADMAP.md update, and a one-line "Phase 213 complete; next phase: <N> per operator verdict; runners-up: <list>" for STATE.md update.
</output>
