---
phase: 213-experience-baseline-harness
plan: 05
type: execute
wave: 4
depends_on: [213-01, 213-02, 213-03, 213-04]
files_modified:
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/manifest.json
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/README.md
  - .planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/
  - .planning/phases/213-experience-baseline-harness/213-REPORT.md
  - .planning/STATE.md
autonomous: false
requirements: [BASE-01, BASE-02, BASE-03]
tags: [baseline, harness, evidence-capture, report, operator-verdict]

must_haves:
  truths:
    - "End-to-end offline manifest-check completes via `--check-manifest` without invoking SSH, flent, or curl-browse traffic, exercising the manifest schema gate (codex HIGH-3)."
    - "Live prereq probe via `--check-prereqs` exercises every gate (SSH + sudo + per-WAN egress probe) against cake-shaper before the real run (codex HIGH-3)."
    - "One real evidence-capturing run captures the full per-WAN suite vs Spectrum (bind 10.10.110.226) to completion, then vs ATT (bind 10.10.110.233) — SERIALIZED per D-11 + codex MEDIUM-7 (NEVER described as 'concurrent')."
    - "Signal-sheet.json and signal-sheet.md exist INSIDE the per-run dir at `evidence/RUN-<ts>/signal-sheet.{json,md}` (codex MEDIUM-5)."
    - "Operator authors 213-REPORT.md citing signal-sheet evidence rows for each bucket verdict and produces a SINGLE ranked next-phase recommendation (one of 214/215/216) with explicit runners-up (BASE-03 success criterion 4, D-13, D-15)."
    - "Evidence redaction posture audited: zero `*.raw.json` artifacts under evidence/ (HIGH-2 design invariant — raw state was only ever in /tmp/, cleaned by EXIT trap); every committed `*.redacted.json` passes the D-08 key-pattern scan."
    - "Evidence is committed via explicit `git add -f` because `.planning/` is gitignored; pre-commit redaction gate scans every staged artifact against the D-08 forbidden-pattern regex; commit fails closed on any match (codex MEDIUM-4)."
    - "Cross-run comparability holds: per-WAN bind map recorded in manifest.bind_map (codex HIGH-1); flent --host dallas; same flent args for rrul/tcp_12down as Phase 191/198."
  artifacts:
    - path: ".planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/manifest.json"
      provides: "Offline manifest-check artifact proving the orchestrator can emit schema-valid manifest with NO production reachability required (HIGH-3 offline mode)"
    - path: ".planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/"
      provides: "Full per-WAN evidence tree (two WAN × five test subdirs each), plus signal-sheet.json/.md INSIDE the RUN dir (MEDIUM-5)"
    - path: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      provides: "Operator-authored final report: per-bucket verdict with cited signal-sheet rows + ranked next-phase recommendation with runners-up (BASE-03 + D-13 + D-15)"
      min_lines: 80
  key_links:
    - from: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      to: "evidence/RUN-<ts>/signal-sheet.{json,md}"
      via: "row citations like 'see evidence/RUN-20260528T021500Z/spectrum/tcp_upload/health-spectrum.ndjson'"
      pattern: "evidence/RUN-"
    - from: ".planning/phases/213-experience-baseline-harness/213-REPORT.md"
      to: "Next phase decision (214 OR 215 OR 216)"
      via: "explicit ## Recommended Next Phase section with primary + runners-up"
      pattern: "## Recommended Next Phase"
---

<objective>
Execute the Phase 213 codepath end-to-end for real and author the operator report that closes BASE-03 success criteria 3 and 4. This plan runs the orchestrator THREE times:
1. `--check-manifest` (offline; pytest-equivalent) — proves the manifest schema gate fires without production reachability (codex HIGH-3).
2. `--check-prereqs` (live; replaces the old `--dry-run`) — proves SSH/sudo/per-WAN-egress probes fire against cake-shaper (codex HIGH-3).
3. Real run with `--bind-map spectrum=10.10.110.226,att=10.10.110.233` — generates production WAN traffic per D-10 allowance, serialized per D-11 + MEDIUM-7 wording, and writes the per-RUN evidence tree with signal-sheet INSIDE the run dir (codex MEDIUM-5).

After the real run, the operator (Kevin) reads the signal sheet and authors `213-REPORT.md` citing evidence rows for each bucket verdict and producing a single ranked next-phase recommendation with runners-up. Commit uses `git add -f` because `.planning/` is gitignored; a pre-commit redaction gate fails-closed on any D-08 forbidden-pattern hit (codex MEDIUM-4).

Codex review patches landed here:
- HIGH-1: real-run invocation uses `--bind-map spectrum=10.10.110.226,att=10.10.110.233` (NOT a single `--local-bind`); orchestrator's per-WAN egress probe runs before each WAN's test suite.
- HIGH-3: dual offline (`--check-manifest`) + live (`--check-prereqs`) mode split exercised separately.
- MEDIUM-4: explicit `git add -f` workflow with pre-commit D-08 redaction grep gate (`.planning/` is gitignored).
- MEDIUM-5: signal-sheet paths under `evidence/RUN-<ts>/signal-sheet.{json,md}` (NOT `evidence/signal-sheet-<ts>.md` at the top level).
- MEDIUM-7: D-11 ordering described as "serialized in <wan-order>", never "concurrent". Operator instruction explicitly warns against the "concurrent" misreading.

Purpose: Plans 01–04 build the harness; Plan 05 USES it. This is the BASE-03 success-criterion-4 plan AND the gate that proves the harness actually works against production rather than only against fixtures.

Output: A `RUN-CHECK-MANIFEST/` artifact directory (offline), a full `RUN-<utc-ts>/` evidence tree from one real run with signal-sheet INSIDE it, and the operator-authored final report. This plan is `autonomous: false` because the bucket-verdict and next-phase-ranking step is human judgment per D-13/D-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-VALIDATION.md
@.planning/phases/213-experience-baseline-harness/213-REVIEWS.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-02-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-03-SUMMARY.md
@.planning/phases/213-experience-baseline-harness/213-04-SUMMARY.md
@.planning/phases/212-production-inventory-and-drift-audit/212-REPORT.md
@scripts/phase213-baseline-capture.sh
@scripts/phase213-classify.py
@docs/RUNBOOKS/baseline.md

<interfaces>
<!-- Real-run target invocation (codex HIGH-1 + MEDIUM-7 patched): -->
<!--   bash scripts/phase213-baseline-capture.sh \ -->
<!--     --bind-map spectrum=10.10.110.226,att=10.10.110.233 \ -->
<!--     --host dallas \ -->
<!--     --flent-duration 60 --browse-duration 60 --pre-buf 10 --post-buf 10 \ -->
<!--     --wans spectrum,att \                  # SERIALIZED — Spectrum runs to completion, THEN ATT. D-11. -->
<!--     --tests browse,tcp_upload,tcp_download,rrul,tcp_12down \ -->
<!--     --evidence-root .planning/phases/213-experience-baseline-harness/evidence -->

<!-- D-03 / D-04 carry-forward (RESEARCH §"User Constraints"): -->
<!-- - Per-WAN bind map: Spectrum=10.10.110.226 (Phase 198 verified egress 70.123.224.169); ATT=10.10.110.233 (egress recorded-only). -->
<!-- - Flent netperf host = `dallas` (D-04 locked). -->

<!-- 213-REPORT.md outline (BASE-03 success criterion 4 + D-13 + D-15): -->
<!-- 1. ## Summary — what was captured, when, against which WAN(s), bind map used (HIGH-1 provenance) -->
<!-- 2. ## Per-Bucket Verdict (six subsections; each verdict cites at least one signal-sheet evidence row by path inside evidence/RUN-<ts>/) -->
<!--    2.1 Upload ceiling / setpoint (BUCKET 1; ceiling sourced from configs/<wan>.yaml per MEDIUM-2) -->
<!--    2.2 Download recovery lag (BUCKET 2) -->
<!--    2.3 Measurement collapse (BUCKET 3) -->
<!--    2.4 Steering drift (BUCKET 4) — verdict cites raw counter deltas only, never threshold-name comparison (D-14) -->
<!--    2.5 Refractory semantics (BUCKET 5) -->
<!--    2.6 External ISP conditions (BUCKET 6) -->
<!-- 3. ## Recommended Next Phase — primary (one of 214/215/216) + at least one runner-up + rationale citing specific evidence rows -->
<!-- 4. ## Downstream Constraints — facts the next phase MUST honor -->
<!-- 5. ## Run Metadata — RUN-<ts>, bind_map, source IPs per WAN, netperf host, flent version, dev VM hostname, wanctl runtime versions per WAN, signal-sheet path (evidence/RUN-<ts>/signal-sheet.{json,md}) -->

<!-- D-08 forbidden-pattern redaction gate (MEDIUM-4) — exact bash to run before `git add -f`: -->
<!--   STAGE_FILES=$(find <RUN-DIR> -type f \( -name '*.json' -o -name '*.md' -o -name '*.csv' -o -name '*.ndjson' -o -name '*.tsv' \)) -->
<!--   for f in $STAGE_FILES; do -->
<!--     if grep -E -i '"(password|secret|token|credential|auth|key|private)"\s*:\s*[^<]' "$f" >/dev/null; then -->
<!--       echo "REDACTION GATE FAIL: $f contains unredacted secret-bearing key"; exit 1 -->
<!--     fi -->
<!--   done -->
<!--   git add -f <RUN-DIR> -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline `--check-manifest` validation + live `--check-prereqs` probe + evidence-redaction grep gate</name>
  <read_first>
    - scripts/phase213-baseline-capture.sh (Plan 04 output — the script being invoked)
    - .planning/phases/213-experience-baseline-harness/213-VALIDATION.md §"Manual-Only Verifications"
    - .planning/phases/213-experience-baseline-harness/evidence/README.md (Plan 04 output — committed evidence redaction policy)
    - tests/fixtures/phase213/manifest-expected-keys.json (Plan 01 — schema this check must satisfy, including `bind_map`)
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md HIGH-3 (offline/live mode split rationale)
  </read_first>
  <acceptance_criteria>
    - `RUN-CHECK-MANIFEST/manifest.json` exists with EVERY required_top_level_key from manifest-expected-keys.json present, including `bind_map`.
    - `--check-manifest` invocation completes successfully even if cake-shaper is unreachable (offline guarantee).
    - `--check-prereqs` invocation runs the live SSH/sudo/per-WAN-egress probe gates and exits 0 (or surfaces a clear failure that the operator can fix before Task 2).
    - All FIVE Wave 0 tests (mutation-boundary, classify, manifest-schema, ndjson-schema, alert-window) GREEN after Task 1.
    - `find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json"` returns empty (HIGH-2 invariant — raw state never under evidence/).
  </acceptance_criteria>
  <files>.planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/manifest.json, .planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/README.md</files>
  <action>
    **Step 1: Offline manifest check (codex HIGH-3).**

    Invoke the orchestrator's offline mode:
    ```
    bash scripts/phase213-baseline-capture.sh --check-manifest \
      --bind-map spectrum=fixture,att=fixture \
      --evidence-root .planning/phases/213-experience-baseline-harness/evidence
    ```

    This MUST complete successfully regardless of cake-shaper reachability (the `--check-manifest` branch in the orchestrator contains no SSH/curl invocations per codex HIGH-3 — verified by test_phase213_manifest_schema.py::test_check_manifest_is_offline). The orchestrator writes `RUN-CHECK-MANIFEST/manifest.json` containing every required_top_level_key from manifest-expected-keys.json, including the new `bind_map` field added in codex HIGH-1.

    **Step 2: Live prereq probe (codex HIGH-3).**

    Confirm current dev VM source IPs by running `ip -4 addr show | grep -E " 10\.10\.110\.(226|233)/"` and record observed IPs. Then invoke the live mode:
    ```
    bash scripts/phase213-baseline-capture.sh --check-prereqs \
      --bind-map spectrum=10.10.110.226,att=10.10.110.233 \
      --host dallas \
      --evidence-root .planning/phases/213-experience-baseline-harness/evidence
    ```

    This MUST pass: flent/jq/curl on PATH, cake-shaper SSH reachable, sudo -n usable, both binds present on dev VM, per-WAN egress probes (Spectrum egress=70.123.224.169 enforced; ATT egress recorded-only). If any gate fails, fix the orchestrator OR fix the environment (e.g. dev VM IP missing) and re-invoke — do NOT proceed to Task 2 until `CHECK_PREREQS OK` prints.

    **Step 3: Wave 0 test rerun.** Confirm Plans 01–04 are still GREEN after the orchestrator invocations:
    ```
    .venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_ndjson_schema.py tests/test_phase213_manifest_schema.py tests/test_phase213_mutation_boundary.py tests/test_phase213_alert_window.py -q
    ```

    **Step 4: Redaction posture audit.** Run focused grep against the evidence dir to confirm no raw state.json artifact landed under evidence/:
    ```
    find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json"
    ```
    This MUST return empty (HIGH-2 invariant — Plan 03's steering-snapshot script writes raw state to /tmp/ only, cleaned by EXIT trap).

    If any step fails, do NOT proceed to Task 2 — fix the orchestrator first and re-invoke this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/manifest.json &amp;&amp; .venv/bin/python -c "import json; m=json.loads(open('.planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST/manifest.json').read()); req=json.loads(open('tests/fixtures/phase213/manifest-expected-keys.json').read())['required_top_level_keys']; missing=[k for k in req if k not in m]; print('missing_keys', missing); assert not missing, f'manifest missing {missing}'; assert 'bind_map' in m, 'bind_map missing'; print('bind_map', m['bind_map'])" &amp;&amp; .venv/bin/pytest tests/test_phase213_classify.py tests/test_phase213_ndjson_schema.py tests/test_phase213_manifest_schema.py tests/test_phase213_mutation_boundary.py tests/test_phase213_alert_window.py -q 2&gt;&amp;1 | tail -5 &amp;&amp; (find .planning/phases/213-experience-baseline-harness/evidence -name "*.raw.json" | head -1 | grep -q . &amp;&amp; { echo "FAIL: raw state json under evidence"; exit 1; } || echo "OK: no raw state json under evidence/")</automated>
  </verify>
  <done>
    RUN-CHECK-MANIFEST/manifest.json exists with every required_top_level_key including `bind_map`. All FIVE Wave 0 tests GREEN. No `*.raw.json` artifact present under evidence/. `--check-manifest` ran offline (no SSH); `--check-prereqs` ran live and passed. Per-RUN README emitted mirrors the top-level 6-column shape. Neither mode executed any flent or curl-browse traffic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute one real evidence-capturing run (D-11 SERIALIZED per-WAN order, codex MEDIUM-7 wording)</name>
  <what-built>
    Plans 01–04 + Task 1 prove the codepath is complete and the live prereq probe passes. This step asks the operator to invoke the orchestrator for real, which generates production WAN traffic (D-10 allowance: dev-VM-originated traffic only) and writes a full per-test evidence tree under `evidence/RUN-<utc-ts>/`, with signal-sheet.{json,md} INSIDE the RUN dir (codex MEDIUM-5).
  </what-built>
  <how-to-verify>
    1. Confirm dev VM source IPs match Phase 191/198 expectations. Spectrum bind=10.10.110.226; ATT bind=10.10.110.233. Run `ip -4 addr show | grep " 10.10.110."` to record current state. If either IP is missing, abort and fix dev-VM networking before continuing.
    2. Confirm cake-shaper is reachable: `ssh -o BatchMode=yes cake-shaper 'echo ok'`.
    3. Confirm autorate endpoints respond: `curl -fsS --max-time 5 http://10.10.110.223:9101/health | jq .version` (Spectrum) and `curl -fsS --max-time 5 http://10.10.110.227:9101/health | jq .version` (ATT).
    4. Confirm no other operator is loading the WANs (check `journalctl -u wanctl@spectrum.service -n 20 -p info --no-pager | grep -i load` if uncertain).
    5. Run the real-run invocation per the interfaces block — note `--bind-map` (NOT `--local-bind`), and that `--wans spectrum,att` means SERIALIZED Spectrum-first-then-ATT (codex MEDIUM-7 wording — this is NOT concurrent):
       ```
       bash scripts/phase213-baseline-capture.sh \
         --bind-map spectrum=10.10.110.226,att=10.10.110.233 \
         --host dallas \
         --flent-duration 60 --browse-duration 60 --pre-buf 10 --post-buf 10 \
         --wans spectrum,att \
         --tests browse,tcp_upload,tcp_download,rrul,tcp_12down \
         --evidence-root .planning/phases/213-experience-baseline-harness/evidence
       ```
    6. Watch the per-test progress messages. Expected total wall-clock: ~12-15 minutes per WAN, ~25-30 minutes total. The orchestrator runs the full Spectrum suite to completion, THEN starts ATT. Per D-11 + codex MEDIUM-7: this is **serialized**, not concurrent. NEVER attempt to "speed up" the run by overlapping WANs.
    7. After completion, confirm artifact tree exists: `find .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts> -type f | wc -l` should be in the range 100-200 (5 tests × 2 WANs × ~10 artifacts/test + manifest + signal sheet + flent symlinks).
    8. Confirm signal sheet emitted INSIDE the RUN dir (codex MEDIUM-5): `ls .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/signal-sheet.{json,md}`. Inspect `signal-sheet.md` — it must list six buckets, a summary header, and a recommended_next_phase row.
    9. Audit redaction (HIGH-2 invariant): `find .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts> -name "*.raw.json"` MUST return empty (raw state was only ever at /tmp/, cleaned by EXIT trap; nothing landed under evidence/). Run the D-08 forbidden-pattern grep against committed redacted artifacts:
       ```
       find .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts> -name "*.redacted.json" \
         -exec grep -l -E -i '"(password|secret|token|credential|auth|key|private)"\s*:\s*[^<]' {} \;
       ```
       This MUST return empty — every match must be `"<REDACTED>"`.
    10. If anything fails: orchestrator failed prerequisite check → operator decides whether to fix and rerun. If a per-test poller errored mid-run, examine `poll-failures.tsv` in the affected test dir and decide whether to discard the run and rerun or accept the partial evidence. **HIGH-4 invariant check:** verify no orphaned poller process: `pgrep -af phase213-health-poller` should return empty.
  </how-to-verify>
  <resume-signal>Type "approved" once the real run completed, signal sheet emitted inside RUN-<ts>/, redaction audit passes, and `pgrep -af phase213-health-poller` returns empty (HIGH-4 trap cleanup worked). If any audit failed, describe the failure and we'll fix the orchestrator before retrying. Per D-11 + codex MEDIUM-7, never describe `--wans att,spectrum` (or any ordered pair) as "loaded concurrently" — both orders are SERIALIZED; they only differ in which WAN runs first.</resume-signal>
  <files>.planning/phases/213-experience-baseline-harness/evidence/RUN-LIVE/</files>
  <action>Human-only checkpoint — see &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; above. Operator runs the orchestrator interactively and verifies the artifact tree + redaction audit + no-orphan-poller check before unblocking Task 3.</action>
  <verify>
    <automated>test -d .planning/phases/213-experience-baseline-harness/evidence &amp;&amp; ls -d .planning/phases/213-experience-baseline-harness/evidence/RUN-*/ 2&gt;/dev/null | grep -v RUN-CHECK-MANIFEST | head -1 | grep -q . &amp;&amp; RUN_DIR=$(ls -dt .planning/phases/213-experience-baseline-harness/evidence/RUN-*/ | grep -v RUN-CHECK-MANIFEST | head -1) &amp;&amp; test -f "${RUN_DIR}/signal-sheet.json" &amp;&amp; test -f "${RUN_DIR}/signal-sheet.md" &amp;&amp; (find "${RUN_DIR}" -name "*.raw.json" | head -1 | grep -q . &amp;&amp; { echo FAIL_RAW; exit 1; } || echo OK_NO_RAW) &amp;&amp; (pgrep -af phase213-health-poller | grep -q . &amp;&amp; { echo FAIL_ORPHAN_POLLER; exit 1; } || echo OK_NO_ORPHAN)</automated>
  </verify>
  <done>Operator typed "approved"; `RUN-<ts>/signal-sheet.{json,md}` present INSIDE the run dir; redaction audit passed (no raw state.json under evidence/, no unredacted secrets in *.redacted.json); no orphaned poller process (HIGH-4 trap cleanup confirmed).</done>
</task>

<task type="auto">
  <name>Task 3: Operator-authored 213-REPORT.md + redaction-gated `git add -f` commit (BASE-03 success criteria 3 + 4, D-13, D-15, codex MEDIUM-4)</name>
  <read_first>
    - .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/signal-sheet.md (the signal sheet generated by Task 2 — INSIDE the run dir per MEDIUM-5)
    - .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/signal-sheet.json (raw evidence rows)
    - .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/manifest.json (run metadata for §"Run Metadata" — note bind_map field)
    - .planning/phases/213-experience-baseline-harness/213-CONTEXT.md §"Symptom → Bucket Classification (BASE-03)"
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Bucket → Evidence Map"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md MEDIUM-4 (git add -f + redaction gate)
    - .planning/phases/212-production-inventory-and-drift-audit/212-REPORT.md (STYLE template)
    - .planning/ROADMAP.md §"Phase 214/215/216" (what each next-phase choice commits to)
    - .gitignore (confirm `.planning/` is ignored — `git add -f` is required for evidence)
  </read_first>
  <acceptance_criteria>
    - 213-REPORT.md exists with all five required sections (Summary, Per-Bucket Verdict, Recommended Next Phase, Downstream Constraints, Run Metadata).
    - At least one of Phase 214 / 215 / 216 named in the Recommended Next Phase section.
    - At least three `evidence/RUN-<ts>/...` citations appear in the body.
    - §"Run Metadata" cites the `bind_map` field from manifest.json (per-WAN bind provenance — HIGH-1).
    - ROADMAP.md Phase 213 entry now `[x]` and dated.
    - Pre-commit redaction grep gate passed on the staged evidence (MEDIUM-4): no `"(password|secret|token|credential|auth|key|private)":\s*[^<]` match in any staged file.
    - `git ls-tree -r HEAD -- .planning/phases/213-experience-baseline-harness/evidence/RUN-<ts>/` returns the expected files (proves `git add -f` worked against the `.planning/` gitignore).
  </acceptance_criteria>
  <files>.planning/phases/213-experience-baseline-harness/213-REPORT.md</files>
  <action>
    **Step 1: Author 213-REPORT.md.**

    Author `.planning/phases/213-experience-baseline-harness/213-REPORT.md` (target ~90-200 lines) following the five-section outline in the interfaces block. Mirror Phase 212's report tone (operator-first, evidence-cited, no speculative language).

    For each of the six buckets in §"Per-Bucket Verdict": state the verdict (flagged / not-flagged / inconclusive), cite at least one signal-sheet evidence row by `evidence_path` (per D-13), and add operator commentary explaining why the threshold did or did not fire. For BUCKET 4 (steering drift) the verdict MUST be expressed in raw counter-delta and state-transition terms ONLY — no comparison to v1.39 threshold field names per D-14. For BUCKET 1 (upload ceiling) cite the ceiling value source as `configs/spectrum.yaml upload.ceiling_mbps` or `configs/att.yaml upload.ceiling_mbps` (per codex MEDIUM-2 — link-agnostic architecture).

    For §"Recommended Next Phase": ONE primary (must be 214, 215, or 216 per D-15 — no other phase IDs accepted), at least one runner-up, evidence-cited rationale for each. If the signal sheet flagged zero buckets, the primary recommendation defaults to Phase 214 (measurement collapse investigation is the conservative next step) with explicit operator note.

    For §"Downstream Constraints": list 3-5 facts the next phase must honor — at minimum: observed Spectrum UL ceiling-pegged % per D-18 (intentional config, not drift); v1.39 steering threshold-name interpretation remains forbidden until Phase 216 alignment; Phase 212 inventory facts are still the authoritative baseline (D-19).

    For §"Run Metadata": include RUN-<ts>, **bind_map (per codex HIGH-1 provenance)**, source IPs per WAN, netperf_host, flent_version, host_dev_vm, wanctl_version_{spectrum,att,steering}_runtime, signal-sheet absolute paths (`evidence/RUN-<ts>/signal-sheet.json` and `.md` per MEDIUM-5).

    End with a closing paragraph noting that Plan 05 has SAFETY-CHECKED the run and confirmed redaction posture holds. Update `.planning/ROADMAP.md` §"Phase 213" to mark `[x]` and append `(completed YYYY-MM-DD)` next to the phase header. Update `.planning/STATE.md` with a one-line entry: "Phase 213 complete; next phase: <N> per operator verdict; runners-up: <list>".

    **Step 2: Pre-commit redaction grep gate (codex MEDIUM-4).**

    Before staging evidence, run the D-08 forbidden-pattern grep against the to-be-added file set. Fail closed on any match:
    ```bash
    RUN_TS="<actual-RUN-timestamp>"
    RUN_DIR=".planning/phases/213-experience-baseline-harness/evidence/RUN-${RUN_TS}"
    STAGE_FILES=$(find "$RUN_DIR" "${RUN_DIR%/*}/RUN-CHECK-MANIFEST" -type f \( -name '*.json' -o -name '*.md' -o -name '*.csv' -o -name '*.ndjson' -o -name '*.tsv' \))
    GATE_FAIL=0
    for f in $STAGE_FILES; do
      if grep -E -i '"(password|secret|token|credential|auth|key|private)"\s*:\s*[^<]' "$f" >/dev/null 2>&1; then
        echo "REDACTION GATE FAIL: $f contains unredacted secret-bearing key"
        GATE_FAIL=1
      fi
    done
    [[ $GATE_FAIL -eq 0 ]] || { echo "ABORTING commit — fix redaction"; exit 1; }
    ```

    **Step 3: `git add -f` (codex MEDIUM-4 — `.planning/` is gitignored).**

    Stage evidence + report + state with `git add -f`:
    ```bash
    git add -f "$RUN_DIR"
    git add -f .planning/phases/213-experience-baseline-harness/evidence/RUN-CHECK-MANIFEST
    git add -f .planning/phases/213-experience-baseline-harness/213-REPORT.md
    git add -f .planning/ROADMAP.md .planning/STATE.md
    ```

    Verify post-stage that the files appear in the index:
    ```bash
    git ls-files --stage -- "$RUN_DIR" | head
    ```

    Do NOT actually commit in this task — that is Kevin's prerogative per the CLAUDE.md project-finalizer workflow. This task stages files for commit; project-finalizer runs the actual commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Per-Bucket Verdict" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Recommended Next Phase" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Downstream Constraints" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "## Run Metadata" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -E "Phase 21[456]" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -Eo "evidence/RUN-[A-Z0-9_-]+" .planning/phases/213-experience-baseline-harness/213-REPORT.md | head -3 &amp;&amp; grep -F "bind_map" .planning/phases/213-experience-baseline-harness/213-REPORT.md &amp;&amp; grep -F "[x] **Phase 213" .planning/ROADMAP.md &amp;&amp; (RUN_DIR=$(ls -dt .planning/phases/213-experience-baseline-harness/evidence/RUN-*/ | grep -v CHECK-MANIFEST | head -1); find "$RUN_DIR" -type f \( -name '*.json' -o -name '*.md' -o -name '*.csv' -o -name '*.ndjson' -o -name '*.tsv' \) -exec grep -l -E -i '"(password|secret|token|credential|auth|key|private)":[[:space:]]*[^&lt;]' {} \; | head -1 | grep -q . &amp;&amp; { echo "FAIL: unredacted secret found in staged evidence"; exit 1; } || echo "OK: redaction gate clean") &amp;&amp; git ls-files --stage -- .planning/phases/213-experience-baseline-harness/213-REPORT.md | grep -q .</automated>
  </verify>
  <done>
    213-REPORT.md exists with all five required sections including bind_map in Run Metadata. At least one of Phase 214 / 215 / 216 named in the Recommended Next Phase section. At least three `evidence/RUN-<ts>/...` citations appear. ROADMAP.md Phase 213 entry `[x]` and dated. STATE.md updated. Redaction grep gate passed on all staged evidence files. `git ls-files --stage` shows the report and RUN-<ts> evidence are present in the index (proves `git add -f` overrode the `.planning/` gitignore).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real-run dev VM → autorate /health + cake-shaper SSH | Inherits Plans 02/03/04 boundaries; per-WAN bind map (HIGH-1) prevents mislabeling; per-WAN egress probe refuses on Spectrum mismatch. |
| Operator authorship → committed report | Report is text; cites already-redacted evidence paths. No fresh production access at report-authoring time. |
| Evidence staging → git index (codex MEDIUM-4) | `.planning/` is gitignored; `git add -f` is required. Pre-commit redaction grep gate fails closed before staging — no unredacted secret-bearing keys can reach the index. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I (Information disclosure) | Committed RUN-<ts> evidence tree | mitigate | Task 1 verify gate AND Task 2 step-9 audit confirm zero `*.raw.json` artifacts under evidence/ (HIGH-2 design — raw state was only ever at /tmp/). Task 3 pre-commit D-08 forbidden-pattern grep gate fails closed on any unredacted key match in `*.redacted.json` / `*.json` / `*.md` / `*.csv` / `*.ndjson` / `*.tsv` before `git add -f`. |
| T-213-02 | T (Tampering) | Orchestrator run mutates evidence dir only | mitigate | Plan 01 mutation-boundary test is part of Task 1's GREEN-gate rerun. |
| T-213-04 | D (Denial of service) | ~25-30 min of dev-VM-originated traffic against both WANs | accept | D-11 + codex MEDIUM-7 wording: orchestrator runs WANs SERIALIZED (never concurrent). Total impact window is auditable from manifest.json `started_utc`/`ended_utc`. Task 2 step-4 asks operator to confirm no concurrent operator load exists before starting. |
| T-213-05 | E (Elevation of privilege) | Operator-authored bucket verdicts could implicitly interpret v1.39 steering thresholds | mitigate | Task 3 action text explicitly forbids BUCKET 4 from comparing values to v1.39 threshold field names; verdict expressed as raw counter deltas / state transitions only per D-14. |
| T-213-08 (codex HIGH-4 follow-through) | D (Denial of service) | Orphaned poller process leaks resources / continues writing NDJSON after script exit | mitigate | Task 2 step-10 includes `pgrep -af phase213-health-poller` check — must return empty after orchestrator exits. The HIGH-4 trap-based cleanup in Plan 04 Task 1 is verified live here. |
| T-213-10 (codex MEDIUM-4) | I (Information disclosure) | `.planning/` is gitignored — operator could forget `-f` and silently drop evidence | mitigate | Task 3 action mandates explicit `git add -f` step; pre-commit redaction grep gate runs BEFORE `git add -f` to prevent staging unredacted artifacts; post-stage `git ls-files --stage` check proves evidence is in the index. |
</threat_model>

<verification>
- Task 1: `--check-manifest` exits 0 offline; manifest contains every required_top_level_key including `bind_map`; `--check-prereqs` exits 0 live; all five Wave 0 tests GREEN; no raw state.json under evidence/.
- Task 2: operator-verified — full per-WAN suite ran to completion in D-11 serialized order; signal-sheet.{json,md} present INSIDE the RUN dir (MEDIUM-5); redaction audit passes; no orphaned poller (HIGH-4).
- Task 3: 213-REPORT.md has all five required sections including `bind_map` in Run Metadata; references Phase 214/215/216 in the recommendation; cites at least three evidence/RUN-<ts>/ paths; ROADMAP.md + STATE.md updated; redaction grep gate clean; `git add -f` succeeded.
- Final state: `.venv/bin/pytest tests/test_phase213_*.py -q` ALL GREEN.
- `git status` shows the report, ROADMAP + STATE updates, RUN-CHECK-MANIFEST, RUN-<ts> tree, and signal-sheet files staged for commit.
</verification>

<success_criteria>
- One offline `--check-manifest` completed with manifest schema GREEN (including bind_map).
- One live `--check-prereqs` completed with per-WAN egress probe passing for Spectrum.
- One real per-WAN SERIALIZED evidence run committed under `evidence/RUN-<ts>/` with bind_map provenance recorded.
- `signal-sheet.json` + `signal-sheet.md` emitted INSIDE the RUN dir with six buckets present and a ranked next-phase recommendation (D-15 + MEDIUM-5).
- Operator-authored 213-REPORT.md exists with bucket verdicts citing evidence rows AND a single ranked next-phase (one of 214/215/216) with runners-up.
- All five Phase 213 Wave 0 tests still GREEN.
- ROADMAP.md Phase 213 marked complete; STATE.md updated.
- Evidence committed via explicit `git add -f` with pre-commit D-08 redaction gate clean.
- D-08/D-09/D-10/D-11/D-13/D-14/D-15/D-19 invariants all observably held in committed artifacts.
- Codex HIGH-1, HIGH-2, HIGH-3, HIGH-4 invariants all observably held: bind_map in manifest, no *.raw.json under evidence, --check-manifest offline + --check-prereqs live both exercised, no orphaned poller.
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-05-SUMMARY.md` covering: offline `--check-manifest` result, live `--check-prereqs` result, real-run start/end timestamps and total artifact count, signal-sheet flagged-bucket list (with path = evidence/RUN-<ts>/signal-sheet.md per MEDIUM-5), operator-chosen primary next phase + runners-up, redaction-audit final state (zero unredacted hits, zero raw state.json under evidence/), HIGH-4 orphan-poller check result, MEDIUM-4 `git add -f` confirmation, Phase 213 closeout note linking to ROADMAP.md + STATE.md updates, and a one-line "Phase 213 complete; next phase: <N> per operator verdict; runners-up: <list>".
</output>
