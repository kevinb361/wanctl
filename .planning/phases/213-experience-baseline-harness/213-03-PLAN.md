---
phase: 213-experience-baseline-harness
plan: 03
type: execute
wave: 2
depends_on: [213-01]
files_modified:
  - scripts/phase213-alert-window.sh
  - scripts/phase213-steering-snapshot.sh
autonomous: true
requirements: [BASE-02]
tags: [baseline, harness, sqlite, steering, ssh, cake-shaper, evidence]

must_haves:
  truths:
    - "SQLite alert windows are extracted read-only from live-writer DBs on cake-shaper using `mode=ro` URI form (never `immutable=1`)."
    - "phase213-alert-window.sh supports `--local-db <path>` offline mode that skips SSH entirely (used by pytest against the fixture DB)."
    - "Per-WAN alert dump + grouped severity-count summary written as JSON for the three DBs: metrics-spectrum.db, metrics-att.db, metrics.db (steering, conditional on existence)."
    - "Steering /health and steering_state.json captured pre/post via SSH; raw state.json is written to a mktemp location, redacted to evidence, then the raw temp is deleted via EXIT trap. ZERO `*.raw.json` artifacts ever land under evidence/."
    - "Neither script restarts services, edits /etc/wanctl, writes to RouterOS, or interprets v1.39 steering threshold-field names."
  artifacts:
    - path: "scripts/phase213-alert-window.sh"
      provides: "SQLite window extraction. Two modes: SSH (--ssh-host) against cake-shaper live DB OR `--local-db <path>` offline against a local fixture DB. Emits alerts-{spectrum,att,steering}.json + alerts-summary.json."
      min_lines: 120
    - path: "scripts/phase213-steering-snapshot.sh"
      provides: "SSH-based pre/post steering /health + state.json capture. Raw state goes to mktemp, gets D-08-redacted into evidence, raw temp deleted by EXIT trap. Emits <prefix>-health.json + <prefix>-state.redacted.json only (NO `*.raw.json` ever under evidence/)."
      min_lines: 80
  key_links:
    - from: "scripts/phase213-alert-window.sh"
      to: "cake-shaper:/var/lib/wanctl/metrics-{spectrum,att}.db + metrics.db (default SSH mode)"
      via: "ssh -o BatchMode=yes ... 'sudo -n sqlite3 -readonly -json file:DB?mode=ro ...'"
      pattern: "sqlite3 -readonly -json file:.*mode=ro"
    - from: "scripts/phase213-alert-window.sh"
      to: "Local fixture DB (--local-db mode, NO SSH)"
      via: "sqlite3 -readonly -json file:${LOCAL_DB}?mode=ro ..."
      pattern: "--local-db"
    - from: "scripts/phase213-steering-snapshot.sh"
      to: "cake-shaper:127.0.0.1:9102/health and /var/lib/wanctl/steering_state.json"
      via: "ssh -o BatchMode=yes ... curl + sudo -n cat into mktemp"
      pattern: "127.0.0.1:9102/health"
    - from: "scripts/phase213-steering-snapshot.sh"
      to: "EXIT trap raw-temp cleanup"
      via: "trap 'rm -f \"$RAW_TMP\"' EXIT INT TERM"
      pattern: "trap.*RAW_TMP.*EXIT"
---

<objective>
Build the two cake-shaper-read surfaces for Phase 213: `phase213-alert-window.sh` (SQLite window extraction — SSH mode against live DBs OR offline `--local-db` mode against fixture DBs) and `phase213-steering-snapshot.sh` (pre/post steering /health + redacted steering_state.json via SSH, with raw temp safely cleaned via EXIT trap).

Codex review patches landed here:
- HIGH-2: steering snapshot writes raw state to mktemp OUTSIDE evidence/, redacts into evidence, deletes raw via EXIT trap. ZERO `*.raw.json` artifacts under evidence/ — resolves the contradiction with Plan 05's no-raw-json verify gate.
- MEDIUM-6: alert-window script gains `--local-db <path>` offline mode so pytest can behaviorally exercise the SQL window/group-by logic against tests/fixtures/phase213/alerts-test.db without SSH.

Output: Two new executable bash scripts. The mutation-boundary test from Plan 01 will GREEN out their previously-skipped script-iterations. The alert-window behavioral test (test_phase213_alert_window.py) will GREEN once `--local-db` mode is wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/213-experience-baseline-harness/213-CONTEXT.md
@.planning/phases/213-experience-baseline-harness/213-RESEARCH.md
@.planning/phases/213-experience-baseline-harness/213-PATTERNS.md
@.planning/phases/213-experience-baseline-harness/213-REVIEWS.md
@.planning/phases/213-experience-baseline-harness/213-01-SUMMARY.md
@scripts/phase196-soak-capture.sh
@scripts/phase198-rerun-flent-3run.sh
@.planning/phases/212-production-inventory-and-drift-audit/evidence/README.md
@.planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json
@src/wanctl/storage/schema.py
@src/wanctl/storage/reader.py

<interfaces>
<!-- alert-window CLI contract (consumed by Wave 3 orchestrator AND Wave 0 pytest): -->
<!-- SSH mode (production live DBs): -->
<!--   bash scripts/phase213-alert-window.sh --start <unix> --end <unix> --output-dir <dir> [--ssh-host cake-shaper] -->
<!-- Local mode (offline, pytest fixture): -->
<!--   bash scripts/phase213-alert-window.sh --local-db <path-to-db> --start <unix> --end <unix> --output-dir <dir> -->
<!-- Mutually exclusive: --local-db disables SSH entirely. -->
<!-- SSH mode emits: alerts-spectrum.json, alerts-att.json, alerts-steering.json, alerts-summary.json. -->
<!-- Local mode emits ONLY: alerts-spectrum.json (with wan="local", db=<--local-db path>). -->
<!-- Per-DB file shape: {"wan": "spectrum"|"att"|"steering"|"local", "db": "<path>", "present": bool, "rows": [...], "summary": [...]} -->

<!-- steering-snapshot CLI contract: -->
<!-- bash scripts/phase213-steering-snapshot.sh --output <prefix> [--ssh-host cake-shaper] -->
<!-- Emits ONLY: <prefix>-health.json + <prefix>-state.redacted.json (NO `*.raw.json` ever under evidence/). -->
<!-- Raw state.json goes to `$(mktemp -t phase213-steering-raw.XXXXXX)` OUTSIDE evidence/, then is deleted by `trap 'rm -f "$RAW_TMP"' EXIT INT TERM`. -->
<!-- Append one line to "${prefix%/*}/.snapshot-log" with iso-ts + redacted-output path. -->

<!-- alerts table columns from src/wanctl/storage/schema.py (verified): -->
<!-- timestamp INTEGER, alert_type TEXT, severity TEXT, wan_name TEXT, details TEXT -->

<!-- D-08 redaction key pattern (verbatim from Phase 212 evidence README): -->
<!-- re.compile(r"(password|secret|token|credential|auth|key|private)", re.I) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/phase213-alert-window.sh (D-07 SQLite read-only window extraction + --local-db offline mode for pytest)</name>
  <read_first>
    - scripts/phase196-soak-capture.sh (entire file ~170 lines — primary analog: SSH + sudo -n sqlite3 -readonly + heredoc SQL pattern)
    - scripts/phase198-rerun-flent-3run.sh lines 333-352 (cross-reference SSH+sqlite shape)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Pattern 4: Read-Only SQLite Window Extraction" (full script body; the mode=ro URI rationale and forbidden immutable=1)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-alert-window.sh"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md MEDIUM-6 (`--local-db` offline mode rationale)
    - tests/test_phase213_alert_window.py (Plan 01 — the three tests this script must satisfy)
    - tests/fixtures/phase213/alerts-test.db (Plan 01 — the fixture DB exercised in --local-db mode)
    - src/wanctl/storage/schema.py lines 68-87 (alerts table column definitions)
    - src/wanctl/storage/reader.py lines 137-223 (query_alerts pattern for read-only URI mode reference)
  </read_first>
  <acceptance_criteria>
    - File exists, executable, passes `bash -n`.
    - `mode=ro` appears in the URI form for BOTH local and SSH modes.
    - `immutable=1` does NOT appear anywhere.
    - `--local-db` flag is parsed; its code branch contains NO `ssh ` invocation (tested by test_phase213_alert_window.py::test_alert_window_local_db_no_ssh_invoked).
    - Running `bash scripts/phase213-alert-window.sh --local-db tests/fixtures/phase213/alerts-test.db --start 1717000000 --end 1717000060 --output-dir <tmp>` produces `<tmp>/alerts-spectrum.json` with the documented shape and row count matching the synthetic window A.
    - test_phase213_mutation_boundary.py PASSES for this script (no D-10 forbidden tokens).
    - Existence probe present for the steering DB path in SSH mode (D-07 conditional).
  </acceptance_criteria>
  <files>scripts/phase213-alert-window.sh</files>
  <action>
    Create a new bash script following RESEARCH §"Pattern 4" body, EXTENDED with a `--local-db` offline mode. `set -euo pipefail`.

    CLI flags: `--start <unix>` (required), `--end <unix>` (required), `--output-dir <dir>` (required), `--ssh-host <name>` (default cake-shaper, ignored in local mode), `--local-db <path>` (mutually exclusive with SSH mode; if set, no SSH happens, no sudo, no cake-shaper reachability needed).

    **Mode dispatch:** After argument parsing, branch on `[[ -n "${LOCAL_DB:-}" ]]`:

    - **Local mode (--local-db, offline for pytest):** Run TWO sqlite3 invocations LOCALLY (no SSH, no sudo):
      ```
      sqlite3 -readonly -json "file:${LOCAL_DB}?mode=ro" \
        "SELECT timestamp, alert_type, severity, wan_name, details FROM alerts WHERE timestamp BETWEEN ${START} AND ${END} ORDER BY timestamp;" > "$ROWS_TMP"
      sqlite3 -readonly -json "file:${LOCAL_DB}?mode=ro" \
        "SELECT alert_type, severity, COUNT(*) AS count FROM alerts WHERE timestamp BETWEEN ${START} AND ${END} GROUP BY alert_type, severity ORDER BY count DESC;" > "$SUMMARY_TMP"
      ```
      Merge via `jq -n --arg wan "local" --arg db "${LOCAL_DB}" --argjson rows "$(cat "$ROWS_TMP" 2>/dev/null || echo '[]')" --argjson summary "$(cat "$SUMMARY_TMP" 2>/dev/null || echo '[]')" '{wan:$wan, db:$db, present:true, rows:$rows, summary:$summary}'` → `${OUTPUT_DIR}/alerts-spectrum.json`. Clean up temps via `trap`. Exit 0. Skip SSH-mode loop entirely.

    - **SSH mode (default, production live DBs):** Iterate three (wan, db, out_basename) entries: `spectrum:/var/lib/wanctl/metrics-spectrum.db:alerts-spectrum`, `att:/var/lib/wanctl/metrics-att.db:alerts-att`, `steering:/var/lib/wanctl/metrics.db:alerts-steering`. For each: (a) existence probe via `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n test -f ${db}"`; if absent (steering DB conditional per D-07), write `{"wan":"<w>","db":"<db>","present":false,"rows":[],"summary":[]}` to `${OUTPUT_DIR}/${out_basename}.json` and continue. (b) Row dump: `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n sqlite3 -readonly -json file:${db}'?mode=ro' \"<row-SQL>\""` to a temp file. (c) Grouped summary via the same SSH shape with the GROUP BY SQL. (d) Merge via `jq -n` per the local-mode form. (e) Clean up temp files. After the loop, write `alerts-summary.json` via `jq -s '{spectrum:.[0], att:.[1], steering:.[2]}'` over the three per-DB files. Add SSH multiplexing flags `-o ControlMaster=auto -o ControlPath=~/.ssh/control-%h:%p:%r -o ControlPersist=5m` (Pitfall 4 — speed, not correctness).

    Add the URI form EXACTLY as `file:${db}'?mode=ro'` (or `file:${LOCAL_DB}'?mode=ro'`) — do NOT add `immutable=1`. Top-of-file docstring states D-07/D-09/D-10 posture AND notes `--local-db` is for pytest fixture exercise only (never against a live writer DB — that's what SSH mode is for). Do NOT include any D-10 forbidden token. chmod +x.
  </action>
  <verify>
    <automated>test -x scripts/phase213-alert-window.sh &amp;&amp; bash -n scripts/phase213-alert-window.sh &amp;&amp; grep -n "mode=ro" scripts/phase213-alert-window.sh &amp;&amp; (grep -n "immutable=1" scripts/phase213-alert-window.sh &amp;&amp; { echo "FAIL: immutable=1 forbidden"; exit 1; } || echo "OK: no immutable=1") &amp;&amp; grep -n -- "--local-db" scripts/phase213-alert-window.sh &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_alert_window.py -q 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    Script exists, executable, passes `bash -n`. Mutation-boundary test PASSES (alert-window no longer skipped). Alert-window behavioral tests PASS (window-A row count, window-B row count, no-SSH-in-local-db-branch source-grep). `mode=ro` appears; `immutable=1` does NOT. SSH multiplexing flags present. Existence probe present for the steering DB path (D-07).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/phase213-steering-snapshot.sh (D-08 pre/post steering capture; raw state goes to mktemp + EXIT trap, ZERO raw artifacts under evidence/)</name>
  <read_first>
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md (Phase 212 verbatim commands: `curl -fsS --max-time 3 http://127.0.0.1:9102/health` and `sudo -n cat /var/lib/wanctl/steering_state.json`)
    - .planning/phases/213-experience-baseline-harness/213-RESEARCH.md §"Pattern 3: Steering Snapshot via SSH" (full script body, redactor heredoc)
    - .planning/phases/213-experience-baseline-harness/213-PATTERNS.md §"scripts/phase213-steering-snapshot.sh"
    - .planning/phases/213-experience-baseline-harness/213-REVIEWS.md HIGH-2 (mktemp + EXIT trap pattern — must produce ZERO raw artifacts under evidence/)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json (the v1.39 /health shape; do NOT interpret threshold field names per D-08/D-14)
    - .planning/phases/212-production-inventory-and-drift-audit/evidence/steering-state.redacted.json (the redaction output shape to match)
    - scripts/phase196-soak-capture.sh lines 130 (SSH BatchMode discipline reference)
  </read_first>
  <acceptance_criteria>
    - File exists, executable, passes `bash -n`.
    - `trap 'rm -f "$RAW_TMP"' EXIT INT TERM` present and registered IMMEDIATELY after `RAW_TMP=$(mktemp ...)` is assigned (before any operation that could fail).
    - The mktemp path is OUTSIDE `.planning/phases/213-experience-baseline-harness/evidence/` (use `mktemp -t phase213-steering-raw.XXXXXX` which uses `/tmp`).
    - Script emits ONLY two artifacts under the `--output` prefix dir: `<prefix>-health.json` and `<prefix>-state.redacted.json`. `*.raw.json` MUST NOT be emitted anywhere under evidence/.
    - Redactor heredoc applies the verbatim D-08 key pattern (`password|secret|token|credential|auth|key|private`, IGNORECASE) recursively.
    - No `if val < green_rtt_ms` (or any other threshold-field-name comparison) — D-14 invariant.
    - test_phase213_mutation_boundary.py PASSES (no D-10 forbidden tokens).
    - After running against any real steering_state.json, `find <prefix-dir> -name "*.raw.json"` returns empty.
  </acceptance_criteria>
  <files>scripts/phase213-steering-snapshot.sh</files>
  <action>
    Create a new bash script implementing the HIGH-2 fix pattern. `set -euo pipefail`. CLI flags: `--output <prefix>` (required; the prefix becomes <prefix>-health.json and <prefix>-state.redacted.json — NOT *-state.raw.json), `--ssh-host <name>` (default cake-shaper).

    **Operations in order:**
    1. Record `ts_utc=$(date -u -Iseconds)` and `mkdir -p "$(dirname "$OUTPUT")"`.
    2. **Create raw temp file OUTSIDE evidence/:** `RAW_TMP=$(mktemp -t phase213-steering-raw.XXXXXX)` (this places the temp under `/tmp/`, never under evidence/).
    3. **Register cleanup trap IMMEDIATELY:** `trap 'rm -f "$RAW_TMP"' EXIT INT TERM`. The trap MUST be the very next statement after the mktemp assignment so that any subsequent failure (curl timeout, SSH refusal, redactor crash) still cleans up the raw temp file.
    4. Fetch /health to evidence (NOT raw — /health is not secret-bearing in v1.39 per Phase 212 evidence): `ssh -o BatchMode=yes "$SSH_HOST" "curl -fsS --max-time 3 http://127.0.0.1:9102/health" > "${OUTPUT}-health.json"`.
    5. Fetch raw state TO THE TEMP FILE (not evidence): `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n cat /var/lib/wanctl/steering_state.json" > "$RAW_TMP"`.
    6. **Redact directly into evidence:** invoke `python3 - "$RAW_TMP" "${OUTPUT}-state.redacted.json" <<'PY' ... PY` heredoc using the verbatim D-08 redactor from RESEARCH §Pattern 3 (PAT = re.compile(r"(password|secret|token|credential|auth|key|private)", re.I); recursive dict/list walk; `"<REDACTED>"` substitution; `json.dump(redact(data), f, indent=2, sort_keys=True)`).
    7. (The EXIT trap will delete $RAW_TMP on script exit — both success and failure paths.)
    8. Append a one-line snapshot-log row: `echo "${ts_utc} ${OUTPUT}-health.json ${OUTPUT}-state.redacted.json" >> "${OUTPUT%/*}/.snapshot-log"`.
    9. Add a comment block referencing the ~100ms race window between /health and state.json reads — acceptable for pre/post bracketing at test-duration granularity per RESEARCH §Pattern 3.

    Top-of-file docstring states D-08/D-09/D-10 posture, references the v1.39 threshold-field-name non-interpretation invariant (D-14), and explicitly documents the HIGH-2 design: raw state lives only in /tmp/ for the script's lifetime; ONLY the redacted artifact lands under evidence/; EXIT trap guarantees cleanup. The script must NOT compare any value to threshold field names (`green_rtt_ms`, `yellow_rtt_ms`, `red_rtt_ms`); it only reads and writes JSON. Do NOT include any D-10 forbidden token. chmod +x.

    **NO .gitignore additions needed** — under the HIGH-2 fix, `*.raw.json` files never exist under evidence/ in the first place, so there's nothing to gitignore.
  </action>
  <verify>
    <automated>test -x scripts/phase213-steering-snapshot.sh &amp;&amp; bash -n scripts/phase213-steering-snapshot.sh &amp;&amp; grep -E "trap .*RAW_TMP.*EXIT" scripts/phase213-steering-snapshot.sh &amp;&amp; grep -E "mktemp.*phase213-steering" scripts/phase213-steering-snapshot.sh &amp;&amp; .venv/bin/pytest tests/test_phase213_mutation_boundary.py -q 2&gt;&amp;1 | tail -5 &amp;&amp; grep -E "password.*secret.*token.*credential.*auth.*key.*private" scripts/phase213-steering-snapshot.sh &amp;&amp; (grep -E "\\bif\\b.*green_rtt_ms|\\bif\\b.*yellow_rtt_ms|\\bif\\b.*red_rtt_ms" scripts/phase213-steering-snapshot.sh &amp;&amp; { echo "FAIL: D-14 violation"; exit 1; } || echo "OK: no threshold-name comparisons") &amp;&amp; (grep -E "raw\\.json" scripts/phase213-steering-snapshot.sh | grep -v "RAW_TMP\\|mktemp\\|HIGH-2\\|raw state\\|#" &amp;&amp; { echo "FAIL: raw.json appears in evidence-write path"; exit 1; } || echo "OK: no raw.json artifact in evidence path")</automated>
  </verify>
  <done>
    Script exists, executable, passes `bash -n`. EXIT trap registered immediately after mktemp. Raw state file path is /tmp/-based (never under evidence/). Mutation-boundary test PASSES (steering-snapshot no longer skipped). Redaction regex matches the verbatim D-08 key pattern. No threshold-field-name comparison present (D-14 invariant). `*.raw.json` is never written to evidence/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev VM → cake-shaper (SSH, default mode) | Read-only commands only. BatchMode=yes refuses password prompts; sudo -n refuses to wait. Failure mode is fail-fast, not hang. |
| Dev VM → local fixture DB (--local-db mode) | Read-only sqlite3 against a local file. No network. Used by pytest only. |
| cake-shaper → /var/lib/wanctl/metrics-*.db (live writer) | SQLite mode=ro URI form provides safe read against the live writer; never blocks the writer. |
| cake-shaper → /var/lib/wanctl/steering_state.json (root-readable) | sudo -n cat into /tmp/$RAW_TMP (NOT evidence). Raw temp deleted via EXIT trap. Only D-08-redacted artifact lands in evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213-01 | I (Information disclosure) | steering_state.json raw output containing secrets | mitigate | HIGH-2 fix: raw state lives only at `/tmp/phase213-steering-raw.XXXXXX` for the script's lifetime; `trap 'rm -f "$RAW_TMP"' EXIT INT TERM` guarantees deletion on any exit (success, error, signal). Redactor heredoc applies verbatim D-08 key pattern recursively before writing to `${OUTPUT}-state.redacted.json`. ZERO `*.raw.json` artifacts ever exist under evidence/. Plan 05 verify gate `find ... -name "*.raw.json"` is consistent (returns empty). |
| T-213-02 | T (Tampering) | cake-shaper SSH session for sudo -n sqlite3 / sudo -n cat | mitigate | BatchMode=yes prevents password-prompt hangs; sudo -n fails loudly if cached sudo absent (per Phase 212 precedent). Project uses SSH key auth exclusively. |
| T-213-03 | I (Information disclosure) | Concurrent SQLite read of live-writer DB | mitigate | URI form `file:DB?mode=ro` is the safe pattern per RESEARCH §Pattern 4. `immutable=1` explicitly forbidden in the verify gate (would give stale views). |
| T-213-05 | E (Elevation of privilege) | Steering threshold-field-name interpretation while v1.39 drift unresolved | mitigate | Script body contains no `if val < green_rtt_ms` or similar comparisons; only JSON capture. D-14 grep gate in verify enforces this. Classifier D-14 enforcement lives in Plan 04 and the test from Plan 01. |
| T-213-06 (Plan 01 NEW per HIGH-3, applies here) | T (Tampering) | "Offline" pytest tests triggering cake-shaper SSH via alert-window | mitigate | Alert-window `--local-db` mode skips SSH entirely. Test source-grep asserts the `--local-db` code branch contains no `ssh ` invocation. Pytest no longer depends on cake-shaper reachability for alert-window behavior. |
</threat_model>

<verification>
- `bash -n` exits 0 on both scripts.
- `.venv/bin/pytest tests/test_phase213_mutation_boundary.py tests/test_phase213_alert_window.py -q` PASSES (alert-window behavioral test included).
- `grep -F "mode=ro" scripts/phase213-alert-window.sh` matches.
- `grep -F "immutable=1" scripts/phase213-alert-window.sh` returns nothing.
- `grep -F "--local-db" scripts/phase213-alert-window.sh` matches (offline mode flag present).
- `grep -E "trap .*RAW_TMP.*EXIT" scripts/phase213-steering-snapshot.sh` matches.
- `grep -E "mktemp.*phase213-steering" scripts/phase213-steering-snapshot.sh` matches (raw temp path is outside evidence/).
- `grep -F "password|secret|token|credential|auth|key|private" scripts/phase213-steering-snapshot.sh` matches the verbatim D-08 pattern.
- `grep -E "if .*green_rtt_ms|if .*yellow_rtt_ms|if .*red_rtt_ms" scripts/phase213-steering-snapshot.sh` returns nothing (D-14 invariant).
- `git diff --stat src/wanctl/` returns empty (SAFE-05 invariant).
</verification>

<success_criteria>
- Both new scripts executable, syntactically valid bash.
- alert-window emits four JSON artifacts in SSH mode (alerts-spectrum, alerts-att, alerts-steering, alerts-summary); emits one in --local-db mode (alerts-spectrum with wan="local").
- steering-snapshot emits exactly two artifacts under <prefix>: -health.json and -state.redacted.json. ZERO *.raw.json under evidence/ (HIGH-2 invariant).
- mutation-boundary test fully GREEN — all six phase213 scripts now scanned without skip.
- alert-window behavioral test PASSES (window A + window B + no-SSH-in-local-db source-grep).
</success_criteria>

<output>
After completion, create `.planning/phases/213-experience-baseline-harness/213-03-SUMMARY.md` covering: script line counts, CLI contracts for both scripts (SSH vs --local-db split for alert-window; mktemp + EXIT trap pattern for steering-snapshot), SQLite URI form actually used (must contain "mode=ro" and NOT "immutable=1"), D-08 redactor key pattern verbatim, raw-state-path verification (raw lives at /tmp/phase213-steering-raw.XXXXXX, never under evidence/), mutation-boundary test final state (zero skipped scripts), alert-window behavioral test GREEN.
</output>
