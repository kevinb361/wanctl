---
phase: 177-live-storage-footprint-investigation
plan: 02
type: execute
wave: 2
depends_on:
  - 177-01-PLAN.md
files_modified:
  - .planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md
requirements: [STOR-04]
autonomous: true
tags: [storage, sqlite, retention, evidence]

must_haves:
  truths:
    - "The phase captures what kinds of data are occupying the active per-WAN DBs"
    - "Retention/downsampling is described from production evidence rather than from code assumptions alone"
    - "The report distinguishes data-volume growth from WAL or free-page artifacts"
  artifacts:
    - path: ".planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md"
      provides: "production evidence for DB composition and retained-history shape"
      contains: "oldest retained"
  key_links:
    - from: ".planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md"
      to: ".planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md"
      via: "measured composition of the files identified as active"
      pattern: "metrics-spectrum.db"
---

<objective>
Measure what is actually consuming space in the active per-WAN DBs and characterize the retained-history shape from live evidence.

Purpose: file size alone is not enough to justify a retention or cleanup change. This plan produces the evidence Phase 178 needs to choose the smallest safe reduction strategy.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/177-live-storage-footprint-investigation/177-RESEARCH.md
@.planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md
@src/wanctl/storage/retention.py
@src/wanctl/storage/downsampler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture active DB schema and retained-window evidence</name>
  <files>.planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md</files>
  <read_first>
    - src/wanctl/storage/retention.py
    - src/wanctl/storage/downsampler.py
    - .planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md
  </read_first>
  <action>
    Create `177-db-composition-report.md` and record:

    - table list for each active per-WAN DB
    - oldest and newest retained metric timestamps
    - oldest/newest observed granularities
    - the inferred retained-window shape from live evidence

    Tie the production observations back to the retention/downsampling code paths, but do not claim code intent and live behavior are equivalent unless the evidence matches.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 /var/lib/wanctl/metrics-spectrum.db ".tables" && echo --- && sudo -n sqlite3 /var/lib/wanctl/metrics-att.db ".tables"'</automated>
  </verify>
  <acceptance_criteria>
    - report lists the active DB tables
    - report gives oldest/newest timestamps for the active metrics history
    - report states whether downsampling appears active in production
  </acceptance_criteria>
  <done>Live DB schema and retained-window evidence are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Quantify whether the footprint is live data, WAL growth, or reclaimable slack</name>
  <files>.planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md</files>
  <read_first>
    - .planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md
  </read_first>
  <action>
    Extend the report with:

    - DB/WAL/SHM size comparison
    - page count, free-list count, and free-page percentage for each active DB
    - a concise conclusion explaining whether the footprint is mostly:
      - live DB content
      - transient WAL accumulation
      - reclaimable slack/free pages

    Keep conclusions narrow and evidence-backed.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 -json /var/lib/wanctl/metrics-spectrum.db "PRAGMA page_count; PRAGMA freelist_count; PRAGMA page_size;" && echo --- && sudo -n sqlite3 -json /var/lib/wanctl/metrics-att.db "PRAGMA page_count; PRAGMA freelist_count; PRAGMA page_size;"'</automated>
  </verify>
  <acceptance_criteria>
    - report includes DB vs WAL comparison for both active DBs
    - report includes free-page percentages for both active DBs
    - report states whether the storage problem is WAL, slack, or live content
  </acceptance_criteria>
  <done>The active DB footprint is characterized as live content versus transient or reclaimable overhead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sqlite read-only queries -> production DBs | large-table introspection must not modify or lock the DBs in disruptive ways |
| evidence -> Phase 178 scope | overclaiming what the evidence proves could lead to the wrong storage-reduction change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-03 | D (Denial of Service) | heavy DB introspection | mitigate | prefer targeted read-only commands and avoid broad invasive scans unless necessary |
| T-177-04 | I (Integrity) | retained-window interpretation | mitigate | separate observed facts from inferred conclusions explicitly in the report |
</threat_model>

<verification>
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 /var/lib/wanctl/metrics-spectrum.db ".tables" && echo --- && sudo -n sqlite3 /var/lib/wanctl/metrics-att.db ".tables"'`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n sqlite3 -json /var/lib/wanctl/metrics-spectrum.db "PRAGMA page_count; PRAGMA freelist_count; PRAGMA page_size;" && echo --- && sudo -n sqlite3 -json /var/lib/wanctl/metrics-att.db "PRAGMA page_count; PRAGMA freelist_count; PRAGMA page_size;"'`
- `rg -n 'oldest retained|newest retained|downsampling|WAL|free-page|live content' .planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md`
</verification>

<success_criteria>
The phase now explains, with production evidence, whether the multi-GB footprint comes from retained live content, WAL accumulation, reclaimable slack, or some mix of those causes.
</success_criteria>

<output>
After completion, update `.planning/phases/177-live-storage-footprint-investigation/177-db-composition-report.md` and create `.planning/phases/177-live-storage-footprint-investigation/177-02-SUMMARY.md`
</output>

