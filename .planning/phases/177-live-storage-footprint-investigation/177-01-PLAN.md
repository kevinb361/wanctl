---
phase: 177-live-storage-footprint-investigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md
requirements: [STOR-04]
autonomous: true
tags: [storage, production, investigation, inventory]

must_haves:
  truths:
    - "The repo and production host agree on which DB paths are intended to be active"
    - "Legacy `metrics.db` presence is recorded as active, inactive residue, or unknown pending evidence"
    - "The phase captures explicit evidence for each DB file class: active per-WAN, legacy residue, and stale zero-byte files"
  artifacts:
    - path: ".planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md"
      provides: "authoritative DB path and file-role inventory for production"
      contains: "metrics-spectrum.db"
  key_links:
    - from: "configs/spectrum.yaml and configs/att.yaml"
      to: ".planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md"
      via: "live-path closure"
      pattern: "db_path"
---

<objective>
Capture an authoritative inventory of active versus legacy metrics DB paths before any retention or cleanup change is proposed.

Purpose: production currently has multi-GB per-WAN DBs plus a legacy `metrics.db`. This plan closes the basic ambiguity around which files are configured, discoverable, and present on the host so later phases do not optimize the wrong target.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/177-live-storage-footprint-investigation/177-RESEARCH.md
@configs/spectrum.yaml
@configs/att.yaml
@src/wanctl/config_base.py
@src/wanctl/storage/db_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit repo-side DB path authority</name>
  <files>.planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md</files>
  <read_first>
    - configs/spectrum.yaml
    - configs/att.yaml
    - src/wanctl/config_base.py
    - src/wanctl/storage/db_utils.py
  </read_first>
  <action>
    Create `177-storage-path-audit.md` and document:

    - the configured per-WAN `db_path` values in the shipped configs
    - any remaining code defaults that still mention `/var/lib/wanctl/metrics.db`
    - the discovery/fallback behavior in `src/wanctl/storage/db_utils.py`

    Keep this task evidence-only. Do not change code or config yet.
  </action>
  <verify>
    <automated>rg -n 'db_path|metrics\\.db|metrics-' configs/spectrum.yaml configs/att.yaml src/wanctl/config_base.py src/wanctl/storage/db_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - audit doc names the active configured DB files
    - audit doc names the legacy default path still present in code
    - audit doc describes when the code falls back to legacy `metrics.db`
  </acceptance_criteria>
  <done>Repo-side DB path authority and fallback behavior are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory production DB files and classify each one</name>
  <files>.planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md</files>
  <read_first>
    - .planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md
  </read_first>
  <action>
    Extend the audit doc with a live production inventory using read-only commands. Capture:

    - active per-WAN DB file sizes and companion WAL/SHM files
    - legacy `metrics.db` size
    - zero-byte or stale-looking DB files

    Classify each file as one of:
    - active runtime DB
    - legacy residue
    - stale/empty artifact
    - unresolved

    If any role cannot be proven yet, mark it unresolved instead of guessing.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n ls -lh /var/lib/wanctl/metrics*.db* /var/lib/wanctl/*metrics.db* 2>/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - audit doc includes file sizes for the active per-WAN DBs
    - audit doc includes the legacy `metrics.db` size
    - every observed DB file is assigned a role or explicitly marked unresolved
  </acceptance_criteria>
  <done>Production DB files are inventoried and classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo analysis -> production host | evidence gathering touches live production paths and must remain read-only |
| production file inventory -> later cleanup decisions | misclassifying an active DB as residue would be destructive in a future phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | T (Tampering) | production DB inventory | mitigate | use read-only file and sqlite commands only; no service restart or write path in this plan |
| T-177-02 | I (Integrity) | file-role classification | mitigate | require explicit evidence or label the role unresolved rather than inferring certainty |
</threat_model>

<verification>
- `rg -n 'db_path|metrics\\.db|metrics-' configs/spectrum.yaml configs/att.yaml src/wanctl/config_base.py src/wanctl/storage/db_utils.py`
- `ssh -o BatchMode=yes kevin@10.10.110.223 'sudo -n ls -lh /var/lib/wanctl/metrics*.db* /var/lib/wanctl/*metrics.db* 2>/dev/null'`
- `rg -n 'metrics-spectrum\\.db|metrics-att\\.db|metrics\\.db|legacy residue|stale/empty artifact|unresolved' .planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md`
</verification>

<success_criteria>
The phase now has a single evidence-backed inventory explaining which DB files are configured to be active, which are merely present on disk, and which file roles remain unresolved.
</success_criteria>

<output>
After completion, update `.planning/phases/177-live-storage-footprint-investigation/177-storage-path-audit.md` and create `.planning/phases/177-live-storage-footprint-investigation/177-01-SUMMARY.md`
</output>

