---
phase: 67-production-config-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/67/AUDIT.md
autonomous: false
requirements:
  - LGCY-01
must_haves:
  truths:
    - "Every YAML config file on cake-spectrum and cake-att has been inspected for legacy parameter names"
    - "A clear list documents which legacy parameters were still in use vs already migrated"
    - "Both containers confirmed running with only modern parameter names (no legacy fallbacks exercised)"
  artifacts:
    - path: ".planning/phases/67/AUDIT.md"
      provides: "Complete audit of legacy vs modern parameter usage across all production configs"
      contains: "Category A"
  key_links:
    - from: ".planning/phases/67/AUDIT.md"
      to: ".planning/phases/67/CONTEXT.md"
      via: "Audit validates CONTEXT.md scouting findings with live container evidence"
      pattern: "spectrum|att|steering"
---

<objective>
Audit production configs on both containers (cake-spectrum, cake-att) for legacy parameter usage, confirm modern parameter adoption, and produce AUDIT.md consumed by phases 68-69.

Purpose: LGCY-01 gates all legacy removal work. Without confirmed production state, phases 68 (dead code) and 69 (fallback removal) risk breaking live configs.
Output: `.planning/phases/67/AUDIT.md` -- table of every audited parameter, its status, and container evidence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67/CONTEXT.md
@configs/spectrum.yaml
@configs/att.yaml
@configs/steering.yaml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: SSH dump and diff production configs against repo</name>
  <files>N/A (read-only SSH inspection)</files>
  <action>
Run the following SSH commands to dump production configs from both containers, then diff against repo configs. Record the output for Task 2.

**Step 1 -- Dump cake-spectrum configs:**
```bash
ssh cake-spectrum 'cat /etc/wanctl/spectrum.yaml'
ssh cake-spectrum 'cat /etc/wanctl/steering.yaml'
```

**Step 2 -- Dump cake-att configs:**
```bash
ssh cake-att 'cat /etc/wanctl/att.yaml'
```

**Step 3 -- Diff each against repo (from project root):**
```bash
ssh cake-spectrum 'cat /etc/wanctl/spectrum.yaml' | diff - configs/spectrum.yaml
ssh cake-spectrum 'cat /etc/wanctl/steering.yaml' | diff - configs/steering.yaml
ssh cake-att 'cat /etc/wanctl/att.yaml' | diff - configs/att.yaml
```

**Step 4 -- Grep for legacy params on containers:**
```bash
ssh cake-spectrum 'grep -n "alpha_baseline\|alpha_load\|bad_samples\|good_samples" /etc/wanctl/*.yaml' || echo "No legacy params found on cake-spectrum"
ssh cake-att 'grep -n "alpha_baseline\|alpha_load\|bad_samples\|good_samples" /etc/wanctl/*.yaml' || echo "No legacy params found on cake-att"
```

**What to report back:**
- Whether each diff showed differences (paste any diff output)
- Whether grep found any legacy parameter names
- Any unexpected config files in /etc/wanctl/ on either container
  </action>
  <verify>
    <automated>echo "CHECKPOINT -- user provides SSH output"</automated>
  </verify>
  <done>SSH output collected: diffs for all 3 config files and legacy param grep results from both containers.</done>
  <resume-signal>Paste the SSH output (diffs and grep results). If configs match and no legacy params found, say "all clean". If differences exist, paste the diff output.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT.md from SSH evidence</name>
  <files>.planning/phases/67/AUDIT.md</files>
  <action>
Create `.planning/phases/67/AUDIT.md` using the SSH evidence from Task 1. Structure:

**Header:** Phase 67 audit, date, purpose (consumed by phases 68-69).

**Section 1 -- Container Config Inventory:**
List every YAML file found on each container with its repo counterpart.

**Section 2 -- Category A Audit (Renamed Parameters):**
Table with columns: Parameter (Legacy Name) | Modern Replacement | spectrum.yaml | att.yaml | steering.yaml | Status

Rows:
- `alpha_baseline` | `baseline_time_constant_sec` | Not present (modern used) | Not present (modern used) | N/A | MIGRATED
- `alpha_load` | `load_time_constant_sec` | Not present (modern used) | Not present (modern used) | N/A | MIGRATED
- `bad_samples` | N/A (code default) | N/A | N/A | Not present (code defaults to 8) | NOT A FALLBACK (code default)
- `good_samples` | N/A (code default) | N/A | N/A | Not present (code defaults to 15) | NOT A FALLBACK (code default)

**Section 3 -- Category B Audit (Mode Flag):**
- `cake_aware` in steering.yaml: value=true, confirming `_update_state_machine_legacy()` is dead code
- Gates Phase 68 dead code removal (LGCY-02)

**Section 4 -- Repo vs Container Diff:**
Record whether diffs were clean or had differences. If differences found, document them.

**Section 5 -- Notable Findings (Non-blocking):**
- `calibrate.py:562` generates legacy `alpha_baseline`/`alpha_load` format (dev tool, deferred to Phase 69/70)
- List old config files in `configs/` that are Phase 68/LGCY-05 scope (spectrum_config.yaml, att_config.yaml, etc.)

**Section 6 -- Conclusion:**
- LGCY-01 status: SATISFIED or needs remediation
- Clear statement: "Both containers run modern-only parameters. No legacy fallbacks are exercised."
- Gate unlocked for phases 68 and 69

Adjust the above template based on actual SSH evidence from Task 1. If legacy params WERE found, add a "Remediation" section documenting what was fixed (edit configs/*.yaml, redeploy via deploy.sh).
  </action>
  <verify>
    <automated>test -f .planning/phases/67/AUDIT.md && grep -q "Category A" .planning/phases/67/AUDIT.md && grep -q "Category B" .planning/phases/67/AUDIT.md && grep -q "LGCY-01" .planning/phases/67/AUDIT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>AUDIT.md exists with all 6 sections, documents every Category A and B parameter, records container evidence, and states LGCY-01 disposition. File is ready for consumption by phases 68-69.</done>
</task>

</tasks>

<verification>
- AUDIT.md exists in `.planning/phases/67/`
- Every Category A parameter (alpha_baseline, alpha_load, bad_samples, good_samples) has a documented status
- Category B parameter (cake_aware) has a documented status with mode flag value
- Container evidence (SSH output or diff results) is recorded
- LGCY-01 requirement disposition is explicitly stated
</verification>

<success_criteria>
1. Every YAML config file on cake-spectrum and cake-att has been inspected for legacy parameter names (SSH evidence recorded)
2. A clear list documents which legacy parameters were still in use vs already migrated (Category A and B tables)
3. Both containers confirmed running with only modern parameter names (no legacy fallbacks exercised) OR remediation completed and re-verified
</success_criteria>

<output>
After completion, create `.planning/phases/67/67-01-SUMMARY.md`
</output>
