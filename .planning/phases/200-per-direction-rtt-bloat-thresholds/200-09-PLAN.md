---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md
autonomous: false
requirements:
  - VALN-06
user_setup: []

must_haves:
  truths:
    - "A written hypothesis exists explaining why the v1.41 controller (42/105 ms thresholds, 18 Mbps ceiling, factor_down_yellow 0.98) hit 122 floor cycles in 900s under saturated iperf3 -P4 UL on Spectrum (gap text: 'The 10-15 min saturated iperf3 -P4 UL canary at 18 Mbit completes without the UL controller collapsing to the 8 Mbit floor in any cycle')."
    - "The hypothesis enumerates at least 3 candidate causes with evidence pointers and selects ONE remediation direction for Plan 200-10 to implement."
    - "The user has explicitly approved the selected remediation direction at the [BLOCKING] checkpoint before any code is touched."
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md"
      provides: "Root-cause hypothesis + selected remediation direction with operator approval"
      contains: "Selected remediation:"
  key_links:
    - from: "canary/20260503T215734Z/verdict.json"
      to: "200-09-HYPOTHESIS.md"
      via: "evidence citation (122 floor hits, sample distribution, state distribution)"
      pattern: "20260503T215734Z"
    - from: "200-DEPLOY-LOG.md Failure Analysis"
      to: "200-09-HYPOTHESIS.md candidate-causes section"
      via: "explicit reference to bimodal oscillation finding"
      pattern: "bimodal|oscillat"
---

<objective>
Diagnose, in writing, why the v1.41 controller failed the saturated UL canary (122 floor hits in 900s) before any code is touched. Produce a hypothesis document that the user reviews and signs off on at a BLOCKING checkpoint. Plan 200-10 will then implement exactly the chosen remediation.

Purpose: CLAUDE.md mandates conservative changes to the production control path. We do not edit `wan_controller.py` until the failure mode is understood and the operator has approved a specific direction. This plan is investigation + checkpoint only — zero code changes.

Output: `200-09-HYPOTHESIS.md` with candidate causes, evidence pointers, the selected remediation direction, and operator approval recorded inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260503T215734Z/verdict.json
@docs/ARCHITECTURE.md
@configs/spectrum.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 200-09-HYPOTHESIS.md candidate-cause analysis</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md (Failure Analysis section, lines 183-200; bimodal sample distribution, 53% ceiling / 14% floor / 33% transitional, YELLOW 59%)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260503T215734Z/verdict.json (122 floor hits, 900s loaded window)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260503T215734Z/loaded_capture.ndjson (run `wc -l` and sample first/last 5 lines to confirm sample shape)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md (D-05 spectrum YAML adoption rationale; deferred ideas: DOCSIS-aware UL congestion mode, step_up_mbps tuning)
    - .planning/spectrum-inline-native-18-upload-test-2026-04-29.md (operator note that recommended "upload-specific higher target/warn thresholds if supported" — the source for the failed code change)
    - docs/ARCHITECTURE.md (control model: RTT-delta based, baseline frozen during load, decreases immediate / increases sustained-cycle gated, 4-state DL / 3-state UL)
    - src/wanctl/wan_controller.py (UL adjust_3state path: locate the function, read it once, do not modify)
  </read_first>
  <action>
Create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md` with this exact structure:

```markdown
# Phase 200 Plan 09 — Canary Failure Root-Cause Hypothesis

**Author:** the agent (gsd-planner gap-closure)
**Date:** <today UTC>
**Status:** awaiting operator approval at Plan 09 checkpoint
**Inputs:** verdict.json (122 floor hits / 900s), 200-DEPLOY-LOG.md Failure Analysis, 200-CONTEXT.md D-05 rationale, .planning/spectrum-inline-native-18-upload-test-2026-04-29.md

## What We Observed

- 900s saturated `iperf3 -P4` UL load against Spectrum at 18 Mbit ceiling, v1.41 binary, 42/105 ms UL thresholds, factor_down_yellow=0.98.
- 886 /health samples in loaded window. Distribution: 18.0 Mbps (ceiling) 470 (53%); 8.0 Mbps (floor) 122 (14%); 8.2-17.9 Mbps (transitional) 294 (33%).
- UL state: GREEN 306 (35%), YELLOW 522 (59%), RED 58 (7%).
- Bimodal: controller actively oscillates ceiling ↔ floor; does not settle at any intermediate equilibrium.
- 122 floor cycles ≈ 1 collapse every 7.4s.

## Candidate Causes (≥ 3, ranked by likelihood)

### C1: Insufficient shaping headroom on DOCSIS upstream
[State the hypothesis: 18 Mbit shaped rate is too close to the modem's actual provisioned upstream, so the wanctl-imposed ceiling does not absorb bufferbloat — the modem's queue does, and queue depth there routinely exceeds 105 ms regardless of shaping rate. Cite 200-DEPLOY-LOG.md "Root cause hypothesis" wording. Evidence: bimodal pattern (would show gradual taper if shaping margin were sufficient).]

### C2: factor_down_yellow=0.98 still cascades to floor on YELLOW dwell
[State the hypothesis: 59% YELLOW samples implies persistent decay; even at 0.98 multiplicative, repeated YELLOW cycles accumulate (0.98^N) until clamped at floor. 18 → 8 Mbps takes log(8/18)/log(0.98) ≈ 40 cycles; at 50ms cycle that's 2s — consistent with 1 collapse/7.4s including recovery. Pointer: src/wanctl/wan_controller.py UL adjust path.]

### C3: 42/105 ms thresholds still trigger because UL queue delta exceeds 105 ms under saturation
[State the hypothesis: the canary failure rejects the simple "wider thresholds fix it" hypothesis. Either DOCSIS UL queue delay routinely exceeds 105 ms (RED-trigger), or baseline RTT is being polluted during load. Pointer: 200-DEPLOY-LOG.md notes baseline RTT path bug means we cannot directly verify baseline integrity from this run.]

### C4 (optional): Hysteresis floor-guard absent
[State the hypothesis: there is no rule that says "do not decay below floor if RTT delta is already below target". Once RED triggers, controller decays without reasserting whether the queue actually warrants it. This is the "missing brake" hypothesis.]

## Selected Remediation Direction (one of):

- **R1 — Lower ceiling further (12-14 Mbps)**: pure YAML; shaping headroom hypothesis. **Pros:** no controller change, conservative under CLAUDE.md. **Cons:** sacrifices ~4-6 Mbps peak throughput on Spectrum; doesn't address C2/C4 if those are real.
- **R2 — UL floor-guard hysteresis term**: add a per-direction config-gated rule that refuses to decay UL rate below floor + epsilon if RTT delta is < target_bloat_ms (the C4 brake). Code change in `wan_controller.py` UL adjust path; gated on a new config flag so non-Spectrum deployments are byte-identical.
- **R3 — Per-direction step-down clamp under saturated load**: cap multiplicative decay to N consecutive YELLOW cycles before re-evaluating; effectively bounds the C2 cascade. Code change in `wan_controller.py` UL adjust path; gated on a new config flag.
- **R4 — Combine R1 + R2**: lower ceiling to 14 Mbps AND add the floor-guard. Hedged.

**Recommended:** [Pick ONE based on evidence. Note: under CLAUDE.md "stability > safety > clarity > elegance", the cheapest fix that closes the gap is preferred. R1 is pure YAML and is the smallest possible change. If C1 dominates, R1 alone may close VALN-06.]

## What This Plan Does NOT Decide

- Final ceiling number, factor_down_yellow value, or threshold values: those are Plan 200-10's task.
- Whether to also adopt the deferred DOCSIS-aware UL congestion mode: that remains a separate v1.42-class deliverable per CONTEXT.md Deferred Ideas.

## Constraints That Plan 200-10 Must Honor

- No DL behavior changes (D-06 lock).
- No `/health` schema changes.
- No `initialize_cake` refactor.
- Architectural spine: RTT-delta based decisions, frozen baseline under load, decreases immediate / increases sustained-cycle gated, 3-state UL preserved.
- Byte-identical behavior when new keys absent (non-Spectrum deployments unchanged).
- Hot-path slice MUST stay green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py`.

## Open Questions for Operator

1. Are you willing to lower Spectrum UL ceiling below 18 Mbps to close VALN-06? If yes, to what value? (R1 / R4)
2. Is a new operator-tunable controller flag acceptable in v1.41, or must the fix be config-only? (R2 / R3 vs R1)
3. Approve which remediation direction (R1, R2, R3, R4)? Plan 200-10 implements only the approved direction.

## Operator Approval

[Filled in at Plan 09 checkpoint:]
- Approved direction: __________
- Rationale: __________
- Date / signal: __________
```

Use today's UTC date in the header. Fill each candidate-cause section with concrete evidence pointers — no hand-waving. The "Recommended" note must pick ONE direction with reasoning. Do not skip C4 — the missing-brake hypothesis is the cheapest controller change if R1 alone is rejected.

The 200-09-HYPOTHESIS.md file is investigation only. No source code edits. No tests. No production touch.
  </action>
  <verify>
    <automated>test -f .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md &amp;&amp; grep -q "Selected Remediation Direction" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md &amp;&amp; grep -cE '^### C[1-4]:' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md | awk '{ exit ($1 &gt;= 3 ? 0 : 1) }' &amp;&amp; grep -q "20260503T215734Z" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md</automated>
  </verify>
  <done>HYPOTHESIS.md exists with at least 3 candidate causes, a Selected Remediation Direction section, and citations to canary verdict and DEPLOY-LOG. No source files modified.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Operator approves remediation direction for Plan 200-10</name>
  <decision>Which remediation direction (R1 / R2 / R3 / R4 from 200-09-HYPOTHESIS.md) does Plan 200-10 implement?</decision>
  <context>
Phase 200 is BLOCKED on VALN-06. The v1.41 controller, with 42/105 ms UL thresholds and 18 Mbps ceiling, hit 122 floor cycles in 900s of saturated UL load. Plan 200-09 has authored a hypothesis with at least 3 candidate causes and 4 candidate remediations. Plan 200-10 must implement exactly the direction approved here. CLAUDE.md mandates conservative production-controller changes; the user must endorse the direction before code is touched.

Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md` end-to-end before answering.
  </context>
  <options>
    <option id="R1">
      <name>R1 — YAML-only: lower Spectrum UL ceiling further (e.g., 12-14 Mbps)</name>
      <pros>No controller change. Smallest possible diff. Reversible by editing one YAML value. Conservative under CLAUDE.md stability rule.</pros>
      <cons>Sacrifices Spectrum UL peak throughput. Doesn't address controller-side oscillation behavior if it recurs at the lower ceiling.</cons>
    </option>
    <option id="R2">
      <name>R2 — Add UL floor-guard hysteresis term (refuse to decay below floor if RTT delta &lt; target)</name>
      <pros>Addresses C4 (missing brake) directly. Gated on a new config flag so non-Spectrum deployments stay byte-identical. Operator-tunable.</pros>
      <cons>Touches src/wanctl/wan_controller.py UL adjust path. Higher review burden. Needs new tests.</cons>
    </option>
    <option id="R3">
      <name>R3 — Per-direction step-down clamp (bound consecutive YELLOW decay cycles)</name>
      <pros>Addresses C2 (factor_down cascade). Gated on a new config flag. Bounds the worst-case decay.</pros>
      <cons>Touches UL adjust path. Picks a magic number (clamp count). Needs new tests.</cons>
    </option>
    <option id="R4">
      <name>R4 — R1 + R2 combined: lower ceiling AND add floor-guard</name>
      <pros>Hedged; addresses both C1 and C4. Higher chance of canary pass on first retry.</pros>
      <cons>Largest diff. Mixes config and controller change in one milestone.</cons>
    </option>
  </options>
  <resume-signal>Reply with "R1", "R2", "R3", or "R4" (or describe a different direction with rationale; planner will revise Plan 09 hypothesis and reissue checkpoint). The selection becomes the input to Plan 200-10's <action> block.</resume-signal>
</task>

</tasks>

<verification>
- `200-09-HYPOTHESIS.md` exists, has ≥ 3 candidate causes, lists 4 remediation options, cites verdict.json and DEPLOY-LOG.
- No source code, test, config, script, or doc file modified by this plan.
- Operator approval recorded inline in HYPOTHESIS.md (filled at checkpoint).
</verification>

<success_criteria>
1. `200-09-HYPOTHESIS.md` is committed.
2. Operator has selected one of R1/R2/R3/R4 (or supplied a new direction) and the selection is recorded in the file.
3. Hot-path test slice was not touched (no risk to introduce); confirm no `src/`, `tests/`, `configs/`, or `scripts/` files appear in `git status`.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-SUMMARY.md` recording the operator's approved direction. Plan 200-10 will read both files.
</output>
