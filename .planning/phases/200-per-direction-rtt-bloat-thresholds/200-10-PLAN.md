---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 10
type: execute
wave: 2
depends_on:
  - "200-09"
gap_closure: true
files_modified:
  - configs/spectrum.yaml
  - src/wanctl/wan_controller.py
  - src/wanctl/autorate_config.py
  - tests/test_wan_controller.py
  - tests/test_autorate_config.py
  - tests/conftest.py
  - CHANGELOG.md
autonomous: false
requirements:
  - VALN-06
  - ARB-05
user_setup: []

must_haves:
  truths:
    - "The controller change (or YAML change) approved at Plan 200-09 checkpoint has landed; Spectrum's UL configuration plus controller behavior is plausibly capable of passing the saturated UL canary (verified by re-canary in Plan 200-14, gap text: 'The 10-15 min saturated iperf3 -P4 UL canary at 18 Mbit completes without the UL controller collapsing to the 8 Mbit floor in any cycle')."
    - "Behavior remains byte-identical for non-Spectrum deployments when no new config keys are present (architectural spine: byte-identical fallback)."
    - "Hot-path test slice passes: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py`."
  artifacts:
    - path: "configs/spectrum.yaml"
      provides: "Updated Spectrum UL config per approved remediation"
      contains: "continuous_monitoring:"
    - path: "src/wanctl/wan_controller.py"
      provides: "Controller change per approved remediation (only if R2/R3/R4)"
    - path: "tests/test_wan_controller.py"
      provides: "Regression tests for the new behavior"
  key_links:
    - from: "200-09-HYPOTHESIS.md approved direction"
      to: "Plan 200-10 implementation"
      via: "1:1 mapping; no scope creep"
      pattern: "Selected Remediation"
    - from: "configs/spectrum.yaml"
      to: "src/wanctl/wan_controller.py UL adjust path"
      via: "Config schema in autorate_config.py"
      pattern: "continuous_monitoring.upload"
---

<objective>
Implement EXACTLY the remediation direction the operator approved at Plan 200-09's BLOCKING checkpoint. Nothing more, nothing less. The controller change must respect the architectural spine (RTT-delta based, frozen baseline under load, decreases immediate / increases sustained, byte-identical fallback) and must keep the hot-path test slice green.

Purpose: Close the canary failure gap (122 floor hits) so Plan 200-14 can re-run the saturated UL canary and have a chance of passing. Production-controller changes require operator approval and conservative scope per CLAUDE.md.

Output: Updated YAML and/or controller code, new regression tests, CHANGELOG entry. No DL behavior changes. No `/health` schema changes. No `initialize_cake` refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-01-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-04-SUMMARY.md
@docs/ARCHITECTURE.md
@configs/spectrum.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Confirm approved remediation direction before any edits</name>
  <what-built>Plan 200-09 produced 200-09-HYPOTHESIS.md with an operator-approved remediation direction (one of R1, R2, R3, R4, or a substituted direction).</what-built>
  <how-to-verify>
1. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md` "Operator Approval" section.
2. Confirm the recorded direction matches what you intend to implement now.
3. If the direction has shifted since plan-time, STOP and re-run `/gsd-plan-phase --gaps` to revise this plan rather than improvise.
  </how-to-verify>
  <resume-signal>Reply "approved — implement R{N}" or describe corrections.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the approved remediation (conditional on R selection)</name>
  <files>configs/spectrum.yaml, src/wanctl/wan_controller.py, src/wanctl/autorate_config.py, tests/test_wan_controller.py, tests/test_autorate_config.py, tests/conftest.py, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md (re-read approved direction)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-01-SUMMARY.md (D-03 per-key presence flag pattern; reuse this exact shape for any new flag)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-04-SUMMARY.md (Spectrum YAML 18/0.98/42/105 baseline; this is what is currently in tree)
    - docs/ARCHITECTURE.md (control model; UL is 3-state: GREEN/YELLOW/RED; baseline frozen under load)
    - src/wanctl/wan_controller.py (locate UL adjust path: search for "adjust_3state" or the UL state-machine function; read it ONCE; identify the YELLOW decay branch and the floor clamp)
    - src/wanctl/autorate_config.py (D-03 flags `_upload_target_bloat_ms_explicit`, `_upload_warn_bloat_ms_explicit` — copy this shape if the approved direction adds a new flag)
    - tests/test_wan_controller.py (test_upload_thresholds_explicit_per_key_independence — pattern for new regression tests)
    - tests/conftest.py (mock_autorate_config — extend if a new attribute is added)
  </read_first>
  <behavior>
Behavior depends on the approved remediation direction. Implement EXACTLY this branch:

**If R1 (YAML-only) approved:**
  - Test 1: Loading `configs/spectrum.yaml` resolves `continuous_monitoring.upload.ceiling_mbps == &lt;new value approved at checkpoint, e.g., 14&gt;`.
  - Test 2: Hot-path slice (4 files) stays green; no controller code change.
  - Test 3: SAFE-05 pin counts in `tests/test_phase_195_replay.py` remain at warn_bloat=6 / target_bloat=6 (no controller diff means no count change).

**If R2 (UL floor-guard hysteresis term) approved:**
  - Test 1: With new flag absent, UL adjust behavior is byte-identical to current main (regression: replay an existing test_wan_controller scenario unchanged).
  - Test 2: With `continuous_monitoring.upload.floor_guard_enabled: true` + RTT delta below target, UL rate does NOT decay below floor + epsilon even when YELLOW dwell would otherwise trigger decay.
  - Test 3: With flag enabled but RTT delta above warn, UL rate decay proceeds normally (the guard only fires when queue is healthy).
  - Test 4: Per-key presence flag `_upload_floor_guard_enabled_explicit` follows D-03 pattern.

**If R3 (per-direction step-down clamp) approved:**
  - Test 1: With new flag absent, behavior byte-identical to current main.
  - Test 2: With `continuous_monitoring.upload.consecutive_decay_clamp: N`, more than N consecutive YELLOW cycles do NOT compound multiplicatively — N+1th YELLOW holds rate or re-evaluates instead of multiplying by factor_down_yellow again.
  - Test 3: Clamp resets to zero on a GREEN cycle.
  - Test 4: Per-key presence flag follows D-03 pattern.

**If R4 (R1 + R2 combined) approved:** union of R1 + R2 test sets.
  </behavior>
  <action>
Branch on the operator-approved direction recorded in `200-09-HYPOTHESIS.md` "Operator Approval" section.

**If R1 — YAML-only:**
1. Edit `configs/spectrum.yaml`: change `continuous_monitoring.upload.ceiling_mbps` from `18` to the operator-approved value (state the value explicitly in the commit message). Leave `factor_down_yellow: 0.98`, `target_bloat_ms: 42`, `warn_bloat_ms: 105` unchanged.
2. Add an entry to `CHANGELOG.md` under v1.41.0 (or a new 1.41.1 patch section if v1.41.0 was already cut — check `pyproject.toml`): "Spectrum UL ceiling lowered &lt;old&gt; → &lt;new&gt; Mbps after 200-09 hypothesis selected R1; operator-approved value addresses C1 (insufficient shaping headroom on DOCSIS upstream)."
3. No source code changes. No new tests required (existing config-load tests still pass). Add one new test in `tests/test_wan_controller.py` or `tests/test_autorate_config.py` only if the new ceiling crosses an existing test's hard-coded expectation; otherwise none.

**If R2 — UL floor-guard hysteresis term:**
1. Add new optional config key `continuous_monitoring.upload.floor_guard_enabled: bool` (default False) to `src/wanctl/autorate_config.py` schema. Follow the exact D-03 shape: record `_upload_floor_guard_enabled_explicit = "floor_guard_enabled" in ul` at config-load time. Resolved attribute `Config.upload_floor_guard_enabled` defaults to `False` when absent.
2. Extend `tests/conftest.py::mock_autorate_config` with `upload_floor_guard_enabled = False` and `_upload_floor_guard_enabled_explicit = False` so existing tests stay byte-identical.
3. Locate the UL adjust path in `src/wanctl/wan_controller.py` (the 3-state UL function). At the point where the controller is about to write a new UL rate that would equal floor (or descend below floor + 0.5 Mbps), add this guard:
   - If `self.upload_floor_guard_enabled is True` AND the latest measured RTT delta is &lt; the resolved `target_delta`: skip the decay write for this cycle, log INFO "phase200 UL floor-guard held rate at &lt;current&gt; Mbps (RTT delta &lt;X&gt; ms &lt; target &lt;Y&gt; ms)" via `self.logger` (per-WAN logger — follow the Plan 06 commit `417e2b9` pattern).
   - Otherwise, behavior is unchanged.
4. Edit `configs/spectrum.yaml`: add `floor_guard_enabled: true` under `continuous_monitoring.upload`.
5. Add tests in `tests/test_wan_controller.py`:
   - `test_upload_floor_guard_byte_identical_when_flag_absent` — fixture without the key behaves exactly like current behavior (capture pre-change rate sequence in a fixture, assert post-change matches).
   - `test_upload_floor_guard_holds_rate_when_rtt_healthy` — flag on, RTT delta below target, decay-trigger condition present → rate stays at or above floor + epsilon.
   - `test_upload_floor_guard_releases_when_rtt_above_warn` — flag on, RTT delta above warn → guard does not fire, decay proceeds.
   - `test_upload_floor_guard_explicit_flag_independence` (D-03 pattern) — flag set vs absent yields different `_upload_floor_guard_enabled_explicit` value.
6. Add a test in `tests/test_autorate_config.py::TestLoadUploadFloorGuardConfig` covering default, explicit-True, and explicit-False.
7. CHANGELOG entry under v1.41.0 (or 1.41.1): "Added optional `continuous_monitoring.upload.floor_guard_enabled` (default False, byte-identical fallback). When True, refuses to decay UL rate below floor + 0.5 Mbps if RTT delta is below target. Closes Plan 200-09 R2."

**If R3 — per-direction step-down clamp:**
1. Add new optional config key `continuous_monitoring.upload.consecutive_decay_clamp: int` (default 0 = disabled, range 1-50) to `src/wanctl/autorate_config.py` schema. Follow D-03 presence-flag shape (`_upload_consecutive_decay_clamp_explicit`).
2. Extend `tests/conftest.py::mock_autorate_config` with `upload_consecutive_decay_clamp = 0`.
3. In `src/wanctl/wan_controller.py` UL adjust path: maintain a `self._ul_consecutive_yellow_decay = 0` counter. Increment on each YELLOW cycle that would multiply rate by factor_down_yellow; reset to 0 on any GREEN cycle. If `self.upload_consecutive_decay_clamp &gt; 0` and the counter would exceed the clamp, hold rate this cycle (log INFO "phase200 UL decay clamp held rate at &lt;current&gt; Mbps after &lt;N&gt; consecutive YELLOW cycles") via per-WAN logger.
4. Edit `configs/spectrum.yaml`: add `consecutive_decay_clamp: &lt;N approved at checkpoint&gt;` under `continuous_monitoring.upload`.
5. Add tests in `tests/test_wan_controller.py`:
   - `test_upload_decay_clamp_byte_identical_when_zero` — clamp=0 → behavior unchanged.
   - `test_upload_decay_clamp_holds_after_n_yellow` — clamp=3, 4 consecutive YELLOW cycles → 4th cycle does not decay further.
   - `test_upload_decay_clamp_resets_on_green` — clamp=3, YELLOW×3, GREEN×1, YELLOW×3 → 4th YELLOW after the second run is allowed (counter reset).
6. Add a config test under `TestLoadUploadDecayClampConfig`.
7. CHANGELOG entry per same pattern as R2.

**If R4 — R1 + R2 combined:** apply BOTH R1 and R2 changes above. Single CHANGELOG entry combining both. Single commit.

Common to all branches:
- Do NOT modify DL paths, `/health` shape, arbitration logic, or `initialize_cake`.
- Do NOT change SAFE-05 expected counts in `tests/test_phase_195_replay.py` unless `wan_controller.py` introduces new occurrences of `warn_bloat` / `target_bloat` literals (count + commit-message note in that case).
- Per-WAN logger: any new INFO line MUST use `self.logger` (the per-WAN configured logger), not `logging.getLogger(__name__)`. The Plan 06 attempt-1 abort proved module-scope INFO is dropped in production.
- Run formatter and linter before commit:
  - `.venv/bin/ruff format src/ tests/`
  - `.venv/bin/ruff check src/ tests/`
  - `.venv/bin/mypy src/wanctl/`
- Hot-path slice MUST pass:
  - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`
- Targeted tests (R2/R3/R4) MUST pass:
  - `.venv/bin/pytest -o addopts='' tests/test_wan_controller.py -k "floor_guard or decay_clamp" -q` (whichever applies)
  - `.venv/bin/pytest -o addopts='' tests/test_autorate_config.py -q`
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_autorate_config.py tests/test_phase_195_replay.py -q &amp;&amp; .venv/bin/ruff check src/ tests/ &amp;&amp; .venv/bin/mypy src/wanctl/ &amp;&amp; grep -q "1.41" CHANGELOG.md</automated>
  </verify>
  <done>
- Approved remediation landed; tests new and existing pass; ruff/mypy clean.
- `git diff --stat` shows changes only in approved files (no DL, /health, or initialize_cake touch).
- CHANGELOG.md has an entry citing Plan 200-09 / 200-10 and the approved direction.
  </done>
</task>

</tasks>

<verification>
- Hot-path slice green.
- For R2/R3/R4 branches: byte-identical fallback test (`*_byte_identical_when_*`) is present and green.
- `git grep -n "phase200 explicit UL thresholds active" src/wanctl/wan_controller.py` still returns the Plan 06 D-06 line (unchanged).
- `git grep -n "logging.getLogger(__name__)" src/wanctl/wan_controller.py` count not increased by this plan (verify via `git diff main -- src/wanctl/wan_controller.py | grep -c "logging.getLogger(__name__)"` shows 0 added lines matching).
- `/health` JSON shape unchanged: `git diff main -- src/wanctl/health.py 2&gt;/dev/null | wc -l` returns 0 (or whichever file renders /health — the file MUST not be in `git diff`).
</verification>

<success_criteria>
1. Plan 200-09 approved remediation is implemented exactly.
2. Hot-path test slice passes (`.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py`).
3. New regression tests (R2/R3/R4) pass and assert both the new behavior AND byte-identical fallback.
4. No file outside `files_modified` is changed.
5. CHANGELOG cites Plan 200-09/200-10 and the canary failure that motivated the change.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-10-SUMMARY.md` recording: chosen direction (R{N}), exact file diffs (line counts), test additions, and explicit confirmation that DL / /health / initialize_cake were not touched.
</output>
