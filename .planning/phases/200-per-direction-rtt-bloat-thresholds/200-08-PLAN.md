---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 08
type: execute
wave: 8
depends_on: [200-01, 200-02, 200-03, 200-04, 200-05, 200-06, 200-07]
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [ARB-05, SAFE-06, VALN-06, DOCS-03]
must_haves:
  truths:
    - "200-VERIFICATION.md exists with structured frontmatter (status, score, requirements, files_touched) and per-requirement evidence citations"
    - "All four phase requirement IDs (ARB-05, SAFE-06, VALN-06, DOCS-03) are explicitly mapped to satisfying evidence in 200-VERIFICATION.md"
    - "Hot-path test slice runs clean (5 files: cake_signal, queue_controller, wan_controller, health_check, autorate_config)"
    - "200-SUMMARY.md captures the milestone-level outcome: deploy state (accepted | rolled-back | gap-closure-needed), VALN-06 verdict, and milestone closeout recommendation"
    - "REQUIREMENTS.md v1.41 traceability table is updated: each ID either ✓ Satisfied or × Blocked with citation"
    - "STATE.md `Decisions` section appends Phase 200 decisions (D-XX entries actually applied) and `Performance Metrics` records execution times for each plan"
    - "STATE.md `Current Position` updates to reflect Phase 200 closure (or partial / blocked)"
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md"
      provides: "Structured phase verification artifact"
      contains: "status:|score:|requirements:|files_touched:"
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md"
      provides: "Milestone-level closeout summary"
      contains: "Phase 200"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated v1.41 traceability table"
      contains: "ARB-05.*Satisfied|VALN-06.*Satisfied"
    - path: ".planning/STATE.md"
      provides: "Updated session state with Phase 200 closure"
      contains: "Phase 200"
  key_links:
    - from: "200-VERIFICATION.md.requirements"
      to: "Plan 01-07 SUMMARYs and evidence files"
      via: "explicit citations per requirement"
      pattern: "ARB-05|SAFE-06|VALN-06|DOCS-03"
    - from: "200-SUMMARY.md.deploy_state"
      to: "200-DEPLOY-LOG.md and 200-SOAK-LOG.md"
      via: "operator-recorded decisions"
      pattern: "deploy-accepted|rolled-back|gap-closure"
---

<objective>
Close Phase 200 with a structured 200-VERIFICATION.md that maps each phase requirement to satisfying evidence (or BLOCKED state with rationale), produce a 200-SUMMARY.md for the milestone-level audit trail, and update REQUIREMENTS.md and STATE.md to reflect the new state.

Purpose: Without this plan, Phase 200's evidence trail is scattered across 7 SUMMARYs, 1 DEPLOY-LOG, 1 SOAK-LOG, and a dozen JSON evidence files. The verifier (a future GSD phase audit) needs ONE structured artifact (VERIFICATION.md) that says "ARB-05 ✓ via plans 01-02 + tests X/Y/Z; SAFE-06 ✓ via plan 03 + test W; VALN-06 ✓ via canary verdict + soak verdict; DOCS-03 ✓ via CHANGELOG section + docs/CONFIGURATION.md migration note section." Plan 08 produces that structured artifact.

Output: 4 files updated/created. No code-path changes. No production state changes. Pure documentation/state-update commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-01-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-02-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-03-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-04-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-05-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-06-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-07-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md

<interfaces>
<!-- Mirror an existing high-quality VERIFICATION format. Examples in repo: -->

.planning/milestones/v1.40-phases/198-spectrum-cake-primary-b-leg-rerun/198-VERIFICATION.md
.planning/milestones/v1.40-phases/199-obs-02-spec-impl-reconciliation/199-VERIFICATION.md

<!-- Required structured frontmatter shape -->

---
phase: 200-per-direction-rtt-bloat-thresholds
status: passed | passed_with_documented_exceptions | blocked | failed
score: <0.0-1.0>
requirements:
  ARB-05: satisfied | blocked | partial
  SAFE-06: satisfied | blocked | partial
  VALN-06: satisfied | blocked | partial
  DOCS-03: satisfied | blocked | partial
files_touched:
  src: [list]
  tests: [list]
  scripts: [list]
  configs: [list]
  docs: [list]
deploy_state: accepted | rolled-back | gap-closure-pending
canary_verdict_path: ".planning/phases/200-*/canary/<UTC>/verdict.json"
soak_verdict_path: ".planning/phases/200-*/soak/<UTC>/soak-verdict.json"
---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run hot-path test slice + collect evidence file paths</name>
  <files>
    (no files modified — pure data-gathering for Task 2)
  </files>
  <action>
    Step 1 — Hot-path test slice (must pass before VERIFICATION can record status=passed):

    ```bash
    .venv/bin/pytest -o addopts='' \
        tests/test_cake_signal.py \
        tests/test_queue_controller.py \
        tests/test_wan_controller.py \
        tests/test_health_check.py \
        tests/test_autorate_config.py \
        tests/test_phase_195_replay.py \
        -q
    ```

    Capture exit code and pass/fail counts. If exit != 0, surface to operator — Phase 200 cannot record `status=passed` without a clean test slice.

    Step 2 — Collect evidence file paths from Plans 01-07 SUMMARYs:

    ```bash
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-01-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-02-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-03-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-04-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-05-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-06-SUMMARY.md
    cat .planning/phases/200-per-direction-rtt-bloat-thresholds/200-07-SUMMARY.md
    ```

    Pin:
    - canary verdict path + verdict (PASS/FAIL/ABORT)
    - soak verdict path + verdict (PASS/FAIL/ABORT)
    - deploy state (accepted | rolled-back | gap-closure-pending) from 200-DEPLOY-LOG.md and 200-SOAK-LOG.md

    Step 3 — Determine the overall phase status:

    | Canary | Soak | Phase Status | Score |
    |---|---|---|---|
    | PASS | PASS | passed | 1.0 |
    | PASS | FAIL | passed_with_documented_exceptions (24h regression to investigate; deploy is live) | 0.7 |
    | PASS | ABORT/incomplete | passed_with_documented_exceptions (canary primary gate per D-07 satisfied; soak watchdog evidence partial) | 0.8 |
    | FAIL | (n/a, blocked) | blocked | 0.3 |
    | ABORT | (n/a, blocked) | blocked | 0.3 |

    Pin chosen status and score for Task 2.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_autorate_config.py tests/test_phase_195_replay.py -q</automated>
  </verify>
  <done>
    Hot-path test slice passed; all SUMMARYs read; canary + soak verdicts pinned; phase status determined.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 200-VERIFICATION.md and 200-SUMMARY.md</name>
  <files>
    .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
    .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md
  </files>
  <action>
    Step 1 — Write `200-VERIFICATION.md`. Use the structured frontmatter shape from the interfaces block, then a body with one section per requirement. Content shape:

    ```markdown
    ---
    phase: 200-per-direction-rtt-bloat-thresholds
    status: <from Task 1>
    score: <from Task 1>
    requirements:
      ARB-05: satisfied
      SAFE-06: satisfied
      VALN-06: <satisfied | blocked | partial>
      DOCS-03: satisfied
    files_touched:
      src:
        - src/wanctl/autorate_config.py
        - src/wanctl/check_config_validators.py
        - src/wanctl/wan_controller.py
        - src/wanctl/__init__.py
      tests:
        - tests/conftest.py
        - tests/test_autorate_config.py
        - tests/test_wan_controller.py
        - tests/test_phase_195_replay.py
      scripts:
        - scripts/phase200-saturation-canary.sh
        - scripts/phase200-saturation-canary.env.example
        - scripts/phase200-soak-capture.sh
        - scripts/phase200-soak-capture.env.example
        - scripts/phase200-soak-analyze.py
      configs:
        - configs/spectrum.yaml
      docs:
        - CHANGELOG.md
        - docs/CONFIGURATION.md
      build:
        - pyproject.toml
        - docker/Dockerfile
    deploy_state: <accepted | rolled-back | gap-closure-pending>
    canary_verdict_path: <pinned in Task 1>
    soak_verdict_path: <pinned in Task 1>
    ---

    # Phase 200 Verification

    Verifier: Plan 200-08 (auto)
    Verified at: <UTC ISO 8601>

    ## Requirement: ARB-05 — Per-direction UL RTT bloat thresholds

    **Status:** Satisfied

    Evidence:

    - Plan 01 (200-01-SUMMARY.md): Replaced value-derived `_upload_thresholds_explicit`
      with per-key presence flags `_upload_target_bloat_ms_explicit` and
      `_upload_warn_bloat_ms_explicit`. `_apply_threshold_param` now gates UL field
      writes on each flag independently.
    - Tests passing:
      - `tests/test_autorate_config.py::TestLoadUploadThresholdConfig::test_upload_thresholds_default_to_global_thresholds`
      - `tests/test_autorate_config.py::TestLoadUploadThresholdConfig::test_upload_thresholds_can_override_global_thresholds`
      - `tests/test_autorate_config.py::TestLoadUploadThresholdConfig::test_upload_thresholds_must_be_ordered`
      - `tests/test_wan_controller.py::test_upload_thresholds_use_upload_specific_config`
      - `tests/test_wan_controller.py::test_upload_thresholds_explicit_when_value_equal_to_global`
      - `tests/test_wan_controller.py::test_upload_thresholds_explicit_per_key_independence`
    - Plan 02 (200-02-SUMMARY.md): SAFE-05 expected counts updated for v1.41
      per-direction wiring, with the other 7 v1.40 pins held fixed (no silent drift).

    ## Requirement: SAFE-06 — Validator unknown-key warning

    **Status:** Satisfied

    Evidence:

    - Plan 03 (200-03-SUMMARY.md): Added startup-time WARNING emission via
      `Config._warn_unknown_continuous_monitoring_keys` against `KNOWN_AUTORATE_PATHS`.
    - Tests passing:
      - `tests/test_autorate_config.py::TestSafe06UnknownKeyWarning::test_unknown_continuous_monitoring_key_warns`
      - `tests/test_autorate_config.py::TestSafe06UnknownKeyWarning::test_known_continuous_monitoring_keys_do_not_warn`
    - Production sanity: `configs/spectrum.yaml` and `configs/att.yaml` produce
      zero unknown-key warnings against the v1.41 KNOWN_AUTORATE_PATHS (per
      200-03-SUMMARY.md `shipped-configs-clean` field).

    ## Requirement: VALN-06 — Spectrum UL saturation canary + 24h soak

    **Status:** <satisfied | partial | blocked — pin from Task 1>

    Evidence:

    - Canary (Plan 06): `<canary_verdict_path>` verdict=`<value>`, UL floor hits=`<int>`.
    - 24h soak (Plan 07): `<soak_verdict_path>` verdict=`<value>`, mean UL hysteresis=`<float>/60s` (vs degraded baseline 31/60s).
    - Deploy state: `<accepted | rolled-back | gap-closure-pending>`
    - DEPLOY-LOG: `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md`
    - SOAK-LOG: `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md`

    Per D-07: the saturation canary is the primary deploy gate. Soak is the
    regression watchdog. <Document the relationship as actually observed in this run.>

    ## Requirement: DOCS-03 — CHANGELOG + docs/CONFIGURATION migration note

    **Status:** Satisfied

    Evidence:

    - `CHANGELOG.md` 1.41.0 section documents the new keys, SAFE-06 warning, and
      the SIGUSR1-vs-restart migration note. Verified by `grep -E "1\.41\.0" CHANGELOG.md`.
    - `docs/CONFIGURATION.md` carries the per-direction upload thresholds section
      with the explicit migration note. Verified by `grep -E "SIGUSR1.*restart|systemctl restart wanctl@" docs/CONFIGURATION.md`.
    - Version surfaces synchronized at 1.41.0 across `pyproject.toml`,
      `src/wanctl/__init__.py`, and `docker/Dockerfile`.

    ## Hot-Path Test Slice

    Command:
    ```
    .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_autorate_config.py tests/test_phase_195_replay.py -q
    ```

    Result: <pass-count> passed, <skip-count> skipped, <fail-count> failed.

    ## Decisions Applied

    | Decision | Plan | Verified By |
    |---|---|---|
    | D-01 | 01, 04 | YAML location + schema entries |
    | D-02 | 01 | mock_autorate_config getattr fallback test |
    | D-03 | 01 | per-key presence flag tests #5 + #6 |
    | D-04 | 01 | TestLoadUploadThresholdConfig::test_upload_thresholds_must_be_ordered |
    | D-05 | 04 | configs/spectrum.yaml D-05 settings verified by yq |
    | D-06 | (all) | DL + /health + arbitration paths untouched (test_phase_195_replay 7 non-UL pins held) |
    | D-07 | 06, 07 | canary primary gate + 24h soak watchdog |
    | D-08 | 03 | TestSafe06UnknownKeyWarning |
    | D-09 | 02 | SAFE-05 expected count update |
    | D-10 | 06 | rollback protocol pre-positioned in canary verdict.json + DEPLOY-LOG.md |
    | D-11 | 04 | three version surfaces synchronized at 1.41.0 |
    | D-12 | 04 | CHANGELOG + docs/CONFIGURATION migration note |
    | D-13 | 01-03 | 4+1 working-tree tests + 2 D-03 tests + 1 SAFE-06 test + 1 SAFE-05 update |

    ## Out-of-Scope Confirmation (from CONTEXT D-06)

    - DL behavior unchanged: confirmed by `test_safe05_threshold_name_counts_are_unchanged`
      which holds 7 non-UL pins constant.
    - No new /health field: `grep -n "signal_arbitration\|cake_signal" src/wanctl/health_check.py` shows no v1.41 additions.
    - No `initialize_cake` refactor: `git diff <pre-phase-200-sha>..HEAD -- src/wanctl/wan_controller.py | grep "def initialize_cake"` shows no signature change.
    - No coverage push: not measured here; deferred per CONTEXT.
    - No ATT canary (VALN-05b): inherited deferral; not addressed in v1.41.
    ```

    Step 2 — Write `200-SUMMARY.md`. Operator-readable milestone-level summary.

    ```markdown
    # Phase 200: Per-Direction RTT Bloat Thresholds — Summary

    **Phase:** 200 (v1.41 single-phase milestone)
    **Started:** <date Plan 01 committed>
    **Closed:** <date Plan 08 committed>
    **Duration:** <wall-clock>
    **Status:** <from VERIFICATION.md>
    **Deploy state:** <accepted | rolled-back | gap-closure-pending>

    ## Outcome

    <2-4 sentence summary of what shipped and how production behaved.>

    Example PASS-state phrasing:
    > Phase 200 shipped per-direction UL RTT bloat thresholds (target_bloat_ms /
    > warn_bloat_ms under continuous_monitoring.upload). Spectrum production
    > adopted 42/105 ms UL thresholds at the latency-first 18 Mbit ceiling.
    > Saturation canary recorded zero UL collapse-to-floor cycles in the 15-min
    > saturated window; 24h regression soak measured mean UL hysteresis
    > suppressions of <X>/60s, down from the 31/60s degraded baseline.
    > VALN-06 satisfied; v1.41 milestone closes.

    Example FAIL-state phrasing:
    > Phase 200 deployed but failed the saturation canary at <UL_FLOOR_HITS>
    > UL collapse-to-floor cycles. D-10 rollback executed at <time>. v1.41 binary
    > is NOT live in production. Gap closure phase opened to investigate
    > whether 42/105 ms thresholds are insufficient or whether per-direction
    > wiring requires additional UL-only control surfaces (factor_down,
    > step_up_mbps).

    ## Requirements

    | ID | Status | Plan(s) | Evidence |
    |---|---|---|---|
    | ARB-05 | <satisfied/blocked> | 01, 02 | Per-key presence flags; 6 tests |
    | SAFE-06 | <satisfied/blocked> | 03 | Startup WARNING; 2 tests |
    | VALN-06 | <satisfied/partial/blocked> | 05, 06, 07 | canary verdict + soak verdict |
    | DOCS-03 | <satisfied/blocked> | 04 | CHANGELOG + migration note |

    ## Key Files Shipped

    See `200-VERIFICATION.md` `files_touched` block.

    ## Operator Decisions Worth Carrying Forward

    - <Any operator-keyed insights from DEPLOY-LOG.md or SOAK-LOG.md>
    - <e.g., "the rollback tarball at /opt/wanctl-prephase200-<UTC>.tar.gz is retained until <date> for additional safety">

    ## Followups (if any)

    - <Open todos generated during Phase 200 — e.g., DOCSIS-aware UL congestion mode if soak revealed residual oscillation>
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md && test -f .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md && grep -qE "ARB-05|SAFE-06|VALN-06|DOCS-03" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md && grep -qE "status:|score:" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </verify>
  <done>
    200-VERIFICATION.md exists with structured frontmatter and per-requirement evidence. 200-SUMMARY.md captures the milestone-level outcome.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md and STATE.md</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    Step 1 — Edit `.planning/REQUIREMENTS.md`. In the `### v1.41` traceability table (currently shows "Pending" for all four IDs), update each row to reflect the actual state from `200-VERIFICATION.md`:

    ```markdown
    ### v1.41

    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | ARB-05  | Phase 200 | <Satisfied | Blocked> (200-VERIFICATION.md) |
    | SAFE-06 | Phase 200 | <Satisfied | Blocked> (200-VERIFICATION.md) |
    | VALN-06 | Phase 200 | <Satisfied | Partial | Blocked> (200-VERIFICATION.md) |
    | DOCS-03 | Phase 200 | <Satisfied | Blocked> (200-VERIFICATION.md) |
    ```

    Also update the requirement entries themselves (lines ~16-29) — change `[ ]` to `[x]` for each satisfied requirement:

    ```markdown
    - [x] **ARB-05**: ...   ← if satisfied
    - [x] **SAFE-06**: ...  ← if satisfied
    - [x] **VALN-06**: ...  ← if satisfied (or leave [ ] if partial/blocked)
    - [x] **DOCS-03**: ...  ← if satisfied
    ```

    Step 2 — Edit `.planning/STATE.md`:

    a) Frontmatter — bump:
    ```yaml
    last_updated: "<UTC ISO 8601 now>"
    last_activity: <YYYY-MM-DD>
    progress:
      total_phases: 1
      completed_phases: <0|1 — 1 if VERIFICATION status=passed; 0 otherwise>
      total_plans: 8
      completed_plans: <count of plans actually committed>
      percent: <100 * completed_plans / 8>
    ```

    Also update `status:` from `planning` to `executing` or `archived` based on outcome:
    - Phase 200 status=passed → set milestone `status: executing` (until milestone-archive is run separately) or `status: ready_to_archive`
    - Phase 200 status=blocked → keep `status: executing` and surface in Blockers

    b) Append to `## Decisions` (in source order, after the v1.40 entries):

    ```markdown
    - [Phase 200]: Per-key presence flags (_upload_target_bloat_ms_explicit, _upload_warn_bloat_ms_explicit) gate live-tuning writes independently; value-derived single flag is the wrong shape (D-03 fix).
    - [Phase 200]: SAFE-05 expected counts for warn_bloat/target_bloat bumped intentionally for v1.41 per-direction wiring; the other 7 v1.40 pins held fixed as drift detector (D-09).
    - [Phase 200]: Validator emits WARNING (not hard-reject) on unknown continuous_monitoring keys at startup; closes the silent-ignore gap that allowed prod spectrum.yaml to carry 4 unrecognized UL keys for 3 days (D-08, SAFE-06).
    - [Phase 200]: Saturation canary at 18 Mbit Spectrum UL is the primary deploy gate per D-07; 24h regression soak is the watchdog, not the verdict.
    - [Phase 200]: Rollback protocol predefined per D-10 — revert /opt/wanctl binary to v1.40; YAML stays in place and is silently no-op'd by older binary.
    - [Phase 200]: Spectrum YAML adoption — ceiling 18 Mbit, factor_down_yellow 0.98, target_bloat_ms 42, warn_bloat_ms 105 — locked from operator evidence trail in three .planning/spectrum-*-2026-04-29.md notes (D-05).
    ```

    c) Append to `## Performance Metrics`:

    ```markdown
    - <YYYY-MM-DD>: Phase 200 Plan 01 completed in <X> min — D-03 per-key presence flags + 2 new tests.
    - <YYYY-MM-DD>: Phase 200 Plan 02 completed in <X> min — SAFE-05 expected count update (D-09).
    - <YYYY-MM-DD>: Phase 200 Plan 03 completed in <X> min — SAFE-06 unknown-key startup WARNING + 2 regression tests.
    - <YYYY-MM-DD>: Phase 200 Plan 04 completed in <X> min — version bump 1.41.0 + Spectrum YAML D-05 + CHANGELOG + migration note.
    - <YYYY-MM-DD>: Phase 200 Plan 05 completed in <X> min — saturation canary script + env example.
    - <YYYY-MM-DD>: Phase 200 Plan 06 completed in <X> min — production deploy + canary verdict=<value>.
    - <YYYY-MM-DD>: Phase 200 Plan 07 completed in <duration_h>h — 24h regression soak verdict=<value>.
    - <YYYY-MM-DD>: Phase 200 Plan 08 completed in <X> min — VERIFICATION + SUMMARY + state updates.
    ```

    d) Update `## Current Position`:

    ```markdown
    Phase: 200 — <closed | partial | blocked>
    Plan: 08 — verification complete
    Status: <Phase passed; v1.41 ready to archive | Phase passed with documented exceptions; <details> | Phase blocked; gap-closure phase planned>
    Last activity: <YYYY-MM-DD> — Phase 200 closeout
    ```

    e) If Phase 200 is BLOCKED, append to `## Blockers`:

    ```markdown
    - Phase 200 closure blocked: <details from 200-VERIFICATION.md status=blocked rationale>. Gap-closure phase planned at .planning/phases/200.1-<slug>/ (TBD).
    ```

    Step 3 — Validate STATE.md frontmatter:

    ```bash
    head -20 .planning/STATE.md | python3 -c "
    import sys, yaml
    text = sys.stdin.read()
    fm_text = text.split('---')[1]
    fm = yaml.safe_load(fm_text)
    assert 'milestone' in fm
    assert 'status' in fm
    assert 'last_updated' in fm
    print('OK: STATE.md frontmatter valid')
    "
    ```

    Step 4 — Final commit:

    ```bash
    git add .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md \
            .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md \
            .planning/REQUIREMENTS.md \
            .planning/STATE.md
    git commit -m "docs(200): close phase verification (status=<value>, requirements=ARB-05/SAFE-06/VALN-06/DOCS-03)"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md && test -f .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SUMMARY.md && grep -qE "Phase 200" .planning/STATE.md && grep -qE "ARB-05|SAFE-06|VALN-06|DOCS-03" .planning/REQUIREMENTS.md && python3 -c "import yaml; text=open('.planning/STATE.md').read(); fm=yaml.safe_load(text.split('---')[1]); assert 'milestone' in fm and 'status' in fm"</automated>
  </verify>
  <done>
    REQUIREMENTS.md v1.41 traceability rows updated; STATE.md decisions, performance metrics, and current position reflect Phase 200 closure (or partial/blocked); final commit lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 200-VERIFICATION.md → milestone audit (next phase) | Future phases consume this artifact's `requirements:` block as ground truth |
| STATE.md state document → cross-session continuity | Stale or wrong STATE.md misroutes the next session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-26 | Tampering | VERIFICATION.md status=passed claimed when canary or soak failed | mitigate | Task 1 hot-path test slice + Task 2 explicit decision matrix tying status to canary/soak verdicts. Verifier reads frontmatter; mismatch with logs is detectable. |
| T-200-27 | Repudiation | REQUIREMENTS.md `[x]` checkmark applied to unsatisfied requirement | mitigate | Each row cites 200-VERIFICATION.md as the evidence path; cross-check between the two files catches drift. |
| T-200-28 | Information Disclosure | STATE.md decisions block leaks operator-private hostnames | accept | The STATE.md template (and existing entries) already include hostnames; out-of-scope for this plan to scrub. |
</threat_model>

<verification>
- 200-VERIFICATION.md exists with all four requirement IDs cited.
- 200-SUMMARY.md exists with the milestone-level outcome.
- REQUIREMENTS.md v1.41 traceability rows reflect actual states.
- STATE.md `Decisions` block grew by Phase 200 entries.
- STATE.md `Performance Metrics` block grew by Phase 200 entries.
- STATE.md `Current Position` reflects Phase 200 closure state.
- One git commit `docs(200): close phase verification ...` exists.
- Hot-path test slice still passes.
</verification>

<success_criteria>
1. 200-VERIFICATION.md follows the structured frontmatter contract; every requirement is cited or blocked.
2. 200-SUMMARY.md is operator-readable and captures the deploy state.
3. REQUIREMENTS.md and STATE.md are consistent with VERIFICATION.md.
4. Phase 200 has a definitive status: passed | passed_with_documented_exceptions | partial | blocked.
5. Followup todos (if any) are recorded for the next milestone.
</success_criteria>

<output>
After completion, the Phase 200 directory carries a closeout-ready evidence trail. Milestone v1.41 is ready for `/gsd-archive-milestone` (if status=passed) or `/gsd-plan-phase 200.1 --gaps` (if status=blocked).

Plan 08 SUMMARY (write last):
- key-files.created: 200-VERIFICATION.md, 200-SUMMARY.md
- key-files.modified: REQUIREMENTS.md, STATE.md
- requirements-addressed: [ARB-05, SAFE-06, VALN-06, DOCS-03]
- phase-status: <pinned value>
- milestone-recommendation: archive | gap-closure | partial-archive
</output>
