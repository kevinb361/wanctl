---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 15
type: execute
wave: 4
depends_on:
  - "200-14"
gap_closure: true
last_revised: 2026-05-03T00:00:00Z
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
autonomous: false
requirements:
  - VALN-06
  - ARB-05
  - SAFE-06
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "Phase 200 VERIFICATION.md frontmatter `status` reflects the canary + soak outcome from Plan 200-14: `verified` ONLY if BOTH canary AND soak passed; `gaps_found` in EVERY OTHER case (canary fail, canary abort, soak fail). The `verified-with-soak-gap` status is removed (Codex MEDIUM finding closed: VALN-06 includes the 24h soak as part of the stated requirement, not as an optional watchdog)."
    - "REQUIREMENTS.md VALN-06 row reflects new status: `Satisfied (200-VERIFICATION.md gap-closure)` only on full pass; otherwise remains `Blocked` with updated artifact pointer."
    - "STATE.md `## Current Position` and `## Blockers` are updated to reflect the gap-closure outcome."
    - "RETRO.md has an appended section recording the gap-closure cycle's findings."
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md"
      provides: "Updated verification with all truths re-evaluated against gap-closure evidence"
      contains: "status:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VALN-06 traceability row updated"
      contains: "VALN-06"
    - path: ".planning/STATE.md"
      provides: "Phase 200 closure recorded"
      contains: "Phase 200"
  key_links:
    - from: "200-14-SUMMARY.md verdict"
      to: "200-VERIFICATION.md status frontmatter"
      via: "outcome map (canary+soak both pass → verified; otherwise → gaps_found)"
      pattern: "status:"
    - from: "200-VERIFICATION.md status"
      to: "REQUIREMENTS.md VALN-06 status"
      via: "consistent reflection"
      pattern: "VALN-06"
---

<objective>
Close out Phase 200 based on Plan 200-14's outcome. Update VERIFICATION, REQUIREMENTS, STATE, ROADMAP, and RETRO to reflect the gap-closure cycle. This plan does NOT touch source code, tests, or production. It is the documentation/traceability seal.

Per Codex MEDIUM finding (200-REVIEWS.md), VALN-06 stipulates BOTH a passed saturated UL canary AND a 24h soak watchdog under <5/60s. If either misses, status MUST be `gaps_found` (with the missed truth recorded as a fresh gap), NOT a hybrid `verified-with-soak-gap` that conflates a partial pass with a full pass. This plan revision removes `verified-with-soak-gap` from the outcome map.

Output: Updated planning files. RETRO appendix. No source / test / config / script changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md
</context>

<outcome_map>
## Outcome Map (revised per Codex MEDIUM)

| Canary verdict | Soak verdict (if reached) | VERIFICATION.md status | REQUIREMENTS.md VALN-06 |
|----------------|---------------------------|------------------------|-------------------------|
| pass           | pass                      | `verified`             | `Satisfied`             |
| pass           | fail                      | `gaps_found` (NEW soak gap) | `Blocked` (soak watchdog gap) |
| fail           | (not run)                 | `gaps_found`           | `Blocked`               |
| abort          | (not run)                 | `gaps_found`           | `Blocked`               |

The `verified-with-soak-gap` status is REMOVED. Two valid statuses only: `verified` or `gaps_found`.

For canary-pass + soak-fail: the canary truth is verified (gap closed), but a NEW gap is recorded for the soak watchdog failure. Phase status is `gaps_found` (not `verified`) because VALN-06 requires both.
</outcome_map>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Operator confirms phase outcome category</name>
  <what-built>Plan 200-14 produced a canary verdict and (if applicable) a soak verdict. Read 200-14-SUMMARY.md.</what-built>
  <how-to-verify>
1. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md`.
2. Identify the outcome category by mapping (canary, soak) → status using the `<outcome_map>` table above:
   - **A — verified**: canary pass + soak pass.
   - **B — gaps_found (canary regression)**: canary fail; rollback executed; soak skipped.
   - **C — gaps_found (canary abort)**: canary abort; rollback executed; soak skipped.
   - **D — gaps_found (soak watchdog gap)**: canary pass; soak fail; canary truth verified BUT new soak gap recorded; phase status remains `gaps_found` (NOT `verified-with-soak-gap` — that hybrid status is removed per Codex MEDIUM).
3. Operator confirms the category and whether to proceed with closeout, or whether category B/C should re-enter gap closure (re-running `/gsd-plan-phase 200 --gaps` to author Plans 200-16+).
  </how-to-verify>
  <resume-signal>Reply "category A: proceed with verified closeout", "category B: open second gap-closure cycle", "category C: open second gap-closure cycle (abort branch)", or "category D: proceed with gaps_found closeout (soak watchdog gap)".</resume-signal>
  <files></files>
  <action>This is a BLOCKING checkpoint. The executor records the operator response inline in the appropriate document and proceeds based on the structured options defined above. No file edits are made by this task itself.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Operator response recorded; downstream tasks/plans branch on the recorded selection.</done>
</task>

<task type="auto">
  <name>Task 2: Update 200-VERIFICATION.md frontmatter and Goal Achievement table</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md (current `gaps_found` state; the gaps: frontmatter list)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md (verdicts)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md (Attempt 3 evidence paths)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md (soak verdict path)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md (Codex MEDIUM on hybrid status)
  </read_first>
  <action>
Branch on the operator-confirmed category from Task 1. Two valid statuses ONLY: `verified` or `gaps_found`.

**Category A — verified (canary pass + soak pass):**
1. Update frontmatter:
   - `status: verified`
   - `score: 6/6 must-haves verified` (or whatever the truth count is)
   - `requirements.VALN-06: satisfied`
   - `canary_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/verdict.json`
   - `soak_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/soak/<NEW-UTC>/verdict.json`
   - Replace the `gaps:` list with `gaps: []` (or remove the key — pick whichever the schema requires).
2. Update Goal Achievement table:
   - Truth (canary): FAILED → VERIFIED, with new evidence path and floor-hit count (0).
   - Truth (soak): FAILED → VERIFIED, with mean suppressions/60s value.
3. Append a "Gap-Closure Cycle Outcome" section noting Plans 09-14 references and the date.

**Category B — gaps_found (canary regression: canary fail):**
1. Update frontmatter:
   - `status: gaps_found` (still)
   - `score: 3/5 must-haves verified` (unchanged or updated based on which truths advanced)
   - `requirements.VALN-06: blocked`
   - `canary_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/verdict.json`
   - Replace `gaps:` list with second-stage gaps: drop closed gaps (Plan 200-11 jq path bug, Plan 200-12 WR-01, Plan 200-13 WR-03), keep canary failure with NEW evidence path, keep soak as failed (no canary-pass-first means soak never ran).
2. Update Goal Achievement table:
   - Truth (canary): annotate "second canary attempt also failed at <new floor-hit count>".
   - Add a row noting the controller change from Plan 200-10 was insufficient.
3. Append a "Gap-Closure Cycle 1 Outcome" section recommending another cycle or operator escalation.

**Category C — gaps_found (canary abort):**
1. Update frontmatter:
   - `status: gaps_found`
   - `requirements.VALN-06: blocked`
   - `canary_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/verdict.json` (with abort reason cited inline)
   - Replace `gaps:` list with second-stage gaps including the abort reason; mark canary still NOT exercised end-to-end.
2. Update Goal Achievement: annotate the canary truth as "aborted, reason=<reason>; not yet a pass-or-fail outcome".
3. Append a "Gap-Closure Cycle 1 Outcome" section noting that an abort is fail-closed but recoverable without a fresh full cycle if the abort cause is environmental.

**Category D — gaps_found (canary pass + soak fail):**
1. Update frontmatter:
   - `status: gaps_found` (NOT `verified-with-soak-gap` — that status is removed per Codex MEDIUM).
   - `requirements.VALN-06: blocked` (with annotation: "canary truth satisfied; soak watchdog gap open").
   - `canary_verdict_path:` populated (pass evidence).
   - `soak_verdict_path:` populated (fail evidence).
   - Update `gaps:` list: REMOVE the canary gap (canary truth verified); ADD a new `soak_watchdog_failure` gap entry referencing the soak verdict.
2. Update Goal Achievement table:
   - Truth (canary): VERIFIED with evidence path.
   - Truth (soak): FAILED with mean suppressions/60s value vs <5/60s threshold.
3. Append a "Gap-Closure Cycle 1 Outcome" section noting that the canary closed but soak remains open; recommends an additional gap-closure cycle scoped to the soak failure.

In all branches, preserve the original `gaps_found` historical record by appending rather than overwriting where structurally feasible. The frontmatter is the live status; the body can grow.
  </action>
  <acceptance_criteria>
    <automated>grep -E "^status: (verified|gaps_found)$" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; ! grep -q 'verified-with-soak-gap' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'VALN-06' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^status: (verified|gaps_found)$" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; ! grep -q 'verified-with-soak-gap' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md frontmatter status is exactly `verified` or `gaps_found` (no hybrid). Goal Achievement table reflects Plan 200-14's outcome. The hybrid `verified-with-soak-gap` status is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md, STATE.md, and ROADMAP.md to reflect outcome</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 103-108 — v1.41 traceability table)
    - .planning/STATE.md (Position, Decisions, Blockers, Current Position sections)
    - .planning/ROADMAP.md (Phase 200 entry)
  </read_first>
  <action>
1. **REQUIREMENTS.md:** Update VALN-06 row.
   - Category A: change "Blocked" to "Satisfied (200-VERIFICATION.md, gap-closure plans 200-09 through 200-15)".
   - Category B/C: keep "Blocked" with updated artifact pointer to the new failed/aborted canary.
   - Category D: change to "Blocked (canary truth satisfied; soak watchdog gap open per 200-VERIFICATION.md)".

2. **STATE.md:**
   - Update `last_updated` to current UTC.
   - Update `stopped_at` to reflect Phase 200 closure (or second-stage block).
   - Append to `## Decisions` section a "[Phase 200 gap closure]" entry recording the approved Plan 200-09 branch(es) and final verdict.
   - Update `## Blockers`:
     - Category A: remove the "Phase 200 closure blocked" bullet.
     - Category B: replace with a second-stage block citing the new canary verdict.
     - Category C: replace with a block citing the abort reason.
     - Category D: replace with a block citing the soak watchdog gap (canary OK, soak missed).
   - Update `## Current Position`: Phase 200 status → CLOSED (A) or BLOCKED-2nd-stage (B/C/D).
   - Append to `## Performance Metrics`: gap-closure cycle stats.

3. **ROADMAP.md:**
   - Update the Phase 200 row in the Phase Summary table to reflect closure verdict and plan count `15/15`.
   - Update the Plans line under Phase 200 detail.
   - Category A: change to "Closed — VALN-06 satisfied via gap-closure".
   - Category B: change to "Blocked — VALN-06 second canary failed; second gap-closure cycle pending".
   - Category C: change to "Blocked — VALN-06 second canary aborted (reason=<reason>); rerun pending".
   - Category D: change to "Blocked — canary closed; soak watchdog gap open; targeted gap-closure cycle pending".

In all branches, preserve historical accuracy: do not rewrite the original Phase 200 BLOCKED narrative; append the closure narrative.
  </action>
  <acceptance_criteria>
    <automated>grep -q "Phase 200 gap closure" .planning/STATE.md &amp;&amp; grep -q "VALN-06" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 200" .planning/ROADMAP.md &amp;&amp; ! grep -q 'verified-with-soak-gap' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'verified-with-soak-gap' .planning/STATE.md &amp;&amp; ! grep -q 'verified-with-soak-gap' .planning/ROADMAP.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 200 gap closure" .planning/STATE.md &amp;&amp; grep -q "VALN-06" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 200" .planning/ROADMAP.md &amp;&amp; ! grep -rq 'verified-with-soak-gap' .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>All three planning files reflect the Plan 200-14 outcome consistently. The hybrid `verified-with-soak-gap` status is absent everywhere.</done>
</task>

<task type="auto">
  <name>Task 4: Append gap-closure RETRO note</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md (existing retro from Plan 08; copy the section style)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md (the hypothesis itself; cite which candidate cause the data eventually validated)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-10-SUMMARY.md (what landed)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md (canary + soak verdicts)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md (Codex cross-AI review findings; cite as a process win)
  </read_first>
  <action>
Append a new section `## Gap-Closure Cycle (Plans 09-15)` to `200-RETRO.md` with at least these subsections:

1. **What worked:**
   - Hypothesis-first plan (200-09) gating the controller change behind operator approval at a BLOCKING checkpoint — preserved CLAUDE.md "stability > safety > clarity" rule.
   - Splitting WR-01 / WR-02 / WR-03 into Wave-1 parallel plans (200-11/12/13) reduced wall-clock cost without crossing files_modified.
   - Codex cross-AI review (200-REVIEWS.md) caught the file-target error in Plan 200-10 (UL decay lives in `queue_controller.py`, not `wan_controller.py`) and surfaced the missing R5 (YAML-only YELLOW hold) as the architecturally conservative remediation; both findings drove the reviews-mode revision.
   - The Plan 200-11 jq path fix proved itself live by populating verdict.json baseline RTT bookends in the Plan 200-14 canary.

2. **What did not work / what was harder than expected:**
   - [Fill in based on actual outcome — e.g., R5 alone insufficient (canary still failed) → had to combine with R3; or canary aborted on iperf3 setup; or soak missed under 5/60s.]

3. **Lessons for v1.42:**
   - DOCSIS-aware UL congestion mode (deferred from CONTEXT.md) is a likely v1.42 candidate if the gap closure showed C1 (insufficient shaping headroom) was the dominant cause.
   - Per-direction `/health` telemetry is now more attractive — debugging Plan 200-14 would have been faster with explicit UL-specific suppression counters.
   - Cross-AI review (Codex) pre-implementation is high-leverage: it caught two HIGH-severity issues (file-target error, missing R5) before any code touched the production path.
   - If Category B/C: the hypothesis-first approach was correct but the candidate-cause enumeration may have missed the actual root cause; future planners should add a stronger evidence-vs-hypothesis cross-check before R-selection.

4. **Cross-milestone reference:** Note the related deferred todo `.planning/todos/pending/2026-04-24-resolve-att-cake-primary-canary-after-phase-196.md` if its blocker (Phase 191 closure) overlaps with v1.42 planning.

Do NOT delete or rewrite the existing Plan 08 retro content. Append only.
  </action>
  <acceptance_criteria>
    <automated>grep -q "Gap-Closure Cycle" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q "Plans 09-15" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q "Codex" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Gap-Closure Cycle" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q "Plans 09-15" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</automated>
  </verify>
  <done>RETRO.md has the gap-closure appendix with all four subsections including the cross-AI review process win.</done>
</task>

<task type="auto">
  <name>Task 5: Final hot-path slice and commit</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Git Workflow section: project-finalizer is mandatory before commit)
  </read_first>
  <action>
1. Final hot-path slice run as a sanity check (this plan should not have touched source, but verify):
   - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`
2. Commit closeout artifacts following the existing pattern (see Phase 200 commit history `docs(200-08): ...`). Suggested commit message:
   - `docs(200-15): close gap-closure cycle and update phase artifacts`
3. Run `project-finalizer` agent before commit per CLAUDE.md mandatory pre-commit policy.
4. Files staged: 200-VERIFICATION.md, REQUIREMENTS.md, STATE.md, ROADMAP.md, 200-RETRO.md, 200-15-SUMMARY.md (created in <output> step).
  </action>
  <acceptance_criteria>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <done>Hot-path slice green; closeout commit landed (after project-finalizer).</done>
</task>

</tasks>

<verification>
- VERIFICATION.md frontmatter status is exactly `verified` or `gaps_found` (regex `^status: (verified|gaps_found)$`).
- The hybrid `verified-with-soak-gap` status is absent from VERIFICATION.md, REQUIREMENTS.md, STATE.md, and ROADMAP.md.
- REQUIREMENTS.md VALN-06 row updated.
- STATE.md `## Current Position` and `## Blockers` updated.
- ROADMAP.md Phase 200 row reflects closure verdict.
- RETRO.md has appended gap-closure cycle section citing Codex cross-AI review.
- Hot-path test slice still green (no source touched in this plan).
</verification>

<success_criteria>
1. All five planning files reflect a consistent picture of Phase 200's gap-closure outcome.
2. Status is exactly `verified` (canary pass + soak pass) or `gaps_found` (every other case). No hybrid states.
3. RETRO records lessons for v1.42 including the cross-AI review process win.
4. Hot-path test slice green.
5. project-finalizer ran before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-15-SUMMARY.md` recording: outcome category (A/B/C/D), final VERIFICATION.md status (`verified` or `gaps_found`), REQUIREMENTS.md VALN-06 row text, STATE.md blocker delta, ROADMAP.md row text, and a one-paragraph closeout statement suitable for archive review.
</output>
