---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 15
type: execute
wave: 4
depends_on:
  - "200-14"
gap_closure: true
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
autonomous: false
requirements:
  - VALN-06
  - ARB-05
  - SAFE-06
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "Phase 200 VERIFICATION.md frontmatter `status` reflects the canary + soak outcome from Plan 200-14: `verified` if both passed; `gaps_found` (with second-stage gap text) if canary failed; `verified-with-soak-gap` if canary passed but 24h soak missed the <5/60s threshold."
    - "REQUIREMENTS.md VALN-06 row reflects new status: `Satisfied (200-VERIFICATION.md gap-closure)` on full pass, or remains `Blocked` with updated artifact pointer on canary-fail."
    - "STATE.md `## Current Position` and `## Blockers` are updated to reflect the gap-closure outcome."
    - "RETRO.md has an appended section recording the gap-closure cycle's findings (what worked, what didn't, lessons for v1.42)."
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md"
      provides: "Updated verification with all 6 truths re-evaluated against gap-closure evidence"
      contains: "status:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VALN-06 traceability row updated"
      contains: "VALN-06"
    - path: ".planning/STATE.md"
      provides: "Phase 200 closure recorded"
      contains: "Phase 200"
  key_links:
    - from: "200-14-SUMMARY.md verdict"
      to: "200-VERIFICATION.md status frontmatter"
      via: "1:1 mapping (pass to verified, fail to gaps_found, soak-fail to verified-with-soak-gap)"
      pattern: "status:"
    - from: "200-VERIFICATION.md status"
      to: "REQUIREMENTS.md VALN-06 status"
      via: "consistent reflection"
      pattern: "VALN-06"
---

<objective>
Close out Phase 200 based on Plan 200-14's outcome. Update VERIFICATION, REQUIREMENTS, STATE, ROADMAP, and RETRO to reflect the gap-closure cycle. This plan does NOT touch source code, tests, or production. It is the documentation/traceability seal.

Purpose: GSD requires VERIFICATION.md, REQUIREMENTS.md, and STATE.md to remain consistent with reality. The gap-closure cycle changed reality; the planning artifacts must catch up. Three possible outcomes (verified / gaps_found_again / verified-with-soak-gap) each have a different update path.

Output: Updated planning files. RETRO appendix. No source / test / config / script changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Operator confirms phase outcome category</name>
  <what-built>Plan 200-14 produced a canary verdict and (if applicable) a soak verdict. Read 200-14-SUMMARY.md.</what-built>
  <how-to-verify>
1. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md`.
2. Identify the outcome category:
   - **A — verified**: canary pass + soak under 5/60s mean.
   - **B — gaps_found_again**: canary fail; rollback executed.
   - **C — verified-with-soak-gap**: canary pass; soak missed under 5/60s threshold.
3. Operator confirms which category applies and whether to proceed with closeout, or whether category B should re-enter gap closure (re-running `/gsd-plan-phase --gaps` to author Plans 200-16+).
  </how-to-verify>
  <resume-signal>Reply "category A: proceed with verified closeout", "category B: open second gap-closure cycle", or "category C: proceed with verified-with-soak-gap closeout".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update 200-VERIFICATION.md frontmatter and Goal Achievement table</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md (current `gaps_found` state; the gaps: frontmatter list)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md (verdicts)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md (Attempt 3 evidence paths)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md (soak verdict path)
  </read_first>
  <action>
Branch on the operator-confirmed category from Task 1.

**Category A — verified:**
1. Update frontmatter:
   - `status: verified`
   - `score: 5/5 must-haves verified` (or 6/6 if you count both canary and soak truths separately)
   - `requirements.VALN-06: satisfied`
   - `canary_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/verdict.json`
   - `soak_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/soak/<NEW-UTC>/verdict.json`
   - Replace the `gaps:` list with `gaps: []` (or remove the key — pick whichever the schema requires).
2. Update Goal Achievement table:
   - Truth 5 (canary): FAILED to VERIFIED, with new evidence path and floor-hit count (0).
   - Truth 6 (soak): FAILED to VERIFIED, with mean suppressions/60s value.
3. Append a "Gap-Closure Cycle Outcome" section noting Plans 09-14 references and the date.

**Category B — gaps_found_again:**
1. Update frontmatter:
   - `status: gaps_found` (still — second-stage)
   - `score: 3/5 must-haves verified` (unchanged or updated based on which truths advanced)
   - `requirements.VALN-06: blocked`
   - `canary_verdict_path: .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/verdict.json`
   - Replace `gaps:` list with second-stage gaps: drop closed gaps (jq path bug from Plan 200-11), keep canary failure with NEW evidence path, keep soak as failed (still no pass-first canary).
2. Update Goal Achievement table:
   - Truth 5 (canary): annotate "second canary attempt also failed at <new floor-hit count>".
   - Add a new "Anti-Patterns" row noting the controller change from Plan 200-10 was insufficient.
3. Append a "Gap-Closure Cycle 1 Outcome" section recommending another cycle or operator escalation.

**Category C — verified-with-soak-gap:**
1. Update frontmatter:
   - `status: verified-with-soak-gap` (or whatever wording is consistent with prior phase precedents — check 198-VERIFICATION.md or 196-VERIFICATION.md if a similar status was used; otherwise use `verified` and document the soak-gap in the gaps list)
   - `requirements.VALN-06: satisfied-with-watchdog-gap` or similar
   - Both verdict paths populated.
2. Update Goal Achievement:
   - Truth 5 (canary): VERIFIED.
   - Truth 6 (soak): PARTIAL with the actual mean value vs the under 5/60s threshold.

In all branches, preserve the original `gaps_found` historical record by appending rather than overwriting where structurally feasible. The frontmatter is the live status; the body can grow.
  </action>
  <verify>
    <automated>grep -E "^status: (verified|gaps_found|verified-with-soak-gap)" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -E "VALN-06: (satisfied|blocked|satisfied-with-watchdog-gap)" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md frontmatter and Goal Achievement table reflect Plan 200-14's outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md, STATE.md, and ROADMAP.md to reflect outcome</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 103-108 — v1.41 traceability table)
    - .planning/STATE.md (Position, Decisions, Blockers, Current Position sections)
    - .planning/ROADMAP.md (Phase 200 entry, line ~124 and ~163)
  </read_first>
  <action>
1. **REQUIREMENTS.md:** Update line 25 (VALN-06 description) and line 107 (traceability table). On Category A: change "Blocked" to "Satisfied (200-VERIFICATION.md, gap-closure plans 200-09 through 200-15)". On Category B: keep "Blocked" with updated artifact pointer to the new failed canary. On Category C: change to "Satisfied (canary); soak watchdog gap recorded".

2. **STATE.md:**
   - Update `last_updated` to current UTC.
   - Update `stopped_at` to reflect Phase 200 closure (or second-stage block).
   - Append to `## Decisions` section a "[Phase 200 gap closure]" entry recording the approved Plan 200-09 remediation direction and final verdict.
   - Update `## Blockers` section: on Category A, remove the "Phase 200 closure blocked" bullet. On Category B, replace it with a second-stage block citing the new canary verdict. On Category C, soften wording to "Phase 200 closed with watchdog soak gap; v1.42 may revisit".
   - Update `## Current Position`: change Phase status from BLOCKED to one of CLOSED / BLOCKED-2nd-stage / CLOSED-with-soak-gap.
   - Append to `## Performance Metrics`: gap-closure cycle stats (Plans 09-15 plan count, total time, final verdict).

3. **ROADMAP.md:**
   - Update the Phase 200 row in the Phase Summary table to reflect the closure verdict and plan count `15/15` (or however many landed).
   - Update the Plans line under Phase 200 detail.
   - On Category A: change "Blocked — VALN-06 canary failed" to "Closed — VALN-06 satisfied via gap-closure".
   - On Category B: change to "Blocked — VALN-06 second canary failed; second gap-closure cycle pending".
   - On Category C: change to "Closed with watchdog gap — VALN-06 canary satisfied; 24h soak watchdog gap recorded for v1.42".

In all branches, preserve historical accuracy: do not rewrite the original Phase 200 BLOCKED narrative; append the closure narrative.
  </action>
  <verify>
    <automated>grep -q "Phase 200 gap closure" .planning/STATE.md &amp;&amp; grep -q "VALN-06" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 200" .planning/ROADMAP.md</automated>
  </verify>
  <done>All three planning files reflect the Plan 200-14 outcome consistently.</done>
</task>

<task type="auto">
  <name>Task 4: Append gap-closure RETRO note</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md (existing retro from Plan 08; copy the section style)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md (the hypothesis itself; cite which candidate cause the data eventually validated)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-10-SUMMARY.md (what landed)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md (canary + soak verdicts)
  </read_first>
  <action>
Append a new section `## Gap-Closure Cycle (Plans 09-15)` to `200-RETRO.md` with at least these subsections:

1. **What worked:**
   - Hypothesis-first plan (200-09) gating the controller change behind operator approval at a BLOCKING checkpoint — preserved CLAUDE.md "stability > safety > clarity" rule.
   - Splitting WR-01 / WR-02 / WR-03 into Wave-1 parallel plans (200-11/12/13) reduced wall-clock cost without crossing files_modified.
   - The Plan 200-11 jq path fix proved itself live by populating verdict.json baseline RTT bookends in the Plan 200-14 canary.

2. **What did not work / what was harder than expected:**
   - [Fill in based on actual outcome — e.g., R1 alone insufficient → had to combine with R2; or R2 floor-guard regressed a non-Spectrum byte-identity test on first try; or canary still failed and a second cycle is needed.]

3. **Lessons for v1.42:**
   - DOCSIS-aware UL congestion mode (deferred from CONTEXT.md) is a likely v1.42 candidate if the gap closure showed C1 (insufficient shaping headroom) was the dominant cause.
   - Per-direction `/health` telemetry is now more attractive — debugging Plan 200-14 would have been faster with explicit UL-specific suppression counters.
   - If Category B: the hypothesis-first approach was correct but the candidate-cause enumeration missed the actual root cause; future planners should add a stronger evidence-vs-hypothesis cross-check before R-selection.

4. **Cross-milestone reference:** Note the related deferred todo `.planning/todos/pending/2026-04-24-resolve-att-cake-primary-canary-after-phase-196.md` if its blocker (Phase 191 closure) overlaps with v1.42 planning.

Do NOT delete or rewrite the existing Plan 08 retro content. Append only.
  </action>
  <verify>
    <automated>grep -q "Gap-Closure Cycle" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q "Plans 09-15" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</automated>
  </verify>
  <done>RETRO.md has the gap-closure appendix with all four subsections.</done>
</task>

<task type="auto">
  <name>Task 5: Final hot-path slice and commit</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Git Workflow section: project-finalizer is mandatory before commit)
  </read_first>
  <action>
1. Final hot-path slice run as a sanity check (this plan should not have touched source, but verify):
   - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`
2. Commit closeout artifacts following the existing pattern (see Phase 200 commit history `docs(200-08): ...`). Suggested commit message:
   - `docs(200-15): close gap-closure cycle and update phase artifacts`
3. Run `project-finalizer` agent before commit per CLAUDE.md mandatory pre-commit policy.
4. Files staged: 200-VERIFICATION.md, REQUIREMENTS.md, STATE.md, ROADMAP.md, 200-RETRO.md, 200-15-SUMMARY.md (created in <output> step).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <done>Hot-path slice green; closeout commit landed (after project-finalizer).</done>
</task>

</tasks>

<verification>
- VERIFICATION.md frontmatter status reflects Plan 200-14 outcome.
- REQUIREMENTS.md VALN-06 row updated.
- STATE.md `## Current Position` and `## Blockers` updated.
- ROADMAP.md Phase 200 row reflects closure verdict.
- RETRO.md has appended gap-closure cycle section.
- Hot-path test slice still green (no source touched in this plan).
</verification>

<success_criteria>
1. All five planning files reflect a consistent picture of Phase 200's gap-closure outcome.
2. RETRO records lessons for v1.42.
3. Hot-path test slice green.
4. project-finalizer ran before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-15-SUMMARY.md` recording: outcome category (A/B/C), final VERIFICATION.md status, REQUIREMENTS.md VALN-06 row text, STATE.md blocker delta, ROADMAP.md row text, and a one-paragraph closeout statement suitable for archive review.
</output>
