---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 16
type: execute
wave: 5
depends_on:
  - "200-15"
gap_closure: true
revision: 2
supersedes: 200-16-PLAN-v1 (reviewed in 200-REVIEWS.md Cycle 2)
review_findings_addressed:
  - HIGH-yaml-tone
  - MEDIUM-inheritance
  - MEDIUM-mitigation-branch
  - MEDIUM-truth-count
  - LOW-allowed-list
  - LOW-files-modified
  - LOW-timestamp
  - LOW-verdict-cite
last_revised: 2026-05-04
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
  - .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-PLAN.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-SUMMARY.md
autonomous: false
requirements:
  - ARB-05
  - SAFE-06
  - VALN-06
  - DOCS-03
user_setup: []
tags: [gap-closure, valn-06, traceability, phase-201-handoff, operator-escalation]

must_haves:
  truths:
    - "200-VERIFICATION.md frontmatter records the operator-escalated deferral via a top-level `closure: deferred-to-phase-201` field, with a `closure_decision` sub-block citing the 2026-05-04 operator decision; status remains `gaps_found` (no failed truth is silently flipped to VERIFIED)."
    - "REQUIREMENTS.md VALN-06 row shows status `Deferred -> Phase 201 (inherited blocking requirement)` with artifact pointers to `200-RETRO.md` plus `201-CONTEXT.md` AND an explicit notation that Phase 201 SPEC and PLAN must carry VALN-06 forward and cannot silently drop it; ARB-05, SAFE-06, and DOCS-03 rows are unchanged."
    - "STATE.md `## Current Position` reads as Phase 200 closed with VALN-06 deferred to Phase 201 / v1.42; `## Blockers` no longer references a pending second Phase 200 gap-closure cycle and instead records VALN-06 as inherited by Phase 201."
    - "ROADMAP.md Phase 200 plan count line reads `16/16 plans complete` and the closure narrative cites operator-escalated deferral of VALN-06 to Phase 201; success criteria 3 and 4 carry a parenthetical `(Deferred to Phase 201 — see 200-RETRO.md)` and remain unchecked."
    - "200-RETRO.md has an appended `## Final Closure (2026-05-04)` section recording the operator decision, the marginal-return rationale, the absence of a second remediation attempt, and the routing of VALN-06 to Phase 201; existing Plans 09-15 retro content is preserved."
    - "201-CONTEXT.md carries an `## Inherited Requirements` block citing VALN-06 with pointers back to 200-VERIFICATION.md and 200-RETRO.md, AND directly cites the Attempt 3 verdict path `canary/20260504T133207Z/verdict.json`."
    - "No file under `src/`, `tests/`, `scripts/`, `configs/`, `docker/`, `pyproject.toml`, `.dockerignore`, `CHANGELOG.md`, or `docs/` is touched by this plan."
    - "Closure Decision narrative in 200-VERIFICATION.md, the Final Closure appendix in 200-RETRO.md, and the ROADMAP closure paragraph reframe v1.41 prod YAML as 'inactive under v1.40 binary, but MUST be reconciled before any future Spectrum deploy or service restart' — they do NOT use the words 'harmless', 'benign', or 'no-op' to describe that state."
    - "VERIFICATION.md Closure Decision narrative says 'two failed-truth rows' (saturated UL canary, 24h soak), not 'four'."
    - "Operator countersignature checkpoint (Task 1) presents three explicit branches: (a) defer with no interim Spectrum mitigation, (b) defer and open a quick task for YAML-only Spectrum mitigation, (c) abort and open a second Phase 200 gap-closure cycle."
    - "Task 8 commit-stat scope check verifies every committed path appears in an allowed-list (positive check), in addition to the existing absence-of-forbidden-paths check."
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md"
      provides: "Operator-escalation deferral seal for Phase 200 closure"
      contains: "closure: deferred-to-phase-201"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VALN-06 traceability row updated to deferred state with inherited-blocking notation"
      contains: "Deferred -> Phase 201 (inherited blocking requirement)"
    - path: ".planning/STATE.md"
      provides: "Phase 200 closed; VALN-06 inheritance recorded"
      contains: "VALN-06 inherited by Phase 201"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 200 closure narrative with 16/16 plan count"
      contains: "16/16 plans complete"
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md"
      provides: "Final Closure appendix recording operator decision"
      contains: "Final Closure (2026-05-04)"
    - path: ".planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md"
      provides: "Inherited Requirements block citing VALN-06 plus Attempt 3 verdict path"
      contains: "Inherited Requirements"
  key_links:
    - from: "200-VERIFICATION.md `closure: deferred-to-phase-201`"
      to: "REQUIREMENTS.md VALN-06 `Deferred -> Phase 201 (inherited blocking requirement)`"
      via: "consistent reflection of operator decision"
      pattern: "Deferred"
    - from: "REQUIREMENTS.md VALN-06 row"
      to: "201-CONTEXT.md `## Inherited Requirements` block"
      via: "phase handoff trail"
      pattern: "VALN-06"
    - from: "STATE.md `## Blockers`"
      to: "200-RETRO.md `## Final Closure (2026-05-04)`"
      via: "operator decision rationale"
      pattern: "VALN-06 inherited"
    - from: "201-CONTEXT.md `## Inherited Requirements` block"
      to: ".planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260504T133207Z/verdict.json"
      via: "direct evidence citation"
      pattern: "20260504T133207Z/verdict.json"
---

<objective>
Seal Phase 200 / v1.41 by recording the operator's 2026-05-04 escalation decision: VALN-06 is deferred to Phase 201 (`docsis-aware-ul-congestion-control`) rather than re-entering a second Phase 200 gap-closure cycle. This plan is documentation-only and produces no source, test, script, config, Docker, or shipped-version changes. v1.41.0 was already cut and rolled back; production remains on the v1.40 binary with the gap-closure tarball as the rollback artifact.

Per the operator decision recorded on 2026-05-04, the rationale for deferral is twofold:

1. Phase 200's own RETRO already concluded that the per-direction-thresholds hypothesis is the wrong fix ("the fix is a different control model, not wider thresholds").
2. Plans 200-09 → 200-14 reduced loaded-window UL floor hits from 122 to 4. Marginal returns on further Phase 200 tuning are low; the remaining 4 hits live in a regime dominated by shaping-headroom geometry, not threshold geometry — i.e., DOCSIS-aware control, which is exactly Phase 201's seeded scope.

This is revision 2 of Plan 200-16. Cycle 2 cross-AI review (Codex) flagged eight findings against v1; this revision addresses each one. Notably:

- v1.41 YAML keys remain on prod `/etc/wanctl/spectrum.yaml` and are inactive under the rolled-back v1.40 binary, but they MUST be reconciled before any future Spectrum deploy or service restart. A future binary that re-recognizes those keys would reactivate rejected-hypothesis state silently. This plan reframes that risk explicitly across VERIFICATION.md, RETRO.md, ROADMAP.md, and STATE.md.
- VALN-06 is recorded as an inherited blocking requirement that Phase 201's eventual SPEC and PLAN MUST carry forward; it cannot be silently dropped during 201 scoping.
- The operator countersignature checkpoint exposes a three-branch decision (defer-no-mitigation / defer-with-quick-task-mitigation / abort-and-open-second-cycle).

Output: Updated planning files. RETRO appendix. Phase 201 inheritance block with direct verdict-path citation. Operator countersignature checkpoint with explicit interim-mitigation branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-15-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md
@.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Operator countersigns the Phase 200 deferral closure (three-branch decision)</name>
  <what-built>This plan formalizes the operator's 2026-05-04 decision to defer VALN-06 to Phase 201 rather than running a second Phase 200 gap-closure cycle. Production state is unchanged: Spectrum is on v1.40 binary post-rollback; the v1.41 binary is not deployed; the gap-closure tarball `/opt/wanctl-prephase200-gap-20260504T132936Z.tar.gz` remains the rollback artifact-of-record on cake-shaper. **Important production-posture note:** the v1.41 YAML keys (`continuous_monitoring.upload.target_bloat_ms`, `warn_bloat_ms`, `consecutive_yellow_decay_clamp`, `factor_down_yellow=1.0`, `ceiling_mbps=18`) remain on disk under `/etc/wanctl/spectrum.yaml` and are inactive under the rolled-back v1.40 binary, but they MUST be reconciled before any future Spectrum deploy or service restart that uses a binary which re-recognizes those keys.</what-built>
  <how-to-verify>
1. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md` — confirm the Plans 09-15 closing block ends with "Lessons for v1.42" and routes the next attempt to Phase 201.
2. Read `.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md` — confirm Phase 201 already exists as a seed and references the 122-collapse Phase 200 evidence.
3. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md` frontmatter — confirm current state is `status: gaps_found` with VALN-06 `blocked` and the two gap entries (canary failure, soak skipped).
4. Read `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md` — confirm Cycle 2 Codex review endorses the deferral closure path and lists the eight findings this revision addresses.
5. Confirm intent: Phase 200 closes as `gaps_found` with VALN-06 deferred to Phase 201 as an inherited blocking requirement, no second remediation attempted, no production binary state change. Then choose ONE of the three branches below.

**Three explicit branches — operator must pick one:**

**Branch A — Approve deferral, no interim Spectrum mitigation.** Proceed with Tasks 2-8. Production YAML stays as-is. Reconciliation of the v1.41-only keys on `/etc/wanctl/spectrum.yaml` is fully deferred to Phase 201's predeploy gate (see `<follow_ons>` block). The Spectrum UL hysteresis storm remains live until Phase 201 ships.

**Branch B — Approve deferral, AND open a quick task for YAML-only Spectrum mitigation.** Proceed with Tasks 2-8 unchanged (this plan still touches no `configs/` paths). THEN, after Plan 200-16 is committed, the operator runs `/gsd-capture quick` to track a YAML-only revert to v1.40-shape values on prod `/etc/wanctl/spectrum.yaml` (no controller code changes). The quick task is OUT OF SCOPE for this plan but is recorded as the operator's chosen interim-mitigation path.

**Branch C — Abort.** Do not seal Phase 200. Stop and re-enter `/gsd-plan-phase 200 --gaps` for a second Phase 200 gap-closure cycle. Plan 200-16 is discarded.
  </how-to-verify>
  <resume-signal>Reply with one of: "approved — branch A: defer, no interim mitigation", "approved — branch B: defer, will open YAML-only mitigation quick task post-commit", or "abort — branch C: open second Phase 200 gap-closure cycle".</resume-signal>
  <files></files>
  <action>This is a BLOCKING checkpoint. The executor records the operator response (literal branch identifier A, B, or C, plus the operator's exact reply text) inline in this plan's task log section. No other file edits in this task. On Branch A or Branch B, proceed with Tasks 2-8. On Branch C, stop and re-enter `/gsd-plan-phase 200 --gaps`. The choice of A vs B does NOT change Tasks 2-8; Branch B's quick task is created OUTSIDE this plan after commit.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <acceptance_criteria>
    <automated>true (operator countersignature recorded with explicit branch identifier; downstream tasks proceed only on Branch A or Branch B; Branch C halts execution)</automated>
  </acceptance_criteria>
  <done>Operator response recorded with explicit branch identifier (A, B, or C). On A/B, downstream tasks proceed identically. On C, no further tasks run.</done>
</task>

<task_log>
Task 1 operator countersignature response recorded during execution: approved — branch A: defer, no interim mitigation. Branch A selected; Tasks 2-8 proceed. No YAML-only mitigation quick task is created in this plan.
</task_log>

<task type="auto">
  <name>Task 2: Add deferral closure field to 200-VERIFICATION.md frontmatter and Closure Decision narrative</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md (current frontmatter ends at line 73; narrative `### Gaps Summary` ends near line 170; observable-truth rows at lines 89-94 — only rows #4 (canary) and #5 (soak) are FAILED, all others VERIFIED — so narrative MUST say "two failed-truth rows" not "four")
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md (existing Plans 09-15 retro section — citation source)
  </read_first>
  <action>
Make exactly two edits to `200-VERIFICATION.md`. Do NOT change `status: gaps_found`. Do NOT mark any failed truth as VERIFIED.

**Edit 1 — Frontmatter additions.**

Locate the existing `re_verification:` block (currently lines 42-53). Immediately AFTER the closing `regressions: []` line and BEFORE the existing `gaps:` key, insert this exact YAML block:

```yaml
closure: deferred-to-phase-201
closure_decision:
  date: 2026-05-04
  decided_by: operator
  rationale: |
    Phase 200 RETRO concluded that the per-direction-thresholds hypothesis is the
    wrong fix; the remaining loaded-window floor regime is dominated by shaping
    headroom rather than threshold geometry. Plans 200-09..200-14 reduced UL
    floor hits from 122 to 4 (a 96.7% improvement) but the deploy gate requires
    zero. Marginal returns on further Phase 200 tuning are judged low; VALN-06
    is escalated to Phase 201 (docsis-aware-ul-congestion-control), which is
    already seeded with the 122-collapse evidence as the proper next-milestone
    candidate.
  no_second_remediation_attempted: true
  production_state_change: none
  production_binary: v1.40 (rolled back post Attempt 3 canary fail)
  prod_yaml_state: |
    v1.41 keys remain on /etc/wanctl/spectrum.yaml and are inactive under the
    rolled-back v1.40 binary, but they MUST be reconciled before any future
    Spectrum deploy or service restart that uses a binary which re-recognizes
    those keys. A Phase 201 predeploy gate (see follow-ons) is required to
    inspect the file and either reconcile or fail closed.
  inherits_to: phase-201-docsis-aware-ul-congestion-control
  inherited_as: blocking_requirement
  see_also:
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
    - .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260504T133207Z/verdict.json
```

**Edit 2 — Narrative "Closure Decision" subsection.**

In the document body, locate the `### Gaps Summary` heading (near line 168). Immediately AFTER the existing `Gaps Summary` paragraph (which ends with "...followed by a passing 24h soak.") and BEFORE the trailing horizontal rule `---`, insert this exact subsection (verbatim):

```markdown
### Closure Decision (2026-05-04, operator-escalated)

Phase 200 is sealed at `gaps_found` with **VALN-06 deferred to Phase 201
(`docsis-aware-ul-congestion-control`)** rather than re-entered into a second
gap-closure cycle. The operator made this call on 2026-05-04 after weighing two
findings:

1. The Phase 200 RETRO concluded that the per-direction-thresholds hypothesis
   is the wrong fix: the remaining failure regime is dominated by shaping
   headroom (DOCSIS upstream queue depth versus wanctl ceiling), not threshold
   geometry. Plans 200-09..200-14 reduced loaded-window UL floor hits from 122
   to 4 — a 96.7% improvement but still non-zero, and the deploy gate is
   strictly fail-closed at zero.
2. Phase 201 already exists as a seed (`.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md`)
   and is loaded with the 122-collapse evidence file as its design input.
   ROADMAP.md explicitly allows operator escalation as a closure path for
   VALN-06.

No second remediation was attempted; production binary remains on v1.40
post-rollback. The v1.41 YAML keys (`continuous_monitoring.upload.target_bloat_ms`,
`warn_bloat_ms`, `consecutive_yellow_decay_clamp`, `factor_down_yellow=1.0`,
`ceiling_mbps=18`) remain on prod `/etc/wanctl/spectrum.yaml` and are inactive
under the rolled-back v1.40 binary, but they MUST be reconciled before any
future Spectrum deploy or service restart that uses a binary which re-recognizes
those keys. A future binary that consumes them would reactivate rejected-hypothesis
state silently. Phase 201's PLAN must include a predeploy gate that inspects
`/etc/wanctl/spectrum.yaml` for v1.41-only keys and either reconciles or fails
closed (see `<follow_ons>` in `200-16-PLAN.md`).

The two failed-truth rows in this report (row #4 saturated UL canary, row #5
24h soak) remain `FAILED` — they are not retroactively marked verified. The
other observable-truth rows (#1 schema/fallback, #2 SAFE-06 unknown-key warning,
#3 Spectrum YAML adoption, #6 changelog/docs) remain `VERIFIED`.

VALN-06 traceability is now carried by Phase 201's `## Inherited Requirements`
block as an **inherited blocking requirement** — Phase 201 SPEC and PLAN must
carry VALN-06 forward and cannot silently drop it during 201 scoping. See
`200-RETRO.md` `## Final Closure (2026-05-04)` for the full operator-decision
narrative and v1.42 lessons, and `canary/20260504T133207Z/verdict.json` for the
direct Attempt 3 evidence (`verdict: fail`, `ul_floor_hits_during_load: 4`).
```

Do not modify any other content. The `status: gaps_found` value, the existing `gaps:` list, the failed Goal Achievement table rows, and the existing `### Gaps Summary` paragraph are preserved verbatim.
  </action>
  <acceptance_criteria>
    <automated>grep -q '^closure: deferred-to-phase-201$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q '^status: gaps_found$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q '### Closure Decision (2026-05-04, operator-escalated)' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'inherits_to: phase-201-docsis-aware-ul-congestion-control' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'inherited_as: blocking_requirement' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'no_second_remediation_attempted: true' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'two failed-truth rows' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'must be reconciled before any future Spectrum deploy' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; ! grep -iE 'harmless no-op|benign no-op|harmless under the older binary|benign under the older binary' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; ! grep -q 'four failed-truth rows' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^closure: deferred-to-phase-201$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q '^status: gaps_found$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q '### Closure Decision (2026-05-04, operator-escalated)' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md &amp;&amp; grep -q 'two failed-truth rows' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md frontmatter carries `closure: deferred-to-phase-201`, the closure_decision sub-block (including `prod_yaml_state` reconciliation language and `inherited_as: blocking_requirement`); status remains `gaps_found`; the body has the new `### Closure Decision (2026-05-04, operator-escalated)` subsection; narrative says "two failed-truth rows" not "four"; no failed truth row was flipped to verified; no "harmless no-op" or "benign no-op" language for prod YAML.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md VALN-06 row to deferred state with inherited-blocking notation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1.41 traceability table at lines 103-108 plus the VALN-06 requirement statement at lines 24-25; row format is three pipe-separated columns: `| Requirement | Phase | Status |` — column 3 carries both status and notes/artifact pointers)
  </read_first>
  <action>
Make exactly two edits to `REQUIREMENTS.md`. Do NOT modify the ARB-05, SAFE-06, or DOCS-03 rows. Do NOT alter the v1.39 traceability table.

**Edit 1 — VALN-06 traceability row (currently line 107).**

Current row (verbatim):

```
| VALN-06 | Phase 200 | Blocked (`200-VERIFICATION.md`; Plan 200-14 Attempt 3 canary failed with 4 floor hits, rollback executed, soak skipped) |
```

Replace with this exact line (matches the existing three-column structure: `| Requirement | Phase | Status |`; column 3 carries both the status field and the notes/artifact pointers, consistent with how other rows in this table embed status + notes):

```
| VALN-06 | Phase 200 (deferred to Phase 201) | Deferred -> Phase 201 (inherited blocking requirement) (`200-VERIFICATION.md` closure: deferred-to-phase-201; `200-RETRO.md` Final Closure 2026-05-04; `201-CONTEXT.md` Inherited Requirements; `canary/20260504T133207Z/verdict.json`). Plan 200-14 Attempt 3 canary improved 122 -> 4 UL floor hits but did not reach zero; operator escalated to DOCSIS-aware control (Phase 201) rather than running a second Phase 200 gap-closure cycle. **Inherited as blocking requirement; Phase 201 SPEC and PLAN must carry VALN-06 forward — cannot be silently dropped during 201 scoping.** |
```

**Edit 2 — VALN-06 requirement statement appendix (currently lines 24-25).**

The current statement ends with: `... See \`200-VERIFICATION.md\`.`

Replace that closing sentence with this exact wording:

```
See `200-VERIFICATION.md`. **Operator-escalated 2026-05-04: deferred to Phase 201 (`docsis-aware-ul-congestion-control`) as an inherited blocking requirement** because Phase 200's per-direction-thresholds hypothesis was diagnosed insufficient by its own RETRO; the residual failure regime is shaping-headroom dominated, not threshold dominated. Phase 201 SPEC and PLAN must carry VALN-06 forward; it cannot be silently dropped during 201 scoping. See `200-RETRO.md` Final Closure section and `201-CONTEXT.md` Inherited Requirements block.
```

Do not modify the leading `[ ]` checkbox marker on the VALN-06 line — VALN-06 is not satisfied; deferral is not satisfaction.
  </action>
  <acceptance_criteria>
    <automated>grep -q 'VALN-06 | Phase 200 (deferred to Phase 201) | Deferred -&gt; Phase 201 (inherited blocking requirement)' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Inherited as blocking requirement; Phase 201 SPEC and PLAN must carry VALN-06 forward' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Operator-escalated 2026-05-04: deferred to Phase 201' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'as an inherited blocking requirement' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^- \[ \] \*\*VALN-06\*\*' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'VALN-06 | Phase 200 | Blocked' .planning/REQUIREMENTS.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VALN-06 | Phase 200 (deferred to Phase 201)' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'VALN-06 | Phase 200 | Blocked' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>VALN-06 row reads `Deferred -> Phase 201 (inherited blocking requirement)` with artifact pointers to 200-VERIFICATION.md, 200-RETRO.md, 201-CONTEXT.md, and the Attempt 3 verdict.json; the row's notation explicitly states Phase 201 SPEC/PLAN must carry VALN-06 forward; the requirement statement carries the operator-escalation appendix with the same language; ARB-05, SAFE-06, DOCS-03 rows untouched; checkbox remains unchecked.</done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md to record Phase 200 closure with VALN-06 inheritance</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (`## Blockers` section at lines 277-281; `## Current Position` at lines 283-288; `last_updated` and `stopped_at` in frontmatter at lines 6-7 — note the existing format is ISO-8601 with milliseconds, e.g., `"2026-05-04T13:59:50.000Z"`; `## Decisions` last entry at line 227)
  </read_first>
  <action>
Make exactly four edits to `STATE.md`. Do NOT touch the v1.39, v1.40, or v1.41 historical Decisions/Performance Metrics entries other than the appendix described below.

**Edit 1 — Frontmatter `stopped_at` and `last_updated` (lines 6-7).**

Current:

```
stopped_at: Completed 200-15-PLAN.md — Phase 200 closed as gaps_found
last_updated: "2026-05-04T13:59:50.000Z"
```

Replace with (the executor MUST substitute a real UTC timestamp at execution time — do NOT commit a hardcoded fabricated value or the literal `$(date ...)` expression):

```
stopped_at: Completed 200-16-PLAN.md — Phase 200 sealed; VALN-06 deferred to Phase 201 (operator escalation 2026-05-04)
last_updated: "<ACTUAL_EXECUTION_UTC>"
```

**Executor instructions for the timestamp:**

```bash
NOW="$(date -u +%Y-%m-%dT%H:%M:%S.000Z)"
# Verify it falls on 2026-05-04 (sanity check against fabrication):
[[ "$NOW" == 2026-05-04T* ]] || { echo "Refusing to commit: clock is not 2026-05-04"; exit 1; }
sed -i "s|^last_updated: \"2026-05-04T13:59:50.000Z\"$|last_updated: \"${NOW}\"|" .planning/STATE.md
```

The committed file must contain a real ISO-8601 timestamp matching the existing format `^"2026-05-04T[0-9]{2}:[0-9]{2}:[0-9]{2}\.000Z"$` — not the literal placeholder `<ACTUAL_EXECUTION_UTC>` and not the literal `$(date ...)` string.

**Edit 2 — `## Blockers` first bullet (line 278).**

Current first bullet:

```
- Phase 200 remains gaps_found after gap-closure cycle 1: VALN-06 failed because Plan 200-14 Attempt 3 canary `20260504T133207Z` recorded 4 UL floor hits during the 900s loaded window; D-10 rollback restored `/opt/wanctl-prephase200-gap-20260504T132936Z.tar.gz`, the 24h soak was skipped fail-closed, and a second gap-closure cycle or operator escalation is pending.
```

Replace with this exact bullet:

```
- VALN-06 inherited by Phase 201 (`docsis-aware-ul-congestion-control`) as a blocking requirement per operator escalation 2026-05-04. Phase 200 closed as `gaps_found` after gap-closure cycle 1: Plan 200-14 Attempt 3 canary `20260504T133207Z` improved loaded-window UL floor hits from 122 (Attempt 2) to 4 but did not reach the zero-hit gate; D-10 rollback restored `/opt/wanctl-prephase200-gap-20260504T132936Z.tar.gz`; the 24h soak was skipped fail-closed. No second Phase 200 remediation cycle was attempted; the residual failure regime is shaping-headroom dominated, which is Phase 201's scope. Production binary remains v1.40; v1.41 YAML keys remain on prod `/etc/wanctl/spectrum.yaml` and are inactive under v1.40 but MUST be reconciled before any future Spectrum deploy/restart (Phase 201 predeploy gate). See `200-VERIFICATION.md` `closure: deferred-to-phase-201`, `200-RETRO.md` `## Final Closure (2026-05-04)`, and `201-CONTEXT.md` `## Inherited Requirements`.
```

**Edit 3 — `## Current Position` block (lines 285-288).**

Current:

```
Phase: 200 (per-direction-rtt-bloat-thresholds) — BLOCKED-2nd-stage / gaps_found
Plan: 15 of 15
Status: Closed as gaps_found; VALN-06 remains unsatisfied after Attempt 3 canary fail and skipped soak
Last activity: 2026-05-04
```

Replace with:

```
Phase: 200 (per-direction-rtt-bloat-thresholds) — CLOSED / gaps_found / VALN-06 deferred to Phase 201
Plan: 16 of 16
Status: Phase 200 sealed via operator-escalated deferral 2026-05-04; v1.41 milestone closed with VALN-06 carried forward to Phase 201 / v1.42 as an inherited blocking requirement. ARB-05, SAFE-06, DOCS-03 satisfied. Production on v1.40 binary post-rollback; v1.41 YAML keys remain on prod /etc/wanctl/spectrum.yaml and are inactive under v1.40 but MUST be reconciled before any future Spectrum deploy/restart (Phase 201 predeploy gate).
Last activity: 2026-05-04
```

**Edit 4 — Append a Decisions entry.**

Append the following bullet to the END of the `## Decisions` section (immediately after the last existing `[Phase 200 gap closure]` bullet at line 227):

```
- [Phase 200 closure 2026-05-04]: Operator escalated VALN-06 to Phase 201 (`docsis-aware-ul-congestion-control`) as an inherited blocking requirement rather than entering a second Phase 200 gap-closure cycle. Rationale: Phase 200's RETRO concluded the per-direction-thresholds hypothesis is the wrong fix; the residual 4 loaded-window floor hits live in a shaping-headroom regime that is Phase 201's seeded scope. No second remediation attempted; no production binary change (Spectrum on v1.40); v1.41 YAML keys remain on prod /etc/wanctl/spectrum.yaml inactive under v1.40 but MUST be reconciled before any future Spectrum deploy/restart — a Phase 201 predeploy gate is required.
```

Do not append to `## Performance Metrics` — Plan 200-16 is documentation-only and the existing 200-15 metric line already records the closeout cycle.
  </action>
  <acceptance_criteria>
    <automated>grep -q '200-16-PLAN.md' .planning/STATE.md &amp;&amp; grep -q 'VALN-06 inherited by Phase 201' .planning/STATE.md &amp;&amp; grep -q 'CLOSED / gaps_found / VALN-06 deferred to Phase 201' .planning/STATE.md &amp;&amp; grep -q 'Plan: 16 of 16' .planning/STATE.md &amp;&amp; grep -q '\[Phase 200 closure 2026-05-04\]' .planning/STATE.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/STATE.md &amp;&amp; grep -q 'MUST be reconciled before any future Spectrum deploy' .planning/STATE.md &amp;&amp; grep -qE '^last_updated: "2026-05-04T[0-9]{2}:[0-9]{2}:[0-9]{2}\.000Z"$' .planning/STATE.md &amp;&amp; ! grep -q 'BLOCKED-2nd-stage / gaps_found' .planning/STATE.md &amp;&amp; ! grep -q 'a second gap-closure cycle or operator escalation is pending' .planning/STATE.md &amp;&amp; ! grep -q '<ACTUAL_EXECUTION_UTC>' .planning/STATE.md &amp;&amp; ! grep -q '\$(date' .planning/STATE.md &amp;&amp; ! grep -iE 'harmless no-op|benign no-op' .planning/STATE.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VALN-06 inherited by Phase 201' .planning/STATE.md &amp;&amp; grep -q 'Plan: 16 of 16' .planning/STATE.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/STATE.md &amp;&amp; ! grep -q 'BLOCKED-2nd-stage / gaps_found' .planning/STATE.md &amp;&amp; ! grep -q '<ACTUAL_EXECUTION_UTC>' .planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter cites Plan 200-16 with a real (substituted) ISO-8601 timestamp matching existing format `2026-05-04T[HH:MM:SS].000Z`; the prior "second gap-closure cycle or operator escalation is pending" blocker is removed and replaced with VALN-06 inheritance language including the inherited-blocking notation and prod-YAML reconciliation language; Current Position reads CLOSED / VALN-06 deferred; a new Decisions bullet records the 2026-05-04 escalation; no "harmless no-op" or "benign no-op" language present; no placeholder strings present.</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md Phase 200 row, plan count, and success-criteria deferrals</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase Summary table line 124; Phase 200 detail block lines 128-163; success criteria lines 156-161)
  </read_first>
  <action>
Make exactly three edits to `ROADMAP.md`. Do NOT modify the v1.39 roadmap section, the v1.40 archive reference, or any unrelated phase rows.

**Edit 1 — Phase Summary table row (line 124).**

Current:

```
| 200 | Per-Direction RTT Bloat Thresholds (Spectrum UL Saturation Containment) | 15/15 | Blocked — VALN-06 second canary failed; second gap-closure cycle pending | 2026-05-04 |
```

Replace with this exact row:

```
| 200 | Per-Direction RTT Bloat Thresholds (Spectrum UL Saturation Containment) | 16/16 | Closed (gaps_found) — ARB-05/SAFE-06/DOCS-03 satisfied; VALN-06 deferred to Phase 201 (inherited blocking requirement) by operator escalation 2026-05-04 | 2026-05-04 |
```

**Edit 2 — Phase 200 success criteria 3 and 4 deferral annotations.**

Current criterion 3 (line 159):

```
3. The 10–15 min saturated `iperf3 -P4` UL canary at 18 Mbit ceiling on Spectrum completes without the UL controller collapsing to the 8 Mbit floor in any cycle. Pre/post idle baselines bookend the run; canary fails if any single cycle reaches floor.
```

Replace with:

```
3. The 10–15 min saturated `iperf3 -P4` UL canary at 18 Mbit ceiling on Spectrum completes without the UL controller collapsing to the 8 Mbit floor in any cycle. Pre/post idle baselines bookend the run; canary fails if any single cycle reaches floor. **(Deferred to Phase 201 — see `200-RETRO.md`. Plan 200-14 Attempt 3 reduced loaded-window floor hits from 122 to 4; zero-hit gate not reached; operator escalated 2026-05-04. Inherited as blocking requirement under Phase 201.)**
```

Current criterion 4 (line 160):

```
4. The 24h Spectrum UL regression soak after canary passes shows UL hysteresis suppression rate drops below 5/60s on average (down from the current 31/60s degraded state).
```

Replace with:

```
4. The 24h Spectrum UL regression soak after canary passes shows UL hysteresis suppression rate drops below 5/60s on average (down from the current 31/60s degraded state). **(Deferred to Phase 201 — see `200-RETRO.md`. Soak watchdog never ran because the canary gate failed; deferral is not a measurement claim. Inherited as blocking requirement under Phase 201.)**
```

Do not check off criteria 3 or 4 — deferral is not satisfaction.

**Edit 3 — Phase 200 closure narrative line (line 163).**

Current:

```
**Plans:** 15/15 plans complete — Phase 200 closed as `gaps_found`; Plan 200-14 Attempt 3 canary failed with 4 UL floor hits, rollback executed, and the 24h soak was skipped. VALN-06 remains blocked pending a second gap-closure cycle or operator escalation.
```

Replace with this exact line:

```
**Plans:** 16/16 plans complete — Phase 200 closed as `gaps_found` with operator-escalated deferral of VALN-06 to Phase 201 (`docsis-aware-ul-congestion-control`) on 2026-05-04 as an inherited blocking requirement. ARB-05, SAFE-06, and DOCS-03 are satisfied. Plan 200-14 Attempt 3 canary improved loaded-window UL floor hits 122 -> 4 but did not reach zero; the residual failure regime is shaping-headroom dominated and is Phase 201's seeded scope. No second Phase 200 remediation was attempted. Production binary remains v1.40 post-rollback; v1.41 YAML keys remain on prod `/etc/wanctl/spectrum.yaml` and are inactive under v1.40 but MUST be reconciled before any future Spectrum deploy or service restart (Phase 201 predeploy gate is required to inspect the file and either reconcile or fail closed). See `200-VERIFICATION.md` `closure: deferred-to-phase-201`, `200-RETRO.md` `## Final Closure (2026-05-04)`, and direct evidence at `canary/20260504T133207Z/verdict.json`.
```

Do not change the `## v1.41 Ordering Rationale` block at lines 167-169.
  </action>
  <acceptance_criteria>
    <automated>grep -q '| 200 | Per-Direction RTT Bloat Thresholds (Spectrum UL Saturation Containment) | 16/16 | Closed (gaps_found)' .planning/ROADMAP.md &amp;&amp; grep -q '16/16 plans complete — Phase 200 closed as `gaps_found` with operator-escalated deferral' .planning/ROADMAP.md &amp;&amp; grep -q 'as an inherited blocking requirement' .planning/ROADMAP.md &amp;&amp; grep -q 'Inherited as blocking requirement under Phase 201' .planning/ROADMAP.md &amp;&amp; grep -q '(Deferred to Phase 201 — see `200-RETRO.md`' .planning/ROADMAP.md &amp;&amp; grep -q 'MUST be reconciled before any future Spectrum deploy or service restart' .planning/ROADMAP.md &amp;&amp; ! grep -q '15/15 plans complete' .planning/ROADMAP.md &amp;&amp; ! grep -q 'second gap-closure cycle pending' .planning/ROADMAP.md &amp;&amp; ! grep -iE 'harmless no-op|benign no-op' .planning/ROADMAP.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q '16/16 plans complete' .planning/ROADMAP.md &amp;&amp; grep -q 'operator-escalated deferral of VALN-06 to Phase 201' .planning/ROADMAP.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/ROADMAP.md &amp;&amp; ! grep -q '15/15 plans complete' .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase Summary table row reads 16/16 with closure-and-deferral language including "inherited blocking requirement"; criteria 3 and 4 carry the parenthetical deferral note plus inherited-blocking notation and remain unchecked; the Phase 200 detail closure line cites operator escalation, the 122->4 improvement, the prod YAML reconciliation requirement (not "harmless no-op"), and direct evidence at `canary/20260504T133207Z/verdict.json`; the v1.41 Ordering Rationale block is untouched.</done>
</task>

<task type="auto">
  <name>Task 6: Append Final Closure section to 200-RETRO.md</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md (existing Plans 09-15 retro section ends near line 92; trailing footer at lines 94-96)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md (Attempt 3 evidence — `4 floor hits`, rollback timestamp `2026-05-04T13:49:19Z`, tarball `/opt/wanctl-prephase200-gap-20260504T132936Z.tar.gz`)
    - .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md (downstream phase context — confirm DOCSIS-aware mode is its scope)
  </read_first>
  <action>
Append the following exact section to `200-RETRO.md`. Insert it BETWEEN the existing `### Cross-milestone reference` block (currently ending at line 91) and the trailing horizontal-rule footer (currently lines 93-96 starting with `---`).

Do not modify any existing retro content. Append-only.

```markdown

## Final Closure (2026-05-04)

**Operator decision:** VALN-06 is **deferred to Phase 201 (`docsis-aware-ul-congestion-control`) as an inherited blocking requirement**. Phase 200 / v1.41 closes as `gaps_found` via operator escalation rather than running a second gap-closure cycle.

### Decision rationale

Two findings drove the escalation:

1. **The hypothesis was already diagnosed insufficient by Phase 200's own retro.** The "What did not work" subsection above states that the residual failure regime — bimodal ceiling/floor oscillation under saturated DOCSIS upload — is dominated by shaping headroom (CMTS-side queue filling before wanctl's qdisc can absorb bufferbloat), not by threshold geometry. Wider thresholds slow the descent but do not stop the controller from reaching the floor when the upstream queue is already deep. This is the architectural conclusion the retro reached after Plans 09-15 ran.

2. **Marginal returns on further Phase 200 work were judged low.** Plans 200-09 -> 200-14 reduced loaded-window UL floor hits from 122 (Attempt 2, 2026-05-03) to 4 (Attempt 3, 2026-05-04 canary `20260504T133207Z`) — a 96.7% improvement under the per-direction-thresholds + R5 + R3 stack. The deploy gate is fail-closed at zero, so the canary still failed and rolled back via `/opt/wanctl-prephase200-gap-20260504T132936Z.tar.gz` at `2026-05-04T13:49:19Z`. The remaining 4 hits live in a regime where additional threshold or decay tuning is unlikely to reach zero without changing the control model. Phase 201 is the right scope for that change; it already exists as a seed and is loaded with the 122-collapse evidence as its design input.

### What was not attempted

- **No second Phase 200 remediation cycle.** The plan to enumerate further candidate causes (additional R-options, lower ceiling, integral-of-RTT) was not undertaken. A second cycle would consume planning + production-canary cost while testing variants of the hypothesis that the retro already rejected as architecturally insufficient.
- **No production binary change.** Spectrum remains on the v1.40 binary post the 2026-05-04T13:49:19Z rollback. The v1.41 YAML keys (`continuous_monitoring.upload.target_bloat_ms=42`, `warn_bloat_ms=105`, `consecutive_yellow_decay_clamp=40`, `factor_down_yellow=1.0`, `ceiling_mbps=18`) remain on disk under `/etc/wanctl/spectrum.yaml` and are inactive under the rolled-back v1.40 binary, but they MUST be reconciled before any future Spectrum deploy or service restart that uses a binary which re-recognizes those keys. A future binary that consumes them would reactivate rejected-hypothesis state silently. This is not a "harmless" condition — it is rejected-hypothesis state sitting in production config and is a real operational risk that Phase 201's PLAN must address with a predeploy gate (inspect `/etc/wanctl/spectrum.yaml` for v1.41-only keys; reconcile or fail closed).
- **No re-cut of v1.41.** The 1.41.0 version was already minted in `pyproject.toml`, `src/wanctl/__init__.py`, and `docker/Dockerfile`, and the CHANGELOG entry stays. The version is recorded in history as the milestone that proved the per-direction-thresholds hypothesis insufficient. v1.42 will pick up the binary again under Phase 201.

### VALN-06 routing

- **Owning phase:** Phase 201 (`docsis-aware-ul-congestion-control`).
- **Inheritance status:** **Inherited blocking requirement.** Phase 201 SPEC and PLAN must carry VALN-06 forward — it cannot be silently dropped during 201 scoping. Future Phase 201 planning that fails to enumerate VALN-06 in its requirements list MUST be treated as a planning defect.
- **Inheritance trail:** `200-VERIFICATION.md` frontmatter `closure: deferred-to-phase-201` + `inherited_as: blocking_requirement` -> `REQUIREMENTS.md` row `Deferred -> Phase 201 (inherited blocking requirement)` -> `201-CONTEXT.md` `## Inherited Requirements` block.
- **Direct evidence pointer:** `.planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260504T133207Z/verdict.json` records the Attempt 3 verdict (`verdict: fail`, `ul_floor_hits_during_load: 4`, `ul_floor_threshold_hit: true`, baseline RTT bookends populated).
- **Closure shape:** Phase 201 will produce its own canary verdict against its own (DOCSIS-aware) control model. VALN-06 closes when that canary passes with zero loaded-window floor hits AND its 24h soak watchdog passes; the same fail-closed gate applies. Phase 201 does NOT inherit Phase 200's "must use 18 Mbit ceiling" or "must use 42/105 ms thresholds" — those were Phase 200's hypothesis-under-test, not the requirement.

### What ARB-05, SAFE-06, DOCS-03 closed

These three Phase 200 requirements remain satisfied and are not affected by the deferral:

- **ARB-05** (per-direction UL thresholds with absent-key fallback and per-key explicit-presence flags) shipped in Plan 200-01, validated by Plan 200-02 and Plan 200-12.
- **SAFE-06** (validator audibly warns on unknown `continuous_monitoring.*` keys) shipped in Plan 200-03 and is exercised by tests in `tests/test_autorate_config.py::TestSafe06UnknownKeyWarning`.
- **DOCS-03** (CHANGELOG.md and docs/CONFIGURATION.md document the new keys plus restart-required migration) shipped in Plan 200-04 and Plan 200-13.

The deferral is scoped to VALN-06 only.

### Open advisory items (do not block VALN-06's Phase 201 path)

- `200-REVIEW.md` WR-01 (Dockerfile unquoted shell-form pip constraints) and IN-01 (stale top-of-file comment) remain advisory. They have no controller-path impact.
- `200-REVIEW.md` WR-02 (canary script Python/PyYAML dependency precheck) remains advisory; Attempt 3's canary did produce a verdict, so this did not affect VALN-06 evidence.
- These can become quick tasks if the operator wants them closed; they are not on the VALN-06 critical path.

### Non-controller mitigation option

While Phase 201 is being designed and shipped, the Spectrum production UL hysteresis storm (5 -> 15 -> 31 suppressions/60s, observed 2026-04-29 onward and again now post-rollback) is operational debt. A non-controller mitigation — e.g., manually lowering Spectrum's deployed UL ceiling well below provisioned upstream rate via YAML-only edit, no code change, AND/OR reverting prod `/etc/wanctl/spectrum.yaml` to its v1.40-shape values to remove the rejected-hypothesis keys — is available as a quick task if the suppression rate becomes operationally painful before Phase 201 ships. Plan 200-16's Task 1 explicitly exposes this as Branch B of the operator countersignature decision. That is a separate operator decision and is **not** a Phase 200 / 201 plan; it is a quick-task mitigation tracked outside the milestone graph.

### Cross-milestone reference

- `.planning/todos/pending/2026-04-24-resolve-att-cake-primary-canary-after-phase-196.md` — the deferred ATT cake-primary canary is unrelated to VALN-06 (different WAN, different control mode) but lives in the same operational neighborhood; v1.42 planning may want to consolidate Spectrum + ATT control-mode work into a single milestone if the timing aligns.

### Lessons reinforced for v1.42

- **A retro that diagnoses the hypothesis as architecturally wrong is a stronger signal than further tuning.** When the retro itself says "the fix is a different control model, not wider thresholds," operator escalation to the next-architecture phase is the correct response, not a second gap-closure cycle on the rejected hypothesis.
- **Diminishing returns under fail-closed gates.** A 96.7% improvement (122 -> 4 floor hits) is operationally meaningful but is not shippable evidence under a zero-hit gate. Future plans involving fail-closed deploy gates should treat "materially improved but still failed" as a distinct, terminal branch — not as license to relax the gate.
- **Operator escalation as a first-class closure path.** ROADMAP.md explicitly allowed operator escalation as a VALN-06 closure path. Treating it as a first-class option (with its own traceability seal — this plan) is cleaner than implicit deferral via inactivity.
- **Rejected-hypothesis state in prod config is real risk, not "harmless."** v1.41 YAML keys on `/etc/wanctl/spectrum.yaml` are inactive under v1.40 but would silently reactivate under any future binary that recognizes them. Future deferrals that leave config-side state behind should mandate a successor-phase predeploy gate, not assume binary rollback alone is sufficient.
```
  </action>
  <acceptance_criteria>
    <automated>grep -q '^## Final Closure (2026-05-04)$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'VALN-06 is \*\*deferred to Phase 201' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'No second Phase 200 remediation cycle' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'Operator escalation as a first-class closure path' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'Gap-Closure Cycle (Plans 09-15)' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'MUST be reconciled before any future Spectrum deploy' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'This is not a "harmless" condition' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'Rejected-hypothesis state in prod config is real risk' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Final Closure (2026-05-04)$' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'Gap-Closure Cycle (Plans 09-15)' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md</automated>
  </verify>
  <done>RETRO.md has the new `## Final Closure (2026-05-04)` appendix between the prior Plans 09-15 cycle section and the footer; all original retro content (Plan 06 outcome, Plans 09-15 cycle, cross-milestone reference) is preserved; the appendix reframes the v1.41 prod YAML state as "rejected-hypothesis state sitting in production config and a real operational risk", explicitly contradicts the "harmless" framing, and adds a v1.42 lesson on rejected-hypothesis state in prod config.</done>
</task>

<task type="auto">
  <name>Task 7: Add Inherited Requirements block to 201-CONTEXT.md (with direct verdict-path citation)</name>
  <files>.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md</files>
  <read_first>
    - .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md (existing seed; the `## Not in Scope for Phase 201` section ends at line 91, footer at lines 93-96)
  </read_first>
  <action>
201-CONTEXT.md exists (read confirmed) and is a pre-spec seed. Append a new `## Inherited Requirements` section. Insert it BETWEEN the existing `## Not in Scope for Phase 201` block (currently ending at line 91) and the trailing footer (lines 93-96 starting with `---`).

Do not modify any existing 201-CONTEXT.md content. Append-only.

```markdown

## Inherited Requirements

This phase inherits the following Phase 200 requirement under the 2026-05-04 operator-escalation deferral. **VALN-06 is an inherited blocking requirement** — Phase 201's eventual SPEC and PLAN must carry it forward and cannot silently drop it during 201 scoping. Future Phase 201 planning that fails to enumerate VALN-06 in its requirements list must be treated as a planning defect.

| Requirement | Inherited From | Reason | Closure Shape Under Phase 201 |
|---|---|---|---|
| **VALN-06** — Spectrum UL saturation gate (10-15 min `iperf3 -P4` saturated upload loop at the deployed UL ceiling completes with zero loaded-window floor-hit cycles, pre/post idle baselines bookend, 24h regression soak as watchdog) | Phase 200 (`200-VERIFICATION.md` `closure: deferred-to-phase-201`, `inherited_as: blocking_requirement`) | Phase 200's per-direction-thresholds hypothesis was diagnosed insufficient by its own RETRO; residual failure regime is shaping-headroom dominated, not threshold dominated. Plans 200-09..200-14 improved loaded-window floor hits 122 -> 4 but the deploy gate is fail-closed at zero. Operator escalated to Phase 201's DOCSIS-aware control model on 2026-05-04 rather than running a second Phase 200 gap-closure cycle. | VALN-06 closes when Phase 201's canary verdict is `pass` with `ul_floor_hits_during_load=0` AND its 24h soak watchdog passes (suppressions <5/60s mean). The same fail-closed gate applies. Phase 201 must NOT inherit Phase 200's specific YAML values (18 Mbit ceiling, 42/105 ms thresholds) — those were the rejected hypothesis-under-test, not part of the requirement. The deployed UL ceiling under Phase 201 is a Phase 201 design choice (likely paired with a DOCSIS-aware setpoint per option D in this CONTEXT). |

### Direct evidence

`.planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260504T133207Z/verdict.json` — Attempt 3 verdict (`verdict: fail`, `ul_floor_hits_during_load: 4`, `ul_floor_threshold_hit: true`, `pre_baseline_rtt_ms: 21.7`, `post_baseline_rtt_ms: 22.23`, `duration_sec: 1022`, `rollback_protocol_recorded: true`). This is the canonical Attempt 3 evidence Phase 201 must improve upon to close VALN-06.

### Inheritance trail

- **Operator decision artifact:** `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md` `## Final Closure (2026-05-04)`.
- **Verification seal:** `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md` frontmatter `closure: deferred-to-phase-201`, `inherited_as: blocking_requirement`, and the `### Closure Decision (2026-05-04, operator-escalated)` body subsection.
- **Requirements ledger:** `.planning/REQUIREMENTS.md` v1.41 traceability row reads `VALN-06 | Phase 200 (deferred to Phase 201) | Deferred -> Phase 201 (inherited blocking requirement) (...)`.
- **State seal:** `.planning/STATE.md` `## Blockers` first bullet records VALN-06 inheritance and the "MUST be reconciled before any future Spectrum deploy/restart" YAML language.

### Required predeploy gate

Phase 201's eventual PLAN MUST include a predeploy gate that inspects `/etc/wanctl/spectrum.yaml` for v1.41-only keys (`continuous_monitoring.upload.target_bloat_ms`, `warn_bloat_ms`, `consecutive_yellow_decay_clamp`, `factor_down_yellow=1.0`, `ceiling_mbps=18` if those values reflect the rejected hypothesis) and either reconciles them with Phase 201's own design choices or fails closed. This is non-optional: the v1.41 YAML keys are currently inactive under the rolled-back v1.40 binary, but a future Phase 201 binary that re-recognizes them would reactivate rejected-hypothesis state silently.

### What Phase 201 does NOT inherit

- Any specific YAML threshold values from Phase 200 (those were the hypothesis-under-test and were rejected).
- Phase 200's specific R5 (`factor_down_yellow=1.0`) and R3 (`consecutive_yellow_decay_clamp=40`) Spectrum-side YAML decisions — Phase 201 may keep, override, or remove them based on its own design.
- Phase 200's `200-REVIEW.md` advisory items (WR-01, WR-02, IN-01) — those are documentation/script hygiene and not on the VALN-06 critical path.

### What Phase 201 DOES inherit (process)

- The fail-closed deploy-gate model (D-07): saturation canary as primary gate, 24h soak as watchdog, predefined rollback (D-10) on canary fail.
- The verified canary tooling: `scripts/phase200-saturation-canary.sh` and its env template are reusable; the Plan 200-11 jq fix and the Plan 200-12 ordering precheck are now part of that tooling.
- The cross-AI review pattern (Codex + Claude) before any production-control change — flagged by Phase 200 as high-leverage and worth keeping for v1.42.
```
  </action>
  <acceptance_criteria>
    <automated>grep -q '^## Inherited Requirements$' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'VALN-06.*Spectrum UL saturation gate' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'closure: deferred-to-phase-201' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'inherited_as: blocking_requirement' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'Phase 201 does NOT inherit' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'Why Phase 201 Exists' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'canary/20260504T133207Z/verdict.json' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'inherited blocking requirement' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'Required predeploy gate' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## Inherited Requirements$' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'VALN-06' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q 'Why Phase 201 Exists' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md &amp;&amp; grep -q '20260504T133207Z/verdict.json' .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md</automated>
  </verify>
  <done>201-CONTEXT.md has the new `## Inherited Requirements` section appended between `## Not in Scope for Phase 201` and the trailing footer; existing seed content (Why Phase 201 Exists, Root Cause Hypothesis, Candidate Design Directions, Constraints, Pre-Spec Questions, Inputs Available, Not in Scope) is unchanged; the new section directly cites `canary/20260504T133207Z/verdict.json` with verdict fields, marks VALN-06 as an inherited blocking requirement, and mandates a Phase 201 predeploy gate against the prod YAML state.</done>
</task>

<task type="auto">
  <name>Task 8: Hot-path regression slice and project-finalizer commit (with allowed-list scope check)</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Git Workflow section: project-finalizer is mandatory before commit; pre-commit hook enforces it)
  </read_first>
  <action>
This plan touches no source, tests, scripts, configs, or shipped-version files. The hot-path slice is a paranoia check — if it fails, something outside this plan's intended scope has been edited.

1. Run the hot-path slice as documented in CLAUDE.md:
   ```bash
   .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
   ```
2. Run `project-finalizer` agent per CLAUDE.md mandatory pre-commit policy.
3. Stage exactly the eight files this plan modified or generates (the six closeout-state files plus this plan and its SUMMARY):
   - `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md`
   - `.planning/REQUIREMENTS.md`
   - `.planning/STATE.md`
   - `.planning/ROADMAP.md`
   - `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md`
   - `.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md`
   - `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-PLAN.md`
   - `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-SUMMARY.md`

   Do NOT stage `200-REVIEWS.md` or `200-REVIEWS-cycle1.md` — both were already committed in earlier commits (`9cffc36`, `2fab80c`) and have no changes in this plan's scope.

4. Commit with message:
   ```
   docs(200-16): seal Phase 200 closure with VALN-06 deferral to Phase 201
   ```

5. **Negative scope check (forbidden paths absent):**
   ```bash
   ! git show --stat HEAD -- 'src/**' 'tests/**' 'scripts/**' 'configs/**' 'docker/**' pyproject.toml .dockerignore CHANGELOG.md 'docs/**' 2>/dev/null | grep -E '^\s+(src|tests|scripts|configs|docker|pyproject\.toml|\.dockerignore|CHANGELOG\.md|docs)/'
   ```
   Acceptance: command produces no matching output.

6. **Positive allowed-list check (every committed path must be allowed-listed):**
   ```bash
   git show --name-only --format='' HEAD | sort -u > /tmp/200-16-committed-files
   cat <<'ALLOWED' | sort -u > /tmp/200-16-allowed-files
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
   .planning/REQUIREMENTS.md
   .planning/STATE.md
   .planning/ROADMAP.md
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md
   .planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-PLAN.md
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-SUMMARY.md
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md
   .planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS-cycle1.md
   ALLOWED
   comm -23 /tmp/200-16-committed-files /tmp/200-16-allowed-files
   ```
   Acceptance: `comm -23` produces empty output (every committed path is in the allowed-list). Equivalently:
   ```bash
   [ -z "$(comm -23 /tmp/200-16-committed-files /tmp/200-16-allowed-files)" ]
   ```

   Note: The allowed-list includes `200-REVIEWS.md` and `200-REVIEWS-cycle1.md` defensively (they should NOT appear in the commit because they were committed earlier and are unchanged in this plan, but if they were inadvertently re-staged the positive check still passes — the negative check above already guards against forbidden paths). The intersection of "actually committed" and "allowed-list" is what matters.

7. Confirm `git status` is clean post-commit.
  </action>
  <acceptance_criteria>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q &amp;&amp; ! git show --stat HEAD -- 'src/**' 'tests/**' 'scripts/**' 'configs/**' 'docker/**' pyproject.toml .dockerignore CHANGELOG.md 'docs/**' 2>/dev/null | grep -E '^\s+(src|tests|scripts|configs|docker|pyproject\.toml|\.dockerignore|CHANGELOG\.md|docs)/' &amp;&amp; git show --name-only --format='' HEAD | sort -u > /tmp/200-16-committed-files &amp;&amp; printf '%s\n' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md' '.planning/REQUIREMENTS.md' '.planning/STATE.md' '.planning/ROADMAP.md' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-RETRO.md' '.planning/phases/201-docsis-aware-ul-congestion-control/201-CONTEXT.md' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-PLAN.md' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-SUMMARY.md' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS.md' '.planning/phases/200-per-direction-rtt-bloat-thresholds/200-REVIEWS-cycle1.md' | sort -u > /tmp/200-16-allowed-files &amp;&amp; [ -z "$(comm -23 /tmp/200-16-committed-files /tmp/200-16-allowed-files)" ]</automated>
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <done>Hot-path slice green; closeout commit landed via project-finalizer; the commit's file list contains zero entries under `src/`, `tests/`, `scripts/`, `configs/`, `docker/`, `pyproject.toml`, `.dockerignore`, `CHANGELOG.md`, or `docs/` (negative check); every committed path appears in the allowed-list (positive check via `comm -23`).</done>
</task>

</tasks>

<verification>
- 200-VERIFICATION.md frontmatter contains `closure: deferred-to-phase-201`, `inherits_to: phase-201-docsis-aware-ul-congestion-control`, `inherited_as: blocking_requirement`, and the `prod_yaml_state` reconciliation language; `status: gaps_found` is preserved; the `### Closure Decision (2026-05-04, operator-escalated)` subsection is present in the body and says "two failed-truth rows" (not four); no failed truth row was flipped to verified; no "harmless no-op" or "benign no-op" framing for prod YAML.
- REQUIREMENTS.md VALN-06 traceability row reads `Deferred -> Phase 201 (inherited blocking requirement)` with four artifact pointers (200-VERIFICATION.md, 200-RETRO.md, 201-CONTEXT.md, canary/20260504T133207Z/verdict.json); the row contains the explicit notation that Phase 201 SPEC and PLAN must carry VALN-06 forward; the requirement statement carries the operator-escalation appendix with the same language; the leading `[ ]` checkbox is unchanged; ARB-05, SAFE-06, DOCS-03 rows untouched.
- STATE.md frontmatter cites Plan 200-16 with a real (substituted) ISO-8601 timestamp matching `^"2026-05-04T[0-9]{2}:[0-9]{2}:[0-9]{2}\.000Z"$`; `## Blockers` first bullet starts with "VALN-06 inherited by Phase 201" and includes the "MUST be reconciled before any future Spectrum deploy/restart" YAML language; `## Current Position` reads CLOSED / VALN-06 deferred to Phase 201 with `Plan: 16 of 16` and the inherited-blocking-requirement notation; `## Decisions` has the new `[Phase 200 closure 2026-05-04]` bullet appended.
- ROADMAP.md Phase 200 row in the Phase Summary table reads `16/16 | Closed (gaps_found) — ...VALN-06 deferred to Phase 201 (inherited blocking requirement)...`; success criteria 3 and 4 carry `(Deferred to Phase 201 — see 200-RETRO.md...Inherited as blocking requirement under Phase 201)` parentheticals and remain unchecked; the Phase 200 detail closure paragraph reads `16/16 plans complete — Phase 200 closed as gaps_found with operator-escalated deferral...as an inherited blocking requirement` and includes the prod-YAML reconciliation language plus the direct verdict.json pointer.
- 200-RETRO.md has the new `## Final Closure (2026-05-04)` appendix with the "inherited blocking requirement" notation, the explicit "this is not a 'harmless' condition" reframing of the prod YAML state, and a v1.42 lesson on rejected-hypothesis state in prod config; existing Plans 09-15 retro content is preserved.
- 201-CONTEXT.md has the new `## Inherited Requirements` section appended between `## Not in Scope for Phase 201` and the footer; the section marks VALN-06 as an inherited blocking requirement, directly cites `canary/20260504T133207Z/verdict.json` with verdict fields, and mandates a Phase 201 predeploy gate against the prod YAML state; existing seed content is unchanged.
- Hot-path test slice still green (no source touched in this plan); commit's `git show --stat` does not list any path under `src/`, `tests/`, `scripts/`, `configs/`, `docker/`, `pyproject.toml`, `.dockerignore`, `CHANGELOG.md`, or `docs/` (negative check); every committed path appears in the allowed-list (positive check via `comm -23 /tmp/200-16-committed-files /tmp/200-16-allowed-files` empty).
</verification>

<success_criteria>
1. All six planning files reflect a consistent picture of Phase 200's operator-escalated deferral closure as an inherited blocking requirement.
2. VERIFICATION.md status remains exactly `gaps_found` and no failed truth row is silently marked verified; the deferral is recorded as a separate frontmatter field, not as a status mutation; the body narrative says "two failed-truth rows" (matching the actual VERIFIED-vs-FAILED count in the report).
3. VALN-06 traceability is consistent across REQUIREMENTS.md, STATE.md, ROADMAP.md, 200-VERIFICATION.md, 200-RETRO.md, and 201-CONTEXT.md — every artifact says "Deferred to Phase 201 (inherited blocking requirement)" via operator escalation 2026-05-04, AND every artifact reframes the prod v1.41 YAML as "inactive under v1.40 but MUST be reconciled before any future Spectrum deploy/restart" (no "harmless"/"benign"/"no-op" framing).
4. RETRO.md records the operator decision as a first-class closure path with a `## Final Closure (2026-05-04)` appendix preserving all prior cycle history and adds a v1.42 lesson on rejected-hypothesis state in prod config.
5. 201-CONTEXT.md `## Inherited Requirements` directly cites the Attempt 3 verdict path `canary/20260504T133207Z/verdict.json` and mandates a Phase 201 predeploy gate against `/etc/wanctl/spectrum.yaml`.
6. Operator countersignature checkpoint (Task 1) presents three explicit branches (A/B/C); Task 1's `<resume-signal>` enumerates exactly those three options.
7. Hot-path test slice green.
8. project-finalizer ran before commit; commit contains zero source/test/script/config/Docker/shipped-version file changes (negative check); every committed path appears in the allowed-list (positive check via `comm -23`).
9. STATE.md `last_updated` carries a real substituted ISO-8601 timestamp at execution time (no fabricated value, no literal `$(date ...)` string, no placeholder).
</success_criteria>

<follow_ons>
The following items are explicitly OUT OF SCOPE for Plan 200-16 but are recorded here so the operator can route them onward without re-discovering them:

- **Phase 201 (`docsis-aware-ul-congestion-control`)** is the owning phase for VALN-06 going forward, and VALN-06 is an **inherited blocking requirement** — Phase 201's SPEC and PLAN must carry it forward and cannot silently drop it during 201 scoping. The 201-CONTEXT.md seed already enumerates four candidate design directions (A: DOCSIS-aware mode with low setpoint; B: substantially lower ceiling; C: time-weighted RTT integral; D: hybrid). Operator should run `/gsd-discuss-phase 201` next to clarify root cause + design direction before any implementation planning.

- **Phase 201 PLAN must include a predeploy gate that inspects `/etc/wanctl/spectrum.yaml` for v1.41-only keys and either reconciles or fails closed.** Operator may also run a quick task NOW (Branch B of Task 1) to revert the prod YAML to v1.40-shape values; see Task 1 mitigation branch. Either path is acceptable; doing nothing leaves rejected-hypothesis state in prod config with a binary-rollback-only safety net.

- **Non-controller mitigation for the Spectrum UL hysteresis storm.** While Phase 201 is being designed and shipped, the operator may want to manually lower Spectrum's deployed UL ceiling well below the provisioned upstream rate via a YAML-only edit (no code change), AND/OR revert prod `/etc/wanctl/spectrum.yaml` to its v1.40-shape values. This is a quick task, not a Phase 200 / Phase 201 plan, and lives outside the milestone graph. Operator can elect this via Branch B of Task 1's countersignature; track via `.planning/quick/` if pursued.

- **`200-REVIEW.md` advisory items remain open as advisory only.**
  - WR-01: `docker/Dockerfile` lines 46-52 unquoted shell-form pip constraints (`requests>=...`) can be parsed as redirections by some shells; packaging-correctness risk, no controller-path impact.
  - WR-02: `scripts/phase200-saturation-canary.sh` lines 328-338 lack an explicit Python/PyYAML dependency precheck before the SSH-based remote YAML probe; on a host missing PyYAML the script could exit without writing an abort verdict (Attempt 3 was unaffected because cake-shaper has PyYAML).
  - IN-01: `docker/Dockerfile` lines 5-7 stale top-of-file build-invocation comment; documentation polish only.
  These can become quick tasks if the operator wants them closed; none are on the VALN-06 critical path and none block Phase 201.

- **Cross-milestone reference: `.planning/todos/pending/2026-04-24-resolve-att-cake-primary-canary-after-phase-196.md`** — the deferred ATT cake-primary canary remains unrelated to VALN-06 (different WAN, different control mode) but lives in the same operational neighborhood. v1.42 planning may want to consolidate Spectrum + ATT control-mode work into a single milestone if the timing aligns; that is a v1.42 roadmap decision, not a Phase 200 / 201 decision.

- **No re-cut of v1.41 is planned.** The 1.41.0 version stamp in `pyproject.toml`, `src/wanctl/__init__.py`, and `docker/Dockerfile` stays. The CHANGELOG entry stays. v1.42 will pick up the binary again under Phase 201.
</follow_ons>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-16-SUMMARY.md` recording: outcome (operator-escalated deferral closure as inherited blocking requirement), which Task 1 branch the operator selected (A/B/C), final 200-VERIFICATION.md frontmatter shape (status: gaps_found + closure: deferred-to-phase-201 + inherited_as: blocking_requirement + prod_yaml_state reconciliation language), REQUIREMENTS.md VALN-06 row text, STATE.md blocker delta (old removed, new added) and the actual substituted `last_updated` timestamp value, ROADMAP.md plan-count and closure narrative deltas, 200-RETRO.md `## Final Closure (2026-05-04)` appendix path, 201-CONTEXT.md `## Inherited Requirements` block path with direct verdict.json citation, hot-path slice result, the positive allowed-list scope check result, and a one-paragraph closeout statement suitable for archive review noting that no production binary state was changed, that the v1.41 prod YAML is "inactive but must be reconciled" (NOT "harmless"), and that VALN-06 is now Phase 201's inherited blocking requirement.
</output>
</content>
</invoke>
