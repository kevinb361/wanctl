---
phase: 200-per-direction-rtt-bloat-thresholds
plan: 14
type: execute
wave: 3
depends_on:
  - "200-10"
  - "200-11"
  - "200-12"
  - "200-13"
gap_closure: true
files_modified:
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/
  - .planning/phases/200-per-direction-rtt-bloat-thresholds/soak/
autonomous: false
requirements:
  - VALN-06
user_setup:
  - service: cake-shaper (production Spectrum host)
    why: "Saturated UL canary requires production deploy + iperf3 -P4 against a remote endpoint; cannot be simulated in CI"
    env_vars:
      - name: PHASE200_REMOTE_YAML_SSH
        source: "operator-supplied; format user@host:/path/to/spectrum.yaml"
      - name: PHASE200_IPERF_TARGET
        source: "operator-supplied iperf3 server (104.200.21.31 used in attempt 1)"
      - name: PHASE200_IPERF_LOCAL_BIND
        source: "operator-supplied source IP for iperf3 (10.10.110.226 used in attempt 1)"
    dashboard_config:
      - task: "Verify no concurrent Spectrum experiment is running before canary launch"
        location: "operator confirmation"
      - task: "Capture pre-deploy /opt/wanctl rollback tarball before deploy"
        location: "cake-shaper:/opt/wanctl-prephase200-<UTC_TS>.tar.gz"

must_haves:
  truths:
    - "Re-run of `scripts/phase200-saturation-canary.sh` against the gap-closure binary (Plan 200-10 + Plan 200-11 fixes deployed) produces verdict.json with `verdict: \"pass\"` and `ul_floor_hits_during_load: 0` (gap text: 'The 10-15 min saturated iperf3 -P4 UL canary at 18 Mbit completes without the UL controller collapsing to the 8 Mbit floor in any cycle')."
    - "Re-run verdict.json has non-null `pre_baseline_rtt_ms` and `post_baseline_rtt_ms` (proves Plan 200-11 baseline RTT fix landed in deployed binary)."
    - "Subsequent 24h Spectrum UL regression soak shows mean UL hysteresis suppressions <5/60s (gap text: 'The 24h Spectrum UL regression soak after canary passes shows UL hysteresis suppression rate below 5/60s on average')."
    - "If canary FAILS again, D-10 rollback executes and Plan 200-15 closes the phase as `gaps_found_again` rather than `verified` — the operator can choose to extend gap closure or accept the rollback."
  artifacts:
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<new-UTC>/verdict.json"
      provides: "Re-canary verdict (pass or fail)"
      contains: "verdict"
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/soak/<new-UTC>/"
      provides: "24h soak capture (only on canary pass)"
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md"
      provides: "Updated with Attempt 3 / gap-closure deploy timeline"
      contains: "Attempt 3"
    - path: ".planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md"
      provides: "Updated with soak verdict (or blocked-again note on canary fail)"
  key_links:
    - from: "Plan 200-10 deployed binary"
      to: "Re-canary verdict"
      via: "scripts/phase200-saturation-canary.sh against /opt/wanctl on cake-shaper"
      pattern: "verdict"
    - from: "Plan 200-11 jq path fix"
      to: "verdict.json baseline RTT fields"
      via: "summarize_baseline() output"
      pattern: "pre_baseline_rtt_ms"
---

<objective>
Re-validate Phase 200's deploy gate against the gap-closure binary. Re-run the D-07 saturated UL canary using the (fixed) `scripts/phase200-saturation-canary.sh` and the (changed) Spectrum YAML/binary from Plan 200-10. On pass, launch the 24h regression soak. On fail, execute D-10 rollback and surface a second-stage gap to the operator.

Purpose: VALN-06 cannot close without a passed canary + 24h soak. This is the production validation step; nothing else in the phase can substitute for it.

Output: New `canary/<UTC>/` evidence directory, updated `200-DEPLOY-LOG.md` Attempt 3 section, soak directory + `200-SOAK-LOG.md` update on canary pass, or rollback evidence on canary fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-CONTEXT.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-VERIFICATION.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-09-HYPOTHESIS.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-10-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-11-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-05-SUMMARY.md
@.planning/phases/200-per-direction-rtt-bloat-thresholds/200-06-SUMMARY.md
@scripts/phase200-saturation-canary.sh
@scripts/phase200-saturation-canary.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Operator pre-deploy gate</name>
  <what-built>Plans 200-10 (controller/YAML), 200-11 (canary script), 200-12 (validator parity), 200-13 (Dockerfile) have all landed and committed; hot-path slice is green on the working tree.</what-built>
  <how-to-verify>
1. `git log --oneline -20` — confirm Plans 10/11/12/13 commits are present.
2. `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py tests/test_check_config_validators.py tests/test_phase200_canary_script.py -q` — must pass.
3. Confirm production /etc/wanctl/spectrum.yaml on cake-shaper does NOT contain unreviewed local edits (compare against `configs/spectrum.yaml` in repo).
4. Confirm no concurrent Spectrum experiment is running.
5. Capture pre-deploy snapshot: `ssh kevin@10.10.110.223 "sudo tar -czf /opt/wanctl-prephase200-gap-<UTC_TS>.tar.gz -C / opt/wanctl"` — record the UTC_TS in `200-DEPLOY-LOG.md` Attempt 3 header.
6. Operator approves deploy.
  </how-to-verify>
  <resume-signal>Reply "approved — pre-deploy snapshot at <UTC_TS>, ready to deploy gap-closure binary" or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy gap-closure binary and verify D-06 explicit-UL log surface</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md (existing Attempt 1 / Attempt 2 sections — copy the heading style)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-06-SUMMARY.md (Plan 06 deploy commands and expected post-restart journal grep)
    - scripts/deploy.sh (deploy command shape)
  </read_first>
  <action>
1. Append a new section to `200-DEPLOY-LOG.md`:

```markdown
## Deploy Timeline (Attempt 3 — Gap-Closure)

- Pre-deploy snapshot: `/opt/wanctl-prephase200-gap-<UTC_TS>.tar.gz` on cake-shaper.
- Deploy commit: `<HEAD-sha>` (working tree HEAD with Plans 10-13 landed).
- Deploy command: `./scripts/deploy.sh spectrum kevin@10.10.110.223` at <UTC>.
- Service restart: `sudo systemctl restart wanctl@spectrum.service` at <UTC>.
- Post-restart `is-active`: <result>.
- Post-restart explicit-UL-thresholds journal grep:
  `journalctl -u wanctl@spectrum.service --since "<deploy-UTC>" | grep "phase200 explicit UL thresholds active"`
  Expected hit: `upload_target_bloat_ms=42 upload_warn_bloat_ms=105` (or whatever Plan 200-10 left in spectrum.yaml).
- Post-restart `/health` upload state, current_rate_mbps, and hysteresis snapshot.
- Local→remote Python-tree sha256 fingerprint: <hash> (must differ from v1.40 fingerprint `b9c7c19...` from Plan 06 rollback).
```

2. Run the actual deploy + restart commands. Capture journal output. Update `200-DEPLOY-LOG.md` with the real values.

3. If the explicit-UL log line is NOT present after restart, ABORT — do not proceed to canary. Document the abort reason and surface it to the operator.

If Plan 200-10's approved direction was R1 (YAML-only), the explicit-UL log line should still be present (it ships with v1.41 binary; Plan 06 attempt 2 already proved this surface works since commit `417e2b9`).
  </action>
  <verify>
    <automated>grep -q "Attempt 3" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md &amp;&amp; grep -q "phase200 explicit UL thresholds active" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md</automated>
  </verify>
  <done>Gap-closure binary deployed; Spectrum service active; D-06 journal grep confirmed. DEPLOY-LOG updated.</done>
</task>

<task type="auto">
  <name>Task 3: Run saturation canary against gap-closure binary</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/canary/, .planning/phases/200-per-direction-rtt-bloat-thresholds/200-DEPLOY-LOG.md</files>
  <read_first>
    - scripts/phase200-saturation-canary.sh (current, post-Plan 200-11 fix)
    - scripts/phase200-saturation-canary.env.example (env var values to use)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-05-SUMMARY.md (canary contract)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/20260503T215734Z/verdict.json (failed reference verdict to compare against)
  </read_first>
  <action>
1. Set environment variables matching Plan 06 attempt 2 (or whatever Plan 200-10 R1 ceiling change requires — if R1 lowered ceiling to 14, set `PHASE200_UL_CEILING_MBPS=14`):
```bash
export PHASE200_OUT_DIR=.planning/phases/200-per-direction-rtt-bloat-thresholds
export PHASE200_SPECTRUM_HEALTH_URL=http://10.10.110.223:9101/health
export PHASE200_IPERF_TARGET=104.200.21.31
export PHASE200_IPERF_LOCAL_BIND=10.10.110.226
export PHASE200_UL_FLOOR_MBPS=8
export PHASE200_UL_CEILING_MBPS=<value matching deployed YAML>
export PHASE200_REMOTE_YAML_SSH=kevin@10.10.110.223:/etc/wanctl/spectrum.yaml
```

2. Execute the canary: `bash scripts/phase200-saturation-canary.sh`.

3. After completion, locate the new run dir: `.planning/phases/200-per-direction-rtt-bloat-thresholds/canary/<NEW-UTC>/`. Verify:
   - `verdict.json` exists.
   - `verdict.json.verdict` is `"pass"` (zero floor hits) or `"fail"` (>= 1 floor hit).
   - `verdict.json.pre_baseline_rtt_ms` is a number, NOT null (proves Plan 200-11 fix landed).
   - `verdict.json.post_baseline_rtt_ms` is a number, NOT null.
   - Script exit code matches verdict (0 = pass, 1 = fail).

4. Append to `200-DEPLOY-LOG.md` a "Canary Run (Attempt 3)" section with:
   - Started, Loaded window start/end, Finished UTC.
   - Sample distribution: count at ceiling, count at floor, count transitional.
   - UL state distribution: GREEN/YELLOW/RED counts.
   - Verdict + script exit code.
   - Pre/post baseline RTT values.

5. **Branch on verdict:**

   **PASS branch (verdict=pass):** proceed to Task 4 (24h soak).

   **FAIL branch (verdict=fail):**
   - Execute D-10 rollback immediately:
     ```bash
     ssh kevin@10.10.110.223 "sudo tar -xzf /opt/wanctl-prephase200-gap-<UTC_TS>.tar.gz -C / && sudo systemctl restart wanctl@spectrum.service"
     ```
   - Verify post-rollback `is-active` and `/health`.
   - Append rollback evidence to `200-DEPLOY-LOG.md`.
   - SKIP Task 4. Document in DEPLOY-LOG that "Plan 200-15 will close phase as gaps_found_again — operator decision required for second gap-closure cycle".
   - Mark this task `done` and stop here.
  </action>
  <verify>
    <automated>NEW=$(ls -1dt .planning/phases/200-per-direction-rtt-bloat-thresholds/canary/*/ 2>/dev/null | head -1) &amp;&amp; test -f "${NEW}verdict.json" &amp;&amp; jq -e '.verdict == "pass" or .verdict == "fail"' "${NEW}verdict.json" &amp;&amp; jq -e '.pre_baseline_rtt_ms != null' "${NEW}verdict.json"</automated>
  </verify>
  <done>
- New canary run dir exists with verdict.json.
- Baseline RTT fields populated (Plan 200-11 fix verified live).
- DEPLOY-LOG updated.
- On FAIL, D-10 rollback executed and recorded.
  </done>
</task>

<task type="auto">
  <name>Task 4: Launch 24h regression soak (PASS branch only)</name>
  <files>.planning/phases/200-per-direction-rtt-bloat-thresholds/soak/, .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md</files>
  <read_first>
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-07-SUMMARY.md (Plan 07 soak contract; was blocked but the plan structure stands)
    - .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md (current blocked record; this task appends a new section, does not delete)
    - scripts/soak-monitor.sh (operator-facing soak helper from CLAUDE.md Quick Operational Commands)
  </read_first>
  <action>
**Skip this task entirely if Task 3 verdict was FAIL.** Mark this task `skipped (canary-fail-branch)`.

If Task 3 verdict was PASS:

1. Define soak window: 24h starting now. Capture UTC_START_TS.

2. Launch the soak via the same mechanism Plan 07 documented (likely `systemd-run --user --on-active=24h ...` plus a 1Hz /health NDJSON capture). Use the Plan 07 SUMMARY as the contract. The soak captures:
   - 1Hz `/health` NDJSON for the full 24h window.
   - UL hysteresis `suppressions_per_min` rolled up to mean over the window.
   - UL state-distribution histogram (GREEN/YELLOW/RED minute buckets).

3. While the soak runs, append a "Soak (Attempt 3)" section to `200-SOAK-LOG.md` with:
   - Soak start UTC, scheduled end UTC.
   - Capture path: `.planning/phases/200-per-direction-rtt-bloat-thresholds/soak/<UTC>/`.
   - Mid-soak progress note (initial /health snapshot).

4. After 24h, compute the soak verdict:
   - Mean UL hysteresis suppressions/60s over the window. Pass if < 5/60s; fail otherwise.
   - Record verdict in `soak/<UTC>/verdict.json` with shape:
     ```json
     {"phase": 200, "soak_run_id": "<UTC>", "duration_sec": 86400, "ul_hysteresis_suppressions_per_60s_mean": <value>, "verdict": "pass" | "fail"}
     ```
   - Append final verdict to `200-SOAK-LOG.md`.

5. If the soak verdict is FAIL, do NOT roll back automatically — document the result and let Plan 200-15 surface to the operator. The canary already passed; a soak miss is a regression-watchdog failure but not necessarily a canary-failure-class outage.

This task is long-running (24h+). The executor should launch the soak as a background job and either wait for completion or hand off to the operator with explicit resume-after instructions.
  </action>
  <verify>
    <automated>SOAK=$(ls -1dt .planning/phases/200-per-direction-rtt-bloat-thresholds/soak/*/ 2>/dev/null | head -1) &amp;&amp; (test -f "${SOAK}verdict.json" &amp;&amp; jq -e '.verdict == "pass" or .verdict == "fail"' "${SOAK}verdict.json") || grep -q "skipped (canary-fail-branch)" .planning/phases/200-per-direction-rtt-bloat-thresholds/200-SOAK-LOG.md</automated>
  </verify>
  <done>
- On canary PASS: 24h soak completed, verdict recorded.
- On canary FAIL: this task explicitly marked skipped in SOAK-LOG.
  </done>
</task>

</tasks>

<verification>
- New canary run directory under `canary/` contains `verdict.json` with non-null baseline RTT bookends.
- `200-DEPLOY-LOG.md` has an "Attempt 3" section.
- `200-SOAK-LOG.md` has either a 24h soak verdict or an explicit "skipped (canary-fail-branch)" record.
- Hot-path test slice still green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
</verification>

<success_criteria>
1. Canary executes against gap-closure binary; verdict captured.
2. Canary verdict.json has populated baseline RTT bookends (Plan 200-11 fix verified live).
3. On pass: 24h soak runs and produces a verdict.
4. On fail: D-10 rollback executes and is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-direction-rtt-bloat-thresholds/200-14-SUMMARY.md` recording: deploy timeline UTCs, canary verdict, baseline RTT bookend values, soak verdict (or skipped), and a recommendation to Plan 200-15 about phase status (verified vs gaps_found_again).
</output>
