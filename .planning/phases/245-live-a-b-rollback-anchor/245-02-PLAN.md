---
phase: 245-live-a-b-rollback-anchor
plan: 02
type: execute
wave: 2
depends_on: ["245-01"]
files_modified:
  - src/wanctl/steering/daemon.py
  - src/wanctl/steering/health.py
  - tests/steering/test_steering_daemon.py
  - tests/steering/test_steering_health.py
autonomous: true
requirements: [AB-01, AB-02, SAFE-17]
must_haves:
  truths:
    - "measure_current_rtt() calls the wanctl RttBackend seam (self.rtt_measurement.probe) FIRST and records the wanctl_backend source on success"
    - "When the seam probe returns None, measure_current_rtt() falls back to autorate_health with the existing fallback chain unchanged"
    - "When the seam probe RAISES an exception, measure_current_rtt() does NOT raise — it logs rate-limited, increments probe_exception_count, and falls through to the autorate fallback chain"
    - "_rtt_source_counts increments a wanctl_backend key when the seam produces a sample, and a probe_exception_count key when probe() raises"
    - "After the flip, steering /health attribution reports producer='wanctl-backend' with backend/source_ip set when the seam produced the sample, and the /health rtt_source.counts block emits wanctl_backend"
    - "The SAFE-17 boundary verifier still passes — the daemon.py + health.py diff is allowlist-bounded with no classification/threshold/state-machine drift"
  artifacts:
    - path: "src/wanctl/steering/daemon.py"
      provides: "Selection-A live flip at four edit sites (frozenset gate, counts dict incl. probe_exception_count, measure_current_rtt seam-first with try/except)"
      contains: "wanctl_backend"
    - path: "src/wanctl/steering/health.py"
      provides: "wanctl_backend count exposed in the /health rtt_source.counts block"
      contains: "wanctl_backend"
  key_links:
    - from: "src/wanctl/steering/daemon.py measure_current_rtt()"
      to: "self.rtt_measurement.probe(hosts)"
      via: "seam-first probe call (wrapped in try/except) before autorate fallback"
      pattern: "self\\.rtt_measurement\\.probe"
    - from: "src/wanctl/steering/daemon.py _WANCTL_BACKEND_RTT_SOURCES"
      to: "the Phase 244 attribution block at daemon.py:1426"
      via: "frozenset membership gate-opens producer='wanctl-backend'"
      pattern: "_WANCTL_BACKEND_RTT_SOURCES.*frozenset\\(\\{"
    - from: "src/wanctl/steering/health.py _build_rtt_source_section()"
      to: "the /health rtt_source.counts block"
      via: "counts.get('wanctl_backend', 0) emitted to the public payload"
      pattern: "wanctl_backend"
---

<objective>
Execute the Selection-A live-path flip: make steering's constructed-but-dead RttBackend seam the primary RTT source for `measure_current_rtt()`, populate the attribution gate so the Phase 244 `/health` `producer="wanctl-backend"` block activates, expose the new `wanctl_backend` count in the `/health` payload so the AB-03 gate can read it, and keep the existing autorate fallback chain fully intact (including on a RAISED probe exception, not just a None return). This is the single control-path edit of the milestone — it enables AB-01 (the seam produces real per-cycle ICMP RTT on the live steering path) and AB-02 (backend-tagged samples in `/health` for interleaved comparison).

Purpose: Phases 239-244 built the seam but deliberately left it off the live path. This flip is the activation. It must be minimal (four identified edit sites + one host-list line), bounded by SAFE-17, must NOT introduce a synchronous fping call in the 50ms loop or touch any classification/threshold/state-machine logic, and must NOT crash the daemon if probe() raises.

Output: edited `src/wanctl/steering/daemon.py` + `src/wanctl/steering/health.py` + extended steering tests proving seam-first behavior, intact fallback (None AND exception), live attribution, and the wanctl_backend count surfacing at /health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/245-live-a-b-rollback-anchor/245-RESEARCH.md
@.planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md

<interfaces>
<!-- Verified against the live codebase this planning session. -->

src/wanctl/steering/daemon.py:1118 — the gate (currently empty by design):
  _WANCTL_BACKEND_RTT_SOURCES: frozenset[str] = frozenset()

src/wanctl/steering/daemon.py:1170-1174 — counts dict init (no wanctl_backend key yet):
  self._rtt_source_counts = {"autorate_health": 0, "autorate_irtt": 0, "history_fallback": 0}

src/wanctl/steering/daemon.py:1782-1794 — measure_current_rtt() reads autorate FIRST today:
  def measure_current_rtt(self) -> float | None:
      live_rtt = self.baseline_loader.load_live_rtt()
      if isinstance(live_rtt, (int, float)):
          self._record_rtt_source_success("autorate_health", float(live_rtt))
          return float(live_rtt)
      live_irtt_rtt = self.baseline_loader.load_live_irtt_rtt()  # autorate_irtt fallback
      ... self._current_rtt_source = "unavailable"; return None

src/wanctl/steering/daemon.py:1846-1853 — recording helper (REUSE, do not duplicate):
  def _record_rtt_source_success(self, source, rtt_ms):
      ... if source in self._rtt_source_counts: self._rtt_source_counts[source] += 1
  (This is why the counts dict needs the wanctl_backend key — it only increments
   keys already present.)

src/wanctl/steering/daemon.py:1426-1461 — Phase 244 attribution block (DO NOT TOUCH):
  if self._current_rtt_source in _WANCTL_BACKEND_RTT_SOURCES:
      rtt_producer = "wanctl-backend"
      rtt_backend = self._rtt_backend_active if ... in {"icmplib","fping"} else None
      rtt_source_ip = self._rtt_source_ip if ... else None
  (Activates automatically once the frozenset is populated with "wanctl_backend".)

src/wanctl/steering/health.py:355-380 — _build_rtt_source_section() (VERIFIED THIS SESSION):
  The counts dict it emits at lines 372-375 currently contains ONLY:
    "autorate_health": int(counts.get("autorate_health", 0)),
    "autorate_irtt": int(counts.get("autorate_irtt", 0)),
    "history_fallback": int(counts.get("history_fallback", 0)),
  This is the ONLY place the public /health rtt_source.counts block is built. If the
  daemon gains a wanctl_backend count but this builder is not updated, AB-03's
  min_backend_cycle_fraction gate (which reads counts["wanctl_backend"] from /health)
  will have NO data. This is Concern 5 from the cross-AI review.

Host list (the one net-new wiring): steering config carries a single
  measurement.ping_host, stored as self.config.ping_host (daemon.py:252). There is
  no _rtt_probe_hosts() helper. The probe host list is [self.config.ping_host].

Seam API — src/wanctl/rtt_measurement.py:325:
  def probe(self, hosts: list[str]) -> "RttSample | None"
  Returns None on total loss (median-of-3+, mean-of-2 aggregation, byte-identical to
  WANController.measure_rtt). RttSample carries .rtt_ms, .per_host_results,
  .per_host_loss, .backend, .source_ip.
  probe() CAN RAISE (subprocess crash, fping binary missing at runtime, socket error)
  — the daemon must catch this and fall through, not crash (Concern 1).

src/wanctl/steering/daemon.py:1139 — self.rtt_measurement is the built backend's
  controller_measurement (factory-wired at daemon.py:2643). Already constructed; just
  not called for RTT today. There is an existing module/instance logger on the daemon
  (self.logger / module logger) — reuse it for the rate-limited probe-exception log.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip measure_current_rtt() to call the seam first (four edit sites + host list)</name>
  <files>src/wanctl/steering/daemon.py, src/wanctl/steering/health.py, tests/steering/test_steering_daemon.py</files>
  <read_first>
    - src/wanctl/steering/daemon.py lines 1108-1175 (the gate constant + counts dict init)
    - src/wanctl/steering/daemon.py lines 1782-1853 (measure_current_rtt + _record_rtt_source_success)
    - src/wanctl/steering/daemon.py lines 240-260 (self.config.ping_host plumbing)
    - src/wanctl/steering/health.py lines 355-380 (_build_rtt_source_section — the counts builder to extend)
    - src/wanctl/rtt_measurement.py lines 325-360 (probe() signature + RttSample construction; note it can raise)
    - src/wanctl/rtt_backend.py lines 36-95 (RttSample fields: rtt_ms, per_host_results, per_host_loss, backend, source_ip)
    - tests/steering/test_steering_daemon.py lines 85-101 (daemon fixture with MagicMock rtt_measurement)
    - .planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md (section "src/wanctl/steering/daemon.py — THE ONLY control-path edit")
  </read_first>
  <behavior>
    - Test 1 (seam-first success): given daemon.rtt_measurement.probe returns an RttSample(rtt_ms=21.4, backend="icmplib", source_ip="10.10.110.223", per_host_results={"1.1.1.1": 21.4}), measure_current_rtt() returns 21.4 and self._current_rtt_source == "wanctl_backend"; baseline_loader.load_live_rtt is NOT called.
    - Test 2 (probe None → fallback intact): given probe returns None, measure_current_rtt() falls through to baseline_loader.load_live_rtt(); on a numeric value it returns that and self._current_rtt_source == "autorate_health" (Pitfall 1: fallback must remain reachable).
    - Test 3 (counts increment): after a seam-first success, self._rtt_source_counts["wanctl_backend"] == 1.
    - Test 4 (probe called with the configured host): probe is invoked with a list containing self.config.ping_host.
    - Test 5 (full fallback chain unchanged): probe None AND load_live_rtt None AND load_live_irtt_rtt numeric → returns that value, source == "autorate_irtt"; probe None AND all None → returns None, source == "unavailable".
    - Test 6 (REVIEW FIX, Concern 1 — probe RAISES → no crash, falls through): given daemon.rtt_measurement.probe.side_effect = Exception("fping binary missing"), measure_current_rtt() does NOT raise; it falls through to baseline_loader.load_live_rtt() (which returns a numeric value in this fixture) and returns it with self._current_rtt_source == "autorate_health"; AND self._rtt_source_counts["probe_exception_count"] == 1.
    - Test 7 (REVIEW FIX, Concern 5 — wanctl_backend surfaces at /health): given daemon._rtt_source_counts contains wanctl_backend=5, the rtt_source section built by health.py emits counts["wanctl_backend"] == 5. (May live in this task's daemon test or in Task 2's health test — place it where the health-section builder fixture is available; if added here, import/exercise health._build_rtt_source_section against a stubbed daemon-derived source dict.)
  </behavior>
  <action>
    Edit site 1 — daemon.py:1118: change `_WANCTL_BACKEND_RTT_SOURCES: frozenset[str] = frozenset()` to `frozenset({"wanctl_backend"})`. Update the preceding comment to note the seam source is now live (Selection A, Phase 245). This single change gate-opens the Phase 244 attribution block at daemon.py:1426 automatically — do NOT touch that block.
    Edit site 2 — daemon.py:1170-1174: add "wanctl_backend": 0 as the first key in the self._rtt_source_counts dict, AND add "probe_exception_count": 0 (REVIEW FIX, Concern 1 — so probe() failures are trackable). Preserve the existing three keys (autorate_health, autorate_irtt, history_fallback). The wanctl_backend key is required because _record_rtt_source_success only increments keys already present (daemon.py:1852); probe_exception_count is incremented directly in the except block (it is not a "source success", so it is not routed through _record_rtt_source_success).
    Edit site 3 — daemon.py:1782 measure_current_rtt(): prepend a seam-first probe before the existing autorate_health read, WRAPPED IN try/except (REVIEW FIX, Concern 1):
      try:
          sample = self.rtt_measurement.probe([self.config.ping_host])
      except Exception:  # probe can raise: subprocess crash, fping missing, socket error
          self._rtt_source_counts["probe_exception_count"] += 1
          <rate-limited log via the existing daemon logger — e.g. logger.warning throttled,
           reuse whatever rate-limit idiom already exists in this file; do NOT re-raise>
          sample = None
      Then: if sample is not None and isinstance(sample.rtt_ms, (int, float)): self._record_rtt_source_success("wanctl_backend", float(sample.rtt_ms)) and return float(sample.rtt_ms). Otherwise (sample is None OR rtt_ms non-numeric OR an exception was caught above) fall through to the existing body UNCHANGED (load_live_rtt → autorate_health, load_live_irtt_rtt → autorate_irtt, else unavailable/None). The except block MUST NOT re-raise — a raised probe must reach the autorate fallback exactly like a None return. Update the docstring to "Measure current RTT from the wanctl RttBackend seam (failing safe to autorate health/IRTT on None or exception)." Do NOT add a synchronous fping call in run_cycle() — probe() runs through the existing backend handle; for the icmplib backend it is synchronous-but-cheap, for fping the factory cadence keeps it off the hot loop (do not change the cadence/threading).
    Edit site 4 — health.py:372-375 (REVIEW FIX, Concern 5): add `"wanctl_backend": int(counts.get("wanctl_backend", 0))` as the FIRST key in the counts dict returned by _build_rtt_source_section() (before autorate_health, autorate_irtt, history_fallback). This is an additive-only change to the /health payload and ensures the wanctl_backend count is exposed at /health for the AB-03 gate-eval (min_backend_cycle_fraction reads it from there). Do not change any other field in that builder.
    Reuse self._record_rtt_source_success (daemon.py:1846) for seam success — do not duplicate the recording logic. Keep sample.per_host_loss available (it is on the returned RttSample) for the loss-detection threshold consumed later by gate-eval via /health; no new field needed here.
    Add the behavior tests above to tests/steering/test_steering_daemon.py using the existing daemon fixture (MagicMock rtt_measurement; set daemon.rtt_measurement.probe.return_value, and daemon.rtt_measurement.probe.side_effect for Test 6). Import RttSample from wanctl.rtt_backend. (Test 7 may instead live in Task 2 alongside the health-section fixture — see the behavior list note.)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/steering/test_steering_daemon.py -k "measure_current_rtt or wanctl_backend or seam or probe_exception" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/wanctl/steering/daemon.py contains `_WANCTL_BACKEND_RTT_SOURCES: frozenset[str] = frozenset({"wanctl_backend"})`
    - self._rtt_source_counts initializer contains a "wanctl_backend" key AND a "probe_exception_count" key
    - measure_current_rtt body wraps the `self.rtt_measurement.probe(` call in a try/except that catches Exception, increments self._rtt_source_counts["probe_exception_count"], logs rate-limited, sets sample=None, and does NOT re-raise (verify: the except block is present and contains probe_exception_count)
    - The seam probe call precedes the `self.baseline_loader.load_live_rtt()` line
    - measure_current_rtt records via _record_rtt_source_success("wanctl_backend", ...) on seam success (no inline duplication of counts logic)
    - src/wanctl/steering/health.py _build_rtt_source_section() counts dict contains `"wanctl_backend": int(counts.get("wanctl_backend", 0))` as the first counts key (verify: grep -c 'wanctl_backend' on the health.py counts builder region >= 1)
    - The Phase 244 attribution block (daemon.py ~1426-1461) is byte-unchanged (verify: that region is not in the diff)
    - `.venv/bin/pytest tests/steering/test_steering_daemon.py -k "measure_current_rtt or wanctl_backend or seam or probe_exception" -x -q` exits 0 with all behavior tests (incl. Test 6 probe-raises-no-crash) passing
    - `.venv/bin/mypy src/wanctl/steering/daemon.py src/wanctl/steering/health.py` reports no new errors
    - `.venv/bin/ruff check src/wanctl/steering/daemon.py src/wanctl/steering/health.py` clean
  </acceptance_criteria>
  <done>measure_current_rtt() probes the seam first inside a try/except, records wanctl_backend on success, increments probe_exception_count and falls through (without raising) when probe() raises, preserves the entire autorate/IRTT/history fallback chain on None, increments the counts key, the Phase 244 attribution block is untouched, and health.py emits wanctl_backend in the /health counts block.</done>
</task>

<task type="auto">
  <name>Task 2: Prove live attribution + wanctl_backend /health exposure + run SAFE-17 boundary gate on the flip</name>
  <files>tests/steering/test_steering_health.py</files>
  <read_first>
    - tests/steering/test_steering_health.py lines 1300-1368 (test_health_includes_rtt_source_section, test_non_seam_current_never_yields_wanctl_backend_producer)
    - src/wanctl/steering/health.py lines 355-380 (_build_rtt_source_section — now emits wanctl_backend after Task 1)
    - src/wanctl/steering/daemon.py lines 1426-1461 (attribution block — now gate-open)
    - scripts/phase245-safe17-boundary-check.sh (created in Plan 01 — the boundary gate this task runs)
  </read_first>
  <action>
    Add a steering-health test that is the inverse of the existing test_non_seam_current_never_yields_wanctl_backend_producer: set daemon._current_rtt_source = "wanctl_backend" and daemon._rtt_backend_active = "icmplib" and daemon._rtt_source_ip = "10.10.110.223", then assert the emitted rtt_source section reports producer == "wanctl-backend", backend == "icmplib", source_ip == "10.10.110.223". Add a second case with _rtt_backend_active = "fping" asserting backend == "fping". Reuse the existing health-section fixture/builder used by the surrounding tests — do not construct a new daemon shape.
    Add Test 7 (REVIEW FIX, Concern 5) if it was not placed in Task 1: given the daemon-derived source dict carries counts["wanctl_backend"] == 5, assert the emitted /health rtt_source.counts["wanctl_backend"] == 5 (proves the count surfaces through the public health builder, not just daemon internals).
    Then run the Phase 245 SAFE-17 boundary verifier against the working tree containing the Plan 01 + this flip diff. It must pass: the daemon.py and health.py diffs are allowlist-bounded (both files are allowlisted) AND the protected-segment byte-identity layer confirms no classification/threshold/state-machine/EWMA/dwell/deadband/arbitration/fusion drift. If the verifier flags the diff, the flip touched something it should not — fix the flip, do not loosen the verifier.
    Capture the verifier evidence JSON to .planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json (the script writes it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/steering/test_steering_health.py -k "rtt_source or wanctl_backend or producer" -x -q && bash scripts/phase245-safe17-boundary-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - tests/steering/test_steering_health.py asserts producer == "wanctl-backend" and backend in {"icmplib","fping"} and source_ip set when _current_rtt_source == "wanctl_backend"
    - The /health rtt_source.counts block in the test path emits wanctl_backend — confirmed by the health tests passing AND by grep: src/wanctl/steering/health.py contains `wanctl_backend` in the counts builder (_build_rtt_source_section)
    - A test proves rtt_source.counts["wanctl_backend"] surfaces with the daemon-provided value (Test 7)
    - `.venv/bin/pytest tests/steering/test_steering_health.py -k "rtt_source or wanctl_backend or producer" -x -q` exits 0
    - `bash scripts/phase245-safe17-boundary-check.sh` exits 0 with the flip diff present
    - .planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json exists with passed:true and controller_path_diff bounded to src/wanctl/steering/daemon.py and src/wanctl/steering/health.py
    - Hot-path regression slice green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0
  </acceptance_criteria>
  <done>Steering /health reports producer="wanctl-backend" with backend/source_ip after the flip AND emits the wanctl_backend count, the SAFE-17 verifier passes with the flip diff present (daemon.py + health.py allowlist-bounded) proving no classification drift, and the hot-path slice is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement seam → controller classification | The flip changes the RTT input; SAFE-17 proves it does not change the state machine/thresholds that consume it |
| probe() None/exception → fallback | A None-returning OR a raising probe must reach the autorate fallback, not break the cycle (Pitfall 1 + Concern 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-03 | Tampering | src/wanctl/steering/daemon.py classification logic | mitigate | SAFE-17 protected-segment byte-identity layer; keep the diff to the four edit sites + one host-list line. |
| T-245-04 | Denial of Service (self) | measure_current_rtt seam call on the live cycle | mitigate | No synchronous fping in run_cycle(); probe() uses the existing backend handle/cadence; probe() wrapped in try/except so a raised probe never propagates; fallback chain intact so a None or raising probe never stalls the cycle. |
| T-245-05 | Information disclosure | source_ip in /health | accept | source_ip (10.10.110.223) is a private LAN address already on /health attribution by design (Phase 244 D-04); no secret. |
</threat_model>

<verification>
- Steering daemon + health test subsets green (incl. probe-raises-no-crash and wanctl_backend surfacing).
- `bash scripts/phase245-safe17-boundary-check.sh` passes with the flip diff present (daemon.py + health.py bounded).
- Hot-path regression slice green.
- mypy + ruff clean on the changed source.
</verification>

<success_criteria>
- measure_current_rtt() is seam-first with the full autorate fallback chain intact on both None and exception.
- /health reports producer="wanctl-backend" with backend/source_ip after the flip AND emits the wanctl_backend count.
- SAFE-17 passes — the diff is bounded to the measurement seam + the additive health count, zero classification drift.
</success_criteria>

<output>
Create `.planning/phases/245-live-a-b-rollback-anchor/245-02-SUMMARY.md` when done.
</output>
