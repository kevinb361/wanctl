---
phase: 245-live-a-b-rollback-anchor
plan: 03
type: execute
wave: 3
depends_on: ["245-01", "245-02"]
files_modified:
  - scripts/phase245-gate-eval.py
  - scripts/phase245-ab-run.sh
  - scripts/phase245-rollback.sh
  - tests/test_phase245_gate_eval.py
autonomous: true
requirements: [AB-01, AB-02, AB-03]
must_haves:
  truths:
    - "A gate-eval script computes a structured verdict across all six AB-03 dimensions against the frozen thresholds, gating restart dimension 5 on UNEXPECTED restarts (total - planned), not total restarts"
    - "'keep icmplib' maps to outcome=='pass' (a valid passing close), distinct from rollback_trigger and input_error"
    - "An A/B run orchestration script drives the interleaved comparison under the real steering.service unit, records baseline_nrestarts and planned_restarts separately, and scrapes /health attribution filtered on producer=='wanctl-backend'"
    - "An armed, --confirm-gated rollback script performs a config-only revert of the Spectrum measurement backend to icmplib under the deployed Phase-245 code, redeploys, restarts steering, and verifies /health shows backend=='icmplib' — checking the anchor against the production-deployed ref on cake-shaper, not local HEAD"
  artifacts:
    - path: "scripts/phase245-gate-eval.py"
      provides: "Six-dimension AB-03 verdict computation with structured JSON output; restart dim gates unexpected restarts"
      contains: "EXIT_PASS"
    - path: "scripts/phase245-ab-run.sh"
      provides: "Interleaved A/B orchestration + /health attribution scrape under systemd, with separated baseline/planned restart accounting"
      contains: "planned_restarts"
    - path: "scripts/phase245-rollback.sh"
      provides: "Armed --confirm-gated config-only Snapshot-A revert (NOT a code rollback)"
      contains: "--confirm"
    - path: "tests/test_phase245_gate_eval.py"
      provides: "Verdict-matrix coverage incl. keep-icmplib==pass and planned-vs-unexpected restart logic"
  key_links:
    - from: "scripts/phase245-gate-eval.py"
      to: "scripts/phase245-thresholds.json"
      via: "load_thresholds default path"
      pattern: "phase245-thresholds\\.json"
    - from: "scripts/phase245-ab-run.sh"
      to: "/health rtt_source (producer/backend/source_ip/counts)"
      via: "curl scrape filtered on producer=='wanctl-backend'"
      pattern: "wanctl-backend"
    - from: "scripts/phase245-rollback.sh"
      to: "steering.service + Spectrum icmplib config"
      via: "config revert → deploy → restart → /health verify"
      pattern: "steering\\.service"
---

<objective>
Build the operator tooling that runs the live A/B, computes the verdict against the frozen thresholds, and arms the Snapshot-A config-only rollback — all as scripts that do NOT execute against production in this plan (execution is Plan 04). This delivers the AB-03 verdict engine (reusing the Phase 243 gate-eval skeleton with the six AB-03 dimensions), the AB-01/AB-02 interleaved-run orchestration, and the armed `--confirm`-gated rollback.

Purpose: Separate tool construction from live execution so the tools are tested and reviewable before any production touch. The gate-eval must treat "keep icmplib" as a first-class passing close (AB-03, v1.46/v1.47 negative-result precedent), gate the restart dimension on UNEXPECTED restarts only (the interleaved design REQUIRES a planned per-window apply-restart — Concern 2), and the rollback must be genuinely executable (the phase227 `--confirm`-gated pattern, NOT the dry-run-only phase226 pattern) and clearly scoped as a CONFIG-only revert (Concern 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/245-live-a-b-rollback-anchor/245-RESEARCH.md
@.planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md

<interfaces>
<!-- Templates and the verified /health attribution shape. -->

scripts/phase243-gate-eval.py — verdict skeleton to reuse:
  EXIT_PASS=0, EXIT_BLOCK=1, EXIT_ABORT=2
  class GateEvalError(ValueError) with .gate_id
  def _gate(verdict, value, **extra) -> dict
  def load_thresholds(path=None) -> dict  (default = sibling phase24N-thresholds.json)
  main() emits sorted-keys JSON; outcome=="pass" → EXIT_PASS,
    "rollback_trigger" → EXIT_BLOCK, else EXIT_ABORT.

scripts/phase245-thresholds.json (from Plan 01) — the six AB-03 dimensions:
  RTT_AGREEMENT_TOL_MS, LOSS_DETECTION_MAX_DELTA_PCT,
  MIN_WANCTL_BACKEND_CYCLE_FRACTION, MAX_UNEXPECTED_RESTARTS, MAX_PLANNED_RESTARTS,
  STEERING_DECISION_STABILITY_MAX_DELTA_PCT, plus cycle-budget basis
  (ICMPLIB_REPRESENTATIVE_AVG_MS/P99_MS + tolerances), CONTROL_WAN, WAN_UNDER_TEST.
  NOTE (Concern 2): the restart gate key is MAX_UNEXPECTED_RESTARTS (NOT the old
  MAX_DAEMON_RESTARTS). MAX_PLANNED_RESTARTS is null by default (operator-set at run time).

scripts/phase243-bench-run.sh — systemd-driven run launcher (BENCH-01 precedent:
  the A/B must run under the real unit, not interactive).
scripts/phase227-rollback.sh — armed --confirm-gated mutation pattern:
  DRY_RUN/CONFIRM flags, print_plan(), health_check() via urlopen, deploy→restart→verify.

/health rtt_source block shape (daemon.py:1448-1461 + health.py counts builder,
  live after Plan 02 flip):
  rtt_source: { producer: "wanctl-backend"|null, backend: "icmplib"|"fping"|null,
    source_ip: <ip>|null, counts: { wanctl_backend, autorate_health, autorate_irtt,
    history_fallback } }
  Spectrum :9101 at http://10.10.110.223:9101/health = under test.
  ATT :9101 at http://10.10.110.227:9101/health = control.
  steering.service is the systemd unit; NRestarts via `systemctl show -p NRestarts --value steering.service`.

Production-deployed ref (Concern 3): the anchor check targets the PRODUCTION host,
  not local repo HEAD. After Plans 01-03 commit, local HEAD != ffaa8a0e, but the
  Snapshot-A anchor question is "what code is deployed on cake-shaper":
    ssh cake-shaper 'git -C /opt/wanctl rev-parse --short HEAD'

tests/test_phase243_gate_eval.py — importlib spec_from_file_location loader +
  pass/fail/rollback fixture matrix; test_pass_fixture_records_provenance_and_outcome_pass.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the six-dimension AB-03 gate-eval with keep-icmplib==pass and unexpected-restart gating</name>
  <files>scripts/phase245-gate-eval.py, tests/test_phase245_gate_eval.py</files>
  <read_first>
    - scripts/phase243-gate-eval.py (full — reuse GateEvalError, _gate, load_thresholds, main/exit-code contract)
    - scripts/phase245-thresholds.json (from Plan 01 — the keys the evaluator reads, incl. MAX_UNEXPECTED_RESTARTS / MAX_PLANNED_RESTARTS)
    - tests/test_phase243_gate_eval.py (full — reuse the importlib loader + verdict-matrix fixtures)
    - .planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md (section "scripts/phase245-gate-eval.py — MODEL ON 243")
    - .planning/REQUIREMENTS.md (AB-03 — the six dimensions and the keep-icmplib-passes mandate)
  </read_first>
  <behavior>
    - Test 1 (keep-icmplib is a pass): a fixture where fping shows no clear win (RTT agreement within tol, cycle budget non-regressed, loss-detection within tol, but no compelling reason to switch) → verdict["outcome"] == "pass" and the recommendation field == "keep-icmplib"; exit code EXIT_PASS (0).
    - Test 2 (clear fping win is a pass-switch): a fixture where all six dimensions pass AND fping shows an improvement → outcome == "pass", recommendation == "switch-eligible" (the actual flip is Phase 246, gated); exit EXIT_PASS.
    - Test 3 (REVIEW FIX, Concern 2 — planned restart does NOT trigger rollback; unexpected DOES): a fixture summary carrying baseline_nrestarts, planned_restarts (one apply-restart per backend flip), and total NRestarts. (a) When total_NRestarts - baseline_nrestarts == planned_restarts (i.e. unexpected_restarts == 0), the restart dimension PASSES — a single planned restart does NOT produce rollback_trigger. (b) When total_NRestarts - baseline_nrestarts > planned_restarts (unexpected_restarts >= 1 > MAX_UNEXPECTED_RESTARTS), the restart dimension FAILS → outcome == "rollback_trigger", exit EXIT_BLOCK.
    - Test 4 (other hard-safety rollback): a fixture where min-backend-cycle-fraction is below MIN_WANCTL_BACKEND_CYCLE_FRACTION (the flip silently fell back — Pitfall 1) OR cycle budget regressed beyond threshold → outcome == "rollback_trigger", exit EXIT_BLOCK (1).
    - Test 5 (input_error distinct): malformed/missing /health scrape input, OR a summary missing baseline_nrestarts/planned_restarts → GateEvalError → outcome == "input_error", exit EXIT_ABORT (2); distinct from a pass and from a rollback.
    - Test 6 (provenance carried): verdict JSON carries a provenance block with the thresholds_blob_sha and prereg_commit_sha (40-hex), proving the verdict is bound to the frozen thresholds.
  </behavior>
  <action>
    Create scripts/phase245-gate-eval.py modeled on scripts/phase243-gate-eval.py. Reuse verbatim: EXIT_PASS/BLOCK/ABORT, GateEvalError, _gate(), load_thresholds() (default to the sibling phase245-thresholds.json), the argparse + main() verdict-emit (sorted-keys JSON to --output, default 245-AB-VERDICT.json) and the exit-code contract.
    Replace the cycle-budget-arm evaluation with the six AB-03 dimensions, each producing a _gate() entry:
    1. rtt_agreement: |median_fping - median_icmplib| <= RTT_AGREEMENT_TOL_MS (computed from the under-test WAN's interleaved /health samples, optionally referenced against the ATT control diurnal trend).
    2. cycle_budget_nonregression: reuse the Phase 243 basis — fping cycle avg/p99 within ICMPLIB_REPRESENTATIVE_*_MS + tolerances and within CYCLE_*_REGRESSION_PCT of the icmplib arm.
    3. loss_detection_nonregression: |fping_loss_rate - icmplib_loss_rate| <= LOSS_DETECTION_MAX_DELTA_PCT (per-host loss from /health / per_host_loss).
    4. min_backend_cycle_fraction: rtt_source.counts["wanctl_backend"] / total_accepted >= MIN_WANCTL_BACKEND_CYCLE_FRACTION (this is the Pitfall-1 guard: proves the seam actually produced samples, not silent autorate fallback).
    5. unexpected_restarts (REVIEW FIX, Concern 2): read baseline_nrestarts, planned_restarts, and the final total NRestarts from the run summary JSON. Compute unexpected_restarts = total_NRestarts - baseline_nrestarts - planned_restarts. Gate: unexpected_restarts <= MAX_UNEXPECTED_RESTARTS (default 0). The per-window apply-restarts the A/B itself initiated are PLANNED and excluded — they must NOT trip this gate. If MAX_PLANNED_RESTARTS is non-null in the thresholds, also assert planned_restarts <= MAX_PLANNED_RESTARTS; if it is null, accept the run-recorded planned_restarts as-is. If baseline_nrestarts or planned_restarts is missing from the summary, raise GateEvalError (input_error) — the gate cannot compute unexpected restarts without them.
    6. steering_decision_stability: change in the enable/disable decision distribution between backends <= STEERING_DECISION_STABILITY_MAX_DELTA_PCT.
    Outcome logic: any dimension that is a hard-safety failure (min_backend_cycle_fraction below floor, unexpected_restarts over max, cycle budget regressed) → outcome="rollback_trigger". If all six dimensions pass, decide recommendation: "switch-eligible" if fping shows a clear improvement on rtt_agreement/loss, else "keep-icmplib"; either recommendation maps to outcome="pass" (BOTH are valid passing closes — keep-icmplib is NOT a failure). Malformed/incomplete input → raise GateEvalError → outcome="input_error".
    Carry a provenance block in the verdict JSON: thresholds_blob_sha + prereg_commit_sha (accept them as CLI args or read via phase245-prereg-provenance.sh record output).
    Create tests/test_phase245_gate_eval.py modeled on tests/test_phase243_gate_eval.py: reuse the importlib spec_from_file_location loader; add the six behavior tests above with inline fixture dicts (synthetic /health scrape summaries that include baseline_nrestarts + planned_restarts). Mirror test_pass_fixture_records_provenance_and_outcome_pass for the keep-icmplib==pass + 40-hex provenance assertion.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase245_gate_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase245-gate-eval.py defines EXIT_PASS=0, EXIT_BLOCK=1, EXIT_ABORT=2 and a GateEvalError class
    - load_thresholds() default path resolves to scripts/phase245-thresholds.json
    - The evaluator produces a _gate entry for all six AB-03 dimensions
    - Dimension 5 gates on unexpected_restarts = total_NRestarts - baseline_nrestarts - planned_restarts and compares against MAX_UNEXPECTED_RESTARTS (verify: scripts/phase245-gate-eval.py references MAX_UNEXPECTED_RESTARTS and planned_restarts; grep -c MAX_DAEMON_RESTARTS scripts/phase245-gate-eval.py == 0)
    - A single planned restart (unexpected_restarts == 0) does NOT produce rollback_trigger; an unexpected restart (unexpected_restarts >= 1) DOES (Test 3)
    - A keep-icmplib verdict has outcome == "pass" (not a failure) and exits EXIT_PASS
    - A below-floor min_backend_cycle_fraction OR unexpected-restarts-over-max produces outcome == "rollback_trigger" exiting EXIT_BLOCK
    - Malformed/incomplete input (incl. missing baseline_nrestarts/planned_restarts) produces outcome == "input_error" exiting EXIT_ABORT, distinct from pass/rollback
    - The verdict JSON carries a provenance block with 40-hex thresholds_blob_sha and prereg_commit_sha
    - `.venv/bin/pytest tests/test_phase245_gate_eval.py -x -q` exits 0 with all six behavior tests passing
    - `.venv/bin/ruff check scripts/phase245-gate-eval.py` clean
  </acceptance_criteria>
  <done>The gate-eval computes the six AB-03 dimensions, gates the restart dimension on UNEXPECTED restarts (excluding planned per-window apply-restarts), treats both keep-icmplib and switch-eligible as passing closes, fails closed to rollback_trigger on safety dimensions, distinguishes input_error, and binds the verdict to the frozen thresholds via provenance SHAs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the interleaved A/B run orchestration + armed config-only rollback scripts</name>
  <files>scripts/phase245-ab-run.sh, scripts/phase245-rollback.sh</files>
  <read_first>
    - scripts/phase243-bench-run.sh (systemd-driven run launcher template)
    - scripts/phase227-rollback.sh (full — the --confirm-gated armed-mutation pattern, DRY_RUN/CONFIRM, print_plan, health_check urlopen)
    - scripts/phase245-prereg-provenance.sh (from Plan 01 — record the prereg commit before the live arm)
    - scripts/phase245-gate-eval.py (from Task 1 — the run script feeds it the scraped /health summary, incl. baseline_nrestarts + planned_restarts)
    - .planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md (sections "scripts/phase245-ab-run.sh" and "scripts/phase245-rollback.sh")
    - CLAUDE.md (Quick Operational Commands — the ssh/curl /health scrape idiom; production-conservative change policy)
  </read_first>
  <action>
    Create scripts/phase245-ab-run.sh modeled on scripts/phase243-bench-run.sh. It orchestrates the interleaved A/B (AB-02) using the alternating-window single-daemon shape (Pattern 2a — smaller control-path diff, attribution filters cleanly on backend): for the Spectrum WAN under test, alternate measurement.backend icmplib<->fping on a fine window (default WINDOW_SEC=300, i.e. 5-min alternation; total run length is operator-set, recommend >= one diurnal cycle), restarting steering.service only as needed to apply each backend.
    REVIEW FIX (Concern 2) — restart accounting: BEFORE the first window, record the pre-run NRestarts as baseline_nrestarts (`ssh cake-shaper 'systemctl show -p NRestarts --value steering.service'`). Maintain a PLANNED_RESTARTS counter, incremented by 1 each time the script issues a `systemctl restart steering.service` to apply a backend flip (these are planned apply-restarts, expected). The final aggregated summary JSON MUST carry both `baseline_nrestarts` and `planned_restarts` as top-level fields (plus the final total NRestarts) so phase245-gate-eval.py can compute unexpected_restarts = total - baseline - planned. Do NOT collapse planned and unexpected restarts into one count.
    ATT (control) gets ZERO config changes throughout (Anti-Pattern: never touch the control WAN). On each window, scrape /health from both WANs:
      ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.223:9101/health' | python3 -c '...rtt_source...' (filter producer=="wanctl-backend"; capture backend, source_ip, counts incl. wanctl_backend, and the measurement RTT)
    and record NRestarts via `ssh cake-shaper 'systemctl show -p NRestarts --value steering.service'`. Append each scrape to a timestamped evidence JSONL under .planning/phases/245-live-a-b-rollback-anchor/evidence/. Before the live arm, record the prereg commit (scripts/phase245-prereg-provenance.sh record) so the evidence descends from the frozen thresholds. The script MUST require an explicit --confirm flag to mutate the Spectrum config / restart steering (read-only preflight scrape allowed without it). Do NOT add a synchronous fping call to the daemon hot loop (that is forbidden; the backend cadence handles fping off-loop). Emit a final aggregated summary JSON (carrying baseline_nrestarts + planned_restarts) that phase245-gate-eval.py consumes via --output.
    Create scripts/phase245-rollback.sh modeled on scripts/phase227-rollback.sh (NOT the dry-run-only phase226 pattern).
    REVIEW FIX (Concern 4) — Snapshot-A rollback is a CONFIG-ONLY operation, not a code rollback. Add this comment near the top of the script:
      `# Snapshot-A rollback: config-revert to icmplib under Phase-245 code (NOT a code rollback to ffaa8a0e)`
    Document in the script header: "Snapshot-A rollback = restore the Spectrum measurement.backend config to icmplib under the currently-deployed Phase-245 code. This is NOT a code rollback to ffaa8a0e. The ffaa8a0e anchor proves Selection-A is deployed (not something earlier); the rollback is a config-only operation. The Phase-245 daemon flip (Plan 02) REMAINS deployed; only the backend config reverts to icmplib."
    Composition (Snapshot-A config revert, Pattern 4): SSH_HOST="cake-shaper", HEALTH_URL="http://10.10.110.223:9101/health", DRY_RUN/CONFIRM flags with --confirm REQUIRED for real mutation. print_plan() lists ordered steps: restore Spectrum config to the icmplib default → scripts/deploy.sh spectrum cake-shaper → ssh cake-shaper 'sudo systemctl restart steering.service' → scrape /health and assert rtt_source.backend == "icmplib" (or producer != "wanctl-backend") → record a proof JSON to evidence/.
    REVIEW FIX (Concern 3) — anchor check targets the PRODUCTION-DEPLOYED ref, not local HEAD: where the script confirms the Snapshot-A anchor, use `ssh ${SSH_HOST} 'git -C /opt/wanctl rev-parse --short HEAD'` (the production-deployed ref on cake-shaper), NOT a local `git rev-parse --short HEAD`. Document in the script that the anchor check targets the production host. Note an optional `git tag snapshot-a-phase245` may pin the anchor commit. health_check() reuses the phase227 urlopen pattern. Without --confirm the script prints the plan and the health probe only; it performs NO mutation.
    Both scripts: bash strict mode (set -euo pipefail), absolute/operator-supplied host, no hardcoded secrets. Keep them executable (chmod +x).
  </action>
  <verify>
    <automated>bash -n scripts/phase245-ab-run.sh && bash -n scripts/phase245-rollback.sh && bash scripts/phase245-rollback.sh --help 2>&1 | grep -q -- --confirm && grep -q "WINDOW_SEC" scripts/phase245-ab-run.sh && grep -q "wanctl-backend" scripts/phase245-ab-run.sh && grep -q "planned_restarts" scripts/phase245-ab-run.sh && grep -q "baseline_nrestarts" scripts/phase245-ab-run.sh && grep -q "config-revert to icmplib under Phase-245 code" scripts/phase245-rollback.sh && grep -q "git -C /opt/wanctl rev-parse" scripts/phase245-rollback.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase245-ab-run.sh` and `bash -n scripts/phase245-rollback.sh` both exit 0 (valid syntax)
    - scripts/phase245-ab-run.sh contains a WINDOW_SEC alternation default and references both http://10.10.110.223:9101/health (under test) and http://10.10.110.227:9101/health (control)
    - scripts/phase245-ab-run.sh filters /health on producer=="wanctl-backend" and records NRestarts via systemctl show -p NRestarts steering.service
    - scripts/phase245-ab-run.sh records baseline_nrestarts before the run and a separate planned_restarts counter, and emits BOTH as fields in the aggregated summary JSON (verify: grep planned_restarts and baseline_nrestarts both present)
    - scripts/phase245-ab-run.sh requires --confirm before any Spectrum config mutation or steering restart; a no-arg invocation does not mutate
    - scripts/phase245-rollback.sh contains the comment `# Snapshot-A rollback: config-revert to icmplib under Phase-245 code (NOT a code rollback to ffaa8a0e)` (verify: grep -q)
    - scripts/phase245-rollback.sh checks the Snapshot-A anchor against the production host via `ssh ${SSH_HOST} 'git -C /opt/wanctl rev-parse --short HEAD'` (NOT local HEAD) (verify: grep -q 'git -C /opt/wanctl rev-parse')
    - scripts/phase245-rollback.sh contains DRY_RUN/CONFIRM handling and requires --confirm for real mutation; without --confirm it prints the plan + health probe only and mutates nothing
    - scripts/phase245-rollback.sh verifies rtt_source.backend == "icmplib" (or producer != "wanctl-backend") after revert
    - Neither script touches the ATT control WAN config
    - Both scripts use set -euo pipefail and contain no hardcoded secrets
  </acceptance_criteria>
  <done>The A/B run script drives the alternating-window interleaved comparison under steering.service with /health attribution scraping and SEPARATED baseline/planned restart accounting (mutation gated behind --confirm, control WAN untouched), and the armed rollback script genuinely performs a CONFIG-ONLY Snapshot-A revert to icmplib under the deployed Phase-245 code — checking the anchor against the production-deployed ref on cake-shaper — with /health verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator script → production cake-shaper | Mutation (config edit, steering restart) crosses into production; gated behind --confirm |
| /health scrape → verdict | Untrusted/incomplete scrape input (incl. missing restart-accounting fields) must fail to input_error, not silently pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-06 | Tampering | verdict computation from /health | mitigate | Malformed/incomplete input (incl. missing baseline_nrestarts/planned_restarts) raises GateEvalError → input_error (distinct from pass); provenance SHAs bind the verdict to frozen thresholds. |
| T-245-07 | Denial of Service (self) | phase245-ab-run.sh restarts steering.service | mitigate | --confirm gate; planned per-window apply-restarts counted separately and excluded from the unexpected-restart gate; ATT control untouched so failover reference stays stable. |
| T-245-08 | Elevation/accidental mutation | phase245-rollback.sh | mitigate | phase227 --confirm-gated pattern (NOT dry-run-only); config-only revert (not a code rollback); anchor checked against the production-deployed ref; no mutation without --confirm; print_plan shows ordered steps before acting. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase245_gate_eval.py -q` passes (incl. keep-icmplib==pass and planned-vs-unexpected restart logic).
- `bash -n` clean on both shell scripts.
- Rollback script refuses to mutate without --confirm, is documented as config-only, and checks the anchor against the production host.
- A/B run script records baseline_nrestarts + planned_restarts separately and never references the ATT config for mutation.
</verification>

<success_criteria>
- Six-dimension AB-03 verdict engine works, keep-icmplib is a passing close, restart dimension gates unexpected restarts only.
- Interleaved A/B orchestration scrapes attribution and counts planned vs unexpected restarts under systemd, mutation gated.
- Armed --confirm rollback genuinely performs a config-only revert to icmplib under Phase-245 code and verifies via /health, anchor checked on the production host.
</success_criteria>

<output>
Create `.planning/phases/245-live-a-b-rollback-anchor/245-03-SUMMARY.md` when done.
</output>
