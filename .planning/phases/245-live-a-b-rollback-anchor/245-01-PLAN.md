---
phase: 245-live-a-b-rollback-anchor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase245-safe17-boundary-check.sh
  - scripts/phase245-thresholds.json
  - scripts/phase245-prereg-provenance.sh
  - tests/test_phase245_safe17_verifier.py
  - tests/test_phase245_prereg.py
autonomous: true
requirements: [AB-01, AB-03, SAFE-17]
must_haves:
  truths:
    - "A SAFE-17 boundary verifier exists for Phase 245, anchored at ffaa8a0e, that fails closed on out-of-allowlist controller-path drift"
    - "A preregistration thresholds JSON is committed to git before any live A/B data is collected"
    - "The preregistration provenance script can emit a blob-SHA + prereg commit SHA and reject evidence commits that do not descend from the prereg commit"
    - "The Phase 245 thresholds carry all six AB-03 verdict dimensions as named keys, with planned vs unexpected restart accounting separated (MAX_UNEXPECTED_RESTARTS, MAX_PLANNED_RESTARTS)"
  artifacts:
    - path: "scripts/phase245-safe17-boundary-check.sh"
      provides: "Fail-closed SAFE-17 source-diff verifier, anchor ffaa8a0e"
      contains: "ANCHOR=\"ffaa8a0e\""
    - path: "scripts/phase245-thresholds.json"
      provides: "Preregistered AB-03 verdict thresholds (frozen before data)"
      contains: "thresholds_schema_version"
    - path: "scripts/phase245-prereg-provenance.sh"
      provides: "Blob-SHA pin + descendancy proof tooling"
      contains: "phase245-thresholds.json"
    - path: "tests/test_phase245_safe17_verifier.py"
      provides: "Verifier self-test + fail-closed regression coverage"
    - path: "tests/test_phase245_prereg.py"
      provides: "Preregistration provenance coverage"
  key_links:
    - from: "scripts/phase245-prereg-provenance.sh"
      to: "scripts/phase245-thresholds.json"
      via: "git rev-parse HEAD:<path> blob-SHA pin"
      pattern: "phase245-thresholds\\.json"
    - from: "scripts/phase245-safe17-boundary-check.sh"
      to: "ffaa8a0e (Snapshot-A anchor)"
      via: "git diff against anchor"
      pattern: "ffaa8a0e"
---

<objective>
Build the Wave-0 integrity scaffold for the Phase 245 live A/B: the fail-closed SAFE-17 boundary verifier (anchored at the Phase 244 close `ffaa8a0e`, which is also the Snapshot-A code anchor), and the preregistration mechanism (frozen thresholds JSON + blob-SHA/descendancy provenance script). Both must exist and be committed BEFORE the daemon flip (Plan 02) and BEFORE any live data is collected (Plan 04).

Purpose: SAFE-17 and preregistration are gates, not deliverables. The SAFE-17 verifier proves the upcoming daemon.py flip stays inside the narrowed v1.53 allowlist and does not drift classification logic. The preregistration freezes the AB-03 verdict thresholds in git before any data exists, so the A/B verdict cannot be reverse-engineered from the data (AB-03 mandate; v1.46/v1.47 negative-result precedent makes "keep icmplib" a valid passing close).

Output: `scripts/phase245-safe17-boundary-check.sh`, `scripts/phase245-thresholds.json`, `scripts/phase245-prereg-provenance.sh`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/245-live-a-b-rollback-anchor/245-RESEARCH.md
@.planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md

<interfaces>
<!-- Phase 243/244 templates these new files are cloned/modeled from. -->

scripts/phase244-safe17-boundary-check.sh — fail-closed SAFE-17 verifier (594 lines).
  Header constants to change: ANCHOR (was 49fb1393), OUT path, ALLOWED_OUT_PREFIX.
  Five fail-closed layers + a --self-test detached-worktree mode. DO NOT change the
  v1.53 allowlist regex — steering/daemon.py and steering/health.py are already in it.

scripts/phase243-thresholds.json — frozen thresholds shape. Keys present today:
  thresholds_schema_version, CYCLE_AVG_REGRESSION_PCT, CYCLE_P99_REGRESSION_PCT,
  CYCLE_P99_ABS_CEILING_MS, ICMPLIB_REPRESENTATIVE_AVG_MS (2.85),
  ICMPLIB_REPRESENTATIVE_P99_MS (6.9), ICMPLIB_REPRESENTATIVE_AVG_TOL_MS (1.5),
  ICMPLIB_REPRESENTATIVE_P99_TOL_MS (3.5), MIN_CYCLES (10000), MIN_MINUTES (30),
  AMENDMENT_ID, AMENDS_THRESHOLDS_BLOB_SHA, AMENDMENT_REASON, _notes.

scripts/phase243-prereg-provenance.sh — three subcommands:
  record() → emits {thresholds_blob_sha, prereg_commit_sha, thresholds_path}
  assert-descends <prereg_commit> <evidence_commit> → git merge-base --is-ancestor
  assert-blob-unchanged <expected_blob_sha> → git rev-parse HEAD:<path> equality
  THRESHOLDS_PATH is the only line to repoint to scripts/phase245-thresholds.json.

tests/test_phase244_safe17_verifier.py — detached-worktree fixture pinned to the
  phase-close anchor (NOT HEAD), static-contract asserts, out-of-allowlist and
  dirty-tree fail-closed tests.
tests/test_phase243_prereg.py — blob+commit SHA emission + descendancy rejection tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the SAFE-17 boundary verifier for Phase 245 (anchor swap only)</name>
  <files>scripts/phase245-safe17-boundary-check.sh, tests/test_phase245_safe17_verifier.py</files>
  <read_first>
    - scripts/phase244-safe17-boundary-check.sh (the template — read full; copy structure verbatim)
    - tests/test_phase244_safe17_verifier.py (the test template — read full)
    - .planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md (section "scripts/phase245-safe17-boundary-check.sh — ANCHOR SWAP ONLY")
  </read_first>
  <action>
    Copy scripts/phase244-safe17-boundary-check.sh to scripts/phase245-safe17-boundary-check.sh.
    Change exactly these header constants and nothing in the allowlist regex:
    - ANCHOR from the Phase-244 value (49fb1393) to "ffaa8a0e" (Phase 244 close == Snapshot-A code anchor; confirmed == current HEAD via git rev-parse --short HEAD at the time this plan was written).
    - OUT to ".planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json".
    - ALLOWED_OUT_PREFIX to ".planning/phases/245-live-a-b-rollback-anchor/evidence/".
    Keep the V153_ALLOWLIST_RE byte-identical to Phase 244 — steering/daemon.py and steering/health.py are already in it; adding files is forbidden scope creep (245-RESEARCH Anti-Patterns, Assumption A6).
    Keep all five fail-closed layers intact: dirty-tree fail-closed, out-of-allowlist diff, RTT-seam-unchanged-since-239, reflector/fping frozen, and the protected-body / protected-segment byte-identity layer (the last is what catches classification drift INSIDE the allowlisted daemon.py per Pitfall 4 — it must remain). Keep the --self-test detached-worktree mode.
    Create the evidence directory: .planning/phases/245-live-a-b-rollback-anchor/evidence/ (mkdir -p in the script's OUT-writing path already handles this if phase244 did; otherwise add it).
    Copy tests/test_phase244_safe17_verifier.py to tests/test_phase245_safe17_verifier.py. Repoint: PHASE_CLOSE_ANCHOR = "ffaa8a0e" (pinned point-in-time, NOT HEAD, per project memory project_safe17_boundary_test_pinning); update static-contract asserts to expect 'ANCHOR="ffaa8a0e"' and "safe17-boundary-245.json" and "steering/daemon" in the script text; repoint the script path the test invokes to scripts/phase245-safe17-boundary-check.sh.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase245_safe17_verifier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase245-safe17-boundary-check.sh contains the exact string ANCHOR="ffaa8a0e"
    - scripts/phase245-safe17-boundary-check.sh contains "safe17-boundary-245.json"
    - The V153 allowlist regex in scripts/phase245-safe17-boundary-check.sh is byte-identical to the one in scripts/phase244-safe17-boundary-check.sh (verify: diff the extracted regex line; must be empty)
    - `bash scripts/phase245-safe17-boundary-check.sh --self-test` exits 0
    - `bash scripts/phase245-safe17-boundary-check.sh` exits 0 against the clean current tree (no out-of-allowlist drift at the anchor)
    - tests/test_phase245_safe17_verifier.py defines PHASE_CLOSE_ANCHOR = "ffaa8a0e"
    - `.venv/bin/pytest tests/test_phase245_safe17_verifier.py -x -q` exits 0
  </acceptance_criteria>
  <done>The Phase 245 SAFE-17 verifier exists, anchored at ffaa8a0e, passes its own self-test and the clean-tree check, and its test suite passes with the anchor pinned to ffaa8a0e (not HEAD).</done>
</task>

<task type="auto">
  <name>Task 2: Preregister AB-03 thresholds + clone the provenance script</name>
  <files>scripts/phase245-thresholds.json, scripts/phase245-prereg-provenance.sh, tests/test_phase245_prereg.py</files>
  <read_first>
    - scripts/phase243-thresholds.json (the JSON shape template — read full)
    - scripts/phase243-prereg-provenance.sh (the provenance script template — read full, 66 lines)
    - tests/test_phase243_prereg.py (the prereg test template — read full, 71 lines)
    - .planning/REQUIREMENTS.md (AB-03 — the six verdict dimensions)
    - .planning/phases/245-live-a-b-rollback-anchor/245-PATTERNS.md (sections "scripts/phase245-thresholds.json" and "scripts/phase245-prereg-provenance.sh")
  </read_first>
  <action>
    Create scripts/phase245-thresholds.json modeled on phase243-thresholds.json. Carry forward the cycle-budget non-regression basis verbatim: ICMPLIB_REPRESENTATIVE_AVG_MS=2.85, ICMPLIB_REPRESENTATIVE_P99_MS=6.9, ICMPLIB_REPRESENTATIVE_AVG_TOL_MS=1.5, ICMPLIB_REPRESENTATIVE_P99_TOL_MS=3.5, CYCLE_AVG_REGRESSION_PCT=20.0, CYCLE_P99_REGRESSION_PCT=20.0, CYCLE_P99_ABS_CEILING_MS=10.0, CYCLE_HZ=20, CYCLE_INTERVAL_MS=50.0. Keep thresholds_schema_version=1, MIN_CYCLES=10000, MIN_MINUTES=30. Preserve the amendment-audit convention keys (AMENDMENT_ID, AMENDS_THRESHOLDS_BLOB_SHA, AMENDMENT_REASON) set to null/empty for the initial preregistration (no amendment yet).
    Add the AB-03 verdict-dimension keys with these initial committed values (these are the preregistered defaults; they are frozen by commit and amended only via the AMENDMENT_ID convention if a genuine clarification is needed):
    - RTT_AGREEMENT_TOL_MS: 3.0  (max |median_fping - median_icmplib| for agreement within the same interleaved window; derived from the ~22-28ms production RTT band and its observed variance — see 238-PROVENANCE-MAP captures)
    - LOSS_DETECTION_MAX_DELTA_PCT: 1.0  (max difference in per-host loss-detection rate between backends; loss must not be missed/invented)
    - MIN_WANCTL_BACKEND_CYCLE_FRACTION: 0.95  (rtt_source.counts["wanctl_backend"] / total accepted cycles must be >= this; the flip must actually be live, not silently falling back to autorate_health — Pitfall 1)
    - MAX_UNEXPECTED_RESTARTS: 0  (REVIEW FIX, Concern 2 — this is the hard gate: unexpected steering.service restarts beyond the planned per-window apply-restarts. unexpected = total_NRestarts - planned_restarts_recorded_by_the_ab_run. The interleaved A/B requires a steering.service restart per backend flip to apply the config change; those are PLANNED and must NOT count against this gate. Only restarts the A/B did not initiate count here. Do NOT use the old key name MAX_DAEMON_RESTARTS — it conflated planned apply-restarts with crashes and would trivially fail.)
    - MAX_PLANNED_RESTARTS: null  (REVIEW FIX, Concern 2 — null means "set by the operator at run time based on the chosen window count": one apply-restart per backend flip. The gate-eval validates planned_restarts <= MAX_PLANNED_RESTARTS only when this is non-null; with null it accepts the run-recorded planned count as-is and only enforces MAX_UNEXPECTED_RESTARTS.)
    - STEERING_DECISION_STABILITY_MAX_DELTA_PCT: 5.0  (max change in the enable/disable steering-decision distribution between backends; the measurement input change must not move classification beyond noise)
    - CONTROL_WAN: "att"  (ATT is the diurnal control, held on icmplib, zero config change)
    - WAN_UNDER_TEST: "spectrum"  (Selection A primary WAN)
    Add a _notes value stating: thresholds committed before data collection; "keep icmplib" is a valid passing close (AB-03); do not duplicate threshold literals in markdown; amend only via AMENDMENT_ID; MAX_UNEXPECTED_RESTARTS gates only restarts the A/B did not plan (per-window apply-restarts are planned and excluded).
    Copy scripts/phase243-prereg-provenance.sh to scripts/phase245-prereg-provenance.sh and change the single line THRESHOLDS_PATH to "scripts/phase245-thresholds.json". Keep the three subcommands (record, assert-descends, assert-blob-unchanged) verbatim.
    Copy tests/test_phase243_prereg.py to tests/test_phase245_prereg.py and repoint THRESHOLDS = ROOT/"scripts"/"phase245-thresholds.json" and PROVENANCE = ROOT/"scripts"/"phase245-prereg-provenance.sh". Keep the two tests (record emits 40-hex blob+commit SHAs; assert-descends rejects a non-ancestor with "does not descend" in stderr).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase245_prereg.py -x -q && python3 -c "import json; d=json.load(open('scripts/phase245-thresholds.json')); assert {'RTT_AGREEMENT_TOL_MS','MIN_WANCTL_BACKEND_CYCLE_FRACTION','MAX_UNEXPECTED_RESTARTS','MAX_PLANNED_RESTARTS','STEERING_DECISION_STABILITY_MAX_DELTA_PCT','LOSS_DETECTION_MAX_DELTA_PCT','CONTROL_WAN','WAN_UNDER_TEST'} <= set(d), 'missing AB-03 dimension keys'; assert 'MAX_DAEMON_RESTARTS' not in d, 'stale MAX_DAEMON_RESTARTS must be renamed to MAX_UNEXPECTED_RESTARTS'"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase245-thresholds.json is valid JSON and contains all AB-03 keys: RTT_AGREEMENT_TOL_MS, LOSS_DETECTION_MAX_DELTA_PCT, MIN_WANCTL_BACKEND_CYCLE_FRACTION, MAX_UNEXPECTED_RESTARTS, MAX_PLANNED_RESTARTS, STEERING_DECISION_STABILITY_MAX_DELTA_PCT, CONTROL_WAN, WAN_UNDER_TEST
    - scripts/phase245-thresholds.json uses MAX_UNEXPECTED_RESTARTS (value 0) and MAX_PLANNED_RESTARTS (value null) and does NOT contain the key MAX_DAEMON_RESTARTS (verify: grep -c MAX_DAEMON_RESTARTS scripts/phase245-thresholds.json == 0)
    - scripts/phase245-thresholds.json contains ICMPLIB_REPRESENTATIVE_AVG_MS with value 2.85 and ICMPLIB_REPRESENTATIVE_P99_MS with value 6.9
    - scripts/phase245-thresholds.json CONTROL_WAN == "att" and WAN_UNDER_TEST == "spectrum"
    - scripts/phase245-prereg-provenance.sh THRESHOLDS_PATH points to scripts/phase245-thresholds.json
    - `bash scripts/phase245-prereg-provenance.sh record` emits JSON with a 40-hex thresholds_blob_sha and a 40-hex prereg_commit_sha (run after the JSON is committed; in CI the test covers this against a temp commit)
    - `.venv/bin/pytest tests/test_phase245_prereg.py -x -q` exits 0
  </acceptance_criteria>
  <done>Frozen AB-03 thresholds JSON exists with all verdict dimensions plus the control/under-test WAN labels and the separated restart accounting (MAX_UNEXPECTED_RESTARTS + MAX_PLANNED_RESTARTS, no stale MAX_DAEMON_RESTARTS), the provenance script is repointed, and the prereg test passes proving blob-SHA emission and descendancy enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → git history | Preregistration integrity depends on thresholds being committed before evidence; a post-hoc edit would invalidate the A/B |
| daemon.py edit (Plan 02) → controller behavior | SAFE-17 verifier is the boundary that proves the edit stays inside the measurement seam, not classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-01 | Tampering | scripts/phase245-thresholds.json | mitigate | Commit thresholds before any data; blob-SHA pin + `git merge-base --is-ancestor` descendancy proof (phase245-prereg-provenance.sh). Genuine amendments require the AMENDMENT_ID audit trail, never a silent edit. |
| T-245-02 | Tampering | scripts/phase245-safe17-boundary-check.sh allowlist | mitigate | Allowlist regex byte-identical to Phase 244; the protected-segment byte-identity layer catches classification drift even inside allowlisted daemon.py. |
| T-245-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (fping vetted in 241-243); slopcheck N/A. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_phase245_safe17_verifier.py tests/test_phase245_prereg.py -q` passes.
- `bash scripts/phase245-safe17-boundary-check.sh --self-test` exits 0.
- `bash scripts/phase245-safe17-boundary-check.sh` exits 0 against the clean tree at ffaa8a0e.
- The thresholds JSON allowlist regex matches Phase 244's byte-for-byte.
- The thresholds JSON uses MAX_UNEXPECTED_RESTARTS / MAX_PLANNED_RESTARTS and contains no MAX_DAEMON_RESTARTS key.
</verification>

<success_criteria>
- The SAFE-17 verifier for Phase 245 exists, anchored at ffaa8a0e, fails closed, and self-tests green.
- The preregistration thresholds JSON exists with all six AB-03 verdict dimensions (planned/unexpected restart accounting separated), frozen before any live data.
- The provenance script can pin the thresholds blob and prove evidence descends from the prereg commit.
</success_criteria>

<output>
Create `.planning/phases/245-live-a-b-rollback-anchor/245-01-SUMMARY.md` when done.
</output>
