---
phase: 237-hil-failure-injection-harness-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_silicom_test_harness.py
  - scripts/phase237-safe16-boundary-check.sh
autonomous: true
requirements: [HARN-01, HARN-02, HARN-03, HARN-04, HARN-05, SAFE-16]
must_haves:
  truths:
    - "A failing pytest file pins the HARN-01..05 contract before the orchestrator exists (RED)"
    - "The HARN-04 safety-critical proof exists as a mid-run-failure test AND a signal test that asserts the harness EXITS non-zero (signal-derived) after restoring — not merely that restore verbs landed"
    - "A RED test pins the live/fake gate boundary: a real (non-fake) invocation without SILICOM_TEST_LIVE_CONFIRM refuses, and att-modem refuses without the louder SILICOM_TEST_ATT_CONFIRM gate"
    - "A read-only SAFE-16 boundary tool exists (emits an evidence JSON; no worktree/index/ref mutation), parameterized to v1.51 with the 237 evidence out-path"
  artifacts:
    - path: "tests/test_silicom_test_harness.py"
      provides: "Fake-silicom-bypass seam + RED tests for HARN-01..05, signal-exit proof, and live/att gate refusal"
      contains: "test_restore_on_midrun_failure"
      min_lines: 140
    - path: "scripts/phase237-safe16-boundary-check.sh"
      provides: "SAFE-16 controller-path zero-diff evidence tool (copy of phase225 pattern, anchor v1.51)"
      contains: "controller_targets"
      min_lines: 100
  key_links:
    - from: "tests/test_silicom_test_harness.py"
      to: "scripts/silicom-test"
      via: "SILICOM_BYPASS env seam + subprocess bash HARNESS"
      pattern: "SILICOM_BYPASS"
    - from: "tests/test_silicom_test_harness.py"
      to: "signal-exit contract"
      via: "SIGTERM yields signal-derived exit code after restore (143)"
      pattern: "returncode == -15|returncode == 143|SIGTERM"
    - from: "scripts/phase237-safe16-boundary-check.sh"
      to: "v1.51"
      via: "ANCHOR default"
      pattern: "ANCHOR=\"v1.51\""
---

<objective>
Build the Wave-0 scaffolds that pin Phase 237's contract before any orchestrator code exists: (1) a pytest file that fakes the `silicom-bypass` CLI and asserts HARN-01..05 behavior PLUS the signal-exit and live/att gate-refusal contracts — RED until Plan 02 lands the orchestrator; (2) the SAFE-16 boundary-check tool, copied from the proven `phase225-safe13-boundary-check.sh` and re-parameterized to anchor `v1.51` with the 237 evidence out-path.

Purpose: The harness is safety-critical (it toggles live-WAN Silicom bypass pairs). Writing the failure-injection proof FIRST guarantees Plan 02's trap is verified, not asserted — and the signal test must prove the harness actually EXITS (not just restores) so a returning trap can never let it continue. The SAFE-16 tool must exist before any phase-boundary proof.
Output: `tests/test_silicom_test_harness.py` (RED), `scripts/phase237-safe16-boundary-check.sh` (runnable, evidence-emitting).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-VALIDATION.md

<interfaces>
<!-- The fake-CLI seam and call-log assertion shape come from the existing bypass test. -->
<!-- The orchestrator under test does not exist yet; the test pins its contract. -->

The harness (built in Plan 02) reads these env seams (from 237-PATTERNS.md):
- SILICOM_BYPASS              -> path to the silicom-bypass CLI (point at fake)
- SILICOM_TEST_SCENARIO_DIR   -> chaos scenario dir
- SILICOM_TEST_RESULT_ROOT    -> result-dir root (HARN-05)
- SILICOM_TEST_SNAPSHOT       -> snapshot helper (default the absolute installed path; overridable to a fake)
- SILICOM_TEST_HEALTH_POLLER  -> health poller (default the absolute installed path; overridable to a fake)

Live/fake gate seams (the live-confirm boundary the harness enforces):
- SILICOM_TEST_LIVE_CONFIRM   -> required for any REAL (live-card) run
- SILICOM_TEST_ATT_CONFIRM    -> ADDITIONAL louder gate required when the pair is att-modem
- Fake-mode is distinguished by SILICOM_BYPASS pointing at a fake CLI (a path under the test tmp_path,
  i.e. NOT the real /usr/local/sbin/silicom-bypass). Fake-mode runs never reach a real card, so they are
  exempt from live-confirm. Live-mode = SILICOM_BYPASS resolves to the real installed CLI; live-confirm
  IS enforced there.

silicom-bypass verbs the harness composes (NEVER bpctl_util directly):
  status | on <pair> --yes | off <pair> | disc <pair> --yes | conn <pair> | mark <label>
Exit-code contract (mirror): 0 ok/no-op, 1 runtime, 2 usage. Signal exits: 130 (INT), 143 (TERM).
off/conn need NO --yes and are idempotent no-ops if already NIC.

SAFE-16 controller_targets (MUST stay byte-identical to phase225 list):
  src/wanctl/wan_controller.py, wan_controller_state.py, queue_controller.py,
  cake_signal.py, alert_engine.py, fusion_healer.py, backends/  (+ configs/att.yaml witness)
v1.51 = 531f36ac36ceccb2e4dd2d47edd84fba9081c053 (matches safe16-boundary-236.json baseline_commit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-16 boundary-check tool (copy + re-parameterize phase225)</name>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the exact analog — copy near-verbatim; note the embedded JSON `notes` string also carries the SAFE-13 label)
    - .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json (target JSON schema + baseline_commit to match)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "scripts/phase237-safe16-boundary-check.sh" — the things to change)
  </read_first>
  <files>scripts/phase237-safe16-boundary-check.sh</files>
  <action>Copy `scripts/phase225-safe13-boundary-check.sh` to `scripts/phase237-safe16-boundary-check.sh`. Change ONLY: (a) the default anchor assignment to exactly `ANCHOR="v1.51"` (quoted, replacing the prior `v1.48`); (b) default `OUT` -> `.planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json`; (c) EVERY `SAFE-13` -> `SAFE-16` occurrence including the header comment, the final echo string, AND the embedded python/JSON `notes` field string (it currently reads SAFE-13 — it must read SAFE-16 in the emitted evidence), and phase number 225 -> 237 in prose only. Keep the embedded python `controller_targets` list and `att_config = "configs/att.yaml"` BYTE-IDENTICAL — that list IS the SAFE-16 protected set. Keep the read-only posture note (the tool DOES write the evidence JSON; the posture is "read-only git inspection — emits evidence JSON, no worktree/index/ref mutation"). Keep `--anchor`/`--out` flag parsing unchanged. Create the `evidence/` dir if absent. Do NOT touch any `src/wanctl` file. chmod +x the script.</action>
  <verify>
    <automated>shellcheck scripts/phase237-safe16-boundary-check.sh && grep -q 'ANCHOR="v1.51"' scripts/phase237-safe16-boundary-check.sh && bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51 && python3 -c "import json,sys; d=json.load(open('.planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json')); assert d['controller_path_diff_count']==0; assert d['passed'] is True; assert d['baseline_commit']=='531f36ac36ceccb2e4dd2d47edd84fba9081c053', d['baseline_commit']; assert 'SAFE-13' not in json.dumps(d), 'stale SAFE-13 label in evidence'; print('SAFE-16 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - shellcheck clean on `scripts/phase237-safe16-boundary-check.sh`
    - `grep -q 'ANCHOR="v1.51"' scripts/phase237-safe16-boundary-check.sh` matches the real quoted bash assignment
    - `grep -c 'SAFE-13' scripts/phase237-safe16-boundary-check.sh` is 0 — the embedded JSON `notes` string, header, and final echo were all updated SAFE-13 -> SAFE-16 (no stale label anywhere)
    - Running with `--anchor v1.51` emits `evidence/safe16-boundary-237.json`; the emitted JSON contains no `SAFE-13` substring
    - JSON asserts `controller_path_diff_count == 0`, `passed == true`, `baseline_commit == 531f36ac36ceccb2e4dd2d47edd84fba9081c053`
    - `git diff --stat` shows zero `src/wanctl/**` changes from this task
    - The embedded `controller_targets` list is byte-identical to phase225's (diff the python block)
  </acceptance_criteria>
  <done>SAFE-16 tool runs read-only (emits evidence JSON, no tree mutation), emits passing 237 evidence anchored at v1.51, controller list unchanged, no stale SAFE-13 label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED pytest harness — fake silicom-bypass seam + HARN-01..05 + signal-exit + gate-refusal contract</name>
  <read_first>
    - tests/test_silicom_bypass_cli.py (lines 64-209 `_fake_bpctl`/`_fake_logger`; 350-380 `_calls_for`/`_run`) — copy the fake-seam + call-log + env-injection scaffolding shape
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "tests/test_silicom_test_harness.py" — exact `_run`, `_fake_silicom_bypass(fail_on=...)`, and the HARN-04 mid-run + signal tests)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-VALIDATION.md (Per-Task Verification Map — the exact test node IDs)
    - pyproject.toml ([tool.pytest.ini_options] addopts — `--timeout=30 -m 'not integration'`)
  </read_first>
  <files>tests/test_silicom_test_harness.py</files>
  <behavior>
    - test_failover_inject_and_recover: failover spec-modem issues `disc spec-modem --yes` then `conn spec-modem`; calls log ordered disc-before-conn (HARN-01)
    - test_ab_cake_runs_both_arms: ab-cake flips `off spec-modem` -> `on spec-modem --yes` -> `off spec-modem`; both A/B arm dirs written (HARN-02)
    - test_chaos_dispatch_no_scheduling: `chaos <name>` sources the named scenario file; harness source contains NO systemctl-timer/cron/at registration token (HARN-03 + static grep)
    - test_restore_on_midrun_failure: fake fails on an injected verb after `disc`; harness exits non-zero AND calls log shows `off spec-modem` and `conn spec-modem` AFTER the disc (HARN-04 safety-critical)
    - test_restore_on_signal: spawn the running harness with the fake set to block after `disc` (SILICOM_TEST_BLOCK_AFTER=disc), wait for the disc call to appear in calls.log, send SIGTERM; assert the process EXITS with a signal-derived code (returncode == -15 from Popen, i.e. 143) AND that `off`/`conn` landed AFTER the disc — proves the INT/TERM handler restores then exits, not EXIT-only and not a returning trap that lets the script continue
    - test_live_gate_refuses_real_path: a NON-fake invocation (SILICOM_BYPASS pointing at a real-looking path, no SILICOM_TEST_LIVE_CONFIRM) REFUSES with a non-zero exit and a clear gate message, and performs NO disc/on mutation
    - test_att_requires_louder_gate: a real-path att-modem invocation with SILICOM_TEST_LIVE_CONFIRM set but SILICOM_TEST_ATT_CONFIRM unset REFUSES (att-modem needs the louder gate); the same call with both gates set is allowed past the gate (still fake-mutation-guarded so no real card is touched)
    - test_result_dir_layout: a run creates `tests/silicom/<ts>-<scenario>/` with pre/post/snapshots dir, journal extract, result.json with the documented schema keys (HARN-05)
  </behavior>
  <action>Create `tests/test_silicom_test_harness.py`. Mirror `tests/test_silicom_bypass_cli.py`: define `HARNESS = REPO_ROOT/"scripts"/"silicom-test"`, a `_fake_silicom_bypass(tmp_path, fail_on=None, block_after=None)` that writes each invocation (`<verb> <args>`) to a `calls.log`, exits non-zero when `$1 == fail_on`, and (when block_after is given) sleeps/blocks after logging the matching verb so a signal can land deterministically. Provide a `_run(tmp_path, fake_cli, *cli_args, extra_env=None)` setting `SILICOM_BYPASS`, `SILICOM_TEST_SCENARIO_DIR`, `SILICOM_TEST_RESULT_ROOT`, `SILICOM_TEST_SNAPSHOT`/`SILICOM_TEST_HEALTH_POLLER` (point at fakes) and prepending tmp to PATH, plus a `_calls_for` filter. Default the exercised pair to `spec-modem` everywhere. For the signal test, spawn with `subprocess.Popen` using `SILICOM_TEST_BLOCK_AFTER=disc`, poll calls.log until the disc verb appears, send `proc.send_signal(signal.SIGTERM)`, `proc.wait(timeout=...)`, then assert `proc.returncode == -15` (Popen reports the negative signal number for an unhandled-then-exit path; equivalently a shell exit code 143) and that `off`/`conn` followed the disc in calls.log. For test_live_gate_refuses_real_path and test_att_requires_louder_gate, point SILICOM_BYPASS at a real-looking absolute path string (e.g. a sentinel like `/usr/local/sbin/silicom-bypass`) so the harness takes its live-mode branch, and assert refusal without the gates; document in a comment that fake-mode (SILICOM_BYPASS under tmp_path) is the test-safe seam exempt from live-confirm while real-mode is gated. Define the expected result.json key set as a module constant and assert it in test_result_dir_layout. Tests are RED now (no `scripts/silicom-test` yet) — that is expected and correct. Do NOT import `wanctl.*`. No `src/wanctl` touch.</action>
  <verify>
    <automated>.venv/bin/ruff check tests/test_silicom_test_harness.py && .venv/bin/pytest tests/test_silicom_test_harness.py -q --no-header 2>&1 | grep -qE "error|No such file|silicom-test" && echo "RED-as-expected (harness absent)" && git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml</automated>
  </verify>
  <acceptance_criteria>
    - File defines all node IDs: test_failover_inject_and_recover, test_ab_cake_runs_both_arms, test_chaos_dispatch_no_scheduling, test_restore_on_midrun_failure, test_restore_on_signal, test_live_gate_refuses_real_path, test_att_requires_louder_gate, test_result_dir_layout
    - test_restore_on_signal asserts a signal-derived EXIT (e.g. `proc.returncode == -15` / shell 143), not merely that restore verbs landed — it proves the harness terminates after restore
    - `_fake_silicom_bypass` supports `block_after` (driven by SILICOM_TEST_BLOCK_AFTER) so the signal test is deterministic, not racy
    - test_live_gate_refuses_real_path asserts a real-path run without SILICOM_TEST_LIVE_CONFIRM refuses non-zero and issues NO disc/on; test_att_requires_louder_gate asserts att-modem refuses without SILICOM_TEST_ATT_CONFIRM
    - `ruff check tests/test_silicom_test_harness.py` clean
    - `grep -nE '^[^#]*att-modem|--both-wan-confirm' tests/test_silicom_test_harness.py` shows att-modem ONLY inside the gate-refusal assertions (test_att_requires_louder_gate) — no att-modem is actually toggled/exercised (no failover/ab-cake/chaos run drives att-modem), and `--both-wan-confirm` never appears
    - Tests are RED (collected but failing because `scripts/silicom-test` does not yet exist) — NOT errors-on-collection from import bugs
    - No `import wanctl` and no `src/wanctl` path in the file
    - `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check)
  </acceptance_criteria>
  <done>RED tests pin HARN-01..05 (incl. HARN-04 mid-run + signal-EXIT proofs) and the live/att gate-refusal boundary; fake-CLI seam mirrors the bypass test with a block-after seam; spec-modem is the only exercised pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git worktree -> SAFE-16 evidence JSON | read-only inspection; emits evidence JSON only; must not mutate worktree/index/refs |
| operator/test invocation -> harness contract | the test seam stands in for live card; no real bpctl reachable in fake-mode |
| fake-mode vs live-mode boundary | fake-mode (SILICOM_BYPASS under tmp_path) is exempt from live-confirm; live-mode (real installed CLI) is gated by SILICOM_TEST_LIVE_CONFIRM (+ SILICOM_TEST_ATT_CONFIRM for att-modem) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-01 | Tampering | phase237-safe16-boundary-check.sh accidentally mutating worktree | mitigate | Copy of read-only phase225 tool; preserve its no-mutation posture note (emits JSON only); verify `git status` clean after run |
| T-237-02 | Spoofing | wrong git anchor producing a false SAFE-16 pass | mitigate | Pin `ANCHOR="v1.51"`; assert `baseline_commit==531f36ac...` in the verify step |
| T-237-03 | Information Disclosure | test fixtures leaking real state/IPs | accept | Tests use synthetic tmp_path fixtures only; no live state read; no secret surface |
| T-237-17 | Elevation of Privilege | a real custom SILICOM_BYPASS path bypassing live-confirm | mitigate | RED test_live_gate_refuses_real_path + test_att_requires_louder_gate pin that only fake-mode (tmp_path CLI) is exempt; real-mode refuses without the gates |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — this phase installs zero external packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- `shellcheck scripts/phase237-safe16-boundary-check.sh` clean
- `grep -c 'SAFE-13' scripts/phase237-safe16-boundary-check.sh` is 0 (notes/header/echo all updated)
- `bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51` -> evidence JSON `passed: true`, `controller_path_diff_count: 0`
- `.venv/bin/ruff check tests/test_silicom_test_harness.py` clean
- Harness test file is RED (orchestrator intentionally absent until Plan 02); signal test asserts signal-derived EXIT
- `git diff --exit-code v1.51..HEAD -- src/wanctl configs/att.yaml` exits 0 (per-wave SAFE-16 drift check)
</verification>

<success_criteria>
- SAFE-16 tool exists, runs read-only (emits evidence JSON), emits passing v1.51-anchored 237 evidence, no stale SAFE-13 label
- RED pytest pins HARN-01..05 including the HARN-04 mid-run-failure AND signal-EXIT proofs, plus the live/att gate-refusal boundary
- spec-modem is the only exercised pair in tests; no `--both-wan-confirm`; att-modem appears only in the gate-refusal assertions, never exercised
- No controller-path file touched (per-wave SAFE-16 diff clean)
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-01-SUMMARY.md` when done
</output>
</content>
</invoke>
