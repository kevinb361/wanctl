---
phase: 237-hil-failure-injection-harness-closeout
plan: 03
type: execute
wave: 3
depends_on: ["237-02"]
files_modified:
  - scripts/deploy.sh
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
autonomous: true
requirements: [DEPLOY-03]
must_haves:
  truths:
    - "deploy.sh --silicom-bypass-only installs silicom-test + scenario files via the existing standalone path"
    - "test_artifacts_repo_owned asserts silicom-test + scenarios are repo-owned and referenced by deploy.sh"
    - "docs/SILICOM-BYPASS.md documents the harness as part of the single standalone deploy path + operator live gate"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "deploy_silicom_bypass extended to stage+install silicom-test + scenarios (atomic, off-by-default)"
      contains: "silicom-test"
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "DEPLOY-03 artifact-ownership assertions for harness artifacts"
      contains: "silicom-test"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "Documented deploy + operator-gated harness procedure"
      contains: "silicom-test"
  key_links:
    - from: "scripts/deploy.sh"
      to: "scripts/silicom-test"
      via: "scp + sudo install in deploy_silicom_bypass"
      pattern: "scripts/silicom-test"
    - from: "tests/test_silicom_bypass_cli.py"
      to: "scripts/deploy.sh"
      via: "test_artifacts_repo_owned parses $PROJECT_ROOT refs from deploy body"
      pattern: "silicom-test"
---

<objective>
Finalize DEPLOY-03: make all bypass tooling — including the new `silicom-test` harness and its scenario files — repo-owned and deployable via the single documented standalone path. Per the RESEARCH DEPLOY-03 decision (and SEED-006 open question), REUSE the existing `deploy.sh --silicom-bypass-only` standalone mode (true-standalone, atomic, off-by-default) rather than a separate installer. Extend the deploy function, extend the artifact-ownership tests, and document the harness in the existing standalone-install + operator-gated sections of `docs/SILICOM-BYPASS.md`.

Purpose: A single, tested, documented deploy path is the DEPLOY-03 contract. A separate installer would duplicate the proven mktemp/atomic-install/daemon-reload logic for zero benefit.
Output: extended `deploy_silicom_bypass`, new ownership assertions, updated docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md

@scripts/deploy.sh
@tests/test_silicom_bypass_cli.py
@docs/SILICOM-BYPASS.md

<interfaces>
<!-- Exact analog identifiers to extend (from 237-PATTERNS.md + read of deploy.sh/tests). -->

deploy.sh: deploy_silicom_bypass() (the standalone install fn) uses per-deploy private staging:
  remote_tmp=$(ssh ... mktemp -d); chmod 700; scp "$PROJECT_ROOT/scripts/<x>" ...;
  ssh ... sudo install -o root -g root -m 0755 ... /usr/local/sbin/<x>; ... rm -rf "$remote_tmp"
  Ends with `systemctl daemon-reload` only; enables/starts NOTHING.
  print_silicom_bypass_plan (dry-run) must also list new artifacts.

tests/test_silicom_bypass_cli.py:
  SILICOM_BYPASS_ARTIFACTS set (~lines 26-37) — add scripts/silicom-test + scenario file(s).
  test_artifacts_repo_owned() (~lines 1323-1351) parses $PROJECT_ROOT/<path> refs out of the deploy
  function body and asserts each exists in repo; new entries MUST appear in the deploy body to be picked up.
  Standalone guards that must stay green: test_deploy_has_silicom_standalone_mode,
  test_silicom_standalone_short_circuits, test_silicom_deploy_uses_private_atomic_staging,
  test_deploy_watchdog_off_by_default.

docs/SILICOM-BYPASS.md: "Standalone install" section (~lines 102-124) bullet list of what
  --silicom-bypass-only installs; "Operator-gated live procedures" section (~line 97).
  Public-safe: no IPs/hostnames beyond existing cake-shaper and 127.0.0.1.

Harness install targets: /usr/local/sbin/silicom-test (0755),
  /usr/local/share/silicom-test-scenarios/*.sh (0644) — matches the SILICOM_TEST_SCENARIO_DIR default.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deploy_silicom_bypass to stage+install silicom-test + scenarios</name>
  <read_first>
    - scripts/deploy.sh (deploy_silicom_bypass ~574-617; the standalone --silicom-bypass-only handler ~865-898; print_silicom_bypass_plan dry-run path)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "scripts/deploy.sh" — atomic-install idiom to copy per artifact)
    - scripts/silicom-test, scripts/silicom-test-scenarios/ (the artifacts to install — from Plan 02)
  </read_first>
  <files>scripts/deploy.sh</files>
  <action>In deploy_silicom_bypass(), add staging+install for scripts/silicom-test using the SAME per-deploy private idiom already in the function: scp "$PROJECT_ROOT/scripts/silicom-test" into the existing remote_tmp, then sudo install -o root -g root -m 0755 to /usr/local/sbin/silicom-test, echo the destination. Install each scripts/silicom-test-scenarios/*.sh into /usr/local/share/silicom-test-scenarios/ at mode 0644 (mkdir the dir via sudo install -d if needed) matching the harness SILICOM_TEST_SCENARIO_DIR default. Reuse the same remote_tmp and the existing trailing rm -rf cleanup; do NOT add a second mktemp. Keep the function ending at systemctl daemon-reload only — enable/start NOTHING (preserve off-by-default posture). Add the same artifacts to the print_silicom_bypass_plan dry-run output so --dry-run lists them. Do NOT touch any src/wanctl file. Do NOT change the standalone short-circuit logic or the WAN-flag rejection.</action>
  <verify>
    <automated>shellcheck scripts/deploy.sh && bash -n scripts/deploy.sh && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k "standalone or watchdog_off or atomic_staging or short_circuit" -q</automated>
  </verify>
  <acceptance_criteria>
    - shellcheck + bash -n clean on scripts/deploy.sh
    - deploy_silicom_bypass body contains scripts/silicom-test and the scenario dir install
    - Existing standalone guards stay green: test_deploy_has_silicom_standalone_mode, test_silicom_standalone_short_circuits, test_silicom_deploy_uses_private_atomic_staging, test_deploy_watchdog_off_by_default
    - No second mktemp staging dir added inside deploy_silicom_bypass (reuses existing remote_tmp)
    - Function still ends with daemon-reload only; no systemctl enable/start added
    - git diff --stat shows zero src/wanctl changes
  </acceptance_criteria>
  <done>Standalone deploy installs the harness + scenarios atomically, off-by-default, with dry-run coverage; existing standalone guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEPLOY-03 artifact-ownership assertions + docs</name>
  <read_first>
    - tests/test_silicom_bypass_cli.py (SILICOM_BYPASS_ARTIFACTS set ~26-37; test_artifacts_repo_owned ~1323-1351)
    - docs/SILICOM-BYPASS.md (Standalone install ~102-124; Operator-gated live procedures ~97)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (sections "tests/test_silicom_bypass_cli.py" + "docs/SILICOM-BYPASS.md")
    - scripts/deploy.sh (the deploy body Task 1 just extended — the test parses $PROJECT_ROOT refs from it)
  </read_first>
  <files>tests/test_silicom_bypass_cli.py, docs/SILICOM-BYPASS.md</files>
  <behavior>
    - test_artifacts_repo_owned: parsed $PROJECT_ROOT refs from deploy body now include scripts/silicom-test; every SILICOM_BYPASS_ARTIFACTS entry (incl. silicom-test + scenarios) exists in repo
    - DEPLOY-03 coverage: the harness + at least one scenario file are asserted repo-owned and referenced by deploy.sh
  </behavior>
  <action>Add scripts/silicom-test and the scenario file(s) (scripts/silicom-test-scenarios/cake-ab-spectrum.sh, scripts/silicom-test-scenarios/failover-spectrum.sh) to the SILICOM_BYPASS_ARTIFACTS set in tests/test_silicom_bypass_cli.py so test_artifacts_repo_owned asserts they exist AND are referenced from the deploy body. If scenarios are installed via a glob/dir loop rather than per-file scp, add a dir-membership assertion proving the scenario dir is repo-owned and wired into deploy.sh. In docs/SILICOM-BYPASS.md: extend the "Standalone install" bullet list to add /usr/local/sbin/silicom-test and /usr/local/share/silicom-test-scenarios/; add a short "Failure-injection harness" subsection under "Operator-gated live procedures" documenting the explicit live-WAN gate (SILICOM_TEST_LIVE_CONFIRM), default-safe spec-modem pair, the louder att-modem gate, and the tests/silicom/<ts>-<scenario>/ result layout. Keep it public-safe (only cake-shaper + 127.0.0.1 literals). Do NOT touch src/wanctl.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -k "deploy or repo_owned or artifacts" -q && python3 -c "t=open('docs/SILICOM-BYPASS.md').read(); assert 'silicom-test' in t and 'spec-modem' in t; print('docs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - test_artifacts_repo_owned green with scripts/silicom-test + scenario file(s) in SILICOM_BYPASS_ARTIFACTS
    - Full tests/test_silicom_bypass_cli.py suite stays green (W-INV -k invariant included)
    - docs/SILICOM-BYPASS.md mentions silicom-test, spec-modem default-safe pair, SILICOM_TEST_LIVE_CONFIRM gate, and tests/silicom/ result layout
    - docs contain no IPs/hostnames beyond cake-shaper and 127.0.0.1
    - git diff --stat shows zero src/wanctl changes
  </acceptance_criteria>
  <done>Harness artifacts proven repo-owned + deploy-referenced; the single documented standalone path covers them; docs describe the operator gate and result layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev repo -> cake-shaper root install | scp + sudo install crosses host + privilege boundary |
| deploy mode selection -> wanctl release path | standalone must NOT trigger the wanctl restart path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-10 | Tampering | non-atomic install leaving a partial harness | mitigate | Reuse the proven per-deploy mktemp + chmod 700 + atomic `sudo install` idiom; staged then installed |
| T-237-11 | Elevation of Privilege | deploy enabling/starting a unit unexpectedly | mitigate | Preserve off-by-default posture: function ends at daemon-reload only; test_deploy_watchdog_off_by_default stays green |
| T-237-12 | Tampering | standalone mode bleeding into wanctl release cadence | mitigate | Do not change the short-circuit; test_silicom_standalone_short_circuits stays green |
| T-237-13 | Information Disclosure | docs leaking private host/IP detail | mitigate | Public-safe doc rule (CLAUDE.md): only cake-shaper + 127.0.0.1 literals |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- shellcheck + bash -n clean on scripts/deploy.sh
- Existing standalone/off-by-default guards green
- test_artifacts_repo_owned green with harness + scenarios
- Full tests/test_silicom_bypass_cli.py suite green (incl. -k invariant)
- docs public-safe, mention harness + operator gate + result layout
- git diff --stat shows zero src/wanctl modifications
</verification>

<success_criteria>
- All bypass tooling (CLI, watchdog units, boot service, harness, scenarios) repo-owned + deployable via one documented standalone path (DEPLOY-03)
- Off-by-default posture preserved; standalone decoupling intact
- Zero controller-path mutation
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-03-SUMMARY.md` when done
</output>
