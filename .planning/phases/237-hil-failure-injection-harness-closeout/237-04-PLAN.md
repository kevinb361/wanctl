---
phase: 237-hil-failure-injection-harness-closeout
plan: 04
type: execute
wave: 4
depends_on: ["237-01", "237-02", "237-03"]
files_modified:
  - .gitignore
  - .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json
autonomous: false
requirements: [SAFE-16, HARN-05]
must_haves:
  truths:
    - "tests/silicom/ runtime result artifacts are gitignored (ephemeral HIL output, redaction-safe by default)"
    - "SAFE-16 controller-path zero-diff is proven at the 237 phase boundary against v1.51"
    - "Operator confirms the close-time SAFE-16 evidence before milestone close"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rule for ephemeral tests/silicom/ HIL result dirs"
      contains: "tests/silicom/"
    - path: ".planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json"
      provides: "SAFE-16 phase-boundary + milestone-close evidence (controller_path_diff_count: 0)"
      contains: "controller_path_diff_count"
  key_links:
    - from: "scripts/phase237-safe16-boundary-check.sh"
      to: ".planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json"
      via: "tool run at phase boundary + close, anchor v1.51"
      pattern: "safe16-boundary-237"
---

<objective>
Close the phase: (1) resolve RESEARCH Open Question 1 by `.gitignore`-ing `tests/silicom/` (ephemeral HIL result dirs — the recommended default; redaction is already enforced by `phase213-steering-snapshot.sh`, but gitignore is the belt-and-suspenders against IP/secret leak per CLAUDE.md public-safe), and (2) run the SAFE-16 boundary-check tool at the 237 phase boundary, proving controller-path zero-diff against `v1.51`, then have the operator confirm the close-time evidence — the 10th consecutive milestone holding the SAFE-07..15 discipline.

Purpose: SAFE-16 is the milestone-close invariant. The harness must not have nudged a single controller-path byte. The gitignore prevents runtime HIL artifacts from leaking sensitive state into the tracked tree.
Output: `.gitignore` rule, `evidence/safe16-boundary-237.json` (passing), operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md

@scripts/phase237-safe16-boundary-check.sh
@.gitignore

<interfaces>
<!-- SAFE-16 evidence contract (schema proven in safe16-boundary-236.json). -->

scripts/phase237-safe16-boundary-check.sh --anchor v1.51 emits:
  .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json
Required asserts: passed == true, controller_path_diff_count == 0, att_config_diff_count == 0,
  dirty_tree_clean == true, all per_file_sha256_equal true,
  baseline_commit == 531f36ac36ceccb2e4dd2d47edd84fba9081c053 (git rev-parse v1.51).
Posture: read-only git inspection — no worktree/index/ref mutation.

Open Question 1 resolution: .gitignore tests/silicom/ (ephemeral). Operator-significant
runs may opt-in to committing a REDACTED result.json under the phase evidence dir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gitignore ephemeral HIL result dirs + run SAFE-16 boundary proof</name>
  <read_first>
    - .gitignore (append target — confirm no existing tests/silicom rule)
    - scripts/phase237-safe16-boundary-check.sh (the SAFE-16 tool from Plan 01)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md (Open Question 1; Pitfall 5 anchor-drift)
    - .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json (schema/asserts to match)
  </read_first>
  <files>.gitignore, .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json</files>
  <action>Append a `tests/silicom/` ignore rule to .gitignore (ephemeral HIL result dirs; redaction-safe default per RESEARCH Open Question 1) with a one-line comment noting redacted result.json summaries may be opt-in committed under the phase evidence dir. Confirm no existing rule already covers it (avoid a duplicate). Then run `bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51` to regenerate evidence/safe16-boundary-237.json at the current phase-boundary HEAD (after Plans 01-03 landed). This is read-only git inspection — it must not mutate the worktree/index/refs. Do NOT touch any src/wanctl file. If the boundary check reports a non-zero controller_path_diff_count, STOP — a controller-path file was touched and must be reverted before proceeding (this is the SAFE-16 violation guard).</action>
  <verify>
    <automated>grep -q "tests/silicom/" .gitignore && bash scripts/phase237-safe16-boundary-check.sh --anchor v1.51 && python3 -c "import json; d=json.load(open('.planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json')); assert d['passed'] is True; assert d['controller_path_diff_count']==0; assert d['att_config_diff_count']==0; assert d['dirty_tree_clean'] is True; assert d['baseline_commit']=='531f36ac36ceccb2e4dd2d47edd84fba9081c053'; print('SAFE-16 boundary OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains a tests/silicom/ rule with an explanatory comment
    - evidence/safe16-boundary-237.json asserts passed==true, controller_path_diff_count==0, att_config_diff_count==0, dirty_tree_clean==true, baseline_commit==531f36ac...
    - all per_file_sha256_equal values are true
    - git status shows tests/silicom/ runtime artifacts are ignored (a stray run dir does not appear as untracked)
    - git diff --stat shows zero src/wanctl modifications across the whole phase
  </acceptance_criteria>
  <done>HIL artifacts gitignored; SAFE-16 phase-boundary evidence passes at v1.51 with zero controller-path diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SAFE-16 milestone-close operator sign-off</name>
  <what-built>
    The full Phase 237 HIL harness (silicom-test failover/ab-cake/chaos with always-on restore trap), the DEPLOY-03 single documented standalone deploy path, and the SAFE-16 phase-boundary zero-diff evidence at v1.51 (controller_path_diff_count: 0). This is the v1.52 milestone-close gate — the 10th consecutive milestone holding the SAFE-07..15 controller-path zero-diff discipline.
  </what-built>
  <how-to-verify>
    1. Review the SAFE-16 evidence: `cat .planning/phases/237-hil-failure-injection-harness-closeout/evidence/safe16-boundary-237.json` — confirm passed=true, controller_path_diff_count=0, baseline_commit matches `git rev-parse v1.51`.
    2. Confirm zero controller-path drift across the milestone: `git diff --stat v1.51..HEAD -- src/wanctl/` should be empty.
    3. (Optional, on cake-shaper) Run a live spec-modem failover behind the gate: `SILICOM_TEST_LIVE_CONFIRM=1 silicom-test failover spec-modem`, then `silicom-bypass status all` to confirm all pairs returned to NIC mode. Inspect the result dir under tests/silicom/.
    4. Confirm HARN-04 safety: review that test_restore_on_midrun_failure and test_restore_on_signal are green in the suite.
  </how-to-verify>
  <resume-signal>Type "approved" to record SAFE-16 milestone-close sign-off, or describe issues to address.</resume-signal>
  <action>Verification-only checkpoint — no mutation. Present the SAFE-16 close-time evidence and the empty src/wanctl milestone diff to the operator and wait for explicit approval before milestone close. Record the sign-off (operator name + UTC timestamp) in the plan SUMMARY.</action>
  <verify><human-check>Operator types "approved" after confirming controller_path_diff_count=0 and empty v1.51..HEAD src/wanctl diff.</human-check></verify>
  <done>Operator sign-off recorded in 237-04-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git worktree -> SAFE-16 evidence | read-only inspection; must not mutate tree/index/refs |
| runtime HIL output -> tracked repo tree | result dirs may contain state JSON; must not leak into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-14 | Spoofing | wrong anchor producing a false SAFE-16 pass | mitigate | Pin v1.51; assert baseline_commit==531f36ac... in verify; operator confirms at close |
| T-237-15 | Information Disclosure | runtime state JSON leaking into tracked tree | mitigate | .gitignore tests/silicom/; redaction already enforced by phase213-steering-snapshot.sh; opt-in commit only redacted summaries |
| T-237-16 | Tampering | a controller-path file silently modified during the phase | mitigate | SAFE-16 tool fails closed on any controller_path_diff_count>0; task STOPs on non-zero; operator gate |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- .gitignore covers tests/silicom/
- SAFE-16 evidence passes at v1.51 (controller_path_diff_count: 0, dirty_tree_clean: true)
- git diff --stat v1.51..HEAD -- src/wanctl/ is empty
- Operator records milestone-close sign-off
</verification>

<success_criteria>
- SAFE-16 zero-diff proven at phase boundary AND confirmed at milestone close (10th consecutive)
- Ephemeral HIL artifacts gitignored; no state/secret leak into tracked tree
- Operator sign-off recorded
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-04-SUMMARY.md` when done
</output>
