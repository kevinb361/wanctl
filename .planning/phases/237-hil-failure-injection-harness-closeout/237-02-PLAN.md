---
phase: 237-hil-failure-injection-harness-closeout
plan: 02
type: execute
wave: 2
depends_on: ["237-01"]
files_modified:
  - scripts/silicom-test
  - scripts/silicom-test-scenarios/cake-ab-spectrum.sh
  - scripts/silicom-test-scenarios/failover-spectrum.sh
autonomous: true
requirements: [HARN-01, HARN-02, HARN-03, HARN-04, HARN-05]
must_haves:
  truths:
    - "silicom-test failover <pair> injects disc, captures state, recovers via conn (HARN-01)"
    - "silicom-test ab-cake <pair> flips off->on->off running both A/B arms on the same pair (HARN-02)"
    - "silicom-test chaos <name> dispatches a named scenario file, operator-invoked only, no scheduling (HARN-03)"
    - "An always-on EXIT+INT+TERM trap restores every touched pair to NIC mode regardless of outcome (HARN-04)"
    - "Each run writes tests/silicom/<timestamp>-<scenario>/ with pre/post state, snapshots, raw output, journal (HARN-05)"
  artifacts:
    - path: "scripts/silicom-test"
      provides: "HIL orchestrator composing silicom-bypass verbs with always-on restore trap"
      contains: "restore_all_touched"
      min_lines: 120
    - path: "scripts/silicom-test-scenarios/cake-ab-spectrum.sh"
      provides: "Named ab-cake chaos scenario (spec-modem)"
      min_lines: 5
    - path: "scripts/silicom-test-scenarios/failover-spectrum.sh"
      provides: "Named failover chaos scenario (spec-modem)"
      min_lines: 5
  key_links:
    - from: "scripts/silicom-test"
      to: "scripts/silicom-bypass"
      via: "SILICOM_BYPASS verb composition (never bpctl_util)"
      pattern: "SILICOM_BYPASS.*\\b(disc|conn|on|off|mark|status)\\b"
    - from: "scripts/silicom-test"
      to: "restore_all_touched"
      via: "trap registered before first mutation"
      pattern: "trap restore_all_touched EXIT INT TERM"
    - from: "scripts/silicom-test"
      to: "scripts/phase213-steering-snapshot.sh"
      via: "capture_state helper"
      pattern: "phase213-steering-snapshot"
---

<objective>
Build `scripts/silicom-test` — the HIL failure-injection orchestrator — and two seed chaos scenario files, turning the RED Plan-01 tests GREEN. The orchestrator composes the proven `silicom-bypass` verbs (NEVER `bpctl_util`), registers an always-on restore trap BEFORE any mutation (the W-INV `sentineled_stop` discipline from Phase 236), reuses the existing `phase213-*` capture scripts, and writes structured results to `tests/silicom/<timestamp>-<scenario>/`.

Purpose: This is the safety-critical deliverable — the harness drives live-WAN Silicom bypass pairs. The trap is the keeper mechanism; a pair must NEVER be left in bypass/disconnect after a crash, error, or Ctrl-C.
Output: `scripts/silicom-test` (failover/ab-cake/chaos), `scripts/silicom-test-scenarios/*.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-RESEARCH.md
@.planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md

@scripts/silicom-bypass
@scripts/phase213-steering-snapshot.sh
@scripts/phase213-health-poller.sh

<interfaces>
<!-- Exact analog identifiers the orchestrator must mirror (from 237-PATTERNS.md). -->

Env seams (overridable for the Plan-01 pytest fakes):
  SILICOM_BYPASS              (default /usr/local/sbin/silicom-bypass)
  SILICOM_TEST_SNAPSHOT       (default scripts/phase213-steering-snapshot.sh)
  SILICOM_TEST_HEALTH_POLLER  (default scripts/phase213-health-poller.sh)
  SILICOM_TEST_SCENARIO_DIR   (default /usr/local/share/silicom-test-scenarios)
  SILICOM_TEST_RESULT_ROOT    (default tests/silicom)
  Do NOT seam BPCTL_UTIL — silicom-bypass owns that internally.

Composed silicom-bypass verbs (from scripts/silicom-bypass):
  status [pair|all] | on <pair> --yes | off <pair> | disc <pair> --yes | conn <pair> | mark <label>
  off/conn: NO --yes, idempotent no-op if already NIC -> safe to call unconditionally in trap.
  on/disc: require --yes; both-WAN gate fires unless --both-wan-confirm (NEVER pass it here).
  Exit codes: 0 ok/no-op, 1 runtime, 2 usage.

Trap precedent: silicom-bypass sentineled_stop (trap before mutation, local rc=$? first, restore best-effort, return rc).
die/journal/journal_best_effort helpers: copy verbatim from scripts/silicom-bypass header.

Capture endpoints (live on cake-shaper; tests stub via fakes):
  steering /health 127.0.0.1:9102 . bridge /health 127.0.0.1:9101 . /var/lib/wanctl/<wan>_state.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: silicom-test orchestrator — trap skeleton + failover + ab-cake</name>
  <read_first>
    - scripts/silicom-bypass (lines 1-46 header/die/journal; 300-324 sentineled_stop trap precedent; 403-431 off/conn idempotency; 485-510 main dispatch)
    - scripts/phase213-steering-snapshot.sh (capture/redaction/EXIT-trap-clean mktemp pattern to wrap in capture_state)
    - scripts/phase213-health-poller.sh (1Hz NDJSON poller invoked in background, killed by trap)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "scripts/silicom-test" — header seams, main(), restore_all_touched, mark_touched, failover/ab-cake flows)
    - tests/test_silicom_test_harness.py (the contract this must satisfy — read the assertions)
  </read_first>
  <files>scripts/silicom-test</files>
  <action>Create scripts/silicom-test (bash, set -euo pipefail). Header: the env seams listed in interfaces (NO BPCTL_UTIL). Copy die/journal/journal_best_effort verbatim from silicom-bypass; reuse exit-code contract 0/1/2. Define declare -a TOUCHED_PAIRS=(), mark_touched <pair> (dedup-append), and restore_all_touched whose FIRST line is local rc=$?, loops touched pairs calling "$SILICOM_BYPASS" off <pair> then conn <pair> best-effort (|| journal_best_effort), emits a final mark, and return "$rc". Register trap restore_all_touched EXIT INT TERM BEFORE any mutation. Implement cmd_failover <pair> (RESEARCH Pattern 3: mark_touched; mark PRE; capture_state pre; background health poller; disc <pair> --yes; mark PULLED; capture during; sleep RECOVERY_WINDOW; conn <pair>; capture post) and cmd_ab_cake <pair> (Pattern 4: mark_touched; off baseline; run_ab_probe A-arm; on <pair> --yes; run_ab_probe B-arm; off). run_ab_probe writes the arm dir and invokes the operator probe via an overridable SILICOM_TEST_PROBE seam (default a documented netperf placeholder; do NOT hardcode iperf — MEMORY: no probe swap without approval). capture_state <dir> wraps $SNAPSHOT; start_health_poller backgrounds $HEALTH_POLLER. Default exercised pair spec-modem; require explicit live-WAN gate env SILICOM_TEST_LIVE_CONFIRM for any real run and a louder gate for att-modem (Pitfall 3); NEVER pass --both-wan-confirm. RESULT dir = $SILICOM_TEST_RESULT_ROOT/<utc-timestamp>-<scenario>-<pair>/. main() dispatches failover/ab-cake/chaos (chaos in Task 2). Do NOT import or call any src/wanctl code or bpctl_util directly.</action>
  <verify>
    <automated>shellcheck scripts/silicom-test && .venv/bin/pytest tests/test_silicom_test_harness.py -k "failover or ab_cake or restore_on_midrun or restore_on_signal or result_dir" -q</automated>
  </verify>
  <acceptance_criteria>
    - shellcheck clean on scripts/silicom-test
    - grep -v '^#' scripts/silicom-test | grep -c 'BPCTL_UTIL\|bpctl_util' is 0 — composes silicom-bypass only
    - grep -q "trap restore_all_touched EXIT INT TERM" scripts/silicom-test and the trap line precedes the first disc/on call (verify by line number)
    - GREEN: test_failover_inject_and_recover, test_ab_cake_runs_both_arms, test_restore_on_midrun_failure, test_restore_on_signal, test_result_dir_layout pass
    - restore_all_touched first non-comment line is local rc=$? and the function ends return "$rc"
    - No src/wanctl path, no import wanctl, no --both-wan-confirm literal in the file
  </acceptance_criteria>
  <done>Orchestrator composes silicom-bypass verbs under an always-on trap; failover/ab-cake/HARN-04/HARN-05 tests pass; no controller-path or raw-bpctl touch.</done>
</task>

<task type="auto">
  <name>Task 2: chaos dispatch + seed scenario files</name>
  <read_first>
    - scripts/silicom-test (the orchestrator from Task 1 — scenarios are sourced into its trap scope)
    - .planning/phases/237-hil-failure-injection-harness-closeout/237-PATTERNS.md (section "scripts/silicom-test-scenarios/*.sh" + cmd_chaos path-traversal guard; Security Domain V5)
    - tests/test_silicom_test_harness.py (test_chaos_dispatch_no_scheduling — the contract)
  </read_first>
  <files>scripts/silicom-test, scripts/silicom-test-scenarios/cake-ab-spectrum.sh, scripts/silicom-test-scenarios/failover-spectrum.sh</files>
  <action>Add cmd_chaos <name> to scripts/silicom-test: reject names containing / or .. (path-traversal guard, exit 2 via die), resolve $SCENARIO_DIR/<name>.sh, require the file exists, then source it (. "$file") so it runs inside the orchestrator trap scope and may call mark_touched/$SILICOM_BYPASS/capture_state. Introduce NO systemd timer, cron, or at registration anywhere — operator-invoked only (HARN-03). Create scripts/silicom-test-scenarios/cake-ab-spectrum.sh (calls cmd_ab_cake spec-modem) and scripts/silicom-test-scenarios/failover-spectrum.sh (calls cmd_failover spec-modem), each with a header comment stating operator-invoked, no scheduling. Scenario files are sourced (mode 0644), not executed.</action>
  <verify>
    <automated>shellcheck scripts/silicom-test scripts/silicom-test-scenarios/*.sh && .venv/bin/pytest tests/test_silicom_test_harness.py -q && grep -rEL "OnCalendar|crontab|systemctl (enable|start).*timer" scripts/silicom-test scripts/silicom-test-scenarios/ >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - shellcheck clean on scripts/silicom-test and all scenario files
    - GREEN: test_chaos_dispatch_no_scheduling passes; full tests/test_silicom_test_harness.py suite green (all six HARN tests)
    - cmd_chaos rejects a name containing / or .. with exit code 2 (path-traversal guard)
    - grep -rE "OnCalendar|crontab|cron|systemctl.*timer|\bat now\b" scripts/silicom-test scripts/silicom-test-scenarios/ returns nothing (no scheduling — HARN-03)
    - Both scenario files reference spec-modem only; neither references att-modem or --both-wan-confirm
  </acceptance_criteria>
  <done>chaos dispatch is path-traversal-guarded and scheduling-free; two spec-modem seed scenarios ship; full harness suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live WAN scenario | running failover/ab-cake against a live Silicom pair (untrusted destructive action) |
| chaos <name> arg -> scenario file path | operator-supplied name must not escape the scenario dir |
| harness -> Silicom card | every mutation crosses into hardware path-switch; must be reversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-04 | DoS (self-inflicted) | pair left in bypass/disc after crash/signal | mitigate | always-on `trap restore_all_touched EXIT INT TERM` registered before first mutation; off/conn idempotent restore; proven by test_restore_on_midrun_failure + test_restore_on_signal |
| T-237-05 | DoS (dual-WAN loss) | single-pair scenario accidentally dropping both WANs | mitigate | default-safe pair = spec-modem; never pass `--both-wan-confirm`; silicom-bypass both-WAN gate stays armed |
| T-237-06 | Tampering / EoP | chaos name path traversal (`chaos ../../etc/x`) | mitigate | reject `/` and `..`, resolve under fixed SCENARIO_DIR, require file exists (exit 2 otherwise) |
| T-237-07 | Information Disclosure | raw state/IPs leaking into tests/silicom/ | mitigate | capture_state wraps phase213-steering-snapshot.sh (redacted-only output; raw in process-lifetime mktemp); never `cat state.json` into result dir |
| T-237-08 | Tampering | running a live scenario against ATT (canary in other workstreams) | mitigate | default pair spec-modem; SILICOM_TEST_LIVE_CONFIRM gate; louder gate for att-modem (Pitfall 3) |
| T-237-09 | Tampering | unintended live watchdog arming during a scenario | mitigate | harness never raw-stops a watchdog; only composes silicom-bypass disarm if needed; W-INV `-k invariant` gate stays green |
| T-237-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero external packages added (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- shellcheck clean on scripts/silicom-test + scenario files
- Full tests/test_silicom_test_harness.py green (HARN-01..05, incl. mid-run + signal restore proofs)
- No scheduling token anywhere in the harness or scenarios (HARN-03)
- No bpctl_util direct call, no src/wanctl path, no --both-wan-confirm literal
- W-INV `-k invariant` gate still green (.venv/bin/pytest tests/ -k invariant -q)
</verification>

<success_criteria>
- silicom-test composes silicom-bypass verbs only, under an always-on restore trap
- failover/ab-cake/chaos all work; mid-run-failure and signal both restore all touched pairs to NIC
- spec-modem default-safe; ATT requires louder gate; dual-WAN structurally guarded
- Zero controller-path mutation
</success_criteria>

<output>
Create `.planning/phases/237-hil-failure-injection-harness-closeout/237-02-SUMMARY.md` when done
</output>
