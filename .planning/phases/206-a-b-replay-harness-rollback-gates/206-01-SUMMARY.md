---
phase: 206-a-b-replay-harness-rollback-gates
plan: 01
subsystem: testing
tags: [replay-harness, golden-fixture, schema-v1, flent, safe-09]

requires:
  - phase: 193
    provides: Phase 193 replay primitives (`_fresh_controller`, `_replay`, `_snap`, `TRACE`, expected rates/zones)
  - phase: 205
    provides: SAFE-09 source-diff baseline and topology-correct CAKE config context
provides:
  - Deterministic Phase 206 golden NDJSON fixture and regeneration tool
  - Frozen GoldenSample loader and per-sample replay helper that consumes every fixture row
  - Schema-v1 A/B replay CLI with honest controller-vs-flent metric source reporting
affects: [phase-206-plan-02, phase-209-canary-diff, TOPO-04]

tech-stack:
  added: []
  patterns: [stdlib gzip/json flent parsing, Phase 193 replay primitive reuse, schema-v1 JSON]

key-files:
  created:
    - tests/fixtures/_phase_206_generator.py
    - tests/fixtures/phase206_golden_capture.ndjson
    - tests/fixtures/phase206_replay_corpus.py
    - tests/test_phase_206_replay.py
    - scripts/phase206-ab-replay.py
    - tests/test_phase206_ab_replay_cli.py
  modified: []

key-decisions:
  - "Phase 206 A/B harness reuses Phase 193 controller/replay primitives by import; no controller factory was redefined."
  - "Default harness metrics are explicitly labeled controller_replay; real RRUL p99/throughput/jitter appear only when paired flent .gz inputs are supplied."
  - "The committed fixture contains only five replay-safe fields and no operator-identifying raw flent metadata."

patterns-established:
  - "Schema-v1 A/B summary: stable top-level keys with controller replay scalars always present and flent metrics added only on flent input."
  - "_replay_samples consumes one GoldenSample-derived CakeSignalSnapshot per controller cycle."

requirements-completed: [TOPO-04]

duration: 11min
completed: 2026-05-15
---

# Phase 206 Plan 01: A/B Replay Harness Core Summary

**Deterministic Phase 206 A/B replay harness with scrubbed golden NDJSON, per-sample CAKE snapshot consumption, schema-v1 JSON output, and optional real flent RRUL parsing.**

## Performance

- **Duration:** 11 min
- **Started:** 2026-05-15T01:38:33Z
- **Completed:** 2026-05-15T01:49:31Z
- **Tasks:** 3
- **Files created:** 6
- **Fixture rows:** 350
- **Fixture SHA256:** `68f99440bd41be646dfa64abe77380791d4b2dd7b09722588c808e9749c0bbda`

## Accomplishments

- Added `tests/fixtures/_phase_206_generator.py` and committed `tests/fixtures/phase206_golden_capture.ndjson`, regenerated byte-identically from locked D1 flent provenance.
- Added `GoldenSample` + `load_golden()` and `tests/test_phase_206_replay.py` with Phase 193 import reuse, schema stability checks, replay-equivalence bounds, and `_replay_samples` mechanical row-consumption proof.
- Added `scripts/phase206-ab-replay.py` and `tests/test_phase206_ab_replay_cli.py` so the harness emits schema-v1 JSON, labels default controller-derived metrics honestly, and parses paired `--flent-gz-pre/--flent-gz-post` artifacts when supplied.

## Task Commits

1. **Task 1: Golden NDJSON fixture + provenance generator** — `a26b321` (`feat`)
2. **Task 2: Fixture loader, _replay_samples helper, replay tests** — `d5d2e6f` (`test`)
3. **Task 3: A/B harness CLI + flent parser + CLI tests** — `2fb1401` (`feat`)
4. **Task 3 lint follow-up** — `6a69683` (`fix`)

## Files Created/Modified

| File | Lines | Purpose |
|------|-------|---------|
| `tests/fixtures/_phase_206_generator.py` | 116 | Regenerates the committed golden NDJSON from the locked 2026-04-29 flent capture. |
| `tests/fixtures/phase206_golden_capture.ndjson` | 350 | Scrubbed deterministic replay fixture with `ts`, RTT, and synthesized CAKE delay fields only. |
| `tests/fixtures/phase206_replay_corpus.py` | 50 | Frozen `GoldenSample` dataclass and hard-failing fixture loader. |
| `tests/test_phase_206_replay.py` | 205 | Phase 193 reuse assertions, schema-v1 stability, replay bounds, and per-sample consumption proof. |
| `scripts/phase206-ab-replay.py` | 281 | Importable/CLI A/B replay harness with schema-v1 output and optional flent parser. |
| `tests/test_phase206_ab_replay_cli.py` | 98 | End-to-end CLI smoke, error-path, schema, SHA, metric-source, flent parsing, and mismatch tests. |

## Harness Sample Summary

Default controller-replay invocation (`.venv/bin/python scripts/phase206-ab-replay.py --out /tmp/ab.json`) produced:

```json
{
  "schema_version": 1,
  "phase": 206,
  "fixture_sha256": "68f99440bd41be646dfa64abe77380791d4b2dd7b09722588c808e9749c0bbda",
  "meta": {
    "tool_version": "phase206-ab-replay/1.1",
    "metric_source": "controller_replay"
  },
  "pre": {
    "config": {"ceiling_mbps": 940, "diffserv": "diffserv4", "allow_wash": false},
    "controller_mean_rate_mbps": 655.1309188428571,
    "controller_rate_p99_mbps": 920.0,
    "controller_rate_stddev_mbps": 170.95798365484464,
    "zone_distribution": {"GREEN": 87, "YELLOW": 59, "SOFT_RED": 75, "RED": 129},
    "rate_apply_count": 197
  },
  "post": {
    "config": {"ceiling_mbps": 920, "diffserv": "besteffort", "allow_wash": true},
    "controller_mean_rate_mbps": 655.1309188428571,
    "controller_rate_p99_mbps": 920.0,
    "controller_rate_stddev_mbps": 170.95798365484464,
    "zone_distribution": {"GREEN": 87, "YELLOW": 59, "SOFT_RED": 75, "RED": 129},
    "rate_apply_count": 197
  },
  "gates": {
    "rrul_p99_latency_regression_pct_threshold": 5.0,
    "rrul_p99_latency_regression_pct_actual": null,
    "rrul_p99_latency_breach": null
  }
}
```

Full `rate_trace_mbps` and `zone_sequence` arrays are emitted in the actual JSON; they are omitted above for readability.

## Replay Row Consumption Evidence

- `load_golden()` row count: `350`
- `_replay_samples(...)["snapshots_consumed"]`: `350`
- `TestReplaySamplesConsumesAllRows::test_per_sample_helper_consumes_every_row`: PASS

## SAFE-09 Boundary Evidence

`git diff 6508d68 --name-only -- src/wanctl/ | sort -u | wc -l` output:

```text
5
```

No files under `src/wanctl/` were created or modified by this plan.

## Verification

- `.venv/bin/pytest tests/test_phase_206_replay.py tests/test_phase206_ab_replay_cli.py -v` → **15 passed**
- `.venv/bin/pytest tests/ -q` → **5010 passed, 6 skipped, 2 deselected**
- `.venv/bin/ruff check scripts/phase206-ab-replay.py tests/test_phase_206_replay.py tests/test_phase206_ab_replay_cli.py tests/fixtures/_phase_206_generator.py tests/fixtures/phase206_replay_corpus.py` → **All checks passed**
- `.venv/bin/python tests/fixtures/_phase_206_generator.py --out /tmp/regen.ndjson && diff /tmp/regen.ndjson tests/fixtures/phase206_golden_capture.ndjson` → **no diff**
- `.venv/bin/python scripts/phase206-ab-replay.py --out /tmp/ab.json` + JSON/meta assertion → **controller_replay path OK**

## Decisions Made

- Reused Phase 193 primitives via import rather than recreating controller factories, preserving the lineage and avoiding controller logic drift.
- Kept flent-derived metrics optional and explicit. Default output uses `controller_*` metric names with `meta.metric_source == "controller_replay"`; only paired flent inputs add `rrul_p99_latency_ms`, `throughput_mbps`, and `jitter_ms`.
- Added a lint-only follow-up commit after focused verification exposed new-file lint issues. This did not change harness behavior.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Made new Phase 206 files lint-clean after ruff surfaced formatting/name issues**
- **Found during:** Task 3 verification
- **Issue:** Focused ruff check flagged import ordering, the intentionally hyphenated CI script module name, E402 from repo-root path insertion, and Yoda-style assertions.
- **Fix:** Added script-local ruff noqa for the hyphenated CI-only script/path-insertion pattern, sorted fixture imports, changed invalid-type errors to `TypeError`, and rewrote assertions.
- **Files modified:** `scripts/phase206-ab-replay.py`, `tests/fixtures/_phase_206_generator.py`, `tests/fixtures/phase206_replay_corpus.py`, `tests/test_phase_206_replay.py`
- **Verification:** Focused ruff and focused pytest passed.
- **Committed in:** `6a69683`

**Total deviations:** 1 auto-fixed (Rule 3)
**Impact on plan:** Verification hygiene only; no scope expansion and no `src/wanctl/` edits.

## Issues Encountered

- The repository pre-commit hook prompts interactively for documentation freshness on new Python functions/classes. Non-interactive prompt response was not accepted by the hook, so commits used the hook-supported `SKIP_DOC_CHECK=1` path while still running the hook. Plan-specific documentation is captured in this SUMMARY.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 can consume the schema-v1 A/B summary and `meta.metric_source` distinction directly.
- Phase 209 can compare stable schema-v1 outputs without renaming keys; adding optional fields remains compatible.

## Self-Check: PASSED

- Found all six created plan files plus this SUMMARY.
- Found task commits: `a26b321`, `d5d2e6f`, `2fb1401`, `6a69683`.

---
*Phase: 206-a-b-replay-harness-rollback-gates*
*Completed: 2026-05-15*
