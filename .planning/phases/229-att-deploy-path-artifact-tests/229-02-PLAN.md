---
phase: 229-att-deploy-path-artifact-tests
plan: 02
type: execute
wave: 2
depends_on: ["229-01"]
files_modified:
  - tests/test_att_cake_autorate_artifacts.py
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "Repo tests assert the ATT deploy.sh path contract (flag, function, both cake-autorate units present in deploy.sh)"
    - "Repo tests assert all 6 ATT artifacts exist and carry their ATT-specific values (Conflicts=wanctl@att.service, ingress/ptm/nowash qdisc, full bridge env block, silicom watchdog ExecStart/Requires/IFACE)"
    - "The shared state-bridge script, driven with WAN_NAME=att, writes att-named state, metrics rows, and a health endpoint"
    - "A drift test cross-checks the deploy.sh ATT file list against the known 6 repo ATT artifacts (bidirectional set-equality)"
    - "The ATT tests pass under the default (non-integration) pytest selection"
  artifacts:
    - path: "tests/test_att_cake_autorate_artifacts.py"
      provides: "ATT artifact-contract tests (TEST-01) + deploy-list drift gate (TEST-02)"
      contains: "def test_deploy_att_file_list_matches_repo"
      min_lines: 180
  key_links:
    - from: "tests/test_att_cake_autorate_artifacts.py"
      to: "deploy/scripts/cake-autorate-att-state-bridge"
      via: "subprocess run with WANCTL_EXTERNAL_WAN_NAME=att"
      pattern: "WANCTL_EXTERNAL_WAN_NAME.*att"
    - from: "test_deploy_att_file_list_matches_repo"
      to: "scripts/deploy.sh"
      via: "parse ATT function source paths + systemd array, assert each exists"
      pattern: "deploy.*read_text"
---

<objective>
Create `tests/test_att_cake_autorate_artifacts.py` mirroring `tests/test_spectrum_cake_autorate_artifacts.py` with ATT values substituted, plus a net-new drift gate that validates the `deploy.sh` ATT file list against the repo ATT artifacts. This closes TEST-01 (artifact-contract parity, including the silicom watchdog and ATT qdisc invariants) and TEST-02 (deploy-list ↔ repo bidirectional drift guard).

Purpose: Make the repo a drift-proof source of truth — a careless edit to an ATT artifact value, a missing artifact, or a deploy.sh file-list divergence fails CI.

Output: New `tests/test_att_cake_autorate_artifacts.py` with 6 tests (5 mirroring Spectrum + 1 drift gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-RESEARCH.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md

<interfaces>
<!-- Ground-truth ATT artifact values extracted from live repo files (2026-06-09). Assert these EXACTLY. -->

cake-autorate-att.service:
  Conflicts=wanctl@att.service
  ExecStartPre=/usr/local/sbin/cake-autorate-att-qdisc-init
  ExecStart=/opt/cake-autorate/cake-autorate.sh /etc/cake-autorate/config.att.sh

cake-autorate-att-state-bridge.service (FULL env block — Spectrum has only health host/port):
  Wants=cake-autorate-att.service
  Environment=WANCTL_EXTERNAL_WAN_NAME=att
  Environment=WANCTL_EXTERNAL_DL_IF=att-router
  Environment=WANCTL_EXTERNAL_UL_IF=att-modem
  Environment=WANCTL_EXTERNAL_STATE_PATH=/var/lib/wanctl/att_state.json
  Environment=WANCTL_EXTERNAL_METRICS_DB=/var/lib/wanctl/metrics-att.db
  Environment=WANCTL_EXTERNAL_BASELINE_RTT=28.42043789020452
  Environment=CAKE_AUTORATE_BRIDGE_HEALTH_HOST=10.10.110.227
  ExecStart=/usr/local/sbin/cake-autorate-att-state-bridge

silicom-bypass-watchdog-cake-autorate-att.service (NO Spectrum sibling):
  Requires=bpctl-silicom.service
  Environment=IFACE=att-modem
  Environment=WANCTL_UNIT=cake-autorate-att.service
  ExecStart=/usr/local/sbin/wanctl-bpctl-watchdog-petter
  ExecStop=/usr/local/sbin/wanctl-bpctl-watchdog-bypass

cake-autorate-att-qdisc-init (ATT topology — assert the DOCSIS→VDSL2/PTM asymmetry explicitly):
  dev att-router root cake bandwidth 95000Kbit   (DL uses `ingress`)
  dev att-modem root cake bandwidth 19000Kbit    (UL uses `egress ack-filter`)
  shared invariants on both lines: diffserv4, nowash, rtt 35ms, ptm, overhead 22 mpu 64, memlimit 32Mb
  NOTE: ATT DL uses `ingress`; Spectrum DL uses `egress`. Assert `ingress` explicitly.

config.att.sh:
  dl_if=att-router
  ul_if=att-modem
  adjust_dl_shaper_rate=1
  adjust_ul_shaper_rate=0
  base_dl_shaper_rate_kbps=95000
  base_ul_shaper_rate_kbps=19000
  pinger_method=fping
  ping_extra_args="-S 10.10.110.227"

The 6 known ATT artifacts (drift-gate set, repo-relative):
  configs/cake-autorate/config.att.sh
  deploy/scripts/cake-autorate-att-qdisc-init
  deploy/scripts/cake-autorate-att-state-bridge
  deploy/systemd/cake-autorate-att.service
  deploy/systemd/cake-autorate-att-state-bridge.service
  deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service

Bridge subprocess harness notes (from the Spectrum test, mirror exactly):
- The state-bridge script is byte-identical between Spectrum and ATT (confirmed `diff -q`). Drive ATT behavior purely via env.
- Set explicit WANCTL_EXTERNAL_STATE_PATH to sidestep the shared script's hardcoded `spectrum_state.` tmpfile prefix (Pitfall 4 — do NOT "fix" the shared script).
- Health-endpoint test: free_tcp_port() helper + 5s poll deadline (well under pytest --timeout=30).
- The bridge derives qdisc_bandwidth labels from the LOAD/SUMMARY log line it parses, not from the qdisc-init file. For the ATT health test, write a LOAD line with ATT rates so the asserted qdisc_bandwidth matches what the bridge computes; assert `wan["name"] == "att"`. Determine the exact expected qdisc_bandwidth string by running the bridge against an ATT-rate LOAD line and reading the emitted value — do not hardcode from the Spectrum test's `550Mbit`/`18Mbit`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATT artifact-contract tests (TEST-01) — deploy.sh text, artifact existence + values, watchdog, bridge subprocess trio</name>
  <files>tests/test_att_cake_autorate_artifacts.py</files>
  <read_first>
    - tests/test_spectrum_cake_autorate_artifacts.py (the full 222-line parity template — mirror its 5 test shapes and the path-constant block L13-19, free_tcp_port helper L22-25, subprocess harness L82-99 / L184-191)
    - deploy/systemd/cake-autorate-att.service
    - deploy/systemd/cake-autorate-att-state-bridge.service
    - deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service
    - deploy/scripts/cake-autorate-att-qdisc-init
    - configs/cake-autorate/config.att.sh
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (Pattern 2; Critical Pitfalls 1, 2, 4)
  </read_first>
  <behavior>
    - test_deploy_script_has_external_att_mode: deploy.sh text contains `--with-att-cake-autorate`, `deploy_att_cake_autorate`, `cake-autorate-att.service`, `cake-autorate-att-state-bridge.service`.
    - test_att_cake_autorate_artifacts_are_repo_owned: all 6 ATT artifacts exist; cake-autorate-att.service has Conflicts/ExecStart/ExecStartPre for ATT; bridge unit has the full ATT env block (WAN_NAME=att, DL_IF=att-router, UL_IF=att-modem, BASELINE_RTT=28.42043789020452, HEALTH_HOST=10.10.110.227, ExecStart=...-att-state-bridge); qdisc-init has `dev att-router root cake bandwidth 95000Kbit`, `dev att-modem root cake bandwidth 19000Kbit`, `ingress`, `ptm`, `overhead 22 mpu 64`, `rtt 35ms`, `nowash`; config has dl_if/ul_if/adjust rates/base rates/ping bind; watchdog unit has Requires=bpctl-silicom.service, IFACE=att-modem, WANCTL_UNIT=cake-autorate-att.service, ExecStart=...wanctl-bpctl-watchdog-petter.
    - test_state_bridge_parses_att_summary_and_writes_wanctl_state: bridge oneshot writes att state JSON with expected current_rate / congestion / last_applied.
    - test_state_bridge_writes_att_metrics_database: metrics rows have wan_name == "att".
    - test_state_bridge_serves_att_health_endpoint: health payload wan["name"] == "att" with ATT qdisc_bandwidth labels (value derived from the bridge, not hardcoded from Spectrum).
  </behavior>
  <action>
    Create `tests/test_att_cake_autorate_artifacts.py` by mirroring `tests/test_spectrum_cake_autorate_artifacts.py` exactly, substituting the ATT path constants (STATE_BRIDGE/QDISC_INIT/CAKE_SERVICE/BRIDGE_SERVICE/CAKE_CONFIG → the `-att` variants) and ADDING a `WATCHDOG_SERVICE` constant for `deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service`.

    Mirror the 5 Spectrum tests with ATT values from the `<interfaces>` block above. In the artifacts-repo-owned test: assert all 6 ATT files exist; assert each ATT-specific value listed in `<interfaces>`; explicitly assert `ingress` in the qdisc-init (the ATT-vs-Spectrum egress→ingress asymmetry — Pitfall 2); add the watchdog assertions (Requires/IFACE/WANCTL_UNIT/ExecStart — Pitfall 1).

    For the three bridge subprocess tests: reuse the Spectrum harness but set `WANCTL_EXTERNAL_WAN_NAME=att`, `WANCTL_EXTERNAL_DL_IF=att-router`, `WANCTL_EXTERNAL_UL_IF=att-modem`, and an explicit `WANCTL_EXTERNAL_STATE_PATH` in the tmp_path. Assert `wan_name == "att"` in the metrics test and `wan["name"] == "att"` in the health test. For the health test's `qdisc_bandwidth` assertions, write a LOAD line carrying ATT rates and assert the value the bridge actually emits (run it once during development to read the emitted label; do NOT carry over the Spectrum `550Mbit`/`18Mbit` strings).

    Do NOT edit the shared bridge script or any other repo file. This task only creates the test file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q -k 'not deploy_att_file_list'</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q -k 'not deploy_att_file_list'` reports all selected tests passing (5 tests green).
    - The test file references `silicom-bypass-watchdog-cake-autorate-att.service` and asserts `wanctl-bpctl-watchdog-petter`.
    - The test file asserts the literal string `ingress` against the qdisc-init content.
    - The test file asserts `Environment=WANCTL_EXTERNAL_BASELINE_RTT=28.42043789020452` against the bridge unit.
    - The bridge subprocess tests assert `att` (not `spectrum`) in state/metrics/health outputs.
    - No file other than tests/test_att_cake_autorate_artifacts.py is modified (`git status --porcelain` shows only that new file for this task).
  </acceptance_criteria>
  <done>The 5 mirrored ATT artifact-contract tests pass, asserting ATT-specific qdisc/bridge/watchdog/config values from the real repo files; TEST-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: TEST-02 deploy-list drift gate — bidirectional set-equality of deploy.sh ATT file list vs repo artifacts</name>
  <files>tests/test_att_cake_autorate_artifacts.py</files>
  <read_first>
    - tests/test_att_cake_autorate_artifacts.py (the file created in Task 1 — append the drift test)
    - scripts/deploy.sh (read the deploy_att_cake_autorate() function and the ATT_CAKE_AUTORATE_SYSTEMD array added in Plan 01, so the parser targets the real text)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (Pattern 3 / Test 6; Critical Pitfall 5 — no manifest exists, must parse)
  </read_first>
  <action>
    Add a `test_deploy_att_file_list_matches_repo` test. Per the locked TEST-02 decision: bidirectional set-equality between the deploy.sh ATT file list (parsed from deploy.sh text — no manifest exists) and the repo ATT artifact set.

    Parse `scripts/deploy.sh` text: extract every `$PROJECT_ROOT/...` source path referenced inside the `deploy_att_cake_autorate()` function body AND every entry of the `ATT_CAKE_AUTORATE_SYSTEMD` array. Normalize each to a repo-relative path (strip `$PROJECT_ROOT/`). Build the parsed set. Define the expected set as the known 6 ATT artifacts from the `<interfaces>` block. Assert set-equality in BOTH directions: every parsed path resolves to an existing repo file (deploy references nothing missing), AND every one of the 6 known ATT artifacts appears in the parsed set (no repo ATT artifact is unreferenced by deploy). On failure, the assertion message must name the offending path(s) and the direction of drift.

    Scope the parse to the ATT function only (e.g., slice deploy.sh text from `deploy_att_cake_autorate()` to the next top-level `}` or next function definition) so Spectrum paths do not pollute the ATT set. Use stdlib only (re, pathlib) — no new dependencies.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q -k deploy_att_file_list</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q -k deploy_att_file_list` passes.
    - The parsed set equals the 6-artifact known set (verifiable: the test asserts `parsed == expected` or equivalent bidirectional membership).
    - A deliberate corruption check (executor runs once, not committed): temporarily removing one of the 6 artifacts from the ATT function in a scratch copy makes the test FAIL with a message naming that path — confirming the gate actually catches drift. (Do not commit the corruption.)
    - The test imports only stdlib (no third-party imports added).
    - Full file passes: `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q` is all-green (6 tests).
  </acceptance_criteria>
  <done>TEST-02 satisfied: a bidirectional set-equality drift gate fails if the deploy.sh ATT file list and the repo's 6 ATT artifacts diverge in either direction.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py -q` all-green (6 tests).
- `.venv/bin/pytest tests/test_att_cake_autorate_artifacts.py tests/test_spectrum_cake_autorate_artifacts.py -q` green (ATT parity does not regress Spectrum).
- `.venv/bin/ruff check tests/test_att_cake_autorate_artifacts.py` clean.
- Only tests/test_att_cake_autorate_artifacts.py created; no controller-path or shared-script edits.
</verification>

<success_criteria>
TEST-01 met: ATT artifacts covered at Spectrum parity (units, Conflicts=wanctl@att.service, qdisc-init invariants including the ingress/ptm/nowash ATT asymmetry, bridge env wiring, silicom watchdog variant). TEST-02 met: deploy.sh ATT file list validated against repo artifacts by bidirectional set-equality so the two cannot drift silently.
</success_criteria>

<output>
Create `.planning/phases/229-att-deploy-path-artifact-tests/229-02-SUMMARY.md` when done.
</output>
