---
phase: 229-att-deploy-path-artifact-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.sh
autonomous: true
requirements: [DEPLOY-01]
must_haves:
  truths:
    - "An operator can run `deploy.sh att cake-shaper --with-att-cake-autorate --dry-run` and see the planned ATT cake-autorate actions"
    - "deploy.sh defines a deploy_att_cake_autorate() sibling function mirroring the Spectrum function with ATT values substituted"
    - "deploy.sh deploys all 6 ATT artifacts including the silicom watchdog unit (which has no Spectrum sibling)"
    - "The --with-att-cake-autorate flag is gated to WAN_NAME=att, mirroring the Spectrum gate"
    - "The Spectrum-only trial drop-in cleanup line is NOT replicated in the ATT function"
  artifacts:
    - path: "scripts/deploy.sh"
      provides: "deploy_att_cake_autorate() function + ATT_CAKE_AUTORATE_SYSTEMD array + flag wiring (var init, parse, gate, dispatch, usage, dry-run, print_next_steps)"
      contains: "deploy_att_cake_autorate"
  key_links:
    - from: "scripts/deploy.sh argument parser"
      to: "deploy_att_cake_autorate"
      via: "WITH_ATT_CAKE_AUTORATE dispatch block"
      pattern: "WITH_ATT_CAKE_AUTORATE.*==.*true.*deploy_att_cake_autorate"
    - from: "deploy_att_cake_autorate"
      to: "silicom-bypass-watchdog-cake-autorate-att.service"
      via: "ATT_CAKE_AUTORATE_SYSTEMD array loop"
      pattern: "silicom-bypass-watchdog-cake-autorate-att.service"
---

<objective>
Add a `deploy_att_cake_autorate()` sibling function and `--with-att-cake-autorate` flag wiring to `scripts/deploy.sh` so an operator can deploy the full ATT cake-autorate artifact set (config, qdisc-init, state bridge, both cake-autorate units, AND the silicom watchdog unit) with the same preflight/validation rigor as the existing Spectrum path. This closes DEPLOY-01.

Purpose: The ATT artifacts were hand-deployed during the 2026-06-08 migration; `deploy.sh` only supports `--with-spectrum-cake-autorate`. This makes the ATT deploy reproducible at Spectrum parity.

Output: Modified `scripts/deploy.sh` with a new sibling function, a new systemd array, and 7 flag-wiring sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-RESEARCH.md
@.planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md

<interfaces>
<!-- Ground-truth values extracted from the live repo ATT artifacts (2026-06-09). Use these directly. -->

ATT artifact repo paths (all confirmed present):
- configs/cake-autorate/config.att.sh
- deploy/scripts/cake-autorate-att-qdisc-init
- deploy/scripts/cake-autorate-att-state-bridge   (byte-identical to the Spectrum bridge — env-parameterized)
- deploy/systemd/cake-autorate-att.service
- deploy/systemd/cake-autorate-att-state-bridge.service
- deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service   (NO Spectrum sibling)

Spectrum template structure in scripts/deploy.sh (VERIFIED line numbers, 2026-06-09):
- SPECTRUM_CAKE_AUTORATE_SYSTEMD array: L62-65 (2 units)
- deploy_spectrum_cake_autorate(): L397-445
- WAN-name guard inside function: L400-403
- preflight `test -x /opt/cake-autorate/cake-autorate.sh`: L405-409
- mkdir: L411 ; scp config: L413-415 ; scp qdisc-init: L417-419 ; scp state-bridge: L421-423
- systemd array loop: L425-435 ; trial drop-in cleanup (DO NOT COPY): L437-440 ; daemon-reload: L442
- usage() help block: L99-101
- print_next_steps Spectrum branch: L553-559 (restart), L573-577 (monitor)
- var init: L617 ; parse case: L627-630 ; WAN-name gate: L694-697 ; status echo: L691 ; dry-run block: L710-713 ; dispatch: L741-743
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ATT_CAKE_AUTORATE_SYSTEMD array and deploy_att_cake_autorate() sibling function</name>
  <files>scripts/deploy.sh</files>
  <read_first>
    - scripts/deploy.sh (read L62-65 for the SPECTRUM_CAKE_AUTORATE_SYSTEMD array and L397-445 for the full deploy_spectrum_cake_autorate function before editing)
    - deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service (the third unit ATT carries that Spectrum lacks)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (Pattern 1 substitution table; Critical Pitfalls 1, 3)
  </read_first>
  <action>
    Add a module-level array `ATT_CAKE_AUTORATE_SYSTEMD` immediately after the `SPECTRUM_CAKE_AUTORATE_SYSTEMD` array (after L65) containing exactly three entries: `deploy/systemd/cake-autorate-att.service`, `deploy/systemd/cake-autorate-att-state-bridge.service`, and `deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service`.

    Add a `deploy_att_cake_autorate()` function as a sibling of `deploy_spectrum_cake_autorate()` (place it directly after that function, after L445). Mirror the Spectrum function step-for-step, substituting these exact ATT values: WAN-name guard requires `"$WAN_NAME" == "att"` (error message references `--with-att-cake-autorate ... wan_name is att`); identical preflight `test -x /opt/cake-autorate/cake-autorate.sh`; identical `sudo mkdir -p /etc/cake-autorate /var/log/cake-autorate`; scp `$PROJECT_ROOT/configs/cake-autorate/config.att.sh` to `/etc/cake-autorate/config.att.sh` (chown root:root, chmod 644); scp `$PROJECT_ROOT/deploy/scripts/cake-autorate-att-qdisc-init` to `/usr/local/sbin/cake-autorate-att-qdisc-init` (chmod 755); scp `$PROJECT_ROOT/deploy/scripts/cake-autorate-att-state-bridge` to `/usr/local/sbin/cake-autorate-att-state-bridge` (chmod 755); loop over `${ATT_CAKE_AUTORATE_SYSTEMD[@]}` with the same scp-to-/tmp then `sudo mv` to `$TARGET_SYSTEMD_DIR` plus chown, with the missing-file branch erroring `Missing ATT cake-autorate systemd unit: $file` and `exit 1`; final `sudo systemctl daemon-reload`.

    Per Pitfall 3 and the locked operator decision: do NOT replicate the Spectrum trial drop-in cleanup line (deploy.sh L437-440). Add a one-line shell comment in the ATT function explaining the ATT unit carries ExecStartPre inline so no drop-in cleanup is needed.

    Per the locked watchdog decision: ship the `silicom-bypass-watchdog-cake-autorate-att.service` UNIT (via the array). Add a preflight check before the daemon-reload that the bpctl runtime petter/bypass scripts exist on the target via `ssh "$TARGET_HOST" "test -x /usr/local/sbin/wanctl-bpctl-watchdog-petter && test -x /usr/local/sbin/wanctl-bpctl-watchdog-bypass"`; on failure, `print_warning` (NOT exit) that the silicom watchdog unit was deployed but its bpctl runtime petter/bypass scripts are absent and the watchdog will fail on enable until they are installed. Do NOT deploy the bpctl tooling chain itself.
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && grep -q 'deploy_att_cake_autorate()' scripts/deploy.sh && grep -q 'silicom-bypass-watchdog-cake-autorate-att.service' scripts/deploy.sh && grep -q 'wanctl-bpctl-watchdog-petter' scripts/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/deploy.sh` exits 0 (no syntax errors).
    - `grep -c 'ATT_CAKE_AUTORATE_SYSTEMD' scripts/deploy.sh` returns ≥ 2 (array definition + loop reference).
    - The three ATT systemd unit paths `deploy/systemd/cake-autorate-att.service`, `deploy/systemd/cake-autorate-att-state-bridge.service`, `deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service` all appear in scripts/deploy.sh.
    - `deploy_att_cake_autorate()` function contains the string `config.att.sh`, `cake-autorate-att-qdisc-init`, and `cake-autorate-att-state-bridge`.
    - The deploy.sh ATT function does NOT contain `cake-autorate-spectrum.service.d` (Spectrum trial drop-in path not copied — verify with `grep -A60 'deploy_att_cake_autorate()' scripts/deploy.sh | grep -v -c 'cake-autorate-spectrum.service.d'` style check; the spectrum drop-in path must not appear inside the ATT function body).
    - A preflight `test -x /usr/local/sbin/wanctl-bpctl-watchdog-petter` check string is present in the ATT function.
  </acceptance_criteria>
  <done>deploy_att_cake_autorate() exists as a sibling function, ships all 6 ATT artifacts (3 files + 3 units), preflights bpctl runtime with a warning (not exit), omits the Spectrum trial drop-in cleanup, and bash -n is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the --with-att-cake-autorate flag through all 7 sites</name>
  <files>scripts/deploy.sh</files>
  <read_first>
    - scripts/deploy.sh (read L99-101 usage block, L553-582 print_next_steps Spectrum branch, L617 var init, L627-630 parse case, L691 status echo, L694-697 WAN-name gate, L710-713 dry-run block, L741-743 dispatch — all sites that mirror the Spectrum flag)
    - .planning/phases/229-att-deploy-path-artifact-tests/229-PATTERNS.md (Flag wiring — 6 mirror sites; print_next_steps ATT branch details)
  </read_first>
  <action>
    Mirror the `WITH_SPECTRUM_CAKE_AUTORATE` wiring for ATT at these exact sites:

    1. Var init (after L617): add `WITH_ATT_CAKE_AUTORATE=false`.
    2. Parse case (after the L627-630 spectrum case): add `--with-att-cake-autorate) WITH_ATT_CAKE_AUTORATE=true; shift ;;`.
    3. WAN-name gate (after L694-697): add `if [[ "$WITH_ATT_CAKE_AUTORATE" == "true" && "$WAN_NAME" != "att" ]]; then print_error "--with-att-cake-autorate is only valid with WAN name 'att'"; exit 1; fi`.
    4. Status echo (near L691): add `echo "ATT cake-autorate: $([[ "$WITH_ATT_CAKE_AUTORATE" == "true" ]] && echo "Yes" || echo "No")"`.
    5. Dry-run block (after L710-713): add an `if [[ "$WITH_ATT_CAKE_AUTORATE" == "true" ]]; then` branch printing the planned ATT actions (deploy ATT cake-autorate config, qdisc init, state bridge, both cake-autorate units, silicom watchdog unit; keep wanctl@att.service disabled while cake-autorate owns ATT rates).
    6. Dispatch (after L741-743): add `if [[ "$WITH_ATT_CAKE_AUTORATE" == "true" ]]; then deploy_att_cake_autorate; fi`.
    7. usage() (after L99-101): add a `--with-att-cake-autorate` help block (valid only with wan_name=att; conflicts with wanctl@att).

    Also mirror the `print_next_steps()` Spectrum branch (L553-559 restart, L573-577 monitor) by adding an ATT branch keyed on `WITH_ATT_CAKE_AUTORATE`: disable `wanctl@att.service`, enable `cake-autorate-att.service cake-autorate-att-state-bridge.service`, rollback comment, monitor commands referencing log `/var/log/cake-autorate/cake-autorate.att.log` and state `/var/lib/wanctl/att_state.json`. Use the same conditional structure already present (extend the existing if/else, or add a parallel ATT conditional branch) — do NOT collapse Spectrum and ATT into a generic `$wan` form (out of scope per binding constraints).
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh && grep -c -- '--with-att-cake-autorate' scripts/deploy.sh && grep -q 'WITH_ATT_CAKE_AUTORATE=false' scripts/deploy.sh && bash scripts/deploy.sh att cake-shaper --with-att-cake-autorate --dry-run | grep -qi 'ATT cake-autorate'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/deploy.sh` exits 0.
    - `grep -c -- '--with-att-cake-autorate' scripts/deploy.sh` returns ≥ 3 (usage, parse case, dry-run/help references).
    - `grep -q 'WITH_ATT_CAKE_AUTORATE=false' scripts/deploy.sh` succeeds (var init present).
    - `bash scripts/deploy.sh att cake-shaper --with-att-cake-autorate --dry-run` exits 0 and prints a line matching `ATT cake-autorate` and does NOT contact any host (dry-run short-circuits before check_prerequisites).
    - `bash scripts/deploy.sh spectrum cake-shaper --with-att-cake-autorate --dry-run` exits non-zero with the message `--with-att-cake-autorate is only valid with WAN name 'att'` (gate fires before the dry-run print, mirroring Spectrum gate ordering).
    - The dispatch block contains `WITH_ATT_CAKE_AUTORATE` and `deploy_att_cake_autorate` on the same conditional.
  </acceptance_criteria>
  <done>All 7 flag-wiring sites mirror the Spectrum flag, the WAN-name gate rejects non-att, the dry-run prints planned ATT actions without host contact, and print_next_steps has an ATT branch.</done>
</task>

</tasks>

<verification>
- `bash -n scripts/deploy.sh` clean.
- `bash scripts/deploy.sh att cake-shaper --with-att-cake-autorate --dry-run` exits 0, prints planned ATT actions, no host contact.
- `bash scripts/deploy.sh spectrum cake-shaper --with-att-cake-autorate --dry-run` exits non-zero with WAN-name gate error.
- No edits to any file other than scripts/deploy.sh (SAFE-14 controller-path untouched; verified at phase boundary in Plan 03).
</verification>

<success_criteria>
DEPLOY-01 met: an operator can deploy the full ATT cake-autorate artifact set (3 files + 3 units including the silicom watchdog) via `deploy.sh --with-att-cake-autorate` with Spectrum-parity preflight/validation rigor, gated to WAN_NAME=att, without replicating the Spectrum-only trial drop-in cleanup.
</success_criteria>

<output>
Create `.planning/phases/229-att-deploy-path-artifact-tests/229-01-SUMMARY.md` when done.
</output>
