---
phase: 148-test-robustness-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_metrics.py
  - tests/test_check_cake.py
  - tests/test_router_behavioral.py
  - tests/test_fusion_healer.py
  - tests/test_autorate_entry_points.py
  - tests/test_autorate_continuous.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "No test file patches more than 3 cross-module private implementation details"
    - "All cross-module private patches are retargeted to Phase 147 public APIs or promoted public methods"
    - "All existing test behaviors are preserved -- same assertions, different mock setup"
    - "All existing tests pass after retargeting (zero regression)"
  artifacts:
    - path: "tests/test_metrics.py"
      provides: "5 patches retargeted from controller._check_connectivity_alerts to public API"
    - path: "tests/test_check_cake.py"
      provides: "8 patches on check_cake_fix._save_snapshot/_confirm_apply/_show_diff_table retargeted"
    - path: "tests/test_check_cake.py"
      provides: "5 patch.object calls on client._request retargeted"
    - path: "tests/test_router_behavioral.py"
      provides: "1 patch on router_client._create_transport_with_password retargeted"
    - path: "tests/test_fusion_healer.py"
      provides: "1 patch on signal_utils._reload_event retargeted"
    - path: "tests/test_autorate_entry_points.py"
      provides: "1 patch on autorate_continuous._start_irtt_thread retargeted"
    - path: "tests/test_autorate_continuous.py"
      provides: "1 patch on ssh._ensure_connected/_client retargeted"
  key_links:
    - from: "tests/test_metrics.py"
      to: "src/wanctl/wan_controller.py"
      via: "get_health_data() instead of _check_connectivity_alerts"
      pattern: "get_health_data"
    - from: "tests/test_check_cake.py"
      to: "src/wanctl/check_cake_fix.py"
      via: "Public API methods instead of _save_snapshot/_confirm_apply"
      pattern: "save_snapshot|confirm_apply"
---

<objective>
Retarget all 22 cross-module private patches across 6 test files to use Phase 147 public APIs or promote private functions to public.

Purpose: Satisfy TEST-02 (brittle mocks replaced) and SC1 (no test patches more than 3 internal implementation details). After this plan, `check_test_brittleness.py` should report 0 cross-module private patches.
Output: 6 modified test files with all cross-module private patches eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-test-robustness-performance/148-CONTEXT.md
@.planning/phases/148-test-robustness-performance/148-RESEARCH.md

<interfaces>
<!-- Phase 147 public APIs to mock against. These replace cross-module private patches. -->

From src/wanctl/wan_controller.py:
```python
def get_health_data(self) -> dict[str, Any]:
    """Public facade for health endpoint data (Phase 147).
    Returns dict with keys: cycle_budget, signal_result, connectivity,
    fusion_config, baseline_rtt_info, pending_rate, congestion_zones, etc.
    """

def get_current_params(self) -> dict[str, float]:
    """Public accessor for all tunable parameters."""

def reload(self) -> None:
    """Public SIGUSR1 reload entry point."""

def shutdown_threads(self) -> None:
    """Public thread lifecycle shutdown."""
```

From src/wanctl/steering/daemon.py:
```python
def get_health_data(self) -> dict[str, Any]:
    """Public facade for steering health endpoint data (Phase 147)."""

def is_wan_grace_period_active(self) -> bool:
    """Public accessor for WAN grace period state."""

def get_effective_wan_zone(self) -> str | None:
    """Public accessor for effective WAN zone."""
```

From src/wanctl/alert_engine.py:
```python
def get_rule_param(self, rule_key: str, param: str, default: Any = None) -> Any:
    """Public accessor for alert rule parameters."""
```

From src/wanctl/steering/cake_stats.py:
```python
@property
def is_linux_cake(self) -> bool:
    """Public property for linux-cake backend detection."""
```

<!-- Cross-module patches inventory (from RESEARCH.md, 22 total): -->
<!-- test_check_cake.py: 8 string patches on check_cake_fix._ + 5 patch.object on client._request -->
<!-- test_metrics.py: 5 patch.object on controller._check_connectivity_alerts -->
<!-- test_router_behavioral.py: 1 string patch on router_client._create_transport_with_password -->
<!-- test_fusion_healer.py: 1 string patch on signal_utils._reload_event -->
<!-- test_autorate_entry_points.py: 1 string patch on autorate_continuous._start_irtt_thread -->
<!-- test_autorate_continuous.py: 1 patch.object on ssh._ensure_connected + ssh._client -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget test_metrics.py and test_check_cake.py cross-module patches (18 patches)</name>
  <files>tests/test_metrics.py, tests/test_check_cake.py, src/wanctl/check_cake_fix.py</files>
  <read_first>
    - tests/test_metrics.py (full file — understand the 5 _check_connectivity_alerts patch contexts)
    - tests/test_check_cake.py (lines 800-950 and 2300-2660 — the cross-module patch areas)
    - src/wanctl/health_check.py (lines 195-270 — _build_wan_status to understand what _check_connectivity_alerts was suppressing)
    - src/wanctl/wan_controller.py (lines 2469-2560 — get_health_data() return dict structure)
    - src/wanctl/check_cake_fix.py (full file — understand _save_snapshot, _confirm_apply, _show_diff_table signatures and whether they can be promoted to public)
    - src/wanctl/check_cake.py (lines 800-850 — _create_audit_client which is same-module, just for context)
    - src/wanctl/backends/routeros_rest.py (first 50 lines — understand the client._request method that test_check_cake.py patches)
  </read_first>
  <action>
**Part A: Retarget test_metrics.py (5 cross-module patches)**

The 5 patches in test_metrics.py (lines 1182, 1209, 1280, 1325, 1368) all do:
```python
patch.object(controller, "_check_connectivity_alerts")
```
This suppresses connectivity alert side-effects during metrics recording tests.

**Retarget strategy:** Instead of patching the private method, mock the controller's `get_health_data()` return value to provide pre-built health data, OR mock the alert_engine dependency that _check_connectivity_alerts calls. The simplest approach: since these tests are about metrics recording (not connectivity alerts), ensure the mock controller simply doesn't trigger connectivity alerts by setting appropriate state on the mock (e.g., `controller.connectivity_state = MagicMock()` with `is_reachable=True`).

Read the 5 test methods containing these patches. For each:
1. Understand what behavior the test is verifying (metrics recording)
2. Determine why _check_connectivity_alerts was patched (prevent side-effects)
3. Replace with either:
   - Setting `controller.rtt_measurement.ping_hosts_concurrent.return_value` to return healthy results (so _check_connectivity_alerts finds no issues and doesn't fire)
   - OR mocking `controller.alert_engine` to be a MagicMock that silently absorbs calls
4. Verify the same assertions still hold

**Part B: Retarget test_check_cake.py (13 cross-module patches)**

**B1: check_cake_fix._ patches (8 string-based patches, lines 2315-2434)**

These patch `wanctl.check_cake_fix._save_snapshot`, `wanctl.check_cake_fix._confirm_apply`, and `wanctl.check_cake_fix._show_diff_table`. Per D-10 these ARE cross-module (test_check_cake.py tests check_cake module, but patches check_cake_fix module).

**Retarget strategy:** Promote the 3 functions to public API in `src/wanctl/check_cake_fix.py`:
- `_save_snapshot` -> `save_snapshot`
- `_confirm_apply` -> `confirm_apply`  
- `_show_diff_table` -> `show_diff_table`

These are called from check_cake.py already (which is a different module), so they're already de-facto public API. Promoting them formalizes this.

Steps:
1. In `src/wanctl/check_cake_fix.py`, rename the 3 functions by removing the `_` prefix
2. Update all callers in `src/wanctl/check_cake.py` (import references and call sites)
3. Update all references in `tests/test_check_cake.py` patch strings to use the public names
4. After this change, the patches become same-module-equivalent (patching public API of the module being tested)

Wait — actually after promoting, the test still patches `check_cake_fix.save_snapshot` which is cross-module from test_check_cake.py's perspective. But it's no longer a PRIVATE cross-module patch (no `_` prefix). The brittleness script only counts `_`-prefixed patches that are cross-module. So promoting to public eliminates these from the brittleness count.

**B2: client._request patch.object (5 calls, lines 998-1719)**

These do `patch.object(client, "_request", ...)` where `client` is a RouterOS REST client instance. The test file is test_check_cake.py but _request belongs to the REST backend module.

**Retarget strategy:** Instead of patching the private `_request` method, mock the `requests.Session` that the REST client uses internally, OR create the REST client with a mock session. Read the RouterOSREST client constructor to understand how to inject a mock session.

Alternative: If RouterOSREST has a public method like `get()` or `set_limits()` that wraps `_request`, mock at that level instead. Read `src/wanctl/backends/routeros_rest.py` to find the public API.

If the simplest approach is to mock `requests.Session.request` or `requests.Session.get`/`requests.Session.patch` at the module level: `patch("wanctl.backends.routeros_rest.requests.Session")`, this replaces the private _request mock with a standard library mock.

Choose the approach that requires the fewest test changes while eliminating the cross-module private patch.
  </action>
  <verify>
    <automated>
      .venv/bin/pytest tests/test_metrics.py tests/test_check_cake.py -x -q --timeout=5 -n 0 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/test_metrics.py contains zero occurrences of `_check_connectivity_alerts`
    - tests/test_check_cake.py contains zero occurrences of `check_cake_fix._save_snapshot` (without quotes — in patch strings the `_` prefix is removed)
    - tests/test_check_cake.py contains zero occurrences of `_confirm_apply` in patch() string arguments
    - tests/test_check_cake.py contains zero occurrences of `"_request"` in patch.object() calls
    - `pytest tests/test_metrics.py -q` exits 0 with all tests passing
    - `pytest tests/test_check_cake.py -q` exits 0 with all tests passing
    - If check_cake_fix.py functions were promoted: `grep "def save_snapshot" src/wanctl/check_cake_fix.py` finds the function
  </acceptance_criteria>
  <done>18 cross-module private patches retargeted in test_metrics.py and test_check_cake.py — all tests pass with public API mocking</done>
</task>

<task type="auto">
  <name>Task 2: Retarget remaining 4 cross-module patches across 4 test files</name>
  <files>tests/test_router_behavioral.py, tests/test_fusion_healer.py, tests/test_autorate_entry_points.py, tests/test_autorate_continuous.py</files>
  <read_first>
    - tests/test_router_behavioral.py (lines 310-340 — the _create_transport_with_password patch context)
    - tests/test_fusion_healer.py (lines 1530-1545 — the signal_utils._reload_event patch context)
    - tests/test_autorate_entry_points.py (lines 35-45 — the _start_irtt_thread patch context)
    - tests/test_autorate_continuous.py (lines 665-690 — the ssh._ensure_connected/_client patch context)
    - src/wanctl/router_client.py (first 60 lines — understand _create_transport_with_password and find public alternative)
    - src/wanctl/signal_utils.py (full file — understand _reload_event and how to mock it via public interface)
    - src/wanctl/autorate_continuous.py (search for _start_irtt_thread — understand its role and find public replacement)
    - src/wanctl/backends/routeros_ssh.py (first 80 lines — understand _ensure_connected and _client)
  </read_first>
  <action>
**Patch 1: test_router_behavioral.py line 316 — `router_client._create_transport_with_password`**

This patches a cross-module private function (test_router_behavioral.py tests behavioral scenarios, but patches router_client module internals).

Retarget approach: `_create_transport_with_password` creates a Paramiko Transport. Instead of patching this private function, mock at the transport/connection level. Options:
- Mock `paramiko.Transport` at the module level: `patch("wanctl.router_client.paramiko.Transport")`
- Or if the test only needs to verify behavioral patterns (not transport creation), mock the public `get_router_client_with_failover()` factory function return value

Read the test to understand what it's actually testing. If it's testing failover behavior, mocking the public router interface (`router.set_limits()`, `router.get_queue_stats()`) is more appropriate than mocking transport creation.

**Patch 2: test_fusion_healer.py line 1535 — `signal_utils._reload_event`**

This patches a cross-module private module-level Event object. The test needs to verify that fusion healer triggers a reload signal.

Retarget approach: Read `src/wanctl/signal_utils.py` to find if there's a public accessor for the reload event, or a public function like `trigger_reload()` or `is_reload_requested()`. If there's a public `set_reload_event()` or similar, mock that. If not, the test can be rewritten to verify the observable effect of the reload (e.g., verify that the controller's `reload()` method gets called) rather than checking the private event directly.

**Patch 3: test_autorate_entry_points.py line 40 — `autorate_continuous._start_irtt_thread`**

This suppresses IRTT thread startup during entry point tests.

Retarget approach: Instead of patching the private `_start_irtt_thread`, mock the IRTT thread at a higher level. Options:
- If WANController has a public `shutdown_threads()` (it does — Phase 147), mock that side of the lifecycle
- Or mock the `BackgroundRTTThread` constructor: `patch("wanctl.autorate_continuous.BackgroundRTTThread")` — this patches a public class, not a private function
- Or if `_start_irtt_thread` is only called from `__init__`, mock the entire thread setup by passing a config with `irtt_enabled=False`

**Patch 4: test_autorate_continuous.py line 671 — `ssh._ensure_connected` and `ssh._client`**

This tests SSH command execution behavior by patching private connection methods.

Retarget approach: Instead of patching `_ensure_connected` and `_client`:
- Mock the SSH backend at a higher level: create a MagicMock SSH backend and inject it into the controller
- Or use `patch.object(ssh, "run_command", ...)` which is a PUBLIC method on RouterOSSSH
- Or if the test needs the SSH client to "appear connected", mock `paramiko.SSHClient` at the module level

Read each test carefully to understand the minimal mock surface needed, then replace private patches with public API mocks.
  </action>
  <verify>
    <automated>
      .venv/bin/pytest tests/test_router_behavioral.py tests/test_fusion_healer.py tests/test_autorate_entry_points.py tests/test_autorate_continuous.py -x -q --timeout=5 -n 0 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/test_router_behavioral.py contains zero occurrences of `_create_transport_with_password`
    - tests/test_fusion_healer.py contains zero occurrences of `signal_utils._reload_event`
    - tests/test_autorate_entry_points.py contains zero occurrences of `_start_irtt_thread`
    - tests/test_autorate_continuous.py contains zero occurrences of `ssh._ensure_connected` and `ssh._client`
    - `pytest tests/test_router_behavioral.py -q` exits 0
    - `pytest tests/test_fusion_healer.py -q` exits 0
    - `pytest tests/test_autorate_entry_points.py -q` exits 0
    - `pytest tests/test_autorate_continuous.py -q` exits 0
  </acceptance_criteria>
  <done>All 4 remaining cross-module private patches retargeted to public APIs — all 4 test files pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mock setup -> production code paths | Changing mock targets must not alter which production code paths are exercised |
| public API promotion (check_cake_fix) | Renaming _private to public must not change runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-05 | T (Tampering) | check_cake_fix function promotion | mitigate | Verify all callers updated: `grep -rn "save_snapshot\|confirm_apply\|show_diff_table" src/` must show only public names |
| T-148-06 | R (Repudiation) | Test behavioral coverage loss | mitigate | Run full test suite after each task; same test count, same pass rate. Assertions preserved — only mock setup changes |
| T-148-07 | T (Tampering) | Mock retargeting silently skips code paths | mitigate | For each retarget, verify the test still exercises the same production method by checking that the test's import and assertion targets are unchanged |
</threat_model>

<verification>
1. `pytest tests/test_metrics.py tests/test_check_cake.py tests/test_router_behavioral.py tests/test_fusion_healer.py tests/test_autorate_entry_points.py tests/test_autorate_continuous.py -q` -- 0 failures
2. `pytest tests/ --co -q 2>&1 | tail -1` -- still collects 4195+ tests
3. `grep -rn "_check_connectivity_alerts" tests/` -- 0 matches
</verification>

<success_criteria>
- All 22 cross-module private patches retargeted to public API mocking across 6 test files
- All retargeted tests pass with public API mocking
- Same test count and behavioral assertions preserved
</success_criteria>

<output>
After completion, create `.planning/phases/148-test-robustness-performance/148-02-SUMMARY.md`
</output>
