---
phase: 219
plan: 03
type: execute
wave: 2
depends_on: [219-02]
files_modified:
  - src/wanctl/operator_summary.py
  - tests/test_history_ingestion_rate_bucketed.py
autonomous: true
requirements: [INGEST-04, SAFE-11]
tags: [ingestion-rate, operator-summary, digest, tolerance]

must_haves:
  truths:
    - "wanctl-operator-summary --digest emits one compact ingestion-rate line per WAN after the existing hard-red block"
    - "Each line carries wan_name, total_rps (sum of per-metric rows_per_sec), and dominant-table per CONTEXT.md tie-break (>=1.20x lead OR mixed:t1/t2 OR n/a)"
    - "Per-WAN ingestion sub-call failures emit `total_rps=n/a top=n/a` lines and increment read_skipped; never abort the summary"
    - "The new digest block is sourced from per_wan_ingestion_rate_bucketed() in-process (no subprocess)"
  artifacts:
    - path: src/wanctl/operator_summary.py
      provides: "_format_ingestion_digest_line(wan_name, rows) with 1.20x tie-break + alphabetical mixed: ordering"
      contains: "def _format_ingestion_digest_line("
    - path: src/wanctl/operator_summary.py
      provides: "print_digest() extended with the ingestion-rate block after the hard-red loop"
      contains: "_DIGEST_INGESTION_PREFIX"
    - path: tests/test_history_ingestion_rate_bucketed.py
      provides: "TestOperatorSummaryDigest class covering the tie-break branches and per-WAN failure tolerance"
      contains: "class TestOperatorSummaryDigest"
  key_links:
    - from: src/wanctl/operator_summary.py::print_digest
      to: src/wanctl/history.py::per_wan_ingestion_rate_bucketed
      via: "in-process import (both processes are out-of-band CLIs; no daemon involvement)"
      pattern: "from wanctl.history import per_wan_ingestion_rate_bucketed"
    - from: src/wanctl/operator_summary.py::print_digest
      to: src/wanctl/operator_summary.py::_format_ingestion_digest_line
      via: "called once per WAN after hard-red emit"
      pattern: "_format_ingestion_digest_line\\("
---

<objective>
Integrate the Phase 219 ingestion block into `wanctl-operator-summary --digest`. The block renders one compact
line per WAN summarizing total rows/sec and the dominant-metric_name table for a 1-hour rolling window. This
satisfies INGEST-04 and resolves the folded todo `2026-04-17-ingestion-rate-tool` (the smoothed tail-rate
window use case is covered by the 3600s window from `per_wan_ingestion_rate_bucketed`).

Purpose: One-glance per-WAN ingestion visibility in the operator digest — the surface Phase 218 audits will use
if they fire during v1.47. Tolerance semantics mirror the existing hard-red block (skip failed WAN, increment
read_skipped, never abort).

Output: `print_digest` emits a second block of lines prefixed
`operator-summary digest: ingestion-rate wan=X total_rps=Y top=Z`. Tie-break and `n/a` semantics match
CONTEXT.md exactly. Test coverage for all four tie-break branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md
@src/wanctl/operator_summary.py
@src/wanctl/history.py
@CLAUDE.md

<interfaces>
<!-- Extracted from operator_summary.py + RESEARCH.md §B + §Step 6 -->

Existing module-level constant (operator_summary.py:20):
  _DIGEST_SKIP_PREFIX = "operator-summary digest:"   # reused as-is; new constant joins it

Existing helper (operator_summary.py:119):
  def _format_digest_line(...) -> str   # the hard-red line formatter; new ingestion formatter sits beside it

Existing handler (operator_summary.py:152-197):
  def print_digest(db_paths) -> dict[str, int]
  # returns counts: {"readable": int, "printed": int, "read_skipped": int, "write_skipped": int}
  # Per-WAN try/except (sqlite3.OperationalError, OSError) at :169-176 — same shape reused for new block

NEW import added by this plan:
  from wanctl.history import per_wan_ingestion_rate_bucketed
(per RESEARCH §"Architectural Responsibility Map" — both processes are out-of-band CLIs; in-process import is
explicitly correct, no subprocess overhead)

Tie-break rule (CONTEXT.md §Dominant-table tie-break):
- top1 rows_per_sec >= 1.20 * top2 rows_per_sec -> render top1.table_name
- Only one non-zero table -> render that table_name
- All rows null/zero -> render `n/a`
- Otherwise -> render `mixed: <alpha1>/<alpha2>` (the two top names, sorted alphabetically inside the `mixed:` payload)

Line format:
  operator-summary digest: ingestion-rate wan=<NAME> total_rps=<F.2f or "n/a"> top=<NAME or mixed: A/B or n/a>

Time window for digest call: last 3600s (one hour) — matches the longest CONTEXT-locked rolling window and the
operator-summary's existing 1-hour digest implicit horizon.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _format_ingestion_digest_line + extend print_digest</name>
  <read_first>
    - src/wanctl/operator_summary.py (entire file; locate `_DIGEST_SKIP_PREFIX` at :20, `_format_digest_line` at :119, `print_digest` at :152-197; the skip-on-OSError pattern at :169-176 is the template for new tolerance)
    - src/wanctl/history.py (the public `per_wan_ingestion_rate_bucketed` from plan 02; confirm signature)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Step 6: Operator-summary integration" (lines 421-476; the in-process call + tie-break reference)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Dominant-table tie-break" (the four-branch decision tree)
  </read_first>
  <behavior>
    - _format_ingestion_digest_line returns a single string per WAN; no I/O; pure function for fixture testability
    - When all rows have rows_per_sec is None OR sum is 0: return line with total_rps=n/a top=n/a
    - When exactly one non-zero table: return that table_name as top
    - When top1.rows_per_sec >= 1.20 * top2.rows_per_sec: return top1.table_name
    - Otherwise: return `mixed: <alpha_sorted_two_names>`
    - print_digest's new block runs AFTER the existing hard-red loop, before the function returns
    - Per-WAN exception during ingestion fetch -> log _DIGEST_SKIP_PREFIX (ingestion) wan=X ... to stderr, increment counts["read_skipped"], continue to next WAN (NEVER abort, NEVER raise)
    - print_digest return dict shape is UNCHANGED ({"readable","printed","read_skipped","write_skipped"}); ingestion failures fold into existing read_skipped/write_skipped per RESEARCH §B last row
  </behavior>
  <action>
    Add module constant near `_DIGEST_SKIP_PREFIX` (operator_summary.py:20):
    `_DIGEST_INGESTION_PREFIX = "operator-summary digest: ingestion-rate"`

    Add NEW pure function `_format_ingestion_digest_line(wan_name: str, rows: list[dict]) -> str` directly after
    `_format_digest_line` at :119. Implement the 4-branch tie-break exactly per CONTEXT.md (RESEARCH §Step 6 has
    the reference). Use `sorted([n1, n2])` for the `mixed:` payload (alphabetical within mixed per CONTEXT
    Claude's Discretion). Format spec: `total_rps={total_rps:.2f}` when numeric, `n/a` when sum is zero or all
    rows null. The function MUST NOT call I/O, MUST NOT call time.time, MUST NOT raise on empty rows list (returns
    an n/a line).

    Extend `print_digest(db_paths)` (operator_summary.py:152-197). After the existing hard-red loop completes
    (find the loop terminator), add a SECOND for-loop over db_paths:

      1. Compute wan_name via the EXISTING `_wan_name_from_db_path(db_path)` helper at `src/wanctl/operator_summary.py:113-116` (the hard-red loop already uses it at :165). DO NOT introduce a second name-derivation pattern. The helper handles both `metrics-<name>.db` (returns `<name>` via `db_path.stem.removeprefix("metrics-")`) and unprefixed paths (returns `db_path.stem`).
      2. Inside try/except (sqlite3.OperationalError, sqlite3.DatabaseError, OSError):
         - `rows, _failures = per_wan_ingestion_rate_bucketed([db_path], start_ts=int(time.time()) - 3600, end_ts=int(time.time()), wan=None)`
         - `line = _format_ingestion_digest_line(wan_name, rows)`
         - `print(line)` — if OSError on print -> log skip-write line to stderr, counts["write_skipped"] += 1, continue
         - else counts["printed"] += 1
      3. On exception inside the fetch: `print(f"{_DIGEST_SKIP_PREFIX} (ingestion) wan={wan_name} db={db_path}: {exc}", file=sys.stderr)`, `counts["read_skipped"] += 1`, continue

    Add `import time` to operator_summary.py if not already present. Add `from wanctl.history import per_wan_ingestion_rate_bucketed` at module top (matches the existing pattern of importing from `wanctl.storage.db_utils` per RESEARCH §"In-process import").

    DO NOT modify `_format_digest_line`. DO NOT modify the hard-red loop. DO NOT change print_digest's return contract.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from wanctl.operator_summary import _format_ingestion_digest_line; print(_format_ingestion_digest_line('spectrum', [{'table_name':'wanctl_rtt_ms','rows_per_sec':10.0},{'table_name':'wanctl_state','rows_per_sec':1.0}])); print(_format_ingestion_digest_line('att', [{'table_name':'wanctl_rtt_ms','rows_per_sec':5.0},{'table_name':'wanctl_state','rows_per_sec':4.5}])); print(_format_ingestion_digest_line('void', [{'table_name':'x','rows_per_sec':None}])); print(_format_ingestion_digest_line('empty', []))"</automated>
    <automated>.venv/bin/python -c "import sys, wanctl.operator_summary as os_mod; os_mod.per_wan_ingestion_rate_bucketed = lambda db_paths, *, start_ts, end_ts, wan: ([{'table_name':'wanctl_rtt_ms','rows_per_sec':10.0,'wan_name':'spectrum','wan_db':str(db_paths[0]),'window_seconds':3600,'row_count':36000,'_snapshot_unix':1767225600,'_snapshot_age_sec':0}], 0); from pathlib import Path; counts = os_mod.print_digest([Path('/tmp/metrics-spectrum.db')]); assert set(counts.keys()) == {'readable','printed','read_skipped','write_skipped'}; print('WIRING_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^def _format_ingestion_digest_line" src/wanctl/operator_summary.py` returns one line
    - `grep -c "_DIGEST_INGESTION_PREFIX" src/wanctl/operator_summary.py` returns at least 1
    - `grep -c "from wanctl.history import per_wan_ingestion_rate_bucketed" src/wanctl/operator_summary.py` returns 1
    - wan_name derivation reuses the existing helper: `grep -c "_wan_name_from_db_path(db_path)" src/wanctl/operator_summary.py` returns at least 2 (one for the hard-red loop at :165, one for the new ingestion loop). No second derivation pattern introduced: `grep -cE "db_path\.stem\.removeprefix" src/wanctl/operator_summary.py` returns 1 (the original at :115 — the new loop calls the helper, it does NOT inline the stem removal).
    - The inline python smoke command above prints 4 lines: line 1 contains `top=wanctl_rtt_ms` (10/1 = 10x > 1.20x tie-break clear winner), line 2 contains `mixed: wanctl_rtt_ms/wanctl_state` (5/4.5 = 1.11x < 1.20x, alphabetical), line 3 contains `total_rps=n/a top=n/a`, line 4 contains `total_rps=n/a top=n/a`
    - `.venv/bin/ruff check src/wanctl/operator_summary.py` exits 0
    - `.venv/bin/mypy src/wanctl/operator_summary.py` exits 0 (or matches existing baseline)
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0 (only operator_summary.py changed within src/wanctl/)
  </acceptance_criteria>
  <done>Pure tie-break formatter compiles + 4 branches verifiable from CLI; print_digest emits the second block per WAN; mutation-boundary green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_history_ingestion_rate_bucketed.py with TestOperatorSummaryDigest class</name>
  <read_first>
    - tests/test_history_ingestion_rate_bucketed.py (the file from plan 01 + plan 02; add a NEW class beside TestIngestionRateBucketed)
    - src/wanctl/operator_summary.py (the just-shipped print_digest extension)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Dominant-table tie-break" (the four-branch table that test methods enumerate)
  </read_first>
  <behavior>
    - Six unit tests covering: clear winner (>=1.20x), tie (<1.20x -> mixed:), single non-zero table, all-null/zero, per-WAN read failure tolerance, multi-WAN happy path
    - Tests drive print_digest with monkeypatched per_wan_ingestion_rate_bucketed to inject row scenarios deterministically (no real DB required for tie-break coverage)
    - capsys captures stdout; assertions check exact line content
  </behavior>
  <action>
    Add a NEW class `TestOperatorSummaryDigest` to `tests/test_history_ingestion_rate_bucketed.py`. Six methods:

      1. `test_digest_clear_winner_above_120pct(self, monkeypatch, capsys, tmp_path)` — monkeypatch `wanctl.operator_summary.per_wan_ingestion_rate_bucketed` to return rows where top1.rows_per_sec is 5x top2; call `print_digest([Path("/tmp/metrics-spectrum.db")])`; assert stdout contains `top=wanctl_rtt_ms` (the winning name); assert no `mixed:` token.
      2. `test_digest_below_120pct_renders_mixed_alphabetical(self, monkeypatch, capsys, tmp_path)` — return rows where ratio is 1.10 (below threshold); assert stdout contains `mixed: <alpha_first>/<alpha_second>` exactly (alphabetical ordering pinned).
      3. `test_digest_single_table_renders_table_name(self, monkeypatch, capsys, tmp_path)` — return one non-zero-rate row; assert stdout `top=<that_name>`.
      4. `test_digest_all_null_renders_na(self, monkeypatch, capsys, tmp_path)` — return rows with all rows_per_sec=None; assert stdout `total_rps=n/a top=n/a`.
      5. `test_digest_per_wan_read_failure_tolerated(self, monkeypatch, capsys, tmp_path)` — monkeypatch `per_wan_ingestion_rate_bucketed` to raise `sqlite3.DatabaseError` for one WAN and return normal rows for the other; assert: the failed WAN emits a `_DIGEST_SKIP_PREFIX (ingestion) wan=X` stderr line, the successful WAN still prints its line, return dict counts["read_skipped"] is 1.
      6. `test_digest_return_contract_unchanged(self, monkeypatch, capsys, tmp_path)` — assert `set(print_digest(...).keys()) == {"readable", "printed", "read_skipped", "write_skipped"}` (no new keys added).

    The monkeypatch uses `monkeypatch.setattr("wanctl.operator_summary.per_wan_ingestion_rate_bucketed", lambda db_paths, *, start_ts, end_ts, wan: (canned_rows, 0))`.

    For tests that need a db_path to satisfy print_digest signature but don't actually read it: use `tmp_path / "metrics-spectrum.db"` and either touch it as an empty file or pass it as a `Path` only — verify by reading print_digest at :152 what shape `db_paths` expects.

    Remove or update the placeholder `TestOperatorSummaryDigest` xfail from plan 01 if it existed (plan 01 task 1 method 4 specified `TestOperatorSummaryDigest` as a coverage target).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py::TestOperatorSummaryDigest -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py::TestOperatorSummaryDigest -x` exits 0 with 6 passed tests
    - `.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x` exits 0 (all 12 tests across both classes green)
    - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0 (hot-path slice unchanged)
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0
    - `.venv/bin/ruff check tests/test_history_ingestion_rate_bucketed.py` exits 0
    - `grep -c "mixed:" tests/test_history_ingestion_rate_bucketed.py` returns at least 1 (tie-break test pins the literal token)
  </acceptance_criteria>
  <done>All 4 tie-break branches and the per-WAN tolerance branch are pinned by deterministic monkeypatched tests; return-contract test prevents accidental dict-key drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics DB read -> operator-summary stdout | Per-WAN sqlite read errors must not leak partial data; existing hard-red loop's skip pattern is the template |
| `print_digest` return dict -> caller at operator_summary.py:244-252 | Return dict shape is contractual; new ingestion failures fold into existing keys to avoid a breaking change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-06 | Denial of Service | per-WAN ingestion fetch in digest loop | mitigate | per_wan_ingestion_rate_bucketed runs ONE indexed SQL pass per WAN; bounded by `idx_metrics_wan_metric_time`; fetch is wrapped in try/except so a slow/failed read for one WAN cannot block the digest emit for another |
| T-219-07 | Information Disclosure | db file path in stderr skip line | accept | db_path is operational metadata, not a secret; matches existing _DIGEST_SKIP_PREFIX hard-red pattern |
</threat_model>

<verification>
After both tasks:

```bash
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x
.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x
.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
.venv/bin/ruff check src/wanctl/operator_summary.py tests/test_history_ingestion_rate_bucketed.py
.venv/bin/mypy src/wanctl/operator_summary.py
```

All five MUST exit 0.

CLI smoke (manual but quick):

```bash
.venv/bin/python -m wanctl.operator_summary --digest --db /tmp/none.db 2>&1 | head
```

The non-existent db should produce a `_DIGEST_SKIP_PREFIX` line (skip, not abort) — proves tolerance is wired.
</verification>

<success_criteria>
- INGEST-04 satisfied: wanctl-operator-summary --digest renders the per-WAN ingestion block sourced from the bucketed helper
- All four tie-break branches covered by deterministic tests (>=1.20x winner, <1.20x mixed alphabetical, single non-zero, all-null n/a)
- Per-WAN read failure tolerated; read_skipped count incremented; never aborts the digest
- print_digest return dict shape unchanged (no consumer break at operator_summary.py:244-252)
- Hot-path regression slice still green
- Mutation-boundary green (only operator_summary.py touched within src/wanctl/)
</success_criteria>

<output>
Create `.planning/phases/219-ingestion-rate-observability-scope-d/219-03-SUMMARY.md`. Note the in-process import
choice (per RESEARCH Architectural Responsibility Map) and that print_digest's return contract was preserved
by folding ingestion failures into existing read_skipped/write_skipped counters.
</output>
