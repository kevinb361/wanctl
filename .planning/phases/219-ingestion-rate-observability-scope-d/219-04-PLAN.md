---
phase: 219
plan: 04
type: execute
wave: 3
depends_on: [219-02]
files_modified:
  - scripts/phase219-ingestion-digest.py
  - docs/CONFIGURATION.md
  - tests/test_phase219_ingestion_digest.py
autonomous: true
requirements: [INGEST-05, SAFE-11]
tags: [ingestion-rate, cron, snapshot, atomic-write, retention, docs]

must_haves:
  truths:
    - "scripts/phase219-ingestion-digest.py is invokable via `python -m` or direct invocation; cron-compatible"
    - "Atomic write: payload lands at /var/lib/wanctl/snapshots/ingestion/<unix_ts>.json via .tmp + os.replace; no partial files visible to concurrent readers"
    - "Count-based retention keeps the 288 most recent files; no per-cron age-based stat sweep (flash-wear-safe)"
    - "Subprocess to wanctl-history is bounded (timeout=30) and failure-tolerant: TimeoutExpired and CalledProcessError caught; script returns 1, never raises (cron-safe)"
    - "docs/CONFIGURATION.md carries a Phase 219 paragraph documenting _snapshot_unix / _snapshot_age_sec staleness semantics matching the v1.38 measurement_stale precedent"
    - "Tests pinned at tests/test_phase219_ingestion_digest.py turn green (xfail markers removed)"
  artifacts:
    - path: scripts/phase219-ingestion-digest.py
      provides: "Cron-callable snapshot writer; calls wanctl-history --ingestion-rate --by-table --rolling=60,300,3600 --json and persists output atomically"
      contains: "MAX_SNAPSHOTS = 288"
      min_lines: 80
    - path: docs/CONFIGURATION.md
      provides: "Phase 219 staleness-semantics paragraph + cron stanza documentation"
      contains: "_snapshot_unix"
    - path: tests/test_phase219_ingestion_digest.py
      provides: "Atomic write + retention + failure tolerance tests (xfail flipped to green)"
      contains: "MAX_SNAPSHOTS_DEFAULT = 288"
  key_links:
    - from: scripts/phase219-ingestion-digest.py
      to: wanctl.history --ingestion-rate --by-table --rolling=60,300,3600 --json
      via: "subprocess.run([sys.executable, '-m', 'wanctl.history', ...], capture_output=True, text=True, timeout=30, check=True)"
      pattern: "subprocess\\.run"
    - from: scripts/phase219-ingestion-digest.py
      to: /var/lib/wanctl/snapshots/ingestion/<unix_ts>.json
      via: "tmp_path.write_text + os.replace"
      pattern: "os\\.replace"
---

<objective>
Ship the cron-callable snapshot primitive that persists `wanctl-history --ingestion-rate --by-table
--rolling=60,300,3600 --json` to disk so Phase 218 audits have an evidence trail even when they fire mid-event.
This is the final Phase 219 surface and the file the operator wires into crontab.

Purpose: Satisfy INGEST-05 (cron-callable, count-based retention, atomic write). Honor flash-wear posture by
avoiding per-cron mtime stat sweeps. Document the staleness contract in docs/CONFIGURATION.md so operators
understand what `_snapshot_age_sec` means when reading a stored file later.

Output: A ~120 line stdlib-only script + docs paragraph + the previously-xfailed tests turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md
@src/wanctl/check_cake_fix.py
@src/wanctl/state_utils.py
@docs/CONFIGURATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from RESEARCH §C, §"Cron Script Pattern", and reference analogs -->

Analog 1 — retention pattern (src/wanctl/check_cake_fix.py:61-76):
  def _prune_snapshots(wan_name: str) -> None:
      if not SNAPSHOT_DIR.exists():
          return
      files = sorted(SNAPSHOT_DIR.glob(f"*_{wan_name}.json"))
      while len(files) > MAX_SNAPSHOTS:
          oldest = files.pop(0)
          oldest.unlink()
# Phase 219 clones this; filename pattern is `<unix_ts>.json` (lexicographic = chronological); no per-WAN suffix.

Analog 2 — atomic write (src/wanctl/state_utils.py:24-70 atomic_write_json):
  - tempfile in SAME parent dir
  - os.replace(tmp, target)  # POSIX atomic on same filesystem
# Phase 219 inlines this pattern (scripts/ should not import from src/wanctl/ for boundary-discipline per RESEARCH §F)

Locked constants (CONTEXT.md §Snapshot persistence):
  SNAPSHOT_DIR = Path("/var/lib/wanctl/snapshots/ingestion")
  MAX_SNAPSHOTS = 288
  DEFAULT_WINDOW_SEC = 3600
  WANCTL_HISTORY_ARGV = [
      sys.executable, "-m", "wanctl.history",
      "--ingestion-rate", "--by-table", "--rolling=60,300,3600", "--json",
  ]

Script CLI flags (for testability):
  --snapshot-dir PATH    # default: SNAPSHOT_DIR const; tests override to tmp_path
  --max-snapshots N      # default: 288; tests override to small N
  --payload-from-stdin   # test affordance: skip subprocess, read JSON from stdin (lets tests avoid forking wanctl-history)
  --inject-timestamp N   # test affordance: use N as the snapshot unix-ts (filename = N.json) instead of int(time.time()); enables deterministic retention-ordering tests
# NB: there is intentionally NO --window-sec flag. The rolling windows 60/300/3600 are CONTEXT-locked; no operator-time override is in scope (CONTEXT.md §Snapshot persistence).

Subprocess failure handling (RESEARCH R5):
  try:
      result = subprocess.run(argv, capture_output=True, text=True, timeout=30, check=True)
  except subprocess.TimeoutExpired as exc:
      print(f"phase219-ingestion-digest: TIMEOUT after {exc.timeout}s: {exc}", file=sys.stderr)
      return 1
  except subprocess.CalledProcessError as exc:
      print(f"phase219-ingestion-digest: wanctl-history failed rc={exc.returncode}: {exc.stderr}", file=sys.stderr)
      return 1
# NEVER let an exception bubble out of main() — cron must exit cleanly.

Cron stanza (documentation only):
  */5 * * * * wanctl /opt/wanctl/.venv/bin/python /opt/wanctl/scripts/phase219-ingestion-digest.py >> /var/log/wanctl/ingestion-digest.log 2>&1

systemd unit ReadWritePaths (deploy/systemd/wanctl@.service:34) already declares /var/lib/wanctl as writable for
the wanctl user. Phase 219 does NOT add a new systemd unit (INGEST-05 explicit: cron, not systemd).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/phase219-ingestion-digest.py (cron snapshot writer)</name>
  <read_first>
    - src/wanctl/check_cake_fix.py (lines 1-100; lift the _prune_snapshots pattern at :61-76 verbatim — adjust max from 20 to 288 and remove the per-WAN suffix)
    - src/wanctl/state_utils.py (lines 24-70; the atomic_write_json pattern; Phase 219 inlines this rather than imports it per RESEARCH §F boundary-discipline)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §C "scripts/phase219-ingestion-digest.py (new)" (the structural skeleton table at lines 196-208) AND §"Cron Script Pattern" (lines 596-633)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Snapshot persistence" (locked constants: path, 288, .tmp + os.rename, mode=0o755)
    - tests/test_phase219_ingestion_digest.py (from plan 01; the xfailed contract this script must satisfy)
    - scripts/ directory (list a few existing Phase 21x scripts for naming convention and module-level docstring style: `ls scripts/phase21*.py | head -5` then read the first one to lift the docstring/argparse style)
  </read_first>
  <behavior>
    - main() returns int (0 success, 1 failure); NEVER raises
    - SNAPSHOT_DIR is created with mode=0o755 if missing; PermissionError logged + write attempted anyway
    - Single subprocess call to wanctl-history with bounded timeout=30 and check=True; both TimeoutExpired and CalledProcessError caught -> return 1
    - Atomic write: payload to <target>.tmp in SAME parent dir, then os.replace to <target>; final chmod 0o644
    - Retention: sorted glob of *.json; pop oldest until len <= max_snapshots; lexicographic sort = chronological because filenames are <unix_ts>.json
    - Argparse: --snapshot-dir, --max-snapshots, --payload-from-stdin (test affordance), --inject-timestamp (test affordance)
    - When --inject-timestamp N is set, the filename uses N instead of int(time.time()); enables deterministic retention tests without sleeping
    - There is NO --window-sec flag; the rolling windows 60/300/3600 are CONTEXT-locked and embedded in the argv to wanctl-history
    - sys.exit(main()) at __name__=="__main__"
  </behavior>
  <action>
    Create `scripts/phase219-ingestion-digest.py`. Stdlib-only: `import argparse, json, os, subprocess, sys, time`, `from pathlib import Path`.

    Module docstring lifted in style from `scripts/phase21*.py` (match conventions). One-line summary: "Phase 219 ingestion-rate snapshot writer (cron-callable)."

    Module constants:
      `SNAPSHOT_DIR_DEFAULT = Path("/var/lib/wanctl/snapshots/ingestion")`
      `MAX_SNAPSHOTS_DEFAULT = 288`
      `DEFAULT_WINDOW_SEC = 3600`
      `SUBPROCESS_TIMEOUT_SEC = 30`

    Helper functions (top-level, all private with leading underscore):

      `_ensure_dir(snapshot_dir: Path) -> None`:
        `snapshot_dir.mkdir(parents=True, exist_ok=True, mode=0o755)`. Wrap in try/except PermissionError; on PermissionError log warning to stderr and return (caller will attempt write; the write may also fail, that's fine — return 1 path covers it).

      `_call_history() -> str`:
        Build argv: `[sys.executable, "-m", "wanctl.history", "--ingestion-rate", "--by-table", "--rolling=60,300,3600", "--json"]`. The rolling windows are CONTEXT-locked (60/300/3600); no operator-time override is in scope, so the helper takes no arguments. (`--from`/`--to` are unnecessary — rolling windows anchor to `now` inside wanctl.history.)
        `result = subprocess.run(argv, capture_output=True, text=True, timeout=SUBPROCESS_TIMEOUT_SEC, check=True)`. Return `result.stdout`. Let TimeoutExpired and CalledProcessError propagate — main() catches them.

      `_atomic_write(payload: str, target: Path) -> None`:
        `tmp = target.with_suffix(target.suffix + ".tmp")` (yields `<unix_ts>.json.tmp`)
        `tmp.write_text(payload, encoding="utf-8")`
        `os.replace(tmp, target)`
        `os.chmod(target, 0o644)`

      `_prune_old(snapshot_dir: Path, max_files: int) -> None`:
        `files = sorted(snapshot_dir.glob("*.json"))`
        `while len(files) > max_files:`
            `oldest = files.pop(0)`
            `oldest.unlink()`
        # NB: do NOT unlink *.tmp files here — a concurrent run may be mid-write.

      `_build_parser() -> argparse.ArgumentParser`:
        Add `--snapshot-dir` (type=Path, default=SNAPSHOT_DIR_DEFAULT)
        Add `--max-snapshots` (type=int, default=MAX_SNAPSHOTS_DEFAULT)
        Add `--payload-from-stdin` (action="store_true", help="Test affordance: read JSON payload from stdin instead of forking wanctl-history")
        Add `--inject-timestamp` (type=int, default=None, help="Test affordance: use this integer as the snapshot unix-ts (filename = <N>.json) instead of int(time.time()); enables deterministic retention-ordering tests")
        Total of FOUR flags. There is intentionally NO `--window-sec` flag — the rolling windows 60/300/3600 are CONTEXT-locked and live inside `_call_history`'s argv to wanctl-history.

      `main(argv: list[str] | None = None) -> int`:
        Parse args. Compute `snapshot_ts = args.inject_timestamp if args.inject_timestamp is not None else int(time.time())`. Call `_ensure_dir(args.snapshot_dir)`.

        If `args.payload_from_stdin`:
            `payload = sys.stdin.read()`
        else:
            try: `payload = _call_history()`
            except subprocess.TimeoutExpired as exc: stderr msg, return 1
            except subprocess.CalledProcessError as exc: stderr msg (include `exc.stderr`), return 1
            except OSError as exc: stderr msg, return 1

        `target = args.snapshot_dir / f"{snapshot_ts}.json"`
        try: `_atomic_write(payload, target)`
        except OSError as exc: stderr msg, return 1

        try: `_prune_old(args.snapshot_dir, args.max_snapshots)`
        except OSError as exc: stderr warning (do NOT fail the run — the snapshot landed successfully), return 0

        return 0

    Final block:
      `if __name__ == "__main__": sys.exit(main())`

    The script MUST be ruff/mypy clean. Make it `chmod +x` after writing (so direct invocation works for cron).

    Add a `# resolves_phase: 219` comment near the top so the milestone-close audit picks it up.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/phase219-ingestion-digest.py --snapshot-dir /tmp/p219-smoke --max-snapshots 2 --payload-from-stdin &lt;&lt;&lt; '{"schema_version":1,"rows":[]}' &amp;&amp; ls /tmp/p219-smoke/*.json | wc -l &amp;&amp; .venv/bin/ruff check scripts/phase219-ingestion-digest.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase219-ingestion-digest.py` exists and is executable (`test -x scripts/phase219-ingestion-digest.py && echo ok`)
    - `grep -c "MAX_SNAPSHOTS_DEFAULT = 288" scripts/phase219-ingestion-digest.py` returns 1
    - `grep -c "os.replace" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c "subprocess.TimeoutExpired" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c "subprocess.CalledProcessError" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c "0o755" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c "0o644" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c "resolves_phase: 219" scripts/phase219-ingestion-digest.py` returns at least 1
    - `grep -c -- "--inject-timestamp" scripts/phase219-ingestion-digest.py` returns at least 1 (the deterministic-retention test affordance)
    - `grep -c -- "--window-sec" scripts/phase219-ingestion-digest.py` returns 0 (no operator-time override; rolling windows are CONTEXT-locked)
    - The inline smoke command creates exactly 1 file in /tmp/p219-smoke/ and `ruff check` exits 0
    - `.venv/bin/mypy scripts/phase219-ingestion-digest.py` exits 0 (or matches an explicit baseline if mypy doesn't cover scripts/ by default)
    - `.venv/bin/python scripts/phase219-ingestion-digest.py --help` exits 0 and prints exactly four flags: `--snapshot-dir`, `--max-snapshots`, `--payload-from-stdin`, `--inject-timestamp` (no `--window-sec`)
  </acceptance_criteria>
  <done>Script lands, smoke produces one atomic file, all subprocess failure paths return 1 without raising; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip tests/test_phase219_ingestion_digest.py from xfail to green; add stdin-mode test paths</name>
  <read_first>
    - tests/test_phase219_ingestion_digest.py (from plan 01; the existing xfailed scaffolds)
    - scripts/phase219-ingestion-digest.py (just-shipped from Task 1; confirm helper names and the --payload-from-stdin affordance)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Validation Architecture" (lines 654-687; INGEST-05 test command map)
  </read_first>
  <behavior>
    - All previously-xfailed tests turn green
    - Tests drive the script via subprocess.run([sys.executable, "scripts/phase219-ingestion-digest.py", ...], input='{"schema_version":1,"rows":[]}', text=True) with --payload-from-stdin so no real wanctl-history fork is needed
    - Retention test invokes 5 times with sleep 0; assert final glob count == max_snapshots
    - Timeout/failure test: monkeypatch subprocess.run via importlib import of the script module, or run the script with PATH manipulated so `python -m wanctl.history` fails — simpler: directly assert via the failure-path test by feeding malformed args
  </behavior>
  <action>
    Update `tests/test_phase219_ingestion_digest.py` (from plan 01). Remove `@pytest.mark.xfail(...)` markers from
    the four test methods + the `MAX_SNAPSHOTS_DEFAULT` pin test. Update the implementation to drive the script
    via `subprocess.run`:

      Helper at module top: `SCRIPT = Path(__file__).resolve().parent.parent / "scripts" / "phase219-ingestion-digest.py"`

      Method 1 `test_atomic_rename_writes_exactly_one_file(self, tmp_path)`:
        Run script with `--snapshot-dir tmp_path --max-snapshots 10 --payload-from-stdin`, stdin=`'{"schema_version":1,"rows":[]}'`. Assert exit code 0. Assert `len(list(tmp_path.glob("*.json"))) == 1`. Assert `len(list(tmp_path.glob("*.tmp"))) == 0`. Assert file content round-trips as JSON with `schema_version == 1`.

      Method 2 `test_retention_keeps_max_files(self, tmp_path)`:
        Invoke the script 5 times sequentially with `--max-snapshots 3 --payload-from-stdin --inject-timestamp <N>` (use N = 1000, 1001, 1002, 1003, 1004 for the five calls — deterministic, no sleeping required; the `--inject-timestamp` flag is part of Task 1's argparse and forces the filename to `<N>.json`). Assert final `len(list(tmp_path.glob("*.json"))) == 3`. Assert the 3 retained filenames are `1002.json`, `1003.json`, `1004.json` (the lexicographically-largest 3 of the 5 created).

      Method 3 `test_subprocess_failure_returns_1_does_not_raise(self, tmp_path)`:
        Run the script WITHOUT `--payload-from-stdin` and with `PYTHONPATH=/nonexistent` env so `python -m wanctl.history` import fails. Assert exit code is non-zero (specifically 1). Assert `len(list(tmp_path.glob("*.tmp"))) == 0`. Assert `len(list(tmp_path.glob("*.json"))) == 0`.

      Method 4 `test_snapshot_dir_created_with_mode_0o755(self, tmp_path)`:
        Run script with `--snapshot-dir tmp_path/nested/ingestion --payload-from-stdin`, stdin=valid JSON. Assert exit 0. Assert `(tmp_path / "nested" / "ingestion").stat().st_mode & 0o777 == 0o755`.

      Method 5 `test_default_max_snapshots_is_288(self)`:
        Import the script module via `importlib.util.spec_from_file_location("phase219_digest", SCRIPT)`; assert `phase219_digest.MAX_SNAPSHOTS_DEFAULT == 288`.

    The `--inject-timestamp` flag used by method 2 is landed in Task 1's argparse (single-task discipline preserved — Task 2 only consumes it, never amends Task 1's script).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase219_ingestion_digest.py -v -x 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase219_ingestion_digest.py -x` exits 0 with 5 passed (zero xfailed)
    - `grep -c "xfail" tests/test_phase219_ingestion_digest.py` returns 0 (all markers removed)
    - `.venv/bin/ruff check tests/test_phase219_ingestion_digest.py scripts/phase219-ingestion-digest.py` exits 0
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0 (no controller-path diff)
    - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0 (hot-path slice)
    - `grep -c "inject-timestamp" scripts/phase219-ingestion-digest.py` returns at least 1 (the deterministic-retention test affordance is consumed by method 2)
  </acceptance_criteria>
  <done>5 cron-script tests green, no xfail residue, mutation-boundary green, hot-path slice green.</done>
</task>

<task type="auto">
  <name>Task 3: Document staleness semantics + cron stanza in docs/CONFIGURATION.md</name>
  <read_first>
    - docs/CONFIGURATION.md (entire file; find the v1.38 measurement_stale / measurement_staleness_sec section — the precedent Phase 219 docs paragraph sits beside)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"JSON output shape" and §"Snapshot persistence" (the locked semantics this paragraph describes)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Cron stanza (documentation only, NOT installed)" (lines 211-213; the verbatim stanza to embed)
  </read_first>
  <action>
    Add a new section to `docs/CONFIGURATION.md` titled `### Ingestion-Rate Snapshot Staleness (Phase 219)` placed
    immediately after the existing `measurement_stale` / `measurement_staleness_sec` section (find via `grep -n "measurement_stale" docs/CONFIGURATION.md`). The new section is CLI-tool-description only — no threshold-tuning language, no future-tuning prediction, no recommended-value tables (SAFE-11 enforcement).

    Required content (~25-40 lines):

      1. One paragraph describing the `--ingestion-rate` CLI envelope: `{schema_version: 1, rows: [...]}` with `_snapshot_unix` and `_snapshot_age_sec` on every row. Explain that `_snapshot_unix` is captured once per invocation (single fetch_time across all rows) and `_snapshot_age_sec = now - _snapshot_unix` at emit. For direct CLI use the age is ~0; the field becomes meaningful when reading a persisted snapshot file later. Include the RESEARCH R1 disambiguation sentence verbatim (pinned by acceptance grep): **"`table_name` in the JSON envelope is the `metric_name` of the row in the metrics SQL table."**

      2. One paragraph describing the snapshot writer (`scripts/phase219-ingestion-digest.py`): persistence path `/var/lib/wanctl/snapshots/ingestion/<unix_ts>.json`, atomic write via .tmp + os.replace, count-based retention (288 most recent files, ~24h at 5-min cadence, ~3MB ceiling). Note this is cron-orchestrated (not systemd) per INGEST-05.

      3. One literal cron stanza block (fenced ```cron) lifted verbatim from RESEARCH §C "Cron stanza (documentation only)":
         ```cron
         */5 * * * * wanctl /opt/wanctl/.venv/bin/python /opt/wanctl/scripts/phase219-ingestion-digest.py >> /var/log/wanctl/ingestion-digest.log 2>&1
         ```
         Followed by one sentence noting that the snapshot directory must exist with the correct ownership before the first cron tick: `sudo install -d -m 0755 -o wanctl -g wanctl /var/lib/wanctl/snapshots/ingestion`.

      4. One paragraph cross-referencing the v1.38 `measurement_stale` precedent: "The staleness contract here mirrors the v1.38 `measurement_stale` / `measurement_staleness_sec` pattern — readers compute age relative to a captured timestamp rather than rely on file mtime."

    DO NOT add any text suggesting threshold values, retention tuning, or "consider increasing/decreasing" language. The Phase 219 mutation-boundary test (`tests/test_phase219_mutation_boundary.py` test 3) scans this file for forbidden tuning tokens via `FORBIDDEN_MUTATION_RE` — keep the section descriptive only.
  </action>
  <verify>
    <automated>grep -c "_snapshot_unix" docs/CONFIGURATION.md &amp;&amp; grep -c "phase219-ingestion-digest.py" docs/CONFIGURATION.md &amp;&amp; grep -c "schema_version" docs/CONFIGURATION.md &amp;&amp; .venv/bin/pytest tests/test_phase219_mutation_boundary.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_snapshot_unix" docs/CONFIGURATION.md` returns at least 1
    - `grep -c "_snapshot_age_sec" docs/CONFIGURATION.md` returns at least 1
    - `grep -c "phase219-ingestion-digest.py" docs/CONFIGURATION.md` returns at least 1
    - `grep -c "schema_version" docs/CONFIGURATION.md` returns at least 1
    - `grep -cE "(consider increasing|consider decreasing|tune|tuning|recommended value|optimal value)" docs/CONFIGURATION.md` returns 0 for the Phase 219 section specifically (run via `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -cE "consider increasing|consider decreasing|tune|tuning|recommended value|optimal value"` returns 0)
    - R1 disambiguation present in the new section: `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -c "metric_name"` returns at least 1 AND `awk '/Ingestion-Rate Snapshot Staleness/,/^### /' docs/CONFIGURATION.md | grep -c "table_name in the JSON envelope is the"` returns at least 1 (the verbatim disambiguation sentence — `table_name` field equals `metric_name` SQL column)
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0 (the FORBIDDEN_MUTATION_RE scan finds no tuning tokens in the new section)
    - `grep -c "measurement_stale" docs/CONFIGURATION.md` returns at least 1 (the cross-reference paragraph)
    - Section heading exists: `grep -c "### Ingestion-Rate Snapshot Staleness" docs/CONFIGURATION.md` returns 1
  </acceptance_criteria>
  <done>Docs paragraph added, cross-references v1.38 precedent, no tuning language, mutation-boundary docs test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron -> phase219-ingestion-digest.py | Cron invokes script with fixed argv; no operator argv at runtime |
| subprocess argv -> python -m wanctl.history | Argv is LIST-form (NEVER shell=True); no user-controlled string interpolation per T-219-03 |
| snapshot files -> filesystem | mode 0o644 (world-readable but not writable); dir 0o755; owner wanctl per systemd unit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-01 | Information Disclosure | /var/lib/wanctl/snapshots/ingestion/ | mitigate | mkdir(mode=0o755); chmod(0o644) on each file; ingestion-rate JSON contains no PII or secrets — only metric_name + row counts; documented in docs/CONFIGURATION.md |
| T-219-03 | Injection | subprocess invocation of wanctl-history | mitigate | argv passed as LIST: `[sys.executable, "-m", "wanctl.history", ...]`; shell=False (default); no f-string interpolation of user input into argv |
| T-219-08 | Denial of Service | unbounded subprocess hang | mitigate | timeout=30s on subprocess.run; TimeoutExpired caught + return 1; flash-wear retention bounded at 288 files |
| T-219-09 | Tampering | concurrent write race | mitigate | tempfile in SAME parent dir + os.replace = POSIX atomic; concurrent readers never see partial JSON |
</threat_model>

<verification>
After all three tasks:

```bash
.venv/bin/pytest tests/test_phase219_ingestion_digest.py -x
.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x
.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
.venv/bin/ruff check scripts/phase219-ingestion-digest.py tests/test_phase219_ingestion_digest.py
.venv/bin/python scripts/phase219-ingestion-digest.py --help | grep -E -- "--snapshot-dir|--max-snapshots|--payload-from-stdin|--inject-timestamp"
.venv/bin/python scripts/phase219-ingestion-digest.py --help 2>&1 | grep -c -- "--window-sec"  # must be 0
```

All six MUST succeed (penultimate finds exactly the four real flags; the trailing `--window-sec` grep must return 0 — that flag does not exist by design).

Full Phase 219 regression slice:

```bash
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py tests/test_phase219_ingestion_digest.py tests/test_phase219_mutation_boundary.py tests/test_history_cli.py -x
```

MUST exit 0 — confirms all Phase 219 tests green and v1.44 envelope migration (R2) holds.
</verification>

<success_criteria>
- INGEST-05 satisfied: cron-callable script exists; atomic write; count-based retention=288; failure-tolerant
- docs/CONFIGURATION.md carries the Phase 219 staleness-semantics paragraph + cron stanza
- All Phase 219 tests green (bucketed + cron-script + mutation-boundary)
- v1.44 envelope-pinning tests still green (no regression)
- Hot-path regression slice still green
- Mutation-boundary green: scripts/phase219-* and docs/CONFIGURATION.md edits are allowlisted; no controller-path diff
- SAFE-11 docs scan finds no threshold-tuning tokens in the new section
</success_criteria>

<output>
Create `.planning/phases/219-ingestion-rate-observability-scope-d/219-04-SUMMARY.md`. Document:
- The folded todo `2026-04-17-ingestion-rate-tool` resolves with this plan (smoothed tail-rate via --rolling=60,300,3600)
- The cron stanza is documentation only — operator installs the crontab entry manually
- Post-deploy: operator MUST run the Phase 217 cycle-budget capture per `docs/PROFILING.md` and record evidence in the phase SUMMARY (success criterion #5: `cycle_total.avg_ms <= 3.0`, `p99_ms <= 7.5`)
</output>
