---
phase: 219
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_history_ingestion_rate_bucketed.py
  - tests/test_phase219_ingestion_digest.py
  - tests/test_phase219_mutation_boundary.py
  - tests/fixtures/phase219/.gitkeep
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, INGEST-05, SAFE-11]
tags: [ingestion-rate, observability, test-scaffold, safe-11]

must_haves:
  truths:
    - "Wave 0 test files exist and import cleanly under pytest collection"
    - "Each test class declares the expected behaviors as test methods (xfail/skip allowed since impl is Wave 1+)"
    - "SAFE-11 mutation-boundary scaffold encodes the Phase 219 allowlist (history.py + operator_summary.py additive; controller-path forbidden)"
    - "Golden-fixture helper deterministically seeds an in-memory SQLite via MetricsWriter for downstream waves"
  artifacts:
    - path: tests/test_history_ingestion_rate_bucketed.py
      provides: "Golden-JSON pin for --by-table + --rolling shapes; pins schema_version=1, _snapshot_unix, _snapshot_age_sec, per-row field set"
      contains: "class TestIngestionRateBucketed"
    - path: tests/test_phase219_ingestion_digest.py
      provides: "Atomic-write + count-based retention test for cron script (script-not-yet-existing tests allowed to xfail)"
      contains: "def test_atomic_rename_writes_exactly_one_file"
    - path: tests/test_phase219_mutation_boundary.py
      provides: "SAFE-11 boundary clone of Phase 214 template; allowlist permits additive diffs to history.py + operator_summary.py; controller-path forbidden"
      contains: "ALLOWED_SRC_PATHS"
    - path: tests/fixtures/phase219/.gitkeep
      provides: "Fixture dir placeholder (golden literals stay inline)"
  key_links:
    - from: tests/test_history_ingestion_rate_bucketed.py
      to: src/wanctl/storage/writer.py::MetricsWriter
      via: "in-test seeding (BASE_TS deterministic anchor)"
      pattern: "MetricsWriter\\(db_path="
    - from: tests/test_phase219_mutation_boundary.py
      to: tests/test_phase214_mutation_boundary.py
      via: "clone of _phase_base_sha + _assert_no_git_diff helpers"
      pattern: "PHASE219_BASE_SHA"
---

<objective>
Land the Wave 0 test scaffolds for Phase 219 BEFORE any implementation work begins. This is the project's
test-first standard: implementation waves (1, 2, 3) execute against these tests rather than after them.

Purpose: Pin contract shape (JSON envelope, schema_version, staleness fields, tie-break semantics, retention
behavior) before code lands so reviewers see the contract as the ground truth and implementation cannot drift
from CONTEXT.md locked decisions.

Output: Four files committed — three test files plus a fixture directory placeholder. Tests MAY xfail/skip
because production helpers are not yet introduced; they MUST collect cleanly under `pytest --collect-only`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-VALIDATION.md
@tests/test_phase214_mutation_boundary.py
@tests/test_history_cli.py
@src/wanctl/storage/writer.py
@CLAUDE.md

<interfaces>
<!-- Key contracts encoded in the tests; sourced from CONTEXT.md locked decisions and RESEARCH.md §Step 5 -->

JSON envelope (locked in CONTEXT.md §JSON output shape):
```json
{
  "schema_version": 1,
  "rows": [
    {
      "wan_name": "spectrum",
      "wan_db": "/var/lib/wanctl/metrics-spectrum.db",
      "table_name": "wanctl_rtt_ms",   // null when --by-table is absent
      "window_seconds": 60,
      "row_count": 60,                  // null on read failure
      "rows_per_sec": 1.0,              // null on read failure
      "_snapshot_unix": 1767225660,
      "_snapshot_age_sec": 0
    }
  ]
}
```

Phase 214 mutation-boundary template (clone targets):
- `_phase_base_sha()` (tests/test_phase214_mutation_boundary.py:56-72) — env override `PHASE214_BASE_SHA` becomes `PHASE219_BASE_SHA`; merge-base + HEAD~10 fallback reused
- `_assert_no_git_diff(paths, label)` (tests/test_phase214_mutation_boundary.py:75-89) — checks unstaged/staged/committed; reused verbatim
- `FORBIDDEN_MUTATION_RE` (tests/test_phase214_mutation_boundary.py:27-43) — reused verbatim for any future docs guard

MetricsWriter seeding pattern (tests/test_history_cli.py:826-840) for the bucketed test:
```python
writer = MetricsWriter(db_path=db_path)
for metric_name in ("wanctl_rtt_ms", "wanctl_state", "wanctl_signal_jitter_ms", "wanctl_cake_drop_rate"):
    for i in range(60):
        writer.write_metric(timestamp=BASE_TS + i, wan_name="spectrum", metric_name=metric_name, value=1.0)
writer.close()
MetricsWriter._reset_instance()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_history_ingestion_rate_bucketed.py scaffold + fixture dir</name>
  <read_first>
    - tests/test_history_cli.py (full file; TestIngestionRateCli class at :809-1077 is the direct template — clone fixture seeding pattern at :826-840, monkeypatch.setattr(sys, "argv", ...) pattern at :842-855, capsys assertion pattern)
    - tests/conftest.py (entire file; reuse any shared fixtures like tmp_path conventions)
    - src/wanctl/storage/writer.py (entire file; confirm MetricsWriter.write_metric signature and _reset_instance helper)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Golden-JSON pin: tests/test_history_ingestion_rate_bucketed.py" (lines 481-549; the test skeleton lives there)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"JSON output shape" (the locked envelope contract this file pins)
  </read_first>
  <action>
    Create `tests/test_history_ingestion_rate_bucketed.py` and `tests/fixtures/phase219/.gitkeep`.

    Module-level constant: `BASE_TS = 1767225600` (2026-01-01 00:00:00 UTC; deterministic anchor for `_snapshot_unix` pins).

    Define ONE class `TestIngestionRateBucketed` with SIX test methods. Each method seeds the SQLite fixture
    using the pattern at tests/test_history_cli.py:826-840 (`MetricsWriter._reset_instance()` → seed metric_name × timestamps loop → `writer.close()` → `MetricsWriter._reset_instance()`). Drive `wanctl.history.main()` via
    `monkeypatch.setattr(sys, "argv", [...])` per tests/test_history_cli.py:842-855. Pin `time.time` via
    `monkeypatch.setattr("wanctl.history.time.time", lambda: float(BASE_TS + 60))` so `_snapshot_unix` is deterministic.

    Test methods (each MUST start with `pytest.importorskip` guard OR be wrapped with `@pytest.mark.xfail(reason="Wave 1 helper not yet implemented", strict=False)` since the production helpers land in plan 02):
      1. `test_schema_version_pinned(self, tmp_path, monkeypatch, capsys)` — drive `--ingestion-rate --by-table --json`; assert `payload["schema_version"] == 1`, `isinstance(payload["rows"], list)`, exact `len == 4` for the 4 metric_names × 1 WAN × 1 window seed, and per-row field set equals the locked CONTEXT.md set: `{"wan_name","wan_db","table_name","window_seconds","row_count","rows_per_sec","_snapshot_unix","_snapshot_age_sec"}`. Assert `row["_snapshot_unix"] == BASE_TS + 60`, `row["wan_name"] == "spectrum"`, `row["row_count"] == 60`, `row["window_seconds"] == 60`. (Covers INGEST-03)
      2. `test_by_table_emits_per_metric_row(self, tmp_path, monkeypatch, capsys)` — same seed; assert `table_name` is not null on every row, the set of `row["table_name"]` equals the seeded metric_name set, `row_count == 60` per row. (Covers INGEST-01)
      3. `test_rolling_emits_one_row_per_window(self, tmp_path, monkeypatch, capsys)` — drive `--ingestion-rate --rolling=60,300 --json` (no `--by-table`); seed 600s of data; assert `len(payload["rows"]) == 2` (1 WAN × 2 windows), distinct `window_seconds` values `{60, 300}`. (Covers INGEST-02)
      4. `test_by_table_and_rolling_cartesian(self, tmp_path, monkeypatch, capsys)` — drive `--ingestion-rate --by-table --rolling=60,300 --json`; assert `len == 4 × 2 == 8`, all (table_name, window_seconds) pairs present.
      5. `test_per_metric_read_failure_emits_null_row(self, tmp_path, monkeypatch, capsys)` — monkeypatch `sqlite3.connect` to raise `sqlite3.DatabaseError` for the second metric_name only; assert that metric row has `row_count is None` and `rows_per_sec is None`, but the other metric_names emit valid rows. (Covers INGEST-01 tolerance per RESEARCH §A.row table)
      6. `test_default_mode_back_compat(self, tmp_path, monkeypatch, capsys)` — drive `--ingestion-rate --json` (neither `--by-table` nor `--rolling`); per CONTEXT discretion §3 ("recommended yes for consistency"), assert envelope still has `schema_version: 1`, `_snapshot_unix`, `_snapshot_age_sec`, one row per WAN, `table_name is None`, `window_seconds == end_ts - start_ts`. NOTE: this test ALSO pins the v1.44 envelope replacement that RESEARCH R2 flags — when the test passes, `tests/test_history_cli.py:862-921` will need its envelope pins updated in plan 02.

    Imports at top: `import json`, `import sys`, `from datetime import datetime`, `import pytest`, `from wanctl.history import main`, `from wanctl.storage.writer import MetricsWriter`.

    Helper method `_ts_arg(self, ts: int) -> str: return datetime.fromtimestamp(ts).isoformat()` for `--from`/`--to` argv values.

    Create `tests/fixtures/phase219/.gitkeep` as a 0-byte file (placeholder per CONTEXT.md §Mutation-boundary: fixture dir is allowlisted; golden literals stay inline).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py --collect-only -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_history_ingestion_rate_bucketed.py` exists and is non-empty
    - File `tests/fixtures/phase219/.gitkeep` exists (0 bytes acceptable)
    - `.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py --collect-only -q` lists exactly 6 test methods under `TestIngestionRateBucketed`
    - `grep -c "BASE_TS = 1767225600" tests/test_history_ingestion_rate_bucketed.py` returns 1
    - `grep -c "_snapshot_unix" tests/test_history_ingestion_rate_bucketed.py` returns at least 3 (env assertions + field-set check)
    - `grep -c 'schema_version' tests/test_history_ingestion_rate_bucketed.py` returns at least 2
    - Running `.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x 2>&1 | grep -E "(passed|xfailed|error)"` shows xfailed or error-on-import — NOT a hard fail (production helpers ship in plan 02)
  </acceptance_criteria>
  <done>Collection succeeds, six test methods present, deterministic anchor encoded, locked field set pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_phase219_ingestion_digest.py scaffold</name>
  <read_first>
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §C "scripts/phase219-ingestion-digest.py (new)" (the structural skeleton table; defines _ensure_dir, _call_history, _atomic_write, _prune_old, main contracts) and §"Cron Script Pattern" (atomic-write reference)
    - src/wanctl/check_cake_fix.py (lines 61-76 specifically; the _prune_snapshots verbatim analog Phase 219 retention will clone)
    - src/wanctl/state_utils.py (lines 24-70 specifically; atomic_write_json pattern Phase 219 inlines)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Snapshot persistence" (locked decisions: path, 288 retention, .tmp + os.rename, mode=0o755)
  </read_first>
  <action>
    Create `tests/test_phase219_ingestion_digest.py`. The production script `scripts/phase219-ingestion-digest.py`
    ships in plan 04 — these tests will xfail until then; that is expected.

    Use `pytest.importorskip` at module level: `phase219_digest = pytest.importorskip("scripts.phase219_ingestion_digest", reason="Wave 3 script not yet implemented")` OR a try/except ImportError fallback that marks the whole module skipped. The script is intended to be invokable via `runpy.run_path` (it lives in `scripts/` which is not a package); prefer driving the script as a subprocess using `sys.executable` for tests that verify CLI behavior, and import internal helpers via `importlib.util.spec_from_file_location` when testing helper functions directly.

    Define class `TestPhase219IngestionDigest` with FIVE test methods:
      1. `test_atomic_rename_writes_exactly_one_file(self, tmp_path)` — invoke the script with `--snapshot-dir tmp_path --max-snapshots 10 --payload-from-stdin` (the `--payload-from-stdin` flag is the test affordance landed by plan 04 Task 1's argparse list); assert `len(list(tmp_path.glob("*.json"))) == 1`, assert no `*.tmp` files remain in `tmp_path`.
      2. `test_retention_keeps_max_files(self, tmp_path)` — invoke the script 5 times sequentially with `--max-snapshots 3 --payload-from-stdin --inject-timestamp <N>` (plan 04 Task 1 lands the `--inject-timestamp` flag so retention tests deterministically pick distinct unix-ts filenames without sleeping); assert `len(list(tmp_path.glob("*.json"))) == 3`, assert the 3 retained filenames are the lexicographically-largest 3 (newest injected unix_ts).
      3. `test_subprocess_failure_returns_1_does_not_raise(self, tmp_path, monkeypatch)` — monkeypatch the subprocess invocation of `wanctl-history` to raise `subprocess.TimeoutExpired` or return non-zero `CalledProcessError`; assert script's `main()` returns 1, asserts no exception bubbles, asserts no partial `*.tmp` file remains in `tmp_path`. (Covers RESEARCH R5)
      4. `test_snapshot_dir_created_with_mode_0o755(self, tmp_path)` — point `--snapshot-dir` to a non-existent subdir under tmp_path; assert it is created with mode `0o755` after invocation (`(tmp_path / "ingestion").stat().st_mode & 0o777 == 0o755`).
      5. `test_default_max_snapshots_is_288(self)` — reads the `MAX_SNAPSHOTS_DEFAULT` constant from the script module (via `importlib.util.spec_from_file_location`) and asserts equality with `288`. This pins the CONTEXT.md §Snapshot persistence locked value.

    Module-level constant pinned in this test file: `MAX_SNAPSHOTS_DEFAULT = 288` (mirrors the script constant; method 5 above asserts they agree).

    Mark every test method (all FIVE: 1, 2, 3, 4, 5) with `@pytest.mark.xfail(reason="scripts/phase219-ingestion-digest.py lands in plan 04", strict=False)`. The xfail markers come off in plan 04 Task 2 (the strip count is 5, matching the 5 methods above).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase219_ingestion_digest.py --collect-only -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_phase219_ingestion_digest.py` exists and collects
    - `.venv/bin/pytest tests/test_phase219_ingestion_digest.py --collect-only -q` lists 5 test methods under `TestPhase219IngestionDigest`
    - `grep -c "MAX_SNAPSHOTS_DEFAULT = 288" tests/test_phase219_ingestion_digest.py` returns 1
    - `grep -c "atomic_rename\\|os.replace\\|*.tmp" tests/test_phase219_ingestion_digest.py` returns at least 2
    - `grep -c "xfail" tests/test_phase219_ingestion_digest.py` returns at least 5 (one per test method)
    - `grep -c "0o755" tests/test_phase219_ingestion_digest.py` returns at least 1
  </acceptance_criteria>
  <done>Collection succeeds with 5 xfailed test methods; CONTEXT.md retention=288 and mode=0o755 pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_phase219_mutation_boundary.py from Phase 214 template</name>
  <read_first>
    - tests/test_phase214_mutation_boundary.py (entire file; verbatim template — copy the structure)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §E "SAFE-11 mutation-boundary test details" (lines 224-234; the allowlist flip and forbidden list)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"Mutation-boundary scope (SAFE-11)" (allowlisted vs forbidden enumerations)
    - .planning/REQUIREMENTS.md §SAFE-11 (the canonical wording)
  </read_first>
  <action>
    Clone `tests/test_phase214_mutation_boundary.py` into `tests/test_phase219_mutation_boundary.py`. Preserve verbatim:
      - `_git(args)` helper
      - `_phase_base_sha()` function structure — RENAME env variable from `PHASE214_BASE_SHA` to `PHASE219_BASE_SHA`
      - `_assert_no_git_diff(paths, label)` function — reused verbatim
      - `FORBIDDEN_MUTATION_RE` regex (already line-anchored per Phase 214 D-14)

    Replace `PROTECTED_PATHS` and supporting constants per Phase 219 allowlist:
      - `ALLOWED_SRC_PATHS = ["src/wanctl/history.py", "src/wanctl/operator_summary.py"]` (per CONTEXT §Mutation-boundary; additive edits explicitly allowlisted)
      - `FORBIDDEN_SRC_PATHS = ["src/wanctl/wan_controller.py", "src/wanctl/queue_controller.py", "src/wanctl/cake_signal.py", "src/wanctl/alert_engine.py"]` plus a runtime expansion: traverse `src/wanctl/backends/` and `src/wanctl/fusion*.py` via `Path.glob` if those subtrees exist.

    Replace test functions:
      1. `test_no_forbidden_controller_path_diff()` — calls `_assert_no_git_diff(FORBIDDEN_SRC_PATHS, "controller-path")`. Asserts zero diff at every Phase 219 boundary.
      2. `test_no_other_src_diff_outside_allowlist()` — uses git pathspec exclude per RESEARCH §E: `pathspec = ["src/wanctl/"] + [f":(exclude){p}" for p in ALLOWED_SRC_PATHS]`; assert `_git(["diff", "--name-only", f"{_phase_base_sha()}..HEAD", "--", *pathspec])` returns empty. (NB: pathspec exclude syntax is `:(exclude)PATH` or `:!PATH`; use whichever Phase 214 already uses.)
      3. `test_phase219_docs_have_no_threshold_tuning_tokens()` — reuse `FORBIDDEN_MUTATION_RE`; scan `docs/CONFIGURATION.md` if it has a Phase 219-attributable diff vs base SHA; assert zero matches. Skip cleanly if `docs/CONFIGURATION.md` has no diff (the docs paragraph lands in plan 04).
      4. `test_phase219_scripts_allowlist()` — assert any `scripts/phase219-*.py` diff vs base IS allowed (no assertion failure for these); confirms the new script path is not forbidden.

    Set module docstring noting "Phase 219 SAFE-11 mutation-boundary clone; allowlist mirrors CONTEXT.md §Mutation-boundary scope; controller-path remains forbidden per REQUIREMENTS.md SAFE-11."

    DO NOT modify `tests/test_phase214_mutation_boundary.py` — Phase 214 has shipped; the boundary is per-phase.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase219_mutation_boundary.py -v --collect-only 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_phase219_mutation_boundary.py` exists
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py --collect-only -q` lists exactly 4 test functions
    - `grep -c "PHASE219_BASE_SHA" tests/test_phase219_mutation_boundary.py` returns at least 1
    - `grep -c 'ALLOWED_SRC_PATHS' tests/test_phase219_mutation_boundary.py | grep -v '^#'` >= 1 (the constant is defined and referenced)
    - `grep -c '"src/wanctl/wan_controller.py"' tests/test_phase219_mutation_boundary.py` returns 1
    - `grep -c '"src/wanctl/queue_controller.py"' tests/test_phase219_mutation_boundary.py` returns 1
    - `grep -c '"src/wanctl/cake_signal.py"' tests/test_phase219_mutation_boundary.py` returns 1
    - `grep -c '"src/wanctl/history.py"' tests/test_phase219_mutation_boundary.py` returns at least 1
    - `grep -c '"src/wanctl/operator_summary.py"' tests/test_phase219_mutation_boundary.py` returns at least 1
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py::test_no_forbidden_controller_path_diff -x` passes green (no controller-path diff exists yet)
  </acceptance_criteria>
  <done>Boundary test exists, allowlist + forbidden list match CONTEXT.md exactly, controller-path-zero-diff test green at Wave 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → repo working tree | Tests read `git diff` output to enforce SAFE-11; only the running developer's working tree is observed; no remote inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-W0-01 | Tampering | tests/test_phase219_mutation_boundary.py | mitigate | Test file itself is part of the SAFE-11 allowlist; tampering would show up as a diff in the test commit and be reviewed via project-finalizer + commit message review |
| T-219-W0-02 | Repudiation | git base SHA resolution | mitigate | `_phase_base_sha()` env override `PHASE219_BASE_SHA` is logged via test name when fallbacks engage; merge-base + HEAD~10 fallback chain matches Phase 214 audited pattern |
</threat_model>

<verification>
After all three tasks:

```bash
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py tests/test_phase219_ingestion_digest.py tests/test_phase219_mutation_boundary.py --collect-only -q
.venv/bin/pytest tests/test_phase219_mutation_boundary.py::test_no_forbidden_controller_path_diff -x
.venv/bin/ruff check tests/test_history_ingestion_rate_bucketed.py tests/test_phase219_ingestion_digest.py tests/test_phase219_mutation_boundary.py
.venv/bin/ruff format --check tests/test_history_ingestion_rate_bucketed.py tests/test_phase219_ingestion_digest.py tests/test_phase219_mutation_boundary.py
```

All four commands MUST return exit 0. The collect-only run lists exactly 15 test items total (6 + 5 + 4).
</verification>

<success_criteria>
- All three test files exist and collect cleanly
- Mutation-boundary test green at Wave 0 (no controller-path diff exists since no impl has landed)
- Ruff and ruff-format clean on the new files
- Fixture directory `tests/fixtures/phase219/` exists (placeholder .gitkeep)
- BASE_TS deterministic anchor encoded in the bucketed test
- 288 retention + 0o755 mode pinned in the digest script test
</success_criteria>

<output>
After completion, create `.planning/phases/219-ingestion-rate-observability-scope-d/219-01-SUMMARY.md` per the
GSD summary template. Note which test methods xfail by design (production helpers ship in Waves 1+).
</output>
