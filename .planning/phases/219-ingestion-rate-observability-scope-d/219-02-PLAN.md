---
phase: 219
plan: 02
type: execute
wave: 1
depends_on: [219-01]
files_modified:
  - src/wanctl/history.py
  - tests/test_history_cli.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, SAFE-11]
tags: [ingestion-rate, cli, history, json-envelope]

must_haves:
  truths:
    - "wanctl-history --ingestion-rate --by-table emits per-WAN x per-metric_name rows"
    - "wanctl-history --ingestion-rate --rolling=60,300,3600 emits one row per (WAN, window_seconds)"
    - "Both flags compose to a cartesian product (WAN x metric_name x window)"
    - "JSON output is the locked envelope {schema_version: 1, rows: [...]} with _snapshot_unix + _snapshot_age_sec on every row"
    - "Per-metric_name read failures emit null rows; never abort the WAN; never abort the call"
    - "Default mode (neither flag) is back-compat with v1.44 envelope shape but ALSO carries schema_version + staleness fields"
    - "tests/test_history_cli.py::TestIngestionRateCli envelope pins are updated to the Phase 219 envelope and remain green"
  artifacts:
    - path: src/wanctl/history.py
      provides: "public per_wan_ingestion_rate_bucketed() helper; --by-table + --rolling argparse flags; envelope formatter"
      contains: "def per_wan_ingestion_rate_bucketed("
    - path: src/wanctl/history.py
      provides: "_resolve_rolling_windows() helper returning [(start_ts, end_ts, window_seconds), ...]"
      contains: "def _resolve_rolling_windows("
    - path: src/wanctl/history.py
      provides: "format_ingestion_rate_envelope_json(rows, snapshot_unix) emitting {schema_version: 1, rows: [...]}"
      contains: "schema_version"
    - path: tests/test_history_cli.py
      provides: "v1.44 envelope pins replaced with Phase 219 envelope; existing TestIngestionRateCli class stays green"
      contains: "schema_version"
  key_links:
    - from: src/wanctl/history.py::_handle_special_query
      to: src/wanctl/history.py::per_wan_ingestion_rate_bucketed
      via: "called once per window when args.by_table is set"
      pattern: "per_wan_ingestion_rate_bucketed\\("
    - from: src/wanctl/history.py::format_ingestion_rate_envelope_json
      to: snapshot_unix captured in _handle_special_query
      via: "single fetch_time per invocation; carried into every row"
      pattern: "snapshot_unix = int\\(time\\.time\\(\\)\\)"
---

<objective>
Extend `wanctl-history --ingestion-rate` with the two locked CLI flags (`--by-table`, `--rolling`) and rewrite
the JSON formatter to the locked envelope shape. This is the Phase 219 core CLI surface — operator-summary
(plan 03) and the cron script (plan 04) both consume what lands here.

Purpose: Satisfy INGEST-01, INGEST-02, INGEST-03 against the test scaffolds from plan 01. Resolve RESEARCH R2
by updating the v1.44 envelope pins at tests/test_history_cli.py:862-921. Honor SAFE-11 by limiting edits to
the allowlisted files (history.py + tests).

Output: Phase 219 envelope reaches every downstream consumer; the Wave 0 bucketed tests turn from xfailed to
green; v1.44 envelope-pinning tests are updated and remain green; no controller-path file is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md
@.planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md
@src/wanctl/history.py
@src/wanctl/storage/reader.py
@tests/test_history_cli.py
@CLAUDE.md

<interfaces>
<!-- Extracted directly from history.py + RESEARCH.md §Code Map A and §Implementation Approach -->

Existing argparse anchor (history.py:558-567):
```python
filter_group.add_argument("--ingestion-rate", dest="ingestion_rate", action="store_true", ...)
```

Existing helper kept as-is (history.py:651-692):
```python
def _per_wan_ingestion_rate(db_paths, *, start_ts, end_ts, wan, metrics) -> tuple[list[dict], int]
# Returns rows shaped {wan_db, wan_name, row_count, window_seconds, rows_per_sec}
```

Existing handler branch (history.py:699-716): `if args.ingestion_rate:` — this is the rewrite point.

Existing formatter (history.py:458-489): `format_ingestion_rate_json(rows)` — REPLACE with envelope formatter.

Existing time-range resolver (history.py:616-624): `_resolve_time_range(args)` — sibling to NEW `_resolve_rolling_windows(args, now)`.

Reader pattern (src/wanctl/storage/reader.py:94, :483): `sqlite3.connect(f"file:{db_path}?mode=ro", uri=True)` — reuse verbatim.

Reader count helper (src/wanctl/storage/reader.py:467-503): `count_metrics(...)` is the fallback per-table read path; preferred is the single-pass `GROUP BY metric_name` query inlined in the new helper (RESEARCH §"Single SQL pass" Alternative).

New PUBLIC helper signature (per RESEARCH R3 — no leading underscore; operator_summary imports it):
```python
def per_wan_ingestion_rate_bucketed(
    db_paths: list[Path],
    *,
    start_ts: int,
    end_ts: int,
    wan: str | None,
) -> tuple[list[dict], int]:
    """Per-WAN x per-metric_name rows. Per-metric failure -> null row.
    Returns (rows, failures) where rows carry: wan_db, wan_name, table_name,
    window_seconds, row_count, rows_per_sec.
    """
```

JSON envelope contract (CONTEXT.md §JSON output shape):
```json
{"schema_version": 1, "rows": [{"wan_name": ..., "wan_db": ..., "table_name": ...|null,
  "window_seconds": int, "row_count": int|null, "rows_per_sec": float|null,
  "_snapshot_unix": int, "_snapshot_age_sec": int}, ...]}
```

Time import (per RESEARCH R4): use `import time` and `time.time()` — NOT `datetime.now().timestamp()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per_wan_ingestion_rate_bucketed() + _resolve_rolling_windows() helpers</name>
  <read_first>
    - src/wanctl/history.py (full file; locate `_per_wan_ingestion_rate` at :651-692 and `_resolve_time_range` at :616-624 — new helpers sit beside these)
    - src/wanctl/storage/reader.py (lines 80-120 and 460-510; confirm `sqlite3.connect(f"file:{db}?mode=ro", uri=True)` URI form and `count_metrics` shape)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Step 2: Bucketed helper" (lines 278-338; the reference implementation) AND §"Step 3: Rolling window resolver" (lines 341-358)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"JSON output shape" (row field contract)
    - tests/test_history_ingestion_rate_bucketed.py (from plan 01; the tests this helper makes pass)
  </read_first>
  <behavior>
    - per_wan_ingestion_rate_bucketed runs ONE indexed SQL pass per WAN db: `SELECT metric_name, COUNT(*) FROM metrics WHERE timestamp BETWEEN ? AND ? [AND wan_name = ?] GROUP BY metric_name ORDER BY metric_name`
    - Per-db DatabaseError or OSError → one null row for that WAN (`table_name=None, row_count=None, rows_per_sec=None`), increment failures counter, do NOT raise
    - Empty window → single row with `row_count=0, rows_per_sec=0.0, table_name=None`
    - window_seconds = max(end_ts - start_ts, 1) (clamp to 1 to avoid div-by-zero)
    - WAN name derivation: `db_path.stem.removeprefix("metrics-")` (matches existing _per_wan_ingestion_rate pattern)
    - _resolve_rolling_windows: if args.rolling is set, parse comma-list of int seconds; each entry must be a positive int; ValueError → argparse.ArgumentTypeError with helpful message. If args.rolling is None/empty, fall back to single window from _resolve_time_range(args)
  </behavior>
  <action>
    Add `import time` to history.py imports if not already present (RESEARCH R4). Add `import sqlite3` only if not already imported.

    Implement `per_wan_ingestion_rate_bucketed` as a PUBLIC function (no leading underscore per RESEARCH R3 —
    `operator_summary.py` imports it in plan 03). Place it directly after the existing `_per_wan_ingestion_rate` at line 692. Match RESEARCH §"Step 2: Bucketed helper" verbatim for the SQL pass + per-metric tolerance flow.
    The helper MUST use the single-pass `GROUP BY metric_name` query per RESEARCH §Alternatives Considered
    "Single SQL pass preferred" — do not loop calling `count_metrics(metrics=[t], ...)` (the N-call fallback is
    rejected in CONTEXT discretion since it doubles query latency).

    Implement `_resolve_rolling_windows(args, now: int) -> list[tuple[int, int, int]]` directly after
    `_resolve_time_range` at :624. Returns list of `(start_ts, end_ts, window_seconds)` tuples. Single-window
    fallback when `args.rolling` is None/empty. Per RESEARCH §Step 3.

    Reject negative or zero window sizes in _resolve_rolling_windows with `argparse.ArgumentTypeError("--rolling values must be positive integers")`.

    DO NOT modify the existing `_per_wan_ingestion_rate` function. DO NOT modify `_resolve_time_range`.

    DO NOT touch the handler branch yet (that's Task 3) or the formatter (Task 2). Each task is independently verifiable.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py::TestIngestionRateBucketed::test_by_table_emits_per_metric_row tests/test_history_ingestion_rate_bucketed.py::TestIngestionRateBucketed::test_per_metric_read_failure_emits_null_row -x 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^def per_wan_ingestion_rate_bucketed" src/wanctl/history.py` returns one line (public name; no leading underscore per R3)
    - `grep -n "^def _resolve_rolling_windows" src/wanctl/history.py` returns one line
    - `grep -c "GROUP BY metric_name" src/wanctl/history.py` returns at least 1 (single-pass SQL)
    - `grep -c "^import time$\\|^from time " src/wanctl/history.py` returns at least 1
    - `.venv/bin/ruff check src/wanctl/history.py` exits 0
    - `.venv/bin/mypy src/wanctl/history.py` exits 0 (or matches existing mypy baseline for the file)
    - Helper does NOT call `count_metrics` in a per-metric loop (verify: `grep -A 30 "def per_wan_ingestion_rate_bucketed" src/wanctl/history.py | grep -c "count_metrics(" | grep -v '^#' ` returns 0)
  </acceptance_criteria>
  <done>Public helper + window resolver compile, ruff/mypy clean, the two single-helper unit tests turn green (xfail flips).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --by-table + --rolling argparse flags and the envelope JSON formatter</name>
  <read_first>
    - src/wanctl/history.py (lines 555-575 for the `filter_group.add_argument("--ingestion-rate", ...)` anchor; lines 455-495 for the `format_ingestion_rate_json` and `format_ingestion_rate_table` functions to replace/extend)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Step 1: Argparse extension" (lines 250-276; verbatim flag specs) AND §"Step 5: Envelope formatter" (lines 401-419)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-CONTEXT.md §"JSON output shape" (locked envelope contract)
  </read_first>
  <behavior>
    - argparse accepts `--by-table` (store_true) and `--rolling=SECS,SECS,...` (str)
    - `--by-table` and `--rolling` without `--ingestion-rate` → `parser.error("--by-table and --rolling require --ingestion-rate")` via post-parse validation. This guard lives in Task 2 (alongside the flag definitions) — Task 3 must NOT re-add it
    - format_ingestion_rate_envelope_json returns `{"schema_version": 1, "rows": [...]}` as a JSON string (indent=2)
    - Each emitted row carries the locked 8-field set exactly; no extra fields, no missing fields
    - _snapshot_age_sec is `max(0, int(time.time()) - snapshot_unix)` computed at emit time
    - table_name field is always present (None when --by-table is absent)
  </behavior>
  <action>
    Add the two argparse flags inside the existing `filter_group` block (after the `--ingestion-rate` add_argument at history.py:558-567). Use the verbatim spec from RESEARCH §Step 1 (`dest="by_table", action="store_true"` and `dest="rolling", metavar="SECS,SECS,...", type=str, default=None`).

    Implement the mutual-exclusion guard NOW (do not defer to Task 3). After `args = parser.parse_args(argv)` in `main()` (grep for existing `parser.error` post-parse patterns to find the analog site), add:
    ```python
    if (args.by_table or args.rolling) and not args.ingestion_rate:
        parser.error("--by-table and --rolling require --ingestion-rate")
    ```

    Replace `format_ingestion_rate_json(rows)` with `format_ingestion_rate_envelope_json(rows: list[dict], snapshot_unix: int) -> str` per RESEARCH §Step 5. Each row in the output MUST contain exactly the 8 locked keys: `wan_name`, `wan_db`, `table_name`, `window_seconds`, `row_count`, `rows_per_sec`, `_snapshot_unix`, `_snapshot_age_sec`. Use `.get("table_name")` to default to None when callers (the legacy path) do not set it. `json.dumps(..., indent=2)`.

    The old `format_ingestion_rate_json` is removed; any internal call sites are updated by Task 3.

    Extend `format_ingestion_rate_table` (history.py:438-455) to render `table_name` and `window_seconds` columns when present. Suppression rule (pinned concrete to avoid drift): when `all(r.get("table_name") is None for r in rows) and len({r["window_seconds"] for r in rows}) <= 1`, drop the `table_name` and `window_seconds` columns (legacy default-mode output stays narrow). Use `tabulate` consistent with the existing pattern.

    DO NOT modify the handler branch yet (Task 3 wires this all together).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py::TestIngestionRateBucketed::test_schema_version_pinned -x 2>&amp;1 | tail -10 ; .venv/bin/python -c "from wanctl.history import format_ingestion_rate_envelope_json; import json; out = format_ingestion_rate_envelope_json([{'wan_name':'a','wan_db':'/x','table_name':None,'window_seconds':60,'row_count':10,'rows_per_sec':0.16}], 1767225660); d=json.loads(out); assert d['schema_version']==1 and d['rows'][0]['_snapshot_unix']==1767225660; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"--by-table"' src/wanctl/history.py` returns 1
    - `grep -c '"--rolling"' src/wanctl/history.py` returns 1
    - `grep -n "^def format_ingestion_rate_envelope_json" src/wanctl/history.py` returns one line
    - `grep -c "format_ingestion_rate_json\\b" src/wanctl/history.py` returns 0 (old name fully removed)
    - The inline python smoke command in `<verify>` prints `OK`
    - `.venv/bin/ruff check src/wanctl/history.py` exits 0
    - `wanctl-history --help` (run via `.venv/bin/python -m wanctl.history --help`) lists both `--by-table` and `--rolling` in the help text
    - Help text for `--by-table` mentions "per-metric_name" or "per-table" (operator-disambiguating per RESEARCH R1)
    - Mutual-exclusion guard wired: `.venv/bin/python -m wanctl.history --by-table 2>&1 | grep -c 'requires --ingestion-rate'` returns at least 1 AND the command's exit code is non-zero. Repeat with `--rolling=60` → also non-zero with the same error.
    - Table-suppression smoke (legacy default-mode stays narrow): `.venv/bin/python -c "from wanctl.history import format_ingestion_rate_table; out = format_ingestion_rate_table([{'wan_name':'spectrum','wan_db':'/x','table_name':None,'window_seconds':60,'row_count':10,'rows_per_sec':0.16}]); assert 'table_name' not in out and 'window_seconds' not in out; print('OK')"` prints `OK`
  </acceptance_criteria>
  <done>Both flags parse, envelope formatter emits the 8-key contract exactly, schema_version test turns green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire handler branch + update tests/test_history_cli.py envelope pins</name>
  <read_first>
    - src/wanctl/history.py (lines 695-720; the `_handle_special_query` block that currently routes `--ingestion-rate`)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §"Step 4: Handler composition" (lines 361-398; the rewrite reference) AND §"Test Strategy" §"Companion tests to cover" (lines 545-549; for the back-compat decision)
    - .planning/phases/219-ingestion-rate-observability-scope-d/219-RESEARCH.md §Risks R2 (lines about tests/test_history_cli.py:862-921 envelope pins needing replacement)
    - tests/test_history_cli.py (lines 800-1100; the full TestIngestionRateCli class; identify every existing envelope assertion at :862-921 that needs replacement)
  </read_first>
  <behavior>
    - --ingestion-rate alone → unchanged from v1.44 EXCEPT JSON now uses the envelope (schema_version+staleness fields per CONTEXT discretion §3)
    - --ingestion-rate --by-table → per-metric rows, single window from --last/--from/--to
    - --ingestion-rate --rolling=60,300 → 1 row per WAN per window (table_name=None)
    - --ingestion-rate --by-table --rolling=60,300 → cartesian product (per WAN x per metric_name x per window)
    - --by-table or --rolling without --ingestion-rate → exit code != 0 with helpful error
    - all WAN DBs fail to read AND filtered_paths is non-empty → exit code 1, stderr "All metrics databases failed to read."
    - snapshot_unix captured ONCE at branch entry; carried to every emitted row
  </behavior>
  <action>
    Rewrite the `if args.ingestion_rate:` branch in `_handle_special_query` per RESEARCH §Step 4 (lines 361-398).
    Key elements:
      - `snapshot_unix = int(time.time())` captured once at branch entry
      - `windows = _resolve_rolling_windows(args, now=snapshot_unix)`
      - Per-window loop: if `args.by_table` → call `per_wan_ingestion_rate_bucketed(filtered_paths, start_ts=W[0], end_ts=W[1], wan=args.wan)`; else → call existing `_per_wan_ingestion_rate(...)` and `r.setdefault("table_name", None)` on each returned row to satisfy the envelope contract
      - `all_rows.extend(rows)` accumulates across windows
      - Total-failure check: `if filtered_paths and total_failures == len(filtered_paths) * len(windows): print("All metrics databases failed to read.", file=sys.stderr); return 1`
      - Emit: `if args.json_output: print(format_ingestion_rate_envelope_json(all_rows, snapshot_unix))` else table formatter
      (NB: the mutual-exclusion guard for `--by-table`/`--rolling` without `--ingestion-rate` is implemented in Task 2 — do NOT re-add it here.)

    Update EVERY assertion in `tests/test_history_cli.py::TestIngestionRateCli` (class spans roughly :809-1077 per plan 01's analysis; the v1.44 envelope pins are concentrated around :862-921 but other assertions outside that range may also reference the legacy keys) that references the v1.44 envelope keys — i.e. any assertion reading `payload["window"]`, `payload["totals"]`, `payload["wans"]`, or `payload["generated_at"]`. Use a content-based scope (not a line-range scope) so v1.44-shape assertions outside :862-921 are not missed.
    Per RESEARCH R2 + CONTEXT discretion §3 ("recommended yes for consistency"): the default-mode `--ingestion-rate --json` output now carries `schema_version: 1`, `_snapshot_unix`, `_snapshot_age_sec`, and rows live under `payload["rows"]` rather than the old top-level list. Rewrite every matching assertion to:
      - assert `payload["schema_version"] == 1`
      - assert `isinstance(payload["rows"], list)`
      - iterate `payload["rows"]` (not `payload` directly) for per-row field assertions
      - assert each row contains `_snapshot_unix` and `_snapshot_age_sec`
      - assert `row["table_name"] is None` (default mode)
      - keep existing assertions about `wan_name`, `wan_db`, `window_seconds`, `row_count`, `rows_per_sec` but read them from `payload["rows"][i]`

    DO NOT delete existing tests; only update the assertions to the new envelope. Preserve test method names so test selection commands in operator runbooks remain stable.

    Also: pin `time.time` in any v1.44 test that asserts `_snapshot_unix` deterministically (use the `BASE_TS` pattern from plan 01's test if a similar anchor is needed — or `monkeypatch.setattr("wanctl.history.time.time", lambda: 1767225600.0)`).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py tests/test_history_cli.py::TestIngestionRateCli -x 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py -x` shows ALL 6 test methods passing (none xfailed, none errored)
    - `.venv/bin/pytest tests/test_history_cli.py::TestIngestionRateCli -x` exits 0 (updated envelope pins green)
    - Content-based scope check: `grep -cE "payload\[['\"](window|totals|wans|generated_at)['\"]\]" tests/test_history_cli.py` returns 0 (every v1.44 envelope-key reference in TestIngestionRateCli has been rewritten — no stragglers outside the original :862-921 range)
    - `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0 (hot-path regression slice; SAFE-11 sanity check)
    - `.venv/bin/python -m wanctl.history --ingestion-rate --by-table --json --help 2>&1 | grep -q -- "--by-table"` exits 0
    - `grep -c "snapshot_unix = int(time.time())" src/wanctl/history.py` returns at least 1 (single-capture pattern)
    - `.venv/bin/ruff check src/wanctl/history.py tests/test_history_cli.py` exits 0
    - `.venv/bin/mypy src/wanctl/history.py` exits 0 (or matches existing baseline)
    - `git diff --stat src/wanctl/wan_controller.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py` shows zero changed lines (SAFE-11 sanity)
    - `.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x` exits 0
  </acceptance_criteria>
  <done>Full Phase 219 CLI surface live; bucketed tests green; v1.44 envelope tests updated and green; hot-path slice unchanged; mutation-boundary green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → history.py | Untrusted CLI input crosses argparse; `--rolling` values pass through int() conversion and a positivity guard |
| metrics DB file path → sqlite3 | DB paths are server-local (`/var/lib/wanctl/metrics-*.db`); not user-supplied at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-02 | Tampering / Injection | `--rolling` argparse value | mitigate | `int(s.strip())` per element + positivity check via `argparse.ArgumentTypeError` rejects non-int and negative inputs (T-219-02 from planner context) |
| T-219-04 | Information Disclosure | metrics DB filenames in `wan_db` JSON field | accept | Field is intentional (operator visibility); DB paths are operational, not secret; matches v1.44 Phase 208 contract |
| T-219-05 | Denial of Service | unbounded `--rolling` window count | mitigate | Cap `len(rolling_secs)` at 16 in `_resolve_rolling_windows` with a clear error message; documented as a parser-level guard |
</threat_model>

<verification>
After all three tasks:

```bash
.venv/bin/pytest tests/test_history_ingestion_rate_bucketed.py tests/test_history_cli.py -x
.venv/bin/pytest tests/test_phase219_mutation_boundary.py -x
.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
.venv/bin/ruff check src/wanctl/history.py tests/test_history_cli.py tests/test_history_ingestion_rate_bucketed.py
.venv/bin/mypy src/wanctl/history.py
```

All five commands MUST exit 0.

CLI smoke (manual but quick):

```bash
.venv/bin/python -m wanctl.history --ingestion-rate --by-table --rolling=60,300 --json --last 600 --db /tmp/none.db 2>&1 | head
```

This should fail with a metrics-db-not-found error (not an argparse error) — proves the flags parse correctly.
</verification>

<success_criteria>
- Phase 219 CLI surface (--by-table + --rolling) implemented additively in src/wanctl/history.py
- JSON envelope is the locked {schema_version: 1, rows: [...]} shape with _snapshot_unix and _snapshot_age_sec on every row
- per_wan_ingestion_rate_bucketed is PUBLIC (no leading underscore) so plan 03 can import it
- Per-metric_name read failure tolerance: null row instead of abort
- v1.44 envelope pins at tests/test_history_cli.py:862-921 updated; tests green
- Hot-path regression slice still green (SAFE-11 sanity)
- mypy + ruff clean on touched files
- INGEST-01, INGEST-02, INGEST-03 covered by green automated tests
</success_criteria>

<output>
Create `.planning/phases/219-ingestion-rate-observability-scope-d/219-02-SUMMARY.md`. Document the v1.44 envelope
break (R2 resolution) explicitly so milestone consumers see the contract change without reading the test diff.
</output>
