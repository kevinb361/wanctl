---
phase: 231-migration-held-criteria-rollback-verification-doc-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase231-migration-held.sh
  - tests/test_phase231_migration_held.py
  - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK01-EVIDENCE.md
autonomous: false
requirements: [SOAK-01]

must_haves:
  truths:
    - "Formal migration-held criteria (bridge health, metrics-DB ingestion, no sustained service errors, qdisc within configured envelope) are defined BEFORE evaluation, with explicit thresholds and an explicit soak window anchored to the 2026-06-08 migration"
    - "The criteria are evaluated against LIVE evidence for BOTH WANs (spectrum and att) via strictly read-only channels — no systemctl start/stop/restart, no tc qdisc replace, no writes on the target"
    - "Per-WAN PASS/FAIL verdicts with raw captured evidence are committed as a phase artifact"
  artifacts:
    - path: "scripts/phase231-migration-held.sh"
      provides: "Read-only migration-held evaluator: per-WAN criteria checks emitting machine-readable JSON"
      contains: "migration-held"
    - path: ".planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK01-EVIDENCE.md"
      provides: "SOAK-01 evidence: formal criteria table + live per-WAN evaluation with capture timestamps"
      contains: "SOAK-01"
    - path: "tests/test_phase231_migration_held.py"
      provides: "Regression proof the evaluator constructs only read-only remote commands"
      contains: "phase231-migration-held"
  key_links:
    - from: "scripts/phase231-migration-held.sh"
      to: "configs/cake-autorate/config.spectrum.sh + config.att.sh"
      via: "qdisc envelope criterion sourced from repo-committed min/base/max shaper rates"
      pattern: "min_dl_shaper_rate_kbps"
---

<objective>
Define the formal "migration held" criteria for the 2026-06-08 both-WAN cake-autorate migration and evaluate them against live evidence for both WANs (SOAK-01). Two deliverables:

1. A read-only evaluator script `scripts/phase231-migration-held.sh` that checks, per WAN: (a) bridge health endpoint healthy, (b) metrics-DB ingestion present in window, (c) no sustained service errors across the live external-mode units, (d) DL CAKE qdisc bandwidth within the configured envelope and UL CAKE bandwidth at the fixed base rate — and emits per-WAN PASS/FAIL JSON with raw evidence values.

2. A committed evidence artifact `231-SOAK01-EVIDENCE.md` containing the formal criteria table (thresholds + soak window, defined before evaluation) and the live both-WAN evaluation output with capture timestamps.

Everything live-host is READ-ONLY: ssh + curl + journalctl + `tc qdisc show` + sqlite3 SELECT (via `sudo -n` where needed). NO `systemctl start/stop/restart`, NO `tc qdisc replace`, NO writes on cake-shaper.

Operator/credential note (per MEMORY): if a privileged read (`journalctl -p err`, `sudo -n sqlite3`) is denied, do NOT escalate credentials — hand Kevin a `! <command>` to run at the keyboard and paste back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-RESEARCH.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-PATTERNS.md

<interfaces>
Live targets (already committed in repo — soak-monitor.sh TARGETS array):
  spectrum: ssh kevin@10.10.110.223, health http://10.10.110.223:9101/health
  att:      ssh kevin@10.10.110.223, health http://10.10.110.227:9101/health
Live external-mode units per WAN (FUTURE doc "Live post-migration state"):
  spectrum: cake-autorate-spectrum.service, cake-autorate-spectrum-state-bridge.service, silicom-bypass-watchdog@spectrum.service
  att:      cake-autorate-att.service, cake-autorate-att-state-bridge.service, silicom-bypass-watchdog-cake-autorate-att.service
Metrics DBs (bridge defaults / unit env): /var/lib/wanctl/metrics-spectrum.db, /var/lib/wanctl/metrics-att.db
Qdisc envelope source of truth (repo == deployed per Phase 229 DEPLOY-02):
  configs/cake-autorate/config.spectrum.sh: DL 500000/550000/600000 kbps autorate; UL fixed base (adjust_ul_shaper_rate=0)
  configs/cake-autorate/config.att.sh:      DL 60000/95000/100000 kbps autorate; UL fixed base (adjust_ul_shaper_rate=0)
  Read the UL base values from the config files at execution time — do not hardcode from this plan.
Interfaces: spectrum DL=spec-router UL=spec-modem; att DL=att-router UL=att-modem
Migration anchor: 2026-06-08 (commit fc47a0c live date). Soak window for criteria: since 2026-06-08T00:00Z for service-error scan; trailing 24h minimum for ingestion-rate check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration-held criteria definition + read-only evaluator script + regression test</name>
  <files>scripts/phase231-migration-held.sh, tests/test_phase231_migration_held.py</files>
  <read_first>
    - scripts/soak-monitor.sh (SSH invocation shape, TARGETS tuple array, external-mode live-unit selection, --json conventions, HAS_JQ guard)
    - scripts/phase227-qdisc-verify.sh (fail-closed exit-code discipline: only fully-proven state exits 0)
    - configs/cake-autorate/config.spectrum.sh and configs/cake-autorate/config.att.sh (envelope values the script must parse — source the thresholds from these files, not hardcoded copies)
    - tests/test_soak_monitor_att_coverage.py (script-source text assertions + fake-ssh shim pattern)
    - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-PATTERNS.md
  </read_first>
  <action>
    Create `scripts/phase231-migration-held.sh` (bash, `set -euo pipefail`, shellcheck-clean at `-S error`):

    Criteria (the formal SOAK-01 definition — encode as named checks with explicit thresholds):
    - C1 bridge_health: GET the per-WAN health endpoint; PASS iff HTTP 200 and payload `status` == `healthy`.
    - C2 metrics_ingestion: read-only row count from the per-WAN metrics DB over the trailing window (default 24h): `ssh <target> "sudo -n sqlite3 -readonly /var/lib/wanctl/metrics-<wan>.db 'SELECT COUNT(*) FROM <metrics table> WHERE <ts column> >= strftime(\"%s\",\"now\",\"-24 hours\")'"`. Discover the actual table/ts-column names from `deploy/scripts/cake-autorate-spectrum-state-bridge` (the bridge's INSERT statements) before writing the query. PASS iff count > 0.
    - C3 no_sustained_errors: `journalctl -u <each live unit> --since "2026-06-08" -p err --no-pager | wc -l` per WAN's live unit set (use `sudo -n` if needed). PASS iff zero err-priority lines, or any present lines are individually listed in the evidence and assessed non-sustained (a non-zero count makes the check report its raw lines so the operator checkpoint can judge "sustained").
    - C4 qdisc_envelope: parse `tc qdisc show dev <dl_if>` and `tc qdisc show dev <ul_if>` over SSH; extract CAKE `bandwidth`. PASS iff DL bandwidth within [min_dl, max_dl] kbps and UL bandwidth equals the fixed base UL rate, with envelope values parsed from `configs/cake-autorate/config.<wan>.sh` (the script reads the repo config files locally for thresholds — repo==deployed proven by Phase 229).

    Script contract:
    - `--wan {spectrum|att|all}` (default all), `--window-hours N` (default 24, C2 only), `--json` output mode.
    - Per-WAN JSON object: `{wan, captured_utc, checks: {bridge_health: {pass, evidence}, metrics_ingestion: {pass, rows, window_hours}, no_sustained_errors: {pass, err_lines, units}, qdisc_envelope: {pass, dl_kbps, dl_min, dl_max, ul_kbps, ul_expected}}, verdict: "PASS"|"FAIL"}`.
    - Fail-closed: SSH failure, curl failure, missing DB, or unparseable qdisc → that check `pass: false` and overall verdict FAIL; script exits non-zero if any WAN verdict is FAIL.
    - STRICTLY read-only remote command construction: the only remote commands are `curl`, `journalctl`, `tc qdisc show`, `sqlite3 -readonly ... SELECT`, `systemctl is-active`/`is-enabled`, `cat`. No `systemctl start|stop|restart|enable|disable`, no `tc qdisc replace|add|del`, no shell redirection writing on the target.

    Create `tests/test_phase231_migration_held.py` (pytest, no live SSH):
    - assert script source contains no mutating verbs: regex assert that `systemctl (start|stop|restart|enable|disable)` and `tc qdisc (replace|add|del)` do NOT appear as executed remote commands in the script text.
    - assert the four criteria check names and the `--wan`/`--json`/`--window-hours` flags appear in the script.
    - assert envelope values are sourced by parsing `configs/cake-autorate/config.*.sh` (e.g., the literal `min_dl_shaper_rate_kbps` parse appears) rather than hardcoded rate numbers for both WANs.
    - assert fail-closed behavior textually: nonzero exit path on failed check exists (e.g., `exit 1` after FAIL verdict aggregation).

    Verify: `shellcheck -S error scripts/phase231-migration-held.sh` exits 0; `.venv/bin/pytest tests/test_phase231_migration_held.py -q` passes; `bash scripts/phase231-migration-held.sh --help` prints usage and exits 0 without contacting any host.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && shellcheck -S error scripts/phase231-migration-held.sh && .venv/bin/pytest tests/test_phase231_migration_held.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase231-migration-held.sh` exists, is executable, and `shellcheck -S error` exits 0.
    - Script source contains the literal check names `bridge_health`, `metrics_ingestion`, `no_sustained_errors`, `qdisc_envelope` and flags `--wan`, `--json`, `--window-hours`.
    - Script parses envelope thresholds from `configs/cake-autorate/config.spectrum.sh` / `config.att.sh` (source text contains `min_dl_shaper_rate_kbps`), not hardcoded per-WAN rate literals for the envelope bounds.
    - `tests/test_phase231_migration_held.py` exists and `.venv/bin/pytest tests/test_phase231_migration_held.py -q` exits 0.
    - No remote mutation commands in the script: grep finds no executed `systemctl start/stop/restart/enable/disable` and no `tc qdisc replace/add/del` remote payloads.
    - `bash scripts/phase231-migration-held.sh --help` exits 0 with no network access.
  </acceptance_criteria>
  <done>The formal criteria are encoded in a shellcheck-clean, read-only, fail-closed evaluator with a passing focused regression test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live both-WAN migration-held evaluation + SOAK-01 evidence artifact</name>
  <files>.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK01-EVIDENCE.md</files>
  <read_first>
    - scripts/phase231-migration-held.sh (the evaluator just built — Task 1 state)
    - .planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md (evidence artifact format: Captured/Scope/Verdict header, Command + Captured-output fenced pairs, Finding paragraphs)
  </read_first>
  <action>
    Run the live evaluation and record SOAK-01 evidence. All live-host contact READ-ONLY.

    Step A: run `bash scripts/phase231-migration-held.sh --wan all --json | python3 -m json.tool` against the live cake-shaper. Capture full output verbatim with UTC timestamp.

    Step B (corroboration, read-only): capture `./scripts/soak-monitor.sh --json` aggregate output (Phase 230 generalized scan) and `systemctl is-active` statuses for all six live external-mode units plus `wanctl@spectrum.service wanctl@att.service` (expected: six active, two inactive) — proving external mode is the live reality the criteria were evaluated against.

    Step C (credential handling): if any privileged read is denied, hand Kevin the exact `! <command>` to run and paste back; do not escalate credentials; record operator-pasted output as such in the evidence.

    Step D: write `231-SOAK01-EVIDENCE.md` mirroring the 230-MON01-EVIDENCE.md shape:
    - Header: Captured (UTC), Scope ("read-only live evaluation, both WANs"), Verdict line.
    - Section "Formal Criteria" — table of C1–C4 with thresholds and the soak window (service-error scan since 2026-06-08; ingestion window 24h), stated as defined BEFORE evaluation.
    - Per-WAN sections with Command + Captured-output fenced pairs for the evaluator JSON and corroboration captures.
    - Closing verdict containing the literal `SOAK-01 PASS` (or `SOAK-01 FAIL` with the failing checks listed — do not massage a FAIL into a PASS; a FAIL pauses at this checkpoint for operator decision).
    - Use only hosts/IPs/units already committed in the repo.

    This task is a human-verify checkpoint: pause for operator confirmation that the criteria are sensible, the live evidence is genuine, and the per-WAN verdicts are correct before committing the evidence.
  </action>
  <verify>
    <human-check>Operator confirms the formal criteria table is correct, both WAN evaluations ran against live endpoints (timestamps present), all four checks per WAN carry raw evidence values, and the final verdict line is justified.</human-check>
  </verify>
  <acceptance_criteria>
    - `231-SOAK01-EVIDENCE.md` exists and contains the literal strings `bridge_health`, `metrics_ingestion`, `no_sustained_errors`, `qdisc_envelope`, a "Formal Criteria" section, and per-WAN captured JSON for both `spectrum` and `att`.
    - Evidence records capture timestamps (UTC) and the six-active/two-inactive unit status corroboration.
    - The verdict line contains `SOAK-01 PASS` or `SOAK-01 FAIL` with failing checks enumerated.
    - No mutating command appears anywhere in the captured command log.
    - Operator types "approved" (or describes what is missing).
  </acceptance_criteria>
  <done>SOAK-01 evidence is committed: formal criteria defined first, evaluated against live both-WAN evidence via read-only channels, with an operator-confirmed verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box -> cake-shaper (ssh/curl) | Live evaluation crosses this read-only; journal/tc/sqlite reads only |
| repo configs -> criteria thresholds | Envelope thresholds sourced from committed configs (repo==deployed per Phase 229) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-231-01 | Tampering | live evaluation run | mitigate | Evaluator constructs only read-only remote commands; regression test asserts no mutating verbs; fail-closed on any anomaly |
| T-231-02 | Elevation of Privilege | sudo -n journal/sqlite reads | mitigate | `sudo -n` only; on denial hand operator a `! <command>` instead of escalating creds (MEMORY) |
| T-231-03 | Information Disclosure | committed evidence | mitigate | Only repo-committed IPs/units/paths appear in evidence |
| T-231-04 | Repudiation | migration-held claim | mitigate | Criteria defined before evaluation; raw outputs + UTC timestamps committed; FAIL is reported honestly and pauses at checkpoint |
</threat_model>

<verification>
- `shellcheck -S error scripts/phase231-migration-held.sh` exits 0.
- `.venv/bin/pytest tests/test_phase231_migration_held.py -q` passes.
- `231-SOAK01-EVIDENCE.md` contains the formal criteria table, both-WAN live captures with timestamps, and an operator-approved verdict line.
- Hot-path regression slice still green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
</verification>

<success_criteria>
- SOAK-01: formal migration-held criteria are defined and evaluated against live evidence for both WANs (bridge health, metrics-DB ingestion, no sustained service errors, qdisc within the configured envelope) with committed evidence and per-WAN verdicts.
</success_criteria>

<output>
Create `.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-01-SUMMARY.md` when done.
</output>
