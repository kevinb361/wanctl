---
phase: 231-migration-held-criteria-rollback-verification-doc-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase231-rollback.sh
  - tests/test_phase231_rollback.py
  - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md
autonomous: false
requirements: [SOAK-02]

must_haves:
  truths:
    - "Rollback to native wanctl@{wan} is verified by the requirement's provable path: a documented, preflighted procedure with evidence captured — completable WITHOUT production mutation"
    - "A live rollback exercise is offered to the operator as an explicit opt-in checkpoint; it NEVER runs without operator approval, and declining it still satisfies SOAK-02 via the provable path"
    - "The rollback script refuses all mutation without --confirm; dry-run and preflight modes are strictly read-only"
    - "Preflight proves rollback preconditions on the live host: native unit + config + code present, native units disabled/inactive (no dual-writer), Conflicts= guard present in cake-autorate units"
  artifacts:
    - path: "scripts/phase231-rollback.sh"
      provides: "Parameterized per-WAN rollback script: --preflight (read-only proof), --dry-run (render plan), --confirm (gated mutation)"
      contains: "--confirm"
    - path: ".planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md"
      provides: "SOAK-02 evidence: documented procedure, live preflight proof both WANs, historical exercise citation, operator decision record"
      contains: "SOAK-02"
    - path: "tests/test_phase231_rollback.py"
      provides: "Regression proof of --confirm gating and per-WAN command rendering"
      contains: "phase231-rollback"
  key_links:
    - from: "scripts/phase231-rollback.sh"
      to: ".planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md"
      via: "ATT rollback command block (lines ~1136-1150) is the procedure source of truth"
      pattern: "silicom-bypass-watchdog@att.service"
---

<objective>
Verify rollback to native `wanctl@{wan}` per SOAK-02. The requirement offers two paths; this plan defaults to the second and offers the first as an operator-gated option:

(a) exercised on one WAN under operator approval — production mutation, NEVER without explicit GO;
(b) trivially provable via a documented, preflighted procedure with evidence captured — the autonomous-completable default.

Deliverables:
1. `scripts/phase231-rollback.sh --wan {spectrum|att}` modeled on `scripts/phase227-rollback.sh`: `--preflight` (read-only precondition proof emitting JSON), `--dry-run` (render the full ordered rollback command sequence, mutate nothing — this is the default), `--confirm` (required for any real mutation).
2. `231-SOAK02-EVIDENCE.md`: the documented procedure, live read-only preflight proof for BOTH WANs, the 2026-06-05 historical ATT rollback exercise citation, and the operator's decision record (exercise performed or provable path accepted).
3. Operator checkpoint offering the optional live exercise on one WAN — including the return-to-cake-autorate re-migration steps, since cake-autorate is the production steady state.

Project memory guardrails: do NOT wire anything to `scripts/phase226-restore.sh` (dry-run-only proof); `phase227-rollback.sh` is the mutation-capable precedent. RouterOS is not touched by this rollback (cake-shaper local units + qdiscs only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-RESEARCH.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-PATTERNS.md

<interfaces>
Procedure source of truth — ATT rollback block in .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md (~lines 1136-1150):
  disable/stop:  cake-autorate-att.service cake-autorate-att-state-bridge.service silicom-bypass-watchdog-cake-autorate-att.service
  enable/start:  wanctl@att.service silicom-bypass-watchdog@att.service
  bpctl:         /opt/bpctl-silicom bpctl_util att-modem set_disc off / set_bypass off / reset_bypass_wd
  qdisc restore: att-router 95Mbit + att-modem 18Mbit native-era CAKE baselines (exact flags in the block)
Spectrum analog (derive, same shape):
  disable/stop:  cake-autorate-spectrum.service cake-autorate-spectrum-state-bridge.service
  enable/start:  wanctl@spectrum.service (silicom-bypass-watchdog@spectrum.service stays — it already watches per-instance)
  qdisc restore: spec-router 550Mbit + spec-modem 18Mbit native-era CAKE baselines (flags per FUTURE doc trial restore sections; native wanctl@spectrum re-applies rates after start)
Re-migration (return to cake-autorate after an exercised rollback) is the exact inverse:
  disable/stop native units, enable/start the cake-autorate trio, qdisc re-init via /usr/local/sbin/cake-autorate-<wan>-qdisc-init (ExecStartPre handles this on unit start)
Preflight preconditions (all read-only via ssh kevin@10.10.110.223):
  - systemctl cat wanctl@.service exists; systemctl is-enabled/is-active wanctl@<wan>.service == disabled/inactive
  - test -f /etc/wanctl/<wan>.yaml; test -f /opt/wanctl/autorate_continuous.py
  - systemctl cat cake-autorate-<wan>.service contains "Conflicts=wanctl@<wan>.service"
  - ATT only: test -d /opt/bpctl-silicom; systemctl cat silicom-bypass-watchdog@.service exists
Health endpoints for post-mutation verify (confirm mode only): spectrum http://10.10.110.223:9101/health, att http://10.10.110.227:9101/health
Historical exercise citation: 2026-06-05 ATT trial rollback, documented with restore verification in WANCTL_CAKE_AUTORATE_FUTURE.md ("Rollback performed" section, ~lines 182-189)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gated per-WAN rollback script + confirm-gating regression test</name>
  <files>scripts/phase231-rollback.sh, tests/test_phase231_rollback.py</files>
  <read_first>
    - scripts/phase227-rollback.sh (the pattern to replicate: usage heredoc, DRY_RUN/CONFIRM string flags, print_plan heredoc, require_command, json_string, proof JSON --out, health_check)
    - .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md lines 1122-1151 (live post-migration unit state + exact ATT rollback command block — copy the qdisc restore commands verbatim)
    - deploy/systemd/cake-autorate-att.service and deploy/systemd/cake-autorate-spectrum.service (unit names, Conflicts= guard)
    - tests/test_soak_monitor_att_coverage.py (script-source text assertion style)
  </read_first>
  <action>
    Create `scripts/phase231-rollback.sh` (bash, `set -euo pipefail`, shellcheck-clean at `-S error`):

    Flags: `--wan {spectrum|att}` (required), `--preflight` (read-only precondition checks, emit JSON proof to `--out` path), `--dry-run` (print the full ordered rollback plan + exact commands, mutate nothing; DEFAULT when neither --preflight nor --confirm given), `--confirm` (required for real mutation), `--ssh-host` (default kevin@10.10.110.223), `--out FILE` (proof JSON path).

    Per-WAN command tables (render in print_plan and execute only under --confirm):
    - att: stop/disable `cake-autorate-att.service cake-autorate-att-state-bridge.service silicom-bypass-watchdog-cake-autorate-att.service`; enable/start `wanctl@att.service silicom-bypass-watchdog@att.service`; bpctl sequence (`set_disc off`, `set_bypass off`, `reset_bypass_wd`); `tc qdisc replace` att-router/att-modem to the native baselines copied VERBATIM from the FUTURE doc rollback block.
    - spectrum: stop/disable `cake-autorate-spectrum.service cake-autorate-spectrum-state-bridge.service`; enable/start `wanctl@spectrum.service`; `tc qdisc replace` spec-router 550Mbit / spec-modem 18Mbit native baselines with the production flag set recorded in the FUTURE doc restore-verification sections.

    Preflight mode (read-only, exits non-zero on any failed precondition, JSON proof records each check):
    - native unit template + instance state: `systemctl cat wanctl@.service` succeeds; `systemctl is-enabled wanctl@<wan>.service` == disabled (or not-enabled), `systemctl is-active` == inactive.
    - native config/code present: `test -f /etc/wanctl/<wan>.yaml`, `test -f /opt/wanctl/autorate_continuous.py`.
    - dual-writer guard: `systemctl cat cake-autorate-<wan>.service` output contains `Conflicts=wanctl@<wan>.service`.
    - ATT extras: `test -d /opt/bpctl-silicom`, `systemctl cat silicom-bypass-watchdog@.service` succeeds.
    - rendered rollback command sequence embedded in the proof JSON (the "documented procedure" is machine-recorded, not prose-only).

    Confirm mode (NEVER reached in this plan's autonomous tasks): executes the sequence, then polls the WAN health endpoint until `status: healthy` or timeout, writes proof JSON with before/after unit states. Guard: refuse `--confirm` unless `--i-have-operator-approval` is ALSO passed (belt-and-suspenders against accidental invocation; document in usage).

    Create `tests/test_phase231_rollback.py` (pytest, no live SSH):
    - assert `--confirm` gating: script text shows mutation commands only inside the confirm-gated path; running `bash scripts/phase231-rollback.sh --wan att --dry-run` with a PATH-shimmed fake `ssh` (records invocations, returns canned output) performs ZERO ssh mutation calls — the fake-ssh log contains no `systemctl stop`, `systemctl disable`, `systemctl enable`, `systemctl start`, or `tc qdisc replace` payloads.
    - assert per-WAN rendering: dry-run stdout for att contains `silicom-bypass-watchdog-cake-autorate-att.service`, `wanctl@att.service`, and `att-router`; for spectrum contains `cake-autorate-spectrum-state-bridge.service`, `wanctl@spectrum.service`, and `spec-router`.
    - assert `--confirm` without `--i-have-operator-approval` exits non-zero before any remote call.
    - assert the script does not reference `phase226-restore.sh`.

    Verify: `shellcheck -S error scripts/phase231-rollback.sh` exits 0; focused pytest passes; `bash scripts/phase231-rollback.sh --wan att --dry-run` prints the ordered plan with zero network access when ssh is shimmed out.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && shellcheck -S error scripts/phase231-rollback.sh && .venv/bin/pytest tests/test_phase231_rollback.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase231-rollback.sh` exists, is executable, `shellcheck -S error` exits 0.
    - Usage documents `--preflight`, `--dry-run` (default), `--confirm`, `--i-have-operator-approval`, `--wan`, `--out`.
    - Dry-run for each WAN renders the correct unit trio, native units, and qdisc restore commands (att includes the bpctl sequence; qdisc flags match the FUTURE doc block verbatim for att).
    - `tests/test_phase231_rollback.py` passes: fake-ssh dry-run log shows zero mutation payloads; `--confirm` alone exits non-zero; per-WAN literals assert correctly.
    - Script contains no reference to `phase226-restore.sh`.
  </acceptance_criteria>
  <done>A gated, shellcheck-clean rollback script exists with regression-proven confirm gating and correct per-WAN rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Live read-only preflight both WANs + SOAK-02 evidence artifact (provable path)</name>
  <files>.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md</files>
  <read_first>
    - scripts/phase231-rollback.sh (Task 1 state)
    - .planning/phases/230-soak-monitor-att-coverage/230-MON01-EVIDENCE.md (evidence format)
    - .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md lines 176-198 (the 2026-06-05 exercised ATT rollback record to cite)
  </read_first>
  <action>
    Execute the provable path and record SOAK-02 evidence. All live-host contact READ-ONLY.

    Step A: run `bash scripts/phase231-rollback.sh --wan att --preflight --out <phase-dir>/evidence/rollback-preflight-att.json` and the spectrum equivalent. Capture stdout + proof JSON verbatim. Every precondition must report its raw check output.

    Step B: run `bash scripts/phase231-rollback.sh --wan att --dry-run` and `--wan spectrum --dry-run`; capture the rendered ordered command sequences (this is the documented procedure).

    Step C (credential handling): on any privileged-read denial, hand Kevin a `! <command>` and paste back; never escalate.

    Step D: write `231-SOAK02-EVIDENCE.md` mirroring 230-MON01-EVIDENCE.md shape:
    - Header: Captured (UTC), Scope ("read-only preflight + documented procedure; no production mutation"), Verdict line.
    - Section "Documented Procedure": the per-WAN dry-run rendered sequences.
    - Section "Preflight Proof": both WANs' preflight JSON + per-check raw outputs (native unit present+disabled+inactive, config/code present, Conflicts= guard, ATT bpctl/watchdog extras).
    - Section "Historical Exercise": cite the 2026-06-05 exercised ATT rollback (rollback performed + restore verification) from WANCTL_CAKE_AUTORATE_FUTURE.md as prior live proof of the same procedure shape.
    - Section "Operator Decision": placeholder filled at Task 3 checkpoint (exercise performed on WAN X / provable path accepted).
    - Closing verdict containing the literal `SOAK-02 PROVABLE-PATH PASS` (preconditions all green) or FAIL with the failed preconditions listed.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && grep -q "SOAK-02" .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md && grep -q "Conflicts=wanctl@" .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `231-SOAK02-EVIDENCE.md` exists with Documented Procedure, Preflight Proof (both WANs), Historical Exercise, and Operator Decision sections.
    - Preflight evidence shows for each WAN: `wanctl@<wan>.service` disabled+inactive, `/etc/wanctl/<wan>.yaml` present, `/opt/wanctl/autorate_continuous.py` present, `Conflicts=wanctl@<wan>.service` guard captured; ATT additionally shows bpctl dir + watchdog template present.
    - Proof JSON files exist under the phase evidence dir for both WANs.
    - The captured command log contains zero mutation commands.
    - Verdict line contains `SOAK-02 PROVABLE-PATH PASS` or an honest FAIL.
  </acceptance_criteria>
  <done>The rollback procedure is documented, preflighted against the live host with captured evidence for both WANs, and the historical exercised rollback is cited — the provable path is complete pending operator decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator decision — accept provable path or approve live rollback exercise on one WAN</name>
  <files>.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SOAK02-EVIDENCE.md (Task 2 state)
  </read_first>
  <action>
    Present the operator with the SOAK-02 decision. Default recommendation: ACCEPT THE PROVABLE PATH (preflight green, procedure documented, historical exercise on record) — zero production risk.

    Option 1 (default): operator accepts the provable path. Fill the Operator Decision section: "Provable path accepted — <date>, operator: Kevin". SOAK-02 complete. No mutation occurs.

    Option 2 (opt-in, production mutation): operator explicitly approves a live exercise on ONE named WAN. Only then:
      a. Run `bash scripts/phase231-rollback.sh --wan <chosen> --confirm --i-have-operator-approval --out <phase-dir>/evidence/rollback-exercise-<wan>.json`.
      b. Verify native mode live: `systemctl is-active wanctl@<wan>.service` == active; WAN health endpoint reports healthy; capture outputs.
      c. RETURN TO PRODUCTION STEADY STATE (cake-autorate): stop/disable the native units, re-enable/start the cake-autorate trio for that WAN (qdisc re-init runs via the unit's ExecStartPre), verify health healthy and `./scripts/soak-monitor.sh --json` all-healthy; capture outputs.
      d. Record the full exercise (rollback proof JSON, native-mode health, re-migration outputs) in the Operator Decision section and update the verdict to `SOAK-02 EXERCISED PASS`.
      If anything degrades during the exercise, the rollback script's rendered re-migration sequence is the recovery path; pause and surface to the operator immediately rather than improvising.

    Either option closes SOAK-02. Do not proceed past this checkpoint without the operator's explicit choice recorded.
  </action>
  <verify>
    <human-check>Operator has chosen: provable path accepted (decision recorded in evidence) OR live exercise executed on one WAN with rollback + re-migration evidence captured and production back on cake-autorate, healthy.</human-check>
  </verify>
  <acceptance_criteria>
    - The Operator Decision section of `231-SOAK02-EVIDENCE.md` records the explicit choice, date, and (for option 2) the full exercise evidence including return-to-cake-autorate verification.
    - No production mutation occurred unless option 2 was explicitly approved.
    - Final verdict line reads `SOAK-02 PROVABLE-PATH PASS` or `SOAK-02 EXERCISED PASS`.
    - Operator types "approved" (or describes what is missing).
  </acceptance_criteria>
  <done>SOAK-02 is closed by an operator-recorded decision: provable path accepted, or live exercise completed with production restored to cake-autorate and healthy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box -> cake-shaper (ssh) | Preflight/dry-run read-only; confirm mode mutates systemd units + qdiscs (operator-gated only) |
| script flags -> production mutation | Double gate: --confirm AND --i-have-operator-approval; default invocation is dry-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-231-05 | Tampering | accidental production rollback | mitigate | Mutation requires --confirm + --i-have-operator-approval; regression test proves dry-run/preflight issue zero mutation payloads; checkpoint gates any exercise |
| T-231-06 | Denial of Service | dual-writer during exercise | mitigate | Conflicts= guard verified in preflight; procedure stops cake-autorate trio before enabling native units; re-migration is the inverse with health verification |
| T-231-07 | Tampering | wrong qdisc baseline on rollback | mitigate | qdisc restore commands copied verbatim from the FUTURE doc exercised-rollback block; rendered sequence committed in evidence for review before any exercise |
| T-231-08 | Elevation of Privilege | remote sudo for preflight | mitigate | Preflight uses read-only systemctl/test/cat; on denial, operator `! <command>` handoff (MEMORY) |
| T-231-09 | Repudiation | rollback verification claim | mitigate | Proof JSONs + dry-run renders + historical exercise citation + operator decision all committed |
</threat_model>

<verification>
- `shellcheck -S error scripts/phase231-rollback.sh` exits 0; `.venv/bin/pytest tests/test_phase231_rollback.py -q` passes.
- `231-SOAK02-EVIDENCE.md` has all five sections, both-WAN preflight proof, and an operator-recorded decision with a final verdict line.
- Production state after plan completion: both WANs on cake-autorate external mode, soak-monitor all-healthy (unchanged if provable path; verified restored if exercised).
</verification>

<success_criteria>
- SOAK-02: rollback to native wanctl@{wan} verified — exercised on one WAN under operator approval, OR trivially provable via the documented, preflighted procedure with evidence captured.
</success_criteria>

<output>
Create `.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-02-SUMMARY.md` when done.
</output>
