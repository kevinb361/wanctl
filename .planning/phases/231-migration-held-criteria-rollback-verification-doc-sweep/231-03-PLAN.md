---
phase: 231-migration-held-criteria-rollback-verification-doc-sweep
plan: 03
type: execute
wave: 2
depends_on: [231-01, 231-02]
files_modified:
  - README.md
  - docs/DEPLOYMENT.md
  - docs/CONFIGURATION.md
  - docs/ARCHITECTURE.md
  - docs/README.md
  - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SAFE14-BOUNDARY.md
autonomous: true
requirements: [DOCS-04, SAFE-14]

must_haves:
  truths:
    - "Active docs describe BOTH deployment modes (native wanctl@ template mode AND external cake-autorate + state-bridge mode) and no longer claim native-wanctl ownership of Spectrum/ATT rate control"
    - "Generic/portable wanctl@ documentation is preserved (it remains the valid native mode for other deployments) — reframed as one of two modes, not deleted"
    - "No timer-era guidance is reintroduced; no new private IPs are added to docs prose"
    - "SAFE-14 controller-path zero-diff is proven at the Phase 231 boundary against SAFE_BASE=87980bdf, and because Phase 231 is the v1.50 closeout phase, this final boundary proof is the milestone-close proof — it runs as the LAST task after all other Phase 231 commits"
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "Both deployment modes documented; stale wanctl@spectrum/att live-path claims swept"
      contains: "cake-autorate"
    - path: "docs/ARCHITECTURE.md"
      provides: "External-mode architecture (cake-autorate owns rate decisions; bridge owns state/health/metrics) described alongside native controller architecture"
      contains: "cake-autorate"
    - path: ".planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SAFE14-BOUNDARY.md"
      provides: "SAFE-14 phase-boundary + milestone-close zero-diff proof, two-baseline pattern"
      contains: "87980bdf"
  key_links:
    - from: "docs/DEPLOYMENT.md"
      to: "CLAUDE.md Service Model section"
      via: "two-mode service model prose source of truth"
      pattern: "cake-autorate-{spectrum,att}"
    - from: "231-SAFE14-BOUNDARY.md"
      to: "230-SAFE14-BOUNDARY.md"
      via: "identical proof skeleton, two-baseline separation per decision [230-02]"
      pattern: "PHASE231_START"
---

<objective>
Close v1.50 with the DOCS-04 stale-doc sweep and the SAFE-14 milestone-close boundary proof.

1. DOCS-04: sweep README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md (+ docs/README.md index as applicable) so each describes both deployment modes correctly and none claims native-wanctl ownership of Spectrum/ATT rate control. Verified 2026-06-10: these files currently contain ZERO mentions of cake-autorate; DEPLOYMENT.md (~lines 187, 201) presents `journalctl -u wanctl@spectrum.service -u wanctl@att.service` as the live soak-evidence path — the clearest stale claims.

2. SAFE-14: produce `231-SAFE14-BOUNDARY.md` as the LAST task — Phase 231 is the v1.50 closeout phase, so this boundary proof doubles as the milestone-close proof. Two-baseline pattern per decision [230-02]: SAFE_BASE=87980bdf for controller-path zero-diff; PHASE231_START (pinned at execution start) for Phase 231 scope accounting.

This plan runs in Wave 2 so the boundary proof lands after all SOAK-01/SOAK-02 implementation commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-RESEARCH.md
@.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-PATTERNS.md
@.planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md

<interfaces>
Stale-claim inventory (grep-verified 2026-06-10):
  docs/DEPLOYMENT.md ~47-56, 141-211: wanctl@<wan> enable/restart as THE flow; ~187 + ~201 journalctl wanctl@spectrum/att as live soak path
  docs/CONFIGURATION.md ~235, ~290: "apply config via systemctl restart wanctl@<wan>.service" with no external-mode caveat
  README.md ~75, ~259-262: quickstart/monitoring via wanctl@wan1 only; no mention that external mode exists
  docs/ARCHITECTURE.md: no wanctl@ claims but native-only architecture description
Two-mode prose source of truth: CLAUDE.md "Service Model" section (wanctl@ mode default/portable; cake-autorate external mode with state bridge feeding steering/health)
SAFE-14 protected set (decision [227-04], includes wan_controller_state.py):
  src/wanctl/wan_controller.py  src/wanctl/wan_controller_state.py  src/wanctl/queue_controller.py
  src/wanctl/cake_signal.py  src/wanctl/alert_engine.py  src/wanctl/fusion_healer.py  src/wanctl/backends/
SAFE_BASE = 87980bdf8ea52e5537110cd9bbc7a368f523d2e2 (pinned Phase 229; do NOT re-pin)
PHASE231_START = capture at Wave-1 execution start: the last pre-implementation commit SHA (record `git rev-parse HEAD` before the first Phase 231 implementation commit; the Wave-1 plans' SUMMARY files record it — else derive as the parent of the first 231 implementation commit via `git log --oneline -- scripts/phase231-migration-held.sh | tail -1`)
Expected Phase 231 scope vs PHASE231_START (paths: scripts/ tests/ docs/ README.md):
  scripts/phase231-migration-held.sh, scripts/phase231-rollback.sh, tests/test_phase231_migration_held.py, tests/test_phase231_rollback.py, README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/README.md (if touched)
Known pre-existing full-suite noise: 21 Phase 220/221 boundary-test failures (refuse src/wanctl drift since PHASE214_BASE_SHA=50f3d513) — classify, do not fix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOCS-04 stale-doc sweep — both deployment modes, no native Spectrum/ATT ownership claims</name>
  <files>README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/README.md</files>
  <read_first>
    - CLAUDE.md (Service Model section — the correct two-mode prose; also the public-safe doc policy)
    - docs/DEPLOYMENT.md (full file — the heaviest sweep target)
    - docs/CONFIGURATION.md (~lines 225-300 — restart guidance)
    - README.md (~lines 60-90, 250-270, 440-460 — quickstart, monitoring, backends prose)
    - docs/ARCHITECTURE.md (to place the external-mode section coherently)
    - deploy/systemd/cake-autorate-att.service (unit names + Conflicts= for accurate doc prose)
  </read_first>
  <action>
    Sweep each doc. Policy: (1) describe both modes; (2) remove/correct any claim that native wanctl@ owns Spectrum/ATT rate control today; (3) PRESERVE generic wanctl@ documentation as the valid native mode — reframe, don't delete; (4) no timer-era guidance; (5) public-safe — generic `<target_host>`/`<wan>` placeholders in prose, do NOT copy LAN IPs into docs.

    docs/DEPLOYMENT.md:
    - Add a "Deployment Modes" section near the top: native `wanctl@{wan}.service` template mode (default, portable) vs external cake-autorate mode (`cake-autorate-{wan}.service` + `cake-autorate-{wan}-state-bridge.service`, optional Silicom watchdog variant), noting external units `Conflicts=wanctl@{wan}.service` (no dual-writer) and that the state bridge publishes the same `/health` payload and state JSON consumed by steering/soak tooling.
    - Document the external-mode deploy path: `scripts/deploy.sh <wan> <host> --with-spectrum-cake-autorate` / `--with-att-cake-autorate` flags (read scripts/deploy.sh usage block lines ~100-118 for exact wording).
    - Fix ~lines 187 and 201: the journalctl soak-evidence examples must reference the external-mode units for Spectrum/ATT-style deployments (e.g., `journalctl -u cake-autorate-<wan>.service -u cake-autorate-<wan>-state-bridge.service -u steering.service`) while keeping the wanctl@ example labeled as native-mode.
    - Reframe the wanctl@ enable/restart instructions (~47-56, 141-176) as the native-mode path with a pointer to the external-mode alternative.

    docs/CONFIGURATION.md (~235, ~290): qualify "restart wanctl@<wan>.service to apply" as native-mode; add the external-mode equivalent (cake-autorate config lives in `/etc/cake-autorate/config.<wan>.sh`, repo source `configs/cake-autorate/`, applied by restarting `cake-autorate-<wan>.service`).

    README.md: in the service/monitoring sections (~75, ~259-262), add one short paragraph + example noting the external cake-autorate deployment mode and pointing to docs/DEPLOYMENT.md; the quickstart stays native-mode (it is the portable default).

    docs/ARCHITECTURE.md: add an "External rate-controller mode (cake-autorate)" section: cake-autorate owns reflector measurement + CAKE bandwidth decisions; the wanctl-side state bridge parses its log, writes `/var/lib/wanctl/<wan>_state.json` + per-WAN metrics DB, and serves the wanctl-compatible health endpoint; steering consumes the same state files in both modes. Keep the native controller sections intact as the native-mode description.

    docs/README.md: update index/blurbs only if they assert the native-only model; otherwise leave unchanged.

    Sweep check before finishing: `grep -rn "wanctl@spectrum\|wanctl@att" README.md docs/*.md` — every remaining hit must be inside text explicitly labeled native-mode/rollback context, not presented as the current live Spectrum/ATT path.

    Verify: `grep -l "cake-autorate" README.md docs/DEPLOYMENT.md docs/CONFIGURATION.md docs/ARCHITECTURE.md` lists all four files; `grep -rn "10\.10\.110\." docs/ README.md` introduces no NEW IP literals vs `git diff` (existing committed IPs elsewhere unaffected); `grep -rni "timer" docs/DEPLOYMENT.md` shows no reintroduced timer-era flow.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && test "$(grep -l 'cake-autorate' README.md docs/DEPLOYMENT.md docs/CONFIGURATION.md docs/ARCHITECTURE.md | wc -l)" = "4" && ! git diff 2>/dev/null | grep -E '^\+.*10\.10\.110\.' | grep -v Binary</automated>
  </verify>
  <acceptance_criteria>
    - All four of README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md contain `cake-autorate` and describe the external mode.
    - docs/DEPLOYMENT.md contains a Deployment Modes section, the `--with-att-cake-autorate` flag documentation, and its soak-evidence journalctl examples no longer present `wanctl@spectrum.service`/`wanctl@att.service` as the live path.
    - Generic wanctl@<wan>/wan1 native-mode documentation still exists (not deleted) and is labeled as native mode.
    - `git diff` for this task adds no new `10.10.110.*` literals to README.md or docs/ prose.
    - No timer-era guidance (systemd timer flow for the controller) appears in the swept docs.
  </acceptance_criteria>
  <done>Active docs describe both deployment modes correctly; stale native-ownership claims for Spectrum/ATT are gone; native-mode docs remain for portable deployments.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-14 boundary + v1.50 milestone-close zero-diff proof (LAST task of the phase)</name>
  <files>.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-SAFE14-BOUNDARY.md</files>
  <read_first>
    - .planning/phases/230-soak-monitor-att-coverage/230-SAFE14-BOUNDARY.md (the exact proof skeleton to mirror)
    - .planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-01-SUMMARY.md and 231-02-SUMMARY.md (Wave-1 outcomes + PHASE231_START determination)
  </read_first>
  <action>
    Run this ONLY after Task 1's doc-sweep commit and all Wave-1 plan commits are in (it must be the final substantive commit of v1.50 before phase/milestone close).

    1. Controller-path zero-diff vs SAFE_BASE: `git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/` — MUST be empty.
    2. Dirty-tree checks: `git status --porcelain -- src/wanctl/`, `git diff --quiet -- src/wanctl/`, `git diff --cached --quiet -- src/wanctl/` — all clean. Fail-closed: any protected diff or dirty state is a VIOLATION; the phase boundary does not pass and v1.50 cannot close.
    3. Determine PHASE231_START (the last pre-implementation commit before the first Phase 231 implementation commit; the parent of the first commit touching `scripts/phase231-migration-held.sh` or `scripts/phase231-rollback.sh`). Record the full SHA and how it was derived.
    4. Scope accounting: `git diff --name-only ${PHASE231_START} -- scripts/ tests/ docs/ README.md` — must list ONLY the Phase 231 surface: scripts/phase231-migration-held.sh, scripts/phase231-rollback.sh, tests/test_phase231_migration_held.py, tests/test_phase231_rollback.py, README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/README.md (if touched). Any unexpected path is a finding to explain or a violation.
    5. Re-record verification outputs: `shellcheck -S error scripts/phase231-migration-held.sh scripts/phase231-rollback.sh` exits; `.venv/bin/pytest tests/test_phase231_migration_held.py tests/test_phase231_rollback.py -q`; hot-path slice `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`; full `.venv/bin/pytest tests/ -q` with the 21 pre-existing Phase 220/221 boundary failures explicitly classified as pre-existing/unrelated.
    6. Write `231-SAFE14-BOUNDARY.md` mirroring the 230 skeleton: Baselines table (SAFE_BASE + PHASE231_START, one line each on what each proves), Protected Controller-Path Diff (command + captured empty output + PASS echo), Dirty-Tree Status, Scope Accounting, Verification Outputs, and a Boundary Verdict that states BOTH "Phase 231 boundary" AND "v1.50 milestone close" explicitly. Note the expected parent-reference semantics per decision [225-05]: the committed note references the commit immediately prior to its own tracking commit.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && test -z "$(git diff --stat 87980bdf -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/)" && echo "SAFE-14 PASS: controller-path zero-diff vs 87980bdf"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 87980bdf -- <protected set>` is EMPTY; automated command echoes `SAFE-14 PASS`.
    - Protected controller path dirty-tree is clean (porcelain + unstaged + staged checks captured).
    - `231-SAFE14-BOUNDARY.md` exists with the Baselines table recording `SAFE_BASE=87980bdf8ea52e5537110cd9bbc7a368f523d2e2` and the derived `PHASE231_START` full SHA with derivation method.
    - Scope accounting lists only the expected Phase 231 surface or explains every extra path.
    - Verification outputs (shellcheck, focused pytest, hot-path slice, classified full suite) are re-recorded in the note.
    - The Boundary Verdict explicitly covers both the Phase 231 boundary and v1.50 milestone close.
  </acceptance_criteria>
  <done>SAFE-14 is proven at the Phase 231 boundary and v1.50 milestone close: empty protected-path diff vs SAFE_BASE, clean dirty-tree, bounded Phase 231 scope, verification outputs recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs prose -> public repo | Doc sweep must not leak private IPs/hosts into docs prose |
| repo history -> milestone-close claim | SAFE-14 proof reads git state only; fail-closed on any protected drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-231-10 | Information Disclosure | swept docs | mitigate | Generic placeholders only; acceptance criterion greps the diff for new `10.10.110.*` literals |
| T-231-11 | Tampering | SAFE-14 protected path | mitigate | Fail-closed boundary proof; empty-diff + dirty-tree checks; runs as the final task so no later commit invalidates the milestone-close claim |
| T-231-12 | Spoofing | stale doc claims persisting | mitigate | Explicit grep sweep: remaining `wanctl@spectrum|wanctl@att` hits must be in labeled native-mode/rollback context |
</threat_model>

<verification>
- All four swept docs contain `cake-autorate`; DEPLOYMENT.md soak examples reference external-mode units; native-mode docs preserved and labeled.
- `231-SAFE14-BOUNDARY.md` PASS with two baselines, clean dirty-tree, bounded scope, and the dual boundary/milestone-close verdict.
- Full suite shows only the 21 classified pre-existing failures; hot-path slice green.
</verification>

<success_criteria>
- DOCS-04: active docs describe both deployment modes correctly and no longer claim native-wanctl ownership of Spectrum/ATT rate control.
- SAFE-14: controller-path zero-diff proven at the Phase 231 boundary AND v1.50 milestone close.
</success_criteria>

<output>
Create `.planning/phases/231-migration-held-criteria-rollback-verification-doc-sweep/231-03-SUMMARY.md` when done.
</output>
