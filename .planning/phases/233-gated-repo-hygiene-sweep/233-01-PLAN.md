---
phase: 233-gated-repo-hygiene-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/cake-autorate-trials/
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json
autonomous: false
requirements: [SWEEP-01]
user_setup: []

must_haves:
  truths:
    - "The 22 superseded one-off run_* trial scripts no longer exist on disk under .planning/cake-autorate-trials/"
    - "The future-doc denylist source WANCTL_CAKE_AUTORATE_FUTURE.md still exists and the BOUND-01 guard exits 0 after removal"
    - "The findings docs (SPECTRUM_CAKE_FINDINGS.md, *-review-*.md) are preserved unless the operator approves their removal"
  artifacts:
    - path: ".planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md"
      provides: "BOUND-01 denylist source; must-exist protected (deletion fails guard)"
      contains: "safe to remove soon"
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json"
      provides: "BOUND-01 guard evidence emitted post-removal"
      contains: "passed"
  key_links:
    - from: "trial-script removal"
      to: "scripts/check-cleanup-boundary.sh"
      via: "post-removal guard run exits 0"
      pattern: "check-cleanup-boundary"
---

<objective>
Remove the superseded one-off `run_*` trial scripts (and their result dirs) from the
git-ignored `.planning/cake-autorate-trials/` subtree per the future-doc "safe to remove
soon" policy, while preserving the denylist-source doc and the findings/knowledge docs.

Purpose: SWEEP-01 — consolidate the repo to the two-mode reality by removing experiment
tooling that is fully superseded by repo-owned deploy artifacts. The trial subtree is ~68M
of untracked one-offs with zero tracked references.
Output: The `run_*` trial scripts removed; BOUND-01 guard evidence emitted at
`evidence/cleanup-boundary-233.json` showing the protected surface was not touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove deletion safety and stage the removal set</name>
  <read_first>
    - .planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md (SWEEP-01 Inventory section: the 22 run_* names, the keep-list, Pitfall 1)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-01 deletion-safety pattern + critical constraints)
    - .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md (cleanup policy lines around "safe to remove soon"; the no-delete list — confirm what must be kept)
  </read_first>
  <action>
    Enumerate the actual current `run_*` scripts in `.planning/cake-autorate-trials/` (the 22 names listed in 233-RESEARCH.md SWEEP-01 Inventory, e.g. run_one_trial.sh, run_netperf_ping_trial.sh, run_att_*.py, run_spectrum_*.py, plus parse_flent_summary.py and the timestamped result subdirs). For EACH script name, run the deletion-safety proof `git grep -l "<basename>" | grep -v cake-autorate-trials` and confirm it returns no output (zero tracked references). Build the explicit removal list: the run_* one-off scripts, parse_flent_summary.py, and their timestamped result directories. EXCLUDE from removal and never touch: WANCTL_CAKE_AUTORATE_FUTURE.md (BOUND-01 must-exist denylist source — deleting it fails the guard), SPECTRUM_CAKE_FINDINGS.md, and any *-review-*.md findings docs (knowledge, not superseded tooling). Do NOT delete anything in this task — only produce and record the verified removal list and the zero-reference proof in the task notes for the operator checkpoint. Do not rely on the boundary guard for deletion safety; the trial files are untracked and outside the guard manifest (Pitfall 1).
  </action>
  <verify>
    <automated>for n in run_one_trial run_netperf_ping_trial run_att_netperf_trial run_spectrum_cap_sweep parse_flent_summary; do git grep -l "$n" | grep -v cake-autorate-trials && echo "TRACKED REF FOUND: $n" && exit 1; done; echo "zero tracked refs"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -l <name> | grep -v cake-autorate-trials` returns no output for every name in the removal list (zero tracked references)
    - The removal list explicitly EXCLUDES WANCTL_CAKE_AUTORATE_FUTURE.md, SPECTRUM_CAKE_FINDINGS.md, and every *-review-*.md file
    - `test -f .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` exits 0 (file still present; nothing deleted yet)
  </acceptance_criteria>
  <done>Verified removal list produced with zero-tracked-reference proof for every entry; keep-list (FUTURE doc + findings docs) excluded; nothing deleted yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator confirms destructive removal of trial scripts</name>
  <what-built>
    A verified removal list of untracked `run_*` one-off trial scripts (+ result dirs) under
    `.planning/cake-autorate-trials/`, each proven to have zero tracked references. The
    denylist-source doc and findings docs are excluded from removal.
  </what-built>
  <how-to-verify>
    1. Review the removal list and the per-name `git grep` zero-reference proof from Task 1.
    2. Confirm the disposition: outright `rm` of the untracked run_* scripts + their result dirs
       (recommended — "archiving" within an already-ignored subtree is a git no-op per RESEARCH A3).
       If you instead want a tracked archive, say so — that is a different (additive) task.
    3. Confirm the keep-list (WANCTL_CAKE_AUTORATE_FUTURE.md, SPECTRUM_CAKE_FINDINGS.md,
       *-review-*.md) must be preserved.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `rm` of the listed run_* scripts + result dirs, or describe the alternative disposition (e.g., tracked archive, keep specific scripts).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove approved trial scripts and emit BOUND-01 guard evidence</name>
  <read_first>
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (Evidence JSONs section: emit via --out, do not hand-author; Gate-before-and-after pattern)
    - scripts/check-cleanup-boundary.sh (header / exit contract: 0 = clean, 1 = denylist violation, 2 = usage)
  </read_first>
  <action>
    Run `bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-pre-233-01.json` and confirm exit 0 (pre-state clean). Remove exactly the operator-approved removal list from Task 2 (the untracked run_* one-off scripts, parse_flent_summary.py, and their timestamped result subdirs) via `rm`/`rm -r`. Do NOT remove WANCTL_CAKE_AUTORATE_FUTURE.md, SPECTRUM_CAKE_FINDINGS.md, or any *-review-*.md. Create the evidence dir `.planning/phases/233-gated-repo-hygiene-sweep/evidence/` if absent, then run `bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json` and confirm exit 0 (no protected surface touched). Run the guard regression test. Note in the SUMMARY that the trial subtree is git-ignored, so removal produces no `git` diff — this is expected. Commit only the new evidence JSON (the removed files are untracked; use SKIP_DOC_CHECK=1 if the doc pre-commit hook fires) after running project-finalizer.
  </action>
  <verify>
    <automated>bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json` exits 0
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` exits 0 (9 passed)
    - `ls .planning/cake-autorate-trials/run_one_trial.sh 2>/dev/null` returns nothing (representative removed script absent)
    - `test -f .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` exits 0 (denylist source preserved)
    - `test -f .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233.json` exits 0 and the JSON does not report a denylist violation
  </acceptance_criteria>
  <done>Approved run_* trial scripts + result dirs removed; FUTURE doc and findings docs preserved; BOUND-01 guard exits 0 with evidence committed; guard test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → git-tracked retention set | Untracked trial files are removed; the protected retention set (denylist) must remain intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-01 | Denial of Service | Accidental deletion of a still-referenced or protected file | mitigate | Per-name `git grep -l` zero-reference proof (Task 1) + BOUND-01 guard exit 0 post-removal (Task 3); FUTURE doc + findings docs on explicit keep-list |
| T-233-02 | Tampering | Collateral touch of a denylisted surface during `rm` | mitigate | `scripts/check-cleanup-boundary.sh` run before and after removal; exit 1 = revert |
| T-233-03 | Denial of Service | Destructive `rm` without operator intent | mitigate | `checkpoint:human-verify` (blocking-human) gates the actual removal; `always_confirm_destructive` honored |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; RESEARCH omits the legitimacy audit by design (zero external packages) |
</threat_model>

<verification>
- BOUND-01 guard exits 0 after removal (`evidence/cleanup-boundary-233.json`)
- `tests/test_cleanup_boundary_guard.py` green (9 passed)
- Representative removed script absent; FUTURE doc + findings docs present
</verification>

<success_criteria>
Superseded `run_*` one-off trial scripts are removed (or operator-chosen disposition applied);
the BOUND-01 boundary guard passes (exit 0), confirming no denylisted/"not safe to remove yet"
surface was touched; the denylist-source doc and findings docs are preserved (SWEEP-01).
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-01-SUMMARY.md` when done
</output>
