---
phase: 233-gated-repo-hygiene-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/systemd/cake-autorate-spectrum-state-bridge.service
autonomous: false
requirements: [SWEEP-03]
user_setup: []

must_haves:
  truths:
    - "The Spectrum state-bridge systemd unit sets WANCTL_EXTERNAL_* env explicitly, mirroring the ATT unit, instead of relying on the script's Spectrum-flavored fallback defaults"
    - "The explicit unit values equal the current script defaults so live bridge behavior is unchanged"
    - "No new $wan abstraction is introduced; the two bridge scripts are not merged or templated; the native controller path is untouched"
  artifacts:
    - path: "deploy/systemd/cake-autorate-spectrum-state-bridge.service"
      provides: "Spectrum bridge unit with explicit WANCTL_EXTERNAL_* env (no longer relying on implicit script defaults for Spectrum identity)"
      contains: "WANCTL_EXTERNAL_WAN_NAME=spectrum"
  key_links:
    - from: "deploy/systemd/cake-autorate-spectrum-state-bridge.service"
      to: "deploy/systemd/cake-autorate-att-state-bridge.service"
      via: "mirrored explicit env block (sibling unit pattern)"
      pattern: "WANCTL_EXTERNAL_"
---

<objective>
Make the Spectrum cake-autorate state-bridge systemd unit set its `WANCTL_EXTERNAL_*` env
explicitly — mirroring the already-explicit ATT sibling unit — so Spectrum identity stops
being load-bearing implicit defaults in the shared bridge script. Values must equal the current
script defaults, so this is behavior-preserving with zero new abstraction.

Purpose: SWEEP-03 — strip Spectrum-only hardcoding where a generic `$wan` pattern already exists.
The two bridge scripts are byte-identical and fully env-driven; the only Spectrum-only hardcoding
is that the Spectrum unit leans on the script's Spectrum-flavored fallbacks while ATT is explicit.
Output: Spectrum unit made explicit (mirrors ATT); repo-only edit (live apply is out of scope and
requires an operator-gated redeploy + daemon-reload).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md

<interfaces>
<!-- Sibling unit pattern to mirror (ATT, already explicit) and the script defaults that supply
     the behavior-preserving Spectrum values. Executor uses these directly. -->

ATT explicit env block (deploy/systemd/cake-autorate-att-state-bridge.service):
  WANCTL_EXTERNAL_WAN_NAME=att, WANCTL_EXTERNAL_DL_IF=att-router, WANCTL_EXTERNAL_UL_IF=att-modem,
  CAKE_AUTORATE_BRIDGE_LOG=/var/log/cake-autorate/cake-autorate.att.log,
  WANCTL_EXTERNAL_STATE_PATH=/var/lib/wanctl/att_state.json,
  WANCTL_EXTERNAL_METRICS_DB=/var/lib/wanctl/metrics-att.db,
  WANCTL_EXTERNAL_BASELINE_RTT=28.42043789020452,
  CAKE_AUTORATE_BRIDGE_HEALTH_HOST=10.10.110.227, CAKE_AUTORATE_BRIDGE_HEALTH_PORT=9101

Current Spectrum unit (deploy/systemd/cake-autorate-spectrum-state-bridge.service) sets ONLY:
  CAKE_AUTORATE_BRIDGE_HEALTH_HOST=10.10.110.223, CAKE_AUTORATE_BRIDGE_HEALTH_PORT=9101

Spectrum script defaults to mirror as explicit env (deploy/scripts/cake-autorate-spectrum-state-bridge):
  WAN_NAME=spectrum, DL_IF=spec-router, UL_IF=spec-modem,
  LOG=/var/log/cake-autorate/cake-autorate.spectrum.log, STATE=/var/lib/wanctl/spectrum_state.json,
  METRICS_DB=/var/lib/wanctl/metrics-spectrum.db, DEFAULT_BASELINE_RTT=22.535852814520855
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator confirms the Spectrum BASELINE_RTT value to pin</name>
  <what-built>
    A plan to add explicit `WANCTL_EXTERNAL_*` env to the Spectrum bridge unit, mirroring ATT.
    Every value equals the current script default EXCEPT that the operator must confirm the
    baseline RTT before it is pinned.
  </what-built>
  <how-to-verify>
    1. The script default is WANCTL_EXTERNAL_BASELINE_RTT=22.535852814520855
       (deploy/scripts/cake-autorate-spectrum-state-bridge:23). The ATT unit pins a different
       per-WAN value (28.42043789020452), so baseline is genuinely per-WAN.
    2. Confirm 22.535852814520855 is the intended live Spectrum bridge baseline. If the live
       Spectrum bridge currently runs a different baseline, pinning the script default would
       change behavior (RESEARCH A2). If a privileged read of the live unit is needed, run it at
       the keyboard: `! ssh <spectrum-host> 'systemctl show cake-autorate-spectrum-state-bridge -p Environment'`.
    3. Confirm all other mirrored values (WAN_NAME=spectrum, DL_IF=spec-router, UL_IF=spec-modem,
       LOG/STATE/METRICS_DB paths) equal the script defaults — these are behavior-preserving.
  </how-to-verify>
  <resume-signal>Type "approved" to pin BASELINE_RTT=22.535852814520855, or provide the correct baseline value to use.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Make the Spectrum bridge unit explicit (mirror ATT) and emit guards</name>
  <read_first>
    - deploy/systemd/cake-autorate-att-state-bridge.service (the explicit sibling unit to mirror in structure)
    - deploy/systemd/cake-autorate-spectrum-state-bridge.service (current unit — keep the existing HEALTH_HOST=10.10.110.223)
    - deploy/scripts/cake-autorate-spectrum-state-bridge (lines ~16-30 — the default values that must equal the explicit env)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-03 section: exact env block to insert, out-of-scope items, repo-not-equals-prod caveat)
  </read_first>
  <action>
    Edit deploy/systemd/cake-autorate-spectrum-state-bridge.service to add explicit Environment= lines
    in the [Service] section mirroring the ATT unit's structure: WANCTL_EXTERNAL_WAN_NAME=spectrum,
    WANCTL_EXTERNAL_DL_IF=spec-router, WANCTL_EXTERNAL_UL_IF=spec-modem,
    CAKE_AUTORATE_BRIDGE_LOG=/var/log/cake-autorate/cake-autorate.spectrum.log,
    WANCTL_EXTERNAL_STATE_PATH=/var/lib/wanctl/spectrum_state.json,
    WANCTL_EXTERNAL_METRICS_DB=/var/lib/wanctl/metrics-spectrum.db, and WANCTL_EXTERNAL_BASELINE_RTT
    set to the operator-approved value from Task 1 (default 22.535852814520855 unless the operator
    gave another). Keep the existing CAKE_AUTORATE_BRIDGE_HEALTH_HOST=10.10.110.223 and
    CAKE_AUTORATE_BRIDGE_HEALTH_PORT=9101, the ExecStart path, Restart, RestartSec, [Unit], and
    [Install] sections unchanged. Do NOT change the ExecStart path, do NOT merge or template the two
    bridge scripts, and do NOT touch the bridge script defaults in this plan (out of scope;
    new-abstraction risk). Validate unit syntax with
    `systemd-analyze verify deploy/systemd/cake-autorate-spectrum-state-bridge.service`; if
    systemd-analyze rejects the out-of-tree path, fall back to a structural inspection confirming
    every Environment= key is present and well-formed. Run the BOUND-01 guard and the hot-path
    regression slice (deploy artifact touched). Note in the SUMMARY: repo edit only — live apply
    requires an operator-gated redeploy + `systemctl daemon-reload`, out of scope for the sweep.
    Run project-finalizer; commit (SKIP_DOC_CHECK=1 only if the hook blocks a hygiene-only commit).
  </action>
  <verify>
    <automated>grep -q 'WANCTL_EXTERNAL_WAN_NAME=spectrum' deploy/systemd/cake-autorate-spectrum-state-bridge.service && bash scripts/check-cleanup-boundary.sh && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WANCTL_EXTERNAL_WAN_NAME=spectrum' deploy/systemd/cake-autorate-spectrum-state-bridge.service` returns 1
    - `grep -c 'WANCTL_EXTERNAL_DL_IF=spec-router' deploy/systemd/cake-autorate-spectrum-state-bridge.service` returns 1 and `WANCTL_EXTERNAL_UL_IF=spec-modem` is present
    - `grep -q 'WANCTL_EXTERNAL_BASELINE_RTT=' deploy/systemd/cake-autorate-spectrum-state-bridge.service` succeeds with the operator-approved value
    - `grep -c 'CAKE_AUTORATE_BRIDGE_HEALTH_HOST=10.10.110.223' deploy/systemd/cake-autorate-spectrum-state-bridge.service` returns 1 (existing value preserved)
    - `grep -c '/usr/local/sbin/cake-autorate-spectrum-state-bridge' deploy/systemd/cake-autorate-spectrum-state-bridge.service` returns 1 (ExecStart path unchanged)
    - `systemd-analyze verify deploy/systemd/cake-autorate-spectrum-state-bridge.service` exits 0 (or structural inspection confirms all Environment= keys present)
    - `bash scripts/check-cleanup-boundary.sh` exits 0
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0
  </acceptance_criteria>
  <done>Spectrum bridge unit mirrors ATT with explicit WANCTL_EXTERNAL_* env equal to the script defaults; ExecStart/health env unchanged; bridge scripts not merged; boundary guard exit 0; hot-path slice green; repo-not-equals-prod redeploy caveat recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo systemd unit → live systemd unit | Repo edit does not auto-apply; live bridge keeps pre-edit env until an operator-gated redeploy |
| deploy artifact edit → controller path | SWEEP-03 touches deploy layer only; controller path must stay zero-diff (SAFE-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-07 | Tampering | Corrupting the systemd unit so the state bridge fails to start | mitigate | `systemd-analyze verify` (or structural inspection) on the edited unit; ExecStart/[Unit]/[Install] preserved; acceptance criteria assert each key present |
| T-233-08 | Tampering | Pinning a baseline RTT that differs from the live value, changing bridge behavior | mitigate | `checkpoint:human-verify` (blocking-human) confirms the BASELINE_RTT value before it is pinned (RESEARCH A2) |
| T-233-09 | Tampering | Drift into a new $wan abstraction (merging/templating bridge scripts) | mitigate | Action forbids merging/templating; only the unit is made explicit; bridge scripts untouched this plan |
| T-233-10 | Tampering | Collateral controller-path edit | mitigate | Hot-path regression slice + SAFE-15 boundary proof (Plan 04) confirm controller zero-diff |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- Spectrum unit carries explicit WANCTL_EXTERNAL_* env equal to script defaults; ExecStart + health env unchanged
- `systemd-analyze verify` passes (or structural inspection)
- BOUND-01 guard exit 0; hot-path regression slice green
- SUMMARY records repo-not-equals-prod redeploy caveat
</verification>

<success_criteria>
Spectrum-only hardcoding is removed by making the Spectrum bridge unit explicit using the env
pattern that already exists — no new abstraction introduced; the boundary guard confirms the
native path is untouched; behavior is preserved (values equal current defaults) (SWEEP-03).
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-03-SUMMARY.md` when done
</output>
