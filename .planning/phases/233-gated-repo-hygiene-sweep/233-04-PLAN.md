---
phase: 233-gated-repo-hygiene-sweep
plan: 04
type: execute
wave: 2
depends_on: ["233-01", "233-02", "233-03"]
files_modified:
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/safe15-boundary-233.json
  - .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json
autonomous: true
requirements: [SAFE-15]
user_setup: []

must_haves:
  truths:
    - "After all three SWEEP plans land, the full pytest suite is green"
    - "Controller-path zero-diff vs v1.50 holds at the phase boundary (controller_path_diff_count == 0)"
    - "The BOUND-01 guard exits 0 on the post-sweep tree, writing phase-final evidence (not the Phase 232 default path)"
  artifacts:
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/safe15-boundary-233.json"
      provides: "SAFE-15 controller-path zero-diff proof at the Phase 233 boundary"
      contains: "passed"
    - path: ".planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json"
      provides: "Post-sweep BOUND-01 guard evidence at the phase boundary (phase-final filename; does not clobber Phase 232 default or sibling plan evidence)"
      contains: "passed"
  key_links:
    - from: "post-sweep worktree"
      to: "scripts/phase225-safe13-boundary-check.sh --anchor v1.50"
      via: "controller-path zero-diff proof emitted to evidence JSON"
      pattern: "controller_path_diff_count"
---

<objective>
Prove the Phase 233 boundary is clean after all three SWEEP plans land: full test suite green,
BOUND-01 guard exit 0, and SAFE-15 controller-path zero-diff vs v1.50 with committed JSON evidence
(mirroring the Phase 232 evidence layout). Every evidence file uses a phase-specific filename so no
guard run clobbers the Phase 232 default path or a sibling plan's evidence.

Purpose: SAFE-15 — the cross-phase controller-path zero-diff invariant must be verified, not
assumed, at every phase boundary. This is the per-wave-merge gate from the 233-VALIDATION.md
sampling contract, run once after the parallel sweep wave completes.
Output: `evidence/safe15-boundary-233.json` and `evidence/cleanup-boundary-233-final.json` committed
(force-added) with passed==true and controller_path_diff_count==0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-VALIDATION.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite, BOUND-01 guard, and SAFE-15 boundary proof; force-add evidence</name>
  <read_first>
    - .planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md (SAFE-15 section: exact zero-diff command, the controller path file list, the checker invocation)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (Evidence JSONs section: emit via --out, do not hand-author; the safe15-boundary-232.json shape)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-VALIDATION.md (Sampling Rate: per-wave-merge = full suite + SAFE-15)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-03-PLAN.md (the established `git add -f` + SKIP_DOC_CHECK=1 commit pattern for gitignored planning artifacts)
    - scripts/phase225-safe13-boundary-check.sh (the SAFE-15 checker; controller path lines ~68-74, --anchor / --out contract)
    - scripts/check-cleanup-boundary.sh (line ~19 — default --out is the Phase 232 evidence path; ALWAYS pass --out here)
  </read_first>
  <action>
    Confirm Plans 233-01, 233-02, 233-03 are complete (their SUMMARYs are tracked: `git ls-files` lists
    233-01-SUMMARY.md, 233-02-SUMMARY.md, 233-03-SUMMARY.md). Run the full suite `.venv/bin/pytest tests/ -v`
    and confirm green. Run the BOUND-01 guard with a PHASE-FINAL --out path (never the Phase 232 default, never a
    sibling plan's filename): `bash scripts/check-cleanup-boundary.sh --out
    .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json` and confirm exit 0 on
    the post-sweep tree. Emit the SAFE-15 proof — do NOT hand-author the JSON — via
    `bash scripts/phase225-safe13-boundary-check.sh --anchor v1.50 --out
    .planning/phases/233-gated-repo-hygiene-sweep/evidence/safe15-boundary-233.json`; confirm stdout reports the
    boundary check passed and the JSON carries passed==true and controller_path_diff_count==0. As a second
    independent check, run `git diff --quiet v1.50..HEAD -- src/wanctl/wan_controller.py
    src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py
    src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/` and confirm exit 0.
    Because `.planning/` is gitignored, the two evidence JSONs and `233-04-SUMMARY.md` will NOT be committed by a
    plain `git add` — force-add all three: `git add -f
    .planning/phases/233-gated-repo-hygiene-sweep/evidence/safe15-boundary-233.json
    .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json
    .planning/phases/233-gated-repo-hygiene-sweep/233-04-SUMMARY.md`. Before committing, run `git status --short`
    and `git diff --cached --name-only` and confirm ONLY those three expected phase-boundary artifacts are staged
    (no stray controller/doc/unit file). Run project-finalizer; commit (SKIP_DOC_CHECK=1 only if the doc hook
    blocks a hygiene-only commit).
  </action>
  <verify>
    <automated>bash scripts/phase225-safe13-boundary-check.sh --anchor v1.50 --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/safe15-boundary-233.json && git diff --quiet v1.50..HEAD -- src/wanctl/wan_controller.py src/wanctl/wan_controller_state.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/ && bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/ -q` exits 0 (full suite green)
    - `bash scripts/check-cleanup-boundary.sh --out .planning/phases/233-gated-repo-hygiene-sweep/evidence/cleanup-boundary-233-final.json` exits 0 (phase-final --out; the Phase 232 default path is NOT modified by this plan)
    - `bash scripts/phase225-safe13-boundary-check.sh --anchor v1.50 --out <evidence>` exits 0 and prints the boundary-check-passed message
    - The emitted `evidence/safe15-boundary-233.json` contains `"passed": true` and `"controller_path_diff_count": 0`
    - `git diff --quiet v1.50..HEAD -- <7 controller paths>` exits 0 (independent zero-diff confirmation)
    - After commit, `git ls-files .planning/phases/233-gated-repo-hygiene-sweep/ | grep -E '(safe15-boundary-233.json|cleanup-boundary-233-final.json|233-04-SUMMARY.md)'` lists all three (force-add succeeded; evidence + SUMMARY committed/tracked, not merely present-on-disk)
    - `git diff --cached --name-only` immediately before commit shows ONLY the three expected phase-boundary artifacts staged
  </acceptance_criteria>
  <done>Full suite green; BOUND-01 guard exit 0 with phase-final --out (Phase 232 default untouched); SAFE-15 zero-diff proven (passed==true, diff_count==0); independent git diff --quiet confirms controller path untouched; both evidence JSONs + SUMMARY force-added and confirmed tracked; staged set verified clean before commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post-sweep worktree → v1.50 controller anchor | The controller path must remain byte-identical to v1.50 after the sweep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-11 | Tampering | Undetected controller-path drift introduced by an earlier SWEEP plan | mitigate | `scripts/phase225-safe13-boundary-check.sh --anchor v1.50` (object-ID + numstat proof) plus independent `git diff --quiet`; both must be clean |
| T-233-12 | Repudiation | No durable record that the boundary was proven clean | mitigate | JSON evidence emitted to phase-specific `evidence/safe15-boundary-233.json` + `evidence/cleanup-boundary-233-final.json`, force-added (gitignored `.planning/`) and confirmed tracked via `git ls-files`, mirroring Phase 232 |
| T-233-15 | Tampering | Guard run without --out clobbers the Phase 232 default evidence file | mitigate | Every guard invocation in this plan passes an explicit phase-final --out; acceptance asserts the Phase 232 default path is not modified |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- Full suite green; BOUND-01 guard exit 0 via phase-final --out (Phase 232 default untouched)
- SAFE-15: passed==true, controller_path_diff_count==0; independent `git diff --quiet` exit 0
- `evidence/safe15-boundary-233.json` + `evidence/cleanup-boundary-233-final.json` + SUMMARY force-added and confirmed tracked
- Staged set verified to contain only the three expected artifacts before commit
</verification>

<success_criteria>
SAFE-15 controller-path zero-diff holds at the Phase 233 boundary, verified (not assumed) with
committed (force-added, confirmed-tracked) JSON evidence; the full test suite and BOUND-01 guard
are green on the post-sweep tree, with all guard evidence written to phase-specific paths so the
Phase 232 default evidence is never clobbered.
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-04-SUMMARY.md` when done
</output>
