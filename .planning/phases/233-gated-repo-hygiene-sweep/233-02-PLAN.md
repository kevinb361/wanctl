---
phase: 233-gated-repo-hygiene-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PROFILING.md
  - docs/PERFORMANCE.md
  - docs/RUNBOOK.md
  - docs/CABLE_TUNING.md
  - docs/STEERING.md
  - docs/SILICOM-BYPASS.md
autonomous: false
requirements: [SWEEP-02]
user_setup: []

must_haves:
  truths:
    - "docs/PROFILING.md, docs/PERFORMANCE.md, and docs/RUNBOOK.md each carry a mode-disambiguation note that mentions external cake-autorate mode"
    - "Native wanctl@<wan> operational command examples are retained (annotation only, no deletion)"
    - "The operator-judgment docs (CABLE_TUNING, STEERING, SILICOM-BYPASS) are edited only per explicit operator decision"
  artifacts:
    - path: "docs/PROFILING.md"
      provides: "Profiling runbook with native/external mode disambiguation note"
      contains: "cake-autorate"
    - path: "docs/PERFORMANCE.md"
      provides: "Performance doc with mode disambiguation note"
      contains: "cake-autorate"
    - path: "docs/RUNBOOK.md"
      provides: "Operator runbook with mode disambiguation note"
      contains: "cake-autorate"
  key_links:
    - from: "docs/PROFILING.md note"
      to: "README.md:265-268 mode-note template"
      via: "copied phrasing (cake-autorate-<wan>.service / state-bridge)"
      pattern: "cake-autorate-<wan>"
---

<objective>
Add a one-line mode-disambiguation note (native `wanctl@` vs external cake-autorate mode) to
the active operational docs that show `wanctl@<wan>` commands without any external-mode mention,
mirroring the canonical README phrasing. Do NOT delete native operational examples — native mode
is a real rollback/profiling procedure and is denylist-protected.

Purpose: SWEEP-02 — close the residual native-ownership doc gap left after v1.50 Phase 231
DOCS-04. The DOCS-04 "no wanctl@ hits" claim is stale: PROFILING/PERFORMANCE/RUNBOOK carry
native-unit references with zero external-mode notes.
Output: Mode-disambiguation notes added to the 3 clear-residual docs; the 3 operator-judgment
docs handled per operator decision; a grep sweep proves the residual is clear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md
@.planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode-disambiguation notes to PROFILING, PERFORMANCE, RUNBOOK</name>
  <read_first>
    - README.md (lines ~263-268 — the canonical mode-note phrasing template to copy verbatim in intent)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-02 section: exact phrasing template, application rule, Pitfall 2 "do not delete native examples")
    - .planning/phases/233-gated-repo-hygiene-sweep/233-RESEARCH.md (SWEEP-02 Inventory table: which line ranges carry the native-unit hits per doc)
    - docs/PROFILING.md (current top + first wanctl@ command block)
    - docs/PERFORMANCE.md (current top + first wanctl@ command block)
    - docs/RUNBOOK.md (current top + the journalctl wanctl@ block near line 398)
  </read_first>
  <action>
    For each of docs/PROFILING.md, docs/PERFORMANCE.md, docs/RUNBOOK.md: insert ONE mode-disambiguation note near the top of the doc or immediately above the first `wanctl@<wan>` command block. The note must state that the examples assume native `wanctl@` mode and that in external cake-autorate mode the relevant units are `cake-autorate-<wan>.service` and `cake-autorate-<wan>-state-bridge.service` (and steering.service), with the state JSON / health contract remaining wanctl-compatible — mirror the README phrasing at README.md:265-268. Do NOT delete or alter any `systemctl`/`journalctl`/`wanctl@` command examples — native mode is a denylist-protected rollback/profiling procedure (Pitfall 2). Do NOT touch docs/DEPLOYMENT.md or docs/UPGRADING.md (must-exist guard rows; already compliant). The literal token "cake-autorate" must appear in each edited doc after the change.
  </action>
  <verify>
    <automated>for f in docs/PROFILING.md docs/PERFORMANCE.md docs/RUNBOOK.md; do c=$(grep -ciE 'cake-autorate|external mode' "$f"); [ "$c" -ge 1 ] || { echo "MISSING note: $f"; exit 1; }; done; echo "all 3 annotated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'cake-autorate|external mode' docs/PROFILING.md` returns >= 1
    - `grep -ciE 'cake-autorate|external mode' docs/PERFORMANCE.md` returns >= 1
    - `grep -ciE 'cake-autorate|external mode' docs/RUNBOOK.md` returns >= 1
    - `git grep -c 'wanctl@' docs/PROFILING.md docs/PERFORMANCE.md docs/RUNBOOK.md` shows the native-unit example counts are not reduced from pre-edit (no command examples deleted)
    - `bash scripts/check-cleanup-boundary.sh` exits 0 (no must-exist/must-match-anchor doc collaterally damaged)
  </acceptance_criteria>
  <done>The 3 clear-residual docs each carry a mode-disambiguation note mentioning external cake-autorate mode; native command examples retained; boundary guard exits 0.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator decides disposition of the 3 judgment-call docs</name>
  <decision>Whether to add a mode-disambiguation note to docs/CABLE_TUNING.md, docs/STEERING.md, and docs/SILICOM-BYPASS.md.</decision>
  <context>
    These three docs carry native `wanctl@<wan>` references but RESEARCH classifies them as
    judgment calls: CABLE_TUNING.md is mostly historical past-tense tuning narrative; STEERING.md
    likely has operational steering examples; SILICOM-BYPASS.md references the native unit by
    design (the bypass watchdog targets `wanctl@<wan>.service` as the thing being stopped/started).
    SWEEP-02's bar is "describes Spectrum/ATT as native-wanctl-owned rate control WITHOUT noting
    external mode" — historical narrative and by-design references may not meet that bar. None are
    guard-protected; all are freely editable. The recommended default is to leave historical/by-design
    references as-is and annotate only if they assert current native ownership.
  </context>
  <options>
    <option id="annotate-none">
      <name>Leave all 3 as-is (recommended default)</name>
      <pros>No edits to historical narrative or by-design references; lowest risk; matches RESEARCH recommendation</pros>
      <cons>A reader could still read a stale current-ownership implication in STEERING.md if it has live examples</cons>
    </option>
    <option id="annotate-steering-only">
      <name>Annotate STEERING.md only</name>
      <pros>Covers the doc most likely to carry live operational examples; leaves clearly-historical docs untouched</pros>
      <cons>Requires confirming STEERING.md's hits are live, not historical</cons>
    </option>
    <option id="annotate-all-three">
      <name>Annotate all 3 with the standard note</name>
      <pros>Uniform disambiguation everywhere a native unit is named</pros>
      <cons>Adds notes to historical/by-design content where the note is arguably noise</cons>
    </option>
  </options>
  <resume-signal>Select: annotate-none, annotate-steering-only, annotate-all-three (or name specific docs to annotate).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply operator decision and prove SWEEP-02 residual is clear</name>
  <read_first>
    - README.md (lines ~265-268 — note phrasing template)
    - .planning/phases/233-gated-repo-hygiene-sweep/233-PATTERNS.md (SWEEP-02 phrasing template + application rule)
    - docs/CABLE_TUNING.md, docs/STEERING.md, docs/SILICOM-BYPASS.md (only the doc(s) the operator chose to annotate)
  </read_first>
  <action>
    Apply exactly the operator's Task 2 decision: if annotate-none, make no doc edits in this task; otherwise add the same one-line mode-disambiguation note (mirroring README.md:265-268) to each operator-selected doc, near the top or above the first `wanctl@<wan>` block, without deleting any existing examples. Then run the boundary guard and the guard regression test to confirm no protected surface was touched. Record in the SUMMARY the final SWEEP-02 disposition: which docs were annotated, which were left as historical/by-design per operator decision, and the resulting grep evidence. Run project-finalizer; commit the doc edits (use SKIP_DOC_CHECK=1 only if the doc pre-commit hook blocks a hygiene-only commit).
  </action>
  <verify>
    <automated>bash scripts/check-cleanup-boundary.sh && .venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cleanup-boundary.sh` exits 0 (no must-exist/anchor doc damaged)
    - `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` exits 0
    - For every doc the operator chose to annotate, `grep -ciE 'cake-autorate|external mode' <doc>` returns >= 1
    - The SUMMARY records the explicit per-doc SWEEP-02 disposition (annotated vs left-as-historical) for all 6 candidate docs
  </acceptance_criteria>
  <done>Operator decision applied to the judgment-call docs; boundary guard exits 0; SWEEP-02 disposition recorded with grep evidence proving the residual is clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc edit → guard-protected doc set | Hygiene annotations must not delete or corrupt must-exist / must-match-anchor docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-04 | Tampering | A doc edit misstates the production mode (e.g., claims native owns rates) | mitigate | Note copies the verified README:265-268 phrasing (external mode owns rates); operator reviews judgment docs in Task 2 |
| T-233-05 | Denial of Service | Deleting a native operational example that is the documented rollback/profiling procedure | mitigate | Annotation-only rule (Pitfall 2); acceptance criterion asserts wanctl@ example counts not reduced; boundary guard exit 0 |
| T-233-06 | Tampering | Collateral edit to a must-exist (DEPLOYMENT/UPGRADING) or must-match-anchor doc | mitigate | Those docs explicitly out of edit scope; `scripts/check-cleanup-boundary.sh` exit 0 confirms untouched |
| T-233-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- PROFILING/PERFORMANCE/RUNBOOK each grep >= 1 for `cake-autorate|external mode`
- Boundary guard exits 0; guard test green
- Operator-decision docs handled per Task 2; per-doc disposition recorded in SUMMARY
</verification>

<success_criteria>
No active doc describes Spectrum/ATT as native-wanctl-owned rate control without noting the
current external cake-autorate mode; a grep/inspection sweep demonstrates the residual is clear;
native operational examples retained; boundary guard confirms no protected doc surface damaged
(SWEEP-02).
</success_criteria>

<output>
Create `.planning/phases/233-gated-repo-hygiene-sweep/233-02-SUMMARY.md` when done
</output>
