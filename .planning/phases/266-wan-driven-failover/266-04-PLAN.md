# Wave 4: Deploy + Live Observation

**Phase**: 266
**Wave**: 4
**Scope**: Deploy to cake-shaper, verify, observe

## Tasks

1. Deploy `failover_bridge.py` + daemon/config changes to cake-shaper via `deploy.sh`

2. Verify deployment:
   - Health endpoint comes up clean (mode=active, owner=wanctl, guard=ok)
   - Failover bridge is initialized but disabled (enabled=false)
   - No route changes during GREEN operation
   - Reconciliation still shows 4 routes

3. Enable failover in steering.yaml (config change + SIGUSR1 reload):
   ```yaml
   route_management:
     failover:
       enabled: true
       wan: "spectrum"
       red_cycles: 3
       green_cycles: 5
   ```

4. Live observation:
   - Monitor for 1h minimum with failover enabled but no injection
   - Verify no unplanned route changes (Spectrum stays GREEN → no failover expected)
   - Watchdog continues monitoring
   - Journal shows bridge initialization, no error signals

## Deliverables
- Failover bridge deployed and enabled on cake-shaper
- Zero unplanned route changes during observation period
- Health endpoint shows `last_failover_event: null` (no failover triggered during GREEN operation)

## Constraints
- Failover enabled is opt-in via config — operator gate
- Abort path remains armed
- Watchdog continues monitoring (job f557c287adfc)
