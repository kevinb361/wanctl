# Wave 3: Synthetic Tests + Verification

**Phase**: 266
**Wave**: 3
**Scope**: Integration tests + verification before deploy

## Tasks

1. Integration tests for daemon + bridge + route_manager:
   - Mock RED injection → verify route_manager receives disable action after N cycles
   - Mock GREEN injection → verify route_manager receives enable action after M cycles
   - YELLOW interposition → verify counters reset, no action
   - Abort override → verify circuit breaker open triggers abort_to_netwatch even mid-failover

2. Existing test regression:
   - Run full route_manager test suite
   - Run health endpoint shape tests (verify last_failover_event field)
   - Run daemon cycle tests

3. Lint/format:
   - `make lint`
   - `make format`
   - `make test`

## Deliverables
- All tests pass
- No lint errors
- Health endpoint shape includes `last_failover_event`

## Constraints
- No production changes in this wave
- Failover remains disabled by default (enabled=false in config)
