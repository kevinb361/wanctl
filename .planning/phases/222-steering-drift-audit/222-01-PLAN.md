---
phase: 222-steering-drift-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/222-steering-drift-audit/evidence/README.md
  - .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json
  - .planning/phases/222-steering-drift-audit/evidence/delta-files.json
  - .planning/phases/222-steering-drift-audit/evidence/delta-commits.json
  - .planning/phases/222-steering-drift-audit/evidence/delta-report.md
  - .planning/phases/222-steering-drift-audit/evidence/classification.md
autonomous: true
requirements: [DRIFT-01, DRIFT-03]
must_haves:
  truths:
    - "Operator can read the source-vs-runtime delta report under .planning/phases/222-steering-drift-audit/evidence/delta-report.md without re-running git commands."
    - "Every file, line count, and semantic category in the delta report is reproducible from the saved evidence JSON via the recorded git commands."
    - "The chosen baseline commit is documented with rationale (v1.39 git tag is the conservative default; deployed-binary commit is preferred if Phase 212 evidence can identify it)."
    - "The audit upper bound is the v1.47-peeled commit SHA (source_commit = `bee343b0c2f16207101aec82007a5e55fa9b6407`, resolved once via `git rev-parse v1.47^{commit}`), NOT a moving HEAD reference. This is the source-floor anchor for Phase 222 (v1.47 Phase 220 base_sha precedent). audit_head is recorded separately for diagnostics only and is never used as the diff endpoint."
    - "Every commit in the v1.40 -> v1.47-close range (`baseline_commit..source_commit`) that touched a steering-surface file appears in the per-milestone classification table exactly once."
    - "Each commit is categorized as one of: behavior-changing, behavior-preserving, observability-only. Category assignment is derived from reading the diff, not from the commit subject alone."
    - "The classification artifact records the rationale for every behavior-changing category assignment (one sentence minimum citing the affected branch / decision / coupling)."
    - "Milestone bucket assignment uses peeled tag commits from `git rev-list -n 1 <tag>`. Because the repo has no `v1.45` tag (tags jump v1.44 -> v1.46), commits whose source_version reads `1.45.0` are bucketed under the synthetic interval `v1.44..v1.46` and labeled `(synthetic — no v1.45 tag exists)` in evidence/README.md."
    - "No production probe is run. No source file outside the audit artifact directory is modified."
    - "The audit covers the steering surface listed in 222-RESEARCH.md Section 3 — extending the surface requires an explicit note in evidence/README.md."
  artifacts:
    - path: ".planning/phases/222-steering-drift-audit/evidence/README.md"
      provides: "Command index, baseline rationale, surface scope, redaction posture, and audit reproducibility instructions"
    - path: ".planning/phases/222-steering-drift-audit/evidence/delta-baseline.json"
      provides: "Baseline commit choice (v1.39 tag), source_commit (v1.47-peeled SHA — pinned audit upper bound), audit_head (diagnostics), audit timestamp, and rationale"
    - path: ".planning/phases/222-steering-drift-audit/evidence/delta-files.json"
      provides: "Per-file added/removed/total line counts within the steering surface"
    - path: ".planning/phases/222-steering-drift-audit/evidence/delta-commits.json"
      provides: "Per-commit list within the baseline..source_commit range (v1.39 -> v1.47-peeled SHA) restricted to steering surface, with semantic category and milestone_bucket (incl. synthetic v1.44..v1.46 for source_version 1.45.0)"
    - path: ".planning/phases/222-steering-drift-audit/evidence/delta-report.md"
      provides: "Human-readable delta report (DRIFT-01 evidence) — file table + per-file diff appendix"
    - path: ".planning/phases/222-steering-drift-audit/evidence/classification.md"
      provides: "Per-milestone change classification table (DRIFT-03 evidence) covering every commit in v1.40 -> v1.47-close range that touched steering source"
  key_links:
    - from: "git tag v1.39 commit"
      to: ".planning/phases/222-steering-drift-audit/evidence/delta-baseline.json#baseline_commit"
      via: "git rev-list -n 1 v1.39"
      pattern: "[0-9a-f]{7,40}"
    - from: "git tag v1.47 peeled commit"
      to: ".planning/phases/222-steering-drift-audit/evidence/delta-baseline.json#source_commit"
      via: "git rev-parse v1.47^{commit}"
      pattern: "bee343b0c2f16207101aec82007a5e55fa9b6407"
    - from: "git log <baseline_commit>..<source_commit> -- <steering surface>"
      to: ".planning/phases/222-steering-drift-audit/evidence/delta-commits.json"
      via: "git log --format=json | per-commit semantic category"
      pattern: "src/wanctl/steering/|check_steering_validators|steering_panel|steering.yaml|steering.service"
---

<objective>
Produce the source-vs-runtime delta report (DRIFT-01) and the per-milestone
change classification (DRIFT-03) for the steering surface between runtime
`1.39` and source `1.45` (i.e., baseline v1.39 git tag vs the v1.47-peeled
commit `bee343b0c2f16207101aec82007a5e55fa9b6407`).

The audit upper bound is **pinned to the v1.47-peeled commit SHA**, not the
current `HEAD`. This is the v1.47 source-floor anchor (Phase 220 `base_sha`
precedent) — without it, any commit landing between planning and execution
silently expands audit scope. `audit_head` is recorded separately for
diagnostics only; it is never used as the diff endpoint.

This plan is read-only and artifact-producing. It does not mutate any source,
touch production, or run new probes against the live daemon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/222-steering-drift-audit/222-RESEARCH.md
@.planning/milestones/v1.46-phases/212-production-inventory-and-drift-audit/212-REPORT.md
@.planning/milestones/v1.46-phases/212-production-inventory-and-drift-audit/evidence/health-steering.json
@src/wanctl/__init__.py
@src/wanctl/steering/daemon.py
@src/wanctl/steering/health.py
@src/wanctl/check_steering_validators.py
</context>

<tasks>

<task type="auto">
  <name>Task 222-01-01: Create evidence directory, baseline manifest, and README</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
    .planning/milestones/v1.46-phases/212-production-inventory-and-drift-audit/212-REPORT.md
    src/wanctl/__init__.py
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/README.md, .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json</files>
  <action>Create `.planning/phases/222-steering-drift-audit/evidence/` if absent. Write `evidence/README.md` documenting: (a) the audit surface scope per 222-RESEARCH.md Section 3, listing each file path that is in scope; (b) the baseline-commit rationale — primary baseline is `git rev-list -n 1 v1.39` (record the resolved 40-char SHA), with a note that deployed-binary commit is preferred if Phase 212 health evidence can identify it; (c) **the source-floor anchor: `source_commit` is the v1.47-peeled commit resolved once via `git rev-parse v1.47^{commit}` (expected literal `bee343b0c2f16207101aec82007a5e55fa9b6407`). This SHA is the audit upper bound for every diff/log command in this plan — NOT current HEAD. `audit_head` (`git rev-parse HEAD`) is recorded separately for diagnostics only and is never used as the diff endpoint.** Document the rationale: v1.47 is the source floor (Phase 220 base_sha precedent); pinning the upper bound prevents silent scope expansion if steering edits land between planning and execution; (d) the milestone bucketing rule, including the explicit synthetic bucket `v1.44..v1.46` for commits whose `source_version` reads `1.45.0` (no `v1.45` tag exists in this repo); (e) the exact git commands used to reproduce each artifact; (f) the read-only posture (no production probe, no source mutation, no deploy). Write `evidence/delta-baseline.json` as a JSON object with keys `baseline_tag` (string, "v1.39"), `baseline_commit` (40-char SHA from `git rev-list -n 1 v1.39`), `source_tag` (string, "v1.47"), `source_commit` (40-char SHA from `git rev-parse v1.47^{commit}` — expected `bee343b0c2f16207101aec82007a5e55fa9b6407`), `audit_head` (40-char SHA from `git rev-parse HEAD` — diagnostics only, NOT the diff endpoint), `source_version` (string, "1.45.0" — read from `src/wanctl/__init__.py`), `audit_timestamp_utc` (ISO 8601), `surface_files` (array of file paths per Section 3), `synthetic_v1_45_bucket` (string, "v1.44..v1.46" — the milestone bucket assigned to commits with source_version 1.45.0 since no v1.45 tag exists), and `rationale` (string explaining the v1.39 baseline choice, the v1.47-peeled source_commit anchor, and noting that Phase 212 evidence at `.planning/milestones/v1.46-phases/212-production-inventory-and-drift-audit/evidence/health-steering.json` is the preferred source for the deployed-binary commit if it can be reconstructed).</action>
  <verify>
    <automated>test -f .planning/phases/222-steering-drift-audit/evidence/README.md &amp;&amp; .venv/bin/python -m json.tool .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,re,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/delta-baseline.json').read_text()); assert d['baseline_tag']=='v1.39'; assert re.match(r'^[0-9a-f]{40}$', d['baseline_commit']); assert d['source_tag']=='v1.47'; assert d['source_commit']=='bee343b0c2f16207101aec82007a5e55fa9b6407', f\"source_commit must be the v1.47-peeled SHA, got {d['source_commit']}\"; assert re.match(r'^[0-9a-f]{40}$', d['audit_head']); assert d['source_version']=='1.45.0'; assert d['synthetic_v1_45_bucket']=='v1.44..v1.46'; assert isinstance(d['surface_files'], list) and len(d['surface_files']) &gt;= 5"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/README.md` exists and contains sections titled "Audit Surface", "Baseline Rationale", "Source-Floor Anchor (v1.47-peeled)", "Milestone Bucketing (incl. v1.45 synthetic bucket)", "Reproducibility", and "Posture".
    - `evidence/delta-baseline.json` parses as JSON and contains keys `baseline_tag`, `baseline_commit`, `source_tag`, `source_commit`, `audit_head`, `source_version`, `audit_timestamp_utc`, `surface_files`, `synthetic_v1_45_bucket`, `rationale`.
    - `baseline_commit` is a 40-character lowercase hex SHA equal to `git rev-list -n 1 v1.39`.
    - `source_commit` is the literal string `bee343b0c2f16207101aec82007a5e55fa9b6407` (the v1.47-peeled commit SHA resolved via `git rev-parse v1.47^{commit}`). This is the audit upper bound and MUST NOT be replaced with `HEAD`.
    - `audit_head` is a 40-character lowercase hex SHA equal to `git rev-parse HEAD` at audit time; the README explicitly notes it is recorded for diagnostics only and is NOT used as a diff endpoint.
    - `source_version` equals the `__version__` string in `src/wanctl/__init__.py` (currently `"1.45.0"`).
    - `synthetic_v1_45_bucket` equals the literal string `"v1.44..v1.46"` and the README explains why (no `v1.45` git tag exists; tags jump v1.44 -> v1.46).
    - `surface_files` is a JSON array containing at minimum: `src/wanctl/steering/daemon.py`, `src/wanctl/steering/health.py`, `src/wanctl/steering/__init__.py`, `src/wanctl/steering/congestion_assessment.py`, `src/wanctl/steering/steering_confidence.py`, `src/wanctl/steering/cake_stats.py`, `src/wanctl/check_steering_validators.py`.
    - `README.md` records the exact `git diff`, `git log`, `git rev-list`, and `git rev-parse v1.47^{commit}` commands used downstream so the audit is reproducible.
  </acceptance_criteria>
  <done>Baseline manifest and reproducibility README exist and parse cleanly.</done>
</task>

<task type="auto">
  <name>Task 222-01-02: Capture per-file diff stats and write delta-files.json</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json
    .planning/phases/222-steering-drift-audit/evidence/README.md
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/delta-files.json</files>
  <action>Read `surface_files` and `source_commit` from `evidence/delta-baseline.json`. The diff range is `<baseline_commit>..<source_commit>` — i.e., v1.39 to the v1.47-peeled SHA (`bee343b0c2f16207101aec82007a5e55fa9b6407`). DO NOT substitute `HEAD` for `source_commit`. For each path in the surface, run two commands: (a) `git diff --numstat <baseline_commit>..<source_commit> -- <path>` to capture added/removed line counts, and (b) `git diff --name-status <baseline_commit>..<source_commit> -- <path>` to capture the file status letter (`A` added, `M` modified, `D` deleted, `R` renamed, or empty when unchanged). `--numstat` alone cannot distinguish added vs modified vs deleted reliably — `--name-status` is the source of truth for the `status` field. Map the status letter as: `A` -> `added`, `M` -> `modified`, `D` -> `deleted`, no output -> `unchanged` (set `added=0, removed=0`). Write `evidence/delta-files.json` as a JSON object with key `files` mapped to an array of objects with keys `path` (string), `added` (int), `removed` (int), `status` (one of `modified` | `added` | `deleted` | `unchanged`). Include every file in the surface even when its `added`/`removed` are both zero (status `unchanged`) so the report is complete. Do NOT run any production probe, do NOT touch any source file. Use only `git diff --numstat` and `git diff --name-status` — no git mutation commands.</action>
  <verify>
    <automated>.venv/bin/python -m json.tool .planning/phases/222-steering-drift-audit/evidence/delta-files.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/delta-files.json').read_text()); assert isinstance(d.get('files'), list); assert all({'path','added','removed','status'} &lt;= set(f.keys()) for f in d['files']); assert all(f['status'] in ('modified','added','deleted','unchanged') for f in d['files']); paths={f['path'] for f in d['files']}; assert 'src/wanctl/steering/daemon.py' in paths and 'src/wanctl/check_steering_validators.py' in paths"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/delta-files.json` parses as JSON and has a `files` array.
    - Every file in `delta-baseline.json.surface_files` has exactly one row in `files`.
    - Each row has keys `path`, `added`, `removed`, `status`; `added` and `removed` are non-negative integers; `status` is one of `modified`, `added`, `deleted`, `unchanged`.
    - For each `modified` row, `added + removed &gt; 0`; for each `unchanged` row, `added == 0 and removed == 0`.
    - The `status` field is derived from `git diff --name-status <baseline_commit>..<source_commit>` (the `A`/`M`/`D` letter), NOT from `--numstat` line counts alone.
    - The diff range used is exactly `<baseline_commit>..<source_commit>` — `source_commit` MUST be the v1.47-peeled SHA from `delta-baseline.json`, not `HEAD`.
    - The artifact was produced by `git diff --numstat` and `git diff --name-status` only — no production probe was run, no git mutation commands.
  </acceptance_criteria>
  <done>Per-file diff statistics exist for every file in the audit surface.</done>
</task>

<task type="auto">
  <name>Task 222-01-03: Capture per-commit list and assign semantic categories</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json
    .planning/phases/222-steering-drift-audit/evidence/delta-files.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
    src/wanctl/steering/daemon.py
    src/wanctl/check_steering_validators.py
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/delta-commits.json</files>
  <action>Run `git log --format=%H%x09%s%x09%ad --date=iso-strict <baseline_commit>..<source_commit> -- <each surface path>` to enumerate every commit in the `baseline..source_commit` range that touched a steering-surface file. The upper bound is `source_commit` (v1.47-peeled SHA, `bee343b0c2f16207101aec82007a5e55fa9b6407`), NOT `HEAD`. Deduplicate by SHA. For each commit, capture: `sha` (40-char), `subject`, `author_date_iso`, `files_in_surface` (subset of surface files that this commit touched). Then for each commit, read the actual diff via `git show --stat --patch <sha> -- <files_in_surface>` and assign a `category` field with one of three values from 222-RESEARCH.md Section 6: `behavior-changing`, `behavior-preserving`, `observability-only`. For each `behavior-changing` assignment, also fill `rationale` (one sentence naming the affected branch / decision / coupling — example: "modifies the early-return guard in `_evaluate_rtt_delta` so the deferred-baseline path now fires under additional input"). For `behavior-preserving` and `observability-only`, `rationale` may be empty string or a brief justification. Also assign `milestone_bucket` — the smallest peeled-tag interval containing the commit, resolved as `git rev-list -n 1 <tag>` for each milestone tag (e.g., `v1.39..v1.40`, `v1.40..v1.41`, etc.). **Synthetic v1.45 rule:** because no `v1.45` git tag exists (tags jump v1.44 -> v1.46), any commit whose nearest preceding tag is `v1.44` AND whose nearest following tag is `v1.46` is assigned `milestone_bucket = "v1.44..v1.46"` (matching `delta-baseline.json.synthetic_v1_45_bucket`). Persist the result to `evidence/delta-commits.json` as a JSON object with key `commits` mapped to an array of the records above. Do NOT touch any source file. Do NOT run production probes.</action>
  <verify>
    <automated>.venv/bin/python -m json.tool .planning/phases/222-steering-drift-audit/evidence/delta-commits.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,re,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/delta-commits.json').read_text()); commits=d['commits']; assert isinstance(commits, list); assert all(re.match(r'^[0-9a-f]{40}$', c['sha']) for c in commits); cats=set(c['category'] for c in commits); assert cats &lt;= {'behavior-changing','behavior-preserving','observability-only'}; assert all(c.get('rationale','') != '' or c['category'] in ('behavior-preserving','observability-only') for c in commits); seen=set(); [seen.add(c['sha']) for c in commits]; assert len(seen)==len(commits), 'duplicate SHA in commits'"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/delta-commits.json` parses as JSON with a `commits` array.
    - Every commit has keys `sha`, `subject`, `author_date_iso`, `files_in_surface`, `category`, `rationale`, `milestone_bucket`.
    - Every `sha` is a 40-character lowercase hex string and is unique within the file (no duplicate entries).
    - `category` is one of `behavior-changing`, `behavior-preserving`, `observability-only`.
    - Every `behavior-changing` row has a non-empty `rationale`.
    - `milestone_bucket` matches the pattern `v1\.\d+\.\.v1\.\d+` (e.g., `v1.39..v1.40`).
    - Any commit with `source_version == "1.45.0"` (or whose nearest preceding tag is `v1.44` and nearest following tag is `v1.46`) has `milestone_bucket == "v1.44..v1.46"` per the synthetic-bucket rule.
    - The git log/show range upper bound is `source_commit` (v1.47-peeled SHA), NOT `HEAD`.
    - Commit `84ad6aa` (`fix: harden steering and storage utility contracts`) appears in the list with category `behavior-changing` and a rationale naming the steering contract guard it tightened.
    - No commit in `commits` touches only files OUTSIDE the surface declared in `delta-baseline.json.surface_files`.
  </acceptance_criteria>
  <done>Per-commit ledger with semantic categories exists and covers every commit in range touching steering source.</done>
</task>

<task type="auto">
  <name>Task 222-01-04: Render DRIFT-01 delta report markdown</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/delta-baseline.json
    .planning/phases/222-steering-drift-audit/evidence/delta-files.json
    .planning/phases/222-steering-drift-audit/evidence/delta-commits.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/delta-report.md</files>
  <action>Render the human-readable DRIFT-01 delta report at `evidence/delta-report.md`. Sections in order: (1) `## Baseline` — table of baseline tag/commit, source tag/commit (the v1.47-peeled SHA, audit upper bound), audit_head (diagnostics only — must be labeled as such), source version, audit timestamp, sourced from `delta-baseline.json`. (2) `## Surface` — bullet list of surface files. (3) `## Per-File Delta` — markdown table with columns `File | Added | Removed | Status | Semantic Category`. Semantic Category is the strongest category among commits touching that file (priority order: behavior-changing &gt; behavior-preserving &gt; observability-only &gt; unchanged). (4) `## Per-Commit Detail` — markdown table with columns `SHA | Date | Subject | Files | Category | Rationale`, rows sourced from `delta-commits.json`. (5) `## Reproducibility` — exact git commands used (copy from `evidence/README.md`), including `git rev-parse v1.47^{commit}` for resolving the source-floor anchor. Do NOT include any production-side data, secrets, or unredacted command output beyond what is already in the evidence JSONs.</action>
  <verify>
    <automated>test -f .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q '^## Baseline$' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q '^## Surface$' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q '^## Per-File Delta$' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q '^## Per-Commit Detail$' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q '^## Reproducibility$' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q 'src/wanctl/steering/daemon.py' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q 'bee343b0c2f16207101aec82007a5e55fa9b6407' .planning/phases/222-steering-drift-audit/evidence/delta-report.md &amp;&amp; grep -q 'git rev-parse v1.47\^{commit}' .planning/phases/222-steering-drift-audit/evidence/delta-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/delta-report.md` contains H2 sections named exactly `## Baseline`, `## Surface`, `## Per-File Delta`, `## Per-Commit Detail`, `## Reproducibility`.
    - The `## Baseline` table includes both `source_commit` (labeled as the audit upper bound, value `bee343b0c2f16207101aec82007a5e55fa9b6407`) and `audit_head` (labeled "diagnostics only — not the diff endpoint").
    - The `## Per-File Delta` table has columns `File | Added | Removed | Status | Semantic Category` and at least one row per surface file from `delta-files.json`.
    - The `## Per-Commit Detail` table has columns `SHA | Date | Subject | Files | Category | Rationale` and at least one row per entry in `delta-commits.json`.
    - The `## Reproducibility` section contains the literal strings `git rev-list -n 1 v1.39`, `git rev-parse v1.47^{commit}`, `git diff --numstat`, and `git diff --name-status`.
    - The report contains no raw secrets, no router passwords, no production probe output.
    - The report does not depend on a successful network connection to render — every value is sourced from the saved evidence JSON files.
  </acceptance_criteria>
  <done>Operator can open `evidence/delta-report.md` and read the full DRIFT-01 delta report end-to-end.</done>
</task>

<task type="auto">
  <name>Task 222-01-05: Render DRIFT-03 per-milestone classification</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/delta-commits.json
    .planning/phases/222-steering-drift-audit/evidence/delta-report.md
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/classification.md</files>
  <action>Render the DRIFT-03 per-milestone classification at `evidence/classification.md`. Sections in order: (1) `## Methodology` — restate the three-category schema and the contract-driven categorization rule from 222-RESEARCH.md Section 6. (2) `## Per-Milestone Tally` — markdown table with columns `Milestone | Behavior-Changing | Behavior-Preserving | Observability-Only | Total`. Rows are one per `milestone_bucket` present in `delta-commits.json`, sorted ascending (`v1.39..v1.40`, `v1.40..v1.41`, etc.). (3) `## Per-Commit Classification` — markdown table with columns `Milestone | SHA | Subject | Category | Rationale`, sorted first by milestone then by date. (4) `## Findings` — one bullet per behavior-changing commit summarizing the change and naming the affected steering branch / decision / coupling. (5) `## Default Dispositions` — restate from 222-RESEARCH.md: behavior-preserving and observability-only default to `go`; behavior-changing requires per-finding rationale in Plan 02. Every behavior-changing commit must appear in `## Findings` exactly once.</action>
  <verify>
    <automated>test -f .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; grep -q '^## Methodology$' .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; grep -q '^## Per-Milestone Tally$' .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; grep -q '^## Per-Commit Classification$' .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; grep -q '^## Findings$' .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; grep -q '^## Default Dispositions$' .planning/phases/222-steering-drift-audit/evidence/classification.md &amp;&amp; .venv/bin/python -c "import json,pathlib,re; commits=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/delta-commits.json').read_text())['commits']; bc=[c for c in commits if c['category']=='behavior-changing']; text=pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/classification.md').read_text(); missing=[c['sha'][:7] for c in bc if c['sha'][:7] not in text and c['sha'] not in text]; assert not missing, f'behavior-changing commits missing from findings: {missing}'"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/classification.md` contains H2 sections named exactly `## Methodology`, `## Per-Milestone Tally`, `## Per-Commit Classification`, `## Findings`, `## Default Dispositions`.
    - `## Per-Milestone Tally` table has columns `Milestone | Behavior-Changing | Behavior-Preserving | Observability-Only | Total`.
    - `## Per-Commit Classification` table has columns `Milestone | SHA | Subject | Category | Rationale`.
    - Every commit with `category == "behavior-changing"` in `delta-commits.json` has a corresponding bullet in `## Findings` (matched by SHA prefix or full SHA).
    - `## Default Dispositions` explicitly states that behavior-preserving and observability-only changes default to `go` and that behavior-changing changes get a per-finding disposition in Plan 02 (DRIFT-04).
    - The `Total` column of each milestone row equals the sum of the three category columns for that row.
  </acceptance_criteria>
  <done>Operator can open `evidence/classification.md` and read the per-milestone classification with rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> committed evidence | Source diffs and commit metadata are encoded into committed JSON / markdown artifacts. |
| 222-RESEARCH.md categorization rules -> per-commit category assignment | The category column on every commit reflects an interpretive call against the spine contract; the rationale text is the audit trail. |
| Phase 212 deployed-binary evidence -> 222 baseline choice | Choosing the v1.39 tag as baseline is conservative; if Phase 212 evidence proves a different deployed commit, the README must record the deviation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-01-01 | I | committed evidence artifacts | mitigate | The audit uses only `git` read-only commands; no production probes; no `/etc/wanctl/secrets` access; no router credentials touched. |
| T-222-01-02 | T | per-commit category assignment | mitigate | Every behavior-changing row carries a written rationale naming the affected branch / decision / coupling; categories are derived from reading the diff, not the commit message. |
| T-222-01-03 | S | baseline-commit choice | mitigate | The baseline manifest records the `v1.39` tag SHA AND the rationale for using it; deviation from the tag (e.g., a deployed-binary commit derived from Phase 212 evidence) must be explicit and reviewable. |
| T-222-01-04 | D | controller-path scope leakage | mitigate | The audit surface explicitly excludes the SAFE-12 controller-path allowlist (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion); Plan 03 verifies the controller path independently. |
| T-222-01-05 | T | floating audit upper bound (audit scope creep) | mitigate | The audit upper bound is pinned to the v1.47-peeled commit SHA (`bee343b0c2f16207101aec82007a5e55fa9b6407`) resolved once via `git rev-parse v1.47^{commit}`, NOT current `HEAD`. Without this pin, any commit landing between planning and execution silently expands audit scope. Acceptance criteria assert `source_commit == "bee343b…"` literally; `audit_head` is recorded separately for diagnostics only and is never used as the diff endpoint. This is the v1.47 source-floor anchor (Phase 220 `base_sha` precedent). |
</threat_model>

<verification>
Run each task's automated check. No new tests are introduced; the audit is
docs/artifacts only. If a helper script is added under
`.planning/phases/222-steering-drift-audit/` during execution, it must ship
with a test under `tests/test_phase222_drift_audit.py` before any subsequent
task depends on it.
</verification>

<success_criteria>
DRIFT-01 and DRIFT-03 evidence artifacts exist under `evidence/`, parse
cleanly, and cover every commit in the `baseline..source_commit` range
(v1.39 -> v1.47-peeled SHA `bee343b0c2f16207101aec82007a5e55fa9b6407`)
touching steering source. The operator can read `delta-report.md` and
`classification.md` end-to-end without re-running git commands.
</success_criteria>

<output>
After completion, create
`.planning/phases/222-steering-drift-audit/222-01-SUMMARY.md` per the standard
GSD summary template.
</output>
