---
phase: 222-steering-drift-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
  - .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
autonomous: true
requirements: [SAFE-12]
must_haves:
  truths:
    - "Controller-path source — wan_controller.py, queue_controller.py, cake_signal.py, backends/, alert_engine.py, and fusion*.py — is byte-identical to the v1.47 close baseline at the Phase 222 boundary."
    - "The SAFE-12 invariant check is captured as a saved evidence artifact, not just a one-time terminal output."
    - "The v1.47 close baseline commit is documented (currently bee343b 'chore: remove REQUIREMENTS.md for v1.47 milestone') and confirmed against the v1.47 git tag."
    - "Any non-empty diff against the allowlist files is a SAFE-12 violation and blocks phase closure — the verify-step automated check enforces this."
    - "This plan is independent of Plans 01 and 02 and can run in Wave 1 alongside Plan 01."
  artifacts:
    - path: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json"
      provides: "Structured SAFE-12 boundary check: baseline commit, HEAD commit, per-path diff size (expected 0), pass/fail flag"
    - path: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md"
      provides: "Human-readable SAFE-12 boundary check artifact with the exact git command, command output (empty diff expected), and verdict"
  key_links:
    - from: "git tag v1.47"
      to: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json#baseline_commit"
      via: "git rev-list -n 1 v1.47"
      pattern: "bee343b0c2f16207101aec82007a5e55fa9b6407"
    - from: "controller-path allowlist (RESEARCH.md Section 4)"
      to: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json#paths"
      via: "git diff <baseline>..HEAD -- <each allowlist path>"
      pattern: "wan_controller|queue_controller|cake_signal|backends|alert_engine|fusion"
---

<objective>
Verify the SAFE-12 controller-path zero-diff invariant at the Phase 222
boundary. Expected outcome: every path in the controller-path allowlist is
byte-identical to the v1.47 close baseline (commit bee343b). Any non-empty
diff is a SAFE-12 violation and blocks phase closure.

This plan is read-only and artifact-producing. It does not mutate any source,
touch production, or run new probes against the live daemon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/222-steering-drift-audit/222-RESEARCH.md
@src/wanctl/wan_controller.py
@src/wanctl/queue_controller.py
@src/wanctl/cake_signal.py
@src/wanctl/alert_engine.py
@src/wanctl/fusion_healer.py
</context>

<tasks>

<task type="auto">
  <name>Task 222-03-01: Confirm v1.47 close baseline commit</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json</files>
  <action>Run `git rev-list -n 1 v1.47` to resolve the v1.47 close baseline commit. Confirm it matches the value cited in 222-RESEARCH.md Section 10 (`bee343b0c2f16207101aec82007a5e55fa9b6407` — short `bee343b`). Resolve `git rev-parse HEAD` for the current audit-time commit. Write the initial structure of `evidence/safe12-boundary-check.json` as a JSON object with keys: `baseline_tag` ("v1.47"), `baseline_commit` (40-char SHA from `git rev-list -n 1 v1.47`), `head_commit` (40-char SHA from `git rev-parse HEAD`), `audit_timestamp_utc` (ISO 8601), `allowlist_paths` (array of file paths from 222-RESEARCH.md Section 4), `per_path_diff` (empty object — populated in Task 222-03-02), `passed` (boolean — set in Task 222-03-03). Do NOT touch any source file. Do NOT modify any file outside `.planning/phases/222-steering-drift-audit/`.</action>
  <verify>
    <automated>.venv/bin/python -m json.tool .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,re,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert d['baseline_tag']=='v1.47'; assert re.match(r'^[0-9a-f]{40}$', d['baseline_commit']); assert re.match(r'^[0-9a-f]{40}$', d['head_commit']); assert isinstance(d['allowlist_paths'], list) and len(d['allowlist_paths']) &gt;= 5; assert 'src/wanctl/wan_controller.py' in d['allowlist_paths']; assert 'src/wanctl/queue_controller.py' in d['allowlist_paths']; assert 'src/wanctl/cake_signal.py' in d['allowlist_paths']; assert 'src/wanctl/alert_engine.py' in d['allowlist_paths']"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` parses as JSON.
    - `baseline_tag` is the literal string `"v1.47"`.
    - `baseline_commit` is a 40-character lowercase hex SHA equal to `git rev-list -n 1 v1.47`.
    - `head_commit` is a 40-character lowercase hex SHA equal to `git rev-parse HEAD` at audit time.
    - `allowlist_paths` is a JSON array containing at minimum: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/alert_engine.py`, `src/wanctl/fusion_healer.py`, and the `src/wanctl/backends/` path (or each backend file under it enumerated explicitly).
    - The artifact is written under `.planning/phases/222-steering-drift-audit/evidence/`; no source file is modified.
  </acceptance_criteria>
  <done>SAFE-12 boundary manifest is initialized with the resolved baseline + HEAD commits and the controller-path allowlist.</done>
</task>

<task type="auto">
  <name>Task 222-03-02: Run per-path zero-diff check and populate per_path_diff</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json</files>
  <action>For each path in `safe12-boundary-check.json.allowlist_paths`, run `git diff --numstat <baseline_commit>..<head_commit> -- <path>` and capture the result. For a single file path the diff is either empty (path unchanged) or one numstat line (`added\tremoved\tpath`). For a directory path (e.g., `src/wanctl/backends/`), the result may contain multiple lines — capture every one. Populate `per_path_diff` as a JSON object keyed by `path` mapped to an object with keys `added` (int, default 0), `removed` (int, default 0), `lines` (array of numstat lines as strings — empty array when path is unchanged). Do NOT touch any source file. Use only `git diff --numstat` — no `git apply`, no `git reset`, no `git checkout`.</action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert isinstance(d['per_path_diff'], dict); assert set(d['per_path_diff'].keys()) == set(d['allowlist_paths']), 'per_path_diff must cover every allowlist path'; assert all({'added','removed','lines'} &lt;= set(v.keys()) for v in d['per_path_diff'].values()); assert all(isinstance(v['added'], int) and isinstance(v['removed'], int) and isinstance(v['lines'], list) for v in d['per_path_diff'].values())"</automated>
  </verify>
  <acceptance_criteria>
    - `per_path_diff` exists in `evidence/safe12-boundary-check.json` and is a JSON object.
    - Every key in `per_path_diff` matches an entry in `allowlist_paths`; the key set equals the array set exactly.
    - Every value has keys `added`, `removed`, `lines`.
    - `added` and `removed` are non-negative integers.
    - `lines` is a JSON array of strings (numstat output lines); the array is empty when the path is unchanged.
    - The artifact was produced by `git diff --numstat` only — no other git mutation commands were run.
    - No source file outside `.planning/phases/222-steering-drift-audit/` was modified.
  </acceptance_criteria>
  <done>Per-path zero-diff data is captured for every allowlist path.</done>
</task>

<task type="auto">
  <name>Task 222-03-03: Compute pass/fail verdict and render SAFE-12 markdown</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json, .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md</files>
  <action>Read `evidence/safe12-boundary-check.json.per_path_diff`. Compute `passed = all(v['added'] == 0 and v['removed'] == 0 for v in per_path_diff.values())`. Write `passed` back into the JSON object. Then render `evidence/safe12-boundary-check.md` with sections in order: (1) `## SAFE-12 Invariant` — restate the invariant from 222-RESEARCH.md Section 4 and `.planning/REQUIREMENTS.md` SAFE-12. (2) `## Baseline` — table of baseline tag, baseline commit, HEAD commit, audit timestamp, sourced from the JSON. (3) `## Allowlist` — bullet list of every path in `allowlist_paths`. (4) `## Per-Path Diff` — markdown table with columns `Path | Added | Removed | Status` where `Status` is `unchanged` if added+removed==0 else `VIOLATION`. (5) `## Verdict` — single line stating `PASSED` or `FAILED` based on `passed`; if FAILED, list every violating path. (6) `## Reproducibility` — the exact `git rev-list`, `git rev-parse`, and `git diff --numstat` commands used. If `passed` is false, the verify-step automated check below MUST fail — i.e., the verify command exits non-zero so phase closure is blocked.</action>
  <verify>
    <automated>test -f .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## SAFE-12 Invariant$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## Verdict$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; .venv/bin/python -c "import json,pathlib,sys; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert 'passed' in d and isinstance(d['passed'], bool); violations={p:v for p,v in d['per_path_diff'].items() if v['added']!=0 or v['removed']!=0}; computed_passed = (len(violations)==0); assert d['passed']==computed_passed, f'passed flag inconsistent with per_path_diff'; sys.exit(0 if d['passed'] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` has `passed` set to a boolean derived from `per_path_diff` (true iff every path has `added == 0` and `removed == 0`).
    - `evidence/safe12-boundary-check.md` contains H2 sections named exactly `## SAFE-12 Invariant`, `## Baseline`, `## Allowlist`, `## Per-Path Diff`, `## Verdict`, `## Reproducibility`.
    - `## Verdict` contains either the literal string `PASSED` or `FAILED`.
    - When `passed == true`, `## Verdict` is `PASSED` and every row in the `## Per-Path Diff` table has Status `unchanged`.
    - When `passed == false`, `## Verdict` is `FAILED`, every violating path is listed with its Added / Removed counts, and the verify-step automated check exits non-zero — blocking Phase 222 closure.
    - The markdown contains the literal commands `git rev-list -n 1 v1.47`, `git rev-parse HEAD`, and `git diff --numstat` under `## Reproducibility`.
  </acceptance_criteria>
  <done>SAFE-12 boundary check is fully rendered with a deterministic pass/fail verdict; phase closure is blocked if the controller path has drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> SAFE-12 evidence | The boundary check encodes a `git diff` result into a committed JSON / markdown artifact; the artifact must be deterministic from the recorded git commands. |
| 222-RESEARCH.md allowlist -> safe12-boundary-check.json.allowlist_paths | The allowlist is the canonical SAFE-12 surface and must match exactly between the research doc and the JSON manifest. |
| verdict computation -> phase closure | A failed verdict MUST block phase closure via the verify-step exit code; silent passes on a non-zero diff are not permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-03-01 | T | controller-path zero-diff invariant | mitigate | The verify step computes `passed` from `per_path_diff` and exits non-zero on any drift; the JSON `passed` field is cross-checked against the computed value. |
| T-222-03-02 | I | allowlist drift between RESEARCH.md and JSON | mitigate | The allowlist in `safe12-boundary-check.json.allowlist_paths` is asserted to include the canonical files; any addition / removal must be reflected in 222-RESEARCH.md Section 4 and committed together. |
| T-222-03-03 | T | git command tampering | mitigate | Reproducibility section records the exact `git rev-list`, `git rev-parse`, `git diff --numstat` commands; no other git commands are permitted in this plan. |
</threat_model>

<verification>
Run each task's automated check. The Task 222-03-03 verify step exits
non-zero when SAFE-12 is violated, blocking phase closure. No new tests are
introduced.
</verification>

<success_criteria>
SAFE-12 boundary evidence exists under `evidence/`, parses cleanly, and
either confirms PASSED (zero diff against v1.47 close baseline) or surfaces
the violating paths with non-zero verify exit so phase closure is blocked.
</success_criteria>

<output>
After completion, create
`.planning/phases/222-steering-drift-audit/222-03-SUMMARY.md` per the
standard GSD summary template.
</output>
