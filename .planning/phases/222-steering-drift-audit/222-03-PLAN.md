---
phase: 222-steering-drift-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
  - .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
autonomous: true
requirements: [SAFE-12]
must_haves:
  truths:
    - "Controller-path source — wan_controller.py, queue_controller.py, cake_signal.py, backends/, alert_engine.py, and fusion*.py — is byte-identical to the v1.47 close baseline at the Phase 222 boundary."
    - "The SAFE-12 boundary check is FAIL-CLOSED against the working tree AND the index AND untracked files in addition to committed history. `git diff <baseline>..HEAD` alone is NOT sufficient — staged, unstaged, and untracked controller-path edits would not be caught. The check follows the SAFE-07 v1.44 Phase 207 precedent (HRDN-01) implemented in `scripts/check-safe07-source-diff.sh`."
    - "The SAFE-12 invariant check is captured as a saved evidence artifact, not just a one-time terminal output."
    - "The v1.47 close baseline commit is documented (`bee343b0c2f16207101aec82007a5e55fa9b6407` 'chore: remove REQUIREMENTS.md for v1.47 milestone') and confirmed against the v1.47 git tag via `git rev-parse v1.47^{commit}`."
    - "Any non-empty diff against the allowlist files — committed, staged, unstaged, or untracked — is a SAFE-12 violation and blocks phase closure. The verify-step automated check enforces this fail-closed."
    - "This plan is independent of Plans 01 and 02 and can run in Wave 1 alongside Plan 01."
  artifacts:
    - path: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json"
      provides: "Structured SAFE-12 boundary check: baseline commit, HEAD commit, per-path diff size (expected 0), dirty-tree state (unstaged/staged/untracked counts, expected 0), pass/fail flag"
    - path: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md"
      provides: "Human-readable SAFE-12 boundary check artifact with the exact git commands, command output (empty diff + clean tree expected), and verdict"
  key_links:
    - from: "git tag v1.47"
      to: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json#baseline_commit"
      via: "git rev-parse v1.47^{commit}"
      pattern: "bee343b0c2f16207101aec82007a5e55fa9b6407"
    - from: "controller-path allowlist (RESEARCH.md Section 4)"
      to: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json#per_path_diff"
      via: "git diff --numstat <baseline>..HEAD -- <each allowlist path>"
      pattern: "wan_controller|queue_controller|cake_signal|backends|alert_engine|fusion"
    - from: "dirty-tree fail-closed check (HRDN-01 / SAFE-07 precedent)"
      to: ".planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json#dirty_tree"
      via: "git diff --cached --numstat -- <paths> + git diff --numstat -- <paths> + git ls-files --others --exclude-standard -- <paths> + git status --porcelain -- <paths>"
      pattern: "unstaged|staged|untracked"
---

<objective>
Verify the SAFE-12 controller-path zero-diff invariant at the Phase 222
boundary. Expected outcome: every path in the controller-path allowlist is
byte-identical to the v1.47 close baseline (commit
`bee343b0c2f16207101aec82007a5e55fa9b6407`). The check is **fail-closed
against committed history AND the index AND the working tree AND untracked
files** — any non-empty committed diff, staged edit, unstaged edit, or
untracked controller-path file is a SAFE-12 violation and blocks phase
closure.

This dirty-tree check follows the SAFE-07 v1.44 Phase 207 precedent
(HRDN-01) implemented in `scripts/check-safe07-source-diff.sh`. Without it,
`git diff <baseline>..HEAD` alone would only see committed history; a staged
or unstaged controller-path edit at phase boundary would silently pass.

This plan is read-only and artifact-producing. It does not mutate any source,
touch production, or run new probes against the live daemon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/222-steering-drift-audit/222-RESEARCH.md
@src/wanctl/wan_controller.py
@src/wanctl/queue_controller.py
@src/wanctl/cake_signal.py
@src/wanctl/alert_engine.py
@src/wanctl/fusion_healer.py
</context>

<tasks>

<task type="auto">
  <name>Task 222-03-01: Confirm v1.47 close baseline commit</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json</files>
  <action>Run `git rev-parse v1.47^{commit}` to resolve the v1.47-peeled close baseline commit. Confirm it matches the value cited in 222-RESEARCH.md Section 10 (`bee343b0c2f16207101aec82007a5e55fa9b6407` — short `bee343b`). Resolve `git rev-parse HEAD` for the current audit-time commit. Write the initial structure of `evidence/safe12-boundary-check.json` as a JSON object with keys: `baseline_tag` ("v1.47"), `baseline_commit` (40-char SHA from `git rev-parse v1.47^{commit}` — expected `bee343b0c2f16207101aec82007a5e55fa9b6407`), `head_commit` (40-char SHA from `git rev-parse HEAD`), `audit_timestamp_utc` (ISO 8601), `allowlist_paths` (array of file paths from 222-RESEARCH.md Section 4), `per_path_diff` (empty object — populated in Task 222-03-02), `dirty_tree` (empty object — populated in Task 222-03-02 with `unstaged`, `staged`, `untracked`, `status_porcelain` keys), `passed` (boolean — set in Task 222-03-03). Do NOT touch any source file. Do NOT modify any file outside `.planning/phases/222-steering-drift-audit/`.</action>
  <verify>
    <automated>.venv/bin/python -m json.tool .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,re,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert d['baseline_tag']=='v1.47'; assert d['baseline_commit']=='bee343b0c2f16207101aec82007a5e55fa9b6407', f\"baseline_commit must be the v1.47-peeled SHA, got {d['baseline_commit']}\"; assert re.match(r'^[0-9a-f]{40}$', d['head_commit']); assert isinstance(d['allowlist_paths'], list) and len(d['allowlist_paths']) &gt;= 5; assert 'src/wanctl/wan_controller.py' in d['allowlist_paths']; assert 'src/wanctl/queue_controller.py' in d['allowlist_paths']; assert 'src/wanctl/cake_signal.py' in d['allowlist_paths']; assert 'src/wanctl/alert_engine.py' in d['allowlist_paths']; assert 'dirty_tree' in d, 'dirty_tree key must be present (populated in Task 222-03-02)'"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` parses as JSON.
    - `baseline_tag` is the literal string `"v1.47"`.
    - `baseline_commit` is the literal string `"bee343b0c2f16207101aec82007a5e55fa9b6407"` — the v1.47-peeled commit SHA from `git rev-parse v1.47^{commit}`.
    - `head_commit` is a 40-character lowercase hex SHA equal to `git rev-parse HEAD` at audit time.
    - `allowlist_paths` is a JSON array containing at minimum: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/alert_engine.py`, `src/wanctl/fusion_healer.py`, and the `src/wanctl/backends/` path (or each backend file under it enumerated explicitly).
    - The JSON contains a `dirty_tree` key (object, may be empty here — populated in Task 222-03-02).
    - The artifact is written under `.planning/phases/222-steering-drift-audit/evidence/`; no source file is modified.
  </acceptance_criteria>
  <done>SAFE-12 boundary manifest is initialized with the resolved baseline + HEAD commits and the controller-path allowlist.</done>
</task>

<task type="auto">
  <name>Task 222-03-02: Run per-path committed-diff + dirty-tree fail-closed check and populate per_path_diff + dirty_tree</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
    scripts/check-safe07-source-diff.sh
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json</files>
  <action>This task runs TWO complementary checks that MUST BOTH pass for SAFE-12 to be satisfied. The pattern is borrowed verbatim from `scripts/check-safe07-source-diff.sh` (HRDN-01, Phase 207 SAFE-07 precedent) — committed-diff alone is insufficient because it cannot see staged/unstaged/untracked controller-path edits.

**(A) Committed-diff check (existing behavior):** For each path in `safe12-boundary-check.json.allowlist_paths`, run `git diff --numstat <baseline_commit>..<head_commit> -- <path>` and capture the result. For a single file path the diff is either empty (path unchanged) or one numstat line (`added\tremoved\tpath`). For a directory path (e.g., `src/wanctl/backends/`), the result may contain multiple lines — capture every one. Populate `per_path_diff` as a JSON object keyed by `path` mapped to an object with keys `added` (int, default 0), `removed` (int, default 0), `lines` (array of numstat lines as strings — empty array when path is unchanged).

**(B) Dirty-tree fail-closed check (NEW — Codex HIGH #2 resolution):** Run the following four git commands restricted to the SAFE-12 allowlist paths and capture results into `dirty_tree`:

  1. `git diff --numstat -- <each allowlist path>` — captures **unstaged worktree** edits. Record into `dirty_tree.unstaged` as an array of numstat lines (empty when clean).
  2. `git diff --cached --numstat -- <each allowlist path>` — captures **staged** edits (in the index but not yet committed). Record into `dirty_tree.staged` as an array of numstat lines (empty when clean).
  3. `git ls-files --others --exclude-standard -- <each allowlist path>` — captures **untracked** files within the allowlist surface (e.g., a new `src/wanctl/wan_controller_v2.py` that hasn't been added yet). Record into `dirty_tree.untracked` as an array of path strings (empty when clean).
  4. `git status --porcelain -- <each allowlist path>` — captures the porcelain status (`?? path` / ` M path` / `A  path` / etc.) for the allowlist surface. Record into `dirty_tree.status_porcelain` as an array of porcelain lines (empty when clean).

Use the **allowlist paths from `safe12-boundary-check.json.allowlist_paths`** as the path filter for all four commands — both individual file paths and the `src/wanctl/backends/` directory. The directory form catches new untracked backend files (e.g., a stray `backends/new_backend.py`).

Write `per_path_diff` and `dirty_tree` back into the JSON. Do NOT touch any source file. Use ONLY the read-only git commands listed above — no `git apply`, no `git reset`, no `git checkout`, no `git stash`.

This dirty-tree check matches the HRDN-01 pattern from `scripts/check-safe07-source-diff.sh` lines 131-162. The verify-step in Task 222-03-03 fails closed (exit non-zero) when ANY of the four dirty-tree arrays is non-empty, blocking phase closure.</action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert isinstance(d['per_path_diff'], dict); assert set(d['per_path_diff'].keys()) == set(d['allowlist_paths']), 'per_path_diff must cover every allowlist path'; assert all({'added','removed','lines'} &lt;= set(v.keys()) for v in d['per_path_diff'].values()); assert all(isinstance(v['added'], int) and isinstance(v['removed'], int) and isinstance(v['lines'], list) for v in d['per_path_diff'].values()); dt=d['dirty_tree']; assert isinstance(dt, dict); assert {'unstaged','staged','untracked','status_porcelain'} &lt;= set(dt.keys()), f'dirty_tree must have unstaged/staged/untracked/status_porcelain keys, got {sorted(dt.keys())}'; assert all(isinstance(dt[k], list) for k in ('unstaged','staged','untracked','status_porcelain')), 'dirty_tree values must be arrays'"</automated>
  </verify>
  <acceptance_criteria>
    - `per_path_diff` exists in `evidence/safe12-boundary-check.json` and is a JSON object.
    - Every key in `per_path_diff` matches an entry in `allowlist_paths`; the key set equals the array set exactly.
    - Every value has keys `added`, `removed`, `lines`.
    - `added` and `removed` are non-negative integers.
    - `lines` is a JSON array of strings (numstat output lines); the array is empty when the path is unchanged.
    - `dirty_tree` exists in `evidence/safe12-boundary-check.json` and is a JSON object with EXACTLY four keys: `unstaged`, `staged`, `untracked`, `status_porcelain`.
    - `dirty_tree.unstaged` is an array of numstat lines from `git diff --numstat -- <allowlist paths>` (empty array when the working tree is clean against the allowlist).
    - `dirty_tree.staged` is an array of numstat lines from `git diff --cached --numstat -- <allowlist paths>` (empty array when the index is clean against the allowlist).
    - `dirty_tree.untracked` is an array of path strings from `git ls-files --others --exclude-standard -- <allowlist paths>` (empty array when no untracked controller-path files exist).
    - `dirty_tree.status_porcelain` is an array of porcelain status lines from `git status --porcelain -- <allowlist paths>` (empty array when the surface is clean).
    - The artifact was produced by the read-only git commands listed in `<action>` only — no `git apply`, no `git reset`, no `git checkout`, no `git stash`.
    - No source file outside `.planning/phases/222-steering-drift-audit/` was modified.
  </acceptance_criteria>
  <done>Per-path committed-diff data AND dirty-tree (staged/unstaged/untracked/porcelain) data are captured for every allowlist path. The SAFE-12 boundary check now sees the full state — committed history, index, working tree, and untracked files.</done>
</task>

<task type="auto">
  <name>Task 222-03-03: Compute pass/fail verdict and render SAFE-12 markdown</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/222-RESEARCH.md
  </read_first>
  <files>.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json, .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md</files>
  <action>Read `evidence/safe12-boundary-check.json.per_path_diff` AND `evidence/safe12-boundary-check.json.dirty_tree`. Compute the verdict as the AND of two independent sub-checks (matching the SAFE-07 v1.44 Phase 207 / HRDN-01 precedent):

  - `committed_clean = all(v['added'] == 0 and v['removed'] == 0 for v in per_path_diff.values())`
  - `dirty_tree_clean = (len(dirty_tree['unstaged']) == 0 and len(dirty_tree['staged']) == 0 and len(dirty_tree['untracked']) == 0 and len(dirty_tree['status_porcelain']) == 0)`
  - `passed = committed_clean and dirty_tree_clean`

Write `passed`, `committed_clean`, and `dirty_tree_clean` back into the JSON object. Then render `evidence/safe12-boundary-check.md` with sections in order: (1) `## SAFE-12 Invariant` — restate the invariant from 222-RESEARCH.md Section 4 and `.planning/REQUIREMENTS.md` SAFE-12; explicitly note that the check is fail-closed against committed history AND the index AND the working tree AND untracked files (HRDN-01 / SAFE-07 precedent in `scripts/check-safe07-source-diff.sh`). (2) `## Baseline` — table of baseline tag, baseline commit (the v1.47-peeled SHA `bee343b0c2f16207101aec82007a5e55fa9b6407`), HEAD commit, audit timestamp, sourced from the JSON. (3) `## Allowlist` — bullet list of every path in `allowlist_paths`. (4) `## Committed Diff` — markdown table with columns `Path | Added | Removed | Status` where `Status` is `unchanged` if added+removed==0 else `VIOLATION (committed)`. (5) `## Dirty Tree` — four subsections: `### Unstaged`, `### Staged`, `### Untracked`, `### Porcelain Status`, each showing either `(clean)` or the captured lines. Any non-empty array is a `VIOLATION (dirty tree)`. (6) `## Verdict` — single line stating `PASSED` or `FAILED` based on `passed`; if FAILED, enumerate every violating path AND every dirty-tree category that is non-empty. (7) `## Reproducibility` — the exact `git rev-parse v1.47^{commit}`, `git rev-parse HEAD`, `git diff --numstat <baseline>..HEAD`, `git diff --cached --numstat`, `git diff --numstat` (unstaged), `git ls-files --others --exclude-standard`, and `git status --porcelain` commands used. If `passed` is false, the verify-step automated check below MUST fail — i.e., the verify command exits non-zero so phase closure is blocked.</action>
  <verify>
    <automated>test -f .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## SAFE-12 Invariant$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## Committed Diff$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## Dirty Tree$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; grep -q '^## Verdict$' .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md &amp;&amp; .venv/bin/python -c "import json,pathlib,sys; d=json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text()); assert 'passed' in d and isinstance(d['passed'], bool); assert 'committed_clean' in d and isinstance(d['committed_clean'], bool); assert 'dirty_tree_clean' in d and isinstance(d['dirty_tree_clean'], bool); committed_violations={p:v for p,v in d['per_path_diff'].items() if v['added']!=0 or v['removed']!=0}; computed_committed_clean = (len(committed_violations)==0); dt=d['dirty_tree']; computed_dirty_clean = (len(dt['unstaged'])==0 and len(dt['staged'])==0 and len(dt['untracked'])==0 and len(dt['status_porcelain'])==0); computed_passed = computed_committed_clean and computed_dirty_clean; assert d['committed_clean']==computed_committed_clean, f'committed_clean inconsistent: stored={d[\"committed_clean\"]} computed={computed_committed_clean}'; assert d['dirty_tree_clean']==computed_dirty_clean, f'dirty_tree_clean inconsistent: stored={d[\"dirty_tree_clean\"]} computed={computed_dirty_clean}'; assert d['passed']==computed_passed, f'passed flag inconsistent (must be committed_clean AND dirty_tree_clean)'; sys.exit(0 if d['passed'] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` has `committed_clean`, `dirty_tree_clean`, and `passed` set as booleans. `passed` MUST equal `committed_clean AND dirty_tree_clean` — both sub-checks must be clean for SAFE-12 to pass.
    - `committed_clean` is true iff every path in `per_path_diff` has `added == 0` and `removed == 0`.
    - `dirty_tree_clean` is true iff `dirty_tree.unstaged`, `dirty_tree.staged`, `dirty_tree.untracked`, AND `dirty_tree.status_porcelain` are ALL empty arrays.
    - `evidence/safe12-boundary-check.md` contains H2 sections named exactly `## SAFE-12 Invariant`, `## Baseline`, `## Allowlist`, `## Committed Diff`, `## Dirty Tree`, `## Verdict`, `## Reproducibility`.
    - The `## Dirty Tree` section contains four H3 subsections: `### Unstaged`, `### Staged`, `### Untracked`, `### Porcelain Status`. Each renders either `(clean)` or the captured lines.
    - `## Verdict` contains either the literal string `PASSED` or `FAILED`.
    - When `passed == true`, `## Verdict` is `PASSED`, every row in the `## Committed Diff` table has Status `unchanged`, AND every dirty-tree subsection shows `(clean)`.
    - When `passed == false`, `## Verdict` is `FAILED`, every violating committed path is listed with its Added / Removed counts, every non-empty dirty-tree category is enumerated, and the verify-step automated check exits non-zero — blocking Phase 222 closure.
    - The markdown contains the literal commands `git rev-parse v1.47^{commit}`, `git rev-parse HEAD`, `git diff --numstat`, `git diff --cached --numstat`, `git ls-files --others --exclude-standard`, and `git status --porcelain` under `## Reproducibility`.
    - The SAFE-12 Invariant section explicitly cites `scripts/check-safe07-source-diff.sh` (HRDN-01) as the source-of-truth precedent for the fail-closed dirty-tree pattern.
  </acceptance_criteria>
  <done>SAFE-12 boundary check is fully rendered with a deterministic pass/fail verdict; phase closure is blocked if the controller path has drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> SAFE-12 evidence | The boundary check encodes a `git diff` result into a committed JSON / markdown artifact; the artifact must be deterministic from the recorded git commands. |
| 222-RESEARCH.md allowlist -> safe12-boundary-check.json.allowlist_paths | The allowlist is the canonical SAFE-12 surface and must match exactly between the research doc and the JSON manifest. |
| verdict computation -> phase closure | A failed verdict MUST block phase closure via the verify-step exit code; silent passes on a non-zero diff are not permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-222-03-01 | T | controller-path zero-diff invariant (committed) | mitigate | The verify step computes `committed_clean` from `per_path_diff` and exits non-zero on any committed drift; the JSON `committed_clean` field is cross-checked against the computed value. |
| T-222-03-02 | I | allowlist drift between RESEARCH.md and JSON | mitigate | The allowlist in `safe12-boundary-check.json.allowlist_paths` is asserted to include the canonical files; any addition / removal must be reflected in 222-RESEARCH.md Section 4 and committed together. |
| T-222-03-03 | T | git command tampering | mitigate | Reproducibility section records the exact `git rev-parse v1.47^{commit}`, `git rev-parse HEAD`, `git diff --numstat`, `git diff --cached --numstat`, `git ls-files --others --exclude-standard`, and `git status --porcelain` commands; no other git commands are permitted in this plan. |
| T-222-03-04 | T | dirty-tree blind spot (Codex HIGH #2 — staged/unstaged/untracked controller-path edits at phase boundary) | mitigate | `git diff <baseline>..HEAD` alone only sees committed history. The check now ALSO runs `git diff --cached --numstat`, `git diff --numstat` (unstaged), `git ls-files --others --exclude-standard`, and `git status --porcelain` restricted to the SAFE-12 allowlist. `dirty_tree_clean` is true iff all four arrays are empty; `passed` requires `committed_clean AND dirty_tree_clean`. The verify-step automated check fails closed (exits non-zero) when any sub-check fails, blocking Phase 222 closure. Pattern borrowed verbatim from `scripts/check-safe07-source-diff.sh` lines 131-162 (HRDN-01, Phase 207 SAFE-07 precedent). |
</threat_model>

<verification>
Run each task's automated check. The Task 222-03-03 verify step exits
non-zero when SAFE-12 is violated by EITHER committed drift OR dirty-tree
state (staged/unstaged/untracked/porcelain), blocking phase closure. The
fail-closed dirty-tree pattern follows the SAFE-07 v1.44 Phase 207 / HRDN-01
precedent (`scripts/check-safe07-source-diff.sh`). No new tests are
introduced.
</verification>

<success_criteria>
SAFE-12 boundary evidence exists under `evidence/`, parses cleanly, and
either confirms PASSED (zero committed diff against the v1.47-peeled
baseline `bee343b…` AND clean working tree / index / untracked surface
against the allowlist) or surfaces the violating paths / dirty-tree
categories with non-zero verify exit so phase closure is blocked.
</success_criteria>

<output>
After completion, create
`.planning/phases/222-steering-drift-audit/222-03-SUMMARY.md` per the
standard GSD summary template.
</output>
