---
phase: 143-dependency-cruft-cleanup
plan: 03
type: execute
wave: 2
depends_on: [143-02]
files_modified:
  - docs/CONFIG_SCHEMA.md
  - src/wanctl/autorate_continuous.py
  - src/wanctl/check_config.py
  - src/wanctl/health_check.py
  - src/wanctl/wan_controller_state.py
  - src/wanctl/backends/
autonomous: true
requirements: [DEAD-04]

must_haves:
  truths:
    - "Docstrings in priority modules reference only parameters, return types, and modules that currently exist"
    - "Inline comments in src/wanctl/ do not reference removed features or deleted modules"
    - "CONFIG_SCHEMA.md documents all config keys present in the cleaned example files"
    - "CONFIG_SCHEMA.md does not document config keys that were removed from example files"
    - "docs/*.md files do not reference modules deleted in Phase 142 (baseline_rtt_manager, steering_logger)"
    - "All 4,177 existing tests pass unchanged"
  artifacts:
    - path: "docs/CONFIG_SCHEMA.md"
      provides: "Synced config schema documentation"
      contains: "storage.retention"
  key_links:
    - from: "docs/CONFIG_SCHEMA.md"
      to: "configs/examples/*.yaml.example"
      via: "documented keys match example keys"
      pattern: "retention"
---

<objective>
Audit and fix stale docstrings/comments in priority source modules, sync CONFIG_SCHEMA.md with the config key audit findings from Plan 02, and scan docs/*.md for references to deleted modules.

Purpose: Complete DEAD-04 -- all stale annotations, outdated docstrings, dead references in docs, and config schema documentation gaps are resolved.
Output: Corrected docstrings/comments in src/wanctl/, updated CONFIG_SCHEMA.md, cleaned docs/*.md references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-dependency-cruft-cleanup/143-CONTEXT.md
@.planning/phases/143-dependency-cruft-cleanup/143-RESEARCH.md
@.planning/phases/143-dependency-cruft-cleanup/143-02-SUMMARY.md

<interfaces>
<!-- Phase 142 deleted modules (from 142-02-SUMMARY.md) -->
Deleted modules to scan for stale references:
  - baseline_rtt_manager (src/wanctl/baseline_rtt_manager.py -- deleted)
  - steering_logger (src/wanctl/steering_logger.py -- deleted)

<!-- Priority modules for docstring audit (from D-25) -->
Priority modules:
  - src/wanctl/autorate_continuous.py (~5,218 LOC)
  - src/wanctl/check_config.py
  - src/wanctl/health_check.py
  - src/wanctl/wan_controller_state.py
  - src/wanctl/backends/ (all files in directory)

<!-- CONFIG_SCHEMA.md gaps identified in research -->
CONFIG_SCHEMA.md gaps to fix:
  - storage.retention.* sub-keys: only documents deprecated retention_days, needs new retention section
  - owd_asymmetry section: not documented at all
  - fusion.healing sub-section: not documented
  - ping_source_ip: not documented
  - irtt key names: documents duration_ms via examples, needs correction to duration_sec
  - irtt.packet_size: documented but removed, needs removal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix stale docstrings and inline comments in priority modules</name>
  <files>src/wanctl/autorate_continuous.py, src/wanctl/check_config.py, src/wanctl/health_check.py, src/wanctl/wan_controller_state.py, src/wanctl/backends/</files>
  <read_first>
    - src/wanctl/autorate_continuous.py (module docstring, class docstrings, method docstrings -- focus on Config class methods and WanController class methods)
    - src/wanctl/check_config.py (module docstring, function docstrings)
    - src/wanctl/health_check.py (module docstring, function docstrings)
    - src/wanctl/wan_controller_state.py (module docstring, class docstrings)
    - src/wanctl/backends/ (list files, read each for docstrings)
  </read_first>
  <action>
Audit docstrings and inline comments in the priority modules per D-25 through D-30.

**What to fix (D-26 -- factually wrong content only):**
- Docstrings that reference parameters that no longer exist on the function/method
- Docstrings that describe wrong return types (e.g., says "returns dict" but actually returns a dataclass)
- Docstrings that reference deleted modules (baseline_rtt_manager, steering_logger, or any other modules removed in Phase 142)
- Docstrings that reference removed features or changed behavior (e.g., references to removed CommandResult methods like unwrap/is_ok)
- Inline comments referencing features/modules/functions that no longer exist

**What NOT to fix (D-26, D-28, D-29):**
- Mediocre but not factually wrong docstrings -- leave them
- Missing docstrings -- don't add new ones (that's Phase 149 TYPE scope)
- Version references like "Added in v1.12" -- these are historical context, keep them
- Don't remove docstrings, only correct them (D-28)
- Test file docstrings -- Phase 146/148 scope (D-30)

**Also scan for stale inline comments (D-27):**
- Comments like "# TODO: wire up baseline_rtt_manager" (deleted module)
- Comments referencing removed functions (unwrap, is_ok, enforce_floor, enforce_ceiling)
- Comments referencing old variable names or removed class attributes

**Scope:** src/wanctl/ only (D-30). Start with the 5 priority modules from D-25 (including all files in src/wanctl/backends/). After those, do a quick grep across all of src/wanctl/ for references to deleted modules/functions:
```
grep -rn "baseline_rtt_manager\|steering_logger\|unwrap\|is_ok\|is_err\|enforce_floor\|enforce_ceiling" src/wanctl/ --include="*.py"
```
Fix any stale references found. Ignore hits in the vulture whitelist (that's expected).

For each fix, make the minimal correction. Example:
- Before: "Uses baseline_rtt_manager to track RTT" -> After: "Tracks RTT via BackgroundRTTThread"
- Before: "Returns CommandResult (use .unwrap() to get value)" -> After: "Returns CommandResult (use .success and .value to access)"
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && grep -rn "baseline_rtt_manager\|steering_logger" src/wanctl/ --include="*.py" | grep -v "vulture_whitelist\|__pycache__" | wc -l | xargs -I{} test {} -eq 0 && echo "No stale module references" && .venv/bin/pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "baseline_rtt_manager" src/wanctl/ --include="*.py" returns zero matches (excluding vulture_whitelist.py)
    - grep -rn "steering_logger" src/wanctl/ --include="*.py" returns zero matches (excluding vulture_whitelist.py)
    - No docstring in priority modules references parameters that don't exist on that function
    - backends/ files audited and corrected if stale docstrings found (per D-25)
    - Version references in docstrings (e.g., "Added in v1.12") are preserved (D-29)
    - No docstrings have been removed, only corrected (D-28)
    - .venv/bin/pytest tests/ -x -q exits 0 (no regression)
  </acceptance_criteria>
  <done>
    Priority modules audited for stale docstrings and comments, including all files in src/wanctl/backends/ (per D-25). All references to deleted modules (baseline_rtt_manager, steering_logger) removed from src/wanctl/. All factually wrong docstrings corrected. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync CONFIG_SCHEMA.md and scan docs/*.md for stale references</name>
  <files>docs/CONFIG_SCHEMA.md</files>
  <read_first>
    - docs/CONFIG_SCHEMA.md (full file -- 874 lines, need to understand current structure)
    - .planning/phases/143-dependency-cruft-cleanup/143-02-SUMMARY.md (to see what config key changes were made in Plan 02)
    - configs/examples/cable.yaml.example (post-Plan-02 state -- this is the reference for what keys exist)
    - src/wanctl/config_base.py (get_storage_config around lines 168-217 for storage.retention defaults and descriptions)
    - src/wanctl/autorate_continuous.py (around lines 903-932 for owd_asymmetry defaults, around lines 967-1038 for fusion.healing defaults)
  </read_first>
  <action>
**Part A: Sync CONFIG_SCHEMA.md with audit findings (D-22)**

Read CONFIG_SCHEMA.md and update it to match the cleaned example files from Plan 02. The specific gaps identified in research:

1. **storage.retention section:** Replace the deprecated `retention_days` documentation with the new `storage.retention.*` sub-keys. Document these keys with types, defaults, and descriptions from get_storage_config in config_base.py:
   - storage.retention.raw_age_seconds (int, default from code)
   - storage.retention.aggregate_1m_age_seconds (int, default from code)
   - storage.retention.aggregate_5m_age_seconds (int, default from code)
   - storage.retention.prometheus_compensated (bool, default from code)
   Read the actual defaults from config_base.py rather than guessing.

2. **owd_asymmetry section:** Add a new section documenting owd_asymmetry and its sub-keys. Read _load_owd_asymmetry_config for field names, types, and defaults.

3. **fusion.healing sub-section:** Add healing parameters to the existing fusion section. Read _load_fusion_config for the 5 healing sub-keys, their types, and defaults.

4. **ping_source_ip:** Add to the appropriate section (likely common fields or ping section). Type: optional string, default: None/empty.

5. **IRTT key corrections:** If CONFIG_SCHEMA.md references `duration_ms`, correct to `duration_sec`. If it references `packet_size`, remove that documentation.

6. **Deprecated key cleanup:** If CONFIG_SCHEMA.md documents `alpha_baseline` or `alpha_load` as current keys, update to document `baseline_time_constant_sec` and `load_time_constant_sec` instead. It's acceptable to add a "Deprecated" note mentioning the old name for migration reference.

7. **Dead key removal:** Remove documentation for any keys that were removed from example files in Plan 02.

Follow the existing documentation style in CONFIG_SCHEMA.md (table format, field descriptions, YAML examples).

**Part B: Scan docs/*.md for stale references (D-31)**

Quick scan of all docs/*.md files for references to modules deleted in Phase 142 and config keys removed in Plan 02:

```
grep -rn "baseline_rtt_manager\|steering_logger\|alpha_baseline\|alpha_load\|duration_ms\|packet_size" docs/ --include="*.md"
```

For each hit:
- If it references a deleted module: update to reference the replacement or remove the reference
- If it references a deprecated/removed config key: update to the current key name
- If it's historical context in a changelog-style doc: leave it (version history is valid)

Do NOT rewrite entire doc sections -- make targeted corrections only.
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && grep -c "retention" docs/CONFIG_SCHEMA.md | xargs -I{} test {} -gt 0 && echo "retention documented" && grep -c "owd_asymmetry" docs/CONFIG_SCHEMA.md | xargs -I{} test {} -gt 0 && echo "owd_asymmetry documented" && .venv/bin/pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - docs/CONFIG_SCHEMA.md contains "raw_age_seconds" (storage.retention sub-key documented)
    - docs/CONFIG_SCHEMA.md contains "owd_asymmetry" (new section added)
    - docs/CONFIG_SCHEMA.md contains "healing" in the fusion section (fusion.healing documented)
    - docs/CONFIG_SCHEMA.md contains "ping_source_ip" (documented)
    - docs/CONFIG_SCHEMA.md contains "duration_sec" (not "duration_ms") in IRTT section
    - docs/CONFIG_SCHEMA.md does not contain "packet_size" as a current config key
    - docs/CONFIG_SCHEMA.md contains "baseline_time_constant_sec" (current name, not just deprecated alpha_baseline)
    - grep -rn "baseline_rtt_manager\|steering_logger" docs/ --include="*.md" returns zero matches (or only historical changelog entries)
    - .venv/bin/pytest tests/ -x -q exits 0 (no regression)
  </acceptance_criteria>
  <done>
    CONFIG_SCHEMA.md synced with all config key audit findings -- storage.retention, owd_asymmetry, fusion.healing, ping_source_ip documented; stale key names corrected; dead keys removed. docs/*.md files scanned and stale references fixed. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan modifies documentation files and source code comments/docstrings. No behavioral changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-04 | Information Disclosure | CONFIG_SCHEMA.md | accept | Config documentation is public. Adding missing key documentation improves usability. No secrets documented. |
</threat_model>

<verification>
- `make ci` passes
- `.venv/bin/pytest tests/ -x -q` passes
- No references to deleted modules in src/wanctl/ or docs/ (excluding vulture_whitelist.py and historical changelogs)
- CONFIG_SCHEMA.md documents all sections that appear in cleaned example files
</verification>

<success_criteria>
DEAD-04 (docstring/comment/doc staleness portion) satisfied: priority module docstrings corrected, stale inline comments fixed, CONFIG_SCHEMA.md synced with config key audit, docs/*.md references to deleted modules/keys cleaned up.
</success_criteria>

<output>
After completion, create `.planning/phases/143-dependency-cruft-cleanup/143-03-SUMMARY.md`
</output>
