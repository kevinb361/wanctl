---
phase: 68-dead-code-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/spectrum_config.yaml
  - configs/att_config.yaml
  - configs/spectrum_config_v2.yaml
  - configs/.obsolete/att_config_v2.yaml
  - configs/dad_fiber_config.yaml
  - configs/att_binary_search.yaml
  - configs/spectrum_binary_search.yaml
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [LGCY-05]

must_haves:
  truths:
    - "No obsolete ISP-specific config files exist in configs/ directory"
    - "The .obsolete/ subdirectory no longer exists"
    - "docs/ARCHITECTURE.md references only current config filenames"
    - "Active config files (spectrum.yaml, att.yaml, steering.yaml) are untouched"
  artifacts:
    - path: "configs/spectrum.yaml"
      provides: "Active Spectrum config (must NOT be deleted)"
    - path: "configs/att.yaml"
      provides: "Active ATT config (must NOT be deleted)"
    - path: "configs/steering.yaml"
      provides: "Active steering config (must NOT be deleted)"
  key_links:
    - from: "docs/ARCHITECTURE.md"
      to: "configs/spectrum.yaml"
      via: "File reference in ISP config section"
      pattern: "configs/spectrum\\.yaml"
---

<objective>
Remove 7 obsolete ISP-specific config files from the `configs/` directory and update documentation references.

Purpose: These files are superseded by the modern FHS-compliant configs (spectrum.yaml, att.yaml, steering.yaml). Removing them eliminates confusion about which configs are current. Satisfies LGCY-05.

Output: Clean configs/ directory with only active configs, examples, and logrotate. Updated ARCHITECTURE.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67/AUDIT.md

@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete config files</name>
  <files>
    configs/spectrum_config.yaml
    configs/att_config.yaml
    configs/spectrum_config_v2.yaml
    configs/.obsolete/att_config_v2.yaml
    configs/dad_fiber_config.yaml
    configs/att_binary_search.yaml
    configs/spectrum_binary_search.yaml
  </files>
  <action>
Delete the following 7 obsolete config files identified in the Phase 67 AUDIT.md:

```bash
git rm configs/spectrum_config.yaml
git rm configs/att_config.yaml
git rm configs/spectrum_config_v2.yaml
git rm configs/.obsolete/att_config_v2.yaml
git rm configs/dad_fiber_config.yaml
git rm configs/att_binary_search.yaml
git rm configs/spectrum_binary_search.yaml
```

Then remove the now-empty `.obsolete/` directory:
```bash
rmdir configs/.obsolete/
```

**SAFETY CHECK before deleting:** Verify no Python source file imports or references any of these filenames:
```bash
grep -rn "spectrum_config\|att_config\|dad_fiber_config\|att_binary_search\|spectrum_binary_search" src/ tests/
```
Expected: 0 matches (confirmed during planning -- no Python code references these files).

**Do NOT delete:** spectrum.yaml, att.yaml, steering.yaml, examples/, logrotate-wanctl -- these are active.
  </action>
  <verify>
    <automated>test ! -f configs/spectrum_config.yaml && test ! -f configs/att_config.yaml && test ! -f configs/spectrum_config_v2.yaml && test ! -f configs/.obsolete/att_config_v2.yaml && test ! -f configs/dad_fiber_config.yaml && test ! -f configs/att_binary_search.yaml && test ! -f configs/spectrum_binary_search.yaml && test ! -d configs/.obsolete && test -f configs/spectrum.yaml && test -f configs/att.yaml && test -f configs/steering.yaml && echo "PASS: obsolete files removed, active files intact"</automated>
  </verify>
  <done>
    - 7 obsolete config files deleted
    - `.obsolete/` directory removed
    - Active configs (spectrum.yaml, att.yaml, steering.yaml) still present
    - examples/ directory still present
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ARCHITECTURE.md config file references</name>
  <files>docs/ARCHITECTURE.md</files>
  <action>
Update docs/ARCHITECTURE.md to reference current config filenames instead of obsolete ones.

**Line 45-46 (verification command examples):**
Change:
```
wan-spectrum: python -m wanctl.autorate_continuous --config spectrum_config.yaml
wan-att:      python -m wanctl.autorate_continuous --config att_config.yaml
```
To:
```
wan-spectrum: python -m wanctl.autorate_continuous --config spectrum.yaml
wan-att:      python -m wanctl.autorate_continuous --config att.yaml
```

**Line 365 (Cable/DOCSIS section):**
Change: `**File:** \`configs/spectrum_config.yaml\``
To: `**File:** \`configs/spectrum.yaml\``

**Line 383 (VDSL2 DSL section):**
Change: `**File:** \`configs/att_config.yaml\``
To: `**File:** \`configs/att.yaml\``

**Line 401 (GPON Fiber section):**
Change: `**File:** \`configs/dad_fiber_config.yaml\` (template exists)`
To: `**File:** \`configs/examples/fiber.yaml.example\` (template)`
(The dad_fiber_config.yaml is deleted; the examples/ directory has fiber.yaml.example as the current template.)

**Do NOT modify** any other sections of ARCHITECTURE.md -- the architectural content is correct.
  </action>
  <verify>
    <automated>grep -c "spectrum_config\|att_config\|dad_fiber_config" docs/ARCHITECTURE.md | grep -q "^0$" && echo "PASS: no obsolete references" || echo "FAIL: obsolete references remain"</automated>
  </verify>
  <done>
    - ARCHITECTURE.md references only current config filenames
    - No references to spectrum_config.yaml, att_config.yaml, or dad_fiber_config.yaml
  </done>
</task>

</tasks>

<verification>
1. `ls configs/` shows only: att.yaml, examples/, logrotate-wanctl, spectrum.yaml, steering.yaml
2. `grep -rn "spectrum_config\|att_config\|dad_fiber_config\|att_binary_search\|spectrum_binary_search" docs/ARCHITECTURE.md` returns 0 matches
3. `.venv/bin/pytest tests/ -x -q` passes (no test depends on removed config files)
</verification>

<success_criteria>
- configs/ directory contains exactly: att.yaml, examples/, logrotate-wanctl, spectrum.yaml, steering.yaml
- No .obsolete/ subdirectory exists
- docs/ARCHITECTURE.md references only current config filenames (spectrum.yaml, att.yaml, fiber.yaml.example)
- All tests pass unchanged (no test references removed files)
</success_criteria>

<output>
After completion, create `.planning/phases/68-dead-code-removal/68-02-SUMMARY.md`
</output>
