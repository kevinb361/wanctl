---
phase: 168-storage-and-runtime-pressure-monitoring
plan: 02
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - src/wanctl/runtime_pressure.py
  - src/wanctl/metrics.py
  - src/wanctl/health_check.py
  - src/wanctl/steering/health.py
  - tests/test_runtime_pressure.py
  - tests/test_health_check.py
  - tests/steering/test_steering_health.py
  - tests/test_metrics.py
autonomous: false
requirements: [OPER-01, OPER-02, OPER-03]
must_haves:
  truths:
    - "Healthy live services can expose the new storage/runtime pressure signals without showing false distress."
    - "Bounded pressure payloads remain stable and useful when files are missing, metrics are disabled, or one signal is unavailable."
    - "Phase 168 closes with evidence that the new visibility is useful for operators and does not add observable hot-path cost."
  artifacts:
    - path: "tests/test_runtime_pressure.py"
      provides: "Edge-case coverage for unavailable DB/WAL/runtime inputs and bounded status output."
    - path: "tests/test_health_check.py"
      provides: "Autorate health validation for healthy-state pressure summaries."
    - path: "tests/steering/test_steering_health.py"
      provides: "Steering health validation for matching pressure summaries."
    - path: "src/wanctl/runtime_pressure.py"
      provides: "Finalized pressure classification semantics used by health and metrics."
  key_links:
    - from: "tests/test_health_check.py"
      to: "src/wanctl/health_check.py"
      via: "healthy and unavailable pressure summary assertions"
      pattern: "pressure|storage|runtime"
    - from: "tests/steering/test_steering_health.py"
      to: "src/wanctl/steering/health.py"
      via: "steering parity checks"
      pattern: "pressure|storage|runtime"
    - from: "manual live checks"
      to: "9101/health, 9102/health, 9100/metrics"
      via: "bounded production sanity sampling"
      pattern: "curl"
---

<objective>
Validate that the new Phase 168 pressure signals stay bounded, stay useful to operators, and read cleanly on healthy live services without forcing a production failure.

Purpose: close the phase with confidence that the new visibility helps future alerting/canary work instead of creating noisy or misleading pressure output.
Output: tightened regression coverage plus a live operator sanity gate on current DB/WAL/runtime state.
</objective>

<execution_context>
@/home/kevin/.codex/get-shit-done/workflows/execute-plan.md
@/home/kevin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/168-storage-and-runtime-pressure-monitoring/168-CONTEXT.md
@.planning/phases/165-storage-write-contention-observability-and-db-topology-decis/165-02-SUMMARY.md
@.planning/phases/167-latency-and-burst-regression-alerts/167-02-SUMMARY.md
@.planning/phases/168-storage-and-runtime-pressure-monitoring/168-01-PLAN.md
@src/wanctl/runtime_pressure.py
@src/wanctl/health_check.py
@src/wanctl/steering/health.py
@src/wanctl/metrics.py
@tests/test_runtime_pressure.py
@tests/test_health_check.py
@tests/steering/test_steering_health.py
@tests/test_metrics.py

<interfaces>
Expected bounded outputs from Plan 01:
```python
{
    "storage": {... existing bounded counters ...},
    "pressure": {
        "storage": {"status": "...", "reasons": [...]},
        "runtime": {"status": "...", "reasons": [...]},
    }
}
```

Prometheus contract:
```text
wanctl_*pressure*
wanctl_*db*_bytes
wanctl_*wal*_bytes
wanctl_*memory*_bytes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validation coverage for healthy-state, unavailable-input, and bounded-output behavior</name>
  <files>tests/test_runtime_pressure.py, tests/test_health_check.py, tests/steering/test_steering_health.py, tests/test_metrics.py</files>
  <behavior>
    - Test 1: healthy-state snapshots stay readable and do not report false critical pressure on nominal DB/WAL/runtime inputs.
    - Test 2: missing DB/WAL files, disabled metrics, or unavailable procfs data collapse to bounded unavailable/warn output rather than exceptions or unstable payloads.
    - Test 3: status and severity fields remain stable enough for later canary/runbook consumers.
  </behavior>
  <action>
Extend the Plan 01 tests so the new pressure surfaces prove operator usefulness per D-03/D-06/D-07. Validate the realistic branches that matter for production:
- healthy steady-state snapshots
- unavailable or partially missing file/runtime data
- metrics-disabled environments where `/health` still has to carry the signal
Keep the tests bounded and read-only. Do not add synthetic failure logic that rewires the control loop or tries to create host pressure in CI.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_runtime_pressure.py tests/test_health_check.py tests/steering/test_steering_health.py tests/test_metrics.py -q</automated>
  </verify>
  <done>Regression coverage proves the new pressure signals are stable across healthy and partially unavailable environments.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten pressure classification semantics only where validation shows ambiguity</name>
  <files>src/wanctl/runtime_pressure.py, src/wanctl/metrics.py, src/wanctl/health_check.py, src/wanctl/steering/health.py, tests/test_runtime_pressure.py, tests/test_health_check.py, tests/steering/test_steering_health.py, tests/test_metrics.py</files>
  <action>
If Task 1 shows noisy statuses, weak reason text, or inconsistent autorate/steering semantics, tighten only the pressure classification and export details required to make the phase usable. Keep changes inside the Phase 168 boundary:
- no new persistence path
- no alert policy redesign
- no Phase 169 payload redesign
- no control-loop retuning
Favor deterministic status mappings and short bounded reason lists that future phases can consume directly.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_runtime_pressure.py tests/test_health_check.py tests/steering/test_steering_health.py tests/test_metrics.py -q</automated>
    <automated>.venv/bin/mypy src/wanctl/runtime_pressure.py src/wanctl/metrics.py src/wanctl/health_check.py src/wanctl/steering/health.py</automated>
  </verify>
  <done>Pressure classifications are deterministic, bounded, and aligned between health and metrics surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the healthy-live pressure sanity gate on cake-shaper</name>
  <what-built>Bounded storage/runtime pressure summaries and metrics for autorate and steering.</what-built>
  <action>
Run a healthy-state production sanity check only, per D-07. Use current DB/WAL/runtime state from the live host, confirm the new signals remain interpretable under normal service conditions, and explicitly avoid any forced host pressure or control-loop disturbance.
  </action>
  <how-to-verify>
    1. On the live host, confirm healthy services remain up: `systemctl is-active wanctl@spectrum`, `systemctl is-active wanctl@att`, and `systemctl is-active steering.service`.
    2. Capture at least two healthy samples from `http://127.0.0.1:9101/health` on each autorate instance and `http://127.0.0.1:9102/health` for steering. Inspect the new pressure summaries plus existing `storage` and `cycle_budget` sections.
    3. If `metrics.enabled` is active on the host, capture `/metrics` and confirm the new DB/WAL/memory/pressure metrics are present and bounded. If metrics are disabled, record that health-only validation was used.
    4. Confirm the new signals reflect current DB/WAL/runtime state, stay readable in healthy service, and do not require forced host pressure to interpret.
    5. Record one of:
       - `approved: pressure signals ready`
       - `approved: ready with follow-up surface polish in Phase 169`
       - `blocked: noisy or misleading pressure output`
  </how-to-verify>
  <resume-signal>Type `approved: ...` or `blocked: ...` with the observed host behavior.</resume-signal>
  <verify>
    <automated>echo 'manual healthy-live pressure gate ready'</automated>
  </verify>
  <done>Manual validation records a clear readiness decision based on healthy live services and current DB/WAL/runtime state, without forcing a failure on the host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/unit inputs -> pressure classifier | Tests inject missing-file and unavailable-runtime cases that must remain bounded and deterministic. |
| live host -> operator judgment | Manual validation consumes health and optional metrics payloads from live services without forced failure injection. |
| pressure summaries -> future phases | Later canary and policy work will depend on the status fields created here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-05 | D | pressure classification tests | mitigate | Cover healthy and unavailable branches so later changes cannot silently turn every scrape into warning/critical output. |
| T-168-06 | I | live health payloads | mitigate | Manual gate checks bounded summaries only; no raw host dumps or forced failure capture on production. |
| T-168-07 | R | future consumers of status fields | mitigate | Lock the status/reason vocabulary with regression tests before closing the phase. |
| T-168-08 | E | phase scope | accept | Any alert-policy, runbook, or dashboard redesign is deferred to Phases 169-171 and explicitly excluded here. |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_runtime_pressure.py tests/test_health_check.py tests/steering/test_steering_health.py tests/test_metrics.py -q`
- `.venv/bin/mypy src/wanctl/runtime_pressure.py src/wanctl/metrics.py src/wanctl/health_check.py src/wanctl/steering/health.py`
- manual healthy-live pressure gate on `cake-shaper`
</verification>

<success_criteria>
- Healthy live services expose the new pressure signals without false distress or payload confusion.
- Unavailable DB/WAL/runtime inputs degrade to bounded output rather than exceptions or noisy raw detail.
- Phase 168 closes with operator-visible evidence that the new signals are useful and low-overhead.
</success_criteria>

<output>
After completion, create `.planning/phases/168-storage-and-runtime-pressure-monitoring/168-02-SUMMARY.md`
</output>
