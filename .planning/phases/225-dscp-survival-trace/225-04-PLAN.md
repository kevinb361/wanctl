---
phase: 225
plan: "04"
title: "Gap closure: harden DSCP ingress capture (injection, falsifiable wash proof, honest DL source proof)"
wave: 1
gap_closure: true
depends_on: []
requirements:
  - DSCP-02
  - DSCP-03
  - SAFE-13
files_modified:
  - scripts/phase225-dscp-ingress-capture.sh
autonomous: true
must_haves:
  truths:
    - "Closes GAP-1 (truth #8, read-only/safety): operator-supplied `--probe-target` and `--probe-port` cannot inject remote shell commands. `--probe-target` is validated against an allowlist `^[A-Za-z0-9_.:-]+$` and `--probe-port` is validated as an integer in 1-65535 BEFORE any SSH command string is built or any remote command runs; an unsafe value (e.g. `a;rm -rf /`) is rejected with exit code 2 and no SSH invocation occurs. The validation lives at argument-parse/validation time, ahead of `capture_probe_window`."
    - "Closes GAP-2 (truths #3/#4, falsifiable capture-point proof): the script NEVER records `WASH_PROOF_PASS=true` / `WASH_ORDERING_PROVEN=true` / `CAPTURE_POINT=pre_wash_ingress` from bridge topology rule text plus CAKE qdisc PRESENCE alone. `derive_topology_and_proof` keeps `WASH_PROOF_PASS=false`, `WASH_ORDERING_PROVEN=false`, `CAPTURE_POINT=unknown` UNLESS a machine-checkable predicate passes: either parsed `qdisc_ordering` establishing the capture hook is upstream of the spec-router CAKE-egress wash qdisc, OR `paired_bitflip` (pre-wash DSCP set, post-wash same 5-tuple cleared). Topology text and qdisc presence are recorded only as supporting context, never as the pass condition. The fix moves STRICTLY toward harder-to-prove: any path that previously set the booleans true from topology+handle presence now resolves to `unknown`/`false` unless the explicit predicate is satisfied."
    - "Closes GAP-3 (truth #4/#5, honest DL source proof): the `--probe dl`/`--probe both` path NEVER silently truncates `raw/dl-ef-probe-source.pcap` to an empty file and then analyzes it as if it were a capture. EITHER the script accepts explicit source-side capture controls (`--dl-source-ssh-host`, `--dl-source-iface`, plus direction/filter) and runs a bounded source-side tcpdump for the SAME probe 5-tuple, OR — when those controls are absent — it emits an explicit `SRC_CAPTURE_POINT=unsupported` / `DL_SOURCE_EF_PROVEN=false` honest-degrade signal WITHOUT writing an empty pcap that masquerades as a real capture. The 225-02 invariant holds: `EF_SURVIVED=false`/`STRIPPED` for `dl_ef_probe` is recorded ONLY when `DL_SOURCE_EF_PROVEN=true`."
    - "Preserves read-only / SAFE-13 posture: the script remains `tcpdump`-only with no `tc filter/qdisc add|del|replace`, no `nft add|delete|flush|-f`, no CAKE diffserv/wash mutation, and references no controller-path source file (`wan_controller`, `queue_controller`, `cake_signal`, `alert_engine`, `fusion_healer`, `backends/`). Any new source-side capture uses bounded read-only tcpdump over SSH exactly like the existing windows."
    - "The fix direction is fail-safe everywhere: every change makes a proof HARDER to assert true and defaults an unproven/unsupported channel to `unknown`/`degraded`/`false`. No gate is loosened to make a previously-failing branch pass."
  artifacts:
    - "scripts/phase225-dscp-ingress-capture.sh — hardened: probe-arg validation gate, predicate-gated wash proof, honest DL source-side proof (real capture or explicit unsupported degrade), shellcheck-clean, mutation-free, controller-path-free."
  key_links:
    - from: "scripts/phase225-dscp-ingress-capture.sh probe-arg validation"
      to: "capture_probe_window SSH command construction"
      via: "validation gate runs before any remote command is built"
      pattern: "PROBE_TARGET.*A-Za-z0-9_.:-"
    - from: "derive_topology_and_proof"
      to: "capture-point-proof.txt WASH_PROOF_PASS/WASH_ORDERING_PROVEN/CAPTURE_POINT"
      via: "machine-checkable predicate (qdisc_ordering or paired_bitflip), not topology text"
      pattern: "WASH_PROOF_PASS"
---

<objective>
Close the three blocking/warning DSCP-02 gaps in `scripts/phase225-dscp-ingress-capture.sh` found by
Phase 225 verification (225-VERIFICATION.md gaps; 225-REVIEW.md CR-01 / WR-01 / WR-02). These are
conservative, fail-safe hardening fixes to a read-only DIAGNOSTIC/EVIDENCE script — they do NOT touch
the 50ms controller path. Every fix tightens a proof or input gate: it must default an unproven or
unsupported condition to `unknown`/`degraded`/`false`, never loosen a gate to make a branch pass.

Purpose: restore the read-only/safety guarantee (GAP-1), make the pre-wash capture point falsifiably
proven instead of asserted from topology prose (GAP-2), and make the DL source-side EF proof honest —
a real source-side capture or an explicit unsupported-degrade — instead of an empty file that
masquerades as a capture (GAP-3).
Output: a hardened `scripts/phase225-dscp-ingress-capture.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/225-dscp-survival-trace/225-VERIFICATION.md
@.planning/phases/225-dscp-survival-trace/225-REVIEW.md
@.planning/phases/225-dscp-survival-trace/225-02-PLAN.md
@.planning/phases/225-dscp-survival-trace/225-RESEARCH.md

# Current code under repair (READ before editing — line numbers in the gaps reference this file):
@scripts/phase225-dscp-ingress-capture.sh

<interfaces>
<!-- Identifiers the executor must preserve / produce. Do NOT rename existing emitted fields the -->
<!-- 225-03 verdict re-derives from; only TIGHTEN the conditions under which they are set true.   -->

Probe args (parsed in the while/case loop ~lines 380-395, validated in the block ~lines 397-406):
  PROBE_TARGET   (string; currently interpolated into remote SSH at lines 312-319 — UNSAFE)
  PROBE_PORT     (string; currently interpolated unvalidated)
  PROBE          (none|ul|dl|both), PROBE_PROTO (udp|tcp)

Wash-proof fields emitted by derive_topology_and_proof() (~lines 328-378) into capture-point-proof.txt:
  WASH_PROOF_METHOD={paired_bitflip|qdisc_ordering}
  WASH_PROOF_PASS=true|false
  WASH_ORDERING_PROVEN=true|false       (and DL_/UL_ prefixed variants)
  CAPTURE_POINT=pre_wash_ingress|unknown (and DL_/UL_ prefixed variants)
  HOOK_PARENT=, WASH_QDISC_HANDLE=       (supporting datum)
  PRE_WASH_DSCP=, POST_WASH_DSCP=        (paired_bitflip supporting datum, if used)

DL source-proof fields emitted into dl-ef-probe-result.txt (analyzer probe_summary, ~lines 203-250;
DL branch ~lines 491-494):
  SRC_PROBE_PKTS_TOTAL=, SRC_EF_PKTS=, EF_PKTS_AT_SOURCE=, SRC_EF_PCT=
  SRC_CAPTURE_POINT=, SRC_ENQUEUE_5TUPLE_MATCH=true|false
  DL_SOURCE_EF_PROVEN=true|false
  (analyzer already derives DL_SOURCE_EF_PROVEN from src_total>=100 AND src_ef_pct>=90 AND src_match
   AND source pcap existing — the bug is the SHELL truncating the source pcap to empty before analysis)
</interfaces>
</context>

<tasks>

<task id="225-04-1" type="auto">
<name>Task 1: Add a probe-argument validation gate that runs before any SSH command (GAP-1 / CR-01)</name>
<files>scripts/phase225-dscp-ingress-capture.sh</files>
<read_first>
- scripts/phase225-dscp-ingress-capture.sh   (lines ~303-326 capture_probe_window; ~380-406 arg parse + validation block)
- .planning/phases/225-dscp-survival-trace/225-REVIEW.md   (CR-01 — concrete validation snippet and rationale)
- .planning/phases/225-dscp-survival-trace/225-VERIFICATION.md   (gap 1 — truth #8 read-only/safety)
</read_first>
<action>
In the post-parse validation block (the section that already validates PROBE / PROBE_PROTO and
requires PROBE_TARGET when PROBE is not none, around lines 397-406), add a validation gate that runs
BEFORE `derive_topology_and_proof`, before `run_ssh_capture`, and before `capture_probe_window` — i.e.
before any SSH command string is constructed. Reject `PROBE_TARGET` when it is non-empty and does not
match the allowlist `^[A-Za-z0-9_.:-]+$`; reject `PROBE_PORT` when it is not an integer or is outside
1-65535. On rejection, print a clear `ERROR:` message to stderr and `exit 2`. Apply the same allowlist
defensiveness to any other operator-supplied value that is interpolated into a remote command string
(e.g. `--ssh-host` host token and `--duration`/`--packet-cap` numerics) so no probe/capture path
builds a remote command from an unvalidated metacharacter-bearing value. Do NOT change the probe
behavior for valid inputs; only reject unsafe ones up front. Reference: 225-REVIEW.md CR-01 has the
exact allowlist/range conditions; place the gate so it dominates `capture_probe_window` (lines 312-319)
which is where the unsafe interpolation lives.
</action>
<verify>
  <automated>bash -n scripts/phase225-dscp-ingress-capture.sh && shellcheck scripts/phase225-dscp-ingress-capture.sh && scripts/phase225-dscp-ingress-capture.sh --output-dir /tmp/opencode/p225g-inj --probe ul --probe-target 'a;rm -rf /' --probe-port 5201; test $? -eq 2</automated>
</verify>
<acceptance_criteria>
- `bash -n scripts/phase225-dscp-ingress-capture.sh` exits 0.
- `shellcheck scripts/phase225-dscp-ingress-capture.sh` exits 0 (no errors).
- Running the script with `--probe ul --probe-target 'a;rm -rf /' --probe-port 5201` exits 2 and prints an `ERROR:` about an unsupported `--probe-target` value, with NO SSH invocation (no `cake-shaper` connection attempted) and NO output dir populated.
- Running with `--probe ul --probe-target good.host --probe-port 70000` exits 2 with an `ERROR:` that `--probe-port` must be an integer 1-65535.
- The validation block textually precedes the first `ssh ` invocation and the `capture_probe_window`/`run_ssh_capture` calls in the script (gate dominates command construction).
- A valid invocation (e.g. `--probe-target 10.10.110.50 --probe-port 5201`) is not rejected by the new gate (the gate only rejects unsafe values).
</acceptance_criteria>
<done>
Operator-supplied probe arguments are allowlist/range-validated before any remote command is built;
an injection attempt is rejected with exit 2 and never reaches SSH. Read-only/safety guarantee
(truth #8) restored.
</done>
</task>

<task id="225-04-2" type="auto">
<name>Task 2: Gate the wash-ordering proof on a machine-checkable predicate, not topology text (GAP-2 / WR-01)</name>
<files>scripts/phase225-dscp-ingress-capture.sh</files>
<read_first>
- scripts/phase225-dscp-ingress-capture.sh   (lines ~328-378 derive_topology_and_proof; ~432-438 direction-flag derivation + proof call)
- .planning/phases/225-dscp-survival-trace/225-REVIEW.md   (WR-01 — proof must rest on a falsifiable predicate)
- .planning/phases/225-dscp-survival-trace/225-02-PLAN.md   (must_have truths #2 and #6 — falsifiable capture-point + auditable WASH_ORDERING_PROVEN)
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md   (lines ~214-246 — falsifiable CAPTURE_POINT rule)
</read_first>
<action>
Rewrite the proof-pass logic in `derive_topology_and_proof` so `dl_pass`/`ul_pass` (and therefore
`WASH_PROOF_PASS`, `WASH_ORDERING_PROVEN`, `CAPTURE_POINT`, plus their `DL_`/`UL_` variants) are set
true ONLY when a concrete machine-checkable predicate passes — NOT from `topology_ok` (bridge rule
text `iif spec-modem oif spec-router`) plus mere PRESENCE of a CAKE qdisc handle. Currently lines
340-347 set `dl_pass=true` / `CAPTURE_POINT=pre_wash_ingress` from topology text + a non-empty
`dl_wash_handle`; that cannot fail even if the hook is post-wash, which is the gap.

Replace that with one of the two pre-registered predicates, defaulting to fail-safe:
- `WASH_PROOF_METHOD=qdisc_ordering`: parse `tc -d qdisc show` for the capture interface and establish
  an explicit ORDERING relation that the capture hook is UPSTREAM of the spec-router CAKE-egress wash
  qdisc — e.g. resolve `HOOK_PARENT=` and `WASH_QDISC_HANDLE=` and assert the hook parent resolves
  upstream of the wash qdisc handle. Presence of a CAKE handle alone is NOT a pass.
- OR `WASH_PROOF_METHOD=paired_bitflip`: require `PRE_WASH_DSCP=` set and `POST_WASH_DSCP=` cleared on
  the same probe 5-tuple (set->cleared). Absent paired evidence is NOT a pass.

If neither predicate is satisfiable from the available evidence, emit `WASH_PROOF_PASS=false`,
`WASH_ORDERING_PROVEN=false`, `CAPTURE_POINT=unknown` (and the `DL_`/`UL_` variants false/unknown).
Keep emitting the topology datum (`HOOK_PARENT`, `WASH_QDISC_HANDLE`, bridge rule match, direction
flag) but explicitly as SUPPORTING CONTEXT — the recorded pass booleans must rest on the predicate
result, not on `topology_ok` or handle presence. The direction-flag derivation at lines 432-438 may
still use topology to CHOOSE the hook, but choosing the hook must not by itself flip any proof boolean
true. Net effect: strictly harder to assert true; the previously-passing topology+presence path now
yields `unknown`/`false` unless the explicit predicate holds.
</action>
<verify>
  <automated>bash -n scripts/phase225-dscp-ingress-capture.sh && shellcheck scripts/phase225-dscp-ingress-capture.sh && grep -n 'WASH_PROOF_PASS\|WASH_ORDERING_PROVEN\|CAPTURE_POINT' scripts/phase225-dscp-ingress-capture.sh</automated>
</verify>
<acceptance_criteria>
- `bash -n` exits 0; `shellcheck` exits 0.
- Source assertion: no code path sets `WASH_PROOF_PASS=true` / `WASH_ORDERING_PROVEN=true` / `CAPTURE_POINT=pre_wash_ingress` solely from the bridge-rule grep (`topology_ok`) plus a non-empty CAKE qdisc handle. The true-setting condition references a parsed ordering relation (`qdisc_ordering`) or paired bitflip evidence (`PRE_WASH_DSCP`/`POST_WASH_DSCP` set->cleared).
- The default emitted values when the predicate is not satisfied are `WASH_PROOF_PASS=false`, `WASH_ORDERING_PROVEN=false`, `CAPTURE_POINT=unknown` (including `DL_`/`UL_` variants).
- `capture-point-proof.txt` still records `WASH_PROOF_METHOD`, the supporting datum (`HOOK_PARENT`/`WASH_QDISC_HANDLE` or `PRE_WASH_DSCP`/`POST_WASH_DSCP`), and a `PROOF_NOTE` clarifying that topology text and qdisc presence are supporting context, not the pass condition.
- The change is strictly tightening: there is no new path that makes the proof EASIER to mark true than before. (Reviewer/executor confirms by diff that `dl_pass`/`ul_pass` true-branches gained a predicate condition and lost the topology-text-only trigger.)
</acceptance_criteria>
<done>
The pre-wash capture point is falsifiably proven — `WASH_ORDERING_PROVEN`/`CAPTURE_POINT` rest on a
parsed qdisc ordering relation or paired bitflip, default `unknown`/`false`, and can no longer pass
from topology prose plus qdisc presence. Truth #4 falsifiability restored, fail-safe direction.
</done>
</task>

<task id="225-04-3" type="auto">
<name>Task 3: Make DL source-side EF proof honest — real capture or explicit unsupported, never an empty masquerade (GAP-3 / WR-02)</name>
<files>scripts/phase225-dscp-ingress-capture.sh</files>
<read_first>
- scripts/phase225-dscp-ingress-capture.sh   (lines ~203-250 analyzer probe_summary; ~284-301 run_ssh_capture; ~491-503 DL probe branch with the `: > raw/dl-ef-probe-source.pcap` truncation)
- .planning/phases/225-dscp-survival-trace/225-REVIEW.md   (WR-02 — empty source pcap, options to fix)
- .planning/phases/225-dscp-survival-trace/225-02-PLAN.md   (must_have truth #4 — DL STRIPPED only when DL_SOURCE_EF_PROVEN=true; truth #6 — raw auditable source fields)
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md   (lines ~101-107, ~169-210 — source-side DL proof required for any negative reading)
</read_first>
<action>
Fix the DL probe branch (currently lines 491-494) so it NEVER does `: > raw/dl-ef-probe-source.pcap`
to create an empty file and then feeds it to the analyzer as if it were a capture. Choose ONE honest
path (executor may implement either; both satisfy the gap and the 225-02 invariant):

(A) Real source-side capture: add opt-in controls `--dl-source-ssh-host`, `--dl-source-iface`, and a
direction/filter selector, and when they are provided run a bounded read-only source-side tcpdump
(same pattern as `run_ssh_capture`, `set +e` around it, treat `timeout` exit 124 as normal) filtered
to the SAME probe 5-tuple (proto + `PROBE_TARGET` + `PROBE_PORT`), writing a REAL
`raw/dl-ef-probe-source.pcap`. Then pass it to the analyzer with the correct `--source-capture-point`.
The analyzer already derives `DL_SOURCE_EF_PROVEN=true` only when `SRC_PROBE_PKTS_TOTAL>=100` AND
`SRC_EF_PCT>=90` AND `SRC_ENQUEUE_5TUPLE_MATCH=true` AND the source pcap exists — do not weaken that.

(B) Explicit unsupported degrade: when the source-side controls are NOT provided, do NOT write an
empty pcap. Instead emit the DL probe result with `SRC_CAPTURE_POINT=unsupported`,
`SRC_PROBE_PKTS_TOTAL=0`, `EF_PKTS_AT_SOURCE=0`, `DL_SOURCE_EF_PROVEN=false`, and `EF_SURVIVED=degraded`,
and either omit the source pcap from the analyzer call (pass an empty `--source-pcap ""` so
`source_exists` is false) or skip producing a `raw/dl-ef-probe-source.pcap` artifact entirely — the
required-artifact loop and MANIFEST must be updated consistently so a non-existent honest source pcap
is not asserted as required. The DL channel then honestly degrades to `unknown`/`degraded` and can
NEVER record `STRIPPED`.

Whichever path is implemented, preserve the 225-02 hard rule: `EF_SURVIVED=false`/`STRIPPED` for
`dl_ef_probe` is recorded ONLY when `DL_SOURCE_EF_PROVEN=true`. Keep the source-side capture (if added)
read-only tcpdump — no tc/nft/CAKE mutation, no controller-path reference. If implementing (A), make
the new `--dl-source-*` values flow through the SAME injection-validation gate added in Task 1 (the
host/iface tokens must be allowlist-checked before any SSH command is built).
</action>
<verify>
  <automated>bash -n scripts/phase225-dscp-ingress-capture.sh && shellcheck scripts/phase225-dscp-ingress-capture.sh && ! grep -n ':[[:space:]]*>[[:space:]]*"\?\$OUTPUT_DIR/raw/dl-ef-probe-source.pcap' scripts/phase225-dscp-ingress-capture.sh</automated>
</verify>
<acceptance_criteria>
- `bash -n` exits 0; `shellcheck` exits 0.
- Source assertion: the DL probe (`--probe dl`/`--probe both`) branch no longer truncates `raw/dl-ef-probe-source.pcap` to an empty file and then analyzes it; the `: > .../raw/dl-ef-probe-source.pcap` masquerade pattern is gone from the DL probe path. (`grep` for `: >` against that source pcap returns no line in the DL probe branch.)
- If path (A): new `--dl-source-ssh-host` / `--dl-source-iface` / direction-filter options exist, run a bounded read-only source-side tcpdump for the same probe 5-tuple, and the resulting `raw/dl-ef-probe-source.pcap` is a real capture; the new host/iface tokens pass through the Task-1 validation gate. If path (B): when source controls are absent, the DL result emits `SRC_CAPTURE_POINT=unsupported`, `DL_SOURCE_EF_PROVEN=false`, `EF_SURVIVED=degraded`, and no empty pcap is written/asserted-as-required.
- The analyzer's `DL_SOURCE_EF_PROVEN=true` derivation (`SRC_PROBE_PKTS_TOTAL>=100` AND `SRC_EF_PCT>=90` AND `SRC_ENQUEUE_5TUPLE_MATCH=true` AND source pcap present) is NOT weakened.
- Source assertion: there is no path where `dl_ef_probe` records `EF_SURVIVED=false`/`STRIPPED` while `DL_SOURCE_EF_PROVEN=false`.
- Read-only preserved: comment-safe mutation grep `grep -vE '^[[:space:]]*#' scripts/phase225-dscp-ingress-capture.sh | grep -E 'tc[[:space:]].*(filter|qdisc).*(add|del|replace)|cake[[:space:]].*(diffserv|wash)|nft[[:space:]]+(add|delete|flush|-f)'` returns no lines; `grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion_healer|backends/' scripts/phase225-dscp-ingress-capture.sh` returns no lines.
- The required-artifact assertion loop and MANIFEST are consistent with whichever DL source path was chosen (no required artifact that the chosen path does not produce).
</acceptance_criteria>
<done>
The DL source-side EF proof is honest: either a real bounded source-side capture for the same 5-tuple,
or an explicit `unsupported` degrade with `DL_SOURCE_EF_PROVEN=false` — never an empty file that
masquerades as a capture. The 225-02 invariant (DL `STRIPPED` only when `DL_SOURCE_EF_PROVEN=true`)
holds. Truths #3/#5 honesty restored, fail-safe direction.
</done>
</task>

</tasks>

<verification>
- `bash -n scripts/phase225-dscp-ingress-capture.sh` and `shellcheck scripts/phase225-dscp-ingress-capture.sh` both clean.
- Injection rejected: `--probe-target 'a;rm -rf /'` exits 2 with no SSH; out-of-range `--probe-port` exits 2.
- No proof boolean (`WASH_PROOF_PASS`/`WASH_ORDERING_PROVEN`/`CAPTURE_POINT=pre_wash_ingress`) is set from topology text + qdisc presence alone; it rests on a parsed `qdisc_ordering` relation or `paired_bitflip`, defaulting `false`/`unknown`.
- DL source pcap masquerade removed: no `: > .../raw/dl-ef-probe-source.pcap` followed by analysis; DL `STRIPPED` only when `DL_SOURCE_EF_PROVEN=true`.
- Read-only / SAFE-13 posture intact: mutation grep and controller-path grep both empty; no controller-path source file modified (only `scripts/phase225-dscp-ingress-capture.sh` changed).
- All three fixes are strictly tightening (harder-to-prove / fail-safe) — no gate loosened.
</verification>

<success_criteria>
- Three verification gaps (GAP-1 injection, GAP-2 non-falsifiable wash proof, GAP-3 empty DL source pcap) are closed in `scripts/phase225-dscp-ingress-capture.sh`.
- Script remains read-only, mutation-free, controller-path-free (SAFE-13 invariant preserved).
- 225-02 invariants preserved: DL `STRIPPED` only when `DL_SOURCE_EF_PROVEN=true`; `WASH_ORDERING_PROVEN`/`DL_SOURCE_EF_PROVEN` remain independently auditable from raw evidence.
</success_criteria>

<output>
Create `.planning/phases/225-dscp-survival-trace/225-04-SUMMARY.md` when done.
</output>
