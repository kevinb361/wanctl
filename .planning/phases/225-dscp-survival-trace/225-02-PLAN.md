---
phase: 225
plan: "02"
title: "CAKE-ingress DSCP distribution under representative + deliberately-marked traffic"
wave: 1
depends_on: []
requirements:
  - DSCP-02
  - SAFE-13
files_modified:
  - scripts/phase225-dscp-ingress-capture.sh
  - .planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/
autonomous: true
must_haves:
  truths:
    - "Operator can run `scripts/phase225-dscp-ingress-capture.sh --output-dir <dir>` and produce a DIRECTION-SPLIT per-DSCP distribution of traffic arriving at the Spectrum CAKE enqueue interfaces — DL on `spec-router`, UL on `spec-modem` — captured read-only and tagged by direction (`organic_dl_histogram`, `organic_ul_histogram`)."
    - "Capture is taken at the PRE-WASH / ingress-to-CAKE observation point (the DSCP byte as it ARRIVES at the enqueue interface, BEFORE the CAKE egress qdisc applies wash), NOT on the post-wash transmit side. Production runs `allow_wash: true` (configs/spectrum.yaml:45), which strips DSCP after tin selection; a post-wash egress capture would falsely show EF stripped. CAPTURE_POINT is a PROVEN property, not an operator-asserted flag: the naive `tcpdump -Q in` recipe is directionally WRONG for DL (DL packets EGRESS spec-router toward the LAN, so host-inbound on spec-router is NOT the pre-CAKE enqueue point). The correct DL observation point — inbound on the spec-router bridge member facing spec-modem/the `spectrum_dl` marking chain — MUST be derived from the live topology (`ip -d link show`, bridge member roles, `iif spec-modem oif spec-router`), not assumed. The script runs a FALSIFIABLE proof step (NOT asserted topology prose) and writes `capture-point-proof.txt` before it may record `CAPTURE_POINT=pre_wash_ingress`. The proof must POSITIVELY ESTABLISH pre-wash ordering via a check that could FAIL if the hook were post-wash — e.g. observing on the chosen DL hook a DSCP value (EF/AF41/CS1) that the `allow_wash: true` egress qdisc WOULD HAVE CLEARED, demonstrated by a paired post-wash-side observation of the same probe 5-tuple showing the bit stripped (pre-wash sees the bit set, post-wash sees it cleared → the hook is provably upstream of wash); OR an equivalent positive demonstration that the capture hook fires before the `spec-router` CAKE-egress wash qdisc (qdisc/filter ordering on the interface vs the hook). Citing topology text alone (interface, direction, bridge member role) is NOT sufficient. The default until the falsifiable proof passes is `CAPTURE_POINT=unknown` / `WASH_ORDERING_PROVEN=false`; an unproven capture point yields an `unknown` survival channel that CANNOT drive MARKS_DO_NOT_SURVIVE."
    - "EF (DSCP 46 / 0xb8) survival is decided on the PROBE 5-TUPLE by count/percentage (a probe-only filtered pcap), split by direction: `dl_ef_probe` exercises the CRS/Ruckus/bridge DL classification path under test (probe target on the LAN/return path), `ul_ef_probe` is the cake-shaper-sourced UL probe. The result records survival % and probe packet count per direction; `ul_ef_probe` alone NEVER drives the negative branch and NEVER unblocks a DL A/B by itself."
    - "The DL EF probe records SOURCE-SIDE DL DSCP proof (NEW HIGH resolution): `STRIPPED` may be counted for `dl_ef_probe` ONLY when the return-leg DL packets are PROVEN to have carried DSCP 46 at the source (i.e. the DL return traffic was actually EF on the wire before the path under test). A LAN client's `--tos 0xb8` marks its OUTBOUND (UL) packets; it does NOT establish that the RETURN-PATH DL packets carry EF. The script therefore records `EF_PKTS_AT_SOURCE=` (DL-direction probe packets observed carrying DSCP 46 at a source-side capture point on the DL return leg — e.g. captured at/near the external/return endpoint or the first hop where the DL leg is observable as marked, on the probe 5-tuple) alongside `EF_PKTS_AT_ENQUEUE=`. `DL_SOURCE_EF_PROVEN=true` requires `EF_PKTS_AT_SOURCE` ≥ the same ≥100-packet floor AND ≥90% of source-side DL probe packets carrying DSCP 46. `EF_SURVIVED=false`/`STRIPPED` is recorded ONLY when `DL_SOURCE_EF_PROVEN=true` (the DL packets were provably EF at source) AND survival at enqueue is <10%. If source-side DL marking cannot be proven (`DL_SOURCE_EF_PROVEN=false` — e.g. the DL return leg cannot be marked or observed as EF), `dl_ef_probe` is `unknown`/`degraded`, NEVER `STRIPPED` — so a never-EF DL packet can never be miscounted as a stripped mark feeding the negative-close AND."
    - "A minimum-sample / representative-load gate is enforced: a DL histogram window is `valid` only when total captured IP packets ≥ 2000 AND active seconds ≥ 30; an EF-probe result is `valid` only when ≥ 100 probe-5-tuple packets were emitted+captured at the source side AND (for `dl_ef_probe` STRIPPED) `DL_SOURCE_EF_PROVEN=true`. A quiet window below the floor, or a DL probe whose source-side EF marking is unproven, is recorded `invalid`/`unknown` and CANNOT close the milestone negative."
    - "The gating booleans are INDEPENDENTLY AUDITABLE — not hand-set summary flags (cycle-4 resolution). `DL_SOURCE_EF_PROVEN` and `WASH_ORDERING_PROVEN` are EACH backed by the raw, machine-checkable evidence the 225-03 verdict re-derives them from, so neither can be asserted `true` without substantiation: (a) the DL source-side proof emits `SRC_PROBE_PKTS_TOTAL=` (total probe-5-tuple packets observed at the source-side capture point), `SRC_EF_PKTS=` (= `EF_PKTS_AT_SOURCE`), `SRC_EF_PCT=`, a `SRC_CAPTURE_POINT=` descriptor, and a source-side probe-only pcap artifact `raw/dl-ef-probe-source.pcap`, and asserts SAME-5-TUPLE LINKAGE (`PROBE_5TUPLE` at source == `PROBE_5TUPLE` at enqueue, recorded as `SRC_ENQUEUE_5TUPLE_MATCH=true|false`); `DL_SOURCE_EF_PROVEN=true` is derivable iff `SRC_PROBE_PKTS_TOTAL ≥ 100` AND `SRC_EF_PCT ≥ 90` AND `SRC_ENQUEUE_5TUPLE_MATCH=true` AND the source pcap exists; (b) `WASH_ORDERING_PROVEN=true` is backed by a concrete machine-checkable predicate result recorded in `capture-point-proof.txt` as `WASH_PROOF_METHOD={paired_bitflip|qdisc_ordering}` plus an explicit `WASH_PROOF_PASS=true|false` and the supporting datum (for `paired_bitflip`: `PRE_WASH_DSCP=`/`POST_WASH_DSCP=` on the same 5-tuple showing set→cleared; for `qdisc_ordering`: the parsed `tc` qdisc/filter handle ordering showing the hook upstream of the CAKE-egress wash qdisc, e.g. `HOOK_PARENT=`/`WASH_QDISC_HANDLE=` with the ordering relation) — NOT topology prose. If the raw evidence for either boolean is absent or fails its predicate, the script records the boolean `false` and the channel as `unknown`/`degraded`."
    - "The direction-split organic histograms record BOTH the non-BestEffort PACKET fraction AND the non-BestEffort BYTE fraction of DL IP traffic at the enqueue interface (the byte-fraction half of the pre-registered NEGLIGIBLE criterion, 225-RESEARCH.md threshold: non-BestEffort packets < 1.0% AND non-BestEffort bytes < 1.0% where measurable). `NONBE_PKT_PCT=` and `NONBE_BYTE_PCT=` are both emitted per direction so the 225-03 verdict can apply the full NEGLIGIBLE definition, not packets alone."
    - "If the test rig cannot mark EF cleanly (e.g. WMM/AP strips it on the DL LAN-client path), the script degrades — it records whatever DSCP actually arrives and a `degraded` state rather than dropping the check (AB-04 graceful-degradation precedent)."
    - "Capture is read-only: bounded `tcpdump` only (no persistent `tc filter` probe attached, no qdisc mutation), no external gear mutation, no CAKE mode change; the EF probe is a bounded low-rate diagnostic flow to an operator-supplied `--probe-target`, not a config change. Controller-path source stays byte-identical to v1.48 (SAFE-13)."
  artifacts:
    - "scripts/phase225-dscp-ingress-capture.sh — read-only direction-split DSCP-at-ingress histogram capture with 5-tuple EF-probe survival (shellcheck-clean)."
    - ".planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/ — direction-split per-DSCP distribution evidence (organic DL/UL + DL/UL EF-probe) + sample-quality metrics + MANIFEST.md."
---

<objective>
Capture the actual DSCP distribution arriving at the Spectrum CAKE enqueue interfaces under both
representative organic traffic and a deliberately EF-marked probe flow, satisfying DSCP-02. Because
production CAKE runs `besteffort` (single tin), per-tin CAKE counters cannot answer survival — so
observe the DSCP byte directly at the egress interface via bounded `tcpdump`, histogrammed offline,
read-only. Pair the organic distribution with a known EF probe to prove whether a specific mark
survives to the shaper.
</objective>

<context>
DL CAKE egress is `spec-router`, UL CAKE egress is `spec-modem`, both on host `cake-shaper`
(10.10.110.223). The bridge `spectrum_dl` chain marks DL flows just upstream of `spec-router`; the
question is whether those marks (and endpoint EF marks) reach the enqueue point intact.

**Cycle-1 + cycle-2 HIGH corrections shape this plan:**
1. **Capture point vs CAKE wash (cycle-1 VERIFIED) + capture-point PROOF (cycle-2 HIGH-C VERIFIED).**
   Production runs `diffserv: besteffort` + `allow_wash: true` (configs/spectrum.yaml:44-45). Wash
   strips DSCP AFTER tin selection at enqueue. A capture taken on the post-wash transmit side would
   falsely show EF as stripped even though it reached CAKE intact — a false negative that closes the
   milestone. So the capture MUST observe the DSCP byte as it ARRIVES at the enqueue interface
   (pre-wash). CRITICAL (cycle-2 HIGH-C): the naive `-Q in` recipe is directionally WRONG for DL —
   DL packets EGRESS `spec-router` toward the LAN, so host-inbound on `spec-router` is NOT the
   pre-CAKE enqueue point. The correct DL point is inbound on the `spec-router` bridge member facing
   `spec-modem`/the `spectrum_dl` marking chain, derived from live topology, NOT assumed.
   `CAPTURE_POINT` is a PROVEN property backed by `capture-point-proof.txt`, defaulting to `unknown`
   until the proof substantiates `pre_wash_ingress`.
2. **Direction split + topology under test (HIGH).** A UL probe sourced on cake-shaper only proves a
   local upload mark reaches `spec-modem`; it does NOT exercise the CRS/Ruckus/bridge DL
   classification path the milestone re-evaluates. So evidence is split into four channels —
   `organic_dl_histogram`, `organic_ul_histogram`, `dl_ef_probe`, `ul_ef_probe` — and only the DL
   channels can drive the DL diffserv verdict.

`tcpdump` reading the IP TOS/DSCP byte (handling Ethernet/VLAN/IPv6/cooked offsets) gives a
read-only per-DSCP histogram without touching the qdisc filter list. EF survival is decided on the
probe's own 5-tuple (a probe-only filtered pcap), by count/percentage — not by unfiltered histogram
presence (background EF or one stray packet must not flip the result). The probe needs an
operator-supplied `--probe-target`; it must never default to an implicit/arbitrary host. The
read-only constraint forbids gear-config mutation and CAKE-mode change — it does not forbid benign
bounded diagnostic traffic, consistent with prior flent/RRUL capture phases.
</context>

<task id="225-02-1" type="execute">
<title>Write the read-only DSCP-at-ingress histogram capture script</title>
<action>
Create `scripts/phase225-dscp-ingress-capture.sh` (`set -euo pipefail`) with args `--output-dir`
(required), `--ssh-host` (default `cake-shaper`), `--duration` (default 90 seconds),
`--probe {none|ul|dl|both}` (default `none` — a probe is opt-in and requires a target),
`--probe-target <host>` (REQUIRED whenever `--probe` is not `none`; the script errors with a clear
message if a probe is requested without a target — it must never default to an implicit/arbitrary
host), `--probe-proto {udp|tcp}` and `--probe-port` (recorded into the 5-tuple), and a
`--min-packets` (default 2000) / `--min-active-seconds` (default 30) sample floor.

**Capture-point proof FIRST (HIGH-C — prove, do not assert):**
- Before any histogram capture, derive the correct DL pre-wash observation point from the LIVE
  topology, not from an assumed `-Q in` recipe. DL packets EGRESS `spec-router` toward the LAN, so
  host-inbound (`-Q in`) on `spec-router` is NOT the pre-CAKE enqueue point. The correct DL point is
  inbound on the `spec-router` bridge member that faces `spec-modem`/the `spectrum_dl` marking chain
  (the byte after the bridge sets DSCP but before the `spec-router` egress qdisc washes it). Read
  `ip -d link show`, the bridge member roles, and the `iif spec-modem oif spec-router` classification
  path to identify the interface + capture direction.
- Run a FALSIFIABLE proof step (HIGH-C — the proof must be able to FAIL if the hook is post-wash;
  asserted topology prose is NOT a proof) and emit `capture-point-proof.txt` per direction. The
  falsifiable check must POSITIVELY establish pre-wash ordering by one of:
  (a) **paired pre/post-wash observation of the same probe 5-tuple** — capture the marked probe at the
      candidate pre-wash DL hook AND at a post-wash egress/transmit-side point simultaneously; if the
      pre-wash hook shows DSCP set (EF/AF41/CS1) while the post-wash side shows it cleared (because
      `allow_wash: true` strips it after tin selection), the hook is PROVEN upstream of wash. This
      check FAILS (and the point stays `unknown`) if both sides show the bit cleared or both show it
      set, so it is genuinely falsifiable; OR
  (b) an equivalent positive demonstration that the capture hook fires before the `spec-router`
      CAKE-egress wash qdisc — e.g. `tc -d qdisc/filter show` ordering on the interface establishing
      the hook is on the ingress/bridge-member side of the egress CAKE qdisc that applies wash.
  Record the topology evidence (interface name, capture direction, bridge member role, qdisc location
  relative to the capture hook) AS SUPPORTING CONTEXT, but the recorded `WASH_ORDERING_PROVEN=true`
  MUST rest on the falsifiable check above, not on the topology citation alone. Record the check as a
  CONCRETE machine-checkable predicate (cycle-4 resolution — the `qdisc_ordering` alternative must NOT
  collapse to prose): emit `WASH_PROOF_METHOD={paired_bitflip|qdisc_ordering}`, an explicit
  `WASH_PROOF_PASS=true|false`, and the supporting datum — for `paired_bitflip`: `PRE_WASH_DSCP=` and
  `POST_WASH_DSCP=` on the same probe 5-tuple (pass requires set→cleared); for `qdisc_ordering`: the
  parsed `HOOK_PARENT=` and `WASH_QDISC_HANDLE=` with the explicit upstream-of-wash ordering relation
  (pass requires the hook parent resolve upstream of the CAKE-egress wash qdisc handle). Only when the
  falsifiable proof passes (`WASH_PROOF_PASS=true` — the hook is demonstrated downstream of the
  DSCP-setting stage AND upstream of the CAKE egress/wash qdisc) may the script record
  `CAPTURE_POINT=pre_wash_ingress` + `WASH_ORDERING_PROVEN=true` for that direction. **Default until the
  falsifiable proof passes is `CAPTURE_POINT=unknown` / `WASH_ORDERING_PROVEN=false`** → that channel is
  `unknown` for the verdict and cannot drive MARKS_DO_NOT_SURVIVE.

**Representative (organic) capture, direction-split + pre-wash:**
- DL on the proven `spec-router` pre-wash interface/direction and UL on `spec-modem`, each via
  `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n timeout -k 5 <duration> tcpdump -n -p -i <proven_if> <proven_direction_flag> -c <cap> -w -"`
  capturing the pre-wash ingress-to-enqueue direction established by the proof step above (the
  direction flag is whatever the proof determined — NOT hardcoded `-Q in`). Save raw pcaps under
  `--output-dir/raw/` and compute direction-tagged per-DSCP histograms
  `dscp-histogram-spec-router-dl.txt` and `dscp-histogram-spec-modem-ul.txt` via a python3 extractor
  that correctly derives DSCP from the IP header across Ethernet, 802.1Q VLAN tags, IPv6 Traffic
  Class, and Linux cooked (SLL) captures — NOT a naive `ip[1] >> 2` that breaks on VLAN/IPv6/cooked
  frames. Each histogram MUST emit BOTH `NONBE_PKT_PCT=` (non-BestEffort packet fraction) AND
  `NONBE_BYTE_PCT=` (non-BestEffort byte fraction, where bytes are measurable) so 225-03 can apply
  the full pre-registered NEGLIGIBLE criterion (packets < 1.0% AND bytes < 1.0%), not packets alone.
- `timeout` exits 124 when the duration elapses before the packet cap — handle this explicitly
  (treat 124 as a normal end-of-window, not a failure) so the script does not abort under `set -e`.
- Compute and record sample-quality metrics per direction into `sample-quality.txt`: total IP
  packets, total IP bytes, active seconds, `NONBE_PKT_PCT`, `NONBE_BYTE_PCT`, and
  `WINDOW_VALID_DL=true|false` / `WINDOW_VALID_UL=true|false` against the `--min-packets` /
  `--min-active-seconds` floor. A window below either floor is recorded invalid (quiet-window guard)
  and the verdict treats it as `invalid`, never as evidence of absence.

**EF probe, direction-split, 5-tuple decided:**
- `--probe ul`: low-rate EF flow from cake-shaper outbound to `--probe-target`
  (`iperf3 -u -b 1M --tos 0xb8 -c <target>` or `nping --udp --tos 0xb8 --rate 50 <target>`),
  captured pre-wash on `spec-modem`. This is `ul_ef_probe`.
- `--probe dl`: EF flow on the DL/return path that traverses the CRS/Ruckus/bridge classification
  path under test (probe target/return toward a LAN endpoint), captured pre-wash on `spec-router`.
  This is `dl_ef_probe` — the channel the milestone actually re-evaluates. **Source-side DL DSCP proof
  (NEW HIGH):** a LAN client's `--tos 0xb8` marks its OUTBOUND (UL) packets, NOT the RETURN-path DL
  packets. So the DL probe MUST also observe the DL return leg at a SOURCE-SIDE point (capture the
  same probe 5-tuple at/near the external/return endpoint, or the first point where the DL leg is
  observable as marked, BEFORE the path under test) and record how many DL return packets carried
  DSCP 46 there. Write a source-side probe-only pcap `raw/dl-ef-probe-source.pcap` and emit the RAW,
  INDEPENDENTLY-AUDITABLE fields (cycle-4 resolution — 225-03 RE-DERIVES the boolean from these, so a
  hand-set flag is rejected): `SRC_PROBE_PKTS_TOTAL=` (total probe-5-tuple packets at the source point),
  `SRC_EF_PKTS=`/`EF_PKTS_AT_SOURCE=` (those carrying DSCP 46), `SRC_EF_PCT=`, `SRC_CAPTURE_POINT=`,
  and `SRC_ENQUEUE_5TUPLE_MATCH=true|false` (source-side and enqueue observations are the IDENTICAL
  `PROBE_5TUPLE`). Set `DL_SOURCE_EF_PROVEN=true` ONLY when `SRC_PROBE_PKTS_TOTAL` ≥ 100 AND
  `SRC_EF_PCT` ≥ 90 AND `SRC_ENQUEUE_5TUPLE_MATCH=true` AND `raw/dl-ef-probe-source.pcap` exists;
  otherwise `false`.
- For each requested direction, write a probe-only pcap filtered to the probe 5-tuple under
  `raw/`, then decide survival by COUNT/PERCENTAGE on that filtered pcap: emit per-direction
  `dl-ef-probe-result.txt` / `ul-ef-probe-result.txt` with `PROBE_5TUPLE=...`, `PROBE_PKTS_SENT=N`,
  `PROBE_PKTS_CAPTURED=M`, `EF_PKTS_AT_ENQUEUE=K`, `EF_SURVIVAL_PCT=<pct>`, and (for DL)
  `EF_PKTS_AT_SOURCE=S` + `DL_SOURCE_EF_PROVEN=true|false`, then `EF_SURVIVED=true|false|degraded` per
  the pre-registered thresholds (≥90% → true, <10% → false, else degraded; and `unknown`/degraded if
  fewer than 100 probe packets were emitted+captured). **Hard rule for the DL channel:**
  `EF_SURVIVED=false`/`STRIPPED` may be recorded ONLY when `DL_SOURCE_EF_PROVEN=true` AND enqueue
  survival <10%. If `DL_SOURCE_EF_PROVEN=false` (the DL return leg was never provably EF at source),
  the DL probe is `unknown`/`degraded` — NEVER `STRIPPED` — so a never-EF DL packet cannot be
  miscounted as a stripped mark and feed the negative-close AND. If the rig cannot mark EF cleanly
  (DL WMM strip, or no source-side observation point), set `degraded`/`unknown` and record the
  observed DSCP.

Write MANIFEST.md and a required-artifact assertion loop. The script MUST use `tcpdump` only (no
`tc filter add`/`tc qdisc` mutation), MUST NOT change CAKE mode, and MUST NOT reference
controller-path source files.
</action>
<read_first>
- scripts/phase224-snapshot-a.sh
- scripts/phase191-flent-capture.sh
- configs/spectrum.yaml
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md
</read_first>
<acceptance_criteria>
- `scripts/phase225-dscp-ingress-capture.sh` exists; `bash -n` exits 0; `shellcheck` reports no errors.
- Comment-safe mutation check: `grep -vE '^[[:space:]]*#' scripts/phase225-dscp-ingress-capture.sh | grep -E 'tc[[:space:]].*(filter|qdisc).*(add|del|replace)|cake[[:space:]].*(diffserv|wash)|nft[[:space:]]+(add|delete|flush|-f)'` returns no lines.
- `grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion_healer' scripts/phase225-dscp-ingress-capture.sh` returns no lines.
- Script uses `tcpdump` for capture, references `spec-router` and `spec-modem`, and bounds capture by both `--duration` (default 90) and a packet count; `timeout` exit code 124 is handled as normal end-of-window (no abort under `set -e`).
- Script derives the DL capture interface+direction from live topology (`ip -d link show` / bridge member roles / `iif spec-modem oif spec-router`) and does NOT hardcode `-Q in` for DL; it emits `capture-point-proof.txt` per direction.
- `capture-point-proof.txt` records a FALSIFIABLE pre-wash check (paired pre/post-wash observation of the same probe 5-tuple showing the DSCP bit set pre-wash and cleared post-wash, OR an equivalent positive demonstration the hook fires before the CAKE-egress wash qdisc) — NOT topology prose alone. `WASH_ORDERING_PROVEN=true` rests on that falsifiable check passing.
- `capture-point-proof.txt` records the check as a CONCRETE machine-checkable predicate, not prose: `WASH_PROOF_METHOD={paired_bitflip|qdisc_ordering}`, an explicit `WASH_PROOF_PASS=true|false`, and the supporting datum (for `paired_bitflip`: `PRE_WASH_DSCP=`/`POST_WASH_DSCP=` on the same 5-tuple; for `qdisc_ordering`: parsed `HOOK_PARENT=`/`WASH_QDISC_HANDLE=` with the upstream-of-wash ordering relation). `WASH_ORDERING_PROVEN=true` is set ONLY when `WASH_PROOF_PASS=true`; the `qdisc_ordering` alternative cannot collapse to topology prose.
- The DL probe emits the raw source-side evidence that makes `DL_SOURCE_EF_PROVEN` independently re-derivable: `SRC_PROBE_PKTS_TOTAL=`, `SRC_EF_PKTS=` (= `EF_PKTS_AT_SOURCE`), `SRC_EF_PCT=`, `SRC_CAPTURE_POINT=`, `SRC_ENQUEUE_5TUPLE_MATCH=true|false`, and a source-side probe-only pcap `raw/dl-ef-probe-source.pcap`. `DL_SOURCE_EF_PROVEN=true` is recorded ONLY when `SRC_PROBE_PKTS_TOTAL ≥ 100` AND `SRC_EF_PCT ≥ 90` AND `SRC_ENQUEUE_5TUPLE_MATCH=true` AND the source pcap exists; otherwise it is `false` and the DL probe is `unknown`/`degraded`.
- Script records `CAPTURE_POINT=` and `WASH_ORDERING_PROVEN=` per direction, defaulting to `CAPTURE_POINT=unknown` / `WASH_ORDERING_PROVEN=false` and only recording `pre_wash_ingress`/`true` when the falsifiable proof in `capture-point-proof.txt` passes (hook demonstrated downstream of the DSCP-setting stage and upstream of the CAKE wash qdisc).
- Script requires `--probe-target` whenever `--probe` is not `none` and errors clearly if a probe is requested without a target; it records the probe 5-tuple, proto, and port.
- Script emits direction-split histograms (`dscp-histogram-spec-router-dl.txt`, `dscp-histogram-spec-modem-ul.txt`) and direction-split probe results (`dl-ef-probe-result.txt` / `ul-ef-probe-result.txt`) each with `EF_SURVIVAL_PCT=` and `EF_SURVIVED=true|false|degraded` decided on the probe 5-tuple by count/percentage.
- The DL probe result records `EF_PKTS_AT_SOURCE=` and `DL_SOURCE_EF_PROVEN=true|false` (source-side DL DSCP proof); `EF_SURVIVED=false`/`STRIPPED` is recorded for the DL channel ONLY when `DL_SOURCE_EF_PROVEN=true` AND enqueue survival <10%. When `DL_SOURCE_EF_PROVEN=false`, the DL probe is `unknown`/`degraded`, never `STRIPPED`.
- Script writes `sample-quality.txt` with total IP packets, total IP bytes, active seconds, `NONBE_PKT_PCT`, `NONBE_BYTE_PCT`, and `WINDOW_VALID_DL`/`WINDOW_VALID_UL` against the `--min-packets` (default 2000) / `--min-active-seconds` (default 30) floor.
- The direction-split organic histograms each emit BOTH `NONBE_PKT_PCT=` and `NONBE_BYTE_PCT=` (non-BestEffort packet AND byte fractions) so the verdict can apply the full pre-registered NEGLIGIBLE criterion (packets < 1.0% AND bytes < 1.0% where measurable), not packets alone.
- The python3 DSCP extractor handles Ethernet, 802.1Q VLAN, IPv6 Traffic Class, and Linux cooked (SLL) captures (not a naive `ip[1] >> 2`).
- Script emits a required-artifact assertion loop.
</acceptance_criteria>
<done>
Read-only ingress-DSCP capture script proves its DL pre-wash capture point with a FALSIFIABLE check (`capture-point-proof.txt` — paired pre/post-wash 5-tuple observation, defaulting to `unknown` until proven, not topology prose and not a hardcoded `-Q in`), produces pre-wash, direction-split per-DSCP histograms with BOTH packet and byte non-BestEffort fractions on both CAKE interfaces, a 5-tuple-decided EF-probe survival result per direction with SOURCE-SIDE DL DSCP proof (`EF_PKTS_AT_SOURCE`/`DL_SOURCE_EF_PROVEN` — DL `STRIPPED` only counts when the DL leg was provably EF at source), mandatory `--probe-target`, sample-quality gates against a representative-load floor, a robust DSCP extractor, and timeout-124 handling — all mutation-free.
</done>
</task>

<task id="225-02-2" type="execute">
<title>Document the capture method and DSCP-02 evidence layout</title>
<action>
Create `.planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/README.md` describing the
DSCP-02 evidence set: what each artifact is (direction-split organic histograms
`dscp-histogram-spec-router-dl.txt` / `dscp-histogram-spec-modem-ul.txt`, direction-split probe
results `dl-ef-probe-result.txt` / `ul-ef-probe-result.txt`, `sample-quality.txt`, raw + probe-only
pcaps under `raw/`, MANIFEST.md), how the histogram maps TOS byte → DSCP → CAKE tin (EF=46→Voice,
AF41=34→Video, CS1=8→Bulk, CS0=0→BestEffort per `bridge-qos.nft` mapping), the probe methodology and
5-tuple, and the read-only posture statement (tcpdump only, bounded probe to an operator-supplied
`--probe-target`, no gear or CAKE-mode mutation). State explicitly: (a) the capture is taken
PRE-WASH at the enqueue ingress because `allow_wash: true` would strip DSCP post-wash and produce a
false negative — and that `CAPTURE_POINT` is a PROVEN property backed by a FALSIFIABLE check in
`capture-point-proof.txt` (paired pre/post-wash observation of the same probe 5-tuple, or equivalent
positive demonstration the hook fires before the CAKE-egress wash qdisc — NOT topology prose alone,
NOT a hardcoded `-Q in`), defaulting to `unknown` until that check passes; (a2) the DL EF probe
records SOURCE-SIDE DL DSCP proof (`EF_PKTS_AT_SOURCE` / `DL_SOURCE_EF_PROVEN`) because a LAN client's
`--tos 0xb8` marks only its UL packets, not the return-path DL packets — so DL `STRIPPED` is counted
ONLY when the DL leg was provably EF at source, otherwise the DL probe is `unknown`/`degraded` and
cannot feed a negative close; (b) the DL channels exercise the
CRS/Ruckus/bridge path under test while a UL probe only proves a local mark reaches spec-modem (so
UL alone neither closes negative nor unblocks the DL A/B); (c) the representative-load floor
(`WINDOW_VALID_*`) and that an invalid window cannot drive a negative verdict; (d) production CAKE
`besteffort` means per-tin CAKE counters are NOT used for survival — the pre-wash DSCP histogram is
the source of truth; (e) the histograms record BOTH packet and byte non-BestEffort fractions
(`NONBE_PKT_PCT`/`NONBE_BYTE_PCT`) because the pre-registered NEGLIGIBLE criterion is packets < 1.0%
AND bytes < 1.0% (where measurable). This README is the operator-facing index that 225-03's verdict
cites.
</action>
<read_first>
- deploy/nftables/bridge-qos.nft
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md
</read_first>
<acceptance_criteria>
- `evidence/dscp-ingress/README.md` exists and lists every artifact the capture script produces (direction-split histograms with `NONBE_PKT_PCT`/`NONBE_BYTE_PCT`, direction-split probe results, sample-quality.txt, `capture-point-proof.txt`, raw/probe-only pcaps, MANIFEST).
- README documents the TOS→DSCP→tin mapping and the EF-probe methodology (5-tuple, mandatory `--probe-target`, and the source-side DL DSCP proof `EF_PKTS_AT_SOURCE`/`DL_SOURCE_EF_PROVEN`).
- README states the pre-wash capture-point rationale (`allow_wash: true` → post-wash false negative), that `CAPTURE_POINT` is proven via a FALSIFIABLE check in `capture-point-proof.txt` (paired pre/post-wash 5-tuple observation or equivalent — not topology prose, not a hardcoded `-Q in`) and defaults to `unknown` until that check passes, that DL `STRIPPED` requires `DL_SOURCE_EF_PROVEN=true`, and the direction-split semantics (UL alone neither closes negative nor unblocks the DL A/B).
- README states the representative-load floor (`WINDOW_VALID_*`) and that an invalid window cannot drive a negative verdict, and that NEGLIGIBLE is decided on BOTH `NONBE_PKT_PCT` and `NONBE_BYTE_PCT` (packets < 1.0% AND bytes < 1.0% where measurable).
- README states the read-only posture and that besteffort per-tin CAKE counters are not used for survival.
- `grep -iE 'password|secret|token' evidence/dscp-ingress/README.md` returns no lines.
</acceptance_criteria>
<done>
DSCP-02 evidence index documents capture method, DSCP→tin mapping, EF-probe methodology, and the read-only posture, ready for the 225-03 verdict to cite.
</done>
</task>

<verification>
- `bash -n` and `shellcheck` on the capture script — both clean.
- Mutation-grep and controller-path-grep both empty.
- Capture direction for DL is derived from live topology (no hardcoded `-Q in` for DL); `capture-point-proof.txt` records a FALSIFIABLE pre-wash check (not topology prose alone) and `CAPTURE_POINT` defaults to `unknown` until that proof passes.
- DL probe result carries `EF_PKTS_AT_SOURCE` + `DL_SOURCE_EF_PROVEN`; DL `STRIPPED` only when `DL_SOURCE_EF_PROVEN=true`, else `unknown`/`degraded`.
- Organic histograms + `sample-quality.txt` carry BOTH `NONBE_PKT_PCT` and `NONBE_BYTE_PCT`.
- `evidence/dscp-ingress/README.md` present and complete.
</verification>
