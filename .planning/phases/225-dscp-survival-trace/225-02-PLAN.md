---
phase: 225
plan: "02"
title: "CAKE-ingress DSCP distribution under representative + deliberately-marked traffic"
wave: 1
depends_on: []
requirements:
  - DSCP-02
  - SAFE-13
files_modified:
  - scripts/phase225-dscp-ingress-capture.sh
  - .planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/
autonomous: true
must_haves:
  truths:
    - "Operator can run `scripts/phase225-dscp-ingress-capture.sh --output-dir <dir>` and produce a DIRECTION-SPLIT per-DSCP distribution of traffic arriving at the Spectrum CAKE enqueue interfaces — DL on `spec-router`, UL on `spec-modem` — captured read-only and tagged by direction (`organic_dl_histogram`, `organic_ul_histogram`)."
    - "Capture is taken at the PRE-WASH / ingress-to-CAKE observation point (the DSCP byte as it ARRIVES at the enqueue interface), NOT on the post-wash transmit side. Production runs `allow_wash: true` (configs/spectrum.yaml:45), which strips DSCP after tin selection; a post-wash egress capture would falsely show EF stripped. The script records `CAPTURE_POINT=pre_wash_ingress|post_wash_egress|unknown` and `WASH_ORDERING_PROVEN=true|false`; an unproven capture point yields an `unknown` survival channel that CANNOT drive MARKS_DO_NOT_SURVIVE."
    - "EF (DSCP 46 / 0xb8) survival is decided on the PROBE 5-TUPLE by count/percentage (a probe-only filtered pcap), split by direction: `dl_ef_probe` exercises the CRS/Ruckus/bridge DL classification path under test (probe target on the LAN/return path), `ul_ef_probe` is the cake-shaper-sourced UL probe. The result records survival % and probe packet count per direction; `ul_ef_probe` alone NEVER drives the negative branch and NEVER unblocks a DL A/B by itself."
    - "A minimum-sample / representative-load gate is enforced: a DL histogram window is `valid` only when total captured IP packets ≥ 2000 AND active seconds ≥ 30; an EF-probe result is `valid` only when ≥ 100 probe-5-tuple packets were emitted+captured at the source side. A quiet window below the floor is recorded `invalid` and CANNOT close the milestone negative."
    - "If the test rig cannot mark EF cleanly (e.g. WMM/AP strips it on the DL LAN-client path), the script degrades — it records whatever DSCP actually arrives and a `degraded` state rather than dropping the check (AB-04 graceful-degradation precedent)."
    - "Capture is read-only: bounded `tcpdump` only (no persistent `tc filter` probe attached, no qdisc mutation), no external gear mutation, no CAKE mode change; the EF probe is a bounded low-rate diagnostic flow to an operator-supplied `--probe-target`, not a config change. Controller-path source stays byte-identical to v1.48 (SAFE-13)."
  artifacts:
    - "scripts/phase225-dscp-ingress-capture.sh — read-only direction-split DSCP-at-ingress histogram capture with 5-tuple EF-probe survival (shellcheck-clean)."
    - ".planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/ — direction-split per-DSCP distribution evidence (organic DL/UL + DL/UL EF-probe) + sample-quality metrics + MANIFEST.md."
---

<objective>
Capture the actual DSCP distribution arriving at the Spectrum CAKE enqueue interfaces under both
representative organic traffic and a deliberately EF-marked probe flow, satisfying DSCP-02. Because
production CAKE runs `besteffort` (single tin), per-tin CAKE counters cannot answer survival — so
observe the DSCP byte directly at the egress interface via bounded `tcpdump`, histogrammed offline,
read-only. Pair the organic distribution with a known EF probe to prove whether a specific mark
survives to the shaper.
</objective>

<context>
DL CAKE egress is `spec-router`, UL CAKE egress is `spec-modem`, both on host `cake-shaper`
(10.10.110.223). The bridge `spectrum_dl` chain marks DL flows just upstream of `spec-router`; the
question is whether those marks (and endpoint EF marks) reach the enqueue point intact.

**Two cycle-1 HIGH corrections shape this plan:**
1. **Capture point vs CAKE wash (VERIFIED).** Production runs `diffserv: besteffort` +
   `allow_wash: true` (configs/spectrum.yaml:44-45). Wash strips DSCP AFTER tin selection at enqueue.
   A capture taken on the post-wash transmit side would falsely show EF as stripped even though it
   reached CAKE intact — a false negative that closes the milestone. So the capture MUST observe the
   DSCP byte as it ARRIVES at the enqueue interface (pre-wash / ingress direction, e.g. `-Q in` /
   ingress capture on the bridge member facing the marking stage), and must record whether that
   ordering is proven.
2. **Direction split + topology under test (HIGH).** A UL probe sourced on cake-shaper only proves a
   local upload mark reaches `spec-modem`; it does NOT exercise the CRS/Ruckus/bridge DL
   classification path the milestone re-evaluates. So evidence is split into four channels —
   `organic_dl_histogram`, `organic_ul_histogram`, `dl_ef_probe`, `ul_ef_probe` — and only the DL
   channels can drive the DL diffserv verdict.

`tcpdump` reading the IP TOS/DSCP byte (handling Ethernet/VLAN/IPv6/cooked offsets) gives a
read-only per-DSCP histogram without touching the qdisc filter list. EF survival is decided on the
probe's own 5-tuple (a probe-only filtered pcap), by count/percentage — not by unfiltered histogram
presence (background EF or one stray packet must not flip the result). The probe needs an
operator-supplied `--probe-target`; it must never default to an implicit/arbitrary host. The
read-only constraint forbids gear-config mutation and CAKE-mode change — it does not forbid benign
bounded diagnostic traffic, consistent with prior flent/RRUL capture phases.
</context>

<task id="225-02-1" type="execute">
<title>Write the read-only DSCP-at-ingress histogram capture script</title>
<action>
Create `scripts/phase225-dscp-ingress-capture.sh` (`set -euo pipefail`) with args `--output-dir`
(required), `--ssh-host` (default `cake-shaper`), `--duration` (default 90 seconds),
`--probe {none|ul|dl|both}` (default `none` — a probe is opt-in and requires a target),
`--probe-target <host>` (REQUIRED whenever `--probe` is not `none`; the script errors with a clear
message if a probe is requested without a target — it must never default to an implicit/arbitrary
host), `--probe-proto {udp|tcp}` and `--probe-port` (recorded into the 5-tuple), and a
`--min-packets` (default 2000) / `--min-active-seconds` (default 30) sample floor.

**Representative (organic) capture, direction-split + pre-wash:**
- DL on `spec-router` and UL on `spec-modem`, each via
  `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n timeout -k 5 <duration> tcpdump -n -p -i <if> -Q in -c <cap> -w -"`
  capturing the INGRESS-to-enqueue (pre-wash) direction. Record `CAPTURE_POINT=pre_wash_ingress`
  when the `-Q in`/ingress observation is confirmed for the interface; if the script cannot prove
  the capture is pre-wash for a given interface, set `CAPTURE_POINT=unknown` and
  `WASH_ORDERING_PROVEN=false` for that direction and note the limitation (that channel becomes
  `unknown` for the verdict). Save raw pcaps under `--output-dir/raw/` and compute direction-tagged
  per-DSCP histograms `dscp-histogram-spec-router-dl.txt` and `dscp-histogram-spec-modem-ul.txt` via
  a python3 extractor that correctly derives DSCP from the IP header across Ethernet, 802.1Q VLAN
  tags, IPv6 Traffic Class, and Linux cooked (SLL) captures — NOT a naive `ip[1] >> 2` that breaks
  on VLAN/IPv6/cooked frames.
- `timeout` exits 124 when the duration elapses before the packet cap — handle this explicitly
  (treat 124 as a normal end-of-window, not a failure) so the script does not abort under `set -e`.
- Compute and record sample-quality metrics per direction into `sample-quality.txt`: total IP
  packets, active seconds, and `WINDOW_VALID_DL=true|false` / `WINDOW_VALID_UL=true|false` against
  the `--min-packets` / `--min-active-seconds` floor. A window below either floor is recorded
  invalid (quiet-window guard) and the verdict treats it as `invalid`, never as evidence of absence.

**EF probe, direction-split, 5-tuple decided:**
- `--probe ul`: low-rate EF flow from cake-shaper outbound to `--probe-target`
  (`iperf3 -u -b 1M --tos 0xb8 -c <target>` or `nping --udp --tos 0xb8 --rate 50 <target>`),
  captured pre-wash on `spec-modem`. This is `ul_ef_probe`.
- `--probe dl`: EF flow on the DL/return path that traverses the CRS/Ruckus/bridge classification
  path under test (probe target/return toward a LAN endpoint), captured pre-wash on `spec-router`.
  This is `dl_ef_probe` — the channel the milestone actually re-evaluates.
- For each requested direction, write a probe-only pcap filtered to the probe 5-tuple under
  `raw/`, then decide survival by COUNT/PERCENTAGE on that filtered pcap: emit per-direction
  `dl-ef-probe-result.txt` / `ul-ef-probe-result.txt` with `PROBE_5TUPLE=...`, `PROBE_PKTS_SENT=N`,
  `PROBE_PKTS_CAPTURED=M`, `EF_PKTS_AT_ENQUEUE=K`, `EF_SURVIVAL_PCT=<pct>`, and
  `EF_SURVIVED=true|false|degraded` per the pre-registered thresholds (≥90% → true, <10% → false,
  else degraded; and `unknown`/degraded if fewer than 100 probe packets were emitted+captured). If
  the rig cannot mark EF cleanly (DL WMM strip), set `degraded` and record the observed DSCP.

Write MANIFEST.md and a required-artifact assertion loop. The script MUST use `tcpdump` only (no
`tc filter add`/`tc qdisc` mutation), MUST NOT change CAKE mode, and MUST NOT reference
controller-path source files.
</action>
<read_first>
- scripts/phase224-snapshot-a.sh
- scripts/phase191-flent-capture.sh
- configs/spectrum.yaml
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md
</read_first>
<acceptance_criteria>
- `scripts/phase225-dscp-ingress-capture.sh` exists; `bash -n` exits 0; `shellcheck` reports no errors.
- Comment-safe mutation check: `grep -vE '^[[:space:]]*#' scripts/phase225-dscp-ingress-capture.sh | grep -E 'tc[[:space:]].*(filter|qdisc).*(add|del|replace)|cake[[:space:]].*(diffserv|wash)|nft[[:space:]]+(add|delete|flush|-f)'` returns no lines.
- `grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion_healer' scripts/phase225-dscp-ingress-capture.sh` returns no lines.
- Script uses `tcpdump` for capture, references `spec-router` and `spec-modem`, and bounds capture by both `--duration` (default 90) and a packet count; `timeout` exit code 124 is handled as normal end-of-window (no abort under `set -e`).
- Script captures the pre-wash ingress direction (`-Q in`/ingress) and records `CAPTURE_POINT=` and `WASH_ORDERING_PROVEN=` per direction; an unproven capture point is recorded as `unknown`.
- Script requires `--probe-target` whenever `--probe` is not `none` and errors clearly if a probe is requested without a target; it records the probe 5-tuple, proto, and port.
- Script emits direction-split histograms (`dscp-histogram-spec-router-dl.txt`, `dscp-histogram-spec-modem-ul.txt`) and direction-split probe results (`dl-ef-probe-result.txt` / `ul-ef-probe-result.txt`) each with `EF_SURVIVAL_PCT=` and `EF_SURVIVED=true|false|degraded` decided on the probe 5-tuple by count/percentage.
- Script writes `sample-quality.txt` with total IP packets, active seconds, and `WINDOW_VALID_DL`/`WINDOW_VALID_UL` against the `--min-packets` (default 2000) / `--min-active-seconds` (default 30) floor.
- The python3 DSCP extractor handles Ethernet, 802.1Q VLAN, IPv6 Traffic Class, and Linux cooked (SLL) captures (not a naive `ip[1] >> 2`).
- Script emits a required-artifact assertion loop.
</acceptance_criteria>
<done>
Read-only ingress-DSCP capture script produces pre-wash, direction-split per-DSCP histograms on both CAKE interfaces, a 5-tuple-decided EF-probe survival result per direction (mandatory `--probe-target`), sample-quality gates against a representative-load floor, a robust DSCP extractor, and timeout-124 handling — all mutation-free.
</done>
</task>

<task id="225-02-2" type="execute">
<title>Document the capture method and DSCP-02 evidence layout</title>
<action>
Create `.planning/phases/225-dscp-survival-trace/evidence/dscp-ingress/README.md` describing the
DSCP-02 evidence set: what each artifact is (direction-split organic histograms
`dscp-histogram-spec-router-dl.txt` / `dscp-histogram-spec-modem-ul.txt`, direction-split probe
results `dl-ef-probe-result.txt` / `ul-ef-probe-result.txt`, `sample-quality.txt`, raw + probe-only
pcaps under `raw/`, MANIFEST.md), how the histogram maps TOS byte → DSCP → CAKE tin (EF=46→Voice,
AF41=34→Video, CS1=8→Bulk, CS0=0→BestEffort per `bridge-qos.nft` mapping), the probe methodology and
5-tuple, and the read-only posture statement (tcpdump only, bounded probe to an operator-supplied
`--probe-target`, no gear or CAKE-mode mutation). State explicitly: (a) the capture is taken
PRE-WASH at the enqueue ingress because `allow_wash: true` would strip DSCP post-wash and produce a
false negative — document `CAPTURE_POINT`/`WASH_ORDERING_PROVEN`; (b) the DL channels exercise the
CRS/Ruckus/bridge path under test while a UL probe only proves a local mark reaches spec-modem (so
UL alone neither closes negative nor unblocks the DL A/B); (c) the representative-load floor
(`WINDOW_VALID_*`) and that an invalid window cannot drive a negative verdict; (d) production CAKE
`besteffort` means per-tin CAKE counters are NOT used for survival — the pre-wash DSCP histogram is
the source of truth. This README is the operator-facing index that 225-03's verdict cites.
</action>
<read_first>
- deploy/nftables/bridge-qos.nft
- .planning/phases/225-dscp-survival-trace/225-RESEARCH.md
</read_first>
<acceptance_criteria>
- `evidence/dscp-ingress/README.md` exists and lists every artifact the capture script produces (direction-split histograms, direction-split probe results, sample-quality.txt, raw/probe-only pcaps, MANIFEST).
- README documents the TOS→DSCP→tin mapping and the EF-probe methodology (5-tuple, mandatory `--probe-target`).
- README states the pre-wash capture-point rationale (`allow_wash: true` → post-wash false negative) and the direction-split semantics (UL alone neither closes negative nor unblocks the DL A/B).
- README states the representative-load floor (`WINDOW_VALID_*`) and that an invalid window cannot drive a negative verdict.
- README states the read-only posture and that besteffort per-tin CAKE counters are not used for survival.
- `grep -iE 'password|secret|token' evidence/dscp-ingress/README.md` returns no lines.
</acceptance_criteria>
<done>
DSCP-02 evidence index documents capture method, DSCP→tin mapping, EF-probe methodology, and the read-only posture, ready for the 225-03 verdict to cite.
</done>
</task>

<verification>
- `bash -n` and `shellcheck` on the capture script — both clean.
- Mutation-grep and controller-path-grep both empty.
- `evidence/dscp-ingress/README.md` present and complete.
</verification>
