---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase238-safe17-boundary-check.sh
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json
autonomous: true
requirements: [SAFE-17]
must_haves:
  decisions:
    - "D-09: Lightweight controller-path git-diff assertion (empty controller-path diff + explicit no-mutation statement); the full fail-closed SAFE-17 verifier and narrowed allowlist are deferred to Phase 239"
  truths:
    - "Operator can run scripts/phase238-safe17-boundary-check.sh and it exits 0 when controller-path is byte-unchanged vs the v1.52 anchor"
    - "The script is read-only: it modifies no worktree, index, refs, external gear, CAKE mode, or controller source"
    - "The controller-path target list is byte-identical to phase237-safe16-boundary-check.sh lines 67-75"
  artifacts:
    - path: "scripts/phase238-safe17-boundary-check.sh"
      provides: "Lightweight controller-path git-diff assertion for SAFE-17"
      contains: "set -euo pipefail"
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json"
      provides: "Machine-readable boundary-check verdict record"
      contains: "passed"
  key_links:
    - from: "scripts/phase238-safe17-boundary-check.sh"
      to: "v1.52 git tag"
      via: "git diff --numstat <anchor> HEAD -- <controller_targets>"
      pattern: "git diff --numstat"
---

<objective>
Author a lightweight, read-only SAFE-17 controller-path boundary-check script that proves Phase 238 introduces zero controller-path drift versus the v1.52 milestone anchor.

Purpose: SAFE-17 is the no-drift guarantee the entire read-only phase rests on. This is the foundational, independent deliverable — it must exist before the other waves so every subsequent commit can be re-checked against it. Per D-09 this is a LIGHTWEIGHT git-diff assertion only; the full fail-closed SAFE-17 verifier and narrowed allowlist are authored in Phase 239, NOT here.

Output: `scripts/phase238-safe17-boundary-check.sh` (committed, read-only) plus its JSON evidence record under the phase evidence dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@scripts/phase237-safe16-boundary-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author phase238-safe17-boundary-check.sh (lightweight clone)</name>
  <files>scripts/phase238-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/phase237-safe16-boundary-check.sh (FULL — this is the exact analog; clone its structure)
    - 238-PATTERNS.md section "scripts/phase238-safe17-boundary-check.sh" (lines 162-267 — tells you exactly what to KEEP and what to DROP)
    - 238-RESEARCH.md section "SAFE-17 Lightweight Boundary Assertion (SC-4, D-09)" (controller-path target list + anchor + scope boundary)
    - 238-CONTEXT.md D-09 (lightweight assertion only; full verifier is Phase 239)
  </read_first>
  <action>
    Clone scripts/phase237-safe16-boundary-check.sh into scripts/phase238-safe17-boundary-check.sh, lightly. KEEP: the `set -euo pipefail` header, the read-only posture banner in usage() (the "does not modify the worktree, index, refs, external gear, CAKE mode, or controller source" sentence — copy verbatim), `require_command date/git/mkdir/python3`, `--anchor`/`--out`/`--help` arg parsing with the unknown-arg `*)` exit-2 case, the controller_targets list (byte-identical to phase237 lines 67-75: wan_controller.py, wan_controller_state.py, queue_controller.py, cake_signal.py, alert_engine.py, fusion_healer.py, backends/), the `git()` helper, `parse_numstat`, `diff_for_path`, `dirty_for_path`, the JSON record write, `raise SystemExit(1)` on failure, and the final success echo. CHANGE the default `ANCHOR` to `v1.52` and the default `OUT` to `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json`. DROP entirely the fail-closed machinery: the `att_config = "configs/att.yaml"` special-case and all att handling, `object_id_at_anchor`/`object_id_at_worktree` per-file sha256 hashing, and `expand_protected_files` directory-union expansion. Simplify the pass condition to exactly three clauses ANDed: committed_clean (no committed diff lines on any controller target), staged_clean (no staged diff lines), dirty_tree_clean (empty `git status --porcelain` + no untracked) for the controller paths. Set the record `notes` field to: "SAFE-17 lightweight read-only boundary check vs v1.52; full fail-closed verifier + narrowed allowlist deferred to Phase 239." Header comment must label this as the Phase 238 SAFE-17 lightweight boundary check and reference D-09. Do NOT edit anything under src/wanctl/ — this is a read-only phase. `chmod +x` the script.
  </action>
  <verify>
    <automated>bash -n scripts/phase238-safe17-boundary-check.sh && grep -q 'set -euo pipefail' scripts/phase238-safe17-boundary-check.sh && grep -Fq 'src/wanctl/fusion_healer.py' scripts/phase238-safe17-boundary-check.sh && ! grep -q 'configs/att.yaml' scripts/phase238-safe17-boundary-check.sh && ! grep -q 'sha256' scripts/phase238-safe17-boundary-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase238-safe17-boundary-check.sh` exits 0 (valid syntax).
    - Script contains `set -euo pipefail`.
    - Script contains the read-only posture banner sentence ("does not modify the worktree, index, refs, external gear, CAKE mode, or controller source").
    - The seven controller-path targets are present and byte-match phase237 lines 67-75 (wan_controller.py, wan_controller_state.py, queue_controller.py, cake_signal.py, alert_engine.py, fusion_healer.py, backends/).
    - Default anchor is `v1.52`; default out path is the 238 evidence dir.
    - Script contains NO `configs/att.yaml` special-case and NO `sha256`/per-file-hash machinery.
    - Script is executable (`test -x`).
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>scripts/phase238-safe17-boundary-check.sh exists, parses clean, is the lightweight clone (no fail-closed machinery), anchored at v1.52, and is executable.</done>
</task>

<task type="auto">
  <name>Task 2: Run the assertion and capture the passing evidence record</name>
  <files>.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json</files>
  <read_first>
    - scripts/phase238-safe17-boundary-check.sh (the script authored in Task 1)
    - 238-VALIDATION.md section "Sampling Rate" (controller-path diff vs v1.52 MUST stay empty)
  </read_first>
  <action>
    Run `scripts/phase238-safe17-boundary-check.sh` with default args. Because Phase 238 changes only `scripts/` and phase-dir markdown (no controller-path files), the assertion MUST exit 0 and write the JSON evidence record to `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json` with `"passed": true`. If it exits non-zero, investigate root cause (a controller-path file was touched — which would violate the read-only phase invariant) and fix before proceeding; do NOT force-pass. The committed JSON record is the per-task / per-wave boundary evidence that the rest of the phase re-runs against.
  </action>
  <verify>
    <automated>scripts/phase238-safe17-boundary-check.sh && python3 -c "import json; d=json.load(open('.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json')); assert d['passed'] is True, d"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase238-safe17-boundary-check.sh` exits 0.
    - The JSON evidence record exists at the evidence path and has `"passed": true`.
    - The record's anchor field is `v1.52`.
    - `git status --porcelain -- src/wanctl/` is empty (no controller-path drift).
  </acceptance_criteria>
  <done>The SAFE-17 assertion exits 0 with a committed passing JSON record proving controller-path is byte-unchanged vs v1.52.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → local git repo | Script reads git state; must never write to worktree/index/refs |
| script arg (`--anchor`/`--out`) → git command | Untrusted CLI input must not inject into git invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-01 | Tampering | phase238-safe17-boundary-check.sh | mitigate | `set -euo pipefail`; only git-read ops (`diff --numstat`, `status --porcelain`), `mkdir -p` for evidence dir; no `git add`/`commit`/`checkout`/`reset`; read-only posture banner |
| T-238-01a | Tampering | `--anchor`/`--out` args | mitigate | args passed to `git()` and Python via argv (not string-interpolated into a shell); unknown-arg `*)` case exits 2 |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; zero external dependencies introduced (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `bash -n` parses the script clean.
- The script exits 0 and writes `"passed": true` to the evidence JSON.
- `git status --porcelain -- src/wanctl/` is empty.
- The controller-path target list byte-matches phase237 lines 67-75.
</verification>

<success_criteria>
- A lightweight read-only SAFE-17 boundary-check script exists, anchored at v1.52, with the fail-closed machinery dropped (D-09).
- Running it produces a passing JSON evidence record proving zero controller-path drift.
- No src/wanctl/ file is modified (read-only phase invariant holds).
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-01-SUMMARY.md` when done
</output>
