---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 03
type: execute
wave: 3
depends_on: ["238-01", "238-02"]
files_modified:
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json
autonomous: false
requirements: [PROV-01, PROV-02, SAFE-17]
must_haves:
  decisions:
    - "D-01: Evidence-first — the plan produces the provenance map plus an A/B recommendation with evidence; the operator makes the binding A/B selection at verification (no pre-baked verdict)"
    - "D-02: Present both interpretations honestly, including why each is or isn't viable; current leaning is genuinely undecided and driven by the evidence"
    - "D-03: Decision rubric = maximize A/B evidence fidelity; SAFE-17/blast-radius cost is documented as a tradeoff, not the primary selector"
    - "D-05: Single phase-dir evidence artifact 238-PROVENANCE-MAP.md; no separate docs/ runbook this phase"
    - "D-06: Artifact embeds (a) live /health measurement capture, (b) verified code-path trace proving the dead RTTMeasurement, (c) deployed-bridge identity with repo-vs-prod reconciliation"
    - "D-09: SAFE-17 boundary is re-asserted as the FINAL phase gate after all Phase 238 artifacts land (re-runs the 238-01 script)"
  truths:
    - "Operator can read a documented provenance map showing whether live steering RTT comes from the state bridge / autorate /health measurement.raw_rtt_ms or from wanctl's RTTMeasurement, captured read-only with zero production mutation"
    - "The map embeds a live /health measurement-block capture from the production cake-shaper host (both WANs)"
    - "The map embeds the verified code-path trace proving steering reads autorate /health raw_rtt_ms and that the constructed RTTMeasurement is dead, with file:line citations re-confirmed against current source before transcription"
    - "The map records the deployed-bridge identity (sha vs repo copy) confirming /usr/local/sbin/cake-autorate-{wan}-state-bridge is the live raw_rtt_ms producer"
    - "The map presents BOTH A/B interpretations honestly with the fidelity-rubric recommendation (lean A) AND an explicit operator-ratification slot that, once filled, contains a concrete Selection: A or Selection: B line"
    - "The SAFE-17 controller-path boundary is re-asserted as the FINAL gate of the phase: after both scripts + the provenance map + the ratification edit are in place, scripts/phase238-safe17-boundary-check.sh is re-run and still exits 0 with passed:true, proving no controller-path drift was introduced by any Phase 238 artifact"
  artifacts:
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md"
      provides: "PROV-01 provenance evidence + PROV-02 A/B recommendation and operator-ratification slot"
      contains: "measurement"
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json"
      provides: "Refreshed final-gate SAFE-17 boundary verdict (re-run after all phase artifacts land)"
      contains: "passed"
  key_links:
    - from: "238-PROVENANCE-MAP.md A/B section"
      to: "Phase 245 live A/B target selection"
      via: "operator-ratified interpretation A or B"
      pattern: "Interpretation A|Interpretation B"
---

<objective>
Author `238-PROVENANCE-MAP.md` — the phase-dir evidence artifact that documents which producer actually feeds live steering RTT in the current cake-autorate topology (PROV-01) and presents an evidence-backed A/B target recommendation with an operator-ratification slot (PROV-02) — then re-assert the SAFE-17 controller-path boundary as the final phase gate.

Purpose: This artifact is the input contract for the rest of v1.53 — its A/B target selection is consumed directly by Phase 245 (live A/B) and shapes Phase 239's seam placement and Phase 242's factory wiring. Per D-01 the plan must NOT pre-bake the verdict: it provides the recommendation plus evidence and leaves an explicit place for the operator (Kevin) to record the binding choice at verification. As the last plan in the phase, it also re-runs the SAFE-17 boundary check (authored in Plan 01) so the no-drift guarantee is proven AFTER every Phase 238 artifact is in place.

Output: `238-PROVENANCE-MAP.md` embedding (a) a live `/health` measurement-block capture, (b) the verified code-path trace, (c) the deployed-bridge identity + repo-vs-prod reconciliation, and a both-interpretations A/B section with a fidelity-rubric recommendation and a ratification slot; plus a refreshed final-gate SAFE-17 evidence JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md

<interfaces>
<!-- Verified code-path citations the executor needs (from 238-RESEARCH.md Code-Path Trace).
     RE-CONFIRM each citation against current source before transcribing (CONCERN 5 fix —
     this is a provenance artifact; stale citations are a real quality risk). -->

Live steering RTT consumption chain (src/wanctl/steering/daemon.py):
  run_cycle() RTT subsystem        :2080-2089  (current_rtt = self._measure_current_rtt_with_retry())
  _measure_current_rtt_with_retry  :1769-1815  (wraps measure_current_rtt; history_fallback)
  measure_current_rtt              :1755-1767  (autorate_health -> autorate_irtt -> unavailable)
  load_live_rtt (BaselineLoader)   :1000-1024  (reads measurement.{available,raw_rtt_ms,staleness_sec}, staleness-gated)
  _load_target_wan_health          :1052-1076  (urlopen primary_health_url, selects primary_wan entry)

Dead RTTMeasurement (constructed, never called):
  construction _create_steering_components :2554-2559  (NO source_ip= passed)
  stored self.rtt_measurement              :1137
  RTTMeasurement.__init__ source_ip param  rtt_measurement.py:139-172
  ping_host() source= (icmplib -S equiv)   rtt_measurement.py:199-207

Production raw_rtt_ms producer (deployed bridge):
  ExecStart -> /usr/local/sbin/cake-autorate-{spectrum,att}-state-bridge   (deploy/systemd/*.service)
  repo source                              deploy/scripts/cake-autorate-{wan}-state-bridge
  rates_from_row (NO RTT parsed)           bridge:65
  build_state ewma load_rtt carried-fwd    bridge:191-218
  old_rtt() defaults DEFAULT_BASELINE_RTT  bridge:96-104, :23  (~22.535852814520855)
  health_payload raw_rtt_ms = load_rtt     bridge:249-271

Prod /health measurement block shape steering consumes:
  { "measurement": { "available": true, "raw_rtt_ms": 22.54, "staleness_sec": 0.7 } }

Per-WAN source IPs / reflectors / health endpoints:
  spectrum: ping_source_ip ABSENT (default route via 10.10.110.223); reflectors 1.1.1.1 9.9.9.9 208.67.222.222; /health 10.10.110.223:9101
  att:      ping_source_ip 10.10.110.227 (FORCE_OUT_ATT);            reflectors 1.1.1.1 8.8.8.8 151.101.1.57; /health 10.10.110.227:9101
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator captures live /health blocks + deployed-bridge sha</name>
  <what-built>The provenance map needs two operator-captured live read-only inputs from the production cake-shaper host: the `/health` `measurement` block (both WANs) and the deployed-bridge sha for repo-vs-prod reconciliation.</what-built>
  <read_first>
    - 238-RESEARCH.md "Code Examples" (the exact read-only `! ssh` / curl commands) and "Open Questions" #1 (deployed-bridge sha compare)
    - 238-CONTEXT.md D-06 (the three required embeds: live /health, code-path trace, deployed bridge identity) and D-08 (operator runs live reads)
    - project credential-reads convention (hand operator `! ssh …` / `! curl …`, do not escalate)
  </read_first>
  <action>
    Two live read-only captures require operator-at-keyboard access to the production cake-shaper (D-06, D-08, credential-reads convention) — do NOT escalate stored credentials. Present the operator with the exact commands: (1) `! ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.223:9101/health'` (Spectrum) and `! ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.227:9101/health'` (ATT), each piped through `python3 -m json.tool`, to capture the live `measurement` block steering actually reads; (2) `! ssh cake-shaper 'sha256sum /usr/local/sbin/cake-autorate-spectrum-state-bridge /usr/local/sbin/cake-autorate-att-state-bridge'`, to compare against the repo copies via `sha256sum deploy/scripts/cake-autorate-{spectrum,att}-state-bridge`. The operator runs these read-only and pastes the output back, NOTING for each capture the exact command, the host it ran on, an ISO timestamp, and whether the output came from prod vs repo (CONCERN 5 / review LOW — live-capture provenance). Note the load-bearing finding to confirm at capture (RESEARCH Pitfall 1): `raw_rtt_ms` is expected to read ~22.5ms and barely move across captures because it is carried-forward `ewma.load_rtt` / `DEFAULT_BASELINE_RTT`, not a fresh sample. Also have ready the Plan 02 egress-proof stdout (captured in 238-02). These three captures (+ egress stdout) are the embedded evidence for Task 2.
  </action>
  <verify>
    <human-check>Operator ran the `! ssh`/`! curl` /health captures (both WANs) and the deployed-bridge `sha256sum` read-only on the cake-shaper host and pasted the output, each annotated with command/host/timestamp/prod-vs-repo; the live measurement block(s) and the deployed-bridge shas are available, alongside the Plan 02 egress-proof stdout, for embedding in the map.</human-check>
  </verify>
  <acceptance_criteria>
    - Live `/health` `measurement` block captured for both WANs (read-only), each annotated with the exact command, host, ISO timestamp, and prod-vs-repo provenance.
    - Deployed-bridge sha256 captured for both bridges and the repo `deploy/scripts/...` shas computed for comparison.
    - Plan 02 egress-proof stdout (both WANs + ip rule) is on hand.
    - No production mutation occurred (only curl GET / sha256sum / ip read queries ran).
  </acceptance_criteria>
  <resume-signal>Paste the live /health captures (both WANs) and the deployed-bridge + repo sha256 values (each with command/host/timestamp), then type "captured" to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author 238-PROVENANCE-MAP.md with all D-06 evidence + A/B recommendation</name>
  <files>.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md</files>
  <read_first>
    - 238-RESEARCH.md "Code-Path Trace (PROV-01) — VERIFIED", "/health measurement Block Shape", and "A/B Interpretation Analysis (PROV-02)" sections (the substance to transcribe)
    - 238-CONTEXT.md D-01..D-06 (artifact requirements; evidence-first; operator ratifies)
    - 238-PATTERNS.md section ".planning/phases/238-.../PROVENANCE-MAP.md" (required embedded content checklist)
    - the actual cited source lines (re-open src/wanctl/steering/daemon.py and deploy/scripts/cake-autorate-{wan}-state-bridge at the cited line ranges and confirm they are current BEFORE transcribing — fresh source check, not stale transcription)
    - the operator captures from Task 1 (live /health blocks, deployed-bridge shas) and the Plan 02 egress-proof stdout
  </read_first>
  <action>
    Before transcribing any citation, RE-OPEN the cited source lines from the `<interfaces>` block (daemon.py and the bridge) and confirm they still say what the citation claims (CONCERN 5 / review MEDIUM — fresh source check for a provenance artifact). Then author `238-PROVENANCE-MAP.md` as a phase-dir evidence artifact (shape reference: prior read-only milestone evidence 212/222/225). Required sections: (1) a "Live RTT Source — Verified Code-Path Trace" section transcribing the verified chain run_cycle -> _measure_current_rtt_with_retry -> measure_current_rtt -> load_live_rtt reading autorate `/health` `measurement.raw_rtt_ms`, WITH file:line citations from the `<interfaces>` block (re-confirmed current), and explicitly documenting that the constructed `RTTMeasurement` (daemon.py:2554, stored :1137) is dead — never invoked — and passes no `source_ip`; (2) a "Live /health Measurement Block" section embedding the operator-captured measurement block as TWO separate fenced json code blocks — one per WAN, with BOTH WAN labels present (spectrum AND att) and each containing `raw_rtt_ms` (the prod/bridge shape steering reads); note the health_check.py richer shape for contrast (Phase 244 must byte-preserve raw_rtt_ms/available/staleness_sec); (3) a "Deployed Bridge Identity + Repo-vs-Prod Reconciliation" section recording `/usr/local/sbin/cake-autorate-{spectrum,att}-state-bridge` as the live raw_rtt_ms producer, embedding FOUR 64-hex SHA-256 values (2 deployed bridges + 2 repo copies) and an explicit match/mismatch statement; (4) the LOAD-BEARING crux: state prominently that the bridge's `raw_rtt_ms` is carried-forward `ewma.load_rtt` / static `DEFAULT_BASELINE_RTT` (~22.5ms), NOT a fresh path RTT (cite bridge:65 no-RTT-parse, :96-104 old_rtt, :249-271 health_payload), and that the real RTT signal lives in upstream BASH cake-autorate which the wanctl Python RttBackend seam cannot reach; (5) embed the Plan 02 egress-proof stdout (PROV-03 cross-reference) as fenced blocks — include the actual `ip rule` block AND the `ip route get` resolved-path output (not a placeholder), each annotated with command/host/timestamp; (6) an "A/B Target Recommendation (PROV-02)" section presenting BOTH interpretations honestly (D-02): Interpretation A (revive steering's own pinger — in v1.53 scope, fidelity-positive, requires wiring source_ip into the source-less daemon.py:2554 construction, higher SAFE-17 cost) and Interpretation B (evaluate at the autorate/bridge producer — largely OUT of v1.53 because the live value is a static baseline and the real signal is unreachable bash; NATIVE-AB-01 deferred), with the fidelity-rubric (D-03) recommendation = lean A, and the SAFE-17/blast-radius cost documented as a tradeoff NOT the selector; (7) an explicit "Operator Ratification" slot — a clearly-marked section for Kevin to record the binding selection at verification (D-01), containing a `Selection: <pending>` line left UNFILLED — do NOT pre-fill the verdict; (8) a "Zero Production Mutation" statement noting all captures were read-only (curl GET / sha256sum / ip route get / ip rule / git read). The map must contain NO placeholder tokens (`TODO`, `CAPTURE_PENDING`, `FIXME`, `XXX`, `<paste`) in the embedded-evidence sections — all live evidence must be the real captured output. Keep all internal IPs in this phase-dir artifact; do NOT promote them to public-safe CLAUDE.md/README (Info Disclosure mitigation).
  </action>
  <verify>
    <automated>F=.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md; test -f "$F" && grep -q 'raw_rtt_ms' "$F" && grep -qi 'Interpretation A' "$F" && grep -qi 'Interpretation B' "$F" && grep -qiE 'operator ratification|ratification slot' "$F" && grep -qE 'daemon\.py:2554|:2554' "$F" && grep -qE 'DEFAULT_BASELINE_RTT|load_rtt' "$F" && grep -qiw spectrum "$F" && grep -qiw att "$F" && grep -q 'ip rule' "$F" && grep -q 'ip route get' "$F" && test "$(grep -c '```json' "$F")" -ge 2 && test "$(grep -oE '[0-9a-f]{64}' "$F" | sort -u | wc -l)" -ge 4 && python3 -c "import re,json; t=open('$F').read(); blocks=re.findall(r'```json(.*?)```', t, re.S); n=sum(1 for b in blocks if 'raw_rtt_ms' in b and (json.loads(b) or True)); assert n>=2, ('parseable json health blocks with raw_rtt_ms:', n)" && ! grep -qE 'TODO|CAPTURE_PENDING|FIXME|XXX|<paste' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `238-PROVENANCE-MAP.md` exists in the phase dir.
    - Contains the verified code-path trace with file:line citations (includes `daemon.py:2554` / `:2554` and `load_live_rtt` chain) and states the constructed RTTMeasurement is dead.
    - Executor re-confirmed the file:line citations against current source before embedding (fresh source check, not stale transcription).
    - Contains at least TWO parseable fenced json blocks (both WAN labels spectrum AND att present), each containing `raw_rtt_ms` and each valid JSON (parsed by python3).
    - Records deployed-bridge identity (`/usr/local/sbin/cake-autorate-{wan}-state-bridge`) with FOUR distinct 64-hex SHA-256 values (2 deployed + 2 repo) and an explicit match/mismatch statement.
    - States the load-bearing crux: `raw_rtt_ms` is carried-forward `ewma.load_rtt` / `DEFAULT_BASELINE_RTT`, not a fresh sample; real signal lives in unreachable bash cake-autorate.
    - Embeds the Plan 02 egress-proof stdout: an actual `ip rule` block AND `ip route get` resolved-path output (not placeholders), each annotated with command/host/timestamp/prod-vs-repo.
    - Presents BOTH "Interpretation A" and "Interpretation B" honestly, recommends lean A on the fidelity rubric, documents the SAFE-17 tradeoff.
    - Contains an explicit operator-ratification slot with an initially-UNFILLED `Selection: <pending>` line (verdict NOT pre-baked).
    - Contains NO placeholder tokens (`TODO`, `CAPTURE_PENDING`, `FIXME`, `XXX`, `<paste`) in the embedded-evidence sections.
    - Contains a zero-production-mutation statement.
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>238-PROVENANCE-MAP.md exists embedding all D-06 evidence (two parseable per-WAN /health json blocks, re-confirmed code-path trace, deployed-bridge identity with four 64-hex shas + match/mismatch), the carried-forward-baseline crux, the real egress-proof stdout (ip rule + ip route get), a both-interpretations A/B recommendation (lean A), and an unfilled `Selection:` ratification slot, with no placeholder tokens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator reviews map and records binding A/B selection</name>
  <what-built>238-PROVENANCE-MAP.md with the full PROV-01 evidence set and the PROV-02 A/B recommendation, plus an operator-ratification slot for the binding A-or-B selection.</what-built>
  <how-to-verify>
    1. Read `238-PROVENANCE-MAP.md`. Confirm the code-path trace, the embedded live `/health` blocks, the deployed-bridge sha reconciliation, and the carried-forward-baseline crux are all present and accurate against your live captures.
    2. Read the A/B Target Recommendation section. Confirm both interpretations are presented honestly and the fidelity-rubric recommendation (lean A) is sound.
    3. Make the binding A/B selection (Interpretation A = revive steering's own pinger, or Interpretation B = evaluate at the autorate/bridge producer) and record it in the operator-ratification slot by setting the `Selection:` line to `Selection: A` or `Selection: B`. This selection is consumed by Phase 245.
  </how-to-verify>
  <read_first>
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
    - 238-CONTEXT.md D-01 (operator makes the binding selection at verification)
  </read_first>
  <action>Operator reviews 238-PROVENANCE-MAP.md per the how-to-verify steps, confirms the PROV-01 evidence matches the live captures, and records the binding A-or-B selection by editing the ratification slot's `Selection:` line to `Selection: A` or `Selection: B` (D-01). This selection is the input contract consumed by Phase 245.</action>
  <verify>
    <automated>grep -qE 'Selection:[[:space:]]*[AB]\b' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md</automated>
    <human-check>Operator reviewed the map, confirmed PROV-01 evidence accuracy, and recorded the binding A/B selection as a concrete `Selection: A` or `Selection: B` line in the operator-ratification slot.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirmed the embedded PROV-01 evidence matches the live captures.
    - The ratified map contains a line matching `Selection:[[:space:]]*(A|B)` (mechanically verifiable, not human-attested only).
  </acceptance_criteria>
  <resume-signal>Type "approved" with your A/B selection (recorded as `Selection: A` or `Selection: B` in the map), or describe corrections needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Final SAFE-17 rerun — re-assert the controller-path boundary as the last phase gate</name>
  <files>.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json</files>
  <read_first>
    - scripts/phase238-safe17-boundary-check.sh (authored in Plan 01 — this task depends on it existing)
    - 238-CONTEXT.md D-09 (lightweight controller-path assertion; the no-drift guarantee)
  </read_first>
  <action>
    This is the FINAL gate of Phase 238 (CONCERN 2 fix — boundary evidence must not go stale). After ALL other Phase 238 artifacts are in place (both committed scripts from Plans 01/02, the provenance map, and the operator's ratification `Selection:` edit from Task 3), re-run `scripts/phase238-safe17-boundary-check.sh` with default args. It MUST still exit 0 and refresh the JSON evidence record at `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json` with `"passed": true`, proving no Phase 238 artifact introduced any controller-path drift vs the v1.52 anchor. This depends on Plan 01 having authored the script. If it exits non-zero, a controller-path file was touched somewhere across the phase — investigate root cause and fix; do NOT force-pass.
  </action>
  <verify>
    <automated>scripts/phase238-safe17-boundary-check.sh && python3 -c "import json; d=json.load(open('.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/evidence/safe17-boundary-238.json')); assert d['passed'] is True, d" && test -z "$(git status --porcelain -- src/wanctl/)"</automated>
  </verify>
  <acceptance_criteria>
    - The post-artifact SAFE-17 rerun exits 0.
    - The refreshed evidence JSON at the evidence path has `"passed": true`.
    - `git status --porcelain -- src/wanctl/` is still empty (no controller-path drift introduced by any Phase 238 artifact).
    - This rerun is the LAST gate executed in the phase (after the operator ratification in Task 3).
  </acceptance_criteria>
  <done>The SAFE-17 boundary check, re-run after all Phase 238 artifacts (both scripts + provenance map + ratification edit) are in place, still exits 0 with a refreshed passed:true evidence record and an empty src/wanctl/ git status — proving the read-only no-drift guarantee held across the entire phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Live `/health` + sha captures must be read-only |
| evidence artifact → public docs | Internal IPs and live values must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-03 | Information Disclosure | 238-PROVENANCE-MAP.md | mitigate | Internal `10.10.110.x` IPs and live `/health` values stay in the phase-dir artifact; never promoted to public-safe CLAUDE.md/README |
| T-238-03a | Tampering | live /health + sha captures | mitigate | Captures are read-only operator commands (`curl` GET, `sha256sum`); no escalation of stored credentials; no prod mutation |
| T-238-03b | Repudiation | A/B verdict | mitigate | Plan does not pre-bake the verdict (D-01); operator records the binding selection as a concrete `Selection: A`/`Selection: B` line in an explicit ratification slot (mechanically grep-checkable), making the decision attributable |
| T-238-03c | Tampering | final SAFE-17 rerun gate | mitigate | Re-runs the Plan 01 read-only boundary-check script after all artifacts land; asserts passed:true + empty src/wanctl/ status; no force-pass on failure |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `238-PROVENANCE-MAP.md` exists with the re-confirmed code-path trace, two parseable per-WAN /health json blocks (with raw_rtt_ms), deployed-bridge sha reconciliation (four 64-hex shas + match/mismatch), the carried-forward-baseline crux, the real egress-proof stdout (ip rule + ip route get), and a both-interpretations A/B recommendation — with NO placeholder tokens.
- An explicit operator-ratification slot exists; operator records the binding A/B selection as a `Selection: A`/`Selection: B` line at the human-verify checkpoint (grep-checkable).
- The FINAL SAFE-17 rerun (Task 4) — the last gate of the phase — exits 0 with a refreshed passed:true evidence record after all artifacts land.
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- The provenance map documents the live steering RTT source with verified (freshly re-confirmed) file:line evidence and zero production mutation (PROV-01), with structural evidence gates (parseable json blocks, four shas, no placeholders) — not grep-on-placeholder.
- The map presents both A/B interpretations honestly with a fidelity-rubric recommendation and an operator-ratified binding `Selection:` line (PROV-02).
- The SAFE-17 controller-path boundary is re-asserted as the final phase gate after all artifacts land (SAFE-17).
- No src/wanctl/ file is modified.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-03-SUMMARY.md` when done
</output>
