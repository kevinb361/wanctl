---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 03
type: execute
wave: 2
depends_on: ["238-02"]
files_modified:
  - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
autonomous: false
requirements: [PROV-01, PROV-02]
must_haves:
  truths:
    - "Operator can read a documented provenance map showing whether live steering RTT comes from the state bridge / autorate /health measurement.raw_rtt_ms or from wanctl's RTTMeasurement, captured read-only with zero production mutation"
    - "The map embeds a live /health measurement-block capture from the production cake-shaper host (both WANs)"
    - "The map embeds the verified code-path trace proving steering reads autorate /health raw_rtt_ms and that the constructed RTTMeasurement is dead, with file:line citations"
    - "The map records the deployed-bridge identity (sha vs repo copy) confirming /usr/local/sbin/cake-autorate-{wan}-state-bridge is the live raw_rtt_ms producer"
    - "The map presents BOTH A/B interpretations honestly with the fidelity-rubric recommendation (lean A) AND an explicit operator-ratification slot left for the binding selection"
  artifacts:
    - path: ".planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md"
      provides: "PROV-01 provenance evidence + PROV-02 A/B recommendation and operator-ratification slot"
      contains: "measurement"
  key_links:
    - from: "238-PROVENANCE-MAP.md A/B section"
      to: "Phase 245 live A/B target selection"
      via: "operator-ratified interpretation A or B"
      pattern: "Interpretation A|Interpretation B"
---

<objective>
Author `238-PROVENANCE-MAP.md` — the phase-dir evidence artifact that documents which producer actually feeds live steering RTT in the current cake-autorate topology (PROV-01) and presents an evidence-backed A/B target recommendation with an operator-ratification slot (PROV-02).

Purpose: This artifact is the input contract for the rest of v1.53 — its A/B target selection is consumed directly by Phase 245 (live A/B) and shapes Phase 239's seam placement and Phase 242's factory wiring. Per D-01 the plan must NOT pre-bake the verdict: it provides the recommendation plus evidence and leaves an explicit place for the operator (Kevin) to record the binding choice at verification.

Output: `238-PROVENANCE-MAP.md` embedding (a) a live `/health` measurement-block capture, (b) the verified code-path trace, (c) the deployed-bridge identity + repo-vs-prod reconciliation, and a both-interpretations A/B section with a fidelity-rubric recommendation and a ratification slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md

<interfaces>
<!-- Verified code-path citations the executor needs (from 238-RESEARCH.md Code-Path Trace).
     Use these directly; re-confirm against source only if a citation looks stale. -->

Live steering RTT consumption chain (src/wanctl/steering/daemon.py):
  run_cycle() RTT subsystem        :2080-2089  (current_rtt = self._measure_current_rtt_with_retry())
  _measure_current_rtt_with_retry  :1769-1815  (wraps measure_current_rtt; history_fallback)
  measure_current_rtt              :1755-1767  (autorate_health → autorate_irtt → unavailable)
  load_live_rtt (BaselineLoader)   :1000-1024  (reads measurement.{available,raw_rtt_ms,staleness_sec}, staleness-gated)
  _load_target_wan_health          :1052-1076  (urlopen primary_health_url, selects primary_wan entry)

Dead RTTMeasurement (constructed, never called):
  construction _create_steering_components :2554-2559  (NO source_ip= passed)
  stored self.rtt_measurement              :1137
  RTTMeasurement.__init__ source_ip param  rtt_measurement.py:139-172
  ping_host() source= (icmplib -S equiv)   rtt_measurement.py:199-207

Production raw_rtt_ms producer (deployed bridge):
  ExecStart → /usr/local/sbin/cake-autorate-{spectrum,att}-state-bridge   (deploy/systemd/*.service)
  repo source                              deploy/scripts/cake-autorate-{wan}-state-bridge
  rates_from_row (NO RTT parsed)           bridge:65
  build_state ewma load_rtt carried-fwd    bridge:191-218
  old_rtt() defaults DEFAULT_BASELINE_RTT  bridge:96-104, :23  (~22.535852814520855)
  health_payload raw_rtt_ms = load_rtt     bridge:249-271

Prod /health measurement block shape steering consumes:
  { "measurement": { "available": true, "raw_rtt_ms": 22.54, "staleness_sec": 0.7 } }

Per-WAN source IPs / reflectors / health endpoints:
  spectrum: ping_source_ip ABSENT (default route via 10.10.110.223); reflectors 1.1.1.1 9.9.9.9 208.67.222.222; /health 10.10.110.223:9101
  att:      ping_source_ip 10.10.110.227 (FORCE_OUT_ATT);            reflectors 1.1.1.1 8.8.8.8 151.101.1.57; /health 10.10.110.227:9101
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator captures live /health blocks + deployed-bridge sha</name>
  <what-built>The provenance map needs two operator-captured live read-only inputs from the production cake-shaper host: the `/health` `measurement` block (both WANs) and the deployed-bridge sha for repo-vs-prod reconciliation.</what-built>
  <read_first>
    - 238-RESEARCH.md "Code Examples" (the exact read-only `! ssh` / curl commands) and "Open Questions" #1 (deployed-bridge sha compare)
    - 238-CONTEXT.md D-06 (the three required embeds: live /health, code-path trace, deployed bridge identity) and D-08 (operator runs live reads)
    - project credential-reads convention (hand operator `! ssh …` / `! curl …`, do not escalate)
  </read_first>
  <action>
    Two live read-only captures require operator-at-keyboard access to the production cake-shaper (D-06, D-08, credential-reads convention) — do NOT escalate stored credentials. Present the operator with the exact commands: (1) `! ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.223:9101/health'` (Spectrum) and `! ssh cake-shaper 'curl -fsS --max-time 5 http://10.10.110.227:9101/health'` (ATT), each piped through `python3 -m json.tool`, to capture the live `measurement` block steering actually reads; (2) `! ssh cake-shaper 'sha256sum /usr/local/sbin/cake-autorate-spectrum-state-bridge /usr/local/sbin/cake-autorate-att-state-bridge'`, to compare against the repo copies via `sha256sum deploy/scripts/cake-autorate-{spectrum,att}-state-bridge`. The operator runs these read-only and pastes the output back. Note the load-bearing finding to confirm at capture (RESEARCH Pitfall 1): `raw_rtt_ms` is expected to read ~22.5ms and barely move across captures because it is carried-forward `ewma.load_rtt` / `DEFAULT_BASELINE_RTT`, not a fresh sample. Also have ready the Plan 02 egress-proof stdout (captured in 238-02). These three captures (+ egress stdout) are the embedded evidence for Task 2.
  </action>
  <verify>
    <human-check>Operator ran the `! ssh`/`! curl` /health captures (both WANs) and the deployed-bridge `sha256sum` read-only on the cake-shaper host and pasted the output; the live measurement block(s) and the deployed-bridge shas are available, alongside the Plan 02 egress-proof stdout, for embedding in the map.</human-check>
  </verify>
  <acceptance_criteria>
    - Live `/health` `measurement` block captured for both WANs (read-only).
    - Deployed-bridge sha256 captured for both bridges and the repo `deploy/scripts/...` shas computed for comparison.
    - Plan 02 egress-proof stdout (both WANs + ip rule) is on hand.
    - No production mutation occurred (only curl GET / sha256sum / ip read queries ran).
  </acceptance_criteria>
  <resume-signal>Paste the live /health captures (both WANs) and the deployed-bridge + repo sha256 values, then type "captured" to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author 238-PROVENANCE-MAP.md with all D-06 evidence + A/B recommendation</name>
  <files>.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md</files>
  <read_first>
    - 238-RESEARCH.md "Code-Path Trace (PROV-01) — VERIFIED", "/health measurement Block Shape", and "A/B Interpretation Analysis (PROV-02)" sections (the substance to transcribe)
    - 238-CONTEXT.md D-01..D-06 (artifact requirements; evidence-first; operator ratifies)
    - 238-PATTERNS.md section ".planning/phases/238-.../PROVENANCE-MAP.md" (required embedded content checklist)
    - the operator captures from Task 1 (live /health blocks, deployed-bridge shas) and the Plan 02 egress-proof stdout
  </read_first>
  <action>
    Author `238-PROVENANCE-MAP.md` as a phase-dir evidence artifact (shape reference: prior read-only milestone evidence 212/222/225). Required sections: (1) a "Live RTT Source — Verified Code-Path Trace" section transcribing the verified chain `run_cycle → _measure_current_rtt_with_retry → measure_current_rtt → load_live_rtt` reading autorate `/health` `measurement.raw_rtt_ms`, WITH file:line citations from the `<interfaces>` block, and explicitly documenting that the constructed `RTTMeasurement` (daemon.py:2554, stored :1137) is dead — never invoked — and passes no `source_ip`; (2) a "Live /health Measurement Block" section embedding the operator-captured measurement block as a fenced ```json block for BOTH WANs (the prod/bridge shape steering reads); note the health_check.py richer shape for contrast (Phase 244 must byte-preserve raw_rtt_ms/available/staleness_sec); (3) a "Deployed Bridge Identity + Repo-vs-Prod Reconciliation" section recording `/usr/local/sbin/cake-autorate-{spectrum,att}-state-bridge` as the live raw_rtt_ms producer, the captured deployed shas vs repo `deploy/scripts/...` shas, and whether they match; (4) the LOAD-BEARING crux: state prominently that the bridge's `raw_rtt_ms` is carried-forward `ewma.load_rtt` / static `DEFAULT_BASELINE_RTT` (~22.5ms), NOT a fresh path RTT (cite bridge:65 no-RTT-parse, :96-104 old_rtt, :249-271 health_payload), and that the real RTT signal lives in upstream BASH cake-autorate which the wanctl Python RttBackend seam cannot reach; (5) embed the Plan 02 egress-proof stdout (PROV-03 cross-reference) as a fenced block; (6) an "A/B Target Recommendation (PROV-02)" section presenting BOTH interpretations honestly (D-02): Interpretation A (revive steering's own pinger — in v1.53 scope, fidelity-positive, requires wiring source_ip into the source-less daemon.py:2554 construction, higher SAFE-17 cost) and Interpretation B (evaluate at the autorate/bridge producer — largely OUT of v1.53 because the live value is a static baseline and the real signal is unreachable bash; NATIVE-AB-01 deferred), with the fidelity-rubric (D-03) recommendation = lean A, and the SAFE-17/blast-radius cost documented as a tradeoff NOT the selector; (7) an explicit "Operator Ratification" slot — a clearly-marked, initially-unfilled section for Kevin to record the binding A-or-B selection at verification (D-01) — do NOT pre-fill the verdict; (8) a "Zero Production Mutation" statement noting all captures were read-only (curl GET / sha256sum / ip route get / ip rule / git read). Keep all internal IPs in this phase-dir artifact; do NOT promote them to public-safe CLAUDE.md/README (Info Disclosure mitigation).
  </action>
  <verify>
    <automated>test -f .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -q 'measurement' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -q 'raw_rtt_ms' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -qi 'Interpretation A' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -qi 'Interpretation B' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -qiE 'operator ratification|ratification slot' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -qE 'daemon\.py:2554|:2554' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md && grep -qE 'DEFAULT_BASELINE_RTT|load_rtt' .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `238-PROVENANCE-MAP.md` exists in the phase dir.
    - Contains the verified code-path trace with file:line citations (includes `daemon.py:2554` / `:2554` and `load_live_rtt` chain) and states the constructed RTTMeasurement is dead.
    - Contains a fenced live `/health` `measurement` block for both WANs (operator-captured), with `raw_rtt_ms`.
    - Records deployed-bridge identity (`/usr/local/sbin/cake-autorate-{wan}-state-bridge`) with deployed-vs-repo sha comparison.
    - States the load-bearing crux: `raw_rtt_ms` is carried-forward `ewma.load_rtt` / `DEFAULT_BASELINE_RTT`, not a fresh sample; real signal lives in unreachable bash cake-autorate.
    - Embeds the Plan 02 egress-proof stdout.
    - Presents BOTH "Interpretation A" and "Interpretation B" honestly, recommends lean A on the fidelity rubric, documents the SAFE-17 tradeoff.
    - Contains an explicit, initially-unfilled operator-ratification slot (verdict NOT pre-baked).
    - Contains a zero-production-mutation statement.
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>238-PROVENANCE-MAP.md exists embedding all D-06 evidence (live /health, code-path trace, deployed-bridge identity), the carried-forward-baseline crux, the egress-proof stdout, a both-interpretations A/B recommendation (lean A), and an unfilled operator-ratification slot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator reviews map and records binding A/B selection</name>
  <what-built>238-PROVENANCE-MAP.md with the full PROV-01 evidence set and the PROV-02 A/B recommendation, plus an operator-ratification slot for the binding A-or-B selection.</what-built>
  <how-to-verify>
    1. Read `238-PROVENANCE-MAP.md`. Confirm the code-path trace, the embedded live `/health` blocks, the deployed-bridge sha reconciliation, and the carried-forward-baseline crux are all present and accurate against your live captures.
    2. Read the A/B Target Recommendation section. Confirm both interpretations are presented honestly and the fidelity-rubric recommendation (lean A) is sound.
    3. Make the binding A/B selection (Interpretation A = revive steering's own pinger, or Interpretation B = evaluate at the autorate/bridge producer) and record it in the operator-ratification slot. This selection is consumed by Phase 245.
  </how-to-verify>
  <read_first>
    - .planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PROVENANCE-MAP.md
    - 238-CONTEXT.md D-01 (operator makes the binding selection at verification)
  </read_first>
  <action>Operator reviews 238-PROVENANCE-MAP.md per the how-to-verify steps, confirms the PROV-01 evidence matches the live captures, and records the binding A-or-B selection in the operator-ratification slot (D-01). This selection is the input contract consumed by Phase 245.</action>
  <verify>
    <human-check>Operator reviewed the map, confirmed PROV-01 evidence accuracy, and recorded the binding A/B selection in the operator-ratification slot.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirmed the embedded PROV-01 evidence matches the live captures.
    - Operator recorded a binding A-or-B selection in the ratification slot.
  </acceptance_criteria>
  <resume-signal>Type "approved" with your A/B selection, or describe corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Live `/health` + sha captures must be read-only |
| evidence artifact → public docs | Internal IPs and live values must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-03 | Information Disclosure | 238-PROVENANCE-MAP.md | mitigate | Internal `10.10.110.x` IPs and live `/health` values stay in the phase-dir artifact; never promoted to public-safe CLAUDE.md/README |
| T-238-03a | Tampering | live /health + sha captures | mitigate | Captures are read-only operator commands (`curl` GET, `sha256sum`); no escalation of stored credentials; no prod mutation |
| T-238-03b | Repudiation | A/B verdict | mitigate | Plan does not pre-bake the verdict (D-01); operator records the binding selection in an explicit ratification slot, making the decision attributable |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `238-PROVENANCE-MAP.md` exists with the code-path trace, live /health blocks (both WANs), deployed-bridge sha reconciliation, the carried-forward-baseline crux, the egress-proof stdout, and a both-interpretations A/B recommendation.
- An explicit operator-ratification slot exists; operator records the binding A/B selection at the human-verify checkpoint.
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- The provenance map documents the live steering RTT source with verified file:line evidence and zero production mutation (PROV-01).
- The map presents both A/B interpretations honestly with a fidelity-rubric recommendation and an operator-ratified binding selection (PROV-02).
- No src/wanctl/ file is modified.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-03-SUMMARY.md` when done
</output>
