---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 02
type: execute
wave: 2
depends_on: ["238-01"]
files_modified:
  - scripts/phase238-egress-proof.sh
autonomous: false
requirements: [PROV-03]
must_haves:
  decisions:
    - "D-07: Cover both WANs (Spectrum + ATT) — prove ip route get <reflector> from <source_ip> per WAN and capture the host ip rule policy table for context"
    - "D-08: Capture via a committed read-only proof script scripts/phase238-egress-proof.sh that the operator runs from the dev host (it SSHes read-only to the cake-shaper); stdout captured into the evidence artifact"
  truths:
    - "Operator can run scripts/phase238-egress-proof.sh --json from the dev host (it SSHes read-only to the cake-shaper) and it proves, per WAN, that fping -S <source_ip> would egress the intended WAN under the host's current ip rule policy routing"
    - "Spectrum is proven via the default-route path (ip route get <reflector>, egress src 10.10.110.223, egress dev spec-modem) because it has no ping_source_ip; ATT is proven source-bound via ip route get <reflector> from 10.10.110.227, and the ATT PASS criterion requires the resolved egress to show BOTH src 10.10.110.227 AND dev att-modem (the source-bound observable on the ATT uplink interface), not merely a path distinct from Spectrum"
    - "The verdict (exit 0) requires EVERY reflector for a WAN to resolve to BOTH that WAN's expected src AND its expected dev (Spectrum src 10.10.110.223 + dev spec-modem; ATT src 10.10.110.227 + dev att-modem) AND the two WANs to resolve to DISTINCT egress paths — the concrete src+dev match is the primary proof and the distinctness check is an additional assertion"
    - "The expected per-WAN egress dev is a repo-derived mechanical criterion: spectrum dev spec-modem and att dev att-modem come from the committed cake-autorate configs configs/cake-autorate/config.spectrum.sh ul_if=spec-modem (line 6) and configs/cake-autorate/config.att.sh ul_if=att-modem (line 6) (the uplink/egress interface fping -S uses), cross-confirmed by scripts/phase231-migration-held.sh TARGETS; they are enforced by verdict_for_line, NOT free-text operator attestation"
    - "A --self-test mode pipes canned ip route get fixtures through the SAME parser/verdict logic the live path uses, with no SSH, and exits 0 iff every fixture produces its expected verdict: Spectrum src 10.10.110.223 + dev spec-modem → PASS, ATT src 10.10.110.227 + dev att-modem → PASS, ATT wrong-src 10.10.110.223 → FAIL, ATT correct-src 10.10.110.227 with wrong dev (not att-modem) → FAIL, AND an injection-rejection fixture proving a chained/metacharacter-laden command (e.g. ip route get 1.1.1.1; reboot) is REJECTED by the full-line allowlist — proving the verdict logic rejects wrong-src, wrong-dev, and injected commands before any live run"
    - "The host ip rule policy table is captured once per host as context"
    - "Remote commands are produced by a SINGLE shared generator (generate_remote_commands) consumed by BOTH --print-commands AND the live SSH execution path; neither path hand-builds remote command strings independently"
    - "The script issues ONLY ip route get and ip rule as remote commands, enforced by a FULL-LINE, argument-shaped allowlist anchored at BOTH ends (^...$) that admits only `ip route get <IPv4>`, `ip route get <IPv4> from <IPv4>`, and `ip rule`, and REJECTS any line containing shell metacharacters (; & | backtick $( > < newline) because metacharacters cannot occur inside the [0-9.] / literal token classes; a denylist grep (sudo/systemctl/tc-change/tee/nft/ip-mutators/redirects) remains as defense-in-depth"
    - "The live SSH path performs a MANDATORY pre-execution allowlist check: before any ssh runs, every command from generate_remote_commands is validated against the SAME full-line allowlist; if ANY command fails, the script ABORTS non-zero and issues NO ssh — so 'only ip route get / ip rule ever runs' is provable on the live execution path, not just the printout"
    - "A --print-commands mode prints the exact read-only ip route get / ip rule commands (the output of generate_remote_commands), one bare remote command per line between --- BEGIN REMOTE COMMANDS --- and --- END REMOTE COMMANDS --- markers, for operator fallback AND for the allowlist gate, WITHOUT executing or SSHing"
  artifacts:
    - path: "scripts/phase238-egress-proof.sh"
      provides: "Both-WAN read-only fping-egress proof under live ip rule policy routing, with per-WAN src+dev verdict, a single shared remote-command generator, a full-line argument-shaped allowlist enforced on BOTH the --print-commands enumeration and a mandatory pre-SSH check on the live path, and a self-test of the verdict logic incl. an injection-rejection fixture"
      contains: "set -euo pipefail"
  key_links:
    - from: "scripts/phase238-egress-proof.sh"
      to: "live cake-shaper host (10.10.110.223)"
      via: "ssh -n -o BatchMode=yes read-only kernel query"
      pattern: "ip route get"
    - from: "scripts/phase238-egress-proof.sh --print-commands"
      to: "scripts/phase238-egress-proof.sh live SSH path"
      via: "single shared generate_remote_commands consumed by both, gated by the same full-line allowlist"
      pattern: "generate_remote_commands"
---

<objective>
Author `scripts/phase238-egress-proof.sh` — a committed, read-only, both-WAN proof that `fping -S <source_ip>` would egress the intended WAN under the cake-shaper host's current `ip rule` policy routing (PROV-03). The script is authored autonomously; the live run is an operator step (it SSHes read-only from the dev host to the production cake-shaper). The script also ships a `--self-test` mode that proves its own parser/verdict logic against canned fixtures (no SSH) — including an injection-rejection fixture — so the pass/fail logic is autonomously enforceable before the operator ever runs it live, and a `--print-commands` mode whose output is the canonical enumeration of remote commands (produced by a single shared generator) gated by a full-line argument-shaped allowlist that is ALSO enforced on the live SSH path immediately before execution.

Purpose: Phase 245's live A/B will run `fping -S <source_ip>`; PROV-03 proves up front that source-IP binding routes out the intended WAN under live policy routing, for BOTH WANs (D-07, safe while the A/B target is undecided). The verdict is fully mechanical: each WAN must resolve to BOTH its expected egress `src` AND its expected egress `dev` (the uplink interface). The read-only boundary is provable on the actual execution path: a single shared command generator feeds both `--print-commands` and live SSH, and a mandatory pre-execution allowlist check aborts before any `ssh` if any command falls outside `ip route get <IPv4>[ from <IPv4>]` / `ip rule`. The operator runs it from the dev host (it SSHes read-only to the cake-shaper) and its stdout is captured into the provenance map (Plan 03).

Output: `scripts/phase238-egress-proof.sh` (committed, read-only), plus its live stdout captured by the operator for Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@scripts/phase231-migration-held.sh
@configs/cake-autorate/config.att.sh
@configs/cake-autorate/config.spectrum.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author phase238-egress-proof.sh (both WANs, read-only, per-WAN src+dev verdict, single shared generator + full-line allowlist enforced on print AND live paths, with --self-test incl. injection-rejection)</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <read_first>
    - scripts/phase231-migration-held.sh (FULL — exact structural analog: header/usage, set -euo pipefail, ssh_readonly helper, TARGETS array + IFS='|' read unpack, --wan/--json arg parsing, validate_wan allowlist, require_command preflight, status_json/json_string python3 helpers, mktemp+trap, dual output mode + exit-code verdict). Note line 8 (spectrum TARGETS: `...|spectrum|10.10.110.223|spec-router|spec-modem`) and line 9 (att TARGETS: `...|att|10.10.110.227|att-router|att-modem`) cross-confirm the expected per-WAN egress devs.
    - configs/cake-autorate/config.att.sh (SOURCE OF TRUTH for the ATT expected egress dev: line 6 `ul_if=att-modem` is the uplink/egress interface `fping -S 10.10.110.227` leaves via; line 10 `ping_extra_args="-S 10.10.110.227"` is the ATT source IP)
    - configs/cake-autorate/config.spectrum.sh (SOURCE OF TRUTH for the Spectrum expected egress dev: line 6 `ul_if=spec-modem`; line 10 `ping_extra_args="-S 10.10.110.223"` is the Spectrum source IP)
    - 238-PATTERNS.md section "scripts/phase238-egress-proof.sh" (lines 21-159 — tells you which phase231 line ranges to clone and what to swap)
    - 238-RESEARCH.md section "Egress-Proof Script Skeleton (PROV-03, D-07/D-08)" and "Config Keys — Source IPs & Reflectors" table (the exact source IPs + reflectors)
    - 238-RESEARCH.md "Common Pitfalls" Pitfall 3 (Spectrum has no ping_source_ip) and Pitfall 5 (no mutation during read-only capture)
    - 238-CONTEXT.md D-07 (both WANs + ip rule table) and D-08 (committed script, operator runs from dev host over SSH)
  </read_first>
  <action>
    Clone the STRUCTURE of scripts/phase231-migration-held.sh (not its checks) into scripts/phase238-egress-proof.sh. Header: shebang, a Phase 238 description line, and a read-only posture banner stating "Read-only. Proves fping -S <source_ip> egress per WAN via ip route get / ip rule. No worktree, prod, CAKE-mode, unit, or controller-source mutation." Then `set -euo pipefail`. Define a TARGETS array with two pipe-delimited entries, both SSHing to the same cake-shaper host `kevin@10.10.110.223`, carrying per-WAN fields: WAN name, source IP (use the literal sentinel `<none>` for Spectrum since it has NO ping_source_ip; `10.10.110.227` for ATT), an EXPECTED-SRC field, an EXPECTED-DEV field, an EXPECTED-PATH label, and the reflector list. Spectrum reflectors: `1.1.1.1 9.9.9.9 208.67.222.222`. ATT reflectors: `1.1.1.1 8.8.8.8 151.101.1.57`. Keep the `ssh_readonly() { ssh -n "${SSH_OPTS[@]}" "$1" "$2"; }` helper with `SSH_OPTS=(-o ConnectTimeout=10 -o BatchMode=yes)`. Reuse phase231's `--wan spectrum|att|all` + `--json` + `--help` arg parsing (DROP `--window-hours`), the `validate_wan` fixed-allowlist check, `require_command python3` + `require_command ssh` local preflight (note: `ip` is required on the REMOTE host inside the SSH command, not locally), `status_json`/`json_string` python3-argv JSON helpers, `mktemp -d` + `trap 'rm -rf "$tmpdir"' EXIT`, the main TARGETS-unpack loop with the `--wan` filter skip.

    ENCODE THE EXPECTED PER-WAN EGRESS DEV AS A REPO-DERIVED MECHANICAL CRITERION (P1 — already RESOLVED, preserve). The expected egress `dev` per WAN is the cake-autorate uplink interface `ul_if` — the interface `fping -S <source_ip>` egress leaves via — taken DIRECTLY from the committed configs:
      - spectrum expected-dev = `spec-modem` (from configs/cake-autorate/config.spectrum.sh line 6 `ul_if=spec-modem`; cross-confirmed by scripts/phase231-migration-held.sh line 8)
      - att expected-dev = `att-modem` (from configs/cake-autorate/config.att.sh line 6 `ul_if=att-modem`; cross-confirmed by scripts/phase231-migration-held.sh line 9)
    Put these expected-dev values in the per-WAN TARGETS structure alongside the expected-src and expected-path fields. The script's usage/header must document the expected per-WAN src AND dev and state explicitly that the expected devs come from `configs/cake-autorate/config.{spectrum,att}.sh` `ul_if` (repo-derived, source of truth) — they are NOT operator-attested. State in the header that if the live host's egress dev differs from the repo-configured `ul_if`, the verdict will FAIL (correctly surfacing drift) rather than silently passing.

    FACTOR THE PARSER/VERDICT INTO A SINGLE REUSABLE FUNCTION (P1 — already RESOLVED, preserve). Write one function (e.g. `verdict_for_line`) that takes a WAN name, the expected src, the expected dev, and a single `ip route get` stdout line, parses `dev`, `src`, and `via` from it (the kernel prints `<reflector> [from <src>] [via <gw>] dev <iface> src <src> ...`), and returns PASS/FAIL plus the parsed `dev`/`src`/`via`. The PASS criterion inside `verdict_for_line` is: parsed `src` == expected src AND parsed `dev` == expected dev (BOTH must match). The LIVE path and the `--self-test` path MUST both call this same function so the self-test proves the live verdict.

    DEFINE THE CONCRETE EGRESS VERDICT (preserve). Per-WAN expected criteria (each enforced by `verdict_for_line` as src+dev):
      - Spectrum (source `<none>`): every reflector must resolve via the host DEFAULT ROUTE with `src 10.10.110.223` AND `dev spec-modem`.
      - ATT (source `10.10.110.227`): `ip route get <reflector> from 10.10.110.227` must resolve via the ATT source-bound policy-routing path with `src 10.10.110.227` AND `dev att-modem`.
    Keep the WAN-distinctness check as an ADDITIONAL (not primary) assertion. Final exit-code verdict: exit 0 iff (every reflector for each in-scope WAN matches that WAN's expected `src` AND expected `dev` via `verdict_for_line`) AND (when both WANs are in scope, the two resolved egress paths are distinct). Emit the parsed `dev`/`src`/`via`, the expected strings, and per-reflector pass/fail into the JSON (full JSON array under `--json`, human summary otherwise).

    INTRODUCE A SINGLE SHARED REMOTE-COMMAND GENERATOR (HIGH facet 2 — NEW). Write ONE function `generate_remote_commands` that, given the in-scope WAN filter, emits to stdout the exact bare remote commands the script will run, ONE PER LINE, with NO `ssh` wrapper, NO host prefix, and NO prose: for each in-scope WAN, per reflector, when source IP is `<none>` emit `ip route get <reflector>` (default-route path) else emit `ip route get <reflector> from <source_ip>` (branch on the `<none>` sentinel — NEVER emit `from <none>`); then emit exactly one `ip rule` line (the policy table, captured once per host). This function is the ONLY place remote command strings are constructed. BOTH `--print-commands` AND the live SSH execution path MUST consume `generate_remote_commands` — neither may hand-build a remote command string independently. The live path iterates the lines from `generate_remote_commands` and passes each, unmodified, to `ssh_readonly`.

    DEFINE THE FULL-LINE, ARGUMENT-SHAPED ALLOWLIST (HIGH facet 1 — NEW). Define a single function `is_allowed_remote_command` (or an equivalent shared regex constant) whose pattern is anchored at BOTH ends and admits ONLY the exact command shapes the generator produces:
      `^(ip route get [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+( from [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+)?|ip rule)$`
    This full-line anchor admits only `ip route get <IPv4>`, `ip route get <IPv4> from <IPv4>`, and the bare `ip rule`. State explicitly (in a comment and in the header) WHY this rejects injection: shell metacharacters (`;`, `&`, `|`, backtick, `$(`, `>`, `<`, newline) CANNOT appear inside the `[0-9.]` digit/dot token classes or the literal `ip route get`/`from`/`ip rule` tokens, so any line carrying a metacharacter or trailing shell tail fails the `$`-anchored match. Use this SAME pattern everywhere a remote command is gated.

    ENFORCE THE ALLOWLIST ON THE LIVE PATH BEFORE ANY SSH (HIGH facet 2 — NEW, MANDATORY). Before the live execution loop issues ANY `ssh`, the script MUST validate EVERY line returned by `generate_remote_commands` against `is_allowed_remote_command`. If ANY line fails, the script prints the offending command to stderr and EXITS NON-ZERO, issuing NO `ssh` at all (fail-closed). This pre-execution check is MANDATORY — not an optional self-assert — and it guarantees that the only commands that can ever reach `ssh_readonly` are the validated `ip route get` / `ip rule` shapes. This makes "only ip route get / ip rule ever runs" provable on the ACTUAL execution path, not just the printout.

    ADD A `--print-commands` MODE bound to the shared generator (HIGH facet 2 + MEDIUM markers). When invoked with `--print-commands`, the script prints `--- BEGIN REMOTE COMMANDS ---`, then the EXACT output of `generate_remote_commands` (one bare remote command per line, no prose, no ssh wrapper), then `--- END REMOTE COMMANDS ---`, and exits 0 WITHOUT executing or SSHing. Because `--print-commands` and the live path consume the same generator, the printed enumeration is provably the live command set. The machine-checkable section between the markers is what both the verify-block allowlist gate AND the optional in-script self-assert parse.

    ADD A `--self-test` MODE (P1 fixtures preserved + injection-rejection fixture NEW). When invoked with `--self-test`, the script (a) feeds canned `ip route get` stdout fixtures through the SAME `verdict_for_line` the live path uses, with NO SSH and NO network, asserting each fixture produces its expected verdict; and (b) runs the injection-rejection fixtures through the SAME `is_allowed_remote_command` allowlist the live pre-exec check uses, asserting each malicious line is REJECTED. Provide these fixtures:
      VERDICT fixtures (preserve all four):
      (a) Spectrum default-route line with `src 10.10.110.223` AND `dev spec-modem` (e.g. `1.1.1.1 via 10.10.110.1 dev spec-modem src 10.10.110.223 uid 1000`) → expected PASS for Spectrum;
      (b) ATT source-bound line with `src 10.10.110.227` AND `dev att-modem` (e.g. `1.1.1.1 from 10.10.110.227 via 10.10.110.2 dev att-modem src 10.10.110.227 uid 1000`) → expected PASS for ATT;
      (c) NEGATIVE wrong-src: an ATT reflector returning `src 10.10.110.223` (the Spectrum src) with a plausible dev → expected FAIL for ATT;
      (d) NEGATIVE wrong-dev: an ATT reflector with the CORRECT `src 10.10.110.227` but the WRONG dev (e.g. `dev spec-modem`, not `att-modem`) → expected FAIL for ATT.
      INJECTION-REJECTION fixtures (NEW — at least two, run through `is_allowed_remote_command`):
      (e) `ip route get 1.1.1.1; reboot` → MUST be REJECTED by the allowlist (the prefix-bypass that cycle 3 flagged);
      (f) at least one more chained/injected form, e.g. `ip route get 1.1.1.1 | tee /etc/x` (pipe-redirect) and/or a backtick/`$(...)` form such as `ip route get $(reboot)` → MUST be REJECTED.
      `--self-test` prints each fixture's expected-vs-actual result and exits 0 IFF ALL verdict fixtures (a)-(d) produce their expected PASS/FAIL AND ALL injection fixtures (e)-(f) are REJECTED by the allowlist; otherwise it exits NON-ZERO (mechanically assert: the injection lines must make the allowlist check return non-zero). This mode runs locally with no SSH and no network.

    HARDEN THE READ-ONLY POSTURE — ALLOWLIST PRIMARY, DENYLIST SECONDARY (P2 closure). The PRIMARY guarantee is the FULL-LINE, both-ends-anchored allowlist (`is_allowed_remote_command`) enforced on BOTH the `--print-commands` enumeration AND, mandatorily, on the live execution path before any `ssh`. The allowlist — not the absence of known-bad tokens — is what bounds the blast radius. Keep the existing denylist greps (`sudo`/`systemctl`/`tc ... change|add|del|replace`/`tee`/`nft`/`ip ... add|del|change|replace|flush`/system-path output redirects) as a SECONDARY defense-in-depth layer over the script source. Do NOT call any mutating command inside the SSH command. Quote all expansions; never interpolate untrusted input into the SSH command string. Per D-08 + the credential-reads convention: the live run is an operator step from the dev host over SSH; if a live read trips a guardrail, `--print-commands` makes it trivial to hand the operator the exact `! ssh cake-shaper '…'` lines instead of escalating credentials. Do NOT edit src/wanctl/. `chmod +x` the script.
  </action>
  <verify>
    <automated>test -x scripts/phase238-egress-proof.sh && bash -n scripts/phase238-egress-proof.sh && grep -q 'set -euo pipefail' scripts/phase238-egress-proof.sh && grep -q 'generate_remote_commands' scripts/phase238-egress-proof.sh && grep -q 'is_allowed_remote_command' scripts/phase238-egress-proof.sh && grep -q 'verdict_for_line' scripts/phase238-egress-proof.sh && grep -Fq '<none>' scripts/phase238-egress-proof.sh && grep -q 'ip route get' scripts/phase238-egress-proof.sh && grep -q 'ip rule' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.227' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.223' scripts/phase238-egress-proof.sh && grep -Fq 'att-modem' scripts/phase238-egress-proof.sh && grep -Fq 'spec-modem' scripts/phase238-egress-proof.sh && ! grep -qE 'ip (route|rule|addr|link) (add|del|change|replace|flush)' scripts/phase238-egress-proof.sh && ! grep -qE '\bsudo\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bsystemctl\b' scripts/phase238-egress-proof.sh && ! grep -qE '\btc\b[^a-z].*(change|add|del|replace)' scripts/phase238-egress-proof.sh && ! grep -qE '\btee\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bnft\b' scripts/phase238-egress-proof.sh && grep -Fq 'ul_if=spec-modem' configs/cake-autorate/config.spectrum.sh && grep -Fq 'ul_if=att-modem' configs/cake-autorate/config.att.sh && scripts/phase238-egress-proof.sh --help && scripts/phase238-egress-proof.sh --print-commands && out="$(scripts/phase238-egress-proof.sh --print-commands)" && printf '%s\n' "$out" | grep -Fq -- '--- BEGIN REMOTE COMMANDS ---' && printf '%s\n' "$out" | grep -Fq -- '--- END REMOTE COMMANDS ---' && cmds="$(printf '%s\n' "$out" | sed -n '/--- BEGIN REMOTE COMMANDS ---/,/--- END REMOTE COMMANDS ---/p' | grep -v -- '--- BEGIN REMOTE COMMANDS ---' | grep -v -- '--- END REMOTE COMMANDS ---' | grep -v '^[[:space:]]*$')" && test -n "$cmds" && test "$(printf '%s\n' "$cmds" | grep -c '^ip route get [0-9][0-9.]*\( from [0-9][0-9.]*\)\?$')" -eq 6 && test "$(printf '%s\n' "$cmds" | grep -cx 'ip rule')" -eq 1 && ! printf '%s\n' "$cmds" | grep -vqE '^(ip route get [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+( from [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+)?|ip rule)$' && scripts/phase238-egress-proof.sh --self-test && test -z "$(git status --porcelain -- src/wanctl/)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase238-egress-proof.sh` exits 0 (valid syntax).
    - `scripts/phase238-egress-proof.sh --help` prints usage and exits 0, and the usage documents the expected per-WAN src AND dev (spectrum: src 10.10.110.223 / dev spec-modem; att: src 10.10.110.227 / dev att-modem) and states the devs are repo-derived from configs/cake-autorate/config.{spectrum,att}.sh `ul_if` (line 6 of each).
    - SINGLE SHARED GENERATOR: a `generate_remote_commands` function is the ONLY constructor of remote command strings, and BOTH `--print-commands` and the live SSH path consume it (grep proves the identifier exists; the live path iterates its output rather than hand-building command strings).
    - FULL-LINE ALLOWLIST: an `is_allowed_remote_command` gate (or shared regex constant) is anchored at BOTH ends and admits ONLY `^(ip route get [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+( from [0-9]+\.[0-9]+\.[0-9]+\.[0-9]+)?|ip rule)$`; a comment/header states this rejects shell metacharacters because they cannot appear inside the digit/dot/literal token classes.
    - MANDATORY PRE-SSH CHECK: before any `ssh`, the live path validates EVERY `generate_remote_commands` line against `is_allowed_remote_command`; if any fails, the script aborts NON-ZERO and issues NO ssh. This is unconditional (not an optional self-assert).
    - `scripts/phase238-egress-proof.sh --print-commands` exits 0, does not SSH or execute, and prints the remote commands one-per-line between `--- BEGIN REMOTE COMMANDS ---` and `--- END REMOTE COMMANDS ---` (both markers present).
    - ALLOWLIST GATE (on print enumeration): every remote-command line inside the `--print-commands` section matches the full-line anchored allowlist; NO line falls outside it. The section contains EXACTLY 6 `ip route get <IPv4>[ from <IPv4>]` lines (Spectrum 3 reflectors + ATT 3 reflectors) and EXACTLY 1 `ip rule` line (7 total, default `--wan all` scope).
    - `scripts/phase238-egress-proof.sh --self-test` exits 0 and runs WITHOUT any SSH/network: it feeds the four verdict fixtures through the same `verdict_for_line` the live path uses — (a) Spectrum src 10.10.110.223 + dev spec-modem → PASS, (b) ATT src 10.10.110.227 + dev att-modem → PASS, (c) ATT wrong-src 10.10.110.223 → FAIL, (d) ATT correct-src 10.10.110.227 + wrong-dev → FAIL — AND runs at least two injection fixtures through the same `is_allowed_remote_command` allowlist: (e) `ip route get 1.1.1.1; reboot` → REJECTED, (f) a pipe/redirect or backtick/`$(...)` form (e.g. `ip route get 1.1.1.1 | tee /etc/x`) → REJECTED. If any verdict fixture mismatches OR any injection fixture is NOT rejected, --self-test exits non-zero.
    - The live path and the `--self-test` path call the SAME `verdict_for_line`; the pre-SSH live check and the `--self-test` injection fixtures use the SAME `is_allowed_remote_command`.
    - Script contains `set -euo pipefail` and the read-only posture banner.
    - Script contains the `<none>` Spectrum sentinel and branches the `ip route get` invocation on it (no `from <none>`).
    - Script contains ATT source IP `10.10.110.227`, Spectrum default-route source `10.10.110.223`, ATT expected dev `att-modem`, Spectrum expected dev `spec-modem`, both WANs' reflector lists, an `ip route get` query, and an `ip rule` capture.
    - The ATT PASS criterion requires BOTH `src 10.10.110.227` AND `dev att-modem`; Spectrum PASS requires BOTH `src 10.10.110.223` AND `dev spec-modem`. Distinctness (Spectrum path != ATT path) is an ADDITIONAL assertion, not the primary proof.
    - The exit-code verdict requires every reflector to match its WAN's expected `src` AND expected `dev` AND (when both WANs in scope) the two resolved egress paths to be DISTINCT.
    - Script validates `--wan` against the fixed `spectrum|att|all` allowlist.
    - PRIMARY remote-command guard is the full-line both-ends-anchored allowlist enforced on the print enumeration AND on the live path pre-SSH; the denylist greps (sudo/systemctl/tc-change/tee/nft/ip-mutators/system-path redirects) remain as secondary defense-in-depth and all still pass (none present).
    - CONFIG RECHECK: `configs/cake-autorate/config.spectrum.sh` still contains `ul_if=spec-modem` and `configs/cake-autorate/config.att.sh` still contains `ul_if=att-modem` (verify block catches config drift in the source of truth for expected devs).
    - The `<automated>` verify block FAIL-FASTS: it is a single `&&`-chain where every check (markers present, command count 6+1, full-line allowlist, `--self-test` incl. injection fixtures, config ul_if recheck, clean-tree assertion) propagates its non-zero exit, with NO trailing `|| true` and NO stray `;` that severs the chain. It returns non-zero when ANY of these hold: a `--print-commands` line outside the full-line allowlist, a missing BEGIN/END marker, a wrong command count, a failing `--self-test` (verdict OR injection), a config `ul_if` mismatch, a denylisted mutator present, a `bash -n` failure, or a dirty src/wanctl/ tree; it exits 0 only when ALL checks pass.
    - The clean-tree assertion is the POSITIVE `&&`-linked check `test -z "$(git status --porcelain -- src/wanctl/)"` (true only when the controller path has no changes), NOT an inverted `&& false || true` guard.
    - `git status --porcelain -- src/wanctl/` is empty (no controller-path drift).
    - Script is executable (`test -x`).
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>scripts/phase238-egress-proof.sh exists, parses clean, --help/--print-commands/--self-test work; remote commands come from a SINGLE shared generate_remote_commands consumed by both --print-commands and the live SSH path; a full-line both-ends-anchored is_allowed_remote_command allowlist admits only `ip route get <IPv4>[ from <IPv4>]` / `ip rule` and is enforced both on the print enumeration AND as a MANDATORY pre-SSH check that aborts before any ssh on any non-allowlisted command; --self-test proves Spectrum-PASS (src+dev), ATT-PASS (src+dev), ATT-wrong-src-FAIL, ATT-correct-src-wrong-dev-FAIL through the same verdict_for_line, AND rejects injection fixtures (`ip route get 1.1.1.1; reboot` plus a pipe/backtick form) through the same allowlist, all with no SSH; the verify block is a single fail-fast `&&`-chain that asserts BEGIN/END markers, exactly 6 ip-route-get + 1 ip-rule lines, the full-line allowlist, the config ul_if source-of-truth, and a positive clean-tree assertion (no `|| true` mask, no severing `;`); covers both WANs with the Spectrum `<none>` default-route branch and ATT source binding; and the script is executable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the egress proof from the dev host (SSHes read-only to the cake-shaper)</name>
  <what-built>scripts/phase238-egress-proof.sh — a read-only both-WAN fping-egress proof that the operator runs FROM the dev host, which SSHes read-only to the cake-shaper (`kevin@10.10.110.223`) to run `ip route get <reflector> [from <source_ip>]` per WAN plus `ip rule`, with a fully mechanical per-WAN src+dev verdict (Spectrum: src 10.10.110.223 + dev spec-modem; ATT: src 10.10.110.227 + dev att-modem), remote commands produced by a single shared generator and gated by a full-line allowlist that is also enforced as a mandatory pre-SSH check, a `--self-test` that pre-proves the verdict logic AND command-injection rejection offline, and a `--print-commands` operator fallback.</what-built>
  <read_first>
    - 238-CONTEXT.md D-08 (operator runs the committed script from the dev host over SSH to the cake-shaper; stdout → evidence)
    - 238-RESEARCH.md "Code Examples" (the exact read-only commands) and Environment Availability table
    - project credential-reads convention (live-host reads handed to operator as `! ssh …`, not escalated)
  </read_first>
  <action>
    The egress proof runs FROM the dev host and SSHes read-only to the production cake-shaper (`kevin@10.10.110.223`) — the TARGETS array already encodes the `ssh kevin@10.10.110.223` connection, and the live path runs ONLY the generator's allowlist-validated `ip route get` / `ip rule` commands (the mandatory pre-SSH check aborts before any ssh if a command falls outside the allowlist). Running the dev-host SSH requires operator-at-keyboard credentials, so this is an operator step, NOT an autonomous executor action (D-08, credential-reads convention). Present the operator with the exact command to run from the dev host: `scripts/phase238-egress-proof.sh --json` (the script SSHes read-only to the cake-shaper itself). If the dev-host SSH trips a guardrail, run `scripts/phase238-egress-proof.sh --print-commands` to get the exact per-WAN `! ssh cake-shaper 'ip route get <reflector> [from <source_ip>]'` / `! ssh cake-shaper 'ip rule'` lines (these are the same generator-produced, allowlist-validated commands the live path would run) for the operator to paste directly. Do NOT run the script locally on the cake-shaper — the canonical mode is dev-host-over-SSH as encoded in the TARGETS. The operator's role is to RUN the script read-only (from the dev host) and paste the full stdout back — the script's own exit-0/verdict mechanically enforces the egress dev and src (the operator does NOT subjectively judge the dev; the verdict already requires `dev att-modem` for ATT and `dev spec-modem` for Spectrum, repo-derived from the cake-autorate `ul_if` configs). Capture that stdout verbatim for Plan 03 (it is the PROV-03 evidence embedded in the provenance map). If the script's verdict is non-zero (e.g. live egress dev or src differs from the repo-configured expectation), that is real drift to surface in Plan 03, not a thing to override. Do NOT attempt to escalate stored credentials to run this autonomously.
  </action>
  <verify>
    <human-check>Operator ran scripts/phase238-egress-proof.sh --json from the dev host (it SSHed read-only to the cake-shaper) — or used the --print-commands `! ssh` equivalents — and pasted back stdout that shows, per WAN, the resolved egress dev/src/via for each reflector plus the ip rule table; the script's verdict (exit code / per-reflector pass) — NOT operator judgment — enforced that Spectrum resolved with src 10.10.110.223 + dev spec-modem and ATT (source 10.10.110.227) resolved with src 10.10.110.227 + dev att-modem; the two paths are distinct; the operator pasted the full stdout back for capture in Plan 03.</human-check>
  </verify>
  <acceptance_criteria>
    - The script was run read-only from the dev host (SSHing to the cake-shaper), or its `--print-commands` `! ssh` equivalents were executed by the operator; the script was NOT run locally on the cake-shaper.
    - Stdout for both WANs is captured verbatim and available for Plan 03.
    - The script's verdict (not operator judgment) confirmed Spectrum's resolved egress shows `src 10.10.110.223` AND `dev spec-modem`.
    - The script's verdict (not operator judgment) confirmed ATT's resolved egress shows `src 10.10.110.227` AND `dev att-modem` (the source-bound ATT uplink path; expected dev repo-derived from config.att.sh `ul_if`).
    - The two WANs' resolved egress paths are distinct.
    - No production mutation occurred (only allowlist-validated `ip route get` / `ip rule` ran; the mandatory pre-SSH allowlist check guarantees no other command reached ssh).
  </acceptance_criteria>
  <resume-signal>Paste the full egress-proof stdout (both WANs + ip rule) and type "captured" to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Remote reads must be read-only; no mutation of routing, units, or CAKE mode |
| generator → live SSH command | Every command reaching `ssh` must be the validated `ip route get`/`ip rule` shape; the live path may not run anything the allowlist rejects |
| script arg (`--wan`) → control flow / SSH command | Untrusted CLI input must not inject into the SSH command string |
| evidence artifact → public docs | Internal IPs captured in stdout must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-02 | Tampering | phase238-egress-proof.sh remote commands | mitigate | `set -euo pipefail`; remote commands come from a SINGLE shared `generate_remote_commands` consumed by BOTH `--print-commands` and the live SSH path; PRIMARY guard is a FULL-LINE, both-ends-anchored allowlist `^(ip route get [0-9.]+( from [0-9.]+)?|ip rule)$` (`is_allowed_remote_command`) — shell metacharacters (`;`/`&`/`\|`/backtick/`$(`/`>`/`<`/newline) cannot appear inside the digit/dot/literal token classes, so injected tails like `ip route get 1.1.1.1; reboot` FAIL the `$`-anchored match; the live path performs a MANDATORY pre-execution allowlist check over every generated command and ABORTS non-zero issuing NO ssh if any fails (fail-closed), so the read-only guarantee holds on the ACTUAL execution path, not just the printout; a `--self-test` injection-rejection fixture mechanically proves `ip route get 1.1.1.1; reboot` (and a pipe/backtick form) is rejected; a static denylist (`sudo`/`systemctl`/`tc ... change/add/del/replace`/`tee`/`nft`/`ip ... add/del/change/replace/flush`/system-path redirects) remains as secondary defense-in-depth; read-only posture banner; `ssh -n -o BatchMode=yes`; `--print-commands` lets the operator inspect the exact (allowlist-validated) commands before running |
| T-238-02a | Tampering | `--wan` arg → SSH command | mitigate | `validate_wan` fixed `spectrum\|att\|all` allowlist; quote all expansions; pass values via shell vars, never interpolate untrusted input into the remote command string; the full-line remote-command allowlist plus mandatory pre-SSH check bound the blast radius even if an arg slipped through, since the resulting command would fail the `$`-anchored match and abort before any ssh |
| T-238-02b | Information Disclosure | captured stdout (internal IPs) | mitigate | Internal `10.10.110.x` IPs stay in phase-dir evidence + the provenance map; never promoted to public-safe CLAUDE.md/README |
| T-238-02c | Spoofing | resolved egress verdict (wrong-src / wrong-dev false pass) | mitigate | ATT PASS requires BOTH the concrete `src 10.10.110.227` AND `dev att-modem` (repo-derived from config.att.sh `ul_if`), not just "distinct from Spectrum"; `--self-test` negative fixtures — ATT returning `src 10.10.110.223` (wrong src) AND ATT returning correct src 10.10.110.227 with a non-`att-modem` dev (wrong dev) — both assert the verdict FAILS, autonomously proving the verdict rejects a spoofed/misrouted src OR egress on the wrong interface before the live run |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `bash -n` parses the script clean; `--help`, `--print-commands`, and `--self-test` work.
- Remote commands are produced by a single shared `generate_remote_commands` consumed by both `--print-commands` and the live SSH path; a full-line both-ends-anchored allowlist (`is_allowed_remote_command`) admits only `ip route get <IPv4>[ from <IPv4>]` / `ip rule`, and the live path runs a MANDATORY pre-SSH allowlist check that aborts (no ssh) on any non-allowlisted command.
- `--self-test` exits 0 proving Spectrum src-10.10.110.223+dev-spec-modem PASS, ATT src-10.10.110.227+dev-att-modem PASS, ATT-wrong-src-10.10.110.223 FAIL, ATT-correct-src-wrong-dev FAIL through the same `verdict_for_line`, AND rejecting injection fixtures (`ip route get 1.1.1.1; reboot` plus a pipe/backtick form) through the same `is_allowed_remote_command` — all with no SSH.
- The `--print-commands` section is delimited by both `--- BEGIN REMOTE COMMANDS ---` and `--- END REMOTE COMMANDS ---`, contains EXACTLY 6 `ip route get` lines + EXACTLY 1 `ip rule` line (default `all` scope), and every line passes the full-line anchored allowlist (no out-of-list command can pass).
- The verify block rechecks the source of truth: `configs/cake-autorate/config.spectrum.sh` contains `ul_if=spec-modem` and `configs/cake-autorate/config.att.sh` contains `ul_if=att-modem` (config drift in the expected-dev source is caught).
- The `<automated>` verify block is a single fail-fast `&&`-chain (no `|| true` mask, no severing `;`): a planted denylisted mutator, a non-allowlisted/injected `--print-commands` line, a missing marker, a wrong command count, a failing `--self-test`, a config `ul_if` mismatch, or a dirty src/wanctl/ tree each force a non-zero exit; the clean-tree check is the positive assertion `test -z "$(git status --porcelain -- src/wanctl/)"`.
- The script contains the `<none>` Spectrum sentinel, ATT source `10.10.110.227`, Spectrum default-route src `10.10.110.223`, ATT expected dev `att-modem`, Spectrum expected dev `spec-modem`, `ip route get`, `ip rule`, a concrete per-WAN expected-src+expected-dev verdict with a distinctness guard, and NONE of the denylisted mutators (sudo/systemctl/tc-change/tee/nft/ip-mutators/system-path redirects) as defense-in-depth.
- Operator-run stdout (both WANs + ip rule), produced by running the script from the dev host over SSH to the cake-shaper, is captured for Plan 03; ATT dev enforced as `att-modem` by the script verdict (not operator judgment).
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- A committed read-only both-WAN egress-proof script exists, with the Spectrum `<none>` default-route branch and the ATT source-bound branch, a fully mechanical per-WAN src+dev verdict (Spectrum: src 10.10.110.223 + dev spec-modem; ATT: src 10.10.110.227 + dev att-modem; expected devs repo-derived from config.{spectrum,att}.sh `ul_if`) plus a distinctness guard, a single shared `generate_remote_commands` feeding BOTH `--print-commands` and the live SSH path, a full-line both-ends-anchored allowlist enforced on the print enumeration AND as a mandatory pre-SSH check (fail-closed before any ssh), and a `--self-test` that autonomously proves the verdict logic (incl. negative wrong-src AND wrong-dev fixtures) AND command-injection rejection (incl. `ip route get 1.1.1.1; reboot`).
- The operator runs it read-only from the dev host (it SSHes to the cake-shaper); the script verdict (not operator judgment) confirms ATT's `src 10.10.110.227` on `dev att-modem` and Spectrum's `src 10.10.110.223` on `dev spec-modem`, and the stdout is captured for the provenance map; only allowlist-validated `ip route get` / `ip rule` could ever have run.
- No src/wanctl/ file is modified and no production state is mutated.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md` when done
</output>
