---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 02
type: execute
wave: 2
depends_on: ["238-01"]
files_modified:
  - scripts/phase238-egress-proof.sh
autonomous: false
requirements: [PROV-03]
must_haves:
  decisions:
    - "D-07: Cover both WANs (Spectrum + ATT) — prove ip route get <reflector> from <source_ip> per WAN and capture the host ip rule policy table for context"
    - "D-08: Capture via a committed read-only proof script scripts/phase238-egress-proof.sh that the operator runs from the dev host (it SSHes read-only to the cake-shaper); stdout captured into the evidence artifact"
  truths:
    - "Operator can run scripts/phase238-egress-proof.sh --json from the dev host (it SSHes read-only to the cake-shaper) and it proves, per WAN, that fping -S <source_ip> would egress the intended WAN under the host's current ip rule policy routing"
    - "Spectrum is proven via the default-route path (ip route get <reflector>, egress src 10.10.110.223, egress dev spec-modem) because it has no ping_source_ip; ATT is proven source-bound via ip route get <reflector> from 10.10.110.227, and the ATT PASS criterion requires the resolved egress to show BOTH src 10.10.110.227 AND dev att-modem (the source-bound observable on the ATT uplink interface), not merely a path distinct from Spectrum"
    - "The verdict (exit 0) requires EVERY reflector for a WAN to resolve to BOTH that WAN's expected src AND its expected dev (Spectrum src 10.10.110.223 + dev spec-modem; ATT src 10.10.110.227 + dev att-modem) AND the two WANs to resolve to DISTINCT egress paths — the concrete src+dev match is the primary proof and the distinctness check is an additional assertion"
    - "The expected per-WAN egress dev is a repo-derived mechanical criterion: spectrum dev spec-modem and att dev att-modem come from the committed cake-autorate configs configs/cake-autorate/config.spectrum.sh ul_if=spec-modem and configs/cake-autorate/config.att.sh ul_if=att-modem (the uplink/egress interface fping -S uses), cross-confirmed by scripts/phase231-migration-held.sh TARGETS; they are enforced by verdict_for_line, NOT free-text operator attestation"
    - "A --self-test mode pipes canned ip route get fixtures through the SAME parser/verdict logic the live path uses, with no SSH, and exits 0 iff every fixture produces its expected verdict: Spectrum src 10.10.110.223 + dev spec-modem → PASS, ATT src 10.10.110.227 + dev att-modem → PASS, ATT wrong-src 10.10.110.223 → FAIL, and ATT correct-src 10.10.110.227 with wrong dev (not att-modem) → FAIL — proving the verdict logic rejects both wrong-src and wrong-dev before any live run"
    - "The host ip rule policy table is captured once per host as context"
    - "The script issues ONLY ip route get and ip rule as remote commands, enforced by an anchored allowlist over the --print-commands enumeration (every remote-command line matches exactly ^ip route get  or ^ip rule$); a denylist grep (sudo/systemctl/tc-change/tee/nft/ip-mutators/redirects) remains as defense-in-depth"
    - "A --print-commands mode prints the exact read-only ip route get / ip rule commands, one bare remote command per line in a machine-checkable section, for operator fallback AND for the allowlist gate, WITHOUT executing or SSHing"
  artifacts:
    - path: "scripts/phase238-egress-proof.sh"
      provides: "Both-WAN read-only fping-egress proof under live ip rule policy routing, with per-WAN src+dev verdict, an allowlist-gated remote-command enumeration, and a self-test of the verdict logic"
      contains: "set -euo pipefail"
  key_links:
    - from: "scripts/phase238-egress-proof.sh"
      to: "live cake-shaper host (10.10.110.223)"
      via: "ssh -n -o BatchMode=yes read-only kernel query"
      pattern: "ip route get"
---

<objective>
Author `scripts/phase238-egress-proof.sh` — a committed, read-only, both-WAN proof that `fping -S <source_ip>` would egress the intended WAN under the cake-shaper host's current `ip rule` policy routing (PROV-03). The script is authored autonomously; the live run is an operator step (it SSHes read-only from the dev host to the production cake-shaper). The script also ships a `--self-test` mode that proves its own parser/verdict logic against canned fixtures (no SSH), so the pass/fail logic is autonomously enforceable before the operator ever runs it live, and a `--print-commands` mode whose output is the canonical enumeration of remote commands gated by an anchored allowlist.

Purpose: Phase 245's live A/B will run `fping -S <source_ip>`; PROV-03 proves up front that source-IP binding routes out the intended WAN under live policy routing, for BOTH WANs (D-07, safe while the A/B target is undecided). The verdict is fully mechanical: each WAN must resolve to BOTH its expected egress `src` AND its expected egress `dev` (the uplink interface). The operator runs it from the dev host (it SSHes read-only to the cake-shaper) and its stdout is captured into the provenance map (Plan 03).

Output: `scripts/phase238-egress-proof.sh` (committed, read-only), plus its live stdout captured by the operator for Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@scripts/phase231-migration-held.sh
@configs/cake-autorate/config.att.sh
@configs/cake-autorate/config.spectrum.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author phase238-egress-proof.sh (both WANs, read-only, per-WAN src+dev verdict, allowlist-gated, with --self-test)</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <read_first>
    - scripts/phase231-migration-held.sh (FULL — exact structural analog: header/usage, set -euo pipefail, ssh_readonly helper, TARGETS array + IFS='|' read unpack, --wan/--json arg parsing, validate_wan allowlist, require_command preflight, status_json/json_string python3 helpers, mktemp+trap, dual output mode + exit-code verdict). Note line 8 (spectrum TARGETS: `...|spectrum|10.10.110.223|spec-router|spec-modem`) and line 9 (att TARGETS: `...|att|10.10.110.227|att-router|att-modem`) cross-confirm the expected per-WAN egress devs.
    - configs/cake-autorate/config.att.sh (SOURCE OF TRUTH for the ATT expected egress dev: `ul_if=att-modem` is the uplink/egress interface `fping -S 10.10.110.227` leaves via; `ping_extra_args="-S 10.10.110.227"` is the ATT source IP)
    - configs/cake-autorate/config.spectrum.sh (SOURCE OF TRUTH for the Spectrum expected egress dev: `ul_if=spec-modem`; `ping_extra_args="-S 10.10.110.223"` is the Spectrum source IP)
    - 238-PATTERNS.md section "scripts/phase238-egress-proof.sh" (lines 21-159 — tells you which phase231 line ranges to clone and what to swap)
    - 238-RESEARCH.md section "Egress-Proof Script Skeleton (PROV-03, D-07/D-08)" and "Config Keys — Source IPs & Reflectors" table (the exact source IPs + reflectors)
    - 238-RESEARCH.md "Common Pitfalls" Pitfall 3 (Spectrum has no ping_source_ip) and Pitfall 5 (no mutation during read-only capture)
    - 238-CONTEXT.md D-07 (both WANs + ip rule table) and D-08 (committed script, operator runs from dev host over SSH)
  </read_first>
  <action>
    Clone the STRUCTURE of scripts/phase231-migration-held.sh (not its checks) into scripts/phase238-egress-proof.sh. Header: shebang, a Phase 238 description line, and a read-only posture banner stating "Read-only. Proves fping -S <source_ip> egress per WAN via ip route get / ip rule. No worktree, prod, CAKE-mode, unit, or controller-source mutation." Then `set -euo pipefail`. Define a TARGETS array with two pipe-delimited entries, both SSHing to the same cake-shaper host `kevin@10.10.110.223`, carrying per-WAN fields: WAN name, source IP (use the literal sentinel `<none>` for Spectrum since it has NO ping_source_ip; `10.10.110.227` for ATT), an EXPECTED-SRC field, an EXPECTED-DEV field, an EXPECTED-PATH label, and the reflector list. Spectrum reflectors: `1.1.1.1 9.9.9.9 208.67.222.222`. ATT reflectors: `1.1.1.1 8.8.8.8 151.101.1.57`. Keep the `ssh_readonly() { ssh -n "${SSH_OPTS[@]}" "$1" "$2"; }` helper with `SSH_OPTS=(-o ConnectTimeout=10 -o BatchMode=yes)`. Per WAN, per reflector run the kernel query: when source IP is `<none>` run `ip route get <reflector>` (default-route path); otherwise run `ip route get <reflector> from <source_ip>`. Branch on the `<none>` sentinel — do NOT pass `from <none>`. Once per host capture `ip rule` (the policy table) as context, not pass/fail-scored (D-07). Reuse phase231's `--wan spectrum|att|all` + `--json` + `--help` arg parsing (DROP `--window-hours`), the `validate_wan` fixed-allowlist check, `require_command python3` + `require_command ssh` local preflight (note: `ip` is required on the REMOTE host inside the SSH command, not locally), `status_json`/`json_string` python3-argv JSON helpers, `mktemp -d` + `trap 'rm -rf "$tmpdir"' EXIT`, the main TARGETS-unpack loop with the `--wan` filter skip.

    ENCODE THE EXPECTED PER-WAN EGRESS DEV AS A REPO-DERIVED MECHANICAL CRITERION (HIGH #1 fix). The expected egress `dev` per WAN is the cake-autorate uplink interface `ul_if` — the interface `fping -S <source_ip>` egress leaves via — taken DIRECTLY from the committed configs:
      - spectrum expected-dev = `spec-modem` (from configs/cake-autorate/config.spectrum.sh `ul_if=spec-modem`; cross-confirmed by scripts/phase231-migration-held.sh line 8)
      - att expected-dev = `att-modem` (from configs/cake-autorate/config.att.sh `ul_if=att-modem`; cross-confirmed by scripts/phase231-migration-held.sh line 9)
    Put these expected-dev values in the per-WAN TARGETS structure alongside the expected-src and expected-path fields. The script's usage/header must document the expected per-WAN src AND dev and state explicitly that the expected devs come from `configs/cake-autorate/config.{spectrum,att}.sh` `ul_if` (repo-derived, source of truth) — they are NOT operator-attested. State in the header that if the live host's egress dev differs from the repo-configured `ul_if`, the verdict will FAIL (correctly surfacing drift) rather than silently passing.

    FACTOR THE PARSER/VERDICT INTO A SINGLE REUSABLE FUNCTION (HIGH #1 + autonomously-testable). Write one function (e.g. `verdict_for_line`) that takes a WAN name, the expected src, the expected dev, and a single `ip route get` stdout line, parses `dev`, `src`, and `via` from it (the kernel prints `<reflector> [from <src>] [via <gw>] dev <iface> src <src> ...`), and returns PASS/FAIL plus the parsed `dev`/`src`/`via`. The PASS criterion inside `verdict_for_line` is: parsed `src` == expected src AND parsed `dev` == expected dev (BOTH must match). The LIVE path and the `--self-test` path MUST both call this same function so the self-test proves the live verdict.

    DEFINE THE CONCRETE EGRESS VERDICT. Per-WAN expected criteria (each enforced by `verdict_for_line` as src+dev):
      - Spectrum (source `<none>`): every reflector must resolve via the host DEFAULT ROUTE with `src 10.10.110.223` AND `dev spec-modem`.
      - ATT (source `10.10.110.227`): `ip route get <reflector> from 10.10.110.227` must resolve via the ATT source-bound policy-routing path with `src 10.10.110.227` AND `dev att-modem`. The "egresses the intended WAN" guarantee is now mechanical on BOTH the source-bound IP and the ATT uplink interface — not "distinct from Spectrum" and not operator judgment.
    Keep the WAN-distinctness check as an ADDITIONAL (not primary) assertion: capture both WANs' resolved `dev`/`src`/`via` and assert the two egress paths differ — this is a secondary guard, the primary proof is each WAN's concrete `src`+`dev` match. Final exit-code verdict: exit 0 iff (every reflector for each in-scope WAN matches that WAN's expected `src` AND expected `dev` via `verdict_for_line`) AND (when both WANs are in scope, the two resolved egress paths are distinct). Emit the parsed `dev`/`src`/`via`, the expected-src/expected-dev/expected-path strings, and the per-reflector pass/fail into the JSON (full JSON array under `--json`, human summary otherwise).

    ADD A `--self-test` MODE (HIGH #1 — makes the src+dev pass/fail logic autonomously enforceable). When invoked with `--self-test`, the script feeds canned `ip route get` stdout fixtures through the SAME `verdict_for_line` function the live path uses, WITHOUT any SSH and WITHOUT touching the network, and asserts each fixture produces its expected verdict. Provide at least these FOUR fixtures:
      (a) Spectrum default-route line with `src 10.10.110.223` AND `dev spec-modem` (e.g. `1.1.1.1 via 10.10.110.1 dev spec-modem src 10.10.110.223 uid 1000`) → expected PASS for Spectrum;
      (b) ATT source-bound line with `src 10.10.110.227` AND `dev att-modem` (e.g. `1.1.1.1 from 10.10.110.227 via 10.10.110.2 dev att-modem src 10.10.110.227 uid 1000`) → expected PASS for ATT;
      (c) NEGATIVE wrong-src: an ATT reflector returning `src 10.10.110.223` (the Spectrum src) with a plausible dev → expected FAIL for ATT (proves wrong src is rejected);
      (d) NEGATIVE wrong-dev: an ATT reflector with the CORRECT `src 10.10.110.227` but the WRONG dev (e.g. `dev spec-modem`, a dev that is NOT `att-modem`) → expected FAIL for ATT (proves correct-src/wrong-dev is rejected — this is the HIGH #1 closure: the verdict rejects egress on the wrong interface even when the source IP is right).
    The two PASS fixtures (a) and (b) must carry the correct dev so they still PASS under the stricter src+dev criterion; fixture (c) keeps a plausible dev so it fails specifically on src. `--self-test` prints each fixture's expected-vs-actual verdict and exits 0 iff ALL four fixtures (including both negatives) produce their expected verdicts; otherwise it exits non-zero. This mode runs locally with no SSH and no network access.

    ADD A `--print-commands` MODE (HIGH #2 — this output is the canonical enumeration of remote commands gated by the allowlist). When invoked with `--print-commands`, the script prints the exact read-only remote commands it would run, ONE BARE REMOTE COMMAND PER LINE in a clearly delimited machine-checkable section (e.g. between markers like `--- BEGIN REMOTE COMMANDS ---` / `--- END REMOTE COMMANDS ---`, with NO prose, host prefixes, or `ssh` wrapper on the command lines themselves — just the bare remote command such as `ip route get 1.1.1.1 from 10.10.110.227` or `ip rule`). The lines emitted are the per-WAN `ip route get <reflector> [from <source_ip>]` lines and the `ip rule` line. It must NOT execute them and must NOT SSH, and must exit 0. The machine-checkable section is what the allowlist gate (verify block + a self-checking assertion) parses: every line in that section MUST match exactly `^ip route get ` or `^ip rule$`. Optionally have the script self-assert this allowlist over its own `--print-commands` section and exit non-zero if any remote-command line falls outside the two anchored patterns (defense-in-depth on top of the verify-block check).

    HARDEN THE READ-ONLY POSTURE TO A TRUE ALLOWLIST (HIGH #2 fix). The PRIMARY guarantee is an anchored allowlist over the GENERATED remote commands, NOT merely the absence of known-bad tokens: every remote command the script would issue (the canonical `--print-commands` enumeration) MUST match exactly one of the two anchored patterns `^ip route get ` or `^ip rule$`. Any remote-command line not matching one of these two anchored patterns fails the gate. Keep the existing denylist greps (`sudo`/`systemctl`/`tc ... change|add|del|replace`/`tee`/`nft`/`ip ... add|del|change|replace|flush`/system-path output redirects) as a SECONDARY defense-in-depth layer. Do NOT call any mutating command inside the SSH command. Quote all expansions; never interpolate untrusted input into the SSH command string. Per D-08 + the credential-reads convention: the live run is an operator step run from the dev host over SSH to the cake-shaper; if a live read trips a guardrail, the script (or its docstring) should make it trivial to hand the operator the exact `! ssh cake-shaper '…'` command (the `--print-commands` output serves this) instead of escalating credentials. Do NOT edit src/wanctl/. `chmod +x` the script.
  </action>
  <verify>
    <automated>test -x scripts/phase238-egress-proof.sh && bash -n scripts/phase238-egress-proof.sh && grep -q 'set -euo pipefail' scripts/phase238-egress-proof.sh && grep -Fq '<none>' scripts/phase238-egress-proof.sh && grep -q 'ip route get' scripts/phase238-egress-proof.sh && grep -q 'ip rule' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.227' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.223' scripts/phase238-egress-proof.sh && grep -Fq 'att-modem' scripts/phase238-egress-proof.sh && grep -Fq 'spec-modem' scripts/phase238-egress-proof.sh && ! grep -qE 'ip (route|rule|addr|link) (add|del|change|replace|flush)' scripts/phase238-egress-proof.sh && ! grep -qE '\bsudo\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bsystemctl\b' scripts/phase238-egress-proof.sh && ! grep -qE '\btc\b[^a-z].*(change|add|del|replace)' scripts/phase238-egress-proof.sh && ! grep -qE '\btee\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bnft\b' scripts/phase238-egress-proof.sh && scripts/phase238-egress-proof.sh --help && scripts/phase238-egress-proof.sh --print-commands && cmds="$(scripts/phase238-egress-proof.sh --print-commands | sed -n '/--- BEGIN REMOTE COMMANDS ---/,/--- END REMOTE COMMANDS ---/p' | grep -v -- '--- BEGIN REMOTE COMMANDS ---' | grep -v -- '--- END REMOTE COMMANDS ---' | grep -v '^[[:space:]]*$')" && test -n "$cmds" && printf '%s\n' "$cmds" | grep -q '^ip route get ' && ! printf '%s\n' "$cmds" | grep -vqE '^(ip route get |ip rule$)' && scripts/phase238-egress-proof.sh --self-test && test -z "$(git status --porcelain -- src/wanctl/)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase238-egress-proof.sh` exits 0 (valid syntax).
    - `scripts/phase238-egress-proof.sh --help` prints usage and exits 0, and the usage documents the expected per-WAN src AND dev (spectrum: src 10.10.110.223 / dev spec-modem; att: src 10.10.110.227 / dev att-modem) and states the devs are repo-derived from configs/cake-autorate/config.{spectrum,att}.sh `ul_if`.
    - `scripts/phase238-egress-proof.sh --print-commands` exits 0, does not SSH or execute, and prints the remote commands one-per-line in a delimited machine-checkable section.
    - ALLOWLIST GATE: every remote-command line inside the `--print-commands` machine-checkable section matches EXACTLY one of the two anchored patterns `^ip route get ` or `^ip rule$`; NO line in that section falls outside those two patterns. (Mechanically: the `--print-commands` section, with delimiter lines stripped, has at least one `^ip route get ` line and zero lines that fail `^(ip route get |ip rule$)`.)
    - `scripts/phase238-egress-proof.sh --self-test` exits 0 and runs WITHOUT any SSH/network: it feeds the four canned fixtures through the same `verdict_for_line` the live path uses and confirms (a) Spectrum src 10.10.110.223 + dev spec-modem → PASS, (b) ATT src 10.10.110.227 + dev att-modem → PASS, (c) ATT wrong-src 10.10.110.223 → FAIL, (d) ATT correct-src 10.10.110.227 + wrong-dev (not att-modem) → FAIL; if any fixture mismatches its expected verdict, --self-test exits non-zero.
    - The live path and the `--self-test` path call the SAME `verdict_for_line` function, and that function's PASS criterion requires parsed src == expected src AND parsed dev == expected dev.
    - Script contains `set -euo pipefail` and the read-only posture banner.
    - Script contains the `<none>` Spectrum sentinel and branches the `ip route get` invocation on it (no `from <none>`).
    - Script contains ATT source IP `10.10.110.227`, Spectrum default-route source `10.10.110.223`, ATT expected dev `att-modem`, Spectrum expected dev `spec-modem`, both WANs' reflector lists, an `ip route get` query, and an `ip rule` capture.
    - The ATT PASS criterion requires BOTH `src 10.10.110.227` AND `dev att-modem`; Spectrum PASS requires BOTH `src 10.10.110.223` AND `dev spec-modem`. Distinctness (Spectrum path != ATT path) is an ADDITIONAL assertion, not the primary proof.
    - The exit-code verdict requires every reflector to match its WAN's expected `src` AND expected `dev` AND (when both WANs in scope) the two resolved egress paths to be DISTINCT.
    - Script validates `--wan` against the fixed `spectrum|att|all` allowlist.
    - PRIMARY remote-command guard is the anchored allowlist over the generated `--print-commands` enumeration; the denylist greps (sudo/systemctl/tc-change/tee/nft/ip-mutators/system-path redirects) remain as secondary defense-in-depth and all still pass (none present).
    - The `<automated>` verify block FAIL-FASTS: it is a single `&&`-chain where every check (incl. `--self-test` and the clean-tree assertion) propagates its non-zero exit, with NO trailing `|| true` that could mask a failure. It returns non-zero when ANY of these hold: `--print-commands` emits a line outside `^ip route get `/`^ip rule$`, `--self-test` fails, src/wanctl/ is dirty, a denylisted mutator is present, or `bash -n` fails; it exits 0 only when ALL checks pass.
    - The clean-tree assertion is a POSITIVE `&&`-linked check `test -z "$(git status --porcelain -- src/wanctl/)"` (true only when the controller path has no changes), NOT an inverted `&& false || true` guard.
    - `git status --porcelain -- src/wanctl/` is empty (no controller-path drift).
    - Script is executable (`test -x`).
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>scripts/phase238-egress-proof.sh exists, parses clean, --help/--print-commands/--self-test work; --self-test proves Spectrum-PASS (src+dev), ATT-PASS (src+dev), ATT-wrong-src-FAIL, and ATT-correct-src-wrong-dev-FAIL through the same verdict function with no SSH; the verdict requires BOTH expected src AND expected dev per WAN (spectrum spec-modem / att att-modem, repo-derived from config.{spectrum,att}.sh ul_if); --print-commands emits a machine-checkable remote-command section gated by an anchored `^ip route get `/`^ip rule$` allowlist (denylist greps remain as defense-in-depth); covers both WANs with the Spectrum `<none>` default-route branch and ATT source binding; the verify block is a fail-fast `&&`-chain with a positive clean-tree assertion (no `|| true` mask); and the script is executable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the egress proof from the dev host (SSHes read-only to the cake-shaper)</name>
  <what-built>scripts/phase238-egress-proof.sh — a read-only both-WAN fping-egress proof that the operator runs FROM the dev host, which SSHes read-only to the cake-shaper (`kevin@10.10.110.223`) to run `ip route get <reflector> [from <source_ip>]` per WAN plus `ip rule`, with a fully mechanical per-WAN src+dev verdict (Spectrum: src 10.10.110.223 + dev spec-modem; ATT: src 10.10.110.227 + dev att-modem), a `--self-test` that pre-proves the verdict logic offline (incl. negative wrong-src and wrong-dev fixtures), and a `--print-commands` operator fallback gated by an anchored remote-command allowlist.</what-built>
  <read_first>
    - 238-CONTEXT.md D-08 (operator runs the committed script from the dev host over SSH to the cake-shaper; stdout → evidence)
    - 238-RESEARCH.md "Code Examples" (the exact read-only commands) and Environment Availability table
    - project credential-reads convention (live-host reads handed to operator as `! ssh …`, not escalated)
  </read_first>
  <action>
    The egress proof runs FROM the dev host and SSHes read-only to the production cake-shaper (`kevin@10.10.110.223`) — the TARGETS array already encodes the `ssh kevin@10.10.110.223` connection. Running the script's dev-host SSH requires operator-at-keyboard credentials, so this is an operator step, NOT an autonomous executor action (D-08, credential-reads convention). Present the operator with the exact command to run from the dev host: `scripts/phase238-egress-proof.sh --json` (the script SSHes read-only to the cake-shaper itself). If the dev-host SSH trips a guardrail, run `scripts/phase238-egress-proof.sh --print-commands` to get the exact per-WAN `! ssh cake-shaper 'ip route get <reflector> [from <source_ip>]'` / `! ssh cake-shaper 'ip rule'` lines for the operator to paste directly. Do NOT run the script locally on the cake-shaper — the canonical mode is dev-host-over-SSH as encoded in the TARGETS. The operator's role is to RUN the script read-only (from the dev host) and paste the full stdout back — the script's own exit-0/verdict mechanically enforces the egress dev and src (the operator does NOT subjectively judge whether the dev is the ATT uplink; the verdict already requires `dev att-modem` for ATT and `dev spec-modem` for Spectrum, repo-derived from the cake-autorate `ul_if` configs). Capture that stdout verbatim for Plan 03 (it is the PROV-03 evidence embedded in the provenance map). If the script's verdict is non-zero (e.g. the live egress dev or src differs from the repo-configured expectation), that is real drift to surface in Plan 03, not a thing to override. Do NOT attempt to escalate stored credentials to run this autonomously.
  </action>
  <verify>
    <human-check>Operator ran scripts/phase238-egress-proof.sh --json from the dev host (it SSHed read-only to the cake-shaper) — or used the --print-commands `! ssh` equivalents — and pasted back stdout that shows, per WAN, the resolved egress dev/src/via for each reflector plus the ip rule table; the script's verdict (exit code / per-reflector pass) — NOT operator judgment — enforced that Spectrum resolved with src 10.10.110.223 + dev spec-modem and ATT (source 10.10.110.227) resolved with src 10.10.110.227 + dev att-modem; the two paths are distinct; the operator pasted the full stdout back for capture in Plan 03.</human-check>
  </verify>
  <acceptance_criteria>
    - The script was run read-only from the dev host (SSHing to the cake-shaper), or its `--print-commands` `! ssh` equivalents were executed by the operator; the script was NOT run locally on the cake-shaper.
    - Stdout for both WANs is captured verbatim and available for Plan 03.
    - The script's verdict (not operator judgment) confirmed Spectrum's resolved egress shows `src 10.10.110.223` AND `dev spec-modem`.
    - The script's verdict (not operator judgment) confirmed ATT's resolved egress shows `src 10.10.110.227` AND `dev att-modem` (the source-bound ATT uplink path; expected dev repo-derived from config.att.sh `ul_if`).
    - The two WANs' resolved egress paths are distinct.
    - No production mutation occurred (only `ip route get` / `ip rule` ran).
  </acceptance_criteria>
  <resume-signal>Paste the full egress-proof stdout (both WANs + ip rule) and type "captured" to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Remote reads must be read-only; no mutation of routing, units, or CAKE mode |
| script arg (`--wan`) → control flow / SSH command | Untrusted CLI input must not inject into the SSH command string |
| evidence artifact → public docs | Internal IPs captured in stdout must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-02 | Tampering | phase238-egress-proof.sh remote commands | mitigate | `set -euo pipefail`; PRIMARY guard is an ANCHORED ALLOWLIST-PARSER over the generated remote commands — `--print-commands` emits the canonical one-per-line remote-command enumeration in a delimited machine-checkable section, and an automated gate asserts EVERY such line matches exactly `^ip route get ` or `^ip rule$`, so any out-of-list mutator/redirect fails even if not denylisted; a static denylist (`sudo`/`systemctl`/`tc ... change/add/del/replace`/`tee`/`nft`/`ip ... add/del/change/replace/flush`/system-path redirects) remains as secondary defense-in-depth; read-only posture banner; `ssh -n -o BatchMode=yes`; `--print-commands` lets the operator inspect the exact commands before running |
| T-238-02a | Tampering | `--wan` arg → SSH command | mitigate | `validate_wan` fixed `spectrum|att|all` allowlist; quote all expansions; pass values via shell vars, never interpolate untrusted input into the remote command string; the anchored remote-command allowlist (`^ip route get `/`^ip rule$`) bounds the blast radius even if an arg slipped through |
| T-238-02b | Information Disclosure | captured stdout (internal IPs) | mitigate | Internal `10.10.110.x` IPs stay in phase-dir evidence + the provenance map; never promoted to public-safe CLAUDE.md/README |
| T-238-02c | Spoofing | resolved egress verdict (wrong-src / wrong-dev false pass) | mitigate | ATT PASS requires BOTH the concrete `src 10.10.110.227` AND `dev att-modem` (repo-derived from config.att.sh `ul_if`), not just "distinct from Spectrum"; `--self-test` negative fixtures — ATT returning `src 10.10.110.223` (wrong src) AND ATT returning correct src 10.10.110.227 with a non-`att-modem` dev (wrong dev) — both assert the verdict FAILS, autonomously proving the verdict rejects a spoofed/misrouted src OR egress on the wrong interface before the live run |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `bash -n` parses the script clean; `--help`, `--print-commands`, and `--self-test` work.
- `--self-test` exits 0 proving Spectrum src-10.10.110.223+dev-spec-modem PASS, ATT src-10.10.110.227+dev-att-modem PASS, ATT-wrong-src-10.10.110.223 FAIL, and ATT-correct-src-wrong-dev FAIL through the same `verdict_for_line` function, with no SSH.
- The `<automated>` verify block is a single fail-fast `&&`-chain (no `|| true` mask): a planted denylisted mutator, a non-anchored `--print-commands` line, a failing `--self-test`, or a dirty src/wanctl/ tree each force a non-zero exit; the clean-tree check is the positive assertion `test -z "$(git status --porcelain -- src/wanctl/)"`.
- The `--print-commands` machine-checkable section enumerates the remote commands one-per-line, and an anchored allowlist gate confirms every such line matches exactly `^ip route get ` or `^ip rule$` (no out-of-list command can pass).
- The script contains the `<none>` Spectrum sentinel, ATT source `10.10.110.227`, Spectrum default-route src `10.10.110.223`, ATT expected dev `att-modem`, Spectrum expected dev `spec-modem`, `ip route get`, `ip rule`, a concrete per-WAN expected-src+expected-dev verdict with a distinctness guard, and NONE of the denylisted mutators (sudo/systemctl/tc-change/tee/nft/ip-mutators/system-path redirects) as defense-in-depth.
- Operator-run stdout (both WANs + ip rule), produced by running the script from the dev host over SSH to the cake-shaper, is captured for Plan 03; ATT dev enforced as `att-modem` by the script verdict (not operator judgment).
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- A committed read-only both-WAN egress-proof script exists, with the Spectrum `<none>` default-route branch and the ATT source-bound branch, a fully mechanical per-WAN src+dev verdict (Spectrum: src 10.10.110.223 + dev spec-modem; ATT: src 10.10.110.227 + dev att-modem; expected devs repo-derived from config.{spectrum,att}.sh `ul_if`) plus a distinctness guard, a `--self-test` that autonomously proves the verdict logic (incl. negative wrong-src AND wrong-dev fixtures), an anchored-allowlist-gated `--print-commands` remote-command enumeration (denylist as defense-in-depth), and a `--print-commands` operator fallback.
- The operator runs it read-only from the dev host (it SSHes to the cake-shaper); the script verdict (not operator judgment) confirms ATT's `src 10.10.110.227` on `dev att-modem` and Spectrum's `src 10.10.110.223` on `dev spec-modem`, and the stdout is captured for the provenance map.
- No src/wanctl/ file is modified and no production state is mutated.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md` when done
</output>
