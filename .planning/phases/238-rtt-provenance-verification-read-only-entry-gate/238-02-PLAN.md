---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase238-egress-proof.sh
autonomous: false
requirements: [PROV-03]
must_haves:
  truths:
    - "Operator can run scripts/phase238-egress-proof.sh --json on the live cake-shaper host and it proves, per WAN, that fping -S <source_ip> would egress the intended WAN under the host's current ip rule policy routing"
    - "Spectrum is proven via the default-route path (ip route get <reflector>) because it has no ping_source_ip; ATT is proven source-bound via ip route get <reflector> from 10.10.110.227"
    - "The host ip rule policy table is captured once per host as context"
    - "The script is read-only: only ip route get / ip rule / curl-less kernel queries; no ip ... add/del, no sudo-write, no unit control"
  artifacts:
    - path: "scripts/phase238-egress-proof.sh"
      provides: "Both-WAN read-only fping-egress proof under live ip rule policy routing"
      contains: "set -euo pipefail"
  key_links:
    - from: "scripts/phase238-egress-proof.sh"
      to: "live cake-shaper host (10.10.110.223)"
      via: "ssh -n -o BatchMode=yes read-only kernel query"
      pattern: "ip route get"
---

<objective>
Author `scripts/phase238-egress-proof.sh` — a committed, read-only, both-WAN proof that `fping -S <source_ip>` would egress the intended WAN under the cake-shaper host's current `ip rule` policy routing (PROV-03). The script is authored autonomously; the live-host run is an operator step (it requires SSH to the production cake-shaper).

Purpose: Phase 245's live A/B will run `fping -S <source_ip>`; PROV-03 proves up front that source-IP binding routes out the intended WAN under live policy routing, for BOTH WANs (D-07, safe while the A/B target is undecided). The operator runs it on the host and its stdout is captured into the provenance map (Plan 03).

Output: `scripts/phase238-egress-proof.sh` (committed, read-only), plus its live stdout captured by the operator for Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@scripts/phase231-migration-held.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author phase238-egress-proof.sh (both WANs, read-only)</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <read_first>
    - scripts/phase231-migration-held.sh (FULL — exact structural analog: header/usage, set -euo pipefail, ssh_readonly helper, TARGETS array + IFS='|' read unpack, --wan/--json arg parsing, validate_wan allowlist, require_command preflight, status_json/json_string python3 helpers, mktemp+trap, dual output mode + exit-code verdict)
    - 238-PATTERNS.md section "scripts/phase238-egress-proof.sh" (lines 21-159 — tells you which phase231 line ranges to clone and what to swap)
    - 238-RESEARCH.md section "Egress-Proof Script Skeleton (PROV-03, D-07/D-08)" and "Config Keys — Source IPs & Reflectors" table (the exact source IPs + reflectors)
    - 238-RESEARCH.md "Common Pitfalls" Pitfall 3 (Spectrum has no ping_source_ip) and Pitfall 5 (no mutation during read-only capture)
    - 238-CONTEXT.md D-07 (both WANs + ip rule table) and D-08 (committed script, operator runs on host)
  </read_first>
  <action>
    Clone the STRUCTURE of scripts/phase231-migration-held.sh (not its checks) into scripts/phase238-egress-proof.sh. Header: shebang, a Phase 238 description line, and a read-only posture banner stating "Read-only. Proves fping -S <source_ip> egress per WAN via ip route get / ip rule. No worktree, prod, CAKE-mode, unit, or controller-source mutation." Then `set -euo pipefail`. Define a TARGETS array with two pipe-delimited entries, both SSHing to the same cake-shaper host `kevin@10.10.110.223`, carrying per-WAN fields: WAN name, source IP (use the literal sentinel `<none>` for Spectrum since it has NO ping_source_ip; `10.10.110.227` for ATT), and the reflector list. Spectrum reflectors: `1.1.1.1 9.9.9.9 208.67.222.222`. ATT reflectors: `1.1.1.1 8.8.8.8 151.101.1.57`. Keep the `ssh_readonly() { ssh -n "${SSH_OPTS[@]}" "$1" "$2"; }` helper with `SSH_OPTS=(-o ConnectTimeout=10 -o BatchMode=yes)`. Per WAN, per reflector run the kernel query: when source IP is `<none>` run `ip route get <reflector>` (default-route path); otherwise run `ip route get <reflector> from <source_ip>`. Branch on the `<none>` sentinel — do NOT pass `from <none>`. Once per host capture `ip rule` (the policy table) as context, not pass/fail-scored (D-07). Reuse phase231's `--wan spectrum|att|all` + `--json` + `--help` arg parsing (DROP `--window-hours`), the `validate_wan` fixed-allowlist check, `require_command python3` + `require_command ssh` local preflight (note: `ip` is required on the REMOTE host inside the SSH command, not locally), `status_json`/`json_string` python3-argv JSON helpers, `mktemp -d` + `trap 'rm -rf "$tmpdir"' EXIT`, the main TARGETS-unpack loop with the `--wan` filter skip, and the dual output mode (full JSON array under `--json`, human summary otherwise) ending in an exit-code verdict: exit 0 iff every resolved egress device/path matches the intended WAN interface. Quote all expansions; never interpolate untrusted input into the SSH command string. Per D-08 + the credential-reads convention: if a live read trips a guardrail, the script (or its docstring) should make it trivial to hand the operator the exact `! ssh cake-shaper '…'` command instead of escalating credentials — the live run is an operator step. Do NOT edit src/wanctl/. `chmod +x` the script.
  </action>
  <verify>
    <automated>bash -n scripts/phase238-egress-proof.sh && grep -q 'set -euo pipefail' scripts/phase238-egress-proof.sh && grep -Fq '<none>' scripts/phase238-egress-proof.sh && grep -q 'ip route get' scripts/phase238-egress-proof.sh && grep -q 'ip rule' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.227' scripts/phase238-egress-proof.sh && ! grep -qE 'ip (route|rule) (add|del|change|replace)' scripts/phase238-egress-proof.sh && scripts/phase238-egress-proof.sh --help</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase238-egress-proof.sh` exits 0 (valid syntax).
    - `scripts/phase238-egress-proof.sh --help` prints usage and exits 0.
    - Script contains `set -euo pipefail` and the read-only posture banner.
    - Script contains the `<none>` Spectrum sentinel and branches the `ip route get` invocation on it (no `from <none>`).
    - Script contains ATT source IP `10.10.110.227`, both WANs' reflector lists, an `ip route get` query, and an `ip rule` capture.
    - Script validates `--wan` against the fixed `spectrum|att|all` allowlist.
    - Script contains NO `ip route|rule add|del|change|replace` and no sudo-write / unit-control commands.
    - Script is executable (`test -x`).
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>scripts/phase238-egress-proof.sh exists, parses clean, --help works, covers both WANs with the Spectrum `<none>` sentinel and ATT source binding, is read-only, and is executable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the egress proof on the live cake-shaper host</name>
  <what-built>scripts/phase238-egress-proof.sh — a read-only both-WAN fping-egress proof that runs `ip route get <reflector> [from <source_ip>]` per WAN plus `ip rule` on the live cake-shaper host.</what-built>
  <read_first>
    - 238-CONTEXT.md D-08 (operator runs the committed script on the live host; stdout → evidence)
    - 238-RESEARCH.md "Code Examples" (the exact read-only commands) and Environment Availability table
    - project credential-reads convention (live-host reads handed to operator as `! ssh …`, not escalated)
  </read_first>
  <action>
    The egress proof must run on the production cake-shaper host (`10.10.110.223`), which requires operator SSH access — this is an operator-at-keyboard step, NOT an autonomous executor action (D-08, credential-reads convention). Present the operator with the exact command to run: `scripts/phase238-egress-proof.sh --json` (or, if SSH from the dev host trips a guardrail, the per-WAN `! ssh cake-shaper 'ip route get <reflector> from <source_ip>'` / `! ssh cake-shaper 'ip rule'` equivalents from RESEARCH Code Examples). The operator runs it read-only on the host and pastes the full stdout back. Capture that stdout verbatim for Plan 03 (it is the PROV-03 evidence embedded in the provenance map). Confirm each WAN's resolved egress device/path matches the intended WAN interface (Spectrum default route; ATT via FORCE_OUT_ATT). Do NOT attempt to escalate stored credentials to run this autonomously.
  </action>
  <verify>
    <human-check>Operator ran scripts/phase238-egress-proof.sh --json (or the `! ssh` equivalents) read-only on the cake-shaper host; stdout shows, per WAN, the resolved egress device for each reflector plus the ip rule table; Spectrum egresses the default-route interface and ATT (source 10.10.110.227) egresses the ATT path; the operator pasted the full stdout back for capture in Plan 03.</human-check>
  </verify>
  <acceptance_criteria>
    - The script (or `! ssh` equivalents) was executed read-only on the live cake-shaper host by the operator.
    - Stdout for both WANs is captured verbatim and available for Plan 03.
    - Each WAN's resolved egress matches the intended interface (Spectrum default route; ATT via the ATT source-bound path).
    - No production mutation occurred (only `ip route get` / `ip rule` ran).
  </acceptance_criteria>
  <resume-signal>Paste the full egress-proof stdout (both WANs + ip rule) and type "captured" to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Remote reads must be read-only; no mutation of routing, units, or CAKE mode |
| script arg (`--wan`) → control flow / SSH command | Untrusted CLI input must not inject into the SSH command string |
| evidence artifact → public docs | Internal IPs captured in stdout must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-02 | Tampering | phase238-egress-proof.sh remote commands | mitigate | `set -euo pipefail`; only `ip route get` / `ip rule` / read-only kernel queries; NO `ip ... add/del/change/replace`, no sudo-write, no unit control; read-only posture banner; `ssh -n -o BatchMode=yes` |
| T-238-02a | Tampering | `--wan` arg → SSH command | mitigate | `validate_wan` fixed `spectrum|att|all` allowlist; quote all expansions; pass values via shell vars, never interpolate untrusted input into the remote command string |
| T-238-02b | Information Disclosure | captured stdout (internal IPs) | mitigate | Internal `10.10.110.x` IPs stay in phase-dir evidence + the provenance map; never promoted to public-safe CLAUDE.md/README |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `bash -n` parses the script clean; `--help` works.
- The script contains the `<none>` Spectrum sentinel, ATT source `10.10.110.227`, `ip route get`, `ip rule`, and no mutating `ip` subcommands.
- Operator-run stdout (both WANs + ip rule) is captured for Plan 03.
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- A committed read-only both-WAN egress-proof script exists, with the Spectrum `<none>` default-route branch and the ATT source-bound branch.
- The operator runs it read-only on the live cake-shaper host and the stdout is captured for the provenance map.
- No src/wanctl/ file is modified and no production state is mutated.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md` when done
</output>
