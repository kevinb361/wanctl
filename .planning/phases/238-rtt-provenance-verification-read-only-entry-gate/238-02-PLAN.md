---
phase: 238-rtt-provenance-verification-read-only-entry-gate
plan: 02
type: execute
wave: 2
depends_on: ["238-01"]
files_modified:
  - scripts/phase238-egress-proof.sh
autonomous: false
requirements: [PROV-03]
must_haves:
  decisions:
    - "D-07: Cover both WANs (Spectrum + ATT) — prove ip route get <reflector> from <source_ip> per WAN and capture the host ip rule policy table for context"
    - "D-08: Capture via a committed read-only proof script scripts/phase238-egress-proof.sh that the operator runs on the live host; stdout captured into the evidence artifact"
  truths:
    - "Operator can run scripts/phase238-egress-proof.sh --json on the live cake-shaper host and it proves, per WAN, that fping -S <source_ip> would egress the intended WAN under the host's current ip rule policy routing"
    - "Spectrum is proven via the default-route path (ip route get <reflector>, egress src 10.10.110.223) because it has no ping_source_ip; ATT is proven source-bound via ip route get <reflector> from 10.10.110.227, and the ATT PASS criterion requires the resolved egress to show src 10.10.110.227 (the source-bound observable), not merely a path distinct from Spectrum"
    - "The verdict (exit 0) requires EVERY reflector for a WAN to resolve to that WAN's expected src observable (Spectrum src 10.10.110.223, ATT src 10.10.110.227) AND the two WANs to resolve to DISTINCT egress paths — the concrete src match is the primary proof and the distinctness check is an additional assertion"
    - "A --self-test mode pipes canned ip route get fixtures (Spectrum src 10.10.110.223 → PASS, ATT src 10.10.110.227 → PASS, and a negative ATT-with-wrong-src 10.10.110.223 → FAIL) through the SAME parser/verdict logic the live path uses, with no SSH, and exits 0 iff every fixture produces its expected verdict — proving the verdict logic is correct before any live run"
    - "The host ip rule policy table is captured once per host as context"
    - "The script issues ONLY ip route get and ip rule as remote commands (allowlist); it contains none of sudo/systemctl/tc-change/tee/nft/ip-mutators or system-path output redirects"
    - "A --print-commands mode prints the exact read-only ip route get / ip rule commands for operator fallback WITHOUT executing or SSHing"
  artifacts:
    - path: "scripts/phase238-egress-proof.sh"
      provides: "Both-WAN read-only fping-egress proof under live ip rule policy routing, with a self-test of the verdict logic"
      contains: "set -euo pipefail"
  key_links:
    - from: "scripts/phase238-egress-proof.sh"
      to: "live cake-shaper host (10.10.110.223)"
      via: "ssh -n -o BatchMode=yes read-only kernel query"
      pattern: "ip route get"
---

<objective>
Author `scripts/phase238-egress-proof.sh` — a committed, read-only, both-WAN proof that `fping -S <source_ip>` would egress the intended WAN under the cake-shaper host's current `ip rule` policy routing (PROV-03). The script is authored autonomously; the live-host run is an operator step (it requires SSH to the production cake-shaper). The script also ships a `--self-test` mode that proves its own parser/verdict logic against canned fixtures (no SSH), so the pass/fail logic is autonomously enforceable before the operator ever runs it live.

Purpose: Phase 245's live A/B will run `fping -S <source_ip>`; PROV-03 proves up front that source-IP binding routes out the intended WAN under live policy routing, for BOTH WANs (D-07, safe while the A/B target is undecided). The operator runs it on the host and its stdout is captured into the provenance map (Plan 03).

Output: `scripts/phase238-egress-proof.sh` (committed, read-only), plus its live stdout captured by the operator for Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-CONTEXT.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-RESEARCH.md
@.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-PATTERNS.md
@scripts/phase231-migration-held.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author phase238-egress-proof.sh (both WANs, read-only, with --self-test)</name>
  <files>scripts/phase238-egress-proof.sh</files>
  <read_first>
    - scripts/phase231-migration-held.sh (FULL — exact structural analog: header/usage, set -euo pipefail, ssh_readonly helper, TARGETS array + IFS='|' read unpack, --wan/--json arg parsing, validate_wan allowlist, require_command preflight, status_json/json_string python3 helpers, mktemp+trap, dual output mode + exit-code verdict)
    - 238-PATTERNS.md section "scripts/phase238-egress-proof.sh" (lines 21-159 — tells you which phase231 line ranges to clone and what to swap)
    - 238-RESEARCH.md section "Egress-Proof Script Skeleton (PROV-03, D-07/D-08)" and "Config Keys — Source IPs & Reflectors" table (the exact source IPs + reflectors)
    - 238-RESEARCH.md "Common Pitfalls" Pitfall 3 (Spectrum has no ping_source_ip) and Pitfall 5 (no mutation during read-only capture)
    - 238-CONTEXT.md D-07 (both WANs + ip rule table) and D-08 (committed script, operator runs on host)
  </read_first>
  <action>
    Clone the STRUCTURE of scripts/phase231-migration-held.sh (not its checks) into scripts/phase238-egress-proof.sh. Header: shebang, a Phase 238 description line, and a read-only posture banner stating "Read-only. Proves fping -S <source_ip> egress per WAN via ip route get / ip rule. No worktree, prod, CAKE-mode, unit, or controller-source mutation." Then `set -euo pipefail`. Define a TARGETS array with two pipe-delimited entries, both SSHing to the same cake-shaper host `kevin@10.10.110.223`, carrying per-WAN fields: WAN name, source IP (use the literal sentinel `<none>` for Spectrum since it has NO ping_source_ip; `10.10.110.227` for ATT), an EXPECTED-PATH field, and the reflector list. Spectrum reflectors: `1.1.1.1 9.9.9.9 208.67.222.222`. ATT reflectors: `1.1.1.1 8.8.8.8 151.101.1.57`. Keep the `ssh_readonly() { ssh -n "${SSH_OPTS[@]}" "$1" "$2"; }` helper with `SSH_OPTS=(-o ConnectTimeout=10 -o BatchMode=yes)`. Per WAN, per reflector run the kernel query: when source IP is `<none>` run `ip route get <reflector>` (default-route path); otherwise run `ip route get <reflector> from <source_ip>`. Branch on the `<none>` sentinel — do NOT pass `from <none>`. Once per host capture `ip rule` (the policy table) as context, not pass/fail-scored (D-07). Reuse phase231's `--wan spectrum|att|all` + `--json` + `--help` arg parsing (DROP `--window-hours`), the `validate_wan` fixed-allowlist check, `require_command python3` + `require_command ssh` local preflight (note: `ip` is required on the REMOTE host inside the SSH command, not locally), `status_json`/`json_string` python3-argv JSON helpers, `mktemp -d` + `trap 'rm -rf "$tmpdir"' EXIT`, the main TARGETS-unpack loop with the `--wan` filter skip.

    FACTOR THE PARSER/VERDICT INTO A SINGLE REUSABLE FUNCTION (CONCERN 3 fix — the verdict logic must be autonomously testable, not operator-attested only). Write one function (e.g. `verdict_for_line`) that takes a WAN name and a single `ip route get` stdout line, parses `dev`, `src`, and `via` from it (the kernel prints `<reflector> [from <src>] [via <gw>] dev <iface> src <src> ...`), and returns PASS/FAIL plus the parsed `dev`/`src`/`via`. The LIVE path and the `--self-test` path MUST both call this same function so the self-test proves the live verdict.

    DEFINE THE CONCRETE EGRESS VERDICT. Per-WAN expected criteria:
      - Spectrum (source `<none>`): every reflector must resolve via the host DEFAULT ROUTE — the resolved egress `src` is exactly `10.10.110.223` (the host primary IP / default-route source). PASS criterion for Spectrum: every reflector resolves with `src 10.10.110.223` (the default-route source observable).
      - ATT (source `10.10.110.227`): `ip route get <reflector> from 10.10.110.227` must resolve via the ATT source-bound policy-routing path, and the resolved egress `src` is exactly `10.10.110.227`. The PRIMARY ATT PASS criterion is the concrete `src 10.10.110.227` observable (NOT merely "distinct from Spectrum"). PASS criterion for ATT: every reflector resolves with `src 10.10.110.227`.
    Encode the expected `src` (and an expected-path label) in an explicit per-WAN field in the TARGETS structure (`spectrum` → `default-route src 10.10.110.223`, `att` → `source-bound src 10.10.110.227`); the script documents this expected per-WAN src/path in its usage/header. Keep the WAN-distinctness check as an ADDITIONAL (not primary) assertion: capture both WANs' resolved `dev`/`src`/`via` and assert the two egress paths differ (Spectrum default-route path != ATT source-bound path) — this is a secondary guard, the primary proof is each WAN's concrete `src` match. Final exit-code verdict: exit 0 iff (every reflector for each in-scope WAN matches that WAN's expected `src` observable via `verdict_for_line`) AND (when both WANs are in scope, the two resolved egress paths are distinct). Emit the parsed `dev`/`src`/`via`, the expected-src/expected-path string, and the per-reflector pass/fail into the JSON (full JSON array under `--json`, human summary otherwise).

    ADD A `--self-test` MODE (CONCERN 3 fix — makes the concrete pass/fail logic autonomously enforceable). When invoked with `--self-test`, the script feeds canned `ip route get` stdout fixtures through the SAME `verdict_for_line` function the live path uses, WITHOUT any SSH and WITHOUT touching the network, and asserts each fixture produces its expected verdict. Provide at least these three fixtures: (a) a Spectrum-style line resolving via the default route with `src 10.10.110.223` (e.g. `1.1.1.1 via 10.10.110.1 dev wan-spectrum src 10.10.110.223 uid 1000`) → expected PASS for Spectrum; (b) an ATT-style line resolving via the source-bound path with `src 10.10.110.227` (e.g. `1.1.1.1 from 10.10.110.227 via 10.10.110.2 dev wan-att src 10.10.110.227 uid 1000`) → expected PASS for ATT; (c) a NEGATIVE fixture where ATT resolves to the WRONG src — an ATT reflector coming back with `src 10.10.110.223` (the Spectrum src) instead of `10.10.110.227` → expected FAIL for ATT, proving the verdict actually REJECTS wrong egress. `--self-test` prints each fixture's expected-vs-actual verdict and exits 0 iff ALL fixtures (including the negative one) produce their expected verdicts; otherwise it exits non-zero. This mode runs locally with no SSH and no network access.

    ADD A `--print-commands` MODE (CONCERN 3 fix + review suggestion): when invoked with `--print-commands`, the script prints (to stdout) the exact read-only commands it would run for the operator fallback — the per-WAN `ip route get <reflector> [from <source_ip>]` lines and the `ip rule` line — WITHOUT executing them and WITHOUT any SSH. It must exit 0 in this mode.

    HARDEN THE READ-ONLY POSTURE TO AN ALLOWLIST (CONCERN 4 fix): build the script so the ONLY remote commands it issues are `ip route get ...` and `ip rule` (allowlist posture). Do NOT call `sudo`, `systemctl`, `tc`, `tee`, `nft`, any mutating `ip ... add|del|change|replace|flush`, or any redirection writing to a system path inside the SSH command. Quote all expansions; never interpolate untrusted input into the SSH command string. Per D-08 + the credential-reads convention: if a live read trips a guardrail, the script (or its docstring) should make it trivial to hand the operator the exact `! ssh cake-shaper '…'` command (the `--print-commands` output serves this) instead of escalating credentials — the live run is an operator step. Do NOT edit src/wanctl/. `chmod +x` the script.
  </action>
  <verify>
    <automated>bash -n scripts/phase238-egress-proof.sh && grep -q 'set -euo pipefail' scripts/phase238-egress-proof.sh && grep -Fq '<none>' scripts/phase238-egress-proof.sh && grep -q 'ip route get' scripts/phase238-egress-proof.sh && grep -q 'ip rule' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.227' scripts/phase238-egress-proof.sh && grep -Fq '10.10.110.223' scripts/phase238-egress-proof.sh && ! grep -qE 'ip (route|rule|addr|link) (add|del|change|replace|flush)' scripts/phase238-egress-proof.sh && ! grep -qE '\bsudo\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bsystemctl\b' scripts/phase238-egress-proof.sh && ! grep -qE '\btc\b[^a-z].*(change|add|del|replace)' scripts/phase238-egress-proof.sh && ! grep -qE '\btee\b' scripts/phase238-egress-proof.sh && ! grep -qE '\bnft\b' scripts/phase238-egress-proof.sh && scripts/phase238-egress-proof.sh --help && scripts/phase238-egress-proof.sh --print-commands && scripts/phase238-egress-proof.sh --print-commands | grep -q 'ip route get' && scripts/phase238-egress-proof.sh --self-test; st=$?; test "$st" -eq 0 && git status --porcelain -- src/wanctl/ | grep -q . && false || true</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase238-egress-proof.sh` exits 0 (valid syntax).
    - `scripts/phase238-egress-proof.sh --help` prints usage and exits 0.
    - `scripts/phase238-egress-proof.sh --print-commands` exits 0 and prints the `ip route get` lines (and `ip rule`) without executing or SSHing.
    - `scripts/phase238-egress-proof.sh --self-test` exits 0 and runs WITHOUT any SSH/network: it feeds the three canned fixtures through the same verdict function the live path uses and confirms (a) Spectrum src 10.10.110.223 → PASS, (b) ATT src 10.10.110.227 → PASS, (c) ATT-with-wrong-src 10.10.110.223 → FAIL (the negative fixture proves wrong egress is rejected); if any fixture mismatches its expected verdict, --self-test exits non-zero.
    - The live path and the `--self-test` path call the SAME parser/verdict function.
    - Script contains `set -euo pipefail` and the read-only posture banner.
    - Script contains the `<none>` Spectrum sentinel and branches the `ip route get` invocation on it (no `from <none>`).
    - Script contains ATT source IP `10.10.110.227`, Spectrum default-route source `10.10.110.223`, both WANs' reflector lists, an `ip route get` query, and an `ip rule` capture.
    - The ATT PASS criterion is the concrete `src 10.10.110.227` observable (NOT merely "distinct from Spectrum"); Spectrum PASS is `src 10.10.110.223`. Distinctness (Spectrum path != ATT path) is an ADDITIONAL assertion, not the primary ATT proof.
    - The exit-code verdict requires every reflector to match its WAN's expected `src` observable AND (when both WANs in scope) the two resolved egress paths to be DISTINCT.
    - Script validates `--wan` against the fixed `spectrum|att|all` allowlist.
    - Script issues only `ip route get` and `ip rule` as remote commands (allowlist) and contains NONE of: `sudo`, `systemctl`, `tc ... change/add/del/replace`, `tee`, `nft`, `ip ... add/del/change/replace/flush`, or system-path output redirects.
    - `git status --porcelain -- src/wanctl/` is empty (no controller-path drift).
    - Script is executable (`test -x`).
    - No file under src/wanctl/ is modified.
  </acceptance_criteria>
  <done>scripts/phase238-egress-proof.sh exists, parses clean, --help/--print-commands/--self-test work (self-test proves Spectrum-PASS, ATT-PASS, and ATT-wrong-src-FAIL through the same verdict function with no SSH), covers both WANs with the Spectrum `<none>` default-route branch and ATT source binding, encodes the concrete ATT `src 10.10.110.227` primary criterion plus a distinctness guard, uses an allowlist read-only posture (no mutators), and is executable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the egress proof on the live cake-shaper host</name>
  <what-built>scripts/phase238-egress-proof.sh — a read-only both-WAN fping-egress proof that runs `ip route get <reflector> [from <source_ip>]` per WAN plus `ip rule` on the live cake-shaper host, with a concrete per-WAN expected-src verdict (Spectrum src 10.10.110.223, ATT src 10.10.110.227), a `--self-test` that pre-proves the verdict logic offline, and a `--print-commands` operator fallback.</what-built>
  <read_first>
    - 238-CONTEXT.md D-08 (operator runs the committed script on the live host; stdout → evidence)
    - 238-RESEARCH.md "Code Examples" (the exact read-only commands) and Environment Availability table
    - project credential-reads convention (live-host reads handed to operator as `! ssh …`, not escalated)
  </read_first>
  <action>
    The egress proof must run on the production cake-shaper host (`10.10.110.223`), which requires operator SSH access — this is an operator-at-keyboard step, NOT an autonomous executor action (D-08, credential-reads convention). Present the operator with the exact command to run: `scripts/phase238-egress-proof.sh --json` (or, if SSH from the dev host trips a guardrail, run `scripts/phase238-egress-proof.sh --print-commands` to get the exact per-WAN `! ssh cake-shaper 'ip route get <reflector> [from <source_ip>]'` / `! ssh cake-shaper 'ip rule'` lines to paste). The operator runs it read-only on the host and pastes the full stdout back. Capture that stdout verbatim for Plan 03 (it is the PROV-03 evidence embedded in the provenance map). Confirm each WAN's resolved egress matches its expected src/path: Spectrum egresses the default-route interface with `src 10.10.110.223`; ATT (source 10.10.110.227) egresses the source-bound FORCE_OUT_ATT path with `src 10.10.110.227` and the `dev` is the ATT uplink interface (not merely a different interface than Spectrum — confirm it is the ATT uplink). Also confirm the two resolved paths are distinct. Do NOT attempt to escalate stored credentials to run this autonomously.
  </action>
  <verify>
    <human-check>Operator ran scripts/phase238-egress-proof.sh --json (or the --print-commands `! ssh` equivalents) read-only on the cake-shaper host; stdout shows, per WAN, the resolved egress dev/src/via for each reflector plus the ip rule table; Spectrum egresses the default-route interface with src 10.10.110.223 and ATT (source 10.10.110.227) egresses with src 10.10.110.227 on the ATT uplink dev (operator confirms the dev is the ATT uplink, not merely different from Spectrum); the two paths are distinct; the operator pasted the full stdout back for capture in Plan 03.</human-check>
  </verify>
  <acceptance_criteria>
    - The script (or `--print-commands` `! ssh` equivalents) was executed read-only on the live cake-shaper host by the operator.
    - Stdout for both WANs is captured verbatim and available for Plan 03.
    - Spectrum's resolved egress shows `src 10.10.110.223` via the default-route interface.
    - ATT's resolved egress shows `src 10.10.110.227` and the operator confirms the resolved `dev` is the ATT uplink (the source-bound FORCE_OUT_ATT path), not merely a different interface than Spectrum.
    - The two WANs' resolved egress paths are distinct.
    - No production mutation occurred (only `ip route get` / `ip rule` ran).
  </acceptance_criteria>
  <resume-signal>Paste the full egress-proof stdout (both WANs + ip rule) and type "captured" to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → production cake-shaper (SSH) | Remote reads must be read-only; no mutation of routing, units, or CAKE mode |
| script arg (`--wan`) → control flow / SSH command | Untrusted CLI input must not inject into the SSH command string |
| evidence artifact → public docs | Internal IPs captured in stdout must stay in phase-dir evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-02 | Tampering | phase238-egress-proof.sh remote commands | mitigate | `set -euo pipefail`; ALLOWLIST posture — the only remote commands issued are `ip route get` and `ip rule`; static denylist check rejects `sudo`/`systemctl`/`tc ... change/add/del/replace`/`tee`/`nft`/`ip ... add/del/change/replace/flush`/system-path redirects; read-only posture banner; `ssh -n -o BatchMode=yes`; `--print-commands` lets the operator inspect the exact commands before running |
| T-238-02a | Tampering | `--wan` arg → SSH command | mitigate | `validate_wan` fixed `spectrum|att|all` allowlist; quote all expansions; pass values via shell vars, never interpolate untrusted input into the remote command string; remote-command allowlist (only `ip route get`/`ip rule`) bounds the blast radius even if an arg slipped through |
| T-238-02b | Information Disclosure | captured stdout (internal IPs) | mitigate | Internal `10.10.110.x` IPs stay in phase-dir evidence + the provenance map; never promoted to public-safe CLAUDE.md/README |
| T-238-02c | Spoofing | resolved egress verdict (wrong-src false pass) | mitigate | ATT PASS requires the concrete `src 10.10.110.227` observable (not just "distinct from Spectrum"); `--self-test` negative fixture (ATT reflector returning `src 10.10.110.223`) asserts the verdict FAILS on wrong egress, autonomously proving the verdict rejects a spoofed/misrouted src before the live run |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | No package installs; zero external dependencies introduced |
</threat_model>

<verification>
- `bash -n` parses the script clean; `--help`, `--print-commands`, and `--self-test` work.
- `--self-test` exits 0 proving Spectrum src-10.10.110.223 PASS, ATT src-10.10.110.227 PASS, and ATT-wrong-src-10.10.110.223 FAIL through the same verdict function, with no SSH.
- The script contains the `<none>` Spectrum sentinel, ATT source `10.10.110.227`, Spectrum default-route src `10.10.110.223`, `ip route get`, `ip rule`, a concrete per-WAN expected-src verdict (ATT primary = `src 10.10.110.227`) with a distinctness guard, and NONE of the denylisted mutators (sudo/systemctl/tc-change/tee/nft/ip-mutators/system-path redirects).
- Operator-run stdout (both WANs + ip rule) is captured for Plan 03; ATT dev confirmed as the ATT uplink.
- `git status --porcelain -- src/wanctl/` is empty.
</verification>

<success_criteria>
- A committed read-only both-WAN egress-proof script exists, with the Spectrum `<none>` default-route branch and the ATT source-bound branch, a concrete per-WAN expected-src verdict (ATT primary = `src 10.10.110.227`, plus a distinctness guard), a `--self-test` that autonomously proves the verdict logic (incl. a negative wrong-src fixture), an allowlist read-only posture, and a `--print-commands` fallback.
- The operator runs it read-only on the live cake-shaper host, confirms ATT's `src 10.10.110.227` on the ATT uplink dev, and the stdout is captured for the provenance map.
- No src/wanctl/ file is modified and no production state is mutated.
</success_criteria>

<output>
Create `.planning/phases/238-rtt-provenance-verification-read-only-entry-gate/238-02-SUMMARY.md` when done
</output>
