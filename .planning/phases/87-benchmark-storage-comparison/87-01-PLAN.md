---
phase: 87-benchmark-storage-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/storage/schema.py
  - src/wanctl/storage/reader.py
  - src/wanctl/storage/__init__.py
  - src/wanctl/benchmark.py
  - tests/test_storage_schema.py
  - tests/test_benchmark.py
autonomous: true
requirements: [STOR-01, STOR-04]

must_haves:
  truths:
    - "Successful benchmark run auto-stores result to SQLite without --save flag"
    - "Stored result contains all BenchmarkResult fields plus wan_name, daemon_running, label metadata"
    - "query_benchmarks() returns stored results with optional time-range, WAN, limit, and ID filters"
    - "BENCHMARKS_SCHEMA creates benchmarks table with correct columns and indexes"
  artifacts:
    - path: "src/wanctl/storage/schema.py"
      provides: "BENCHMARKS_SCHEMA constant and updated create_tables()"
      contains: "CREATE TABLE IF NOT EXISTS benchmarks"
    - path: "src/wanctl/storage/reader.py"
      provides: "query_benchmarks() read-only query function"
      exports: ["query_benchmarks"]
    - path: "src/wanctl/benchmark.py"
      provides: "store_benchmark() writer, auto-store in main(), subparser skeleton"
      exports: ["store_benchmark"]
  key_links:
    - from: "src/wanctl/benchmark.py"
      to: "src/wanctl/storage/schema.py"
      via: "create_tables() call inside store_benchmark()"
      pattern: "create_tables\\(conn\\)"
    - from: "src/wanctl/benchmark.py"
      to: "src/wanctl/storage/writer.py"
      via: "DEFAULT_DB_PATH import"
      pattern: "from wanctl\\.storage\\.writer import DEFAULT_DB_PATH"
    - from: "src/wanctl/storage/reader.py"
      to: "benchmarks table"
      via: "query_benchmarks() SELECT"
      pattern: "SELECT.*FROM benchmarks"
---

<objective>
Add SQLite storage for benchmark results and wire auto-store into the existing wanctl-benchmark CLI run path.

Purpose: STOR-01 requires automatic persistence of every benchmark run. STOR-04 requires metadata (server, duration, daemon_running) for comparability. This plan creates the storage layer and query reader that Plan 02's compare/history subcommands will consume.

Output: BENCHMARKS_SCHEMA in schema.py, store_benchmark() in benchmark.py, query_benchmarks() in reader.py, auto-store wiring in main(), argparse subparser skeleton for backward-compatible bare invocation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-benchmark-storage-comparison/87-CONTEXT.md
@.planning/phases/87-benchmark-storage-comparison/87-RESEARCH.md
@.planning/phases/86-bufferbloat-benchmarking/86-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/wanctl/benchmark.py:
```python
@dataclass
class BenchmarkResult:
    download_grade: str
    upload_grade: str
    download_latency_avg: float
    download_latency_p50: float
    download_latency_p95: float
    download_latency_p99: float
    upload_latency_avg: float
    upload_latency_p50: float
    upload_latency_p95: float
    upload_latency_p99: float
    download_throughput: float
    upload_throughput: float
    baseline_rtt: float
    server: str
    duration: int
    timestamp: str

def check_daemon_running() -> tuple[bool, str]: ...
def create_parser() -> argparse.ArgumentParser: ...
def main() -> int: ...
```

From src/wanctl/storage/schema.py:
```python
METRICS_SCHEMA: str = "..."
ALERTS_SCHEMA: str = "..."
def create_tables(conn: sqlite3.Connection) -> None: ...
```

From src/wanctl/storage/reader.py:
```python
def query_metrics(db_path=DEFAULT_DB_PATH, start_ts=None, end_ts=None, metrics=None, wan=None, granularity=None) -> list[dict]: ...
def query_alerts(db_path=DEFAULT_DB_PATH, start_ts=None, end_ts=None, alert_type=None, wan=None) -> list[dict]: ...
```

From src/wanctl/storage/writer.py:
```python
DEFAULT_DB_PATH = Path("/var/lib/wanctl/metrics.db")
```

From src/wanctl/history.py:
```python
def parse_duration(value: str) -> timedelta: ...
def parse_timestamp(value: str) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BENCHMARKS_SCHEMA, store_benchmark(), query_benchmarks()</name>
  <files>src/wanctl/storage/schema.py, src/wanctl/storage/reader.py, src/wanctl/storage/__init__.py, src/wanctl/benchmark.py, tests/test_storage_schema.py, tests/test_benchmark.py</files>
  <behavior>
    - BENCHMARKS_SCHEMA creates benchmarks table with columns: id, timestamp (TEXT), wan_name, download_grade, upload_grade, download_latency_avg/p50/p95/p99, upload_latency_avg/p50/p95/p99, download_throughput, upload_throughput, baseline_rtt, server, duration, daemon_running (INTEGER), label (TEXT nullable)
    - BENCHMARKS_SCHEMA creates indexes idx_benchmarks_timestamp and idx_benchmarks_wan
    - create_tables() executes BENCHMARKS_SCHEMA alongside existing schemas
    - store_benchmark(result, wan_name, daemon_running, label=None, db_path=DEFAULT_DB_PATH) inserts one row, returns row ID or None on error
    - store_benchmark() calls create_tables() before insert (idempotent, handles pre-Phase-87 databases)
    - store_benchmark() creates db_path parent directory if needed
    - query_benchmarks(db_path, start_ts, end_ts, wan, limit, ids) returns list[dict] with WHERE 1=1 builder
    - query_benchmarks() uses read-only connection, returns [] if db missing or table missing
    - query_benchmarks() filters by ISO 8601 TEXT timestamp comparison (start_ts/end_ts are ISO strings)
    - query_benchmarks() supports ids filter (list[int]) for compare-by-ID
    - Existing create_tables tests still pass (idempotent)
  </behavior>
  <action>
  **storage/schema.py:** Add BENCHMARKS_SCHEMA constant after ALERTS_SCHEMA. Flat table with all BenchmarkResult fields as columns plus id (INTEGER PRIMARY KEY AUTOINCREMENT), wan_name (TEXT NOT NULL), daemon_running (INTEGER NOT NULL DEFAULT 0), label (TEXT nullable). timestamp is TEXT NOT NULL (ISO 8601 from BenchmarkResult.timestamp). Add CREATE INDEX IF NOT EXISTS idx_benchmarks_timestamp ON benchmarks(timestamp) and idx_benchmarks_wan ON benchmarks(wan_name, timestamp). In create_tables(), add conn.executescript(BENCHMARKS_SCHEMA) after the existing ALERTS_SCHEMA executescript.

  **storage/reader.py:** Add query_benchmarks() following the exact query_alerts() pattern: read-only connection via file:{db_path}?mode=ro, conn.row_factory = sqlite3.Row, WHERE 1=1 builder. Parameters: db_path (Path|str = DEFAULT_DB_PATH), start_ts (str|None -- ISO string), end_ts (str|None -- ISO string), wan (str|None), limit (int|None), ids (list[int]|None). If ids provided, add AND id IN (?,?,...). ORDER BY timestamp DESC. LIMIT if provided. Return [dict(row) for row in cursor.fetchall()]. Return [] on missing DB or OperationalError.

  **storage/__init__.py:** Add import of query_benchmarks from reader.py and BENCHMARKS_SCHEMA from schema.py. Add both to __all__.

  **benchmark.py:** Add store_benchmark() function near the top (after BenchmarkResult). Imports: sqlite3, logging, from wanctl.storage.schema import create_tables, from wanctl.storage.writer import DEFAULT_DB_PATH. Function signature: store_benchmark(result: BenchmarkResult, wan_name: str, daemon_running: bool, label: str | None = None, db_path: Path | str = DEFAULT_DB_PATH) -> int | None. Implementation: Path(db_path).parent.mkdir(parents=True, exist_ok=True), sqlite3.connect(), create_tables(conn), INSERT with dataclasses.asdict(result) field mapping, conn.commit(), return cursor.lastrowid. Catch Exception -> logger.warning + return None. Finally conn.close().

  **tests/test_storage_schema.py:** Add TestBenchmarksSchema class testing: BENCHMARKS_SCHEMA string contains correct CREATE TABLE, create_tables() creates benchmarks table with correct columns (PRAGMA table_info), indexes created (idx_benchmarks_timestamp, idx_benchmarks_wan), insert works, idempotent.

  **tests/test_benchmark.py:** Add TestStoreBenchmark class testing: store returns row ID, stores all fields correctly (query back), returns None on error, creates parent directory, handles label=None and label="before-fix". Add TestQueryBenchmarks class testing: returns stored results, filters by wan, filters by start_ts/end_ts, filters by ids, returns [] on missing DB, returns [] on missing table, limit works, ORDER BY timestamp DESC. Use tmp_path for real SQLite (not mocks).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_storage_schema.py tests/test_benchmark.py -x -v -k "TestBenchmarksSchema or TestStoreBenchmark or TestQueryBenchmarks" && .venv/bin/ruff check src/wanctl/storage/schema.py src/wanctl/storage/reader.py src/wanctl/benchmark.py && .venv/bin/mypy src/wanctl/storage/schema.py src/wanctl/storage/reader.py src/wanctl/benchmark.py</automated>
  </verify>
  <done>BENCHMARKS_SCHEMA creates table with all columns and indexes. store_benchmark() persists a BenchmarkResult row and returns its ID. query_benchmarks() reads back stored results with optional filters. All new and existing storage tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-store wiring in main() and argparse subparser skeleton</name>
  <files>src/wanctl/benchmark.py, tests/test_benchmark.py</files>
  <behavior>
    - Bare `wanctl-benchmark` (no subcommand) runs existing benchmark flow AND auto-stores result to SQLite afterward
    - Auto-store prints "Result stored (#N)" to stderr on success, silently continues on failure
    - --wan flag sets WAN name (default: auto-detect from hostname via socket.gethostname, "cake-spectrum" -> "spectrum", fallback "unknown")
    - --label flag sets optional label for the stored result
    - --db flag overrides database path (default: DEFAULT_DB_PATH)
    - args.command is None for bare invocation (backward compatible with all existing --server/--quick/--json/--no-color flags)
    - args.command == "compare" and args.command == "history" parsed but route to stub (Plan 02 implements handlers)
    - Existing TestMain and TestCreateParser tests still pass (backward compatibility)
  </behavior>
  <action>
  **benchmark.py create_parser():** Refactor to add subparsers. Add global flags to parent parser: existing --server, --quick, --json, --no-color PLUS new --wan (default=None, auto-detect), --label (default=None), --db (type=Path, default=DEFAULT_DB_PATH). Add sub = parser.add_subparsers(dest="command"). Add compare_p = sub.add_parser("compare", help="Compare two benchmark runs") with positional ids (nargs="*", type=int). Add history_p = sub.add_parser("history", help="List past benchmark runs") with --last (type=parse_duration from history.py), --from (dest="from_ts", type=parse_timestamp), --to (dest="to_ts", type=parse_timestamp), --wan (metavar="NAME"). Import parse_duration and parse_timestamp from wanctl.history at top of file.

  **benchmark.py detect_wan_name():** New helper function. Uses socket.gethostname(). If starts with "cake-", return hostname[5:]. Else return "unknown".

  **benchmark.py main():** After run_benchmark() succeeds and before output formatting, add auto-store block: wan_name = args.wan or detect_wan_name(). running, _ = check_daemon_running(). row_id = store_benchmark(result, wan_name=wan_name, daemon_running=running, label=args.label, db_path=args.db). If row_id is not None, print(f"Result stored (#{row_id})", file=sys.stderr). Route args.command == "compare" and "history" to placeholder functions that print "Not implemented yet" and return 0 (Plan 02 replaces these).

  **tests/test_benchmark.py:** Update TestCreateParser tests: bare invocation still works (args.command is None), new --wan/--label/--db flags parse correctly, "compare" and "history" subcommands parse. Add TestMainAutoStore class: mock store_benchmark, verify it's called after successful run with correct args (wan_name, daemon_running, label, db_path). Verify auto-store failure (store returns None) does not affect exit code 0. Verify stderr shows "Result stored" on success. Add TestDetectWanName class: hostname "cake-spectrum" -> "spectrum", hostname "cake-att" -> "att", hostname "myhost" -> "unknown".
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_benchmark.py -x -v && .venv/bin/ruff check src/wanctl/benchmark.py && .venv/bin/mypy src/wanctl/benchmark.py</automated>
  </verify>
  <done>Bare wanctl-benchmark auto-stores result to SQLite. --wan/--label/--db flags available. Subparser skeleton routes "compare" and "history" to stubs. All existing benchmark tests pass (backward compatible). detect_wan_name() extracts WAN from container hostname.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/test_benchmark.py tests/test_storage_schema.py -x -v
.venv/bin/ruff check src/wanctl/benchmark.py src/wanctl/storage/schema.py src/wanctl/storage/reader.py
.venv/bin/mypy src/wanctl/benchmark.py src/wanctl/storage/schema.py src/wanctl/storage/reader.py
</verification>

<success_criteria>
- BENCHMARKS_SCHEMA creates benchmarks table with all 19 columns and 2 indexes
- store_benchmark() persists a BenchmarkResult and returns row ID
- query_benchmarks() reads back results with optional filters
- Bare `wanctl-benchmark` auto-stores after successful run
- --wan, --label, --db flags work
- Argparse subparsers accept "compare" and "history" subcommands
- All existing 76+ benchmark tests still pass (backward compatible)
- All existing storage schema tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/87-benchmark-storage-comparison/87-01-SUMMARY.md`
</output>
