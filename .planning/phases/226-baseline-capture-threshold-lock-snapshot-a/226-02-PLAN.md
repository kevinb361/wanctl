---
phase: 226
plan: "02"
type: execute
title: "Snapshot A rollback anchor: phase226-snapshot-a.sh capturing Spectrum CAKE/qdisc + spectrum.yaml + bridge nft"
wave: 1
depends_on: []
requirements:
  - AB-01
  - SAFE-13
# REVIEW NOTE (codex HIGH#1): Snapshot A runs FIRST (Wave 1, no deps). Baseline load
# generation (226-01) is now Wave 2 and depends on this plan, because flent traffic
# mutates qdisc counters / controller state / logs and would contaminate the anchor.
files_modified:
  - scripts/phase226-snapshot-a.sh
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/
autonomous: true
must_haves:
  truths:
    - "Operator can run `scripts/phase226-snapshot-a.sh --output-dir <dir> --raw-dir <operator-private-dir>` and capture a Snapshot A rollback anchor of the live `920/18 besteffort wash` Spectrum state: redacted committable evidence + a MANIFEST under `--output-dir`, and unredacted restore sources (mode 0600, outside the git tree) under `--raw-dir` (AB-01)."
    - "The snapshot captures Spectrum CAKE state specifically — `configs/spectrum.yaml` (repo + deployed, with the deployed copy as the unredacted restore source), `tc -s qdisc` on spec-router and spec-modem, the cake-shaper `bridge qos` nft ruleset, the pre-deploy git commit SHA, and Spectrum `/health` — NOT steering-daemon state (this is a clean separate tool from the steering snapshot, keeping the two capture domains uncoupled under SAFE-13)."
    - "Capture is read-only on the target (`sudo -n cat` / `sudo -n tc -s qdisc` / `/health` only — no deploy, restart, CAKE-mode change, or `/etc/wanctl` write), the `--raw-dir` is refused if it falls inside the git working tree, and unredacted restore sources are mode 0600 and MUST NOT be committed."
    - "The MANIFEST records a targeted restore/revert sequence whose apply-command is byte-identical to the Phase 228 rollback path. Scope (review MEDIUM — restore-proof overclaim): the MANIFEST proves CONFIG-ARTIFACT EQUALITY and COMMAND IDENTITY only; it does NOT claim runtime qdisc restoration, sudo/install permission validity at rollback time, or service-reload behavior — those require a live drill that is explicitly out of bounds for Phase 226. The script never references controller-path source files (SAFE-13)."
  artifacts:
    - "scripts/phase226-snapshot-a.sh — read-only Spectrum CAKE Snapshot A capture wrapper (bash -n clean, shellcheck-clean), sibling to scripts/phase224-snapshot-a.sh."
    - ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/ — redacted Snapshot A evidence + MANIFEST.md (raw restore sources live operator-private outside git)."
---

<objective>
Build the AB-01 Snapshot A rollback anchor for the Spectrum CAKE deployment, so the pre-A/B
`920/18 besteffort wash` state is restorable to the exact byte-for-byte config before any Phase 227
candidate deploy. Deliver `scripts/phase226-snapshot-a.sh` as a clean sibling of the proven
`scripts/phase224-snapshot-a.sh` — same redacted-evidence + operator-private-raw + read-only-on-target
shape — but capturing Spectrum CAKE state (`configs/spectrum.yaml`, `tc -s qdisc` on spec-router +
spec-modem, `bridge qos` nft rules, pre-deploy git ref, `/health`) instead of steering-daemon state.

Purpose: AB-01 deliverable; the rollback anchor Phase 228 restores from. No production mutation.
Output: `scripts/phase226-snapshot-a.sh` and a redacted `evidence/snapshot-a/` tree with MANIFEST;
unredacted restore sources written mode 0600 under an operator-private `--raw-dir` outside git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
@configs/spectrum.yaml

`scripts/phase224-snapshot-a.sh` is the structural template (D-09): it splits redacted committable
evidence (under `--output-dir`) from unredacted operator-private restore sources (mode 0600 under
`--raw-dir`, refused if inside the git tree via `repo_contains_path`), captures a pre-deploy git ref
(`commit_sha`, `tree_sha`), redacts secrets out of YAML via a `redact_yaml` python filter, captures
`/health`, computes sha256 of every artifact, writes a MANIFEST.md with a numbered "Targeted Revert
Sequence", and ends with a required-artifact assertion loop. The 226 variant SWAPS the steering
capture (`phase213-steering-snapshot.sh` delegation, `/etc/wanctl/steering.yaml`, steering state
JSON) for Spectrum CAKE capture: deployed + repo `configs/spectrum.yaml`, `tc -s qdisc` on
`spec-router` and `spec-modem`, and the cake-shaper `bridge qos` nft ruleset
(`deploy/nftables/bridge-qos.nft` repo copy + live `sudo -n nft list table bridge qos`). The
candidate flip in Phase 227 is a one-line `configs/spectrum.yaml` change
(`diffserv: besteffort` → `diffserv4`, `allow_wash` unchanged), so the restore target is the
deployed `configs/spectrum.yaml` — the unredacted deployed copy is the raw restore source. The
restore apply-command in the MANIFEST must match what Phase 228 (and 226-04's dry-run proof) will run.
</context>

<task id="226-02-1" type="execute">
<name>Task 1: Write the Spectrum CAKE Snapshot A capture wrapper</name>
<title>Write the Spectrum CAKE Snapshot A capture wrapper</title>
<files>scripts/phase226-snapshot-a.sh</files>
<action>
Create `scripts/phase226-snapshot-a.sh` by adapting `scripts/phase224-snapshot-a.sh`:
`set -euo pipefail`; keep the `require_command` (curl, git, python3, sha256sum, ssh, plus nft via
remote), `repo_contains_path` (refuse `--raw-dir` inside the git tree — resolve BOTH the repo root and
the candidate `--raw-dir` with `realpath`/`python3 -c "import os;print(os.path.realpath(...))"` BEFORE
the containment test so a symlinked raw-dir pointing back inside the tree is still refused — review
LOW finding), `redact_yaml`, and
`json_field` helpers verbatim in shape. Args: `--output-dir <dir>` (required, refuse non-empty),
`--raw-dir <operator-private-dir>` (required, refuse non-empty, refuse inside git tree, chmod 700),
`--ssh-host` (default `cake-shaper`), `--health-url` (default `http://10.10.110.223:9101/health`).
REMOVE the steering delegation (`phase213-steering-snapshot.sh`) and steering YAML/state capture.
INSTEAD capture, read-only via `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n <read-cmd>"`:
- Redacted committable (under `--output-dir`): `repo-spectrum.redacted.yaml` (redact_yaml of repo `configs/spectrum.yaml`), `deployed-spectrum.redacted.yaml` (redact_yaml of `sudo -n cat /etc/wanctl/spectrum.yaml`), `tc-qdisc-spec-router.txt` (`sudo -n tc -s qdisc show dev spec-router`), `tc-qdisc-spec-modem.txt` (`sudo -n tc -s qdisc show dev spec-modem`), `bridge-qos.live.txt` (`sudo -n nft list table bridge qos`), `repo-bridge-qos.nft` (copy of `deploy/nftables/bridge-qos.nft`), `pre-deploy-git-ref.txt` (`commit_sha` + `tree_sha` of `configs/` or `HEAD:configs/spectrum.yaml`), `snapshot-a-health.bound.redacted.json` (`curl -fsS --max-time 5 <health-url>`).
- Unredacted operator-private restore sources (under `--raw-dir`, mode 0600): `deployed-spectrum.yaml` (raw `sudo -n cat /etc/wanctl/spectrum.yaml`) and `deployed-spectrum.source-path.txt` (the literal `/etc/wanctl/spectrum.yaml` restore destination).
Compute a `config_equality` verdict comparing the redacted deployed vs repo spectrum.yaml sha256
(equal/diff). Write `MANIFEST.md` (Captured UTC, Source Posture = read-only/no-mutation, Health
Source URL, Deployed/Repo version, Pre-Deploy Git Ref, Deployed Config Equality verdict + both
sha256, Raw Artifact Restore Path with `operator_private: true` / `committed: false`, Artifacts list,
and a numbered "Targeted Revert Sequence" whose restore step installs `<raw-dir>/deployed-spectrum.yaml`
to `/etc/wanctl/spectrum.yaml` via `ssh <host> sudo -n install -m 0640 -o root -g wanctl /dev/stdin
/etc/wanctl/spectrum.yaml < <raw-dir>/deployed-spectrum.yaml` — this exact apply-command is the
Phase 228 rollback path). End with a required-artifact assertion loop that exits non-zero on any
missing/empty file. The script MUST NOT deploy, restart, mutate target state, change CAKE mode, run
any `tc add/del/replace` or nft `add/delete/flush/-f`, or reference controller-path source files.
</action>
<read_first>
- scripts/phase224-snapshot-a.sh
- scripts/phase224-rollback.sh
- configs/spectrum.yaml
- deploy/nftables/bridge-qos.nft
- .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
</read_first>
<acceptance_criteria>
- `scripts/phase226-snapshot-a.sh` exists and `bash -n scripts/phase226-snapshot-a.sh` exits 0.
- `shellcheck scripts/phase226-snapshot-a.sh` reports no errors (justified inline warnings acceptable).
- Mutation check (command-aware, executed-path only — review HIGH#3): the snapshot script issues NO
  target-mutating command. Verify the remote command surface is read-only by checking that every
  `ssh ... sudo -n` invocation in the executed code path uses only read verbs (`cat`, `tc -s qdisc show`,
  `nft list`, `curl`): `grep -nE 'ssh[^#]*sudo -n' scripts/phase226-snapshot-a.sh | grep -vE 'cat|tc -s qdisc show|nft list|curl'` returns no lines.
  The Phase 228 `install -m 0640 ...` apply-command appears ONLY as MANIFEST revert-sequence TEXT
  (emitted via heredoc/printf into the MANIFEST), never as an executed statement — confirm with
  `grep -nE '^[[:space:]]*(ssh[^#]*)?install -m' scripts/phase226-snapshot-a.sh` returning no lines that
  are outside a MANIFEST-writing heredoc/printf (i.e. no bare executed `install`/`ssh ... install`).
- `grep -E '\b(wan_controller|queue_controller|cake_signal|alert_engine|fusion)\b' scripts/phase226-snapshot-a.sh` returns no lines (SAFE-13).
- Script refuses a `--raw-dir` inside the git working tree (reuses `repo_contains_path` logic) and chmod 700 the raw dir; raw artifacts are written mode 0600.
- Script captures repo+deployed `spectrum.yaml`, `tc -s qdisc` for spec-router AND spec-modem, the live `bridge qos` nft ruleset, a pre-deploy git ref, and `/health`; it does NOT delegate to `phase213-steering-snapshot.sh` and does NOT capture `/etc/wanctl/steering.yaml`.
- `MANIFEST.md` contains a "Targeted Revert Sequence" whose restore apply-command installs `deployed-spectrum.yaml` to `/etc/wanctl/spectrum.yaml` (the Phase 228 rollback path).
- Script contains a required-artifact assertion loop that exits non-zero on any missing/empty output file.
</acceptance_criteria>
<verify>
  <automated>bash -n scripts/phase226-snapshot-a.sh && shellcheck scripts/phase226-snapshot-a.sh && ! grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion' scripts/phase226-snapshot-a.sh</automated>
</verify>
<done>
Spectrum CAKE Snapshot A wrapper exists, syntax- and shellcheck-clean, mutation-free and controller-path-free, splits redacted committable evidence from operator-private raw restore sources (0600, outside git), captures spectrum.yaml + tc qdisc + bridge nft + git ref + /health, and records a revert sequence matching the Phase 228 rollback path.
</done>
</task>

<task id="226-02-2" type="execute">
<name>Task 2: Run Snapshot A and commit the redacted evidence tree</name>
<title>Run Snapshot A and commit the redacted evidence tree</title>
<files>.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/</files>
<action>
Execute `scripts/phase226-snapshot-a.sh --output-dir
.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a --raw-dir
<operator-private-dir-outside-git>` against the live `cake-shaper` host. Confirm the run is
read-only (no service change, no CAKE-mode change) and that the `config_equality` verdict in the
MANIFEST reflects the true deployed-vs-repo `configs/spectrum.yaml` relationship (expected `equal`
if the deployed config matches the committed `920/18 besteffort wash`; if `diff`, record the
deployed config as the authoritative restore source and note the drift in the MANIFEST). Commit ONLY
the redacted `evidence/snapshot-a/` tree (redacted spectrum.yaml copies, tc/nft/health/git-ref
evidence, MANIFEST.md). Do NOT commit the `--raw-dir` contents — verify the operator-private raw
restore sources resolve to a path OUTSIDE the repo and confirm `git status` shows no raw artifacts
staged. Record the operator-private raw-dir path only as a MANIFEST reference (not its contents).
</action>
<read_first>
- scripts/phase226-snapshot-a.sh
- scripts/phase224-snapshot-a.sh
- configs/spectrum.yaml
</read_first>
<acceptance_criteria>
- `evidence/snapshot-a/MANIFEST.md` exists and records Source Posture = read-only, a Deployed Config Equality verdict + both sha256 values, and a Raw Artifact Restore Path marked `operator_private: true` / `committed: false`.
- `evidence/snapshot-a/` contains redacted `spectrum.yaml` copies, `tc-qdisc-spec-router.txt`, `tc-qdisc-spec-modem.txt`, `bridge-qos.live.txt`, `pre-deploy-git-ref.txt`, and `snapshot-a-health.bound.redacted.json`.
- `git status --porcelain` shows no files under the operator-private `--raw-dir` staged or tracked; the `--raw-dir` was `realpath`-resolved before the containment test so a symlink back into the tree is also refused (raw restore sources are outside the git tree).
- Redaction validator (review HIGH#3 — `redact_yaml` REPLACES secret values with `REDACTED`, so the
  redacted YAML legitimately contains the KEY token `password:`/`secret:`/`token:`; a bare
  `grep -i password` would false-fail its own gate). Assert no UNREDACTED secret VALUE remains: every
  secret-bearing key is followed by `REDACTED` (or empty) — `grep -inE '^[[:space:]]*(password|secret|token|api[_-]?key)[[:space:]]*:' evidence/snapshot-a/*.yaml | grep -vE ':[[:space:]]*("?REDACTED"?|""|''|null)[[:space:]]*$'` returns no lines (any secret key with a real value would be flagged; `REDACTED` placeholders pass).
</acceptance_criteria>
<verify>
  <automated>test -s .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/MANIFEST.md && ! grep -inE '^[[:space:]]*(password|secret|token|api[_-]?key)[[:space:]]*:' .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/*.yaml | grep -vE ':[[:space:]]*("?REDACTED"?|""|null)[[:space:]]*$'</automated>
</verify>
<done>
Snapshot A captured read-only against cake-shaper; the redacted evidence tree + MANIFEST is committed; unredacted raw restore sources live operator-private outside git and are not committed; the deployed-vs-repo config equality verdict is recorded.
</done>
</task>

<verification>
- `bash -n` and `shellcheck` on `scripts/phase226-snapshot-a.sh` are clean.
- Mutation-grep (executed code path) and controller-path-grep both return empty.
- The committed `evidence/snapshot-a/` tree is redacted and contains the MANIFEST with the revert sequence; no raw restore sources are committed.
- MANIFEST restore apply-command targets `/etc/wanctl/spectrum.yaml` (Phase 228 rollback path).
</verification>

<success_criteria>
- AB-01 satisfied: a Snapshot A rollback anchor (Spectrum config + production CAKE/qdisc state) is captured before any production change, restorable to the exact pre-A/B `920/18 besteffort wash` state.
- D-09 satisfied: clean separate Spectrum-CAKE capture tool mirroring the phase224 redacted/raw/read-only shape; raw restore sources operator-private and uncommitted.
- SAFE-13 preserved: no controller-path reference; read-only on target; no CAKE-mode change.
- The MANIFEST restore apply-command is byte-identical to the Phase 228 rollback path (feeds 226-04's dry-run proof).
</success_criteria>

<output>
Create `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-02-SUMMARY.md` when done.
</output>
