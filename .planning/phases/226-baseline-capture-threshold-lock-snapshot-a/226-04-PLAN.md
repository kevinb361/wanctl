---
phase: 226
plan: "04"
type: execute
title: "Dry-run restore proof (no CAKE-mode change) + SAFE-13 phase-boundary verification"
wave: 2
depends_on:
  - "226-02"
requirements:
  - AB-01
  - SAFE-13
files_modified:
  - scripts/phase226-restore.sh
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/restore-proof/
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json
autonomous: true
must_haves:
  truths:
    - "Operator can run `scripts/phase226-restore.sh --snapshot <dir> --raw-dir <operator-private-dir> --dry-run` and prove the Snapshot A restore artifact reproduces the current `configs/spectrum.yaml` BYTE-FOR-BYTE (sha256 match) AND that the dry-run apply-command printed is byte-identical to the Phase 228 rollback path — WITHOUT mutating production CAKE mode (D-10, respecting success-criterion 4 and the SAFE-13 boundary)."
    - "The restore proof is dry-run only: no `diffserv4` flip, no live restore drill, no service restart, no CAKE-mode change — a live flip-then-restore drill was explicitly REJECTED as an out-of-bounds candidate deploy for this phase (D-10)."
    - "SAFE-13 is verified at the phase boundary via `scripts/phase225-safe13-boundary-check.sh --anchor v1.48`: controller-path source (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion) is zero-diff vs the v1.48 close, and ATT config is byte-identical — evidence written to a committed JSON."
    - "No production change is deployed in this phase (success-criterion 4): baseline + anchor + locked thresholds + dry-run restore proof only; the candidate deploy is reserved for Phase 227."
  artifacts:
    - "scripts/phase226-restore.sh — dry-run-capable Spectrum CAKE restore wrapper (mirrors scripts/phase224-rollback.sh --dry-run), proving the restore path without mutation."
    - ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/restore-proof/ — byte-for-byte restore-equivalence proof + the dry-run apply-command transcript."
    - ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json — SAFE-13 controller-path zero-diff + ATT byte-identical evidence at the 226 boundary."
---

<objective>
Prove the Snapshot A rollback anchor actually restores the pre-A/B `920/18 besteffort wash` state
WITHOUT mutating production CAKE mode (D-10), and verify the SAFE-13 controller-path freeze holds at
the Phase 226 boundary. Deliver a dry-run-capable `scripts/phase226-restore.sh` (mirroring
`scripts/phase224-rollback.sh --dry-run`) that asserts the raw restore source reproduces
`configs/spectrum.yaml` byte-for-byte and prints an apply-command identical to the Phase 228
rollback path, plus a committed SAFE-13 boundary-check JSON from the existing
`scripts/phase225-safe13-boundary-check.sh`.

Purpose: closes AB-01 (restore is proven, not just captured) and SAFE-13 at the phase boundary.
Output: `scripts/phase226-restore.sh`, a `restore-proof/` evidence dir, and `safe13-boundary-check.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
@scripts/phase224-rollback.sh
@scripts/phase225-safe13-boundary-check.sh

This plan depends on 226-02 (the Snapshot A capture: `evidence/snapshot-a/MANIFEST.md` + the
operator-private raw `deployed-spectrum.yaml`). `scripts/phase224-rollback.sh` is the precedent:
it parses the MANIFEST, checks raw-artifact sha256, and `--dry-run` prints a numbered restore plan
WITHOUT mutating git or the target. The 226 restore mirrors that but for Spectrum CAKE
(`/etc/wanctl/spectrum.yaml` restore target) and the D-10 stance is STRICT: dry-run ONLY for this
phase — proving (a) the raw restore source sha256 equals the current deployed/repo
`configs/spectrum.yaml`, and (b) the printed apply-command (`ssh <host> sudo -n install -m 0640
-o root -g wanctl /dev/stdin /etc/wanctl/spectrum.yaml < <raw-dir>/deployed-spectrum.yaml`) is
byte-identical to the Phase 228 rollback path recorded in the 226-02 MANIFEST. A LIVE flip to
`diffserv4` then restore is OUT OF BOUNDS (success-criterion 4). `scripts/phase225-safe13-boundary-check.sh`
already does the controller-path zero-diff vs `v1.48` (per-file hashes, fail-closed on add/delete)
and ATT byte-identical check, writing JSON evidence — reuse it verbatim with `--anchor v1.48` and an
`--out` under the 226 evidence dir.
</context>

<task id="226-04-1" type="execute">
<name>Task 1: Write the dry-run restore proof wrapper and capture the byte-for-byte proof</name>
<title>Write the dry-run restore proof wrapper and capture the byte-for-byte proof</title>
<files>scripts/phase226-restore.sh, .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/restore-proof/</files>
<action>
Create `scripts/phase226-restore.sh` by adapting `scripts/phase224-rollback.sh` for Spectrum CAKE:
`set -euo pipefail`; `require_command` git, python3, sha256sum, ssh; args `--snapshot <dir>`
(required, must contain MANIFEST.md), `--raw-dir <operator-private-dir>` (required, must contain the
unredacted `deployed-spectrum.yaml`), `--ssh-host` (default `cake-shaper`), and `--dry-run`. Default
this phase's invocation to `--dry-run`; the non-dry-run path may exist for Phase 228 reuse but MUST
be guarded behind an explicit `--apply` (and even then refuse a CAKE-mode change — restore target is
`/etc/wanctl/spectrum.yaml` content only, never a `diffserv` flip). In `--dry-run`, the script MUST:
(1) compute sha256 of the raw `deployed-spectrum.yaml` restore source and compare it to the current
repo `configs/spectrum.yaml` AND the deployed `/etc/wanctl/spectrum.yaml` (read-only `sudo -n cat`),
asserting byte-for-byte equivalence and reporting `RESTORE_EQUIVALENCE=equal|diff`; (2) print the
numbered restore plan whose apply-command (`ssh <host> sudo -n install -m 0640 -o root -g wanctl
/dev/stdin /etc/wanctl/spectrum.yaml < <raw-dir>/deployed-spectrum.yaml`) is byte-identical to the
"Targeted Revert Sequence" apply-command in the 226-02 MANIFEST, and assert that identity
(`APPLY_COMMAND_MATCHES_MANIFEST=true`); (3) write the proof transcript to
`evidence/restore-proof/restore-dry-run.txt` and the sha256 comparison to
`evidence/restore-proof/restore-equivalence.json`. The dry-run path MUST NOT mutate git, the target
host, CAKE mode, or any service, and MUST NOT reference controller-path source files.
</action>
<read_first>
- scripts/phase224-rollback.sh
- scripts/phase226-snapshot-a.sh
- configs/spectrum.yaml
- .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/snapshot-a/MANIFEST.md
</read_first>
<acceptance_criteria>
- `scripts/phase226-restore.sh` exists and `bash -n scripts/phase226-restore.sh` exits 0.
- `shellcheck scripts/phase226-restore.sh` reports no errors (justified inline warnings acceptable).
- `scripts/phase226-restore.sh --snapshot <dir> --raw-dir <dir> --dry-run` exits 0 and prints `RESTORE_EQUIVALENCE=equal` plus `APPLY_COMMAND_MATCHES_MANIFEST=true` (against a real Snapshot A from 226-02).
- Mutation check (comment-safe, executed code path): `grep -vE '^[[:space:]]*#' scripts/phase226-restore.sh | grep -E 'git checkout|systemctl[[:space:]]+(restart|start|stop)|deploy\.sh|diffserv|tc[[:space:]].*(add|del|replace)|nft[[:space:]]+(add|delete|flush|-f)'` returns no lines reachable in the `--dry-run` path (any non-dry-run apply lines are gated behind an explicit `--apply` flag, never executed without it).
- `grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion' scripts/phase226-restore.sh` returns no lines (SAFE-13).
- `evidence/restore-proof/restore-equivalence.json` records the raw-source sha256, the repo+deployed `spectrum.yaml` sha256, and the `equal` verdict; `restore-dry-run.txt` contains the numbered restore plan with the install apply-command.
- No `diffserv4` flip or live restore drill is performed (D-10); the script's default invocation is `--dry-run`.
</acceptance_criteria>
<verify>
  <automated>bash -n scripts/phase226-restore.sh && shellcheck scripts/phase226-restore.sh && ! grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion' scripts/phase226-restore.sh</automated>
</verify>
<done>
A dry-run restore wrapper proves the Snapshot A raw source reproduces `configs/spectrum.yaml` byte-for-byte and that its apply-command matches the Phase 228 rollback path recorded in the 226-02 MANIFEST — with no CAKE-mode change, no live drill, no git/target/service mutation, and no controller-path reference.
</done>
</task>

<task id="226-04-2" type="execute">
<name>Task 2: Run the SAFE-13 phase-boundary check and commit the evidence</name>
<title>Run the SAFE-13 phase-boundary check and commit the evidence</title>
<files>.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json</files>
<action>
Run `scripts/phase225-safe13-boundary-check.sh --anchor v1.48 --out
.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json`
verbatim (it is the ready-made controller-path zero-diff + ATT byte-identical check; read-only git
inspection, no worktree mutation). Confirm the emitted JSON shows controller-path source
(`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion)
zero-diff vs the `v1.48` close (per-file hash match, fail-closed on add/delete) and ATT config
byte-identical. If the boundary check reports ANY controller-path drift, STOP and surface it (the
phase cannot close SAFE-13 with drift) rather than editing source. Commit the `restore-proof/`
evidence dir and the `safe13-boundary-check.json` to the phase evidence tree (public-safe: no
secrets). Record in the SUMMARY that no production change was deployed in this phase
(success-criterion 4: baseline + anchor + locked thresholds + dry-run restore proof only).
</action>
<read_first>
- scripts/phase225-safe13-boundary-check.sh
- .planning/REQUIREMENTS.md
- .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
</read_first>
<acceptance_criteria>
- `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json` exists and `python3 -m json.tool <file>` exits 0.
- The JSON evidence indicates controller-path zero-diff vs `v1.48` (no per-file hash mismatch, no added/deleted controller files) and ATT config byte-identical.
- The SAFE-13 check ran read-only: `git status --porcelain` shows no source-file modifications introduced by the check (only the evidence JSON + plan artifacts).
- `grep -iE 'password|secret|token' evidence/safe13-boundary-check.json evidence/restore-proof/*` returns no lines.
- The committed evidence confirms no candidate `diffserv4 wash` was deployed in this phase (success-criterion 4).
</acceptance_criteria>
<verify>
  <automated>python3 -m json.tool .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/evidence/safe13-boundary-check.json</automated>
</verify>
<done>
SAFE-13 is verified at the 226 phase boundary (controller-path zero-diff vs v1.48, ATT byte-identical) with committed JSON evidence, the restore proof is committed, and the record confirms no production CAKE-mode change was deployed this phase.
</done>
</task>

<verification>
- `bash -n` and `shellcheck` on `scripts/phase226-restore.sh` are clean.
- `--dry-run` prints `RESTORE_EQUIVALENCE=equal` and `APPLY_COMMAND_MATCHES_MANIFEST=true` with no mutation.
- `safe13-boundary-check.json` shows controller-path zero-diff vs v1.48 and ATT byte-identical.
- No CAKE-mode change, no live restore drill, no service restart anywhere in the phase (success-criterion 4 + D-10).
</verification>

<success_criteria>
- AB-01 restore proven: the Snapshot A artifact reproduces `configs/spectrum.yaml` byte-for-byte and its apply-command matches the Phase 228 rollback path — dry-run only (D-10).
- Success-criterion 4 satisfied: no candidate `diffserv4 wash` deployed this phase.
- SAFE-13 verified at the phase boundary: controller-path zero-diff vs v1.48 close; ATT config byte-identical.
</success_criteria>

<output>
Create `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-04-SUMMARY.md` when done.
</output>
