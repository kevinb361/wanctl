---
phase: 226
plan: "03"
type: execute
title: "GATE-01 threshold lock: phase226-thresholds.json (pre-registered accept/rollback gates)"
wave: 1
depends_on: []
requirements:
  - GATE-01
  - SAFE-13
files_modified:
  - scripts/phase226-thresholds.json
  - .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md
autonomous: true
must_haves:
  truths:
    - "A committed `scripts/phase226-thresholds.json` (sibling to `scripts/phase206-thresholds.json`, schema-versioned, single-source-of-truth `_notes`, no numeric literals duplicated into prose) locks the GATE-01 accept/rollback thresholds before any candidate deploy: RRUL p99 regression tolerance, daemon restart-rate gate, pressure-state transition-rate gate, upload-stability gate, and the tin-separation rule (GATE-01, pre-registration discipline)."
    - "The inherited numeric gates match `phase206-thresholds.json`: `RRUL_P99_REGRESSION_PCT=5.0` (D-01), `RESTART_RATE_INCREASE_PCT=10` (D-02), `TRANSITION_RATE_INCREASE_PCT=10` (D-03); and the new gates are added: UL-stability two-arm gate (D-04: UL p99 regression <= 5% AND no increase in floor-hit-cycles / SOFT_RED dwell vs baseline) and the tin-separation occupancy+delay-gap definition (D-05) with a magnitude RULE frozen at plan time (D-06: non-BE vs BE per-tin delay gap must exceed the Snapshot A baseline 3-run spread)."
    - "The tin-separation magnitude is locked as a DECISION RULE at plan time (un-gameable, pre-registered) while its concrete noise-band CONSTANT is explicitly marked as derived-from-baseline-spread (filled by the Phase 226-01 `tin_queue_delay_spread_ms`), NOT guessed before path noise is known — this is pre-registration, not reverse-fitting (D-06)."
    - "The JSON is schema-eval-compatible with the Phase 228 gate-check (mirrors the `phase206-gate-check.py` / `phase224-gate-eval.py` consumption shape), and no controller-path source is touched (SAFE-13)."
  artifacts:
    - "scripts/phase226-thresholds.json — versioned threshold-lock JSON, eval-compatible with the Phase 228 gate-check."
    - ".planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md — pre-registration record: each gate's source decision (D-01..D-06), inherited-vs-new provenance, and the tin-separation rule (with the noise-band constant explicitly marked derived-from-baseline)."
---

<objective>
Lock the GATE-01 accept/rollback thresholds in a committed artifact at plan time, so the Phase 228
verdict reads fixed numbers it cannot reverse-fit. Deliver `scripts/phase226-thresholds.json` as a
schema-versioned sibling of `scripts/phase206-thresholds.json`: inherit the v1.44 RRUL p99 (5%),
restart-rate (+10%), and transition-rate (+10%) gates verbatim, add the new UL-stability two-arm gate
(D-04) and the tin-separation occupancy+delay-gap definition (D-05) whose magnitude is a frozen RULE
with a baseline-derived noise-band constant (D-06). Record the pre-registration provenance in
`GATE-01-THRESHOLDS.md`.

Purpose: GATE-01 deliverable; the pre-registered, un-reverse-fittable gate spec for Phase 228.
Output: `scripts/phase226-thresholds.json` + `GATE-01-THRESHOLDS.md` provenance record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
@scripts/phase206-thresholds.json

`scripts/phase206-thresholds.json` is the exact format to mirror (D-01..D-03 inherited):
`{"thresholds_schema_version": 1, "RRUL_P99_REGRESSION_PCT": 5.0, "RESTART_RATE_INCREASE_PCT": 10.0,
"TRANSITION_RATE_INCREASE_PCT": 10.0, "_notes": "<single source of truth; do not duplicate numeric
literals in documentation>"}`. `scripts/phase206-gate-check.py` and `scripts/phase224-gate-eval.py`
are the eval-consumption precedent — the new JSON must remain readable by that shape (named numeric
keys + structured sub-objects, not free prose). The new gates are NOT in phase206: D-04 (UL
stability — two required arms: UL p99 regression <= 5% AND no increase in floor-hit-cycles /
SOFT_RED dwell vs baseline) and D-05/D-06 (tin separation — occupancy AND delay-gap both required;
magnitude RULE = non-BE-vs-BE per-tin delay gap must exceed the Snapshot A baseline 3-run spread).
The D-06 noise-band CONSTANT comes from the Phase 226-01 `baseline-summary.json`
`tin_queue_delay_spread_ms` — at plan time it is recorded as a derived placeholder/reference
(e.g. `"source": "phase226 baseline-summary.json tin_queue_delay_spread_ms"`), NOT a guessed number;
the RULE is frozen now, only the constant is data-filled. Per the v1.44/v1.47 pre-registration
discipline, numeric literals live ONLY in the JSON, never duplicated into the markdown prose.
</context>

<task id="226-03-1" type="execute">
<name>Task 1: Author the schema-versioned phase226-thresholds.json gate-lock</name>
<title>Author the schema-versioned phase226-thresholds.json gate-lock</title>
<files>scripts/phase226-thresholds.json</files>
<action>
Create `scripts/phase226-thresholds.json` mirroring `scripts/phase206-thresholds.json` structure
(schema-versioned, `_notes` single-source-of-truth). Inherit verbatim: `thresholds_schema_version`
(bump or keep `1` consistent with phase206 — match the version the Phase 228 gate-check expects),
`RRUL_P99_REGRESSION_PCT: 5.0` (D-01, the v1.44 rollback gate), `RESTART_RATE_INCREASE_PCT: 10.0`
(D-02), `TRANSITION_RATE_INCREASE_PCT: 10.0` (D-03). ADD a structured `UL_STABILITY` object (D-04)
with both required arms named as machine-readable keys: `UL_P99_REGRESSION_PCT: 5.0` and a
floor-churn arm (e.g. `FLOOR_CHURN_NO_INCREASE: true` with sub-keys naming the baseline-comparison
metrics `floor_hit_cycles` and `soft_red_dwell` — candidate fails if either increases vs baseline).
ADD a structured `TIN_SEPARATION` object (D-05/D-06): an `OCCUPANCY_REQUIRED: true` arm (marked
traffic lands in a non-BE tin: non-zero packets/sojourn) AND a `DELAY_GAP_REQUIRED: true` arm (non-BE
tin shows measurably lower per-tin queue delay/backlog than BE under load), PLUS a
`MAGNITUDE_RULE` describing the frozen rule ("non-BE vs BE per-tin delay gap must exceed the Snapshot
A baseline 3-run spread") and a `NOISE_BAND_MS` field set to a derived placeholder/reference object
(e.g. `{"value": null, "source": "phase226 baseline-summary.json tin_queue_delay_spread_ms",
"derivation": "max non-BE-vs-BE per-tin delay gap noise band from Snapshot A 3-run spread"}`) — NOT
a guessed number. Write `_notes` declaring this file the single source of truth, citing
GATE-01-THRESHOLDS.md as the prose record that must NOT duplicate numeric literals, and noting the
pre-registration discipline (locked before any candidate deploy). Validate it parses as JSON.
</action>
<read_first>
- scripts/phase206-thresholds.json
- scripts/phase206-gate-check.py
- scripts/phase224-gate-eval.py
- .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
</read_first>
<acceptance_criteria>
- `scripts/phase226-thresholds.json` exists and `python3 -m json.tool scripts/phase226-thresholds.json` exits 0 (valid JSON).
- Inherited keys present with exact values: `RRUL_P99_REGRESSION_PCT == 5.0`, `RESTART_RATE_INCREASE_PCT == 10.0`, `TRANSITION_RATE_INCREASE_PCT == 10.0` (verify: `python3 -c "import json;d=json.load(open('scripts/phase226-thresholds.json'));assert d['RRUL_P99_REGRESSION_PCT']==5.0 and d['RESTART_RATE_INCREASE_PCT']==10.0 and d['TRANSITION_RATE_INCREASE_PCT']==10.0;print('ok')"`).
- A `UL_STABILITY` object exists with a UL p99 regression arm (5.0) AND a floor-churn arm referencing `floor_hit_cycles` and `soft_red_dwell` (D-04, both arms).
- A `TIN_SEPARATION` object exists with both `OCCUPANCY_REQUIRED` and `DELAY_GAP_REQUIRED` arms (D-05) AND a `MAGNITUDE_RULE` plus a `NOISE_BAND_MS` whose value is null/placeholder with a `source` citing the phase226 baseline spread (D-06 — rule frozen, constant derived, not guessed).
- A `thresholds_schema_version` key is present and `_notes` declares single-source-of-truth + pre-registration; `python3 -c "import json;json.load(open('scripts/phase206-gate-check.py'.replace('gate-check.py','thresholds.json')))"` is not required, but the JSON key shape stays eval-compatible with `phase206-gate-check.py` (named keys + nested objects).
- `grep -E 'wan_controller|queue_controller|cake_signal|alert_engine|fusion' scripts/phase226-thresholds.json` returns no lines (SAFE-13; the JSON does not touch controller source).
</acceptance_criteria>
<verify>
  <automated>python3 -m json.tool scripts/phase226-thresholds.json && python3 -c "import json;d=json.load(open('scripts/phase226-thresholds.json'));assert d['RRUL_P99_REGRESSION_PCT']==5.0 and d['RESTART_RATE_INCREASE_PCT']==10.0 and d['TRANSITION_RATE_INCREASE_PCT']==10.0 and 'UL_STABILITY' in d and 'TIN_SEPARATION' in d;print('ok')"</automated>
</verify>
<done>
`phase226-thresholds.json` exists, is valid JSON, inherits the v1.44 RRUL/restart/transition gates verbatim, adds the D-04 UL two-arm stability gate and the D-05/D-06 tin-separation occupancy+delay-gap definition with a frozen magnitude rule and a derived (not guessed) noise-band constant, schema-versioned and eval-compatible with the Phase 228 gate-check.
</done>
</task>

<task id="226-03-2" type="execute">
<name>Task 2: Write the GATE-01 pre-registration provenance record</name>
<title>Write the GATE-01 pre-registration provenance record</title>
<files>.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md</files>
<action>
Create `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md` as
the human-readable pre-registration record. For EACH gate, state: the source decision ID (D-01..D-06),
whether it is INHERITED (cite `scripts/phase206-thresholds.json` and the v1.44 rollback-gate lineage)
or NEW (D-04 UL stability, D-05/D-06 tin separation), and what the gate evaluates — but DO NOT
duplicate the numeric literals (point to `scripts/phase226-thresholds.json` as the single source of
truth; the v1.44/v1.47 discipline forbids numeric duplication in prose). Explicitly document the
tin-separation rule (D-06) as "rule frozen at plan time; noise-band constant derived from the
Snapshot A baseline 3-run spread (`phase226 baseline-summary.json` `tin_queue_delay_spread_ms`),
filled at Snapshot-A time, not before — this is pre-registration, NOT reverse-fitting." Record the
lock timestamp/commit context and state that these thresholds are locked BEFORE any candidate
`diffserv4 wash` deploy (which is reserved for Phase 227). Cross-reference the Phase 228 gate-check
consumer. Keep it public-safe (no secrets, no credentials).
</action>
<read_first>
- scripts/phase226-thresholds.json
- scripts/phase206-thresholds.json
- .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md
- .planning/ROADMAP.md
</read_first>
<acceptance_criteria>
- `GATE-01-THRESHOLDS.md` exists and lists all five gate families: RRUL p99 (D-01), restart-rate (D-02), transition-rate (D-03), UL stability (D-04), tin separation (D-05/D-06).
- Each gate is labeled INHERITED (with phase206/v1.44 citation) or NEW, and points to `scripts/phase226-thresholds.json` as the single source of truth.
- No numeric threshold literal is duplicated in the prose: `grep -E '\b5(\.0)?%?\b|\b10%?\b' GATE-01-THRESHOLDS.md` returns no lines that state a gate threshold value (incidental phase/decision numbers like D-01 or Phase 228 are fine — verify no `=5.0`/`5%`/`10%` gate-value restatements).
- The tin-separation entry explicitly states the rule is frozen at plan time and the noise-band constant is derived from the Snapshot A 3-run spread (pre-registration, not reverse-fitting), citing `baseline-summary.json` `tin_queue_delay_spread_ms`.
- `grep -iE 'password|secret|token' GATE-01-THRESHOLDS.md` returns no lines.
</acceptance_criteria>
<verify>
  <automated>test -s .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md && grep -q 'phase226-thresholds.json' .planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md</automated>
</verify>
<done>
The pre-registration record documents every GATE-01 gate with its decision source and inherited/new provenance, points to the JSON as the single source of truth without duplicating numeric literals, and locks the tin-separation rule at plan time with an explicitly-derived (not reverse-fitted) noise-band constant — committed before any candidate deploy.
</done>
</task>

<verification>
- `python3 -m json.tool scripts/phase226-thresholds.json` exits 0; inherited gate values match phase206 exactly; UL_STABILITY and TIN_SEPARATION objects present with both arms each.
- `GATE-01-THRESHOLDS.md` lists all five gate families with provenance and does not duplicate numeric literals.
- The tin-separation noise-band constant is a derived placeholder citing the baseline spread, not a guessed number.
- No controller-path reference (SAFE-13).
</verification>

<success_criteria>
- GATE-01 satisfied: accept/rollback thresholds (RRUL p99, restart-rate, transition-rate, upload stability, tin separation) are pre-registered in a committed artifact before any candidate deploy, so the verdict cannot be reverse-fitted.
- D-01..D-03 inherited verbatim from phase206; D-04 (UL two-arm) and D-05/D-06 (tin separation, frozen rule + derived constant) added.
- The JSON is schema-versioned and eval-compatible with the Phase 228 gate-check.
- SAFE-13 preserved: no controller-path source touched.
</success_criteria>

<output>
Create `.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-03-SUMMARY.md` when done.
</output>
