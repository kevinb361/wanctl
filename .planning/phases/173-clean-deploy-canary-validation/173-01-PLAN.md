---
phase: 173-clean-deploy-canary-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/__init__.py
  - pyproject.toml
  - CLAUDE.md
autonomous: true
requirements: [DEPL-01]

must_haves:
  truths:
    - "Version string 1.35.0 appears in all three canonical version files"
    - "Python import of wanctl.__version__ returns 1.35.0"
    - "pyproject.toml version field matches __init__.py exactly"
    - "No other code changes beyond version bump"
  artifacts:
    - path: "src/wanctl/__init__.py"
      provides: "Runtime version string consumed by health endpoint"
      contains: '__version__ = "1.35.0"'
    - path: "pyproject.toml"
      provides: "Package version"
      contains: 'version = "1.35.0"'
    - path: "CLAUDE.md"
      provides: "Documentation version"
      contains: "**Version:** 1.35.0"
  key_links:
    - from: "src/wanctl/__init__.py"
      to: "src/wanctl/health_check.py"
      via: "import __version__"
      pattern: "from wanctl import __version__"
---

<objective>
Bump version to 1.35.0 in the three canonical version files, commit, and push.

Purpose: The health endpoint serves `__version__` from `__init__.py`. The canary's `--expect-version 1.35.0` check (Plan 02/03) will fail if this bump is missing. Version is a release gate, not cosmetic -- incorrect version causes canary exit 2 (WARN), failing DEPL-01. This must be committed and pushed before deploy.
Output: Three files updated, committed, pushed to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-clean-deploy-canary-validation/173-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 1.35.0 in three files</name>
  <files>src/wanctl/__init__.py, pyproject.toml, CLAUDE.md</files>
  <read_first>
    - src/wanctl/__init__.py (current version string to replace)
    - pyproject.toml (current version field to replace)
    - CLAUDE.md (current **Version:** line to replace)
    - src/wanctl/health_check.py (verify __version__ import path -- confirms health endpoint will report the bumped version)
  </read_first>
  <action>
Update exactly three files per D-01. No other changes.

1. `src/wanctl/__init__.py` line 3: change `__version__ = "1.32.2"` to `__version__ = "1.35.0"`
2. `pyproject.toml` line 3: change `version = "1.32.2"` to `version = "1.35.0"`
3. `CLAUDE.md` line 11: change `**Version:** 1.32.2` to `**Version:** 1.35.0`

Addresses review concern: version is a release gate (canary --expect-version depends on it), not cosmetic. All three surfaces must be identical.

After editing, verify the full version propagation chain:
```
python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0', f'Got {__version__}'"
```

Also verify pyproject.toml and CLAUDE.md match:
```
grep -q 'version = "1.35.0"' pyproject.toml && echo "pyproject OK" || echo "pyproject FAIL"
grep -q '**Version:** 1.35.0' CLAUDE.md && echo "CLAUDE.md OK" || echo "CLAUDE.md FAIL"
```

Commit all three files in a single atomic commit with message:
```
chore(173): bump version to 1.35.0
```

Then push to main. The deploy host will receive this exact commit because deploy.sh rsyncs from the local checkout (which is at HEAD after push).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0'" && grep -q 'version = "1.35.0"' pyproject.toml && grep -q '**Version:** 1.35.0' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - src/wanctl/__init__.py contains exactly `__version__ = "1.35.0"`
    - pyproject.toml contains exactly `version = "1.35.0"`
    - CLAUDE.md contains exactly `**Version:** 1.35.0`
    - `python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; print(__version__)"` outputs `1.35.0`
    - git log --oneline -1 shows a commit with "bump version to 1.35.0"
    - git diff HEAD shows no changes (everything committed)
  </acceptance_criteria>
  <done>Version 1.35.0 is committed in all three canonical files and pushed to main</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> production | Version string propagates via deploy.sh rsync to /opt/wanctl; canary validates match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | T (Tampering) | version string | mitigate | Version is a release gate: canary --expect-version 1.35.0 depends on health endpoint reporting exact match. All three files checked in verify step. Git commit integrity protects source. Addresses review concern: version mismatch causes canary exit 2 (deploy failure). |
</threat_model>

<verification>
- `python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0'"`
- `grep '__version__ = "1.35.0"' src/wanctl/__init__.py`
- `grep 'version = "1.35.0"' pyproject.toml`
- `grep '**Version:** 1.35.0' CLAUDE.md`
- `git log --oneline -1` shows version bump commit
</verification>

<success_criteria>
All three version files contain 1.35.0, commit pushed to main, no other files modified. Version propagation chain verified: __init__.py -> health_check.py import -> /health JSON .version field -> canary --expect-version match.
</success_criteria>

<output>
After completion, create `.planning/phases/173-clean-deploy-canary-validation/173-01-SUMMARY.md`
</output>
