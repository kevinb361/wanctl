---
phase: 173-clean-deploy-canary-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/__init__.py
  - pyproject.toml
  - CLAUDE.md
  - scripts/migrate-storage.sh
autonomous: true
requirements: [DEPL-01]

must_haves:
  truths:
    - "Version string 1.35.0 appears in all three canonical version files"
    - "Python import of wanctl.__version__ returns 1.35.0"
    - "pyproject.toml version field matches __init__.py exactly"
    - "All uncommitted Phase 172 changes are included in the commit"
    - "Commit is pushed to origin/main so deploy.sh receives the correct code"
  artifacts:
    - path: "src/wanctl/__init__.py"
      provides: "Runtime version string consumed by health endpoint"
      contains: '__version__ = "1.35.0"'
    - path: "pyproject.toml"
      provides: "Package version"
      contains: 'version = "1.35.0"'
    - path: "CLAUDE.md"
      provides: "Documentation version"
      contains: "**Version:** 1.35.0"
  key_links:
    - from: "src/wanctl/__init__.py"
      to: "src/wanctl/health_check.py"
      via: "from wanctl import __version__"
      pattern: "from wanctl import __version__"
    - from: "src/wanctl/health_check.py"
      to: "/health endpoint JSON"
      via: "version field in health response"
      pattern: '"version": __version__'
---

<objective>
Bump version to 1.35.0 in the three canonical version files, include all uncommitted Phase 172 changes (scripts/migrate-storage.sh), commit, and push to origin/main.

Purpose: The health endpoint serves __version__ from __init__.py. The canary's --expect-version 1.35.0 check (Plan 02/03) will fail if this bump is missing. Version is a release gate, not cosmetic -- incorrect version causes canary exit 2 (WARN), failing DEPL-01. The commit must also include the uncommitted migrate-storage.sh fixes from Phase 172 (Pitfall 7 from research: deploy.sh rsyncs from local checkout).
Output: Three version files updated + migrate-storage.sh changes committed and pushed to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-clean-deploy-canary-validation/173-CONTEXT.md

<interfaces>
<!-- Version propagation chain (VERIFIED in research): -->
<!-- __init__.py -> health_check.py (import) -> /health JSON -> canary-check.sh (--expect-version) -->

From src/wanctl/__init__.py (current):
```python
__version__ = "1.32.2"
```

From pyproject.toml (current, line 3):
```toml
version = "1.32.2"
```

From CLAUDE.md (current, line 11):
```markdown
**Version:** 1.32.2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 1.35.0 and commit with Phase 172 changes</name>
  <files>src/wanctl/__init__.py, pyproject.toml, CLAUDE.md, scripts/migrate-storage.sh</files>
  <read_first>
    - src/wanctl/__init__.py (verify current version is 1.32.2)
    - pyproject.toml (verify current version is 1.32.2, line 3)
    - CLAUDE.md (find **Version:** line, verify 1.32.2)
    - src/wanctl/health_check.py (verify __version__ import path -- confirms health endpoint will report the bumped version)
    - Run `git status` to identify ALL uncommitted changes including Phase 172 leftovers
  </read_first>
  <action>
Per D-01: Update version string from "1.32.2" to "1.35.0" in exactly three files:

1. `src/wanctl/__init__.py` line 3: change `__version__ = "1.32.2"` to `__version__ = "1.35.0"`
2. `pyproject.toml` line 3: change `version = "1.32.2"` to `version = "1.35.0"`
3. `CLAUDE.md` line 11: change `**Version:** 1.32.2` to `**Version:** 1.35.0`

After version edits, run `git status` to enumerate ALL uncommitted files. Known uncommitted change: `scripts/migrate-storage.sh` (Phase 172 table_exists function, sudo test fixes). There may be other uncommitted source/script changes.

Stage the 3 version files plus `scripts/migrate-storage.sh` plus any other uncommitted source/script/config changes from Phase 172. Do NOT stage .planning/ directory files -- those are tracked separately by GSD tooling.

Verify version consistency before committing:
```bash
python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0', f'Got {__version__}'"
grep -q 'version = "1.35.0"' pyproject.toml && echo "pyproject OK" || echo "pyproject FAIL"
grep -q '**Version:** 1.35.0' CLAUDE.md && echo "CLAUDE.md OK" || echo "CLAUDE.md FAIL"
```

Commit message: `release(v1.35.0): version bump and Phase 172 migrate-storage fixes`

Push to origin/main. deploy.sh in Wave 2 rsyncs from the local checkout at HEAD.

Addresses review concern: version is a release gate (canary --expect-version depends on it). All three surfaces must be identical 1.35.0. Addresses Pitfall 7: uncommitted Phase 172 changes included so deploy.sh receives the correct migrate-storage.sh.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0'" && grep -q 'version = "1.35.0"' pyproject.toml && grep -q '1.35.0' CLAUDE.md && echo "ALL VERSION CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/wanctl/__init__.py contains exactly `__version__ = "1.35.0"`
    - pyproject.toml line 3 contains exactly `version = "1.35.0"`
    - CLAUDE.md contains exactly `**Version:** 1.35.0`
    - `python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; print(__version__)"` outputs `1.35.0`
    - git log --oneline -1 shows the version bump commit
    - git status shows scripts/migrate-storage.sh is clean (committed)
    - git diff origin/main..HEAD is empty (pushed)
  </acceptance_criteria>
  <done>Version 1.35.0 committed in all three canonical files, Phase 172 migrate-storage.sh changes included, pushed to origin/main</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> production (via deploy.sh rsync) | Version string must match across all consumers; deploy.sh sends local checkout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | T (Tampering) | version string in 3 files | mitigate | Automated check: all 3 files must contain "1.35.0" before commit. Canary --expect-version validates at runtime. Addresses review: version mismatch causes canary exit 2 = deploy failure. |
| T-173-02 | I (Information Disclosure) | commit includes unintended files | mitigate | Explicit file staging (named files, not git add -A). No secrets in version files. |
</threat_model>

<verification>
- `python3 -c "import sys; sys.path.insert(0, 'src'); from wanctl import __version__; assert __version__ == '1.35.0'"`
- `grep '__version__ = "1.35.0"' src/wanctl/__init__.py`
- `grep 'version = "1.35.0"' pyproject.toml`
- `grep '**Version:** 1.35.0' CLAUDE.md`
- `git log --oneline -1` shows version bump commit
- `git status scripts/migrate-storage.sh` is clean
</verification>

<success_criteria>
All three version files contain 1.35.0, migrate-storage.sh committed, pushed to main. Version propagation chain verified: __init__.py -> health_check.py import -> /health JSON .version field -> canary --expect-version match.
</success_criteria>

<output>
After completion, create `.planning/phases/173-clean-deploy-canary-validation/173-01-SUMMARY.md`
</output>
