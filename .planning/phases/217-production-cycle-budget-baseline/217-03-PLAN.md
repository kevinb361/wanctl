---
phase: 217-production-cycle-budget-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - .planning/perf/v1.45-baseline-spectrum.profile.json
  - .planning/perf/217-cycle-budget-summary.md
  - .planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md
files_modified_note: "exact profile.json filename gets a -<date> suffix at write time per D-06"
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
must_haves:
  truths:
    - "The committed profile artifact contains the canonical autorate_* labels including autorate_cycle_total"
    - "The subsystem-cost summary identifies which of the 4 hot-path categories dominates, with storage attributed out-of-band"
    - "The close-or-promote decision is justified against the D-03/D-04 absolute-bar partition (50ms headroom AND no subsystem >40%)"
    - "The observer-effect delta (DEBUG cycle_total vs /health cycle_budget) is stated as a number or an explicit caveat"
    - "The pending profiling todo is either moved to done/ (no-action) or recorded as promoted to a follow-on optimization phase"
  artifacts:
    - path: ".planning/perf/v1.45-baseline-spectrum.profile.json"
      provides: "profiling_collector.py --output json artifact (per-label count/min/p50/avg/max/p95/p99 ms)"
      contains: "autorate_cycle_total"
    - path: ".planning/perf/217-cycle-budget-summary.md"
      provides: "1-page summary: D-03 verdict, PERF-02 dominance breakdown, observer-effect, PERF-03 decision"
      min_lines: 30
    - path: ".planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md"
      provides: "Closed todo (if no-action outcome) — moved from pending/ to done/"
      contains: "Profile post-hotpath baseline"
  key_links:
    - from: ".planning/perf/spectrum-capture-raw.log"
      to: ".planning/perf/v1.45-baseline-spectrum.profile.json"
      via: "profiling_collector.py --all --output json"
      pattern: "autorate_cycle_total"
    - from: ".planning/perf/v1.45-baseline-spectrum.profile.json"
      to: ".planning/perf/217-cycle-budget-summary.md"
      via: "profiling_categorize.py dominance verdict feeds the summary"
      pattern: "40%"
---

<objective>
Turn the retrieved Spectrum capture (Plan 02) into the committed deliverables: the
`.profile.json` artifact (D-06), the per-category dominance breakdown (PERF-02), the
observer-effect cross-check, the 1-page summary stating the D-03/D-04 close-or-promote
verdict and the explicit deprioritize/promote decision (PERF-03), and then close-or-promote
the pending profiling todo (PERF-01).

Purpose: This is the decision plan. It consumes the helper from Plan 01 and the raw log
from Plan 02 and produces the phase's exit-gate verdict. No new measurement, no control-path
change — pure analysis + documentation + todo lifecycle.

Output: `.planning/perf/v1.45-baseline-spectrum-<date>.profile.json`,
`.planning/perf/217-cycle-budget-summary.md`, and the todo moved to `done/` (or a promotion
recorded). NO `src/` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md
@.planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md
@.planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md

<interfaces>
<!-- Inputs produced by Plans 01/02: -->
<!--   .planning/perf/spectrum-capture-raw.log        (Plan 02 — DEBUG/text, full window) -->
<!--   .planning/perf/spectrum-health-midwindow.json  (Plan 02 — /health snapshot, no observer-effect) -->
<!--   scripts/profiling_categorize.py                (Plan 01 — category roll-up + >40% verdict) -->
<!-- Analysis invocations (217-RESEARCH.md Q3, Code Examples): -->
<!--   .venv/bin/python scripts/profiling_collector.py "$LOG" --all --output json > <profile.json> -->
<!--   .venv/bin/python scripts/analyze_profiling.py --log-file "$LOG" --budget 50 --output <md> -->
<!--   .venv/bin/python scripts/profiling_categorize.py <profile.json> --budget 50 -->
<!--   .venv/bin/python -m wanctl.history --ingestion-rate --wan spectrum   (storage attribution; if unavailable, note unmeasured) -->
<!-- Observer-effect (217-RESEARCH.md Q5/Open Q2): DEBUG-log autorate_cycle_total.avg_ms MINUS -->
<!--   /health cycle_budget.cycle_time_ms.avg (from spectrum-health-midwindow.json) ≈ observer-effect. -->
<!-- D-03/D-04 partition: no-action if cycle_total has comfortable 50ms headroom AND max category % <= 40; -->
<!--   promote if either headroom is thin OR any category > 40%. Drop the ±15%/±25%-vs-v1.39 clauses. -->
<!-- Archived baselines (D-04, informational ONLY): v1.0 RTT-dominant at 2s interval; v1.9 expected 30-45ms at 50ms. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the .profile.json artifact + per-label analysis + category/storage attribution</name>
  <read_first>
    - .planning/perf/spectrum-capture-raw.log (the source log from Plan 02)
    - scripts/profiling_collector.py (JSON schema)
    - scripts/analyze_profiling.py (percentage/utilization/headroom output)
    - scripts/profiling_categorize.py (Plan 01 helper — category roll-up + >40% verdict)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (Q3 invocations, Q2 category map, Pitfall 2 storage gap, Pitfall 3 router double-count)
  </read_first>
  <files>.planning/perf/v1.45-baseline-spectrum.profile.json</files>
  <action>Run `.venv/bin/python scripts/profiling_collector.py .planning/perf/spectrum-capture-raw.log --all --output json` and write the result to `.planning/perf/v1.45-baseline-spectrum-<date>.profile.json` (use the capture date from Plan 02, e.g. `v1.45-baseline-spectrum-20260530.profile.json`). Then run `.venv/bin/python scripts/analyze_profiling.py --log-file .planning/perf/spectrum-capture-raw.log --budget 50 --output /tmp/v1.45-spectrum-analysis.md` for the per-label percentage/utilization/headroom markdown, and `.venv/bin/python scripts/profiling_categorize.py .planning/perf/v1.45-baseline-spectrum-<date>.profile.json --budget 50` for the PERF-02 category roll-up and the D-03 `>40%` dominance verdict. Separately attribute storage-write load (which has NO hot-path label by design — Pitfall 2) via `.venv/bin/python -m wanctl.history --ingestion-rate --wan spectrum`; if that tool is unavailable against the dev checkout, record storage-write attribution as "unmeasured — requires host-side wanctl-history" rather than inventing a number. Capture all four outputs (collector JSON, analyzer markdown, categorize JSON, ingestion-rate) for use in Task 2. Use `autorate_router_communication` (parent) as the router category total — do NOT sum the router sub-timers (Pitfall 3). Do not modify the raw log.</action>
  <verify>
    <automated>F=$(ls .planning/perf/v1.45-baseline-spectrum-*.profile.json | head -1) && .venv/bin/python -c "import json,sys; d=json.load(open('$F')); assert 'autorate_cycle_total' in d, 'missing cycle_total'; assert d['autorate_cycle_total']['count']>0; print('cycle_total count:', d['autorate_cycle_total']['count'])" && .venv/bin/python scripts/profiling_categorize.py "$F" --budget 50 | .venv/bin/python -c "import json,sys; r=json.load(sys.stdin); assert 'dominance_exceeded' in r and 'dominant_category' in r and 'max_category_pct' in r; print('dominant:', r['dominant_category'], 'max_pct:', round(r['max_category_pct'],1), 'exceeded:', r['dominance_exceeded'])"</automated>
  </verify>
  <acceptance_criteria>
    - A file `.planning/perf/v1.45-baseline-spectrum-<date>.profile.json` exists, parses as JSON, and contains `autorate_cycle_total` with `count > 0`.
    - The artifact contains the four PERF-02 hot-path labels (`autorate_rtt_measurement`, `autorate_cake_stats`, `autorate_router_communication`, `autorate_logging_metrics`) where present in the capture.
    - `profiling_categorize.py` against the artifact returns `dominant_category`, `max_category_pct`, and `dominance_exceeded` (boolean).
    - Storage-write attribution is either a `wanctl-history --ingestion-rate` figure OR explicitly recorded as "unmeasured — requires host-side wanctl-history".
    - The router category total uses `autorate_router_communication` only (no sub-timer summing).
  </acceptance_criteria>
  <done>The committed `.profile.json` artifact exists with `autorate_cycle_total`, and the category/dominance/storage outputs are captured for the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 1-page summary with the D-03/D-04 verdict, observer-effect, and PERF-03 decision</name>
  <read_first>
    - .planning/perf/v1.45-baseline-spectrum.profile.json (Task 1 artifact)
    - /tmp/v1.45-spectrum-analysis.md (Task 1 analyzer output)
    - .planning/perf/spectrum-health-midwindow.json (Plan 02 — /health cross-check, no observer-effect)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (Q5 observer-effect, Q8 archived baselines, Validation Architecture)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-03/D-04 partition, D-06 summary requirement)
  </read_first>
  <files>.planning/perf/217-cycle-budget-summary.md</files>
  <action>Write `.planning/perf/217-cycle-budget-summary.md` — a 1-page operator summary (D-06) with these sections: (1) header noting Spectrum `1.45.0`, capture window UTC start/end (from Plan 02), and the artifact filename; (2) "Cycle budget" — `autorate_cycle_total` avg/p50/p95/p99 ms with explicit headroom vs the 50ms budget and utilization %; (3) "Subsystem dominance (PERF-02)" — a table of the 4 hot-path categories with avg_ms and % of cycle_total, the `dominant_category`, and the boolean `>40%` verdict from `profiling_categorize.py`; plus a separate line attributing storage-write load via `wanctl-history --ingestion-rate` (or "unmeasured") noting storage is off the hot path by design; (4) "Observer-effect" — state the delta between the DEBUG-log `autorate_cycle_total.avg_ms` and the `/health cycle_budget.cycle_time_ms.avg` from the mid-window snapshot as a number; if the snapshot is missing, state the explicit caveat that DEBUG-captured cycle_total is an upper bound vs steady-state INFO; (5) "Historical context (informational only, D-04)" — one or two lines citing the archived v1.0 (RTT-dominant, 2s interval) and v1.9 (expected 30-45ms at 50ms) expectations, explicitly marked NOT a pass/fail gate; (6) "Verdict (D-03/D-04 partition)" — state which condition holds: NO-ACTION/close if cycle_total has comfortable 50ms headroom AND no category >40%, else PROMOTE; (7) "Decision (PERF-03)" — one explicit sentence: either "Performance work is deprioritized in favor of quality/tuning work" (no-action) or "Promote to a v1.46+ optimization phase targeting <category>" (promote). Use the actual measured numbers — no placeholders. Do NOT recommend any threshold/algorithm change.</action>
  <verify>
    <automated>test -f .planning/perf/217-cycle-budget-summary.md && grep -qi "cycle.*budget\|cycle_total" .planning/perf/217-cycle-budget-summary.md && grep -q "40%" .planning/perf/217-cycle-budget-summary.md && grep -qi "observer" .planning/perf/217-cycle-budget-summary.md && grep -qiE "deprioritiz|promote" .planning/perf/217-cycle-budget-summary.md && grep -qi "storage" .planning/perf/217-cycle-budget-summary.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf/217-cycle-budget-summary.md` exists.
    - States `autorate_cycle_total` avg + p99 with explicit numeric headroom vs 50ms.
    - Contains the 4-category dominance table and the boolean `>40%` verdict (greppable: `40%`).
    - States storage-write attribution separately (greppable: `storage`).
    - States the observer-effect delta number OR the explicit upper-bound caveat (greppable: `observer`).
    - Contains the explicit PERF-03 decision sentence (greppable: `deprioritiz` or `promote`).
    - Uses real measured numbers, no placeholder tokens like `<...>` or `TBD`.
    - Contains no recommendation to change thresholds/algorithms/control-path code.
  </acceptance_criteria>
  <done>The 1-page summary states the cycle budget, the PERF-02 dominance verdict, storage attribution, observer-effect, and the explicit PERF-03 close-or-promote decision against the D-03/D-04 bars.</done>
</task>

<task type="auto">
  <name>Task 3: Close or promote the pending profiling todo (PERF-01)</name>
  <read_first>
    - .planning/perf/217-cycle-budget-summary.md (the verdict from Task 2)
    - .planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md (the todo + its close conditions)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-04 reinterpretation: the partition uses absolute bars, drop the ±15%/±25%-vs-v1.39 clauses)
  </read_first>
  <files>.planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md</files>
  <action>Apply the Task 2 verdict to the todo lifecycle under the D-04 reinterpretation (the absolute-bar partition supersedes the todo's original ±15%/±25%-vs-v1.39 clauses, which are unfalsifiable). If the verdict is NO-ACTION: append a short "## Closed — 2026-05-30 (Phase 217)" section to the todo body recording the close outcome, the cycle_total headroom and max-category %, and a pointer to `.planning/perf/217-cycle-budget-summary.md` + the `.profile.json` artifact, then move the file from `.planning/todos/pending/` to `.planning/todos/done/` (use `git mv`). If the verdict is PROMOTE: append a "## Promoted — 2026-05-30 (Phase 217)" section recording which clause tripped (a category >40% or thin headroom) and that a v1.46+ optimization phase targeting that subsystem should be opened, then move the file to `.planning/todos/done/` (the todo closes when the follow-on phase is opened, per its condition 2) and note the recommended phase scope in the summary. Either way the file ends in `done/`. Do NOT delete the todo content; preserve its history.</action>
  <verify>
    <automated>test -f .planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md && ! test -f .planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md && grep -qiE "Closed|Promoted" .planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md && grep -q "217-cycle-budget-summary.md" .planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md</automated>
  </verify>
  <acceptance_criteria>
    - The todo file now lives in `.planning/todos/done/` and NOT in `.planning/todos/pending/`.
    - It contains a `## Closed` or `## Promoted` section dated and attributed to Phase 217.
    - It references `.planning/perf/217-cycle-budget-summary.md`.
    - Original todo content is preserved (not deleted).
  </acceptance_criteria>
  <done>The pending profiling todo is moved to `done/` with a close-or-promote disposition referencing the summary (PERF-01 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieved production log → committed repo artifacts | profile.json + summary derive from a DEBUG log that may carry operating points / router IPs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-06 | Information disclosure | .planning/perf/v1.45-baseline-spectrum-<date>.profile.json + 217-cycle-budget-summary.md | mitigate | The collector JSON is aggregate timing stats only (no IPs/secrets by construction), but the raw capture log (T-217-05) and any embedded snapshot are inspected before commit per CLAUDE.md public-safe policy. Before committing, grep the artifacts for secrets/keys/credentials and confirm only non-secret timing/operating-point data is present (Phase 212/213 redaction precedent). |
| T-217-07 | Repudiation | todo lifecycle move | accept | `git mv` preserves history; the close/promote section is dated and attributed. Low risk, fully reversible via git. |

No HIGH threats: this plan reads a log and emits aggregate stats + a markdown summary + a todo move. The disclosure surface is the committed artifacts, mitigated by a pre-commit secret scan. No production interaction, no new endpoints, no control-path change.
</threat_model>

<verification>
- `.planning/perf/v1.45-baseline-spectrum-<date>.profile.json` exists, parses, contains `autorate_cycle_total` with count > 0.
- `profiling_categorize.py` against the artifact yields the dominance verdict consumed by the summary.
- `.planning/perf/217-cycle-budget-summary.md` states cycle budget + headroom, 4-category dominance + >40% verdict, storage attribution, observer-effect, and the explicit PERF-03 decision.
- Storage-write cost is attributed out-of-band (never asserted as a % of cycle_total).
- The profiling todo is in `.planning/todos/done/` with a Closed/Promoted disposition referencing the summary.
- No `src/` files modified.
- Pre-commit secret scan of `.planning/perf/` artifacts passes (no keys/credentials).
</verification>

<success_criteria>
- Committed `.profile.json` artifact for the Spectrum baseline (D-06, PERF-01).
- 1-page summary with the PERF-02 dominance breakdown and the D-03/D-04 close-or-promote verdict (PERF-02, PERF-03).
- Explicit deprioritize-or-promote decision sentence recorded (PERF-03).
- Pending profiling todo closed or promoted (PERF-01).
- Zero control-path edits.
</success_criteria>

<output>
After completion, create `.planning/phases/217-production-cycle-budget-baseline/217-03-SUMMARY.md`
(record the cycle_total numbers, dominant category + %, observer-effect delta, the verdict, and the todo disposition).
</output>
