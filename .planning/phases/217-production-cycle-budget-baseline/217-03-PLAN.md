---
phase: 217-production-cycle-budget-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - .planning/perf/v1.45-baseline-spectrum.profile.json
  - .planning/perf/v1.45-baseline-spectrum.ingestion.json
  - .planning/perf/217-cycle-budget-summary.md
  - .planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md
files_modified_note: "exact profile.json/ingestion.json filenames get a -<YYYYMMDD> suffix at write time per D-06; the date is derived from .planning/perf/217-capture-window.json"
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
must_haves:
  truths:
    - "D-06: The committed profile artifact at .planning/perf/v1.45-baseline-spectrum-<date>.profile.json contains the canonical autorate_* labels including autorate_cycle_total (produced by scripts/profiling_collector_json.py from the Plan 02 NDJSON, NOT by the legacy regex collector)"
    - "PERF-02: The subsystem-cost summary identifies which of the 4 hot-path categories dominates using autorate_router_communication as the router parent (no double-counting of router_apply_*/router_write_* children), with storage attributed out-of-band via window-aligned wanctl-history --ingestion-rate --from/--to (research Pitfall 4)"
    - "D-03: The close-or-promote decision is justified against the absolute-bar partition cycle_total.avg_ms < 40.0 AND cycle_total.p99_ms < 50.0 AND max(category_pct) < 40.0 — a falsifiable verdict pair stated as booleans BEFORE the narrative"
    - "D-04: The decision drops the unanchored ±15%/±25%-vs-v1.39 clauses; archived v1.0/v1.9 baselines are informational citation only, never a pass/fail gate"
    - "D-06: A 1-page summary at .planning/perf/217-cycle-budget-summary.md states the verdict pair, the dominant category + %, the observer-effect number-or-caveat, the windowed storage attribution, and the explicit deprioritize/promote decision (PERF-03)"
    - "Observer-effect: stated as a number (when Plan 02 collected adjacent ON/OFF /health windows) OR as the documented caveat that DEBUG-captured cycle_total is an upper bound on real cost (research §Q5 option b) — NEVER a same-window /health-vs-DEBUG delta (~0 by construction, research Pitfall 3)"
    - "PERF-01: The pending profiling todo is either moved to done/ (no-action) or recorded as promoted to a follow-on optimization phase; pure steady-state (router_write_coverage == absent_gap) cannot return unconditional no-action — verdict must be qualified or PROMOTE"
  artifacts:
    - path: ".planning/perf/v1.45-baseline-spectrum.profile.json"
      provides: "scripts/profiling_collector_json.py output (per-label count/min/p50/avg/max/p95/p99 ms) — the canonical .profile.json schema, D-06 committed artifact"
      contains: "autorate_cycle_total"
    - path: ".planning/perf/v1.45-baseline-spectrum.ingestion.json"
      provides: "wanctl-history --ingestion-rate --from/--to --json — storage attribution rows-per-second over the explicit capture window (research Pitfall 4 closed)"
    - path: ".planning/perf/217-cycle-budget-summary.md"
      provides: "1-page summary: D-03 verdict pair, PERF-02 dominance breakdown, observer-effect, storage attribution, PERF-03 decision"
      min_lines: 30
    - path: ".planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md"
      provides: "Closed/promoted todo — moved from pending/ to done/, with the verdict + summary pointer appended"
      contains: "Profile post-hotpath baseline"
  key_links:
    - from: ".planning/perf/capture/spectrum_debug.ndjson"
      to: ".planning/perf/v1.45-baseline-spectrum-<date>.profile.json"
      via: "scripts/profiling_collector_json.py (NEW from Plan 01; NOT the legacy regex collector)"
      pattern: "autorate_cycle_total"
    - from: ".planning/perf/217-capture-window.json"
      to: ".planning/perf/v1.45-baseline-spectrum-<date>.ingestion.json"
      via: "wanctl-history --ingestion-rate --wan spectrum --from <window.start_utc> --to <window.end_utc> --json"
      pattern: "ingestion-rate"
    - from: ".planning/perf/v1.45-baseline-spectrum-<date>.profile.json"
      to: ".planning/perf/217-cycle-budget-summary.md"
      via: "jq pipeline computes verdict pair (passes_headroom, passes_dominance)"
      pattern: "40"
---

<objective>
Turn the retrieved Spectrum NDJSON capture (Plan 02) into the committed deliverables: the
`.profile.json` artifact via the new JSON parser from Plan 01 (D-06), the per-category
dominance breakdown (PERF-02) using the falsifiable D-03 verdict pair, the windowed
storage attribution via `wanctl-history --ingestion-rate --from/--to` (research Pitfall 4),
the observer-effect number-or-caveat, the 1-page summary stating the close-or-promote
verdict and the explicit PERF-03 decision, and then close-or-promote the pending
profiling todo (PERF-01).

Purpose: This is the decision plan. It consumes the JSON parser from Plan 01 + the NDJSON
+ window metadata from Plan 02 and produces the phase's exit-gate verdict. No new
measurement, no control-path change — pure analysis + documentation + todo lifecycle.

Output: `.planning/perf/v1.45-baseline-spectrum-<date>.profile.json`,
`.planning/perf/v1.45-baseline-spectrum-<date>.ingestion.json`,
`.planning/perf/217-cycle-budget-summary.md`, and the todo moved to `done/` with a verdict.
NO `src/` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md
@.planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md
@.planning/phases/217-production-cycle-budget-baseline/217-REVIEWS.md
@.planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md

<interfaces>
<!-- Inputs from Plans 01/02: -->
<!--   .planning/perf/capture/spectrum_debug.ndjson         (Plan 02 — DEBUG/JSON, full window; gitignored) -->
<!--   .planning/perf/capture/spectrum-health-on-window.ndjson  (Plan 02 OPTIONAL — /health poll with --debug ON) -->
<!--   .planning/perf/capture/spectrum-health-off-window.ndjson (Plan 02 OPTIONAL — /health poll with --debug OFF baseline) -->
<!--   .planning/perf/217-capture-window.json               (Plan 02 — committed window metadata + validation block) -->
<!--   scripts/profiling_collector_json.py                  (Plan 01 — NDJSON → .profile.json) -->
<!-- Analysis invocations (research §"Capture & Analysis Data Path > Analysis commands"): -->
<!--   .venv/bin/python scripts/profiling_collector_json.py .planning/perf/capture/spectrum_debug.ndjson \ -->
<!--       --output .planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.profile.json -->
<!--   .venv/bin/python -m wanctl.history --ingestion-rate --wan spectrum \ -->
<!--       --from "<window.start_utc>" --to "<window.end_utc>" --json \ -->
<!--       > .planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.ingestion.json -->
<!-- Verdict math (research §"Acceptance criteria #6", encoded as a jq pipeline): -->
<!--   passes_headroom  = (autorate_cycle_total.avg_ms < 40.0)  AND  (autorate_cycle_total.p99_ms < 50.0) -->
<!--   passes_dominance = max(category_pct.values()) < 40.0 -->
<!--   verdict          = "no_action" if (passes_headroom AND passes_dominance) else "promote" -->
<!-- Category map (research §Q2): -->
<!--   RTT measurement       -> autorate_rtt_measurement -->
<!--   CAKE stats            -> autorate_cake_stats -->
<!--   Router communication  -> autorate_router_communication  (PARENT — do NOT sum router_apply_*/router_write_*) -->
<!--   Logging / metrics     -> autorate_logging_metrics -->
<!--   Storage writes        -> NO hot-path label — attributed via ingestion.json -->
<!-- Observer-effect shapes (research §Q5): -->
<!--   (a) Number: avg(on_window cycle_time_ms.avg) - avg(off_window cycle_time_ms.avg) -->
<!--   (b) Caveat: "DEBUG-captured cycle_total is an upper bound on real cost; passing D-03 under DEBUG is a stronger pass" -->
<!-- Driven-segment coupling (research §Q6): if 217-capture-window.json.validation.router_write_coverage == "absent_gap", -->
<!--   unconditional no_action is forbidden — verdict must be qualified as "no-action conditional on steady-state only — router-write category unverified" OR PROMOTE. -->
<!-- D-04: archived v1.0 (RTT-dominant, 2s interval) and v1.9 (expected 30-45ms at 50ms) baselines are informational ONLY, never a pass/fail gate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate .profile.json + windowed ingestion attribution + compute verdict pair</name>
  <read_first>
    - .planning/perf/capture/spectrum_debug.ndjson (the NDJSON source from Plan 02)
    - .planning/perf/217-capture-window.json (window timestamps + validation block from Plan 02)
    - scripts/profiling_collector_json.py (NEW parser from Plan 01 — the load-bearing tool here)
    - scripts/profiling_collector.py (referenced ONLY for schema compatibility — its regex path is NOT used)
    - src/wanctl/history.py (lines 599-715, the --ingestion-rate CLI; line 616-624 _resolve_time_range — confirms --from/--to required to override last-hour default)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Capture & Analysis Data Path > Analysis commands"; §"Q2 category map" router parent-only rule; §"Q3 Analysis invocation"; Pitfalls 3 + 4)
  </read_first>
  <files>.planning/perf/v1.45-baseline-spectrum.profile.json, .planning/perf/v1.45-baseline-spectrum.ingestion.json</files>
  <action>Derive the date suffix from `217-capture-window.json` (`window.start_utc`, format `YYYYMMDD`). Then run three steps:

(1) **Aggregate NDJSON → .profile.json** (D-06 committed artifact). Run:
```bash
DATE=$(jq -r '.window.start_utc[:10] | gsub("-";"")' .planning/perf/217-capture-window.json)
.venv/bin/python scripts/profiling_collector_json.py \
    .planning/perf/capture/spectrum_debug.ndjson \
    --output .planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json
```
Sanity check: `jq '.autorate_cycle_total.count' .planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json` ≥ 60000.

(2) **Windowed storage attribution** (PERF-02 fifth category; research Pitfall 4 — EXPLICIT --from/--to required, default is last hour at `src/wanctl/history.py:616-624`):
```bash
START=$(jq -r '.window.start_utc' .planning/perf/217-capture-window.json)
END=$(jq -r '.window.end_utc' .planning/perf/217-capture-window.json)
.venv/bin/python -m wanctl.history --ingestion-rate --wan spectrum \
    --from "$START" --to "$END" --json \
  > .planning/perf/v1.45-baseline-spectrum-${DATE}.ingestion.json
```
If the dev checkout cannot reach the production SQLite DB (the history CLI reads `/var/lib/wanctl/<wan>/history.db`), write a small JSON stub to the same path with `{"status":"unmeasured", "reason":"<reason>", "window":{"start_utc":"$START","end_utc":"$END"}}` — Plan 03 Task 2 will report storage as "unmeasured — host-side wanctl-history required" rather than inventing a number. Do NOT assert a storage % of cycle_total under any circumstance (research §Q2 — storage has no hot-path label by design).

(3) **Verdict computation** via the canonical jq pipeline (research §"Q3 Analysis invocation"). Capture stdout to a variable for use in Task 2:
```bash
VERDICT=$(jq -n --slurpfile p .planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json '
  def cat(labels): [labels[] as $l | $p[0][$l].avg_ms // 0] | add;
  ($p[0]["autorate_cycle_total"].avg_ms) as $total
  | ($p[0]["autorate_cycle_total"].p99_ms) as $p99
  | { cycle_total_avg_ms: $total,
      cycle_total_p99_ms: $p99,
      utilization_pct: ($total / 50 * 100),
      headroom_avg_ms: (50 - $total),
      passes_headroom: ($total < 40 and $p99 < 50),
      categories: {
        rtt_measurement: cat(["autorate_rtt_measurement"]),
        cake_stats: cat(["autorate_cake_stats"]),
        router_communication: cat(["autorate_router_communication"]),
        logging_metrics: cat(["autorate_logging_metrics"])
      }}
  | .category_pct = (.categories | with_entries(.value = (.value / $total * 100)))
  | .max_category_pct = (.category_pct | [.[]] | max)
  | .passes_dominance = (.max_category_pct < 40)
  | .verdict = (if .passes_headroom and .passes_dominance then "no_action" else "promote" end)')
echo "$VERDICT" > /tmp/217-verdict.json
```
Critical rule (research Pitfall in Q2): router category uses `autorate_router_communication` (parent) ONLY — the jq pipeline above already enforces this; do NOT modify it to sum router sub-timers.

Hand off all three outputs (`.profile.json`, `.ingestion.json` or unmeasured stub, `/tmp/217-verdict.json`) to Task 2.</action>
  <verify>
    <automated>set -e; DATE=$(jq -r '.window.start_utc[:10] | gsub("-";"")' .planning/perf/217-capture-window.json); test -f ".planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json"; jq -e '.autorate_cycle_total.count >= 60000' ".planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json"; jq -e '.autorate_cycle_total | (.avg_ms != null and .p99_ms != null and .p95_ms != null)' ".planning/perf/v1.45-baseline-spectrum-${DATE}.profile.json"; test -f ".planning/perf/v1.45-baseline-spectrum-${DATE}.ingestion.json"; test -f /tmp/217-verdict.json; jq -e '(.passes_headroom | type == "boolean") and (.passes_dominance | type == "boolean") and (.verdict == "no_action" or .verdict == "promote") and (.max_category_pct != null) and (.cycle_total_avg_ms != null)' /tmp/217-verdict.json</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.profile.json` exists, parses as JSON, contains `autorate_cycle_total` with `count >= 60000`, `avg_ms` and `p99_ms` populated.
    - The artifact contains the four PERF-02 hot-path labels (`autorate_rtt_measurement`, `autorate_cake_stats`, `autorate_router_communication`, `autorate_logging_metrics`) — each with `avg_ms` populated where present in the capture.
    - `.planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.ingestion.json` exists; either contains real `wanctl-history --ingestion-rate` JSON output bounded to the capture window OR a structured `{"status":"unmeasured", ...}` stub (NEVER a fabricated number).
    - `/tmp/217-verdict.json` exists and contains `passes_headroom` (bool), `passes_dominance` (bool), `verdict` (`"no_action"` or `"promote"`), `max_category_pct` (number), `cycle_total_avg_ms` (number), `cycle_total_p99_ms` (number).
    - The verdict jq pipeline used `autorate_router_communication` (parent) only — no sub-timer summing.
    - The ingestion call used explicit `--from "<window.start_utc>" --to "<window.end_utc>"` derived from `217-capture-window.json` (research Pitfall 4 closed).
  </acceptance_criteria>
  <done>The committed `.profile.json` artifact + the windowed `.ingestion.json` exist, and the structured verdict object captures the falsifiable D-03 boolean pair for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 1-page summary with the D-03 verdict pair, observer-effect, and PERF-03 decision</name>
  <read_first>
    - .planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.profile.json (Task 1 — the canonical artifact)
    - .planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.ingestion.json (Task 1 — windowed storage attribution OR unmeasured stub)
    - /tmp/217-verdict.json (Task 1 — the falsifiable verdict pair)
    - .planning/perf/217-capture-window.json (Plan 02 — window + driven-segment timestamps + router_write_coverage in validation block)
    - .planning/perf/capture/spectrum-health-on-window.ndjson + .planning/perf/capture/spectrum-health-off-window.ndjson (Plan 02 OPTIONAL — adjacent ON/OFF /health windows for the observer-effect number)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Q5 Observer-effect" options a/b; §"Q8 Archived baselines"; §"Validation Architecture")
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-03/D-04 partition, D-06 summary requirement, D-07 runbook)
  </read_first>
  <files>.planning/perf/217-cycle-budget-summary.md</files>
  <action>Write `.planning/perf/217-cycle-budget-summary.md` — a 1-page operator summary (D-06) with these sections in order. Use REAL measured numbers extracted from the JSON artifacts — no placeholders, no `<...>` tokens, no `TBD`.

**(1) Header.** Spectrum `1.45.0`. Capture window UTC start/end from `217-capture-window.json`. Artifact filename. Driven-segment outcome (`router_write_coverage` from the window file's validation block: `present` or `absent_gap`).

**(2) Cycle budget (D-03 numeric bar).** Restate the falsifiable bar verbatim: "`passes_headroom = cycle_total.avg_ms < 40.0 AND cycle_total.p99_ms < 50.0`". Then the actual numbers from `/tmp/217-verdict.json`: `cycle_total_avg_ms`, `cycle_total_p99_ms`, `headroom_avg_ms` (= 50 − avg), `utilization_pct`. State `passes_headroom` as a BOOLEAN (`true` or `false`).

**(3) Subsystem dominance (PERF-02; D-03 dominance bar).** Restate the bar: "`passes_dominance = max(category_pct.values()) < 40.0`". Table of the 4 hot-path categories with `avg_ms` and `pct` from `/tmp/217-verdict.json.category_pct`. State `dominant_category` (= argmax), `max_category_pct`, and `passes_dominance` as a BOOLEAN. Cite the parent-only rule: "Router category uses `autorate_router_communication` only; `autorate_router_apply_*` and `autorate_router_write_*` are child sub-timers and would double-count if summed (research §Q2)."

**(4) Storage attribution (PERF-02 fifth category).** A separate line/paragraph. From `.ingestion.json`: either the rows-per-second number with the explicit window bounds, or "Unmeasured — host-side `wanctl-history --ingestion-rate --from/--to` required (research Pitfall 4 — default is last hour, which would misalign with our capture window run hours after)." State plainly: "Storage writes have NO hot-path label by design (DeferredIOWorker, research §Q2). Storage % of cycle_total is undefined and is not asserted."

**(5) Observer-effect.** Two acceptable shapes — pick whichever the inputs support:
- If `.planning/perf/capture/spectrum-health-on-window.ndjson` AND `spectrum-health-off-window.ndjson` both exist: compute the delta. For each file, extract `cycle_budget.cycle_time_ms.avg` from each `/health` snapshot and average; report `observer_effect_ms = avg(on) - avg(off)` as a number with both window timestamps. The jq one-liner: `jq -s 'map(.cycle_budget.cycle_time_ms.avg // empty) | add / length'` per file.
- Else (one or both windows missing per `217-capture-window.json.observer_effect`): state the documented caveat verbatim from research §Q5 option (b): "Captured under `--debug`; per-cycle DEBUG JSON emission (~12 records/cycle × 20Hz ≈ 240 records/sec) adds I/O cost absent in steady-state INFO operation. The DEBUG-derived `cycle_total` is an upper bound on real cost. A `cycle_total` that still passes the D-03 bars under DEBUG is therefore a stronger no-action signal." Also explicitly state: "We did NOT compute a same-window `/health`-vs-DEBUG delta — that would be ~0 by construction because both reads come from the same in-process `profiler.stats('autorate_cycle_total')` (research Pitfall 3, `src/wanctl/health_check.py:101`)."

**(6) Historical context (informational only, D-04).** One to two short lines citing archived v1.0 (RTT-dominant at 2s interval — not comparable to 50ms) and v1.9 (expected 30-45ms at 50ms). Explicitly: "Per D-04, these are informational context only and NOT a pass/fail gate. The ±15%/±25%-vs-v1.39 clauses from the original todo are dropped as unfalsifiable (no concrete v1.39-era number was ever committed)."

**(7) Verdict (D-03/D-04 partition).** State `verdict` from `/tmp/217-verdict.json` (`no_action` or `promote`). Restate the rule: "`no_action` if `passes_headroom AND passes_dominance`; `promote` otherwise." Then the driven-segment coupling (research §Q6): "If `router_write_coverage == absent_gap` (i.e. the driven segment did not populate the change-gated router-write labels), unconditional `no_action` is forbidden — the router-communication category was not exercised under change events. In that case the verdict is either qualified as `no-action conditional on steady-state only — router-write category unverified` OR `promote`." Apply this rule using the actual `router_write_coverage` from `217-capture-window.json.validation`.

**(8) Decision (PERF-03).** ONE explicit sentence. If verdict is `no_action` (and not gated absent): "Performance work is deprioritized in favor of quality/tuning work." If verdict is `promote`: "Promote to a v1.46+ optimization phase targeting `<dominant_category>`." If verdict is `no-action conditional on steady-state only`: "Performance work is deprioritized for the steady-state path; router-write category remains unverified and is carried as a follow-up if a future capture lands a driven segment."

**(9) Artifact pointer.** Reference the `.profile.json`, `.ingestion.json`, and `217-capture-window.json` filenames.

Do NOT recommend any threshold/algorithm/control-path change. Keep the entire document close to 1 page (target ~50-80 lines markdown). No real production IPs/hostnames per CLAUDE.md public-safe policy — placeholders only if any host string is needed.</action>
  <verify>
    <automated>set -e; F=.planning/perf/217-cycle-budget-summary.md; test -f "$F"; grep -q "cycle_total" "$F"; grep -q "passes_headroom" "$F"; grep -q "passes_dominance" "$F"; grep -q "40" "$F"; grep -qE "observer.effect|observer_effect" "$F"; grep -qi "storage" "$F"; grep -qiE "deprioritiz|promote" "$F"; grep -q "router_communication" "$F"; grep -qE "1\.45\.0|v1\.45" "$F"; ! grep -qE "TBD|<\.\.\.>" "$F"; ! grep -qE "10\.10\.[0-9]+\.[0-9]+" "$F"; RW=$(jq -r '.validation.router_write_coverage' .planning/perf/217-capture-window.json); V=$(jq -r '.verdict' /tmp/217-verdict.json); if [ "$RW" = "absent_gap" ] && [ "$V" = "no_action" ]; then grep -qE "conditional on steady-state|promote" "$F" || { echo "FAIL: router_write_coverage absent_gap requires qualified verdict or promote" >&2; exit 1; }; fi</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf/217-cycle-budget-summary.md` exists.
    - States `autorate_cycle_total` `avg` and `p99` from the actual artifact (greppable: `cycle_total`).
    - Restates the D-03 bars literally and reports `passes_headroom` + `passes_dominance` as booleans (greppable: `passes_headroom` + `passes_dominance`).
    - Contains the 4-category dominance table including the parent-only rule (greppable: `40` and `router_communication`).
    - States storage attribution separately, either as a windowed `wanctl-history` number with explicit `--from/--to` OR as `unmeasured` (greppable: `storage`).
    - States the observer-effect either as a computed number (when ON/OFF windows were collected) OR as the documented caveat — explicitly NOT a same-window `/health`-vs-DEBUG delta (greppable: `observer`).
    - Contains the explicit PERF-03 decision sentence (greppable: `deprioritiz` or `promote`).
    - Uses real measured numbers; no `TBD` and no `<...>` placeholder tokens.
    - Contains NO real production IP (no `10.10.x.x` literal).
    - Contains no recommendation to change thresholds/algorithms/control-path code.
    - **Driven-segment coupling enforcement:** if `217-capture-window.json.validation.router_write_coverage == "absent_gap"` AND `/tmp/217-verdict.json.verdict == "no_action"`, the summary must contain either `conditional on steady-state` or `promote` (unconditional no-action is forbidden when the driven segment did not land).
  </acceptance_criteria>
  <done>The 1-page summary states the falsifiable D-03 verdict pair, the PERF-02 dominance breakdown with the router parent-only rule, storage attribution with the explicit window, observer-effect (number-or-caveat, never a same-window delta), and the explicit PERF-03 close-or-promote decision.</done>
</task>

<task type="auto">
  <name>Task 3: Close or promote the pending profiling todo (PERF-01)</name>
  <read_first>
    - .planning/perf/217-cycle-budget-summary.md (the verdict from Task 2)
    - /tmp/217-verdict.json (the structured verdict from Task 1)
    - .planning/perf/217-capture-window.json (the actual capture date for the lifecycle stamp)
    - .planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md (the todo + its close conditions)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-04 reinterpretation: the absolute-bar partition supersedes the todo's ±15%/±25%-vs-v1.39 clauses)
  </read_first>
  <files>.planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md</files>
  <action>Apply the Task 2 verdict to the todo lifecycle under the D-04 reinterpretation. Derive the close date from `217-capture-window.json.window.end_utc` (format `YYYY-MM-DD`) — do NOT hard-code a date (REVIEWS LOW finding fix). Read `/tmp/217-verdict.json` for the verdict; read `217-capture-window.json.validation.router_write_coverage` for the driven-segment gate.

(1) If verdict is `no_action` AND `router_write_coverage == "present"`: append a `## Closed — <YYYY-MM-DD> (Phase 217)` section to the todo body recording: the close outcome, the `cycle_total.avg_ms`, `cycle_total.p99_ms`, `max_category_pct`, `dominant_category`, the explicit "absolute-bar partition supersedes ±15%/±25%-vs-v1.39 (D-04)" note, and a pointer to `.planning/perf/217-cycle-budget-summary.md` and the `.profile.json` artifact. Then `git mv` the file from `.planning/todos/pending/` to `.planning/todos/done/`.

(2) If verdict is `no_action` AND `router_write_coverage == "absent_gap"`: append a `## Closed (Conditional) — <YYYY-MM-DD> (Phase 217)` section noting the verdict is "no-action conditional on steady-state only — router-write category unverified", same numeric context as (1), plus the carry-forward note that a future capture should land a driven segment to verify the router category. Then `git mv` to `done/`.

(3) If verdict is `promote`: append a `## Promoted — <YYYY-MM-DD> (Phase 217)` section recording which clause tripped (failed headroom: `cycle_total.avg_ms >= 40` or `p99_ms >= 50`; OR failed dominance: `dominant_category` at `max_category_pct%`), the recommendation to open a v1.46+ optimization phase targeting `<dominant_category>`, and a pointer to the summary. Then `git mv` to `done/` (the original todo's close-condition 2 is "promoted to a follow-on phase").

In all three cases the file ends in `done/`. Do NOT delete or rewrite the original todo content — append only.</action>
  <verify>
    <automated>set -e; T=.planning/todos/done/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md; test -f "$T"; ! test -f .planning/todos/pending/2026-04-15-profile-post-hotpath-baseline-on-production-wan.md; grep -qE "## (Closed|Promoted)" "$T"; grep -q "Phase 217" "$T"; grep -q "217-cycle-budget-summary.md" "$T"; END_DATE=$(jq -r '.window.end_utc[:10]' .planning/perf/217-capture-window.json); grep -q "$END_DATE" "$T" || { echo "FAIL: close date $END_DATE not found in todo" >&2; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - The todo file now lives in `.planning/todos/done/` and NOT in `.planning/todos/pending/`.
    - It contains a `## Closed`, `## Closed (Conditional)`, or `## Promoted` section attributed to Phase 217.
    - It references `.planning/perf/217-cycle-budget-summary.md`.
    - The close-section date matches `217-capture-window.json.window.end_utc[:10]` (no hard-coded `2026-05-30` — REVIEWS LOW finding fix).
    - Original todo content is preserved (not deleted or rewritten).
    - The disposition matches the actual verdict (no_action / no_action conditional / promote) and the driven-segment coverage in `217-capture-window.json.validation.router_write_coverage`.
  </acceptance_criteria>
  <done>The pending profiling todo is moved to `done/` with a Closed/Closed-Conditional/Promoted disposition referencing the summary, dated from the actual capture window (PERF-01 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieved production NDJSON → committed repo artifacts | .profile.json + .ingestion.json + summary derive from JSON-format DEBUG records that may carry richer structured operator detail than text logs |
| dev workstation → wanctl-history CLI | --ingestion-rate reads the per-WAN SQLite history DB (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-06 | Information disclosure | .planning/perf/v1.45-baseline-spectrum-<date>.profile.json + .ingestion.json + 217-cycle-budget-summary.md | mitigate | The .profile.json is aggregate per-label timing stats (`count/min/p50/avg/max/p95/p99` only — no IPs, secrets, or operating points by construction of `scripts/profiling_collector_json.py`). The .ingestion.json contains rows-per-second per metric — non-secret operating context (Phase 212/213 precedent). Before commit, grep all three for credential/key/IP patterns and confirm only non-secret timing/operating data is present (CLAUDE.md public-safe policy). The raw NDJSON source stays in `.planning/perf/capture/` (gitignored from Plan 01) — never crosses the commit boundary. |
| T-217-07 | Repudiation | todo lifecycle move | accept | `git mv` preserves history; the close/promote section is dated from `217-capture-window.json.window.end_utc` (not hard-coded) and attributed to Phase 217. Low risk, fully reversible via git. |
| T-217-10 | Tampering / data integrity | jq verdict pipeline | mitigate | The verdict math is encoded inline in this PLAN.md and committed to docs/PROFILING.md (Plan 01); reproducibility is guaranteed by the .profile.json + the encoded pipeline. The verdict object (`/tmp/217-verdict.json`) is regenerable from committed inputs. |

No HIGH threats: this plan reads NDJSON and emits aggregate stats + a markdown summary + a todo move. The disclosure surface is the committed artifacts, mitigated by the construction of the parser (timing stats only) and a pre-commit secret scan. No production interaction, no new endpoints, no control-path change.
</threat_model>

<verification>
- `.planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.profile.json` exists, parses, contains `autorate_cycle_total` with `count >= 60000` and populated `avg_ms`/`p99_ms`.
- `.planning/perf/v1.45-baseline-spectrum-<YYYYMMDD>.ingestion.json` exists; either real windowed `wanctl-history` output (with `--from/--to` matching `217-capture-window.json`) or a structured unmeasured stub — never a fabricated number.
- The verdict jq pipeline produced a `/tmp/217-verdict.json` with `passes_headroom`, `passes_dominance`, `verdict`, and the underlying numbers.
- `.planning/perf/217-cycle-budget-summary.md` states the cycle budget + headroom, 4-category dominance + parent-only rule, windowed storage attribution, observer-effect (number-or-caveat — never a same-window delta), and the explicit PERF-03 decision; no `TBD`/`<...>` placeholders; no production IPs.
- Driven-segment coupling enforced: if `router_write_coverage == absent_gap` and verdict is `no_action`, the summary contains either `conditional on steady-state` or `promote`.
- Storage-write cost is attributed out-of-band (never asserted as a % of cycle_total).
- The profiling todo is in `.planning/todos/done/` with a `Closed`/`Closed (Conditional)`/`Promoted` disposition referencing the summary, dated from `window.end_utc[:10]`.
- No `src/` files modified.
- Pre-commit secret scan of `.planning/perf/` artifacts passes (no keys/credentials/real IPs).
</verification>

<success_criteria>
- Committed `.profile.json` artifact for the Spectrum baseline produced by the new JSON parser (D-06, PERF-01).
- Windowed storage attribution via `wanctl-history --ingestion-rate --from/--to` matching the actual capture window (research Pitfall 4 closed; PERF-02 fifth category attributed out-of-band, never as a hot-path %).
- 1-page summary with the PERF-02 dominance breakdown (router parent-only) and the falsifiable D-03 verdict pair (PERF-02, PERF-03).
- Explicit deprioritize-or-promote decision sentence recorded; driven-segment coupling enforced (PERF-03).
- Pending profiling todo closed or promoted, dated from the actual capture window (PERF-01).
- Zero control-path edits.
</success_criteria>

<output>
After completion, create `.planning/phases/217-production-cycle-budget-baseline/217-03-SUMMARY.md`
recording: the cycle_total numbers, dominant category + %, observer-effect (number-or-caveat),
storage attribution status, the verdict pair, the driven-segment coverage status,
and the todo disposition.
</output>
</content>
</invoke>