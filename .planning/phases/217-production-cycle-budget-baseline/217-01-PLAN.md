---
phase: 217-production-cycle-budget-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PROFILING.md
  - scripts/profiling_collector_json.py
  - .planning/perf/.gitkeep
  - .planning/perf/capture/.gitignore
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "D-07: A repeatable enable/capture/revert/analyze profiling runbook exists at docs/PROFILING.md (the missing runbook the todo's files: block referenced) and encodes the JSON capture path"
    - "D-06: The .planning/perf/ artifact directory exists in the repo (home for v1.45-baseline-spectrum-<date>.profile.json written in Plan 03); .planning/perf/capture/ is gitignored so raw DEBUG NDJSON is not committed"
    - "PERF-02: An offline JSON parser scripts/profiling_collector_json.py converts NDJSON 'Cycle timing' records into the existing profiling_collector.py --output json schema, so the Plan 03 analysis pipeline has a falsifiable source for autorate_cycle_total stats"
  artifacts:
    - path: "docs/PROFILING.md"
      provides: "Durable profiling runbook (enable JSON-format drop-in, drive load, capture spectrum_debug.log, revert, analyze with the new JSON parser, interpret vs D-03 absolute bars)"
      contains: "systemctl revert wanctl@spectrum"
    - path: "scripts/profiling_collector_json.py"
      provides: "Offline NDJSON parser: filters .message=='Cycle timing' records, extracts cycle_total_ms and *_ms extras, reconstructs canonical autorate_* labels, emits the existing scripts/profiling_collector.py --output json schema (per-label count/min/p50/avg/max/p95/p99 ms)"
      min_lines: 80
    - path: ".planning/perf/.gitkeep"
      provides: "Committed artifact directory placeholder (home for v1.45-baseline-spectrum-<date>.profile.json and 217-cycle-budget-summary.md)"
    - path: ".planning/perf/capture/.gitignore"
      provides: "Keeps the raw DEBUG NDJSON retrieval directory out of git (raw production DEBUG logs are not committed per D-06 + research Pitfall 7)"
  key_links:
    - from: "scripts/profiling_collector_json.py"
      to: "scripts/profiling_collector.py --output json schema"
      via: "emits identical per-label dict with count/min/p50/avg/max/p95/p99 keyed by autorate_*"
      pattern: "autorate_cycle_total"
    - from: "docs/PROFILING.md"
      to: "scripts/profiling_collector_json.py + wanctl-history --ingestion-rate"
      via: "documented analyze step invokes the JSON parser then the windowed storage attribution"
      pattern: "profiling_collector_json.py"
---

<objective>
Build the repo-side scaffolding for the Spectrum cycle-budget baseline: create the
`.planning/perf/` artifact directory (with `.planning/perf/capture/` gitignored for raw
NDJSON retrieval), author the missing `docs/PROFILING.md` runbook (D-07) encoding the
JSON-format capture data path, and write a small offline JSON parser
`scripts/profiling_collector_json.py` that converts NDJSON `"Cycle timing"` records into
the existing `scripts/profiling_collector.py --output json` schema.

Purpose: This is the autonomous, no-production-touch foundation. It runs alongside the
pilot+main capture (Plan 02) and produces the parser that Plan 03 (analysis) consumes.
The existing regex-based `scripts/profiling_collector.py` cannot produce
`autorate_cycle_total` from text DEBUG logs (research Corrections finding #1, REVIEWS
HIGH finding 1) — the new JSON parser is the corrected data path.

Output: `docs/PROFILING.md`, `scripts/profiling_collector_json.py`,
`.planning/perf/.gitkeep`, `.planning/perf/capture/.gitignore`.
NO `src/` control-path changes. NO production-host interaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md
@.planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md
@.planning/phases/217-production-cycle-budget-baseline/217-REVIEWS.md

<interfaces>
<!-- Input shape the parser consumes (NDJSON, one record per line). Source: research §"Capture & Analysis Data Path > Per-record JSON shape", JSONFormatter at src/wanctl/logging_utils.py:74-102 + record_cycle_profiling at src/wanctl/perf_profiler.py:266-294. -->
<!-- Per-record example: -->
<!--   {"timestamp":"2026-05-30T12:34:56.789Z","level":"DEBUG","logger":"cake_continuous_spectrum","message":"Cycle timing", -->
<!--    "cycle_total_ms":14.2,"overrun":false, -->
<!--    "rtt_measurement_ms":8.1,"state_management_ms":0.3,"router_communication_ms":2.4,"signal_processing_ms":0.5, -->
<!--    "ewma_spike_ms":0.1,"cake_stats_ms":1.2,"congestion_assess_ms":0.4,"irtt_observation_ms":0.0, -->
<!--    "logging_metrics_ms":0.7,"router_apply_primary_ms":0.0,"router_apply_pending_ms":0.0, -->
<!--    "router_write_download_ms":0.0,"router_write_upload_ms":0.0,"router_write_skipped_ms":0.0,"router_write_fallback_ms":0.0} -->
<!-- Label transform: the JSON keys have the `autorate_` prefix stripped and `_ms` appended (perf_profiler.py:290-293). The parser must invert this: -->
<!--   `cycle_total_ms`      -> `autorate_cycle_total` -->
<!--   `rtt_measurement_ms`  -> `autorate_rtt_measurement` -->
<!--   `<x>_ms`              -> `autorate_<x>` -->
<!-- Output schema (must match scripts/profiling_collector.py --output json exactly; see scripts/profiling_collector.py:51-80 calculate_statistics): -->
<!--   { "<autorate_label>": {"count": int, "min_ms": f, "p50_ms": f, "avg_ms": f, "max_ms": f, "p95_ms": f, "p99_ms": f}, ... } -->
<!-- Stats math: pure sort+index percentile, identical to OperationProfiler.stats() at src/wanctl/perf_profiler.py:132-153. -->
<!-- PERF-02 category -> hot-path label map (from 217-RESEARCH.md Q2 and wan_controller.py:2434-2450): -->
<!--   RTT measurement       -> autorate_rtt_measurement -->
<!--   CAKE stats            -> autorate_cake_stats -->
<!--   Router communication  -> autorate_router_communication  (PARENT timer; do NOT also sum router_apply_*/router_write_* — they are children, double-count trap) -->
<!--   Logging / metrics     -> autorate_logging_metrics -->
<!--   Storage writes        -> NO hot-path label by design (DeferredIOWorker); attributed out-of-band via wanctl-history --ingestion-rate --from <S> --to <E> -->
<!-- Falsifiable bars (research §"Acceptance criteria the planner can encode"): -->
<!--   passes_headroom  = cycle_total.avg_ms < 40.0  AND  cycle_total.p99_ms < 50.0 -->
<!--   passes_dominance = max(category_pct.values()) < 40.0 -->
<!--   no_action        = passes_headroom AND passes_dominance, else promote -->
<!-- Drop-in (verbatim, the runbook encodes this in the Enable section): -->
<!--   [Service] -->
<!--   Environment=WANCTL_LOG_FORMAT=json -->
<!--   ExecStart= -->
<!--   ExecStart=/usr/bin/python3 /opt/wanctl/autorate_continuous.py --config /etc/wanctl/spectrum.yaml --profile --debug -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/perf/ artifact dir + gitignored capture subdir</name>
  <read_first>
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-06 artifact path)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Wave 0 gaps"; Pitfall 7 — don't commit raw DEBUG logs)
  </read_first>
  <files>.planning/perf/.gitkeep, .planning/perf/capture/.gitignore</files>
  <action>Create directory `.planning/perf/` and add an empty `.gitkeep` so the directory is tracked — this is the D-06 home for `v1.45-baseline-spectrum-<date>.profile.json` and `217-cycle-budget-summary.md` (written in Plan 03). Then create directory `.planning/perf/capture/` and add a `.gitignore` inside it with the content `*` followed by `!.gitignore` on the next line, so the directory exists in the tree but NDJSON retrievals (`spectrum_debug.log*`, `spectrum_journal.ndjson`, pilot capture, `/health` polls) stay out of git per research Pitfall 7 ("Committing raw DEBUG capture to the repo"). Do not write any `.profile.json` or summary in this plan — those land in Plan 03.</action>
  <verify>
    <automated>test -d .planning/perf && test -f .planning/perf/.gitkeep && test -d .planning/perf/capture && test -f .planning/perf/capture/.gitignore && grep -q '^\*$' .planning/perf/capture/.gitignore && grep -q '^!.gitignore$' .planning/perf/capture/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/perf/` exists and contains `.gitkeep`.
    - Directory `.planning/perf/capture/` exists and contains a `.gitignore` whose body is `*` then `!.gitignore` (i.e. ignores everything except itself).
    - No other files written under `.planning/perf/` in this plan.
  </acceptance_criteria>
  <done>The artifact dir is committable and the capture subdir is gitignored so raw DEBUG NDJSON cannot be accidentally tracked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write scripts/profiling_collector_json.py — NDJSON → profile.json parser</name>
  <read_first>
    - scripts/profiling_collector.py (existing regex collector; lines 51-80 `calculate_statistics` and the `--output json` writer at ~:226-227 — the new parser must emit the IDENTICAL schema so analyze_profiling.py and the Plan 03 jq pipeline don't have to branch on source)
    - src/wanctl/perf_profiler.py (lines 132-153 `OperationProfiler.stats` — the percentile math; lines 266-294 `record_cycle_profiling` — how the `extra` dict is built and which keys appear)
    - src/wanctl/logging_utils.py (lines 74-102 JSONFormatter — confirms `extra=` keys are preserved verbatim as top-level JSON keys)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Capture & Analysis Data Path > Per-record JSON shape"; §"Q1 Profiler output schema"; Assumption A2)
  </read_first>
  <behavior>
    - Test 1 (golden parse): given a 3-line NDJSON fixture with `{"message":"Cycle timing","cycle_total_ms":12.0,"rtt_measurement_ms":6.0,"cake_stats_ms":1.0,"router_communication_ms":2.0,"logging_metrics_ms":1.0}` (and two more rows with `cycle_total_ms` 14.0 and 16.0), the parser emits a dict containing `autorate_cycle_total` with `count==3`, `min_ms==12.0`, `max_ms==16.0`, `avg_ms==14.0`, and `p50_ms`/`p95_ms`/`p99_ms` populated by sorted-index percentile.
    - Test 2 (label reconstruction): same fixture yields keys `autorate_rtt_measurement`, `autorate_cake_stats`, `autorate_router_communication`, `autorate_logging_metrics` (the `_ms` suffix is stripped and `autorate_` is prepended). `cycle_total_ms` maps to `autorate_cycle_total` specifically.
    - Test 3 (non-cycle-timing records ignored): records with `message != "Cycle timing"` (e.g. a stray INFO line that snuck into the NDJSON) are silently skipped — they don't appear in any label's count.
    - Test 4 (malformed line tolerance): a non-JSON line (e.g. a stray text line from journald `-o cat`) is skipped without crashing; valid lines around it still parse.
    - Test 5 (missing extras): a `Cycle timing` record with only `cycle_total_ms` (no subsystem extras) increments `autorate_cycle_total.count` but does not create spurious zero-valued subsystem entries.
    - Test 6 (CLI shape): `--input <ndjson>` and `--output <profile.json>` flags both work; absent `--output` writes to stdout; absent `--input` reads stdin. Exit code 0 on success; exit code 2 with stderr message if zero `"Cycle timing"` records found in the input.
  </behavior>
  <files>scripts/profiling_collector_json.py</files>
  <action>Create a standalone Python 3.11 stdlib-only CLI script `scripts/profiling_collector_json.py` (target ~80-150 lines). Required behavior:
(1) CLI: positional `input` (NDJSON path) OR `--input <path>` (synonymous); `--output <path>` (default stdout); standard `--help`. If `input` is `-` or absent and `--input` is absent, read stdin.
(2) Read NDJSON line-by-line. For each line: `try: rec = json.loads(line)` — skip silently on `JSONDecodeError`. Skip records where `rec.get("message") != "Cycle timing"`.
(3) For each retained record, iterate `rec.items()`. For any key ending in `_ms` whose value is a number (int or float), accumulate the value into a per-label samples list. Reconstruct the canonical label: `cycle_total_ms` → `autorate_cycle_total`; for any other `<x>_ms` → `autorate_<x>`. Ignore the `overrun` key and any non-numeric / non-`_ms` keys.
(4) After consuming input, compute per-label stats EXACTLY as `scripts/profiling_collector.py:calculate_statistics` does (sort + index percentile). For each label emit a dict with `count` (int), `min_ms`, `p50_ms`, `avg_ms`, `max_ms`, `p95_ms`, `p99_ms` (all floats, rounded to 3 decimals via `round(x, 3)` to match the existing collector's float precision). Percentile indices: p50 = `samples[n//2]`, p95 = `samples[int(n*0.95)]`, p99 = `samples[int(n*0.99)]` with bounds clamp to `n-1`.
(5) Emit the resulting dict to the chosen output as `json.dumps(..., indent=2, sort_keys=True)`. The schema must match `scripts/profiling_collector.py --output json` byte-for-byte at the key level (so downstream tools that already consume that schema work unchanged).
(6) Error path: if zero `"Cycle timing"` records were found, write `ERROR: no Cycle timing records in input` to stderr and exit 2 (NOT 1 — reserve 1 for unexpected exceptions; the planner uses 2 as the "wrong source file / wrong capture format" sentinel).
(7) NO third-party imports. Stdlib only (`argparse`, `json`, `sys`, `pathlib`). Module docstring shows the canonical invocation: `python scripts/profiling_collector_json.py .planning/perf/capture/spectrum_debug.ndjson --output .planning/perf/v1.45-baseline-spectrum-<date>.profile.json`.
Reference the research section name in a header comment: `# See .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md §"Capture & Analysis Data Path (recommended)" for the data-path contract.`
Do NOT import anything from `src/wanctl/`; this script is offline, stdlib-only, and must run without the project venv on stripped-down workstations (the executor may still invoke it via `.venv/bin/python` for ruff/style consistency).</action>
  <verify>
    <automated>.venv/bin/python -c "
import json, subprocess, tempfile, os, sys
# Fixture: 3 'Cycle timing' records + 1 stray INFO record + 1 malformed line
lines = [
  '{\"message\":\"Cycle timing\",\"cycle_total_ms\":12.0,\"rtt_measurement_ms\":6.0,\"cake_stats_ms\":1.0,\"router_communication_ms\":2.0,\"logging_metrics_ms\":1.0}',
  'this is not json at all',
  '{\"message\":\"Some other log\",\"cycle_total_ms\":999.0}',
  '{\"message\":\"Cycle timing\",\"cycle_total_ms\":14.0,\"rtt_measurement_ms\":7.0,\"cake_stats_ms\":1.5,\"router_communication_ms\":2.5,\"logging_metrics_ms\":1.2}',
  '{\"message\":\"Cycle timing\",\"cycle_total_ms\":16.0,\"rtt_measurement_ms\":8.0,\"cake_stats_ms\":2.0,\"router_communication_ms\":3.0,\"logging_metrics_ms\":1.5,\"router_write_download_ms\":0.3}',
]
f = tempfile.NamedTemporaryFile('w', suffix='.ndjson', delete=False)
f.write('\n'.join(lines) + '\n'); f.close()
r = subprocess.run(['.venv/bin/python','scripts/profiling_collector_json.py',f.name], capture_output=True, text=True)
os.unlink(f.name)
assert r.returncode == 0, f'exit={r.returncode} stderr={r.stderr}'
out = json.loads(r.stdout)
assert 'autorate_cycle_total' in out, out
assert out['autorate_cycle_total']['count'] == 3, out['autorate_cycle_total']
assert out['autorate_cycle_total']['min_ms'] == 12.0, out['autorate_cycle_total']
assert out['autorate_cycle_total']['max_ms'] == 16.0, out['autorate_cycle_total']
assert out['autorate_cycle_total']['avg_ms'] == 14.0, out['autorate_cycle_total']
assert 'autorate_rtt_measurement' in out and out['autorate_rtt_measurement']['count'] == 3, out
assert 'autorate_cake_stats' in out and 'autorate_router_communication' in out and 'autorate_logging_metrics' in out
assert 'autorate_router_write_download' in out and out['autorate_router_write_download']['count'] == 1
assert all(k.startswith('autorate_') for k in out.keys()), 'all labels canonical autorate_*'
# Error path: empty NDJSON
import io
ef = tempfile.NamedTemporaryFile('w', suffix='.ndjson', delete=False); ef.close()
r2 = subprocess.run(['.venv/bin/python','scripts/profiling_collector_json.py',ef.name], capture_output=True, text=True)
os.unlink(ef.name)
assert r2.returncode == 2, f'expected exit 2 for empty, got {r2.returncode}'
print('OK: parser passes golden + label-reconstruction + skip-stray + skip-malformed + missing-extras + error-path')
"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/profiling_collector_json.py` exists, is executable as `.venv/bin/python scripts/profiling_collector_json.py <path>`, and uses stdlib only (no third-party `import` lines).
    - Under the 5-line fixture (3 Cycle timing + 1 stray INFO + 1 malformed), the script exits 0 and emits a dict with `autorate_cycle_total.count == 3`, `min_ms == 12.0`, `max_ms == 16.0`, `avg_ms == 14.0`.
    - All output keys are canonical `autorate_*` form; `cycle_total_ms` → `autorate_cycle_total`; `<x>_ms` → `autorate_<x>`.
    - Non-`Cycle timing` records and malformed lines are skipped without crashing.
    - `autorate_router_write_download.count == 1` (proves missing-extra records don't pollute other labels' counts).
    - With an empty NDJSON input, exits 2 with a stderr message (NOT 0, NOT 1).
    - Output schema matches `scripts/profiling_collector.py --output json` (per-label dict with `count, min_ms, p50_ms, avg_ms, max_ms, p95_ms, p99_ms`).
  </acceptance_criteria>
  <done>The JSON parser is in `scripts/`, stdlib-only, and produces the canonical collector schema from NDJSON `Cycle timing` records — passing the fixture verify above.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/PROFILING.md runbook (D-07) — JSON capture data path</name>
  <read_first>
    - docs/PERFORMANCE.md (house style — H1, short intro, `## Section` headers, fenced `bash` blocks, "Production Standard / Operational Guardrails" framing)
    - docs/archive/PROFILING.md (the stale archived runbook this supersedes — its parser commands point the regex collector at a `--profile`-only main log and return no `autorate_cycle_total`)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (§"Corrections Superseding the Previous Research"; §"Capture & Analysis Data Path (recommended)"; §"Q7 Runbook content"; Pitfalls 1, 2, 3, 4, 5, 6, 7)
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-05 transient drop-in, D-06 artifact path, D-07 runbook requirement)
  </read_first>
  <files>docs/PROFILING.md</files>
  <action>Create `docs/PROFILING.md` matching `docs/PERFORMANCE.md` house style (H1 title, short intro paragraph, `## Section` headers, fenced `bash`/`ini` blocks, "Production Standard / Operational Guardrails" framing). Sections, in this order:

(1) **Title + intro:** "Production Profiling Runbook" H1. One paragraph stating this is the active profiling runbook for v1.45+; supersedes `docs/archive/PROFILING.md` (which points at a broken regex/text-DEBUG data path); links to `docs/PERFORMANCE.md` for performance background.

(2) **`## Critical: capture format is JSON, not text`** — prominently explain that `autorate_cycle_total` is NOT emitted as a parseable text DEBUG line (it lives in the `extra=` dict of `"Cycle timing"`, which the text formatter drops; only the JSON formatter preserves it). Cite `src/wanctl/perf_profiler.py:266-294` and `src/wanctl/logging_utils.py:91-96`. State that the capture window therefore runs under `WANCTL_LOG_FORMAT=json` AND `--profile --debug`. State that the legacy `scripts/profiling_collector.py` regex collector is NOT load-bearing for cycle_total under this runbook; the new `scripts/profiling_collector_json.py` is.

(3) **`## Enable (transient systemd drop-in)`** — `sudo systemctl edit wanctl@spectrum` and paste the EXACT drop-in:
```ini
[Service]
Environment=WANCTL_LOG_FORMAT=json
ExecStart=
ExecStart=/usr/bin/python3 /opt/wanctl/autorate_continuous.py --config /etc/wanctl/spectrum.yaml --profile --debug
```
Followed by `sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum`. Record window start UTC. Note that ExecStart= MUST be cleared then re-set (systemd single-value directive).

(4) **`## Pilot first (5 min disk-cost + JSON-key gate)`** — before committing to the full 1h window, run a 5-minute pilot. After the 5 minutes: `ssh <spectrum-host> 'jq -c "select(.message == \"Cycle timing\") | keys" /var/log/wanctl/spectrum_debug.log | head -1'` MUST list `cycle_total_ms` among the keys (or extras visible at the top level — research Assumption A2). Also tail `journalctl -u wanctl@spectrum -f` during the pilot for `Cycle overrun:` warnings or unit restarts. If either check fails, abort and re-evaluate before the full window.

(5) **`## Drive load (D-02 driven segment, inside the window)`** — note `autorate_router_write_*` only fire on rate change; reference `scripts/phase213-baseline-capture.sh --host dallas --flent-duration 60 --tests tcp_upload,rrul --wans spectrum` run from the dev VM inside the capture window.

(6) **`## Capture (≥1h)`** — let the window run ≥1h (≈72000 cycles at 20Hz). Watch `journalctl -u wanctl@spectrum -f` for overruns/restarts. Record window end UTC.

(7) **`## Revert (mandatory safety gate)`** — `sudo systemctl revert wanctl@spectrum && sudo systemctl daemon-reload && sudo systemctl restart wanctl@spectrum`. Then verify: `systemctl cat wanctl@spectrum` shows NO drop-in (`override.conf` gone); `ps -ef | grep [a]utorate_continuous` shows NEITHER `--profile` NOR `--debug` NOR `WANCTL_LOG_FORMAT=json` in the running ExecStart. Service active; `/health.version == 1.45.0`.

(8) **`## Retrieve (DEBUG sink + journald)`** — the DEBUG sink is `/var/log/wanctl/spectrum_debug.log` (NOT `spectrum.log`, which is INFO-only — cite `configs/spectrum.yaml:153-154` and `src/wanctl/logging_utils.py:199-208`). Pull the rotated set chronologically: `scp '<spectrum-host>:/var/log/wanctl/spectrum_debug.log*' .planning/perf/capture/` then `cat spectrum_debug.log.3 spectrum_debug.log.2 spectrum_debug.log.1 spectrum_debug.log > spectrum_debug.ndjson` (oldest→newest). Belt-and-braces: also save `ssh <spectrum-host> 'journalctl -u wanctl@spectrum --since "<start>" --until "<end>" -o cat' > .planning/perf/capture/spectrum_journal.ndjson`. The raw NDJSON files stay under `.planning/perf/capture/` which is gitignored — they are NOT committed.

(9) **`## Analyze`** — three invocations:
```bash
# (a) Aggregate the captured NDJSON into the committed .profile.json (D-06):
.venv/bin/python scripts/profiling_collector_json.py \
    .planning/perf/capture/spectrum_debug.ndjson \
    --output .planning/perf/v1.45-baseline-spectrum-$(date +%Y%m%d).profile.json

# (b) Storage-write attribution (out-of-band; PERF-02 fifth category). EXPLICIT --from/--to required
#     (default is last hour — src/wanctl/history.py:616-624):
.venv/bin/python -m wanctl.history --ingestion-rate --wan spectrum \
    --from "<window start ISO>" --to "<window end ISO>" --json \
  > .planning/perf/v1.45-baseline-spectrum-$(date +%Y%m%d).ingestion.json

# (c) Verdict (jq pipeline over the .profile.json — encoded inline):
jq -n --slurpfile p .planning/perf/v1.45-baseline-spectrum-*.profile.json '
  def cat(labels): [labels[] as $l | $p[0][$l].avg_ms // 0] | add;
  ($p[0]["autorate_cycle_total"].avg_ms) as $total
  | ($p[0]["autorate_cycle_total"].p99_ms) as $p99
  | { utilization_pct: ($total / 50 * 100),
      headroom_avg_ms: (50 - $total),
      passes_headroom: ($total < 40 and $p99 < 50),
      categories: {
        rtt_measurement: cat(["autorate_rtt_measurement"]),
        cake_stats: cat(["autorate_cake_stats"]),
        router_communication: cat(["autorate_router_communication"]),
        logging_metrics: cat(["autorate_logging_metrics"])
      }}
  | .category_pct = (.categories | with_entries(.value = (.value / $total * 100)))
  | .max_category_pct = (.category_pct | [.[]] | max)
  | .passes_dominance = (.max_category_pct < 40)
  | .verdict = (if .passes_headroom and .passes_dominance then "no_action" else "promote" end)'
```
Critical sub-rule (cite Pitfall in research Q2): for the router category use `autorate_router_communication` (parent) ONLY — do NOT sum the `router_apply_*` / `router_write_*` sub-timers (they live inside the parent; summing double-counts).

(10) **`## Interpret vs D-03 absolute bars`** — close/no-action if `cycle_total.avg_ms < 40.0` AND `cycle_total.p99_ms < 50.0` AND `max(category_pct) < 40.0`. Promote to a v1.46+ optimization phase if any of those fail. Note that under D-04, archived v1.0/v1.9 numbers are informational context only and never a pass/fail gate.

(11) **`## Observer-effect`** — DEBUG capture inflates cycle_total (~12 records/cycle × 20Hz ≈ 240 records/sec extra I/O). Two acceptable shapes: (a) adjacent ON/OFF `/health` windows (poll `cycle_budget.cycle_time_ms.avg` every 10s for ~5 min with `--debug` ON, then again with the drop-in reverted, report the delta) — RECOMMENDED, ~10 min total; (b) documented caveat: state that DEBUG-captured cycle_total is an UPPER BOUND on real cost and that passing D-03 under DEBUG is a stronger pass. Do NOT mid-window cross-check `/health` against DEBUG cycle_total — both read the same in-process profiler (`src/wanctl/health_check.py:101`); the delta is ~0 by construction (Pitfall 3).

(12) **`## Common pitfalls`** — short bulleted list pointing at the research's Pitfalls 1-7: regex collector against JSON capture (won't match); pulling `spectrum.log` instead of `spectrum_debug.log` (INFO-only); same-window `/health` cross-check (~0 by construction); editing the checked-in unit instead of a drop-in; committing raw NDJSON (use `.planning/perf/capture/` gitignored dir); `wanctl-history --ingestion-rate` without `--from/--to`.

Do NOT recommend any threshold/algorithm/control-path change. Keep it operational and concise like `docs/PERFORMANCE.md` — runbook, not essay. Use placeholders (`<spectrum-host>`, `<window start ISO>`) for any host-specific identifier; no real IPs/hostnames (CLAUDE.md public-safe policy).</action>
  <verify>
    <automated>test -f docs/PROFILING.md && grep -q "WANCTL_LOG_FORMAT=json" docs/PROFILING.md && grep -q -- "--profile --debug" docs/PROFILING.md && grep -q "systemctl revert wanctl@spectrum" docs/PROFILING.md && grep -q "profiling_collector_json.py" docs/PROFILING.md && grep -q "spectrum_debug.log" docs/PROFILING.md && grep -q "40" docs/PROFILING.md && grep -q "ingestion-rate" docs/PROFILING.md && grep -q "router_communication" docs/PROFILING.md && grep -q "Pilot" docs/PROFILING.md && ! grep -qE "10\.10\.[0-9]+\.[0-9]+" docs/PROFILING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PROFILING.md` exists.
    - Contains the JSON-format requirement (greppable: `WANCTL_LOG_FORMAT=json`).
    - Contains the `--profile --debug` requirement (greppable: `--profile --debug`).
    - Contains the mandatory revert step (greppable: `systemctl revert wanctl@spectrum`).
    - References the new JSON parser (greppable: `profiling_collector_json.py`).
    - References `spectrum_debug.log` as the DEBUG sink (NOT `spectrum.log` as the DEBUG source).
    - Contains the D-03 absolute-bar interpretation (the `40` bar appears) and the router parent-only rule (greppable: `router_communication`).
    - Contains the windowed storage attribution (greppable: `ingestion-rate`).
    - Has a "Pilot" section gating the full window on a 5-min disk-cost + JSON-key check.
    - Contains NO real production IP/hostname (no `10.10.x.x` literal); uses placeholders.
    - Contains no recommendation to change thresholds/algorithms/control-path code.
  </acceptance_criteria>
  <done>`docs/PROFILING.md` is a durable enable→pilot→drive→capture→revert→retrieve→analyze→interpret runbook encoding the corrected JSON data path and passing all grep checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → committed docs/scripts | New runbook + parser are static, no runtime input, no production reach |
| operator-followed runbook → live production host | docs/PROFILING.md prescribes a transient systemd drop-in on a 24/7 daemon (executed by Plan 02, not this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-01 | Information disclosure | docs/PROFILING.md | mitigate | Runbook uses placeholders (`<spectrum-host>`, `<window start ISO>`) and embeds no secrets/real IPs/credentials, per CLAUDE.md public-safe policy. Final task verifies absence of `10.10.x.x` literals. |
| T-217-02 | Tampering | scripts/profiling_collector_json.py | accept | Stdlib-only read-only post-processor; no network, no writes outside the chosen output path, no production reach. Low-value local analysis tool. |
| T-217-09 | Information disclosure (JSON capture) | docs/PROFILING.md retrieval section | mitigate | The runbook explicitly routes raw `spectrum_debug.log*` retrieval into `.planning/perf/capture/` which is gitignored (Task 1) — raw NDJSON never enters git. JSON-format logs may contain richer structured operator detail than the text format; the runbook calls this out and instructs the operator to commit ONLY the aggregate `.profile.json`, the `ingestion.json`, and the summary, NEVER the raw NDJSON. |

No HIGH threats: this plan writes only static documentation, a read-only stdlib parser, and directory scaffolding. No new inputs, endpoints, or production interaction in this plan.
</threat_model>

<verification>
- `.planning/perf/.gitkeep` exists and `.planning/perf/capture/.gitignore` ignores all but itself.
- `scripts/profiling_collector_json.py` passes the inline fixture: 3 cycle samples through, stray INFO + malformed lines skipped, label reconstruction correct, error path returns exit 2 on empty input.
- `docs/PROFILING.md` exists and passes the grep checks (`WANCTL_LOG_FORMAT=json`, `--profile --debug`, `systemctl revert`, `profiling_collector_json.py`, `spectrum_debug.log`, `40`, `ingestion-rate`, `router_communication`, `Pilot`) and contains no `10.10.x.x` literal.
- `.venv/bin/ruff check scripts/profiling_collector_json.py` passes (style consistency with the repo).
- No `src/` files modified.
</verification>

<success_criteria>
- The repo has a durable profiling runbook encoding the corrected JSON data path (D-07 satisfied, supersedes broken `docs/archive/PROFILING.md`).
- The `.planning/perf/` artifact directory exists; the `.planning/perf/capture/` retrieval subdir is gitignored (D-06 prerequisite + research Pitfall 7).
- An offline NDJSON → `.profile.json` parser exists and is correct on a known fixture — Plan 03's analysis path has a falsifiable cycle_total source (replaces the broken regex/text-DEBUG assumption).
- Zero control-path edits.
</success_criteria>

<output>
After completion, create `.planning/phases/217-production-cycle-budget-baseline/217-01-SUMMARY.md`
</output>
</content>
</invoke>