---
phase: 217-production-cycle-budget-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PROFILING.md
  - scripts/profiling_categorize.py
  - .planning/perf/.gitkeep
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "D-07: A repeatable enable/capture/revert/analyze profiling runbook exists at docs/PROFILING.md (the missing runbook the todo's files: block referenced)"
    - "D-06: The .planning/perf/ artifact directory exists in the repo (home for v1.45-baseline-spectrum-<date>.profile.json written in Plan 03)"
    - "D-03: A post-processing helper rolls per-label collector JSON into the 4 PERF-02 hot-path categories and emits the >40% dominance verdict (the absolute-bar dominance test)"
  artifacts:
    - path: "docs/PROFILING.md"
      provides: "Durable profiling runbook (enable drop-in, drive load, capture, revert, analyze, interpret vs D-03 bars)"
      contains: "systemctl revert wanctl@spectrum"
    - path: "scripts/profiling_categorize.py"
      provides: "Roll collector JSON into PERF-02 categories + compute >40% dominance verdict vs 50ms budget"
      min_lines: 40
    - path: ".planning/perf/.gitkeep"
      provides: "Committed artifact directory placeholder"
  key_links:
    - from: "scripts/profiling_categorize.py"
      to: "scripts/profiling_collector.py JSON output"
      via: "reads the autorate_* keyed dict produced by profiling_collector.py --output json"
      pattern: "autorate_cycle_total"
    - from: "docs/PROFILING.md"
      to: "scripts/profiling_collector.py + scripts/analyze_profiling.py + scripts/profiling_categorize.py"
      via: "documented analyze step invokes all three"
      pattern: "profiling_collector.py"
---

<objective>
Build the repo-side scaffolding for the Spectrum cycle-budget baseline: create the
`.planning/perf/` artifact directory, author the missing `docs/PROFILING.md` runbook
(D-07), and write a small post-processing helper that rolls the per-label collector
JSON into the 4 PERF-02 hot-path categories and emits the D-03 `>40%` dominance verdict.

Purpose: This is the autonomous, no-production-touch foundation. It runs in parallel
with the live capture (Plan 02) and produces the helper that Plan 03 (analysis) consumes.
The analyzer scripts already report per-label percentages but NOT the PERF-02 5-category
roll-up nor a boolean dominance verdict — that small gap is what this helper fills.

Output: `docs/PROFILING.md`, `scripts/profiling_categorize.py`, `.planning/perf/.gitkeep`.
NO `src/` control-path changes. NO production-host interaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md
@.planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md

<interfaces>
<!-- The collector JSON schema the helper must consume (from scripts/profiling_collector.py:51-80,226-227). -->
<!-- profiling_collector.py --output json emits a dict keyed by raw autorate_* label: -->
<!--   { "autorate_cycle_total": {"count": int, "min_ms": f, "p50_ms": f, "avg_ms": f, -->
<!--     "max_ms": f, "p95_ms": f, "p99_ms": f}, "autorate_rtt_measurement": {...}, ... } -->
<!-- PERF-02 category -> hot-path label map (from 217-RESEARCH.md Q2; wan_controller.py:2434-2450): -->
<!--   RTT measurement       -> autorate_rtt_measurement -->
<!--   CAKE stats            -> autorate_cake_stats -->
<!--   Router communication  -> autorate_router_communication  (PARENT timer; do NOT also sum router_apply_*/router_write_* — they are children, double-count trap) -->
<!--   Logging / metrics     -> autorate_logging_metrics -->
<!--   Storage writes        -> NO hot-path label by design (DeferredIOWorker); attributed out-of-band via wanctl-history --ingestion-rate -->
<!-- Dominance test: category % = category.avg_ms / autorate_cycle_total.avg_ms * 100. -->
<!--   D-03 verdict: dominant if max(category %) > 40. Budget reference = 50.0 ms. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/perf/ artifact directory</name>
  <read_first>
    - .planning/phases/217-production-cycle-budget-baseline/217-CONTEXT.md (D-06 artifact path)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (Wave 0 gaps section)
  </read_first>
  <files>.planning/perf/.gitkeep</files>
  <action>Create directory `.planning/perf/` and add an empty `.gitkeep` so the directory is tracked. This is the D-06 home for `v1.45-baseline-spectrum-<date>.profile.json` (written in Plan 03). Do not write any other file here in this plan.</action>
  <verify>
    <automated>test -d .planning/perf && test -f .planning/perf/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/perf/` exists.
    - File `.planning/perf/.gitkeep` exists.
    - No other files were written under `.planning/perf/` in this plan.
  </acceptance_criteria>
  <done>`.planning/perf/.gitkeep` exists and the directory is git-trackable.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/profiling_categorize.py post-processing helper</name>
  <read_first>
    - scripts/profiling_collector.py (the JSON schema this helper consumes — see calculate_statistics + json.dumps at :51-80,226-227)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (Q2 category map; Pitfall 3 router double-counting)
  </read_first>
  <files>scripts/profiling_categorize.py</files>
  <action>Write a standalone Python 3.11 stdlib-only CLI `scripts/profiling_categorize.py` that takes the path to a `profiling_collector.py --output json` file as a positional arg and an optional `--budget` (default `50.0`). It must: (1) load the JSON dict keyed by `autorate_*` label; (2) read `autorate_cycle_total.avg_ms` as the denominator; (3) roll the 4 PERF-02 hot-path categories using exactly these single labels — RTT measurement=`autorate_rtt_measurement`, CAKE stats=`autorate_cake_stats`, Router communication=`autorate_router_communication` (the PARENT timer ONLY — must NOT sum `router_apply_*`/`router_write_*`, which are children, to avoid the double-counting trap), Logging/metrics=`autorate_logging_metrics`; (4) for each category compute `pct = category.avg_ms / cycle_total.avg_ms * 100`; (5) compute `dominant_category` = the category with max pct and `dominance_exceeded` = boolean `max_pct > 40.0`; (6) compute `cycle_total_avg_ms`, `cycle_total_p99_ms`, `headroom_avg_ms = budget - cycle_total.avg_ms`, `utilization_pct = cycle_total.avg_ms / budget * 100`; (7) emit a JSON object to stdout with keys `budget_ms`, `cycle_total` (avg/p99/headroom/utilization), `categories` (each: label, avg_ms, pct_of_cycle), `dominant_category`, `max_category_pct`, `dominance_exceeded`, and a `storage_note` string stating storage writes have no hot-path label by design and are attributed via `wanctl-history --ingestion-rate`. If a category label is absent from the input, set its avg_ms/pct to null and include it in a `missing_labels` list rather than crashing. Exit 0 on success, exit 1 with a stderr message if `autorate_cycle_total` is absent or its avg_ms is 0. Do NOT import any third-party packages. Add a module docstring with a usage example.</action>
  <verify>
    <automated>.venv/bin/python -c "import json,subprocess,tempfile,os; d={'autorate_cycle_total':{'count':100,'min_ms':10.0,'p50_ms':14.0,'avg_ms':15.0,'max_ms':30.0,'p95_ms':22.0,'p99_ms':28.0},'autorate_rtt_measurement':{'count':100,'min_ms':5.0,'p50_ms':8.0,'avg_ms':9.0,'max_ms':18.0,'p95_ms':14.0,'p99_ms':17.0},'autorate_cake_stats':{'count':100,'min_ms':1.0,'p50_ms':2.0,'avg_ms':2.0,'max_ms':4.0,'p95_ms':3.0,'p99_ms':4.0},'autorate_router_communication':{'count':100,'min_ms':0.0,'p50_ms':0.5,'avg_ms':1.0,'max_ms':5.0,'p95_ms':3.0,'p99_ms':4.0},'autorate_logging_metrics':{'count':100,'min_ms':0.5,'p50_ms':1.0,'avg_ms':1.0,'max_ms':2.0,'p95_ms':1.5,'p99_ms':2.0}}; f=tempfile.NamedTemporaryFile('w',suffix='.json',delete=False); json.dump(d,f); f.close(); out=subprocess.run(['.venv/bin/python','scripts/profiling_categorize.py',f.name],capture_output=True,text=True); os.unlink(f.name); assert out.returncode==0, out.stderr; r=json.loads(out.stdout); assert r['dominant_category']=='RTT measurement', r; assert r['dominance_exceeded']==False, r; assert abs(r['max_category_pct']-60.0)<0.1, r; assert r['budget_ms']==50.0; assert 'storage_note' in r; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/profiling_categorize.py` exists and runs under `.venv/bin/python` with stdlib only (no ImportError on third-party).
    - Given the fixture above, exits 0 and emits JSON where `dominant_category == "RTT measurement"`, `max_category_pct ≈ 60.0`, `dominance_exceeded == false`, `budget_ms == 50.0`.
    - Router category uses ONLY `autorate_router_communication` (verified by absence of router_apply_*/router_write_* in the category sum — the fixture has none and pct stays 2.0%).
    - Output includes a `storage_note` describing the out-of-band storage attribution.
    - Exits 1 with a stderr message when `autorate_cycle_total` is missing.
  </acceptance_criteria>
  <done>The helper categorizes collector JSON into the 4 PERF-02 categories, computes the D-03 >40% verdict, and passes the fixture verify.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/PROFILING.md runbook (D-07)</name>
  <read_first>
    - docs/PERFORMANCE.md (house style — H1, short intro, ## sections, fenced bash, "Production Standard / Operational Guardrails" framing, archived-material links)
    - docs/archive/PROFILING.md (the stale archived runbook this supersedes — its parser commands point at a --profile-only log and return no data)
    - .planning/phases/217-production-cycle-budget-baseline/217-RESEARCH.md (Q3 invocations, Q4 capture mechanics, Q5 observer-effect, Q7 runbook content, Pitfalls 1/3/4)
  </read_first>
  <files>docs/PROFILING.md</files>
  <action>Create `docs/PROFILING.md` matching `docs/PERFORMANCE.md` house style (H1 title, short intro paragraph, `## Section` headers, fenced `bash` blocks). Sections, in order: (1) intro stating this is the active profiling runbook and linking to `docs/PERFORMANCE.md` and the archived `docs/archive/PROFILING.md`; (2) "Critical: capture requires --profile AND --debug with text-format logs" — prominently explain that `profiling_collector.py`/`analyze_profiling.py` parse only `PerfTimer` DEBUG lines matching `(\w+): (\d+\.\d+)ms`, that `--profile` alone emits only periodic `report()` aggregates the regex cannot parse, and that JSON-format logs (`WANCTL_LOG_FORMAT=json`) also break the regex — so confirm the deployed log is text format before capture; (3) "Enable (systemd drop-in)" — `sudo systemctl edit wanctl@spectrum` creating `/etc/systemd/system/wanctl@spectrum.service.d/override.conf` that clears then re-sets `ExecStart` to `/usr/bin/python3 /opt/wanctl/autorate_continuous.py --config /etc/wanctl/spectrum.yaml --profile --debug`, then `daemon-reload` + `restart`; (4) "Drive load (optional, for router-write coverage)" — note `autorate_router_write_*` only fire on rate change, and reference the Phase 213 harness `scripts/phase213-baseline-capture.sh --host dallas --tests tcp_upload,rrul --wans spectrum` run inside the window; (5) "Capture" — ≥1h, retrieve `/var/log/wanctl/spectrum.log` plus rotated backups (RotatingFileHandler rotates fast at DEBUG/20Hz) or pull from `journalctl -u wanctl@spectrum`; (6) "Revert (mandatory)" — `sudo systemctl revert wanctl@spectrum` + `daemon-reload` + `restart`, then verify `systemctl cat wanctl@spectrum` shows no drop-in and the running ExecStart has no `--profile`/`--debug`; (7) "Analyze" — the three invocations: `profiling_collector.py "$LOG" --all --output json > .planning/perf/v1.45-baseline-spectrum-<date>.profile.json`, `analyze_profiling.py --log-file "$LOG" --budget 50 --output <md>`, `profiling_categorize.py <profile.json> --budget 50`, plus `wanctl-history --ingestion-rate --wan spectrum` for storage-write attribution; (8) "Interpret vs absolute bars (D-03)" — close/no-action if `autorate_cycle_total` has comfortable headroom vs 50ms AND no single subsystem >40% of cycle_total; promote to an optimization phase if either fails; note the observer-effect caveat that DEBUG-captured cycle_total is an upper bound and should be cross-checked against `/health cycle_budget`. Do NOT recommend any threshold/algorithm changes. Keep it operational and concise like PERFORMANCE.md.</action>
  <verify>
    <automated>test -f docs/PROFILING.md && grep -q -- "--profile --debug" docs/PROFILING.md && grep -q "systemctl revert wanctl@spectrum" docs/PROFILING.md && grep -q "profiling_categorize.py" docs/PROFILING.md && grep -q "40%" docs/PROFILING.md && grep -q "ingestion-rate" docs/PROFILING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PROFILING.md` exists.
    - Contains the `--profile --debug` requirement and the text-format/regex caveat (greppable: `--profile --debug`).
    - Contains the mandatory revert step (greppable: `systemctl revert wanctl@spectrum`).
    - Documents all four analyze invocations including `profiling_categorize.py` and `wanctl-history --ingestion-rate`.
    - Contains the D-03 absolute-bar interpretation (greppable: `40%`).
    - Contains no recommendation to change thresholds/algorithms/control-path code.
  </acceptance_criteria>
  <done>`docs/PROFILING.md` is a durable enable→drive→capture→revert→analyze→interpret runbook matching house style and passes the grep checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → committed docs/scripts | New runbook + helper are static, no runtime input, no production reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-01 | Information disclosure | docs/PROFILING.md | mitigate | Runbook uses placeholders (`<spectrum-host>`, `<date>`) and never embeds secrets, real IPs, or credentials per CLAUDE.md public-safe policy. Reviewer greps for IP/hostname leakage before commit. |
| T-217-02 | Tampering | scripts/profiling_categorize.py | accept | Stdlib-only read-only post-processor; no network, no writes outside stdout, no production reach. Low-value local analysis tool. |

No HIGH threats: this plan writes only static documentation, a read-only analysis helper, and an empty directory placeholder. No new inputs, endpoints, or production interaction.
</threat_model>

<verification>
- `docs/PROFILING.md` exists and passes the grep checks (--profile --debug, revert, categorize, 40%, ingestion-rate).
- `scripts/profiling_categorize.py` passes the fixture verify (RTT dominant, 60%, not exceeded, budget 50).
- `.planning/perf/.gitkeep` exists.
- `.venv/bin/ruff check scripts/profiling_categorize.py` passes (style consistency with the repo).
- No `src/` files modified.
</verification>

<success_criteria>
- The repo has a durable profiling runbook (D-07 satisfied).
- The `.planning/perf/` artifact directory exists (D-06 prerequisite).
- A category-roll-up + dominance-verdict helper exists and is correct on a known fixture (PERF-02 tooling gap closed).
- Zero control-path edits.
</success_criteria>

<output>
After completion, create `.planning/phases/217-production-cycle-budget-baseline/217-01-SUMMARY.md`
</output>
