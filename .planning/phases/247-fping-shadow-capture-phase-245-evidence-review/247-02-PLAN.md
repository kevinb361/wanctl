---
phase: "247"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase247-safe18-boundary-check.sh
  - tests/test_phase247_safe18_verifier.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "phase247-safe18-boundary-check.sh runs without error on a clean working tree and exits 0"
    - "The script produces a safe18-boundary-247.json evidence artifact in the phase evidence/ directory"
    - "The script fails (exit non-zero) if any SAFE-18 protected src/wanctl/ file is modified vs anchor e090a200"
    - "The --self-test mode proves the allowlist rejects a committed edit to a protected file"
    - "test_phase247_safe18_verifier.py passes with pytest"
  artifacts:
    - path: "scripts/phase247-safe18-boundary-check.sh"
      provides: "SAFE-18 boundary verifier for Phase 247; zero-diff proof in protected controller files"
      contains: "ANCHOR=\"e090a200\""
    - path: "tests/test_phase247_safe18_verifier.py"
      provides: "Automated regression test for the SAFE-18 verifier script"
      contains: "e090a200"
  key_links:
    - from: "scripts/phase247-safe18-boundary-check.sh"
      to: "src/wanctl/wan_controller.py"
      via: "git diff --name-only against ANCHOR to detect unauthorized edits"
      pattern: "git diff.*e090a200"
---

<objective>
Write the SAFE-18 boundary verifier script (modeled on phase245-safe17-boundary-check.sh)
and its pytest wrapper. SAFE-18 is a stricter invariant than SAFE-17: zero diff is required
in ALL listed src/wanctl/ controller-path files — no allowlist exceptions, because this
phase adds only scripts/ and .planning/ artifacts.

Purpose: Provides a machine-verifiable proof that Phase 247 does not touch any
controller-path file. Establishes the SAFE-18 evidence artifact that will carry forward
to Phases 248-250 as the milestone-close verifier.

Output:
  scripts/phase247-safe18-boundary-check.sh
  tests/test_phase247_safe18_verifier.py
  .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/safe18-boundary-247.json
  (evidence JSON is written by the script when run, not by the plan itself)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-CONTEXT.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-RESEARCH.md
@.planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-PATTERNS.md

<interfaces>
<!-- Primary analog — read this entire file before writing the new script -->
From scripts/phase245-safe17-boundary-check.sh:
  - set -euo pipefail header pattern
  - require_command() function
  - emit_evidence() python3 heredoc pattern (simplified for SAFE-18)
  - --anchor / --out / --self-test argparse pattern
  - dirty tree check: git diff --quiet HEAD -- src/wanctl/
  - self-test worktree pattern: git worktree add, make a disallowed edit, verify script exits non-zero

<!-- Test analog -->
From tests/test_phase245_safe17_verifier.py (or test_phase243_safe17_verifier.py):
  - PHASE_CLOSE_ANCHOR = "e090a200"  # pin to v1.53 close, NOT HEAD
  - run() helper using subprocess.run with capture_output=True, check=False
  - Test that clean tree → exit 0
  - Test that script emits evidence JSON at expected path

SAFE-18 protected files (ALL must show zero diff vs e090a200):
  src/wanctl/wan_controller.py
  src/wanctl/queue_controller.py
  src/wanctl/cake_signal.py
  src/wanctl/rtt_backend.py
  src/wanctl/fping_measurement.py
  src/wanctl/rtt_measurement.py
  src/wanctl/alert_engine.py

Evidence output path:
  .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/safe18-boundary-247.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phase247-safe18-boundary-check.sh</name>
  <files>scripts/phase247-safe18-boundary-check.sh</files>
  <read_first>
    - scripts/phase245-safe17-boundary-check.sh — read the full file; copy structure, simplify for SAFE-18 (no allowlist, no phase-anchor chain)
    - .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-RESEARCH.md section "Pattern 4: SAFE-18 Boundary Script"
    - .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-PATTERNS.md section for safe18-boundary-check.sh
    - .planning/REQUIREMENTS.md — SAFE-18 definition and protected file list
  </read_first>
  <action>
    Write scripts/phase247-safe18-boundary-check.sh as a simplified SAFE-18 verifier:

    Header block: Copy the Phase 245 header comments, update to describe SAFE-18 (Phase 247 v1.54).
    SAFE-18 is simpler than SAFE-17: no allowlist of permitted changes; any diff in src/wanctl/ vs anchor e090a200 is a failure.

    Core variables:
      ANCHOR="e090a200"   (v1.53 close commit)
      OUT=".planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/safe18-boundary-247.json"

    SAFE18_PROTECTED list (the 7 files above, space-separated for iteration).

    Functions to include:
      - usage() — describe --anchor, --out, --self-test flags
      - require_command() — copy verbatim from phase245 script
      - emit_evidence() — simplified Python heredoc that writes the JSON artifact;
        fields: schema_version=1, phase="247", milestone="v1.54", anchor, anchor_sha,
        protected_files (list), changed_files (list, should be empty on pass),
        passed (bool), evaluated_at (ISO8601 UTC), safe18_verdict ("pass" or "fail")
      - check_dirty_tree() — run `git diff --quiet HEAD -- src/wanctl/` and fail if dirty
      - check_safe18() — run `git diff --name-only "${ANCHOR_SHA}"..HEAD -- src/wanctl/`
        and store result; any output = fail

    Main flow:
      1. Parse args (--anchor, --out, --self-test)
      2. Resolve ANCHOR to ANCHOR_SHA via `git rev-parse "${ANCHOR}^{commit}"`
      3. check_dirty_tree
      4. check_safe18 → capture changed file list
      5. If changed_files is empty: emit_evidence passed=true; exit 0
      6. Else: print changed files to stderr; emit_evidence passed=false; exit 1

    --self-test mode (copy Phase 245 worktree pattern):
      - git worktree add to a temp dir at a detached HEAD
      - Make a one-line comment edit to src/wanctl/queue_controller.py in the worktree
      - git -C <worktree> commit -am "self-test: disallowed edit (will be discarded)"
      - Run the verifier against the worktree (adjust OUT to temp path)
      - Assert script exits non-zero (disallowed edit detected)
      - git worktree remove --force
      - Report PASS/FAIL

    Script must be executable: chmod +x scripts/phase247-safe18-boundary-check.sh
    (or note chmod requirement in the action for the executor to apply).

    Do NOT include an allowlist regex — SAFE-18 is zero-tolerance for any src/wanctl/ change.
    The evidence dir must exist: mkdir -p for the dirname of OUT before writing.
  </action>
  <verify>
    <automated>
      bash -n scripts/phase247-safe18-boundary-check.sh
      grep -c 'ANCHOR="e090a200"' scripts/phase247-safe18-boundary-check.sh
      grep -c 'set -euo pipefail' scripts/phase247-safe18-boundary-check.sh
      grep -c 'safe18-boundary-247.json' scripts/phase247-safe18-boundary-check.sh
    </automated>
  </verify>
  <done>
    scripts/phase247-safe18-boundary-check.sh passes bash -n (syntax clean), contains
    ANCHOR="e090a200", set -euo pipefail, and the correct evidence output path.
    Script is committed and executable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_phase247_safe18_verifier.py and run the verifier</name>
  <files>tests/test_phase247_safe18_verifier.py</files>
  <read_first>
    - tests/test_phase245_safe17_verifier.py — read the full file for the importlib + subprocess + pinned-anchor test pattern
    - tests/test_phase243_safe17_verifier.py — secondary analog for run() helper
    - scripts/phase247-safe18-boundary-check.sh — the script under test (just written in Task 1)
  </read_first>
  <behavior>
    - test_script_exists: scripts/phase247-safe18-boundary-check.sh exists and is executable
    - test_clean_tree_exits_zero: running the verifier on the current clean tree exits 0
    - test_clean_tree_writes_evidence: after running on clean tree, evidence JSON exists at .planning/phases/247-.../evidence/safe18-boundary-247.json
    - test_evidence_json_structure: evidence JSON contains keys: phase, milestone, anchor, passed, safe18_verdict with passed=true and safe18_verdict="pass"
    - test_anchor_pinned_to_v153_close: PHASE_CLOSE_ANCHOR constant in the test equals "e090a200" (not HEAD, not a branch name)
  </behavior>
  <action>
    Write tests/test_phase247_safe18_verifier.py following the Phase 245 safe17 verifier test pattern:

    Constants at module level:
      ROOT = Path(__file__).resolve().parents[1]
      VERIFIER = ROOT / "scripts" / "phase247-safe18-boundary-check.sh"
      PHASE_CLOSE_ANCHOR = "e090a200"   # v1.53 close — pin to commit, not HEAD (per MEMORY.md guidance)
      EVIDENCE_PATH = ROOT / ".planning" / "phases" / "247-fping-shadow-capture-phase-245-evidence-review" / "evidence" / "safe18-boundary-247.json"

    run() helper: subprocess.run(cmd, cwd=ROOT, text=True, capture_output=True, check=False)

    Test class TestSafe18Verifier:
      - test_script_exists: assert VERIFIER.exists() and os.access(VERIFIER, os.X_OK)
      - test_clean_tree_exits_zero: run(["bash", str(VERIFIER)]) → assert returncode == 0
      - test_clean_tree_writes_evidence: run verifier, then assert EVIDENCE_PATH.exists()
      - test_evidence_json_passed_true: load EVIDENCE_PATH as JSON, assert data["passed"] is True
      - test_evidence_json_verdict_pass: load EVIDENCE_PATH as JSON, assert data["safe18_verdict"] == "pass"
      - test_anchor_is_pinned: assert PHASE_CLOSE_ANCHOR == "e090a200"

    After writing tests, also run the verifier script itself on the current working tree:
      bash scripts/phase247-safe18-boundary-check.sh
    This produces the actual evidence artifact for Phase 247.

    Then run the tests:
      .venv/bin/pytest tests/test_phase247_safe18_verifier.py -v
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase247_safe18_verifier.py -v</automated>
  </verify>
  <done>
    All 6 tests pass. Evidence JSON exists at evidence/safe18-boundary-247.json with
    passed=true. PHASE_CLOSE_ANCHOR is pinned to "e090a200" in the test file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → verifier | Script reads committed state; no network; no user input |
| worktree (self-test) | Ephemeral detached worktree; always removed with --force after test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-247-02-01 | Tampering | Protected src/wanctl/ files | mitigate | Script diffs against pinned commit e090a200; any modification detected and fails closed |
| T-247-02-02 | Tampering | Anchor commit spoofing | accept | Anchor is a specific commit SHA embedded in source; changing it requires a code change that would itself be detectable |
| T-247-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; bash script + pytest only |
</threat_model>

<verification>
bash -n scripts/phase247-safe18-boundary-check.sh                           # syntax clean
bash scripts/phase247-safe18-boundary-check.sh                              # exits 0, writes JSON
.venv/bin/pytest tests/test_phase247_safe18_verifier.py -v                 # all tests pass
cat .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/evidence/safe18-boundary-247.json | python3 -m json.tool   # valid JSON with passed=true
</verification>

<success_criteria>
SAFE-18 verifier script committed and passing on current tree. Evidence JSON written.
Tests green. Both PROF-01 and PROF-02 now have a committed boundary-check artifact as
their SAFE-18 gate. The test anchor is pinned to e090a200, not HEAD.
</success_criteria>

<output>
Create .planning/phases/247-fping-shadow-capture-phase-245-evidence-review/247-02-SUMMARY.md when done.
</output>
