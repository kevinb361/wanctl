---
phase: 221-matrix-evidence-closeout-scope-a2
plan: 04
type: execute
wave: 3
depends_on: [221-03]
files_modified:
  - .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
  - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
autonomous: true
requirements:
  - CLOSEOUT-03
  - CRITERIA-02
  - SAFE-11

must_haves:
  truths:
    - "Plan 04 fires AFTER Plan 03 commit so the verdict written in §1 + frontmatter of 221-CLOSEOUT.md is authoritative — Plan 04 reads it and applies it (CONTEXT D-13)"
    - "Todo move is a git mv from .planning/todos/pending/<file> to .planning/todos/closed/<file> on ANY verdict per CONTEXT D-13 — carried_narrower is operationally a close due to the prejudice rule"
    - "Three new YAML frontmatter fields appended INSIDE the existing frontmatter delimiters per CONTEXT D-14: closed_by_phase, verdict, close_with_prejudice"
    - "Closure stanza ## Phase 221 Closeout appended at END of todo body per CONTEXT D-15 — historical investigation log preserved, never overwritten"
    - "On verdict = carried_narrower_with_close_with_prejudice_rule: CRITERIA-02 rule pasted VERBATIM from .planning/REQUIREMENTS.md per CONTEXT D-15 + D-17 (REQ-traceability is exact, no paraphrase)"
    - "Todo move + body append + frontmatter mutation happen in ONE commit (chore(221): close folded tcp_12down todo (verdict: <X>))"
    - "Closeout report §10 closeout_commit_for_todo placeholder PENDING_PLAN_04_COMMIT is REPLACED with the todo-move commit SHA in a SECOND commit (docs(221): cite todo-move commit in closeout) per CONTEXT D-16 + RESEARCH R5 two-commit recommendation"
    - "D-17 narrower-definition: NO scope narrowing in the closure stanza — the rule itself IS the narrowing; closure stanza must not include 'carried, but only daytime' or similar"
    - "git history preserves the todo via --follow: rename detection succeeds; pre-Phase 221 commits remain reachable from the closed/ path"
    - "Final SAFE-11 mutation-boundary pytest passes green AT phase close — controller-path / Phase 213/214/220 / docs/ diff all empty; the only diffs vs Phase 220 base_sha are inside .planning/phases/221-... and .planning/todos/"
    - "If verdict = carried_narrower_with_close_with_prejudice_rule, close_with_prejudice frontmatter field is true; otherwise false (CONTEXT D-14)"
    - ".planning/todos/closed/ directory is created by the git mv if it does not exist (git creates intermediate directories on rename)"
  artifacts:
    - path: .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
      provides: "Folded todo at final resting place — closed with verdict, frontmatter mutated, closure stanza appended; historical body preserved verbatim"
      contains: "## Phase 221 Closeout"
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
      provides: "Closeout report amended with closeout_commit_for_todo SHA — completes the bidirectional citation per CONTEXT D-16"
      contains: "closeout_commit_for_todo"
  key_links:
    - from: .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
      to: .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
      via: git-mv-rename
      pattern: "git log --follow"
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
      to: .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
      via: closeout_commit_for_todo-citation
      pattern: "closeout_commit_for_todo: [0-9a-f]{7,40}"
    - from: .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
      to: .planning/REQUIREMENTS.md
      via: CRITERIA-02-verbatim-extraction
      pattern: "CRITERIA-02"
---

<objective>
Apply the Phase 221 verdict to the folded `tcp_12down` todo per CONTEXT D-13 through D-17: `git mv` from `pending/` to `closed/`, append three frontmatter fields, append a `## Phase 221 Closeout` stanza, then amend the closeout report's `closeout_commit_for_todo` field with the move-commit SHA. Final SAFE-11 mutation-boundary re-run at phase close confirms no controller-path / Phase 213/214/220 / docs/ diff.

Purpose: CONTEXT D-13 mandates close-on-any-verdict (carried_narrower is operationally a close due to the prejudice rule); D-14/D-15 specify the frontmatter + body mutation; D-16 specifies the bidirectional citation; D-17 forbids per-axis narrowing on carry verdict; SAFE-11 at phase close is the v1.47 closeout invariant. RESEARCH R5 + R9 specify the exact mechanics.

Output: Two commits — (1) todo move + body append + frontmatter mutation; (2) closeout amend. Plus the final SAFE-11 re-run as a verification gate (no new commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
@.planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md

<interfaces>
<!-- Plan 04 reads the verdict from Plan 03's CLOSEOUT.md frontmatter and
     applies it to the folded todo. The verdict drives D-14 close_with_prejudice
     boolean and the conditional D-15 CRITERIA-02 verbatim paste. -->

Verdict extraction:
  .venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); print(d['verdict'])"

  Produces exactly one of:
    defect_located
    hypothesis_killed
    carried_narrower_with_close_with_prejudice_rule

D-14 frontmatter field rule:
  closed_by_phase: 221
  verdict: <extracted>
  close_with_prejudice: true   IFF verdict == carried_narrower_with_close_with_prejudice_rule
                       false  otherwise

CRITERIA-02 verbatim extraction (from REQUIREMENTS.md):
  awk '/CRITERIA-02/,/^- \[/{print; if (/^- \[/ && !/CRITERIA-02/) exit}' .planning/REQUIREMENTS.md
  # OR
  grep -A 2 'CRITERIA-02' .planning/REQUIREMENTS.md | head -3
  # The text is one paragraph following "- [x] **CRITERIA-02**:"; extract verbatim including leading bullet structure.

Existing folded todo (preserve historical body):
  .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
  # Frontmatter has: created, title, area, resolves_phase: 221, files: [...]
  # Body has: ## Problem, ## investigation log, etc. PRESERVE EVERYTHING.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify Plan 03 completed and extract verdict + closeout SHAs</name>
  <files></files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md (frontmatter — verdict source of truth)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json (matrix_verdict cross-check)
    - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (preserve-this-content reference)
  </read_first>
  <action>
    Gate-only task. Verify Plan 03 prerequisites before any file mutation:

    1. **CLOSEOUT.md exists with valid verdict:** parse frontmatter, assert `verdict` is one of three permitted tokens.
    2. **CLOSEOUT.json matrix_verdict matches CLOSEOUT.md verdict:** consistency check; if they diverge, halt and surface to operator (Plan 03 contract was a pure read — divergence is a bug).
    3. **MATRIX-INVALID.md NOT present:** Plan 04 only fires on matrix-success path.
    4. **Folded todo still in pending/:** `.planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` exists; `.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` does NOT exist.
    5. **Extract values into session-local variables:**
       - `VERDICT = <verdict from closeout frontmatter>`
       - `CLOSE_WITH_PREJUDICE = (VERDICT == "carried_narrower_with_close_with_prejudice_rule")`
       - `MATRIX_BASE_SHA = <matrix_base_sha from closeout frontmatter>`
       - `PHASE220_YAML_SHA = <phase220_yaml_sha from closeout frontmatter>`
       - `CRITERIA_02_VERBATIM = <text from REQUIREMENTS.md>` — extracted only if CLOSE_WITH_PREJUDICE is true

    6. **CRITERIA-02 extraction (conditional):** if CLOSE_WITH_PREJUDICE is true, run:
       ```bash
       awk '/^- \[x\] \*\*CRITERIA-02\*\*/' .planning/REQUIREMENTS.md
       ```
       Capture the full bullet line text verbatim (including `**CRITERIA-02**:` prefix). RESEARCH R5 + CONTEXT D-15 require "verbatim from REQUIREMENTS.md (no paraphrase)" — preserve the exact bullet structure including the markdown bullet marker.

    No files written in this task.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import yaml, json; from pathlib import Path; m=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); j=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json')); assert m['verdict']==j['matrix_verdict']"` exits 0
    - Folded todo at expected source: `test -f .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md`
    - Destination not yet occupied: `test ! -f .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md`
    - MATRIX-INVALID absent: `test ! -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md`
    - If verdict == carried_narrower, CRITERIA-02 text extractable: `! .venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); import sys; sys.exit(0 if d['verdict'] != 'carried_narrower_with_close_with_prejudice_rule' else (0 if open('.planning/REQUIREMENTS.md').read().count('CRITERIA-02') > 0 else 1))"`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import yaml, json; from pathlib import Path; m=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); j=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json')); assert m['verdict']==j['matrix_verdict']" && test -f .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md && test ! -f .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md</automated>
  </verify>
  <done>Verdict extracted; pre-flight gates pass; CRITERIA-02 verbatim text captured if needed.</done>
</task>

<task type="auto">
  <name>Task 2: git mv folded todo from pending/ to closed/</name>
  <files>
    - .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
    - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
  </files>
  <read_first>
    - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (sanity-check current content unchanged from Plan 01 state)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-13 (always-close rule), D-17 (no narrowing)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R5 (move mechanics)
  </read_first>
  <action>
    Execute the rename. Use `git mv` so git detects the rename (preserves `git log --follow` history per RESEARCH R5):

    ```bash
    git mv .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md \
           .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
    ```

    `git mv` auto-creates `.planning/todos/closed/` if it does not exist (currently the repo has `pending/`, `completed/`, `done/` — no `closed/`). This is the FIRST file landing in `closed/`. Confirm post-move:
    - `pending/<file>` deleted
    - `closed/<file>` exists with byte-identical content as pre-move pending/<file>
    - `git status` shows the rename detected

    DO NOT commit yet. Tasks 3 and 4 must batch into the same commit (atomic: move + frontmatter + body append).
  </action>
  <acceptance_criteria>
    - `test ! -f .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md`
    - `test -f .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md`
    - git detects rename: `git diff --staged -M --name-status -- .planning/todos/ | grep -E '^R[0-9]+'` returns the rename line (R-status with similarity ≥ 90)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md && test ! -f .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md && git diff --staged -M --name-status -- .planning/todos/ | grep -qE '^R[0-9]+'</automated>
  </verify>
  <done>File moved via git mv; rename detected by git; commit not yet created.</done>
</task>

<task type="auto">
  <name>Task 3: Append three frontmatter fields per CONTEXT D-14</name>
  <files>.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md</files>
  <read_first>
    - .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (post-move state — verify frontmatter `---...---` block intact)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-14 (three required fields + boolean rule)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R9 (frontmatter mutation safety)
  </read_first>
  <action>
    Insert three NEW fields INSIDE the existing `---...---` frontmatter block, BEFORE the closing `---` delimiter. Per RESEARCH R9, use Python (not `sed`) to avoid `---` matching ambiguity if the body contains `---` substrings.

    Python-based safe insertion (run from repo root):

    ```python
    from pathlib import Path
    import yaml

    PATH = Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md')
    text = PATH.read_text()

    # Split: pre, frontmatter_text, body
    parts = text.split('---\n', 2)
    if len(parts) < 3 or parts[0] != '':
        raise SystemExit("Frontmatter block not found at file start")
    fm_text = parts[1]
    body = parts[2]

    fm = yaml.safe_load(fm_text) or {}
    fm['closed_by_phase'] = 221
    fm['verdict'] = '<VERDICT extracted in Task 1>'  # one of three tokens
    fm['close_with_prejudice'] = (fm['verdict'] == 'carried_narrower_with_close_with_prejudice_rule')

    new_fm_text = yaml.safe_dump(fm, sort_keys=False, default_flow_style=False)
    PATH.write_text(f"---\n{new_fm_text}---\n{body}")
    ```

    NOTE: `yaml.safe_dump` may reorder fields by insertion vs alphabetical. The `sort_keys=False` flag preserves insertion order; existing fields stay in their original positions and the three new fields appear at the end of the frontmatter (or wherever `fm.update` placed them in insertion order). This is acceptable per CONTEXT D-14 (fields appended).

    Validation: `yaml.safe_load` the resulting frontmatter must produce a dict with all original fields PLUS the three new ones.
  </action>
  <acceptance_criteria>
    - File parses with YAML frontmatter intact: `.venv/bin/python -c "import yaml; from pathlib import Path; t=Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text(); parts=t.split('---\n',2); assert parts[0]=='' and yaml.safe_load(parts[1]) is not None"`
    - All three fields present and correctly typed: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text().split('---',2)[1]); assert d['closed_by_phase']==221 and d['verdict'] in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'} and isinstance(d['close_with_prejudice'], bool)"`
    - close_with_prejudice boolean matches verdict: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text().split('---',2)[1]); assert d['close_with_prejudice'] == (d['verdict']=='carried_narrower_with_close_with_prejudice_rule')"`
    - Original frontmatter fields preserved: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text().split('---',2)[1]); assert d.get('created') and d.get('title') and d.get('resolves_phase')==221"`
    - Body still contains the original ## Problem section: `grep -c '^## Problem$' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 1
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text().split('---',2)[1]); assert d['closed_by_phase']==221 and d['verdict'] in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'} and isinstance(d['close_with_prejudice'], bool) and d['close_with_prejudice']==(d['verdict']=='carried_narrower_with_close_with_prejudice_rule')"</automated>
  </verify>
  <done>Three frontmatter fields appended; original frontmatter intact; body preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Append ## Phase 221 Closeout stanza per CONTEXT D-15 + D-17</name>
  <files>.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md</files>
  <read_first>
    - .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (post-frontmatter-mutation state — verify body intact)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-15 (closure stanza shape), D-17 (no per-axis narrowing)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md (verdict + matrix_base_sha + phase220_yaml_sha — citation source)
    - .planning/REQUIREMENTS.md (CRITERIA-02 verbatim text — only if carry verdict)
  </read_first>
  <action>
    Append a new section AT THE END of the todo body (after all existing content). DO NOT modify or remove any existing body text — historical investigation log is preserved verbatim per CONTEXT D-15.

    Section template (substitute `<...>` placeholders from session-local variables):

    ```markdown


    ## Phase 221 Closeout

    **Verdict:** <VERDICT>
    **Summary:** <one-line summary derived from §1 narrative of 221-CLOSEOUT.md>

    **Report:** See `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md`
    **Matrix base_sha:** `<MATRIX_BASE_SHA>`
    **Phase 220 YAML SHA:** `<PHASE220_YAML_SHA>`
    **Closeout written:** <ISO 8601 from 221-CLOSEOUT.md frontmatter closeout_written_utc>
    ```

    Per CONTEXT D-17: NO per-axis narrowing. The closure stanza must NOT include phrases like "carried, but only daytime" or "carried, only on Spectrum." The carry verdict is the verbatim token `carried_narrower_with_close_with_prejudice_rule` with the rule itself as the narrowing.

    **Conditional CRITERIA-02 block** (only if VERDICT == carried_narrower_with_close_with_prejudice_rule):

    Append after the main stanza:

    ```markdown

    ### Close-With-Prejudice Rule (CRITERIA-02, verbatim from REQUIREMENTS.md)

    > <CRITERIA_02_VERBATIM extracted in Task 1>
    ```

    The `> ` blockquote prefix is a markdown convention to make the verbatim quote visually distinct from the surrounding stanza. The text inside is the verbatim bullet from REQUIREMENTS.md, no paraphrase.

    Append with a Python script (avoiding shell heredoc escaping issues):

    ```python
    from pathlib import Path
    PATH = Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md')
    body = PATH.read_text()
    stanza = "\n\n## Phase 221 Closeout\n\n..."  # filled with substitutions
    if not body.endswith('\n'):
        body += '\n'
    PATH.write_text(body + stanza)
    ```

    Stage and commit Tasks 2+3+4 atomically:
    ```bash
    git add .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
    git commit -m "chore(221): close folded tcp_12down todo (verdict: <VERDICT>)"
    ```

    Capture the resulting commit SHA: `TODO_MOVE_COMMIT_SHA = $(git rev-parse HEAD)`. This is the SHA Task 5 writes back into 221-CLOSEOUT.md §10.
  </action>
  <acceptance_criteria>
    - Closure stanza header exists exactly once: `grep -c '^## Phase 221 Closeout$' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 1
    - Stanza cites the closeout report by path: `grep -c '221-CLOSEOUT.md' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` ≥ 1
    - Stanza cites Phase 220 YAML SHA (40-char hex): `grep -cE 'Phase 220 YAML SHA.*[0-9a-f]{40}' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 1
    - Stanza cites matrix base SHA: `grep -cE 'Matrix base_sha.*[0-9a-f]{40}' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 1
    - Original ## Problem section preserved: `grep -c '^## Problem$' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 1
    - No per-axis narrowing language present: `grep -ciE 'carried.*(only|just).*(daytime|prime-time|off-peak|spectrum|att|dallas|chicago|vultr)' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` equals 0
    - If verdict is carry: CRITERIA-02 verbatim block present: `.venv/bin/python -c "import yaml; from pathlib import Path; t=Path('.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md').read_text(); d=yaml.safe_load(t.split('---',2)[1]); import sys; sys.exit(0 if d['verdict'] != 'carried_narrower_with_close_with_prejudice_rule' else (0 if 'Close-With-Prejudice Rule (CRITERIA-02' in t else 1))"`
    - Commit recorded with verdict token in subject: `git log -1 --format=%s` matches `chore(221): close folded tcp_12down todo (verdict: <verdict>)`
    - git log --follow finds pre-move history: `git log --follow --format=%h .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md | wc -l` ≥ 2 (at least one pre-move commit + this move commit)
    - SAFE-11 green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && grep -c '^## Phase 221 Closeout$' .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md | grep -q '^1$' && git log --follow --format=%h .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md | head -2 | wc -l | grep -qE '^[ ]*2$'</automated>
  </verify>
  <done>Closure stanza appended; CRITERIA-02 verbatim included if carry verdict; commit SHA captured; SAFE-11 green; git --follow preserves history.</done>
</task>

<task type="auto">
  <name>Task 5: Amend 221-CLOSEOUT.md §10 with todo-move commit SHA per CONTEXT D-16</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md (current state — verify PENDING_PLAN_04_COMMIT placeholder present)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-16 (bidirectional citation)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R5 (two-commit recommendation)
  </read_first>
  <action>
    Read `TODO_MOVE_COMMIT_SHA` from Task 4 (`git rev-parse HEAD` immediately after the Task 4 commit; if the session was interrupted, recover with `git log --grep='chore(221): close folded tcp_12down todo' -1 --format=%H`).

    Replace BOTH occurrences of `PENDING_PLAN_04_COMMIT` in 221-CLOSEOUT.md with the captured SHA (40-char form):
    - Frontmatter: `closeout_commit_for_todo: <SHA>`
    - §10 body: `**Closeout commit for todo move:** \`<SHA>\``

    Use Python to avoid sed/awk escaping issues:

    ```python
    from pathlib import Path
    PATH = Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md')
    SHA = '<TODO_MOVE_COMMIT_SHA>'
    text = PATH.read_text()
    new_text = text.replace('PENDING_PLAN_04_COMMIT', SHA)
    assert new_text != text, "no PENDING_PLAN_04_COMMIT marker found"
    PATH.write_text(new_text)
    ```

    Commit:
    ```bash
    git add .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
    git commit -m "docs(221): cite todo-move commit in closeout (CONTEXT D-16)"
    ```

    Per RESEARCH R5, this is the two-commit completion: (1) todo move under its own commit for auditability, (2) closeout amendment under its own commit showing the cross-reference being inserted as a deliberate operation.
  </action>
  <acceptance_criteria>
    - No PENDING_PLAN_04_COMMIT remaining: `! grep -c PENDING_PLAN_04_COMMIT .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md`
    - Frontmatter closeout_commit_for_todo is a SHA (≥7 hex chars, ≤40): `.venv/bin/python -c "import yaml, re; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); assert re.match(r'^[0-9a-f]{7,40}$', d['closeout_commit_for_todo'])"`
    - §10 body cites the same SHA: `.venv/bin/python -c "import yaml; from pathlib import Path; t=Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text(); d=yaml.safe_load(t.split('---',2)[1]); sha=d['closeout_commit_for_todo']; assert sha in t"`
    - SHA matches the actual move commit: `.venv/bin/python -c "import yaml, subprocess; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); sha=d['closeout_commit_for_todo']; r=subprocess.run(['git','log','--grep=chore(221): close folded tcp_12down todo','-1','--format=%H'], capture_output=True, text=True); actual=r.stdout.strip(); assert actual.startswith(sha) or sha.startswith(actual), f'sha mismatch: closeout={sha} commit={actual}'"`
    - Commit subject matches: `git log -1 --format=%s` equals `docs(221): cite todo-move commit in closeout (CONTEXT D-16)`
    - SAFE-11 green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && ! grep -q PENDING_PLAN_04_COMMIT .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md && .venv/bin/python -c "import yaml, re; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); assert re.match(r'^[0-9a-f]{7,40}$', d['closeout_commit_for_todo'])"</automated>
  </verify>
  <done>Closeout report cites todo-move commit SHA in both frontmatter and §10 body; bidirectional citation complete per CONTEXT D-16.</done>
</task>

<task type="auto">
  <name>Task 6: Final SAFE-11 mutation-boundary re-run at phase close</name>
  <files></files>
  <read_first>
    - tests/test_phase221_mutation_boundary.py (the test being re-run)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md SAFE-11 row in `<code_context>` (Phase 221 allowlist surface)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R6 (Phase 221 allowlist + base SHA chain)
  </read_first>
  <action>
    Final phase-close verification. No file modifications — this is the closing gate.

    1. **Run the mutation-boundary suite:**
       ```bash
       .venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -v
       ```
       All three test functions (test_no_forbidden_controller_path_diff, test_phase221_scripts_allowlist, test_phase221_docs_have_no_threshold_tuning_tokens) must pass green.

    2. **Confirm allowlist surface intact:**
       - `git diff --name-only $(git rev-parse HEAD:scripts/phase220-matrix.yaml | head -c 40)..HEAD -- src/wanctl/` returns empty
       - `git diff --name-only $(git log --grep='docs(phase-221): begin phase execution' -1 --format=%H 2>/dev/null || git rev-parse HEAD:scripts/phase220-matrix.yaml | head -c 40)..HEAD -- 'scripts/phase213-*' 'scripts/phase214-*' 'scripts/phase220-*'` returns empty
       - `git diff --name-only $(<phase-221 base sha>)..HEAD -- docs/` returns empty
       (Use the same base SHA resolution chain the test uses: PHASE221_BASE_SHA env > git log marker > scripts/phase220-matrix.yaml base_sha field.)

    3. **Confirm Phase 221 allowed surface contained:**
       - `git diff --name-only <phase-221 base sha>..HEAD` returns ONLY paths matching `.planning/phases/221-matrix-evidence-closeout-scope-a2/` OR `.planning/todos/(pending|closed)/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` OR `tests/test_phase221_mutation_boundary.py`.

    4. **Run quick test slice for regression catch:**
       ```bash
       .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q
       ```
       Phase 221 should not have touched any controller code; this slice MUST stay green.

    5. **Phase close signal:** if all four checks pass, Plan 04 is complete and Phase 221 is ready for `/gsd:verify-work`.

    No commit in this task — it is a verification gate.
  </action>
  <acceptance_criteria>
    - SAFE-11 boundary test all-green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -v` exits 0 AND reports ≥ 3 passing tests
    - No src/wanctl/ diff vs Phase 221 base SHA: `git diff --name-only $(grep base_sha scripts/phase220-matrix.yaml | head -1 | awk '{print $2}' | tr -d '\"\"')..HEAD -- src/wanctl/` returns empty
    - No Phase 213/214/220 script diff: `git diff --name-only $(grep base_sha scripts/phase220-matrix.yaml | head -1 | awk '{print $2}' | tr -d '\"\"')..HEAD -- 'scripts/phase213-*' 'scripts/phase214-*' 'scripts/phase220-*'` returns empty
    - No docs/ diff: `git diff --name-only $(grep base_sha scripts/phase220-matrix.yaml | head -1 | awk '{print $2}' | tr -d '\"\"')..HEAD -- docs/` returns empty
    - Hot-path controller tests still green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q` exits 0
    - Phase 221 diff surface contained to allowlist: `git diff --name-only $(grep base_sha scripts/phase220-matrix.yaml | head -1 | awk '{print $2}' | tr -d '\"\"')..HEAD` produces ONLY paths under `.planning/phases/221-`, `.planning/todos/`, or `tests/test_phase221_mutation_boundary.py`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && .venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q</automated>
  </verify>
  <done>Phase 221 closes with SAFE-11 green, controller hot-path tests green, allowlist contained. Phase 221 ready for /gsd:verify-work.</done>
</task>

</tasks>

<verification_criteria>
- Folded tcp_12down todo is at .planning/todos/closed/<file> with three new frontmatter fields and a ## Phase 221 Closeout stanza
- On carry verdict: CRITERIA-02 verbatim text appears in the closure stanza
- 221-CLOSEOUT.md §10 frontmatter and body cite the todo-move commit SHA (no PENDING_PLAN_04_COMMIT remaining)
- git log --follow shows pre-move history for the closed todo (rename detected)
- D-17 honored: no per-axis narrowing language in closure stanza
- Final SAFE-11 mutation-boundary pytest passes green
- No src/wanctl/, Phase 213/214/220 script, or docs/ diff vs Phase 221 base SHA
- Hot-path controller tests stay green (regression-free)
</verification_criteria>

<summary_template>
@$HOME/.claude/get-shit-done/templates/summary.md
</summary_template>
