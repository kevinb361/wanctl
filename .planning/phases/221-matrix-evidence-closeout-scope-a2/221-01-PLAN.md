---
phase: 221-matrix-evidence-closeout-scope-a2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase221_mutation_boundary.py
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
autonomous: true
requirements:
  - SAFE-11
  - CLOSEOUT-01
  - CLOSEOUT-02

must_haves:
  truths:
    - "tests/test_phase221_mutation_boundary.py exists and passes green on first commit (no src/wanctl/, no scripts/phase213*, no scripts/phase214*, no scripts/phase220* diff exists yet)"
    - "PHASE221_SCRIPT_ALLOWLIST is the empty set (no new phase221-* scripts permitted) per R6"
    - "FORBIDDEN_SRC_PATHS, FORBIDDEN_MUTATION_RE, and the docs/ no-threshold-tuning assertion are copied verbatim from tests/test_phase220_mutation_boundary.py"
    - "Phase 220 scripts (phase220-target-path-matrix.sh, phase220-matrix.yaml, phase220-matrix-aggregator.py, phase220-precompute-pins.py) are added to FORBIDDEN_SRC_PATHS so Phase 221 cannot edit them"
    - "Base SHA detection chain: PHASE221_BASE_SHA env > git log --grep='docs(phase-221): begin phase execution' > scripts/phase220-matrix.yaml base_sha field"
    - "221-EVIDENCE-LEDGER.md scaffold contains a header row plus 18 cell rows (one per matrix cell), each with status=pending and replicates=0/3, EXCEPT the dallas__spectrum__daytime row which is pre-populated as complete with the Phase 220 rehearsal cell evidence (per CONTEXT D-03 multi-session resume + Phase 220-04 wet rehearsal)"
    - "Ledger schema columns exactly match R2: cell_id | replicates | window | path | target | last_replicate_utc | mtr_pre_sha256 | mtr_post_flag | base_sha | status | notes"
    - "Ledger header comment documents the status vocabulary: pending|partial|complete|incomplete|bgp-changed"
  artifacts:
    - path: tests/test_phase221_mutation_boundary.py
      provides: "SAFE-11 mutation-boundary clone (Phase 221 allowlist) — landed in Wave 0 so subsequent commits are gated"
      contains: "FORBIDDEN_SRC_PATHS"
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      provides: "Multi-session evidence ledger scaffold — operator-facing matrix status surface"
      contains: "cell_id"
  key_links:
    - from: tests/test_phase221_mutation_boundary.py
      to: tests/test_phase220_mutation_boundary.py
      via: clone-then-extend-FORBIDDEN-list
      pattern: "PHASE220_SCRIPT_ALLOWLIST.*PHASE221_SCRIPT_ALLOWLIST"
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      to: .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/
      via: pre-populated-rehearsal-cell-row
      pattern: "dallas__spectrum__daytime.*complete"
---

<objective>
Land the SAFE-11 mutation-boundary test and the multi-session evidence ledger scaffold in Wave 0 BEFORE any operator-driven cell run, so every subsequent commit (ledger update, closeout writeup, todo move) is gated by a passing controller-path-clean assertion. The Phase 221 allowlist excludes ALL `scripts/phase221-*` entries — Phase 221 produces no new scripts. The Phase 220 scripts are added to the FORBIDDEN list so the Phase 220 harness lock is enforced from Phase 221's first commit.

Purpose: per RESEARCH R6 the SAFE-11 clone is Phase 221's only pytest; per R12 it's the per-commit gate; per R1 Wave 0 is the immutable contract Plans 02-04 cannot regress. The ledger (R2) is the operator-facing state surface for the multi-day Phase 221 execution; landing it in Wave 0 means session-start procedures (R7) have a deterministic starting point.

Output: One new pytest file (cloning tests/test_phase220_mutation_boundary.py verbatim, extending FORBIDDEN_SRC_PATHS with Phase 220 scripts, replacing PHASE220 references with PHASE221, setting PHASE221_SCRIPT_ALLOWLIST to {}) plus one markdown ledger scaffold with the 18-cell table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-VALIDATION.md
@tests/test_phase220_mutation_boundary.py
@scripts/phase220-matrix.yaml

<interfaces>
<!-- Phase 220's mutation-boundary test is the clone template. The Phase 221
     version adds Phase 220 scripts to FORBIDDEN_SRC_PATHS (Phase 220 harness
     is now frozen from Phase 221's view) and sets the allowlist to empty
     (Phase 221 emits no new scripts). -->

From tests/test_phase220_mutation_boundary.py (clone target):
  REPO_ROOT, PHASE_DIR, ALLOWED_SRC_PATHS, FORBIDDEN_SRC_PATHS, FORBIDDEN_MUTATION_RE,
  PHASE213_PATHS, PHASE214_PATHS, PHASE220_SCRIPT_ALLOWLIST,
  fail(), ensure(), git(), changed(), forbidden_matches(), no_git_diff(),
  test_no_forbidden_controller_path_diff(),
  test_phase220_scripts_allowlist(),  # rename + redefine for 221
  test_phase220_docs_have_no_threshold_tuning_tokens(),  # rename for 221

Phase 221 transformations:
  PHASE220 → PHASE221 (variable names, env var, marker grep, test function names)
  PHASE220_SCRIPT_ALLOWLIST = {...} → PHASE221_SCRIPT_ALLOWLIST = set()
  FORBIDDEN_SRC_PATHS += [
    "scripts/phase220-matrix.yaml",
    "scripts/phase220-target-path-matrix.sh",
    "scripts/phase220-matrix-aggregator.py",
    "scripts/phase220-precompute-pins.py",
  ]
  Base SHA resolution chain (in order):
    PHASE221_BASE_SHA env var
    git log --grep='docs(phase-221): begin phase execution' -1 --format=%H
    yaml.safe_load('scripts/phase220-matrix.yaml')['base_sha']

From scripts/phase220-matrix.yaml (read-only — source for ledger pre-population):
  cells[] — full 18-cell list (cell_id, target, path, window, replicates)
  base_sha — used for ledger column default
  thresholds — referenced by Plan 03 closeout report, not Plan 01

From .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/
  phase220-cell.json — provides started_utc, base_sha for ledger row pre-population
  signal-sheet.json — provides p99 confirmation (rehearsal complete)
  mtr-pre-1.txt — provides sha256 for ledger column
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone tests/test_phase220_mutation_boundary.py to tests/test_phase221_mutation_boundary.py with Phase 221 allowlist</name>
  <files>tests/test_phase221_mutation_boundary.py</files>
  <read_first>
    - tests/test_phase220_mutation_boundary.py (clone target — READ FIRST so the rename pattern is exact)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R6 (Phase 221 allowlist)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md (SAFE-11 allowlist for Phase 221 in `<code_context>`)
    - .planning/REQUIREMENTS.md SAFE-11 verbatim
    - scripts/phase220-matrix.yaml (base_sha — needed as fallback in resolution chain)
  </read_first>
  <action>
    Create tests/test_phase221_mutation_boundary.py by cloning tests/test_phase220_mutation_boundary.py and applying these EXACT transformations:

    1. Module docstring: replace "Phase 220 SAFE-11 mutation-boundary clone" with "Phase 221 SAFE-11 mutation-boundary clone." Replace the body to describe Phase 221 as "the operator-driven matrix-execution and closeout-reporting phase. Phase 221 is read-only with respect to controller behavior AND Phase 220 harness. Additive edits are limited to .planning/phases/221-... artifacts, the .planning/todos/closed/ destination of the folded tcp_12down todo, and this test file itself."

    2. PHASE_DIR: replace ".planning/phases/220-matrix-runner-scope-a1" with ".planning/phases/221-matrix-evidence-closeout-scope-a2"

    3. ALLOWED_SRC_PATHS: keep as `[]` (Phase 221 makes zero src/wanctl/ edits).

    4. FORBIDDEN_SRC_PATHS: keep the Phase 220 list verbatim AND append (preserving the dynamic backends_dir / fusion*.py glob extensions exactly as in Phase 220):
       ```python
       FORBIDDEN_SRC_PATHS.extend([
           "scripts/phase220-matrix.yaml",
           "scripts/phase220-target-path-matrix.sh",
           "scripts/phase220-matrix-aggregator.py",
           "scripts/phase220-precompute-pins.py",
       ])
       ```
       This freezes the Phase 220 harness from Phase 221's edits per RESEARCH R6.

    5. FORBIDDEN_MUTATION_RE: keep verbatim — same controller-mutation language is forbidden.

    6. PHASE213_PATHS, PHASE214_PATHS: keep verbatim — both globs continue to be checked by test_no_forbidden_controller_path_diff via no_git_diff.

    7. PHASE220_SCRIPT_ALLOWLIST: rename variable to `PHASE221_SCRIPT_ALLOWLIST` and set to `set()` (empty). Update the docstring/comment to: "Phase 221 emits zero new scripts. Any scripts/phase221-* file appearing in git diff is a SAFE-11 violation."

    8. Base SHA resolution: rename env var `PHASE220_BASE_SHA` → `PHASE221_BASE_SHA`. Update the marker grep from `"docs(phase-220): begin phase execution"` → `"docs(phase-221): begin phase execution"`. Keep the fallback to `scripts/phase220-matrix.yaml`'s `base_sha` field as the LAST resort (Phase 221 anchors against the Phase 220 source floor — per RESEARCH R6 base-SHA chain).

    9. Function renames:
       - `test_phase220_docs_have_no_threshold_tuning_tokens` → `test_phase221_docs_have_no_threshold_tuning_tokens`
       - `test_phase220_scripts_allowlist` → `test_phase221_scripts_allowlist`
       - Update the allowlist test body to assert ANY git-diff line under `scripts/phase221*` is a failure (the allowlist is empty so any phase221 script diff fails). Pattern: collect git diff name-only for `scripts/phase221*` across unstaged/staged/committed channels; fail if any line is present.

    10. test_no_forbidden_controller_path_diff: no body changes required — the forbidden list is now extended.

    11. test_phase221_docs_have_no_threshold_tuning_tokens: body unchanged from Phase 220 — checks `git diff` for `docs/` content and asserts FORBIDDEN_MUTATION_RE does not match. Phase 221 expects ZERO `docs/` diff (the closeout report lives in .planning/, not docs/), so the test passes trivially unless a future commit edits docs/.

    The clone preserves the byte-stability spirit: the Phase 220 boundary tests passed during Phase 220 closure, so this clone+extend pattern is known-green.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/test_phase221_mutation_boundary.py`
    - Imports cleanly: `.venv/bin/python -c "import importlib.util, sys; spec=importlib.util.spec_from_file_location('m','tests/test_phase221_mutation_boundary.py'); mod=importlib.util.module_from_spec(spec); spec.loader.exec_module(mod)"`
    - Variable rename complete: `grep -c 'PHASE221_SCRIPT_ALLOWLIST' tests/test_phase221_mutation_boundary.py` ≥ 1 AND `grep -c 'PHASE220_SCRIPT_ALLOWLIST' tests/test_phase221_mutation_boundary.py` equals 0
    - Allowlist is empty: `grep -E 'PHASE221_SCRIPT_ALLOWLIST\s*=\s*set\(\)' tests/test_phase221_mutation_boundary.py` matches exactly one line
    - Phase 220 scripts in forbidden list: `grep -c 'scripts/phase220-matrix.yaml' tests/test_phase221_mutation_boundary.py` ≥ 1
    - Base SHA env renamed: `grep -c 'PHASE221_BASE_SHA' tests/test_phase221_mutation_boundary.py` ≥ 1 AND no occurrence of `PHASE220_BASE_SHA` remains
    - Marker grep updated: `grep -c "docs(phase-221): begin phase execution" tests/test_phase221_mutation_boundary.py` ≥ 1
    - Test functions renamed: `grep -cE 'def test_phase221_(docs_have_no_threshold_tuning_tokens|scripts_allowlist|.+)\(' tests/test_phase221_mutation_boundary.py` ≥ 3
    - Collect succeeds: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py --collect-only -q` exits 0 and reports ≥ 3 tests
    - Passes green NOW: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0 (no src/wanctl/, Phase 213, Phase 214, or Phase 220 diff exists at this point — first Phase 221 commit MUST be green)
    - Ruff clean: `.venv/bin/ruff check tests/test_phase221_mutation_boundary.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && .venv/bin/ruff check tests/test_phase221_mutation_boundary.py</automated>
  </verify>
  <done>Phase 221 mutation-boundary test exists, collects, and passes green on first commit; Phase 220 harness frozen from Phase 221 view; PHASE221_SCRIPT_ALLOWLIST empty per R6.</done>
</task>

<task type="auto">
  <name>Task 2: Create 221-EVIDENCE-LEDGER.md scaffold with 18 cell rows + Phase 220 rehearsal pre-population</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R2 (ledger schema) + R7 (multi-session resume)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-03 (multi-session resume), D-05 (cell-row columns — schema source), D-08/D-09 (failure vocabulary)
    - scripts/phase220-matrix.yaml (the 18-cell list — source of cell_id / target / path / window values)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json (rehearsal cell's started_utc, base_sha for pre-population)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json (rehearsal verdict — pre-populated row's notes)
  </read_first>
  <action>
    Create .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md as a multi-session resume surface. The ledger is a markdown table with one row per cell (18 cells from scripts/phase220-matrix.yaml's cells[] list). All rows start `pending` with `replicates: 0/3` EXCEPT the dallas__spectrum__daytime row which is pre-populated as `complete` with the Phase 220-04 wet rehearsal evidence already on disk.

    File structure:

    ```markdown
    ---
    phase: 221
    slug: matrix-evidence-closeout-scope-a2
    status_vocabulary: [pending, partial, complete, incomplete, bgp-changed]
    last_session_utc: 2026-06-01T00:00:00Z   # bumped each session-update commit per Plan 02
    completed_replicates: 1  # bumped by Plan 02 per session — count of *__r*/signal-sheet.json under Phase 220 evidence
    target_replicates: 54
    ---

    # Phase 221 — Matrix Evidence Ledger

    > Multi-session resume surface for the 54-run target/path/window matrix. Operator triggers cell runs out-of-session via `./scripts/phase220-target-path-matrix.sh --cell <cell_id> --replicate <N>`; each Claude session reads `.planning/phases/220-matrix-runner-scope-a1/evidence/`, updates this ledger, commits, exits. The aggregator (Phase 220-02) is NOT invoked from this ledger — only from the dedicated closeout-writing plan (Plan 03) when `completed_replicates == 54` or a CONTEXT D-09 matrix-fail trigger fires.

    ## Status Vocabulary

    | Status | Meaning |
    |--------|---------|
    | `pending` | 0/3 replicates run |
    | `partial` | 1 or 2 of 3 replicates run, no failure (operator paused) |
    | `complete` | 3/3 replicates with no D-08 failure |
    | `incomplete` | ≥1 replicate hit 3 failed attempts per D-08 — cell does NOT enter closeout table |
    | `bgp-changed` | Replicate's `mtr-pre` differs from previous cell's `mtr-post` for same target; D-10 flag set; cell still computable but excluded from defect-corroboration in §6 |

    ## Matrix-Fail Triggers (CONTEXT D-09)

    - **INVALID:** > 2 supplemental cells `incomplete` OR ANY canonical control cell `incomplete` in any window. Phase 221 reopens for replan; no closeout written.
    - **VALID with footnote:** ≤ 2 supplemental cells `incomplete`. Closeout report cites which cells were skipped and why.

    ## Cell Status

    | cell_id | replicates | window | path | target | last_replicate_utc | mtr_pre_sha256 | mtr_post_flag | base_sha | status | notes |
    |---------|------------|--------|------|--------|--------------------|----------------|---------------|----------|--------|-------|
    | dallas__spectrum__off-peak | 0/3 | off-peak | spectrum | dallas | — | — | — | — | pending | canonical control |
    | dallas__spectrum__daytime | 3/3 | daytime | spectrum | dallas | <ISO timestamp from Phase 220-04 rehearsal phase220-cell.json> | <sha256 of mtr-pre-1.txt> | false | <base_sha from phase220-cell.json> | complete | Phase 220-04 wet rehearsal; verdict ambiguous, primary_driver reflector_loss |
    | dallas__spectrum__prime-time | 0/3 | prime-time | spectrum | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__off-peak | 0/3 | off-peak | att | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__daytime | 0/3 | daytime | att | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__prime-time | 0/3 | prime-time | att | dallas | — | — | — | — | pending | canonical control |
    | vultr-dallas__spectrum__off-peak | 0/3 | off-peak | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__spectrum__daytime | 0/3 | daytime | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__spectrum__prime-time | 0/3 | prime-time | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__off-peak | 0/3 | off-peak | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__daytime | 0/3 | daytime | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__prime-time | 0/3 | prime-time | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__off-peak | 0/3 | off-peak | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__daytime | 0/3 | daytime | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__prime-time | 0/3 | prime-time | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__off-peak | 0/3 | off-peak | att | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__daytime | 0/3 | daytime | att | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__prime-time | 0/3 | prime-time | att | vultr-chicago | — | — | — | — | pending | supplemental |
    ```

    Replace placeholder tokens for the dallas__spectrum__daytime row using these exact extractions:
    - `last_replicate_utc` = value of `started_utc` field in `.planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json`
    - `mtr_pre_sha256` = first 8 hex chars of `sha256sum .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/mtr-pre-1.txt` (short form for table readability)
    - `base_sha` = value of `base_sha` field in `phase220-cell.json` (40-char SHA)
    - `mtr_post_flag` = `true` if any `mtr-post-*.txt` exists under that cell dir, else `false`

    Notes column for canonical rows tags them `canonical control` so closeout-time Table 1 / Table 2 separation (CONTEXT D-04) can be derived from this column alone.

    NOTE: the dallas__spectrum__daytime row reports `3/3` despite only one rehearsal replicate existing on disk. RESEARCH R7 says completion is detected via `find … -path '*__r*/signal-sheet.json'`. The rehearsal cell directory is `dallas__spectrum__daytime__r1/` — exactly 1 replicate, NOT 3. Adjust the row to `replicates: 1/3` and `status: partial` (NOT complete). The operator must run r2 and r3 before this cell graduates to `complete`. The notes column documents the rehearsal provenance.
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md`
    - YAML frontmatter parses: `.venv/bin/python -c "import yaml; from pathlib import Path; c=Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').read_text().split('---',2)[1]; d=yaml.safe_load(c); assert d['phase']==221 and d['target_replicates']==54 and 'pending' in d['status_vocabulary']"`
    - Header + 18 cell rows present: `awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -cE '^\| [a-z_-]+__[a-z]+__[a-z-]+ \|'` equals 18
    - Cell-id surface matches the 18-cell Cartesian product (3 targets × 2 paths × 3 windows): every `{target}__{path}__{window}` triple appears exactly once
    - Status vocabulary lines printed: `grep -cE 'pending\|partial\|complete\|incomplete\|bgp-changed' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 1
    - Pre-populated rehearsal row exists with status=partial and replicates=1/3: `grep -c 'dallas__spectrum__daytime .* 1/3 .* partial' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 1
    - Pre-populated row cites the rehearsal base_sha (40-char hex): `.venv/bin/python -c "import re; line=[l for l in open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').readlines() if 'dallas__spectrum__daytime ' in l and ' 1/3 ' in l][0]; assert re.search(r'\b[0-9a-f]{40}\b', line)"`
    - All canonical control rows annotated: `grep -c 'canonical control' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 6 (3 windows × 2 paths for the dallas canonical target)
    - All supplemental rows annotated: `grep -c 'supplemental' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 12
    - SAFE-11 still green after this commit: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -cE '^\| [a-z_-]+__[a-z]+__[a-z-]+ \|' | grep -q '^18$'</automated>
  </verify>
  <done>Ledger scaffold contains 18 cell rows + rehearsal pre-population; frontmatter parses; SAFE-11 remains green.</done>
</task>

</tasks>

<verification_criteria>
- Both files exist and pass their automated acceptance checks
- `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
- Ledger row count matches 18 cells from scripts/phase220-matrix.yaml
- PHASE221_SCRIPT_ALLOWLIST is empty per R6
- Phase 220 scripts appear in FORBIDDEN_SRC_PATHS per R6
- No src/wanctl/ diff, no scripts/phase213*/phase214*/phase220* diff, no docs/ diff exists at commit time
</verification_criteria>

<summary_template>
@$HOME/.claude/get-shit-done/templates/summary.md
</summary_template>
