---
phase: 221-matrix-evidence-closeout-scope-a2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase221_mutation_boundary.py
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
autonomous: true
requirements:
  - SAFE-11
  - CLOSEOUT-01
  - CLOSEOUT-02

must_haves:
  truths:
    - "tests/test_phase221_mutation_boundary.py exists and passes green on first commit (no src/wanctl/ diff, no scripts/phase213*, no scripts/phase214*, no scripts/phase220* diff exists yet)"
    - "PHASE221_SCRIPT_ALLOWLIST is the empty set (no new phase221-* scripts permitted) per R6"
    - "FORBIDDEN_SRC_PATHS, FORBIDDEN_MUTATION_RE, and the docs/ no-threshold-tuning assertion are copied verbatim from tests/test_phase220_mutation_boundary.py"
    - "Phase 220 scripts (phase220-target-path-matrix.sh, phase220-matrix.yaml, phase220-matrix-aggregator.py, phase220-precompute-pins.py) are added to FORBIDDEN_SRC_PATHS so Phase 221 cannot edit them"
    - "Base SHA detection chain: PHASE221_BASE_SHA env > git log --grep='docs(phase-221): begin phase execution' > scripts/phase220-matrix.yaml base_sha field — and a NEW dedicated test asserts that the resolved base_sha is the EXACT same chain used by tests/test_phase220_mutation_boundary.py (single source of truth across phase boundaries)"
    - "221-EVIDENCE-LEDGER.md scaffold contains a header row plus 18 cell rows (one per matrix cell), each with status=pending and replicates=0/3, EXCEPT the dallas__spectrum__daytime row which is pre-populated as partial 1/3 (NOT complete) reflecting the single Phase 220-04 rehearsal replicate at dallas__spectrum__daytime__r1/"
    - "Ledger schema columns exactly match R2: cell_id | replicates | window | path | target | last_replicate_utc | mtr_pre_sha256 | mtr_post_flag | base_sha | status | notes"
    - "Status vocabulary is exactly {pending, partial, complete, incomplete} — `bgp-changed` is NOT a status; BGP detection is reported via the independent boolean column `mtr_post_flag` derived from phase220-cell.json[\"path_change_detected\"] (per Codex review HIGH on Plan 01)"
    - "Ledger row-count acceptance regex uses `^\\| [^|]+__[^|]+__[^|]+ \\|` so hyphenated targets (vultr-dallas, vultr-chicago) are matched correctly (per Codex review MEDIUM on Plan 01)"
    - "Plan 01 acceptance includes an EXPLICIT three-channel check (unstaged + staged + committed vs base_sha) that scripts/phase220-* and scripts/phase213-* and scripts/phase214-* diffs are empty — Phase 220 freeze visible across all three git channels (per Codex review LOW on Plan 01)"
  artifacts:
    - path: tests/test_phase221_mutation_boundary.py
      provides: "SAFE-11 mutation-boundary clone (Phase 221 allowlist) — landed in Wave 0 so subsequent commits are gated"
      contains: "FORBIDDEN_SRC_PATHS"
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      provides: "Multi-session evidence ledger scaffold — operator-facing matrix status surface"
      contains: "cell_id"
  key_links:
    - from: tests/test_phase221_mutation_boundary.py
      to: tests/test_phase220_mutation_boundary.py
      via: clone-then-extend-FORBIDDEN-list
      pattern: "PHASE220_SCRIPT_ALLOWLIST.*PHASE221_SCRIPT_ALLOWLIST"
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      to: .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/
      via: pre-populated-rehearsal-cell-row
      pattern: "dallas__spectrum__daytime.*partial.*1/3"
---

<objective>
Land the SAFE-11 mutation-boundary test and the multi-session evidence ledger scaffold in Wave 0 BEFORE any operator-driven cell run, so every subsequent commit (ledger update, closeout writeup, todo move) is gated by a passing controller-path-clean assertion. The Phase 221 allowlist excludes ALL `scripts/phase221-*` entries — Phase 221 produces no new scripts. The Phase 220 scripts are added to the FORBIDDEN list so the Phase 220 harness lock is enforced from Phase 221's first commit.

Replan note (post-Codex review, 2026-06-01): the ledger uses `path_change_detected` from `phase220-cell.json` for BGP flagging (NOT the existence of `mtr-post-*.txt`, which the wrapper writes every replicate). The rehearsal-cell row is `partial 1/3` (matching the single replicate on disk), not `complete 3/3`. Status vocabulary drops `bgp-changed` — BGP is an independent boolean column (`mtr_post_flag`), not a primary status. Ledger row-count regex switches to `[^|]+__[^|]+__[^|]+` for hyphenated-target safety. New acceptance check explicitly verifies Phase 220 script freeze across unstaged/staged/committed channels.

Purpose: per RESEARCH R6 the SAFE-11 clone is Phase 221's only pytest; per R12 it's the per-commit gate; per R1 Wave 0 is the immutable contract Plans 02-04 cannot regress. The ledger (R2) is the operator-facing state surface for the multi-day Phase 221 execution; landing it in Wave 0 means session-start procedures (R7) have a deterministic starting point.

Output: One new pytest file (cloning tests/test_phase220_mutation_boundary.py verbatim, extending FORBIDDEN_SRC_PATHS with Phase 220 scripts, replacing PHASE220 references with PHASE221, setting PHASE221_SCRIPT_ALLOWLIST to {}) plus one markdown ledger scaffold with the 18-cell table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-VALIDATION.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-REVIEWS.md
@tests/test_phase220_mutation_boundary.py
@scripts/phase220-matrix.yaml
@scripts/phase220-target-path-matrix.sh

<interfaces>
<!-- Phase 220's mutation-boundary test is the clone template. The Phase 221
     version adds Phase 220 scripts to FORBIDDEN_SRC_PATHS (Phase 220 harness
     is now frozen from Phase 221's view) and sets the allowlist to empty
     (Phase 221 emits no new scripts). -->

From tests/test_phase220_mutation_boundary.py (clone target):
  REPO_ROOT, PHASE_DIR, ALLOWED_SRC_PATHS, FORBIDDEN_SRC_PATHS, FORBIDDEN_MUTATION_RE,
  PHASE213_PATHS, PHASE214_PATHS, PHASE220_SCRIPT_ALLOWLIST,
  fail(), ensure(), git(), changed(), forbidden_matches(), no_git_diff(),
  test_no_forbidden_controller_path_diff(),
  test_no_other_src_diff_outside_allowlist(),
  test_no_phase213_or_phase214_scripts_diff(),

Phase 221 transformations:
  PHASE220 → PHASE221 (variable names, env var, marker grep, test function names)
  PHASE220_SCRIPT_ALLOWLIST = {...} → PHASE221_SCRIPT_ALLOWLIST = set()
  Append a NEW constant PHASE220_FROZEN_PATHS = ["scripts/phase220-*"] used by the
  new test_no_phase220_scripts_diff function (NOT by extending FORBIDDEN_SRC_PATHS;
  the controller-path forbidden list stays clean per the Phase 220 test convention
  of one path-class per test function).
  Base SHA resolution chain (in order):
    PHASE221_BASE_SHA env var
    git log --grep='docs(phase-221): begin phase execution' -1 --format=%H
    yaml.safe_load('scripts/phase220-matrix.yaml')['base_sha']

  NEW test function:
    test_no_phase220_scripts_diff() — calls no_git_diff(PHASE220_FROZEN_PATHS, "phase220-*")
    so Phase 220 scripts are frozen across unstaged/staged/committed channels.

From scripts/phase220-matrix.yaml (read-only — source for ledger pre-population):
  cells[] — full 18-cell list (cell_id, target, path, window, replicates)
  base_sha — used for ledger column default
  thresholds — referenced by Plan 03 closeout report, not Plan 01

From .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/
  phase220-cell.json — provides started_utc, base_sha, path_change_detected for ledger row pre-population
  signal-sheet.json — provides p99 confirmation (rehearsal complete)
  mtr-pre-1.txt — provides sha256 for ledger column

  Note: rehearsal cell has ONE replicate on disk (r1 only). Ledger row reports
  `partial 1/3`, NOT `complete 3/3`. Operator must run r2 + r3 to graduate to
  `complete`. The `notes` column documents rehearsal provenance.

BGP / path-change derivation:
  mtr_post_flag column value = bool(phase220-cell.json["path_change_detected"])
  The wrapper ALWAYS writes mtr-post-{N}.txt (scripts/phase220-target-path-matrix.sh:372),
  so file existence is not a usable signal. The authoritative signal is the
  `path_change_detected` boolean inside the sidecar manifest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone tests/test_phase220_mutation_boundary.py to tests/test_phase221_mutation_boundary.py with Phase 221 allowlist + Phase 220 freeze test</name>
  <files>tests/test_phase221_mutation_boundary.py</files>
  <read_first>
    - tests/test_phase220_mutation_boundary.py (clone target — READ FIRST so the rename pattern is exact)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R6 (Phase 221 allowlist)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md (SAFE-11 allowlist for Phase 221 in `<code_context>`)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-REVIEWS.md Plan 01 concerns (LOW: explicit Phase 220 freeze acceptance check)
    - .planning/REQUIREMENTS.md SAFE-11 verbatim
    - scripts/phase220-matrix.yaml (base_sha — needed as fallback in resolution chain)
  </read_first>
  <action>
    Create tests/test_phase221_mutation_boundary.py by cloning tests/test_phase220_mutation_boundary.py and applying these EXACT transformations:

    1. Module docstring: replace "Phase 220 SAFE-11 mutation-boundary clone" with "Phase 221 SAFE-11 mutation-boundary clone." Replace the body to describe Phase 221 as "the operator-driven matrix-execution and closeout-reporting phase. Phase 221 is read-only with respect to controller behavior AND Phase 220 harness. Additive edits are limited to .planning/phases/221-... artifacts, the .planning/todos/closed/ destination of the folded tcp_12down todo, and this test file itself."

    2. PHASE_DIR: replace ".planning/phases/220-matrix-runner-scope-a1" with ".planning/phases/221-matrix-evidence-closeout-scope-a2".

    3. ALLOWED_SRC_PATHS: keep as `[]` (Phase 221 makes zero src/wanctl/ edits).

    4. FORBIDDEN_SRC_PATHS: keep VERBATIM from the Phase 220 test (do NOT append Phase 220 scripts here). The controller-path forbidden list stays focused on src/wanctl/ paths. Phase 220 script freeze is enforced by a new dedicated test function (see step 7) — one path-class per test function, matching the Phase 220 convention of test_no_phase213_or_phase214_scripts_diff.

    5. FORBIDDEN_MUTATION_RE: keep verbatim — same controller-mutation language is forbidden.

    6. PHASE213_PATHS, PHASE214_PATHS: keep verbatim — both globs continue to be checked by test_no_phase213_or_phase214_scripts_diff via no_git_diff.

    7. Replace PHASE220_SCRIPT_ALLOWLIST with TWO new constants:
       ```python
       PHASE220_FROZEN_PATHS = ["scripts/phase220-*"]
       PHASE221_SCRIPT_ALLOWLIST: set[str] = set()
       ```
       Comment: "Phase 221 emits zero new scripts. Phase 220 scripts are frozen from Phase 221's edits (test_no_phase220_scripts_diff). Any scripts/phase221-* file appearing in git diff is a SAFE-11 violation (test_phase221_scripts_allowlist)."

    8. Base SHA resolution: rename env var `PHASE220_BASE_SHA` → `PHASE221_BASE_SHA`. Update the marker grep from `"docs(phase-220): begin phase execution"` → `"docs(phase-221): begin phase execution"`. Keep the fallback to `scripts/phase220-matrix.yaml`'s `base_sha` field as the LAST resort (Phase 221 anchors against the Phase 220 source floor — per RESEARCH R6 base-SHA chain). Wrap the resolution in a module-level function `resolve_phase221_base_sha()` returning the SHA string — the function is exposed so Plan 02/03/04 acceptance scripts can import and reuse the same resolver (per Codex MEDIUM on Plan 04).

    9. Function renames + new function:
       - `test_no_forbidden_controller_path_diff` — keep name, body unchanged.
       - `test_no_other_src_diff_outside_allowlist` — keep name, body unchanged.
       - `test_no_phase213_or_phase214_scripts_diff` — keep name, body unchanged.
       - NEW `test_no_phase220_scripts_diff` — body: `no_git_diff(PHASE220_FROZEN_PATHS, "phase220-*")`. This is the explicit Phase 220 freeze acceptance the Codex LOW review requested.
       - NEW `test_phase221_scripts_allowlist` — collect git diff name-only for `scripts/phase221*` across unstaged/staged/committed channels; assert no line matches (empty allowlist means any phase221 script file fails).
       - NEW `test_phase221_docs_have_no_threshold_tuning_tokens` — checks `git diff` for `docs/` content and asserts FORBIDDEN_MUTATION_RE does not match. Phase 221 expects ZERO `docs/` diff, so the test passes trivially unless a future commit edits docs/.

    10. The clone preserves the byte-stability spirit: the Phase 220 boundary tests passed during Phase 220 closure, so this clone+extend pattern is known-green.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/test_phase221_mutation_boundary.py`
    - Imports cleanly: `.venv/bin/python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','tests/test_phase221_mutation_boundary.py'); mod=importlib.util.module_from_spec(spec); spec.loader.exec_module(mod)"`
    - Variable rename complete: `grep -c 'PHASE221_SCRIPT_ALLOWLIST' tests/test_phase221_mutation_boundary.py` ≥ 1 AND `grep -c 'PHASE220_SCRIPT_ALLOWLIST' tests/test_phase221_mutation_boundary.py` equals 0
    - Allowlist is empty: `grep -E 'PHASE221_SCRIPT_ALLOWLIST.*set\(\)' tests/test_phase221_mutation_boundary.py` matches at least one line
    - PHASE220_FROZEN_PATHS constant present: `grep -c "PHASE220_FROZEN_PATHS" tests/test_phase221_mutation_boundary.py` ≥ 2 (constant declaration + use)
    - Base SHA env renamed: `grep -c 'PHASE221_BASE_SHA' tests/test_phase221_mutation_boundary.py` ≥ 1 AND no occurrence of `PHASE220_BASE_SHA` remains
    - Marker grep updated: `grep -c "docs(phase-221): begin phase execution" tests/test_phase221_mutation_boundary.py` ≥ 1
    - Reusable base-SHA resolver exposed: `grep -cE "^def resolve_phase221_base_sha\(" tests/test_phase221_mutation_boundary.py` ≥ 1
    - Test functions present: `grep -cE 'def test_(no_forbidden_controller_path_diff|no_other_src_diff_outside_allowlist|no_phase213_or_phase214_scripts_diff|no_phase220_scripts_diff|phase221_scripts_allowlist|phase221_docs_have_no_threshold_tuning_tokens)\(' tests/test_phase221_mutation_boundary.py` equals 6
    - Collect succeeds: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py --collect-only -q` exits 0 and reports ≥ 6 tests
    - Passes green NOW: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
    - Ruff clean: `.venv/bin/ruff check tests/test_phase221_mutation_boundary.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && .venv/bin/ruff check tests/test_phase221_mutation_boundary.py</automated>
  </verify>
  <done>Phase 221 mutation-boundary test exists, collects, and passes green on first commit; Phase 220 harness frozen via dedicated test_no_phase220_scripts_diff (Codex LOW addressed); PHASE221_SCRIPT_ALLOWLIST empty per R6; base-SHA resolver exposed for downstream plan reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Create 221-EVIDENCE-LEDGER.md scaffold with 18 cell rows + rehearsal pre-population as partial 1/3</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R2 (ledger schema) + R7 (multi-session resume) + R8 (BGP handling)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-03, D-05, D-08, D-09, D-10
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-REVIEWS.md Plan 01 + Plan 02 concerns (BGP semantics, status vocabulary, regex)
    - scripts/phase220-matrix.yaml (18-cell list)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json (rehearsal cell's started_utc, base_sha, path_change_detected)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json (rehearsal verdict — notes column)
  </read_first>
  <action>
    Create .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md as the multi-session resume surface. The ledger is a markdown table with one row per cell (18 cells from scripts/phase220-matrix.yaml's cells[] list). All rows start `pending` with `replicates: 0/3` EXCEPT the dallas__spectrum__daytime row which is pre-populated as `partial 1/3` (matching the single rehearsal replicate on disk; per Codex Plan 01 MEDIUM the previous `3/3 complete` framing was inconsistent with the on-disk state and is dropped here).

    File structure:

    ```markdown
    ---
    phase: 221
    slug: matrix-evidence-closeout-scope-a2
    status_vocabulary: [pending, partial, complete, incomplete]
    last_session_utc: 2026-06-01T00:00:00Z   # bumped each session-update commit per Plan 02
    completed_replicates: 1   # bumped by Plan 02 per session — count of valid (schema_version=1, cell_id non-null) RUN-*/phase220-cell.json sidecars under Phase 220 evidence
    target_replicates: 54
    canonical_target: dallas
    supplemental_targets: [vultr-dallas, vultr-chicago]
    ---

    # Phase 221 — Matrix Evidence Ledger

    > Multi-session resume surface for the 54-run target/path/window matrix. Operator triggers cell runs out-of-session via `./scripts/phase220-target-path-matrix.sh --cell <cell_id> --replicate <N>`; each Claude session reads `.planning/phases/220-matrix-runner-scope-a1/evidence/`, updates this ledger, commits, exits. The aggregator (Phase 220-02) is NOT invoked from this ledger — only from the dedicated closeout-writing plan (Plan 03) at end-of-matrix or D-09 valid-with-footnote readiness.

    ## Status Vocabulary (no `bgp-changed` — BGP is an independent column)

    | Status | Meaning |
    |--------|---------|
    | `pending` | 0/3 replicates run |
    | `partial` | 1 or 2 of 3 replicates run, no D-08 failure |
    | `complete` | 3/3 replicates with no D-08 failure (regardless of mtr_post_flag — BGP is reported separately) |
    | `incomplete` | D-08 retry budget exhausted (≥1 replicate hit 3 failed attempts via the ledger `attempts` annotation) — cell does NOT enter the closeout canonical/supplemental table |

    `mtr_post_flag` is an INDEPENDENT boolean column derived from `phase220-cell.json["path_change_detected"]` (NOT from `mtr-post-*.txt` file existence; the wrapper writes that file every replicate per scripts/phase220-target-path-matrix.sh:372). A `complete 3/3` cell with `mtr_post_flag: true` is still `complete`; the flag affects §6 decision-tree exclusion in Plan 03 but does NOT downgrade cell status here.

    ## Matrix-Fail Triggers (CONTEXT D-09)

    - **INVALID:** ANY canonical control cell `incomplete` in any window OR > 2 supplemental cells `incomplete`. Phase 221 reopens for replan; no closeout written; Plan 02 writes `221-MATRIX-INVALID.md` and halts.
    - **VALID with footnote:** ≤ 2 supplemental cells `incomplete` AND all 6 canonical cells `complete`. Plan 03 fires at this state too — see "Plan 03 Readiness Signal" below.

    ## Plan 03 Readiness Signal (resolves Codex Plan 02 HIGH deadlock)

    Plan 03 is ready to fire when EITHER:
    1. `completed_replicates: 54` AND all 18 cells `complete` (canonical happy path), OR
    2. All 6 canonical cells `complete` AND between 0 and 2 supplemental cells `incomplete` AND every other supplemental cell is `complete` (D-09 valid-with-footnote path), OR
    3. `221-MATRIX-INVALID.md` exists (matrix-fail short-circuit; Plan 03 writes nothing).

    Acceptance encoded via two frontmatter helper fields maintained by Plan 02:
    - `canonical_complete: <0..6>` — number of canonical cells with status=complete
    - `supplemental_incomplete: <0..12>` — number of supplemental cells with status=incomplete

    Plan 03 reads frontmatter and fires iff (canonical_complete == 6 AND supplemental_incomplete <= 2) OR matrix-fail file present.

    ## Cell Status

    | cell_id | replicates | window | path | target | last_replicate_utc | mtr_pre_sha256 | mtr_post_flag | base_sha | status | notes |
    |---------|------------|--------|------|--------|--------------------|----------------|---------------|----------|--------|-------|
    | dallas__spectrum__off-peak | 0/3 | off-peak | spectrum | dallas | — | — | — | — | pending | canonical control |
    | dallas__spectrum__daytime | 1/3 | daytime | spectrum | dallas | <ISO timestamp from Phase 220-04 rehearsal phase220-cell.json> | <first 8 hex of sha256 of mtr-pre-1.txt> | <bool from path_change_detected> | <40-char base_sha from phase220-cell.json> | partial | Phase 220-04 wet rehearsal r1 only; verdict ambiguous, primary_driver reflector_loss |
    | dallas__spectrum__prime-time | 0/3 | prime-time | spectrum | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__off-peak | 0/3 | off-peak | att | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__daytime | 0/3 | daytime | att | dallas | — | — | — | — | pending | canonical control |
    | dallas__att__prime-time | 0/3 | prime-time | att | dallas | — | — | — | — | pending | canonical control |
    | vultr-dallas__spectrum__off-peak | 0/3 | off-peak | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__spectrum__daytime | 0/3 | daytime | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__spectrum__prime-time | 0/3 | prime-time | spectrum | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__off-peak | 0/3 | off-peak | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__daytime | 0/3 | daytime | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-dallas__att__prime-time | 0/3 | prime-time | att | vultr-dallas | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__off-peak | 0/3 | off-peak | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__daytime | 0/3 | daytime | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__spectrum__prime-time | 0/3 | prime-time | spectrum | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__off-peak | 0/3 | off-peak | att | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__daytime | 0/3 | daytime | att | vultr-chicago | — | — | — | — | pending | supplemental |
    | vultr-chicago__att__prime-time | 0/3 | prime-time | att | vultr-chicago | — | — | — | — | pending | supplemental |
    ```

    Add a brief inline note above the Cell Status table documenting the placeholder substitutions for the rehearsal row:
    - `last_replicate_utc` = `started_utc` from `.planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json`
    - `mtr_pre_sha256` = first 8 hex of `sha256sum .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/mtr-pre-1.txt`
    - `base_sha` = `base_sha` field in `phase220-cell.json` (40-char SHA — must equal scripts/phase220-matrix.yaml's base_sha)
    - `mtr_post_flag` = `bool(path_change_detected)` from `phase220-cell.json` (per Codex Plan 01 HIGH; NOT file existence)

    Notes column on canonical rows tags them `canonical control`; supplemental rows tag `supplemental`. This drives Plan 02's canonical_complete / supplemental_incomplete frontmatter counters.

    Evidence durability decision (resolves Codex Plan 02 MEDIUM): raw evidence (Phase 220 RUN-* directories under `.planning/phases/220-matrix-runner-scope-a1/evidence/`) is NOT committed by Phase 221. Phase 221 commits only ledger + closeout + todo move. The ledger's `last_replicate_utc` + `mtr_pre_sha256` + `base_sha` columns ARE the durable per-cell audit trail; raw RUN-* dirs remain on the operator's local filesystem (matching Phase 220 evidence policy). The SAFE-11 boundary test does NOT check evidence dirs, so this policy is allowlist-compatible.
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md`
    - YAML frontmatter parses: `.venv/bin/python -c "import yaml; from pathlib import Path; c=Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').read_text().split('---',2)[1]; d=yaml.safe_load(c); assert d['phase']==221 and d['target_replicates']==54 and set(d['status_vocabulary'])=={'pending','partial','complete','incomplete'}"`
    - Header + 18 cell rows present using hyphenated-safe regex (per Codex Plan 01 MEDIUM): `awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -cE '^\| [^|]+__[^|]+__[^|]+ \|'` equals 18
    - Cell-id surface matches the 18-cell Cartesian product (3 targets × 2 paths × 3 windows): every `{target}__{path}__{window}` triple from scripts/phase220-matrix.yaml appears exactly once
    - Status vocabulary documented (no `bgp-changed`): `grep -c '| ``bgp-changed`` |' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 0
    - Pre-populated rehearsal row exists with status=partial and replicates=1/3 (per Codex Plan 01 MEDIUM): `grep -cE 'dallas__spectrum__daytime .* 1/3 .* partial' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 1
    - Pre-populated row cites a 40-char base_sha: `.venv/bin/python -c "import re; line=[l for l in open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').readlines() if 'dallas__spectrum__daytime ' in l and ' 1/3 ' in l][0]; assert re.search(r'\b[0-9a-f]{40}\b', line)"`
    - All canonical control rows annotated: `grep -c 'canonical control' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 6
    - All supplemental rows annotated: `awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -c '| supplemental |'` equals 12
    - Readiness counters present in frontmatter design (canonical_complete / supplemental_incomplete documented in body): `grep -c 'canonical_complete' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 1 AND `grep -c 'supplemental_incomplete' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 1
    - SAFE-11 still green after this commit: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && test "$(awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -cE '^\| [^|]+__[^|]+__[^|]+ \|')" = "18"</automated>
  </verify>
  <done>Ledger scaffold contains 18 cell rows with hyphenated-target-safe pattern; rehearsal pre-populated as partial 1/3 (Codex MEDIUM resolved); status vocabulary drops `bgp-changed`; mtr_post_flag derives from path_change_detected (Codex HIGH resolved); SAFE-11 remains green.</done>
</task>

</tasks>

<verification_criteria>
- Both files exist and pass their automated acceptance checks
- `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
- Ledger row count matches 18 cells from scripts/phase220-matrix.yaml (regex `[^|]+__[^|]+__[^|]+` — hyphenated-target safe)
- PHASE221_SCRIPT_ALLOWLIST is empty per R6
- A NEW test function `test_no_phase220_scripts_diff` exists and passes — explicit three-channel Phase 220 freeze acceptance per Codex LOW
- Reusable `resolve_phase221_base_sha()` is exposed for Plan 02/03/04 acceptance scripts
- Status vocabulary is {pending, partial, complete, incomplete} — `bgp-changed` is NOT a status; BGP is a column
- mtr_post_flag derives from phase220-cell.json["path_change_detected"], NOT mtr-post-*.txt file existence
- Rehearsal row is `partial 1/3`, not `complete 3/3`
- Plan 03 readiness signal documented inline (canonical_complete == 6 AND supplemental_incomplete <= 2) — resolves D-09 deadlock
- No src/wanctl/ diff, no scripts/phase213*/phase214*/phase220* diff, no docs/ diff exists at commit time
</verification_criteria>

<summary_template>
@$HOME/.claude/get-shit-done/templates/summary.md
</summary_template>
