---
phase: 221-matrix-evidence-closeout-scope-a2
plan: 03
type: execute
wave: 2
depends_on: [221-02]
files_modified:
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
autonomous: true
requirements:
  - CLOSEOUT-01
  - CLOSEOUT-02
  - SAFE-11

must_haves:
  truths:
    - "Plan 03 fires ONLY when ledger frontmatter shows completed_replicates: 54 AND 221-MATRIX-INVALID.md does NOT exist (per Plan 02 Task 2 readiness signal)"
    - "If 221-MATRIX-INVALID.md exists, Plan 03 SHORT-CIRCUITS and writes nothing — matrix-fail path goes straight to Phase 221 replan, no closeout"
    - "Aggregator (scripts/phase220-matrix-aggregator.py) is invoked EXACTLY ONCE here, never in Plan 02 — per CONTEXT D-07 + RESEARCH R4 anti-data-driven-stop posture"
    - "221-CLOSEOUT.json IS the aggregator output, written with schema_version: 1 envelope per Phase 219 D-17 convention (RESEARCH R11)"
    - "221-CLOSEOUT.md is a PURE READ of the aggregator JSON plus BGP-exclusion overlay (R8) plus §9 historical-reportage overlay (R10) — does NOT re-derive cell verdicts"
    - "Verdict in §1 is EXACTLY ONE of {defect_located, hypothesis_killed, carried_narrower_with_close_with_prejudice_rule} — no other token permitted (CONTEXT D-07)"
    - "§2 cites Phase 220 YAML SHA verbatim AND copies the six locked threshold values from scripts/phase220-matrix.yaml's thresholds: block — citation not redefinition"
    - "§3 Table 1 has EXACTLY 6 rows (3 windows × 2 paths for dallas canonical) and §4 Table 2 has EXACTLY 12 rows (2 supplemental × 2 paths × 3 windows) — canonical separation enforced visually per CONTEXT D-04"
    - "§5 per-axis rollup cites aggregation rule inline: 'any cell_defect wins → any cell_carry wins → else cell_kill_clear'"
    - "§6 decision-tree trace shows each branch as MATCHED or FAILED with evidence; on BGP-flagged cells (mtr_post_flag: true AND cell_verdict: cell_defect), the trace EXPLICITLY EXCLUDES the cell from orthogonal-corroboration arguments per CONTEXT D-10"
    - "§7 BGP footnote lists cells with mtr_post_flag: true; §8 failed-cell footnote lists cells with status: incomplete (if any)"
    - "§9 historical context references 2026-04-15 02:45 CDT p99=3059ms and Phase 214 daytime/prime-time anchors (606ms, 560ms) — reportage NOT verdict input"
    - "§10 todo disposition section has closeout_commit_for_todo field initially set to 'PENDING_PLAN_04_COMMIT' — Plan 04 amends with actual SHA"
    - "§11 cites tests/test_phase221_mutation_boundary.py last-run timestamp + pass status"
    - "SAFE-11 mutation-boundary test passes green after Plan 03 commit — only .planning/phases/221-... files modified"
  artifacts:
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
      provides: "Aggregator output JSON — cube rollup + MWU + bootstrap CI + decision-tree trace + schema_version=1 envelope (Phase 219 D-17)"
      contains: "schema_version"
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
      provides: "Human-readable closeout report — 11 sections per RESEARCH R3; verdict locked in §1; pending todo-move commit SHA in §10"
      contains: "## §1"
  key_links:
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
      to: scripts/phase220-matrix-aggregator.py
      via: read-only-invocation
      pattern: "--evidence-root .*evidence --output"
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
      to: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
      via: pure-read-formatting
      pattern: "cells|per_axis|matrix_verdict"
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md
      to: scripts/phase220-matrix.yaml
      via: threshold-citation-by-SHA
      pattern: "phase220-matrix.yaml.*[0-9a-f]{7,40}"
---

<objective>
Invoke the Phase 220 aggregator ONCE on the completed 54-replicate evidence corpus, producing `221-CLOSEOUT.json` (schema_version=1 envelope). Then write `221-CLOSEOUT.md` as a PURE READ of that JSON formatted into the 11-section report structure from RESEARCH R3, with BGP-exclusion overlay (R8) and §9 historical reportage (R10). The verdict written in §1 is exactly one of `{defect_located, hypothesis_killed, carried_narrower_with_close_with_prejudice_rule}`. No re-derivation of cell verdicts inside this plan — the aggregator owns the math, the closeout owns the formatting.

Purpose: CONTEXT D-04 through D-07 specify the report's section structure; CONTEXT D-10 specifies BGP handling; RESEARCH R3 + R10 mandate the pure-read flow so the closeout cannot drift from aggregator verdicts; RESEARCH R11 confirms the JSON companion. The §1 verdict is the single deliverable everything else cites; getting it from aggregator output (not human paraphrase) is the anti-leakage discipline.

Output: One JSON file (aggregator output) + one markdown file (11 sections, frontmatter, references). Two commits — one per file, OR one combined commit. Recommended: combined commit `docs(221): write closeout report (verdict: <X>)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-VALIDATION.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
@scripts/phase220-matrix.yaml
@scripts/phase220-matrix-aggregator.py

<interfaces>
<!-- Plan 03 reads the aggregator's output and formats markdown. The aggregator
     emits structured cube data; the closeout report is the human view. -->

Phase 220 aggregator invocation (Phase 220-02 contract):
  .venv/bin/python scripts/phase220-matrix-aggregator.py \
    --evidence-root .planning/phases/220-matrix-runner-scope-a1/evidence \
    --output .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json

Expected aggregator JSON envelope (per Phase 219 D-17 + Phase 220-02 spec):
  {
    "schema_version": 1,
    "phase": 220,
    "matrix_base_sha": "<sha>",
    "phase220_yaml_sha": "<sha>",
    "aggregator_run_utc": "<ISO 8601>",
    "thresholds": { ...six locked values from yaml... },
    "cells": [
      {
        "cell_id": "<id>",
        "target_kind": "canonical|supplemental",
        "target_name": "<name>",
        "path": "spectrum|att",
        "window": "off-peak|daytime|prime-time",
        "replicate_count": "3/3" | "1/3 (incomplete)",
        "run_timestamps_utc": ["<iso>", ...],
        "p50_ms": float, "p95_ms": float, "p99_ms": float,
        "primary_driver": "<one of six>",
        "ranked_drivers_top3": [{"name": "<>", "weight": float}, ...],
        "mtr_pre_ref": "<path>",
        "mtr_post_flag": bool,
        "base_sha": "<sha>",
        "cell_verdict": "cell_defect|cell_kill_clear|cell_carry"
      },
      ...
    ],
    "per_axis": {
      "per_target":  [{"target": "<>", "verdict": "<>"}, ...],
      "per_path":    [{"path": "<>", "verdict": "<>"}, ...],
      "per_window":  [{"window": "<>", "verdict": "<>"}, ...]
    },
    "matrix_verdict": "defect_located|hypothesis_killed|carried_narrower_with_close_with_prejudice_rule",
    "decision_tree_trace": [
      {"branch": "defect_located", "result": "MATCHED|FAILED", "evidence": "<text>"},
      {"branch": "hypothesis_killed", "result": "MATCHED|FAILED", "evidence": "<text>"},
      {"branch": "carried_narrower_*", "result": "MATCHED|FAILED", "evidence": "<text>"}
    ]
  }

  If aggregator does NOT emit `decision_tree_trace` (verify by reading
  scripts/phase220-matrix-aggregator.py source FIRST), Plan 03 Task 2 synthesizes
  the trace from matrix_verdict + per_axis + cells per RESEARCH R10 algorithm.

Phase 220 YAML SHA extraction:
  git log -1 --format=%H -- scripts/phase220-matrix.yaml
  # OR (preferred) blob SHA: git ls-files -s scripts/phase220-matrix.yaml | awk '{print $2}'

Phase 214 anchor numbers for §9 historical reportage:
  Phase 214 daytime canonical dallas p99 ≈ 606ms (verdict ambiguous)
  Phase 214 prime-time canonical dallas p99 ≈ 560ms (verdict ambiguous)
  Folded-todo historical worst-case: 2026-04-15 02:45 CDT p99 = 3059ms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks — readiness signal + aggregator schema verification</name>
  <files></files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md (frontmatter)
    - scripts/phase220-matrix-aggregator.py (source — verify output schema fields)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-04 through D-10
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R10 (pure-read contract) + R11 (JSON emission)
  </read_first>
  <action>
    Verify Plan 03 readiness BEFORE touching any output file:

    1. **Matrix-fail short-circuit:** if `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md` exists, Plan 03 STOPS. Print: "Plan 03 aborted: matrix-fail state detected; Phase 221 must replan." Exit 0 (this is not an error — it's the matrix-fail path).

    2. **Completion gate:** parse `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` frontmatter. Assert `completed_replicates == 54`. If less, Plan 03 STOPS with: "Plan 03 not yet ready: ledger shows <N>/54 replicates. Continue Plan 02 sessions."

    3. **Filesystem cross-check:** confirm `find .planning/phases/220-matrix-runner-scope-a1/evidence -path '*__r*/signal-sheet.json' -type f | wc -l` equals 54. If diverges from ledger, halt with "Ledger drift detected: ledger=<L>, disk=<D>. Plan 02 must re-reconcile."

    4. **Aggregator schema discovery:** read `scripts/phase220-matrix-aggregator.py` and identify whether the output JSON includes a `decision_tree_trace` field. Capture the answer in a session-local variable `HAS_DECISION_TREE_TRACE = true|false`. If false, Plan 03 Task 2 synthesizes the trace per RESEARCH R10. If true, Plan 03 Task 2 reads it directly. Document the answer in commit message.

    5. **Phase 220 YAML SHA extraction:** capture `PHASE220_YAML_SHA = $(git rev-parse HEAD:scripts/phase220-matrix.yaml)` — the blob SHA, NOT the commit SHA. This is the threshold-citation anchor for §2.

    6. **Locked thresholds extraction:** load `scripts/phase220-matrix.yaml` and extract the six values under `thresholds:`. Confirm match against CONTEXT D-04 / RESEARCH R3 list:
       - `canonical_control_p99_kill_ms` (expected: 200)
       - `canonical_min_windows_kill` (expected: 2)
       - `canonical_max_windows_kill_total` (expected: 3)
       - `supplemental_defect_p99_ms` (expected: 500)
       - `supplemental_defect_min_windows` (expected: 2)
       - `supplemental_carry_multiplier_of_control` (expected: 1.5)
       If ANY value diverges from CONTEXT/locked spec, Plan 03 HALTS — this is a Pitfall 2 leakage signal.

    No files written in this task; this is gate-only.
  </action>
  <acceptance_criteria>
    - All four pre-flight checks pass before Task 2 starts (verified by stdout transcript or session-local variables)
    - `PHASE220_YAML_SHA` is a 40-char hex SHA: `git rev-parse HEAD:scripts/phase220-matrix.yaml | grep -qE '^[0-9a-f]{40}$'`
    - Six locked thresholds match locked values exactly: `.venv/bin/python -c "import yaml; t=yaml.safe_load(open('scripts/phase220-matrix.yaml'))['thresholds']; assert t['canonical_control_p99_kill_ms']==200 and t['canonical_min_windows_kill']==2 and t['canonical_max_windows_kill_total']==3 and t['supplemental_defect_p99_ms']==500 and t['supplemental_defect_min_windows']==2 and t['supplemental_carry_multiplier_of_control']==1.5"`
    - Matrix-fail short-circuit honored: `test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md && echo "stop"; test ! -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md && echo "go"` returns the correct branch
    - Ledger frontmatter `completed_replicates == 54`: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').read_text().split('---',2)[1]); assert d['completed_replicates']==54"`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').read_text().split('---',2)[1]); assert d['completed_replicates']==54" && .venv/bin/python -c "import yaml; t=yaml.safe_load(open('scripts/phase220-matrix.yaml'))['thresholds']; assert all([t['canonical_control_p99_kill_ms']==200, t['supplemental_defect_p99_ms']==500, t['supplemental_carry_multiplier_of_control']==1.5])"</automated>
  </verify>
  <done>Pre-flight gates pass; aggregator schema verified; PHASE220_YAML_SHA captured for §2 citation.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke aggregator and write 221-CLOSEOUT.json</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json</files>
  <read_first>
    - scripts/phase220-matrix-aggregator.py (CLI surface — confirm --evidence-root and --output flags)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R4 (aggregator timing) + R11 (JSON emission)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/ (read-only — aggregator's input root)
  </read_first>
  <action>
    Invoke the Phase 220 aggregator exactly once with this command:

    ```bash
    .venv/bin/python scripts/phase220-matrix-aggregator.py \
      --evidence-root .planning/phases/220-matrix-runner-scope-a1/evidence \
      --output .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json
    ```

    If the aggregator's CLI surface differs from `--evidence-root/--output` (verify in Task 1), adjust the invocation. The aggregator must produce a JSON file at the output path with the envelope shape documented in `<interfaces>` above.

    After invocation, validate:

    1. `schema_version` field present and equals 1.
    2. `matrix_verdict` field present with exactly one of three permitted values.
    3. `cells` array has exactly 18 entries (one per cell — even if a cell is `incomplete`, it gets an entry with `cell_verdict: null` and `replicate_count: "<N>/3 (incomplete)"`).
    4. Each canonical cell has `target_kind: canonical`; each supplemental has `target_kind: supplemental`.
    5. `per_axis` has three sub-arrays (per_target, per_path, per_window) with axis-level verdicts.
    6. `phase220_yaml_sha` matches the `PHASE220_YAML_SHA` captured in Task 1.

    If `decision_tree_trace` is present (HAS_DECISION_TREE_TRACE = true from Task 1), nothing extra — closeout Task 3 reads it directly. If absent, synthesize it from `matrix_verdict + per_axis + cells` per RESEARCH R10:

    ```python
    # synthesis pseudocode for decision_tree_trace
    supp_defects = [c for c in cells if c['target_kind']=='supplemental' and c['cell_verdict']=='cell_defect']
    windows_with_defect = {c['window'] for c in supp_defects}
    canonical_under_kill = sum(1 for c in cells if c['target_kind']=='canonical' and c['p99_ms'] <= 200)
    canonical_windows = {c['window'] for c in cells if c['target_kind']=='canonical' and c['p99_ms'] <= 200}

    trace = []
    # Branch 1: defect_located
    defect_match = (
      len(windows_with_defect) >= 2
      and orthogonal_corroboration_present(supp_defects)
    )
    trace.append({
      "branch": "defect_located",
      "result": "MATCHED" if defect_match else "FAILED",
      "evidence": (
        f"supplemental cell_defects: {len(supp_defects)} across windows "
        f"{sorted(windows_with_defect)}; orthogonal corroboration: "
        f"{orthogonal_corroboration_summary(supp_defects)}"
      ),
    })
    # Branch 2: hypothesis_killed
    kill_match = (
      len(canonical_windows) >= 2
      and not any(c['p99_ms'] > 1.5 * control_p99_for_same_window(c) for c in cells if c['target_kind']=='supplemental')
    )
    trace.append({
      "branch": "hypothesis_killed",
      "result": "MATCHED" if kill_match else "FAILED",
      "evidence": (
        f"canonical windows under 200ms: {len(canonical_windows)}/3; "
        f"max supplemental ratio: {max_supplemental_control_ratio(cells):.2f}"
      ),
    })
    # Branch 3: carried_narrower
    trace.append({
      "branch": "carried_narrower_with_close_with_prejudice_rule",
      "result": "MATCHED" if not defect_match and not kill_match else "FAILED",
      "evidence": "Neither defect_located nor hypothesis_killed criteria met",
    })
    ```

    If synthesizing, REWRITE the JSON file to include the synthesized `decision_tree_trace` field. Commit message documents whether trace came from aggregator (preferred) or synthesis (fallback).
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json`
    - Valid JSON: `.venv/bin/python -c "import json; json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))"`
    - schema_version: 1: `.venv/bin/python -c "import json; assert json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['schema_version']==1"`
    - matrix_verdict is exactly one of three: `.venv/bin/python -c "import json; v=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['matrix_verdict']; assert v in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'}"`
    - 18 cells: `.venv/bin/python -c "import json; assert len(json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['cells'])==18"`
    - 6 canonical + 12 supplemental: `.venv/bin/python -c "import json; c=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['cells']; assert sum(1 for x in c if x['target_kind']=='canonical')==6 and sum(1 for x in c if x['target_kind']=='supplemental')==12"`
    - per_axis has three sub-arrays: `.venv/bin/python -c "import json; a=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['per_axis']; assert set(a.keys())=={'per_target','per_path','per_window'}"`
    - decision_tree_trace has at least 3 entries: `.venv/bin/python -c "import json; t=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json')).get('decision_tree_trace',[]); assert len(t) >= 3 and {e['branch'] for e in t} >= {'defect_located','hypothesis_killed'}"`
    - phase220_yaml_sha matches Task 1's capture: `.venv/bin/python -c "import json, subprocess; expected=subprocess.run(['git','rev-parse','HEAD:scripts/phase220-matrix.yaml'], capture_output=True, text=True).stdout.strip(); got=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json'))['phase220_yaml_sha']; assert expected==got, f'sha mismatch: yaml={expected} json={got}'"`
    - SAFE-11 green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.load(open('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json')); assert d['schema_version']==1 and d['matrix_verdict'] in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'} and len(d['cells'])==18" && .venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q</automated>
  </verify>
  <done>221-CLOSEOUT.json exists with schema_version=1, 18 cells, valid matrix_verdict, decision_tree_trace ≥ 3 entries; phase220_yaml_sha matches the yaml blob SHA; SAFE-11 green.</done>
</task>

<task type="auto">
  <name>Task 3: Write 221-CLOSEOUT.md — 11-section human report formatted from the JSON</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.json (just-written aggregator output)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-04 through D-10 (report schema + BGP handling)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R3 (11-section structure) + R8 (BGP overlay) + R10 (pure-read contract)
    - scripts/phase220-matrix.yaml (thresholds block — verbatim copy into §2)
    - .planning/REQUIREMENTS.md (CRITERIA-01 + CRITERIA-02 — referenced from §2 + §10)
  </read_first>
  <action>
    Write `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` with exactly this 11-section structure. The JSON is the source of truth; markdown is presentation.

    Frontmatter:

    ```yaml
    ---
    phase: 221
    slug: matrix-evidence-closeout-scope-a2
    verdict: <matrix_verdict from JSON>
    matrix_base_sha: <matrix_base_sha from JSON>
    phase220_yaml_sha: <phase220_yaml_sha from JSON>
    aggregator_run_utc: <aggregator_run_utc from JSON>
    closeout_commit_for_todo: PENDING_PLAN_04_COMMIT
    closeout_written_utc: <now ISO 8601>
    ---
    ```

    ### §1 Verdict

    Single-sentence prose verdict + the literal token on its own line:

    ```markdown
    ## §1 Verdict

    The Phase 221 matrix produced a {one-sentence narrative from decision_tree_trace MATCHED branch}.

    **Verdict:** <matrix_verdict>
    ```

    The token MUST be one of `{defect_located, hypothesis_killed, carried_narrower_with_close_with_prejudice_rule}`. No paraphrase.

    ### §2 Threshold Citation

    ```markdown
    ## §2 Threshold Citation

    Thresholds applied unchanged from Phase 220 commit. Phase 220 YAML blob SHA: `<phase220_yaml_sha>`.

    | Threshold | Value | REQ reference |
    |-----------|-------|---------------|
    | canonical_control_p99_kill_ms | 200 | CRITERIA-01 kill |
    | canonical_min_windows_kill | 2 | CRITERIA-01 kill |
    | canonical_max_windows_kill_total | 3 | CRITERIA-01 kill |
    | supplemental_defect_p99_ms | 500 | CRITERIA-01 defect |
    | supplemental_defect_min_windows | 2 | CRITERIA-01 defect |
    | supplemental_carry_multiplier_of_control | 1.5 | CRITERIA-01 kill (no supp > 1.5× control) |

    Verification: `git rev-parse HEAD:scripts/phase220-matrix.yaml` returns `<phase220_yaml_sha>` at the time of this report.
    ```

    ### §3 Table 1: Canonical Control

    Render EXACTLY 6 rows for the 6 canonical cells (dallas × 2 paths × 3 windows). Columns per CONTEXT D-05. Filter JSON `cells[]` by `target_kind == "canonical"`. Sort by (path, window) for stability.

    ```markdown
    ## §3 Table 1: Canonical Control

    | cell_id | target_kind | target | path | window | replicate_count | run_timestamps_utc | p50_ms | p95_ms | p99_ms | primary_driver | ranked_drivers_top3 | mtr_pre_ref | mtr_post_flag | base_sha | cell_verdict |
    |---------|-------------|--------|------|--------|-----------------|--------------------|--------|--------|--------|----------------|---------------------|-------------|---------------|----------|--------------|
    | ... 6 rows ... |
    ```

    ### §4 Table 2: Supplemental Cells

    EXACTLY 12 rows (vultr-dallas + vultr-chicago × 2 paths × 3 windows). Same columns as §3.

    ### §5 Per-Axis Rollup

    Three sub-sections per CONTEXT D-06. Aggregation rule cited inline at the top: `any cell_defect wins → any cell_carry wins → else cell_kill_clear`.

    ```markdown
    ## §5 Per-Axis Rollup

    > **Aggregation rule:** any `cell_defect` wins → any `cell_carry` wins → else `cell_kill_clear`. Canonical control is reported separately from supplemental targets per CONTEXT D-04.

    ### Per-Target Rollup

    | target | axis_verdict |
    |--------|--------------|
    | dallas (canonical) | <verdict from per_axis.per_target> |
    | vultr-dallas (supplemental) | <verdict> |
    | vultr-chicago (supplemental) | <verdict> |

    ### Per-Path Rollup

    | path | axis_verdict |
    |------|--------------|
    | spectrum | <verdict from per_axis.per_path> |
    | att | <verdict> |

    ### Per-Window Rollup

    | window | axis_verdict |
    |--------|--------------|
    | off-peak | <verdict from per_axis.per_window> |
    | daytime | <verdict> |
    | prime-time | <verdict> |
    ```

    ### §6 Matrix-Level Verdict Decision Tree

    Render the `decision_tree_trace` from JSON as a bulleted checklist with MATCHED/FAILED markers. For each branch:

    ```markdown
    - **[MATCHED|FAILED]** `<branch>`
      - Evidence: <evidence from JSON>
      - {if branch == 'defect_located' AND any cell_defect has mtr_post_flag: true:
         "BGP-exclusion overlay: cell(s) <list> excluded from orthogonal-corroboration arguments per CONTEXT D-10."}
    ```

    **BGP-exclusion overlay (RESEARCH R8 / CONTEXT D-10):** before rendering the §6 trace, identify cells with `mtr_post_flag: true AND cell_verdict == cell_defect`. For each such cell, add a line to the `defect_located` branch's evidence: "Cell `<cell_id>` carries `mtr_post_flag: true` and is EXCLUDED from defect-corroboration count per CONTEXT D-10."

    The exclusion may FLIP the §6 verdict relative to the JSON's `matrix_verdict` field. If so:
    - Document the flip explicitly: "JSON aggregator verdict: `<X>`; BGP-overlay verdict: `<Y>`. Final verdict (used in §1): `<Y>`."
    - Update §1 verdict + frontmatter to reflect the overlay verdict.
    - This is the ONLY place the closeout report deviates from a pure JSON read (RESEARCH R10).

    ### §7 BGP-Change Footnote

    List cells with `mtr_post_flag: true`. If none: "No BGP path changes detected during matrix execution."

    ### §8 Failed-Cell Footnote

    List cells with `replicate_count` containing `(incomplete)` or status `incomplete`. If none: "All 18 cells completed 3/3 replicates without D-08 failure."

    ### §9 Historical Context (Reportage — NOT verdict input)

    Compare matrix results against folded-todo and Phase 214 anchors per CONTEXT specifics:

    ```markdown
    ## §9 Historical Context

    > **This section is reportage. Verdict computation in §6 already happened.**

    ### Folded-Todo 2026-04-15 02:45 CDT Worst-Case Reproduction

    The folded `tcp_12down` todo's worst-case live capture (2026-04-15 02:45 CDT) recorded p99 = 3059ms. Reproduction status in Phase 221 matrix:

    - Cells with p99 > 1000ms: <count> — <list>
    - Cells with p99 > 500ms (defect threshold): <count>
    - Did the matrix reproduce the 3059ms worst case: <yes|no|partial>

    ### Phase 214 Anchor Reconciliation

    Phase 214 daytime canonical dallas p99 was 606ms (verdict ambiguous); Phase 221 daytime canonical dallas p99 was <p99>. Phase 214 prime-time canonical dallas p99 was 560ms; Phase 221 prime-time canonical dallas p99 was <p99>. Phase 214's `ambiguous` driver classification at these p99s is resolved by the Phase 221 corroboration rule to: <one of the three matrix verdicts>.
    ```

    Pull all `p99_ms` values from `cells[]` for the relevant cells.

    ### §10 Todo Disposition

    ```markdown
    ## §10 Todo Disposition

    **Verdict applied:** <matrix_verdict>
    **Todo file:** `.planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md`
    **Closeout commit for todo move:** `<closeout_commit_for_todo>` <!-- PENDING_PLAN_04_COMMIT — amended by Plan 04 Task 5 -->

    {IF verdict == carried_narrower_with_close_with_prejudice_rule:}

    ### Close-With-Prejudice Rule (CRITERIA-02, verbatim)

    > {grep CRITERIA-02 from .planning/REQUIREMENTS.md and paste verbatim — no paraphrase}
    ```

    Extract the CRITERIA-02 text via:
    ```bash
    grep -A 1 'CRITERIA-02' .planning/REQUIREMENTS.md | head -3
    ```

    ### §11 Mutation Boundary

    ```markdown
    ## §11 Mutation Boundary (SAFE-11)

    Phase 221 mutation-boundary pytest: `tests/test_phase221_mutation_boundary.py`
    - Last run (before this commit): PASSED
    - Controller-path diff vs Phase 220 base_sha: empty
    - Phase 213/214/220 script diff: empty
    - docs/ diff: empty (closeout report lives in .planning/, not docs/)

    Final SAFE-11 re-run is Plan 04 Task 6 at phase close.
    ```

    Commit message: `docs(221): write closeout report (verdict: <matrix_verdict>)`. Combined commit covering both 221-CLOSEOUT.json (Task 2) and 221-CLOSEOUT.md (Task 3) — they are co-authored and review benefits from atomic view.
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md`
    - YAML frontmatter parses + verdict is exactly one of three: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); assert d['verdict'] in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'}"`
    - closeout_commit_for_todo placeholder present: `grep -cE 'closeout_commit_for_todo: PENDING_PLAN_04_COMMIT' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` equals 1
    - All 11 section headers present (§1 through §11): `grep -cE '^## §([0-9]+|11) ' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` equals 11
    - §2 cites Phase 220 YAML SHA (40-char hex): `grep -cE 'phase220.*yaml.*[0-9a-f]{40}' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` ≥ 1
    - §3 Table 1 has exactly 6 data rows: `awk '/^## §3 /,/^## §4 /' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md | grep -cE '^\| [a-z]+__[a-z]+__[a-z-]+ \|'` equals 6
    - §4 Table 2 has exactly 12 data rows: `awk '/^## §4 /,/^## §5 /' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md | grep -cE '^\| [a-z-]+__[a-z]+__[a-z-]+ \|'` equals 12
    - §5 cites the aggregation rule: `grep -c 'any .cell_defect. wins' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` ≥ 1
    - §6 decision tree shows ≥ 3 branches with MATCHED/FAILED markers: `grep -cE '^- \*\*\[(MATCHED\|FAILED)\]\*\*' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md` ≥ 3
    - On carry verdict, CRITERIA-02 verbatim text present: `.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); v=d['verdict']; import sys; sys.exit(0) if v != 'carried_narrower_with_close_with_prejudice_rule' else (0 if 'close-with-prejudice' in Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().lower() else sys.exit(1))"`
    - SAFE-11 green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md').read_text().split('---',2)[1]); assert d['verdict'] in {'defect_located','hypothesis_killed','carried_narrower_with_close_with_prejudice_rule'}" && test $(grep -cE '^## §([0-9]+|11) ' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CLOSEOUT.md) -eq 11 && .venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q</automated>
  </verify>
  <done>221-CLOSEOUT.md has 11 sections; verdict is one-of-three; §3/§4 have 6/12 rows; threshold citation present; SAFE-11 green.</done>
</task>

</tasks>

<verification_criteria>
- 221-CLOSEOUT.json exists with schema_version=1, valid matrix_verdict, 18 cells (6 canonical + 12 supplemental), decision_tree_trace synthesized if absent from aggregator
- 221-CLOSEOUT.md exists with exactly 11 sections, verdict locked in §1 + frontmatter, closeout_commit_for_todo = PENDING_PLAN_04_COMMIT
- §2 cites Phase 220 YAML blob SHA verbatim; six locked thresholds copied verbatim
- §3 Table 1 has 6 canonical rows; §4 Table 2 has 12 supplemental rows
- §6 decision tree has MATCHED/FAILED markers per branch; BGP exclusion overlay applied if any cell_defect has mtr_post_flag: true
- §9 historical reportage references 2026-04-15 worst case and Phase 214 anchors WITHOUT being a verdict input
- On carry verdict, §10 has CRITERIA-02 verbatim text
- SAFE-11 mutation-boundary pytest passes green
- No diff against src/wanctl/, scripts/phase213-*, scripts/phase214-*, scripts/phase220-*, or docs/
</verification_criteria>

<summary_template>
@$HOME/.claude/get-shit-done/templates/summary.md
</summary_template>
