---
phase: 221-matrix-evidence-closeout-scope-a2
plan: 02
type: execute
wave: 1
depends_on: [221-01]
files_modified:
  - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
autonomous: false
requirements:
  - CLOSEOUT-01
  - CLOSEOUT-02
  - SAFE-11

must_haves:
  truths:
    - "Plan 02 is the operator-driven, multi-session, multi-day execution wave per RESEARCH R1 and CONTEXT D-03 — each session is ONE commit to 221-EVIDENCE-LEDGER.md reflecting new evidence dirs that appeared since last session"
    - "Each session-update reads .planning/phases/220-matrix-runner-scope-a1/evidence/ ONLY (read-only) and writes .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md ONLY (write target) per CONTEXT D-03"
    - "Aggregator is NEVER invoked from Plan 02 — that's Plan 03's job per CONTEXT D-07 + RESEARCH R4 (intermediate aggregator runs would violate Pitfall 2 data-driven-stop avoidance)"
    - "Session-update procedure is deterministic per RESEARCH R7: count *__r*/signal-sheet.json under Phase 220 evidence root; compare to ledger replicates column; reconcile differences"
    - "Ledger frontmatter completed_replicates field is bumped to match find-based count after every session-update"
    - "Cell status transitions follow R2 vocabulary: pending → partial (1-2/3 replicates) → complete (3/3) OR pending/partial → incomplete (D-08 hits 3 failures) OR pending/partial/complete → bgp-changed (D-10 mtr-post detected)"
    - "Matrix-fail trigger detection runs at every session-update: if >2 supplemental cells incomplete OR any canonical incomplete (CONTEXT D-09), Plan 02 writes .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md AND halts — does NOT advance to Plan 03"
    - "SAFE-11 mutation-boundary test re-runs after every session-update commit (validation per VALIDATION.md sampling rate)"
    - "Plan 02 transitions Plan-03 readiness when ledger frontmatter completed_replicates equals 54 (success) OR 221-MATRIX-INVALID.md exists (matrix-fail)"
  artifacts:
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      provides: "Living ledger updated across N operator sessions until completed_replicates == 54 OR matrix-fail trigger fires"
      contains: "completed_replicates"
    - path: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md
      provides: "[CONDITIONAL] Matrix-fail closure document — written ONLY when CONTEXT D-09 trigger fires; presence halts Plan 03"
      contains: "matrix_fail_reason"
  key_links:
    - from: .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
      to: .planning/phases/220-matrix-runner-scope-a1/evidence/
      via: find-based-completion-count
      pattern: "*__r*/signal-sheet.json"
    - from: 221-EVIDENCE-LEDGER.md
      to: scripts/phase220-target-path-matrix.sh
      via: operator-invocation-protocol
      pattern: "--cell <cell_id> --replicate <N>"
---

<objective>
Maintain the multi-session evidence ledger across the operator-driven matrix execution. Each Claude session is a SHORT visit: detect new evidence dirs added by the operator since last session, reconcile against ledger rows, commit ledger, exit. The aggregator is NEVER invoked here. Plan 02 advances to Plan 03 readiness when the ledger reports `completed_replicates == 54` OR a CONTEXT D-09 matrix-fail trigger fires.

Purpose: RESEARCH R1 split Phase 221 into execution + reporting; this plan IS the execution wave. CONTEXT D-03 mandates multi-session resume via ledger state. CONTEXT D-01 mandates all-54 with no early termination — by withholding aggregator invocation until completion, Plan 02 enforces Pitfall 2 (data-driven threshold leakage) at the procedural level. RESEARCH R7 specifies the deterministic session-start procedure.

Output: N commits to 221-EVIDENCE-LEDGER.md (one per operator session) updating cell status as evidence accumulates, OR (on matrix-fail) one commit creating 221-MATRIX-INVALID.md halting the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-VALIDATION.md
@.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md
@scripts/phase220-matrix.yaml
@scripts/phase220-target-path-matrix.sh
@docs/PHASE220-MATRIX-RUNNER.md

<interfaces>
<!-- Plan 02 is operator-coordinated. Each session executes Tasks 1 through 3
     against current state. Tasks are sized for one session each. -->

Phase 220 evidence directory layout (READ-ONLY for Plan 02):
  .planning/phases/220-matrix-runner-scope-a1/evidence/{cell_id}__r{N}/
    phase220-cell.json         # canonical sidecar — Phase 214 schema + Phase 220 extensions
    signal-sheet.json           # classifier output: verdict, primary_driver, ranked, latency.*
    mtr-pre-{N}.txt             # per-replicate pre-flight mtr
    mtr-post-{N}.txt            # OPTIONAL — present iff BGP change detected (CONTEXT D-10)
    REHEARSAL-VERDICT.md        # only on rehearsal cell (dallas__spectrum__daytime__r1)

Operator invocation (out-of-session — NOT executed by Plan 02):
  ./scripts/phase220-target-path-matrix.sh --cell <cell_id> --replicate <N>
  # Runs ONE replicate of ONE cell per invocation. Operator schedules these
  # across off-peak / daytime / prime-time windows per CONTEXT D-02.

Ledger reconciliation logic (Plan 02's actual work):
  for cell_id in 18-cell list:
      replicates_on_disk = count(*__r*/signal-sheet.json for cell_id)
      ledger_replicates  = parse ledger row "replicates" column
      if replicates_on_disk != ledger_replicates:
          UPDATE ledger row:
            replicates = f"{replicates_on_disk}/3"
            status = (
              "complete"   if replicates_on_disk == 3 and no D-08 failure,
              "partial"    if 1 <= replicates_on_disk <= 2 and no D-08 failure,
              "incomplete" if D-08 hit (3 failed attempts on same replicate index),
              "bgp-changed" if any mtr-post-*.txt present for the cell
            )
            last_replicate_utc = max(signal-sheet.started_utc across replicates)
            mtr_pre_sha256 = short hash of mtr-pre-{latest}.txt
            mtr_post_flag = (any mtr-post-*.txt exists)
            base_sha = phase220-cell.json["base_sha"] of latest replicate
  bump frontmatter completed_replicates = total replicate count across all cells
  bump frontmatter last_session_utc = now (UTC ISO 8601)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-start procedure — detect new replicates and reconcile against ledger</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md (CURRENT state — last-session snapshot)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R7 (session-start procedure)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-03, D-08, D-09, D-10 (status transitions + matrix-fail triggers + BGP handling)
    - scripts/phase220-matrix.yaml (cells[] — canonical 18-cell source of truth)
    - .planning/phases/220-matrix-runner-scope-a1/evidence/ (READ-ONLY — current evidence root)
  </read_first>
  <action>
    Session-start procedure executed by Claude at the top of EVERY Plan 02 session:

    1. **Count completed replicates on disk:**
       ```bash
       find .planning/phases/220-matrix-runner-scope-a1/evidence \
         -path '*__r*/signal-sheet.json' -type f | wc -l
       ```
       Call this `COMPLETED`. Compare to the ledger frontmatter `completed_replicates` field.

       - `COMPLETED < 54` and ledger value matches: no new evidence since last session. STOP — nothing to update. Exit Plan 02 session.
       - `COMPLETED < 54` and ledger value is stale: new evidence arrived. Proceed to step 2.
       - `COMPLETED == 54`: matrix complete. Update ledger to final state, then signal Plan 03 readiness (the ledger frontmatter `completed_replicates: 54` IS the signal — Plan 03's depends_on check reads this value).

    2. **Reconcile each cell row:** for each of the 18 cells in `scripts/phase220-matrix.yaml`'s cells[]:
       - Glob: `.planning/phases/220-matrix-runner-scope-a1/evidence/{cell_id}__r*/signal-sheet.json`
       - Count matching files → `replicates_on_disk`
       - Detect failure: if any `phase220-cell.json` for this cell has a top-level `failure_reason` or `attempts_exceeded: true` field (per CONTEXT D-08), mark `status: incomplete` AND write the failure reason to the `notes` column.
       - Detect BGP change: if ANY `mtr-post-*.txt` exists for this cell, set `mtr_post_flag: true` and `status: bgp-changed` (NOTE: bgp-changed and complete are not mutually exclusive — a 3/3 cell with mtr_post_flag is `complete` with `mtr_post_flag: true`; per CONTEXT D-10 the cell verdict is still computed, the flag just affects §6 decision-tree exclusion. Refine status assignment: `incomplete` wins over `bgp-changed` wins over `complete/partial/pending` as the row's primary status; `mtr_post_flag` is a separate column).
       - Extract `last_replicate_utc` from the most recent `signal-sheet.json`'s `started_utc` field (or `ended_utc` if present).
       - Extract `mtr_pre_sha256` (first 8 hex of `sha256sum` of the latest replicate's `mtr-pre-N.txt`).
       - Extract `base_sha` from the latest replicate's `phase220-cell.json`'s `base_sha` field.

    3. **Update ledger row** for any cell whose disk state diverges from ledger state. Preserve the `notes` column except when adding failure reasons or BGP footnotes — never overwrite operator-added notes.

    4. **Bump frontmatter fields:**
       - `last_session_utc: <now in UTC ISO 8601>`
       - `completed_replicates: <COMPLETED>`

    5. **Matrix-fail trigger detection (CONTEXT D-09):** after reconciliation, count:
       - `supplemental_incomplete = count of supplemental-target rows with status=incomplete`
       - `canonical_incomplete = count of canonical-target rows with status=incomplete`
       If `supplemental_incomplete > 2 OR canonical_incomplete > 0`: matrix is INVALID. Halt Plan 02 by jumping to Task 3. Otherwise: continue to step 6.

    6. **Commit** the ledger update with message `docs(221): ledger update — <COMPLETED>/54 replicates`. The `tests/test_phase221_mutation_boundary.py` run on commit asserts no controller-path / Phase 213 / Phase 214 / Phase 220 / docs/ diff exists.

    7. **Exit Plan 02 session.** Next operator session (next-day or next-window) re-enters at Task 1.

    **No aggregator invocation.** Plan 02 does not run `scripts/phase220-matrix-aggregator.py`. That's Plan 03's job per CONTEXT D-07 + RESEARCH R4.
  </action>
  <acceptance_criteria>
    - After session-update commit: `grep -E '^completed_replicates: [0-9]+$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` value equals `find .planning/phases/220-matrix-runner-scope-a1/evidence -path '*__r*/signal-sheet.json' -type f | wc -l`
    - Ledger row count remains 18 (cell list unchanged): `awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -cE '^\| [a-z_-]+__[a-z]+__[a-z-]+ \|'` equals 18
    - Status values stay within vocabulary: `awk '/^\| cell_id /,0' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | awk -F'\\|' 'NR>2 {gsub(/ /,"",$11); print $11}' | sort -u` is a subset of `{pending, partial, complete, incomplete, bgp-changed}`
    - `last_session_utc` parses as ISO 8601: `.venv/bin/python -c "import yaml, datetime; from pathlib import Path; c=Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md').read_text().split('---',2)[1]; d=yaml.safe_load(c); datetime.datetime.fromisoformat(d['last_session_utc'].replace('Z','+00:00'))"`
    - No write to anything outside the ledger this session: `git diff --staged --name-only` shows ONLY `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md`
    - SAFE-11 still green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
    - Aggregator NOT invoked: `git log -1 --format=%B` does NOT mention `phase220-matrix-aggregator` AND no `.json` cube output is staged
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && test "$(grep -E '^completed_replicates: [0-9]+$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | awk '{print $2}')" = "$(find .planning/phases/220-matrix-runner-scope-a1/evidence -path '*__r*/signal-sheet.json' -type f | wc -l | tr -d ' ')"</automated>
  </verify>
  <done>Ledger reflects current evidence-on-disk state; SAFE-11 green; aggregator not invoked; matrix-fail trigger not fired.</done>
</task>

<task type="auto">
  <name>Task 2: Operator runbook documentation INSIDE the ledger header (NOT in docs/)</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md (existing scaffold from Plan 01)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-01, D-02, D-03 (operator orchestration + pacing)
    - docs/PHASE220-MATRIX-RUNNER.md (cite by reference — do NOT modify; SAFE-11 forbids docs/ edits)
  </read_first>
  <action>
    Add an operator runbook section to the ledger header (BEFORE the cell-status table) describing the per-session operator + Claude protocol. This sits in `.planning/phases/221-...` — NOT in `docs/` (SAFE-11 forbids docs/ threshold-tuning language and Phase 221 emits no `docs/` diff per RESEARCH R6).

    Insert this section after the existing `## Matrix-Fail Triggers` block and before `## Cell Status`:

    ```markdown
    ## Operator Runbook

    ### Per-Window Operator Session (out-of-Claude-session)

    Operator picks a calendar day matching ONE of the three window-hour gates from Phase 220 YAML:
    - off-peak: hours 01–05 local
    - daytime: hours 10–16 local
    - prime-time: hours 19–22 local

    Within the gate, operator runs ONE OR MORE cells for that window. Each cell-replicate is ONE wrapper invocation:

    ```bash
    ./scripts/phase220-target-path-matrix.sh \
      --cell <cell_id> \
      --replicate <N>
    ```

    Per CONTEXT D-02: multiple cells inside the same window-slot on the same day are permitted; Phase 220 60s inter-cell spacing already covers this.

    Per CONTEXT D-08: on replicate failure, RE-RUN the same `--cell/--replicate` invocation. Up to 3 total attempts per replicate index. After 3 failed attempts on the same replicate, mark the cell `incomplete` in the ledger.

    See `docs/PHASE220-MATRIX-RUNNER.md` for wrapper invocation details (read-only — Phase 221 does not modify).

    ### Per-Claude-Session Ledger Update Protocol

    1. Claude reads `.planning/phases/220-matrix-runner-scope-a1/evidence/` to count `*__r*/signal-sheet.json` files (replicates completed since matrix start, including the Phase 220-04 rehearsal cell).
    2. Claude reconciles each of 18 ledger rows against on-disk evidence per Task 1.
    3. Claude bumps frontmatter `completed_replicates` and `last_session_utc`.
    4. Claude detects matrix-fail trigger (CONTEXT D-09). If triggered, Claude writes `221-MATRIX-INVALID.md` (Task 3) and HALTS. If not triggered, Claude commits the ledger update and exits.
    5. Claude DOES NOT invoke `scripts/phase220-matrix-aggregator.py`. Aggregator runs only in Plan 03 after ledger reports `completed_replicates: 54`.

    ### Calendar Spread Guidance (CONTEXT D-02)

    The 18 cells × 3 replicates spread across at minimum 3 calendar days (one per window-slot) and realistically 1–2 weeks (operator availability + alignment with off-peak hours). The matrix is run-to-completion per CONTEXT D-01 — even if kill OR defect criteria appear satisfied mid-matrix, every replicate completes before Plan 03 fires.

    ### Plan 03 Readiness Signal

    Plan 03 (closeout report) is ready to fire when EITHER:
    - Ledger frontmatter shows `completed_replicates: 54` (success path), OR
    - File `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md` exists (failure path — Plan 03 short-circuits and the phase reopens for replan).

    Plan 03 will refuse to write `221-CLOSEOUT.md` if neither condition is met.
    ```

    This section is documentation-as-data; it lives in the same file as the ledger so operator/Claude sessions only need to load ONE file to understand the full protocol.
  </action>
  <acceptance_criteria>
    - Operator Runbook section exists: `grep -c '^## Operator Runbook$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 1
    - Wrapper invocation example present: `grep -c 'phase220-target-path-matrix.sh' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 1
    - Three window-hour gates listed: `grep -cE '(off-peak|daytime|prime-time): hours' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` equals 3
    - Plan 03 readiness signals documented: `grep -c 'completed_replicates: 54' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md` ≥ 2 (one in runbook, one in frontmatter target)
    - docs/ unchanged: `git diff --name-only docs/` is empty AND `git diff --staged --name-only docs/` is empty
    - SAFE-11 still green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q && grep -c '^## Operator Runbook$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md | grep -q '^1$' && test -z "$(git diff --name-only docs/)" && test -z "$(git diff --staged --name-only docs/)"</automated>
  </verify>
  <done>Operator runbook inline in ledger; docs/ untouched; SAFE-11 green.</done>
</task>

<task type="auto">
  <name>Task 3: [CONDITIONAL] Matrix-fail closure — write 221-MATRIX-INVALID.md if D-09 trigger fires</name>
  <files>.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md</files>
  <read_first>
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-EVIDENCE-LEDGER.md (post-reconciliation state; status column counts)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-CONTEXT.md D-09 (matrix-fail criteria — `>2 supplemental cells INCOMPLETE` OR `any canonical control cell INCOMPLETE`)
    - .planning/phases/221-matrix-evidence-closeout-scope-a2/221-RESEARCH.md R7 (matrix-fail handling flow)
  </read_first>
  <action>
    EXECUTE THIS TASK ONLY IF Task 1 step 5 detected a matrix-fail trigger (`supplemental_incomplete > 2 OR canonical_incomplete > 0`).

    If trigger did NOT fire: skip this task. Plan 02 continues across N more sessions until `completed_replicates == 54` or trigger fires.

    If trigger DID fire:

    1. Create `.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md` with this content:

       ```markdown
       ---
       phase: 221
       slug: matrix-evidence-closeout-scope-a2
       matrix_status: invalid
       trigger_utc: <now ISO 8601>
       matrix_fail_reason: <one of: too_many_supplemental_incomplete | canonical_incomplete>
       supplemental_incomplete_count: <N>
       canonical_incomplete_count: <N>
       completed_replicates_at_fail: <COMPLETED from Task 1>
       phase220_base_sha: <base_sha from any cell's phase220-cell.json>
       ---

       # Phase 221 — Matrix INVALID

       The matrix execution hit a CONTEXT D-09 matrix-fail trigger and is INVALID. Phase 221 reopens for replan; **no 221-CLOSEOUT.md is written**, no aggregator is invoked, no folded-todo move occurs.

       ## Trigger Detail

       Per CONTEXT D-09:
       - Matrix is INVALID and Phase 221 reopens for re-plan if: `>2 supplemental cells INCOMPLETE` OR `any canonical control cell INCOMPLETE in any window`.
       - Canonical is foundational — without it, MWU and 1.5× control-comparison are undefined.

       Observed at trigger time:
       - Supplemental cells incomplete: <count> — <list of cell_ids>
       - Canonical cells incomplete: <count> — <list of cell_ids>

       ## Failure Reasons Per Incomplete Cell

       <For each incomplete cell, copy the failure_reason from its phase220-cell.json or the ledger row's notes column.>

       ## Recovery

       Phase 221 reopens for replan. Recovery actions are out-of-scope for this document; recommended next steps:
       1. Diagnose why D-08 retries exhausted (network outage, source-bind miss, RouterOS state corruption, etc).
       2. Decide whether to re-pin `base_sha` per CONTEXT D-12 (only if controller-path hotfix is in scope, which v1.47 forbids).
       3. Re-run the failed replicates and update the ledger; matrix-fail status persists in this file until Phase 221 is replanned.

       ## SAFE-11 Status

       SAFE-11 mutation-boundary pytest must still pass green at the matrix-fail commit. No controller-path / Phase 213 / Phase 214 / Phase 220 / docs/ diff is permitted.
       ```

    2. Commit with message `docs(221): matrix INVALID — D-09 trigger fired (<matrix_fail_reason>)`.

    3. Plan 02 session HALTS. Subsequent operator-driven cell runs may continue, but Plan 02 does NOT update the ledger or remove this file. Phase 221 is in matrix-fail state; operator must replan.

    Plan 03 reads this file's existence at startup and refuses to write CLOSEOUT.md if present (per Task 2 readiness-signal contract).
  </action>
  <acceptance_criteria>
    - File exists ONLY IF trigger fired: `test ! -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md || ( grep -E '^matrix_status: invalid$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md && grep -E '^matrix_fail_reason: (too_many_supplemental_incomplete|canonical_incomplete)$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md )`
    - Frontmatter parses: `.venv/bin/python -c "import yaml, sys; from pathlib import Path; p=Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md'); sys.exit(0) if not p.exists() else (yaml.safe_load(p.read_text().split('---',2)[1]), None)"`
    - If file exists: cited Phase 220 base SHA is 40-char hex: `! test -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md || .venv/bin/python -c "import yaml,re; from pathlib import Path; d=yaml.safe_load(Path('.planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md').read_text().split('---',2)[1]); assert re.match(r'^[0-9a-f]{40}$', d['phase220_base_sha'])"`
    - SAFE-11 green: `.venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q` exits 0
    - Ledger frontmatter NOT changed in this commit (matrix-fail and ledger-update are separate concerns): `git log -1 --name-only --format=` does NOT list 221-EVIDENCE-LEDGER.md when 221-MATRIX-INVALID.md is the commit
  </acceptance_criteria>
  <verify>
    <automated>test ! -f .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md || ( grep -qE '^matrix_status: invalid$' .planning/phases/221-matrix-evidence-closeout-scope-a2/221-MATRIX-INVALID.md && .venv/bin/pytest tests/test_phase221_mutation_boundary.py -x -q )</automated>
  </verify>
  <done>If trigger fired, MATRIX-INVALID file exists and Phase 221 is in replan posture; if not fired, file does not exist and Plan 02 continues across more sessions.</done>
</task>

</tasks>

<verification_criteria>
- 221-EVIDENCE-LEDGER.md frontmatter `completed_replicates` matches the on-disk `signal-sheet.json` count after every session-update commit
- Cell row statuses stay within {pending, partial, complete, incomplete, bgp-changed}
- No aggregator invocation; no controller-path diff; no docs/ diff
- Plan 03 readiness signal: either `completed_replicates: 54` OR `221-MATRIX-INVALID.md` exists
- SAFE-11 pytest passes green after every commit in this plan
</verification_criteria>

<summary_template>
@$HOME/.claude/get-shit-done/templates/summary.md
</summary_template>
