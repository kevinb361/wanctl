# Phase 262, Plan 02 — Deploy + Rollback Drill

**Goal:** Deploy abort scaffolding to cake-shaper, execute staged flip→revert rollback drill on the canary route, and prove the revert restores pre-flip ownership state.

**Requirements:** ABORT-01 (rollback drill exercised and proven before live flip)

**SAFE-22:** May deploy code changes to /opt/wanctl and touch one canary route. Must NOT modify CAKE/qdisc, controller thresholds, Netwatch deletion, or any route beyond the canary.

**Preconditions:** Plan 01 code is committed and passing tests. Phase 261 rollback anchor exists.

## Task 1: Deploy abort scaffolding

**What:** Run `deploy.sh spectrum --with-steering --with-spectrum-cake-autorate` to cake-shaper, then `deploy.sh att --with-att-cake-autorate`. Preserve steering.yaml (restore dry_run mode after deploy).

**Steps:**
1. Pre-deploy SAFE-22 check (verify only steering/ and scripts/ changed at HEAD)
2. Run deploy.sh spectrum to cake-shaper
3. Run deploy.sh att to cake-shaper
4. Restore steering.yaml from backup (preserve dry_run mode)
5. Verify no service restart (ActiveEnterTimestampMonotonic unchanged)

**Verification:**
- Deploy exits 0
- steering.yaml sha matches pre-deploy backup
- Monotonic timestamps unchanged
- :9102 healthy, mode=dry_run

## Task 2: Staged rollback drill

**What:** Execute the flip→revert drill on the canary route:
1. Flip: enable wanctl ownership for the canary route
2. Verify: confirm :9102 shows wanctl as active owner
3. Revert: trigger manual rollback (mode → dry_run + steering restart)
4. Verify: confirm :9102 shows Netwatch as active owner
5. Record evidence

**Steps:**
1. Change steering.yaml mode from "dry_run" to "active"
2. Restart steering.service
3. Wait for cycle completion (~500ms)
4. Capture :9102 health — verify active_owner=wanctl, mode=active
5. Trigger manual rollback (change mode to "dry_run", restart steering)
6. Wait for cycle completion
7. Capture :9102 health — verify active_owner=netwatch, mode=dry_run
8. Record monotonic timestamps (steering should advance exactly twice)
9. Compare pre-drill and post-drill ownership state

**Verification:**
- Drill PASS: pre-flip ownership = post-revert ownership (Netwatch active)
- :9102 shows clean transitions: netwatch → wanctl → netwatch
- Shaper units not restarted (monotonic timestamps unchanged)
- No route flap evidence (no dual-ownership period)

**Checkpoint: human-action** — the drill touches production routes. Operator must approve.

## Task 3: Evidence + commit

**What:** Record drill evidence and commit.

**Steps:**
1. Write drill evidence with :9102 snapshots (pre-flip, post-flip, post-revert)
2. Record monotonic timestamp comparison
3. Write PLAN 02 summary
4. Commit evidence

**Verification:**
- Evidence file contains all required snapshots
- PHASE262_ROLLBACK_DRILL_PASS verdict recorded
- ABORT-01 satisfied
