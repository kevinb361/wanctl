---
phase: 240-config-validator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/check_config_validators.py
  - src/wanctl/check_steering_validators.py
  - tests/test_check_config.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
must_haves:
  truths:
    - "Operator can set measurement.backend: icmplib or fping in a WAN config and it validates without an unknown-key warning"
    - "An absent measurement.backend key produces no validator result at all (resolves to icmplib downstream)"
    - "An unknown backend value (including irtt) produces a Severity.ERROR"
    - "backend: fping with the fping binary absent produces a non-gating Severity.WARN, not an ERROR"
    - "All three committed real configs (att/spectrum/steering) gain zero NEW Schema/Unknown-Keys/Measurement-Backend results when the key is added vs absent"
  artifacts:
    - path: "src/wanctl/check_config_validators.py"
      provides: "MEASUREMENT_BACKENDS constant, validate_measurement_backend helper, measurement.backend registered in KNOWN_AUTORATE_PATHS, dispatcher wiring"
      contains: "MEASUREMENT_BACKENDS"
    - path: "src/wanctl/check_steering_validators.py"
      provides: "measurement.backend registered in KNOWN_STEERING_PATHS, shared helper wired into steering dispatcher"
      contains: "measurement.backend"
    - path: "tests/test_check_config.py"
      provides: "TestMeasurementBackendValidation unit vectors + CFG-03 delta regression"
      contains: "validate_measurement_backend"
  key_links:
    - from: "src/wanctl/check_steering_validators.py::_run_steering_validators"
      to: "src/wanctl/check_config_validators.py::validate_measurement_backend"
      via: "function-local import (cross-import dodge)"
      pattern: "from wanctl.check_config_validators import.*validate_measurement_backend"
    - from: "src/wanctl/check_config_validators.py::validate_measurement_backend"
      to: "measurement.backend"
      via: "_get_nested read"
      pattern: "_get_nested\\(data, \"measurement.backend\""
---

<objective>
Add the inert `measurement.backend: icmplib|fping` enum to wanctl's two offline config
validators (autorate + steering) per D-01, with zero migration. One shared validator helper
emits ERROR on unknown values (incl. `irtt`), a non-gating WARN when `fping` is selected but the
binary is absent, and NOTHING when the key is absent. Register the path in both allow-lists so a
present-valid key does not trip the unknown-key warning. Prove CFG-01/02/03 with hermetic unit
vectors plus a real-corpus delta regression.

Purpose: This is the config→string surface (CFG-01/02/03) that Phase 242 will later consume at the
construction sites. The key is additive and inert this phase — `absent → icmplib` means zero
behavior change in either consumer.

Output: Modified `check_config_validators.py`, `check_steering_validators.py`, and
`tests/test_check_config.py`. No new module, no edit to `check_config.py` or `autorate_config.py`
(keeps the SAFE-17 allowlist minimal — D-02: key is validated, not loaded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-config-validator/240-CONTEXT.md
@.planning/phases/240-config-validator/240-RESEARCH.md
@.planning/phases/240-config-validator/240-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH/PATTERNS file:line anchors. -->
<!-- Use these directly. Read the analog files named in <read_first> before editing. -->

From src/wanctl/check_config.py:30-46 (DO NOT EDIT this file — reuse these):
- `class Severity(Enum)`: PASS, WARN, ERROR
- `@dataclass class CheckResult`: (category: str, field: str, severity: Severity, message: str, suggestion: str | None = None)

From src/wanctl/config_base.py:41 (reuse — DO NOT EDIT):
- `_get_nested(data, "measurement.backend")` -> value or None; handles non-dict intermediates safely.

Existing analog (the tc-binary probe to clone for the fping WARN):
- src/wanctl/check_steering_validators.py:481-570 `validate_linux_cake`; the shutil.which("tc") → non-gating WARN block is lines 547-568.

Existing analog (the optional-block registry pattern):
- src/wanctl/check_config_validators.py:173-182 `irtt` registration block (registered in KNOWN_AUTORATE_PATHS but NOT in Config.SCHEMA).

Dispatchers to wire into:
- src/wanctl/check_config_validators.py:954-968 `_run_autorate_validators(data)`
- src/wanctl/check_steering_validators.py:578-592 `_run_steering_validators(data)`

Registries:
- src/wanctl/check_config_validators.py:30-273 `KNOWN_AUTORATE_PATHS` (NO `measurement` block today — add BOTH `measurement` and `measurement.backend`)
- src/wanctl/check_steering_validators.py:25-156 `KNOWN_STEERING_PATHS` (`measurement` ALREADY at :46-49 — add ONLY `measurement.backend`)

Test analog:
- tests/test_check_config.py:1157-1206 `TestLinuxCakeValidation` (mirror this class structure; `unittest.mock.patch` already imported at :18; yaml available).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared enum constant + validate_measurement_backend helper + autorate wiring</name>
  <files>src/wanctl/check_config_validators.py</files>
  <read_first>
    - src/wanctl/check_config_validators.py (the file being edited — read the irtt registration block :173-182, KNOWN_AUTORATE_PATHS, and _run_autorate_validators :954-968)
    - src/wanctl/check_steering_validators.py:481-570 (validate_linux_cake — the tc-probe analog to clone, esp. :547-568)
    - src/wanctl/check_config.py:30-46 (Severity + CheckResult — reuse, do not redefine)
    - src/wanctl/config_base.py:41-58 (_get_nested)
    - .planning/phases/240-config-validator/240-PATTERNS.md (Edits 1-4 for this file)
  </read_first>
  <behavior>
    - validate_measurement_backend({}) returns [] (absent key → no result; CFG-01)
    - validate_measurement_backend({"measurement": {"backend": "icmplib"}}) returns exactly one PASS, zero ERROR/WARN (CFG-01)
    - validate_measurement_backend({"measurement": {"backend": "fping"}}) with fping present returns one PASS, zero WARN
    - validate_measurement_backend({"measurement": {"backend": "irtt"}}) returns a Severity.ERROR (CFG-02a — irtt is NOT selectable)
    - validate_measurement_backend({"measurement": {"backend": "bogus"}}) returns a Severity.ERROR (CFG-02a)
    - validate_measurement_backend({"measurement": {"backend": "fping"}}) with shutil.which patched to None returns a Severity.WARN and zero Severity.ERROR (CFG-02b non-gating)
  </behavior>
  <action>
    Three in-file edits, no new module:
    (1) Add module-level constant `MEASUREMENT_BACKENDS: tuple[str, ...] = ("icmplib", "fping")` — `irtt` intentionally excluded (IRTT-MIG-01).
    (2) Add `validate_measurement_backend(data: dict) -> list[CheckResult]`: read `_get_nested(data, "measurement.backend")`; if None return `[]` (NO result emitted — Pitfall 1); if value not in MEASUREMENT_BACKENDS return a single `CheckResult` with category "Measurement Backend", field "measurement.backend", `Severity.ERROR`; otherwise emit a `Severity.PASS` result, and IF value == "fping" AND `shutil.which("fping") is None` append a `Severity.WARN` whose message states the validator host may differ from the deploy host (advisory only). Use `shutil.which` for the name lookup — NEVER execute or shell-out fping. Import `shutil` at module scope if not already imported.
    (3) Register the new paths in `KNOWN_AUTORATE_PATHS` near the continuous_monitoring block (autorate has NO measurement block today, so add BOTH lines): `"measurement"` and `"measurement.backend"`. Copy the irtt registration style at :173-182.
    (4) Wire `results.extend(validate_measurement_backend(data))` into `_run_autorate_validators` (:954-968), alongside the existing `validate_linux_cake` call.
    Do NOT add a SCHEMA `choices=` entry (Pitfall 1 — perturbs the CFG-03 baseline with a PASS line for absent keys). Do NOT touch check_config.py or autorate_config.py.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from wanctl.check_config_validators import MEASUREMENT_BACKENDS, validate_measurement_backend; assert MEASUREMENT_BACKENDS == ('icmplib','fping'); assert validate_measurement_backend({}) == []; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `check_config_validators.py` contains `MEASUREMENT_BACKENDS: tuple[str, ...] = ("icmplib", "fping")`
    - `grep -n '"measurement",' src/wanctl/check_config_validators.py` AND `grep -n '"measurement.backend",' src/wanctl/check_config_validators.py` both match inside KNOWN_AUTORATE_PATHS
    - `_run_autorate_validators` contains `validate_measurement_backend(data)`
    - The fping probe uses `shutil.which("fping")` — `grep -c 'subprocess\|os.system\|Popen' ` against the new helper region returns 0 (name lookup only, no shell-out)
    - `irtt` hits the ERROR branch (not allow-listed)
    - `.venv/bin/python -c "..."` smoke command above exits 0
  </acceptance_criteria>
  <done>MEASUREMENT_BACKENDS constant exists, validate_measurement_backend implements all four branches (absent→[], unknown/irtt→ERROR, valid→PASS, fping+absent→WARN), both paths registered in KNOWN_AUTORATE_PATHS, helper wired into autorate dispatcher.</done>
</task>

<task type="auto">
  <name>Task 2: Register measurement.backend for steering + wire shared helper into steering dispatcher</name>
  <files>src/wanctl/check_steering_validators.py</files>
  <read_first>
    - src/wanctl/check_steering_validators.py (the file being edited — KNOWN_STEERING_PATHS :25-156 with the existing `measurement` block at :46-49, and _run_steering_validators :578-592)
    - .planning/phases/240-config-validator/240-PATTERNS.md (the steering section — Pitfall 2 asymmetry)
    - .planning/phases/240-config-validator/240-RESEARCH.md (Pitfall 2)
  </read_first>
  <action>
    Two edits:
    (1) In `KNOWN_STEERING_PATHS`, add ONLY `"measurement.backend"`. Steering ALREADY registers `measurement`, `measurement.interval_seconds`, `measurement.ping_host`, `measurement.ping_count` at :46-49 — do NOT re-add `measurement` and do NOT drop the existing required sub-keys (Pitfall 2).
    (2) In `_run_steering_validators` (:578-592), add `from wanctl.check_config_validators import validate_measurement_backend` using the existing function-local cross-import dodge (this function already locally imports check_env_vars/check_paths from check_config_validators), then `results.extend(validate_measurement_backend(data))`.
    Do NOT define a second copy of the helper — import the shared one from check_config_validators (single source of truth, D-disc).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from wanctl.check_steering_validators import KNOWN_STEERING_PATHS; assert 'measurement.backend' in KNOWN_STEERING_PATHS; assert 'measurement' in KNOWN_STEERING_PATHS; assert 'measurement.interval_seconds' in KNOWN_STEERING_PATHS; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `KNOWN_STEERING_PATHS` contains `measurement.backend` AND still contains `measurement`, `measurement.interval_seconds`, `measurement.ping_host`, `measurement.ping_count` (none dropped)
    - `_run_steering_validators` contains a function-local `from wanctl.check_config_validators import validate_measurement_backend` and a `validate_measurement_backend(data)` call
    - No second definition of `validate_measurement_backend` in check_steering_validators.py (`grep -c 'def validate_measurement_backend' src/wanctl/check_steering_validators.py` == 0)
    - Smoke command above exits 0
  </acceptance_criteria>
  <done>measurement.backend registered in KNOWN_STEERING_PATHS (existing sub-keys preserved), shared helper imported and called from _run_steering_validators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit vectors (CFG-01/02) + real-corpus delta regression (CFG-03)</name>
  <files>tests/test_check_config.py</files>
  <read_first>
    - tests/test_check_config.py (the file being edited — TestLinuxCakeValidation :1157-1206 as the class structure analog; imports :18-35)
    - .planning/phases/240-config-validator/240-PATTERNS.md (the test section — vectors + CFG-03 delta recipe)
    - .planning/phases/240-config-validator/240-RESEARCH.md (Pitfall 3 delta, Pitfall 4 enum-identity)
    - configs/att.yaml, configs/spectrum.yaml, configs/steering.yaml (the read-only regression corpus)
  </read_first>
  <behavior>
    - test absent: validate_measurement_backend({}) == []
    - test valid icmplib/fping: zero ERROR, zero WARN (fping case patches shutil.which to a truthy path)
    - test unknown + irtt: each yields exactly one Severity.ERROR
    - test fping+absent: patch("wanctl.check_config_validators.shutil.which", return_value=None) → exactly one Severity.WARN, zero Severity.ERROR (non-gating)
    - test CFG-03 delta (per corpus file): _run_autorate_validators / _run_steering_validators with key absent vs key="icmplib" added yields ZERO new ERROR/WARN in categories Schema Validation, Unknown Keys, Measurement Backend
  </behavior>
  <action>
    Add a `TestMeasurementBackendValidation` class mirroring `TestLinuxCakeValidation` (:1157-1206). Extend the existing `from wanctl.check_config_validators import (...)` block with `validate_measurement_backend` and `MEASUREMENT_BACKENDS`. Cover the four unit vectors above; monkeypatch `shutil.which` via `unittest.mock.patch("wanctl.check_config_validators.shutil.which", ...)` (already imported :18). For `irtt`, assert it produces a Severity.ERROR (it is NOT selectable).
    Add a CFG-03 delta regression: for each of `configs/{att,spectrum,steering}.yaml`, `yaml.safe_load` the file, run the correct dispatcher (`_run_autorate_validators` for att/spectrum, `_run_steering_validators` for steering) twice — once with the key absent (as-committed) and once with `measurement.backend` set to a valid value injected into the loaded dict — and assert no NEW result appears in the {Schema Validation, Unknown Keys, Measurement Backend} categories beyond the absent-key run (excluding the expected single Measurement-Backend PASS for the present-valid case). Do NOT assert exit 0 / clean exit (Pitfall 3 — corpus fails on environmental path/env checks on the dev host). Call dispatcher functions directly; do NOT invoke `python -m wanctl.check_config` (Pitfall 4 — enum-identity KeyError).
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_check_config.py -k "MeasurementBackend or cfg03 or measurement_backend" -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_check_config.py` contains `class TestMeasurementBackendValidation`
    - Tests cover all four CFG-01/02 vectors AND the CFG-03 corpus delta over att/spectrum/steering
    - The fping-absent test patches `wanctl.check_config_validators.shutil.which` and asserts exactly one WARN, zero ERROR
    - The `irtt` test asserts a Severity.ERROR
    - No test invokes `python -m wanctl.check_config`
    - `.venv/bin/pytest -o addopts='' tests/test_check_config.py -k "MeasurementBackend or cfg03 or measurement_backend" -q` exits 0
    - Full validator slice green: `.venv/bin/pytest -o addopts='' tests/test_check_config.py -q` exits 0
  </acceptance_criteria>
  <done>TestMeasurementBackendValidation covers the 3 decision vectors (absent/unknown+irtt/fping-absent-WARN) plus valid PASS; CFG-03 delta regression over all three real configs asserts zero new Schema/Unknown-Keys/Measurement-Backend results; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator YAML → validator | Operator-controlled config file content (`measurement.backend` value) crosses into the offline validator. The value is untrusted text. |
| validator host PATH → shutil.which | The `fping` binary-name lookup reads the host PATH; no binary is executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-01 | Tampering / DoS | `validate_measurement_backend` parsing of `measurement.backend` | mitigate | Read via `_get_nested` (handles non-dict intermediates safely); any non-matching/malformed value returns a Severity.ERROR CheckResult — never raises an unhandled exception. Unit vectors include `irtt` and `bogus`. |
| T-240-02 | Elevation / Injection | `shutil.which("fping")` probe | mitigate | Name lookup only via stdlib `shutil.which`; NO subprocess/os.system/Popen, no shell, no execution of the binary — zero command-injection surface. Acceptance criterion greps for subprocess/Popen/os.system == 0 in the helper. |
| T-240-03 | Information Disclosure | validator output messages | accept | Messages contain only the operator-supplied backend string and a fixed advisory; no secrets, IPs, or env values surfaced. LOW. |
| T-240-SC | Tampering | npm/pip/cargo installs | n/a | Zero new dependencies — pure stdlib (`shutil`) + existing in-repo primitives. No package-legitimacy gate applies (RESEARCH §Standard Stack). |

**Overall surface: LOW.** Operator-controlled YAML parsing in an offline validator, no network calls, no new secret handling, no binary execution. No HIGH-severity threats.
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_check_config.py -q` exits 0
- `.venv/bin/ruff check src/wanctl/check_config_validators.py src/wanctl/check_steering_validators.py tests/test_check_config.py` clean
- `.venv/bin/mypy src/wanctl/check_config_validators.py src/wanctl/check_steering_validators.py` clean
- `wanctl-check-config configs/att.yaml` (console script — NOT `python -m`) shows no NEW Schema/Unknown-Keys/Measurement-Backend warning vs the committed baseline (environmental path/env errors are pre-existing and expected)
</verification>

<success_criteria>
- CFG-01: operator can set `measurement.backend: icmplib|fping`; absent key resolves to icmplib (no validator result); valid key registered → no unknown-key warning.
- CFG-02: unknown value (incl. `irtt`) → Severity.ERROR; `fping` + absent binary → non-gating Severity.WARN (never ERROR).
- CFG-03: all three real configs gain zero new Schema/Unknown-Keys/Measurement-Backend results when the key is present vs absent.
- Both validators wired via a single shared helper (one source of truth); `check_config.py` and `autorate_config.py` untouched.
</success_criteria>

<output>
Create `.planning/phases/240-config-validator/240-01-SUMMARY.md` when done.
</output>
