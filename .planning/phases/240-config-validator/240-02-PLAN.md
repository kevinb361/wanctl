---
phase: 240-config-validator
plan: 02
type: execute
wave: 2
depends_on: [240-01]
files_modified:
  - scripts/phase240-safe17-boundary-check.sh
  - tests/test_phase240_safe17_verifier.py
autonomous: true
requirements: [SAFE-17]
must_haves:
  truths:
    - "A new Phase 240 boundary script exists and is NOT an edit of the Phase 239 script — the SAFE-17 boundary-verifier proof layer per D-04"
    - "The 240 allowlist permits exactly the 239 seam files plus the two validator files, and rejects any other src/wanctl/ path"
    - "No new diff exists in rtt_backend.py / rtt_measurement.py relative to the Phase 239 close anchor (proves Phase 240 added no RTT-seam drift)"
    - "Running the script against a committed clean tree with only allowlisted edits exits 0 and writes evidence JSON"
    - "The script fail-closes (exit 1) on a dirty/staged/untracked src/wanctl/ tree"
    - "The 239 protected-body AST layer still passes (240 does not touch rtt_backend.py / rtt_measurement.py qualnames)"
  artifacts:
    - path: "scripts/phase240-safe17-boundary-check.sh"
      provides: "Phase 240 fail-closed SAFE-17 boundary verifier with expanded allowlist + RTT-seam no-drift check vs Phase 239 close anchor"
      contains: "check_config_validators"
    - path: "tests/test_phase240_safe17_verifier.py"
      provides: "regression test mirroring tests/test_phase239_safe17_verifier.py (incl. negative coverage)"
      contains: "phase240-safe17-boundary-check"
  key_links:
    - from: "scripts/phase240-safe17-boundary-check.sh"
      to: "scripts/phase239-protected-body-diff.py"
      via: "reused AS-IS (240 does not touch protected qualnames)"
      pattern: "phase239-protected-body-diff.py"
    - from: "scripts/phase240-safe17-boundary-check.sh"
      to: "v1.52"
      via: "git diff anchor (union allowlist breadth check)"
      pattern: "ANCHOR=\"v1.52\""
    - from: "scripts/phase240-safe17-boundary-check.sh"
      to: "PHASE239_CLOSE_ANCHOR"
      via: "second diff check proving zero new RTT-seam diff (rtt_backend.py/rtt_measurement.py) since Phase 239 close"
      pattern: "PHASE239_CLOSE_ANCHOR"
---

<objective>
Author `scripts/phase240-safe17-boundary-check.sh` — a fail-closed SAFE-17 boundary verifier for
Phase 240. The Phase 239 script's allowlist regex (`^src/wanctl/(rtt_backend|rtt_measurement)\.py$`)
rejects 240's validator edits, so 240 needs its OWN script with an expanded allowlist (union of the
239 seam files + the two validator files). Clone the 239 structure verbatim; reuse
`phase239-protected-body-diff.py` AS-IS (240 does not touch the protected controller-path qualnames).
The v1.52 union allowlist would, however, PERMIT a phase-local edit to `rtt_backend.py` /
`rtt_measurement.py` and the protected-body helper does NOT cover `rtt_backend.py` at all — a
false-pass hole. Close it with a SECOND diff check that proves Phase 240 introduced ZERO new diff in
the RTT seam files relative to the Phase 239 close. Add a regression test mirroring the 239 verifier
test, including negative coverage.

Purpose: Prove SAFE-17 at the 240 phase boundary — additive config/validator surface only, no
controller-path drift (state machine, thresholds, EWMA, dwell, deadband, arbitration, fusion) AND
no Phase-240 drift into the RTT seam the milestone is supposed to leave untouched this phase.

Output: `scripts/phase240-safe17-boundary-check.sh`, `tests/test_phase240_safe17_verifier.py`, and
SAFE-17 evidence JSON under the 240 evidence dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/240-config-validator/240-RESEARCH.md
@.planning/phases/240-config-validator/240-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The 239 script is the exact clone source. The load-bearing changes are the allowlist AND the
     second Phase-239-close anchor RTT-seam no-drift check. -->

Clone source: scripts/phase239-safe17-boundary-check.sh
Reused AS-IS (do NOT modify or re-clone): scripts/phase239-protected-body-diff.py — its PROTECTED
dict (scripts/phase239-protected-body-diff.py:21-32) covers ONLY qualnames in `rtt_measurement.py`
and `wan_controller.py`. It does NOT protect `rtt_backend.py` at all. That is exactly why the v1.52
union allowlist alone is insufficient and the second anchor check (below) is required.
Test analog: tests/test_phase239_safe17_verifier.py (worktree-based fixture: `detached_worktree`,
`commit_worktree_change`; negative tests at :72-128).

239 allowlist (TOO NARROW for 240) — scripts/phase239-safe17-boundary-check.sh:15:
  V153_ALLOWLIST_RE='^src/wanctl/(rtt_backend\.py|rtt_measurement\.py)$'

240 allowlist (union with 239 seam files, anchor stays v1.52 so 239's committed edits remain in-allowlist):
  V153_ALLOWLIST_RE='^src/wanctl/(rtt_backend\.py|rtt_measurement\.py|check_config_validators\.py|check_steering_validators\.py)$'

Phase 239 close anchor (the second-check baseline): there is NO `v1.53.0-phase239` style tag in this
repo (only `v1.52` exists). The Phase 239 close commit is the last commit of Phase 239 —
candidate SHA `03c82de0` ("docs(phase-239): evolve PROJECT.md after phase completion"). The executor
MUST confirm/pin the actual Phase-239-close commit SHA at implementation time (e.g.
`git log --oneline | grep -iE 'phase.?239'` for the final phase-239 SUMMARY/PROJECT-evolve commit)
and hard-code it as `PHASE239_CLOSE_ANCHOR` (rebases can change the SHA, so pin a concrete value, not
a moving ref). This anchor is used ONLY for the RTT-seam no-drift check, NOT for the union-allowlist
breadth check (which keeps `ANCHOR="v1.52"`).

Note: check_config.py and autorate_config.py are NOT in the allowlist — Plan 01 deliberately does not
touch them. If a stray edit to either appears, the script SHOULD reject it (that is correct behavior).

Other mechanical changes when cloning (independent of the regex):
- OUT default → .planning/phases/240-config-validator/evidence/safe17-boundary-240.json
- ALLOWED_OUT_PREFIX → .planning/phases/240-config-validator/evidence/
- embedded-Python disallowed-paths set (239 script :89-93, currently {rtt_backend.py, rtt_measurement.py}) → the 240 four-file allowed set
- error message (239 script :228, "Allowed Phase 239 src/wanctl files: ...") → 240 file set
- notes string (239 script :143) → "Phase 240 boundary check"
Preserve verbatim: dirty/staged/untracked fail-closed guard (:199-221), --out path-traversal guards
(:165-180), the protected-body AST invocation (:241-246), and the allowed-shape layer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the 239 boundary script into a Phase 240 verifier with expanded allowlist + RTT-seam no-drift check</name>
  <files>scripts/phase240-safe17-boundary-check.sh</files>
  <read_first>
    - scripts/phase239-safe17-boundary-check.sh (the full clone source — read end to end; note the embedded-Python diff at :88-93 and the shell allowlist gate at :223-232)
    - scripts/phase239-protected-body-diff.py:21-32 (the PROTECTED dict — confirm it does NOT cover rtt_backend.py; this is the gap the second check closes)
    - .planning/phases/240-config-validator/240-PATTERNS.md (the boundary-script section — mechanical change list)
    - .planning/phases/240-config-validator/240-RESEARCH.md (§SAFE-17 Boundary, Pitfall 5, Open Q2 union-at-v1.52)
  </read_first>
  <action>
    PRECONDITION (review MEDIUM): this script diffs committed src/wanctl/ vs v1.52 and fail-closes on a dirty tree, so it MUST be run only AFTER Plan 01's edits are committed and the `src/wanctl` tree is clean (no staged/unstaged/untracked changes). State this precondition in the script header comment and run order.
    Create `scripts/phase240-safe17-boundary-check.sh` as a clone of `scripts/phase239-safe17-boundary-check.sh`. Keep `ANCHOR="v1.52"` for the union-allowlist breadth check (union-at-v1.52 is rebase-robust and keeps 239's committed edits in-allowlist). Apply exactly these changes:
    (1) `V153_ALLOWLIST_RE='^src/wanctl/(rtt_backend\.py|rtt_measurement\.py|check_config_validators\.py|check_steering_validators\.py)$'`.
    (2) `OUT` default → `.planning/phases/240-config-validator/evidence/safe17-boundary-240.json`; `ALLOWED_OUT_PREFIX` → `.planning/phases/240-config-validator/evidence/`.
    (3) The embedded-Python `disallowed_paths` set (239 :89-93, `{"src/wanctl/rtt_backend.py", "src/wanctl/rtt_measurement.py"}`) → the four-file 240 set (`rtt_backend.py`, `rtt_measurement.py`, `check_config_validators.py`, `check_steering_validators.py`).
    (4) Error message (239 :228) and `notes` string (239 :143) → Phase 240 wording.
    (5) Reuse `phase239-protected-body-diff.py` AS-IS via `$SCRIPT_DIR/phase239-protected-body-diff.py` (240 does NOT touch the protected qualnames; do not clone or edit this helper).
    (6) SECOND ANCHOR / RTT-SEAM NO-DRIFT CHECK (closes the false-pass hole): add a `PHASE239_CLOSE_ANCHOR` variable pinned to the confirmed Phase-239-close commit SHA (see interfaces — confirm via `git log` and hard-code, candidate `03c82de0`). After the v1.52 allowlist gate passes, add a gate that runs `git diff --name-only "${PHASE239_CLOSE_ANCHOR}" HEAD -- src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py` and requires the result to be EMPTY. If non-empty → fail-closed (exit 1) with a clear "SAFE-17 VIOLATION: Phase 240 introduced RTT-seam drift since Phase 239 close" message, list the offending file(s), and `emit_evidence false` before exiting. Mirror the existing allowlist-gate idiom (capture into a var, test for non-empty, print, emit_evidence, exit 1). This proves Phase 240 added NO diff to the RTT seam on top of the Phase 239 close — closing the hole that the union allowlist + rtt_backend-blind protected-body helper would otherwise leave open.
    (7) Evidence JSON auditability: add a `rtt_seam_unchanged_since_phase239` boolean (true when the seam diff is empty) and a `phase239_close_anchor` field (the pinned SHA) to the emitted record in `emit_evidence` so the result is auditable. Pass these through the existing python3 here-doc the same way `ANCHOR_SHA`/`passed` are passed.
    Preserve verbatim: dirty/staged/untracked fail-closed guard, `--out` path-traversal guards, the protected-body AST invocation, and the allowed-shape layer. `chmod +x` the new script.
  </action>
  <verify>
    <automated>bash -n scripts/phase240-safe17-boundary-check.sh && grep -q 'check_config_validators' scripts/phase240-safe17-boundary-check.sh && grep -q 'check_steering_validators' scripts/phase240-safe17-boundary-check.sh && grep -q 'safe17-boundary-240.json' scripts/phase240-safe17-boundary-check.sh && grep -q 'PHASE239_CLOSE_ANCHOR' scripts/phase240-safe17-boundary-check.sh && grep -q 'rtt_seam_unchanged_since_phase239' scripts/phase240-safe17-boundary-check.sh && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase240-safe17-boundary-check.sh` exists, is executable, and passes `bash -n` (syntax check)
    - Allowlist regex includes all four files: `rtt_backend\.py`, `rtt_measurement\.py`, `check_config_validators\.py`, `check_steering_validators\.py` (and NOT `check_config.py` or `autorate_config.py`)
    - `OUT`/`ALLOWED_OUT_PREFIX` point under `.planning/phases/240-config-validator/evidence/`
    - Embedded disallowed-paths set lists the four 240 files (no leftover 239-only set)
    - The 239 protected-body helper is invoked AS-IS (`phase239-protected-body-diff.py` referenced, not re-cloned)
    - `PHASE239_CLOSE_ANCHOR` is defined as a pinned commit SHA, and a `git diff --name-only "$PHASE239_CLOSE_ANCHOR" HEAD -- src/wanctl/rtt_backend.py src/wanctl/rtt_measurement.py` gate is present and fail-closes (exit 1) when non-empty
    - The emitted evidence JSON includes `rtt_seam_unchanged_since_phase239` and `phase239_close_anchor`
    - After Plan 01 is committed and the tree is clean: `scripts/phase240-safe17-boundary-check.sh` exits 0 and writes `safe17-boundary-240.json` with `"passed": true` and `"rtt_seam_unchanged_since_phase239": true`
    - Negative check: with an uncommitted src/wanctl/ edit present, the script exits 1 (fail-closed)
  </acceptance_criteria>
  <done>Phase 240 boundary script exists with the expanded union allowlist (v1.52), a second PHASE239_CLOSE_ANCHOR gate proving zero new rtt_backend.py/rtt_measurement.py diff since Phase 239 close, evidence pointed at the 240 dir with seam-unchanged audit fields, reuses the 239 protected-body helper unchanged, preserves all fail-closed guards, and passes against the committed Plan 01 tree.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test for the Phase 240 boundary verifier (static + negative coverage)</name>
  <files>tests/test_phase240_safe17_verifier.py</files>
  <read_first>
    - tests/test_phase239_safe17_verifier.py (the test analog — mirror its structure: `detached_worktree` fixture, `commit_worktree_change`, the negative tests at :72-128)
    - scripts/phase240-safe17-boundary-check.sh (the script under test, from Task 1)
    - .planning/phases/240-config-validator/240-PATTERNS.md (test section)
  </read_first>
  <action>
    Create `tests/test_phase240_safe17_verifier.py` mirroring `tests/test_phase239_safe17_verifier.py` (reuse its worktree-based harness: `detached_worktree` fixture + `commit_worktree_change`).
    STATIC assertions (parse the script text): (a) the 240 script exists and is executable; (b) the allowlist regex string includes the two validator files AND the two rtt seam files; (c) the allowlist does NOT include `check_config.py` or `autorate_config.py`; (d) the evidence OUT path targets the 240 evidence dir; (e) BOTH allowlist mechanisms are asserted — the shell `V153_ALLOWLIST_RE` regex string AND the embedded-Python `disallowed_paths` set both list the four 240 files (review MEDIUM: static-test both mechanisms); (f) `PHASE239_CLOSE_ANCHOR` is present.
    POSITIVE: running the script against the committed clean tree exits 0 and writes the 240 evidence JSON.
    NEGATIVE coverage (mirror the 239 verifier tests, review MEDIUM): (a) committed OUT-OF-ALLOWLIST drift fails — e.g. a committed edit to `src/wanctl/wan_controller.py` (or `check_config.py`) in the worktree makes the script exit non-zero; (b) UNCOMMITTED / dirty `src/wanctl` edit fail-closes (exit 1); (c) PROTECTED-BODY drift fails — a committed edit inside a protected `rtt_measurement.py` qualname makes the script exit non-zero; (d) RTT-SEAM no-drift: a committed but otherwise-allowlisted edit to `rtt_backend.py` in the worktree (which the union allowlist would PERMIT and the protected-body helper does NOT cover) makes the script exit non-zero via the new `PHASE239_CLOSE_ANCHOR` gate — this is the direct regression for the closed false-pass hole.
    Do NOT invoke `python -m wanctl.check_config` anywhere.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_phase240_safe17_verifier.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase240_safe17_verifier.py` exists and mirrors the 239 verifier test structure (worktree fixture)
    - Asserts the shell allowlist regex contains `check_config_validators` and `check_steering_validators` and the two rtt seam files, AND that the embedded-Python `disallowed_paths` set lists the same four files (both mechanisms statically tested)
    - Asserts the allowlist excludes `check_config.py` and `autorate_config.py`
    - Asserts the evidence path targets the 240 evidence dir and that `PHASE239_CLOSE_ANCHOR` is present in the script
    - Negative tests present and passing: out-of-allowlist committed drift fails; uncommitted/dirty `src/wanctl` edit fail-closes; protected-body `rtt_measurement.py` drift fails; a committed `rtt_backend.py` edit (allowlisted but seam drift) fails via the PHASE239_CLOSE_ANCHOR gate
    - `.venv/bin/pytest -o addopts='' tests/test_phase240_safe17_verifier.py -q` exits 0
  </acceptance_criteria>
  <done>Regression test for the 240 boundary verifier exists and is green: statically asserts both allowlist mechanisms and the second anchor, and exercises the four negative paths (out-of-allowlist, dirty tree, protected-body drift, rtt_backend seam drift), mirroring the 239 verifier test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → boundary script | The script inspects committed/staged/untracked src/wanctl/ state via git; read-only inspection, no mutation of source, refs, or gear. |
| `--out` argument → filesystem | Operator-supplied output path; constrained to the evidence dir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-04 | Tampering | `--out` path argument | mitigate | Preserve the 239 path-traversal guards verbatim: reject paths containing `..`, reject paths targeting `src/wanctl`, and require the realpath to live under ALLOWED_OUT_PREFIX. |
| T-240-05 | Repudiation / integrity | SAFE-17 evidence | mitigate | New per-phase script (not an in-place edit of the 239 script — Pitfall 5), so 239 evidence integrity is preserved and 240 evidence is independently attributable; fail-closed on dirty/staged/untracked tree. |
| T-240-06 | DoS / false-pass | allowlist regex breadth (v1.52 union permits rtt_backend.py/rtt_measurement.py; protected-body helper does NOT cover rtt_backend.py) | mitigate | Closed by the second `PHASE239_CLOSE_ANCHOR` gate: `git diff --name-only` over `rtt_backend.py`/`rtt_measurement.py` since the Phase 239 close must be EMPTY or the script fail-closes (exit 1). Evidence records `rtt_seam_unchanged_since_phase239`. Regression-tested in Task 2 (committed `rtt_backend.py` edit → fail). The union allowlist itself remains pinned to the explicit four-file Plan-01 edit set and is unit-asserted (both mechanisms) in Task 2. |

**Overall surface: LOW.** Read-only git/source inspection script; no network calls, no source mutation, no secret handling. No HIGH-severity threats. The script is itself a safety control (it enforces the SAFE-17 boundary, now including phase-local RTT-seam drift).
</threat_model>

<verification>
- `bash -n scripts/phase240-safe17-boundary-check.sh` passes
- After Plan 01 is committed + clean tree: `scripts/phase240-safe17-boundary-check.sh` exits 0, writes `safe17-boundary-240.json` with `passed: true`, `disallowed_paths: []`, `rtt_seam_unchanged_since_phase239: true`, and the 239 protected-body layer `all_identical: true`
- `.venv/bin/pytest -o addopts='' tests/test_phase240_safe17_verifier.py -q` exits 0
- Negative: a deliberately uncommitted src/wanctl/ edit makes the script exit 1; a committed `rtt_backend.py` edit makes the script exit 1 via the PHASE239_CLOSE_ANCHOR gate
</verification>

<success_criteria>
- SAFE-17: a new Phase 240 boundary script (not a 239 edit) proves only allowlisted files (239 seam + two validators) changed vs v1.52, that Phase 240 introduced ZERO new diff in rtt_backend.py/rtt_measurement.py since the Phase 239 close (closing the union-allowlist false-pass hole), the 239 protected-body qualnames remain byte-identical, and no controller-path drift occurred; evidence JSON (with seam-unchanged audit fields) emitted under the 240 dir.
</success_criteria>

<output>
Create `.planning/phases/240-config-validator/240-02-SUMMARY.md` when done.
</output>
