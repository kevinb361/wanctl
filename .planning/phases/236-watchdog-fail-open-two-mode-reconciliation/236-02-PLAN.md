---
phase: 236-watchdog-fail-open-two-mode-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["236-01"]
files_modified:
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
  - scripts/phase231-rollback.sh
  - scripts/soak-monitor.sh
autonomous: false
requirements: [WDOG-02, SAFE-16]
user_setup: []

must_haves:
  truths:
    - "INVARIANT (W-INV): No fail-open watchdog unit is ever stopped, disabled, restarted, or masked (systemctl stop / disable / disable --now / restart / mask / mask --now) anywhere — CLI, deploy, rollback, retirement, or test — without first writing the operator-disarm sentinel, and the sentinel write + the stop/disable/mask must be under a shell EXIT trap that guarantees a known sentinel state (sentinel normalized/removed) on both success AND failure, even on interrupt or set -e abort. The ONE sanctioned exception to the raw-mask prohibition is the N1 retirement ExecStop-override drop-in (an `ExecStop=` blank-reset `.conf`, NOT a `systemctl mask`), and it is itself preceded by a root-correct sentinel write."
    - "This plan brings the rollback + soak surfaces into W-INV compliance: every watchdog stop/disable in scripts/phase231-rollback.sh (and the retired-variant retirement in Task 4) is the sentinel-clean `silicom-bypass disarm <pair>` verb (which routes through the Plan 01 sanctioned sentineled_stop helper) — NOT a raw `systemctl disable --now silicom-bypass-watchdog...`. The Plan 01 `-k invariant` static global-compliance test (verb set includes mask/mask --now per N3) re-runs HERE as the cross-plan gate and FAILS on any raw unsentineled watchdog stop/disable/mask in these surfaces."
    - "A non-destructive test proves: watched-unit inactive → petter calls set_bypass on and withholds reset_bypass_wd; watched-unit active → petter pets and restores inline (software behavior, NOT hardware relay expiry)"
    - "The 2026-06-08 ATT migration failure mode is documented in docs/SILICOM-BYPASS.md as understood and covered by the reconciled units"
    - "The intended shutdown/boot fail-open is DOCUMENTED in docs/SILICOM-BYPASS.md in a greppable subsection 'Shutdown / boot fail-open is intended' (the /run sentinel is tmpfs and wiped on boot; host shutdown/reboot fires every armed watchdog's ExecStop set_bypass on; this is INTENDED — host down -> raw ISP passthrough keeps the link up; operators wanting CAKE preserved across a planned reboot may pre-disarm) AND the threat model marks the shutdown/lifecycle ExecStop path as accepted/intended (NOT a residual). No persistent-sentinel / lifecycle-ordering / startup-grace / pre-reboot-disarm-enforcement mechanism is added — documenting the intended behavior is the entire deliverable (N4)"
    - "A REQUIRED live-env migration gate detects any stale WANCTL_UNIT=wanctl@... in /etc/wanctl/bpctl-watchdog/*.env and either fixes it under operator approval or records 'watchdog must remain disarmed' BEFORE any arm (HIGH-4)"
    - "phase231-rollback.sh reconciles watchdog state BY MODE, and for EACH wan (att AND spectrum) the native rollback ACTUALLY RE-POINTS a RUNNING petter — not merely rewrites the env file. The command-order assertion proves one of two invariants AND that it reconfigures a live petter: (A) env-rewrite-to-native (WANCTL_UNIT=wanctl@<wan>.service) executes BEFORE a sentinel-clean RESTART of silicom-bypass-watchdog@<wan> (so the running petter re-reads the new WANCTL_UNIT), OR (B) a sentinel-clean disarm (the `silicom-bypass disarm <pair>` verb) executes BEFORE the cake unit is stopped. env-rewrite + daemon-reload ALONE against an active petter is NOT acceptable (it does not re-point a running process) and FAILS the gate; 'Leave untouched' is NOT acceptable for an active @spectrum (HIGH-2-NEW running-petter / HIGH-3 / HIGH-C)"
    - "The `-k rollback_order` gate REQUIRES the sentinel-clean `silicom-bypass disarm <pair>` verb (or env-rewrite-before-sentinel-clean-restart) and REJECTS raw `systemctl disable --now silicom-bypass-watchdog...` as a 'clean disarm' — raw disable --now is itself unsentineled and does not count (HIGH-4 / HIGH-C residual)"
    - "Stale silicom-bypass-watchdog-cake-autorate references in scripts/soak-monitor.sh are reconciled to the folded @att path (MED-2)"
    - "Retirement of silicom-bypass-watchdog-cake-autorate-att.service is SENTINEL-FIRST + ExecStop-MASKED (N1): (1) the att-modem operator-disarm sentinel is written ROOT-CORRECT (via `sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` or `sudo tee ... </dev/null`, NEVER `sudo : > ...` which runs the redirection as the non-root caller) and VERIFIED to exist before any stop; (2) the retired unit's ExecStop is MASKED by a systemd `ExecStop=` blank-reset drop-in + daemon-reload BEFORE the stop (belt-and-suspenders, so even if the sentinel is not honored the stop cannot fire set_bypass on); (3) THEN `systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`; (4) att-modem confirmed INLINE post-retire. A bare unsentineled+unmasked `disable --now` of the retired unit is FORBIDDEN. Operator-gated, ordering-aware (the Plan 01 sentinel-aware bypass script + sanctioned disarm verb installed first), never an executor-run live mutation; proven offline by a no-bypass test (HIGH-3-NEW retired-variant)"
    - "SAFE-16 controller-path zero-diff holds at the phase boundary (passed=true, controller_path_diff_count=0) AND a companion gate confirms no other tracked/staged/dirty/untracked file under src/wanctl/ changed vs v1.51 (MED-5)"
  artifacts:
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "petter-body heartbeat-death → relay-bypass software-behavior shim proof; rollback_order running-petter re-point gate; sentinel-first+ExecStop-masked retirement no-bypass proof (N1); re-run of the W-INV invariant test (mask in verb set) over the reconciled rollback/soak surfaces"
      contains: "petter_expiry"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "2026-06-08 ATT migration RCA + arm/disarm usage + native-rollback running-petter-repoint note + sentinel-first+ExecStop-masked retirement note + 'Shutdown / boot fail-open is intended' subsection (N4)"
      contains: "fail-open is intended"
    - path: ".planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json"
      provides: "SAFE-16 boundary evidence (fixed controller list)"
      contains: "passed"
    - path: ".planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/src-wanctl-tree-236.json"
      provides: "MED-5 companion gate: full src/wanctl/ tree diff vs v1.51"
      contains: "src_wanctl_changed_count"
  key_links:
    - from: "scripts/phase231-rollback.sh native rollback (per wan)"
      to: "running silicom-bypass-watchdog@<wan> petter / WANCTL_UNIT"
      via: "command-order proof: env-rewrite-to-native BEFORE a sentinel-clean RESTART of @<wan> (re-points the running petter), OR sentinel-clean `silicom-bypass disarm <pair>` BEFORE cake-stop; env-rewrite + daemon-reload ALONE FAILS the gate"
      pattern: "WANCTL_UNIT=wanctl@|silicom-bypass disarm"
    - from: "scripts/phase231-rollback.sh + Task 4 retirement"
      to: "scripts/silicom-bypass disarm verb (sanctioned sentineled_stop)"
      via: "all watchdog stops are the sentinel-clean disarm verb, not raw systemctl disable --now (W-INV compliance; re-checked by the -k invariant test)"
      pattern: "silicom-bypass disarm"
    - from: "Task 4 retired-variant retirement (att-modem sentinel + ExecStop-mask drop-in)"
      to: "silicom-bypass-watchdog-cake-autorate-att.service ExecStop"
      via: "root-correct sentinel-first write (sudo sh -c ': > ...') VERIFIED present + ExecStop= blank-reset drop-in + daemon-reload BEFORE disable --now, so the retired unit's shared ExecStop cannot fire set_bypass on (N1)"
      pattern: "att-modem.disarm|ExecStop="
    - from: "scripts/phase225-safe13-boundary-check.sh"
      to: "evidence/safe16-boundary-236.json"
      via: "--anchor v1.51 --out"
      pattern: "passed.*true"
---

<objective>
Prove heartbeat-death → relay-bypass non-destructively (as software behavior, not hardware relay expiry), document the 2026-06-08 ATT migration failure mode AND the intended shutdown/boot fail-open, reconcile rollback/native-mode behavior BY MODE under operator review with a per-wan command-order proof that ACTUALLY RE-POINTS a running petter, add a required live-env migration gate, retire the ATT variant SENTINEL-FIRST + ExecStop-MASKED, sweep stale references, and close the SAFE-16 phase boundary with a companion full-tree gate (WDOG-02, SAFE-16). This plan brings the rollback + soak surfaces into compliance with the global invariant introduced in Plan 01.

INVARIANT (W-INV) — stated verbatim, enforced here too:
"No fail-open watchdog unit is ever stopped, disabled, restarted, or masked (systemctl stop / disable / disable --now / restart / mask / mask --now) anywhere — CLI, deploy, rollback, retirement, or test — without first writing the operator-disarm sentinel, and the sentinel write + the stop/disable/mask must be under a shell EXIT trap that guarantees a known sentinel state (sentinel normalized/removed) on both success AND failure, even on interrupt or set -e abort."

Purpose: Plan 01 reconciled the units, wired SAFE arm/disarm, and introduced the sanctioned `sentineled_stop` helper + the `silicom-bypass disarm <pair>` verb as the ONLY sanctioned way to stop a fail-open watchdog. This plan applies that discipline to the remaining surfaces the reviewer flagged as the SAME class:
- HIGH-3-NEW / N1 (retired-variant retirement): Task 4 retires `silicom-bypass-watchdog-cake-autorate-att.service` SENTINEL-FIRST + ExecStop-MASKED. The att-modem operator-disarm sentinel is written ROOT-CORRECT (`sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` — NEVER `sudo : > ...` which redirects as the non-root caller and fails with EACCES) and VERIFIED to exist; the retired unit's ExecStop is then MASKED by an `ExecStop=` blank-reset systemd drop-in + daemon-reload BEFORE the stop; THEN `systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`; att-modem confirmed inline. It is operator-gated, ordering-aware (the sentinel-aware bypass script + the disarm verb from Plan 01 must be installed first), never executor-run, and proven offline by a no-bypass test.
- HIGH-2-NEW (running-petter re-point): an env rewrite + `daemon-reload` does NOT reconfigure an already-running petter (it reads `WANCTL_UNIT` once at start). So the native rollback for an ACTIVE `@<wan>` must either (A) env-rewrite-to-native BEFORE a sentinel-clean RESTART of `@<wan>` (so the petter re-reads the new name), OR (B) sentinel-clean `silicom-bypass disarm <pair>` BEFORE the cake unit is stopped. The `-k rollback_order` gate now asserts the running petter is actually RE-POINTED — env-rewrite + daemon-reload alone FAILS.
- HIGH-4 / HIGH-C residual: the `-k rollback_order` gate REQUIRES the sentinel-clean `silicom-bypass disarm <pair>` verb and REJECTS raw `systemctl disable --now silicom-bypass-watchdog...` as a clean disarm.
- N4 (lifecycle fail-open): Task 2 DOCUMENTS the shutdown/boot fail-open as INTENDED and the threat model marks it accepted — no enforcement mechanism is added (operator decision).
The Plan 01 `-k invariant` static global-compliance test (verb set includes mask per N3) re-runs HERE over the reconciled rollback/soak surfaces as the cross-plan W-INV gate.

Output: `petter_expiry` behavior tests; a sentinel-first+ExecStop-masked retirement no-bypass test (N1); a 2026-06-08 RCA + arm/disarm + native-rollback running-petter-repoint + sentinel-first-ExecStop-masked-retirement + "Shutdown / boot fail-open is intended" (N4) section in docs/SILICOM-BYPASS.md; mode-aware reconciliation of phase231-rollback.sh with a per-wan running-petter-repoint command-order invariant and a required live-env migration gate; sentinel-first ExecStop-masked operator-gated retirement of the ATT variant; soak-monitor reference sweep; SAFE-16 boundary evidence JSON plus a companion full-tree evidence JSON.

This plan is non-autonomous: it contains operator-gated checkpoints for the live-env migration gate (HIGH-4), the sentinel-first ExecStop-masked retired-variant retirement (HIGH-3-NEW / N1), and the conservative mode-aware rollback-script edit (HIGH-3/HIGH-C / CLAUDE.md network/prod policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-RESEARCH.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-PATTERNS.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-VALIDATION.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-REVIEWS.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; extracted from codebase. -->

Sanctioned watchdog-stop discipline (Plan 01 — W-INV):
  silicom-bypass disarm <pair>     # the ONLY sanctioned way to stop/disable a fail-open watchdog from a script.
                                   # Routes through sentineled_stop: writes /run/wanctl/bpctl-watchdog/<iface>.disarm
                                   # under `trap 'rm -f $sentinel' EXIT`, runs `systemctl disable --now @<instance>` non-fatally,
                                   # so the unit's sentinel-aware ExecStop restores inline (never set_bypass on) and no sentinel leaks.
  pair tokens: att-modem -> @att ; spec-modem -> @spectrum
  Raw `systemctl disable --now silicom-bypass-watchdog...` is NOT sanctioned (unsentineled) — the -k invariant test fails on it.
  NOTE (N1): `silicom-bypass disarm att-modem` maps att-modem -> @att, so it disables `silicom-bypass-watchdog@att`,
             NOT the differently-named retired `silicom-bypass-watchdog-cake-autorate-att.service`. Retiring the RETIRED
             VARIANT (a separate unit) requires the Task 4 SENTINEL-FIRST + ExecStop-MASKED procedure, not the disarm verb.
  N2: the `Conflicts=` mechanism was REMOVED in Plan 01 (no template-wide directive, no @att drop-in). The double-petter
      guarantee is the CLI arm-time guard + the Task 4 operator not-both-active live check — there is no unit Conflicts= here.
  N3: the `-k invariant` verb set includes `mask`/`mask --now`. The Task 4 ExecStop-mask is an `ExecStop=` drop-in (NOT a
      `systemctl mask`); if any `systemctl mask` line is used in the sentinel-first sequence it MUST carry the pinned
      `# W-INV-SANCTIONED-RETIRE-MASK` marker so the gate does not false-positive.

Petter loop body (scripts/wanctl-bpctl-watchdog-petter, SYSTEMCTL-seamed by Plan 01 Task 4):
  if "$SYSTEMCTL" is-active --quiet "$WANCTL_UNIT"; then
      # active branch: on transition util set_disc off; util set_bypass off (inline restore); then util reset_bypass_wd (PET)
  else
      # inactive branch: on transition util set_bypass on; WITHHOLD reset_bypass_wd (EXPIRY)
  fi
  arm sequence: set_wd_exp_mode bypass; set_wd_autoreset 0; set_dis_bypass off; set_disc off; set_bypass_wd $TIMEOUT_MS
  IMPORTANT: WANCTL_UNIT is read ONCE at process start — a live env rewrite + daemon-reload does NOT change a running
             petter's loaded WANCTL_UNIT. Re-pointing a RUNNING petter requires a restart (sentinel-clean) or a disarm.

Test harness (Plan 01 added these):
  _fake_bpctl watchdog verbs: set_wd_exp_mode/set_wd_autoreset/set_bypass_wd/reset_bypass_wd/get_bypass_wd/get_wd_exp_mode
  _fake_systemctl(tmp_path): PATH-injected systemctl; rc from FAKE_SYSTEMCTL_RC (is-active); per-unit is-active state via SANITIZED key;
                             on stop/disable/mask --now RUNS the unit's ExecStop against fake-bpctl (non-vacuous); SEPARATE FAKE_SYSTEMCTL_DISABLE_RC
  _calls_for(calls, iface): filters the bpctl call log per iface
  test_w_inv_no_raw_watchdog_stop (-k invariant): static global-compliance gate (verb set includes mask, N3) scanning silicom-bypass/deploy.sh/phase231-rollback.sh/soak-monitor.sh — re-run here

SAFE-16 boundary checker (scripts/phase225-safe13-boundary-check.sh):
  .venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out <evidence.json>
  Protected files (FIXED controller list): src/wanctl/wan_controller.py, wan_controller_state.py, queue_controller.py,
    cake_signal.py, alert_engine.py, fusion_healer.py, backends/
  Expect: passed=true, controller_path_diff_count=0. Anchor tag v1.51 exists.
  MED-5: Task 5 adds a companion gate over the full src/wanctl/ tree.

phase231-rollback.sh references to the RETIRED ATT variant + native-mode watchdog state (live line numbers):
  line 73  : disable --now ... cake-autorate-att ... silicom-bypass-watchdog-cake-autorate-att.service  (external→native: stops the retired variant)
  line 74  : enable --now wanctl@att.service silicom-bypass-watchdog@att.service                         (external→native: arms @att)
  line 100 : (att return→cake) disable --now wanctl@att.service silicom-bypass-watchdog@att.service
  line 101 : (att return→cake) enable --now cake-autorate-att ... silicom-bypass-watchdog-cake-autorate-att.service
  line 107-109: (spectrum) disable wanctl@spectrum; enable cake-autorate-spectrum; comment "@spectrum stays active in both modes; do not stop/disable it here"
  lines 120-122: print_plan watchdog-state narrative
  line 233 : run_check "att_cake_watchdog_active" ... silicom-bypass-watchdog-cake-autorate-att.service
  line 234 : run_check "att_native_watchdog_inactive" ... silicom-bypass-watchdog@att.service
  HIGH-3/HIGH-2-NEW: a native rollback (wanctl@<wan>) while %i.env still names cake-autorate-<wan>.service, or that only rewrites the
    env + daemon-reload against a RUNNING petter, leaves the petter watching a DEAD unit → bypass.
  HIGH-C/HIGH-4: acceptance must be COMMAND-ORDER AND require the sentinel-clean `silicom-bypass disarm` verb (raw disable --now rejected).
  W-INV: lines 73/100 currently RAW `systemctl disable --now silicom-bypass-watchdog...` — these MUST become the sentinel-clean
         `silicom-bypass disarm <pair>` verb (or env-rewrite-before-sentinel-clean-restart) for the -k invariant test to pass.

soak-monitor.sh stale refs (MED-2):
  line 286: att) echo "... silicom-bypass-watchdog-cake-autorate-att.service" ;;  -> folded @att path

docs/SILICOM-BYPASS.md anchors:
  ## Per-WAN Watchdog Fail-Open (line 250); env path block (265-266)
  hardware quirk: requested 5000ms rounds up to 6400ms (lines 289-292) — arm verb must not promise exact ms
  ### Spectrum Migration Validation And Pitfalls (line 435)
  doc-test coupling: test_init_service_manual_reapply_docs_match_remain_after_exit (test file 489-504) — keep procedure blocks test-consistent
  N4 anchor: the new "Shutdown / boot fail-open is intended" subsection lands under ## Per-WAN Watchdog Fail-Open (line 250)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-destructive heartbeat-death → relay-bypass petter-body software-behavior proof</name>
  <files>tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - tests/test_silicom_bypass_cli.py (file being modified; _fake_bpctl watchdog verbs + _fake_systemctl from Plan 01; _calls_for 190-195; _run 198-221)
    - scripts/wanctl-bpctl-watchdog-petter (the SYSTEMCTL-seamed loop body — the test target; active/inactive branches at lines 28-42)
    - 236-RESEARCH.md (Code Examples expiry proof — "pure-logic test of the petter loop body, not a wall-clock wait")
    - 236-PATTERNS.md (petter section — loop body IS the WDOG-02 test target; assertion spec)
    - 236-VALIDATION.md (WDOG-02 row + petter_expiry selector)
    - 236-REVIEWS.md (MED-3 — do NOT overstate as "relay fires"; this proves software behavior: set_bypass on + withheld reset_bypass_wd)
  </read_first>
  <action>
    Add a `petter_expiry` test group proving the reconciled fail-open SOFTWARE behavior offline — no wall-clock wait, no live card, no real timer. Drive the petter with `SYSTEMCTL` set to the `_fake_systemctl` and `BPCTL_UTIL` set to the `_fake_bpctl`, with `IFACE` and `WANCTL_UNIT` via env. To avoid the petter's infinite `while :;` loop, run it with a controllable iteration bound — set `HEARTBEAT_SECONDS` small and run under a short `timeout` so it executes at least one full branch then is killed, OR (preferred) factor the test to invoke the loop body once. Assert from the bpctl call log via `_calls_for`:
    (a) Inactive proof (FAKE_SYSTEMCTL_RC=1 → controller dead): the bpctl calls for the iface include `set_bypass on` and do NOT include `reset_bypass_wd` (pet withheld) — fail-open firing in software.
    (b) Active proof (FAKE_SYSTEMCTL_RC=0 → controller alive): the bpctl calls include `reset_bypass_wd` (pet) and, on the first transition, `set_disc off` + `set_bypass off` (restore inline), and do NOT include `set_bypass on`.
    Also assert the arm sequence ran on startup: `set_wd_exp_mode bypass`, `set_wd_autoreset 0`, `set_bypass_wd <ms>`. Add a docstring/comment stating this proves SOFTWARE behavior (the petter emits `set_bypass on` and withholds `reset_bypass_wd`), NOT actual hardware relay expiry (MED-3, deferred to Phase 237 HIL harness). NEVER set BPCTL_UTIL to the real binary and NEVER call the real systemctl.
  </action>
  <behavior>
    - petter_expiry_inactive: SYSTEMCTL is-active rc!=0 → bpctl log has `set_bypass on`, no `reset_bypass_wd`
    - petter_expiry_active: SYSTEMCTL is-active rc==0 → bpctl log has `reset_bypass_wd` + inline restore, no `set_bypass on`
    - petter_arm_sequence: startup writes set_wd_exp_mode bypass, set_wd_autoreset 0, set_bypass_wd <ms>
  </behavior>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q` exits 0
    - inactive-branch test asserts presence of `set_bypass on` AND absence of `reset_bypass_wd` in the iface call log
    - active-branch test asserts presence of `reset_bypass_wd` AND absence of `set_bypass on`
    - the test group carries an explicit comment/docstring that it proves SOFTWARE behavior, not hardware relay expiry (MED-3)
    - no test sets `BPCTL_UTIL` to a real path and no test calls a real `systemctl` (all via PATH/env fakes)
    - test completes in < 5s (no real timer wait)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>petter_expiry proves heartbeat-death → bypass and heartbeat-alive → pet/inline offline as software behavior (MED-3 caveat documented); WDOG-02 behavior demonstrated non-destructively.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 2026-06-08 ATT migration RCA + arm/disarm usage + native-rollback running-petter-repoint + sentinel-first ExecStop-masked retirement + intended shutdown/boot fail-open (N4) in docs/SILICOM-BYPASS.md</name>
  <files>docs/SILICOM-BYPASS.md</files>
  <read_first>
    - docs/SILICOM-BYPASS.md (file being modified; ## Per-WAN Watchdog Fail-Open line 250; env-path block 265-266; hardware quirk 289-292; ### Spectrum Migration Validation And Pitfalls line 435)
    - tests/test_silicom_bypass_cli.py (test_init_service_manual_reapply_docs_match_remain_after_exit 489-504 — doc-procedure/test coupling)
    - 236-RESEARCH.md (Pitfall 1 — the full 2026-06-08 failure mode; hardware quirk + settled constraint)
    - 236-PATTERNS.md (docs section — what to add; do not re-litigate unpowered fail-open)
    - 236-REVIEWS.md (HIGH-1 disarm; HIGH-2 timeout; HIGH-2-NEW running-petter; HIGH-3/HIGH-C native-rollback; HIGH-3-NEW/N1 sentinel-first ExecStop-masked retirement; HIGH-A clean re-arm; MED-3 software-behavior wording; N4 — shutdown/boot fail-open is INTENDED, document it)
  </read_first>
  <action>
    Add a dated RCA subsection under `## Per-WAN Watchdog Fail-Open` (e.g. `### 2026-06-08 ATT Migration Watchdog Failure Mode (RCA)`) capturing, as understood and now COVERED:
    - What happened: the migration from `wanctl@<wan>` to cake-autorate (live 2026-06-08) left the watched-unit name `wanctl@<wan>.service` permanently inactive; an armed watchdog reading that name sees a healthy controller as dead → `set_bypass on` + withheld pet → relay fires bypass (raw ISP, CAKE shaping gone) within the ~6400ms timeout.
    - Why: the migration bolted on a one-off `silicom-bypass-watchdog-cake-autorate-att.service` for ATT but left Spectrum on the stale generic template pointing at the disabled `wanctl@spectrum.service` — an asymmetric two-unit reality.
    - The fix (this phase): one configurable template whose watched-unit name (`WANCTL_UNIT` in `%i.env`) names the live controller (`cake-autorate-<wan>.service`) for both pairs. The double-petter mutual-exclusion guarantee against the retired ATT variant is enforced by the CLI arm-time guard (NOT a unit `Conflicts=`, which was removed because it is a symmetric indirect unsentineled stop path). ATT variant retired (sentinel-first + ExecStop-masked), folded into `@att`.
    Add an `arm`/`disarm` usage block: `silicom-bypass arm <pair> [timeout]` (default from `WD_TIMEOUT_MS`) requires `--yes`; the timeout is a POSITIVE integer in ms written ATOMICALLY to the per-pair `%i.env` `TIMEOUT_MS` (HIGH-2); re-arming an already-armed pair is CLEAN — it routes the stop through the sanctioned sentineled discipline (no raw restart) so a re-arm never drops the pair to bypass and never leaks a disarm sentinel (HIGH-A / HIGH-1-NEW); `silicom-bypass disarm <pair>` performs a CLEAN inline restore (never leaves bypass on, fail-safe even if systemctl disable fails) via the operator-disarm sentinel under an EXIT trap (HIGH-1). State the W-INV rule explicitly: any script that stops/disables/masks a fail-open watchdog MUST use `silicom-bypass disarm <pair>` (sentinel-clean), NEVER a raw `systemctl disable --now silicom-bypass-watchdog...` and NEVER a raw `systemctl mask --now` (N3). Pair tokens are `att-modem`/`spec-modem` (mapped to `@att`/`@spectrum`).
    Add a NATIVE-ROLLBACK note (HIGH-3/HIGH-C/HIGH-2-NEW): a watchdog reads `WANCTL_UNIT` ONCE at start, so an env rewrite + `daemon-reload` alone does NOT re-point a RUNNING petter. When rolling a WAN back from cake-autorate to native `wanctl@<wan>`, phase231-rollback.sh enforces, per WAN, ONE of: EITHER the `%i.env` `WANCTL_UNIT` is rewritten to `wanctl@<wan>.service` BEFORE a sentinel-clean RESTART of the watchdog (so the petter re-reads the native name), OR the watchdog is cleanly DISARMED (`silicom-bypass disarm <pair>`) BEFORE the cake unit is stopped. "Leave the watchdog untouched" or "rewrite the env file only" is NOT safe for an active `@spectrum` — it keeps watching the now-dead cake unit and fires bypass. Reference the reconciled phase231-rollback.sh.
    Add a SENTINEL-FIRST + ExecStop-MASKED RETIREMENT note (HIGH-3-NEW / N1): retiring `silicom-bypass-watchdog-cake-autorate-att.service` (its ExecStop is the same shared bypass script) MUST (1) write the `att-modem` operator-disarm sentinel ROOT-CORRECT (`sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` — note `sudo : > ...` does NOT work because the redirection runs as the non-root caller) and verify it exists; (2) install an `ExecStop=` blank-reset systemd drop-in for the retired unit + `daemon-reload` so its ExecStop cannot fire `set_bypass on` even if the sentinel is not honored; (3) THEN `systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`; (4) confirm att-modem inline. This requires the Plan 01 sentinel-aware bypass script + the disarm verb to be installed first (ordering). A bare unsentineled+unmasked `disable --now` is forbidden.
    Add a "Shutdown / boot fail-open is intended" subsection (N4) — greppable phrase `fail-open is intended` — stating: the `/run/wanctl/bpctl-watchdog/` sentinel is tmpfs and wiped on boot; a host shutdown/reboot stops every armed watchdog and fires its ExecStop `set_bypass on`; this is INTENDED (host down → the relay drops to raw ISP passthrough, keeping the internet up), not a residual risk. Operators who want CAKE-shaped behavior preserved across a PLANNED reboot may pre-disarm (`silicom-bypass disarm <pair>`) first, but the default lifecycle fail-open is correct and accepted. Do NOT describe any persistent-sentinel, startup-grace, or pre-reboot-disarm-enforcement mechanism — none exists by operator decision.
    Cite the hardware quirk: requested 5000ms rounds up to a 6400ms hardware timeout — do NOT promise exact ms. State the non-destructive proof demonstrates software behavior, not a measured hardware relay expiry (MED-3). Do NOT re-litigate unpowered fail-open. Keep procedure/codeblock edits consistent with the doc-test coupling at test_silicom_bypass_cli.py:489-504.
  </action>
  <verify>
    <automated>grep -c '2026-06-08' docs/SILICOM-BYPASS.md && grep -c 'silicom-bypass arm' docs/SILICOM-BYPASS.md && grep -c 'silicom-bypass disarm' docs/SILICOM-BYPASS.md && grep -qi 'fail-open is intended' docs/SILICOM-BYPASS.md && grep -c 'wanctl@' docs/SILICOM-BYPASS.md && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SILICOM-BYPASS.md` contains a 2026-06-08 RCA referencing the watched-unit/cake-autorate failure mode and stating it is covered by the reconciled units
    - doc contains `silicom-bypass arm` and `silicom-bypass disarm` usage with the `--yes` live-arm rationale, the HIGH-2 timeout-reaches-timer note, the HIGH-A/HIGH-1-NEW clean-re-arm note, and the HIGH-1 clean-disarm (never leaves bypass on, fail-safe) note
    - doc states the W-INV rule: scripts stop watchdogs via `silicom-bypass disarm <pair>`, never raw `systemctl disable --now silicom-bypass-watchdog...` and never raw `systemctl mask --now` (N3)
    - doc contains a native-rollback note stating a running petter reads WANCTL_UNIT once and the command-order invariant: env-rewrite BEFORE a sentinel-clean RESTART (re-points the running petter), OR clean-disarm BEFORE cake-stop; env-rewrite + daemon-reload alone and "leave untouched" are NOT safe for an active @spectrum (HIGH-3/HIGH-C/HIGH-2-NEW)
    - doc contains a sentinel-first + ExecStop-masked retirement note for the ATT variant (root-correct `sudo sh -c ': > ...'` sentinel write verified present, ExecStop= blank-reset drop-in + daemon-reload, THEN disable --now; ordering: sentinel-aware script installed first; bare unsentineled+unmasked disable forbidden) (HIGH-3-NEW / N1)
    - N4: doc contains a greppable "Shutdown / boot fail-open is intended" subsection (`grep -qi 'fail-open is intended' docs/SILICOM-BYPASS.md` succeeds) stating the tmpfs sentinel is wiped on boot, host shutdown/reboot fires every armed watchdog's ExecStop set_bypass on, this is INTENDED (host down -> raw ISP passthrough), and operators may pre-disarm before a planned reboot; no persistent-sentinel/lifecycle-ordering mechanism is described
    - doc states the non-destructive proof shows software behavior, not hardware relay expiry (MED-3)
    - doc does NOT add any claim contradicting the settled unpowered-fail-open constraint
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q` exits 0 (doc-test coupling intact)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>The 2026-06-08 failure mode is documented as understood and covered; arm/disarm usage, the HIGH-1/HIGH-2/HIGH-A/HIGH-1-NEW safety semantics, the W-INV sanctioned-disarm rule (incl. no raw mask, N3), the HIGH-3/HIGH-C/HIGH-2-NEW running-petter-repoint command-order invariant, the HIGH-3-NEW/N1 sentinel-first ExecStop-masked retirement note, the N4 "Shutdown / boot fail-open is intended" subsection, and the MED-3 software-behavior caveat are recorded; doc-test coupling stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mode-aware phase231-rollback.sh reconciliation with a PER-WAN running-petter-repoint command-order invariant (sentinel-clean disarm verb only) + W-INV compliance + offline sentinel-first ExecStop-masked retirement no-bypass proof (N1) + soak-monitor sweep (offline; live mutation deferred to the operator checkpoint)</name>
  <files>scripts/phase231-rollback.sh, scripts/soak-monitor.sh, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - scripts/phase231-rollback.sh (file being modified; ATT variant + RAW watchdog disable refs at lines 73, 100-101, 109, 120-122, 233-237; the env-rewrite/arm command emitters; run_check predicates)
    - scripts/soak-monitor.sh (file being modified; stale att-variant ref at line 286)
    - tests/test_silicom_bypass_cli.py (add the command-order assertion test AND the retirement no-bypass proof here; the `-k invariant` test re-runs over the reconciled rollback/soak text; analog: existing static rollback-text asserts; _silicom_systemd_array_entries pattern for reading a script's text; _fake_systemctl/_fake_bpctl from Plan 01)
    - scripts/silicom-bypass (the sanctioned `disarm <pair>` verb — the sentinel-clean stop these surfaces must use; W-INV)
    - deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service (the retired unit; ExecStop=/usr/local/sbin/wanctl-bpctl-watchdog-bypass on IFACE=att-modem — the no-bypass proof target)
    - 236-RESEARCH.md (Risk flags for the planner; Open Q2; Runtime State Inventory + A1)
    - 236-PATTERNS.md (phase231-rollback.sh section — CONSERVATIVE; "mark any edit here for explicit operator review")
    - 236-REVIEWS.md (HIGH-3/HIGH-C native-rollback re-bypass; HIGH-2-NEW running-petter re-point; HIGH-4/HIGH-C residual raw disable --now rejected; HIGH-3-NEW/N1 sentinel-first+ExecStop-masked retirement; MED-2 stale soak-monitor refs; LOW grep -c trap)
  </read_first>
  <action>
    Conservative, mode-aware (CLAUDE.md network/prod: minimal, watchdog-references only). This task prepares the RENDERED/dry-run text and reference reconciliation AND the OFFLINE retirement no-bypass proof; NO live host mutation runs here (the live apply is the operator checkpoint Task 4).
    (a) W-INV compliance — convert the RAW watchdog disables to the sentinel-clean verb: at line 73 (external→native: stops the retired ATT variant) and line 100 (att return→cake: stops `@att`), the watchdog-unit stop MUST become the sanctioned `silicom-bypass disarm att-modem` verb (which writes the att-modem sentinel under the trap and disables the unit cleanly), NOT a raw `systemctl disable --now silicom-bypass-watchdog...`. Leave the non-watchdog units on those lines (cake-autorate / wanctl@ / state-bridge) as ordinary `systemctl disable --now` — W-INV applies only to fail-open watchdog units. NOTE (N1): line 73 stops the RETIRED VARIANT (`silicom-bypass-watchdog-cake-autorate-att.service`), which the `disarm att-modem` verb does NOT directly target (it targets `@att`). For the rollback path, prefer routing line 73 through `silicom-bypass disarm att-modem` for the `@att` instance AND treat the one-time retirement of the differently-named retired unit as the Task 4 sentinel-first+ExecStop-masked operator procedure (do NOT emit a bare raw `disable --now` of the retired unit in the script). Update the `run_check` assertion names/targets (233-237) so ATT external mode is asserted via `silicom-bypass-watchdog@att` and `att.env` points at `cake-autorate-att.service`.
    (b) phase231-rollback.sh — HIGH-3/HIGH-C/HIGH-2-NEW mode-aware watchdog reconciliation with a RUNNING-PETTER-REPOINT command-order invariant for EACH wan. A petter reads `WANCTL_UNIT` once at start, so for an ACTIVE `@<wan>` the native rollback MUST emit commands so ONE of these orderings is provable by line position AND re-points a running petter:
      * env-rewrite-then-sentinel-clean-RESTART: the line setting `WANCTL_UNIT=wanctl@<wan>.service` in `/etc/wanctl/bpctl-watchdog/<wan>.env` (plus `daemon-reload`) appears STRICTLY BEFORE a sentinel-clean RESTART of `silicom-bypass-watchdog@<wan>` — expressed as `silicom-bypass disarm <pair>` (sentinel-clean stop) FOLLOWED BY a fresh `systemctl start silicom-bypass-watchdog@<wan>` (or `silicom-bypass arm <pair>`), so the running petter is re-exec'd and re-reads the native name. An env-rewrite + `daemon-reload` with NO restart/disarm of an active petter is NOT acceptable; OR
      * sentinel-clean-disarm-precedes-cake-stop: a `silicom-bypass disarm <pair>` (sentinel-clean) appears STRICTLY BEFORE the line that stops the cake unit `cake-autorate-<wan>.service`.
      HIGH-4/HIGH-C residual: a RAW `systemctl disable --now silicom-bypass-watchdog...` does NOT count as a clean disarm — only the `silicom-bypass disarm <pair>` verb (or env-rewrite-then-sentinel-clean-restart) qualifies.
      For att (native rollback already arms `@att`), the env-rewrite-before-sentinel-clean-restart ordering is REQUIRED. For spectrum, "leave untouched" is NO LONGER acceptable: pick sentinel-clean-disarm-before-cake-stop as the conservative default (so spectrum is never left watching a dead cake unit), with an operator note that re-arming spectrum native requires the env to name the native unit first. Change ONLY watchdog-unit references and add the mode-aware env-rewrite / sentinel-clean-disarm / restart lines — leave every tc/qdisc command, health URL, and the `--confirm/--i-have-operator-approval` mutation gate byte-for-byte unchanged.
    (c) Add a COMMAND-ORDER assertion (offline) proving the invariant for BOTH wans (selectable `-k rollback_order`). Render/dry-run the native rollback command sequence per wan (invoke the script's plan/dry-run mode that prints the command list without executing, or grep the function body), capture the ordered command lines, and assert via pytest:
      * for att: the `WANCTL_UNIT=wanctl@att` (or daemon-reload) line index is strictly LESS than a sentinel-clean RESTART (`silicom-bypass disarm att-modem` followed by a `systemctl start silicom-bypass-watchdog@att`/`silicom-bypass arm att-modem`) — i.e. the running petter is re-pointed, not just the env file;
      * for spectrum: EITHER the env-rewrite line index < the sentinel-clean restart line index, OR the `silicom-bypass disarm spec-modem` line index < the `cake-autorate-spectrum` stop line index;
      * the test FAILS if, for a wan, the only watchdog reconciliation is an env-rewrite + daemon-reload with no restart/disarm of an active petter (HIGH-2-NEW), OR if a watchdog "disarm" in the sequence is a RAW `systemctl disable --now silicom-bypass-watchdog...` rather than the sentinel-clean `silicom-bypass disarm <pair>` verb (HIGH-4/HIGH-C). This replaces the prior narrative/raw-disable-accepting acceptance.
    (d) N1 OFFLINE RETIREMENT NO-BYPASS PROOF (selectable `-k retire_nobypass`): add a test that simulates retiring `silicom-bypass-watchdog-cake-autorate-att.service` via the sanctioned SENTINEL-FIRST + ExecStop-MASKED sequence using `_fake_systemctl` + `_fake_bpctl` + tmp `WD_RUN_DIR`. The test: (1) writes the att-modem sentinel ROOT-CORRECT into the tmp `WD_RUN_DIR` (model the `sudo sh -c ': > ...'` form by creating the file directly in the test — the point under test is that the sentinel EXISTS before the stop), and asserts it exists; (2) simulates the ExecStop-mask (either by setting the fake to treat the unit as ExecStop-masked, or by asserting the mask drop-in step precedes the disable in the rendered procedure); (3) invokes the fake `systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service` (which the fake runs the unit's ExecStop for, against the fake-bpctl, mapping the unit -> att-modem); (4) asserts the att-modem bpctl calls.log from the retirement contains NO `set_bypass on` (the sentinel-aware ExecStop took the clean inline-restore branch). This is the checkable proof that the retirement sequence (sentinel-first + ExecStop-masked) does not fail-open even though the retired unit's ExecStop ran. Also assert the FORBIDDEN path: a bare disable WITHOUT a preceding sentinel (delete/omit the sentinel) DOES produce `set_bypass on` in the fake log — proving the test is non-vacuous and the sentinel-first ordering is load-bearing.
    (e) Re-run the W-INV global-compliance gate: the Plan 01 `test_w_inv_no_raw_watchdog_stop` (`-k invariant`, verb set includes mask, N3) now scans the RECONCILED phase231-rollback.sh and soak-monitor.sh and must pass — i.e. no raw `systemctl (stop|disable|restart|mask) ... silicom-bypass-watchdog` remains in either file (all watchdog stops are the sentinel-clean `silicom-bypass disarm` verb). If the test still flags a line, fix the source (this is the cross-plan gate, not a new test).
    (f) soak-monitor.sh (MED-2) — update line 286 so the att watchdog reference is the folded `silicom-bypass-watchdog@att.service` (matching the retired-variant fold), so monitoring does not point at a dead unit.
    Run `bash -n scripts/phase231-rollback.sh` and `bash -n scripts/soak-monitor.sh` only; do NOT execute either script for live effect and do NOT run any systemctl/.env mutation against cake-shaper (a print/plan/dry-run render with no host mutation is allowed for the command-order capture). Defer the ATT variant file deletion and any live apply until after the operator approval in Task 4.
  </action>
  <verify>
    <automated>bash -n scripts/phase231-rollback.sh && bash -n scripts/soak-monitor.sh && ! grep -q 'silicom-bypass-watchdog-cake-autorate-att' scripts/soak-monitor.sh && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k 'rollback_order or invariant or retire_nobypass' -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase231-rollback.sh` exits 0 and `bash -n scripts/soak-monitor.sh` exits 0
    - HIGH-3/HIGH-C/HIGH-2-NEW: a pytest test `-k rollback_order` proves, by command-ORDER (line index), for EACH wan that the running petter is RE-POINTED — env-rewrite-to-native precedes a sentinel-clean RESTART (disarm + start/arm), OR sentinel-clean disarm precedes the cake-stop. The test FAILS on env-rewrite + daemon-reload alone against an active petter, and FAILS if neither ordering holds. For att the env-rewrite-before-sentinel-clean-restart ordering is asserted; for spectrum either ordering is accepted but "leave untouched" / "env-rewrite-only" FAILS.
    - HIGH-4/HIGH-C residual: the `-k rollback_order` test REJECTS a raw `systemctl disable --now silicom-bypass-watchdog...` as a clean disarm — only `silicom-bypass disarm <pair>` (or env-rewrite-then-sentinel-clean-restart) counts
    - N1: a `-k retire_nobypass` test proves the sentinel-FIRST + ExecStop-masked retirement of `silicom-bypass-watchdog-cake-autorate-att.service` produces NO `set_bypass on` in the att-modem fake-bpctl log (sentinel written + verified present before the stop, ExecStop ran clean), AND proves the non-vacuity counter-case (a bare stop WITHOUT a preceding sentinel DOES produce `set_bypass on`)
    - W-INV: `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k invariant -q` exits 0 over the reconciled phase231-rollback.sh + soak-monitor.sh (no raw `systemctl (stop|disable|restart|mask) ... silicom-bypass-watchdog` remains; all watchdog stops are the sentinel-clean `silicom-bypass disarm` verb; N3 mask in verb set)
    - phase231-rollback.sh att watchdog references route through `silicom-bypass-watchdog@att` (folded) / the `silicom-bypass disarm att-modem` verb, not the retired `silicom-bypass-watchdog-cake-autorate-att.service` (except where the dry-run intentionally documents the retired unit for historical context)
    - `! grep -q 'silicom-bypass-watchdog-cake-autorate-att' scripts/soak-monitor.sh` succeeds (MED-2 swept; LOW: `! grep -q` not `grep -c` to avoid the zero-match exit-1 trap)
    - git diff for phase231-rollback.sh shows NO change to tc/qdisc commands, health URLs, or the mutation gate (verified at the Task 4 checkpoint)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>phase231-rollback.sh reconciles watchdog state by mode with a PER-WAN RUNNING-PETTER-REPOINT command-order invariant (env-rewrite-before-sentinel-clean-restart OR sentinel-clean-disarm-before-cake-stop, proven by an offline line-index assertion; env-rewrite-only and raw disable --now both FAIL — HIGH-2-NEW/HIGH-3/HIGH-C/HIGH-4); the N1 sentinel-first+ExecStop-masked retirement is proven offline to produce no set_bypass on (non-vacuous); all watchdog stops are the sentinel-clean `silicom-bypass disarm` verb so the W-INV `-k invariant` gate (mask in verb set) passes over the reconciled surfaces; ATT routed through the folded @att; soak-monitor stale ref swept; offline-only, no live mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Operator-gated SENTINEL-FIRST + ExecStop-MASKED retirement of the ATT variant (HIGH-3-NEW / N1) + live-env migration gate (HIGH-4) + per-wan running-petter-repoint rollback review (HIGH-3/HIGH-C/HIGH-2-NEW) + A1 live-state re-verification</name>
  <files>scripts/phase231-rollback.sh</files>
  <read_first>
    - scripts/phase231-rollback.sh (the mode-aware diff from Task 3 — operator review surface)
    - deploy/systemd/silicom-bypass-watchdog-cake-autorate-att.service (the retired unit; ExecStop=/usr/local/sbin/wanctl-bpctl-watchdog-bypass on IFACE=att-modem — its retirement fires that ExecStop)
    - scripts/wanctl-bpctl-watchdog-bypass (the sentinel-aware ExecStop from Plan 01 Task 2 — the clean branch only exists once Plan 01 is deployed)
    - scripts/silicom-bypass (the `disarm att-modem` verb that writes the att-modem sentinel under the trap — Plan 01; NOTE it targets @att, not the retired variant)
    - 236-RESEARCH.md (Runtime State Inventory + A1; Risk flags for the planner)
    - 236-PATTERNS.md (phase231-rollback.sh CONSERVATIVE section)
    - 236-VALIDATION.md (Manual-Only Verifications table)
    - 236-REVIEWS.md (HIGH-3-NEW/N1 retired-variant disable fires unsentineled bypass — fix with root-correct sentinel-first + ExecStop-mask; HIGH-3/HIGH-C native-rollback command-order; HIGH-2-NEW running-petter; HIGH-4 stale live env must be a REQUIRED success condition before arming)
  </read_first>
  <action>
    Operator-gated, conservative. This checkpoint enforces (1) the SENTINEL-FIRST + ExecStop-MASKED retirement of the ATT variant (HIGH-3-NEW / N1), (2) the HIGH-4 live-env migration gate, and (3) the per-wan running-petter-repoint rollback review (HIGH-3/HIGH-C/HIGH-2-NEW), all as BLOCKING success conditions. The executor prepares the detection/retirement commands and the proposed diff; the operator runs the live reads/mutations and decides remediation. No executor step runs `systemctl enable/start/disable/mask`, `silicom-bypass arm/disarm`, daemon-reload, or any `.env` mutation on cake-shaper.

    ORDERING PRECONDITION (HIGH-3-NEW): the sentinel-aware bypass script (Plan 01 Task 2) and the `silicom-bypass disarm` verb (Plan 01 Task 3) MUST already be installed on cake-shaper BEFORE retiring the ATT variant — otherwise the retired unit's shared ExecStop predates the sentinel branch and a stop would still fire `set_bypass on`. The operator confirms `silicom-bypass disarm --help`/the verb exists on-host and the installed `/usr/local/sbin/wanctl-bpctl-watchdog-bypass` contains the `.disarm` sentinel branch before proceeding.

    ORDER OF OPERATIONS for the N1 retirement (encode as the SUMMARY-recorded procedure; ALL steps operator-run, never executor):
    (1) confirm Plan-01 sentinel-aware bypass script + disarm verb installed on-host (ordering precondition);
    (2) ROOT-CORRECT SENTINEL-FIRST write + VERIFY: `sudo install -d -m0755 /run/wanctl/bpctl-watchdog && sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` (OR `sudo tee /run/wanctl/bpctl-watchdog/att-modem.disarm </dev/null`) — NEVER `sudo : > ...` (the redirection runs as the non-root caller against the root-owned dir → EACCES, sentinel never created). Then VERIFY: `sudo test -f /run/wanctl/bpctl-watchdog/att-modem.disarm` succeeds BEFORE any stop;
    (3) ExecStop-MASK the retired unit: install an `ExecStop=` blank-reset systemd drop-in for `silicom-bypass-watchdog-cake-autorate-att.service` (a `.conf` drop-in under `/etc/systemd/system/silicom-bypass-watchdog-cake-autorate-att.service.d/` with an empty `ExecStop=` reset) + `sudo systemctl daemon-reload`, so even if the sentinel is somehow not honored the stop cannot fire `set_bypass on` (belt-and-suspenders on top of the sentinel);
    (4) THEN `sudo systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`;
    (5) confirm the att-modem relay is INLINE (not bypassed) post-retire via a live relay read.
    A bare unsentineled+unmasked `disable --now` of the retired unit is FORBIDDEN.

    Surface to the operator: (1) the SENTINEL-FIRST + ExecStop-MASKED retirement procedure for the ATT variant (the exact ordered commands above); (2) the Task 3 `git diff scripts/phase231-rollback.sh` PLUS the rendered/dry-run native rollback command sequence for BOTH wans so the operator can confirm the command ORDER re-points a running petter; (3) the HIGH-4 stale-env detection + remediation; (4) the A1 live enablement reads. The phase MUST NOT be marked successful until the operator (a) retires the ATT variant SENTINEL-FIRST + ExecStop-MASKED (root-correct sentinel written and verified present, ExecStop-mask drop-in installed, BEFORE any stop), (b) confirms the per-wan running-petter-repoint command order for both wans, and (c) records the stale-env finding and either fixes it under approval or explicitly records "watchdog must remain disarmed."
  </action>
  <what-built>
    Claude has prepared (offline, no live host touched):
    - A SENTINEL-FIRST + ExecStop-MASKED retirement procedure for `silicom-bypass-watchdog-cake-autorate-att.service`: (1) write the att-modem operator-disarm sentinel ROOT-CORRECT via `sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` (NOT `sudo : > ...`, which runs the redirection as the non-root caller) and VERIFY it exists with `sudo test -f`; (2) install an `ExecStop=` blank-reset systemd drop-in for the retired unit + `daemon-reload` (so its shared ExecStop cannot fire `set_bypass on`); (3) THEN `sudo systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`; (4) confirm att-modem inline. This requires the Plan 01 sentinel-aware bypass script + disarm verb installed first (ordering precondition). The offline `-k retire_nobypass` test (Task 3) proves this sequence produces no `set_bypass on` (HIGH-3-NEW / N1).
    - A mode-aware conservative reconciliation of `scripts/phase231-rollback.sh` (Task 3): ATT watchdog stops routed through the sentinel-clean `silicom-bypass disarm att-modem` verb; native rollback emits, for EACH wan, a command sequence that RE-POINTS a running petter (env-rewrite-to-native BEFORE a sentinel-clean restart, OR sentinel-clean disarm BEFORE the cake-stop) — proven by the `-k rollback_order` line-index test; raw `disable --now` of a watchdog is rejected as clean (HIGH-2-NEW/HIGH-3/HIGH-C/HIGH-4). No tc/qdisc/health/mutation-gate lines changed.
    - A HIGH-4 live-env migration gate: a detection command over the live env dir to find any stale `WANCTL_UNIT=wanctl@...` that would survive install-if-absent and cause spurious bypass if armed.
    This file carries LIVE-ROLLBACK semantics. No live host is touched by Claude. The retirement, the live-env migration, the A1 re-verification, and any live `.env`/daemon-reload/enable/arm/disarm/mask are operator actions.
  </what-built>
  <how-to-verify>
    1. SENTINEL-FIRST + ExecStop-MASKED ATT-variant retirement (operator-only, on `cake-shaper`) — HIGH-3-NEW / N1:
       a. ORDERING PRECONDITION: confirm Plan 01 is installed on-host — `sudo grep -c 'disarm' /usr/local/sbin/wanctl-bpctl-watchdog-bypass` returns >=1 (the `.disarm` sentinel branch is present) AND `silicom-bypass` has the `disarm` verb (`silicom-bypass 2>&1 | grep -c 'disarm <pair>'` >= 1). If NOT present, do NOT run `disable --now` on the retired unit — install Plan 01 first.
       b. ROOT-CORRECT SENTINEL-FIRST write + VERIFY: `sudo install -d -m0755 /run/wanctl/bpctl-watchdog && sudo sh -c ': > /run/wanctl/bpctl-watchdog/att-modem.disarm'` (or `sudo tee /run/wanctl/bpctl-watchdog/att-modem.disarm </dev/null`). DO NOT use `sudo : > /run/.../att-modem.disarm` — the redirection runs as the non-root caller against a root-owned dir (EACCES) and the sentinel is never created. Then VERIFY: `sudo test -f /run/wanctl/bpctl-watchdog/att-modem.disarm && echo SENTINEL_OK` BEFORE any stop.
       c. ExecStop-MASK the retired unit BEFORE stopping: `sudo install -d -m0755 /etc/systemd/system/silicom-bypass-watchdog-cake-autorate-att.service.d` then write a drop-in `.conf` containing a `[Service]` section with an `ExecStop=` blank reset (empty) line; `sudo systemctl daemon-reload`. This guarantees the retired unit's stop cannot fire `set_bypass on` even if the sentinel is not honored (belt-and-suspenders).
       d. THEN retire: `sudo systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`. Confirm via the live relay read that att-modem is INLINE (not bypassed) after retirement.
       e. Record in SUMMARY: the ordering-precondition check result, the SENTINEL_OK verification, the ExecStop-mask drop-in path, the exact ordered retirement commands run, and the post-retirement relay state (att-modem inline). A bare `sudo systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service` with NO preceding sentinel AND NO ExecStop-mask is FORBIDDEN (it fires fail-open on live att-modem).
    2. Review the proposed `scripts/phase231-rollback.sh` diff: only watchdog-unit references + the mode-aware env-rewrite / sentinel-clean-disarm / restart lines changed; tc/qdisc/health/mutation-gate untouched (`git diff scripts/phase231-rollback.sh`). For BOTH wans, confirm the rendered/dry-run native rollback RE-POINTS a running petter: the `WANCTL_UNIT=wanctl@<wan>.service` env-rewrite (+ daemon-reload) appears STRICTLY BEFORE a sentinel-clean RESTART (`silicom-bypass disarm <pair>` then start/arm) of `silicom-bypass-watchdog@<wan>`, OR a `silicom-bypass disarm <pair>` appears STRICTLY BEFORE the `cake-autorate-<wan>.service` stop. env-rewrite + daemon-reload ALONE against an active petter, "leave untouched" for an active @spectrum, and a raw `disable --now` of a watchdog are all NOT acceptable (HIGH-2-NEW/HIGH-3/HIGH-C/HIGH-4). The `-k rollback_order` + `-k invariant` + `-k retire_nobypass` pytest gate this; confirm visually too.
    3. HIGH-4 stale-env migration gate (operator-only, on `cake-shaper`):
       Detection: `sudo grep -rn 'WANCTL_UNIT=wanctl@' /etc/wanctl/bpctl-watchdog/ ; echo "exit=$?"`
       - If it finds ANY stale `wanctl@...` line: that env file MUST be remediated before any arm — either `sudo sed -i 's#WANCTL_UNIT=wanctl@\\(.*\\)\\.service#WANCTL_UNIT=cake-autorate-\\1.service#' <file>` (under operator approval, matching current external mode) then `sudo systemctl daemon-reload`, OR record "watchdog for <pair> must remain DISARMED until env is migrated." Do NOT arm any pair whose env still names a dead unit.
       - Record the finding (file paths, before/after WANCTL_UNIT, chosen remediation) in SUMMARY. BLOCKING success condition (HIGH-4).
    4. A1 live enablement reads (operator-only, on `cake-shaper`):
       `systemctl is-enabled silicom-bypass-watchdog@spectrum.service ; systemctl is-active silicom-bypass-watchdog@spectrum.service`
       `systemctl is-enabled silicom-bypass-watchdog-cake-autorate-att.service ; systemctl is-active silicom-bypass-watchdog-cake-autorate-att.service`
       `systemctl is-enabled silicom-bypass-watchdog@att.service ; systemctl is-active silicom-bypass-watchdog@att.service`
       `sudo cat /etc/wanctl/bpctl-watchdog/spectrum.env /etc/wanctl/bpctl-watchdog/att.env`
       Confirm the retired ATT variant and the folded `@att` are not BOTH active (HIGH-5 live — the CLI guard + this check are the sole double-petter mechanism now that N2 removed Conflicts=). If both are active, retire the variant SENTINEL-FIRST + ExecStop-MASKED per step 1.
    5. Any live apply (retirement, env migration, daemon-reload, re-enable, arm) is a separate explicit operator action. Rollback for any live arm = `silicom-bypass disarm <pair>` (clean inline restore) + restore prior `.env`.
  </how-to-verify>
  <verify>
    <human-check>Operator retires the ATT variant SENTINEL-FIRST + ExecStop-MASKED (att-modem.disarm sentinel written ROOT-CORRECT via `sudo sh -c ': > ...'` and VERIFIED present, ExecStop= blank-reset drop-in installed + daemon-reload, BEFORE any stop; ordering precondition confirmed; post-retirement att-modem inline, NOT bypassed) and records it; git diff scripts/phase231-rollback.sh shows only watchdog-ref + mode-aware env-rewrite/disarm/restart changes; bash -n passes; `-k rollback_order`, `-k invariant`, and `-k retire_nobypass` tests green; operator confirms the per-wan running-petter-repoint command ORDER for BOTH wans (env-rewrite-before-sentinel-clean-restart OR sentinel-clean-disarm-before-cake-stop; env-rewrite-only and raw disable --now rejected); operator runs the HIGH-4 stale-env detection and records the finding + remediation; operator records the A1 live-state + HIGH-5 not-both-active finding</human-check>
  </verify>
  <resume-signal>Type "approved" once the ATT variant is retired SENTINEL-FIRST + ExecStop-MASKED (root-correct sentinel verified present AND ExecStop-mask drop-in installed before any stop, att-modem confirmed inline) AND the rollback diff is accepted AND the per-wan running-petter-repoint command order is confirmed for both wans AND the HIGH-4 stale-env finding (fixed or recorded as must-remain-disarmed) plus the A1/HIGH-5 live-state finding are recorded in SUMMARY; or describe required changes.</resume-signal>
  <acceptance_criteria>
    - Operator retired `silicom-bypass-watchdog-cake-autorate-att.service` SENTINEL-FIRST + ExecStop-MASKED: the att-modem `.disarm` sentinel was written ROOT-CORRECT (`sudo sh -c ': > ...'` or `sudo tee`, NOT `sudo : > ...`) and VERIFIED to exist (`sudo test -f`) BEFORE any stop; an `ExecStop=` blank-reset drop-in for the retired unit was installed + daemon-reload BEFORE the stop; the ordering precondition (Plan 01 sentinel-aware script installed) was confirmed; post-retirement att-modem is INLINE not bypassed; a bare unsentineled+unmasked `disable --now` of the retired unit was NOT run (HIGH-3-NEW / N1)
    - `git diff scripts/phase231-rollback.sh` shows ONLY watchdog-unit reference changes and the mode-aware env-rewrite / sentinel-clean-disarm / restart lines — no tc/qdisc/health-URL/mutation-gate edits
    - Operator has confirmed, for BOTH wans, the command ORDER RE-POINTS a running petter: env-rewrite-to-native strictly precedes a sentinel-clean restart (or sentinel-clean disarm strictly precedes the cake-stop); env-rewrite + daemon-reload alone, "leave untouched" for an active @spectrum, and raw `disable --now` of a watchdog are all rejected (HIGH-2-NEW/HIGH-3/HIGH-C/HIGH-4) — the `-k rollback_order`, `-k invariant`, and `-k retire_nobypass` tests are green
    - Operator has run the HIGH-4 detection `grep -rn 'WANCTL_UNIT=wanctl@' /etc/wanctl/bpctl-watchdog/` and recorded the result; any stale env on an arm-intended pair is migrated (under approval) or recorded as "must remain disarmed" (HIGH-4 blocking)
    - Operator has recorded the A1 live enablement finding and confirmed the retired ATT variant and `@att` are not both active (HIGH-5 live; CLI guard + this check are the sole mechanism after N2)
    - No executor step ran `systemctl enable/start/disable/mask`, `silicom-bypass arm/disarm`, daemon-reload, or any live `.env` mutation on cake-shaper
  </acceptance_criteria>
  <done>ATT variant retired SENTINEL-FIRST + ExecStop-MASKED (root-correct sentinel verified present + ExecStop-mask drop-in before stop, att-modem inline — HIGH-3-NEW / N1); HIGH-4 live-env migration gate enforced; per-wan running-petter-repoint rollback confirmed for both wans (HIGH-2-NEW/HIGH-3/HIGH-C, no env-rewrite-only or raw-disable pass); A1 + HIGH-5 live-state recorded; no live mutation by the executor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [BLOCKING]: SAFE-16 controller-path zero-diff gate + MED-5 companion full-tree gate</name>
  <files>.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json, .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/src-wanctl-tree-236.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the turnkey checker; FIXED protected file list at lines 68-74)
    - 236-RESEARCH.md (Don't Hand-Roll SAFE-16 row + Code Examples boundary proof)
    - 236-PATTERNS.md (SAFE-16 Shared Pattern)
    - 236-REVIEWS.md (MED-5 — checker protects a FIXED list, not the whole src/wanctl/ tree; add a companion gate enumerating all tracked/staged/dirty/untracked files under src/wanctl/ vs v1.51)
    - .planning/REQUIREMENTS.md (SAFE-16 wording — the fixed controller list IS the intended scope)
  </read_first>
  <action>
    Run the turnkey controller-path zero-diff checker at the phase gate (do NOT hand-roll a git-diff). Invoke scripts/phase225-safe13-boundary-check.sh with --anchor v1.51 and --out evidence/safe16-boundary-236.json, then assert the emitted JSON has passed=true and controller_path_diff_count=0.
    MED-5 companion gate: SAFE-16's normative scope is the FIXED controller-file list (confirmed against REQUIREMENTS.md SAFE-16 wording), but the reviewer asked for defense-in-depth over the WHOLE tree. Emit a companion evidence file `evidence/src-wanctl-tree-236.json` enumerating ALL files under `src/wanctl/` that differ from anchor v1.51, computed as the union of: `git diff --name-only v1.51 -- src/wanctl/`, `git diff --name-only --cached -- src/wanctl/`, `git diff --name-only -- src/wanctl/` (dirty), and `git ls-files --others --exclude-standard -- src/wanctl/` (untracked). Record the de-duplicated list and `src_wanctl_changed_count`. The phase boundary passes when the fixed-list `controller_path_diff_count=0` AND `src_wanctl_changed_count=0`. If the companion count is non-zero but the fixed list is clean, surface the offending files for operator review — a non-controller `src/wanctl/` file is unexpected for this units/scripts/docs/tests phase and must be explained or reverted before approval.
    Also run the hot-path regression slice to confirm no control-path regression (full-suite-green is NOT the gate per [233-04]; SAFE-16 passing is).
  </action>
  <what-built>
    Two boundary evidence files at anchor `v1.51`: the turnkey SAFE-16 checker output `evidence/safe16-boundary-236.json` (fixed controller list) and a companion `evidence/src-wanctl-tree-236.json` enumerating any change anywhere under `src/wanctl/` (tracked/staged/dirty/untracked). Together they prove the phase touched only units/scripts/docs/tests and the one scoped failure-behavior exception — never the controller path, and nothing unexpected elsewhere in the tree.
  </what-built>
  <how-to-verify>
    Run:
    `.venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json`
    Confirm the emitted JSON has `passed=true` and `controller_path_diff_count=0`.
    Then emit and inspect the companion full-tree evidence: the de-duplicated union of staged/dirty/committed/untracked changes under `src/wanctl/` vs v1.51 must be empty (`src_wanctl_changed_count=0`). If either gate is non-zero, a `src/wanctl/` file was touched — stop and revert before approving.
    Also run the hot-path regression slice (CLAUDE.md) to confirm no control-path regression. (Known Phase 220/221 historical boundary-test failures are pre-existing per STATE.md decision [233-04]; SAFE-16 passing is the gate, not full-suite-green.)
  </how-to-verify>
  <verify>
    <automated>mkdir -p .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence && .venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json && python3 - <<'PY'
import json, subprocess

ev = '.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence'
d = json.load(open(f'{ev}/safe16-boundary-236.json'))
assert d.get('passed') is True, d
assert d.get('controller_path_diff_count', 1) == 0, d
print('SAFE-16 OK')

def names(args):
    out = subprocess.run(['git', *args], capture_output=True, text=True).stdout
    return {x for x in out.split() if x}

changed = set()
changed |= names(['diff', '--name-only', 'v1.51', '--', 'src/wanctl/'])
changed |= names(['diff', '--name-only', '--cached', '--', 'src/wanctl/'])
changed |= names(['diff', '--name-only', '--', 'src/wanctl/'])
changed |= names(['ls-files', '--others', '--exclude-standard', '--', 'src/wanctl/'])
json.dump(
    {'anchor': 'v1.51', 'src_wanctl_changed_files': sorted(changed),
     'src_wanctl_changed_count': len(changed)},
    open(f'{ev}/src-wanctl-tree-236.json', 'w'), indent=2)
assert len(changed) == 0, f'MED-5 companion gate: src/wanctl/ changed: {sorted(changed)}'
print('MED-5 companion gate OK: src_wanctl_changed_count=0')
PY</automated>
  </verify>
  <resume-signal>Type "approved" once SAFE-16 evidence shows passed=true, controller_path_diff_count=0, AND the companion src-wanctl-tree evidence shows src_wanctl_changed_count=0.</resume-signal>
  <acceptance_criteria>
    - `evidence/safe16-boundary-236.json` exists with `passed: true` and `controller_path_diff_count` == 0
    - `evidence/src-wanctl-tree-236.json` exists with `src_wanctl_changed_count` == 0 (MED-5 companion full-tree gate: tracked/staged/dirty/untracked union vs v1.51)
    - No file under `src/wanctl/` (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion) appears in the boundary diff
    - hot-path regression slice green (control path untouched)
  </acceptance_criteria>
  <done>SAFE-16 boundary evidence emitted with passed=true, controller_path_diff_count=0, AND the MED-5 companion gate confirms src_wanctl_changed_count=0; the scoped failure-behavior exception confirmed to not touch controller logic or anything else under src/wanctl/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → live host | proof must NOT touch cake-shaper; live arming/`.env` migration/daemon-reload/retirement are operator checkpoints |
| script → fail-open watchdog unit | ALL watchdog stops/disables/masks in rollback/retirement MUST be the sentinel-clean `silicom-bypass disarm <pair>` verb (W-INV) — raw `systemctl disable --now`/`mask --now silicom-bypass-watchdog...` is unsentineled and fires the unit's fail-open ExecStop |
| live env dir → petter | stale `WANCTL_UNIT=wanctl@...` surviving install-if-absent is the HIGH-4 spurious-bypass surface; a running petter reads it ONCE at start (re-point requires restart/disarm — HIGH-2-NEW) |
| rollback mode → running petter | native rollback that only rewrites `%i.env` + daemon-reload does NOT re-point a RUNNING petter watching the cake unit (HIGH-2-NEW); the safe ordering (env-rewrite-before-sentinel-clean-restart OR sentinel-clean-disarm-before-cake-stop) must be provable per wan (HIGH-3/HIGH-C) |
| retired-variant retirement → live att-modem | the retired unit's shared ExecStop fires `set_bypass on` unless the att-modem sentinel is written ROOT-CORRECT first AND the ExecStop is masked (the `disarm att-modem` verb targets `@att`, not this differently-named unit) (HIGH-3-NEW / N1) |
| host lifecycle → ExecStop sentinel | the `/run/wanctl/bpctl-watchdog/` sentinel dir is tmpfs, wiped on boot; host shutdown/reboot fires every armed watchdog's ExecStop set_bypass on — INTENDED fail-open (N4), documented and accepted, NOT a mitigated residual |
| phase boundary → controller path | SAFE-16: no `src/wanctl` source diff may cross the gate (fixed list + companion full-tree) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-236-02 | Denial of Service | executor/test arms a live relay during the expiry proof | mitigate | Task 1 proof is pure-logic over the petter loop body via fake bpctl + fake systemctl; no real timer, no live card, BPCTL_UTIL never set to the real binary |
| T-236-11 | Denial of Service | native rollback leaves a RUNNING petter watching a dead cake unit — either by "leaving @spectrum untouched" or by rewriting the env + daemon-reload only (which does NOT re-point a running petter) (HIGH-3/HIGH-C/HIGH-2-NEW) | mitigate | Task 3 reconciles rollback BY MODE with a PER-WAN RUNNING-PETTER-REPOINT command-order invariant — for att AND spectrum, env-rewrite-to-native precedes a sentinel-clean RESTART (re-execs the petter) OR sentinel-clean disarm precedes the cake-stop, proven by the offline `-k rollback_order` line-index test (env-rewrite-only FAILS); Task 4 operator confirms the command order re-points a running petter for both wans |
| T-236-12 | Denial of Service / Integrity | stale live `WANCTL_UNIT=wanctl@...` env survives install-if-absent and fires bypass when armed (HIGH-4) | mitigate | Task 4 operator-gated detection gate is a BLOCKING success condition: stale env migrated or pair recorded must-remain-disarmed before any arm; complemented by the Plan 01 `cmd_arm` env preflight |
| T-236-17 | Denial of Service | retiring the ATT variant via a raw unsentineled `disable --now` (or via a broken `sudo : > ...` sentinel write that never creates the sentinel) fires its shared fail-open ExecStop `set_bypass on` on live att-modem (HIGH-3-NEW / N1) | mitigate | Task 4 retirement is SENTINEL-FIRST + ExecStop-MASKED: (1) the att-modem `.disarm` sentinel is written ROOT-CORRECT via `sudo sh -c ': > ...'` / `sudo tee` (NOT `sudo : > ...` which runs the redirect as the non-root caller → EACCES) and VERIFIED present (`sudo test -f`) BEFORE any stop; (2) an `ExecStop=` blank-reset systemd drop-in + daemon-reload masks the retired unit's ExecStop before the stop (belt-and-suspenders); (3) THEN `disable --now`; ordering precondition (Plan 01 sentinel-aware script installed first); operator-gated, no executor live mutation; offline `-k retire_nobypass` test proves no `set_bypass on` (and the non-vacuity counter-case); post-retirement att-modem confirmed inline |
| T-236-18 | Tampering | a rollback/soak watchdog stop uses a raw `systemctl disable --now`/`mask --now silicom-bypass-watchdog...` (unsentineled) — the recurring fail-open class | mitigate | Task 3 converts all watchdog stops to the sentinel-clean `silicom-bypass disarm <pair>` verb; the Plan 01 `-k invariant` static global-compliance test (verb set includes mask, N3) re-runs over the reconciled phase231-rollback.sh + soak-monitor.sh and FAILS on any raw unsentineled watchdog stop/mask (W-INV cross-plan gate) |
| T-236-10 | Denial of Service | retired ATT variant active alongside `@att` (double-petter) at live verification | mitigate | N2: there is no unit `Conflicts=`; Task 4 operator confirms not-both-active and retires the variant SENTINEL-FIRST + ExecStop-MASKED if needed (the CLI arm-time guard + this operator check are the sole double-petter mechanism) |
| T-236-19 | Denial of Service | host shutdown/reboot stops every armed watchdog with the tmpfs `/run` sentinel wiped, firing `set_bypass on` on each armed pair (HIGH-N4) | accept (intended) | N4 OPERATOR DECISION: this is the INTENDED fail-open — host down → relay drops to raw ISP passthrough keeps the internet up. Documented-and-accepted, NOT a mitigated residual: Task 2 adds a greppable `docs/SILICOM-BYPASS.md` subsection "Shutdown / boot fail-open is intended" (operators wanting CAKE preserved across a planned reboot may pre-disarm; the default lifecycle fail-open is correct). NO persistent-sentinel, NO systemd-lifecycle-ordering, NO startup-grace, NO pre-reboot-disarm-enforcement mechanism is added (explicitly out of scope) |
| T-236-06 | Tampering | a careless phase231-rollback.sh edit changes live-rollback tc/qdisc/mutation-gate behavior | mitigate | Task 3 changes only watchdog refs + mode-aware env/disarm/restart lines; Task 4 is `autonomous: false` operator-review; acceptance asserts no tc/qdisc/health/gate edits; `bash -n` syntax gate |
| T-236-05 | Tampering | controller-path source change smuggled into this docs/tests/rollback plan | mitigate | Task 5 [BLOCKING] SAFE-16 boundary check (passed=true + controller_path_diff_count=0) AND MED-5 companion full-tree gate (src_wanctl_changed_count=0) |
| T-236-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k 'petter_expiry or manual_reapply or rollback_order or invariant or retire_nobypass' -q` exits 0.
- W-INV cross-plan gate: `-k invariant` (verb set includes mask, N3) passes over the RECONCILED phase231-rollback.sh + soak-monitor.sh (no raw `systemctl (stop|disable|restart|mask) ... silicom-bypass-watchdog`; all watchdog stops are the sentinel-clean `silicom-bypass disarm` verb).
- N1: `-k retire_nobypass` proves the sentinel-first + ExecStop-masked retirement produces no `set_bypass on` (with a non-vacuous counter-case).
- N4: `grep -qi 'fail-open is intended' docs/SILICOM-BYPASS.md` succeeds AND the threat model marks T-236-19 accepted/intended.
- `.venv/bin/pytest tests/ -q` plus the hot-path regression slice — control path unchanged (known 220/221 historical failures excepted per [233-04]).
- SAFE-16: `evidence/safe16-boundary-236.json` passed=true, controller_path_diff_count=0; companion `evidence/src-wanctl-tree-236.json` src_wanctl_changed_count=0.
- HIGH-2-NEW/HIGH-3/HIGH-C enforced offline by `-k rollback_order` (per-wan running-petter-repoint, env-rewrite-only and raw-disable rejected) AND at the operator checkpoint; HIGH-3-NEW/N1 sentinel-first ExecStop-masked retirement confirmed by the operator (att-modem inline); HIGH-4 stale-env detection run and recorded.
</verification>

<success_criteria>
- W-INV holds across the rollback + soak surfaces: every watchdog stop/disable/mask is the sentinel-clean `silicom-bypass disarm <pair>` verb; the `-k invariant` static global-compliance gate (mask in verb set, N3) passes over the reconciled phase231-rollback.sh + soak-monitor.sh.
- Heartbeat-death → relay-bypass and heartbeat-alive → pet/inline proven offline as software behavior (not hardware relay expiry — MED-3) via the petter-body shim (WDOG-02).
- 2026-06-08 ATT migration failure mode documented as understood and covered; arm/disarm usage + HIGH-1/HIGH-2/HIGH-A/HIGH-1-NEW semantics + the W-INV sanctioned-disarm rule (incl. no raw mask) + HIGH-3/HIGH-C/HIGH-2-NEW running-petter-repoint invariant + HIGH-3-NEW/N1 sentinel-first ExecStop-masked retirement + N4 "Shutdown / boot fail-open is intended" + hardware quirk recorded (WDOG-02).
- phase231-rollback.sh reconciled BY MODE with a PER-WAN RUNNING-PETTER-REPOINT command-order invariant proven offline (env-rewrite-before-sentinel-clean-restart OR sentinel-clean-disarm-before-cake-stop for att AND spectrum; env-rewrite-only and raw disable --now both FAIL; @spectrum cannot be "left untouched" — HIGH-2-NEW/HIGH-3/HIGH-C/HIGH-4) and soak-monitor stale ref swept (MED-2) under operator review; the ATT variant retired SENTINEL-FIRST + ExecStop-MASKED with an offline no-bypass proof (HIGH-3-NEW / N1); HIGH-4 live-env migration gate enforced; A1 + HIGH-5 live-state recorded; no executor live mutation.
- N4 shutdown/boot fail-open documented as intended (greppable) AND threat model T-236-19 marked accepted/intended — NOT a residual; no lifecycle-enforcement mechanism added.
- SAFE-16 boundary holds: passed=true, controller_path_diff_count=0; MED-5 companion full-tree gate src_wanctl_changed_count=0; scoped failure-behavior exception confirmed to not touch `src/wanctl` (SAFE-16).
</success_criteria>

<output>
Create `.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-02-SUMMARY.md` when done.
</output>
</content>
