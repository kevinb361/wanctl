---
phase: 236-watchdog-fail-open-two-mode-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["236-01"]
files_modified:
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
  - scripts/phase231-rollback.sh
  - scripts/soak-monitor.sh
autonomous: false
requirements: [WDOG-02, SAFE-16]
user_setup: []

must_haves:
  truths:
    - "A non-destructive test proves: watched-unit inactive → petter calls set_bypass on and withholds reset_bypass_wd; watched-unit active → petter pets and restores inline (software behavior, NOT hardware relay expiry)"
    - "The 2026-06-08 ATT migration failure mode is documented in docs/SILICOM-BYPASS.md as understood and covered by the reconciled units"
    - "A REQUIRED live-env migration gate detects any stale WANCTL_UNIT=wanctl@... in /etc/wanctl/bpctl-watchdog/*.env and either fixes it under operator approval or records 'watchdog must remain disarmed' BEFORE any arm (HIGH-4)"
    - "phase231-rollback.sh reconciles watchdog state BY MODE, and for EACH wan (att AND spectrum) a COMMAND-ORDER assertion proves one of two invariants holds: env-rewrite-to-native (WANCTL_UNIT=wanctl@<wan>.service + daemon-reload) executes BEFORE any enable/start/restart of silicom-bypass-watchdog@<wan>, OR a clean disarm (sentinel-aware stop / disabled) executes BEFORE the cake unit is stopped. 'Leave untouched' is NOT acceptable for spectrum — an active @spectrum must be env-rewritten-before-arm OR clean-disarmed-before-cake-stop, verified by command order, not narrative (HIGH-3 / HIGH-C)"
    - "Stale silicom-bypass-watchdog-cake-autorate-att references in scripts/soak-monitor.sh are reconciled to the folded @att path (MED-2)"
    - "SAFE-16 controller-path zero-diff holds at the phase boundary (passed=true, controller_path_diff_count=0) AND a companion gate confirms no other tracked/staged/dirty/untracked file under src/wanctl/ changed vs v1.51 (MED-5)"
  artifacts:
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "petter-body heartbeat-death → relay-bypass software-behavior shim proof"
      contains: "petter_expiry"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "2026-06-08 ATT migration RCA + arm/disarm usage + native-rollback env-rewrite note"
      contains: "2026-06-08"
    - path: ".planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json"
      provides: "SAFE-16 boundary evidence (fixed controller list)"
      contains: "passed"
    - path: ".planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/src-wanctl-tree-236.json"
      provides: "MED-5 companion gate: full src/wanctl/ tree diff vs v1.51"
      contains: "src_wanctl_changed_count"
  key_links:
    - from: "tests/test_silicom_bypass_cli.py petter_expiry"
      to: "scripts/wanctl-bpctl-watchdog-petter loop body"
      via: "fake systemctl returns inactive → assert set_bypass on + no reset_bypass_wd"
      pattern: "set_bypass on"
    - from: "scripts/phase231-rollback.sh native rollback (per wan)"
      to: "/etc/wanctl/bpctl-watchdog/<wan>.env WANCTL_UNIT"
      via: "command-order proof: env-rewrite-to-native BEFORE arm, OR clean-disarm BEFORE cake-stop (line index of rewrite/disarm < line index of cake-stop/arm), for att AND spectrum"
      pattern: "WANCTL_UNIT=wanctl@"
    - from: "scripts/phase225-safe13-boundary-check.sh"
      to: "evidence/safe16-boundary-236.json"
      via: "--anchor v1.51 --out"
      pattern: "passed.*true"
---

<objective>
Prove heartbeat-death → relay-bypass non-destructively (as software behavior, not hardware relay expiry), document the 2026-06-08 ATT migration failure mode as understood and covered, reconcile rollback/native-mode behavior BY MODE under operator review with a COMMAND-ORDER proof for both WANs, add a required live-env migration gate, sweep stale ATT-variant references, and close the SAFE-16 phase boundary with a companion full-tree gate (WDOG-02, SAFE-16), incorporating the HIGH-3/HIGH-C and HIGH-4 review findings and MED-2/MED-3/MED-5.

Purpose: Plan 01 reconciled the units and wired SAFE arm/disarm. This plan proves the reconciled fail-open path behaves correctly using the offline petter-body shim (no live arming), records the migration RCA, makes stale-env cleanup and mode-aware rollback BLOCKING conditions before any live arm (HIGH-3/HIGH-C, HIGH-4), sweeps the remaining stale references (MED-2), and runs the turnkey SAFE-16 boundary checker plus a companion gate over the whole `src/wanctl/` tree (MED-5). The HIGH-3 rollback acceptance is now COMMAND-ORDER based for EACH wan — no narrative-only pass; "leave untouched" is no longer acceptable for an active @spectrum.

Output: `petter_expiry` behavior tests (fake bpctl + fake systemctl); a 2026-06-08 RCA + arm/disarm + native-rollback-env-rewrite section in docs/SILICOM-BYPASS.md; mode-aware reconciliation of phase231-rollback.sh with a per-wan command-order invariant and a required live-env migration gate; soak-monitor reference sweep; SAFE-16 boundary evidence JSON (passed=true) plus a companion full-tree evidence JSON.

This plan is non-autonomous: it contains operator-gated checkpoints for the live-env migration gate (HIGH-4 / research A1) and the conservative mode-aware rollback-script edit (HIGH-3/HIGH-C / CLAUDE.md network/prod policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-RESEARCH.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-PATTERNS.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-VALIDATION.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-REVIEWS.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; extracted from codebase. -->

Petter loop body (scripts/wanctl-bpctl-watchdog-petter, SYSTEMCTL-seamed by Plan 01 Task 4):
  if "$SYSTEMCTL" is-active --quiet "$WANCTL_UNIT"; then
      # active branch: on transition util set_disc off; util set_bypass off (inline restore); then util reset_bypass_wd (PET)
  else
      # inactive branch: on transition util set_bypass on; WITHHOLD reset_bypass_wd (EXPIRY)
  fi
  arm sequence (lines 18-22): set_wd_exp_mode bypass; set_wd_autoreset 0; set_dis_bypass off; set_disc off; set_bypass_wd $TIMEOUT_MS

Test harness (Plan 01 added these):
  _fake_bpctl watchdog verbs: set_wd_exp_mode/set_wd_autoreset/set_bypass_wd/reset_bypass_wd/get_bypass_wd/get_wd_exp_mode
  _fake_systemctl(tmp_path): PATH-injected systemctl; rc from FAKE_SYSTEMCTL_RC; argv logged to systemctl.log; per-unit is-active state via SANITIZED key
  _calls_for(calls, iface): filters the bpctl call log per iface for asserting set_bypass on / absence of reset_bypass_wd

SAFE-16 boundary checker (scripts/phase225-safe13-boundary-check.sh):
  .venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out <evidence.json>
  Protected files (FIXED controller list): src/wanctl/wan_controller.py, wan_controller_state.py, queue_controller.py,
    cake_signal.py, alert_engine.py, fusion_healer.py, backends/
  Expect: passed=true, controller_path_diff_count=0. Anchor tag v1.51 exists.
  MED-5: this checker is a FIXED list, not the whole src/wanctl/ tree — Task 4 adds a companion gate over the full tree.

phase231-rollback.sh references to the RETIRED ATT variant + native-mode watchdog state:
  line 73  : disable --now ... silicom-bypass-watchdog-cake-autorate-att.service
  line 100 : (att return) enable --now ... silicom-bypass-watchdog@att.service
  line 101 : (att return) enable --now ... silicom-bypass-watchdog-cake-autorate-att.service
  line 109 : (spectrum) comment "silicom-bypass-watchdog@spectrum.service stays active in both modes"
  lines 120-122: print_plan watchdog-state narrative (per-mode expectations)
  line 233 : run_check "att_cake_watchdog_active" ... silicom-bypass-watchdog-cake-autorate-att.service
  line 234 : run_check "att_native_watchdog_inactive" ... silicom-bypass-watchdog@att.service
  line 237 : run_check "att_watchdog_env_present" ... /etc/wanctl/bpctl-watchdog/att.env
  HIGH-3: a native rollback (wanctl@<wan>) while %i.env still names cake-autorate-<wan>.service makes the petter watch a DEAD unit → bypass.
  HIGH-C: acceptance must be COMMAND-ORDER, not narrative. For EACH wan, prove env-rewrite-to-native executes BEFORE arm,
          OR a clean disarm executes BEFORE the cake unit is stopped. "Leave untouched" is NOT acceptable for an active @spectrum.

soak-monitor.sh stale ref (MED-2):
  line 286: att) echo "... silicom-bypass-watchdog-cake-autorate-att.service" ;;  -> folded @att path

docs/SILICOM-BYPASS.md anchors:
  ## Per-WAN Watchdog Fail-Open (line 250); env path block (265-266)
  hardware quirk: requested 5000ms rounds up to 6400ms (lines 289-292) — arm verb must not promise exact ms
  ### Spectrum Migration Validation And Pitfalls (line 435)
  doc-test coupling: test_init_service_manual_reapply_docs_match_remain_after_exit (test file 489-504) — keep procedure blocks test-consistent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-destructive heartbeat-death → relay-bypass petter-body software-behavior proof</name>
  <files>tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - tests/test_silicom_bypass_cli.py (file being modified; _fake_bpctl watchdog verbs + _fake_systemctl from Plan 01; _calls_for 190-195; _run 198-221)
    - scripts/wanctl-bpctl-watchdog-petter (the SYSTEMCTL-seamed loop body — the test target; active/inactive branches at lines 28-42)
    - 236-RESEARCH.md (Code Examples expiry proof — "pure-logic test of the petter loop body, not a wall-clock wait")
    - 236-PATTERNS.md (petter section 183-209 — loop body IS the WDOG-02 test target; assertion spec at 206-209)
    - 236-VALIDATION.md (WDOG-02 row + petter_expiry selector, line 44)
    - 236-REVIEWS.md (MED-3 — do NOT overstate as "relay fires"; this proves software behavior: set_bypass on + withheld reset_bypass_wd)
  </read_first>
  <action>
    Add a `petter_expiry` test group proving the reconciled fail-open SOFTWARE behavior offline — no wall-clock wait, no live card, no real timer. Drive the petter with `SYSTEMCTL` set to the `_fake_systemctl` from Plan 01 and `BPCTL_UTIL` set to the `_fake_bpctl`, with `IFACE` and `WANCTL_UNIT` set via env. To avoid the petter's infinite `while :;` loop, run it with a controllable iteration bound — set `HEARTBEAT_SECONDS` small and run the petter under a short `timeout` so it executes at least one full branch then is killed, OR (preferred) factor the test to invoke the loop body once. Assert from the bpctl call log via `_calls_for`:
    (a) Inactive proof (FAKE_SYSTEMCTL_RC=1 → controller dead): the bpctl calls for the iface include `set_bypass on` and do NOT include `reset_bypass_wd` (pet withheld) — this is the fail-open firing in software.
    (b) Active proof (FAKE_SYSTEMCTL_RC=0 → controller alive): the bpctl calls include `reset_bypass_wd` (pet) and, on the first transition, `set_disc off` + `set_bypass off` (restore inline), and do NOT include `set_bypass on`.
    Also assert the arm sequence ran on startup: `set_wd_exp_mode bypass`, `set_wd_autoreset 0`, `set_bypass_wd <ms>` appear in the call log. Add a docstring/comment on the test group stating explicitly that this proves SOFTWARE behavior (the petter emits `set_bypass on` and withholds `reset_bypass_wd`), NOT actual hardware relay expiry — hardware relay firing is only provable on live/HIL hardware (MED-3, deferred to Phase 237 HIL harness). NEVER set BPCTL_UTIL to `/opt/bpctl-silicom/bpctl_util` and NEVER call the real systemctl (Pitfall 2 — no live arming in any automated step).
  </action>
  <behavior>
    - petter_expiry_inactive: SYSTEMCTL is-active rc!=0 → bpctl log has `set_bypass on`, no `reset_bypass_wd`
    - petter_expiry_active: SYSTEMCTL is-active rc==0 → bpctl log has `reset_bypass_wd` + inline restore, no `set_bypass on`
    - petter_arm_sequence: startup writes set_wd_exp_mode bypass, set_wd_autoreset 0, set_bypass_wd <ms>
  </behavior>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q` exits 0
    - inactive-branch test asserts presence of `set_bypass on` AND absence of `reset_bypass_wd` in the iface call log
    - active-branch test asserts presence of `reset_bypass_wd` AND absence of `set_bypass on`
    - the test group carries an explicit comment/docstring that it proves SOFTWARE behavior, not hardware relay expiry (MED-3)
    - no test sets `BPCTL_UTIL` to a real path and no test calls a real `systemctl` (all via PATH/env fakes)
    - test completes in < 5s (no real timer wait)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>petter_expiry proves heartbeat-death → bypass and heartbeat-alive → pet/inline offline as software behavior (MED-3 caveat documented); WDOG-02 behavior demonstrated non-destructively.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 2026-06-08 ATT migration RCA + arm/disarm usage + native-rollback env-rewrite in docs/SILICOM-BYPASS.md</name>
  <files>docs/SILICOM-BYPASS.md</files>
  <read_first>
    - docs/SILICOM-BYPASS.md (file being modified; ## Per-WAN Watchdog Fail-Open line 250; env-path block 265-266; hardware quirk 289-292; ### Spectrum Migration Validation And Pitfalls line 435)
    - tests/test_silicom_bypass_cli.py (test_init_service_manual_reapply_docs_match_remain_after_exit 489-504 — doc-procedure/test coupling to keep consistent)
    - 236-RESEARCH.md (Pitfall 1 — the full 2026-06-08 failure mode reconstruction; hardware quirk + settled constraint)
    - 236-PATTERNS.md (docs section 308-321 — what to add; do not re-litigate unpowered fail-open)
    - 236-REVIEWS.md (HIGH-1 disarm; HIGH-2 timeout reaches the timer; HIGH-3/HIGH-C native-rollback env rewrite command-order; HIGH-A clean re-arm; MED-3 software-behavior wording)
  </read_first>
  <action>
    Add a dated RCA subsection under `## Per-WAN Watchdog Fail-Open` (e.g. `### 2026-06-08 ATT Migration Watchdog Failure Mode (RCA)`) capturing, as understood and now COVERED by the reconciled units:
    - What happened: the migration from `wanctl@<wan>` to cake-autorate (live 2026-06-08) left the watched-unit name `wanctl@<wan>.service` permanently inactive; an armed watchdog reading that name sees a healthy controller as dead → `set_bypass on` + withheld pet → relay fires bypass (raw ISP, CAKE shaping gone) within the ~6400ms timeout.
    - Why: the migration bolted on a one-off `silicom-bypass-watchdog-cake-autorate-att.service` for ATT but left Spectrum on the stale generic template pointing at the disabled `wanctl@spectrum.service` — an asymmetric two-unit reality.
    - The fix (this phase): one configurable template whose watched-unit name (`WANCTL_UNIT` in `%i.env`) names the live controller (`cake-autorate-<wan>.service`) for both pairs, with the mutual-exclusion guarantee against the retired ATT variant scoped to `@att` via an `@att.service.d` drop-in (not a template-wide directive). ATT variant retired, folded into `@att`.
    Add an `arm`/`disarm` usage block: `silicom-bypass arm <pair> [timeout]` (default from `WD_TIMEOUT_MS`) requires `--yes` because arming a live pair will bypass it on controller death; the timeout is a POSITIVE integer in ms and is written ATOMICALLY to the per-pair `%i.env` `TIMEOUT_MS` so the armed timer actually uses it (HIGH-2); re-arming an already-armed pair is CLEAN (no raw restart — it disarms-via-sentinel then re-starts, so a re-arm never drops the pair to bypass, HIGH-A); `silicom-bypass disarm <pair>` performs a CLEAN inline restore (never leaves bypass on, fail-safe even if systemctl disable fails) via the operator-disarm sentinel (HIGH-1). State the pair tokens are `att-modem`/`spec-modem` (mapped internally to the `@att`/`@spectrum` watchdog instances).
    Add a NATIVE-ROLLBACK note (HIGH-3/HIGH-C): if the operator rolls a WAN back from cake-autorate to native `wanctl@<wan>`, then for that WAN one of two orderings MUST hold and is enforced by phase231-rollback.sh as a command-order invariant: EITHER the watched-unit name in `%i.env` is rewritten to `WANCTL_UNIT=wanctl@<wan>.service` (and daemon-reload) BEFORE the watchdog is (re)armed, OR the watchdog is cleanly disarmed BEFORE the cake unit is stopped. "Leave the watchdog untouched" is NOT safe for an already-active `@spectrum` — it would watch the now-dead cake unit and fire bypass. Reference the reconciled phase231-rollback.sh as the canonical mode-aware procedure.
    Cite the hardware quirk: requested 5000ms rounds up to a 6400ms hardware timeout (fixed timer steps) — do NOT promise exact ms. State that the non-destructive proof demonstrates software behavior (petter emits `set_bypass on` + withholds pet), not a measured hardware relay expiry (MED-3). Do NOT re-litigate unpowered fail-open (settled). Keep any procedure/codeblock edits consistent with the doc-test coupling at test_silicom_bypass_cli.py:489-504.
  </action>
  <verify>
    <automated>grep -c '2026-06-08' docs/SILICOM-BYPASS.md && grep -c 'silicom-bypass arm' docs/SILICOM-BYPASS.md && grep -c 'wanctl@' docs/SILICOM-BYPASS.md && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SILICOM-BYPASS.md` contains a 2026-06-08 RCA referencing the watched-unit/cake-autorate failure mode and stating it is covered by the reconciled units
    - doc contains `silicom-bypass arm` and `silicom-bypass disarm` usage with the `--yes` live-arm rationale, the HIGH-2 timeout-reaches-timer note, the HIGH-A clean-re-arm note, and the HIGH-1 clean-disarm (never leaves bypass on, fail-safe) note
    - doc contains a native-rollback note stating the command-order invariant: `WANCTL_UNIT=wanctl@<wan>.service` env rewrite BEFORE re-arm, OR clean-disarm BEFORE cake-stop, and that "leave untouched" is not safe for an active @spectrum (HIGH-3/HIGH-C)
    - doc states the non-destructive proof shows software behavior, not hardware relay expiry (MED-3)
    - doc does NOT add any claim contradicting the settled unpowered-fail-open constraint
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q` exits 0 (doc-test coupling intact)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>The 2026-06-08 failure mode is documented as understood and covered; arm/disarm usage, the HIGH-1/HIGH-2/HIGH-A safety semantics, the HIGH-3/HIGH-C native-rollback command-order invariant, and the MED-3 software-behavior caveat are recorded; doc-test coupling stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Mode-aware phase231-rollback.sh reconciliation with a PER-WAN command-order invariant + soak-monitor reference sweep (offline; live mutation deferred to the operator checkpoint)</name>
  <files>scripts/phase231-rollback.sh, scripts/soak-monitor.sh, tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - scripts/phase231-rollback.sh (file being modified; ATT variant refs at lines 73, 100-101, 109, 120-122, 233-237; rollback_commands_for_wan/return_commands_for_wan; run_check predicates)
    - scripts/soak-monitor.sh (file being modified; stale att-variant ref at line 286)
    - tests/test_silicom_bypass_cli.py (add the command-order assertion test here — analog: existing static rollback-text asserts; _silicom_systemd_array_entries helper pattern for reading a script's text)
    - 236-RESEARCH.md (Risk flags for the planner; Open Q2; Runtime State Inventory + A1)
    - 236-PATTERNS.md (phase231-rollback.sh section 237-256 — CONSERVATIVE; "mark any edit here for explicit operator review")
    - 236-REVIEWS.md (HIGH-3/HIGH-C native-rollback re-bypass — command-order acceptance for BOTH wans; MED-2 stale soak-monitor refs)
  </read_first>
  <action>
    Conservative, mode-aware (CLAUDE.md network/prod: minimal, watchdog-references only). This task prepares the RENDERED/dry-run text and reference reconciliation; NO live host mutation runs here (the live apply is the operator checkpoint Task 4).
    (a) phase231-rollback.sh — reconcile the retired ATT variant references to the folded `silicom-bypass-watchdog@att` instance: update the disable line (73), the att return-to-cake enable lines (100-101 collapse to the folded `@att` instance), the print_plan watchdog narrative (120-122), and the `run_check` assertion names/targets (233-237) so ATT external mode is asserted via `silicom-bypass-watchdog@att` and `att.env` points at `cake-autorate-att.service`.
    (b) phase231-rollback.sh — HIGH-3/HIGH-C mode-aware watchdog reconciliation with a COMMAND-ORDER invariant for EACH wan (att AND spectrum). In the NATIVE rollback path for each wan (the `rollback_commands_for_wan` direction that returns the WAN to `wanctl@<wan>`), emit the commands so that ONE of these two orderings is provable by line position in the rendered/dry-run command sequence:
      * env-rewrite-precedes-arm: the line that sets `WANCTL_UNIT=wanctl@<wan>.service` in `/etc/wanctl/bpctl-watchdog/<wan>.env` (plus `daemon-reload`) appears STRICTLY BEFORE any `enable`/`start`/`restart` of `silicom-bypass-watchdog@<wan>`; OR
      * clean-disarm-precedes-cake-stop: a clean disarm of `silicom-bypass-watchdog@<wan>` (sentinel-aware stop / `disable --now` / `silicom-bypass disarm <pair>`) appears STRICTLY BEFORE the line that stops the cake unit `cake-autorate-<wan>.service`.
      For att (native rollback already enables `silicom-bypass-watchdog@att`), the env-rewrite-before-arm ordering is REQUIRED. For spectrum, "leave untouched" is NO LONGER acceptable: if `@spectrum` is active at rollback time, emit EITHER the env-rewrite-before-arm OR a clean-disarm-before-cake-stop — pick clean-disarm-before-cake-stop as the conservative default for spectrum (so spectrum is never left watching a dead cake unit), and add an explicit operator note that re-arming spectrum in native mode requires the env to name the native unit first. Change ONLY watchdog-unit references and add the mode-aware env-rewrite / clean-disarm lines — leave every tc/qdisc command, health URL, and the `--confirm/--i-have-operator-approval` mutation gate byte-for-byte unchanged.
    (c) Add a COMMAND-ORDER assertion (offline, not narrative) proving the invariant for BOTH wans. Render or dry-run the native rollback command sequence for each wan (e.g. invoke the rollback script's plan/dry-run mode that prints the command list without executing, or grep the relevant function body), capture the ordered command lines, and assert via a pytest test in tests/test_silicom_bypass_cli.py (selectable `-k rollback_order`): for att, the `WANCTL_UNIT=wanctl@att` (or daemon-reload) line index is strictly LESS than the `silicom-bypass-watchdog@att` enable/start/restart line index; for spectrum, EITHER the env-rewrite line index < the arm line index OR the disarm line index < the `cake-autorate-spectrum` stop line index. The test FAILS if neither ordering holds for a wan (this is the HIGH-C executable gate replacing the narrative-only acceptance).
    (d) soak-monitor.sh (MED-2) — update line 286 so the att watchdog reference is the folded `silicom-bypass-watchdog@att.service` (matching the retired-variant fold), so monitoring does not point at a dead unit.
    Run `bash -n scripts/phase231-rollback.sh` and `bash -n scripts/soak-monitor.sh` only; do NOT execute either script for live effect and do NOT run any systemctl/.env mutation against cake-shaper (a print/plan/dry-run render with no host mutation is allowed for the command-order capture). Defer the ATT variant file deletion and any live apply until after the operator approval in Task 4.
  </action>
  <verify>
    <automated>bash -n scripts/phase231-rollback.sh && bash -n scripts/soak-monitor.sh && grep -c 'silicom-bypass-watchdog-cake-autorate-att' scripts/soak-monitor.sh && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k rollback_order -q</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase231-rollback.sh` exits 0 and `bash -n scripts/soak-monitor.sh` exits 0
    - HIGH-3/HIGH-C: a pytest test `-k rollback_order` proves, by command-ORDER (line index), for EACH wan that env-rewrite-to-native precedes any arm OR clean-disarm precedes the cake-stop — the test FAILS on a narrative-only edit where neither ordering holds. For att the env-rewrite-before-arm ordering is asserted; for spectrum either ordering is accepted but "leave untouched" (no env-rewrite AND no disarm before cake-stop) FAILS.
    - phase231-rollback.sh att references route through `silicom-bypass-watchdog@att` (folded), not the retired `silicom-bypass-watchdog-cake-autorate-att.service`, except where the dry-run intentionally documents the retired unit for historical context
    - `grep -c 'silicom-bypass-watchdog-cake-autorate-att' scripts/soak-monitor.sh` returns 0 (MED-2 swept)
    - git diff for phase231-rollback.sh shows NO change to tc/qdisc commands, health URLs, or the mutation gate (verified at the Task 4 checkpoint)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>phase231-rollback.sh reconciles watchdog state by mode with a PER-WAN command-order invariant (env-rewrite-before-arm OR clean-disarm-before-cake-stop, proven by an offline line-index assertion, not narrative — HIGH-C); spectrum can no longer be "left untouched" while watching a dead cake unit; ATT routed through the folded @att; soak-monitor stale ref swept; offline-only, no live mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Operator-gated live-env migration gate (HIGH-4) + per-wan command-order rollback diff review (HIGH-C) + A1 live-state re-verification</name>
  <files>scripts/phase231-rollback.sh</files>
  <read_first>
    - scripts/phase231-rollback.sh (the mode-aware diff from Task 3 — operator review surface)
    - 236-RESEARCH.md (Runtime State Inventory + A1; Risk flags for the planner)
    - 236-PATTERNS.md (phase231-rollback.sh CONSERVATIVE section 237-256)
    - 236-VALIDATION.md (Manual-Only Verifications table, lines 64-70)
    - 236-REVIEWS.md (HIGH-3/HIGH-C native-rollback command-order; HIGH-4 stale live env must be a REQUIRED success condition before arming)
  </read_first>
  <action>
    Operator-gated, conservative. This checkpoint enforces the HIGH-4 live-env migration gate as a BLOCKING success condition and reviews the Task 3 mode-aware rollback diff with a PER-WAN command-order confirmation. The executor prepares the detection command and the proposed diff; the operator runs the live reads and decides remediation. No executor step runs `systemctl enable/start`, `silicom-bypass arm`, daemon-reload, or any `.env` mutation on cake-shaper.
    Surface to the operator: (1) the Task 3 `git diff scripts/phase231-rollback.sh` for review PLUS the rendered/dry-run native rollback command sequence for BOTH wans so the operator can confirm the command ORDER (env-rewrite/disarm strictly precedes cake-stop/arm); (2) the HIGH-4 stale-env detection command and the remediation decision; (3) the A1 live enablement reads. The phase MUST NOT be marked successful until the operator (a) confirms the per-wan command order for both wans, and (b) records the stale-env finding and either fixes it under approval or explicitly records "watchdog must remain disarmed."
  </action>
  <what-built>
    Claude has prepared (offline, no live host touched):
    - A mode-aware conservative reconciliation of `scripts/phase231-rollback.sh` (Task 3): ATT references routed to the folded `silicom-bypass-watchdog@att`; native rollback emits, for EACH wan, a command sequence where env-rewrite of `%i.env` `WANCTL_UNIT` to the native unit precedes any arm, OR a clean disarm precedes the cake-stop — proven by the `-k rollback_order` line-index test, so a native rollback can never leave the watchdog watching a dead cake unit, including an active @spectrum (HIGH-3/HIGH-C). No tc/qdisc/health/mutation-gate lines changed.
    - A HIGH-4 live-env migration gate: a detection command over the live env dir to find any stale `WANCTL_UNIT=wanctl@...` that would survive the install-if-absent deploy and cause spurious bypass if armed.
    This file carries LIVE-ROLLBACK semantics. No live host is touched by Claude. The live-env migration, the A1 live-state re-verification, and any live `.env`/daemon-reload/enable/arm are operator actions.
  </what-built>
  <how-to-verify>
    1. Review the proposed `scripts/phase231-rollback.sh` diff. Confirm: only watchdog-unit references and the mode-aware env-rewrite / clean-disarm lines changed; tc/qdisc/health/mutation-gate lines untouched. `git diff scripts/phase231-rollback.sh`. Then for BOTH wans (att AND spectrum), confirm the COMMAND ORDER in the rendered/dry-run native rollback sequence: the `WANCTL_UNIT=wanctl@<wan>.service` env-rewrite (+ daemon-reload) line appears STRICTLY BEFORE any `enable`/`start`/`restart` of `silicom-bypass-watchdog@<wan>`, OR a clean disarm line appears STRICTLY BEFORE the `cake-autorate-<wan>.service` stop line. "Leave untouched" for an active @spectrum is NOT acceptable (HIGH-3/HIGH-C). The `-k rollback_order` pytest already gates this; confirm it visually too.
    2. HIGH-4 stale-env migration gate (operator-only, on `cake-shaper`):
       Detection: `sudo grep -rn 'WANCTL_UNIT=wanctl@' /etc/wanctl/bpctl-watchdog/ ; echo "exit=$?"`
       - If it finds ANY stale `wanctl@...` line: that env file MUST be remediated before any arm — either `sudo sed -i 's#WANCTL_UNIT=wanctl@\\(.*\\)\\.service#WANCTL_UNIT=cake-autorate-\\1.service#' <file>` (under operator approval, matching current external mode) followed by `sudo systemctl daemon-reload`, OR record explicitly "watchdog for <pair> must remain DISARMED until env is migrated." Do NOT arm any pair whose env still names a dead unit.
       - Record the finding (file paths, before/after WANCTL_UNIT values, and the chosen remediation) in the plan SUMMARY. This is a BLOCKING success condition (HIGH-4): the phase is not complete while a stale `wanctl@...` env exists on a pair intended to be armed.
    3. A1 live enablement reads (operator-only, on `cake-shaper`):
       `systemctl is-enabled silicom-bypass-watchdog@spectrum.service ; systemctl is-active silicom-bypass-watchdog@spectrum.service`
       `systemctl is-enabled silicom-bypass-watchdog-cake-autorate-att.service ; systemctl is-active silicom-bypass-watchdog-cake-autorate-att.service`
       `systemctl is-enabled silicom-bypass-watchdog@att.service ; systemctl is-active silicom-bypass-watchdog@att.service`
       `sudo cat /etc/wanctl/bpctl-watchdog/spectrum.env /etc/wanctl/bpctl-watchdog/att.env`
       Confirm the retired ATT variant and the folded `@att` are not BOTH active (HIGH-5 live check). If both are active, disable the retired variant first (`sudo systemctl disable --now silicom-bypass-watchdog-cake-autorate-att.service`).
    4. Any live apply (env migration, daemon-reload, re-enable, arm) is a separate explicit operator action. Rollback for any live arm = `silicom-bypass disarm <pair>` (clean inline restore) + restore prior `.env`.
  </how-to-verify>
  <verify>
    <human-check>git diff scripts/phase231-rollback.sh shows only watchdog-ref + mode-aware env-rewrite/disarm changes; bash -n passes; the `-k rollback_order` test is green; operator confirms the per-wan command ORDER (env-rewrite/disarm strictly precedes cake-stop/arm) for BOTH wans; operator runs the HIGH-4 stale-env detection and records the finding + remediation (or "must remain disarmed") in SUMMARY; operator records the A1 live-state + HIGH-5 not-both-active finding</human-check>
  </verify>
  <resume-signal>Type "approved" once the rollback diff is accepted AND the per-wan command order is confirmed for both wans AND the HIGH-4 stale-env finding (fixed or recorded as must-remain-disarmed) plus the A1/HIGH-5 live-state finding are recorded in SUMMARY; or describe required changes.</resume-signal>
  <acceptance_criteria>
    - `git diff scripts/phase231-rollback.sh` shows ONLY watchdog-unit reference changes and the mode-aware env-rewrite / clean-disarm lines — no tc/qdisc/health-URL/mutation-gate edits
    - Operator has confirmed, for BOTH wans, the COMMAND ORDER in the rendered native rollback: env-rewrite-to-native (or clean-disarm) strictly precedes the arm (or cake-stop); "leave untouched" for an active @spectrum is rejected (HIGH-3/HIGH-C) — the `-k rollback_order` test is green
    - Operator has run the HIGH-4 detection `grep -rn 'WANCTL_UNIT=wanctl@' /etc/wanctl/bpctl-watchdog/` and recorded the result; any stale env on an arm-intended pair is either migrated (under approval) or recorded as "must remain disarmed" (HIGH-4 blocking condition)
    - Operator has recorded the A1 live enablement finding (enabled/active state of the spectrum + ATT watchdog units and the live `.env` WANCTL_UNIT values) and confirmed the retired ATT variant and `@att` are not both active (HIGH-5 live)
    - No executor step ran `systemctl enable/start`, `silicom-bypass arm`, daemon-reload, or any live `.env` mutation on cake-shaper
  </acceptance_criteria>
  <done>HIGH-4 live-env migration gate enforced (stale env fixed or recorded as must-remain-disarmed); per-wan command-order rollback confirmed for both wans (HIGH-3/HIGH-C, no narrative-only pass); A1 + HIGH-5 live-state finding recorded; no live mutation by the executor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [BLOCKING]: SAFE-16 controller-path zero-diff gate + MED-5 companion full-tree gate</name>
  <files>.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json, .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/src-wanctl-tree-236.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the turnkey checker; FIXED protected file list at lines 68-74)
    - 236-RESEARCH.md (Don't Hand-Roll SAFE-16 row + Code Examples boundary proof)
    - 236-PATTERNS.md (SAFE-16 Shared Pattern, lines 356-364)
    - 236-REVIEWS.md (MED-5 — checker protects a FIXED list, not the whole src/wanctl/ tree; add a companion gate enumerating all tracked/staged/dirty/untracked files under src/wanctl/ vs v1.51)
    - .planning/REQUIREMENTS.md (SAFE-16 wording — the fixed controller list IS the intended scope: wan_controller.py, queue_controller.py, cake_signal.py, backends, alert_engine.py, fusion)
  </read_first>
  <action>
    Run the turnkey controller-path zero-diff checker at the phase gate (do NOT hand-roll a git-diff). Invoke scripts/phase225-safe13-boundary-check.sh with --anchor v1.51 and --out evidence/safe16-boundary-236.json, then assert the emitted JSON has passed=true and controller_path_diff_count=0.
    MED-5 companion gate: SAFE-16's normative scope is the FIXED controller-file list (confirmed against REQUIREMENTS.md SAFE-16 wording), but the reviewer asked for defense-in-depth over the WHOLE tree. Emit a companion evidence file `evidence/src-wanctl-tree-236.json` enumerating ALL files under `src/wanctl/` that differ from anchor v1.51, computed as the union of: `git diff --name-only v1.51 -- src/wanctl/`, `git diff --name-only --cached -- src/wanctl/`, `git diff --name-only -- src/wanctl/` (dirty), and `git ls-files --others --exclude-standard -- src/wanctl/` (untracked). Record the de-duplicated list and `src_wanctl_changed_count`. The phase boundary passes when the fixed-list `controller_path_diff_count=0` (normative SAFE-16) AND `src_wanctl_changed_count=0` (companion defense-in-depth). If the companion count is non-zero but the fixed list is clean, surface the offending files for operator review — a non-controller `src/wanctl/` file (if any exists) is unexpected for this units/scripts/docs/tests phase and must be explained or reverted before approval.
    Also run the hot-path regression slice to confirm no control-path regression (full-suite-green is NOT the gate per [233-04]; SAFE-16 passing is).
  </action>
  <what-built>
    Two boundary evidence files at anchor `v1.51`: the turnkey SAFE-16 checker output `evidence/safe16-boundary-236.json` (fixed controller list) and a companion `evidence/src-wanctl-tree-236.json` enumerating any change anywhere under `src/wanctl/` (tracked/staged/dirty/untracked). Together they prove the phase touched only units/scripts/docs/tests and the one scoped failure-behavior exception — never the controller path, and nothing unexpected elsewhere in the tree.
  </what-built>
  <how-to-verify>
    Run:
    `.venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json`
    Confirm the emitted JSON has `passed=true` and `controller_path_diff_count=0`.
    Then emit and inspect the companion full-tree evidence: the de-duplicated union of staged/dirty/committed/untracked changes under `src/wanctl/` vs v1.51 must be empty (`src_wanctl_changed_count=0`). If either gate is non-zero, a `src/wanctl/` file was touched — stop and revert before approving.
    Also run the hot-path regression slice (CLAUDE.md) to confirm no control-path regression. (Known Phase 220/221 historical boundary-test failures are pre-existing per STATE.md decision [233-04]; SAFE-16 passing is the gate, not full-suite-green.)
  </how-to-verify>
  <verify>
    <automated>mkdir -p .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence && .venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json && python3 - <<'PY'
import json, subprocess

ev = '.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence'
d = json.load(open(f'{ev}/safe16-boundary-236.json'))
assert d.get('passed') is True, d
assert d.get('controller_path_diff_count', 1) == 0, d
print('SAFE-16 OK')

def names(args):
    out = subprocess.run(['git', *args], capture_output=True, text=True).stdout
    return {x for x in out.split() if x}

changed = set()
changed |= names(['diff', '--name-only', 'v1.51', '--', 'src/wanctl/'])
changed |= names(['diff', '--name-only', '--cached', '--', 'src/wanctl/'])
changed |= names(['diff', '--name-only', '--', 'src/wanctl/'])
changed |= names(['ls-files', '--others', '--exclude-standard', '--', 'src/wanctl/'])
json.dump(
    {'anchor': 'v1.51', 'src_wanctl_changed_files': sorted(changed),
     'src_wanctl_changed_count': len(changed)},
    open(f'{ev}/src-wanctl-tree-236.json', 'w'), indent=2)
assert len(changed) == 0, f'MED-5 companion gate: src/wanctl/ changed: {sorted(changed)}'
print('MED-5 companion gate OK: src_wanctl_changed_count=0')
PY</automated>
  </verify>
  <resume-signal>Type "approved" once SAFE-16 evidence shows passed=true, controller_path_diff_count=0, AND the companion src-wanctl-tree evidence shows src_wanctl_changed_count=0.</resume-signal>
  <acceptance_criteria>
    - `evidence/safe16-boundary-236.json` exists with `passed: true` and `controller_path_diff_count` == 0
    - `evidence/src-wanctl-tree-236.json` exists with `src_wanctl_changed_count` == 0 (MED-5 companion full-tree gate: tracked/staged/dirty/untracked union vs v1.51)
    - No file under `src/wanctl/` (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion) appears in the boundary diff
    - hot-path regression slice green (control path untouched)
  </acceptance_criteria>
  <done>SAFE-16 boundary evidence emitted with passed=true, controller_path_diff_count=0, AND the MED-5 companion gate confirms src_wanctl_changed_count=0; the scoped failure-behavior exception confirmed to not touch controller logic or anything else under src/wanctl/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → live host | proof must NOT touch cake-shaper; live arming/`.env` migration/daemon-reload are operator checkpoints |
| live env dir → petter | stale `WANCTL_UNIT=wanctl@...` surviving install-if-absent is the HIGH-4 spurious-bypass surface |
| rollback mode → watched unit | native rollback while `%i.env` names the cake unit is the HIGH-3 spurious-bypass surface; the safe ordering (env-rewrite-before-arm OR disarm-before-cake-stop) must be provable per wan (HIGH-C) |
| phase boundary → controller path | SAFE-16: no `src/wanctl` source diff may cross the gate (fixed list + companion full-tree) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-236-02 | Denial of Service | executor/test arms a live relay during the expiry proof | mitigate | Task 1 proof is pure-logic over the petter loop body via fake bpctl + fake systemctl; no real timer, no live card, BPCTL_UTIL never set to the real binary |
| T-236-11 | Denial of Service | native rollback leaves the watchdog watching a dead cake unit → spurious bypass, incl. an active @spectrum "left untouched" (HIGH-3/HIGH-C) | mitigate | Task 3 reconciles rollback BY MODE with a PER-WAN command-order invariant — for att AND spectrum, env-rewrite-to-native precedes any arm OR clean-disarm precedes the cake-stop, proven by the offline `-k rollback_order` line-index test (not narrative); Task 4 operator confirms the command order for both wans |
| T-236-12 | Denial of Service / Integrity | stale live `WANCTL_UNIT=wanctl@...` env survives install-if-absent and fires bypass when armed (HIGH-4) | mitigate | Task 4 operator-gated detection gate is a BLOCKING success condition: stale env must be migrated or the pair recorded as must-remain-disarmed before any arm; complemented by the Plan 01 `cmd_arm` env preflight (defense-in-depth) |
| T-236-10 | Denial of Service | retired ATT variant active alongside `@att` (double-petter) at live verification | mitigate | Task 4 operator confirms not-both-active and disables the retired variant if needed (complements the Plan 01 `@att` drop-in `Conflicts=` hard guarantee) |
| T-236-06 | Tampering | a careless phase231-rollback.sh edit changes live-rollback tc/qdisc/mutation-gate behavior | mitigate | Task 3 changes only watchdog refs + mode-aware env/disarm lines; Task 4 is `autonomous: false` operator-review; acceptance asserts no tc/qdisc/health/gate edits; `bash -n` syntax gate |
| T-236-05 | Tampering | controller-path source change smuggled into this docs/tests/rollback plan | mitigate | Task 5 [BLOCKING] SAFE-16 boundary check (passed=true + controller_path_diff_count=0) AND MED-5 companion full-tree gate (src_wanctl_changed_count=0) |
| T-236-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k 'petter_expiry or manual_reapply or rollback_order' -q` exits 0.
- `.venv/bin/pytest tests/ -q` plus the hot-path regression slice — control path unchanged (known 220/221 historical failures excepted per [233-04]).
- SAFE-16: `evidence/safe16-boundary-236.json` passed=true, controller_path_diff_count=0; companion `evidence/src-wanctl-tree-236.json` src_wanctl_changed_count=0.
- HIGH-3/HIGH-C enforced offline by `-k rollback_order` (per-wan command order) AND at the operator checkpoint (command order confirmed for both wans); HIGH-4 stale-env detection run and recorded.
</verification>

<success_criteria>
- Heartbeat-death → relay-bypass and heartbeat-alive → pet/inline proven offline as software behavior (not hardware relay expiry — MED-3) via the petter-body shim (WDOG-02).
- 2026-06-08 ATT migration failure mode documented as understood and covered; arm/disarm usage + HIGH-1/HIGH-2/HIGH-A semantics + HIGH-3/HIGH-C native-rollback command-order invariant + hardware quirk recorded (WDOG-02).
- phase231-rollback.sh reconciled BY MODE with a PER-WAN command-order invariant proven offline (env-rewrite-before-arm OR clean-disarm-before-cake-stop for att AND spectrum; no narrative-only pass; @spectrum cannot be "left untouched" — HIGH-3/HIGH-C) and soak-monitor stale ref swept (MED-2) under operator review; HIGH-4 live-env migration gate enforced as a blocking condition; A1 + HIGH-5 live-state recorded; no executor live mutation.
- SAFE-16 boundary holds: passed=true, controller_path_diff_count=0; MED-5 companion full-tree gate src_wanctl_changed_count=0; scoped failure-behavior exception confirmed to not touch `src/wanctl` (SAFE-16).
</success_criteria>

<output>
Create `.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-02-SUMMARY.md` when done.
</output>
</content>
