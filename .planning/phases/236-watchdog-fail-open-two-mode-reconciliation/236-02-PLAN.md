---
phase: 236-watchdog-fail-open-two-mode-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["236-01"]
files_modified:
  - tests/test_silicom_bypass_cli.py
  - docs/SILICOM-BYPASS.md
  - scripts/phase231-rollback.sh
autonomous: false
requirements: [WDOG-02, SAFE-16]
user_setup: []

must_haves:
  truths:
    - "A non-destructive test proves: watched-unit inactive → petter calls set_bypass on and withholds reset_bypass_wd; watched-unit active → petter pets and restores inline"
    - "The 2026-06-08 ATT migration failure mode is documented in docs/SILICOM-BYPASS.md as understood and covered by the reconciled units"
    - "phase231-rollback.sh references to the retired ATT variant are reconciled under operator review (live-rollback-semantics file)"
    - "SAFE-16 controller-path zero-diff holds at the phase boundary (passed=true, controller_path_diff_count=0)"
  artifacts:
    - path: "tests/test_silicom_bypass_cli.py"
      provides: "petter-body heartbeat-death → relay-bypass shim proof"
      contains: "petter_expiry"
    - path: "docs/SILICOM-BYPASS.md"
      provides: "2026-06-08 ATT migration RCA + arm/disarm usage"
      contains: "2026-06-08"
    - path: ".planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json"
      provides: "SAFE-16 boundary evidence"
      contains: "passed"
  key_links:
    - from: "tests/test_silicom_bypass_cli.py petter_expiry"
      to: "scripts/wanctl-bpctl-watchdog-petter loop body"
      via: "fake systemctl returns inactive → assert set_bypass on + no reset_bypass_wd"
      pattern: "set_bypass on"
    - from: "scripts/phase225-safe13-boundary-check.sh"
      to: "evidence/safe16-boundary-236.json"
      via: "--anchor v1.51 --out"
      pattern: "passed.*true"
---

<objective>
Prove heartbeat-death → relay-bypass non-destructively, document the 2026-06-08 ATT migration failure mode as understood and covered, reconcile the live-rollback-semantics references under operator review, and close the SAFE-16 phase boundary (WDOG-02, SAFE-16).

Purpose: Plan 01 reconciled the units and wired arm/disarm. This plan proves the reconciled fail-open path behaves correctly using the offline petter-body shim (no live arming), records the migration RCA so the failure mode is captured as understood, conservatively updates the rollback script's now-stale references to the retired ATT variant (operator-gated — it carries live-rollback semantics), and runs the turnkey SAFE-16 boundary checker at the phase gate.

Output: `petter_expiry` behavior tests (fake bpctl + fake systemctl); a 2026-06-08 RCA + arm/disarm usage section in docs/SILICOM-BYPASS.md; operator-reviewed reconciliation of phase231-rollback.sh; SAFE-16 boundary evidence JSON with passed=true.

This plan is non-autonomous: it contains operator-gated checkpoints for the live-state re-verification (research A1) and the conservative rollback-script edit (CLAUDE.md network/prod policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-RESEARCH.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-PATTERNS.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-VALIDATION.md
@.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; extracted from codebase. -->

Petter loop body (scripts/wanctl-bpctl-watchdog-petter, SYSTEMCTL-seamed by Plan 01 Task 4):
  if "$SYSTEMCTL" is-active --quiet "$WANCTL_UNIT"; then
      # active branch: util set_disc off; util set_bypass off (on transition); util reset_bypass_wd   (PET)
  else
      # inactive branch: util set_bypass on (on transition); WITHHOLD reset_bypass_wd                 (EXPIRY)
  fi
  arm sequence (lines 18-22): set_wd_exp_mode bypass; set_wd_autoreset 0; set_dis_bypass off; set_disc off; set_bypass_wd $TIMEOUT_MS

Test harness (Plan 01 added these):
  _fake_bpctl watchdog verbs: set_wd_exp_mode/set_wd_autoreset/set_bypass_wd/reset_bypass_wd/get_bypass_wd/get_wd_exp_mode
  _fake_systemctl(tmp_path): PATH-injected systemctl; rc from FAKE_SYSTEMCTL_RC
  _calls_for(calls, iface) (lines 193-195): filters the bpctl call log per iface for asserting set_bypass on / absence of reset_bypass_wd

SAFE-16 boundary checker (scripts/phase225-safe13-boundary-check.sh):
  .venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out <evidence.json>
  Protected files (controller path): wan_controller.py, queue_controller.py, cake_signal.py, backends, alert_engine.py, fusion
  Expect: passed=true, controller_path_diff_count=0. Anchor tag v1.51 exists.

phase231-rollback.sh references to the RETIRED ATT variant (conservative edit surface):
  line 73  : disable --now ... silicom-bypass-watchdog-cake-autorate-att.service
  line 101 : enable  --now ... silicom-bypass-watchdog-cake-autorate-att.service
  lines 120-122: print_plan watchdog-state narrative
  line 233 : run_check "att_cake_watchdog_active" ... silicom-bypass-watchdog-cake-autorate-att.service
  line 234 : run_check "att_native_watchdog_inactive" ... silicom-bypass-watchdog@att.service
  line 237 : run_check "att_watchdog_env_present" ... /etc/wanctl/bpctl-watchdog/att.env
  After retirement, ATT external mode is covered by silicom-bypass-watchdog@att (env -> cake-autorate-att.service).

docs/SILICOM-BYPASS.md anchors:
  ## Per-WAN Watchdog Fail-Open (line 250); env path block (265-266)
  hardware quirk: requested 5000ms rounds up to 6400ms (lines 289-292) — arm verb must not promise exact ms
  ### Spectrum Migration Validation And Pitfalls (line 435)
  RemainAfterExit/manual-exercise doc-test coupling: test_init_service_manual_reapply_docs_match_remain_after_exit (test file 489-504) — keep procedure blocks test-consistent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-destructive heartbeat-death → relay-bypass petter-body proof</name>
  <files>tests/test_silicom_bypass_cli.py</files>
  <read_first>
    - tests/test_silicom_bypass_cli.py (file being modified; _fake_bpctl watchdog verbs + _fake_systemctl from Plan 01; _calls_for 193-195; _run 198-221)
    - scripts/wanctl-bpctl-watchdog-petter (the SYSTEMCTL-seamed loop body — the test target; active/inactive branches at lines 28-42)
    - 236-RESEARCH.md (Code Examples expiry proof, lines 214-226 — "pure-logic test of the petter loop body, not a wall-clock wait")
    - 236-PATTERNS.md (petter section 183-209 — loop body IS the WDOG-02 test target; assertion spec at 206-209)
    - 236-VALIDATION.md (WDOG-02 row + petter_expiry selector, line 44)
  </read_first>
  <action>
    Add a `petter_expiry` test group proving the reconciled fail-open behavior offline — no wall-clock wait, no live card, no sleep loop. Drive the petter (or an extracted single-iteration invocation of its loop body) with `SYSTEMCTL` set to the `_fake_systemctl` from Plan 01 and `BPCTL_UTIL` set to the `_fake_bpctl`, with `IFACE` and `WANCTL_UNIT` set via env. To avoid the petter's infinite `while :;` loop, run it with a controllable iteration bound — set `HEARTBEAT_SECONDS` and run the petter under a short `timeout` so it executes at least one full branch then is killed, OR (preferred) factor the test to invoke the loop body once. Assert from the bpctl call log via `_calls_for`:
    (a) Inactive proof (FAKE_SYSTEMCTL_RC=1 → controller dead): the bpctl calls for the iface include `set_bypass on` and do NOT include `reset_bypass_wd` (pet withheld) — this is the fail-open firing.
    (b) Active proof (FAKE_SYSTEMCTL_RC=0 → controller alive): the bpctl calls include `reset_bypass_wd` (pet) and, on the first transition, `set_disc off` + `set_bypass off` (restore inline), and do NOT include `set_bypass on`.
    Also assert the arm sequence ran on startup: `set_wd_exp_mode bypass`, `set_wd_autoreset 0`, `set_bypass_wd <ms>` appear in the call log. NEVER set BPCTL_UTIL to `/opt/bpctl-silicom/bpctl_util` and NEVER call the real systemctl (Pitfall 2 — no live arming in any automated step).
  </action>
  <behavior>
    - petter_expiry_inactive: SYSTEMCTL is-active rc!=0 → bpctl log has `set_bypass on`, no `reset_bypass_wd`
    - petter_expiry_active: SYSTEMCTL is-active rc==0 → bpctl log has `reset_bypass_wd` + inline restore, no `set_bypass on`
    - petter_arm_sequence: startup writes set_wd_exp_mode bypass, set_wd_autoreset 0, set_bypass_wd <ms>
  </behavior>
  <verify>
    <automated>.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k petter_expiry -q` exits 0
    - inactive-branch test asserts presence of `set_bypass on` AND absence of `reset_bypass_wd` in the iface call log
    - active-branch test asserts presence of `reset_bypass_wd` AND absence of `set_bypass on`
    - no test sets `BPCTL_UTIL` to a real path and no test calls a real `systemctl` (all via PATH/env fakes)
    - test completes in < 5s (no real timer wait)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>petter_expiry proves heartbeat-death → bypass and heartbeat-alive → pet/inline offline; WDOG-02 behavior demonstrated non-destructively.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 2026-06-08 ATT migration failure mode (RCA) + arm/disarm usage in docs/SILICOM-BYPASS.md</name>
  <files>docs/SILICOM-BYPASS.md</files>
  <read_first>
    - docs/SILICOM-BYPASS.md (file being modified; ## Per-WAN Watchdog Fail-Open line 250; env-path block 265-266; hardware quirk 289-292; ### Spectrum Migration Validation And Pitfalls line 435)
    - tests/test_silicom_bypass_cli.py (test_init_service_manual_reapply_docs_match_remain_after_exit 489-504 — doc-procedure/test coupling to keep consistent)
    - 236-RESEARCH.md (Pitfall 1 — the full 2026-06-08 failure mode reconstruction, lines 191-195; hardware quirk + settled constraint, lines 61-63)
    - 236-PATTERNS.md (docs section 308-321 — what to add; do not re-litigate unpowered fail-open)
  </read_first>
  <action>
    Add a dated RCA subsection under `## Per-WAN Watchdog Fail-Open` (or as a new `### 2026-06-08 ATT Migration Watchdog Failure Mode (RCA)` subsection) capturing, as understood and now COVERED by the reconciled units:
    - What happened: the migration from `wanctl@<wan>` to cake-autorate (`fc47a0c8`, live 2026-06-08) left the watched-unit name `wanctl@<wan>.service` permanently inactive; an armed watchdog reading that name sees a healthy controller as dead → `set_bypass on` + withheld pet → relay fires bypass (raw ISP, CAKE shaping gone) within the ~6400ms timeout.
    - Why: the migration bolted on a one-off `silicom-bypass-watchdog-cake-autorate-att.service` for ATT but left Spectrum on the stale generic template pointing at the disabled `wanctl@spectrum.service` — an asymmetric two-unit reality.
    - The fix (this phase): one configurable template whose watched-unit name (`WANCTL_UNIT` in `%i.env`) names the live controller (`cake-autorate-<wan>.service`) for both pairs; native-rollback mode sets it back to `wanctl@<wan>.service`. ATT variant retired, folded into `@att`.
    Add an `arm`/`disarm` usage block: `silicom-bypass arm <pair> [timeout]` (default from `WD_TIMEOUT_MS`) requires `--yes` because arming a live pair will bypass it on controller death; `silicom-bypass disarm <pair>`. State the pair tokens are `att-modem`/`spec-modem` (mapped internally to the `@att`/`@spectrum` watchdog instances). Cite the hardware quirk: requested 5000ms rounds up to a 6400ms hardware timeout (fixed timer steps) — do NOT promise exact ms. Do NOT re-litigate unpowered fail-open (settled: monostable relays, AuxCurrent=0mA, UPS covers it). Keep any procedure/codeblock edits consistent with the doc-test coupling at test_silicom_bypass_cli.py:489-504.
  </action>
  <verify>
    <automated>grep -c '2026-06-08' docs/SILICOM-BYPASS.md && grep -c 'silicom-bypass arm' docs/SILICOM-BYPASS.md && .venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SILICOM-BYPASS.md` contains a 2026-06-08 RCA referencing the watched-unit/cake-autorate failure mode and stating it is covered by the reconciled units
    - doc contains `silicom-bypass arm` and `silicom-bypass disarm` usage with the `--yes` live-arm rationale
    - doc references the `cake-autorate-<wan>.service` watched unit and the `att-modem`/`spec-modem` -> `@att`/`@spectrum` mapping
    - doc does NOT add any claim contradicting the settled unpowered-fail-open constraint
    - `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k manual_reapply -q` exits 0 (doc-test coupling intact)
    - No edits to any file under `src/wanctl/`
  </acceptance_criteria>
  <done>The 2026-06-08 failure mode is documented as understood and covered; arm/disarm usage and the hardware quirk are recorded; doc-test coupling stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator-gated reconciliation of phase231-rollback.sh + live-state re-verification (research A1)</name>
  <files>scripts/phase231-rollback.sh</files>
  <read_first>
    - scripts/phase231-rollback.sh (file being modified; ATT variant refs at lines 73, 101, 120-122, 233-237; spectrum watchdog narrative at 109, 121)
    - 236-RESEARCH.md (Risk flags for the planner, lines 371-374; Open Q2, lines 275-278; Runtime State Inventory + A1, lines 176-187, 262)
    - 236-PATTERNS.md (phase231-rollback.sh section 237-256 — CONSERVATIVE; "mark any edit here for explicit operator review")
    - 236-VALIDATION.md (Manual-Only Verifications table, lines 64-70)
    - <locked_constraints> #5 (conservative edits to phase231-rollback.sh; autonomous:false) and live-arming gate constraints
  </read_first>
  <action>
    Operator-gated, conservative (CLAUDE.md network/prod: suggest-don't-implement). Prepare a minimal diff to scripts/phase231-rollback.sh that reroutes the retired ATT variant references to the folded silicom-bypass-watchdog@att instance: update the disable line (73), the enable line (101), the print_plan watchdog narrative (120-122), and the run_check assertion names/targets (233-237) so ATT external mode is asserted via silicom-bypass-watchdog@att and att.env points at cake-autorate-att.service. Change ONLY watchdog-unit references — leave every tc/qdisc command, health URL, and the --confirm/--i-have-operator-approval mutation gate byte-for-byte unchanged. Run `bash -n` only; do NOT execute the rollback script and do NOT run any systemctl/.env mutation against cake-shaper. Surface the proposed diff plus the operator-only A1 live-state re-verification commands at the checkpoint; the operator runs the live reads and records the finding. Defer the ATT variant file deletion until after operator approval of the rollback-script edit.
  </action>
  <what-built>
    Claude has prepared a proposed conservative reconciliation of `scripts/phase231-rollback.sh` to match the retired ATT variant: route ATT external-mode watchdog references from `silicom-bypass-watchdog-cake-autorate-att.service` to the folded `silicom-bypass-watchdog@att` instance (env -> cake-autorate-att.service), keeping the spectrum narrative consistent with the reconciled template. The proposed diff updates the `enable/disable --now` lines (73, 101), the `print_plan` watchdog-state narrative (120-122), and the `run_check` assertion names/targets (233-237) WITHOUT changing any tc/qdisc commands, health URLs, or the `--confirm --i-have-operator-approval` mutation gate.
    This file carries LIVE-ROLLBACK semantics. No live host is touched by this task. The live-state re-verification (research A1) and any live `.env`/daemon-reload/enable/arm are operator actions, not executor actions.
  </what-built>
  <how-to-verify>
    1. Review the proposed `scripts/phase231-rollback.sh` diff. Confirm: only watchdog-unit references changed; tc/qdisc/health/mutation-gate lines untouched. `git diff scripts/phase231-rollback.sh`.
    2. Confirm the ATT external-mode watchdog now routes through `silicom-bypass-watchdog@att` consistently with the retired variant, and the spectrum narrative matches the reconciled (cake-autorate-named) env.
    3. Operator-only, on `cake-shaper` (resolves research A1 — do NOT let the executor run these):
       `systemctl is-enabled silicom-bypass-watchdog@spectrum.service ; systemctl is-active silicom-bypass-watchdog@spectrum.service`
       `systemctl is-enabled silicom-bypass-watchdog-cake-autorate-att.service ; systemctl is-active ...`
       `sudo cat /etc/wanctl/bpctl-watchdog/spectrum.env /etc/wanctl/bpctl-watchdog/att.env`
       Decide whether any live `.env` update / daemon-reload / re-enable is warranted now or deferred. Any live arming is a separate explicit `silicom-bypass arm <live-pair> --yes` decision — never implicit, never executor-run.
    4. If a live apply is approved, the operator runs the `.env` update + `daemon-reload` + per-pair re-enable manually. Rollback for any live arm = `silicom-bypass disarm <pair>` + restore inline.
  </how-to-verify>
  <verify>
    <human-check>git diff scripts/phase231-rollback.sh shows only watchdog-unit ref changes; bash -n scripts/phase231-rollback.sh exits 0; operator records A1 live-state finding in SUMMARY</human-check>
  </verify>
  <resume-signal>Type "approved" to accept the phase231-rollback.sh reconciliation (and record the A1 live-state finding), or describe required changes.</resume-signal>
  <acceptance_criteria>
    - `git diff scripts/phase231-rollback.sh` shows ONLY watchdog-unit reference changes — no tc/qdisc/health-URL/mutation-gate edits
    - `bash -n scripts/phase231-rollback.sh` exits 0
    - Operator has recorded the A1 live enablement finding (enabled/active state of the spectrum + ATT watchdog units and the live `.env` WANCTL_UNIT values) in the plan SUMMARY
    - No executor step ran `systemctl enable/start`, `silicom-bypass arm`, or any live `.env` mutation on cake-shaper
  </acceptance_criteria>
  <done>phase231-rollback.sh references reconciled to the folded ATT watchdog under operator review; A1 live-state finding recorded; no live mutation performed by the executor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [BLOCKING]: SAFE-16 controller-path zero-diff boundary gate</name>
  <files>.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the turnkey checker; protected file list)
    - 236-RESEARCH.md (Don't Hand-Roll SAFE-16 row + Code Examples boundary proof, lines 165-167, 236-243)
    - 236-PATTERNS.md (SAFE-16 Shared Pattern, lines 356-364)
    - <locked_constraints> #1 (SAFE-16 zero-diff; controller_path_diff_count=0)
  </read_first>
  <action>
    Run the turnkey controller-path zero-diff checker at the phase gate (do NOT hand-roll a git-diff). Invoke scripts/phase225-safe13-boundary-check.sh with --anchor v1.51 and --out evidence/safe16-boundary-236.json, then assert the emitted JSON has passed=true and controller_path_diff_count=0. If either fails, a protected src/wanctl file (wan_controller.py, queue_controller.py, cake_signal.py, backends, alert_engine.py, fusion) was touched — stop and revert before approving. Also run the hot-path regression slice to confirm no control-path regression (full-suite-green is NOT the gate per [233-04]; SAFE-16 passing is).
  </action>
  <what-built>
    The turnkey SAFE-16 boundary checker run against anchor `v1.51`, producing `evidence/safe16-boundary-236.json`. This proves the phase touched only units/scripts/docs/tests and the one scoped exception (cake-shaper bypass FAILURE behavior) — never the `src/wanctl` controller path.
  </what-built>
  <how-to-verify>
    Run:
    `.venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json`
    Confirm the emitted JSON has `passed=true` and `controller_path_diff_count=0`. If either is false, a controller-path file was touched in error — stop and revert that edit before proceeding.
    Also run the full suite + hot-path regression slice to confirm no control-path regression:
    `.venv/bin/pytest tests/ -q` (note: known Phase 220/221 historical boundary-test failures are pre-existing per STATE.md decision [233-04]; SAFE-16 passing is the gate, not full-suite-green).
  </how-to-verify>
  <verify>
    <automated>.venv/bin/python scripts/phase225-safe13-boundary-check.sh --anchor v1.51 --out .planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json && python3 -c "import json,sys; d=json.load(open('.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/evidence/safe16-boundary-236.json')); assert d.get('passed') is True, d; assert d.get('controller_path_diff_count',1)==0, d; print('SAFE-16 OK')"</automated>
  </verify>
  <resume-signal>Type "approved" once SAFE-16 evidence shows passed=true and controller_path_diff_count=0.</resume-signal>
  <acceptance_criteria>
    - `evidence/safe16-boundary-236.json` exists with `passed: true`
    - `controller_path_diff_count` equals 0 in the evidence JSON
    - No file under `src/wanctl/` (`wan_controller.py`, `queue_controller.py`, `cake_signal.py`, backends, `alert_engine.py`, fusion) appears in the boundary diff
    - hot-path regression slice green (control path untouched)
  </acceptance_criteria>
  <done>SAFE-16 boundary evidence emitted with passed=true, controller_path_diff_count=0; the scoped failure-behavior exception confirmed to not touch controller logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → live host | proof must NOT touch cake-shaper; live arming/`.env`/daemon-reload are operator checkpoints |
| repo edit → live rollback | phase231-rollback.sh edits affect a live-rollback procedure (conservative surface) |
| phase boundary → controller path | SAFE-16: no `src/wanctl` source diff may cross the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-236-02 | Denial of Service | executor/test arms a live relay during the expiry proof | mitigate | Task 1 proof is pure-logic over the petter loop body via fake bpctl + fake systemctl; no real timer, no live card, BPCTL_UTIL never set to the real binary |
| T-236-06 | Tampering | a careless phase231-rollback.sh edit changes live-rollback tc/qdisc/mutation-gate behavior | mitigate | Task 3 is `autonomous: false` operator-review; acceptance asserts only watchdog-unit refs change; `bash -n` syntax gate |
| T-236-07 | Denial of Service | operator unknowingly arms a live pair watching a still-stale unit | mitigate | Task 3 forces the A1 live-state re-verification before any live apply; arm requires explicit `--yes`; rollback = `disarm` + restore inline |
| T-236-05 | Tampering | controller-path source change smuggled into this docs/tests/rollback plan | mitigate | Task 4 [BLOCKING] SAFE-16 boundary check, passed=true + controller_path_diff_count=0 |
| T-236-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_silicom_bypass_cli.py -k 'petter_expiry or manual_reapply' -q` exits 0.
- `.venv/bin/pytest tests/ -q` plus the hot-path regression slice — control path unchanged (known 220/221 historical failures excepted per [233-04]).
- SAFE-16: `evidence/safe16-boundary-236.json` passed=true, controller_path_diff_count=0.
</verification>

<success_criteria>
- Heartbeat-death → relay-bypass and heartbeat-alive → pet/inline proven offline via the petter-body shim (WDOG-02).
- 2026-06-08 ATT migration failure mode documented as understood and covered by the reconciled units; arm/disarm usage + hardware quirk recorded (WDOG-02).
- phase231-rollback.sh references reconciled to the folded ATT watchdog under operator review; A1 live-state finding recorded; no executor live mutation.
- SAFE-16 boundary holds: passed=true, controller_path_diff_count=0; scoped failure-behavior exception confirmed to not touch `src/wanctl` (SAFE-16).
</success_criteria>

<output>
Create `.planning/phases/236-watchdog-fail-open-two-mode-reconciliation/236-02-SUMMARY.md` when done.
</output>
