---
phase: 116-test-documentation-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONFIG_SCHEMA.md
  - docs/DOCKER.md
  - docs/CONTAINER_NETWORK_AUDIT.md
  - docs/DEPLOYMENT.md
  - docs/PRODUCTION_INTERVAL.md
  - docs/SPECTRUM_WATCHDOG_RESTARTS.md
  - docs/FALLBACK_CHECKS_IMPLEMENTATION.md
  - docs/FALLBACK_CONNECTIVITY_CHECKS.md
  - docs/INTERVAL_TESTING_50MS.md
  - docs/INTERVAL_TESTING_250MS.md
  - docs/PROFILING.md
  - docs/STEERING_CONFIG_MISMATCH_ISSUE.md
  - docs/TRANSPORT_COMPARISON.md
  - .archive/manifest.md
  - .archive/  # archived scripts
autonomous: true
requirements: [TDOC-03, TDOC-04, TDOC-05]

must_haves:
  truths:
    - "All docs/* files reviewed and container/LXC references replaced with VM/bridge/CAKE-on-Linux"
    - "CONFIG_SCHEMA.md documents every param accepted by config_validation_utils.py with no stale entries"
    - "Container-era scripts are moved to .archive/ with a manifest explaining each"
  artifacts:
    - path: "docs/CONFIG_SCHEMA.md"
      provides: "Aligned config reference matching config_validation_utils.py"
      contains: "storage"
    - path: ".archive/manifest.md"
      provides: "Archival manifest for container-era scripts"
      contains: "## Archived Scripts"
    - path: "docs/DOCKER.md"
      provides: "Updated or archived Docker doc with deprecation notice"
      contains: "VM"
  key_links:
    - from: "docs/CONFIG_SCHEMA.md"
      to: "src/wanctl/config_validation_utils.py"
      via: "Every accepted param in code is documented in schema"
      pattern: "config_validation_utils"
    - from: ".archive/manifest.md"
      to: ".archive/*.sh"
      via: "Each archived script has a manifest entry"
      pattern: "container_install"
---

<objective>
Update all documentation for post-v1.21 VM architecture, align CONFIG_SCHEMA.md with code, and archive container-era scripts.

Purpose: Docs must reflect reality (VM, not containers). CONFIG_SCHEMA.md is the most impactful update (per D-06) -- operators depend on it. Container scripts are dead code that should be archived, not deleted.
Output: Updated docs/, aligned CONFIG_SCHEMA.md, .archive/ directory with manifest
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-test-documentation-hygiene/116-CONTEXT.md

# Source of truth for config params
@src/wanctl/config_validation_utils.py

# Docs to update
@docs/CONFIG_SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align CONFIG_SCHEMA.md with config_validation_utils.py</name>
  <read_first>
    - src/wanctl/config_validation_utils.py (source of truth for accepted params)
    - docs/CONFIG_SCHEMA.md (current state -- needs alignment)
    - .planning/phases/116-test-documentation-hygiene/116-CONTEXT.md (D-06)
  </read_first>
  <files>docs/CONFIG_SCHEMA.md</files>
  <action>
Per D-06: Align CONFIG_SCHEMA.md with config_validation_utils.py -- every accepted param documented, no stale entries.

**Step 1: Extract accepted params from code.**
Read config_validation_utils.py and extract every parameter name that is validated/accepted. Also check for any VALID_* constants, allowed_keys sets, or validation functions that define the config schema.

**Step 2: Cross-reference with CONFIG_SCHEMA.md.**
For each param in code: verify it appears in CONFIG_SCHEMA.md with correct type, default, and description.
For each param in CONFIG_SCHEMA.md: verify it still exists in code (remove stale entries).

**Step 3: Update CONFIG_SCHEMA.md.**
- Add any missing params with type, default value, and description derived from code
- Remove any stale params no longer in code
- Update any params whose type/default/description has drifted from code
- Preserve the existing markdown table format
- Add sections for any new config groups (e.g., storage.retention, tuning.*, signal.*)
- Note the v1.21 VM migration where relevant (e.g., transport options now include linux-cake)

Do NOT rewrite the entire file -- targeted updates only (per D-04 spirit applied to CONFIG_SCHEMA.md).
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && grep -c "|" docs/CONFIG_SCHEMA.md && python3 -c "
import re
# Quick check: every VALID_ constant in config_validation_utils.py should have corresponding entries in CONFIG_SCHEMA.md
with open('src/wanctl/config_validation_utils.py') as f: code = f.read()
with open('docs/CONFIG_SCHEMA.md') as f: doc = f.read()
valid_consts = re.findall(r'VALID_(\w+)', code)
print(f'VALID_ constants in code: {len(valid_consts)}')
missing = [v for v in valid_consts if v.lower().replace('_', '') not in doc.lower().replace('_', '').replace('-', '')]
print(f'Potentially missing from docs: {missing}')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "transport" docs/CONFIG_SCHEMA.md (transport param documented)
    - grep "storage" docs/CONFIG_SCHEMA.md (storage section exists)
    - grep -c "linux-cake" docs/CONFIG_SCHEMA.md returns at least 1 (VM backend documented)
  </acceptance_criteria>
  <done>CONFIG_SCHEMA.md documents every param in config_validation_utils.py, no stale entries remain, new config groups from v1.20/v1.21 are represented</done>
</task>

<task type="auto">
  <name>Task 2: Update docs for VM architecture and archive container scripts</name>
  <read_first>
    - docs/DOCKER.md (62 container references -- primary candidate)
    - docs/CONTAINER_NETWORK_AUDIT.md (8 container references)
    - docs/DEPLOYMENT.md (6 container references)
    - docs/PRODUCTION_INTERVAL.md
    - docs/SPECTRUM_WATCHDOG_RESTARTS.md
    - docs/TRANSPORT_COMPARISON.md
    - scripts/container_install_att.sh
    - scripts/container_install_spectrum.sh
    - scripts/verify_steering.sh
    - scripts/verify_steering_new.sh
    - .planning/phases/116-test-documentation-hygiene/116-CONTEXT.md (D-04, D-05, D-07, D-08)
  </read_first>
  <files>
    - docs/DOCKER.md
    - docs/CONTAINER_NETWORK_AUDIT.md
    - docs/DEPLOYMENT.md
    - docs/PRODUCTION_INTERVAL.md
    - docs/SPECTRUM_WATCHDOG_RESTARTS.md
    - docs/FALLBACK_CHECKS_IMPLEMENTATION.md
    - docs/FALLBACK_CONNECTIVITY_CHECKS.md
    - docs/INTERVAL_TESTING_50MS.md
    - docs/INTERVAL_TESTING_250MS.md
    - docs/PROFILING.md
    - docs/STEERING_CONFIG_MISMATCH_ISSUE.md
    - docs/TRANSPORT_COMPARISON.md
    - .archive/manifest.md
    - .archive/container_install_att.sh
    - .archive/container_install_spectrum.sh
    - .archive/verify_steering.sh
    - .archive/verify_steering_new.sh
  </files>
  <action>
**Documentation Updates (per D-04, D-05):**
Targeted updates to each doc in docs/ that references containers/LXC/Docker:

1. **docs/DOCKER.md** (62 refs): Add deprecation notice at top: "# DEPRECATED: Docker deployment was superseded by VM deployment in v1.21. See docs/VM_INFRASTRUCTURE.md for current architecture." Keep content for historical reference but clearly mark as superseded.

2. **docs/CONTAINER_NETWORK_AUDIT.md** (8 refs): Add note at top that this was conducted pre-v1.21 migration. Container overhead findings are historical. VM architecture eliminates container networking layer entirely.

3. **docs/DEPLOYMENT.md** (6 refs): Replace container deployment instructions with VM deployment references. Point to deploy/systemd/ for current unit files. Replace "target container" with "target VM" or "production host".

4. **docs/PRODUCTION_INTERVAL.md** (1 ref): Replace container reference with VM reference.

5. **docs/SPECTRUM_WATCHDOG_RESTARTS.md** (3 refs): Replace container references. This doc describes a historical issue -- note it was pre-VM.

6. **docs/FALLBACK_CHECKS_IMPLEMENTATION.md** (2 refs), **docs/FALLBACK_CONNECTIVITY_CHECKS.md** (2 refs): Replace container references with VM/host references.

7. **docs/INTERVAL_TESTING_50MS.md** (1 ref), **docs/INTERVAL_TESTING_250MS.md** (2 refs): Replace container refs. These document historical testing -- mark as pre-VM baseline.

8. **docs/PROFILING.md** (2 refs): Replace container references with VM references.

9. **docs/STEERING_CONFIG_MISMATCH_ISSUE.md** (1 ref): Replace container reference.

10. **docs/TRANSPORT_COMPARISON.md** (1 ref): Update to reflect linux-cake transport option alongside REST/SSH.

For all updates: Replace "container" with "VM" or "production host" where it refers to the runtime environment. Keep "container" where it refers to Docker concepts in historical context. Do NOT rewrite entire docs (per D-04).

**Container Script Archival (per D-07, D-08):**
1. Create `.archive/` directory
2. Move these scripts from scripts/ to .archive/:
   - `container_install_att.sh` -- LXC container provisioning for ATT WAN
   - `container_install_spectrum.sh` -- LXC container provisioning for Spectrum WAN
   - `verify_steering.sh` -- Pre-VM steering verification
   - `verify_steering_new.sh` -- Updated pre-VM steering verification
3. Also evaluate and potentially archive: `container_network_audit.py` (container-era audit script), `fix_passthrough.sh` (if container-specific)
4. Use `git mv` for moves to preserve history
5. Write `.archive/manifest.md`:

```markdown
# Archived Scripts

Scripts moved here during v1.22 Phase 116 (Test & Documentation Hygiene).
These are container/LXC-era scripts superseded by the v1.21 VM migration.

## Scripts

| Script | Original Purpose | Archived Because |
|--------|-----------------|------------------|
| container_install_att.sh | LXC container provisioning for ATT WAN controller | Superseded by VM deployment (cake-shaper VM 206) |
| container_install_spectrum.sh | LXC container provisioning for Spectrum WAN controller | Superseded by VM deployment (cake-shaper VM 206) |
| verify_steering.sh | Pre-VM WAN steering verification | Superseded by VM-based steering service |
| verify_steering_new.sh | Updated pre-VM steering verification | Superseded by VM-based steering service |
| (others if identified) | ... | ... |

## Context

Prior to v1.21, wanctl ran in LXC containers (cake-spectrum, cake-att) on Proxmox.
v1.21 migrated to a single VM (cake-shaper, VM 206) with PCIe passthrough NICs
and Linux CAKE qdiscs replacing MikroTik-only CAKE.

These scripts are preserved for historical reference but should not be used.
```
  </action>
  <verify>
    <automated>cd /home/kevin/projects/wanctl && test -f .archive/manifest.md && grep -c "Archived" .archive/manifest.md && test -f .archive/container_install_att.sh && grep -c "DEPRECATED\|deprecated\|superseded\|Superseded\|historical\|Historical" docs/DOCKER.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f .archive/manifest.md (manifest exists)
    - test -f .archive/container_install_att.sh (scripts moved)
    - test -f .archive/container_install_spectrum.sh (scripts moved)
    - test ! -f scripts/container_install_att.sh (removed from scripts/)
    - grep -i "deprecated\|superseded" docs/DOCKER.md (deprecation notice present)
    - grep -i "VM\|vm\|virtual" docs/DEPLOYMENT.md (VM references added)
  </acceptance_criteria>
  <done>All docs updated with VM references replacing container references, CONFIG_SCHEMA.md aligned with code, container scripts archived to .archive/ with manifest, no container-era scripts remain in scripts/</done>
</task>

</tasks>

<verification>
- CONFIG_SCHEMA.md matches config_validation_utils.py accepted params
- All 12 doc files with container references have been reviewed and updated
- Container scripts moved to .archive/ via git mv
- .archive/manifest.md exists with purpose and context for each archived script
- No stale container references in DEPLOYMENT.md (should reference VM)
</verification>

<success_criteria>
- `grep -rn "container" docs/ | wc -l` is significantly reduced (remaining refs are in historical/deprecated context)
- CONFIG_SCHEMA.md has entries for every VALID_* constant in config_validation_utils.py
- `.archive/manifest.md` has a row for each archived script
- `ls scripts/container_install_*.sh 2>/dev/null` returns nothing (moved to .archive/)
</success_criteria>

<output>
After completion, create `.planning/phases/116-test-documentation-hygiene/116-02-SUMMARY.md`
</output>
