---
phase: 260-dry-run-observation-rerun-canary-readiness
plan: 02
type: execute
wave: 2
depends_on: ["260-01"]
files_modified:
  - tests/test_phase260_observation.py
autonomous: true
requirements: [OBSERVE-01, OBSERVE-02, OBSERVE-03, SAFE-21]

must_haves:
  truths:
    - "A test proves the mutation-rejection gate fires BEFORE any live run_cmd (no command issued)."
    - "A test proves a single inspector_status=error OR match=false sample forces OBSERVE_VERDICT: not-ready (fail-closed)."
    - "A test proves an all-clean window with an agreeing cross-check and a matching per-route table yields OBSERVE_VERDICT: ready-for-approval."
    - "A test proves one bad mid-window sample (even if it recovers) forces not-ready, recording that sample as the concrete blocker (D-02)."
    - "A test proves a cross-check disagreement records a divergence and forces not-ready (D-04/D-07)."
  artifacts:
    - path: "tests/test_phase260_observation.py"
      provides: "Offline test slice for the observation harness (mutation rejection, fail-closed, happy path, mid-window blip, cross-check divergence)"
      contains: "validate_commands_before_run"
      min_lines: 100
  key_links:
    - from: "tests/test_phase260_observation.py"
      to: "scripts/phase260-observation.py"
      via: "importlib.util.spec_from_file_location (hyphenated script import)"
      pattern: "spec_from_file_location"
    - from: "tests/test_phase260_observation.py"
      to: "validate_commands_before_run"
      via: "assert mutating command raises before run_cmd"
      pattern: "validate_commands_before_run"
---

<objective>
Build `tests/test_phase260_observation.py` — the offline test slice that pins the harness's safety and fail-closed behavior without touching live state. It models `tests/test_phase259_ownership_proof.py`: import the hyphenated script via `importlib`, drive a `FakeClient` returning RouterOS-shaped JSON, and assert the SAFE-21 / D-01 / D-02 / D-04 invariants.

Purpose: These tests are the durable proof that the mutation-rejection gate precedes any live I/O and that any single bad sample or divergence forces `not-ready`. They run in CI offline, decoupled from cake-shaper.
Output: `tests/test_phase260_observation.py`.

SAFE-21: Tests use fakes only — no live RouterOS, no mutation, no route-owner flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-CONTEXT.md
@.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md

<interfaces>
From tests/test_phase259_ownership_proof.py (the analog to model):
- Script-with-a-dash import: `SCRIPT = Path(__file__).resolve().parents[1] / "scripts" / "phase260-observation.py"`; `spec = importlib.util.spec_from_file_location("phase260_observation", SCRIPT)`; `module_from_spec` + `spec.loader.exec_module(mod)`.
- `FakeClient(fail=None)`: records `self.commands`; `run_cmd(cmd,...)` appends cmd, returns `(1,"","boom")` if `fail and fail in cmd`, else branches on `"netwatch"`/`"script"`/`"route"` in cmd returning `(0, json.dumps([...]), "")`. Includes default-route rows (`dst-address": "0.0.0.0/0"`).
- `FakeRouteManager.status_snapshot()` returns `{"active_owner": "netwatch", "mode": "dry_run"}` (extend with a reconciliation `routes` map if the harness reads it directly).
- Mutation-rejection test: `proof.validate_commands_before_run(client, ["/ip route disable 0"])` raises `ValueError(match="mutating action")` AND `client.commands == []`.

From scripts/phase260-observation.py (built in 260-01):
- `validate_commands_before_run(client, commands)`, `gate_sample(ownership_inspection, route_management, prev_last_inspected_at)`, `cross_check(client)`, `standing_intent_table(route_manager, ownership_inspection)`, `assemble_divergences(...)`, `scan_mutation_tokens(...)`, `render_packet(...)`, the verdict-computing entrypoint, `_NoRunClient`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test harness scaffold + mutation-rejection + fail-closed-on-inspector-error</name>
  <read_first>
    - tests/test_phase259_ownership_proof.py (the analog to model — read whole)
    - scripts/phase260-observation.py (the module under test — read for exact function signatures)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md (test section; mandatory test cases table)
  </read_first>
  <files>tests/test_phase260_observation.py</files>
  <behavior>
    - Test: `validate_commands_before_run(FakeClient(), ["/ip route disable 0"])` raises `ValueError` matching "mutating action" AND `FakeClient().commands == []` (no command issued before validation).
    - Test: a single `inspector_status="error"` (or `match=False`) sample drives the verdict path to `not-ready` and surfaces the inspector error / bad sample as the recorded blocker.
    - Test: `scan_mutation_tokens([])` returns `[]`; `scan_mutation_tokens(["/ip route disable 0"])` returns a non-empty hit list.
  </behavior>
  <action>
    Create `tests/test_phase260_observation.py`. Import the hyphenated script via `importlib.util.spec_from_file_location("phase260_observation", SCRIPT)` exactly as the analog does. Reuse the analog's `FakeClient` (branch on `"netwatch"`/`"script"`/`"route"`, `fail=` support) and extend its route branch to include the four baseline default-route rows so the D-05 table has live rows to compare. Reuse/extend `FakeRouteManager`.

    Write `test_rejects_mutating_command_before_run_cmd`: assert `validate_commands_before_run(FakeClient(), ["/ip route disable 0"])` raises `ValueError(match="mutating action")` and that `client.commands == []` (keeper from D-08/SAFE-21).

    Write `test_fail_closed_on_inspector_error`: feed a sample with `inspector_status="error"` (or `match=False`) through the harness's gate/verdict path; assert the result is `not-ready` and the bad sample is the recorded blocker.

    Write `test_scan_mutation_tokens`: assert empty-in → empty-out, and that a disable command produces a non-empty hit list.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_phase260_observation.py -k "mutating or inspector_error or mutation_tokens" -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -o addopts='' tests/test_phase260_observation.py -k "mutating or inspector_error or mutation_tokens" -v` exits 0.
    - The mutation-rejection test asserts both `ValueError(match="mutating action")` AND `client.commands == []`.
    - The inspector-error test asserts the verdict is `not-ready` (never an upgrade to readiness).
    - The mutation-token test asserts `[]` for clean input and a non-empty hit list for a disable command.
  </acceptance_criteria>
  <done>Scaffold imports the hyphenated script; mutation-rejection, fail-closed-on-error, and mutation-token tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Happy-path ready-for-approval + mid-window blip (D-02) + cross-check divergence (D-04/D-07)</name>
  <read_first>
    - tests/test_phase260_observation.py (the file being extended — read current state)
    - scripts/phase260-observation.py (gate_sample, cross_check, standing_intent_table, assemble_divergences, verdict path)
    - .planning/phases/260-dry-run-observation-rerun-canary-readiness/260-PATTERNS.md (mandatory test cases table, new D-02 + D-04 rows)
  </read_first>
  <files>tests/test_phase260_observation.py</files>
  <behavior>
    - Test: an all-clean window (every sample inspector_status=ok, match=True, advancing last_inspected_at) + an agreeing cross-check + a matching per-route table yields `OBSERVE_VERDICT: ready-for-approval`.
    - Test: one bad mid-window sample (one `match=False` among otherwise-clean samples, even if later samples recover) forces `not-ready`, with that sample recorded as the concrete blocker (D-02).
    - Test: a cross-check whose direct RouterOS read disagrees with the sampled `ownership_inspection` records a divergence (D-07 class 3) and forces `not-ready`.
  </behavior>
  <action>
    Extend `tests/test_phase260_observation.py`.

    Write `test_happy_path_ready_for_approval`: build a sequence of all-clean samples (advancing `last_inspected_at`), a cross-check via `FakeClient` that agrees with the sampled `ownership_inspection`, and a `standing_intent_table` whose rows match; assert the verdict is `ready-for-approval` and the divergence union is empty and `MUTATION_TOKEN_HITS` is `[]`.

    Write `test_midwindow_blip_forces_not_ready`: inject one `match=False` (or `inspector_status="error"`) sample among otherwise-clean samples, with later samples recovering; assert the verdict is `not-ready` and the recorded blocker references that specific bad sample (D-02 — a transient blip is NOT smoothed over).

    Write `test_crosscheck_disagreement_records_divergence`: make the direct cross-check read disagree with `ownership_inspection` (e.g. different default-route count); assert a D-07 class-3 divergence is recorded and the verdict is `not-ready`.

    Use the harness functions directly (gate_sample / assemble_divergences / verdict path) rather than spinning a live window, so the tests stay offline and deterministic.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_phase260_observation.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -o addopts='' tests/test_phase260_observation.py -v` exits 0 with all tests passing.
    - The happy-path test asserts `ready-for-approval` AND an empty divergence union AND `MUTATION_TOKEN_HITS == []`.
    - The mid-window-blip test asserts `not-ready` even though later samples recover, and that the bad sample is the recorded blocker.
    - The cross-check-disagreement test asserts a recorded D-07 class-3 divergence and a `not-ready` verdict.
    - No test performs live RouterOS I/O, mutation, or a route-owner flip.
  </acceptance_criteria>
  <done>Full test slice passes: happy path, mid-window blip fail-closed, and cross-check divergence — all offline with fakes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → harness module | Tests import and exercise the harness with fakes; no live boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-T1 | Tampering | a regression silently disables the mutation-rejection gate | mitigate | `test_rejects_mutating_command_before_run_cmd` pins that a mutating command raises before any `run_cmd` and `client.commands == []`; runs in CI. |
| T-260-T2 | Spoofing | a regression lets a bad sample upgrade to readiness | mitigate | `test_fail_closed_on_inspector_error` and `test_midwindow_blip_forces_not_ready` pin fail-closed on any single bad sample (D-02). |
| T-260-T3 | Spoofing | a regression ignores cross-check disagreement | mitigate | `test_crosscheck_disagreement_records_divergence` pins that a divergence forces `not-ready` (D-04/D-07). |
| T-260-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pytest + stdlib only. |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_phase260_observation.py -v` exits 0.
- Tests are fully offline (fakes only); no live RouterOS, no mutation.
</verification>

<success_criteria>
- The test slice proves: mutation rejection before any live command, fail-closed on inspector error / match=false, ready-for-approval only on an all-clean agreeing window, mid-window blip forces not-ready (D-02), and cross-check disagreement forces not-ready (D-04/D-07).
</success_criteria>

<output>
Create `.planning/phases/260-dry-run-observation-rerun-canary-readiness/260-02-SUMMARY.md` when done.
</output>
