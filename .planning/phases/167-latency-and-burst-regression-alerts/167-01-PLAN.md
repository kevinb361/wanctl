---
phase: 167-latency-and-burst-regression-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/wan_controller.py
  - src/wanctl/alert_engine.py
  - tests/test_alert_engine.py
  - tests/test_health_check.py
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-03]
---

<objective>
Add bounded alert rules for latency-regression and burst-related production risk using the existing AlertEngine and existing health/metrics signals.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/167-latency-and-burst-regression-alerts/167-RESEARCH.md
@src/wanctl/alert_engine.py
@src/wanctl/wan_controller.py
@src/wanctl/health_check.py
@src/wanctl/metrics.py
@tests/test_alert_engine.py
@tests/test_health_check.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for sustained latency-regression and burst-health alert contracts</name>
  <files>tests/test_alert_engine.py, tests/test_health_check.py</files>
  <action>
Describe the production-risk cases that v1.34 must catch:
- sustained elevated latency / degraded health derived from existing bounded health signals
- repeated burst-health churn that deserves an alert
- healthy/idle conditions that must stay quiet
Keep the tests focused on alert firing, cooldown behavior, severity, and bounded details rather than on live flent mechanics.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement latency-regression and burst alert rules on the existing alert path</name>
  <files>src/wanctl/wan_controller.py, src/wanctl/alert_engine.py, tests/test_alert_engine.py</files>
  <action>
Wire new alert types through the existing AlertEngine flow. Reuse bounded controller state and existing cooldown/rule-key mechanisms instead of adding a second alert subsystem. Ensure severities and details are explicit enough for operators, but keep payloads small and stable.
Do not add new SQLite tables or high-rate alert source metrics in this phase.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_alert_engine.py -q</automated>
    <automated>.venv/bin/mypy src/wanctl/wan_controller.py src/wanctl/alert_engine.py</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Expose and document the new alert rules through config/test contracts</name>
  <files>src/wanctl/wan_controller.py, tests/test_alert_engine.py</files>
  <action>
Make the new rules fit the existing alert configuration pattern: named alert types, severity mapping, cooldown policy, and stable detail keys. Validate that normal healthy state and low-signal burst counter activity do not chatter.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q</automated>
    <automated>.venv/bin/ruff check src/wanctl/wan_controller.py src/wanctl/alert_engine.py tests/test_alert_engine.py tests/test_health_check.py</automated>
  </verify>
</task>

</tasks>

<threat_model>
| Threat | Risk | Mitigation |
|--------|------|------------|
| Alert spam from noisy latency/burst signals | High | Require sustained conditions, explicit cooldowns, and healthy-state non-fire tests |
| Hidden rule drift between config and code | Medium | Reuse existing AlertEngine rule model and cover new rule names in tests |
| Turning health into a second persistence path | Low | Reuse bounded in-memory/controller state only; no new SQLite alert-source tables |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q`
- `.venv/bin/mypy src/wanctl/wan_controller.py src/wanctl/alert_engine.py`
- `.venv/bin/ruff check src/wanctl/wan_controller.py src/wanctl/alert_engine.py tests/test_alert_engine.py tests/test_health_check.py`
</verification>

<output>
After completion, create `.planning/phases/167-latency-and-burst-regression-alerts/167-01-SUMMARY.md`
</output>
