---
phase: 167-latency-and-burst-regression-alerts
plan: 02
type: execute
wave: 2
depends_on: [167-01]
files_modified:
  - src/wanctl/wan_controller.py
  - tests/test_alert_engine.py
  - tests/test_health_check.py
autonomous: false
requirements: [ALRT-01, ALRT-02, ALRT-03]
---

<objective>
Validate that the new latency/burst alert rules behave correctly in healthy and degraded scenarios, with bounded severity and cooldown behavior suitable for production.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/167-latency-and-burst-regression-alerts/167-RESEARCH.md
@.planning/phases/167-latency-and-burst-regression-alerts/167-01-PLAN.md
@src/wanctl/wan_controller.py
@src/wanctl/alert_engine.py
@tests/test_alert_engine.py
@tests/test_health_check.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validation coverage for healthy-state silence, cooldown suppression, and severity boundaries</name>
  <files>tests/test_alert_engine.py, tests/test_health_check.py</files>
  <action>
Add focused validation tests that prove the new rules stay quiet under normal/idle conditions, suppress duplicates through cooldowns, and choose the intended severity bands for degraded-but-warning vs clearly-critical conditions.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Tighten any rule details or thresholds exposed by validation</name>
  <files>src/wanctl/wan_controller.py, tests/test_alert_engine.py</files>
  <action>
If validation shows chatter, weak severity separation, or ambiguous payload details, tighten the rule implementation without broadening scope into Phase 168/169 work. Keep the output stable and bounded.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q</automated>
    <automated>.venv/bin/mypy src/wanctl/wan_controller.py src/wanctl/alert_engine.py</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual rule sanity check against live operator state</name>
  <action>
On the production host or equivalent staging path, inspect the new alert-ready state using existing `/health`, `/metrics`, and alert-config surfaces. Confirm:
1. healthy steady-state stays quiet
2. the rule details are interpretable by an operator
3. cooldown/severity choices look operationally sane
Record one of:
- approved: alert rules ready
- approved: ready with follow-up threshold tweaks later
- blocked: alert behavior is too noisy or unclear
  </action>
  <verify>
    <automated>echo 'manual alert sanity gate ready'</automated>
  </verify>
</task>

</tasks>

<threat_model>
| Threat | Risk | Mitigation |
|--------|------|------------|
| Production-alert fatigue | High | Manual sanity gate plus cooldown/healthy-state validation before phase closeout |
| Ambiguous operator payloads | Medium | Keep detail keys stable and human-readable; test for bounded content |
| Scope bleed into monitoring/canary phases | Low | Restrict fixes to alert semantics and validation-only corrections |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_alert_engine.py tests/test_health_check.py -q`
- `.venv/bin/mypy src/wanctl/wan_controller.py src/wanctl/alert_engine.py`
- manual alert sanity gate
</verification>

<output>
After completion, create `.planning/phases/167-latency-and-burst-regression-alerts/167-02-SUMMARY.md`
</output>
