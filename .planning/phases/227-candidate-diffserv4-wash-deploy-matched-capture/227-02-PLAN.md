---
phase: 227-candidate-diffserv4-wash-deploy-matched-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase227-qdisc-verify.sh
  - tests/test_phase227_qdisc_verify.py
autonomous: true
requirements: [AB-03]
must_haves:
  truths:
    - "An operator can run a single gate that asserts diffserv4 is live on BOTH spec-router AND spec-modem before any flent run starts"
    - "A mode mismatch (besteffort, or any non-diffserv4 mode) on either NIC makes the gate exit non-zero so the capture aborts and no evidence is collected on the wrong qdisc mode"
    - "The gate is read-only on the target — it reads tc -s qdisc show via sudo -n, never mutates"
    - "The gate accepts an expected mode argument so it can also be used to confirm besteffort before the pre-flip baseline capture"
  artifacts:
    - path: scripts/phase227-qdisc-verify.sh
      provides: "post-deploy / pre-capture qdisc mode verification gate (D-01)"
      contains: "diffserv4"
    - path: tests/test_phase227_qdisc_verify.py
      provides: "regression proof the gate passes on a matching mode and aborts on a mismatch"
  key_links:
    - from: scripts/phase227-qdisc-verify.sh
      to: cake-shaper
      via: "ssh -o BatchMode=yes sudo -n tc -s qdisc show dev spec-router / spec-modem (mirrors 226 harness remote_read)"
      pattern: "tc -s qdisc show"
    - from: scripts/phase227-qdisc-verify.sh
      to: scripts/phase226-baseline-capture.sh
      via: "same SSH_HOST / iface defaults so the gate and capture see the same qdisc"
      pattern: "spec-router|spec-modem"
---

<objective>
Create the Phase 227 qdisc-verify gate (D-01): a read-only check that asserts the live CAKE
qdisc mode on BOTH Spectrum NICs matches an expected mode before any load is generated. This is
the A/B-provenance guard — a silent restart failure or a stale qdisc must never pollute the
candidate dataset by letting a capture run against the wrong diffserv mode.

Purpose: D-01 requires that after the besteffort→diffserv4 flip, `tc -s qdisc show` reports
`diffserv4` on spec-router AND spec-modem before the candidate capture starts; a mismatch aborts.
The same gate, with expected=besteffort, also confirms the anchor state before the pre-flip
baseline capture (D-07 sequence step 1).

Output: `scripts/phase227-qdisc-verify.sh` and a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md

# Reuse the existing read-only remote-read pattern; do not invent a new SSH path
@scripts/phase226-baseline-capture.sh

<interfaces>
<!-- Pattern to reuse from phase226-baseline-capture.sh (already in context). -->
remote_read() { ssh -o BatchMode=yes "$SSH_HOST" "sudo -n $cmd"; }
Defaults that MUST match the capture harness so the gate and capture see the same qdisc:
  SSH_HOST=cake-shaper  ROUTER_IFACE=spec-router  MODEM_IFACE=spec-modem

`tc -s qdisc show dev <iface>` output for CAKE includes a token such as `diffserv4` or
`besteffort` on the `qdisc cake` line. Match the mode token with a word-boundary grep on the
qdisc cake line; do NOT bare-`grep -c` an unfiltered file (grep gate hygiene).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the qdisc-verify gate script</name>
  <files>scripts/phase227-qdisc-verify.sh</files>
  <read_first>
    - scripts/phase226-baseline-capture.sh (remote_read pattern, SSH_HOST/iface defaults, require_command, arg-parse style to mirror)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md (D-01 gate contract; abort-on-mismatch is the point)
  </read_first>
  <action>
    Create `scripts/phase227-qdisc-verify.sh` (set -euo pipefail, executable). Args:
    `--expected-mode <diffserv4|besteffort>` (required), `--ssh-host` (default cake-shaper),
    `--router-iface` (default spec-router), `--modem-iface` (default spec-modem),
    `--out <file>` (optional JSON proof), `--dry-run` (print defaults/prereqs only, no SSH),
    `--help`. Reuse the 226 remote_read pattern: `ssh -o BatchMode=yes "$SSH_HOST" "sudo -n
    tc -s qdisc show dev <iface>"`. For each NIC, extract the CAKE mode token from the `qdisc cake`
    line (filter comments / non-cake lines first), and assert it equals the expected mode. Exit 0
    only when BOTH NICs match; exit non-zero with a clear "MODE MISMATCH on <iface>: expected
    <x> got <y>" message otherwise (this is the abort signal D-01 relies on). When --out is given,
    write a JSON proof {checked_utc, ssh_host, expected_mode, router_mode, modem_mode, match}. Keep
    it strictly read-only — no tc/nft/systemctl mutation, mirroring the 226 read-only posture.
  </action>
  <verify>
    <automated>bash -n scripts/phase227-qdisc-verify.sh && scripts/phase227-qdisc-verify.sh --expected-mode diffserv4 --dry-run 2>&1 | grep -qi "diffserv4\|expected"</automated>
  </verify>
  <done>`bash -n` passes; `--dry-run` prints the expected-mode/host/iface defaults and performs no SSH; the script is chmod +x.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test for pass and abort-on-mismatch</name>
  <files>tests/test_phase227_qdisc_verify.py</files>
  <read_first>
    - scripts/phase227-qdisc-verify.sh (the mode-token extraction + exit-code contract)
    - tests/ (closest existing script-shape test for style)
  </read_first>
  <action>
    Add pytest checks that exercise the mode-token matcher without a live target: feed sample
    `tc -s qdisc show` fixtures (one with `diffserv4`, one with `besteffort`) through the gate's
    parsing path (refactor the token-extraction into something testable, e.g. an injectable input
    file via an env hook or a `--router-input/--modem-input` test-only flag the script already
    reads when set), assert exit 0 when both NICs match the expected mode and non-zero on any
    mismatch. Also assert `--dry-run` exits 0 and emits the expected-mode line. Do not require SSH.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase227_qdisc_verify.py -q</automated>
  </verify>
  <done>Tests pass: a matching-mode fixture yields exit 0; a besteffort-on-one-NIC fixture yields non-zero (abort).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → cake-shaper (SSH read-only) | Gate reads `tc -s qdisc show` via `sudo -n`; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-04 | Spoofing | qdisc mode token parse | mitigate | Match the mode token on the `qdisc cake` line with comment-filtered word-boundary grep; reject if either NIC token is absent (treated as mismatch → abort). |
| T-227-05 | Tampering | gate read-only posture | mitigate | Script contains no tc/nft/systemctl mutation verbs; Task 2 covers the abort path so a wrong mode cannot pass. |
| T-227-06 | Denial of Service | SSH unreachable during gate | accept | SSH failure surfaces non-zero → capture aborts (fail-closed), which is the desired A/B-provenance behavior. |
| T-227-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses ssh + standard coreutils already present. |
</threat_model>

<verification>
- `bash -n scripts/phase227-qdisc-verify.sh` clean; script executable.
- `--dry-run` performs no SSH.
- `.venv/bin/pytest tests/test_phase227_qdisc_verify.py -q` green (pass + abort-on-mismatch).
- No production mutation in this plan.
</verification>

<success_criteria>
- A single gate asserts diffserv4 on both spec-router AND spec-modem before capture (D-01).
- A mode mismatch on either NIC aborts (non-zero) — no evidence on the wrong qdisc.
- Reusable with expected=besteffort to confirm the anchor before the pre-flip baseline (D-07).
</success_criteria>

<output>
Create `.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-02-SUMMARY.md` when done
</output>
