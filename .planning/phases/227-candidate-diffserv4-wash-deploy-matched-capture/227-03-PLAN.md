---
phase: 227-candidate-diffserv4-wash-deploy-matched-capture
plan: 03
type: execute
wave: 2
depends_on: [227-01, 227-02]
files_modified:
  - configs/spectrum.yaml
  - scripts/phase227-capture-runbook.sh
  - scripts/phase227-rollback.sh
  - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/
autonomous: false
requirements: [AB-03, AB-04]
user_setup: []
must_haves:
  truths:
    - "A fresh besteffort+EF capture is taken on the Snapshot A anchor state BEFORE any flip (D-07 step 1)"
    - "The candidate is deployed by editing exactly one configs/spectrum.yaml line (diffserv: besteffort -> diffserv4) with allow_wash:true and DL920/UL18/docsis_mode unchanged (D-01)"
    - "Deploy runs the standard scripts/deploy.sh path including the Phase 201 Spectrum predeploy gate, then restarts wanctl@spectrum.service"
    - "The phase227 qdisc-verify gate confirms diffserv4 on BOTH spec-router and spec-modem before the candidate capture starts; a mismatch aborts the capture (D-01)"
    - "The candidate+EF capture is taken into a candidate-<UTC> tree mirroring the 226 evidence layout so it diffs directly against the baseline (D-02)"
    - "diffserv4 is left live in production after capture (single deploy, D-08); no capture-then-restore"
    - "The D-09 armed abort is a GENUINE mutation-capable rollback (scripts/phase227-rollback.sh): it re-applies the Snapshot A besteffort config bytes, redeploys via scripts/deploy.sh, restarts wanctl@spectrum, then verifies besteffort on BOTH NICs via the qdisc-verify gate and confirms /health — it does NOT call the dry-run-only phase226-restore.sh as the live rollback"
    - "The abort triggers are concretely defined: (a) qdisc-verify proof state != besteffort/diffserv4 as expected; (b) /health from the SAME endpoint the 226 harness uses (http://10.10.110.223:9101/health) reports an unhealthy/degraded state per a defined field; (c) daemon crashloop defined as systemctl restart counter increasing across two polls or is-active != active — not vague language"
    - "The rollback is operator-gated (autonomous:false) but the script performs the real mutation when the operator runs it; it records a rollback proof (deployed config hash, qdisc-verify JSON, systemctl is-active, restart counters, journal excerpt)"
  artifacts:
    - path: configs/spectrum.yaml
      provides: "the one-line candidate flip (diffserv4), allow_wash unchanged"
      contains: "diffserv4"
    - path: scripts/phase227-capture-runbook.sh
      provides: "ordered D-07 sequence driver: besteffort+EF -> flip+gate -> candidate+EF -> leave live, with the D-09 armed abort delegating to phase227-rollback.sh"
      contains: "phase227-rollback"
    - path: scripts/phase227-rollback.sh
      provides: "GENUINE mutation-capable D-09 rollback: re-apply Snapshot A besteffort config + deploy + restart + verify besteffort on both NICs + health + record proof (replaces the dry-run-only phase226-restore.sh as the live abort)"
      contains: "besteffort"
    - path: .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/
      provides: "baseline-<UTC> (besteffort+EF) and candidate-<UTC> (diffserv4+EF) capture trees"
  key_links:
    - from: scripts/phase227-capture-runbook.sh
      to: scripts/deploy.sh
      via: "deploy.sh spectrum cake-shaper (standard path; Phase 201 predeploy gate fires)"
      pattern: "deploy.sh spectrum"
    - from: scripts/phase227-capture-runbook.sh
      to: scripts/phase227-qdisc-verify.sh
      via: "qdisc-verify --expected-mode diffserv4 gate before candidate capture (abort on mismatch)"
      pattern: "phase227-qdisc-verify"
    - from: scripts/phase227-capture-runbook.sh
      to: scripts/phase227-rollback.sh
      via: "D-09 armed abort delegates to the mutation-capable phase227-rollback.sh on objective failure (NOT the dry-run-only phase226-restore.sh)"
      pattern: "phase227-rollback"
    - from: scripts/phase227-rollback.sh
      to: scripts/deploy.sh
      via: "rollback re-applies Snapshot A besteffort config bytes then redeploys via the standard deploy path + restart"
      pattern: "deploy.sh spectrum"
    - from: scripts/phase227-rollback.sh
      to: scripts/phase227-qdisc-verify.sh
      via: "rollback verifies besteffort on both NICs after re-apply (fail-closed proof)"
      pattern: "phase227-qdisc-verify"
    - from: scripts/phase227-capture-runbook.sh
      to: scripts/phase226-baseline-capture.sh
      via: "verbatim harness reuse with --marked-ef for both pre-flip and candidate captures (D-02/D-03)"
      pattern: "phase226-baseline-capture.sh --marked-ef"
---

<objective>
Execute the load-bearing Phase 227 capture sequence (D-07) end to end: capture a fresh
besteffort+EF baseline on the Snapshot A anchor, flip Spectrum to diffserv4 via the standard
deploy path with a post-deploy qdisc-verify gate, capture the diffserv4+EF candidate, and leave
diffserv4 live for the Phase 228 verdict — all with an armed Snapshot-A abort path.

Purpose: AB-03 (candidate deploy + matched capture) and AB-04 (realtime-flow protection on both
besteffort and candidate). D-07 fixes the order: besteffort+EF must be captured on the SAME
anchor state, before the flip, so it is not a post-experiment re-derived baseline. D-04 requires
the marked-EF arm on BOTH the fresh besteffort capture and the candidate, empirically proving
whether besteffort wash strips the EF mark. D-08 keeps it a single deploy left live. D-09 arms
the only in-phase mutation rollback — a GENUINE, mutation-capable besteffort rollback
(phase227-rollback.sh) that re-applies Snapshot A and verifies besteffort live, NOT the
dry-run-only phase226-restore.sh.

This is the only plan in Phase 227 that mutates production (the one-line flip + restart). It is
NOT autonomous: the flip and the leave-live posture are checkpoint-gated.

Output: a `phase227-capture-runbook.sh` sequence driver, a `phase227-rollback.sh` genuine
mutation-capable D-09 rollback, the flipped `configs/spectrum.yaml`, and a `baseline-<UTC>` +
`candidate-<UTC>` evidence tree (plus qdisc-verify / rollback proof JSON) under the phase evidence dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/226-CONTEXT.md

# The config under test + the deploy / capture / restore machinery this plan orchestrates
@configs/spectrum.yaml
@scripts/deploy.sh
@scripts/phase201-predeploy-gate.sh
@scripts/phase226-baseline-capture.sh
@scripts/phase227-qdisc-verify.sh
@scripts/phase226-restore.sh
@scripts/phase226-snapshot-a.sh
@docs/BRIDGE_QOS.md

<interfaces>
<!-- Concrete invocation contracts (sources already in context). -->
Flip target line in configs/spectrum.yaml (line ~44):
  diffserv: besteffort  ->  diffserv: diffserv4     (allow_wash: true UNCHANGED; DL 920 / UL 18;
  docsis_mode: true; backend linux-cake-netlink — all unchanged.)

Deploy:        scripts/deploy.sh spectrum cake-shaper      (Phase 201 predeploy gate fires when
               WAN_NAME=spectrum; TARGET_HOST=cake-shaper). Then restart wanctl@spectrum.service.
Qdisc gate:    scripts/phase227-qdisc-verify.sh --expected-mode diffserv4   (from 227-02; abort on
               mismatch). Also run --expected-mode besteffort before the pre-flip baseline.
Capture:       scripts/phase226-baseline-capture.sh --output-dir <dir> --marked-ef   (from 227-01;
               verbatim matched arms + additive EF; reflector needs an iperf3 server on ef_ref_port).

Health (SINGLE SOURCE OF TRUTH — REVIEW HIGH 227-03):
               Use the SAME endpoint the 226 harness uses: http://10.10.110.223:9101/health
               (HEALTH_URL default in scripts/phase226-baseline-capture.sh line 8). Do NOT use
               127.0.0.1:9101 from the operator host — localhost there is the wrong host unless
               SSH-wrapped. The runbook/rollback take --health-url defaulting to that harness value.

Abort triggers (CONCRETE — REVIEW HIGH 227-03; replaces vague "/health RED" / "daemon crashloop"):
  (a) qdisc mismatch: phase227-qdisc-verify.sh --expected-mode <mode> exits non-zero (any proof
      state missing|ssh_failed|no_cake|ambiguous|wrong-token).
  (b) health unhealthy: curl -fsS --max-time 3 "$HEALTH_URL" fails (non-2xx / unreachable) OR the
      returned JSON's status field is not the healthy value the /health contract defines (read the
      health payload shape; treat any non-healthy status, or a missing/unparseable payload, as a
      trigger).
  (c) daemon crashloop: on cake-shaper, `systemctl is-active wanctl@spectrum.service` != "active",
      OR the restart counter (`systemctl show -p NRestarts wanctl@spectrum.service`) increases
      across two polls ~10s apart after the flip.

Rollback (D-09 armed abort — GENUINE mutation, NOT dry-run):
  scripts/phase227-rollback.sh  re-applies the Snapshot A besteffort config bytes
  (operator-private raw-dir deployed-spectrum.yaml from phase226-snapshot-a.sh) into
  configs/spectrum.yaml, runs scripts/deploy.sh spectrum cake-shaper, restarts
  wanctl@spectrum.service, then phase227-qdisc-verify.sh --expected-mode besteffort MUST pass on
  both NICs and /health MUST be healthy; records a rollback proof. phase226-restore.sh is dry-run
  ONLY (hard-errors on mutation) and is NOT the live rollback — it remains a config-equality proof.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ordered capture-sequence runbook driver with the armed abort</name>
  <files>scripts/phase227-capture-runbook.sh</files>
  <read_first>
    - scripts/phase226-baseline-capture.sh (capture invocation + --output-dir contract; --marked-ef + --ef-ref-port from 227-01; HEALTH_URL default at line 8)
    - scripts/phase227-qdisc-verify.sh (gate exit-code + proof-state contract from 227-02)
    - scripts/deploy.sh (spectrum deploy invocation + predeploy gate hook)
    - scripts/phase226-snapshot-a.sh (where the Snapshot A besteffort config bytes / raw-dir come from — input to the rollback)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md (D-07 order; D-08 leave-live; D-09 abort)
  </read_first>
  <action>
    Create `scripts/phase227-capture-runbook.sh` (set -euo pipefail, executable) that drives the
    D-07 sequence as discrete, individually-invocable phases so the operator stays in control of
    the production flip (this is why the plan is autonomous:false). Take `--health-url` defaulting to
    the harness value `http://10.10.110.223:9101/health` (REVIEW HIGH 227-03 — single source of truth;
    NOT 127.0.0.1:9101). Sub-commands:
    `precheck` (verify-besteffort gate via phase227-qdisc-verify --expected-mode besteffort + a
    health check against $HEALTH_URL using the concrete healthy-status check from the interfaces
    block), `baseline` (run phase226-baseline-capture.sh --output-dir <evidence>/besteffort
    --marked-ef on the anchor), `flip` (PRINT the one-line spectrum.yaml edit + the exact
    `deploy.sh spectrum cake-shaper` + `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'`
    commands for operator execution — do NOT silently mutate prod inside the script; the operator runs
    the flip at the checkpoint), `verify` (phase227-qdisc-verify --expected-mode diffserv4, abort
    non-zero on any non-diffserv4 proof state), `candidate` (phase226-baseline-capture.sh
    --output-dir <evidence>/candidate --marked-ef), `abort` (delegate to
    `scripts/phase227-rollback.sh` — the GENUINE mutation-capable D-09 rollback from Task 1; do NOT
    call phase226-restore.sh, which is dry-run only). Add a `--dry-run` that prints the full ordered
    plan and the exact commands without executing, and an armed-abort guard: after `flip`/`verify`, if
    any of the three CONCRETE abort triggers from the interfaces block fires (qdisc mismatch /
    health unhealthy / daemon crashloop), emit the abort instruction (D-09) pointing at
    phase227-rollback.sh. Evidence dirs mirror the 226 layout (baseline-<UTC> / candidate-<UTC>) so
    the trees diff directly (D-02). Do NOT add controller-path logic — this is orchestration only (SAFE-13).
  </action>
  <verify>
    <automated>bash -n scripts/phase227-capture-runbook.sh && scripts/phase227-capture-runbook.sh --dry-run 2>&1 | grep -qi "phase227-rollback" && ! scripts/phase227-capture-runbook.sh --dry-run 2>&1 | grep -qi "127.0.0.1:9101"</automated>
  </verify>
  <done>`bash -n` passes; `--dry-run` prints the full D-07 ordered sequence (precheck → baseline → flip → verify → candidate → leave live), references phase227-rollback.sh for the abort (not phase226-restore.sh), uses the 10.10.110.223:9101 health endpoint (never 127.0.0.1:9101), mutates nothing; script is chmod +x.</done>
</task>

<task type="auto">
  <name>Task 1b: Write the GENUINE mutation-capable D-09 rollback script</name>
  <files>scripts/phase227-rollback.sh</files>
  <read_first>
    - scripts/phase226-restore.sh (CONFIRM it is dry-run only — it hard-errors on any non-dry-run invocation; you are NOT extending it, you are writing a real rollback that the runbook abort calls instead)
    - scripts/phase226-snapshot-a.sh (the operator-private raw-dir / deployed-spectrum.yaml that holds the Snapshot A besteffort config bytes to re-apply)
    - scripts/deploy.sh (the standard spectrum deploy path + restart the rollback reuses)
    - scripts/phase227-qdisc-verify.sh (post-rollback besteffort verification, fail-closed proof state)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md (D-09 armed abort; SAFE-13 — controller path MUST NOT change)
  </read_first>
  <action>
    Create `scripts/phase227-rollback.sh` (set -euo pipefail, executable) — the GENUINE
    mutation-capable D-09 rollback that replaces the dry-run-only phase226-restore.sh as the live
    abort (REVIEW HIGH 227-03 CONFIRMED: phase226-restore.sh hard-errors on mutation and explicitly
    does not restore runtime qdisc/service state). Args: `--raw-dir <operator-private-dir>` (required;
    holds the Snapshot A `deployed-spectrum.yaml` besteffort config bytes), `--ssh-host`
    (default cake-shaper), `--health-url` (default http://10.10.110.223:9101/health),
    `--out <file>` (rollback proof JSON), `--dry-run` (print the ordered rollback plan + exact
    commands, mutate nothing), `--confirm` (REQUIRED for real mutation — without it the script
    refuses to mutate and only prints the plan, mirroring the conservative production-change posture
    in CLAUDE.md). With --confirm the script: (1) asserts the raw-dir besteffort config is
    byte-equal to the Snapshot A source before applying (no surprise bytes); (2) copies it into
    configs/spectrum.yaml; (3) runs `scripts/deploy.sh spectrum "$SSH_HOST"` (Phase 201 predeploy gate
    fires); (4) `ssh "$SSH_HOST" 'sudo systemctl restart wanctl@spectrum.service'`; (5) runs
    `scripts/phase227-qdisc-verify.sh --expected-mode besteffort` which MUST pass on BOTH NICs;
    (6) curls $HEALTH_URL and asserts healthy; (7) records a rollback proof JSON
    {rolled_back_utc, raw_dir_sha256, deployed_config_sha256, qdisc_verify (router_got/modem_got/match),
    systemctl_is_active, nrestarts_before, nrestarts_after, journal_excerpt}. If any verify/health
    step fails after re-apply, exit non-zero and surface the failure loudly — a failed rollback is an
    operator-escalation event, not a silent pass. Do NOT touch any controller-path source (SAFE-13);
    the only repo mutation is configs/spectrum.yaml (permitted drift).
  </action>
  <verify>
    <automated>bash -n scripts/phase227-rollback.sh && scripts/phase227-rollback.sh --raw-dir /tmp/nonexistent --dry-run 2>&1 | grep -qi "besteffort" && ! scripts/phase227-rollback.sh --raw-dir /tmp/nonexistent --dry-run 2>&1 | grep -qi "phase226-restore"</automated>
  </verify>
  <done>`bash -n` passes; `--dry-run` prints the real besteffort re-apply → deploy → restart → verify-besteffort → health → proof sequence and mutates nothing; the script refuses to mutate without --confirm; it does NOT delegate to the dry-run-only phase226-restore.sh; chmod +x.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator executes the production flip + leave-live (D-01, D-08, D-09 checkpoint)</name>
  <files>configs/spectrum.yaml, .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/</files>
  <action>BLOCKING human checkpoint — the only production mutation in Phase 227. The operator performs the besteffort→diffserv4 flip, the standard deploy + restart, the qdisc-verify gate, and the candidate capture by hand following the how-to-verify steps; Claude does not auto-execute the production flip. Per D-08 diffserv4 is left live afterward; per D-09 an objective failure (any of the three CONCRETE triggers) triggers the GENUINE mutation-capable rollback (scripts/phase227-rollback.sh --confirm), NOT the dry-run-only phase226-restore.sh.</action>
  <what-built>The runbook driver (Task 1), the GENUINE D-09 rollback script (Task 1b), the additive --marked-ef harness with distinct EF port + iperf validity (227-01), and the fail-closed qdisc-verify gate (227-02) are ready. The pre-flip besteffort+EF capture has been taken on the Snapshot A anchor. The operator-private Snapshot A raw-dir (besteffort config bytes) is available for the rollback.</what-built>
  <how-to-verify>
    1. Confirm the off-peak window, that Snapshot A is the current anchor state, and that the reference reflector (dallas) has an iperf3 server on the ef_ref_port, then run
       `scripts/phase227-capture-runbook.sh precheck` — expect the besteffort gate to PASS and the health check against http://10.10.110.223:9101/health to report healthy.
    2. Run `scripts/phase227-capture-runbook.sh baseline` (or confirm the besteffort+EF capture tree already exists).
    3. Edit `configs/spectrum.yaml`: `diffserv: besteffort` -> `diffserv: diffserv4` (allow_wash:true, DL920/UL18/docsis_mode unchanged). Record the deployed config hash.
    4. Deploy: `scripts/deploy.sh spectrum cake-shaper` (Phase 201 predeploy gate must PASS), then `ssh cake-shaper 'sudo systemctl restart wanctl@spectrum.service'`.
    5. Gate: `scripts/phase227-qdisc-verify.sh --expected-mode diffserv4 --out <evidence>/qdisc-verify-candidate.json` — MUST report diffserv4 on spec-router AND spec-modem. If ANY of the three CONCRETE abort triggers fires — (a) qdisc-verify exits non-zero (any proof state); (b) `curl -fsS --max-time 3 http://10.10.110.223:9101/health` fails or returns a non-healthy status; (c) `ssh cake-shaper 'systemctl is-active wanctl@spectrum.service'` != active OR NRestarts increases across two ~10s-apart polls — run the GENUINE D-09 rollback: `scripts/phase227-rollback.sh --raw-dir <operator-private-snapshot-A-raw-dir> --confirm --out <evidence>/rollback-proof.json` (re-applies besteffort, redeploys, restarts, verifies besteffort on both NICs + health), then STOP. Do NOT use phase226-restore.sh (dry-run only).
    6. Capture: `scripts/phase227-capture-runbook.sh candidate`.
    7. Post-candidate guard: confirm qdisc still diffserv4 on both NICs, /health healthy, `systemctl is-active` active, no restart surge, candidate capture validity retained. Then leave diffserv4 live for Phase 228 (D-08 — do NOT restore to besteffort).
  </how-to-verify>
  <resume-signal>Type "approved — diffserv4 deployed, gate passed, candidate captured, left live" or describe the failure / rollback taken (and confirm besteffort was verified live after rollback).</resume-signal>
  <verify>Operator confirms: qdisc-verify reports diffserv4 on spec-router AND spec-modem (proof JSON recorded); /health at 10.10.110.223:9101 healthy; systemctl active with no restart surge; candidate-&lt;UTC&gt; capture tree exists; diffserv4 left live — OR phase227-rollback.sh --confirm ran and besteffort is verified live on both NICs with a rollback proof recorded.</verify>
  <done>diffserv4 deployed and confirmed live on both NICs, candidate+EF capture complete, post-candidate guard passed, diffserv4 left in production (D-08) — OR the GENUINE D-09 rollback (phase227-rollback.sh --confirm) re-applied besteffort, verified it live on both NICs, and recorded a rollback proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production Spectrum CAKE (deploy/restart) | The single production mutation in Phase 227; gated by Phase 201 predeploy gate + operator checkpoint + D-09 armed abort. |
| operator workstation → cake-shaper (SSH) | Read-only for capture/gate; write only at the deploy/restart step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-07 | Tampering | wrong / stale qdisc pollutes candidate data | mitigate | D-01 qdisc-verify gate (227-02) aborts the capture unless diffserv4 is live on both NICs. |
| T-227-08 | Denial of Service | bad flip degrades production Spectrum | mitigate | Phase 201 predeploy gate fail-closes a bad config; the GENUINE D-09 rollback (phase227-rollback.sh --confirm) re-applies besteffort + redeploys + restarts + verifies besteffort on both NICs + health on any of the three CONCRETE triggers (qdisc mismatch / health unhealthy / crashloop). The dry-run-only phase226-restore.sh is NOT the live rollback (REVIEW HIGH 227-03 CONFIRMED). |
| T-227-16 | Repudiation | rollback claimed but not actually applied | mitigate | phase227-rollback.sh records a rollback proof (raw-dir + deployed config sha256, qdisc-verify JSON, systemctl is-active, NRestarts before/after, journal excerpt); a failed post-rollback verify exits non-zero and escalates rather than silently passing. |
| T-227-09 | Tampering | unintended config drift beyond the one line | mitigate | Flip is exactly one line (diffserv); SAFE-13 boundary check in 227-04 proves controller path + ATT config byte-identical. |
| T-227-10 | Repudiation | which mode was actually deployed at verdict time | mitigate | diffserv4 left live (D-08, single deploy) + qdisc-verify proof recorded so the 228 verdict reads the actually-deployed mode. |
| T-227-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses deploy.sh / restore / capture machinery. |
</threat_model>

<verification>
- `bash -n scripts/phase227-capture-runbook.sh` and `bash -n scripts/phase227-rollback.sh` clean; both `--dry-run` print the full ordered sequence without mutation.
- The runbook abort delegates to phase227-rollback.sh (NOT phase226-restore.sh); health endpoint is 10.10.110.223:9101 (never 127.0.0.1:9101).
- phase227-rollback.sh refuses to mutate without --confirm; the real path re-applies besteffort + deploy + restart + verify-besteffort on both NICs + health + records a proof.
- The three abort triggers are concretely defined (qdisc proof state / health status field / systemctl is-active+NRestarts), not vague.
- Pre-flip besteffort+EF capture tree exists on the anchor (D-07 step 1).
- Phase 201 predeploy gate PASSES on the diffserv4 config (AB-03 success criterion 1).
- qdisc-verify reports diffserv4 on spec-router AND spec-modem before candidate capture (D-01); proof JSON recorded.
- candidate-<UTC> capture tree exists with the marked-EF arm (AB-04, D-04); post-candidate guard passes.
- diffserv4 is left live post-capture (D-08).
- The only production mutation is the one-line flip + restart (and, on abort, the besteffort rollback); controller path untouched (SAFE-13, verified in 227-04).
</verification>

<success_criteria>
- Besteffort+EF captured on the anchor, then diffserv4 deployed via config + existing allow_wash gate (no controller change, AB-03 criterion 3).
- diffserv4 confirmed live on both NICs before candidate capture; mismatch aborts (D-01).
- candidate+EF captured into a baseline-diffable tree (D-02); diffserv4 left live (D-08).
- A GENUINE mutation-capable Snapshot-A rollback (phase227-rollback.sh) is available and operator-gated for objective failure, with concrete triggers and a recorded proof — NOT the dry-run-only phase226-restore.sh (D-09, REVIEW HIGH 227-03).
- Health checks use the single source-of-truth endpoint (10.10.110.223:9101/health), matching the 226 harness (REVIEW HIGH 227-03).
</success_criteria>

<output>
Create `.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-03-SUMMARY.md` when done
</output>
