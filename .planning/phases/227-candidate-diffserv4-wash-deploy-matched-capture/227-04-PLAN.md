---
phase: 227-candidate-diffserv4-wash-deploy-matched-capture
plan: 04
type: execute
wave: 3
depends_on: [227-03]
files_modified:
  - scripts/phase225-safe13-boundary-check.sh
  - tests/test_phase227_safe13_boundary.py
  - scripts/phase227-evidence-completeness.py
  - tests/test_phase227_evidence_completeness.py
  - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json
autonomous: true
requirements: [AB-03, SAFE-13]
must_haves:
  truths:
    - "The SAFE-13 boundary check protects wan_controller_state.py: it is controller-path (imported by wan_controller.py) but is NOT in the current protected list, and expand_protected_files() only expands directory-suffixed targets — so the hole is closed by adding the file explicitly, and a change there can no longer pass the boundary check (REVIEW HIGH 227-04 CONFIRMED)"
    - "At the phase boundary, the controller path (wan_controller.py, queue_controller.py, wan_controller_state.py, cake_signal.py, alert_engine.py, fusion_healer.py) is byte-identical vs the v1.48 close — zero diff"
    - "configs/att.yaml is byte-identical at the phase boundary (ATT untouched the entire milestone)"
    - "The SAFE-13 boundary check passes even though the cake-shaper bridge nft rules / configs/spectrum.yaml diffserv line may have changed (bridge nft MAY change, controller MUST NOT)"
    - "An evidence-completeness check confirms the candidate capture produced every signal the GATE-01 thresholds will read in Phase 228 (RRUL p99, restart count, transition count, UL stability, per-tin separation), so the 228 verdict is not blocked by a missing signal"
    - "The completeness check is SCHEMA-AWARE: it requires stable top-level fields and the BE/non-BE metrics needed for tin separation, but ALLOWS mode-dependent tin key names (besteffort and diffserv4 naturally expose different tins) rather than demanding exact nested-tin equality"
    - "The completeness check validates artifact SUCCESS and run count (run_count == 3, qdisc mode proof before/during/after, marked & unmarked iperf flows valid), not merely field presence — it fails with 'not verdict-ready' rather than letting Phase 228 discover missing evidence"
    - "The candidate-<UTC> and baseline-<UTC> summaries are structurally diffable (same stable top-level shape) for the 228 apples-to-apples comparison"
  artifacts:
    - path: scripts/phase225-safe13-boundary-check.sh
      provides: "controller-path protected list extended to include wan_controller_state.py (closes the SAFE-13 hole)"
      contains: "wan_controller_state.py"
    - path: tests/test_phase227_safe13_boundary.py
      provides: "regression proof that wan_controller_state.py is in the SAFE-13 protected/expanded list so a change there fails the boundary check"
      contains: "wan_controller_state"
    - path: .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json
      provides: "SAFE-13 boundary proof (controller zero-diff incl. wan_controller_state.py + ATT byte-identical) from phase225-safe13-boundary-check.sh"
      contains: "controller_path_diff_count"
    - path: scripts/phase227-evidence-completeness.py
      provides: "asserts the candidate capture has every signal GATE-01 thresholds read (228 readiness)"
      contains: "thresholds"
    - path: tests/test_phase227_evidence_completeness.py
      provides: "regression proof the completeness check flags a missing GATE-01 signal"
  key_links:
    - from: scripts/phase227-evidence-completeness.py
      to: scripts/phase226-thresholds.json
      via: "reads the locked GATE-01 gates and asserts each required signal exists in the candidate summary"
      pattern: "phase226-thresholds.json"
    - from: .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json
      to: scripts/phase225-safe13-boundary-check.sh
      via: "boundary check run with --anchor v1.48 (controller targets + configs/att.yaml protected)"
      pattern: "safe13-boundary-check"
---

<objective>
Close Phase 227 by proving the SAFE-13 invariant holds at the phase boundary and that the
candidate capture is complete enough for the Phase 228 verdict — without doing the verdict itself.

Purpose: SAFE-13 requires zero controller-path source diff vs the v1.48 close and a byte-identical
ATT config; the bridge nft and the spectrum.yaml diffserv line MAY change. Reuse the ready-made
`phase225-safe13-boundary-check.sh` (Claude's Discretion in CONTEXT) — but FIRST close the
CONFIRMED SAFE-13 hole (REVIEW HIGH 227-04): wan_controller_state.py is controller-path (imported by
wan_controller.py) yet not in the boundary check's protected list, so a controller-state change
would currently pass; Task 0 adds it. AB-03's matched-capture
intent only pays off if the candidate produced every signal the GATE-01 thresholds read — so an
evidence-completeness check guards against a 228 verdict blocked by a missing signal. The verdict,
accept/reject decision, and evidence-gated SAFE-13-lift call all remain Phase 228.

Output: a SAFE-13 boundary proof JSON, an evidence-completeness checker, and its regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md

# Boundary + threshold machinery to reuse
@scripts/phase225-safe13-boundary-check.sh
@scripts/phase226-thresholds.json
@scripts/phase226-baseline-summary.py
@.planning/phases/226-baseline-capture-threshold-lock-snapshot-a/GATE-01-THRESHOLDS.md

<interfaces>
<!-- Concrete contracts (sources already in context). -->
SAFE-13 check: scripts/phase225-safe13-boundary-check.sh --anchor v1.48 --out <file>
  SOURCE FACT (verified, REVIEW HIGH 227-04 CONFIRMED): today the `controller_targets` list is
  wan_controller.py, queue_controller.py, cake_signal.py, alert_engine.py, fusion_healer.py
  (+ att_config = configs/att.yaml). It does NOT include src/wanctl/wan_controller_state.py, even
  though wan_controller.py imports it (`from wanctl.wan_controller_state import WANControllerState`,
  line 64). `expand_protected_files()` only expands targets that end in "/" (directories); a bare
  file like wan_controller_state.py is never auto-discovered. So a change to controller-state logic
  would currently PASS the boundary check — a real SAFE-13 hole.
  FIX (Task 0): add "src/wanctl/wan_controller_state.py" to the `controller_targets` list so it is
  protected and counted in controller_path_diff_count. (Editing this script is permitted under
  SAFE-13 — it is tooling under scripts/, not controller-path source.)
  Protected after fix (must be zero-diff): src/wanctl/wan_controller.py,
  src/wanctl/wan_controller_state.py, src/wanctl/queue_controller.py, src/wanctl/cake_signal.py,
  src/wanctl/alert_engine.py, src/wanctl/fusion_healer.py, configs/att.yaml. Emits JSON with
  controller_path_diff_count + att_config_diff_count; passed=true only when both are 0. Bridge nft /
  configs/spectrum.yaml diffserv changes do NOT fail it.

GATE-01 thresholds (scripts/phase226-thresholds.json): RRUL_P99=5.0, RESTART=10, TRANSITION=10,
  plus UL-stability and a per-tin separation rule + derived noise band. The candidate summary
  MUST expose every signal these gates read (228 consumes them via phase224-gate-eval.py /
  phase206-gate-check.py precedent).

Completeness contract (REVIEW HIGH/MEDIUM 227-04 — schema-aware + success-aware, not exact shape):
  - Require STABLE top-level fields (RRUL p99, restart/transition rates, UL-stability metric, the
    unmarked-UDP jitter/loss + unmarked-TCP throughput BUILT in 227-01, and the marked_ef block).
  - ALLOW mode-dependent tin key names: besteffort and diffserv4 expose different tins, so do NOT
    demand exact nested-tin equality between baseline and candidate; instead require the BE/non-BE
    metrics needed for the per-tin separation rule to be computable.
  - Validate SUCCESS, not just presence: run_count == 3; per-run qdisc mode proof exists
    before/during/after; the unmarked + marked iperf flows are valid (the 227-01 iperf-validity
    guard: no top-level error, has end.sum/end.sum_received); manifest validity intact.
  - On any gap, exit non-zero with "not verdict-ready: <reason>" so Phase 228 never discovers
    missing evidence after the fact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Close the SAFE-13 hole — add wan_controller_state.py to the protected list</name>
  <files>scripts/phase225-safe13-boundary-check.sh, tests/test_phase227_safe13_boundary.py</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (CONFIRM the controller_targets list at ~lines 68-72 and expand_protected_files() at ~line 107 only expands directory-suffixed targets — a bare file is never auto-added; you are adding wan_controller_state.py explicitly)
    - src/wanctl/wan_controller.py (CONFIRM line ~64 imports WANControllerState from wanctl.wan_controller_state — it IS controller-path)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md (SAFE-13 controller-path-freeze invariant)
  </read_first>
  <action>
    Add `"src/wanctl/wan_controller_state.py"` to the `controller_targets` list in
    scripts/phase225-safe13-boundary-check.sh (alongside wan_controller.py, queue_controller.py,
    cake_signal.py, alert_engine.py, fusion_healer.py). This closes the REVIEW HIGH 227-04 CONFIRMED
    hole: wan_controller_state.py is imported by wan_controller.py but was unprotected, and
    expand_protected_files() does not auto-discover bare files. Do NOT change any other protected
    entry, the diff logic, the att_config protection, or expand_protected_files() semantics — this is
    a minimal additive entry. Then add tests/test_phase227_safe13_boundary.py asserting that
    wan_controller_state.py appears in the script's protected/expanded file set (e.g. parse the
    controller_targets list out of the script source, or run the script with --out against the current
    tree and assert wan_controller_state.py is in expanded_protected_files), so the protection cannot
    silently regress. Editing this script is permitted under SAFE-13 (tooling under scripts/, not
    controller-path source).
  </action>
  <verify>
    <automated>grep -q "wan_controller_state.py" scripts/phase225-safe13-boundary-check.sh && .venv/bin/pytest tests/test_phase227_safe13_boundary.py -q</automated>
  </verify>
  <done>wan_controller_state.py is in the SAFE-13 controller_targets list; the regression test proves it is protected/expanded so a change there would be counted in controller_path_diff_count and fail the boundary check.</done>
</task>

<task type="auto">
  <name>Task 1: Run the SAFE-13 boundary check and record the proof</name>
  <files>.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the --anchor / --out contract and what counts as a controller-path / ATT diff)
    - .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-CONTEXT.md (SAFE-13: bridge nft MAY change, controller MUST NOT; ATT byte-identical)
  </read_first>
  <action>
    Run `scripts/phase225-safe13-boundary-check.sh --anchor v1.48 --out
    .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json`
    AFTER Task 0 has added wan_controller_state.py to the protected list. Confirm the emitted JSON has
    controller_path_diff_count == 0 AND att_config_diff_count == 0 (passed=true), and that
    expanded_protected_files includes src/wanctl/wan_controller_state.py (proves the hole is closed).
    If non-zero, STOP and surface which protected file drifted — a controller-path or ATT diff is a
    SAFE-13 violation and must be reconciled before the phase can close. Do not edit the controller
    files to force a pass; investigate the source of the drift. The spectrum.yaml diffserv flip from
    227-03 is expected and does NOT fail this check.
  </action>
  <verify>
    <automated>test -s .planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json && python3 -c "import json; d=json.load(open('.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/evidence/SAFE-13-BOUNDARY.json')); assert d.get('controller_path_diff_count')==0 and d.get('att_config_diff_count')==0, d; assert any('wan_controller_state.py' in f for f in d.get('expanded_protected_files', [])), 'SAFE-13 hole not closed'"</automated>
  </verify>
  <done>SAFE-13-BOUNDARY.json exists with controller_path_diff_count==0 and att_config_diff_count==0 (passed=true), and wan_controller_state.py is in expanded_protected_files (hole closed); the spectrum.yaml flip is not flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Evidence-completeness checker against the GATE-01 thresholds</name>
  <files>scripts/phase227-evidence-completeness.py</files>
  <read_first>
    - scripts/phase226-thresholds.json (the exact GATE-01 gates and the signals they read)
    - scripts/phase226-baseline-summary.py (the baseline-summary.json field shape the candidate produces, incl. the 227-01 marked_ef block)
  </read_first>
  <action>
    Create `scripts/phase227-evidence-completeness.py` that takes `--candidate-summary <baseline-summary.json>`
    (and optionally `--baseline-summary`), `--thresholds scripts/phase226-thresholds.json`, and
    optionally `--run-tree <dir>` (the candidate capture tree, for run-count / artifact-validity
    checks). It is SCHEMA-AWARE and SUCCESS-AWARE (REVIEW HIGH/MEDIUM 227-04):
    (1) Require STABLE top-level signals the GATE-01 gates read: RRUL p99 latency, restart count,
    transition count, UL-stability metric, the per-tin separation inputs, plus the unmarked-UDP
    jitter/loss + unmarked-TCP throughput (BUILT in 227-01) and the marked_ef block (AB-04). For each
    missing signal print "not verdict-ready: MISSING GATE-01 signal: <name>" and exit non-zero.
    (2) Do NOT demand exact nested-tin equality — besteffort and diffserv4 expose DIFFERENT tin key
    names; require the BE/non-BE metrics needed for the separation rule to be computable, but allow
    mode-dependent tin names.
    (3) Validate SUCCESS, not just presence: when --run-tree is given, assert run_count == 3, that each
    run has the qdisc mode proof before/during/after, and that the unmarked + marked iperf flows are
    valid (consume the 227-01 iperf-validity record / valid flag — a flow recorded valid:false is a
    gap). Fail with "not verdict-ready: <reason>" on any gap.
    (4) When --baseline-summary is given, assert both summaries share the same STABLE top-level shape
    (diffable), allowing mode-dependent tin keys. Exit 0 only when all required signals are present,
    valid, and the run is complete. This is a readiness gate, not the verdict — do not compute
    accept/reject (that is Phase 228).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/phase227-evidence-completeness.py').read())" && python3 scripts/phase227-evidence-completeness.py --help 2>&1 | head -1</automated>
  </verify>
  <done>Module parses; running it against a complete candidate summary (+ a 3-run tree with valid iperf flows) exits 0; against a summary missing a GATE-01 signal, a tree with run_count != 3, or a flow recorded valid:false it exits non-zero with "not verdict-ready: <reason>"; mode-dependent tin key names do NOT cause a false gap.</done>
</task>

<task type="auto">
  <name>Task 3: Regression test for the completeness checker</name>
  <files>tests/test_phase227_evidence_completeness.py</files>
  <read_first>
    - scripts/phase227-evidence-completeness.py (the required-signal list + exit-code contract)
    - scripts/phase226-thresholds.json (build fixture summaries that satisfy / violate the gates)
  </read_first>
  <action>
    Add pytest checks: (a) a complete summary (all GATE-01 signals + unmarked iperf + marked_ef
    present) + a 3-run tree with valid iperf flows → exit 0; (b) an incomplete summary (drop one
    required signal, e.g. RRUL p99) → non-zero naming the missing signal; (c) a run tree with
    run_count != 3 → non-zero "not verdict-ready"; (d) a flow recorded valid:false (iperf error)
    → non-zero "not verdict-ready"; (e) SCHEMA-AWARENESS: a baseline summary with besteffort tin keys
    and a candidate summary with diffserv4 tin keys do NOT trigger a false shape-mismatch gap (the
    stable top-level fields match; mode-dependent tin names are allowed); (f) the --baseline-summary
    stable-shape check still fails when a STABLE top-level field is missing. Do not require a live
    capture — use temp JSON fixtures and a temp run-tree dir.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase227_evidence_completeness.py -q</automated>
  </verify>
  <done>Tests pass: complete summary+3-run tree → 0; missing signal / run_count!=3 / invalid flow → non-zero "not verdict-ready"; mode-dependent tin keys do not false-fail; stable-shape diff enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → SAFE-13 boundary check | Reads git state + file hashes vs v1.48; no mutation. |
| candidate evidence → completeness gate | Read-only validation of capture artifacts against locked thresholds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-11 | Tampering | controller-state drift slips past the boundary check | mitigate | Task 0 adds wan_controller_state.py to the SAFE-13 protected list (it was a CONFIRMED hole — imported by wan_controller.py but unprotected; expand_protected_files only expands directories). The boundary check now counts it in controller_path_diff_count; a state change fails the gate. |
| T-227-12 | Tampering | controller / ATT config drift | mitigate | phase225-safe13-boundary-check.sh asserts all controller_targets (incl. wan_controller_state.py) + configs/att.yaml byte-identical vs v1.48; non-zero diff stops the phase (Task 1). |
| T-227-13 | Information Disclosure | incomplete / failed-flow candidate capture silently accepted | mitigate | Schema+success-aware completeness gate fails closed ("not verdict-ready") on any missing GATE-01 signal, run_count != 3, or invalid iperf flow, so 228 is not blocked later. |
| T-227-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses python3 stdlib + existing scripts. |
</threat_model>

<verification>
- wan_controller_state.py is in the SAFE-13 protected list (hole closed, REVIEW HIGH 227-04); `.venv/bin/pytest tests/test_phase227_safe13_boundary.py -q` green.
- `phase225-safe13-boundary-check.sh --anchor v1.48` → controller_path_diff_count==0, att_config_diff_count==0, and wan_controller_state.py in expanded_protected_files.
- The spectrum.yaml diffserv flip is NOT flagged by the boundary check (bridge/spectrum may change).
- Completeness gate exits 0 on the real candidate summary + 3-run tree (all GATE-01 signals present, flows valid); fails "not verdict-ready" on a missing signal / run_count!=3 / invalid flow.
- Mode-dependent tin key names (besteffort vs diffserv4) do not cause a false shape-mismatch.
- `.venv/bin/pytest tests/test_phase227_evidence_completeness.py -q` green.
- No verdict / accept-reject computed here (that is Phase 228).
</verification>

<success_criteria>
- The SAFE-13 hole is closed: wan_controller_state.py is protected; a controller-state change fails the boundary check (REVIEW HIGH 227-04).
- SAFE-13 verified at the phase boundary: zero controller-path diff (incl. wan_controller_state.py) vs v1.48 close; ATT byte-identical (AB-03 criterion 4, SAFE-13).
- Bridge nft / spectrum.yaml diffserv change permitted and not flagged.
- Candidate capture proven complete AND successful (schema-aware, run_count==3, valid flows) — not just field-present — so 228 is never blocked by missing evidence (REVIEW HIGH/MEDIUM 227-04).
</success_criteria>

<output>
Create `.planning/phases/227-candidate-diffserv4-wash-deploy-matched-capture/227-04-SUMMARY.md` when done
</output>
