---
phase: 214-measurement-collapse-investigation
plan: 06
type: execute
wave: 5
depends_on: ["05"]
files_modified:
  - .planning/phases/214-measurement-collapse-investigation/214-REPORT.md
  - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
autonomous: false
requirements: [MEAS-02, MEAS-03]
tags: [report, runbook, todo-closure, operator-driven, observational-disposition]

must_haves:
  truths:
    - "Operator has run the three Spectrum `tcp_12down` windows (off-peak, daytime, prime-time) via scripts/phase214-flent-matrix.sh and captured one valid run per window (D-02)."
    - "Per-window signal-sheet.json + matrix-summary.json exist under `.planning/phases/214-measurement-collapse-investigation/evidence/`."
    - "214-REPORT.md exists with: matrix verdict, per-window p99 table, ranked driver narrative with cited evidence, Signal Disposition section (Form B/C documented; Form A described as future-phase candidate; or 'no signal needed' with cited evidence per D-12), and v1.46 safety attestation."
    - "Folded todo `2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` is either closed (verdict=pass, documented 'not reproduced') OR carried forward with narrower next steps (verdict=fail OR ambiguous)."
    - "MEAS-03 mutation-boundary structural test PASSES against the final REPORT (no forbidden mutation-recommendation tokens in 214-REPORT.md)."
    - "Optional ATT contrast (D-04) run iff Spectrum reproduces or is inconclusive; if run, captured in matrix-summary as an `att-contrast` window."
  artifacts:
    - path: ".planning/phases/214-measurement-collapse-investigation/214-REPORT.md"
      provides: "Operator-facing matrix verdict, ranked driver classification, Signal Disposition recommendation (observational-first per D-12/D-13), folded-todo decision, v1.46 safety attestation"
    - path: ".planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md"
      provides: "Updated with Phase 214 verdict; either marked closed (move to closed/) or carried with narrower next steps"
  key_links:
    - from: ".planning/phases/214-measurement-collapse-investigation/214-REPORT.md"
      to: ".planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json"
      via: "explicit citation of matrix-summary.json fields and per-window run_dir paths"
      pattern: "matrix-summary\\.json|RUN-"
    - from: ".planning/phases/214-measurement-collapse-investigation/214-REPORT.md"
      to: ".planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md"
      via: "Folded-todo decision section names the todo and states close/carry"
      pattern: "2026-04-08-investigate-tcp-12down"
---

<objective>
Close the phase. Operator runs the bounded reproduction matrix per D-01/D-02/D-03 (manual; window-gated), aggregates outputs via 214-05's matrix-summary, and authors `214-REPORT.md` carrying the verdict + signal disposition + folded-todo decision. The report MUST be observational-first per D-12/D-13/D-14 and the MEAS-03 structural mutation-guard from 214-05 MUST pass against the committed REPORT before /gsd-verify-work runs. Live runs are non-autonomous (operator-driven, calendar-gated); analyzer composition + report authorship are guided steps for the operator + Claude.

Purpose: MEAS-02 = explicit explanation or documented "not reproduced"; MEAS-03 = observational-first signal disposition. The REPORT is where both close.
Output: Committed `214-REPORT.md` + updated folded-todo state (closed or carried-narrower).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-measurement-collapse-investigation/214-CONTEXT.md
@.planning/phases/214-measurement-collapse-investigation/214-RESEARCH.md
@.planning/phases/214-measurement-collapse-investigation/214-VALIDATION.md
@.planning/phases/213-experience-baseline-harness/213-REPORT.md
@.planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md
@scripts/phase214-flent-matrix.sh
@scripts/phase214-matrix-summary.py

<interfaces>
<!-- 214-REPORT.md required section structure (locked by MEAS-02/MEAS-03 + D-12/D-13/D-14).

# Phase 214 — Measurement Collapse Investigation REPORT

## TL;DR
- Matrix verdict: pass | fail | ambiguous
- Folded-todo decision: closed (not reproduced) | carried-narrower
- Signal disposition: form_b | form_c | none
- Safety attestation: no src/wanctl/ edits; no production mutation

## Matrix Verdict
- Per-window table:
  | Window | Run Dir | p50_ms | p95_ms | p99_ms | Verdict | Primary Driver |
- Cited matrix-summary.json path.

## Driver Classification
- Primary driver narrative (with cycle-level evidence citations: row indices from
  aligned-window.json, timestamps, /health field values).
- Ranked drivers list if multiple fired.
- "Missing evidence" call-out if a driver was suspected but data was incomplete.

## Signal Disposition
- If verdict=fail:
    - Form B (this phase's signal-sheet rule) — already shipped in classifier
    - Form C (alert recommendation) — design only, deferred to follow-up phase
    - Form A (/health.degraded_quality field) — described as future-phase candidate,
      NOT implemented in Phase 214 (per D-12/D-13)
- If verdict=pass: "No signal needed; bad-p99-while-GREEN not reproduced under
   matrix conditions."
- If verdict=ambiguous: "Observational signal not yet justified; carry todo with
   narrower next steps."

## Folded Todo Decision
- Reference: .planning/todos/pending/2026-04-08-investigate-tcp-12down-...
- Close (move to closed/) iff verdict=pass.
- Carry forward with narrower next steps (specific window/condition/test type)
  iff verdict=fail or ambiguous.

## v1.46 Safety Attestation (D-14)
- Zero src/wanctl/ edits across the phase.
- Zero RouterOS writes.
- Zero production service restarts.
- Zero steering toggles.
- Zero /etc/wanctl/*.yaml edits.
- PHASE214_BASE_SHA used by the mutation-guard structural test: <sha>.
- mutation-boundary pytest result: PASS.

## Open Questions Carried (from 214-RESEARCH.md)
- A1: fusion-suspension field name in v1.45.0 health_check.py — verified during
  Wave 0 or carried as future-phase work.
- A2: journal log regex stability — verified or noted as drift candidate.
- Q3: ATT contrast windowing decision (same-window or separate) — record actual
  choice if D-04 contrast was run.

## Provenance
- Phase commit range: <BASE_SHA>..<HEAD_SHA>
- Live runs: <off-peak-RUN-id>, <daytime-RUN-id>, <prime-time-RUN-id>, optional <att-RUN-id>
- Analyzer scripts: phase214-{flent-matrix.sh,extract,align,classify,matrix-summary}.py
- Test suite snapshot: `pytest tests/test_phase214_*.py -q` output

## Phase 215 Hand-off
- matrix-summary.json schema fields Phase 215 will consume.
- Any operating-point constraints surfaced by this matrix (read-only annotations only;
  Phase 215 owns any reclaim canary decision).

-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator executes the three-window Spectrum tcp_12down matrix and aggregates per-window signal-sheets into matrix-summary.json (calendar-gated; non-autonomous)</name>
  <action>Operator-driven; see the structured fields below (<what-built>, <how-to-verify>, <resume-signal>). The autonomous portion is the per-window post-processing chain (phase214-align.py → phase214-classify.py → phase214-matrix-summary.py); the calendar-gated portion is the three live invocations of scripts/phase214-flent-matrix.sh.</action>
  <files>.planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/{signal-sheet.json,signal-sheet.md,aligned-window.json}, .planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json</files>
  <verify><automated>test -f .planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json && jq -e ".verdict" .planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json</automated></verify>
  <done>Three Spectrum windows captured (or operator declared partial-state carry-forward); matrix-summary.json exists with a verdict ∈ {pass,fail,ambiguous}; structural mutation-guard pytest still passes with the new signal-sheet.md files in scope.</done>
  <what-built>
    Plans 214-01..214-05 ship the matrix orchestrator, extractor, aligner, classifier, and matrix-summary aggregator with full pytest coverage and a structural mutation-guard. This task is the calendar-gated live-run step that produces real evidence — three Spectrum `tcp_12down` windows captured across off-peak / daytime / prime-time per D-01/D-02/D-03.
  </what-built>
  <how-to-verify>
    Before triggering the live runs, the operator must:

    1. Export `PHASE214_BASE_SHA="$(git rev-parse HEAD)"` to lock the D-14 src/wanctl/ diff guard against the planning-time SHA.
    2. Confirm a clean `src/wanctl/` tree: `git diff --quiet HEAD -- src/wanctl/ && echo CLEAN`.
    3. Confirm dev-VM source bind `10.10.110.226` is present: `ip -4 addr show | grep -q 10.10.110.226 && echo BIND-OK`.
    4. Confirm Spectrum egress signature from that bind: `curl -s --interface 10.10.110.226 --max-time 5 https://ifconfig.io` returns `70.123.224.169`.
    5. Confirm cake-shaper SSH + sudo: `ssh -o BatchMode=yes cake-shaper "sudo -n true && echo SUDO-OK"`.

    Live runs (operator-driven, calendar-gated):

    - Off-peak window (local hours 01..05): `bash scripts/phase214-flent-matrix.sh off-peak`
    - Daytime window  (local hours 10..16): `bash scripts/phase214-flent-matrix.sh daytime`
    - Prime-time window (local hours 19..22): `bash scripts/phase214-flent-matrix.sh prime-time`

    Each run produces a `RUN-<UTC-ts>/spectrum/tcp_12down/` directory containing `.flent.gz`, `health-spectrum.ndjson`, `alerts-spectrum.json`, `steering-pre-state.json`, `steering-post-state.json`, `journal-window.ndjson`, `manifest.json`, plus the Phase 214 sidecar `phase214-window.json`.

    Retry policy (D-02): one retry permitted per window iff the run failed with invalid artifacts OR netperf no-data. NEVER retry "because the p99 looked too clean" — retrying to chase a desired result invalidates the matrix.

    Optional ATT contrast (D-04): only run iff Spectrum reproduces collapse OR is inconclusive. Same wrapper, but operator manually invokes phase213-baseline-capture.sh with `--bind-map att=10.10.110.233 --wans att --tests tcp_12down --evidence-root .planning/phases/214-measurement-collapse-investigation/evidence` in the same window as the triggering Spectrum run. Record the windowing decision (Open Question Q3) in matrix-summary.json.

    Per-window post-processing (autonomous; can be scripted in the next task):

    - For each `RUN-*/spectrum/tcp_12down/`:
      `.venv/bin/python3 scripts/phase214-align.py --flent-gz RUN-*/spectrum/tcp_12down/*.flent.gz --health-ndjson RUN-*/spectrum/tcp_12down/health-spectrum.ndjson --journal-ndjson RUN-*/spectrum/tcp_12down/journal-window.ndjson --alerts-json RUN-*/spectrum/tcp_12down/alerts-spectrum.json --output-json RUN-*/spectrum/tcp_12down/aligned-window.json`
      `.venv/bin/python3 scripts/phase214-classify.py --aligned-window RUN-*/spectrum/tcp_12down/aligned-window.json --flent-gz RUN-*/spectrum/tcp_12down/*.flent.gz --window-label <off-peak|daytime|prime-time> --output-json RUN-*/spectrum/tcp_12down/signal-sheet.json --output-md RUN-*/spectrum/tcp_12down/signal-sheet.md`

    After all three windows complete, run the aggregator:
    `.venv/bin/python3 scripts/phase214-matrix-summary.py --evidence-root .planning/phases/214-measurement-collapse-investigation/evidence --output-json .planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json`

    Verification checklist before resuming:
    - [ ] Three `RUN-*/spectrum/tcp_12down/` directories exist with `.flent.gz` + supporting artifacts.
    - [ ] `evidence/matrix-summary.json` exists and parses (`jq . evidence/matrix-summary.json`).
    - [ ] `matrix-summary.json.verdict` is one of {pass, fail, ambiguous}.
    - [ ] `.venv/bin/pytest tests/test_phase214_mutation_boundary.py -x -q` PASSES (the structural test now has real signal-sheet.md files to scan — if any contain forbidden tokens, the test will fail and the operator must edit the classifier output or revisit Plan 214-04 before resuming).
    - [ ] `git diff --quiet "${PHASE214_BASE_SHA}..HEAD" -- src/wanctl/` returns 0.
  </how-to-verify>
  <resume-signal>
    Type "matrix complete" with the matrix verdict (pass/fail/ambiguous) once all three windows have been captured, the aggregator has emitted `matrix-summary.json`, and the structural mutation-guard test has passed. If a window had to be skipped (e.g. off-peak window unavailable for several days), type "carry-forward partial: <reason>" instead and Task 2 will switch to an interim REPORT that documents the partial-matrix state and defers full closure.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author .planning/phases/214-measurement-collapse-investigation/214-REPORT.md from matrix-summary.json + aligned-window evidence</name>
  <files>.planning/phases/214-measurement-collapse-investigation/214-REPORT.md, .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md</files>
  <read_first>
    - .planning/phases/214-measurement-collapse-investigation/evidence/matrix-summary.json (the canonical verdict + per-window roll-up; emitted by Task 1)
    - .planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/signal-sheet.md (per-window narrative)
    - .planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/signal-sheet.json (cycle-level evidence for narrative citations)
    - .planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/aligned-window.json (specific row indices + timestamps for evidence quotes)
    - .planning/phases/213-experience-baseline-harness/213-REPORT.md (structural analog — section ordering, table shape, hand-off pattern)
    - .planning/phases/214-measurement-collapse-investigation/214-CONTEXT.md D-08, D-12, D-13, D-14 (signal-disposition + safety attestation language)
    - .planning/phases/214-measurement-collapse-investigation/214-RESEARCH.md §Open Questions Q1..Q4 (which questions to carry/close)
    - .planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (folded todo — destination of close/carry decision)
  </read_first>
  <action>
    Author `214-REPORT.md` following the section structure locked in `<interfaces>`. Populate every section from real evidence — no placeholders.

    TL;DR section: cite the `matrix-summary.json.verdict`; state the folded-todo decision; state signal disposition; state safety attestation outcome.

    Matrix Verdict section: build the per-window table by reading `matrix-summary.json.windows[]`. Each row: window label, run_dir basename, p50/p95/p99 (ms), per-window verdict, per-window primary driver. Cite the matrix-summary.json path.

    Driver Classification section: open the primary-driver's per-window signal-sheet.md AND the corresponding aligned-window.json. Quote specific row indices, timestamps, and field values (e.g. "at t_unix=1779920863 row 12, measurement_successful_count=0 while download_state=GREEN — see aligned-window.json"). If multiple drivers fired, write a ranked narrative; if a driver was suspected from the folded-todo but evidence was incomplete, write a "Missing evidence" call-out (e.g. "icmp_udp_divergence not classifiable because journal log strings for v1.45.0 differ from the v1.39 strings quoted in the folded todo — see Open Question A2 carry-forward").

    Signal Disposition section per D-12/D-13:
    - If verdict=fail: describe Form B (already shipped — point at the classifier output), describe Form C as a follow-up phase recommendation with specific `alert_type` proposal + cooldown sketch (DO NOT implement; design only), describe Form A (`/health.degraded_quality` field) as a future-phase candidate with field name + payload shape sketch — explicitly state "NOT implemented in Phase 214."
    - If verdict=pass: write "No signal needed; bad-p99-while-GREEN not reproduced across <N> windows. Evidence: <citations>."
    - If verdict=ambiguous: write "Observational signal not yet justified; carry folded todo with narrower next steps (e.g. specific time-of-day, specific reflector pool composition)."

    Folded Todo Decision section:
    - If verdict=pass: append a "Phase 214 closure (YYYY-MM-DD)" section to the folded todo file, then `git mv` it from `.planning/todos/pending/` to `.planning/todos/closed/`.
    - If verdict=fail or ambiguous: append a "Phase 214 narrower next steps (YYYY-MM-DD)" section to the folded todo specifying the unresolved question, the windows/conditions to re-investigate, and any new evidence requirements (e.g. log-string regex update). Leave it in `pending/`.

    v1.46 Safety Attestation section: state the exact `PHASE214_BASE_SHA` used; state the mutation-guard pytest result (PASS); enumerate zero counts for src/wanctl/ edits, RouterOS writes, service restarts, steering toggles, /etc/wanctl/ edits.

    Open Questions Carried section: review 214-RESEARCH.md §Open Questions A1, A2, Q3, Q4. For each, mark verified-during-this-phase, resolved-in-report, or carried-forward with destination (Phase 215, Phase 216, or a new follow-up phase).

    Provenance section: list commit range `<PHASE214_BASE_SHA>..$(git rev-parse HEAD)`, the three RUN-* basenames, the analyzer script paths, and a one-line `pytest` summary from running `.venv/bin/pytest tests/test_phase214_*.py -q | tail -1`.

    Phase 215 Hand-off section: state the matrix-summary.json schema fields Phase 215 will read; list any read-only operating-point annotations the matrix surfaced (e.g. "Spectrum p99 stayed below 200ms in off-peak — no upload-ceiling tuning recommended from THIS phase; Phase 215 decides independently").

    REPORT discipline: the entire file MUST NOT contain any of the forbidden mutation-recommendation tokens enforced by `tests/test_phase214_mutation_boundary.py::test_report_has_no_mutation_recommendation_tokens`. Run that test as part of verification.

    Do NOT modify any file under `src/wanctl/`. Do NOT back-edit any `scripts/phase213-*` or any `scripts/phase214-*` script. Do NOT add controller-path recommendations to the REPORT.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase214_mutation_boundary.py::test_report_has_no_mutation_recommendation_tokens tests/test_phase214_mutation_boundary.py::test_no_src_wanctl_diff -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/214-measurement-collapse-investigation/214-REPORT.md`.
    - REPORT contains literal substrings: `TL;DR`, `Matrix Verdict`, `Driver Classification`, `Signal Disposition`, `Folded Todo Decision`, `v1.46 Safety Attestation`, `Open Questions Carried`, `Provenance`, `Phase 215 Hand-off`.
    - REPORT contains `Signal Disposition` AND `observational` (matches the MEAS-03 grep gate from 214-VALIDATION.md).
    - REPORT does NOT contain any forbidden mutation-recommendation token (enforced by `tests/test_phase214_mutation_boundary.py::test_report_has_no_mutation_recommendation_tokens` — must PASS).
    - REPORT cites at least one specific `t_unix` or aligned-window row index when discussing the primary driver (verified by `grep -E 't_unix|row [0-9]+' 214-REPORT.md`).
    - Folded todo `.planning/todos/pending/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md` either has a new "Phase 214 closure" / "Phase 214 narrower next steps" section appended, OR has been `git mv`-ed to `.planning/todos/closed/` (verdict=pass case).
    - `git diff --quiet "${PHASE214_BASE_SHA}..HEAD" -- src/wanctl/` returns 0 (verified by the structural test).
    - `.venv/bin/pytest tests/test_phase214_mutation_boundary.py -x -q` reports all unskipped tests passing.
  </acceptance_criteria>
  <done>
    `214-REPORT.md` is committed; folded todo is closed or carried-narrower with explicit Phase 214 closure section; all structural mutation-boundary pytest cases pass against the final REPORT; matrix verdict and primary-driver narrative cite specific aligned-window evidence; no controller-path recommendations slipped into the REPORT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live matrix runs | Calendar-gated; runs invoke phase213-baseline-capture.sh which performs read-only production touches (HTTP GET /health, SSH read journalctl + sqlite3, steering snapshot) and source-bound flent traffic generation. |
| operator → REPORT authorship | Human authorship + Claude assistance; outputs are documentation; no code change. |
| REPORT → folded-todo state mutation | `git mv` from pending/ to closed/, or append section in pending/. Local filesystem only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-06-01 | Tampering | REPORT slips a mutation recommendation past the operator (e.g. "consider lowering ceiling_mbps to 16") | mitigate | `test_report_has_no_mutation_recommendation_tokens` runs against the committed REPORT; fails the verification step if any forbidden token appears. |
| T-214-06-02 | Tampering | Folded todo silently closed (`git rm` instead of `git mv`) and decision rationale lost | mitigate | Acceptance criteria require either an appended closure section OR `git mv` to closed/; both preserve history. `git rm` is not authorized. |
| T-214-06-03 | Tampering | Live runs accidentally trigger production mutation (e.g. operator runs the wrong wrapper) | mitigate | Plan 214-01's wrapper enforces source-bound egress refusal + D-14 src/wanctl/ diff guard at runtime; checkpoint instructions reiterate pre-flight checks. |
| T-214-06-04 | Information Disclosure | REPORT cites raw journal events that contain hostnames/IPs not intended for the public-safe project docs | accept | This REPORT lives in `.planning/phases/` (not public-safe per CLAUDE.md). Reflector public IPs (8.8.8.8, 1.1.1.1, etc.) are not sensitive. Steering snapshots already D-08-redacted. |
| T-214-06-05 | Repudiation | REPORT's safety attestation lacks the PHASE214_BASE_SHA needed for an after-the-fact audit | mitigate | Acceptance criteria require the exact SHA in the Safety Attestation section AND the Provenance section's commit range. |
| T-214-06-06 | Tampering | Open Questions section silently drops a verified-as-needed carry-forward (e.g. A1 fusion field never resolved and not flagged) | mitigate | Section is mandatory and enumerates all four Open Questions from 214-RESEARCH.md with one of three explicit dispositions (verified / resolved / carried). |
</threat_model>

<verification>
- Three Spectrum windows captured under `.planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/` (verified by `ls -1d .planning/phases/214-measurement-collapse-investigation/evidence/RUN-* | wc -l` >= 3, OR documented partial state if Task 1's checkpoint resumed with "carry-forward partial").
- `evidence/matrix-summary.json` exists and parses; `verdict` is one of {pass, fail, ambiguous}.
- `214-REPORT.md` exists with all 9 required sections present.
- `.venv/bin/pytest tests/test_phase214_mutation_boundary.py -x -q` — all unskipped tests PASS (signal-sheet.md scans no longer skip; REPORT scan no longer skips).
- Folded todo state matches the verdict (closed/ for pass; pending/ with narrower-next-steps section for fail/ambiguous).
- `git diff --quiet "${PHASE214_BASE_SHA}..HEAD" -- src/wanctl/` returns 0.
- `git diff --quiet "${PHASE214_BASE_SHA}..HEAD" -- scripts/phase213-classify.py scripts/phase213-baseline-capture.sh` returns 0.
</verification>

<success_criteria>
- Matrix verdict authored from real evidence (or partial-state interim REPORT if operator carry-forwarded).
- Folded todo closed (verdict=pass) or carried with narrower next steps (verdict=fail/ambiguous) — explicitly, not silently.
- Signal disposition is observational-first per D-12/D-13.
- v1.46 safety attestation states zero controller/RouterOS/steering/service mutation with the exact BASE SHA.
- Structural mutation-boundary pytest enforces the observational invariant against the committed REPORT.
- No edits under `src/wanctl/`; no back-edit of any prior Phase 213 or Phase 214 script.
</success_criteria>

<output>
After completion, create `.planning/phases/214-measurement-collapse-investigation/214-06-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY records: matrix verdict; primary driver; folded-todo final state (closed vs carried); signal-disposition form chosen; whether D-04 ATT contrast was run; carried open questions destined for Phase 215/216.
</output>
