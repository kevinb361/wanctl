---
phase: 220-matrix-runner-scope-a1
plan: 04
type: execute
wave: 2
depends_on:
  - 220-02
  - 220-03
files_modified:
  - docs/PHASE220-MATRIX-RUNNER.md
  - .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md
  - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json
  - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json
  - .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md
autonomous: false
requirements:
  - MATRIX-01
  - MATRIX-02
  - MATRIX-03
  - MATRIX-04
  - AGGREGATE-01
  - AGGREGATE-02
  - AGGREGATE-03
  - SAFE-11

must_haves:
  truths:
    - "docs/PHASE220-MATRIX-RUNNER.md exists and describes the new CLI tools (wrapper + aggregator) for operator consumption only — no threshold-tuning language, no future-tuning prediction, no controller-policy recommendation"
    - "One wet daytime control cell rehearsal evidence directory exists under .planning/phases/220-matrix-runner-scope-a1/evidence/ with a committed phase220-cell.json sidecar, the Phase 214 signal-sheet.json, and a REHEARSAL-VERDICT.md noting whether the verdict matches the Phase 214 canonical dallas anchor"
    - "The rehearsal evidence has reproduced the Phase 214 canonical dallas daytime verdict against the UNCHANGED extractor + aligner + classifier chain (faithful harness proof per Success Criterion #3)"
    - "tests/test_phase220_mutation_boundary.py docs-token test asserts no FORBIDDEN_MUTATION_RE matches in docs/PHASE220-MATRIX-RUNNER.md (no threshold-tuning language, no future-tuning prediction)"
    - "All Phase 220 success criteria (#1 through #5) from ROADMAP have committed artifacts; the phase is ready for /gsd:verify-work"
  artifacts:
    - path: docs/PHASE220-MATRIX-RUNNER.md
      provides: "Operator-facing description of phase220-target-path-matrix.sh + phase220-matrix-aggregator.py"
      contains: "Usage"
    - path: .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md
      provides: "Step-by-step protocol for operator to run the wet daytime control cell"
      contains: "phase220-target-path-matrix.sh"
    - path: .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json
      provides: "Wet rehearsal cell-manifest sidecar (Success Criterion #3 evidence)"
      contains: "schema_version"
    - path: .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md
      provides: "Verdict comparison: Phase 214 anchor vs Phase 220 rehearsal"
      contains: "verdict"
  key_links:
    - from: .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md
      to: .planning/phases/214-measurement-collapse-investigation/evidence/  # or the v1.46 archived path
      via: cross-reference to the Phase 214 dallas daytime signal-sheet verdict
      pattern: "Phase 214 dallas daytime"
    - from: docs/PHASE220-MATRIX-RUNNER.md
      to: scripts/phase220-target-path-matrix.sh
      via: usage documentation refers to the wrapper CLI surface
      pattern: "phase220-target-path-matrix.sh"
---

<objective>
Land Success Criterion #3 (wet daytime control cell rehearsal reproducing the Phase 214 canonical dallas anchor) and document the new CLI tools for operators. The rehearsal is operator-driven (requires live Spectrum + dallas reflector + local hour in 10..16); the plan documents the protocol, ships the docs, and pauses for the operator to execute the rehearsal and commit its evidence. Plan 04 is `autonomous: false` because the wet rehearsal is gated on a checkpoint:human-action — the operator runs the wrapper on the live network and confirms the verdict reproduces the Phase 214 anchor.

Purpose: Per ROADMAP Success Criteria #3 ("One wet daytime control cell (Spectrum + Phase 214 canonical `dallas`) reproduces the Phase 214 canonical anchor verdict against the unchanged extractor + aligner + classifier chain — proves the harness is faithful before any supplemental cells are run") and CONTEXT.md scope ("docs/ edits describe new CLI tools only — no threshold-tuning language, no future-tuning prediction").

Output: One docs file describing the CLI tools, one rehearsal protocol document in the evidence dir, plus the committed evidence (cell-manifest + signal-sheet + verdict comparison) produced by the operator-driven wet run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-matrix-runner-scope-a1/220-CONTEXT.md
@.planning/phases/220-matrix-runner-scope-a1/220-RESEARCH.md
@.planning/phases/220-matrix-runner-scope-a1/220-VALIDATION.md
@scripts/phase220-target-path-matrix.sh
@scripts/phase220-matrix.yaml
@scripts/phase220-matrix-aggregator.py
@tests/test_phase220_mutation_boundary.py

<interfaces>
<!-- Plan 04 produces documentation and rehearsal evidence; no new code surfaces.
     The rehearsal protocol invokes the Plan 03 wrapper exactly as a Phase 221
     operator would, then runs the Plan 02 aggregator against the resulting
     evidence. The verdict is cross-referenced against the Phase 214 dallas
     daytime committed signal-sheet to prove faithful composition. -->

Phase 214 anchor reference (the verdict we must reproduce):
  Look for the dallas daytime signal-sheet.json under one of:
    .planning/phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/signal-sheet.json
    .planning/milestones/v1.46-phases/214-measurement-collapse-investigation/evidence/RUN-*/spectrum/tcp_12down/signal-sheet.json

  The `verdict` field (pass | fail | ambiguous) AND `primary_driver` is the anchor.
  REHEARSAL-VERDICT.md compares the Phase 220 rehearsal cell's verdict against it.

Wet rehearsal command line (run by operator during local hour 10..16 on Spectrum):
  export PHASE220_BASE_SHA=$(.venv/bin/python -c "import yaml; print(yaml.safe_load(open('scripts/phase220-matrix.yaml'))['base_sha'])")
  ./scripts/phase220-target-path-matrix.sh --cell dallas__spectrum__daytime --replicate 1

  Produces:
    .planning/phases/220-matrix-runner-scope-a1/evidence/RUN-<timestamp>/spectrum/tcp_12down/signal-sheet.json
    .planning/phases/220-matrix-runner-scope-a1/evidence/RUN-<timestamp>/phase220-cell.json
    (file paths from Phase 213 delegate; symlinked or copied into dallas__spectrum__daytime__r1/ for ROADMAP-readable naming)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/PHASE220-MATRIX-RUNNER.md — operator-facing CLI documentation, no tuning tokens</name>
  <files>docs/PHASE220-MATRIX-RUNNER.md</files>
  <read_first>
    - scripts/phase220-target-path-matrix.sh (Plan 03 deliverable — extract usage() output verbatim)
    - scripts/phase220-matrix-aggregator.py (Plan 02 deliverable — extract argparse --help output and public API surface)
    - scripts/phase220-matrix.yaml (Plan 02 deliverable — describe schema for operators)
    - tests/test_phase220_mutation_boundary.py FORBIDDEN_MUTATION_RE (the regex that will reject any tuning tokens in this doc)
    - .planning/phases/220-matrix-runner-scope-a1/220-CONTEXT.md "Mutation-Boundary Scope (SAFE-11)" → "docs/ — describe new CLI tool only; NO threshold-tuning language; NO future-tuning prediction"
    - docs/CONFIGURATION.md (NOT to edit — read the docs/ tone; Phase 219 used this file)
  </read_first>
  <action>
    Create docs/PHASE220-MATRIX-RUNNER.md as operator-facing documentation for the two new Phase 220 CLI tools.

    Content sections (in order):

    ## Overview
    One paragraph: "Phase 220 ships two CLI tools — `scripts/phase220-target-path-matrix.sh` (per-cell evidence collector) and `scripts/phase220-matrix-aggregator.py` (cube verdict aggregator). Both compose existing Phase 213/214 surfaces without edits. The thresholds and decision rules used by the aggregator are pre-registered in `scripts/phase220-matrix.yaml` and locked at plan-commit time; this document describes how to USE the tools, not how to tune them."

    ## `phase220-target-path-matrix.sh` — per-cell wrapper
    - Usage block from the script's usage() heredoc (copy verbatim)
    - Required env vars: PHASE220_BASE_SHA (must equal the YAML's base_sha)
    - Exit codes table: 0/2/3/4/5/7 with one-line descriptions matching the script
    - Example dry-run command + expected DRY-RUN: stdout (real command, real cell_id)
    - Example live command (with the operator-driven warning: "Live runs require Spectrum + the indicated target reflector + the indicated local hour-of-day window")

    ## `phase220-matrix-aggregator.py` — cube aggregator
    - Usage block from --help output (copy verbatim; mention argparse-generated)
    - Inputs: evidence_root directory containing per-cell signal-sheet.json + phase220-cell.json pairs
    - Output: matrix-summary.json with schema_version=1 envelope; describe the JSON shape per Plan 02's matrix_verdict() return dict
    - Example: `.venv/bin/python scripts/phase220-matrix-aggregator.py --evidence-root .planning/phases/220-matrix-runner-scope-a1/evidence/ --yaml scripts/phase220-matrix.yaml --output /tmp/matrix-summary.json`

    ## `phase220-matrix.yaml` — pre-registered decision rules
    - Describe the schema fields: phase, schema_version, base_sha, driver_allowlist, locked_at, thresholds (six fields), windows, targets, paths, cells (18 entries), statistical (mwu + bootstrap_ci sections)
    - Note: "The threshold values are READ-ONLY for the aggregator. They are pre-registered per CRITERIA-01 and the kill/defect/carry verdict logic in the aggregator reads them at runtime — do not edit the YAML between cell runs."
    - DO NOT discuss what the thresholds mean operationally beyond "kill_criteria threshold" / "defect criteria threshold" — no commentary on whether they're right or wrong, no suggestion to tune them later.

    ## Statistical methods (read-only summary)
    - Mann-Whitney U two-sided with Wilcoxon mid-rank tie correction (cite by name; one sentence)
    - Bootstrap 95% percentile CI on median difference; B=2000; seed=220 (cite the seed-pinned reproducibility property; do not editorialize)
    - Both are stdlib implementations; no third-party deps

    ## Workflow
    1. Operator confirms `git rev-parse HEAD` matches `scripts/phase220-matrix.yaml`'s `base_sha` field
    2. Operator confirms src/wanctl/ is clean (git status)
    3. Operator chooses a cell id from `scripts/phase220-matrix.yaml`'s `cells:` list
    4. Operator runs the wrapper in the appropriate window-hour for the cell
    5. Operator runs the aggregator over the accumulated evidence
    6. Operator reads the matrix-summary.json verdict field

    ## What this document does NOT do
    - Does not recommend any controller threshold or algorithm change
    - Does not predict what future tuning would look like
    - Does not describe v1.48+ scope
    - Does not editorialize on whether a verdict is "good" or "bad"

    Style constraints (LOCKED by SAFE-11 mutation-boundary):
    - No occurrence of the FORBIDDEN_MUTATION_RE patterns: no `systemctl restart wanctl`, no `ceiling_mbps =`, no `setpoint_mbps =`, no `/etc/wanctl/* edit`, no `mikrotik write|set|change`, no `routeros write|set|change`, no `steering toggle|enable|disable`
    - No future-tuning predictions: forbid phrases like "in the future this will…", "v1.48 should…", "next steps include tuning…"
    - No controller policy recommendations: forbid "the controller should…", "wanctl needs to…"
    - Stick to descriptive documentation of the three artifacts (script, python script, YAML) and the operator workflow.

    The test test_phase220_docs_have_no_threshold_tuning_tokens (added in Plan 01) scans docs/*.md files that have a non-empty diff against base_sha and asserts FORBIDDEN_MUTATION_RE matches == []. This task must produce a docs file that passes that test.
  </action>
  <acceptance_criteria>
    - File docs/PHASE220-MATRIX-RUNNER.md exists and is non-empty (`test -s docs/PHASE220-MATRIX-RUNNER.md`)
    - grep -ciE "systemctl\\s+restart\\s+wanctl|ceiling_mbps\\s*[:=]|setpoint_mbps\\s*[:=]|mikrotik\\s+(write|set|change)|routeros\\s+(write|set|change)|steering\\s+(toggle|enable|disable)" docs/PHASE220-MATRIX-RUNNER.md == 0 (no FORBIDDEN_MUTATION_RE matches; case-insensitive grep on the same patterns)
    - grep -cE "^##\\s+" docs/PHASE220-MATRIX-RUNNER.md >= 5 (at least 5 H2 sections)
    - grep -c "phase220-target-path-matrix.sh" docs/PHASE220-MATRIX-RUNNER.md >= 2 (script described)
    - grep -c "phase220-matrix-aggregator.py" docs/PHASE220-MATRIX-RUNNER.md >= 2 (aggregator described)
    - grep -c "phase220-matrix.yaml" docs/PHASE220-MATRIX-RUNNER.md >= 2 (YAML described)
    - .venv/bin/pytest tests/test_phase220_mutation_boundary.py::test_phase220_docs_have_no_threshold_tuning_tokens -x -q exits 0
    - grep -ciE "in the future|v1.48|next steps include tuning|future tuning" docs/PHASE220-MATRIX-RUNNER.md == 0 (no future-tuning predictions)
    - grep -ciE "controller should|wanctl needs to" docs/PHASE220-MATRIX-RUNNER.md == 0 (no controller policy recommendations)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase220_mutation_boundary.py::test_phase220_docs_have_no_threshold_tuning_tokens -x -q</automated>
  </verify>
  <done>Docs file committed; no forbidden tokens; mutation-boundary docs test still green; operator has a complete read-only reference for the new CLI tools.</done>
</task>

<task type="auto">
  <name>Task 2: Write the rehearsal protocol document for the wet daytime control cell</name>
  <files>.planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md</files>
  <read_first>
    - .planning/phases/220-matrix-runner-scope-a1/220-CONTEXT.md "Wave 0 Fixture Strategy" + "Specific Ideas" + "Out of scope (deferred)"
    - .planning/phases/220-matrix-runner-scope-a1/220-VALIDATION.md "Manual-Only Verifications" row 1 (wet rehearsal protocol)
    - scripts/phase220-target-path-matrix.sh (the wrapper the operator will invoke)
    - scripts/phase220-matrix.yaml (the cell list — confirm dallas__spectrum__daytime exists)
    - .planning/phases/214-measurement-collapse-investigation/ OR .planning/milestones/v1.46-phases/214-measurement-collapse-investigation/ (find the dallas daytime signal-sheet.json the rehearsal must reproduce; pick whichever path exists on disk)
  </read_first>
  <action>
    Create .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md as the step-by-step protocol the operator follows to land Success Criterion #3.

    Content sections (in order):

    ## Purpose
    "ROADMAP Success Criterion #3 requires one wet daytime control cell run on the canonical Phase 214 `dallas` reflector via Spectrum to reproduce the Phase 214 canonical anchor verdict. This proves the Phase 220 harness composition (wrapper → phase213-baseline-capture.sh → unchanged Phase 214 extractor + aligner + classifier) is faithful before any supplemental cells run. The rehearsal evidence becomes the cross-check that Plan 02's aggregator and Plan 03's wrapper compose Phase 213/214 correctly."

    ## Preconditions
    - Operator is on `dev` (the production-adjacent dev VM that has SSH access to `cake-shaper`)
    - Local time is in 10..16 (daytime window per scripts/phase220-matrix.yaml)
    - Spectrum WAN is connected and reachable on the configured bind IP
    - `dallas` reflector is reachable (mtr round-trip < 200ms expected per Phase 214 anchor)
    - `git rev-parse HEAD` equals `.venv/bin/python -c "import yaml; print(yaml.safe_load(open('scripts/phase220-matrix.yaml'))['base_sha'])"`
    - `git status --porcelain src/wanctl/` is empty
    - All Phase 220 Wave 0 and Wave 1 plans (Plans 01, 02, 03) are committed and their tests pass

    ## Step-by-step
    1. Verify preconditions above (one bash one-liner per precondition; copy-paste runnable)
    2. Export PHASE220_BASE_SHA from the YAML:
       ```
       export PHASE220_BASE_SHA=$(.venv/bin/python -c "import yaml; print(yaml.safe_load(open('scripts/phase220-matrix.yaml'))['base_sha'])")
       ```
    3. Run the wrapper for the dallas__spectrum__daytime cell, replicate 1:
       ```
       ./scripts/phase220-target-path-matrix.sh --cell dallas__spectrum__daytime --replicate 1
       ```
    4. Verify the RUN-* directory was created and contains:
       - `<RUN_DIR>/spectrum/tcp_12down/signal-sheet.json` (from Phase 214 unchanged classifier)
       - `<RUN_DIR>/phase220-cell.json` (Phase 220 sidecar)
       - `<RUN_DIR>/mtr-pre-1.txt` (Phase 220 mtr snapshot)
    5. Read the verdict from the Phase 214 signal-sheet:
       ```
       jq -r '.verdict, .primary_driver' <RUN_DIR>/spectrum/tcp_12down/signal-sheet.json
       ```
    6. Cross-reference against the Phase 214 canonical dallas daytime anchor:
       - Locate the Phase 214 anchor signal-sheet.json (one of: `.planning/phases/214-measurement-collapse-investigation/evidence/...` or `.planning/milestones/v1.46-phases/214-measurement-collapse-investigation/evidence/...`)
       - Read its `verdict` and `primary_driver` fields
    7. Write REHEARSAL-VERDICT.md (Task 3) with the comparison
    8. Commit the evidence directory + REHEARSAL-VERDICT.md (Task 3 also covers the file paths)

    ## Acceptance criteria for the rehearsal
    - The Phase 220 rehearsal verdict matches the Phase 214 anchor verdict on the verdict field (pass | fail | ambiguous)
    - The Phase 220 rehearsal primary_driver matches the Phase 214 anchor primary_driver (or both are null — "pass" verdicts often have null primary_driver)
    - The Phase 220 mtr-pre.txt is committed alongside the cell evidence
    - The Phase 220 cell-manifest fields all populated correctly (target_name=dallas, target_kind=canonical, path_name=spectrum, window_name=daytime, replicate_index=1)

    ## Failure modes
    - If verdict diverges from the Phase 214 anchor: STOP. The harness is not faithful. Re-investigate Plan 03 wrapper composition; do NOT proceed to Phase 221.
    - If RUN-* directory is not created: re-run with --dry-run to inspect the delegate command; check `journalctl -u wanctl@spectrum` on cake-shaper for visibility into the upstream wanctl service health.
    - If mtr fails: install mtr (`sudo apt install mtr` on the dev VM) and re-run; mtr is required for BGP path-change detection per Pitfall 4.

    ## After the rehearsal
    - Plan 04 Task 3 writes REHEARSAL-VERDICT.md and commits the evidence
    - Plan 04 is marked complete
    - Phase 220 is ready for /gsd:verify-work
    - Phase 221 may begin (supplemental cell collection)

    This protocol is operator-driven; it is NOT runnable by an autonomous agent. The /gsd:execute-phase orchestrator will checkpoint on Plan 04 Task 3 awaiting operator confirmation that the rehearsal has been completed and the evidence committed.
  </action>
  <acceptance_criteria>
    - File .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md exists, non-empty
    - grep -c "PHASE220_BASE_SHA" .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md >= 2 (env var documented)
    - grep -c "dallas__spectrum__daytime" .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md >= 2 (cell id called out)
    - grep -c "phase214" .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md >= 2 (cross-reference to Phase 214 anchor)
    - grep -cE "^##\\s+" .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md >= 5 (well-sectioned)
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md && grep -c "PHASE220_BASE_SHA" .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md</automated>
  </verify>
  <done>Protocol file committed; operator has copy-paste-runnable steps; mutation-boundary still green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Operator runs the wet daytime control cell + commits the rehearsal evidence + writes REHEARSAL-VERDICT.md</name>
  <files>
    .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json,
    .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json,
    .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md
  </files>
  <action>
    OPERATOR-DRIVEN — see <how-to-verify> below for the full step list. In summary: operator exports PHASE220_BASE_SHA from the YAML, runs `./scripts/phase220-target-path-matrix.sh --cell dallas__spectrum__daytime --replicate 1` during local hour 10..16, confirms the RUN-* directory was created, cross-references the resulting verdict against the Phase 214 dallas daytime anchor signal-sheet.json, writes REHEARSAL-VERDICT.md with the comparison, symlinks/copies the evidence into the dallas__spectrum__daytime__r1/ subdir, and commits everything. The autonomous Plan 03/02 artifacts (wrapper, aggregator, YAML, tests) are the inputs; this checkpoint produces the wet evidence that satisfies ROADMAP Success Criterion #3.
  </action>
  <verify>
    Manual operator confirmation. Automated post-check: `test -f .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/phase220-cell.json && test -f .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json && test -f .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md && grep -q "MATCH" .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md`
  </verify>
  <done>
    Rehearsal evidence committed; Phase 220 rehearsal verdict matches Phase 214 dallas daytime anchor (✓ MATCH in REHEARSAL-VERDICT.md); Phase 220 is ready for /gsd:verify-work.
  </done>
  <what-built>
    Plan 04 Task 1 shipped docs/PHASE220-MATRIX-RUNNER.md (operator-facing CLI docs). Plan 04 Task 2 shipped the REHEARSAL-PROTOCOL.md with copy-paste-runnable steps. The remaining work is the operator-driven wet daytime control cell rehearsal — this CANNOT be autonomously executed because it requires:

    - Live Spectrum + dallas reflector connectivity (network-state-dependent)
    - Local hour in 10..16 (wall-clock-dependent)
    - sudo journalctl access on cake-shaper (auth-dependent)
    - The operator's judgment that the Phase 214 anchor cross-reference matches

    Plans 01-03 are complete; the Phase 220 codebase (wrapper, aggregator, YAML, all tests) is shipped and green. This task is the final ROADMAP Success Criterion #3 evidence committed by the operator.
  </what-built>
  <how-to-verify>
    The operator must:

    1. Read .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md (created in Task 2)
    2. Verify preconditions per the protocol (`git rev-parse HEAD` matches YAML base_sha, src/wanctl/ clean, local hour 10..16, Spectrum reachable)
    3. Run the wrapper:
       ```
       export PHASE220_BASE_SHA=$(.venv/bin/python -c "import yaml; print(yaml.safe_load(open('scripts/phase220-matrix.yaml'))['base_sha'])")
       ./scripts/phase220-target-path-matrix.sh --cell dallas__spectrum__daytime --replicate 1
       ```
    4. After completion (~60-90 seconds), confirm the evidence directory created under `.planning/phases/220-matrix-runner-scope-a1/evidence/RUN-<timestamp>/` contains:
       - `spectrum/tcp_12down/signal-sheet.json`
       - `phase220-cell.json`
       - `mtr-pre-1.txt`
       - `spectrum/tcp_12down/journal-window.ndjson`
    5. Read the Phase 220 rehearsal verdict:
       ```
       jq -r '.verdict, .primary_driver, .latency.p99_ms' <RUN_DIR>/spectrum/tcp_12down/signal-sheet.json
       ```
    6. Locate the Phase 214 canonical dallas daytime anchor signal-sheet.json (one of `.planning/phases/214-measurement-collapse-investigation/evidence/...` or `.planning/milestones/v1.46-phases/214-measurement-collapse-investigation/evidence/...`) and read its verdict + primary_driver fields.
    7. Write `.planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/REHEARSAL-VERDICT.md` containing:
       - Heading: "# Phase 220 Wet Daytime Control Cell Rehearsal — Verdict"
       - Phase 220 rehearsal verdict + primary_driver + p99_ms
       - Phase 214 anchor verdict + primary_driver
       - Comparison: ✓ MATCH (verdicts equal) or ✗ DIVERGENCE (verdicts differ; STOP)
       - Reference to the source RUN-* dir
       - Date of rehearsal (UTC)
    8. Symlink or copy the RUN-* evidence files into a friendlier path `.planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/` so future readers (Phase 221 closeout) can find them by cell-id name:
       - `dallas__spectrum__daytime__r1/phase220-cell.json` → RUN-*/phase220-cell.json
       - `dallas__spectrum__daytime__r1/signal-sheet.json` → RUN-*/spectrum/tcp_12down/signal-sheet.json
    9. Stage and commit:
       ```
       git add .planning/phases/220-matrix-runner-scope-a1/evidence/
       git commit -m "evidence(220): wet daytime control cell rehearsal reproduces Phase 214 dallas anchor"
       ```
       (project-finalizer hook will run; if SKIP_DOC_CHECK is needed, use it sparingly per CLAUDE.md)
    10. Run the final regression suite:
       ```
       .venv/bin/pytest tests/test_phase220_*.py -q
       ```
       All Phase 220 tests must pass. Specifically test_phase220_mutation_boundary.py must remain green AFTER the evidence commit (the evidence files are under .planning/, which is outside the controller path).

    Expected outcome: Verdicts match (✓ MATCH). The harness is faithful. Phase 220 is ready for /gsd:verify-work.

    Failure outcome: Verdicts diverge. STOP. Re-investigate Plan 03 wrapper composition before proceeding to Phase 221.
  </how-to-verify>
  <resume-signal>
    Type "approved — rehearsal evidence committed, verdict matches Phase 214 anchor" OR describe the divergence and we'll roll back to Plan 03 for re-investigation.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs file → mutation-boundary regex scanner | docs/PHASE220-MATRIX-RUNNER.md is scanned by tests/test_phase220_mutation_boundary.py for FORBIDDEN_MUTATION_RE matches; any threshold-tuning token is caught at test time |
| Rehearsal evidence → git commit | Evidence files (signal-sheet.json, phase220-cell.json, mtr-pre-1.txt) are committed under .planning/; no executable content, no secrets, just verdicts + latency stats + IP hops |
| Operator → live Spectrum network | Wrapper invokes phase213-baseline-capture.sh which generates real flent traffic via Spectrum; this is the same network surface Phase 213/214 already exercised; no new threat surface |
| REHEARSAL-VERDICT.md cross-reference | Document compares Phase 220 verdict to Phase 214 anchor; both are read-only file reads; no mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-04-01 | Tampering | Threshold-tuning language in docs | mitigate | Task 1 acceptance_criteria runs `tests/test_phase220_mutation_boundary.py::test_phase220_docs_have_no_threshold_tuning_tokens`; the FORBIDDEN_MUTATION_RE regex catches every banned pattern. Task 1 also has dedicated grep checks for future-tuning language and controller policy recommendations |
| T-220-04-02 | Tampering | Premature rehearsal commit | mitigate | Task 3 is `checkpoint:human-action` with `gate="blocking-human"`; orchestrator pauses until operator explicitly confirms verdict match. The operator's signature is the gate — no automation can shortcut Success Criterion #3 |
| T-220-04-03 | Information Disclosure | mtr-pre-1.txt hop IPs | accept | mtr snapshot exposes intermediate hop IPs; documented as known disclosure in Phase 220 evidence (operator awareness — same posture as Phase 214). Not a new disclosure surface for v1.47 |
| T-220-04-04 | Repudiation | Operator-driven verdict comparison | mitigate | REHEARSAL-VERDICT.md is committed with date + Phase 214 anchor file reference + Phase 220 rehearsal evidence path. Future readers can re-verify the comparison from the on-disk evidence. Operator cannot quietly fudge a verdict — the source signal-sheet.json + Phase 214 anchor are both committed |
| T-220-04-05 | Denial of Service | Wet rehearsal failure | accept | If the wrapper fails (e.g., dallas reflector unreachable), the operator iterates the protocol until it succeeds or escalates. The phase does NOT advance until the verdict committal happens. No silent-failure mode |
| T-220-04-SC | Tampering | No new package installs in Plan 04 | accept | Plan 04 ships docs + a markdown protocol + manual rehearsal evidence; no Python/npm/cargo installs. Operator may need `apt install mtr` on the dev VM (system-level, outside Python's package surface) — that is a one-time operational step, not a Phase 220 deliverable |
</threat_model>

<verification>
- .venv/bin/pytest tests/test_phase220_*.py -q  # all Phase 220 tests pass
- .venv/bin/pytest tests/test_phase220_mutation_boundary.py::test_phase220_docs_have_no_threshold_tuning_tokens -x -q  # docs scan green
- ls -1 .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/ | wc -l  # should be >= 3 after Task 3 (phase220-cell.json, signal-sheet.json, REHEARSAL-VERDICT.md)
- jq -r '.verdict' .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json  # exits 0 with non-empty verdict string
- diff <(jq -r '.verdict' .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/signal-sheet.json) <(jq -r '.verdict' <phase214_anchor_path>/signal-sheet.json)  # exit 0 = verdicts match
- git diff --stat src/wanctl/  # zero
- git diff --stat scripts/phase213-* scripts/phase214-*  # zero
</verification>

<success_criteria>
- docs/PHASE220-MATRIX-RUNNER.md committed; passes mutation-boundary docs scan
- .planning/phases/220-matrix-runner-scope-a1/evidence/REHEARSAL-PROTOCOL.md committed; operator-runnable
- Wet rehearsal evidence committed under .planning/phases/220-matrix-runner-scope-a1/evidence/dallas__spectrum__daytime__r1/
- REHEARSAL-VERDICT.md confirms ✓ MATCH between Phase 220 rehearsal and Phase 214 dallas daytime anchor
- All five ROADMAP Success Criteria satisfied:
  #1: scripts/phase220-matrix.yaml committed (Plan 02)
  #2: wrapper dry-run zero-cell rehearsal passes (Plan 03)
  #3: wet daytime control cell reproduces Phase 214 anchor (Plan 04 Task 3)
  #4: Wave 0 unit tests pass green (Plan 01)
  #5: mutation-boundary pytest green (Plan 01 + all subsequent plans)
- Phase 220 is ready for /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/220-matrix-runner-scope-a1/220-04-SUMMARY.md` when done. Include:
- Final ROADMAP Success Criteria checklist (1-5) with the evidence path for each
- Cross-reference to Phase 214 anchor used for the rehearsal verdict comparison
- Confirmation that all Phase 220 tests pass and SAFE-11 mutation-boundary is green
- Hand-off note for Phase 221: the harness is faithful; supplemental cells may now be collected per the operator's window-hour schedule
</output>
