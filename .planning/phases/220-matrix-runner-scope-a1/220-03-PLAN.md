---
phase: 220-matrix-runner-scope-a1
plan: 03
type: execute
wave: 1
depends_on:
  - 220-01
files_modified:
  - scripts/phase220-target-path-matrix.sh
  - tests/test_phase220_matrix_wrapper.py
autonomous: true
requirements:
  - MATRIX-02
  - MATRIX-03
  - MATRIX-04
  - SAFE-11

must_haves:
  truths:
    - "scripts/phase220-target-path-matrix.sh is a per-cell wrapper that mirrors the Phase 214 Wrapper Composition Pattern verbatim — gate entry, D-14 src-diff triple-check against base_sha from YAML, window-hour gate, delegate to phase213-baseline-capture.sh, write phase220-cell.json sidecar, never touches Phase 213/214 scripts"
    - "All wrapper xfail scaffolds from Plan 01 flip from XFAIL to PASS — seven dry-run gates assert exit codes 0/2/4/7 and DRY-RUN stdout shape"
    - "Per-cell pre-flight mtr snapshot is captured before delegating; post-flight mtr only re-snapshots when pre-flight diverges from previous cell's post-flight on the same target (Pitfall 4 BGP path change detection)"
    - "Wrapper reads scripts/phase220-matrix.yaml, resolves cell dimensions from --cell <cell_id>, sets PHASE214_BASE_SHA from the YAML's base_sha field (composing Phase 214's D-14 mechanism unchanged)"
    - "scripts/phase213-baseline-capture.sh and scripts/phase214-*.{sh,py} have ZERO git diff against base_sha — MATRIX-02 'zero edits' is enforced by tests/test_phase220_mutation_boundary.py"
    - "shellcheck passes clean on scripts/phase220-target-path-matrix.sh"
  artifacts:
    - path: scripts/phase220-target-path-matrix.sh
      provides: "Per-cell Phase 220 wrapper composing Phase 213/214 unchanged"
      contains: "DRY-RUN"
    - path: tests/test_phase220_matrix_wrapper.py
      provides: "Wrapper dry-run unit tests (flipped from xfail)"
      contains: "subprocess.run"
  key_links:
    - from: scripts/phase220-target-path-matrix.sh
      to: scripts/phase213-baseline-capture.sh
      via: bash subprocess invocation with --bind-map/--wans/--tests/--flent-duration/--host/--evidence-root
      pattern: "bash scripts/phase213-baseline-capture.sh"
    - from: scripts/phase220-target-path-matrix.sh
      to: scripts/phase220-matrix.yaml
      via: yq-or-python YAML read at startup; cell lookup by --cell arg
      pattern: "phase220-matrix.yaml"
    - from: scripts/phase220-target-path-matrix.sh
      to: PHASE214_BASE_SHA env var
      via: wrapper exports PHASE214_BASE_SHA="$(yq .base_sha phase220-matrix.yaml)" before D-14 guard runs
      pattern: "PHASE214_BASE_SHA"
---

<objective>
Implement the per-cell wrapper script that composes Phase 213 + Phase 214 without editing either. The wrapper accepts `--cell <cell_id> --replicate <N>` (plus the Phase 214-parity `--dry-run --test-hour <H>` debug affordance), resolves the cell's dimensions from scripts/phase220-matrix.yaml, anchors the D-14 src/wanctl/ guard against the YAML's base_sha, delegates traffic generation to phase213-baseline-capture.sh, and writes the phase-owned phase220-cell.json sidecar manifest.

Purpose: Per CONTEXT.md "Wrapper Composition Pattern" + Research R6 (Phase 220 wrapper differs from Phase 214 only in: --cell resolution, base_sha source, phase220-cell.json sidecar, mtr snapshot mechanics, --inter-cell-delay). MATRIX-02 forbids editing Phase 213/214 scripts. SAFE-11 forbids touching src/wanctl/. This plan flips all wrapper xfail scaffolds from Plan 01 to passing.

Output: One new bash script in scripts/, the existing wrapper test file in tests/ with xfail markers removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/220-matrix-runner-scope-a1/220-CONTEXT.md
@.planning/phases/220-matrix-runner-scope-a1/220-RESEARCH.md
@.planning/phases/220-matrix-runner-scope-a1/220-VALIDATION.md
@.planning/phases/220-matrix-runner-scope-a1/220-01-PLAN.md
@scripts/phase214-flent-matrix.sh
@scripts/phase213-baseline-capture.sh
@tests/test_phase220_matrix_wrapper.py

<interfaces>
<!-- The Phase 214 wrapper is the verbatim composition pattern. Phase 220 wrapper
     reuses the same structural skeleton: usage/CLI parse → window+hour gate →
     D-14 guard → delegate to phase213-baseline-capture.sh → sidecar manifest.
     The DIFFERENCES are documented in Research R6. -->

From scripts/phase214-flent-matrix.sh (composition pattern source — DO NOT EDIT):
  CLI flags: --dry-run, --test-hour <H>, <window-positional>
  Env vars: PHASE214_FLENT_DURATION (default 30), PHASE214_BASE_SHA (required), PHASE214_CAKE_SHAPER (default cake-shaper)
  Exit codes: 0 success, 2 out-of-window OR invalid CLI, 4 src/wanctl/ mutation guard failed, 5 journal/manifest failure, 7 --test-hour without --dry-run
  Window-hour gate: off-peak 01..05, daytime 10..16, prime-time 19..22
  D-14 guard: git diff --quiet for unstaged + staged + base_sha..HEAD against src/wanctl/; git status --porcelain for untracked
  Delegate cmd shape: bash scripts/phase213-baseline-capture.sh --bind-map <bind> --wans <wan> --tests <test> --flent-duration <D> --host <target> --evidence-root <root>
  Manifest write: jq -n ... > "${RUN_DIR}/phase214-window.json"

Phase 220 wrapper public surface (Plan 03 creates):
  CLI: --cell <cell_id> --replicate <N> [--dry-run] [--test-hour <H>] [--inter-cell-delay <S>] [--help|-h]
  Env: PHASE220_BASE_SHA (required, fetched from YAML and exported to PHASE214_BASE_SHA before D-14 guard), PHASE214_FLENT_DURATION (default 30 — match Phase 214), PHASE214_CAKE_SHAPER (default cake-shaper)
  Exit codes: identical mapping to Phase 214 (0/2/4/5/7) plus exit 3 for "--cell not found in YAML"
  Evidence root: .planning/phases/220-matrix-runner-scope-a1/evidence/

  YAML lookup at startup (use Python one-liner via .venv/bin/python or yq — Phase 214 uses jq for JSON; for YAML, use `.venv/bin/python -c "import yaml, sys; d=yaml.safe_load(open('scripts/phase220-matrix.yaml')); ..." ` to extract base_sha + per-cell target/path/window/bind_map; document the choice inline)

  Sidecar manifest fields (Research R2):
    schema_version=1, phase=220, target_kind, target_name, path_name, window_name,
    replicate_index, cell_id, base_sha, mtr_snapshot_path, started_utc, ended_utc, run_dir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/phase220-target-path-matrix.sh — per-cell wrapper composing Phase 213/214 unchanged</name>
  <files>scripts/phase220-target-path-matrix.sh</files>
  <read_first>
    - scripts/phase214-flent-matrix.sh (composition pattern source — copy structure verbatim, do NOT import or shell-source)
    - scripts/phase213-baseline-capture.sh (delegate target — read to understand the --bind-map, --wans, --tests, --flent-duration, --host, --evidence-root contract)
    - scripts/phase220-matrix.yaml (created by Plan 02; this wrapper reads it)
    - .planning/phases/220-matrix-runner-scope-a1/220-RESEARCH.md R6 (Phase 220 wrapper differences from Phase 214), R10 (dry-run test surface)
    - .planning/phases/220-matrix-runner-scope-a1/220-CONTEXT.md "Wrapper Composition Pattern" + "mtr/traceroute Snapshot Mechanics"
  </read_first>
  <action>
    Create scripts/phase220-target-path-matrix.sh with `#!/usr/bin/env bash` shebang and `set -euo pipefail`. Mark executable (chmod +x).

    Structural skeleton (mirror scripts/phase214-flent-matrix.sh verbatim except where R6 mandates a delta):

    1. usage() — heredoc matching Phase 214 style; documents:
       - Usage with --cell <cell_id> --replicate <N> (live) and --dry-run --test-hour <H> --cell <id> --replicate <N> (dry-run)
       - Env vars: PHASE220_BASE_SHA (required), PHASE214_FLENT_DURATION (default 30; reuse Phase 214 var name for delegate compatibility), PHASE214_CAKE_SHAPER (default cake-shaper)
       - Exit codes 0 (success), 2 (out-of-window or invalid CLI), 3 (cell not found in YAML), 4 (src/wanctl/ guard failed), 5 (journal/manifest failure), 7 (--test-hour without --dry-run)

    2. CLI parse — getopts-style while-loop matching Phase 214; required flag: --cell; required flag: --replicate; optional flags: --dry-run, --test-hour, --inter-cell-delay (default 60), --help/-h.

    3. YAML read — invoke .venv/bin/python -c "import yaml, sys; d=yaml.safe_load(open('scripts/phase220-matrix.yaml')); …" to extract:
       - base_sha (40-char hex; if not present or doesn't match `^[0-9a-f]{40}$`, exit 4)
       - cell dict matching --cell <cell_id> (if absent, exit 3 with "REFUSED: cell <id> not in scripts/phase220-matrix.yaml")
       - cell.target → resolve to targets[i].host
       - cell.path → resolve to paths[i].bind_map
       - cell.window → use as positional input to the window-hour gate
       - thresholds.canonical_control_p99_kill_ms (informational; logged but not enforced in wrapper)
       Use `python -c` with sys.exit(<code>) on failures; write a small temp script to /tmp/ if the one-liner gets unwieldy (document why inline).

    4. Export PHASE214_BASE_SHA="${PHASE220_BASE_SHA:-$YAML_BASE_SHA}" BEFORE the D-14 guard runs. Per Research R6: "reads `base_sha` from the YAML and uses it as `PHASE214_BASE_SHA` for the D-14 guard". If PHASE220_BASE_SHA is explicitly set in env, prefer it; else fall back to YAML.

    5. --test-hour validation:
       - If --test-hour is set but --dry-run is not, exit 7 ("REFUSED: --test-hour is only honored with --dry-run")
       - If --test-hour is set and --dry-run is set, use it as LOCAL_HOUR; else LOCAL_HOUR="$(date +%H)"

    6. Window-hour gate — same case/esac as Phase 214 (off-peak 01..05, daytime 10..16, prime-time 19..22); exit 2 if outside. Window value comes from the YAML cell lookup, NOT a CLI positional (R6 difference).

    7. D-14 guard — verbatim from Phase 214 (PHASE214_BASE_SHA env var, four checks: unstaged, staged, base_sha..HEAD, untracked); exit 4 on any failure.

    8. Build DELEGATE_CMD array — same shape as Phase 214:
         bash scripts/phase213-baseline-capture.sh \
           --bind-map "$BIND_MAP" \
           --wans "$WAN" \
           --tests "tcp_12down" \
           --flent-duration "$DURATION" \
           --host "$TARGET" \
           --evidence-root ".planning/phases/220-matrix-runner-scope-a1/evidence"

       BIND_MAP, WAN, TARGET resolved from the YAML cell lookup. tcp_12down is hard-coded (Phase 220 scope per CONTEXT.md "Deferred Ideas" — multi-protocol matrix is out of scope).

    9. Dry-run branch — if --dry-run, printf 'DRY-RUN:' then printf ' %q' for each delegate-cmd token; print newline; exit 0. Do NOT touch network, do NOT invoke delegate. Exactly mirrors Phase 214's dry-run.

    10. Live branch:
        a. STARTED_UTC="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        b. Pre-flight mtr: `mtr --no-dns --report -c 10 "$TARGET" > "/tmp/phase220-mtr-pre-${CELL_ID}-${REPLICATE}.txt"`. If `command -v mtr` returns non-zero, exit 5 with "ERROR: mtr not installed; required for BGP path-change detection".
        c. Pre-flight mtr divergence check: read previous cell's post-flight mtr for the SAME target (from $EVIDENCE_ROOT/.last-mtr-post-<target_name>.txt). If file exists and `diff -q` differs from pre-flight, set MTR_POST_REQUIRED=1; else MTR_POST_REQUIRED=0 (will set to 1 on first cell per target).
        d. Execute delegate: "${DELEGATE_CMD[@]}"
        e. ENDED_UTC="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        f. Find latest RUN-* dir under evidence_root; if absent, exit 5.
        g. Post-flight mtr if MTR_POST_REQUIRED == 1: capture to "${RUN_DIR}/mtr-post-${REPLICATE}.txt" and update the cached .last-mtr-post-<target>.txt; else write a tiny marker noting "post-flight skipped — pre-flight matched previous cell's post-flight".
        h. Copy pre-flight mtr to "${RUN_DIR}/mtr-pre-${REPLICATE}.txt".
        i. Write sidecar manifest:
           jq -n \
             --argjson schema_version 1 \
             --argjson phase 220 \
             --arg target_kind "$TARGET_KIND" \
             --arg target_name "$TARGET_NAME" \
             --arg path_name "$PATH_NAME" \
             --arg window_name "$WINDOW_NAME" \
             --argjson replicate_index "$REPLICATE" \
             --arg cell_id "${CELL_ID}__r${REPLICATE}" \
             --arg base_sha "$YAML_BASE_SHA" \
             --arg mtr_snapshot_path "${RUN_DIR}/mtr-pre-${REPLICATE}.txt" \
             --arg started_utc "$STARTED_UTC" \
             --arg ended_utc "$ENDED_UTC" \
             --arg run_dir "$RUN_DIR" \
             '{schema_version, phase, target_kind, target_name, path_name, window_name, replicate_index, cell_id, base_sha, mtr_snapshot_path, started_utc, ended_utc, run_dir}' \
             > "${RUN_DIR}/phase220-cell.json"
        j. Inter-cell delay: sleep "$INTER_CELL_DELAY" if --inter-cell-delay is set; default 60s. Document inline: caller may run multiple cells back-to-back; the sleep enforces the CONTEXT.md 60s minimum spacing.
        k. echo "Phase 220 cell capture complete: ${RUN_DIR}"

    11. ZERO EDITS to scripts/phase213-baseline-capture.sh or scripts/phase214-*.{sh,py}. The wrapper composes them by subprocess invocation only — no shell-source, no import, no patching. This is the MATRIX-02 "zero edits" contract.

    12. Exit code semantics — document at top of file in a comment block; mirror Phase 214 exactly.

    Shellcheck cleanliness — run `shellcheck scripts/phase220-target-path-matrix.sh` after writing; resolve all warnings (mostly: quote variable expansions, use $(…) not backticks, avoid `cd` without checking).
  </action>
  <acceptance_criteria>
    - File scripts/phase220-target-path-matrix.sh exists, is executable (`test -x scripts/phase220-target-path-matrix.sh`)
    - Shebang line: `head -1 scripts/phase220-target-path-matrix.sh` == `#!/usr/bin/env bash`
    - shellcheck scripts/phase220-target-path-matrix.sh exits 0 (or only warnings ≤ info severity)
    - bash -n scripts/phase220-target-path-matrix.sh exits 0 (syntax-valid)
    - grep -c "bash scripts/phase213-baseline-capture.sh" scripts/phase220-target-path-matrix.sh >= 1 (delegate composition present)
    - grep -c "PHASE214_BASE_SHA" scripts/phase220-target-path-matrix.sh >= 1 (D-14 guard reuses Phase 214 env var)
    - grep -c "DRY-RUN:" scripts/phase220-target-path-matrix.sh >= 1 (dry-run output marker)
    - grep -c "phase220-cell.json" scripts/phase220-target-path-matrix.sh >= 1 (sidecar manifest write)
    - grep -c "schema_version" scripts/phase220-target-path-matrix.sh >= 1 (D-17 envelope)
    - grep -c "mtr --no-dns --report -c 10" scripts/phase220-target-path-matrix.sh >= 1 (Pitfall 4 mitigation)
    - git diff --stat scripts/phase213-baseline-capture.sh exits with zero lines changed
    - git diff --stat scripts/phase214-*.sh exits with zero lines changed
    - .venv/bin/pytest tests/test_phase220_mutation_boundary.py -x -q exits 0 (SAFE-11 still green)
  </acceptance_criteria>
  <verify>
    <automated>shellcheck scripts/phase220-target-path-matrix.sh && bash -n scripts/phase220-target-path-matrix.sh && .venv/bin/pytest tests/test_phase220_mutation_boundary.py -x -q</automated>
  </verify>
  <done>Wrapper script committed; shellcheck clean; D-14 + delegate composition reuse Phase 214's mechanism unchanged; mutation-boundary green; mtr snapshot mechanics in place.</done>
</task>

<task type="auto">
  <name>Task 2: Flip wrapper xfail scaffolds in tests/test_phase220_matrix_wrapper.py to passing</name>
  <files>tests/test_phase220_matrix_wrapper.py</files>
  <read_first>
    - tests/test_phase220_matrix_wrapper.py (current xfail scaffolds from Plan 01)
    - scripts/phase220-target-path-matrix.sh (just created in Task 1; tests must pass against this)
    - scripts/phase220-matrix.yaml (created by Plan 02; tests use the resolved daytime cell for the dry-run hour gate)
    - .planning/phases/220-matrix-runner-scope-a1/220-RESEARCH.md R10 (the seven dry-run test cases)
  </read_first>
  <action>
    Remove the @pytest.mark.xfail decorator (or set strict=False with reason="now passing — plan 03") from every test in tests/test_phase220_matrix_wrapper.py. The seven test functions now must pass against the wrapper script created in Task 1:

    1. test_dry_run_inside_window_returns_0 — invoke wrapper with --dry-run --test-hour 12 --cell dallas__spectrum__daytime --replicate 1, env={"PHASE220_BASE_SHA": "<test_fixture_sha>"}; assert exit code 0
    2. test_dry_run_outside_window_returns_2 — same as above but --test-hour 23; assert exit code 2
    3. test_dry_run_stdout_starts_with_dry_run_marker — assert stdout.startswith("DRY-RUN:") and "bash scripts/phase213-baseline-capture.sh" in stdout
    4. test_dry_run_stdout_includes_flent_duration_30 — assert "--flent-duration 30" in stdout (or "--flent-duration" "30" if printf %q quotes them separately — the assertion must be flexible enough to match either spacing)
    5. test_dry_run_stdout_includes_resolved_bind_map_and_host — assert "--bind-map" in stdout and "spectrum=" in stdout (literal IP comes from YAML) and "--host" in stdout and "dallas" in stdout
    6. test_dry_run_does_not_create_run_dir — invoke wrapper as in test 1, then assert no new RUN-* directory under .planning/phases/220-matrix-runner-scope-a1/evidence/ (capture pre/post ls)
    7. test_missing_base_sha_returns_4 — invoke wrapper without PHASE220_BASE_SHA in env (use env={k:v for k,v in os.environ.items() if k not in {"PHASE220_BASE_SHA","PHASE214_BASE_SHA"}}); assert exit code 4
    8. test_test_hour_without_dry_run_returns_7 — invoke wrapper with --test-hour 12 --cell dallas__spectrum__daytime --replicate 1 (NO --dry-run); assert exit code 7

    Test fixtures for the "<test_fixture_sha>" placeholder: use the real base_sha from scripts/phase220-matrix.yaml (parse via yaml.safe_load) OR use `git rev-parse HEAD` at test time. Pick the YAML value — it's the contract the wrapper guards against; if HEAD has drifted, the wrapper would refuse a LIVE run, but a DRY-RUN should still succeed because the D-14 guard fires only in the live branch (verify by reading Task 1's script — the d-14 guard may or may not run pre-dry-run; if it does run pre-dry-run, the test must set PHASE220_BASE_SHA to a value that matches actual HEAD). DOCUMENT the choice in a comment at the top of the test file.

    Edge case: tests must not require network access, must not invoke ssh, must not invoke flent. The wrapper's dry-run branch handles this by exiting before any of those run.

    Failure mode for test_dry_run_does_not_create_run_dir: if the wrapper accidentally invokes phase213-baseline-capture.sh in dry-run mode (a bug), the test catches it via the file-system-state assertion.

    Mutation-boundary: this task touches tests/ only (allowlisted). DO NOT touch scripts/phase213-* or phase214-*. Verify by running .venv/bin/pytest tests/test_phase220_mutation_boundary.py -x -q AFTER this task.
  </action>
  <acceptance_criteria>
    - grep -c "@pytest.mark.xfail" tests/test_phase220_matrix_wrapper.py == 0 (all xfail markers removed) OR all remaining xfail markers have strict=False with explicit "now passing" reason
    - .venv/bin/pytest tests/test_phase220_matrix_wrapper.py -x -q exits 0 with all 8 tests passing
    - .venv/bin/pytest tests/test_phase220_mutation_boundary.py -x -q exits 0 (SAFE-11 still green)
    - .venv/bin/pytest tests/test_phase220_matrix_aggregator.py -x -q exits 0 (regression check — Plan 02 work still green)
    - No new RUN-* directory created in .planning/phases/220-matrix-runner-scope-a1/evidence/ after the dry-run tests: `find .planning/phases/220-matrix-runner-scope-a1/evidence -mindepth 1 -maxdepth 1 -type d -name 'RUN-*' 2>/dev/null | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase220_matrix_wrapper.py tests/test_phase220_mutation_boundary.py tests/test_phase220_matrix_aggregator.py -x -q</automated>
  </verify>
  <done>All eight wrapper tests pass; xfail markers gone; mutation-boundary + aggregator tests still green; no RUN-* dirs leaked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML on-disk → wrapper bash | Wrapper parses YAML via .venv/bin/python with yaml.safe_load; no eval, no shell expansion of YAML values |
| --cell CLI arg → YAML lookup | Cell id used as a YAML dict key; if not present, exit 3 (no shell interpretation, no SQL-style injection surface) |
| Wrapper → phase213-baseline-capture.sh subprocess | Wrapper passes resolved args via "${DELEGATE_CMD[@]}" array (no shell=True; no string interpolation); delegate runs in same shell context |
| mtr subprocess | mtr invoked with explicit --no-dns --report -c 10 flags and the resolved target hostname; target value comes from YAML (trusted on-disk source); no user-supplied input crosses this boundary |
| Env var PHASE220_BASE_SHA → D-14 guard | Wrapper validates SHA matches ^[0-9a-f]{40}$ before exporting to PHASE214_BASE_SHA; downstream git diff calls use it as a literal ref |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-03-01 | Tampering | --cell arg → YAML lookup | mitigate | Wrapper validates --cell matches a literal cell_id in the YAML cells[] list; unknown cell → exit 3 with REFUSED message. No shell interpretation of the cell_id (treated as a string dict key) |
| T-220-03-02 | Spoofing | base_sha drift mid-run | mitigate | Wrapper re-reads base_sha from YAML every invocation; D-14 guard verifies HEAD's src/wanctl/ matches base_sha's tree at unstaged + staged + committed channels. Operator-driven multi-day Phase 221 runs that drift fail closed (CONTEXT.md "base_sha Anchor Mechanism") |
| T-220-03-03 | Tampering | Phase 213/214 script edits | mitigate | tests/test_phase220_mutation_boundary.py asserts zero git diff for scripts/phase213-* and scripts/phase214-* against base_sha; if Plan 03 commit ever touches them, the test fails closed |
| T-220-03-04 | Elevation of Privilege | sudo journalctl in delegate | accept | Delegate is phase213-baseline-capture.sh which already runs sudo for journalctl; wrapper does not introduce new sudo surface. Mitigation already established in Phase 213/214 production deployment |
| T-220-03-05 | Information Disclosure | mtr snapshot contains hop IPs | accept | mtr output is committed to git under .planning/phases/220-matrix-runner-scope-a1/evidence/; it contains intermediate hop IPs and may include the operator's own egress IP. Documented as known disclosure in Phase 220 evidence (operator awareness — same posture as Phase 214 RUN dirs which also commit traceroute-derived data) |
| T-220-03-06 | Denial of Service | --inter-cell-delay sleep | accept | Default 60s sleep enforces CONTEXT.md minimum spacing; operator can override via flag. Not a hot-path concern (this is offline evidence collection, not the 50ms control loop) |
| T-220-03-SC | Tampering | shellcheck + bash -n gates | mitigate | Task 1 acceptance_criteria require shellcheck clean + bash -n clean before commit; any syntax error or quoting anti-pattern is caught at plan-commit time |
</threat_model>

<verification>
- .venv/bin/pytest tests/test_phase220_matrix_wrapper.py tests/test_phase220_mutation_boundary.py tests/test_phase220_matrix_aggregator.py -q  # all pass
- shellcheck scripts/phase220-target-path-matrix.sh  # exit 0
- bash -n scripts/phase220-target-path-matrix.sh  # syntax-valid
- git diff --stat scripts/phase213-baseline-capture.sh scripts/phase214-flent-matrix.sh scripts/phase214-extract.py scripts/phase214-align.py scripts/phase214-classify.py scripts/phase214-matrix-summary.py  # zero (MATRIX-02 zero-edits)
- git diff --stat src/wanctl/  # zero (SAFE-11)
- find .planning/phases/220-matrix-runner-scope-a1/evidence -mindepth 1 -maxdepth 1 -type d -name 'RUN-*' 2>/dev/null | wc -l  # zero (dry-run tests must not create evidence dirs)
</verification>

<success_criteria>
- scripts/phase220-target-path-matrix.sh committed; executable; shellcheck clean
- All eight tests in tests/test_phase220_matrix_wrapper.py pass without xfail markers
- mtr pre-flight snapshot mechanics in place; post-flight only re-snapshots on divergence (Pitfall 4 BGP path change detection)
- D-14 guard composes Phase 214's PHASE214_BASE_SHA mechanism; base_sha sourced from YAML
- MATRIX-02, MATRIX-03, MATRIX-04, SAFE-11 requirements all satisfied
- Plans 02 and 03 run in parallel within Wave 1 (no file overlap)
</success_criteria>

<output>
Create `.planning/phases/220-matrix-runner-scope-a1/220-03-SUMMARY.md` when done. Include:
- shellcheck warnings resolved (or accepted with rationale)
- Confirmation that scripts/phase213-* and scripts/phase214-* have zero git diff
- Wrapper exit-code matrix table (0/2/3/4/5/7) matching the implementation
- Cross-link to Plan 04 (rehearsal) as the Wave 2 consumer that uses this wrapper for the wet daytime control cell
</output>
