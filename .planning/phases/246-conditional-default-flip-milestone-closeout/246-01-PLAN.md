---
phase: 246-conditional-default-flip-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: ["245-04"]
files_modified:
  - .planning/phases/246-conditional-default-flip-milestone-closeout/246-CLOSEOUT.md
  - .planning/phases/246-conditional-default-flip-milestone-closeout/evidence/
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [FLIP-01, SAFE-17]
must_haves:
  truths:
    - "Phase 245 verdict is read from committed evidence and is rollback_trigger / keep-icmplib"
    - "No production default flip is performed in Phase 246"
    - "The milestone records a documented stay-on-icmplib recommendation as the valid FLIP-01 close"
    - "A future non-production fping profiling follow-up is captured as deferred work, not as a v1.53 production mutation"
    - "Production health is checked read-only and still shows steering active with Spectrum backend icmplib"
    - "SAFE-17 milestone-close boundary evidence is refreshed and passes"
  artifacts:
    - path: ".planning/phases/246-conditional-default-flip-milestone-closeout/246-CLOSEOUT.md"
      provides: "Phase 246 closeout decision, no-flip rationale, and future fping profiling handoff"
      contains: "stay-on-icmplib"
    - path: ".planning/phases/246-conditional-default-flip-milestone-closeout/evidence/production-health-final.json"
      provides: "Read-only final production health evidence after no-flip decision"
      contains: "icmplib"
    - path: ".planning/phases/246-conditional-default-flip-milestone-closeout/evidence/safe17-milestone-close.json"
      provides: "Milestone-close SAFE-17 pass evidence"
      contains: "passed"
---

<objective>
Close Phase 246 without flipping production to fping. The user selected the non-production profiling path after Phase 245 produced `rollback_trigger / keep-icmplib`, so this plan records the stay-on-icmplib decision, captures a future profiling follow-up, verifies production remains healthy on icmplib, refreshes SAFE-17 milestone-close evidence, and prepares v1.53 for milestone closeout.

This plan must not deploy, restart, or mutate RouterOS/production defaults. All production checks are read-only. Stability remains the priority.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md
@.planning/phases/245-live-a-b-rollback-anchor/245-04-SUMMARY.md
@.planning/phases/245-live-a-b-rollback-anchor/evidence/phase245-rollback-proof.json

<decision>
Phase 245 verdict:
  outcome: rollback_trigger
  recommendation: keep-icmplib
  failing gate: cycle_budget_nonregression, due to pre-registered absolute p99 ceiling

User selected option 2 after Phase 245: keep caring about fping, but as future non-production profiling/investigation — not as a Phase 246 production default flip.
</decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record FLIP-01 no-flip decision and future profiling handoff</name>
  <files>.planning/phases/246-conditional-default-flip-milestone-closeout/246-CLOSEOUT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.md
    - .planning/phases/245-live-a-b-rollback-anchor/245-AB-VERDICT.json
    - .planning/phases/245-live-a-b-rollback-anchor/245-04-SUMMARY.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Write `246-CLOSEOUT.md` documenting that Phase 246 satisfies FLIP-01 via the no-flip branch: the A/B did not clearly win, so production stays on icmplib. Include the exact Phase 245 evidence paths and gate summary. Add a future requirement/deferred item for non-production fping profiling, e.g. `FPING-PROFILE-01`, scoped to profiling p99-cycle behavior and threshold methodology without changing production defaults.
  </action>
  <verify>
    <automated>grep -q "stay-on-icmplib" .planning/phases/246-conditional-default-flip-milestone-closeout/246-CLOSEOUT.md && grep -q "FPING-PROFILE-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Closeout says no production default flip occurs in Phase 246.
    - Closeout names the Phase 245 verdict as `rollback_trigger / keep-icmplib`.
    - Future fping work is captured as non-production profiling, not a production flip.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Read-only production health final check</name>
  <files>.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/production-health-final.json</files>
  <action>
    Perform a read-only check against `cake-shaper` steering health. Save the parsed evidence JSON with checked_at, service active state, NRestarts, rtt_source backend/producer/source_ip/counts, runtime status, and an explicit `production_mutated: false` field. Do not restart or deploy.
  </action>
  <verify>
    <automated>python3 -c "import json; p='.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/production-health-final.json'; d=json.load(open(p)); assert d['service_active']=='active'; assert d['rtt_source']['backend']=='icmplib'; assert d['production_mutated'] is False"</automated>
  </verify>
  <acceptance_criteria>
    - Service is active.
    - Backend is icmplib.
    - Evidence explicitly records no production mutation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refresh milestone-close SAFE-17 evidence</name>
  <files>.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/safe17-milestone-close.json</files>
  <action>
    Run the current SAFE-17 verifier from Phase 245 and copy/normalize its JSON output to the Phase 246 evidence path. The verifier may print the known legacy confusing `FAIL allowed-shape ... unexpected added qualnames: []` line, but the JSON `passed: true` and exit code 0 are authoritative.
  </action>
  <verify>
    <automated>bash scripts/phase245-safe17-boundary-check.sh && cp .planning/phases/245-live-a-b-rollback-anchor/evidence/safe17-boundary-245.json .planning/phases/246-conditional-default-flip-milestone-closeout/evidence/safe17-milestone-close.json && python3 -c "import json; d=json.load(open('.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/safe17-milestone-close.json')); assert d.get('passed') is True"</automated>
  </verify>
  <acceptance_criteria>
    - Milestone-close SAFE-17 evidence exists under Phase 246.
    - JSON has `passed: true`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update roadmap/state/summary and prepare milestone closeout</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/246-conditional-default-flip-milestone-closeout/246-01-SUMMARY.md</files>
  <action>
    Mark Phase 246 plan complete, record that FLIP-01 was satisfied through the documented no-flip branch, and write a plan summary. Keep the next step as milestone closeout, not production fping flip.
  </action>
  <verify>
    <automated>grep -q "246-01-PLAN.md" .planning/ROADMAP.md && grep -q "stay-on-icmplib" .planning/phases/246-conditional-default-flip-milestone-closeout/246-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP shows Phase 246 has an executable plan.
    - STATE points to milestone closeout or Phase 246 completion, not a fping flip.
    - Summary records the no-flip decision and future profiling follow-up.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
grep -q "FPING-PROFILE-01" .planning/REQUIREMENTS.md
grep -q "stay-on-icmplib" .planning/phases/246-conditional-default-flip-milestone-closeout/246-CLOSEOUT.md
python3 -c "import json; d=json.load(open('.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/production-health-final.json')); assert d['service_active']=='active'; assert d['rtt_source']['backend']=='icmplib'"
python3 -c "import json; d=json.load(open('.planning/phases/246-conditional-default-flip-milestone-closeout/evidence/safe17-milestone-close.json')); assert d.get('passed') is True"
```
</verification>
