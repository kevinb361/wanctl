---
phase: 145-method-extraction-simplification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/steering/daemon.py
autonomous: true
requirements:
  - CPLX-02
must_haves:
  truths:
    - "SteeringDaemon.run_cycle() is under 50 lines excluding docstrings"
    - "SteeringDaemon.__init__() is under 50 lines excluding docstrings"
    - "steering/daemon.py main() is under 50 lines excluding docstrings"
    - "SteeringConfig loader methods are each under 50 lines"
    - "All existing tests pass unchanged after extraction"
  artifacts:
    - path: "src/wanctl/steering/daemon.py"
      provides: "SteeringDaemon with extracted private methods"
      contains: "_init_cake_reader"
  key_links:
    - from: "src/wanctl/steering/daemon.py"
      to: "tests/test_steering_daemon.py"
      via: "1238 mock/patch references remain valid"
      pattern: "patch.*wanctl.steering.daemon"
---

<objective>
Extract the four largest functions in steering/daemon.py (run_cycle 269, main 215, __init__ 136, plus config loaders 128+115 LOC) into focused private methods.

Purpose: steering/daemon.py mirrors the autorate daemon pattern. Per D-02, it is explicitly in scope. Per D-06, main() stays in steering/daemon.py. The same lifecycle decomposition (D-04) and PerfTimer subsystem extraction patterns apply.

Output: steering/daemon.py with all functions under 50 lines, ~12-15 new private methods
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-method-extraction-simplification/145-CONTEXT.md
@.planning/phases/145-method-extraction-simplification/145-RESEARCH.md
@src/wanctl/steering/daemon.py
@tests/test_steering_daemon.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SteeringDaemon.__init__(), run_cycle(), and config loaders</name>
  <files>src/wanctl/steering/daemon.py</files>
  <read_first>
    - src/wanctl/steering/daemon.py (full file -- __init__ at 957-1092, run_cycle at 1797-2069, config loaders at 321-582)
    - tests/test_steering_daemon.py (1238 mock/patch refs -- understand patch targets)
    - .planning/phases/145-method-extraction-simplification/145-CONTEXT.md (D-02, D-06, D-09)
  </read_first>
  <action>
Extract the five largest functions in steering/daemon.py. All helpers stay as private methods/functions in the same file (per D-09).

**SteeringDaemon.__init__() (136 lines, L957-1092) -- extract into concern groups:**
1. `_init_rtt_and_baseline(self)` -- RTT measurement, baseline loader/manager, alpha config
2. `_init_cake_reader(self)` -- CakeStatsReader setup, congestion thresholds
3. `_init_confidence_controller(self)` -- ConfidenceController, WAN state loading
4. `_init_steering_state(self)` -- StateManager, dry_run, flash wear protection
5. `_init_steering_alerting(self)` -- webhook, alert config, throttle timers
6. `_init_steering_profiling(self)` -- profiling flag, perf counters

__init__ becomes ~25 lines calling these methods after setting core attributes.

**SteeringDaemon.run_cycle() (269 lines, L1797-2069) -- extract by PerfTimer blocks:**
1. `_run_steering_rtt_subsystem(self)` -- RTT measurement + ICMP failure handling. Returns (measured_rtt, early_return).
2. `_run_steering_cake_stats_subsystem(self)` -- CAKE stats reading. Returns CongestionSignals.
3. `_run_steering_state_management_subsystem(self, measured_rtt, signals)` -- congestion assessment, confidence update, steering decision, state save. The bulk of the logic.
4. `_run_steering_post_cycle_subsystem(self, cycle_start)` -- profiling, cycle budget recording.

Keep PerfTimer context managers in run_cycle() -- call helpers INSIDE them.

**SteeringConfig._load_alerting_config() (128 lines, L446-582) -- extract by alert type:**
1. `_load_congestion_alert_config(self, alerting_config)` -- congestion alert settings
2. `_load_steering_change_alert_config(self, alerting_config)` -- steering change settings
3. `_load_confidence_alert_config(self, alerting_config)` -- confidence threshold settings
4. Or group by logical sections if fewer natural boundaries exist.

**SteeringConfig._load_wan_state_config() (115 lines, L321-444) -- extract by concern:**
1. `_load_wan_state_paths(self, wan_state_config)` -- path resolution logic
2. `_load_wan_state_zone_config(self, wan_state_config)` -- zone mapping, thresholds
Or similar decomposition based on the actual code structure.

**main() (215 lines, L2169-2414) -- lifecycle decomposition per D-06:**
1. `_parse_steering_args()` -- argument parsing (if not already extracted)
2. `_setup_steering_daemon(args)` -- config loading, daemon creation, lock acquisition. Returns (daemon, lock_files).
3. `_run_steering_daemon_loop(daemon, ...)` -- the while-not-shutdown loop
4. `_cleanup_steering_daemon(daemon, lock_files, ...)` -- finally block cleanup

main() becomes ~30 lines orchestrating these helpers.

CRITICAL: The test file has 1238 mock/patch refs (5741 lines). Every mock target path must remain valid. Since all helpers stay in the same module, `patch("wanctl.steering.daemon.SomeClass")` paths are preserved. Do NOT move any class or function to a different module.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_steering_daemon.py -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py src/wanctl/steering/daemon.py --threshold 50 returns no violations for __init__, run_cycle, main, _load_alerting_config, _load_wan_state_config
    - All 259 test_steering_daemon.py tests pass: .venv/bin/pytest tests/test_steering_daemon.py -q shows "259 passed"
    - grep -c "def _init_" src/wanctl/steering/daemon.py returns at least 5
    - grep -c "def _run_steering_" src/wanctl/steering/daemon.py returns at least 3
    - ruff check src/wanctl/steering/daemon.py passes clean
    - Entry point unchanged: grep "steering.daemon:main" pyproject.toml returns the entry point line
  </acceptance_criteria>
  <done>All 5 target functions in steering/daemon.py under 50 lines, ~12-15 new private methods, all 259 tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Verify all remaining functions in steering/daemon.py are under 50 lines</name>
  <files>src/wanctl/steering/daemon.py</files>
  <read_first>
    - src/wanctl/steering/daemon.py (scan for any remaining functions >50 lines after Task 1)
  </read_first>
  <action>
Run the verification script on steering/daemon.py to check for ANY remaining functions over 50 lines after Task 1 extractions. The research inventory shows additional functions that may need attention:

- `run_daemon_loop()` (69 lines) -- may be under 50 after main() extraction restructures the loop, or may need its own extraction
- `SteeringConfig._load_alerting_config()` from the steering config (128 lines at L446-582) -- should have been addressed in Task 1, verify
- Any newly created helpers that ended up over 50 lines

For each remaining violation:
1. Read the function
2. Identify natural extraction boundaries
3. Extract into same-module private helpers (per D-09)
4. Verify tests still pass

Continue until python scripts/check_function_lines.py src/wanctl/steering/daemon.py --threshold 50 returns zero violations.
  </action>
  <verify>
    <automated>python scripts/check_function_lines.py src/wanctl/steering/daemon.py --threshold 50 2>&1; .venv/bin/pytest tests/test_steering_daemon.py -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py src/wanctl/steering/daemon.py --threshold 50 returns zero violations (exit 0)
    - All 259 test_steering_daemon.py tests pass
    - ruff check and mypy pass on steering/daemon.py
  </acceptance_criteria>
  <done>All functions in steering/daemon.py under 50 lines, all tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure internal refactoring.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | Tampering | steering/daemon.py extraction | accept | Pure refactoring: no new code paths. Entry point unchanged. All 259 tests verify unchanged behavior. |
</threat_model>

<verification>
1. All 259 test_steering_daemon.py tests pass unchanged
2. python scripts/check_function_lines.py src/wanctl/steering/daemon.py --threshold 50 exits 0
3. ruff check src/wanctl/steering/daemon.py exits 0
4. mypy src/wanctl/steering/daemon.py exits 0 (or matches prior baseline)
5. grep "steering.daemon:main" pyproject.toml confirms entry point unchanged
</verification>

<success_criteria>
- All 5 target functions (run_cycle, main, __init__, _load_alerting_config, _load_wan_state_config) under 50 lines
- All remaining functions in the file also under 50 lines
- ~12-15 new private methods with descriptive names and docstrings
- Zero behavioral regression -- all 259 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/145-method-extraction-simplification/145-03-SUMMARY.md`
</output>
