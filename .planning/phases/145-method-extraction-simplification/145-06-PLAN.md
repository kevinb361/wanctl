---
phase: 145-method-extraction-simplification
plan: 06
type: execute
wave: 3
depends_on:
  - 145-01
  - 145-02
  - 145-03
  - 145-04
  - 145-05
files_modified:
  - src/wanctl/error_handling.py
  - src/wanctl/router_connectivity.py
  - src/wanctl/history.py
  - src/wanctl/tuning/strategies/signal_processing.py
  - pyproject.toml
autonomous: true
requirements:
  - CPLX-04
must_haves:
  truths:
    - "ruff check src/wanctl/ --select C901 --config 'lint.mccabe.max-complexity = 15' reports zero violations"
    - "pyproject.toml max-complexity is set to 15"
    - "All C901 per-file-ignores are removed from pyproject.toml"
    - "All existing tests pass unchanged after C901 reduction"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated C901 threshold and cleaned per-file-ignores"
      contains: "max-complexity = 15"
  key_links:
    - from: "pyproject.toml"
      to: "src/wanctl/"
      via: "ruff C901 enforcement"
      pattern: "max-complexity = 15"
---

<objective>
Resolve all 10 C901 complexity violations at threshold 15, remove per-file-ignores, and lower the pyproject.toml max-complexity from 20 to 15.

Purpose: CPLX-04 requires refactoring high cyclomatic complexity functions. The phase success criteria state "C901 pass at threshold 15 or lower." Currently there are 10 violations at threshold 15 plus 5 files with C901 per-file-ignores at threshold 20. After Plans 01-05 extract long functions, many C901 violations will be naturally resolved. This plan handles the remaining violations and updates the configuration.

Output: Zero C901 violations at threshold 15, pyproject.toml updated, per-file-ignores cleaned
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-method-extraction-simplification/145-CONTEXT.md
@.planning/phases/145-method-extraction-simplification/145-RESEARCH.md
@.planning/phases/145-method-extraction-simplification/145-01-SUMMARY.md
@.planning/phases/145-method-extraction-simplification/145-02-SUMMARY.md
@.planning/phases/145-method-extraction-simplification/145-03-SUMMARY.md
@.planning/phases/145-method-extraction-simplification/145-04-SUMMARY.md
@.planning/phases/145-method-extraction-simplification/145-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve remaining C901 violations and clean up pyproject.toml</name>
  <files>src/wanctl/error_handling.py, src/wanctl/router_connectivity.py, src/wanctl/history.py, src/wanctl/tuning/strategies/signal_processing.py, pyproject.toml</files>
  <read_first>
    - pyproject.toml (current per-file-ignores and max-complexity settings at lines 105-116)
    - Run: .venv/bin/ruff check src/wanctl/ --select C901 --config "lint.mccabe.max-complexity = 15" to get current violations AFTER Plans 01-05
    - src/wanctl/error_handling.py (handle_errors/decorator/wrapper at L38-166, combined complexity 51)
    - src/wanctl/router_connectivity.py (classify_failure_type at L17, complexity 16)
    - src/wanctl/history.py (main at L542, complexity 17)
    - src/wanctl/tuning/strategies/signal_processing.py (tune_alpha_load at L241, complexity 18)
    - .planning/phases/145-method-extraction-simplification/145-RESEARCH.md (Pitfall 4: C901 not reduced by line extraction; Open Question 2: error_handling nested closures)
  </read_first>
  <action>
First, run `ruff check src/wanctl/ --select C901 --config "lint.mccabe.max-complexity = 15"` to see which violations remain after Plans 01-05 extractions. Many will have been resolved by the line-count extractions (when branches were extracted into helpers). Address any remaining violations.

**Expected remaining violations (if not already resolved by Plans 01-05):**

1. **error_handling.py: handle_errors (18), decorator (17), wrapper (16)** -- these three are nested functions forming a decorator pattern. Per Research: "Don't restructure the decorator pattern itself." Strategy:
   - Extract the error classification if/elif chain in wrapper() into `_classify_error(exception) -> tuple[str, int]` that returns (error_message, log_level). This moves 8-10 branches out of wrapper, reducing its complexity.
   - Extract the logger discovery logic from decorator() into `_discover_logger(args, func) -> logging.Logger`. This moves 3-4 branches out.
   - These two extractions should bring each nested function under 15. If not possible without breaking the closure, add a targeted `# noqa: C901` with a comment explaining the structural complexity of the decorator pattern is irreducible.

2. **router_connectivity.py: classify_failure_type (16)** -- an if/elif chain classifying exception types. Extract groups:
   - `_classify_connection_error(exception) -> str | None` -- connection-related errors
   - `_classify_timeout_error(exception) -> str | None` -- timeout-related errors
   - `_classify_auth_error(exception) -> str | None` -- authentication errors
   Or use a lookup table pattern: `ERROR_CLASSIFICATIONS = [(ExceptionType, "category"), ...]` to replace the if/elif chain entirely. This is often cleaner for pure classification functions.

3. **history.py: main (17)** -- CLI argument parsing + dispatch. Should have been partially addressed in Plan 05 if history.py main was extracted. If not:
   - Extract subcommand dispatch into `_dispatch_subcommand(args) -> int`
   - Extract each subcommand handler into a separate function

4. **tuning/strategies/signal_processing.py: tune_alpha_load (18)** -- should have been addressed in Plan 05 Task 2. Verify it's resolved. If not, extract branching logic into helpers.

5. **Verify per-file-ignore files** -- after Plans 01-05, these files should no longer need C901 suppression:
   - autorate_continuous.py (main was 68, now extracted)
   - wan_controller.py (run_cycle was 36, now extracted)
   - queue_controller.py (adjust_4state was 21, now extracted)
   - health_check.py (_get_health_status was 24, now extracted)
   - steering/daemon.py (main was 23, now extracted)

**After fixing all violations:**

6. Update pyproject.toml:
   - Change `max-complexity = 20` to `max-complexity = 15`
   - Remove all C901 per-file-ignores: delete lines 110-115 (the 5 src/wanctl/ C901 ignore lines)
   - Keep the `"tests/*" = ["SIM102"]` line (it's unrelated to C901)
   - Keep the C901 rule in the select list (it's already there)

7. Final verification: `.venv/bin/ruff check src/wanctl/ --select C901` should exit 0 (now using threshold 15 from pyproject.toml)

CRITICAL constraints:
- Do NOT add new per-file-ignores or inline noqa comments unless the complexity is genuinely irreducible (e.g., decorator pattern closure)
- If a noqa is needed, add a comment explaining WHY
- The error_handling.py decorator API must not change (it's used in 73+ call sites)
  </action>
  <verify>
    <automated>.venv/bin/ruff check src/wanctl/ --select C901 2>&1; .venv/bin/pytest tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/ruff check src/wanctl/ --select C901 exits 0 with zero violations
    - grep "max-complexity = 15" pyproject.toml returns 1 match
    - grep -c "C901" pyproject.toml per-file-ignores section returns 0 (all C901 per-file-ignores removed)
    - All existing tests pass: .venv/bin/pytest tests/ -x --tb=short -q
    - ruff check src/wanctl/ (full check, not just C901) exits 0
    - mypy src/wanctl/ exits 0
  </acceptance_criteria>
  <done>Zero C901 violations at threshold 15, pyproject.toml updated to max-complexity=15, all per-file-ignores for C901 removed, all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification -- all functions under 50 lines and C901 clean</name>
  <files>scripts/check_function_lines.py</files>
  <read_first>
    - Run python scripts/check_function_lines.py src/wanctl/ --threshold 50 to see complete status
    - Run .venv/bin/ruff check src/wanctl/ --select C901 to verify C901 clean
  </read_first>
  <action>
Run the full verification suite to confirm Phase 145 success criteria are met:

1. `python scripts/check_function_lines.py src/wanctl/ --threshold 50` -- verify no function >50 lines excluding docstrings. Document any functions in the 50-60 range that were intentionally left per D-07.

2. `.venv/bin/ruff check src/wanctl/ --select C901` -- verify zero C901 violations at the new threshold of 15.

3. `.venv/bin/ruff check src/wanctl/` -- full lint check clean.

4. `.venv/bin/mypy src/wanctl/` -- type check clean.

5. `make ci` -- full CI passes.

6. `make dead-code` -- no dead code introduced.

If any violations remain from step 1:
- Functions 50-55 lines that are cohesive single-concern: acceptable per D-07, document them
- Functions >55 lines: extract one more helper to bring under 50

If any violations remain from step 2:
- Fix the specific function's branching complexity

After all checks pass, run `python scripts/check_function_lines.py src/wanctl/ --show-all` and save a summary of the final distribution for the SUMMARY.md.
  </action>
  <verify>
    <automated>python scripts/check_function_lines.py src/wanctl/ --threshold 50 2>&1; .venv/bin/ruff check src/wanctl/ --select C901 2>&1; make ci 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py src/wanctl/ --threshold 50 exits 0 (or lists only D-07 documented exceptions)
    - .venv/bin/ruff check src/wanctl/ --select C901 exits 0
    - make ci exits 0
    - make dead-code exits 0
  </acceptance_criteria>
  <done>Phase 145 fully verified: all functions under target, C901 clean at 15, full CI passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Configuration change only affects lint thresholds.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-09 | Tampering | pyproject.toml threshold change | accept | Stricter linting threshold (20->15) only affects development-time checks. No runtime behavioral change. |
| T-145-10 | Tampering | error_handling.py extraction | accept | Decorator API unchanged. Extract classification logic only. All 73+ call sites verified by tests. |
</threat_model>

<verification>
1. .venv/bin/ruff check src/wanctl/ --select C901 exits 0
2. python scripts/check_function_lines.py src/wanctl/ --threshold 50 exits 0 (or documented D-07 exceptions)
3. make ci exits 0
4. make dead-code exits 0
5. grep "max-complexity = 15" pyproject.toml confirms threshold update
6. No C901 per-file-ignores remain
</verification>

<success_criteria>
- All 10 C901 violations at threshold 15 resolved
- pyproject.toml max-complexity lowered from 20 to 15
- All 5 C901 per-file-ignores removed
- Phase 145 success criteria fully met
- Full CI passes with the stricter configuration
</success_criteria>

<output>
After completion, create `.planning/phases/145-method-extraction-simplification/145-06-SUMMARY.md`
</output>
