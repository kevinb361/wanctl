---
phase: 145-method-extraction-simplification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wanctl/health_check.py
  - src/wanctl/steering/health.py
autonomous: true
requirements:
  - CPLX-02
must_haves:
  truths:
    - "HealthCheckHandler._get_health_status() is under 50 lines excluding docstrings"
    - "SteeringHealthHandler._get_health_status() is under 50 lines excluding docstrings"
    - "All existing tests pass unchanged after extraction"
  artifacts:
    - path: "src/wanctl/health_check.py"
      provides: "Autorate health handler with section-builder helpers"
      contains: "_build_wan_status_section"
    - path: "src/wanctl/steering/health.py"
      provides: "Steering health handler with section-builder helpers"
      contains: "_build_steering_status_section"
  key_links:
    - from: "src/wanctl/health_check.py"
      to: "tests/test_health_check.py"
      via: "329 mock/patch references remain valid"
      pattern: "patch.*wanctl.health_check"
    - from: "src/wanctl/steering/health.py"
      to: "tests/test_steering_health.py"
      via: "133 mock/patch references remain valid"
      pattern: "patch.*wanctl.steering.health"
---

<objective>
Extract _get_health_status() in health_check.py (347 LOC) and steering/health.py (212 LOC) into section-builder helpers.

Purpose: Both health handlers build large JSON response dictionaries by assembling sections (WAN status, signal chain, cycle budget, alerts, etc.). Each section is a natural extraction boundary. Per D-09, helpers stay in the same module as private methods on the handler class.

Output: Both health handlers with _get_health_status() as a ~20-30 line response assembler calling section builders
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-method-extraction-simplification/145-CONTEXT.md
@.planning/phases/145-method-extraction-simplification/145-RESEARCH.md
@src/wanctl/health_check.py
@src/wanctl/steering/health.py
@tests/test_health_check.py
@tests/test_steering_health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HealthCheckHandler._get_health_status() into section builders</name>
  <files>src/wanctl/health_check.py</files>
  <read_first>
    - src/wanctl/health_check.py (full file -- _get_health_status at lines 179-526, 347 lines)
    - tests/test_health_check.py (329 mock/patch refs -- understand what tests verify in the response dict)
    - .planning/phases/145-method-extraction-simplification/145-CONTEXT.md (D-09: helpers same module, D-11: descriptive names)
  </read_first>
  <action>
Extract HealthCheckHandler._get_health_status() (347 lines, L179-526) into section-builder private methods. The function builds a large dict with sections for each WAN controller's status. Each section of the response dict is a natural extraction boundary.

_get_health_status() should become a ~25-35 line assembler:
```python
def _get_health_status(self) -> dict:
    status = {"daemon": "running", "version": ..., "uptime": ...}
    status["wan_controllers"] = [self._build_wan_status(wc) for wc in self.controller.wan_controllers]
    return status
```

Extract these section-builder methods on HealthCheckHandler:

1. `_build_wan_status_section(self, wan_info: dict) -> dict` -- the per-WAN status block. This is likely the largest section and may itself need sub-extraction:
   - `_build_rtt_status(self, controller) -> dict` -- RTT baseline, load, delta, EWMA
   - `_build_rate_status(self, controller) -> dict` -- current rates, zones, floors/ceilings
   - `_build_signal_chain_status(self, controller) -> dict` -- Hampel, fusion, alpha values
   - `_build_cycle_budget_status(self, controller) -> dict` -- PerfTimer subsystem timings (note: _build_cycle_budget() may already exist as a standalone function)
   - `_build_alert_status(self, controller) -> dict` -- alert states, throttle timers
   - `_build_tuning_status(self, controller) -> dict` -- tuning layer, parameters
   - `_build_reflector_status(self, controller) -> dict` -- reflector scores, active count
   - `_build_hysteresis_status(self, controller) -> dict` -- dwell state, deadband, suppression rates

2. Check if `_build_cycle_budget()` already exists as a standalone function. If so, reuse it. If not, extract it.

Each builder returns a dict that gets merged into the per-WAN status. Each builder should be under 50 lines.

CRITICAL constraints:
- The response dict structure (keys, nesting) must NOT change -- tests assert specific response paths
- isinstance(stats, dict) guard patterns must be preserved (MagicMock safety)
- The order of data collection matters for some values (e.g., uptime calculated once)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_health_check.py -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _get_health_status() is under 50 lines: python scripts/check_function_lines.py src/wanctl/health_check.py --threshold 50 | grep _get_health_status returns nothing
    - Section builder methods exist: grep -c "def _build_" src/wanctl/health_check.py returns at least 5
    - All 77 test_health_check.py tests pass: .venv/bin/pytest tests/test_health_check.py -q shows "77 passed"
    - Response dict structure unchanged: tests that assert response["wan_controllers"][0]["rtt"] still pass
    - ruff check src/wanctl/health_check.py passes clean
    - No function in health_check.py exceeds 50 lines: python scripts/check_function_lines.py src/wanctl/health_check.py --threshold 50 returns zero violations
  </acceptance_criteria>
  <done>_get_health_status() reduced from 347 to under 50 lines, 5+ section-builder methods, all 77 tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Extract SteeringHealthHandler._get_health_status() into section builders</name>
  <files>src/wanctl/steering/health.py</files>
  <read_first>
    - src/wanctl/steering/health.py (full file -- _get_health_status at lines 103-328, 212 lines)
    - tests/test_steering_health.py (133 mock/patch refs)
    - src/wanctl/health_check.py (after Task 1 -- follow the same section-builder pattern for consistency)
  </read_first>
  <action>
Extract SteeringHealthHandler._get_health_status() (212 lines, L103-328) into section-builder private methods. Follow the same pattern established in Task 1 for consistency.

_get_health_status() should become a ~20-30 line assembler calling section builders:

1. `_build_steering_status_section(self) -> dict` -- core steering state (enabled, confidence, zone)
2. `_build_steering_rtt_section(self) -> dict` -- RTT baseline, load, delta, EWMA
3. `_build_steering_cake_section(self) -> dict` -- CAKE stats, tin distribution
4. `_build_steering_confidence_section(self) -> dict` -- confidence details, weights, signals
5. `_build_steering_alerts_section(self) -> dict` -- alert states, webhook status
6. `_build_steering_cycle_budget_section(self) -> dict` -- profiling timings

Adjust the number and naming of builders based on the actual response dict structure (read the file first to identify natural section boundaries).

CRITICAL constraints:
- Response dict structure must NOT change
- isinstance() guard patterns preserved
- Same helper placement pattern as Task 1 (private methods on the handler class)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_steering_health.py -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _get_health_status() is under 50 lines: python scripts/check_function_lines.py src/wanctl/steering/health.py --threshold 50 | grep _get_health_status returns nothing
    - Section builder methods exist: grep -c "def _build_" src/wanctl/steering/health.py returns at least 4
    - All 57 test_steering_health.py tests pass: .venv/bin/pytest tests/test_steering_health.py -q shows "57 passed"
    - ruff check src/wanctl/steering/health.py passes clean
    - No function in steering/health.py exceeds 50 lines: python scripts/check_function_lines.py src/wanctl/steering/health.py --threshold 50 returns zero violations
  </acceptance_criteria>
  <done>_get_health_status() reduced from 212 to under 50 lines, 4+ section-builder methods, all 57 tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP health endpoints | Health handlers serve responses to localhost clients -- no trust boundary change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-05 | Tampering | health handler extraction | accept | Pure refactoring: response dict structure unchanged, no new endpoints, no new data exposed. All tests verify response format. |
| T-145-06 | Information Disclosure | health response | accept | No new fields added. Existing health data (RTT, rates, zones) already served on localhost only. |
</threat_model>

<verification>
1. All 77 test_health_check.py tests pass unchanged
2. All 57 test_steering_health.py tests pass unchanged
3. python scripts/check_function_lines.py src/wanctl/health_check.py src/wanctl/steering/health.py --threshold 50 exits 0
4. ruff check on both files exits 0
</verification>

<success_criteria>
- Both _get_health_status() functions under 50 lines
- Section-builder pattern consistent between autorate and steering health handlers
- Zero behavioral regression -- all health endpoint tests pass
- Response dict structures identical to pre-extraction
</success_criteria>

<output>
After completion, create `.planning/phases/145-method-extraction-simplification/145-04-SUMMARY.md`
</output>
