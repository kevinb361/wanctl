---
phase: 145-method-extraction-simplification
plan: 05
type: execute
wave: 2
depends_on:
  - 145-01
files_modified:
  - src/wanctl/autorate_config.py
  - src/wanctl/check_cake.py
  - src/wanctl/check_config_validators.py
  - src/wanctl/queue_controller.py
  - src/wanctl/tuning/strategies/signal_processing.py
  - src/wanctl/webhook_delivery.py
  - src/wanctl/check_cake_fix.py
  - src/wanctl/calibrate.py
autonomous: true
requirements:
  - CPLX-02
must_haves:
  truths:
    - "No function in any of the 8 target files exceeds 50 lines excluding docstrings"
    - "All existing tests pass unchanged after extraction"
    - "All extracted helpers have descriptive _verb_noun names and docstrings"
  artifacts:
    - path: "src/wanctl/autorate_config.py"
      provides: "Config class with extracted loader helpers"
      contains: "_load_alerting_webhook_config"
    - path: "src/wanctl/check_cake.py"
      provides: "CAKE audit with extracted check helpers"
      contains: "def _check_"
    - path: "src/wanctl/queue_controller.py"
      provides: "QueueController with extracted zone handlers"
      contains: "_handle_green_zone"
  key_links:
    - from: "src/wanctl/autorate_config.py"
      to: "src/wanctl/wan_controller.py"
      via: "Config import unchanged"
      pattern: "from wanctl.autorate_config import Config"
---

<objective>
Extract 12 remaining large functions (100-167 lines) across 8 files into focused helpers: autorate_config.py (3 functions), check_cake.py (2), check_config_validators.py (1), queue_controller.py (1), signal_processing.py (1), webhook_delivery.py (1), check_cake_fix.py (1), calibrate.py (1).

Purpose: These are the "long tail" of Tier 2 functions (100-167 lines each). Each file has 1-3 functions exceeding the 50-line target. Unlike the mega-functions that need extensive decomposition, most of these need 2-3 helper extractions each.

Output: All 12 functions under 50 lines, ~20-25 new helpers across 8 files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-method-extraction-simplification/145-CONTEXT.md
@.planning/phases/145-method-extraction-simplification/145-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract large functions in autorate_config.py and queue_controller.py</name>
  <files>src/wanctl/autorate_config.py, src/wanctl/queue_controller.py</files>
  <read_first>
    - src/wanctl/autorate_config.py (full file -- _load_tuning_config at L964-1139 167 lines, _load_alerting_config at L461-597 128 lines, _load_fusion_config at L841-962 119 lines)
    - src/wanctl/queue_controller.py (full file -- adjust_4state at L179-335 135 lines)
    - tests/test_autorate_config.py or tests that cover Config loading
    - tests/test_queue_controller.py or tests/test_wan_controller.py (for QueueController tests)
    - .planning/phases/145-method-extraction-simplification/145-CONTEXT.md (D-09: same module)
  </read_first>
  <action>
**autorate_config.py -- 3 functions to extract:**

1. `Config._load_tuning_config()` (167 lines, L964-1139) -- extracts config for 4 tuning strategy layers. Extract by layer:
   - `_load_tuning_signal_config(self, tuning_data: dict) -> dict` -- signal processing strategy params (alpha_load, hampel_sigma, hampel_window)
   - `_load_tuning_response_config(self, tuning_data: dict) -> dict` -- response strategy params (factor_down, step_up, green_required per direction)
   - `_load_tuning_threshold_config(self, tuning_data: dict) -> dict` -- congestion threshold params
   - `_load_tuning_advanced_config(self, tuning_data: dict) -> dict` -- advanced strategy params (fusion weight, baseline bounds, reflector)
   - `_load_tuning_safety_config(self, tuning_data: dict) -> dict` -- safety/revert config

2. `Config._load_alerting_config()` (128 lines, L461-597) -- loads webhook + per-alert-type configs. Extract by alert concern:
   - `_load_alerting_webhook_config(self, alerting: dict)` -- webhook URL, retry, severity filter
   - `_load_alerting_throttle_config(self, alerting: dict)` -- per-alert-type throttle intervals
   - `_load_alerting_threshold_config(self, alerting: dict)` -- congestion/flapping thresholds
   Or split by whatever logical boundaries exist in the code.

3. `Config._load_fusion_config()` (119 lines, L841-962) -- loads signal fusion config. Extract:
   - `_load_fusion_irtt_config(self, fusion_data: dict)` -- IRTT-specific fusion params
   - `_load_fusion_weights_config(self, fusion_data: dict)` -- ICMP/IRTT weight params
   Or 2-3 helpers based on actual code structure.

**queue_controller.py -- 1 function to extract:**

4. `QueueController.adjust_4state()` (135 lines, L179-335) -- 4-state download state machine. Extract by zone:
   - `_handle_green_zone(self, delta: float) -> tuple[str, int, str]` -- GREEN zone logic
   - `_handle_yellow_zone(self, delta: float) -> tuple[str, int, str]` -- YELLOW zone logic
   - `_handle_soft_red_zone(self, delta: float) -> tuple[str, int, str]` -- SOFT_RED zone logic
   - `_handle_red_zone(self, delta: float) -> tuple[str, int, str]` -- RED zone logic
   
   adjust_4state() becomes a dispatcher: classify zone, call zone handler, update dwell/deadband state. The zone handlers each implement the rate adjustment logic for their specific state.
   
   Note: Also check `QueueController.adjust()` (95 lines) -- it's at the border. If it reads well as-is (3-state machine is simpler), leave it per D-07. If it exceeds 50 lines excluding docstrings, extract similarly.

Run tests after each file modification:
- autorate_config.py: find and run relevant test file (tests/test_autorate_config.py or similar)
- queue_controller.py: .venv/bin/pytest tests/ -k "queue_controller or adjust_4state" -x

After all extractions, run: .venv/bin/ruff check src/wanctl/autorate_config.py src/wanctl/queue_controller.py
  </action>
  <verify>
    <automated>python scripts/check_function_lines.py src/wanctl/autorate_config.py src/wanctl/queue_controller.py --threshold 50 2>&1; .venv/bin/pytest tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py src/wanctl/autorate_config.py --threshold 50 returns zero violations
    - python scripts/check_function_lines.py src/wanctl/queue_controller.py --threshold 50 returns zero violations
    - grep -c "def _load_tuning_" src/wanctl/autorate_config.py returns at least 3 (sub-loaders)
    - grep -c "def _handle_" src/wanctl/queue_controller.py returns at least 3 (zone handlers)
    - All existing tests pass: .venv/bin/pytest tests/ -x --tb=short -q
    - ruff check passes on both files
  </acceptance_criteria>
  <done>4 functions across 2 files reduced to under 50 lines each, all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Extract large functions in check_cake.py, check_config_validators.py, and remaining files</name>
  <files>src/wanctl/check_cake.py, src/wanctl/check_config_validators.py, src/wanctl/tuning/strategies/signal_processing.py, src/wanctl/webhook_delivery.py, src/wanctl/check_cake_fix.py, src/wanctl/calibrate.py</files>
  <read_first>
    - src/wanctl/check_cake.py (check_tin_distribution at L549-705 141 lines, run_audit at L788-907 103 lines)
    - src/wanctl/check_config_validators.py (validate_cross_fields at L249-402 149 lines)
    - src/wanctl/tuning/strategies/signal_processing.py (tune_alpha_load at L241-387 135 lines)
    - src/wanctl/webhook_delivery.py (WebhookDelivery._do_deliver at L387-506 109 lines)
    - src/wanctl/check_cake_fix.py (run_fix at L226-342 104 lines)
    - src/wanctl/calibrate.py (generate_config at L134-235 101 lines)
    - .planning/phases/145-method-extraction-simplification/145-RESEARCH.md (Example 4: validate_cross_fields branch extraction)
  </read_first>
  <action>
Extract 8 remaining large functions across 6 files. Per D-09, all helpers stay in the same module.

**check_cake.py -- 2 functions:**

1. `check_tin_distribution()` (141 lines) -- checks CAKE tin distribution. Extract by check type:
   - `_check_tin_traffic_distribution(tins, ...)` -- traffic volume checks per tin
   - `_check_tin_drop_rates(tins, ...)` -- drop rate checks per tin
   - `_check_tin_backlog(tins, ...)` -- backlog/queue depth checks
   Or split by whatever logical sections exist.

2. `run_audit()` (103 lines) -- orchestrates all audit checks. Extract groups of checks:
   - `_run_bandwidth_checks(data, config_type, client)` -- bandwidth-related checks
   - `_run_queue_checks(data, config_type)` -- queue-related checks
   Or split the check dispatch into 2-3 helper groups.

**check_config_validators.py -- 1 function:**

3. `validate_cross_fields()` (149 lines, C901=17) -- validates cross-field constraints. Per Research Example 4, extract each validation group:
   - `_validate_floor_ordering(data: dict) -> list[CheckResult]` -- floor < ceiling checks
   - `_validate_threshold_consistency(data: dict) -> list[CheckResult]` -- threshold value range checks
   - `_validate_ceiling_constraints(data: dict) -> list[CheckResult]` -- ceiling constraint checks
   - Additional groups as the code reveals.
   
   validate_cross_fields() becomes: `results = []; results.extend(_validate_floor_ordering(data)); results.extend(...); return results`
   This also reduces C901 complexity from 17 to well under 15.

**tuning/strategies/signal_processing.py -- 1 function:**

4. `tune_alpha_load()` (135 lines, C901=18) -- tunes alpha_load parameter. Extract by analysis phase:
   - `_analyze_alpha_load_metrics(metrics_data, ...)` -- metric extraction and analysis
   - `_compute_alpha_load_adjustment(analysis_result, current_value, ...)` -- adjustment calculation
   - `_validate_alpha_load_bounds(proposed_value, ...)` -- bounds checking and clamping
   This also reduces C901 from 18 to under 15.

**webhook_delivery.py -- 1 function:**

5. `WebhookDelivery._do_deliver()` (109 lines) -- webhook delivery with retries. Extract:
   - `_prepare_webhook_payload(self, alert)` -- payload construction
   - `_send_webhook_request(self, url, payload)` -- HTTP request with retry
   Or split by preparation vs execution phases.

**check_cake_fix.py -- 1 function:**

6. `run_fix()` (104 lines) -- CAKE fix application. Extract:
   - `_apply_cake_fix(client, fix_params)` -- fix application logic
   - `_verify_cake_fix(client, fix_params)` -- post-fix verification

**calibrate.py -- 1 function:**

7. `generate_config()` (101 lines) -- generates config YAML from calibration. Extract:
   - `_build_calibration_yaml(results)` -- YAML dict construction
   - `_write_calibration_output(yaml_data, output_path)` -- file writing

Also check if check_config_validators.py has `check_paths()` (100 lines) -- if it exceeds 50 lines, extract similarly.

For each file, run targeted tests after extraction, then verify with the line count script.
  </action>
  <verify>
    <automated>python scripts/check_function_lines.py src/wanctl/check_cake.py src/wanctl/check_config_validators.py src/wanctl/tuning/strategies/signal_processing.py src/wanctl/webhook_delivery.py src/wanctl/check_cake_fix.py src/wanctl/calibrate.py --threshold 50 2>&1; .venv/bin/pytest tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py on all 6 files with --threshold 50 returns zero violations (exit 0)
    - validate_cross_fields() under 50 lines: grep for helper functions _validate_ in check_config_validators.py
    - tune_alpha_load() under 50 lines
    - All existing tests pass: .venv/bin/pytest tests/ -x --tb=short -q
    - ruff check passes on all 6 files
  </acceptance_criteria>
  <done>All 8 functions across 6 files reduced to under 50 lines each, all tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Sweep remaining files for any functions >50 lines</name>
  <files>src/wanctl/</files>
  <read_first>
    - Run python scripts/check_function_lines.py src/wanctl/ --threshold 50 to find any remaining violations not covered by Plans 01-05
  </read_first>
  <action>
Run the verification script across ALL of src/wanctl/ to catch any functions >50 lines that were not explicitly targeted by the research inventory. The research identified 23 functions >100 lines, but there may be functions in the 50-100 line range that the user left at Claude's discretion (per CONTEXT D-01: "Medium functions 50-100 lines at Claude's discretion -- split proactively if it improves readability, otherwise leave").

For each remaining violation (functions >50 lines excluding docstrings):
1. Read the function
2. Assess if it's cohesive and readable as-is (per D-07: allow 50-60 for cohesive code)
3. If >60 lines or not cohesive: extract helpers per D-09/D-11
4. If 50-60 and cohesive: leave as-is (within D-07 tolerance)
5. Document any functions left in the 50-60 range with rationale

Known candidates from the research inventory (50-100 range, at discretion):
- check_cake.py: check_queue_tree (95 lines), main (85 lines)
- autorate_config.py: _load_threshold_config (94 lines)
- calibrate.py: run_calibration (90 lines), _step_binary_search (75 lines)
- tuning/strategies/signal_processing.py: tune_hampel_sigma (88 lines)
- wan_controller.py: _reload_fusion_config (85 lines), _check_irtt_loss_alerts (82 lines), _check_flapping_alerts (72 lines), _reload_hysteresis_config (69 lines)
- steering/daemon.py: run_daemon_loop (69 lines)
- error_handling.py: handle_errors (77 lines -- nested closure, structural complexity per Research)
- history.py: create_parser (97 lines)
- autorate_continuous.py: ContinuousAutoRate.__init__ (81 lines -- should be handled in Plan 02)
- router_connectivity.py: classify_failure_type (contained in same file -- check actual line count)

Use judgment per D-07. Focus on functions >60 lines. Functions 50-60 that are single sequential blocks (no nested branches) can stay.

IMPORTANT: error_handling.py's handle_errors/decorator/wrapper nested closure structure is explicitly called out in Research as "Don't force-extract -- the complexity is structural." Leave the decorator pattern intact. If possible, extract the error classification if/elif chain from wrapper into a helper, but do not restructure the decorator itself.
  </action>
  <verify>
    <automated>python scripts/check_function_lines.py src/wanctl/ --threshold 60 2>&1; .venv/bin/pytest tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/check_function_lines.py src/wanctl/ --threshold 60 returns zero or near-zero violations (any remaining are documented as D-07 exceptions)
    - All existing tests pass: .venv/bin/pytest tests/ -x --tb=short -q
    - ruff check src/wanctl/ passes clean
  </acceptance_criteria>
  <done>All functions in src/wanctl/ are under 50-60 lines (per D-07 tolerance), all tests passing, any exceptions documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure internal refactoring across utility and CLI tool files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-07 | Tampering | Multi-file extraction | accept | Pure refactoring: no behavioral changes. Each file's tests verify unchanged behavior. No new code paths. |
| T-145-08 | Tampering | validate_cross_fields extraction | accept | Validation logic unchanged -- helpers return same CheckResult lists. Tests verify same validation outcomes. |
</threat_model>

<verification>
1. python scripts/check_function_lines.py src/wanctl/ --threshold 60 returns minimal or zero violations
2. .venv/bin/pytest tests/ -x --tb=short -q -- all tests pass
3. .venv/bin/ruff check src/wanctl/ exits 0
4. make dead-code exits 0
</verification>

<success_criteria>
- All 12 Tier 2 functions (100-167 lines) reduced to under 50 lines
- Medium functions (50-100 lines) addressed at Claude's discretion per D-07
- ~20-25 new helpers across 8 files
- Zero behavioral regression across all test files
</success_criteria>

<output>
After completion, create `.planning/phases/145-method-extraction-simplification/145-05-SUMMARY.md`
</output>
