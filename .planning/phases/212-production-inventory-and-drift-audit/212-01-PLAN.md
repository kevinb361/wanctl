---
phase: 212-production-inventory-and-drift-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-spectrum.txt
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-att.txt
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-steering.txt
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/config-spectrum.redacted.yaml
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/config-att.redacted.yaml
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/config-steering.redacted.yaml
  - .planning/phases/212-production-inventory-and-drift-audit/evidence/steering-state.redacted.json
autonomous: true
requirements: [DRIFT-01, DRIFT-03]
must_haves:
  truths:
    - "Operator can inspect saved read-only evidence for Spectrum, ATT, and steering without re-running production probes."
    - "D-03: Steering restart/degraded-state persistence is captured only as current inventory/state evidence; no controlled degraded restart is staged."
    - "D-04: Primary live evidence covers systemd status/uptime, bound health endpoints, deployed YAML, repo config, deployed package/version surfaces, and steering health/state."
    - "D-05: Health endpoint evidence records daemon-reported version, state, rates, measurement quality, and active status, while preserving that it is not UX proof."
    - "D-06: Systemd evidence records daemon active/restart/watchdog/config-path facts and preserves any systemd-vs-health disagreement."
    - "D-07: RouterOS readback, if used, is optional and read-only for critical operating points that config and health evidence cannot prove."
    - "Saved config/state artifacts redact secret-like values while preserving proof-relevant operating points."
    - "D-08: Audit artifacts exclude secrets, tokens, private keys, raw router passwords, and full secret-bearing config dumps."
    - "D-09: Config comparisons preserve proof-relevant non-secret values needed for inventory and drift interpretation."
    - "D-10: Sensitive local-operator commands may be documented, but sensitive command output is not committed."
    - "Health endpoint evidence is captured from deployed/bound endpoints, not assumed loopback endpoints."
    - "D-12: Raw evidence is saved as redacted snapshots or summarized command output under the Phase 212 directory for stable later citation."
  artifacts:
    - path: ".planning/phases/212-production-inventory-and-drift-audit/evidence/README.md"
      provides: "Evidence command index, timestamps, host/source notes, and redaction policy"
    - path: ".planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json"
      provides: "Repo expected version, systemd, config, and endpoint summary"
    - path: ".planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json"
      provides: "Spectrum daemon-reported health, version, state, rates, and summary"
    - path: ".planning/phases/212-production-inventory-and-drift-audit/evidence/config-spectrum.redacted.yaml"
      provides: "Secret-safe deployed Spectrum config snapshot"
  key_links:
    - from: "deployed /etc/wanctl/*.yaml"
      to: ".planning/phases/212-production-inventory-and-drift-audit/evidence/config-*.redacted.yaml"
      via: "recursive redaction before artifact write"
      pattern: "password|secret|token|credential|auth|key|private"
    - from: "deployed health_check host/port"
      to: ".planning/phases/212-production-inventory-and-drift-audit/evidence/health-*.json"
      via: "curl bound endpoint derived from deployed config"
      pattern: "10\\.10\\.110\\.(223|227):9101|health-steering"
---

<objective>
Capture the read-only production evidence set for Spectrum, ATT, and steering.

Purpose: Phase 212 must establish live reality before later baseline or tuning work, while preserving the default no-mutation boundary per D-01, D-03, D-04, D-05, D-06, D-08, D-09, D-10, and D-12.
Output: Redacted, stable evidence artifacts under `.planning/phases/212-production-inventory-and-drift-audit/evidence/`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/212-production-inventory-and-drift-audit/212-CONTEXT.md
@.planning/phases/212-production-inventory-and-drift-audit/212-RESEARCH.md
@.planning/phases/212-production-inventory-and-drift-audit/212-VALIDATION.md
@.planning/phases/212-production-inventory-and-drift-audit/212-PATTERNS.md
@.planning/phases/211-production-verification-milestone-closure/211-01-SUMMARY.md
@configs/spectrum.yaml
@configs/att.yaml
@configs/steering.yaml
@deploy/systemd/wanctl@.service
@deploy/systemd/steering.service
@src/wanctl/health_check.py
@src/wanctl/steering/health.py
@src/wanctl/config_base.py
</context>

<tasks>

<task type="auto">
  <name>Task 212-01-01: Create repo expectation and evidence-command index</name>
  <read_first>
    .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md
    .planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json
    .planning/phases/212-production-inventory-and-drift-audit/212-CONTEXT.md
    .planning/phases/212-production-inventory-and-drift-audit/212-RESEARCH.md
    .planning/phases/212-production-inventory-and-drift-audit/212-PATTERNS.md
    configs/spectrum.yaml
    configs/att.yaml
    configs/steering.yaml
    deploy/systemd/wanctl@.service
    deploy/systemd/steering.service
    pyproject.toml
    src/wanctl/__init__.py
  </read_first>
  <files>.planning/phases/212-production-inventory-and-drift-audit/evidence/README.md, .planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json</files>
  <action>Create the `evidence/` directory if absent. Write `evidence/README.md` with table-first rows for command timestamp, source host, command purpose, redaction method, output file, and mutation posture. Write `evidence/repo-expected-summary.json` containing repo version from `pyproject.toml` and `src/wanctl/__init__.py`, expected units `wanctl@spectrum.service`, `wanctl@att.service`, `steering.service`, expected systemd config paths `/etc/wanctl/spectrum.yaml`, `/etc/wanctl/att.yaml`, `/etc/wanctl/steering.yaml`, and repo expected non-secret health endpoints from `configs/spectrum.yaml` and `configs/att.yaml` (`10.10.110.223:9101`, `10.10.110.227:9101`). Per D-08/D-09, omit raw secret-bearing values and record that health is daemon-state evidence, not UX proof per D-05.</action>
  <verify>
    <automated>test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md &amp;&amp; test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/212-production-inventory-and-drift-audit/evidence/repo-expected-summary.json &gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `repo-expected-summary.json` contains keys `repo_version`, `units`, `config_paths`, `repo_health_endpoints`, and `proof_relevant_fields`.
    - `repo_health_endpoints.spectrum` is `http://10.10.110.223:9101/health` and `repo_health_endpoints.att` is `http://10.10.110.227:9101/health` unless repo config has changed, in which case the changed repo value is cited.
    - No value matching raw router passwords, webhook URLs, private key material, `/etc/wanctl/secrets` contents, tokens, or credentials appears in either artifact.
  </acceptance_criteria>
  <done>Repo expectations and evidence-command index exist, parse cleanly, and encode the read-only/redaction boundary.</done>
</task>

<task type="auto">
  <name>Task 212-01-02: Capture systemd and health evidence read-only</name>
  <read_first>
    .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-spectrum.txt
    .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-att.txt
    .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-steering.txt
    .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json
    .planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json
    .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json
    .planning/phases/212-production-inventory-and-drift-audit/212-CONTEXT.md
    .planning/phases/212-production-inventory-and-drift-audit/212-RESEARCH.md
    .planning/phases/212-production-inventory-and-drift-audit/212-PATTERNS.md
    .planning/phases/211-production-verification-milestone-closure/211-01-SUMMARY.md
    deploy/systemd/wanctl@.service
    deploy/systemd/steering.service
    src/wanctl/health_check.py
    src/wanctl/steering/health.py
  </read_first>
  <files>.planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-spectrum.txt, .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-att.txt, .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-steering.txt, .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json, .planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json, .planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json, .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md</files>
  <action>Using read-only commands only, run production probes against `cake-shaper` via `ssh cake-shaper ...`, or run locally only when already logged into the production host. Do not inspect the dev VM as production evidence. Capture systemd facts for `wanctl@spectrum.service`, `wanctl@att.service`, and `steering.service`: `ActiveState`, `SubState`, `ExecMainStartTimestamp`, `ExecMainPID`, `NRestarts`, `ExecStart`, `FragmentPath`, `WatchdogUSec`, and service user/group if available. Do not save raw `Environment=` or `/etc/wanctl/secrets` output. Capture autorate `/health` JSON from deployed/bound endpoints, deriving endpoints from deployed config first; use known prior endpoints `http://10.10.110.223:9101/health` and `http://10.10.110.227:9101/health` only after confirming they match deployed config or recording the mismatch. Discover steering health endpoint from deployed steering config, service logs, or listening sockets read-only; do not hard-code loopback. If steering endpoint discovery fails, write `evidence/health-steering.json` as a JSON object with `status: "unavailable"`, `searched_sources`, `error`, and `downstream_impact`. Append every command, production host/source (`cake-shaper` or local-production), and output path to `evidence/README.md`. Per D-01/D-03/D-04/D-06, do not deploy, restart, write config, write RouterOS state, or stage a steering degraded restart.</action>
  <verify>
    <automated>test -s .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-spectrum.txt &amp;&amp; test -s .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-att.txt &amp;&amp; test -s .planning/phases/212-production-inventory-and-drift-audit/evidence/systemd-steering.txt &amp;&amp; .venv/bin/python -m json.tool .planning/phases/212-production-inventory-and-drift-audit/evidence/health-spectrum.json &gt;/dev/null &amp;&amp; .venv/bin/python -m json.tool .planning/phases/212-production-inventory-and-drift-audit/evidence/health-att.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,pathlib; p=pathlib.Path('.planning/phases/212-production-inventory-and-drift-audit/evidence/health-steering.json'); d=json.loads(p.read_text()); assert isinstance(d, dict); assert (d.get('status') == 'unavailable' and 'searched_sources' in d and 'downstream_impact' in d) or ('status' in d and ('steering' in d or 'decision' in d or 'counters' in d))"</automated>
  </verify>
  <acceptance_criteria>
    - Systemd artifacts identify active/inactive state, start timestamp, restart count, ExecStart, unit fragment path, and watchdog setting for all three services.
    - Health artifacts for Spectrum and ATT contain JSON object roots with `status`, `version`, `uptime_seconds`, and either `summary` or `wans` fields as emitted by the daemon.
    - Steering evidence either contains valid steering `/health` JSON or a clear read-only discovery failure note with searched config/log/socket sources.
    - Artifact text contains no `systemctl restart`, `scripts/deploy.sh`, RouterOS write command, config-write command, or raw secret-bearing environment dump.
  </acceptance_criteria>
  <done>Systemd and health evidence exists for Spectrum, ATT, and steering, with endpoint provenance recorded.</done>
</task>

<task type="auto">
  <name>Task 212-01-03: Capture redacted deployed config and steering state snapshots</name>
  <read_first>
    .planning/phases/212-production-inventory-and-drift-audit/evidence/config-spectrum.redacted.yaml
    .planning/phases/212-production-inventory-and-drift-audit/evidence/config-att.redacted.yaml
    .planning/phases/212-production-inventory-and-drift-audit/evidence/config-steering.redacted.yaml
    .planning/phases/212-production-inventory-and-drift-audit/evidence/steering-state.redacted.json
    .planning/phases/212-production-inventory-and-drift-audit/212-CONTEXT.md
    .planning/phases/212-production-inventory-and-drift-audit/212-RESEARCH.md
    .planning/phases/212-production-inventory-and-drift-audit/212-VALIDATION.md
    .planning/phases/212-production-inventory-and-drift-audit/212-PATTERNS.md
    src/wanctl/config_base.py
    src/wanctl/state_manager.py
    configs/spectrum.yaml
    configs/att.yaml
    configs/steering.yaml
  </read_first>
  <files>.planning/phases/212-production-inventory-and-drift-audit/evidence/config-spectrum.redacted.yaml, .planning/phases/212-production-inventory-and-drift-audit/evidence/config-att.redacted.yaml, .planning/phases/212-production-inventory-and-drift-audit/evidence/config-steering.redacted.yaml, .planning/phases/212-production-inventory-and-drift-audit/evidence/steering-state.redacted.json, .planning/phases/212-production-inventory-and-drift-audit/evidence/README.md</files>
  <action>Read deployed `/etc/wanctl/spectrum.yaml`, `/etc/wanctl/att.yaml`, `/etc/wanctl/steering.yaml`, and `/var/lib/wanctl/steering_state.json` from `cake-shaper` via `ssh cake-shaper ...`, or locally only when already logged into the production host; do not use dev-VM files as production evidence. Before writing artifacts, recursively redact any key whose lowercase name contains `password`, `secret`, `token`, `credential`, `auth`, `key`, or `private`; preserve D-09 proof fields such as WAN name, transport, router host identity, queue names, floors, ceilings, setpoints, DOCSIS mode, health/metrics ports, steering thresholds, state paths, and cooldowns. For steering state, capture shape/current state/transition timestamps if present, but do not trigger state recovery or backup mutation. If a file is absent or permission-blocked, write a small artifact stating the exact production host/source, read-only command, error class, and downstream impact rather than escalating to mutation.</action>
  <verify>
    <automated>test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/config-spectrum.redacted.yaml &amp;&amp; test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/config-att.redacted.yaml &amp;&amp; test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/config-steering.redacted.yaml &amp;&amp; test -f .planning/phases/212-production-inventory-and-drift-audit/evidence/steering-state.redacted.json &amp;&amp; ! grep -RIE '(password|secret|token|credential|auth|key|private)[[:space:]]*[:=][[:space:]]*[^&lt;{]' .planning/phases/212-production-inventory-and-drift-audit/evidence</automated>
  </verify>
  <acceptance_criteria>
    - Redacted config artifacts exist for Spectrum, ATT, and steering, even if a permission/error note is required for one surface.
    - `steering-state.redacted.json` exists and contains either valid redacted JSON or a JSON object with `status: "unavailable"`, `source`, `error`, and `downstream_impact`.
    - Secret-like keys are replaced with `&lt;REDACTED&gt;` or omitted with explanation; raw `/etc/wanctl/secrets` contents are never saved.
    - Folded todo `2026-04-17-investigate-steering-degraded-on-clean-restart` is represented only as current-state inventory; no controlled degraded restart is staged.
  </acceptance_criteria>
  <done>Deployed config/state evidence is saved in secret-safe form and ready for comparison in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production host → planning artifacts | Live service/config/state data crosses into committed evidence files. |
| deployed YAML/state → redaction process | Potential secrets must be removed before artifact persistence. |
| docs/repo expectations → live probes | Stale expectations must not override live reality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-01 | I | `evidence/config-*.redacted.yaml`, `evidence/steering-state.redacted.json` | mitigate | Recursively redact keys matching `password`, `secret`, `token`, `credential`, `auth`, `key`, or `private`; never save `/etc/wanctl/secrets` output. |
| T-212-02 | T/D | production services and RouterOS | mitigate | Restrict tasks to read-only `systemctl show/status`, config reads, and `/health` GETs; reject deploy/restart/config write/RouterOS write commands in artifacts. |
| T-212-03 | S/I | `/health` endpoints | mitigate | Derive endpoints from deployed config and cross-check with systemd service state; do not assume `127.0.0.1:9101`. |
| T-212-04 | I | codebase maps and prior docs | mitigate | Treat docs as expectations only; save live probe output as authoritative evidence and annotate mismatches. |
</threat_model>

<verification>
Run all task automated checks. Because this plan is docs/artifacts only, do not add helper tests unless new reusable helper code is introduced. If helper code is introduced despite this plan, follow 212-VALIDATION.md and add `tests/test_phase212_inventory.py` before relying on it.
</verification>

<success_criteria>
DRIFT-01 evidence surfaces and DRIFT-03 redacted config/state evidence are captured under stable paths with no production mutation and no raw secret leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/212-production-inventory-and-drift-audit/212-01-SUMMARY.md`.
</output>
