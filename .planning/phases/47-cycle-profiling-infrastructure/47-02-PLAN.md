---
phase: 47-cycle-profiling-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - scripts/analyze_profiling.py
  - scripts/profiling_collector.py
  - docs/PROFILING.md
autonomous: true
requirements: [PROF-01, PROF-02]
must_haves:
  truths:
    - "Analysis report references 50ms cycle budget, not 2s or 10min"
    - "Analysis report calculates utilization percentage against 50ms budget"
    - "Analysis report includes P50 percentile alongside P95 and P99"
    - "PROFILING.md documents --profile flag usage and 50ms interval context"
    - "PROFILING.md documents correct subsystem labels for both daemons"
    - "profiling_collector.py regex still matches new label format"
  artifacts:
    - path: "scripts/analyze_profiling.py"
      provides: "Updated analysis script with 50ms budget and P50 percentile"
      contains: "p50_ms"
    - path: "scripts/profiling_collector.py"
      provides: "Updated collector with P50 in output formats"
      contains: "p50_ms"
    - path: "docs/PROFILING.md"
      provides: "Updated profiling guide for v1.9"
      contains: "--profile"
  key_links:
    - from: "scripts/analyze_profiling.py"
      to: "scripts/profiling_collector.py"
      via: "shared parse_timing_lines regex pattern"
      pattern: "parse_timing_lines"
    - from: "docs/PROFILING.md"
      to: "scripts/analyze_profiling.py"
      via: "documents usage of analysis script"
      pattern: "analyze_profiling"
---

<objective>
Update analysis scripts and documentation for 50ms cycle budget context. The existing scripts reference stale 2-second intervals from v1.0. Update to 50ms budget, add P50 percentile, and document the --profile flag.

Purpose: Ensures the profiling pipeline (PROF-01) works end-to-end with correct budget calculations. Without these updates, analysis would show misleading 1900ms headroom instead of 10ms.

Output: Updated analysis scripts with 50ms budget and P50 support. Updated PROFILING.md with v1.9 context.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-cycle-profiling-infrastructure/47-RESEARCH.md
@.planning/phases/47-cycle-profiling-infrastructure/47-01-SUMMARY.md

@scripts/analyze_profiling.py
@scripts/profiling_collector.py
@docs/PROFILING.md

<interfaces>
<!-- Labels finalized in Plan 01 -->
Autorate: autorate_rtt_measurement, autorate_router_communication, autorate_state_management, autorate_cycle_total
Steering: steering_rtt_measurement, steering_cake_stats, steering_state_management, steering_cycle_total

<!-- Budget context -->
Both daemons: 50ms (0.05s) cycle interval. Current utilization: 60-80%.

<!-- Log output format from OperationProfiler.report() -->
```
=== Profiling Report ===
autorate_cycle_total: count=1200, min=28.5ms, avg=35.2ms, max=72.1ms, p95=45.3ms, p99=62.8ms
autorate_rtt_measurement: count=1200, min=20.1ms, avg=30.5ms, max=55.2ms, p95=40.1ms, p99=50.3ms
```

<!-- Existing regex in both scripts (works with new labels) -->
```python
pattern = r'(\w+): (\d+\.\d+)ms'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update analysis scripts for 50ms budget context</name>
  <files>scripts/analyze_profiling.py, scripts/profiling_collector.py</files>
  <action>
    **In scripts/analyze_profiling.py:**

    1. In `calculate_statistics()` -- add P50 percentile:
       ```python
       p50_idx = int((50 / 100) * (count - 1))
       ```
       Add `"p50_ms": sorted_samples[p50_idx]` to the return dict (between min_ms and avg_ms entries).

    2. In `generate_markdown_report()` -- accept `budget_ms` parameter (default 50.0):
       - Replace "Steering Cycle (2-second intervals)" header with "Steering Cycle (50ms intervals)"
       - Replace "Autorate Cycle (10-minute intervals by default)" header with "Autorate Cycle (50ms intervals)"
       - For both cycle sections, calculate and display:
         - Utilization: `(avg_ms / budget_ms) * 100`
         - Headroom: `budget_ms - avg_ms`
         - Warning if P99 exceeds budget_ms
       - Add P50 column to the summary statistics table
       - Update the Notes section: replace "2-second target" with cycle budget reference

    3. Add `--budget` CLI argument to argparse:
       ```python
       parser.add_argument("--budget", type=float, default=50.0,
                           help="Cycle budget in ms for utilization calculations (default: 50.0)")
       ```
       Pass `args.budget` to `generate_markdown_report()`.

    **In scripts/profiling_collector.py:**

    1. In `calculate_statistics()` -- add P50:
       ```python
       p50_idx = int((50 / 100) * (count - 1))
       ```
       Add `"p50_ms": sorted_samples[p50_idx]` to return dict.

    2. In `format_text_output()` -- add P50 to display format.

    3. In `format_csv_output()` -- add p50_ms column to CSV header and data rows.
  </action>
  <verify>
    <automated>python3 scripts/analyze_profiling.py --help | grep -q budget && python3 scripts/profiling_collector.py --help && echo "OK: scripts updated"</automated>
  </verify>
  <done>Both scripts include P50 percentile. analyze_profiling.py uses 50ms budget for utilization calculations. --budget flag available for custom budget values. No stale 2-second or 10-minute references in generated reports.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROFILING.md for v1.9 context</name>
  <files>docs/PROFILING.md</files>
  <action>
    Rewrite docs/PROFILING.md to reflect v1.9 context. Major changes:

    1. **Overview:** Change "must stay under 2 seconds" to "must stay under 50ms per cycle". Both daemons now run at 50ms (20Hz), not 2s/10min. Current utilization: 60-80%.

    2. **Add new section "Using the --profile Flag":**
       - Both daemons accept `--profile` to enable periodic profiling reports
       - When enabled: timing collected every cycle (<0.1ms overhead), aggregate report every 60s at INFO level
       - Individual per-cycle timing visible at DEBUG level (with --debug)
       - Production usage: edit systemd service to add --profile, restart, collect, remove flag

    3. **Prerequisites:** Update to mention --profile flag instead of checking for timing hooks in logs. Show new label examples (autorate_rtt_measurement, steering_cake_stats, etc.).

    4. **Data Collection:** Change minimum from 7 days to 1 hour (72,000 cycles at 50ms). Recommended: 4-24 hours. Add systemd service edit instructions.

    5. **Expected Baseline Latencies table:** Replace with 50ms budget context:

       | Subsystem | Typical | % of 50ms |
       |-----------|---------|-----------|
       | autorate_rtt_measurement | 20-40ms | 40-80% |
       | autorate_router_communication | 0ms / 15-25ms | 0% / 30-50% |
       | autorate_state_management | <1ms | <2% |
       | autorate_cycle_total | 30-45ms | 60-90% |
       | steering_rtt_measurement | 20-40ms | 40-80% |
       | steering_cake_stats | 15-25ms | 30-50% |
       | steering_state_management | <1ms | <2% |
       | steering_cycle_total | 30-50ms | 60-100% |

    6. **Interpreting Results:** Replace all 2-second references with 50ms. "p99 should be under 50ms". "Utilization >80% means optimization needed (Phase 48 target: ~40%)".

    7. **Common Findings:** Update example profiles for 50ms budget.

    8. **Next Steps:** Reference Phase 48 (Hot Path Optimization).

    9. **Troubleshooting:** Replace "wait 10 min / 2 sec" with "wait 60 seconds for first profiling report". Add "check for '=== Profiling Report ===' in logs".

    10. **Scripts Reference:** Document --budget argument for analyze_profiling.py. Document P50 in output.
  </action>
  <verify>
    <automated>grep -c "50ms" docs/PROFILING.md && grep -c "\-\-profile" docs/PROFILING.md && ! grep -q "2.second" docs/PROFILING.md && echo "OK: PROFILING.md updated"</automated>
  </verify>
  <done>PROFILING.md fully updated for v1.9: 50ms budget context throughout, --profile flag documented, correct subsystem labels, updated baselines. No stale 2-second or 10-minute references remain.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "2-second\|2 seconds\|10.min\|10-minute" docs/PROFILING.md` -- zero matches (stale refs removed)
2. `grep -c "50ms" docs/PROFILING.md` -- multiple matches
3. `python3 scripts/analyze_profiling.py --help` -- shows --budget argument
4. `python3 scripts/profiling_collector.py --help` -- still works
5. `.venv/bin/pytest tests/ -v` -- full suite green
</verification>

<success_criteria>
- analyze_profiling.py generates reports with 50ms budget context, utilization %, P50 percentile
- profiling_collector.py includes P50 in all output formats
- analyze_profiling.py accepts --budget argument
- PROFILING.md fully updated: 50ms intervals, --profile flag, correct subsystem labels
- No stale 2-second or 10-minute references in PROFILING.md
</success_criteria>

<output>
After completion, create `.planning/phases/47-cycle-profiling-infrastructure/47-02-SUMMARY.md`
</output>
