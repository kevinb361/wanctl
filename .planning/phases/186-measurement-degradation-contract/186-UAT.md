---
status: complete
phase: 186-measurement-degradation-contract
source:
  - 186-01-SUMMARY.md
  - 186-02-SUMMARY.md
  - 186-03-SUMMARY.md
started: 2026-04-15T09:45:16Z
updated: 2026-04-15T09:49:20Z
---

## Current Test

[testing complete]

## Tests

### 1. Measurement Payload Surface
expected: Inspect the `wan_health[wan].measurement` payload on `/health` or through a focused local harness. It should still expose the legacy fields `available`, `raw_rtt_ms`, `staleness_sec`, `active_reflector_hosts`, and `successful_reflector_hosts`, and it should now also expose `state`, `successful_count`, and `stale`. The separate `reflector_quality` section should remain unchanged.
result: pass

### 2. Successful Count State Mapping
expected: A measurement with 3 successful reflectors reports `state="healthy"` and `successful_count=3`; 2 successful reflectors report `state="reduced"`; 0 or 1 successful reflectors report `state="collapsed"`.
result: pass

### 3. Staleness Boundary And Fallback
expected: `stale` stays false when `staleness_sec == 3 * cadence_sec`, flips true when `staleness_sec > 3 * cadence_sec`, and defaults to true when cadence is missing, non-positive, or the sample age is missing.
result: pass

### 4. Contract Regression Coverage
expected: The focused Phase 186 regression checks still pass: `tests/test_health_check.py` is green, the hot-path health slice is green, and the collected test IDs include all six explicit state/stale combinations.
result: pass

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
