---
phase: 232-cleanup-boundary-guard-tooling-fixes
plan: 03
type: execute
wave: 2
depends_on: [232-01, 232-02]
files_modified:
  - .planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md
  - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md
  - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json
autonomous: true
requirements: [FIX-02, SAFE-15]

must_haves:
  truths:
    - "The 2026-04-17 digest-permission todo is closed by recorded validation against the v1.44 Phase 208 T12/TOOL-03 tolerance, not by silent deletion"
    - "Validation evidence maps each T12/TOOL-03 truth (skip+continue on unreadable DB, exit-0 hint when none readable, query errors bubble) to the pinning test that proves it, with a fresh green run recorded"
    - "No reimplementation is performed unless validation shows the acceptance criterion unmet (current evidence: met)"
    - "SAFE-15 controller-path zero-diff vs the v1.50 anchor is VERIFIED at the phase boundary with committed JSON evidence — not assumed"
  artifacts:
    - path: ".planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md"
      provides: "Closed todo with closed_by_phase/verdict frontmatter and Resolution section"
      contains: "closed_by_phase: 232"
    - path: ".planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md"
      provides: "FIX-02 validation evidence (test mapping + fresh run output + optional live check)"
    - path: ".planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json"
      provides: "SAFE-15 phase-boundary proof JSON"
      contains: "\"passed\": true"
  key_links:
    - from: ".planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md"
      to: "tests/test_operator_digest.py"
      via: "truth→test mapping table"
      pattern: "test_digest_skips_unreadable_db"
    - from: ".planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json"
      to: "scripts/phase225-safe13-boundary-check.sh"
      via: "--anchor v1.50 --out invocation"
      pattern: "v1.50"
---

<objective>
Close FIX-02 by validating actual `wanctl-operator-summary --digest` behavior against the
v1.44 Phase 208 T12/TOOL-03 unreadable-DB open tolerance and recording the evidence, then
moving the 2026-04-17 todo to closed/ — and close the phase by VERIFYING the SAFE-15
controller-path zero-diff invariant at the 232 boundary with committed JSON evidence.

Purpose: FIX-02 (todo hygiene with evidence, no blind reimplementation) + SAFE-15
(9th consecutive milestone of controller-path zero-diff discipline, verified not assumed).
Output: closed todo, FIX-02 evidence doc, SAFE-15 boundary JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-RESEARCH.md
@.planning/todos/pending/2026-04-17-operator-summary-digest-permission-handling.md
@tests/test_operator_digest.py
@src/wanctl/operator_summary.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate digest tolerance against T12/TOOL-03 and close the todo with evidence</name>
  <files>.planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md, .planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-17-operator-summary-digest-permission-handling.md (the todo being closed — its acceptance criteria)
    - .planning/milestones/v1.44-phases/208-carry-on-quick-tasks-t17a-t9-t12/208-03-PLAN.md (T12/TOOL-03 must_haves truths — the tolerance contract being validated against)
    - src/wanctl/operator_summary.py (print_digest implementation — read-only; do NOT modify)
    - tests/test_operator_digest.py (the 9 pinning tests)
    - .planning/todos/closed/2026-04-08-investigate-tcp-12down-latency-spikes-under-multi-flow-downl.md (closed-todo frontmatter convention: closed_by_phase, verdict)
  </read_first>
  <action>
    Validation-first, per FIX-02 ("reimplemented only if validation shows the acceptance criterion
    unmet"):

    1. Run `.venv/bin/pytest -o addopts='' tests/test_operator_digest.py -q` and capture the output.
    2. Write `.planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md`
       containing: (a) a truth→test mapping table pairing each T12/TOOL-03 truth from 208-03-PLAN.md
       must_haves with the pinning test — unreadable-DB skip+continue → test_digest_skips_unreadable_db;
       all-unreadable exit-0 hint → test_digest_all_unreadable_exits_zero_with_hint; write-OSError
       skip → test_digest_skips_on_output_write_oserror; query errors bubble (NOT skipped) →
       test_digest_missing_alerts_table_bubbles_not_skipped; all-writes-fail exit-1 →
       test_digest_all_writes_fail_emits_distinct_message; discovery OSError →
       test_digest_discovery_oserror_caught — plus the implementation anchor
       (`print_digest` open-boundary catch of `(sqlite3.OperationalError, OSError)` and
       `_DIGEST_SKIP_PREFIX`); (b) the fresh pytest run output; (c) a best-effort READ-ONLY live
       check: `ssh -o ConnectTimeout=10 -o BatchMode=yes kevin@10.10.110.223 'wanctl-operator-summary --digest; echo rc=$?'`
       (or the deployed entrypoint path if the wrapper is absent) recording stdout/stderr/rc — this
       command only opens DBs read-only and mutates nothing; if the host or command is unavailable,
       record the failure verbatim and note that tests remain the primary deterministic evidence
       (per 232-RESEARCH.md Pitfall 5); (d) verdict line: T12/TOOL-03 acceptance criterion MET —
       no reimplementation required. If (and only if) the pytest run FAILS, stop, do not close the
       todo, and surface the failure for replanning — do not patch src/wanctl/ in this plan.
    3. Close the todo: `git mv .planning/todos/pending/2026-04-17-operator-summary-digest-permission-handling.md .planning/todos/closed/`
       and edit the moved file: add frontmatter keys `closed_by_phase: 232` and
       `verdict: validated_already_implemented_v144_phase208_tool03`, and append a `## Resolution`
       section stating the todo's proposed behavior was implemented (in stricter form) by v1.44
       Phase 208 plan 208-03 (T12/TOOL-03), pointing at the evidence file and noting the message
       texts differ from the todo's literal suggestion by design (stable `operator-summary digest:`
       stderr prefixes from Phase 208 supersede the todo's draft wording).
    4. Commit planning files with `git add -f` (`.planning/` is gitignored but tracked); use
       `SKIP_DOC_CHECK=1` if the doc hook blocks.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_operator_digest.py -q && test -f .planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md && ! test -f .planning/todos/pending/2026-04-17-operator-summary-digest-permission-handling.md && grep -q "closed_by_phase: 232" .planning/todos/closed/2026-04-17-operator-summary-digest-permission-handling.md && grep -q "test_digest_skips_unreadable_db" .planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/fix02-digest-validation.md</automated>
  </verify>
  <done>
    Digest tests green; evidence doc exists with truth→test mapping, fresh run output, live-check
    result-or-fallback, and MET verdict; todo moved to closed/ with convention-conformant
    frontmatter; `src/wanctl/operator_summary.py` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify SAFE-15 controller-path zero-diff at the phase 232 boundary</name>
  <files>.planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (the reused checker — controller_targets list matches SAFE-15; takes --anchor/--out; exits 1 on failure; JSON `passed` key; requires a clean tree for its dirty-tree check)
  </read_first>
  <action>
    This task runs LAST in the phase, after all plan-01/plan-02 commits are in (wave 2 ordering
    enforces this; additionally confirm `git status --porcelain` is clean for src/wanctl/ before
    running, and that no uncommitted phase work remains).

    Run the existing checker against the v1.50 milestone anchor (no new abstraction, per
    REQUIREMENTS.md):
    `bash scripts/phase225-safe13-boundary-check.sh --anchor v1.50 --out .planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json`

    Requirements: exit code 0 and JSON `passed: true` — controller targets
    (wan_controller.py, wan_controller_state.py, queue_controller.py, cake_signal.py,
    alert_engine.py, fusion_healer.py, backends/) byte-identical to v1.50. If the check FAILS,
    stop the phase and surface the diff — a controller-path mutation in this phase is a hard
    SAFE-15 violation, not something to allowlist.

    Commit the evidence JSON with `git add -f` (and `SKIP_DOC_CHECK=1` if the hook blocks),
    message `docs(232): record SAFE-15 boundary proof vs v1.50`.
  </action>
  <verify>
    <automated>bash scripts/phase225-safe13-boundary-check.sh --anchor v1.50 --out /tmp/safe15-recheck.json && python3 -c "import json;d=json.load(open('.planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/safe15-boundary-232.json'));assert d['passed'] is True" && git diff v1.50..HEAD --stat -- src/wanctl/wan_controller.py src/wanctl/queue_controller.py src/wanctl/cake_signal.py src/wanctl/alert_engine.py src/wanctl/fusion_healer.py src/wanctl/backends/ | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>
    SAFE-15 evidence JSON committed with `passed: true`; independent `git diff v1.50..HEAD` over the
    controller-path files is empty; phase boundary verified, not assumed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → live host (read-only SSH) | Optional FIX-02 live digest check crosses to production host; must remain inspection-only |
| planning artifacts → milestone audit | Evidence files are the audit trail; falsified/absent evidence breaks SAFE-15 accountability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-09 | Tampering | live digest check | mitigate | Command is `wanctl-operator-summary --digest` only (opens DBs `mode=ro`); BatchMode SSH; no sudo, no service/qdisc verbs; recorded verbatim in evidence |
| T-232-10 | Repudiation | SAFE-15 claim | mitigate | Committed JSON proof from the established phase225 checker plus an independent `git diff v1.50..HEAD` assertion in verify |
| T-232-11 | Information disclosure | evidence files | accept | Evidence lives in gitignored-but-tracked `.planning/` (private repo hosting); contains hostnames/IPs already present throughout `.planning/` per project convention |
| T-232-SC | Tampering | package installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
- `.venv/bin/pytest -o addopts='' tests/test_operator_digest.py -q` green; `src/wanctl/` untouched by this plan.
- Todo present in `closed/`, absent from `pending/`, frontmatter conforms to the Phase 221 closed-todo convention.
- SAFE-15 JSON committed with `passed: true`; recheck run also exits 0.
- Full suite green before phase verify: `.venv/bin/pytest tests/ -q`.
</verification>

<success_criteria>
- FIX-02 satisfied: todo closed with tests + recorded evidence validating the T12/TOOL-03 tolerance;
  reimplementation correctly NOT performed (criterion met).
- SAFE-15 satisfied at the 232 boundary: controller-path zero-diff vs v1.50 verified with committed
  evidence.
</success_criteria>

<output>
Create `.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-03-SUMMARY.md` when done
</output>
