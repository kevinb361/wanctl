---
phase: 232-cleanup-boundary-guard-tooling-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-cleanup-boundary.sh
  - tests/test_cleanup_boundary_guard.py
autonomous: true
requirements: [BOUND-01]

must_haves:
  truths:
    - "Operator can run `bash scripts/check-cleanup-boundary.sh` on demand and gets exit 0 on a clean repo, exit 1 on any denylist violation, exit 2 on usage/git error"
    - "Removing any future-doc denylisted file from the worktree makes the guard exit non-zero (fails closed)"
    - "Every manifest row carries an explicit policy — `must-match-anchor` (exists AND hash-equal to v1.50 anchor) or `must-exist` (exists; content drift allowed) — so the guard's intent per file is machine-readable, not implied"
    - "Modifying a `must-match-anchor` file vs the v1.50 anchor makes the guard exit non-zero"
    - "Modifying a `must-exist` file (FIX-01 surfaces, living docs) does NOT fail the guard, but removing it still does"
    - "The default pytest suite contains a test that runs the guard against the real repo, so sweep work cannot land past a denylist violation without turning the suite red — and that gate cannot silently skip: a checkout missing the planning workspace FAILS the test unless `WANCTL_BOUND01_ALLOW_NO_PLANNING=1` is explicitly set"
  artifacts:
    - path: "scripts/check-cleanup-boundary.sh"
      provides: "BOUND-01 machine-checkable denylist guard with JSON evidence"
      contains: "src/wanctl/autorate_continuous.py"
    - path: "tests/test_cleanup_boundary_guard.py"
      provides: "Sweep-gate wiring: clean-repo pass + synthetic fail-closed proofs"
      min_lines: 60
  key_links:
    - from: "tests/test_cleanup_boundary_guard.py"
      to: "scripts/check-cleanup-boundary.sh"
      via: "subprocess.run bash invocation"
      pattern: "check-cleanup-boundary\\.sh"
    - from: "scripts/check-cleanup-boundary.sh"
      to: ".planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md"
      via: "denylist manifest entries sourced from the future doc"
      pattern: "WANCTL_CAKE_AUTORATE_FUTURE"
---

<objective>
Create the BOUND-01 cleanup boundary guard: a read-only script that encodes the
`WANCTL_CAKE_AUTORATE_FUTURE.md` "Not safe to remove until after ATT migration soak"
denylist as a concrete file manifest verified against the immutable `v1.50` git anchor,
failing closed (non-zero) if any denylisted surface is removed or modified — plus a
default-suite pytest that wires the guard as the gate all v1.51 sweep work (Phase 233)
must pass.

Purpose: BOUND-01 — the boundary must exist before any sweep. ROLE-01 (native
controller retirement) is event-gated and NOT met; the denylist is binding.
Output: `scripts/check-cleanup-boundary.sh`, `tests/test_cleanup_boundary_guard.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-RESEARCH.md
@.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-PATTERNS.md
@scripts/phase225-safe13-boundary-check.sh
@scripts/check-safe07-source-diff.sh
@.planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-cleanup-boundary.sh denylist guard</name>
  <files>scripts/check-cleanup-boundary.sh</files>
  <read_first>
    - scripts/phase225-safe13-boundary-check.sh (structural template: set -euo pipefail, --anchor/--out arg parse, python3 heredoc comparison, JSON evidence, fail-closed exit)
    - scripts/check-safe07-source-diff.sh (allowlist-in-header precedent and 0/1/2 exit contract)
    - .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md sections "What not to delete yet" and "Cleanup policy" (the canonical denylist this script encodes)
    - .planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-RESEARCH.md section "The Denylist Manifest" (exact path list, verified at v1.50)
  </read_first>
  <action>
    Create `scripts/check-cleanup-boundary.sh` modeled on `phase225-safe13-boundary-check.sh`:

    - Header comment: BOUND-01 cleanup boundary guard; encodes the WANCTL_CAKE_AUTORATE_FUTURE.md
      no-delete list; read-only posture (no worktree/refs/service mutation); run on demand and
      before/after every Phase 233 sweep commit; exit contract `0` clean / `1` BOUND-01 VIOLATION /
      `2` usage or git error.
    - `set -euo pipefail`. Flags: `--anchor REF` (default `v1.50`), `--out PATH` (default
      `.planning/phases/232-cleanup-boundary-guard-tooling-fixes/evidence/cleanup-boundary-check.json`),
      `--print-manifest` (print manifest rows as `class<TAB>path<TAB>policy` and exit 0 — consumed
      by the pytest harness), `--help`. Unknown args -> usage to stderr, exit 2. Validate anchor with
      `git rev-parse --verify "${ANCHOR}^{commit}"`; failure -> exit 2.
    - Embed the denylist manifest exactly as enumerated in 232-RESEARCH.md "The Denylist Manifest":
      class `native-controller`: src/wanctl/autorate_continuous.py;
      class `native-deploy-path`: deploy/systemd/wanctl@.service, scripts/install.sh,
      scripts/install-systemd.sh, scripts/deploy.sh;
      class `native-tests`: tests/test_autorate_continuous.py, tests/test_autorate_config.py,
      tests/test_autorate_baseline_bounds.py, tests/test_autorate_entry_points.py,
      tests/test_autorate_error_recovery.py, tests/test_autorate_metrics_recording.py,
      tests/test_autorate_telemetry.py;
      class `native-config-validation`: src/wanctl/autorate_config.py, src/wanctl/config_base.py,
      src/wanctl/config_validation_utils.py, src/wanctl/check_config.py,
      src/wanctl/check_config_validators.py, tests/test_check_config.py, tests/test_config_base.py,
      tests/test_config_validation_utils.py;
      class `rollback-commands-docs`: scripts/phase231-rollback.sh, scripts/phase227-rollback.sh,
      tests/test_phase231_rollback.py, docs/UPGRADING.md, docs/DEPLOYMENT.md,
      .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md.
    - Explicit per-row policy field (cross-AI review: name the semantics so BOUND-01's intent is
      unambiguous — this is a no-delete guard with a stricter no-touch tier, not one fuzzy rule):
      `must-match-anchor` (default — must exist in worktree AND, when the path exists in the anchor
      tree, worktree content must hash-equal the anchor blob) or `must-exist` (must exist in
      worktree; content drift vs anchor allowed — deletion still forbidden). Mark as `must-exist`,
      each with an inline comment naming its authorization: scripts/phase231-rollback.sh (FIX-01 /
      Phase 231 review CR-01), tests/test_phase231_rollback.py (FIX-01 proof + WR-02),
      docs/UPGRADING.md and docs/DEPLOYMENT.md (SWEEP-02 residual doc verification may annotate mode
      notes; deletion still forbidden), .planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md
      (living planning doc; appended trial logs are normal). All other paths are `must-match-anchor`.
    - Comparison core in a `python3 - "$ANCHOR" "$OUT" <<'PY'` heredoc (repo convention): for each
      manifest row capture worktree existence (`Path.exists()`), anchor blob hash via
      `git rev-parse <anchor>:<path>` (absent at anchor -> existence-only check, recorded as
      `anchor_present: false`), worktree hash via `git hash-object <path>`.
      VERIFIED constraint: `.planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` is
      gitignored AND untracked (`git check-ignore` hits `.gitignore:12`; `git ls-files` empty) —
      for it the guard uses worktree existence only and records `tracked: false` in its row;
      its absence is still a violation (it is the canonical denylist source). Violation when: missing
      from worktree (any policy), or hashes differ for `must-match-anchor` policy with anchor present.
      Write JSON evidence: `proof_type: "bound01-cleanup-boundary-check"`, `captured_utc`, `anchor`,
      `overall_pass`, and per-path rows `{class, path, policy, anchor_present, exists, status}` where
      status is one of `ok | allowlisted-modified | MISSING | MODIFIED`. Print each violation to
      stderr prefixed `BOUND-01 VIOLATION:`. Exit 1 if any violation, else 0 after printing
      `cleanup boundary check passed: <out>`.
    - chmod 755 the script.
  </action>
  <verify>
    <automated>bash -n scripts/check-cleanup-boundary.sh && shellcheck scripts/check-cleanup-boundary.sh && bash scripts/check-cleanup-boundary.sh --print-manifest | grep -c "	" | grep -qv '^0$' && bash scripts/check-cleanup-boundary.sh --out /tmp/bound01-smoke.json && python3 -c "import json;d=json.load(open('/tmp/bound01-smoke.json'));assert d['overall_pass'] is True and d['proof_type']=='bound01-cleanup-boundary-check' and len(d['checks'])>=24"</automated>
  </verify>
  <done>
    Guard exists, is executable, passes shellcheck, prints a tab-separated manifest with
    `--print-manifest`, exits 0 on the current clean repo, and writes JSON evidence containing
    one row per denylisted path (>= 24 rows) with `overall_pass: true`. Running it with a bogus
    anchor (`--anchor no-such-ref`) exits 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_cleanup_boundary_guard.py sweep-gate wiring</name>
  <files>tests/test_cleanup_boundary_guard.py</files>
  <read_first>
    - tests/test_phase231_rollback.py (REPO_ROOT/SCRIPT subprocess.run harness convention)
    - scripts/check-cleanup-boundary.sh (the guard created in Task 1 — flag surface and JSON shape)
  </read_first>
  <action>
    Create `tests/test_cleanup_boundary_guard.py` following the `REPO_ROOT = Path(__file__).resolve().parents[1]`
    + `subprocess.run(["bash", str(SCRIPT), ...], check=False, capture_output=True, text=True, timeout=60)`
    convention. This file is the BOUND-01 "wired so sweep work cannot proceed" gate: it lives in the
    default pytest suite, so any commit violating the denylist turns the suite red.

    Tests:
    1. `test_guard_passes_on_real_repo` — run the guard against the actual repo with `--out` to
       tmp_path; assert returncode 0 and JSON `overall_pass is True`. Docstring: this test IS the
       sweep gate (BOUND-01). Stripped-checkout handling must be LOUD, not a silent skip (cross-AI
       review: a silent skip weakens "default suite blocks sweep violations" outside the dev
       checkout): if `REPO_ROOT / ".planning" / "cake-autorate-trials"` does not exist, then
       `pytest.fail("BOUND-01 gate cannot run: planning workspace absent. Sweep work is only valid "
       "in the dev checkout. Set WANCTL_BOUND01_ALLOW_NO_PLANNING=1 only for intentionally "
       "planning-less checkouts.")` UNLESS the env var `WANCTL_BOUND01_ALLOW_NO_PLANNING=1` is set,
       in which case `pytest.skip(...)` with the same explanatory message — the future doc is
       gitignored/untracked so a fresh clone lacks it, and that opt-out must be an explicit operator
       decision, never the default.
    2. Synthetic scratch-repo fixture (`_make_scratch_repo(tmp_path)`): `git init` a temp repo
       (set user.email/user.name via `-c` flags), build the full manifest tree by parsing
       `--print-manifest` output from the real guard (create each listed path with placeholder
       content, including parent dirs), `git add -A`, commit, `git tag bound01-test-anchor`. Copy
       the guard script into the scratch repo's scripts/ dir is NOT needed — invoke the real script
       with `cwd=scratch_repo` so all git/worktree resolution happens inside the scratch repo, and
       pass `--anchor bound01-test-anchor --out <tmp json>`.
    3. `test_guard_fails_closed_on_removed_file` — in scratch repo, delete
       `src/wanctl/autorate_continuous.py` from the worktree; assert returncode 1, stderr contains
       `BOUND-01 VIOLATION`, JSON row for that path has status `MISSING`, `overall_pass is False`.
    4. `test_guard_fails_closed_on_modified_immutable_file` — in scratch repo, append a line to
       `deploy/systemd/wanctl@.service`; assert returncode 1 and that path's row status `MODIFIED`.
    5. `test_guard_allows_modification_of_must_exist_file` — in scratch repo, append a
       line to `scripts/phase231-rollback.sh` (policy `must-exist`); assert returncode 0 and that
       row status `allowlisted-modified`; then ALSO delete the same file and assert returncode 1
       (removal of a `must-exist` file still fails).
    6. `test_guard_handles_anchor_absent_untracked_row` (cross-AI review: real manifest case — the
       future doc is gitignored/untracked) — in scratch repo, gitignore the
       `.planning/cake-autorate-trials/WANCTL_CAKE_AUTORATE_FUTURE.md` path BEFORE the commit so it
       is absent from the anchor tree but present in the worktree; run the guard and assert
       returncode 0 with that row recording `anchor_present` false (existence-only check); then
       delete the file from the worktree and assert returncode 1 with status `MISSING` (the
       canonical denylist source going missing is always a violation).
    7. `test_guard_exits_2_on_unknown_anchor` — real repo, `--anchor definitely-not-a-ref`,
       assert returncode 2.

    Keep tests hermetic: never mutate the real worktree; scratch repo only.
  </action>
  <verify>
    <automated>.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q</automated>
  </verify>
  <done>
    All 6+ tests pass; the clean-repo gate test passes against the real repo and fails loudly (not
    skips) on planning-less checkouts unless WANCTL_BOUND01_ALLOW_NO_PLANNING=1; fail-closed
    behavior (removed file, modified `must-match-anchor` file), `must-exist` semantics (modify ok,
    remove fails), and the anchor-absent untracked row are each pinned by a dedicated test;
    `.venv/bin/ruff check tests/test_cleanup_boundary_guard.py` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree/git state → guard verdict | Guard consumes repo state to authorize sweep work; a wrong verdict authorizes destructive deletion |
| operator CLI args → guard | `--anchor`/`--out` are operator-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-01 | Tampering | denylist manifest in guard | mitigate | Manifest is in-script and the guard itself is exercised by a default-suite test; manifest rows asserted >= 24 in smoke verify; future-doc named in header so drift is reviewable |
| T-232-02 | Repudiation | guard verdict | mitigate | JSON evidence with `captured_utc`, anchor, per-path status written on every run |
| T-232-03 | Elevation of privilege | guard execution | accept | Script is read-only (git plumbing + hashing only); no sudo, no SSH, no service or qdisc calls — verified by shellcheck review and absence of mutation verbs |
| T-232-04 | Spoofing | `--anchor` ref | mitigate | `git rev-parse --verify "${ANCHOR}^{commit}"` before any comparison; failure exits 2 fail-closed |
| T-232-SC | Tampering | package installs | accept | No package-manager installs in this plan (bash + stdlib python3 + pytest already present) |
</threat_model>

<verification>
- `bash scripts/check-cleanup-boundary.sh` exits 0 on the current repo and writes evidence JSON.
- `.venv/bin/pytest -o addopts='' tests/test_cleanup_boundary_guard.py -q` green.
- `shellcheck scripts/check-cleanup-boundary.sh` clean.
- Hot-path slice still green: `.venv/bin/pytest -o addopts='' tests/test_cake_signal.py tests/test_queue_controller.py tests/test_wan_controller.py tests/test_health_check.py -q`.
- `git status` shows no changes outside scripts/ and tests/ (SAFE-15 surface discipline).
</verification>

<success_criteria>
- BOUND-01 satisfied: machine-checkable guard encodes the future-doc denylist, fails closed
  (non-zero) on touch/removal of any denylisted surface, is runnable on demand, and is wired into
  the default test suite so sweep work cannot proceed past a violation.
- Zero `src/wanctl/` diff introduced by this plan.
</success_criteria>

<output>
Create `.planning/phases/232-cleanup-boundary-guard-tooling-fixes/232-01-SUMMARY.md` when done
</output>
