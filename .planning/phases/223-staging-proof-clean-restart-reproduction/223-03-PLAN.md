---
phase: 223-staging-proof-clean-restart-reproduction
plan: 03
type: execute
wave: 3
depends_on: [223-01, 223-02]
files_modified:
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md
autonomous: true
requirements: [PROOF-03, SAFE-12]
must_haves:
  truths:
    - "Operator can read `evidence/spine-evidence.{json,md}` and see, for every fixture in the corpus including `clean-restart-degraded`, an explicit verdict (`preserves | breaks | ambiguous`) against each of the three steering spine invariants: binary on/off, only-new-connections rerouted, autorate-baseline-RTT-authoritative."
    - "The spine-evidence verdicts are computed from observable harness output (FakeRouterTransport toggle log + tempdir state-file reads) recorded by Plan 01 and Plan 02 — NOT from re-running git diffs or re-reading Plan 222 audit artifacts."
    - "If ANY fixture's spine verdict is `breaks` OR if `clean-restart-degraded` outcome was `reproduced-bug`, `spine-evidence.md` records the divergence with a cited evidence row and a recommendation directed at Phase 224's pre-canary gate or a follow-up phase. The phase does not auto-fail on `breaks`; verify-phase + the operator decide whether Phase 224 can proceed."
    - "SAFE-12 boundary check passes: zero controller-path source diff vs the v1.47-close commit `bee343b0c2f16207101aec82007a5e55fa9b6407` across committed, staged, unstaged, untracked, and porcelain checks. Allowlist matches Phase 222 §10 exactly: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`, `src/wanctl/fusion*.py`."
    - "`evidence/safe12-boundary-check.json` and `evidence/safe12-boundary-check.md` follow the Phase 222 artifact contract EXACTLY (REVIEWS.md MEDIUM): same key set including `allowlist_paths` (NOT `allowlist`), same nested `dirty_tree.status_porcelain` (NOT `dirty_tree.porcelain`), same allowlist coverage, internally-consistent pass booleans, and a reproducibility section the operator can copy-paste to re-verify against any future HEAD. The exact Phase 222 schema is in `.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json`."
    - "No production mutation occurs in this plan — read-only over the working tree + the Plan 01/02 evidence artifacts. No live router call, no `/var/lib/wanctl/` write, no deploy."
  artifacts:
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      provides: "PROOF-03 structured evidence: per-fixture, per-invariant verdict (`preserves | breaks | ambiguous`); overall corpus verdict; cited evidence rows from replay-results.json and clean-restart-reproduction.json."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md"
      provides: "PROOF-03 operator-facing report — spine-contract table, per-fixture verdict rows, divergence notes (if any), Phase 224 readiness note."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json"
      provides: "SAFE-12 structured boundary check (same shape as Phase 222 artifact): baseline_commit, allowlist, per_path_diff, committed_clean, dirty_tree object with staged/unstaged/untracked/porcelain arrays, pass booleans."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md"
      provides: "Human-readable SAFE-12 boundary report — invariant, baseline, allowlist, committed diff, dirty tree, verdict, reproducibility section."
  key_links:
    - from: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      via: "Per-fixture steering_interactions + effective_steering_state_per_cycle + baseline_rtt_per_cycle -> spine invariant verdict computation"
      pattern: "preserves|breaks|ambiguous"
    - from: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      via: "clean-restart-degraded outcome row -> per-invariant verdict for that fixture"
      pattern: "clean-restart-degraded|reproduced-intentional|reproduced-bug|not-reproducible"
    - from: "git rev-parse v1.47^{commit}"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json#baseline_commit"
      via: "Read-only git command (expected literal bee343b0c2f16207101aec82007a5e55fa9b6407)"
      pattern: "bee343b0c2f16207101aec82007a5e55fa9b6407"
    - from: "SAFE-12 allowlist"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json#per_path_diff"
      via: "git diff against baseline_commit restricted to allowlist; expected empty per path"
      pattern: "wan_controller|queue_controller|cake_signal|backends|alert_engine|fusion"
---

<objective>
Produce the PROOF-03 spine-contract evidence artifacts (one per-invariant
verdict per fixture, with overall corpus verdict) and the SAFE-12 phase
boundary check artifacts.

The PROOF-03 verdicts are computed strictly from Plan 01 and Plan 02
observable evidence — `evidence/replay-results.json`,
`evidence/clean-restart-reproduction.json`, and (where present) the
FakeRouterTransport toggle logs. The plan does NOT re-spawn the harness; it
reads what is already on disk.

The SAFE-12 boundary check is methodologically identical to Phase 222 §10:
same v1.47-peeled baseline commit, same allowlist, same five-state
verification (committed diff + unstaged + staged + untracked + porcelain),
same artifact contract.

This plan does not modify steering source, controller-path source, or any
production state. It is read-only over the working tree and the prior plans'
evidence artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-01-PLAN.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-02-PLAN.md
@.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
@.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 223-03-01: Compute per-fixture, per-invariant spine verdict and write spine-evidence.{json,md}</name>
  <read_first>
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
    .planning/phases/222-steering-drift-audit/evidence/contract-diff.json
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md</files>
  <action>Read `evidence/replay-results.json` (corpus, with the enriched schema from Plan 01: `pre_steering_rule_state`, `effective_steering_state_per_cycle`, `steering_interactions`, `baseline_rtt_per_cycle`) and `evidence/clean-restart-reproduction.json` (PROOF-02 outcome). For each fixture row, compute three invariant verdicts.

(a) **Binary on/off (invariant 1)**: read the `steering_interactions` list and `effective_steering_state_per_cycle`. Verdict is `preserves` if every `enable_steering`/`disable_steering` interaction returned `True`/`False` (no partial/weighted/blended state) AND every `effective_steering_state_per_cycle` entry is in `{True, False}`; `breaks` if any entry has an out-of-range value OR if `effective_steering_state` is True on a cycle whose `current_state` is `SPECTRUM_GOOD` AND there was no `enable_steering` call (i.e., a stuck-enabled rule on a healthy state — captures the REVIEWS.md HIGH #3 case); `ambiguous` if `steering_interactions` is empty AND no effective steering happened AND the fixture's expected_decision did not require any toggle.

(b) **Only-new-connections rerouted (invariant 2)**: verdict is `preserves` if `FakeRouterTransport.assert_only_documented_calls()` succeeded for the fixture (i.e., the daemon only called `get_rule_status`/`enable_steering`/`disable_steering` — no conntrack flush, no connection-tracking mutation, no other RouterOSController-shaped method); `breaks` if any other call was attempted; `ambiguous` only if the fake transport did not log calls for the fixture (should not happen given Plan 01 acceptance criteria). NOTE: this verdict is a daemon-side surrogate — Plan 03's Markdown report MUST explicitly label it that way (REVIEWS.md suggestion): the harness verifies the daemon does not invoke conntrack-mutation methods, but the actual "only new connections rerouted" property is enforced by the RouterOS mangle rule definition itself, which the harness does not verify. If the operator wants stronger evidence, the report points to the exported RouterOS mangle rule config (out of scope for this phase).

(c) **Autorate-baseline-RTT-authoritative (invariant 3)**: read `baseline_rtt_per_cycle` from each fixture row. Verdict is `preserves` if every entry has `baseline_loader_called: true`, `baseline_read_path` resolves under the tempdir staging workspace (NOT under `/var/lib/wanctl/`, `/etc/wanctl/`, `/var/log/wanctl/`, or `/run/wanctl/`), `baseline_read_value` matches the value seeded into `workspace/spectrum_state.json` for that cycle (with `pytest.approx` tolerance), AND `spectrum_state_write_attempted` is False for every cycle (the daemon must not write to the spectrum state file in this phase); `breaks` if any of those conditions fail; `ambiguous` if `baseline_rtt_per_cycle` is empty (defensive).

For the `clean-restart-degraded` fixture specifically, incorporate the PROOF-02 outcome:
- If `clean-restart-reproduction.json.outcome == "reproduced-bug"` → invariant 1 is `breaks` for that fixture (spurious effective steering on persisted state, regardless of whether `enable_steering` was called).
- If `outcome == "reproduced-intentional"` → invariant 1 stays `preserves`.
- If `outcome == "not-reproducible"` → invariant 1 stays `preserves`.

Write `evidence/spine-evidence.json` with top-level `corpus_verdict` (`preserves` if every fixture row preserves all three invariants; `breaks` if any row has any `breaks`; `ambiguous` otherwise), `methodology` (one-paragraph derivation of the verdict rules above, including the explicit note that invariant 2 is a daemon-side surrogate), and `fixtures` (one row per fixture with keys `fixture`, `harness_mode` (copied from replay-results), `invariant_1_binary_on_off`, `invariant_2_new_only`, `invariant_3_autorate_baseline`, `overall_fixture_verdict`, `cited_evidence_rows`).

Write `evidence/spine-evidence.md` rendering the JSON as a per-fixture table, plus:
- Methodology section (with explicit "invariant 2 is a daemon-side surrogate" note)
- Corpus Verdict section
- Invariant-2 caveat section explaining the mangle-rule-config gap
- Phase 224 Readiness section that names which findings (if any) must be resolved or accepted before Phase 224 pre-canary work. If the `clean-restart-degraded` outcome is `reproduced-bug`, this section MUST explicitly say "Phase 224 BLOCKED unless fix lands or operator accepts risk" (matching Plan 02's Block Recommendation line).</action>
  <verify>
    <automated>test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json').read_text()); assert d['corpus_verdict'] in ('preserves','breaks','ambiguous'); assert isinstance(d['fixtures'], list) and len(d['fixtures']) &gt;= 5; req_keys = {'fixture','invariant_1_binary_on_off','invariant_2_new_only','invariant_3_autorate_baseline','overall_fixture_verdict','cited_evidence_rows'}; assert all(req_keys.issubset(set(f.keys())) for f in d['fixtures']); assert all(f['invariant_1_binary_on_off'] in ('preserves','breaks','ambiguous') for f in d['fixtures']); names = {f['fixture'] for f in d['fixtures']}; assert 'clean-restart-degraded' in names" &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md &amp;&amp; grep -q "Phase 224 Readiness" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/spine-evidence.json` parses with `json.tool`.
    - Top-level keys present: `corpus_verdict`, `methodology`, `fixtures`.
    - `corpus_verdict` is one of `preserves | breaks | ambiguous`.
    - `fixtures` is a list with at least 5 entries — one per fixture in the corpus (steady-good, onset-degraded, recovery, cake-read-failure, clean-restart-degraded — plus any others Plan 01/02 added).
    - Every fixture row has keys `fixture`, `invariant_1_binary_on_off`, `invariant_2_new_only`, `invariant_3_autorate_baseline`, `overall_fixture_verdict`, `cited_evidence_rows`. Each invariant verdict is one of `preserves | breaks | ambiguous`.
    - `clean-restart-degraded` appears as one of the fixture names AND its invariant 1 verdict reflects the PROOF-02 outcome rule (reproduced-bug → breaks; intentional/not-reproducible → preserves).
    - `evidence/spine-evidence.md` exists and contains sections "Methodology", "Per-Fixture Verdict Table", "Corpus Verdict", "Phase 224 Readiness".
    - If `corpus_verdict == "breaks"`, the Markdown "Phase 224 Readiness" section names every breaking fixture and the proposed remediation path (Phase 224 pre-canary gate OR follow-up phase). If `preserves`, the section explicitly states no blockers.
  </acceptance_criteria>
  <done>PROOF-03 spine evidence artifacts emitted with cited per-fixture, per-invariant verdicts and corpus rollup.</done>
</task>

<task type="auto">
  <name>Task 223-03-02: Run SAFE-12 boundary check and write safe12-boundary-check.{json,md}</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md</files>
  <action>Reproduce the Phase 222 §10 SAFE-12 boundary check against the v1.47-close baseline commit `bee343b0c2f16207101aec82007a5e55fa9b6407`. Allowlist: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/` (directory), `src/wanctl/alert_engine.py`, and any `src/wanctl/fusion*.py` file (default `src/wanctl/fusion_healer.py`; if other `fusion*.py` files exist in the working tree, include them too — never extend the allowlist beyond `fusion*.py`). For each allowlist path, run: `git diff bee343b0c2f16207101aec82007a5e55fa9b6407 -- <path>` (committed diff), `git diff -- <path>` (unstaged), `git diff --cached -- <path>` (staged), `git ls-files --others --exclude-standard <path>` (untracked under the path; for the `backends/` directory, scan the directory), and `git status --porcelain -- <path>`. All five commands must return empty output for SAFE-12 to pass for that path.

**Schema MUST match Phase 222 artifact EXACTLY (REVIEWS.md MEDIUM):** Read `.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json` and use its key set verbatim. The required top-level keys are: `baseline_tag` ("v1.47"), `baseline_commit` ("bee343b0c2f16207101aec82007a5e55fa9b6407"), `audit_timestamp_utc` (ISO 8601), `allowlist_paths` (array — NOT `allowlist`), `per_path_diff` (object keyed by allowlist path with `added`, `removed`, `lines` per path — match the Phase 222 shape), `dirty_tree` (object with arrays `unstaged`, `staged`, `untracked`, `status_porcelain` — NOTE `status_porcelain` NOT `porcelain`), `passed` (bool), `committed_clean` (bool), `dirty_tree_clean` (bool). Add (Phase 223-specific, allowed since they extend the schema without contradicting it): `phase` (string, "223"), `invariant` (string, "SAFE-12"), and `reproducibility` (array of shell commands). All-empty arrays + all-`passed: true` per-path entries are required. Write `evidence/safe12-boundary-check.md` mirroring the Phase 222 §10 report layout: Invariant, Baseline, Allowlist, Committed Diff, Dirty Tree, Verdict, Reproducibility, and Precedent (cite SAFE-07/HRDN-01 dirty-tree precedent + Phase 222 SAFE-12 PASSED).

A schema-equivalence test MUST be included: load `.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json`, extract its top-level key set, and assert that all of those keys are also present in this phase's `safe12-boundary-check.json` (this phase may add the new optional keys `phase`, `invariant`, `reproducibility` without breaking compatibility, but MUST NOT rename existing keys).</action>
  <verify>
    <automated>test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "
import json, pathlib
d = json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json').read_text())
# Phase 223-specific keys
assert d['phase'] == '223'
assert d['invariant'] == 'SAFE-12'
assert d['baseline_commit'] == 'bee343b0c2f16207101aec82007a5e55fa9b6407'
assert d['committed_clean'] is True
assert d['dirty_tree_clean'] is True
assert d['passed'] is True
# Schema MUST match Phase 222 exactly: allowlist_paths (not allowlist), dirty_tree.status_porcelain (not dirty_tree.porcelain)
assert 'allowlist_paths' in d, 'must use allowlist_paths key (matches Phase 222 schema)'
assert 'allowlist' not in d, 'must NOT use allowlist key (renamed in Cycle 1 plan; revert to allowlist_paths)'
allowlist = {'src/wanctl/wan_controller.py', 'src/wanctl/queue_controller.py', 'src/wanctl/cake_signal.py', 'src/wanctl/backends/', 'src/wanctl/alert_engine.py'}
assert allowlist.issubset(set(d['allowlist_paths']))
assert 'status_porcelain' in d['dirty_tree'], 'dirty_tree must use status_porcelain key (matches Phase 222 schema)'
assert 'porcelain' not in d['dirty_tree'], 'dirty_tree must NOT use porcelain key (renamed in Cycle 1 plan; revert to status_porcelain)'
assert d['dirty_tree']['staged'] == [] and d['dirty_tree']['unstaged'] == [] and d['dirty_tree']['untracked'] == [] and d['dirty_tree']['status_porcelain'] == []
# Schema equivalence vs Phase 222
p222 = json.loads(pathlib.Path('.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json').read_text())
required_keys = set(p222.keys())
missing = required_keys - set(d.keys())
assert not missing, f'phase 223 SAFE-12 artifact missing keys from phase 222 schema: {missing}'
" &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md &amp;&amp; grep -q "PASSED" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md &amp;&amp; grep -q "bee343b0c2f16207101aec82007a5e55fa9b6407" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` parses with `json.tool` and contains all top-level keys from the Phase 222 artifact schema verbatim: `baseline_tag`, `baseline_commit`, `audit_timestamp_utc`, `allowlist_paths` (NOT `allowlist`), `per_path_diff`, `dirty_tree`, `passed`, `committed_clean`, `dirty_tree_clean`. Plus Phase 223-specific extensions: `phase`, `invariant`, `reproducibility`.
    - The key MUST be `allowlist_paths` (matching Phase 222), NOT `allowlist`. Schema-equivalence test loads `.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json` and asserts every key in that file is present in this phase's artifact (REVIEWS.md MEDIUM).
    - The nested key MUST be `dirty_tree.status_porcelain` (matching Phase 222), NOT `dirty_tree.porcelain`.
    - `phase == "223"`, `invariant == "SAFE-12"`, `baseline_commit == "bee343b0c2f16207101aec82007a5e55fa9b6407"`.
    - `allowlist_paths` contains at minimum: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`. `fusion*.py` paths are included (at least `src/wanctl/fusion_healer.py` if present in working tree).
    - `per_path_diff` covers every allowlist path with the Phase 222 per-path shape (e.g., `{"added": 0, "removed": 0, "lines": []}`) and each path is internally consistent with the file being unchanged.
    - `committed_clean: true`, `dirty_tree_clean: true`, `passed: true`.
    - `dirty_tree.staged`, `dirty_tree.unstaged`, `dirty_tree.untracked`, `dirty_tree.status_porcelain` are all empty arrays.
    - `reproducibility` lists the exact shell commands the operator can copy-paste to re-verify against any future HEAD.
    - `evidence/safe12-boundary-check.md` exists, contains the verdict `PASSED`, cites the baseline commit, and references the SAFE-07/HRDN-01 dirty-tree precedent.
  </acceptance_criteria>
  <done>SAFE-12 boundary check passes; PHASE 223 closure-gate-blocking SAFE-12 evidence artifact contract matches Phase 222 precedent.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "PROOF-03: Operator can read explicit per-fixture, per-invariant verdicts in `evidence/spine-evidence.{json,md}` covering the full Plan 01/02 corpus including `clean-restart-degraded`. The corpus verdict is `preserves`, `breaks`, or `ambiguous` with a documented Phase 224 readiness note."
    - "SAFE-12: The phase-boundary check artifact `evidence/safe12-boundary-check.{json,md}` shows `passed: true` across all five state categories (committed, staged, unstaged, untracked, porcelain) for the full allowlist."
    - "No production mutation occurs in this plan: read-only over the working tree and the Plan 01/02 evidence artifacts."
  </truths>
</must_haves>

<verification>
  <required>
    - "`evidence/spine-evidence.{json,md}` exist, JSON parses, every fixture has all three invariant verdicts, corpus verdict is one of `preserves|breaks|ambiguous`."
    - "Spine-evidence methodology explicitly labels invariant 2 (`only-new-connections rerouted`) as a daemon-side surrogate; the markdown report includes an invariant-2 caveat section."
    - "Spine-evidence invariant 3 (`autorate-baseline authority`) verdicts are computed from enriched `baseline_rtt_per_cycle` provenance (read_path, baseline_loader_called, spectrum_state_write_attempted) — not just bare `baseline_rtt_reads`."
    - "`evidence/safe12-boundary-check.{json,md}` exist, JSON parses, `passed: true`, `allowlist_paths` (NOT `allowlist`) covers controller-path files + backends + fusion*, and `dirty_tree.status_porcelain` (NOT `dirty_tree.porcelain`) is empty."
    - "Schema equivalence test passes: every key in `.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json` is present in this phase's `safe12-boundary-check.json` (REVIEWS.md MEDIUM)."
    - "Baseline commit literal in SAFE-12 JSON is `bee343b0c2f16207101aec82007a5e55fa9b6407`."
    - "Phase 224 readiness section in spine-evidence.md names blockers (if any) or explicitly states no blockers. If `clean-restart-degraded` outcome is `reproduced-bug`, the readiness section says 'Phase 224 BLOCKED' explicitly."
  </required>
</verification>
