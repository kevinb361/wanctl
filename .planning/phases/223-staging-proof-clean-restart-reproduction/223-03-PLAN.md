---
phase: 223-staging-proof-clean-restart-reproduction
plan: 03
type: execute
wave: 3
depends_on: [223-01, 223-02]
files_modified:
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md
autonomous: true
requirements: [PROOF-03, SAFE-12]
must_haves:
  truths:
    - "Operator can read `evidence/spine-evidence.{json,md}` and see, for every fixture in the corpus including `clean-restart-degraded`, an explicit verdict (`preserves | breaks | ambiguous`) against each of the three steering spine invariants: binary on/off, only-new-connections rerouted, autorate-baseline-RTT-authoritative."
    - "The spine-evidence verdicts are computed from observable harness output (FakeRouterTransport toggle log + tempdir state-file reads) recorded by Plan 01 and Plan 02 — NOT from re-running git diffs or re-reading Plan 222 audit artifacts."
    - "If ANY fixture's spine verdict is `breaks` OR if `clean-restart-degraded` outcome was `reproduced-bug`, `spine-evidence.md` records the divergence with a cited evidence row and a recommendation directed at Phase 224's pre-canary gate or a follow-up phase. The phase does not auto-fail on `breaks`; verify-phase + the operator decide whether Phase 224 can proceed."
    - "SAFE-12 boundary check passes: zero controller-path source diff vs the v1.47-close commit `bee343b0c2f16207101aec82007a5e55fa9b6407` across committed, staged, unstaged, untracked, and porcelain checks. Allowlist matches Phase 222 §10 exactly: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`, `src/wanctl/fusion*.py`."
    - "`evidence/safe12-boundary-check.json` and `evidence/safe12-boundary-check.md` follow the Phase 222 artifact contract: same key set, same allowlist coverage, internally-consistent pass booleans, and a reproducibility section the operator can copy-paste to re-verify against any future HEAD."
    - "No production mutation occurs in this plan — read-only over the working tree + the Plan 01/02 evidence artifacts. No live router call, no `/var/lib/wanctl/` write, no deploy."
  artifacts:
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      provides: "PROOF-03 structured evidence: per-fixture, per-invariant verdict (`preserves | breaks | ambiguous`); overall corpus verdict; cited evidence rows from replay-results.json and clean-restart-reproduction.json."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md"
      provides: "PROOF-03 operator-facing report — spine-contract table, per-fixture verdict rows, divergence notes (if any), Phase 224 readiness note."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json"
      provides: "SAFE-12 structured boundary check (same shape as Phase 222 artifact): baseline_commit, allowlist, per_path_diff, committed_clean, dirty_tree object with staged/unstaged/untracked/porcelain arrays, pass booleans."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md"
      provides: "Human-readable SAFE-12 boundary report — invariant, baseline, allowlist, committed diff, dirty tree, verdict, reproducibility section."
  key_links:
    - from: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      via: "Per-fixture mangle_toggles + baseline_rtt_reads -> spine invariant verdict computation"
      pattern: "preserves|breaks|ambiguous"
    - from: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json"
      via: "clean-restart-degraded outcome row -> per-invariant verdict for that fixture"
      pattern: "clean-restart-degraded|reproduced-intentional|reproduced-bug|not-reproducible"
    - from: "git rev-parse v1.47^{commit}"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json#baseline_commit"
      via: "Read-only git command (expected literal bee343b0c2f16207101aec82007a5e55fa9b6407)"
      pattern: "bee343b0c2f16207101aec82007a5e55fa9b6407"
    - from: "SAFE-12 allowlist"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json#per_path_diff"
      via: "git diff against baseline_commit restricted to allowlist; expected empty per path"
      pattern: "wan_controller|queue_controller|cake_signal|backends|alert_engine|fusion"
---

<objective>
Produce the PROOF-03 spine-contract evidence artifacts (one per-invariant
verdict per fixture, with overall corpus verdict) and the SAFE-12 phase
boundary check artifacts.

The PROOF-03 verdicts are computed strictly from Plan 01 and Plan 02
observable evidence — `evidence/replay-results.json`,
`evidence/clean-restart-reproduction.json`, and (where present) the
FakeRouterTransport toggle logs. The plan does NOT re-spawn the harness; it
reads what is already on disk.

The SAFE-12 boundary check is methodologically identical to Phase 222 §10:
same v1.47-peeled baseline commit, same allowlist, same five-state
verification (committed diff + unstaged + staged + untracked + porcelain),
same artifact contract.

This plan does not modify steering source, controller-path source, or any
production state. It is read-only over the working tree and the prior plans'
evidence artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-01-PLAN.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-02-PLAN.md
@.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
@.planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 223-03-01: Compute per-fixture, per-invariant spine verdict and write spine-evidence.{json,md}</name>
  <read_first>
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
    .planning/phases/222-steering-drift-audit/evidence/contract-diff.json
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md</files>
  <action>Read `evidence/replay-results.json` (corpus) and `evidence/clean-restart-reproduction.json` (PROOF-02 outcome). For each fixture row, compute three invariant verdicts. (a) **Binary on/off (invariant 1)**: read the `mangle_toggles` list; verdict is `preserves` if every entry has `enabled in {True, False}` (no partial/weighted/blended state), `breaks` if any entry has an out-of-range value, `ambiguous` if `mangle_toggles` is empty AND the fixture's expected_decision did not require any toggle. (b) **Only-new-connections rerouted (invariant 2)**: verdict is `preserves` if `FakeRouterTransport.assert_only_documented_calls()` succeeded for the fixture (i.e., the daemon only called `get_rule_status`/`set_rule_status` — no conntrack flush, no connection-tracking mutation, no other RouterOSController-shaped method); `breaks` if any other call was attempted; `ambiguous` only if the fake transport did not log calls for the fixture (should not happen given Plan 01 acceptance criteria). (c) **Autorate-baseline-RTT-authoritative (invariant 3)**: verdict is `preserves` if every per-cycle `baseline_rtt` value in the harness record came from the tempdir-redirected spectrum state file (i.e., the daemon read from `workspace/spectrum_state.json`, NOT computed a fresh baseline from its own samples — the conftest factory's no-production-paths assertion already enforces the path, so the verdict reduces to: every cycle's `baseline_rtt` matches the spectrum state file value at read time); `breaks` if the daemon ever wrote to the spectrum state file or computed a baseline absent from the file; `ambiguous` if no baseline reads are recorded (defensive). For the `clean-restart-degraded` fixture specifically, also incorporate the PROOF-02 outcome: if `outcome == reproduced-bug` then invariant 1 is downgraded to `breaks` for that fixture (spurious enable toggle on persisted state); if `outcome == reproduced-intentional` then invariant 1 stays `preserves` (recovery happens cleanly without spurious toggle); if `outcome == not-reproducible` then invariant 1 stays `preserves`. Write `evidence/spine-evidence.json` with top-level `corpus_verdict` (`preserves` if every fixture row preserves all three invariants; `breaks` if any row has any `breaks`; `ambiguous` otherwise), `methodology` (one-paragraph derivation of the verdict rules above), and `fixtures` (one row per fixture with keys `fixture`, `invariant_1_binary_on_off`, `invariant_2_new_only`, `invariant_3_autorate_baseline`, `overall_fixture_verdict`, `cited_evidence_rows`). Write `evidence/spine-evidence.md` rendering the JSON as a per-fixture table, plus a Methodology section, a Corpus Verdict section, and a "Phase 224 Readiness" section that names which findings (if any) must be resolved or accepted before Phase 224 pre-canary work.</action>
  <verify>
    <automated>test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.json').read_text()); assert d['corpus_verdict'] in ('preserves','breaks','ambiguous'); assert isinstance(d['fixtures'], list) and len(d['fixtures']) &gt;= 5; req_keys = {'fixture','invariant_1_binary_on_off','invariant_2_new_only','invariant_3_autorate_baseline','overall_fixture_verdict','cited_evidence_rows'}; assert all(req_keys.issubset(set(f.keys())) for f in d['fixtures']); assert all(f['invariant_1_binary_on_off'] in ('preserves','breaks','ambiguous') for f in d['fixtures']); names = {f['fixture'] for f in d['fixtures']}; assert 'clean-restart-degraded' in names" &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md &amp;&amp; grep -q "Phase 224 Readiness" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/spine-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/spine-evidence.json` parses with `json.tool`.
    - Top-level keys present: `corpus_verdict`, `methodology`, `fixtures`.
    - `corpus_verdict` is one of `preserves | breaks | ambiguous`.
    - `fixtures` is a list with at least 5 entries — one per fixture in the corpus (steady-good, onset-degraded, recovery, cake-read-failure, clean-restart-degraded — plus any others Plan 01/02 added).
    - Every fixture row has keys `fixture`, `invariant_1_binary_on_off`, `invariant_2_new_only`, `invariant_3_autorate_baseline`, `overall_fixture_verdict`, `cited_evidence_rows`. Each invariant verdict is one of `preserves | breaks | ambiguous`.
    - `clean-restart-degraded` appears as one of the fixture names AND its invariant 1 verdict reflects the PROOF-02 outcome rule (reproduced-bug → breaks; intentional/not-reproducible → preserves).
    - `evidence/spine-evidence.md` exists and contains sections "Methodology", "Per-Fixture Verdict Table", "Corpus Verdict", "Phase 224 Readiness".
    - If `corpus_verdict == "breaks"`, the Markdown "Phase 224 Readiness" section names every breaking fixture and the proposed remediation path (Phase 224 pre-canary gate OR follow-up phase). If `preserves`, the section explicitly states no blockers.
  </acceptance_criteria>
  <done>PROOF-03 spine evidence artifacts emitted with cited per-fixture, per-invariant verdicts and corpus rollup.</done>
</task>

<task type="auto">
  <name>Task 223-03-02: Run SAFE-12 boundary check and write safe12-boundary-check.{json,md}</name>
  <read_first>
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.json
    .planning/phases/222-steering-drift-audit/evidence/safe12-boundary-check.md
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md</files>
  <action>Reproduce the Phase 222 §10 SAFE-12 boundary check against the v1.47-close baseline commit `bee343b0c2f16207101aec82007a5e55fa9b6407`. Allowlist: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/` (directory), `src/wanctl/alert_engine.py`, and any `src/wanctl/fusion*.py` file (default `src/wanctl/fusion_healer.py`; if other `fusion*.py` files exist in the working tree, include them too — never extend the allowlist beyond `fusion*.py`). For each allowlist path, run: `git diff bee343b0c2f16207101aec82007a5e55fa9b6407 -- <path>` (committed diff), `git diff -- <path>` (unstaged), `git diff --cached -- <path>` (staged), `git ls-files --others --exclude-standard <path>` (untracked under the path; for the `backends/` directory, scan the directory), and `git status --porcelain -- <path>`. All five commands must return empty output for SAFE-12 to pass for that path. Aggregate the results into `evidence/safe12-boundary-check.json` matching the Phase 222 schema: top-level keys `phase` (string, "223"), `invariant` (string, "SAFE-12"), `baseline_tag` ("v1.47"), `baseline_commit` ("bee343b0c2f16207101aec82007a5e55fa9b6407"), `audit_timestamp_utc` (ISO 8601), `allowlist` (array of allowlist paths), `per_path_diff` (object keyed by allowlist path with the five command outputs and a per-path `passed` bool), `committed_clean` (bool), `dirty_tree` (object with arrays `staged`, `unstaged`, `untracked`, `porcelain`), `dirty_tree_clean` (bool), `passed` (bool — committed_clean AND dirty_tree_clean), and `reproducibility` (array of the exact shell commands the operator can copy-paste). All-empty arrays + all-`passed: true` per-path entries are required. Write `evidence/safe12-boundary-check.md` mirroring the Phase 222 §10 report layout: Invariant, Baseline, Allowlist, Committed Diff, Dirty Tree, Verdict, Reproducibility, and Precedent (cite SAFE-07/HRDN-01 dirty-tree precedent + Phase 222 SAFE-12 PASSED).</action>
  <verify>
    <automated>test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.json').read_text()); assert d['phase']=='223'; assert d['invariant']=='SAFE-12'; assert d['baseline_commit']=='bee343b0c2f16207101aec82007a5e55fa9b6407'; assert d['committed_clean'] is True; assert d['dirty_tree_clean'] is True; assert d['passed'] is True; assert d['dirty_tree']['staged']==[] and d['dirty_tree']['unstaged']==[] and d['dirty_tree']['untracked']==[] and d['dirty_tree']['porcelain']==[]; assert all(v['passed'] is True for v in d['per_path_diff'].values()); allowlist={'src/wanctl/wan_controller.py','src/wanctl/queue_controller.py','src/wanctl/cake_signal.py','src/wanctl/backends/','src/wanctl/alert_engine.py'}; assert allowlist.issubset(set(d['allowlist']))" &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md &amp;&amp; grep -q "PASSED" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md &amp;&amp; grep -q "bee343b0c2f16207101aec82007a5e55fa9b6407" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/safe12-boundary-check.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/safe12-boundary-check.json` parses with `json.tool` and contains top-level keys `phase`, `invariant`, `baseline_tag`, `baseline_commit`, `audit_timestamp_utc`, `allowlist`, `per_path_diff`, `committed_clean`, `dirty_tree`, `dirty_tree_clean`, `passed`, `reproducibility`.
    - `phase == "223"`, `invariant == "SAFE-12"`, `baseline_commit == "bee343b0c2f16207101aec82007a5e55fa9b6407"`.
    - `allowlist` contains at minimum: `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`. `fusion*.py` paths are included (at least `src/wanctl/fusion_healer.py` if present in working tree).
    - `per_path_diff` covers every allowlist path; each per-path entry has `passed: true` AND empty committed/unstaged/staged/untracked/porcelain command outputs.
    - `committed_clean: true`, `dirty_tree_clean: true`, `passed: true`.
    - `dirty_tree.staged`, `dirty_tree.unstaged`, `dirty_tree.untracked`, `dirty_tree.porcelain` are all empty arrays.
    - `reproducibility` lists the exact shell commands the operator can copy-paste to re-verify against any future HEAD.
    - `evidence/safe12-boundary-check.md` exists, contains the verdict `PASSED`, cites the baseline commit, and references the SAFE-07/HRDN-01 dirty-tree precedent.
  </acceptance_criteria>
  <done>SAFE-12 boundary check passes; PHASE 223 closure-gate-blocking SAFE-12 evidence artifact contract matches Phase 222 precedent.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "PROOF-03: Operator can read explicit per-fixture, per-invariant verdicts in `evidence/spine-evidence.{json,md}` covering the full Plan 01/02 corpus including `clean-restart-degraded`. The corpus verdict is `preserves`, `breaks`, or `ambiguous` with a documented Phase 224 readiness note."
    - "SAFE-12: The phase-boundary check artifact `evidence/safe12-boundary-check.{json,md}` shows `passed: true` across all five state categories (committed, staged, unstaged, untracked, porcelain) for the full allowlist."
    - "No production mutation occurs in this plan: read-only over the working tree and the Plan 01/02 evidence artifacts."
  </truths>
</must_haves>

<verification>
  <required>
    - "`evidence/spine-evidence.{json,md}` exist, JSON parses, every fixture has all three invariant verdicts, corpus verdict is one of `preserves|breaks|ambiguous`."
    - "`evidence/safe12-boundary-check.{json,md}` exist, JSON parses, `passed: true`, allowlist covers controller-path files + backends + fusion*."
    - "Baseline commit literal in SAFE-12 JSON is `bee343b0c2f16207101aec82007a5e55fa9b6407`."
    - "Phase 224 readiness section in spine-evidence.md names blockers (if any) or explicitly states no blockers."
  </required>
</verification>
