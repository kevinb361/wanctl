---
phase: 223-staging-proof-clean-restart-reproduction
plan: 02
type: execute
wave: 2
depends_on: [223-01]
files_modified:
  - tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml
  - tests/integration/steering_replay/test_clean_restart.py
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.md
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md
  - .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "Operator can reproduce (or read a fail-closed reason why reproduction is not feasible) the `2026-04-17-investigate-steering-degraded-on-clean-restart` symptom by running `.venv/bin/pytest tests/integration/steering_replay/test_clean_restart.py -v`."
    - "The clean-restart fixture pre-seeds the staging steering_state.json with `current_state: SPECTRUM_DEGRADED`, `good_count: 0`, a plausible `baseline_rtt`, and plausible `history_rtt`/`history_delta` arrays, then feeds GOOD-consistent measurement inputs (low RTT delta, no CAKE drops) on every cycle."
    - "The harness records (a) `pre_steering_rule_state` — the initial fake-router rule state at cycle 0 BEFORE any daemon code runs; (b) the daemon's cycle-1 observable `current_state`; (c) `cycle_1_effective_steering_state` — the fake-router rule state AFTER cycle 1 runs (this captures the effective-steering case where the rule is pre-enabled from boot AND persisted state is SPECTRUM_DEGRADED, even if the daemon issues NO `enable_steering` call — REVIEWS.md HIGH #3); (d) the cycle index at which the daemon transitions to `SPECTRUM_GOOD`; (e) `effective_steering_state_per_cycle` — the per-cycle effective steering state across the recovery window, not just `enable_steering` invocations — all into `evidence/clean-restart-reproduction.json`."
    - "`evidence/clean-restart-reproduction.md` records exactly ONE of the three §5 outcomes from 223-RESEARCH.md: (1) symptom reproduced + classified intentional with recovery-bound semantics; (2) symptom reproduced + classified bug with proposed-fix scope (no fix landed in this phase); (3) symptom not reproducible against post-drift code with documented reason."
    - "The folded todo `.planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md` is updated with: the PROOF-02 outcome, the exact evidence artifact paths, and a closure status note. The todo file is NOT silently deleted — it is annotated and either kept in `pending/` with a closure note OR moved per project todo-close convention, whichever the harness operator confirms."
    - "Plan 01's `evidence/replay-results.{json,md}` are extended (not overwritten) with the `clean-restart-degraded` fixture row so the operator sees the full corpus in one place."
    - "No fix to steering source lands in this plan. If outcome (2) names a fix, it is recorded as a recommendation for Phase 224's pre-canary gate or a follow-up phase. Steering source remains at the v1.47-close baseline modulo any Plan 01 Task 01 seam-edit (which itself MUST NOT change decision behavior)."
    - "SAFE-12 invariant holds: no edit to `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`, or `src/wanctl/fusion*.py`. Verified by Plan 03's boundary check."
  artifacts:
    - path: "tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml"
      provides: "PROOF-02 fixture: pre-seeds steering_state.json with SPECTRUM_DEGRADED + good_count 0; feeds GOOD-consistent measurement inputs on every cycle."
    - path: "tests/integration/steering_replay/test_clean_restart.py"
      provides: "Dedicated pytest entry for the clean-restart scenario; runs the fixture through `run_fixture` from Plan 01 and asserts the recorded outcome matches whichever of the three §5 outcomes the operator certifies."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json"
      provides: "Structured PROOF-02 evidence: pre_state, per-cycle observable state, transitions, mangle toggles, recovery cycle index, and final outcome verdict (one of `reproduced-intentional`, `reproduced-bug`, `not-reproducible`)."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.md"
      provides: "Operator-facing PROOF-02 report — the §5 outcome, the cited evidence rows, and the folded-todo closure note."
    - path: ".planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md"
      provides: "Folded-todo closure annotation — outcome, evidence links, and closure status."
  key_links:
    - from: "fixtures/clean-restart-degraded.yaml"
      to: "evidence/clean-restart-reproduction.json"
      via: "tests/integration/steering_replay/test_clean_restart.py drives run_fixture(); records observed cycle-1 state + recovery cycle"
      pattern: "cycle_1_observed_state|recovery_cycle|outcome|reproduced-intentional|reproduced-bug|not-reproducible"
    - from: "evidence/clean-restart-reproduction.md"
      to: ".planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md"
      via: "Closure annotation block referencing the evidence artifact path"
      pattern: "PROOF-02|outcome|reproduced|not-reproducible"
---

<objective>
Reproduce the folded `2026-04-17-investigate-steering-degraded-on-clean-restart`
symptom in the staging harness — OR produce a fail-closed reason why
reproduction is not feasible against the v1.47-close steering source —
using the Plan 01 harness machinery and a dedicated fixture.

PROOF-02 closes either way: per ROADMAP success criterion 2, reproduction
AND a documented fail-closed reason are both acceptable outcomes. The folded
todo closes either way (per the todo file's `resolves_phase: 223` field).

No fix to steering source lands in this plan. If outcome (2)
"reproduced-bug" is reached, the proposed fix is named and held against
Phase 224's pre-canary gate or a follow-up phase. Steering source remains
at the v1.47-close baseline modulo any Plan 01 Task 01 seam-edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-01-PLAN.md
@.planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md
@CLAUDE.md
@src/wanctl/steering/daemon.py
@src/wanctl/state_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 223-02-01: Author clean-restart-degraded fixture</name>
  <read_first>
    .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
    src/wanctl/steering/daemon.py
    src/wanctl/state_manager.py
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/README.md
  </read_first>
  <files>tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml</files>
  <action>Author `tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml` conforming to the enriched fixture schema documented in Plan 01's `evidence/README.md`.

Required top-level content:
- `name: clean-restart-degraded`
- `description`: cites the folded todo and the 2026-04-17 21:05:27 symptom
- `harness_mode: hysteresis-only` (consistent with the rest of the corpus by default; if any anchored runtime evidence pins this to confidence mode, switch to `confidence` and add the required `cycle_budget_derivation`)
- `cycle_budget_derivation`: matches the harness_mode rule from Plan 01
- `corpus_source: derived-from-phase-212-evidence` (the symptom comes from production observation, not synthesis)
- `derivation`: cites "binary on/off + autorate-baseline authority" (the symptom is interesting because it could spuriously toggle steering on at cycle 1, violating both invariants if persisted-DEGRADED is wrong)

`pre_state` MUST use the split schema (REVIEWS.md HIGH #3 + MEDIUM):

(a) `steering_pre_state` mirrors `SteeringStateManager` schema returned by `_get_steering_state_schema` in `src/wanctl/steering/daemon.py:779` and seeds: `current_state: SPECTRUM_DEGRADED`, `good_count: 0`, `history_rtt: [25, 25, 25, 25, 25]`, `history_delta: [0.5, 0.5, 0.5, 0.5, 0.5]`, `transitions: []`, `last_transition_time: null`, `rtt_delta_ewma: 0.5`, `queue_ewma: 0.0`, `cake_drops_history: []`, `queue_depth_history: []`, `cake_state_history: []`, `red_count: 0`, `congestion_state: GREEN`, `cake_read_failures: 0`.

(b) `autorate_state_by_cycle.default`: `{"ewma": {"baseline_rtt": 25.0}, "congestion": {"dl_state": "GREEN", "ul_state": "GREEN"}}` (real spectrum-state shape so `BaselineLoader.load_baseline_rtt` reads the canonical file).

(c) `initial_steering_rule_state` (NEW top-level field — REVIEWS.md HIGH #3): a single boolean specifying the fake `RouterOSController`'s initial rule-enabled state at cycle 0. Author this fixture with `initial_steering_rule_state: true` to model the worst-case "rule already enabled from a previous boot + persisted state SPECTRUM_DEGRADED" condition. The harness MUST also accept a `pre_rule_disabled` variant if the operator wants to additionally test the rule-pre-disabled-with-persisted-DEGRADED case; default fixture is the pre-enabled case because that is what the folded todo names as the riskier surface.

`cycles` lists at least 35 entries — every cycle feeds GOOD-consistent inputs: low RTT delta (0.5ms), no CAKE drops, queue_depth 0, CAKE state GREEN. `expected_decision` per cycle is intentionally NOT pinned (the harness records observed behavior, not pre-judged behavior) — instead set `expected_decision: observe` for all cycles.

Top-level `observation_targets` lists exactly what to record:
- `pre_steering_rule_state` (initial fake-router rule state at cycle 0, captured before daemon runs)
- `cycle_1_observed_current_state`
- `cycle_1_effective_steering_state` (fake-router rule state AFTER cycle 1, regardless of toggle activity — captures pre-enabled-but-untouched case)
- `effective_steering_state_per_cycle` (full list — captures every cycle whose effective steering state is `True` even if no `enable_steering` call was issued)
- `enable_steering_call_log` (subset of `interactions_log` filtered to `method == "enable_steering"`)
- `disable_steering_call_log`
- `recovery_cycle_to_GOOD` (first cycle where `current_state` becomes `SPECTRUM_GOOD`)
- `effective_steering_during_recovery_window` (subset of `effective_steering_state_per_cycle` from cycle 0 through `recovery_cycle_to_GOOD`)

Top-level `acceptable_outcomes` lists the three §5 outcomes from 223-RESEARCH.md.</action>
  <verify>
    <automated>test -f tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml &amp;&amp; .venv/bin/python -c "
import yaml
d = yaml.safe_load(open('tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml'))
assert d['name'] == 'clean-restart-degraded'
assert d['corpus_source'] == 'derived-from-phase-212-evidence'
assert d['harness_mode'] in ('hysteresis-only', 'confidence')
assert 'cycle_budget_derivation' in d
sp = d['pre_state']['steering_pre_state']
assert sp['current_state'] == 'SPECTRUM_DEGRADED' and sp['good_count'] == 0
ar = d['pre_state']['autorate_state_by_cycle']
default = ar.get('default') or ar.get(0)
assert default and 'ewma' in default and 'baseline_rtt' in default['ewma']
assert d['initial_steering_rule_state'] is True
assert len(d['cycles']) &gt;= 30
ot = d['observation_targets']
required = {'pre_steering_rule_state', 'cycle_1_observed_current_state', 'cycle_1_effective_steering_state', 'effective_steering_state_per_cycle', 'enable_steering_call_log', 'recovery_cycle_to_GOOD'}
assert required.issubset(set(ot)), f'missing observation_targets: {required - set(ot)}'
assert len(d['acceptable_outcomes']) == 3
"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml` exists and parses with `yaml.safe_load`.
    - `name == "clean-restart-degraded"`, `corpus_source == "derived-from-phase-212-evidence"`, `harness_mode` set, `cycle_budget_derivation` present.
    - `pre_state.steering_pre_state.current_state == "SPECTRUM_DEGRADED"`, `pre_state.steering_pre_state.good_count == 0`.
    - `pre_state.autorate_state_by_cycle` contains either a `default` entry or a `0` entry with the real spectrum-state shape `{ewma: {baseline_rtt: float}, congestion: {dl_state: str, ...}}`.
    - **`initial_steering_rule_state == true`** — the fixture models the worst-case rule-pre-enabled + persisted-DEGRADED case (REVIEWS.md HIGH #3).
    - `cycles` is a list with at least 30 entries (enough to observe recovery); every cycle's `inputs` reflects GOOD-consistent measurement (low RTT delta, no CAKE drops).
    - `expected_decision` for each cycle is the literal string `observe` (the harness records observed behavior, not asserts a pre-judged outcome).
    - Top-level `observation_targets` lists at least: `pre_steering_rule_state`, `cycle_1_observed_current_state`, `cycle_1_effective_steering_state`, `effective_steering_state_per_cycle`, `enable_steering_call_log`, `recovery_cycle_to_GOOD`.
    - Top-level `acceptable_outcomes` lists exactly three entries matching the §5 outcomes (`reproduced-intentional`, `reproduced-bug`, `not-reproducible`).
    - `derivation` field cites at least one of the three spine-contract invariants.
  </acceptance_criteria>
  <done>Clean-restart-degraded fixture authored with observation-mode semantics and explicit acceptable-outcome list.</done>
</task>

<task type="auto">
  <name>Task 223-02-02: Implement test_clean_restart.py and emit clean-restart-reproduction.{json,md}</name>
  <read_first>
    tests/integration/steering_replay/fixtures/clean-restart-degraded.yaml
    tests/integration/steering_replay/replay_harness.py
    tests/integration/steering_replay/conftest.py
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
    .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md
  </read_first>
  <files>tests/integration/steering_replay/test_clean_restart.py, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.md</files>
  <action>Implement `tests/integration/steering_replay/test_clean_restart.py` with two tests.

(a) `test_clean_restart_reproduction_runs`: calls `run_fixture("fixtures/clean-restart-degraded.yaml", staging_workspace)` from the Plan 01 harness with the fake router seeded to `initial_enabled = fixture["initial_steering_rule_state"]` (True for the default fixture). Captures the resulting observation dict and writes `evidence/clean-restart-reproduction.json`.

The JSON object MUST contain keys (REVIEWS.md HIGH #3 enrichment):
- `pre_state`: copy of fixture's split pre_state (`steering_pre_state` + `autorate_state_by_cycle`)
- `pre_steering_rule_state` (bool): initial fake-router rule state at cycle 0 BEFORE the daemon runs
- `cycle_1_observed_state` (string): daemon's `current_state` after cycle 1
- `cycle_1_effective_steering_state` (bool): fake-router rule state AFTER cycle 1 runs
- `effective_steering_state_per_cycle` (list[bool]): per-cycle effective steering state across all cycles
- `enable_steering_call_log` (list): every cycle in which the daemon called `enable_steering`
- `disable_steering_call_log` (list): every cycle in which the daemon called `disable_steering`
- `interactions_log` (list): the full fake-router interactions log
- `baseline_rtt_per_cycle` (list): per-cycle baseline-loader provenance (read_path, returned_value, loader_called)
- `recovery_cycle_to_GOOD` (int or null)
- `final_state` (string)
- `outcome` (one of `reproduced-intentional | reproduced-bug | not-reproducible`)
- `outcome_rationale` (string ≥ 40 chars citing the per-cycle effective steering state, not just toggle calls)
- `evidence_links` (object listing per-cycle observable rows)

**Outcome classification (REVIEWS.md HIGH #3 — fixed to key on effective steering state, not just `enable_steering` invocations):**

Define `recovery_window = effective_steering_state_per_cycle[0 : recovery_cycle_to_GOOD]` (or the whole list if no recovery happens within the fixture).

Define `any_effective_steering_during_recovery_window = any(state is True for state in recovery_window)`. This captures BOTH the case where the daemon called `enable_steering` AND the case where the rule was pre-enabled at boot and the daemon never touched it but the rule remained effective during persisted-DEGRADED cycles.

Classify:

- If `cycle_1_observed_state == "SPECTRUM_DEGRADED"` AND `recovery_cycle_to_GOOD` is not null AND `any_effective_steering_during_recovery_window` is True →
  → `outcome = "reproduced-bug"`. Rationale MUST cite: the cycle indices in the recovery window where `effective_steering_state == True`, the source (pre-enabled boot vs daemon-issued `enable_steering` call, distinguished by `enable_steering_call_log`), and the spine-contract clause violated (binary-on/off + autorate-baseline-authoritative — the rule is effectively steering traffic on persisted DEGRADED state without measurement-driven justification).

- If `cycle_1_observed_state == "SPECTRUM_DEGRADED"` AND `recovery_cycle_to_GOOD` is not null AND `any_effective_steering_during_recovery_window` is False (rule was pre-disabled, daemon issued no `enable_steering` call, and recovery happened cleanly) →
  → `outcome = "reproduced-intentional"`. Rationale MUST cite: rule pre-state, absence of any `enable_steering` calls in `enable_steering_call_log` during the recovery window, and the cycle at which recovery completed.

- If `cycle_1_observed_state == "SPECTRUM_GOOD"` (post-drift code re-derived state from inputs on cold start, ignoring persisted DEGRADED) →
  → `outcome = "not-reproducible"`. Rationale MUST cite: persisted-state value, cycle-1 observed state, and the implication that persisted-DEGRADED is not load-bearing on cold start.

- If none of the above (e.g., daemon raised an exception, fixture produced inconsistent observations) →
  → `outcome = "reproduced-bug"` with rationale `"harness produced inconsistent observations — see evidence_links for per-cycle rows"` AND the test FAILS with an `AssertionError` so the operator sees the inconsistency loudly (do NOT silently downgrade to `not-reproducible`).

(b) `test_clean_restart_outcome_is_documented`: re-reads `evidence/clean-restart-reproduction.json` and asserts: `outcome` field is one of the three documented values; `outcome_rationale` length ≥ 40 chars; the rationale string contains either `effective_steering` OR `pre-enabled` OR `enable_steering` OR `not load-bearing` (so the rationale demonstrably cites the per-cycle effective state, not just toggle calls).

Then write `evidence/clean-restart-reproduction.md` as a human-readable report containing: pre_state seed table (split into steering_pre_state + autorate_state_by_cycle), initial rule state line, per-cycle observation table (cycle, current_state, effective_steering_state, enable_steering_called, disable_steering_called, baseline_rtt, RTT delta), outcome verdict + rationale, the cited 223-RESEARCH.md §5 acceptable outcome, and a "Folded-Todo Closure" section that records the closure note that Task 03 will copy into the todo file.</action>
  <verify>
    <automated>test -f tests/integration/steering_replay/test_clean_restart.py &amp;&amp; .venv/bin/pytest -o addopts='' tests/integration/steering_replay/test_clean_restart.py -v 2&gt;&amp;1 | tail -30 &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json &gt;/dev/null &amp;&amp; .venv/bin/python -c "
import json, pathlib
d = json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json').read_text())
assert d['outcome'] in ('reproduced-intentional', 'reproduced-bug', 'not-reproducible')
assert len(d['outcome_rationale']) &gt;= 40
required = {'pre_state', 'pre_steering_rule_state', 'cycle_1_observed_state', 'cycle_1_effective_steering_state', 'effective_steering_state_per_cycle', 'enable_steering_call_log', 'disable_steering_call_log', 'interactions_log', 'baseline_rtt_per_cycle', 'recovery_cycle_to_GOOD', 'final_state', 'outcome', 'outcome_rationale', 'evidence_links'}
missing = required - set(d.keys())
assert not missing, f'missing keys in clean-restart-reproduction.json: {missing}'
assert isinstance(d['pre_steering_rule_state'], bool)
assert isinstance(d['effective_steering_state_per_cycle'], list)
rationale = d['outcome_rationale']
assert any(tok in rationale for tok in ('effective_steering', 'pre-enabled', 'enable_steering', 'not load-bearing')), 'rationale must cite per-cycle effective steering state, not just toggle calls'
"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/steering_replay/test_clean_restart.py` exists and contains the two tests named above.
    - `.venv/bin/pytest -o addopts='' tests/integration/steering_replay/test_clean_restart.py -v` exits 0.
    - `evidence/clean-restart-reproduction.json` parses with `json.tool` and contains all required keys: `pre_state`, `pre_steering_rule_state`, `cycle_1_observed_state`, `cycle_1_effective_steering_state`, `effective_steering_state_per_cycle`, `enable_steering_call_log`, `disable_steering_call_log`, `interactions_log`, `baseline_rtt_per_cycle`, `recovery_cycle_to_GOOD`, `final_state`, `outcome`, `outcome_rationale`, `evidence_links`.
    - `pre_steering_rule_state` is a boolean (initial fake-router rule state at cycle 0).
    - `effective_steering_state_per_cycle` is a list of booleans with length equal to `cycles_run`.
    - `outcome` is one of `reproduced-intentional | reproduced-bug | not-reproducible` (matches the three §5 outcomes).
    - `outcome_rationale` is a non-empty string of at least 40 characters AND contains at least one of the tokens `effective_steering`, `pre-enabled`, `enable_steering`, `not load-bearing` (so the rationale demonstrably cites per-cycle effective state, not only toggle invocations — REVIEWS.md HIGH #3).
    - For the default fixture (`initial_steering_rule_state: true` + `steering_pre_state.current_state: SPECTRUM_DEGRADED`), if `enable_steering_call_log` is empty AND `effective_steering_state_per_cycle[0]` is True AND `cycle_1_observed_state == "SPECTRUM_DEGRADED"`, the outcome MUST be `reproduced-bug` (the spurious-effective-steering case REVIEWS.md HIGH #3 calls out).
    - `evidence/clean-restart-reproduction.md` exists and contains sections titled "Pre-State Seed", "Initial Rule State", "Per-Cycle Observations", "Outcome Verdict", "Folded-Todo Closure". The per-cycle table columns include `effective_steering_state` and `enable_steering_called` (NOT only `mangle_enabled`).
    - The cited §5 outcome name in the Markdown report matches the JSON `outcome` field exactly.
    - If outcome is `reproduced-bug`: the Markdown report names the proposed fix scope (file, function, contract) but explicitly states the fix DID NOT land in this plan and is held against Phase 224 pre-canary or a follow-up phase. The Markdown report MUST also include a "Phase 224 Block Recommendation" line that says either "Phase 224 BLOCKED on this outcome unless fix lands or operator accepts the risk" or "Phase 224 NOT BLOCKED" (REVIEWS.md suggestion).
  </acceptance_criteria>
  <done>Clean-restart reproduction test runs; PROOF-02 evidence artifacts emitted with one of the three documented outcomes; folded-todo closure note drafted in the Markdown report.</done>
</task>

<task type="auto">
  <name>Task 223-02-03: Append clean-restart-degraded row to replay-results.{json,md} and annotate the folded todo</name>
  <read_first>
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/clean-restart-reproduction.md
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md
    .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md, .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md</files>
  <action>Read the Plan 01 corpus result file `evidence/replay-results.json` and append a new `fixtures[]` entry for `clean-restart-degraded` with `fixture=clean-restart-degraded`, `cycles_run`, `final_state`, `transitions`, `mangle_toggles`, `baseline_rtt_reads` (all copied/translated from `clean-restart-reproduction.json`), `verdict=observe` (this fixture uses observation mode, not match/diverge), and `verdict_rationale` set to the PROOF-02 outcome name + rationale. Update `evidence/replay-results.md` to add a matching row at the end of the per-fixture table. Then append a closure annotation block to `.planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md` at the bottom of the existing content (do NOT delete or rewrite existing content). The closure block MUST contain: a heading `## PROOF-02 Closure (Phase 223)`, the closure date, the outcome name from `clean-restart-reproduction.json`, the rationale (verbatim or summarized in ≤ 4 sentences), a link to `evidence/clean-restart-reproduction.md`, a link to `evidence/replay-results.md`, and a closure status line. If the outcome was `reproduced-bug`, also include a one-line "Fix held against Phase 224 pre-canary gate or follow-up phase — NOT landing in v1.48 Phase 223" statement. The todo file STAYS in `pending/` (do not move it) — the closure annotation is the resolution; project-level archival is a separate operator decision.</action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; data=json.loads(pathlib.Path('.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json').read_text()); fixtures = data.get('fixtures', data) if isinstance(data, dict) else data; names = [f['fixture'] for f in fixtures]; assert 'clean-restart-degraded' in names, f'clean-restart-degraded missing from replay-results.json, got {names}'" &amp;&amp; grep -q "clean-restart-degraded" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md &amp;&amp; grep -q "PROOF-02 Closure" .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md &amp;&amp; grep -qE "reproduced-intentional|reproduced-bug|not-reproducible" .planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/replay-results.json` contains a `fixtures[]` entry where `fixture == "clean-restart-degraded"` AND `verdict == "observe"`.
    - `evidence/replay-results.md` contains a table row mentioning `clean-restart-degraded` with the recorded outcome name.
    - `.planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md` contains a heading `## PROOF-02 Closure (Phase 223)`.
    - The closure block names the outcome (one of `reproduced-intentional | reproduced-bug | not-reproducible`), the closure date, and links to both `evidence/clean-restart-reproduction.md` and `evidence/replay-results.md`.
    - Existing content of the todo file is unchanged — closure is appended only.
    - If outcome is `reproduced-bug`: closure block includes the "Fix held against Phase 224 pre-canary gate or follow-up phase — NOT landing in v1.48 Phase 223" statement.
  </acceptance_criteria>
  <done>PROOF-02 results integrated into the corpus artifact; folded todo annotated with closure block citing the outcome and evidence.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "PROOF-02 closes: operator sees one of the three documented §5 outcomes recorded in `evidence/clean-restart-reproduction.{json,md}`, integrated into the corpus `evidence/replay-results.{json,md}`, and reflected as a closure annotation in the folded todo."
    - "PROOF-02 outcome classification keys on `pre_steering_rule_state` (initial rule state at cycle 0) AND `effective_steering_state_per_cycle` (per-cycle effective steering across the recovery window) — NOT only on `enable_steering` call invocations. This addresses REVIEWS.md HIGH #3: the rule-pre-enabled + persisted-DEGRADED case is now classified `reproduced-bug` even when the daemon issues zero `enable_steering` calls."
    - "If outcome is `reproduced-bug`, the Markdown report includes a 'Phase 224 Block Recommendation' line. The fix itself does NOT land in this plan — it is held against Phase 224 pre-canary gate or a follow-up phase."
    - "The folded todo file is annotated, not deleted. Project-level archival is a separate operator decision."
    - "SAFE-12 invariant holds: no controller-path edit. Steering source remains at v1.47-close baseline modulo any Plan 01 Task 01 seam edit (which itself does not change decision behavior)."
  </truths>
</must_haves>

<verification>
  <required>
    - "`evidence/clean-restart-reproduction.json` parses cleanly and contains all required enriched keys including `pre_steering_rule_state`, `effective_steering_state_per_cycle`, and `enable_steering_call_log`; `outcome` is one of the three documented values."
    - "`outcome_rationale` cites per-cycle effective steering state (contains at least one of `effective_steering`, `pre-enabled`, `enable_steering`, `not load-bearing`)."
    - "`evidence/clean-restart-reproduction.md` documents the outcome with cited per-cycle observables — including initial rule state row and per-cycle effective steering state column."
    - "If outcome is `reproduced-bug`, the Markdown report includes a 'Phase 224 Block Recommendation' line."
    - "`evidence/replay-results.{json,md}` include the `clean-restart-degraded` row."
    - "`.planning/todos/pending/2026-04-17-investigate-steering-degraded-on-clean-restart.md` has a PROOF-02 Closure annotation citing the outcome and evidence paths."
    - "`.venv/bin/pytest -o addopts='' tests/integration/steering_replay/test_clean_restart.py -v` exits 0."
  </required>
</verification>
