---
phase: 223-staging-proof-clean-restart-reproduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/__init__.py
  - tests/integration/steering_replay/__init__.py
  - tests/integration/steering_replay/conftest.py
  - tests/integration/steering_replay/fake_router_transport.py
  - tests/integration/steering_replay/replay_harness.py
  - tests/integration/steering_replay/fixtures/steady-good.yaml
  - tests/integration/steering_replay/fixtures/onset-degraded.yaml
  - tests/integration/steering_replay/fixtures/recovery.yaml
  - tests/integration/steering_replay/fixtures/cake-read-failure.yaml
  - tests/integration/steering_replay/test_replay_corpus.py
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/README.md
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json
  - .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "Operator can run `.venv/bin/pytest tests/integration/steering_replay/ -v` and see the offline replay harness drive the v1.47-close steering daemon decision pipeline through every fixture in the corpus with exit code 0."
    - "Operator can run `.venv/bin/python tests/integration/steering_replay/replay_harness.py --all` standalone (no pytest) and get JSON output covering every fixture in the corpus with explicit per-fixture verdicts."
    - "The harness uses a fake RouterOS transport that NEVER calls the live router REST/SSH. The fake mimics the ACTUAL `RouterOSController` daemon-facing surface verified in `src/wanctl/steering/daemon.py` (lines 805–920): `get_rule_status() -> bool | None`, `enable_steering() -> bool`, and `disable_steering() -> bool`. There is NO `set_rule_status()` method — that was a Cycle 1 plan error corrected via REVIEWS.md HIGH #1."
    - "The fake `RouterOSController` records every `enable_steering`/`disable_steering` call AND every `get_rule_status` read in an `interactions_log: list[dict]` (each entry has `cycle`, `method`, `result`, `timestamp_iso`). `get_rule_status` returns the fake's current rule state. `enable_steering`/`disable_steering` mutate the state, optionally invoke a configurable verification-retry stub, and return success."
    - "Harness confidence-mode posture is EXPLICIT, not implicit. The harness MUST run in one of two modes documented in `evidence/README.md`: (a) **hysteresis-only mode** — overrides `SteeringConfig.use_confidence_scoring=False` and authors fixture cycle counts against the simple hysteresis state machine; OR (b) **confidence mode** — keeps `use_confidence_scoring=True` and derives every fixture's degrade/recovery cycle counts from `configs/steering.yaml` (`confidence.sustain_duration_sec`, `confidence.recovery_sustain_sec`, `confidence.hold_down_duration_sec`) divided by `ASSESSMENT_INTERVAL_SECONDS = 0.05` (from `src/wanctl/steering/daemon.py:127`). Cycle 1 plans used 15-cycle degrade / ~35-cycle recovery against confidence-mode config; this would never satisfy `2.0s` sustain + `30.0s` hold-down. REVIEWS.md HIGH #2 corrected this."
    - "The harness redirects ALL state-file IO to a pytest tempdir or an explicit staging workspace under `.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/staging-state/`. No fixture path resolves to `/var/lib/wanctl/`, `/etc/wanctl/`, `/var/log/wanctl/`, or `/run/wanctl/`."
    - "Each fixture YAML conforms to the documented schema in `evidence/README.md`: top-level keys `name`, `description`, `pre_state` (object), `cycles` (list of per-cycle objects with `inputs`, `expected_decision`), and `corpus_source` (one of `synthesized-from-spine-contract`, `derived-from-phase-212-evidence`)."
    - "Fixtures are derived from the spine contract (binary on/off, only-new-connections rerouted, autorate-baseline authority) or from Phase 212 / Phase 222 read-only evidence. No fixture is derived from observing the post-drift code's own behavior (that would make the proof circular)."
    - "SAFE-12 invariant holds: no edit to `src/wanctl/wan_controller.py`, `src/wanctl/queue_controller.py`, `src/wanctl/cake_signal.py`, `src/wanctl/backends/`, `src/wanctl/alert_engine.py`, or `src/wanctl/fusion*.py`. Verified by Plan 03's boundary check."
    - "If a steering-source seam edit is required to make the daemon driveable from the harness, the seam is explicitly documented in `evidence/README.md` with the rationale, the exact diff scope (file + lines), and a regression test under `tests/steering/` covering the seam contract. Default expectation is NO steering-source edit — prefer constructor injection through existing arguments (config, logger, state-file path, router transport)."
    - "`evidence/replay-results.json` contains one entry per fixture with keys `fixture`, `harness_mode` (`hysteresis-only` | `confidence`), `cycle_interval_sec` (0.05), `cycle_budget_derivation` (object showing how degrade/recovery cycle counts were computed from config — e.g., `{\"source\": \"configs/steering.yaml\", \"sustain_duration_sec\": 2.0, \"hold_down_duration_sec\": 30.0, \"derived_sustain_cycles\": 40, \"derived_hold_down_cycles\": 600}` for confidence mode, or `{\"mode\": \"hysteresis-only\", \"confidence_disabled\": true}` for hysteresis mode), `cycles_run`, `pre_steering_rule_state` (initial fake-router rule state at cycle 0), `effective_steering_state_per_cycle` (list of per-cycle observable steering state — what the daemon's `run_cycle` actually drove the rule to, including cycles where no toggle was issued because the rule was already in the desired state), `final_state`, `transitions`, `steering_interactions` (replaces `mangle_toggles` — list of every fake-router method invocation with cycle, method name, result), `baseline_rtt_per_cycle` (replaces bare `baseline_rtt_reads` — each entry has `cycle`, `baseline_read_path`, `baseline_read_value`, `baseline_loader_called` bool, `spectrum_state_write_attempted` bool), `verdict`, `verdict_rationale`. Verdict is one of `matches | diverges | inconclusive`."
    - "`evidence/replay-results.md` renders the harness output as a human-readable table the operator can read without re-running pytest."
  artifacts:
    - path: "tests/integration/steering_replay/replay_harness.py"
      provides: "Operator-runnable entry point: drives the `SteeringDaemon` decision pipeline cycle-by-cycle against a fixture, captures `enable_steering`/`disable_steering`/`get_rule_status` interactions via the fake `RouterOSController`, captures state-file IO under tempdir, records initial rule state + per-cycle effective steering state, emits per-fixture JSON results."
    - path: "tests/integration/steering_replay/test_replay_corpus.py"
      provides: "Pytest entry point that runs every fixture and asserts the per-fixture verdict is `matches` (or has documented `diverges` justification recorded under §5 outcome 2 of 223-RESEARCH.md)."
    - path: "tests/integration/steering_replay/fake_router_transport.py"
      provides: "In-memory RouterOSController-shaped double exposing the ACTUAL daemon-facing surface: `get_rule_status() -> bool | None`, `enable_steering() -> bool`, `disable_steering() -> bool` (matches `src/wanctl/steering/daemon.py:805–920`). Records every interaction with cycle index, method name, and result. Never opens a network socket; raises `RuntimeError` if any other RouterOSController method is invoked."
    - path: "tests/integration/steering_replay/conftest.py"
      provides: "Pytest fixtures: tempdir state workspace, fake `RouterOSController` instance, `baseline_loader_spy` capturing per-call provenance, factory that wires a `SteeringDaemon` with redirected state files and the requested `harness_mode` (hysteresis-only | confidence)."
    - path: "tests/integration/steering_replay/fixtures/"
      provides: "YAML fixture corpus — steady-good, onset-degraded, recovery, cake-read-failure (clean-restart-degraded fixture is owned by Plan 02)."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/README.md"
      provides: "Operator command index: how to run the harness, fixture schema, corpus source policy, staging workspace layout, SAFE-12 posture statement, link to RESEARCH.md."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json"
      provides: "Structured per-fixture replay results (one entry per fixture in the corpus excluding clean-restart-degraded, which is owned by Plan 02 but appended into this artifact by that plan)."
    - path: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md"
      provides: "Human-readable replay results table — PROOF-01 operator-facing evidence."
  key_links:
    - from: "pytest tests/integration/steering_replay/ -v"
      to: ".planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json"
      via: "replay_harness.py emits JSON; test_replay_corpus.py asserts verdicts"
      pattern: "fixture|verdict|matches|diverges|inconclusive"
    - from: "tests/integration/steering_replay/fake_router_transport.py"
      to: "src/wanctl/steering/daemon.py::RouterOSController interface"
      via: "Constructor injection via SteeringConfig.router config OR explicit attribute override"
      pattern: "get_rule_status|enable_steering|disable_steering"
---

<objective>
Stand up the offline replay harness (PROOF-01). The harness drives the
v1.47-close `SteeringDaemon` decision pipeline through a fixture corpus
using a fake `RouterOSController` and tempdir-redirected state files. It
produces per-fixture verdicts (`matches | diverges | inconclusive`) and the
operator-facing `evidence/replay-results.{json,md}` artifacts.

**Class name correction (REVIEWS.md LOW):** The class is `SteeringDaemon` at
`src/wanctl/steering/daemon.py:1122` — NOT `WANSteeringDaemon`. Plan text
elsewhere may still say `WANSteeringDaemon`; the executor must use the
actual class name discovered during Task 01's seam inventory.

**Fake router API correction (REVIEWS.md HIGH #1):** The fake MUST mimic
the actual daemon-facing surface: `get_rule_status() -> bool | None`,
`enable_steering() -> bool`, `disable_steering() -> bool` (verified in
`src/wanctl/steering/daemon.py:805–920`). There is no `set_rule_status()`
method on `RouterOSController`. A fake with the wrong surface would test
an adapter rather than the daemon's real routing path.

**Confidence-mode timing correction (REVIEWS.md HIGH #2):** Production
`configs/steering.yaml` runs `use_confidence_scoring: true` with
`sustain_duration_sec: 2.0`, `recovery_sustain_sec: 3.0`, and
`hold_down_duration_sec: 30.0` against `ASSESSMENT_INTERVAL_SECONDS = 0.05`
(`daemon.py:127`). The harness MUST EITHER (a) explicitly disable
confidence scoring (`SteeringConfig.use_confidence_scoring=False`) and
author hysteresis-only fixtures with the cycle counts originally drafted,
OR (b) keep confidence scoring on and derive every fixture's cycle counts
from config (sustain → ≥40 cycles, recovery → ≥60 cycles, hold-down → 600
cycles). The mode choice is recorded in `evidence/README.md` and on every
fixture's `harness_mode` field. Hysteresis-only mode is the recommended
default because it isolates the spine-contract verdicts from confidence
controller timing.

The harness must NOT touch the live router, NOT write to production state
file paths, and NOT mutate controller-path source (SAFE-12). The preferred
implementation strategy is constructor injection through existing seams in
`SteeringDaemon` (`SteeringConfig`, `SteeringStateManager`,
`RouterOSController`, `BaselineLoader`). Any new steering-source seam
requires an explicit named, scoped edit documented in `evidence/README.md`
with a regression test under `tests/steering/`.

Plan 02 owns the clean-restart-degraded fixture (PROOF-02) and appends its
results into the same `evidence/replay-results.json` artifact. Plan 03 owns
PROOF-03 spine evidence + SAFE-12 boundary check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
@.planning/phases/222-steering-drift-audit/222-RESEARCH.md
@.planning/phases/222-steering-drift-audit/evidence/delta-baseline.json
@.planning/phases/222-steering-drift-audit/evidence/recommendations.md
@CLAUDE.md
@src/wanctl/steering/daemon.py
@src/wanctl/steering/health.py
@src/wanctl/state_manager.py
@configs/steering.yaml
@tests/steering/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 223-01-01: Inspect existing steering-daemon seams before designing the harness</name>
  <read_first>
    src/wanctl/steering/daemon.py
    src/wanctl/state_manager.py
    src/wanctl/steering/health.py
    tests/steering/conftest.py
    tests/steering/test_steering_daemon.py
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
  </read_first>
  <files>.planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md</files>
  <action>Read the full `SteeringDaemon` (NOT `WANSteeringDaemon` — verify the actual class name at `src/wanctl/steering/daemon.py:1122`) constructor signature, `run_cycle()` body, `RouterOSController` constructor signature, `SteeringStateManager` constructor signature, `BaselineLoader` interface and method names (e.g., `load_baseline_rtt()`), and the existing `tests/steering/conftest.py` to inventory existing injection seams. Record findings in `evidence/seam-inventory.md` with one row per seam: `seam name`, `current injection point` (constructor arg | attribute | module-level singleton), `usable by harness?` (yes | no | needs-edit), `notes`. The inventory MUST cover at minimum: (a) how `SteeringDaemon` obtains its `SteeringConfig`; (b) how `RouterOSController` is constructed inside the daemon and whether it can be replaced from the outside without a steering-source edit, AND the EXACT method surface the daemon's decision path invokes — the inventory MUST list every `RouterOSController` method called from `SteeringDaemon` (expected set: `get_rule_status`, `enable_steering`, `disable_steering`; flag any other method discovered); (c) how `SteeringStateManager` resolves its `state_file: Path`; (d) how the daemon obtains its baseline RTT — via `BaselineLoader` constructor injection (verify with grep) — and the method name(s) the daemon calls on the loader (e.g., `load_baseline_rtt`); record what file path the baseline loader resolves to and whether it can be redirected to a tempdir; (e) confidence-mode posture: read `configs/steering.yaml` and confirm `use_confidence_scoring` value, `confidence.sustain_duration_sec`, `confidence.recovery_sustain_sec`, `confidence.hold_down_duration_sec`; read `ASSESSMENT_INTERVAL_SECONDS` from `src/wanctl/steering/daemon.py:127`; record both production values and the harness's chosen mode (hysteresis-only vs confidence) with derived cycle budgets; (f) any module-level globals that would break a tempdir-redirected harness. If ANY seam is rated `needs-edit`, write a brief proposed edit in the inventory: file, function, lines, contract — but do not implement the edit in this task; surface it for human review before Plan 01 Task 02 lands.</action>
  <verify>
    <automated>test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -q "SteeringDaemon" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -q "RouterOSController" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -q "SteeringStateManager" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -q "BaselineLoader" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -qE "get_rule_status|enable_steering|disable_steering" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -qE "use_confidence_scoring|hysteresis-only" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md &amp;&amp; grep -qE "ASSESSMENT_INTERVAL_SECONDS|0\.05" .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/seam-inventory.md` exists and contains a markdown table with columns `seam`, `injection_point`, `usable_by_harness`, `notes`.
    - The table covers all six required seams above (daemon→config, daemon→RouterOSController + exact method surface, daemon→SteeringStateManager state_file, daemon→BaselineLoader + load method names + resolved baseline file path, confidence-mode posture (config values + chosen harness mode + derived cycle budgets), module-level globals).
    - The RouterOSController-method-surface row enumerates exactly which methods the daemon calls (expected: `get_rule_status`, `enable_steering`, `disable_steering`). If the daemon calls any other method, the inventory flags it explicitly so the fake transport can cover or reject it.
    - The confidence-mode row records: production `use_confidence_scoring` value from `configs/steering.yaml`; production `confidence.sustain_duration_sec`, `confidence.recovery_sustain_sec`, `confidence.hold_down_duration_sec`; `ASSESSMENT_INTERVAL_SECONDS` literal from `daemon.py:127`; the harness's chosen mode (`hysteresis-only` or `confidence`); and the derived per-fixture cycle budgets (sustain cycles = ceil(sustain_duration_sec / ASSESSMENT_INTERVAL_SECONDS), recovery cycles, hold-down cycles).
    - For every `needs-edit` row, the inventory records a proposed edit (file, function, lines, contract) AND does not implement the edit in this task.
    - If all seams are rated `yes`, the inventory records a one-line "no steering-source edit required" verdict, and Plan 01 Task 02 may proceed with pure test-side wiring.
  </acceptance_criteria>
  <done>Seam inventory exists; harness design is either green-lit for pure test-side wiring or has explicit named edits queued for human review.</done>
</task>

<task type="auto">
  <name>Task 223-01-02: Implement fake RouterOS transport and pytest fixtures</name>
  <read_first>
    src/wanctl/steering/daemon.py
    tests/steering/conftest.py
    .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/seam-inventory.md
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
  </read_first>
  <files>tests/integration/__init__.py, tests/integration/steering_replay/__init__.py, tests/integration/steering_replay/conftest.py, tests/integration/steering_replay/fake_router_transport.py</files>
  <action>Create `tests/integration/` and `tests/integration/steering_replay/` package init files. Implement `tests/integration/steering_replay/fake_router_transport.py` defining a `FakeRouterTransport` class that mimics the ACTUAL `RouterOSController` daemon-facing interface (verified by Task 01's seam inventory): `get_rule_status() -> bool | None`, `enable_steering() -> bool`, `disable_steering() -> bool`. There is NO `set_rule_status()` method. The fake MUST: (a) accept an `initial_enabled: bool` argument in its constructor and persist that value as its current rule state; (b) implement `get_rule_status()` to return the current rule state and append `{"cycle": <current_cycle>, "method": "get_rule_status", "result": <bool>, "timestamp_iso": ...}` to `interactions_log`; (c) implement `enable_steering()` to set state to True, append `{"cycle": ..., "method": "enable_steering", "result": True, "timestamp_iso": ...}` to `interactions_log`, return True (the fake may optionally simulate `verify_with_retry` success without delay); (d) implement `disable_steering()` symmetrically; (e) never open a network socket — explicitly raise `RuntimeError("FakeRouterTransport: no live router calls allowed")` if any other RouterOSController method (e.g., `run_cmd`) is invoked, using `__getattr__` fallback; (f) expose `assert_only_documented_calls()` that fails if `interactions_log` contains a method outside `{get_rule_status, enable_steering, disable_steering}`; (g) expose `set_current_cycle(idx)` so the harness can attribute each interaction to a cycle index; (h) expose `pre_state` snapshot showing the initial rule state and any pre-test seeded state for evidence capture. Implement `tests/integration/steering_replay/conftest.py` providing pytest fixtures: `staging_workspace` (tempdir under pytest `tmp_path` containing seeded state files), `fake_router` (instance of FakeRouterTransport), `baseline_loader_spy` (a real or wrapping `BaselineLoader` pointed at `staging_workspace/spectrum_state.json` that records every `load_baseline_rtt` call with the read path and returned value), and `daemon_factory(harness_mode: Literal["hysteresis-only","confidence"] = "hysteresis-only")` — callable that constructs a `SteeringDaemon` (NOT `WANSteeringDaemon`) wired with the fake transport, tempdir state files, the baseline_loader_spy, and the requested harness mode. When `harness_mode="hysteresis-only"`, the factory MUST override `SteeringConfig.use_confidence_scoring=False` before constructing the daemon; when `harness_mode="confidence"`, the factory MUST keep `use_confidence_scoring=True` and emit a banner reminding the test author that fixture cycle counts must satisfy `sustain_duration_sec / ASSESSMENT_INTERVAL_SECONDS` cycle budgets. The factory MUST raise `AssertionError` if the resulting daemon's resolved state-file path, cake-state-source path, or baseline-loader path is anywhere under `/var/lib/wanctl/`, `/etc/wanctl/`, `/var/log/wanctl/`, or `/run/wanctl/`.</action>
  <verify>
    <automated>test -f tests/integration/steering_replay/fake_router_transport.py &amp;&amp; test -f tests/integration/steering_replay/conftest.py &amp;&amp; .venv/bin/python -c "import sys; sys.path.insert(0, 'tests/integration/steering_replay'); import fake_router_transport as m; t = m.FakeRouterTransport(initial_enabled=False); assert t.get_rule_status() is False; assert t.enable_steering() is True; assert t.get_rule_status() is True; assert t.disable_steering() is True; assert t.get_rule_status() is False; methods = [e['method'] for e in t.interactions_log]; assert 'enable_steering' in methods and 'disable_steering' in methods and 'get_rule_status' in methods; t.assert_only_documented_calls()"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/__init__.py` and `tests/integration/steering_replay/__init__.py` exist (may be empty).
    - `tests/integration/steering_replay/fake_router_transport.py` defines `FakeRouterTransport` with methods `get_rule_status`, `enable_steering`, `disable_steering`. There is NO `set_rule_status` method on the fake.
    - Calling any other `RouterOSController`-shaped method on `FakeRouterTransport` (e.g., `run_cmd`, `set_rule_status`) raises `RuntimeError("FakeRouterTransport: no live router calls allowed")` via `__getattr__` fallback (or equivalent explicit denylist).
    - `FakeRouterTransport.interactions_log` records each invocation with at least `cycle`, `method`, `result`, `timestamp_iso`.
    - `FakeRouterTransport.assert_only_documented_calls()` passes after the documented surface is exercised and fails if any other method was invoked.
    - `tests/integration/steering_replay/conftest.py` defines `staging_workspace`, `fake_router`, `baseline_loader_spy`, and `daemon_factory` pytest fixtures.
    - `daemon_factory` accepts `harness_mode: Literal["hysteresis-only", "confidence"]` and sets `SteeringConfig.use_confidence_scoring` accordingly. Default is `"hysteresis-only"`.
    - `daemon_factory` raises `AssertionError` (caught by an early-fail check in the fixture) if any production path under `/var/lib/wanctl/`, `/etc/wanctl/`, `/var/log/wanctl/`, or `/run/wanctl/` is resolved as the daemon's state-file path, cake state source path, or baseline-loader path.
    - `baseline_loader_spy.calls` is a list capturing each `load_baseline_rtt` invocation with `read_path`, `returned_value`, `timestamp_iso`.
    - Running `.venv/bin/python -c "from tests.integration.steering_replay.fake_router_transport import FakeRouterTransport; FakeRouterTransport(initial_enabled=False)"` exits 0.
  </acceptance_criteria>
  <done>Fake transport and pytest fixtures exist; the daemon-factory enforces the no-production-paths invariant.</done>
</task>

<task type="auto">
  <name>Task 223-01-03: Define fixture YAML schema and author the steady-good, onset-degraded, recovery, and cake-read-failure fixtures</name>
  <read_first>
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
    src/wanctl/steering/daemon.py
    src/wanctl/steering/congestion_assessment.py
    configs/steering.yaml
  </read_first>
  <files>tests/integration/steering_replay/fixtures/steady-good.yaml, tests/integration/steering_replay/fixtures/onset-degraded.yaml, tests/integration/steering_replay/fixtures/recovery.yaml, tests/integration/steering_replay/fixtures/cake-read-failure.yaml, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/README.md</files>
  <action>Document the fixture YAML schema in `evidence/README.md` with these top-level keys: `name` (string), `description` (string), `harness_mode` (enum: `hysteresis-only` | `confidence` — recorded so the harness knows which `daemon_factory` posture to use), `cycle_budget_derivation` (object — see below), `corpus_source` (enum: `synthesized-from-spine-contract` | `derived-from-phase-212-evidence`), `derivation` (string citing the spine-contract clause), `pre_state` (object — see schema below), `cycles` (list with `inputs` and `expected_decision`).

**`pre_state` schema MUST split into two sub-objects (REVIEWS.md MEDIUM, baseline-authority enrichment):** (a) `steering_pre_state` — mirrors `SteeringStateManager` schema returned by `_get_steering_state_schema` in `src/wanctl/steering/daemon.py:779` (`current_state`, `good_count`, `transitions`, `last_transition_time`, etc.); (b) `autorate_state_by_cycle` — uses the real spectrum-state file shape `{"ewma": {"baseline_rtt": float}, "congestion": {"dl_state": str, ...}}`. The harness writes `autorate_state_by_cycle[<cycle_idx>]` (or `default` if absent) to `staging_workspace/spectrum_state.json` BEFORE that cycle runs, so `BaselineLoader.load_baseline_rtt` reads the canonical shape. Document the schema in `evidence/README.md` with a YAML example.

**`cycle_budget_derivation` is MANDATORY (REVIEWS.md HIGH #2).** For `hysteresis-only` fixtures: `{"mode": "hysteresis-only", "confidence_disabled": true, "rationale": "cycles chosen against simple hysteresis state machine; confidence controller is bypassed via SteeringConfig.use_confidence_scoring=False"}`. For `confidence` fixtures: `{"mode": "confidence", "source": "configs/steering.yaml", "assessment_interval_sec": 0.05, "sustain_duration_sec": <value>, "recovery_sustain_sec": <value>, "hold_down_duration_sec": <value>, "derived_sustain_cycles": <int>, "derived_recovery_cycles": <int>, "derived_hold_down_cycles": <int>}`. The harness MUST refuse to run any fixture whose `cycles` length is below the derived budget for its mode (early-fail with a clear error).

Author four fixture files (default `harness_mode: hysteresis-only` — the simplest path that isolates spine-contract verdicts from confidence-controller timing):

(a) `steady-good.yaml`: 10 cycles, `harness_mode: hysteresis-only`, `steering_pre_state.current_state: SPECTRUM_GOOD`, `autorate_state_by_cycle.default: {ewma: {baseline_rtt: 25.0}, congestion: {dl_state: "GREEN"}}`, low-RTT-delta inputs, expected `current_state=SPECTRUM_GOOD` and `effective_mangle_state=False` for every cycle. Corpus source: `synthesized-from-spine-contract`.

(b) `onset-degraded.yaml`: 15 cycles, `harness_mode: hysteresis-only`, `steering_pre_state.current_state: SPECTRUM_GOOD`, `autorate_state_by_cycle.default` shape as above, RTT delta crosses the degraded threshold by cycle ~5 and stays high; expected transition to `SPECTRUM_DEGRADED` with `effective_mangle_state=True`. Corpus source: `synthesized-from-spine-contract`.

(c) `recovery.yaml`: 20 cycles, `harness_mode: hysteresis-only`, `steering_pre_state.current_state: SPECTRUM_DEGRADED` with `good_count: 0`, `autorate_state_by_cycle.default` shape as above, RTT delta returns to low values; expected recovery to `SPECTRUM_GOOD` with `effective_mangle_state=False` once `good_count` crosses the configured hysteresis threshold. Corpus source: `synthesized-from-spine-contract`.

(d) `cake-read-failure.yaml`: 8 cycles, `harness_mode: hysteresis-only`, `steering_pre_state.current_state: SPECTRUM_GOOD`, CAKE read returns failure on cycles 3-5; expected daemon does NOT flap state on read failure (preserves last state); explicit `cake_read_failures` increment expected. Corpus source: `synthesized-from-spine-contract`.

**Anchor at least one fixture to runtime evidence (REVIEWS.md MEDIUM #4):** Add a fifth fixture `onset-degraded-from-phase212.yaml` whose `cycles[].inputs` (RTT samples + CAKE drops + queue depth) are extracted verbatim from Phase 212 / Phase 222 read-only evidence (cite the exact source file path in `derivation`). Use `corpus_source: derived-from-phase-212-evidence`. Document the extraction provenance in `evidence/README.md` ("Provenance: anchored fixtures" section). If no concrete row is available in those phases' evidence, record that fact in `evidence/README.md` and mark `onset-degraded-from-phase212.yaml` as `corpus_source: synthesized-from-spine-contract` with a `provenance_note` field explaining the fallback — do not silently mislabel.

Each fixture lists the exact spine-contract clause it derives from in the top-level `derivation` field.</action>
  <verify>
    <automated>for f in steady-good onset-degraded recovery cake-read-failure onset-degraded-from-phase212; do test -f tests/integration/steering_replay/fixtures/$f.yaml || exit 1; done &amp;&amp; .venv/bin/python -c "import yaml,glob; [yaml.safe_load(open(p)) for p in glob.glob('tests/integration/steering_replay/fixtures/*.yaml')]" &amp;&amp; .venv/bin/python -c "import yaml,glob; data=[yaml.safe_load(open(p)) for p in sorted(glob.glob('tests/integration/steering_replay/fixtures/*.yaml'))]; required = {'cycles','pre_state','corpus_source','derivation','harness_mode','cycle_budget_derivation'}; missing = [(d.get('name','?'), required - set(d.keys())) for d in data if not required.issubset(set(d.keys()))]; assert not missing, f'fixture schema violation: {missing}'" &amp;&amp; .venv/bin/python -c "import yaml,glob; data=[yaml.safe_load(open(p)) for p in glob.glob('tests/integration/steering_replay/fixtures/*.yaml')]; assert all('steering_pre_state' in d['pre_state'] and 'autorate_state_by_cycle' in d['pre_state'] for d in data), 'pre_state must split into steering_pre_state + autorate_state_by_cycle'"</automated>
  </verify>
  <acceptance_criteria>
    - At least five fixture YAML files exist under `tests/integration/steering_replay/fixtures/`: `steady-good`, `onset-degraded`, `recovery`, `cake-read-failure`, and `onset-degraded-from-phase212` (the anchored-to-runtime fixture).
    - Every fixture parses with `yaml.safe_load` and has top-level keys `name`, `description`, `harness_mode`, `cycle_budget_derivation`, `corpus_source`, `pre_state`, `cycles`, `derivation`.
    - Every fixture's `pre_state` is an object with exactly two sub-keys: `steering_pre_state` (SteeringStateManager schema) and `autorate_state_by_cycle` (real spectrum-state shape `{ewma: {baseline_rtt: float}, congestion: {dl_state: str, ...}}` either keyed by integer cycle index or with a `default` entry).
    - Every fixture's `harness_mode` is exactly one of `hysteresis-only` or `confidence`. Default is `hysteresis-only`.
    - Every fixture's `cycle_budget_derivation` documents the derivation rule: hysteresis-only fixtures have `{"mode": "hysteresis-only", "confidence_disabled": true, ...}`; confidence-mode fixtures derive cycle counts from `configs/steering.yaml` using `ASSESSMENT_INTERVAL_SECONDS = 0.05` and record `derived_sustain_cycles`, `derived_recovery_cycles`, `derived_hold_down_cycles` as integers ≥ ceil(seconds / 0.05).
    - Every fixture's `corpus_source` is exactly one of `synthesized-from-spine-contract` or `derived-from-phase-212-evidence`.
    - Every fixture's `derivation` field cites the specific spine-contract clause it derives from (e.g., "binary on/off", "autorate-baseline authority", "only-new-connections rerouted").
    - The `onset-degraded-from-phase212.yaml` fixture either (a) has `corpus_source: derived-from-phase-212-evidence` and cites a concrete source path under `.planning/phases/212-*/` or `.planning/phases/222-steering-drift-audit/` in its `derivation` AND a `provenance_source_file` field; OR (b) has a `provenance_note` field explaining why anchoring was not feasible and reverts to `synthesized-from-spine-contract`. No silent mislabeling.
    - `evidence/README.md` documents the fixture schema with a YAML example block and a sentence explaining each field, AND records the corpus-source policy: NO fixture is derived from observing post-drift code behavior, AND records the "Provenance: anchored fixtures" section.
  </acceptance_criteria>
  <done>Fixture corpus authored, schema documented, corpus-source policy recorded.</done>
</task>

<task type="auto">
  <name>Task 223-01-04: Implement replay_harness.py and test_replay_corpus.py and emit replay-results.{json,md}</name>
  <read_first>
    src/wanctl/steering/daemon.py
    tests/integration/steering_replay/conftest.py
    tests/integration/steering_replay/fake_router_transport.py
    .planning/phases/223-staging-proof-clean-restart-reproduction/223-RESEARCH.md
  </read_first>
  <files>tests/integration/steering_replay/replay_harness.py, tests/integration/steering_replay/test_replay_corpus.py, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json, .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md</files>
  <action>Implement `replay_harness.py` exposing a `run_fixture(fixture_path: Path, workspace: Path) -> dict` function and a `__main__` block accepting `--fixture <path>` or `--all` plus `--workspace <dir>` (default: pytest tempdir if pytest is active else a `.harness-tmp/` workspace under the evidence dir). `run_fixture` MUST:

(a) load the fixture YAML and validate it has `harness_mode`, `cycle_budget_derivation`, `pre_state.steering_pre_state`, `pre_state.autorate_state_by_cycle` (early-fail with clear error if not);
(b) seed `workspace/steering_state.json` from `pre_state.steering_pre_state` and write the cycle-0 spectrum-state shape from `pre_state.autorate_state_by_cycle` (using `default` if no `0` key) to `workspace/spectrum_state.json`;
(c) construct a `SteeringDaemon` (NOT `WANSteeringDaemon`) via the conftest `daemon_factory(harness_mode=fixture["harness_mode"])` (or its standalone equivalent) wired to `FakeRouterTransport` AND the baseline-loader spy;
(d) capture the **initial steering rule state** from `FakeRouterTransport.get_rule_status()` as `pre_steering_rule_state` BEFORE any cycle runs (REVIEWS.md HIGH #3 — must record initial state, not just toggles);
(e) iterate through `cycles`, and on each cycle: call `fake_router.set_current_cycle(idx)`; if `autorate_state_by_cycle[<idx>]` exists, rewrite `workspace/spectrum_state.json` with that shape; feed inputs through the daemon's decision pipeline (prefer `update_state_machine` / `run_cycle` whichever can be driven without networking — see Task 01 seam inventory); after the cycle, record the **effective steering state** = `FakeRouterTransport.get_rule_status()` (this captures the per-cycle effective state regardless of whether a toggle was issued — addresses REVIEWS.md HIGH #3); record per-cycle observable `current_state`, `effective_mangle_state`, the cycle's interactions delta from `fake_router.interactions_log`, the baseline_loader_spy entries for this cycle, and the spectrum-state file value at read time;
(f) compare per-cycle observed decision to fixture `expected_decision`. If the fixture's `expected_decision` field is the literal string `"observe"` (Plan 02's clean-restart fixture convention), skip the per-cycle match check for that cycle and only record observations;
(g) compute per-fixture verdict: `matches` if every non-`observe` cycle matches AND `pre_steering_rule_state` is consistent with the fixture's `steering_pre_state.current_state` invariant (e.g., if `steering_pre_state.current_state == SPECTRUM_DEGRADED`, the harness records whether the rule is pre-enabled at cycle 0 as part of evidence, but does not auto-fail — Plan 03 classifies); `diverges` with a list of mismatched cycles; `inconclusive` if the daemon raised an unexpected exception.

Implement `test_replay_corpus.py` with one parametrized pytest function over every fixture in `fixtures/` (excluding `clean-restart-degraded.yaml` if present — Plan 02 owns that) that calls `run_fixture` and asserts the verdict is `matches`. Also write a `test_harness_does_not_touch_production_paths` pytest test that scans `replay_harness.py` and `conftest.py` for any literal `/var/lib/wanctl`, `/etc/wanctl`, `/var/log/wanctl`, `/run/wanctl` outside a comment or assertion message. After running pytest (or by capturing harness JSON directly), write `evidence/replay-results.json` listing one entry per fixture with the enriched schema (see truths section: `harness_mode`, `cycle_budget_derivation`, `pre_steering_rule_state`, `effective_steering_state_per_cycle`, `steering_interactions`, `baseline_rtt_per_cycle`, etc.) and render the human-readable `evidence/replay-results.md` table.</action>
  <verify>
    <automated>test -f tests/integration/steering_replay/replay_harness.py &amp;&amp; test -f tests/integration/steering_replay/test_replay_corpus.py &amp;&amp; .venv/bin/pytest -o addopts='' tests/integration/steering_replay/ -v 2&gt;&amp;1 | tail -40 &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json &amp;&amp; .venv/bin/python -m json.tool .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.json &gt;/dev/null &amp;&amp; test -f .planning/phases/223-staging-proof-clean-restart-reproduction/evidence/replay-results.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/steering_replay/replay_harness.py` exists, exposes `run_fixture(fixture_path, workspace)`, and is runnable as `.venv/bin/python tests/integration/steering_replay/replay_harness.py --all` (exit 0 expected once harness is wired correctly).
    - `tests/integration/steering_replay/test_replay_corpus.py` exists and contains a `@pytest.mark.parametrize` over every fixture file in `fixtures/` (excluding `clean-restart-degraded.yaml` if it exists — Plan 02 owns that fixture).
    - `.venv/bin/pytest -o addopts='' tests/integration/steering_replay/ -v` exits 0; the run includes at least 5 fixture cases (steady-good, onset-degraded, recovery, cake-read-failure, onset-degraded-from-phase212).
    - `evidence/replay-results.json` is a JSON object with key `fixtures` whose value is a list of objects each containing AT LEAST: `fixture`, `harness_mode`, `cycle_interval_sec` (0.05), `cycle_budget_derivation` (object copied from the fixture), `cycles_run`, `pre_steering_rule_state` (bool — initial fake-router rule state captured before cycle 0), `effective_steering_state_per_cycle` (list of bools — per-cycle observable rule state after each cycle), `final_state`, `transitions`, `steering_interactions` (list with `cycle`, `method`, `result`), `baseline_rtt_per_cycle` (list with `cycle`, `baseline_read_path`, `baseline_read_value`, `baseline_loader_called`, `spectrum_state_write_attempted`), `verdict` (`matches | diverges | inconclusive | observe`), and `verdict_rationale`.
    - `evidence/replay-results.md` renders a markdown table with one row per fixture and columns `fixture`, `harness_mode`, `cycles_run`, `pre_rule_state`, `final_state`, `verdict`, `verdict_rationale`.
    - A pytest test `test_harness_does_not_touch_production_paths` exists and passes — it asserts no production path string appears in harness/conftest source outside comments or assertion messages.
    - The FakeRouterTransport `assert_only_documented_calls()` check passes after every fixture run.
    - For every fixture, `pre_steering_rule_state` is recorded as an explicit boolean (it captures the initial fake-router rule state at cycle 0, even when the daemon never toggles it during the run — addresses REVIEWS.md HIGH #3).
    - For every fixture's `cycles`, `effective_steering_state_per_cycle[i]` is recorded for every cycle index `i` (the per-cycle observable steering state), regardless of whether a toggle was issued in that cycle.
    - For confidence-mode fixtures, the harness refuses to run any fixture whose `cycles` length is below the `derived_sustain_cycles + derived_hold_down_cycles` budget — verified by a unit test under `tests/integration/steering_replay/test_cycle_budget_gate.py` (or equivalent) that the harness raises a clear error when given a too-short confidence-mode fixture.
  </acceptance_criteria>
  <done>Replay harness, pytest entry point, and PROOF-01 evidence artifacts (replay-results.{json,md}) exist; harness exits 0 across the corpus.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "PROOF-01: Operator can run the offline steering replay harness against the v1.47-close steering daemon decision pipeline via either `pytest tests/integration/steering_replay/` or `python replay_harness.py --all` and read per-fixture verdicts in `evidence/replay-results.{json,md}`."
    - "The harness uses ONLY a fake `RouterOSController` (with the actual daemon-facing surface: `get_rule_status`, `enable_steering`, `disable_steering`) and ONLY tempdir-redirected state files; no live router call, no production-path write."
    - "Harness confidence-mode posture is EXPLICIT and recorded per fixture. Default `harness_mode=hysteresis-only` (overrides `use_confidence_scoring=False`). Confidence-mode fixtures derive cycle budgets from `configs/steering.yaml` against `ASSESSMENT_INTERVAL_SECONDS = 0.05`. The harness refuses to run too-short confidence-mode fixtures."
    - "The fixture corpus is derived from the spine contract (and Phase 212/222 read-only evidence where applicable — at least one fixture anchored to runtime evidence per REVIEWS.md MEDIUM #4), NOT from observing post-drift code behavior."
    - "Evidence records initial steering rule state (`pre_steering_rule_state`) and per-cycle effective steering state (`effective_steering_state_per_cycle`), not just toggle events — so PROOF-02 can classify the case where the rule is pre-enabled at boot while persisted state is SPECTRUM_DEGRADED (REVIEWS.md HIGH #3)."
    - "Baseline RTT is recorded with full provenance: `baseline_read_path`, `baseline_read_value`, `baseline_loader_called`, `spectrum_state_write_attempted` per cycle. Baseline data is seeded into `workspace/spectrum_state.json` using the real spectrum-state shape `{ewma: {baseline_rtt: ...}, congestion: {dl_state: ...}}` (REVIEWS.md MEDIUM #5)."
    - "SAFE-12 controller-path source is untouched by this plan (verified by Plan 03's boundary check)."
    - "Any steering-source seam edit is explicitly named in `evidence/README.md` with rationale + regression coverage under `tests/steering/`. A cheap SAFE-12 check is run after any Plan 01 Task seam edit lands, not only at final Plan 03 (REVIEWS.md suggestion). Default expectation is no steering-source edit."
  </truths>
</must_haves>

<verification>
  <required>
    - "All five fixture YAML files exist and parse cleanly with the enriched schema (`harness_mode`, `cycle_budget_derivation`, split `pre_state`)."
    - "`.venv/bin/pytest -o addopts='' tests/integration/steering_replay/ -v` exits 0."
    - "`evidence/replay-results.json` parses with `json.tool` and includes one entry per non-PROOF-02 fixture with the enriched schema (`pre_steering_rule_state`, `effective_steering_state_per_cycle`, `steering_interactions`, `baseline_rtt_per_cycle`, `harness_mode`, `cycle_budget_derivation`)."
    - "`evidence/replay-results.md` renders the per-fixture verdict table including `harness_mode` and `pre_rule_state` columns."
    - "`evidence/seam-inventory.md` exists and covers all six required seams from Task 01, including the RouterOSController method-surface enumeration and confidence-mode posture row."
    - "`evidence/README.md` documents the fixture schema (split `pre_state`), the corpus-source policy, the harness-mode policy, the cycle-budget derivation rules, the 'Provenance: anchored fixtures' section, and any named steering-source seam edits."
    - "If any steering-source seam edit landed in Task 01 or Task 02, a SAFE-12 boundary-check command result is recorded in `evidence/README.md` (or a dedicated `evidence/safe12-post-seam-check.txt`) showing the controller-path source diff is still empty after the seam edit."
    - "No fixture / harness / conftest path resolves to `/var/lib/wanctl/`, `/etc/wanctl/`, `/var/log/wanctl/`, or `/run/wanctl/` — verified by the dedicated pytest test."
  </required>
</verification>
